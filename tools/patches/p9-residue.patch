diff -ruN -x objects -x vim -x config.log -x config.status -x config.cache -x '*.o' -x '*.d' a/vim.c b/vim.c
--- a/vim.c	1970-01-01 00:00:00.000000000 +0000
+++ b/vim.c	1970-01-01 00:00:00.000000000 +0000
@@ -44,335 +44,209 @@
 #include <dlfcn.h>
 #include <sys/resource.h>
 
+// Constants that were #defines inside a struct body, where a bare enum
+// declaration declares nothing.  They are plain integers with no
+// dependencies, so they sit together here.
+enum { BH_DIRTY = 1 };
+enum { BH_LOCKED = 2 };
+enum { EXPAND_BUF_LEN = 256 };
+enum { CMOD_SANDBOX = 0x0001 };
+enum { CMOD_SILENT = 0x0002 };
+enum { CMOD_ERRSILENT = 0x0004 };
+enum { CMOD_UNSILENT = 0x0008 };
+enum { CMOD_NOAUTOCMD = 0x0010 };
+enum { CMOD_HIDE = 0x0020 };
+enum { CMOD_KEEPALT = 0x0100 };
+enum { CMOD_KEEPMARKS = 0x0200 };
+enum { CMOD_KEEPJUMPS = 0x0400 };
+enum { CMOD_LOCKMARKS = 0x0800 };
+enum { CMOD_KEEPPATTERNS = 0x1000 };
+enum { CMOD_NOSWAPFILE = 0x2000 };
+enum { CMOD_VIM9CMD = 0x4000 };
+enum { CMOD_LEGACY = 0x8000 };
+enum { ML_EMPTY = 0x01 };
+enum { ML_LINE_DIRTY = 0x02 };
+enum { ML_LOCKED_DIRTY = 0x04 };
+enum { ML_LOCKED_POS = 0x08 };
+enum { ML_ALLOCATED = 0x10 };
+enum { B_IMODE_NONE = 0 };
+enum { B_IMODE_LMAP = 1 };
+enum { B_IMODE_LAST = 2 };
+enum { TRUNC_ON_OPEN = 0 };
+enum { BRACE_IN_COL0 = 1 };
+enum { BRACE_AT_START = 2 };
+enum { BRACE_AT_END = 3 };
+enum { RULER_BUF_LEN = 70 };
+enum { INPUT_BUFLEN = 100 };
+enum { FGETS_SIZE = 200 };
+enum { MAXSUFLEN = 30 };
+enum { LOOKBACK_LINE_COUNT = 1000 };
+enum { MIN_SPACE = 75 };
+enum { IDX_LATIN_1 = 0 };
+enum { IDX_ISO_2 = 1 };
+enum { IDX_ISO_3 = 2 };
+enum { IDX_ISO_4 = 3 };
+enum { IDX_ISO_5 = 4 };
+enum { IDX_ISO_6 = 5 };
+enum { IDX_ISO_7 = 6 };
+enum { IDX_ISO_8 = 7 };
+enum { IDX_ISO_9 = 8 };
+enum { IDX_ISO_10 = 9 };
+enum { IDX_ISO_11 = 10 };
+enum { IDX_ISO_13 = 11 };
+enum { IDX_ISO_14 = 12 };
+enum { IDX_ISO_15 = 13 };
+enum { IDX_UTF8 = 16 };
+enum { IDX_UCS2 = 17 };
+enum { IDX_UCS2LE = 18 };
+enum { IDX_UTF16 = 19 };
+enum { IDX_UTF16BE = 20 };
+enum { IDX_UTF16LE = 21 };
+enum { IDX_UCS4 = 22 };
+enum { IDX_UCS4LE = 23 };
+enum { IDX_EUC_JP = 25 };
+enum { IDX_SJIS = 26 };
+enum { IDX_EUC_KR = 27 };
+enum { IDX_EUC_CN = 28 };
+enum { IDX_EUC_TW = 29 };
+enum { IDX_BIG5 = 30 };
+enum { IDX_CP932 = 46 };
+enum { IDX_CP936 = 47 };
+enum { IDX_CP949 = 48 };
+enum { IDX_CP950 = 49 };
+enum { IDX_MACROMAN = 58 };
+enum { IDX_COUNT = 61 };
+enum { ICONV_TESTLEN = 400 };
+enum { MAX_SEARCH_COUNT = 9999 };
+enum { INC = 20 };
+enum { GAP = 3 };
+enum { BUFLEN = 100 };
+enum { STYLE_ECHO = 0 };
+enum { STYLE_GLOB = 1 };
+enum { STYLE_VIMGLOB = 2 };
+enum { STYLE_PRINT = 3 };
+enum { STYLE_BT = 4 };
+enum { STYLE_GLOBSTAR = 5 };
+enum { CLASS_not = 0x80 };
+enum { CLASS_af = 0x40 };
+enum { CLASS_AF = 0x20 };
+enum { CLASS_az = 0x10 };
+enum { CLASS_AZ = 0x08 };
+enum { CLASS_o7 = 0x04 };
+enum { CLASS_o9 = 0x02 };
+enum { CLASS_underscore = 0x01 };
+enum { A_grave = 0xc0 };
+enum { A_acute = 0xc1 };
+enum { A_circumflex = 0xc2 };
+enum { A_virguilla = 0xc3 };
+enum { A_diaeresis = 0xc4 };
+enum { A_ring = 0xc5 };
+enum { C_cedilla = 0xc7 };
+enum { E_grave = 0xc8 };
+enum { E_acute = 0xc9 };
+enum { E_circumflex = 0xca };
+enum { E_diaeresis = 0xcb };
+enum { I_grave = 0xcc };
+enum { I_acute = 0xcd };
+enum { I_circumflex = 0xce };
+enum { I_diaeresis = 0xcf };
+enum { N_virguilla = 0xd1 };
+enum { O_grave = 0xd2 };
+enum { O_acute = 0xd3 };
+enum { O_circumflex = 0xd4 };
+enum { O_virguilla = 0xd5 };
+enum { O_diaeresis = 0xd6 };
+enum { O_slash = 0xd8 };
+enum { U_grave = 0xd9 };
+enum { U_acute = 0xda };
+enum { U_circumflex = 0xdb };
+enum { U_diaeresis = 0xdc };
+enum { Y_acute = 0xdd };
+enum { a_grave = 0xe0 };
+enum { a_acute = 0xe1 };
+enum { a_circumflex = 0xe2 };
+enum { a_virguilla = 0xe3 };
+enum { a_diaeresis = 0xe4 };
+enum { a_ring = 0xe5 };
+enum { c_cedilla = 0xe7 };
+enum { e_grave = 0xe8 };
+enum { e_acute = 0xe9 };
+enum { e_circumflex = 0xea };
+enum { e_diaeresis = 0xeb };
+enum { i_grave = 0xec };
+enum { i_acute = 0xed };
+enum { i_circumflex = 0xee };
+enum { i_diaeresis = 0xef };
+enum { n_virguilla = 0xf1 };
+enum { o_grave = 0xf2 };
+enum { o_acute = 0xf3 };
+enum { o_circumflex = 0xf4 };
+enum { o_virguilla = 0xf5 };
+enum { o_diaeresis = 0xf6 };
+enum { o_slash = 0xf8 };
+enum { u_grave = 0xf9 };
+enum { u_acute = 0xfa };
+enum { u_circumflex = 0xfb };
+enum { u_diaeresis = 0xfc };
+enum { y_acute = 0xfd };
+enum { y_diaeresis = 0xff };
+enum { GOTO_COST = 7 };
+enum { HIGHL_COST = 5 };
+enum { PLAN_LE = 1 };
+enum { PLAN_CR = 2 };
+enum { PLAN_NL = 3 };
+enum { PLAN_WRITE = 4 };
+enum { TMP_LEN = 350 };
+enum { ML_EXTRA = 0 };
+enum { INC3 = 27 };
+enum { INC2 = 40 };
+enum { TERMCODE_GAP = 2 };
+enum { ME_UNKNOWN_OPTION = 0 };
+enum { ME_TOO_MANY_ARGS = 1 };
+enum { ME_ARG_MISSING = 2 };
+enum { ME_GARBAGE = 3 };
+enum { ME_EXTRA_CMD = 4 };
+
 // Set before the headers because they change what the headers
 // produce: EXTERN makes globals.h define rather than declare, and
 // IN_OPTION_C does the same for a few options.
-#define EXTERN
-#define IN_OPTION_C
-#define USING_FLOAT_STUFF
 
 // ==================== alloc.c ====================
 
 // ---------------- begin vim.h ----------------
 
-#define VIM__H
-
-// ---------------- begin protodef.h ----------------
-
-// ---------------- end protodef.h ----------------
-
 // ---------------- begin config.h ----------------
 
-#define HAVE_DATE_TIME 1
-
-#define HAVE_ATTRIBUTE_UNUSED 1
-
-#define UNIX 1
-
-#define VIM_SIZEOF_INT 4
-
-#define VIM_SIZEOF_LONG 8
-
-#define SIZEOF_OFF_T 8
-
-#define SIZEOF_TIME_T 8
-
-#define USEMEMMOVE 1
-
-#define SYS_SELECT_WITH_SYS_TIME 1
-
-#define SELECT_TYPE_ARG234 (fd_set *)
-
-#define HAVE_SVR4_PTYS 1
-
-#define HAVE_SIGCONTEXT 1
-
-#define ST_MTIM_NSEC st_mtim.tv_nsec
-
-#define HAVE_FCHDIR 1
-#define HAVE_FCHOWN 1
-#define HAVE_FCHMOD 1
-#define HAVE_FSEEKO 1
-#define HAVE_FSYNC 1
-#define HAVE_FTRUNCATE 1
-#define HAVE_GETCWD 1
-#define HAVE_GETPGID 1
-#define HAVE_GETPWENT 1
-#define HAVE_GETPWNAM 1
-#define HAVE_GETPWUID 1
-#define HAVE_GETRLIMIT 1
-#define HAVE_GETTIMEOFDAY 1
-#define HAVE_ICONV 1
-#define HAVE_LOCALTIME_R 1
-#define HAVE_LSTAT 1
-#define HAVE_MEMSET 1
-#define HAVE_MKDTEMP 1
-#define HAVE_NANOSLEEP 1
-#define HAVE_NL_LANGINFO_CODESET 1
-#define HAVE_OPENDIR 1
-#define HAVE_POSIX_OPENPT 1
-#define HAVE_PUTENV 1
-#define HAVE_QSORT 1
-#define HAVE_READLINK 1
-#define HAVE_RENAME 1
-#define HAVE_SELECT 1
-#define HAVE_SETENV 1
-#define HAVE_SETPGID 1
-#define HAVE_SETSID 1
-#define HAVE_SIGACTION 1
-#define HAVE_SIGALTSTACK 1
-#define HAVE_SIGSET 1
-#define HAVE_SIGSETJMP 1
-#define HAVE_SIGPROCMASK 1
-#define HAVE_STRCASECMP 1
-#define HAVE_STRCOLL 1
-#define HAVE_STRERROR 1
-#define HAVE_STRFTIME 1
-#define HAVE_STRNCASECMP 1
-#define HAVE_STRPBRK 1
-#define HAVE_STRPTIME 1
-#define HAVE_STRTOL 1
-#define HAVE_ST_BLKSIZE 1
-#define HAVE_SYNC 1
-#define HAVE_SYSCONF 1
-#define HAVE_SYSINFO 1
-#define HAVE_SYSINFO_MEM_UNIT 1
-#define HAVE_SYSINFO_UPTIME 1
-#define HAVE_TOWLOWER 1
-#define HAVE_TOWUPPER 1
-#define HAVE_ISWUPPER 1
-#define HAVE_TZSET 1
-#define HAVE_UNSETENV 1
-#define HAVE_USLEEP 1
-#define HAVE_UTIME 1
-#define HAVE_MBLEN 1
-#define HAVE_TIMER_CREATE 1
-#define HAVE_CLOCK_GETTIME 1
-#define HAVE_XATTR 1
-
-#define HAVE_UTIMES 1
-
-#define HAVE_DIRENT_H 1
-#define HAVE_ERRNO_H 1
-#define HAVE_FCNTL_H 1
-#define HAVE_ICONV_H 1
-#define HAVE_INTTYPES_H 1
-#define HAVE_LANGINFO_H 1
-#define HAVE_LIBGEN_H 1
-#define HAVE_LIBINTL_H 1
-#define HAVE_LOCALE_H 1
-#define HAVE_MATH_H 1
-#define HAVE_POLL_H 1
-#define HAVE_PWD_H 1
-#define HAVE_SETJMP_H 1
-#define HAVE_STDINT_H 1
-#define HAVE_STRINGS_H 1
-#define HAVE_STROPTS_H 1
-#define HAVE_SYS_IOCTL_H 1
-#define HAVE_SYS_PARAM_H 1
-#define HAVE_SYS_POLL_H 1
-#define HAVE_SYS_RESOURCE_H 1
-#define HAVE_SYS_SELECT_H 1
-#define HAVE_SYS_STATFS_H 1
-#define HAVE_SYS_SYSINFO_H 1
-#define HAVE_SYS_TIME_H 1
-#define HAVE_SYS_TYPES_H 1
-#define HAVE_SYS_UTSNAME_H 1
-#define HAVE_TERMCAP_H 1
-#define HAVE_TERMIOS_H 1
-#define HAVE_WCHAR_H 1
-#define HAVE_WCTYPE_H 1
-#define HAVE_UNISTD_H 1
-#define HAVE_UTIME_H 1
-
-#define HAVE_SYS_WAIT_H 1
-
-#define HAVE_STDLIB_H 1
-#define HAVE_STRING_H 1
-
-#define FEAT_TINY 1
-
-#define ENABLE_CSCOPE 1
-
-#define HAVE_DLFCN_H 1
-
-#define HAVE_DLOPEN 1
-
-#define HAVE_DLSYM 1
-
-#define USE_XSMP_INTERACT 1
-
-#define HAVE_FD_CLOEXEC 1
-
-#define PROC_EXE_LINK "/proc/self/exe"
-
-#define HAVE_ISINF 1
-
-#define HAVE_ISNAN 1
-
-#define HAVE_DIRFD 1
-
-#define HAVE_FLOCK 1
-
-#define HAVE_SYSCONF_SIGSTKSZ 1
+enum { VIM_SIZEOF_INT = 4 };
 
 // ---------------- end config.h ----------------
-# define HAVE_PATHDEF
-
-# define UINT32_TYPEDEF uint32_t
-
-# define ROOT_UID 0
 
-// ---------------- begin feature.h ----------------
-
-#define FEAT_SEARCH_EXTRA
-
-# define USE_ICONV
-
-# define USE_DLOPEN
-
-# define FEAT_WRITEBACKUP
-
-#define SESSION_FILE    "Session.vim"
-
-# define FILETYPE_FILE          "filetype.vim"
-# define FTPLUGIN_FILE          "ftplugin.vim"
-# define INDENT_FILE            "indent.vim"
-# define FTOFF_FILE             "ftoff.vim"
-# define FTPLUGOF_FILE          "ftplugof.vim"
-# define INDOFF_FILE            "indoff.vim"
-
-# define SYS_OPTWIN_FILE        "$VIMRUNTIME/optwin.vim"
-
-# define RUNTIME_DIRNAME "runtime"
-
-# define FEAT_MOUSE_XTERM
-
-# define FEAT_FILTERPIPE
-
-// ---------------- end feature.h ----------------
-
-# define VIMPACKAGE     "vim"
+enum { ROOT_UID = 0 };
 
 // ---------------- begin os_unix.h ----------------
 
-# define USE_GETCWD
-
-#  define vim_mkdir(x, y) mkdir((char *)(x), y)
-#  define mch_rmdir(x) rmdir((char *)(x))
-#  define mch_remove(x) unlink((char *)(x))
-
-#define SIGHASARG
-
-# define SIGPROTOARG    (int)
-# define SIGDEFARG(s)   (int s UNUSED)
-# define SIGDUMMYARG    0
-
-typedef void (*sighandler_T) SIGPROTOARG;
-
-#  define NAMLEN(dirent) strlen((dirent)->d_name)
-
-#define BASENAMELEN     (MAXNAMLEN - 5)
-
-# define HAVE_TOTAL_MEM
-
-# define SYS_VIMRC_FILE "$VIM/vimrc"
-# define SYS_GVIMRC_FILE "$VIM/gvimrc"
-# define DFLT_HELPFILE  "$VIMRUNTIME/doc/help.txt"
-# define SYS_MENU_FILE  "$VIMRUNTIME/menu.vim"
-
-#  define USR_EXRC_FILE "$HOME/.exrc"
-
-#  define USR_VIMRC_FILE "$HOME/.vimrc"
-
-#  define USR_VIMRC_FILE2       "~/.vim/vimrc"
-
-# define XDG_VIMRC_FILE (mch_getenv((char_u *)"XDG_CONFIG_HOME")        ? "$XDG_CONFIG_HOME/vim/vimrc"  : "~/.config/vim/vimrc")
-
-#  define USR_GVIMRC_FILE "$HOME/.gvimrc"
-
-#  define USR_GVIMRC_FILE2      "~/.vim/gvimrc"
-
-#  define USR_GVIMRC_FILE3 (mch_getenv("XDG_CONFIG_HOME")       ? "$XDG_CONFIG_HOME/vim/gvimrc"         : "~/.config/vim/gvimrc")
-
-# define VIM_DEFAULTS_FILE "$VIMRUNTIME/defaults.vim"
-
-# define EVIM_FILE      "$VIMRUNTIME/evim.vim"
-
-# define EXRC_FILE      ".exrc"
-
-# define VIMRC_FILE     ".vimrc"
-
-# define SYNTAX_FNAME   "$VIMRUNTIME/syntax/%s.vim"
-
-#  define DFLT_BDIR    ".,~/tmp,~/"
-
-#  define DFLT_DIR     ".,~/tmp,/var/tmp,/tmp"
-
-#  define DFLT_VDIR    "$HOME/.vim/view"
-#  define XDG_VDIR     (mch_getenv("XDG_CONFIG_HOME") ?         "$XDG_CONFIG_HOME/vim/view" : "~/.config/vim/view")
-
-#define DFLT_ERRORFILE          "errors.err"
-
-#   define DFLT_RUNTIMEPATH     "~/.vim,$VIM/vimfiles,$VIMRUNTIME,$VIM/vimfiles/after,~/.vim/after"
-#   define XDG_RUNTIMEPATH      "$XDG_CONFIG_HOME/vim,$VIM/vimfiles,$VIMRUNTIME,$VIM/vimfiles/after,$XDG_CONFIG_HOME/vim/after"
-#   define XDG_RUNTIMEPATH_FB   "~/.config/vim,$VIM/vimfiles,$VIMRUNTIME,$VIM/vimfiles/after,~/.config/vim/after"
-#   define CLEAN_RUNTIMEPATH    "$VIM/vimfiles,$VIMRUNTIME,$VIM/vimfiles/after"
-
-# define TEMPDIRNAMES  "$TMPDIR", "/tmp", ".", "$HOME"
-# define TEMPNAMELEN    256
-
-#define SPECIAL_WILDCHAR    "`'{"
-
-#define CMDBUFFSIZE 1024
+typedef void (*sighandler_T)  (int) ;
 
-# define MAXPATHL       PATH_MAX
-
-#define CHECK_INODE
-#  define DFLT_MAXMEM   (5*1024)
-#  define DFLT_MAXMEMTOT        (10*1024)
-
-# define mch_memmove(to, from, len) memmove((char *)(to), (char *)(from), len)
-
-#  define mch_rename(src, dst) rename(src, dst)
-#   define mch_getenv(x) (char_u *)getenv((char *)(x))
-#  define mch_setenv(name, val, x) setenv(name, val, x)
-
-#  define JMP_BUF sigjmp_buf
-#  define SETJMP(x) sigsetjmp((x), 1)
-#  define LONGJMP siglongjmp
-
-# define HAVE_DUP
-#define HAVE_ST_MODE
-
-#define HAVE_ACL (HAVE_POSIX_ACL || HAVE_SOLARIS_ACL || HAVE_AIX_ACL)
+enum { TEMPNAMELEN = 256 };
 
 // ---------------- end os_unix.h ----------------
 
-#  define UNUSED __attribute__((unused))
-
 // ---------------- begin osdef.h ----------------
 
 extern int      _Xmblen(char *, size_t);
 
 // ---------------- end osdef.h ----------------
 
-#  define PATH_ESC_CHARS ((char_u *)" \t\n*?[{`$\\%#'\"|!<")
-#  define SHELL_ESC_CHARS ((char_u *)" \t\n*?[{`$\\%#'\"|!<>();&")
-# define BUFFER_ESC_CHARS ((char_u *)" \t\n*?[`$\\%#'\"|!<")
-
-#define NUMBUFLEN 65
-
-#define STR2NR_BIN  0x01
-#define STR2NR_OCT  0x02
-#define STR2NR_HEX  0x04
-#define STR2NR_OOCT 0x08
-#define STR2NR_ALL (STR2NR_BIN + STR2NR_OCT + STR2NR_HEX + STR2NR_OOCT)
-#define STR2NR_NO_OCT (STR2NR_BIN + STR2NR_HEX + STR2NR_OOCT)
+enum { NUMBUFLEN = 65 };
 
-#define STR2NR_FORCE 0x80
+enum { STR2NR_BIN = 0x01 };
+enum { STR2NR_OCT = 0x02 };
+enum { STR2NR_HEX = 0x04 };
+enum { STR2NR_OOCT = 0x08 };
 
-#define STR2NR_QUOTE 0x10
+enum { STR2NR_FORCE = 0x80 };
+
+enum { STR2NR_QUOTE = 0x10 };
 
 typedef unsigned char   char_u;
 typedef unsigned short  short_u;
@@ -380,19 +254,12 @@
 
 typedef unsigned long           long_u;
 typedef          long           long_i;
-# define SCANF_HEX_LONG_U       "%lx"
-# define SCANF_DECIMAL_LONG_U   "%lu"
-# define PRINTF_HEX_LONG_U      "0x%lx"
-#define PRINTF_DECIMAL_LONG_U SCANF_DECIMAL_LONG_U
 
 typedef off_t off_T;
-#  define vim_lseek lseek
-#  define vim_ftell ftello
-#  define vim_fseek fseeko
 
 typedef char_u schar_T;
 typedef unsigned short sattr_T;
-#define MAX_TYPENR 65535
+enum { MAX_TYPENR = 65535 };
 
 typedef unsigned int u8char_T;
 
@@ -400,135 +267,91 @@
 
 // ---------------- begin ascii.h ----------------
 
-#define CharOrd(x)      ((x) < 'a' ? (x) - 'A' : (x) - 'a')
-#define CharOrdLow(x)   ((x) - 'a')
-#define CharOrdUp(x)    ((x) - 'A')
-#define ROT13(c, a)     (((((c) - (a)) + 13) % 26) + (a))
-
-#define NUL             '\000'
-#define BELL            '\007'
-#define BS              '\010'
-#define TAB             '\011'
-#define NL              '\012'
-#define NL_STR          (char_u *)"\012"
-#define FF              '\014'
-#define CAR             '\015'
-#define ESC             '\033'
-#define ESC_STR         (char_u *)"\033"
-#define ESC_STR_nc      "\033"
-#define DEL             0x7f
-#define DEL_STR         (char_u *)"\177"
-
-#define POUND           0xA3
-
-#define Ctrl_chr(x)     (TOUPPER_ASC(x) ^ 0x40)
-#define Meta(x)         ((x) | 0x80)
-
-#define CTRL_F_STR      "\006"
-#define CTRL_H_STR      "\010"
-#define CTRL_V_STR      "\026"
-
-#define Ctrl_AT         0
-#define Ctrl_A          1
-#define Ctrl_B          2
-#define Ctrl_C          3
-#define Ctrl_D          4
-#define Ctrl_E          5
-#define Ctrl_F          6
-#define Ctrl_G          7
-#define Ctrl_H          8
-#define Ctrl_I          9
-#define Ctrl_J          10
-#define Ctrl_K          11
-#define Ctrl_L          12
-#define Ctrl_M          13
-#define Ctrl_N          14
-#define Ctrl_O          15
-#define Ctrl_P          16
-#define Ctrl_Q          17
-#define Ctrl_R          18
-#define Ctrl_S          19
-#define Ctrl_T          20
-#define Ctrl_U          21
-#define Ctrl_V          22
-#define Ctrl_W          23
-#define Ctrl_X          24
-#define Ctrl_Y          25
-#define Ctrl_Z          26
-#define Ctrl_BSL        28
-#define Ctrl_RSB        29
-#define Ctrl_HAT        30
-#define Ctrl__          31
-
-#define CSI             0x9b
-#define CSI_STR         "\233"
-#define DCS             0x90
-#define OSC             0x9d
-#define STERM           0x9c
-
-# define PATHSEP        ((char_u)'/')
-# define PATHSEPSTR     "/"
+enum { NUL = '\000' };
+enum { BELL = '\007' };
+enum { BS = '\010' };
+enum { TAB = '\011' };
+enum { NL = '\012' };
+enum { CAR = '\015' };
+enum { ESC = '\033' };
+enum { DEL = 0x7f };
+
+enum { POUND = 0xA3 };
+
+enum { Ctrl_A = 1 };
+enum { Ctrl_B = 2 };
+enum { Ctrl_C = 3 };
+enum { Ctrl_D = 4 };
+enum { Ctrl_E = 5 };
+enum { Ctrl_F = 6 };
+enum { Ctrl_G = 7 };
+enum { Ctrl_H = 8 };
+enum { Ctrl_I = 9 };
+enum { Ctrl_J = 10 };
+enum { Ctrl_K = 11 };
+enum { Ctrl_L = 12 };
+enum { Ctrl_N = 14 };
+enum { Ctrl_O = 15 };
+enum { Ctrl_P = 16 };
+enum { Ctrl_Q = 17 };
+enum { Ctrl_R = 18 };
+enum { Ctrl_S = 19 };
+enum { Ctrl_T = 20 };
+enum { Ctrl_U = 21 };
+enum { Ctrl_V = 22 };
+enum { Ctrl_W = 23 };
+enum { Ctrl_X = 24 };
+enum { Ctrl_Y = 25 };
+enum { Ctrl_Z = 26 };
+enum { Ctrl_BSL = 28 };
+enum { Ctrl_RSB = 29 };
+enum { Ctrl_HAT = 30 };
+enum { Ctrl__ = 31 };
+
+enum { CSI = 0x9b };
+enum { DCS = 0x90 };
+enum { OSC = 0x9d };
+enum { STERM = 0x9c };
 
 // ---------------- end ascii.h ----------------
 // ---------------- begin keymap.h ----------------
 
-#define K_NUL                   (0xce)
-
-#define K_SPECIAL               (0x80)
-
-#define IS_SPECIAL(c)           ((c) < 0)
-
-#define ABBR_OFF                0x100
-
-#define KS_ZERO                 255
-
-#define KS_SPECIAL              254
+enum { ABBR_OFF = 0x100 };
 
-#define KS_EXTRA                253
+enum { KS_ZERO = 255 };
 
-#define KS_MODIFIER             252
+enum { KS_SPECIAL = 254 };
 
-#define KS_MOUSE                251
-#define KS_MENU                 250
-#define KS_VER_SCROLLBAR        249
-#define KS_HOR_SCROLLBAR        248
+enum { KS_EXTRA = 253 };
 
-#define KS_NETTERM_MOUSE        247
-#define KS_DEC_MOUSE            246
+enum { KS_MODIFIER = 252 };
 
-#define KS_SELECT               245
-#define K_SELECT_STRING         (char_u *)"\200\365X"
+enum { KS_MOUSE = 251 };
+enum { KS_MENU = 250 };
+enum { KS_VER_SCROLLBAR = 249 };
+enum { KS_HOR_SCROLLBAR = 248 };
 
-#define KS_TEAROFF              244
+enum { KS_NETTERM_MOUSE = 247 };
+enum { KS_DEC_MOUSE = 246 };
 
-#define KS_JSBTERM_MOUSE        243
+enum { KS_SELECT = 245 };
 
-#define KS_KEY                  242
+enum { KS_TEAROFF = 244 };
 
-#define KS_PTERM_MOUSE          241
+enum { KS_JSBTERM_MOUSE = 243 };
 
-#define KS_TABLINE              240
+enum { KS_KEY = 242 };
 
-#define KS_TABMENU              239
+enum { KS_PTERM_MOUSE = 241 };
 
-#define KS_URXVT_MOUSE          238
+enum { KS_TABLINE = 240 };
 
-#define KS_SGR_MOUSE            237
-#define KS_SGR_MOUSE_RELEASE    236
+enum { KS_TABMENU = 239 };
 
-#define KS_GPM_MOUSE            235
+enum { KS_URXVT_MOUSE = 238 };
 
-#define KE_FILLER               ('X')
-
-#define TERMCAP2KEY(a, b)       (-((a) + ((int)(b) << 8)))
-#define KEY2TERMCAP0(x)         ((-(x)) & 0xff)
-#define KEY2TERMCAP1(x)         (((unsigned)(-(x)) >> 8) & 0xff)
-
-#define K_SECOND(c)     ((c) == K_SPECIAL ? KS_SPECIAL : (c) == NUL ? KS_ZERO : KEY2TERMCAP0(c))
-
-#define K_THIRD(c)      (((c) == K_SPECIAL || (c) == NUL) ? KE_FILLER : KEY2TERMCAP1(c))
-
-#define TO_SPECIAL(a, b)    ((a) == KS_SPECIAL ? K_SPECIAL : (a) == KS_ZERO ? K_ZERO : TERMCAP2KEY(a, b))
+enum { KS_SGR_MOUSE = 237 };
+enum { KS_SGR_MOUSE_RELEASE = 236 };
 
 enum key_extra
 {
@@ -658,222 +481,17 @@
     , KE_COMPLETE_DELAY = 110
 };
 
-#define K_ZERO          TERMCAP2KEY(KS_ZERO, KE_FILLER)
-
-#define K_ESC           TERMCAP2KEY(KS_EXTRA, KE_ESC)
-
-#define K_UP            TERMCAP2KEY('k', 'u')
-#define K_DOWN          TERMCAP2KEY('k', 'd')
-#define K_LEFT          TERMCAP2KEY('k', 'l')
-#define K_RIGHT         TERMCAP2KEY('k', 'r')
-#define K_S_UP          TERMCAP2KEY(KS_EXTRA, KE_S_UP)
-#define K_S_DOWN        TERMCAP2KEY(KS_EXTRA, KE_S_DOWN)
-#define K_S_LEFT        TERMCAP2KEY('#', '4')
-#define K_C_LEFT        TERMCAP2KEY(KS_EXTRA, KE_C_LEFT)
-#define K_S_RIGHT       TERMCAP2KEY('%', 'i')
-#define K_C_RIGHT       TERMCAP2KEY(KS_EXTRA, KE_C_RIGHT)
-
-#define K_S_HOME        TERMCAP2KEY('#', '2')
-#define K_C_HOME        TERMCAP2KEY(KS_EXTRA, KE_C_HOME)
-#define K_S_END         TERMCAP2KEY('*', '7')
-#define K_C_END         TERMCAP2KEY(KS_EXTRA, KE_C_END)
-
-#define K_TAB           TERMCAP2KEY(KS_EXTRA, KE_TAB)
-#define K_S_TAB         TERMCAP2KEY('k', 'B')
-#define K_S_BS          TERMCAP2KEY(KS_EXTRA, KE_S_BS)
-
-#define K_XF1           TERMCAP2KEY(KS_EXTRA, KE_XF1)
-#define K_XF2           TERMCAP2KEY(KS_EXTRA, KE_XF2)
-#define K_XF3           TERMCAP2KEY(KS_EXTRA, KE_XF3)
-#define K_XF4           TERMCAP2KEY(KS_EXTRA, KE_XF4)
-
-#define K_XUP           TERMCAP2KEY(KS_EXTRA, KE_XUP)
-#define K_XDOWN         TERMCAP2KEY(KS_EXTRA, KE_XDOWN)
-#define K_XLEFT         TERMCAP2KEY(KS_EXTRA, KE_XLEFT)
-#define K_XRIGHT        TERMCAP2KEY(KS_EXTRA, KE_XRIGHT)
-
-#define K_F1            TERMCAP2KEY('k', '1')
-#define K_F2            TERMCAP2KEY('k', '2')
-#define K_F3            TERMCAP2KEY('k', '3')
-#define K_F4            TERMCAP2KEY('k', '4')
-#define K_F5            TERMCAP2KEY('k', '5')
-#define K_F6            TERMCAP2KEY('k', '6')
-#define K_F7            TERMCAP2KEY('k', '7')
-#define K_F8            TERMCAP2KEY('k', '8')
-#define K_F9            TERMCAP2KEY('k', '9')
-#define K_F10           TERMCAP2KEY('k', ';')
-
-#define K_F11           TERMCAP2KEY('F', '1')
-#define K_F12           TERMCAP2KEY('F', '2')
-#define K_F13           TERMCAP2KEY('F', '3')
-#define K_F14           TERMCAP2KEY('F', '4')
-#define K_F15           TERMCAP2KEY('F', '5')
-#define K_F16           TERMCAP2KEY('F', '6')
-#define K_F17           TERMCAP2KEY('F', '7')
-#define K_F18           TERMCAP2KEY('F', '8')
-#define K_F19           TERMCAP2KEY('F', '9')
-#define K_F20           TERMCAP2KEY('F', 'A')
-
-#define K_F21           TERMCAP2KEY('F', 'B')
-#define K_F22           TERMCAP2KEY('F', 'C')
-#define K_F23           TERMCAP2KEY('F', 'D')
-#define K_F24           TERMCAP2KEY('F', 'E')
-#define K_F25           TERMCAP2KEY('F', 'F')
-#define K_F26           TERMCAP2KEY('F', 'G')
-#define K_F27           TERMCAP2KEY('F', 'H')
-#define K_F28           TERMCAP2KEY('F', 'I')
-#define K_F29           TERMCAP2KEY('F', 'J')
-#define K_F30           TERMCAP2KEY('F', 'K')
-
-#define K_F31           TERMCAP2KEY('F', 'L')
-#define K_F32           TERMCAP2KEY('F', 'M')
-#define K_F33           TERMCAP2KEY('F', 'N')
-#define K_F34           TERMCAP2KEY('F', 'O')
-#define K_F35           TERMCAP2KEY('F', 'P')
-#define K_F36           TERMCAP2KEY('F', 'Q')
-#define K_F37           TERMCAP2KEY('F', 'R')
-
-#define K_S_XF1         TERMCAP2KEY(KS_EXTRA, KE_S_XF1)
-#define K_S_XF2         TERMCAP2KEY(KS_EXTRA, KE_S_XF2)
-#define K_S_XF3         TERMCAP2KEY(KS_EXTRA, KE_S_XF3)
-#define K_S_XF4         TERMCAP2KEY(KS_EXTRA, KE_S_XF4)
-
-#define K_S_F1          TERMCAP2KEY(KS_EXTRA, KE_S_F1)
-#define K_S_F2          TERMCAP2KEY(KS_EXTRA, KE_S_F2)
-#define K_S_F3          TERMCAP2KEY(KS_EXTRA, KE_S_F3)
-#define K_S_F4          TERMCAP2KEY(KS_EXTRA, KE_S_F4)
-#define K_S_F5          TERMCAP2KEY(KS_EXTRA, KE_S_F5)
-#define K_S_F6          TERMCAP2KEY(KS_EXTRA, KE_S_F6)
-#define K_S_F7          TERMCAP2KEY(KS_EXTRA, KE_S_F7)
-#define K_S_F8          TERMCAP2KEY(KS_EXTRA, KE_S_F8)
-#define K_S_F9          TERMCAP2KEY(KS_EXTRA, KE_S_F9)
-#define K_S_F10         TERMCAP2KEY(KS_EXTRA, KE_S_F10)
-
-#define K_S_F11         TERMCAP2KEY(KS_EXTRA, KE_S_F11)
-#define K_S_F12         TERMCAP2KEY(KS_EXTRA, KE_S_F12)
-
-#define K_HELP          TERMCAP2KEY('%', '1')
-#define K_UNDO          TERMCAP2KEY('&', '8')
-
-#define K_BS            TERMCAP2KEY('k', 'b')
-
-#define K_INS           TERMCAP2KEY('k', 'I')
-#define K_KINS          TERMCAP2KEY(KS_EXTRA, KE_KINS)
-#define K_DEL           TERMCAP2KEY('k', 'D')
-#define K_KDEL          TERMCAP2KEY(KS_EXTRA, KE_KDEL)
-#define K_HOME          TERMCAP2KEY('k', 'h')
-#define K_KHOME         TERMCAP2KEY('K', '1')
-#define K_XHOME         TERMCAP2KEY(KS_EXTRA, KE_XHOME)
-#define K_ZHOME         TERMCAP2KEY(KS_EXTRA, KE_ZHOME)
-#define K_END           TERMCAP2KEY('@', '7')
-#define K_KEND          TERMCAP2KEY('K', '4')
-#define K_XEND          TERMCAP2KEY(KS_EXTRA, KE_XEND)
-#define K_ZEND          TERMCAP2KEY(KS_EXTRA, KE_ZEND)
-#define K_PAGEUP        TERMCAP2KEY('k', 'P')
-#define K_PAGEDOWN      TERMCAP2KEY('k', 'N')
-#define K_KPAGEUP       TERMCAP2KEY('K', '3')
-#define K_KPAGEDOWN     TERMCAP2KEY('K', '5')
-
-#define K_KPLUS         TERMCAP2KEY('K', '6')
-#define K_KMINUS        TERMCAP2KEY('K', '7')
-#define K_KDIVIDE       TERMCAP2KEY('K', '8')
-#define K_KMULTIPLY     TERMCAP2KEY('K', '9')
-#define K_KENTER        TERMCAP2KEY('K', 'A')
-#define K_KPOINT        TERMCAP2KEY('K', 'B')
-#define K_PS            TERMCAP2KEY('P', 'S')
-#define K_PE            TERMCAP2KEY('P', 'E')
-
-#define K_K0            TERMCAP2KEY('K', 'C')
-#define K_K1            TERMCAP2KEY('K', 'D')
-#define K_K2            TERMCAP2KEY('K', 'E')
-#define K_K3            TERMCAP2KEY('K', 'F')
-#define K_K4            TERMCAP2KEY('K', 'G')
-#define K_K5            TERMCAP2KEY('K', 'H')
-#define K_K6            TERMCAP2KEY('K', 'I')
-#define K_K7            TERMCAP2KEY('K', 'J')
-#define K_K8            TERMCAP2KEY('K', 'K')
-#define K_K9            TERMCAP2KEY('K', 'L')
-
-#define K_MOUSE         TERMCAP2KEY(KS_MOUSE, KE_FILLER)
-#define K_MENU          TERMCAP2KEY(KS_MENU, KE_FILLER)
-#define K_VER_SCROLLBAR TERMCAP2KEY(KS_VER_SCROLLBAR, KE_FILLER)
-#define K_HOR_SCROLLBAR   TERMCAP2KEY(KS_HOR_SCROLLBAR, KE_FILLER)
-
-#define K_NETTERM_MOUSE TERMCAP2KEY(KS_NETTERM_MOUSE, KE_FILLER)
-#define K_DEC_MOUSE     TERMCAP2KEY(KS_DEC_MOUSE, KE_FILLER)
-#define K_JSBTERM_MOUSE TERMCAP2KEY(KS_JSBTERM_MOUSE, KE_FILLER)
-#define K_PTERM_MOUSE   TERMCAP2KEY(KS_PTERM_MOUSE, KE_FILLER)
-#define K_URXVT_MOUSE   TERMCAP2KEY(KS_URXVT_MOUSE, KE_FILLER)
-#define K_SGR_MOUSE     TERMCAP2KEY(KS_SGR_MOUSE, KE_FILLER)
-#define K_SGR_MOUSERELEASE TERMCAP2KEY(KS_SGR_MOUSE_RELEASE, KE_FILLER)
-
-#define K_SELECT        TERMCAP2KEY(KS_SELECT, KE_FILLER)
-#define K_TEAROFF       TERMCAP2KEY(KS_TEAROFF, KE_FILLER)
-
-#define K_TABLINE       TERMCAP2KEY(KS_TABLINE, KE_FILLER)
-#define K_TABMENU       TERMCAP2KEY(KS_TABMENU, KE_FILLER)
-
-#define K_LEFTMOUSE     TERMCAP2KEY(KS_EXTRA, KE_LEFTMOUSE)
-#define K_LEFTMOUSE_NM  TERMCAP2KEY(KS_EXTRA, KE_LEFTMOUSE_NM)
-#define K_LEFTDRAG      TERMCAP2KEY(KS_EXTRA, KE_LEFTDRAG)
-#define K_LEFTRELEASE   TERMCAP2KEY(KS_EXTRA, KE_LEFTRELEASE)
-#define K_LEFTRELEASE_NM TERMCAP2KEY(KS_EXTRA, KE_LEFTRELEASE_NM)
-#define K_MOUSEMOVE     TERMCAP2KEY(KS_EXTRA, KE_MOUSEMOVE)
-#define K_MIDDLEMOUSE   TERMCAP2KEY(KS_EXTRA, KE_MIDDLEMOUSE)
-#define K_MIDDLEDRAG    TERMCAP2KEY(KS_EXTRA, KE_MIDDLEDRAG)
-#define K_MIDDLERELEASE TERMCAP2KEY(KS_EXTRA, KE_MIDDLERELEASE)
-#define K_RIGHTMOUSE    TERMCAP2KEY(KS_EXTRA, KE_RIGHTMOUSE)
-#define K_RIGHTDRAG     TERMCAP2KEY(KS_EXTRA, KE_RIGHTDRAG)
-#define K_RIGHTRELEASE  TERMCAP2KEY(KS_EXTRA, KE_RIGHTRELEASE)
-#define K_X1MOUSE       TERMCAP2KEY(KS_EXTRA, KE_X1MOUSE)
-#define K_X1MOUSE       TERMCAP2KEY(KS_EXTRA, KE_X1MOUSE)
-#define K_X1DRAG        TERMCAP2KEY(KS_EXTRA, KE_X1DRAG)
-#define K_X1RELEASE     TERMCAP2KEY(KS_EXTRA, KE_X1RELEASE)
-#define K_X2MOUSE       TERMCAP2KEY(KS_EXTRA, KE_X2MOUSE)
-#define K_X2DRAG        TERMCAP2KEY(KS_EXTRA, KE_X2DRAG)
-#define K_X2RELEASE     TERMCAP2KEY(KS_EXTRA, KE_X2RELEASE)
-
-#define K_IGNORE        TERMCAP2KEY(KS_EXTRA, KE_IGNORE)
-#define K_NOP           TERMCAP2KEY(KS_EXTRA, KE_NOP)
-#define K_CANCEL        TERMCAP2KEY(KS_EXTRA, KE_CANCEL)
-
-#define K_MOUSEDOWN     TERMCAP2KEY(KS_EXTRA, KE_MOUSEDOWN)
-#define K_MOUSEUP       TERMCAP2KEY(KS_EXTRA, KE_MOUSEUP)
-#define K_MOUSELEFT     TERMCAP2KEY(KS_EXTRA, KE_MOUSELEFT)
-#define K_MOUSERIGHT    TERMCAP2KEY(KS_EXTRA, KE_MOUSERIGHT)
-
-#define K_CSI           TERMCAP2KEY(KS_EXTRA, KE_CSI)
-#define K_OSC           TERMCAP2KEY(KS_EXTRA, KE_OSC)
-#define K_SNR           TERMCAP2KEY(KS_EXTRA, KE_SNR)
-#define K_PLUG          TERMCAP2KEY(KS_EXTRA, KE_PLUG)
-#define K_CMDWIN        TERMCAP2KEY(KS_EXTRA, KE_CMDWIN)
-
-#define K_DROP          TERMCAP2KEY(KS_EXTRA, KE_DROP)
-#define K_FOCUSGAINED   TERMCAP2KEY(KS_EXTRA, KE_FOCUSGAINED)
-#define K_FOCUSLOST     TERMCAP2KEY(KS_EXTRA, KE_FOCUSLOST)
-
-#define K_CURSORHOLD    TERMCAP2KEY(KS_EXTRA, KE_CURSORHOLD)
-#define K_COMPLETE_DELAY TERMCAP2KEY(KS_EXTRA, KE_COMPLETE_DELAY)
-
-#define K_COMMAND       TERMCAP2KEY(KS_EXTRA, KE_COMMAND)
-#define K_SCRIPT_COMMAND TERMCAP2KEY(KS_EXTRA, KE_SCRIPT_COMMAND)
-#define K_SID           TERMCAP2KEY(KS_EXTRA, KE_SID)
-
-#define K_WILD          TERMCAP2KEY(KS_EXTRA, KE_WILD)
-
-#define MOD_MASK_SHIFT      0x02
-#define MOD_MASK_CTRL       0x04
-#define MOD_MASK_ALT        0x08
-#define MOD_MASK_META       0x10
-#define MOD_MASK_2CLICK     0x20
-#define MOD_MASK_3CLICK     0x40
-#define MOD_MASK_4CLICK     0x60
-
-#define MOD_MASK_MULTI_CLICK    (MOD_MASK_2CLICK|MOD_MASK_3CLICK|MOD_MASK_4CLICK)
+enum { MOD_MASK_SHIFT = 0x02 };
+enum { MOD_MASK_CTRL = 0x04 };
+enum { MOD_MASK_ALT = 0x08 };
+enum { MOD_MASK_META = 0x10 };
+enum { MOD_MASK_2CLICK = 0x20 };
+enum { MOD_MASK_3CLICK = 0x40 };
+enum { MOD_MASK_4CLICK = 0x60 };
 
-#define MAX_KEY_NAME_LEN    32
+enum { MAX_KEY_NAME_LEN = 32 };
 
-#define MAX_KEY_CODE_LEN    6
+enum { MAX_KEY_CODE_LEN = 6 };
 
 // ---------------- end keymap.h ----------------
 // ---------------- begin termdefs.h ----------------
@@ -976,105 +594,8 @@
     KS_ESU
 };
 
-#define KS_LAST     KS_ESU
-
 static char_u *(term_strings[]);
 
-#define T_NAME  (TERM_STR(KS_NAME))
-#define T_CE    (TERM_STR(KS_CE))
-#define T_AL    (TERM_STR(KS_AL))
-#define T_CAL   (TERM_STR(KS_CAL))
-#define T_DL    (TERM_STR(KS_DL))
-#define T_CDL   (TERM_STR(KS_CDL))
-#define T_CS    (TERM_STR(KS_CS))
-#define T_CSV   (TERM_STR(KS_CSV))
-#define T_CL    (TERM_STR(KS_CL))
-#define T_CD    (TERM_STR(KS_CD))
-#define T_UT    (TERM_STR(KS_UT))
-#define T_DA    (TERM_STR(KS_DA))
-#define T_DB    (TERM_STR(KS_DB))
-#define T_VI    (TERM_STR(KS_VI))
-#define T_VE    (TERM_STR(KS_VE))
-#define T_VS    (TERM_STR(KS_VS))
-#define T_CVS   (TERM_STR(KS_CVS))
-#define T_CSH   (TERM_STR(KS_CSH))
-#define T_CRC   (TERM_STR(KS_CRC))
-#define T_CRS   (TERM_STR(KS_CRS))
-#define T_ME    (TERM_STR(KS_ME))
-#define T_MR    (TERM_STR(KS_MR))
-#define T_MD    (TERM_STR(KS_MD))
-#define T_SE    (TERM_STR(KS_SE))
-#define T_SO    (TERM_STR(KS_SO))
-#define T_CZH   (TERM_STR(KS_CZH))
-#define T_CZR   (TERM_STR(KS_CZR))
-#define T_UE    (TERM_STR(KS_UE))
-#define T_US    (TERM_STR(KS_US))
-#define T_UCE   (TERM_STR(KS_UCE))
-#define T_UCS   (TERM_STR(KS_UCS))
-#define T_USS   (TERM_STR(KS_USS))
-#define T_DS    (TERM_STR(KS_DS))
-#define T_CDS   (TERM_STR(KS_CDS))
-#define T_STE   (TERM_STR(KS_STE))
-#define T_STS   (TERM_STR(KS_STS))
-#define T_MS    (TERM_STR(KS_MS))
-#define T_CM    (TERM_STR(KS_CM))
-#define T_SR    (TERM_STR(KS_SR))
-#define T_CRI   (TERM_STR(KS_CRI))
-#define T_VB    (TERM_STR(KS_VB))
-#define T_KS    (TERM_STR(KS_KS))
-#define T_KE    (TERM_STR(KS_KE))
-#define T_TI    (TERM_STR(KS_TI))
-#define T_CTI   (TERM_STR(KS_CTI))
-#define T_CRK   (TERM_STR(KS_CRK))
-#define T_TE    (TERM_STR(KS_TE))
-#define T_CTE   (TERM_STR(KS_CTE))
-#define T_BC    (TERM_STR(KS_BC))
-#define T_CCS   (TERM_STR(KS_CCS))
-#define T_CCO   (TERM_STR(KS_CCO))
-#define T_CSF   (TERM_STR(KS_CSF))
-#define T_CSB   (TERM_STR(KS_CSB))
-#define T_XS    (TERM_STR(KS_XS))
-#define T_XN    (TERM_STR(KS_XN))
-#define T_MB    (TERM_STR(KS_MB))
-#define T_CAF   (TERM_STR(KS_CAF))
-#define T_CAB   (TERM_STR(KS_CAB))
-#define T_CAU   (TERM_STR(KS_CAU))
-#define T_CFO   (TERM_STR(KS_CF))
-#define T_LE    (TERM_STR(KS_LE))
-#define T_ND    (TERM_STR(KS_ND))
-#define T_CIS   (TERM_STR(KS_CIS))
-#define T_CIE   (TERM_STR(KS_CIE))
-#define T_TS    (TERM_STR(KS_TS))
-#define T_FS    (TERM_STR(KS_FS))
-#define T_CSC   (TERM_STR(KS_CSC))
-#define T_CEC   (TERM_STR(KS_CEC))
-#define T_CWP   (TERM_STR(KS_CWP))
-#define T_CGP   (TERM_STR(KS_CGP))
-#define T_CWS   (TERM_STR(KS_CWS))
-#define T_CSI   (TERM_STR(KS_CSI))
-#define T_CEI   (TERM_STR(KS_CEI))
-#define T_CSR   (TERM_STR(KS_CSR))
-#define T_CRV   (TERM_STR(KS_CRV))
-#define T_CXM   (TERM_STR(KS_CXM))
-#define T_RFG   (TERM_STR(KS_RFG))
-#define T_RBG   (TERM_STR(KS_RBG))
-#define T_OP    (TERM_STR(KS_OP))
-#define T_U7    (TERM_STR(KS_U7))
-#define T_8F    (TERM_STR(KS_8F))
-#define T_8B    (TERM_STR(KS_8B))
-#define T_8U    (TERM_STR(KS_8U))
-#define T_BE    (TERM_STR(KS_CBE))
-#define T_BD    (TERM_STR(KS_CBD))
-#define T_CST   (TERM_STR(KS_CST))
-#define T_CRT   (TERM_STR(KS_CRT))
-#define T_SSI   (TERM_STR(KS_SSI))
-#define T_SRI   (TERM_STR(KS_SRI))
-#define T_FD    (TERM_STR(KS_FD))
-#define T_FE    (TERM_STR(KS_FE))
-#define T_XON   (TERM_STR(KS_XON))
-#define T_BSU   (TERM_STR(KS_BSU))
-#define T_ESU   (TERM_STR(KS_ESU))
-
 typedef enum {
     TMODE_COOK,
     TMODE_SLEEP,
@@ -1085,708 +606,458 @@
 // ---------------- end termdefs.h ----------------
 // ---------------- begin macros.h ----------------
 
-#define LT_POS(a, b) (((a).lnum != (b).lnum)               ? (a).lnum < (b).lnum                   : (a).col != (b).col                        ? (a).col < (b).col                     : (a).coladd < (b).coladd)
-#define LT_POSP(a, b) (((a)->lnum != (b)->lnum)                    ? (a)->lnum < (b)->lnum                 : (a)->col != (b)->col                      ? (a)->col < (b)->col                   : (a)->coladd < (b)->coladd)
-#define EQUAL_POS(a, b) (((a).lnum == (b).lnum) && ((a).col == (b).col) && ((a).coladd == (b).coladd))
-#define CLEAR_POS(a) do {(a)->lnum = 0; (a)->col = 0; (a)->coladd = 0;} while (0)
-#define EMPTY_POS(a) ((a).lnum == 0 && (a).col == 0 && (a).coladd == 0)
-
-#define LTOREQ_POS(a, b) (LT_POS(a, b) || EQUAL_POS(a, b))
-
-#define VIM_ISWHITE(x)          ((x) == ' ' || (x) == '\t')
-#define IS_WHITE_OR_NUL(x)      ((x) == ' ' || (x) == '\t' || (x) == NUL)
-#define IS_WHITE_NL_OR_NUL(x)   ((x) == ' ' || (x) == '\t' || (x) == '\n' || (x) == NUL)
-
-#define LINEEMPTY(p) (*ml_get(p) == NUL)
-
-#define BUFEMPTY() (curbuf->b_ml.ml_line_count == 1 && *ml_get((linenr_T)1) == NUL)
-
-#define SAFE_isalnum(c)  (isalnum ((unsigned char)(c)))
-#define SAFE_isalpha(c)  (isalpha ((unsigned char)(c)))
-#define SAFE_isblank(c)  (isblank ((unsigned char)(c)))
-#define SAFE_iscntrl(c)  (iscntrl ((unsigned char)(c)))
-#define SAFE_isdigit(c)  (isdigit ((unsigned char)(c)))
-#define SAFE_isgraph(c)  (isgraph ((unsigned char)(c)))
-#define SAFE_islower(c)  (islower ((unsigned char)(c)))
-#define SAFE_isprint(c)  (isprint ((unsigned char)(c)))
-#define SAFE_ispunct(c)  (ispunct ((unsigned char)(c)))
-#define SAFE_isspace(c)  (isspace ((unsigned char)(c)))
-#define SAFE_isupper(c)  (isupper ((unsigned char)(c)))
-#define SAFE_isxdigit(c) (isxdigit((unsigned char)(c)))
-#define SAFE_tolower(c)  (tolower ((unsigned char)(c)))
-#define SAFE_toupper(c)  (toupper ((unsigned char)(c)))
-
-#  define TOUPPER_LOC           SAFE_toupper
-#  define TOLOWER_LOC           SAFE_tolower
-
-#define TOUPPER_ASC(c)  (((c) < 'a' || (c) > 'z') ? (c) : (c) - ('a' - 'A'))
-#define TOLOWER_ASC(c)  (((c) < 'A' || (c) > 'Z') ? (c) : (c) + ('a' - 'A'))
-
-#define MB_ISLOWER(c)   vim_islower(c)
-#define MB_ISUPPER(c)   vim_isupper(c)
-#define MB_TOLOWER(c)   vim_tolower(c)
-#define MB_TOUPPER(c)   vim_toupper(c)
-#define MB_CASEFOLD(c)  (enc_utf8 ? utf_fold(c) : MB_TOLOWER(c))
-
-#define VIM_ISDIGIT(c) ((unsigned)(c) - '0' < 10)
-
-#define ASCII_ISLOWER(c) ((unsigned)(c) - 'a' < 26)
-#define ASCII_ISUPPER(c) ((unsigned)(c) - 'A' < 26)
-#define ASCII_ISALPHA(c) (ASCII_ISUPPER(c) || ASCII_ISLOWER(c))
-#define ASCII_ISALNUM(c) (ASCII_ISALPHA(c) || VIM_ISDIGIT(c))
-
-#define EMPTY_IF_NULL(x) ((x) ? (x) : (char_u *)"")
-
-# define LANGMAP_ADJUST(c, condition)
-
-#define VIM_ISBREAK(c) ((c) < 256 && breakat_flags[(char_u)(c)])
-
-#  define mch_access(n, p)      access((n), (p))
-
-#  define mch_fstat(n, p)       fstat((n), (p))
-
-#   define mch_stat(n, p)       stat((n), (p))
-
-# define mch_lstat(n, p)        lstat((n), (p))
-
-# define mch_open_rw(n, f)      mch_open((n), (f), (mode_t)0600)
-
-# define TIME_MSG(s) do {   } while (0)
-
-#define REPLACE_NORMAL(s) (((s) & REPLACE_FLAG) && !((s) & VREPLACE_FLAG))
-
-# define UTF_COMPOSINGLIKE(p1, p2)  utf_iscomposing(utf_ptr2char(p2))
-
-#define MB_PTR_ADV(p)       p += (*mb_ptr2len)(p)
-#define MB_CPTR_ADV(p)      p += enc_utf8 ? utf_ptr2len(p) : (*mb_ptr2len)(p)
-#define MB_PTR_BACK(s, p)  p -= has_mbyte ? ((*mb_head_off)(s, (p) - 1) + 1) : 1
-#define MB_CPTR2LEN(p)      (enc_utf8 ? utf_ptr2len(p) : (*mb_ptr2len)(p))
-
-#define MB_COPY_CHAR(f, t) do { if (has_mbyte) mb_copy_char(&(f), &(t)); else *(t)++ = *(f)++; } while (0)
-#define MB_CHARLEN(p)       (has_mbyte ? mb_charlen(p) : (int)STRLEN(p))
-#define MB_CHAR2LEN(c)      (has_mbyte ? mb_char2len(c) : 1)
-#define PTR2CHAR(p)         (has_mbyte ? mb_ptr2char(p) : (int)*(p))
-#define MB_CHAR2BYTES(c, b) do { if (has_mbyte) (b) += (*mb_char2bytes)((c), (b)); else *(b)++ = (c); } while (0)
-
-# define DO_AUTOCHDIR do {   } while (0)
-
-#define RESET_BINDING(wp)  do { (wp)->w_p_scb = FALSE; (wp)->w_p_crb = FALSE;                       } while (0)
-
-# define PLINES_NOFILL(x) plines(x)
-# define PLINES_WIN_NOFILL(w, l, h) plines_win((w), (l), (h))
-
-#define DI2HIKEY(di) ((di)->di_key)
-#define HIKEY2DI(p)  ((dictitem_T *)((p) - offsetof(dictitem_T, di_key)))
-#define HI2DI(hi)     HIKEY2DI((hi)->hi_key)
-
-# define mch_enable_flush()
-# define mch_disable_flush()
-
-#define VIM_CLEAR(p)     do {   vim_free(p);    (p) = NULL;     } while (0)
-
-#define VIM_CLEAR_STRING(s)     do {    VIM_CLEAR(s.string);    s.length = 0;     } while (0)
-
-#define STR_LITERAL_INIT(s)     {(char_u *)(s), STRLEN_LITERAL(s)}
-
-#define STR_LITERAL_SET(str, s)     do {        (str).string = (char_u *)(s);   (str).length = STRLEN_LITERAL(s);     } while (0)
-
-#define IS_USER_CMDIDX(idx) ((int)(idx) < 0)
-
-# define WIN_IS_POPUP(wp) 0
-# define ERROR_IF_POPUP_WINDOW 0
-# define ERROR_IF_ANY_POPUP_WINDOW 0
-# define ERROR_IF_TERM_POPUP_WINDOW 0
-
-# define ESTACK_CHECK_DECLARATION do {   } while (0)
-# define ESTACK_CHECK_SETUP do {   } while (0)
-# define ESTACK_CHECK_NOW do {   } while (0)
-# define CHECK_CURBUF do {   } while (0)
-
-#define CHECK_LIST_MATERIALIZE(l)     do {      if ((l)->lv_first == &range_list_item)      range_list_materialize(l);     } while (0)
-
-#define GA_GROW_FAILS(gap, n) unlikely((((gap)->ga_maxlen - (gap)->ga_len < (n)) ? ga_grow_inner((gap), (n)) : OK) == FAIL)
-#define GA_GROW_OK(gap, n) likely((((gap)->ga_maxlen - (gap)->ga_len < (n)) ? ga_grow_inner((gap), (n)) : OK) == OK)
-#define GA_CONCAT_LITERAL(gap, s) ga_concat_len((gap), (char_u *)(s), STRLEN_LITERAL(s))
-
-#define ARRAY_LENGTH(a) (sizeof(a) / sizeof((a)[0]))
-
-#define FOR_ALL_WINDOWS(wp)     for ((wp) = firstwin; (wp) != NULL; (wp) = (wp)->w_next)
-#define FOR_ALL_FRAMES(frp, first_frame)     for ((frp) = first_frame; (frp) != NULL; (frp) = (frp)->fr_next)
-#define FOR_ALL_TABPAGES(tp)     for ((tp) = first_tabpage; (tp) != NULL; (tp) = (tp)->tp_next)
-#define FOR_ALL_WINDOWS_IN_TAB(tp, wp)     for ((wp) = ((tp) == NULL || (tp) == curtab)             ? firstwin : (tp)->tp_firstwin; (wp); (wp) = (wp)->w_next)
-#define FOR_ALL_TAB_WINDOWS(tp, wp)     for ((tp) = first_tabpage; (tp) != NULL; (tp) = (tp)->tp_next)  for ((wp) = ((tp) == curtab)            ? firstwin : (tp)->tp_firstwin; (wp); (wp) = (wp)->w_next)
-
-#define FOR_ALL_POPUPWINS(wp)     for ((wp) = first_popupwin; (wp) != NULL; (wp) = (wp)->w_next)
-#define FOR_ALL_POPUPWINS_IN_TAB(tp, wp)     for ((wp) = (tp)->tp_first_popupwin; (wp) != NULL; (wp) = (wp)->w_next)
-
-#define FOR_ALL_BUFFERS(buf)     for ((buf) = firstbuf; (buf) != NULL; (buf) = (buf)->b_next)
-
-#define FOR_ALL_BUF_WININFO(buf, wip)     for ((wip) = (buf)->b_wininfo; (wip) != NULL; (wip) = (wip)->wi_next)
-
-#define FOR_ALL_SIGNS_IN_BUF(buf, sign)     for ((sign) = (buf)->b_signlist; (sign) != NULL; (sign) = (sign)->se_next)
-
-#define FOR_ALL_LIST_ITEMS(l, li)     for ((li) = (l) == NULL ? NULL : (l)->lv_first; (li) != NULL; (li) = (li)->li_next)
-
-#define FOR_ALL_HASHTAB_ITEMS(ht, hi, todo)     for ((hi) = (ht)->ht_array; (todo) > 0; ++(hi))
-
-#define TUPLE_LEN(t)        (t->tv_items.ga_len)
-#define TUPLE_ITEM(t, i)            (((typval_T *)t->tv_items.ga_data) + i)
+enum { ERROR_IF_POPUP_WINDOW = 0 };
+enum { ERROR_IF_ANY_POPUP_WINDOW = 0 };
+enum { ERROR_IF_TERM_POPUP_WINDOW = 0 };
 
 // ---------------- end macros.h ----------------
 
-# define _(x) ((char *)(x))
-# define NGETTEXT(x, xs, n) (((n) == 1) ? (char *)(x) : (char *)(xs))
-# define N_(x) x
-# define bindtextdomain(x, y) ""
-# define bind_textdomain_codeset(x, y)
-# define textdomain(x)
-
-#define UPD_VALID_NO_UPDATE      5
-#define UPD_VALID               10
-#define UPD_INVERTED            20
-#define UPD_INVERTED_ALL        25
-#define UPD_REDRAW_TOP          30
-#define UPD_SOME_VALID          35
-#define UPD_NOT_VALID           40
-#define UPD_CLEAR               50
-
-#define SLF_RIGHTLEFT   1
-#define SLF_POPUP       2
-#define SLF_INC_VCOL    4
-
-#define MB_FILLER_CHAR '<'
-
-#define VALID_WROW      0x01
-#define VALID_WCOL      0x02
-#define VALID_VIRTCOL   0x04
-#define VALID_CHEIGHT   0x08
-#define VALID_CROW      0x10
-#define VALID_BOTLINE   0x20
-#define VALID_BOTLINE_AP 0x40
-#define VALID_TOPLINE   0x80
-
-#define POPF_IS_POPUP   0x01
-#define POPF_HIDDEN     0x02
-#define POPF_HIDDEN_FORCE 0x04
-#define POPF_CURSORLINE 0x08
-#define POPF_ON_CMDLINE 0x10
-#define POPF_DRAG       0x20
-#define POPF_DRAGALL    0x40
-#define POPF_RESIZE     0x80
-#define POPF_MAPPING    0x100
-#define POPF_INFO       0x200
-#define POPF_INFO_MENU  0x400
-#define POPF_POSINVERT  0x800
-#define POPF_OPACITY 0x1000
-#define POPF_CLIPWINDOW 0x2000
-
-#define POPUP_HANDLED_1     0x01
-#define POPUP_HANDLED_2     0x02
-#define POPUP_HANDLED_3     0x04
-#define POPUP_HANDLED_4     0x08
-#define POPUP_HANDLED_5     0x10
-
-#define HL_NORMAL               0x00
-#define HL_INVERSE              0x01
-#define HL_BOLD                 0x02
-#define HL_ITALIC               0x04
-#define HL_UNDERLINE            0x08
-#define HL_UNDERCURL            0x10
-#define HL_UNDERDOUBLE          0x20
-#define HL_UNDERDOTTED          0x40
-#define HL_UNDERDASHED          0x80
-#define HL_STANDOUT             0x100
-#define HL_NOCOMBINE            0x200
-#define HL_STRIKETHROUGH        0x400
-#define HL_ALL                  0x7ff
-
-#define MSG_HIST                0x1000
-
-#define MODE_NORMAL     0x01
-#define MODE_VISUAL     0x02
-#define MODE_OP_PENDING 0x04
-#define MODE_CMDLINE    0x08
-#define MODE_INSERT     0x10
-#define MODE_LANGMAP    0x20
-#define MODE_SELECT     0x40
-#define MODE_TERMINAL   0x80
-
-#define MAP_ALL_MODES   0xff
-
-#define REPLACE_FLAG    0x100
-#define MODE_REPLACE    (REPLACE_FLAG | MODE_INSERT)
-#define VREPLACE_FLAG   0x200
-#define MODE_VREPLACE   (REPLACE_FLAG | VREPLACE_FLAG | MODE_INSERT)
-#define MODE_LREPLACE   (REPLACE_FLAG | MODE_LANGMAP)
-
-#define MODE_NORMAL_BUSY (0x1000 | MODE_NORMAL)
-#define MODE_HITRETURN  (0x2000 | MODE_NORMAL)
-#define MODE_ASKMORE    0x3000
-#define MODE_SETWSIZE   0x4000
-#define MODE_EXTERNCMD  0x5000
-#define MODE_SHOWMATCH  (0x6000 | MODE_INSERT)
-#define MODE_CONFIRM    0x7000
-#define MODE_ALL        0xffff
-
-#define MODE_MAX_LENGTH 4
-
-#define FORWARD                 1
-#define BACKWARD                (-1)
-#define FORWARD_FILE            3
-#define BACKWARD_FILE           (-3)
-
-# define OK                     1
-#define FAIL                    0
-#define NOTDONE                 2
-
-#define BF_RECOVERED    0x01
-#define BF_CHECK_RO     0x02
-#define BF_NEVERLOADED  0x04
-#define BF_NOTEDITED    0x08
-#define BF_NEW          0x10
-#define BF_NEW_W        0x20
-#define BF_READERR      0x40
-#define BF_DUMMY        0x80
-#define BF_PRESERVED    0x100
-#define BF_SYN_SET      0x200
-#define BF_NO_SEA       0x400
-
-#define BF_WRITE_MASK   (BF_NOTEDITED + BF_NEW + BF_READERR)
-
-#define EXPAND_UNSUCCESSFUL     (-2)
-#define EXPAND_OK               (-1)
-#define EXPAND_NOTHING          0
-#define EXPAND_COMMANDS         1
-#define EXPAND_FILES            2
-#define EXPAND_DIRECTORIES      3
-#define EXPAND_SETTINGS         4
-#define EXPAND_BOOL_SETTINGS    5
-#define EXPAND_TAGS             6
-#define EXPAND_OLD_SETTING      7
-#define EXPAND_HELP             8
-#define EXPAND_BUFFERS          9
-#define EXPAND_EVENTS           10
-#define EXPAND_MENUS            11
-#define EXPAND_SYNTAX           12
-#define EXPAND_HIGHLIGHT        13
-#define EXPAND_AUGROUP          14
-#define EXPAND_USER_VARS        15
-#define EXPAND_MAPPINGS         16
-#define EXPAND_TAGS_LISTFILES   17
-#define EXPAND_FUNCTIONS        18
-#define EXPAND_USER_FUNC        19
-#define EXPAND_EXPRESSION       20
-#define EXPAND_MENUNAMES        21
-#define EXPAND_USER_COMMANDS    22
-#define EXPAND_USER_CMD_FLAGS   23
-#define EXPAND_USER_NARGS       24
-#define EXPAND_USER_COMPLETE    25
-#define EXPAND_ENV_VARS         26
-#define EXPAND_LANGUAGE         27
-#define EXPAND_COLORS           28
-#define EXPAND_COMPILER         29
-#define EXPAND_USER_DEFINED     30
-#define EXPAND_USER_LIST        31
-#define EXPAND_SHELLCMD         32
-#define EXPAND_CSCOPE           33
-#define EXPAND_SIGN             34
-#define EXPAND_PROFILE          35
-#define EXPAND_BEHAVE           36
-#define EXPAND_FILETYPE         37
-#define EXPAND_FILES_IN_PATH    38
-#define EXPAND_OWNSYNTAX        39
-#define EXPAND_LOCALES          40
-#define EXPAND_HISTORY          41
-#define EXPAND_USER             42
-#define EXPAND_SYNTIME          43
-#define EXPAND_USER_ADDR_TYPE   44
-#define EXPAND_PACKADD          45
-#define EXPAND_MESSAGES         46
-#define EXPAND_MAPCLEAR         47
-#define EXPAND_ARGLIST          48
-#define EXPAND_DIFF_BUFFERS     49
-#define EXPAND_DISASSEMBLE      50
-#define EXPAND_BREAKPOINT       51
-#define EXPAND_SCRIPTNAMES      52
-#define EXPAND_RUNTIME          53
-#define EXPAND_STRING_SETTING   54
-#define EXPAND_SETTING_SUBTRACT 55
-#define EXPAND_ARGOPT           56
-#define EXPAND_TERMINALOPT      57
-#define EXPAND_KEYMAP           58
-#define EXPAND_DIRS_IN_CDPATH   59
-#define EXPAND_SHELLCMDLINE     60
-#define EXPAND_FINDFUNC         61
-#define EXPAND_HIGHLIGHT_GROUP  62
-#define EXPAND_FILETYPECMD      63
-#define EXPAND_PATTERN_IN_BUF   64
-#define EXPAND_RETAB            65
-#define EXPAND_USER_COMPLETEOPT 66
-
-#define EXMODE_NORMAL           1
-#define EXMODE_VIM              2
-
-#define WILD_FREE               1
-#define WILD_EXPAND_FREE        2
-#define WILD_EXPAND_KEEP        3
-#define WILD_NEXT               4
-#define WILD_PREV               5
-#define WILD_ALL                6
-#define WILD_LONGEST            7
-#define WILD_ALL_KEEP           8
-#define WILD_CANCEL             9
-#define WILD_APPLY              10
-#define WILD_PAGEUP             11
-#define WILD_PAGEDOWN           12
-
-#define WILD_LIST_NOTFOUND          0x01
-#define WILD_HOME_REPLACE           0x02
-#define WILD_USE_NL                 0x04
-#define WILD_NO_BEEP                0x08
-#define WILD_ADD_SLASH              0x10
-#define WILD_KEEP_ALL               0x20
-#define WILD_SILENT                 0x40
-#define WILD_ESCAPE                 0x80
-#define WILD_ICASE                  0x100
-#define WILD_ALLLINKS               0x200
-#define WILD_IGNORE_COMPLETESLASH   0x400
-#define WILD_NOERROR                0x800
-#define WILD_BUFLASTUSED            0x1000
-#define BUF_DIFF_FILTER             0x2000
-#define WILD_NOSELECT               0x4000
-#define WILD_MAY_EXPAND_PATTERN     0x8000
-#define WILD_FUNC_TRIGGER           0x10000
-#define WILD_NOINSERT               0x20000
-
-#define EW_DIR          0x01
-#define EW_FILE         0x02
-#define EW_NOTFOUND     0x04
-#define EW_ADDSLASH     0x08
-#define EW_KEEPALL      0x10
-#define EW_SILENT       0x20
-#define EW_EXEC         0x40
-#define EW_PATH         0x80
-#define EW_ICASE        0x100
-#define EW_NOERROR      0x200
-#define EW_NOTWILD      0x400
-#define EW_KEEPDOLLAR   0x800
-#define EW_ALLLINKS     0x1000
-#define EW_SHELLCMD     0x2000
-#define EW_DODOT        0x4000
-#define EW_EMPTYOK      0x8000
-#define EW_NOTENV       0x10000
-#define EW_CDPATH       0x20000
-
-#define FINDFILE_FILE   0
-#define FINDFILE_DIR    1
-#define FINDFILE_BOTH   2
-
-# define COLUMNS_WITHOUT_TPL()          Columns
-# define TPL_LCOL()                     0
-
-#define W_ENDCOL(wp)    ((wp)->w_wincol + (wp)->w_width)
-# define W_WINROW(wp)   (wp->w_winrow)
-
-#define FIND_ANY        1
-#define FIND_DEFINE     2
-#define CHECK_PATH      3
-
-#define ACTION_SHOW     1
-#define ACTION_GOTO     2
-#define ACTION_SPLIT    3
-#define ACTION_SHOW_ALL 4
-#define ACTION_EXPAND   5
-
-#define SEARCH_REV    0x01
-#define SEARCH_ECHO   0x02
-#define SEARCH_MSG    0x0c
-#define SEARCH_NFMSG  0x08
-#define SEARCH_OPT    0x10
-#define SEARCH_HIS    0x20
-#define SEARCH_END    0x40
-#define SEARCH_NOOF   0x80
-#define SEARCH_START 0x100
-#define SEARCH_MARK  0x200
-#define SEARCH_KEEP  0x400
-#define SEARCH_PEEK  0x800
-#define SEARCH_COL  0x1000
-
-#define FIND_IDENT      1
-#define FIND_STRING     2
-#define FIND_EVAL       4
-#define FIND_NOERROR    8
-
-#define FNAME_MESS      1
-#define FNAME_EXP       2
-#define FNAME_HYP       4
-#define FNAME_INCL      8
-#define FNAME_REL       16
-#define FNAME_UNESC     32
-
-#define GETF_SETMARK    0x01
-#define GETF_ALT        0x02
-#define GETF_SWITCH     0x04
-
-#define GETFILE_ERROR       1
-#define GETFILE_NOT_WRITTEN 2
-#define GETFILE_SAME_FILE   0
-#define GETFILE_OPEN_OTHER (-1)
-#define GETFILE_UNUSED      8
-#define GETFILE_SUCCESS(x)  ((x) <= 0)
-
-#define BLN_CURBUF      1
-#define BLN_LISTED      2
-#define BLN_DUMMY       4
-#define BLN_NEW         8
-#define BLN_NOOPT       16
-#define BLN_DUMMY_OK    32
-#define BLN_REUSE       64
-#define BLN_NOCURWIN    128
-
-#define KEY_OPEN_FORW   0x101
-#define KEY_OPEN_BACK   0x102
-#define KEY_COMPLETE    0x103
-
-#define MAPTYPE_MAP             0
-#define MAPTYPE_UNMAP           1
-#define MAPTYPE_NOREMAP         2
-#define MAPTYPE_UNMAP_LHS       3
-
-#define REMAP_YES       0
-#define REMAP_NONE      (-1)
-#define REMAP_SCRIPT    (-2)
-#define REMAP_SKIP      (-3)
-
-#define SHELL_FILTER    1
-#define SHELL_EXPAND    2
-#define SHELL_COOKED    4
-#define SHELL_DOOUT     8
-#define SHELL_SILENT    16
-#define SHELL_READ      32
-#define SHELL_WRITE     64
-
-#define NODE_NORMAL     0
-#define NODE_WRITABLE   1
-#define NODE_OTHER      2
-
-#define READ_NEW        0x01
-#define READ_FILTER     0x02
-#define READ_STDIN      0x04
-#define READ_BUFFER     0x08
-#define READ_DUMMY      0x10
-#define READ_KEEP_UNDO  0x20
-#define READ_FIFO       0x40
-#define READ_NOWINENTER 0x80
-#define READ_NOFILE     0x100
-
-#define INDENT_SET      1
-#define INDENT_INC      2
-#define INDENT_DEC      3
-
-#define FM_BACKWARD     0x01
-#define FM_FORWARD      0x02
-#define FM_BLOCKSTOP    0x04
-#define FM_SKIPCOMM     0x08
-
-#define DOBUF_GOTO      0
-#define DOBUF_SPLIT     1
-#define DOBUF_UNLOAD    2
-#define DOBUF_DEL       3
-#define DOBUF_WIPE      4
-#define DOBUF_WIPE_REUSE 5
-
-#define DOBUF_CURRENT   0
-#define DOBUF_FIRST     1
-#define DOBUF_LAST      2
-#define DOBUF_MOD       3
-
-#define DOBUF_FORCEIT   1
-#define DOBUF_NOPOPUP   2
-#define DOBUF_SKIPHELP  4
-
-#define RE_SEARCH       0
-#define RE_SUBST        1
-#define RE_BOTH         2
-#define RE_LAST         2
-
-#define RE_MAGIC        1
-#define RE_STRING       2
-#define RE_STRICT       4
-#define RE_AUTO         8
-
-#define FPC_SAME        1
-#define FPC_DIFF        2
-#define FPC_NOTX        4
-#define FPC_DIFFX       6
-#define FPC_SAMEX       7
-
-#define ECMD_HIDE       0x01
-#define ECMD_SET_HELP   0x02
-#define ECMD_OLDBUF     0x04
-#define ECMD_FORCEIT    0x08
-#define ECMD_ADDBUF     0x10
-#define ECMD_ALTBUF     0x20
-#define ECMD_NOWINENTER 0x40
-
-#define ECMD_LASTL      (linenr_T)0
-#define ECMD_LAST       ((linenr_T)-1)
-#define ECMD_ONE        (linenr_T)1
-
-#define DOCMD_VERBOSE   0x01
-#define DOCMD_NOWAIT    0x02
-#define DOCMD_REPEAT    0x04
-#define DOCMD_KEYTYPED  0x08
-#define DOCMD_EXCRESET  0x10
-#define DOCMD_KEEPLINE  0x20
-#define DOCMD_RANGEOK   0x40
-
-#define BL_WHITE        1
-#define BL_SOL          2
-#define BL_FIX          4
-
-#define MFS_ALL         1
-#define MFS_STOP        2
-#define MFS_FLUSH       4
-#define MFS_ZERO        8
-
-#define BCO_ENTER       1
-#define BCO_ALWAYS      2
-#define BCO_NOHELP      4
-
-#define PUT_FIXINDENT   1
-#define PUT_CURSEND     2
-#define PUT_CURSLINE    4
-#define PUT_LINE        8
-#define PUT_LINE_SPLIT  16
-#define PUT_LINE_FORWARD 32
-#define PUT_BLOCK_INNER 64
-
-#define SIN_CHANGED     1
-#define SIN_INSERT      2
-#define SIN_UNDO        4
-
-#define INSCHAR_FORMAT  1
-#define INSCHAR_DO_COM  2
-#define INSCHAR_CTRLV   4
-#define INSCHAR_NO_FEX  8
-#define INSCHAR_COM_LIST 16
-
-#define OPENLINE_DELSPACES      0x01
-#define OPENLINE_DO_COM         0x02
-#define OPENLINE_KEEPTRAIL      0x04
-#define OPENLINE_MARKFIX        0x08
-#define OPENLINE_COM_LIST       0x10
-#define OPENLINE_FORMAT         0x20
-#define OPENLINE_FORCE_INDENT   0x40
-
-#define HIST_CMD        0
-#define HIST_SEARCH     1
-#define HIST_EXPR       2
-#define HIST_INPUT      3
-#define HIST_DEBUG      4
-#define HIST_COUNT      5
-
-#define BARTYPE_VERSION 1
-#define BARTYPE_HISTORY 2
-#define BARTYPE_REGISTER 3
-#define BARTYPE_MARK 4
-
-#define VIMINFO_VERSION 4
-#define VIMINFO_VERSION_WITH_HISTORY 2
-#define VIMINFO_VERSION_WITH_REGISTERS 3
-#define VIMINFO_VERSION_WITH_MARKS 4
-
-#define DT_TAG          1
-#define DT_POP          2
-#define DT_NEXT         3
-#define DT_PREV         4
-#define DT_FIRST        5
-#define DT_LAST         6
-#define DT_SELECT       7
-#define DT_HELP         8
-#define DT_JUMP         9
-#define DT_CSCOPE       10
-#define DT_LTAG         11
-#define DT_FREE         99
-
-#define TAG_HELP        1
-#define TAG_NAMES       2
-#define TAG_REGEXP      4
-#define TAG_NOIC        8
-#define TAG_VERBOSE     32
-#define TAG_INS_COMP    64
-#define TAG_KEEP_LANG   128
-#define TAG_NO_TAGFUNC  256
-
-#define TAG_MANY        300
-
-#define VIM_GENERIC     0
-#define VIM_ERROR       1
-#define VIM_WARNING     2
-#define VIM_INFO        3
-#define VIM_QUESTION    4
-#define VIM_LAST_TYPE   4
-
-#define VIM_YES         2
-#define VIM_NO          3
-#define VIM_CANCEL      4
-#define VIM_ALL         5
-#define VIM_DISCARDALL  6
-
-#define WSP_ROOM        0x01
-#define WSP_VERT        0x02
-#define WSP_HOR         0x04
-#define WSP_TOP         0x08
-#define WSP_BOT         0x10
-#define WSP_HELP        0x20
-#define WSP_BELOW       0x40
-#define WSP_ABOVE       0x80
-#define WSP_NEWLOC      0x100
-#define WSP_FORCE_ROOM  0x200
-#define WSP_QUICKFIX    0x400
-
-#define RESIZE_VERT     1
-#define RESIZE_HOR      2
-#define RESIZE_BOTH     15
-
-#define CCGD_AW         1
-#define CCGD_MULTWIN    2
-#define CCGD_FORCEIT    4
-#define CCGD_ALLBUF     8
-#define CCGD_EXCMD      16
-
-#define OPT_FREE        0x01
-#define OPT_GLOBAL      0x02
-#define OPT_LOCAL       0x04
-#define OPT_MODELINE    0x08
-#define OPT_WINONLY     0x10
-#define OPT_NOWIN       0x20
-#define OPT_ONECOLUMN   0x40
-#define OPT_NO_REDRAW   0x80
-#define OPT_SKIPRTP     0x100
-
-#define DLG_BUTTON_SEP  '\n'
-#define DLG_HOTKEY_CHAR '&'
-
-#define NO_SCREEN       2
-#define NO_BUFFERS      1
-
-#define SEA_NONE        0
-#define SEA_DIALOG      1
-#define SEA_QUIT        2
-#define SEA_RECOVER     3
-#define SEA_READONLY    4
-
-#define MIN_SWAP_PAGE_SIZE 1048
-#define MAX_SWAP_PAGE_SIZE 50000
-
-#define SID_MODELINE    (-1)
-#define SID_CMDARG      (-2)
-#define SID_CARG        (-3)
-#define SID_ENV         (-4)
-#define SID_ERROR       (-5)
-#define SID_NONE        (-6)
-#define SID_WINLAYOUT   (-7)
+// _() and NGETTEXT keep their names as functions rather than being expanded.
+// format_arg(1) is how glibc declares gettext: it tells gcc the return value is
+// the argument's format string, so -Wformat, -Wformat-security and
+// -Wformat-nonliteral still see through the call.  Without it this conversion
+// silently loses those diagnostics, which is why the check is that the warning
+// set is unchanged, not that it builds.
+static inline __attribute__((format_arg(1))) char *_(const char *x)
+{
+    return (char *)x;
+}
+
+static inline __attribute__((format_arg(1))) __attribute__((format_arg(2)))
+char *NGETTEXT(const char *x, const char *xs, unsigned long n)
+{
+    return (char *)((n) == 1 ? x : xs);
+}
+
+enum { UPD_VALID_NO_UPDATE = 5 };
+enum { UPD_VALID = 10 };
+enum { UPD_INVERTED = 20 };
+enum { UPD_INVERTED_ALL = 25 };
+enum { UPD_REDRAW_TOP = 30 };
+enum { UPD_SOME_VALID = 35 };
+enum { UPD_NOT_VALID = 40 };
+enum { UPD_CLEAR = 50 };
+
+enum { SLF_INC_VCOL = 4 };
+
+enum { MB_FILLER_CHAR = '<' };
+
+enum { VALID_WROW = 0x01 };
+enum { VALID_WCOL = 0x02 };
+enum { VALID_VIRTCOL = 0x04 };
+enum { VALID_CHEIGHT = 0x08 };
+enum { VALID_CROW = 0x10 };
+enum { VALID_BOTLINE = 0x20 };
+enum { VALID_BOTLINE_AP = 0x40 };
+enum { VALID_TOPLINE = 0x80 };
+
+enum { HL_NORMAL = 0x00 };
+enum { HL_INVERSE = 0x01 };
+enum { HL_BOLD = 0x02 };
+enum { HL_ITALIC = 0x04 };
+enum { HL_UNDERLINE = 0x08 };
+enum { HL_UNDERCURL = 0x10 };
+enum { HL_UNDERDOUBLE = 0x20 };
+enum { HL_UNDERDOTTED = 0x40 };
+enum { HL_UNDERDASHED = 0x80 };
+enum { HL_STANDOUT = 0x100 };
+enum { HL_NOCOMBINE = 0x200 };
+enum { HL_STRIKETHROUGH = 0x400 };
+enum { HL_ALL = 0x7ff };
+
+enum { MSG_HIST = 0x1000 };
+
+enum { MODE_NORMAL = 0x01 };
+enum { MODE_VISUAL = 0x02 };
+enum { MODE_OP_PENDING = 0x04 };
+enum { MODE_CMDLINE = 0x08 };
+enum { MODE_INSERT = 0x10 };
+enum { MODE_LANGMAP = 0x20 };
+enum { MODE_SELECT = 0x40 };
+enum { MODE_TERMINAL = 0x80 };
+
+enum { MAP_ALL_MODES = 0xff };
+
+enum { REPLACE_FLAG = 0x100 };
+enum { VREPLACE_FLAG = 0x200 };
+
+enum { MODE_ASKMORE = 0x3000 };
+enum { MODE_SETWSIZE = 0x4000 };
+enum { MODE_EXTERNCMD = 0x5000 };
+enum { MODE_CONFIRM = 0x7000 };
+
+enum { FORWARD = 1 };
+
+enum { OK = 1 };
+enum { FAIL = 0 };
+enum { NOTDONE = 2 };
+
+enum { BF_RECOVERED = 0x01 };
+enum { BF_CHECK_RO = 0x02 };
+enum { BF_NEVERLOADED = 0x04 };
+enum { BF_NOTEDITED = 0x08 };
+enum { BF_NEW = 0x10 };
+enum { BF_NEW_W = 0x20 };
+enum { BF_READERR = 0x40 };
+enum { BF_DUMMY = 0x80 };
+enum { BF_PRESERVED = 0x100 };
+enum { BF_NO_SEA = 0x400 };
+
+enum { EXPAND_NOTHING = 0 };
+enum { EXPAND_COMMANDS = 1 };
+enum { EXPAND_FILES = 2 };
+enum { EXPAND_DIRECTORIES = 3 };
+enum { EXPAND_SETTINGS = 4 };
+enum { EXPAND_BOOL_SETTINGS = 5 };
+enum { EXPAND_TAGS = 6 };
+enum { EXPAND_OLD_SETTING = 7 };
+enum { EXPAND_HELP = 8 };
+enum { EXPAND_BUFFERS = 9 };
+enum { EXPAND_EVENTS = 10 };
+enum { EXPAND_MENUS = 11 };
+enum { EXPAND_HIGHLIGHT = 13 };
+enum { EXPAND_AUGROUP = 14 };
+enum { EXPAND_USER_VARS = 15 };
+enum { EXPAND_MAPPINGS = 16 };
+enum { EXPAND_TAGS_LISTFILES = 17 };
+enum { EXPAND_FUNCTIONS = 18 };
+enum { EXPAND_USER_FUNC = 19 };
+enum { EXPAND_EXPRESSION = 20 };
+enum { EXPAND_MENUNAMES = 21 };
+enum { EXPAND_USER_COMMANDS = 22 };
+enum { EXPAND_USER_CMD_FLAGS = 23 };
+enum { EXPAND_USER_NARGS = 24 };
+enum { EXPAND_USER_COMPLETE = 25 };
+enum { EXPAND_ENV_VARS = 26 };
+enum { EXPAND_LANGUAGE = 27 };
+enum { EXPAND_COLORS = 28 };
+enum { EXPAND_COMPILER = 29 };
+enum { EXPAND_USER_DEFINED = 30 };
+enum { EXPAND_USER_LIST = 31 };
+enum { EXPAND_SHELLCMD = 32 };
+enum { EXPAND_BEHAVE = 36 };
+enum { EXPAND_FILETYPE = 37 };
+enum { EXPAND_FILES_IN_PATH = 38 };
+enum { EXPAND_OWNSYNTAX = 39 };
+enum { EXPAND_LOCALES = 40 };
+enum { EXPAND_HISTORY = 41 };
+enum { EXPAND_USER = 42 };
+enum { EXPAND_USER_ADDR_TYPE = 44 };
+enum { EXPAND_PACKADD = 45 };
+enum { EXPAND_MESSAGES = 46 };
+enum { EXPAND_MAPCLEAR = 47 };
+enum { EXPAND_ARGLIST = 48 };
+enum { EXPAND_DIFF_BUFFERS = 49 };
+enum { EXPAND_DISASSEMBLE = 50 };
+enum { EXPAND_SCRIPTNAMES = 52 };
+enum { EXPAND_RUNTIME = 53 };
+enum { EXPAND_STRING_SETTING = 54 };
+enum { EXPAND_SETTING_SUBTRACT = 55 };
+enum { EXPAND_ARGOPT = 56 };
+enum { EXPAND_TERMINALOPT = 57 };
+enum { EXPAND_KEYMAP = 58 };
+enum { EXPAND_DIRS_IN_CDPATH = 59 };
+enum { EXPAND_SHELLCMDLINE = 60 };
+enum { EXPAND_FINDFUNC = 61 };
+enum { EXPAND_HIGHLIGHT_GROUP = 62 };
+enum { EXPAND_FILETYPECMD = 63 };
+enum { EXPAND_PATTERN_IN_BUF = 64 };
+enum { EXPAND_RETAB = 65 };
+enum { EXPAND_USER_COMPLETEOPT = 66 };
+
+enum { EXMODE_NORMAL = 1 };
+enum { EXMODE_VIM = 2 };
+
+enum { WILD_FREE = 1 };
+enum { WILD_EXPAND_FREE = 2 };
+enum { WILD_EXPAND_KEEP = 3 };
+enum { WILD_NEXT = 4 };
+enum { WILD_PREV = 5 };
+enum { WILD_ALL = 6 };
+enum { WILD_LONGEST = 7 };
+enum { WILD_ALL_KEEP = 8 };
+enum { WILD_CANCEL = 9 };
+enum { WILD_APPLY = 10 };
+enum { WILD_PAGEUP = 11 };
+enum { WILD_PAGEDOWN = 12 };
+
+enum { WILD_LIST_NOTFOUND = 0x01 };
+enum { WILD_HOME_REPLACE = 0x02 };
+enum { WILD_USE_NL = 0x04 };
+enum { WILD_NO_BEEP = 0x08 };
+enum { WILD_ADD_SLASH = 0x10 };
+enum { WILD_KEEP_ALL = 0x20 };
+enum { WILD_SILENT = 0x40 };
+enum { WILD_ESCAPE = 0x80 };
+enum { WILD_ICASE = 0x100 };
+enum { WILD_ALLLINKS = 0x200 };
+enum { WILD_NOERROR = 0x800 };
+enum { WILD_BUFLASTUSED = 0x1000 };
+enum { WILD_NOSELECT = 0x4000 };
+enum { WILD_MAY_EXPAND_PATTERN = 0x8000 };
+enum { WILD_FUNC_TRIGGER = 0x10000 };
+enum { WILD_NOINSERT = 0x20000 };
+
+enum { EW_DIR = 0x01 };
+enum { EW_FILE = 0x02 };
+enum { EW_NOTFOUND = 0x04 };
+enum { EW_ADDSLASH = 0x08 };
+enum { EW_KEEPALL = 0x10 };
+enum { EW_SILENT = 0x20 };
+enum { EW_EXEC = 0x40 };
+enum { EW_PATH = 0x80 };
+enum { EW_ICASE = 0x100 };
+enum { EW_NOERROR = 0x200 };
+enum { EW_NOTWILD = 0x400 };
+enum { EW_KEEPDOLLAR = 0x800 };
+enum { EW_ALLLINKS = 0x1000 };
+enum { EW_SHELLCMD = 0x2000 };
+enum { EW_DODOT = 0x4000 };
+enum { EW_EMPTYOK = 0x8000 };
+enum { EW_NOTENV = 0x10000 };
+enum { EW_CDPATH = 0x20000 };
+
+enum { FINDFILE_FILE = 0 };
+enum { FINDFILE_DIR = 1 };
+enum { FINDFILE_BOTH = 2 };
+
+enum { SEARCH_REV = 0x01 };
+enum { SEARCH_ECHO = 0x02 };
+enum { SEARCH_MSG = 0x0c };
+enum { SEARCH_NFMSG = 0x08 };
+enum { SEARCH_OPT = 0x10 };
+enum { SEARCH_HIS = 0x20 };
+enum { SEARCH_END = 0x40 };
+enum { SEARCH_NOOF = 0x80 };
+enum { SEARCH_START = 0x100 };
+enum { SEARCH_MARK = 0x200 };
+enum { SEARCH_KEEP = 0x400 };
+enum { SEARCH_PEEK = 0x800 };
+enum { SEARCH_COL = 0x1000 };
+
+enum { FIND_IDENT = 1 };
+enum { FIND_STRING = 2 };
+enum { FIND_EVAL = 4 };
+enum { FIND_NOERROR = 8 };
+
+enum { FNAME_MESS = 1 };
+enum { FNAME_EXP = 2 };
+enum { FNAME_HYP = 4 };
+enum { FNAME_REL = 16 };
+enum { FNAME_UNESC = 32 };
+
+enum { GETF_SETMARK = 0x01 };
+enum { GETF_ALT = 0x02 };
+enum { GETF_SWITCH = 0x04 };
+
+enum { GETFILE_ERROR = 1 };
+enum { GETFILE_NOT_WRITTEN = 2 };
+enum { GETFILE_SAME_FILE = 0 };
+enum { GETFILE_UNUSED = 8 };
+
+enum { BLN_CURBUF = 1 };
+enum { BLN_LISTED = 2 };
+enum { BLN_DUMMY = 4 };
+enum { BLN_NEW = 8 };
+enum { BLN_NOOPT = 16 };
+enum { BLN_REUSE = 64 };
+enum { BLN_NOCURWIN = 128 };
+
+enum { KEY_OPEN_FORW = 0x101 };
+enum { KEY_OPEN_BACK = 0x102 };
+enum { KEY_COMPLETE = 0x103 };
+
+enum { MAPTYPE_MAP = 0 };
+enum { MAPTYPE_UNMAP = 1 };
+enum { MAPTYPE_NOREMAP = 2 };
+enum { MAPTYPE_UNMAP_LHS = 3 };
+
+enum { REMAP_YES = 0 };
+
+enum { SHELL_FILTER = 1 };
+enum { SHELL_EXPAND = 2 };
+enum { SHELL_COOKED = 4 };
+enum { SHELL_DOOUT = 8 };
+enum { SHELL_SILENT = 16 };
+enum { SHELL_READ = 32 };
+enum { SHELL_WRITE = 64 };
+
+enum { NODE_NORMAL = 0 };
+enum { NODE_WRITABLE = 1 };
+enum { NODE_OTHER = 2 };
+
+enum { READ_NEW = 0x01 };
+enum { READ_FILTER = 0x02 };
+enum { READ_STDIN = 0x04 };
+enum { READ_BUFFER = 0x08 };
+enum { READ_DUMMY = 0x10 };
+enum { READ_KEEP_UNDO = 0x20 };
+enum { READ_FIFO = 0x40 };
+enum { READ_NOWINENTER = 0x80 };
+enum { READ_NOFILE = 0x100 };
+
+enum { INDENT_SET = 1 };
+enum { INDENT_INC = 2 };
+enum { INDENT_DEC = 3 };
+
+enum { FM_BACKWARD = 0x01 };
+enum { FM_FORWARD = 0x02 };
+enum { FM_BLOCKSTOP = 0x04 };
+enum { FM_SKIPCOMM = 0x08 };
+
+enum { DOBUF_GOTO = 0 };
+enum { DOBUF_SPLIT = 1 };
+enum { DOBUF_UNLOAD = 2 };
+enum { DOBUF_DEL = 3 };
+enum { DOBUF_WIPE = 4 };
+enum { DOBUF_WIPE_REUSE = 5 };
+
+enum { DOBUF_CURRENT = 0 };
+enum { DOBUF_FIRST = 1 };
+enum { DOBUF_LAST = 2 };
+enum { DOBUF_MOD = 3 };
+
+enum { DOBUF_FORCEIT = 1 };
+enum { DOBUF_NOPOPUP = 2 };
+enum { DOBUF_SKIPHELP = 4 };
+
+enum { RE_SEARCH = 0 };
+enum { RE_SUBST = 1 };
+enum { RE_BOTH = 2 };
+enum { RE_LAST = 2 };
+
+enum { RE_MAGIC = 1 };
+enum { RE_STRING = 2 };
+enum { RE_STRICT = 4 };
+enum { RE_AUTO = 8 };
+
+enum { FPC_SAME = 1 };
+enum { FPC_DIFF = 2 };
+enum { FPC_NOTX = 4 };
+enum { FPC_DIFFX = 6 };
+enum { FPC_SAMEX = 7 };
+
+enum { ECMD_HIDE = 0x01 };
+enum { ECMD_SET_HELP = 0x02 };
+enum { ECMD_OLDBUF = 0x04 };
+enum { ECMD_FORCEIT = 0x08 };
+enum { ECMD_ADDBUF = 0x10 };
+enum { ECMD_ALTBUF = 0x20 };
+enum { ECMD_NOWINENTER = 0x40 };
+
+enum { DOCMD_VERBOSE = 0x01 };
+enum { DOCMD_NOWAIT = 0x02 };
+enum { DOCMD_REPEAT = 0x04 };
+enum { DOCMD_KEYTYPED = 0x08 };
+enum { DOCMD_KEEPLINE = 0x20 };
+enum { DOCMD_RANGEOK = 0x40 };
+
+enum { BL_WHITE = 1 };
+enum { BL_SOL = 2 };
+enum { BL_FIX = 4 };
+
+enum { MFS_ALL = 1 };
+enum { MFS_STOP = 2 };
+enum { MFS_FLUSH = 4 };
+enum { MFS_ZERO = 8 };
+
+enum { BCO_ENTER = 1 };
+enum { BCO_ALWAYS = 2 };
+enum { BCO_NOHELP = 4 };
+
+enum { PUT_FIXINDENT = 1 };
+enum { PUT_CURSEND = 2 };
+enum { PUT_CURSLINE = 4 };
+enum { PUT_LINE = 8 };
+enum { PUT_LINE_SPLIT = 16 };
+enum { PUT_LINE_FORWARD = 32 };
+enum { PUT_BLOCK_INNER = 64 };
+
+enum { SIN_CHANGED = 1 };
+enum { SIN_INSERT = 2 };
+enum { SIN_UNDO = 4 };
+
+enum { INSCHAR_FORMAT = 1 };
+enum { INSCHAR_DO_COM = 2 };
+enum { INSCHAR_CTRLV = 4 };
+enum { INSCHAR_NO_FEX = 8 };
+enum { INSCHAR_COM_LIST = 16 };
+
+enum { OPENLINE_DELSPACES = 0x01 };
+enum { OPENLINE_DO_COM = 0x02 };
+enum { OPENLINE_KEEPTRAIL = 0x04 };
+enum { OPENLINE_MARKFIX = 0x08 };
+enum { OPENLINE_COM_LIST = 0x10 };
+enum { OPENLINE_FORMAT = 0x20 };
+enum { OPENLINE_FORCE_INDENT = 0x40 };
+
+enum { HIST_CMD = 0 };
+enum { HIST_SEARCH = 1 };
+enum { HIST_EXPR = 2 };
+enum { HIST_INPUT = 3 };
+enum { HIST_DEBUG = 4 };
+enum { HIST_COUNT = 5 };
+
+enum { DT_TAG = 1 };
+enum { DT_POP = 2 };
+enum { DT_NEXT = 3 };
+enum { DT_PREV = 4 };
+enum { DT_FIRST = 5 };
+enum { DT_LAST = 6 };
+enum { DT_SELECT = 7 };
+enum { DT_HELP = 8 };
+enum { DT_JUMP = 9 };
+enum { DT_CSCOPE = 10 };
+
+enum { TAG_HELP = 1 };
+enum { TAG_NAMES = 2 };
+enum { TAG_REGEXP = 4 };
+enum { TAG_NOIC = 8 };
+enum { TAG_VERBOSE = 32 };
+enum { TAG_INS_COMP = 64 };
+enum { TAG_KEEP_LANG = 128 };
+enum { TAG_NO_TAGFUNC = 256 };
+
+enum { TAG_MANY = 300 };
+
+enum { WSP_ROOM = 0x01 };
+enum { WSP_VERT = 0x02 };
+enum { WSP_HOR = 0x04 };
+enum { WSP_TOP = 0x08 };
+enum { WSP_BOT = 0x10 };
+enum { WSP_HELP = 0x20 };
+enum { WSP_BELOW = 0x40 };
+enum { WSP_ABOVE = 0x80 };
+enum { WSP_FORCE_ROOM = 0x200 };
+enum { WSP_QUICKFIX = 0x400 };
+
+enum { CCGD_AW = 1 };
+enum { CCGD_MULTWIN = 2 };
+enum { CCGD_FORCEIT = 4 };
+enum { CCGD_ALLBUF = 8 };
+enum { CCGD_EXCMD = 16 };
+
+enum { OPT_FREE = 0x01 };
+enum { OPT_GLOBAL = 0x02 };
+enum { OPT_LOCAL = 0x04 };
+enum { OPT_MODELINE = 0x08 };
+enum { OPT_WINONLY = 0x10 };
+enum { OPT_NOWIN = 0x20 };
+enum { OPT_ONECOLUMN = 0x40 };
+enum { OPT_NO_REDRAW = 0x80 };
+enum { OPT_SKIPRTP = 0x100 };
+
+enum { NO_SCREEN = 2 };
+enum { NO_BUFFERS = 1 };
+
+enum { SEA_NONE = 0 };
+enum { SEA_DIALOG = 1 };
+enum { SEA_QUIT = 2 };
+enum { SEA_RECOVER = 3 };
+enum { SEA_READONLY = 4 };
+
+enum { MIN_SWAP_PAGE_SIZE = 1048 };
+enum { MAX_SWAP_PAGE_SIZE = 50000 };
 
 enum auto_event
 {
@@ -1998,162 +1269,67 @@
     , HLF_COUNT
 } hlf_T;
 
-#define HL_FLAGS {'8', '~', '@', 'd', 'e', 'h', 'i', 'l', 'y', 'm', 'M',                  'n', 'a', 'b', 'N', 'G', 'O', 'r', 's', 'S', 'c', '|', 't', 'v', 'V',                   'w', 'W', 'f', 'F', 'A', 'C', 'D', 'T', 'E', '-', '>',                  'B', 'P', 'R', 'L',             '+', '=', 'k', '<','[', ']', '{', '}', 'x', 'X', 'j', 'H',              'p', 'J', 'Q',                  '*', '#', '_', '!', '.', 'o', 'q',              'z', 'Z', 'g',                  '%', '^', '&', 'I', '('}
-
-# define FALSE  0
-# define TRUE   1
+enum { FALSE = 0 };
+enum { TRUE = 1 };
 
-#define MAYBE   2
+enum { MAYBE = 2 };
 
-#define LOG_ALWAYS 9
+typedef  uint32_t  UINT32_T;
 
-# define MAY_WANT_TO_LOG_THIS
-
-typedef UINT32_TYPEDEF UINT32_T;
-
-#define OP_NOP          0
-#define OP_DELETE       1
-#define OP_YANK         2
-#define OP_CHANGE       3
-#define OP_LSHIFT       4
-#define OP_RSHIFT       5
-#define OP_FILTER       6
-#define OP_TILDE        7
-#define OP_INDENT       8
-#define OP_FORMAT       9
-#define OP_COLON        10
-#define OP_UPPER        11
-#define OP_LOWER        12
-#define OP_JOIN         13
-#define OP_JOIN_NS      14
-#define OP_ROT13        15
-#define OP_REPLACE      16
-#define OP_INSERT       17
-#define OP_APPEND       18
-#define OP_FOLD         19
-#define OP_FOLDOPEN     20
-#define OP_FOLDOPENREC  21
-#define OP_FOLDCLOSE    22
-#define OP_FOLDCLOSEREC 23
-#define OP_FOLDDEL      24
-#define OP_FOLDDELREC   25
-#define OP_FORMAT2      26
-#define OP_FUNCTION     27
-#define OP_NR_ADD       28
-#define OP_NR_SUB       29
-
-#define MCHAR   0
-#define MLINE   1
-#define MBLOCK  2
-
-#define MAUTO   0xff
-
-#define MIN_COLUMNS     12
-#define MIN_LINES       2
-#define MIN_CMDHEIGHT   1
-#define STATUS_HEIGHT   1
-# define WINBAR_HEIGHT(wp)      0
-# define VISIBLE_HEIGHT(wp)     (wp)->w_height
-#define QF_WINHEIGHT    10
-
-#define LSIZE       512
-
-#define IOSIZE     (1024+1)
-
-#define DIALOG_MSG_SIZE 1000
-
-#define MSG_BUF_LEN 480
-#define MSG_BUF_CLEN  (MSG_BUF_LEN / 6)
-
-#define FOLD_TEXT_LEN  51
-
-#define TBUFSZ 2048
-
-#define MAXMAPLEN   50
-
-#define MAX_FUNC_NAME_LEN   200
-
-#define UNDO_HASH_SIZE 32
-
-# define WRITEBIN   "w"
-# define READBIN    "r"
-# define APPENDBIN  "a"
-
-# define O_EXTRA    0
-
-#define ALLOC_ONE(type)  (type *)alloc(sizeof(type))
-#define ALLOC_ONE_ID(type, id)  (type *)alloc_id(sizeof(type), id)
-#define ALLOC_MULT(type, count)  (type *)alloc(sizeof(type) * (count))
-#define ALLOC_CLEAR_ONE(type)  (type *)alloc_clear(sizeof(type))
-#define ALLOC_CLEAR_ONE_ID(type, id)  (type *)alloc_clear_id(sizeof(type), id)
-#define ALLOC_CLEAR_MULT(type, count)  (type *)alloc_clear(sizeof(type) * (count))
-#define LALLOC_CLEAR_ONE(type)  (type *)lalloc_clear(sizeof(type), FALSE)
-#define LALLOC_CLEAR_MULT(type, count)  (type *)lalloc_clear(sizeof(type) * (count), FALSE)
-#define LALLOC_MULT(type, count)  (type *)lalloc(sizeof(type) * (count), FALSE)
-
-# define vim_memset(ptr, c, size)   memset((ptr), (c), (size))
-#define CLEAR_FIELD(field)  vim_memset(&(field), 0, sizeof(field))
-#define CLEAR_POINTER(ptr)  vim_memset((ptr), 0, sizeof(*(ptr)))
-
-#define STRLEN(s)           strlen((char *)(s))
-#define STRCPY(d, s)        strcpy((char *)(d), (char *)(s))
-#define STRNCPY(d, s, n)    strncpy((char *)(d), (char *)(s), (n))
-#define STRCMP(d, s)        strcmp((char *)(d), (char *)(s))
-#define STRNCMP(d, s, n)    strncmp((char *)(d), (char *)(s), (n))
-# define STRICMP(d, s)      strcasecmp((char *)(d), (char *)(s))
-# define STRCOLL(d, s)     strcoll((char *)(d), (char *)(s))
-
-#define STRMOVE(d, s)       mch_memmove((d), (s), STRLEN(s) + 1)
-
-# define STRNICMP(d, s, n)  strncasecmp((char *)(d), (char *)(s), (n))
-
-#define MB_STRICMP(d, s)        mb_strnicmp((char_u *)(d), (char_u *)(s), (int)MAXCOL)
-#define MB_STRNICMP(d, s, n)    mb_strnicmp((char_u *)(d), (char_u *)(s), (int)(n))
-#define MB_STRNICMP2(d, s, n1, n2)      mb_strnicmp2((char_u *)(d), (char_u *)(s), (n1), (n2))
-
-#define STRCAT(d, s)        strcat((char *)(d), (char *)(s))
-#define STRNCAT(d, s, n)    strncat((char *)(d), (char *)(s), (n))
+enum { OP_NOP = 0 };
+enum { OP_DELETE = 1 };
+enum { OP_YANK = 2 };
+enum { OP_CHANGE = 3 };
+enum { OP_LSHIFT = 4 };
+enum { OP_RSHIFT = 5 };
+enum { OP_FILTER = 6 };
+enum { OP_TILDE = 7 };
+enum { OP_INDENT = 8 };
+enum { OP_FORMAT = 9 };
+enum { OP_COLON = 10 };
+enum { OP_UPPER = 11 };
+enum { OP_LOWER = 12 };
+enum { OP_JOIN = 13 };
+enum { OP_JOIN_NS = 14 };
+enum { OP_ROT13 = 15 };
+enum { OP_REPLACE = 16 };
+enum { OP_INSERT = 17 };
+enum { OP_APPEND = 18 };
+enum { OP_FORMAT2 = 26 };
+enum { OP_FUNCTION = 27 };
+enum { OP_NR_ADD = 28 };
+enum { OP_NR_SUB = 29 };
+
+enum { MCHAR = 0 };
+enum { MLINE = 1 };
+enum { MBLOCK = 2 };
+
+enum { MAUTO = 0xff };
+
+enum { MIN_COLUMNS = 12 };
+enum { MIN_LINES = 2 };
+enum { MIN_CMDHEIGHT = 1 };
+enum { STATUS_HEIGHT = 1 };
 
-# define vim_strpbrk(s, cs) (char_u *)strpbrk((char *)(s), (char *)(cs))
+enum { LSIZE = 512 };
 
-#define OUT_STR(s)                  out_str((char_u *)(s))
-#define OUT_STR_NF(s)               out_str_nf((char_u *)(s))
+enum { MSG_BUF_LEN = 480 };
 
-# define IS_CTERM                   (t_colors > 1)
+enum { MAXMAPLEN = 50 };
 
-# define PERROR(msg)                (void)semsg("%s: %s", (char *)(msg), strerror(errno))
+enum { O_EXTRA = 0 };
 
 typedef long    linenr_T;
 typedef int     colnr_T;
-# define MAXCOL  0x7fffffffL
-# define MAXLNUM LONG_MAX
+enum { MAXCOL = 0x7fffffffL };
 
-#define SHOWCMD_COLS 10
+enum { SHOWCMD_COLS = 10 };
 
 typedef void        *vim_acl_T;
 
-#define fnamecmp(x, y) vim_fnamecmp((char_u *)(x), (char_u *)(y))
-#define fnamencmp(x, y, n) vim_fnamencmp((char_u *)(x), (char_u *)(y), (n))
-
-# define USE_INPUT_BUF
-
-# define vim_read(fd, buf, count)   read((fd), (char *)(buf), (count))
-# define vim_write(fd, buf, count)  write((fd), (char *)(buf), (count))
-
-#define HL_ATTR(n)      highlight_attr[(int)(n)]
-#define TERM_STR(n)     term_strings[(int)(n)]
+enum { MAX_MCO = 6 };
 
-#  define INIT(x) x
-#  define INIT2(a, b) = {a, b}
-#  define INIT3(a, b, c) = {a, b, c}
-#  define INIT4(a, b, c, d) = {a, b, c, d}
-#  define INIT5(a, b, c, d, e) = {a, b, c, d, e}
-#  define INIT6(a, b, c, d, e, f) = {a, b, c, d, e, f}
-#  define DO_INIT
-
-#define MAX_MCO 6
-
-#define MB_MAXBYTES     21
+enum { MB_MAXBYTES = 21 };
 
 typedef time_t      time_T;
 
@@ -2161,304 +1337,190 @@
 
 // ---------------- begin option.h ----------------
 
-#define _OPTION_H_
-
-#define P_BOOL          0x01
-#define P_NUM           0x02
-#define P_STRING        0x04
-#define P_ALLOCED       0x08
-#define P_EXPAND        0x10
-#define P_NO_CMD_EXPAND 0x20
-#define P_NODEFAULT     0x40
-#define P_DEF_ALLOCED   0x80
-#define P_WAS_SET       0x100
-#define P_NO_MKRC       0x200
-#define P_VI_DEF        0x400
-#define P_VIM           0x800
-
-#define P_RSTAT         0x1000
-#define P_RWIN          0x2000
-#define P_RBUF          0x4000
-#define P_RALL          0x6000
-#define P_RCLR          0x7000
-
-#define P_COMMA          0x8000
-#define P_ONECOMMA      0x18000L
-#define P_NODUP         0x20000L
-#define P_FLAGLIST      0x40000L
-
-#define P_SECURE        0x80000L
-#define P_GETTEXT      0x100000L
-#define P_NOGLOB       0x200000L
-#define P_NFNAME       0x400000L
-#define P_INSECURE     0x800000L
-#define P_PRI_MKRC    0x1000000L
-#define P_NO_ML       0x2000000L
-#define P_CURSWANT    0x4000000L
-#define P_NDNAME      0x8000000L
-#define P_HLONLY     0x10000000L
-#define P_MLE        0x20000000L
-#define P_FUNC       0x40000000L
-#define P_COLON      0x80000000L
-
-#    define DFLT_EFM    "%*[^\"]\"%f\"%*\\D%l: %m,\"%f\"%*\\D%l: %m,%-Gg%\\?make[%*\\d]: *** [%f:%l:%m,%-Gg%\\?make: *** [%f:%l:%m,%-G%f:%l: (Each undeclared identifier is reported only once,%-G%f:%l: for each function it appears in.),%-GIn file included from %f:%l:%c:,%-GIn file included from %f:%l:%c\\,,%-GIn file included from %f:%l:%c,%-GIn file included from %f:%l,%-G%*[ ]from %f:%l:%c,%-G%*[ ]from %f:%l:,%-G%*[ ]from %f:%l\\,,%-G%*[ ]from %f:%l,%f:%l:%c:%m,%f(%l):%m,%f:%l:%m,\"%f\"\\, line %l%*\\D%c%*[^ ] %m,%D%*\\a[%*\\d]: Entering directory %*[`']%f',%X%*\\a[%*\\d]: Leaving directory %*[`']%f',%D%*\\a: Entering directory %*[`']%f',%X%*\\a: Leaving directory %*[`']%f',%DMaking %*\\a in %f,%f|%l| %m"
-
-#define DFLT_GREPFORMAT "%f:%l:%m,%f:%l%m,%f  %l%m"
-
-#define FF_DOS          "dos"
-#define FF_MAC          "mac"
-#define FF_UNIX         "unix"
-
-# define DFLT_FF        "unix"
-# define DFLT_FFS_VIM   "unix,dos"
-#  define DFLT_FFS_VI   ""
-#  define DFLT_TEXTAUTO FALSE
-
-#define ENC_UCSBOM      "ucs-bom"
-
-# define ENC_DFLT       "latin1"
-
-#define EOL_UNKNOWN     (-1)
-#define EOL_UNIX        0
-#define EOL_DOS         1
-#define EOL_MAC         2
-
-#define FO_WRAP         't'
-#define FO_WRAP_COMS    'c'
-#define FO_RET_COMS     'r'
-#define FO_OPEN_COMS    'o'
-#define FO_NO_OPEN_COMS '/'
-#define FO_Q_COMS       'q'
-#define FO_Q_NUMBER     'n'
-#define FO_Q_SECOND     '2'
-#define FO_INS_VI       'v'
-#define FO_INS_LONG     'l'
-#define FO_INS_BLANK    'b'
-#define FO_MBYTE_BREAK  'm'
-#define FO_MBYTE_JOIN   'M'
-#define FO_MBYTE_JOIN2  'B'
-#define FO_ONE_LETTER   '1'
-#define FO_WHITE_PAR    'w'
-#define FO_AUTO         'a'
-#define FO_RIGOROUS_TW  ']'
-#define FO_REMOVE_COMS  'j'
-#define FO_PERIOD_ABBR  'p'
-
-#define DFLT_FO_VI      "vt"
-#define DFLT_FO_VIM     "tcq"
-#define FO_ALL          "tcro/q2vlb1mMBn,aw]jp"
-
-#define CPO_ALTREAD     'a'
-#define CPO_ALTWRITE    'A'
-#define CPO_BAR         'b'
-#define CPO_BSLASH      'B'
-#define CPO_SEARCH      'c'
-#define CPO_CONCAT      'C'
-#define CPO_DOTTAG      'd'
-#define CPO_DIGRAPH     'D'
-#define CPO_EXECBUF     'e'
-#define CPO_EMPTYREGION 'E'
-#define CPO_FNAMER      'f'
-#define CPO_FNAMEW      'F'
-#define CPO_GOTO1       'g'
-#define CPO_INSEND      'H'
-#define CPO_INTMOD      'i'
-#define CPO_INDENT      'I'
-#define CPO_JOINSP      'j'
-#define CPO_ENDOFSENT   'J'
-#define CPO_KEYCODE     'k'
-#define CPO_KOFFSET     'K'
-#define CPO_LITERAL     'l'
-#define CPO_LISTWM      'L'
-#define CPO_SHOWMATCH   'm'
-#define CPO_MATCHBSL    'M'
-#define CPO_NUMCOL      'n'
-#define CPO_LINEOFF     'o'
-#define CPO_OVERNEW     'O'
-#define CPO_LISP        'p'
-#define CPO_FNAMEAPP    'P'
-#define CPO_JOINCOL     'q'
-#define CPO_REDO        'r'
-#define CPO_REMMARK     'R'
-#define CPO_BUFOPT      's'
-#define CPO_BUFOPTGLOB  'S'
-#define CPO_TAGPAT      't'
-#define CPO_UNDO        'u'
-#define CPO_BACKSPACE   'v'
-#define CPO_CW          'w'
-#define CPO_FWRITE      'W'
-#define CPO_ESC         'x'
-#define CPO_REPLCNT     'X'
-#define CPO_YANK        'y'
-#define CPO_KEEPRO      'Z'
-#define CPO_WORD        'z'
-#define CPO_DOLLAR      '$'
-#define CPO_FILTER      '!'
-#define CPO_MATCH       '%'
-#define CPO_STAR        '*'
-#define CPO_PLUS        '+'
-#define CPO_MINUS       '-'
-#define CPO_SPECI       '<'
-#define CPO_REGAPPEND   '>'
-#define CPO_HASH        '#'
-#define CPO_PARA        '{'
-#define CPO_TSIZE       '|'
-#define CPO_PRESERVE    '&'
-#define CPO_SUBPERCENT  '/'
-#define CPO_BACKSL      '\\'
-#define CPO_CHDIR       '.'
-#define CPO_SCOLON      ';'
-#define CPO_NOSYMLINKS  '~'
-#define CPO_VIM         "aABceFsz"
-#define CPO_VI          "aAbBcCdDeEfFgHiIjJkKlLmMnoOpPqrRsStuvwWxXyZz$!%*-+<>;"
-#define CPO_ALL         "aAbBcCdDeEfFgHiIjJkKlLmMnoOpPqrRsStuvwWxXyZz$!%*-+<>#{|&/\\.;~"
-
-#define WW_ALL          "bshl<>[]~"
-
-#define MOUSE_NORMAL    'n'
-#define MOUSE_VISUAL    'v'
-#define MOUSE_INSERT    'i'
-#define MOUSE_COMMAND   'c'
-#define MOUSE_HELP      'h'
-#define MOUSE_RETURN    'r'
-#define MOUSE_A         "nvich"
-#define MOUSE_ALL       "anvichr"
-#define MOUSE_NONE      ' '
-#define MOUSE_NONEF     'x'
-
-#define COCU_ALL        "nvic"
-
-#define SHM_RO          'r'
-#define SHM_MOD         'm'
-#define SHM_FILE        'f'
-#define SHM_LAST        'i'
-#define SHM_TEXT        'x'
-#define SHM_LINES       'l'
-#define SHM_NEW         'n'
-#define SHM_WRI         'w'
-#define SHM_A           "rmfixlnw"
-#define SHM_WRITE       'W'
-#define SHM_TRUNC       't'
-#define SHM_TRUNCALL    'T'
-#define SHM_OVER        'o'
-#define SHM_OVERALL     'O'
-#define SHM_SEARCH      's'
-#define SHM_ATTENTION   'A'
-#define SHM_INTRO       'I'
-#define SHM_COMPLETIONMENU  'c'
-#define SHM_COMPLETIONSCAN  'C'
-#define SHM_RECORDING   'q'
-#define SHM_FILEINFO    'F'
-#define SHM_SEARCHCOUNT  'S'
-#define SHM_UNDO        'u'
-#define SHM_POSIX       "AS"
-#define SHM_ALL         "rmfixlnwaWtToOsAIcCqFSu"
-#define SHM_LEN         30
-
-#define GO_TERMINAL     '!'
-#define GO_ASEL         'a'
-#define GO_ASELML       'A'
-#define GO_BOT          'b'
-#define GO_CONDIALOG    'c'
-#define GO_TITLEBAR     'C'
-#define GO_DARKTHEME    'd'
-#define GO_TABLINE      'e'
-#define GO_FORG         'f'
-#define GO_GREY         'g'
-#define GO_HORSCROLL    'h'
-#define GO_ICON         'i'
-#define GO_LEFT         'l'
-#define GO_VLEFT        'L'
-#define GO_MENUS        'm'
-#define GO_NOSYSMENU    'M'
-#define GO_POINTER      'p'
-#define GO_ASELPLUS     'P'
-#define GO_RIGHT        'r'
-#define GO_VRIGHT       'R'
-#define GO_FULLSCREEN   's'
-#define GO_TEAROFF      't'
-#define GO_TOOLBAR      'T'
-#define GO_FOOTER       'F'
-#define GO_VERTICAL     'v'
-#define GO_KEEPWINSIZE  'k'
-#define GO_ALL          "!aAbcCdefFghilLmMpPrRstTvk"
-
-#define COM_NEST        'n'
-#define COM_BLANK       'b'
-#define COM_START       's'
-#define COM_MIDDLE      'm'
-#define COM_END         'e'
-#define COM_AUTO_END    'x'
-#define COM_FIRST       'f'
-#define COM_LEFT        'l'
-#define COM_RIGHT       'r'
-#define COM_NOBACK      'O'
-#define COM_ALL         "nbsmexflrO"
-#define COM_MAX_LEN     50
-
-#define STL_FILEPATH    'f'
-#define STL_FULLPATH    'F'
-#define STL_FILENAME    't'
-#define STL_COLUMN      'c'
-#define STL_VIRTCOL     'v'
-#define STL_VIRTCOL_ALT 'V'
-#define STL_LINE        'l'
-#define STL_NUMLINES    'L'
-#define STL_BUFNO       'n'
-#define STL_KEYMAP      'k'
-#define STL_OFFSET      'o'
-#define STL_OFFSET_X    'O'
-#define STL_BYTEVAL     'b'
-#define STL_BYTEVAL_X   'B'
-#define STL_ROFLAG      'r'
-#define STL_ROFLAG_ALT  'R'
-#define STL_HELPFLAG    'h'
-#define STL_HELPFLAG_ALT 'H'
-#define STL_FILETYPE    'y'
-#define STL_FILETYPE_ALT 'Y'
-#define STL_PREVIEWFLAG 'w'
-#define STL_PREVIEWFLAG_ALT 'W'
-#define STL_MODIFIED    'm'
-#define STL_MODIFIED_ALT 'M'
-#define STL_QUICKFIX    'q'
-#define STL_PERCENTAGE  'p'
-#define STL_ALTPERCENT  'P'
-#define STL_ARGLISTSTAT 'a'
-#define STL_PAGENUM     'N'
-#define STL_SHOWCMD     'S'
-#define STL_VIM_EXPR    '{'
-#define STL_SEPARATE    '='
-#define STL_TRUNCMARK   '<'
-#define STL_USER_HL     '*'
-#define STL_HIGHLIGHT   '#'
-#define STL_LINEBREAK   '@'
-#define STL_CLICKFUNC   '['
-#define STL_TABPAGENR   'T'
-#define STL_TABCLOSENR  'X'
-#define STL_ALL         ((char_u *) "fFtcvVlLknoObBrRhHmYyWwMqpPaNS{#@[")
-
-#define WIM_FULL        0x01
-#define WIM_LONGEST     0x02
-#define WIM_LIST        0x04
-#define WIM_BUFLASTUSED 0x08
-#define WIM_NOSELECT    0x10
-#define WIM_NOINSERT    0x20
-
-#define WOP_FUZZY       'z'
-#define WOP_TAGFILE     'g'
-#define WOP_PUM         'p'
-#define WOP_EXACTTEXT   'x'
-
-#define BS_INDENT       'i'
-#define BS_EOL          'l'
-#define BS_START        's'
-#define BS_NOSTOP       'p'
-
-#define CULOPT_LINE     0x01
-#define CULOPT_SCRLINE  0x02
-#define CULOPT_NBR      0x04
-
-#define LISPWORD_VALUE  "defun,define,defmacro,set!,lambda,if,case,let,flet,let*,letrec,do,do*,define-syntax,let-syntax,letrec-syntax,destructuring-bind,defpackage,defparameter,defstruct,deftype,defvar,do-all-symbols,do-external-symbols,do-symbols,dolist,dotimes,ecase,etypecase,eval-when,labels,macrolet,multiple-value-bind,multiple-value-call,multiple-value-prog1,multiple-value-setq,prog1,progv,typecase,unless,unwind-protect,when,with-input-from-string,with-open-file,with-open-stream,with-output-to-string,with-package-iterator,define-condition,handler-bind,handler-case,restart-bind,restart-case,with-simple-restart,store-value,use-value,muffle-warning,abort,continue,with-slots,with-slots*,with-accessors,with-accessors*,defclass,defmethod,print-unreadable-object"
+enum { P_BOOL = 0x01 };
+enum { P_NUM = 0x02 };
+enum { P_STRING = 0x04 };
+enum { P_ALLOCED = 0x08 };
+enum { P_EXPAND = 0x10 };
+enum { P_NO_CMD_EXPAND = 0x20 };
+enum { P_NODEFAULT = 0x40 };
+enum { P_DEF_ALLOCED = 0x80 };
+enum { P_WAS_SET = 0x100 };
+enum { P_NO_MKRC = 0x200 };
+enum { P_VI_DEF = 0x400 };
+enum { P_VIM = 0x800 };
+
+enum { P_RSTAT = 0x1000 };
+enum { P_RWIN = 0x2000 };
+enum { P_RBUF = 0x4000 };
+enum { P_RALL = 0x6000 };
+enum { P_RCLR = 0x7000 };
+
+enum { P_COMMA = 0x8000 };
+enum { P_ONECOMMA = 0x18000L };
+enum { P_NODUP = 0x20000L };
+enum { P_FLAGLIST = 0x40000L };
+
+enum { P_SECURE = 0x80000L };
+enum { P_GETTEXT = 0x100000L };
+enum { P_NOGLOB = 0x200000L };
+enum { P_NFNAME = 0x400000L };
+enum { P_INSECURE = 0x800000L };
+enum { P_PRI_MKRC = 0x1000000L };
+enum { P_NO_ML = 0x2000000L };
+enum { P_CURSWANT = 0x4000000L };
+enum { P_NDNAME = 0x8000000L };
+enum { P_HLONLY = 0x10000000L };
+enum { P_MLE = 0x20000000L };
+enum { P_FUNC = 0x40000000L };
+enum :
+    long { P_COLON = 0x80000000L };
+
+enum { EOL_UNIX = 0 };
+enum { EOL_DOS = 1 };
+enum { EOL_MAC = 2 };
+
+enum { FO_WRAP = 't' };
+enum { FO_WRAP_COMS = 'c' };
+enum { FO_RET_COMS = 'r' };
+enum { FO_OPEN_COMS = 'o' };
+enum { FO_NO_OPEN_COMS = '/' };
+enum { FO_Q_COMS = 'q' };
+enum { FO_Q_NUMBER = 'n' };
+enum { FO_Q_SECOND = '2' };
+enum { FO_INS_VI = 'v' };
+enum { FO_INS_LONG = 'l' };
+enum { FO_INS_BLANK = 'b' };
+enum { FO_MBYTE_BREAK = 'm' };
+enum { FO_MBYTE_JOIN = 'M' };
+enum { FO_MBYTE_JOIN2 = 'B' };
+enum { FO_ONE_LETTER = '1' };
+enum { FO_WHITE_PAR = 'w' };
+enum { FO_AUTO = 'a' };
+enum { FO_RIGOROUS_TW = ']' };
+enum { FO_REMOVE_COMS = 'j' };
+enum { FO_PERIOD_ABBR = 'p' };
+
+enum { CPO_ALTREAD = 'a' };
+enum { CPO_ALTWRITE = 'A' };
+enum { CPO_BAR = 'b' };
+enum { CPO_BSLASH = 'B' };
+enum { CPO_SEARCH = 'c' };
+enum { CPO_CONCAT = 'C' };
+enum { CPO_DOTTAG = 'd' };
+enum { CPO_EXECBUF = 'e' };
+enum { CPO_EMPTYREGION = 'E' };
+enum { CPO_FNAMER = 'f' };
+enum { CPO_FNAMEW = 'F' };
+enum { CPO_GOTO1 = 'g' };
+enum { CPO_INSEND = 'H' };
+enum { CPO_INTMOD = 'i' };
+enum { CPO_INDENT = 'I' };
+enum { CPO_JOINSP = 'j' };
+enum { CPO_ENDOFSENT = 'J' };
+enum { CPO_KEYCODE = 'k' };
+enum { CPO_KOFFSET = 'K' };
+enum { CPO_LITERAL = 'l' };
+enum { CPO_LISTWM = 'L' };
+enum { CPO_SHOWMATCH = 'm' };
+enum { CPO_MATCHBSL = 'M' };
+enum { CPO_NUMCOL = 'n' };
+enum { CPO_LINEOFF = 'o' };
+enum { CPO_OVERNEW = 'O' };
+enum { CPO_LISP = 'p' };
+enum { CPO_FNAMEAPP = 'P' };
+enum { CPO_JOINCOL = 'q' };
+enum { CPO_REDO = 'r' };
+enum { CPO_REMMARK = 'R' };
+enum { CPO_BUFOPT = 's' };
+enum { CPO_BUFOPTGLOB = 'S' };
+enum { CPO_TAGPAT = 't' };
+enum { CPO_UNDO = 'u' };
+enum { CPO_BACKSPACE = 'v' };
+enum { CPO_CW = 'w' };
+enum { CPO_FWRITE = 'W' };
+enum { CPO_ESC = 'x' };
+enum { CPO_REPLCNT = 'X' };
+enum { CPO_YANK = 'y' };
+enum { CPO_KEEPRO = 'Z' };
+enum { CPO_WORD = 'z' };
+enum { CPO_DOLLAR = '$' };
+enum { CPO_FILTER = '!' };
+enum { CPO_MATCH = '%' };
+enum { CPO_STAR = '*' };
+enum { CPO_PLUS = '+' };
+enum { CPO_MINUS = '-' };
+enum { CPO_SPECI = '<' };
+enum { CPO_REGAPPEND = '>' };
+enum { CPO_HASH = '#' };
+enum { CPO_PARA = '{' };
+enum { CPO_TSIZE = '|' };
+enum { CPO_PRESERVE = '&' };
+enum { CPO_SUBPERCENT = '/' };
+enum { CPO_BACKSL = '\\' };
+enum { CPO_CHDIR = '.' };
+enum { CPO_SCOLON = ';' };
+enum { CPO_NOSYMLINKS = '~' };
+
+enum { MOUSE_NORMAL = 'n' };
+enum { MOUSE_VISUAL = 'v' };
+enum { MOUSE_INSERT = 'i' };
+enum { MOUSE_COMMAND = 'c' };
+enum { MOUSE_HELP = 'h' };
+enum { MOUSE_RETURN = 'r' };
+
+enum { SHM_RO = 'r' };
+enum { SHM_MOD = 'm' };
+enum { SHM_FILE = 'f' };
+enum { SHM_LAST = 'i' };
+enum { SHM_TEXT = 'x' };
+enum { SHM_LINES = 'l' };
+enum { SHM_NEW = 'n' };
+enum { SHM_WRI = 'w' };
+enum { SHM_WRITE = 'W' };
+enum { SHM_TRUNC = 't' };
+enum { SHM_TRUNCALL = 'T' };
+enum { SHM_OVER = 'o' };
+enum { SHM_OVERALL = 'O' };
+enum { SHM_SEARCH = 's' };
+enum { SHM_ATTENTION = 'A' };
+enum { SHM_INTRO = 'I' };
+enum { SHM_COMPLETIONMENU = 'c' };
+enum { SHM_COMPLETIONSCAN = 'C' };
+enum { SHM_RECORDING = 'q' };
+enum { SHM_FILEINFO = 'F' };
+enum { SHM_SEARCHCOUNT = 'S' };
+enum { SHM_UNDO = 'u' };
+enum { SHM_LEN = 30 };
+
+enum { COM_NEST = 'n' };
+enum { COM_BLANK = 'b' };
+enum { COM_START = 's' };
+enum { COM_MIDDLE = 'm' };
+enum { COM_END = 'e' };
+enum { COM_AUTO_END = 'x' };
+enum { COM_FIRST = 'f' };
+enum { COM_LEFT = 'l' };
+enum { COM_RIGHT = 'r' };
+enum { COM_NOBACK = 'O' };
+enum { COM_MAX_LEN = 50 };
+
+enum { WIM_FULL = 0x01 };
+enum { WIM_LONGEST = 0x02 };
+enum { WIM_LIST = 0x04 };
+enum { WIM_BUFLASTUSED = 0x08 };
+enum { WIM_NOSELECT = 0x10 };
+enum { WIM_NOINSERT = 0x20 };
+
+enum { WOP_FUZZY = 'z' };
+enum { WOP_TAGFILE = 'g' };
+enum { WOP_PUM = 'p' };
+enum { WOP_EXACTTEXT = 'x' };
+
+enum { BS_INDENT = 'i' };
+enum { BS_EOL = 'l' };
+enum { BS_START = 's' };
+enum { BS_NOSTOP = 'p' };
 
 static char_u   *p_ambw;
 static int      p_ai;
@@ -2478,44 +1540,40 @@
 static int      p_bk;
 static char_u   *p_bkc;
 static unsigned bkc_flags;
-#define BKC_YES         0x001
-#define BKC_AUTO                0x002
-#define BKC_NO                  0x004
-#define BKC_BREAKSYMLINK        0x008
-#define BKC_BREAKHARDLINK       0x010
+enum { BKC_YES = 0x001 };
+enum { BKC_AUTO = 0x002 };
+enum { BKC_NO = 0x004 };
+enum { BKC_BREAKSYMLINK = 0x008 };
+enum { BKC_BREAKHARDLINK = 0x010 };
 static char_u   *p_bdir;
 static char_u   *p_bex;
 static char_u   *p_bo;
 static unsigned bo_flags;
 
-#define BO_ALL          0x0001
-#define BO_BS           0x0002
-#define BO_CRSR         0x0004
-#define BO_COMPL        0x0008
-#define BO_COPY         0x0010
-#define BO_CTRLG        0x0020
-#define BO_ERROR        0x0040
-#define BO_ESC          0x0080
-#define BO_EX           0x0100
-#define BO_HANGUL       0x0200
-#define BO_IM           0x0400
-#define BO_LANG         0x0800
-#define BO_MESS         0x1000
-#define BO_MATCH        0x2000
-#define BO_OPER         0x4000
-#define BO_REG          0x8000
-#define BO_SH           0x10000
-#define BO_SPELL        0x20000
-#define BO_TERM         0x40000
-#define BO_WILD         0x80000
+enum { BO_ALL = 0x0001 };
+enum { BO_BS = 0x0002 };
+enum { BO_CRSR = 0x0004 };
+enum { BO_COMPL = 0x0008 };
+enum { BO_COPY = 0x0010 };
+enum { BO_CTRLG = 0x0020 };
+enum { BO_ERROR = 0x0040 };
+enum { BO_ESC = 0x0080 };
+enum { BO_EX = 0x0100 };
+enum { BO_IM = 0x0400 };
+enum { BO_MESS = 0x1000 };
+enum { BO_MATCH = 0x2000 };
+enum { BO_OPER = 0x4000 };
+enum { BO_REG = 0x8000 };
+enum { BO_SH = 0x10000 };
+enum { BO_WILD = 0x80000 };
 
 static char_u   *p_bsk;
 static char_u   *p_bh;
 static char_u   *p_bt;
 static char_u   *p_cmp;
 static unsigned cmp_flags;
-#define CMP_INTERNAL            0x001
-#define CMP_KEEPASCII           0x002
+enum { CMP_INTERNAL = 0x001 };
+enum { CMP_KEEPASCII = 0x002 };
 static char_u   *p_enc;
 static int      p_deco;
 static int      p_cdh;
@@ -2536,24 +1594,15 @@
 static int      p_ac;
 static long     p_act;
 static long     p_acl;
-#define COT_MENU        0x001
-#define COT_MENUONE     0x002
-#define COT_ANY_MENU    0x003
-#define COT_LONGEST     0x004
-#define COT_PREVIEW         0x008
-#define COT_POPUP           0x010
-#define COT_POPUPHIDDEN     0x020
-#define COT_ANY_PREVIEW     0x038
-#define COT_NOINSERT        0x040
-#define COT_NOSELECT        0x080
-#define COT_FUZZY           0x100
-#define COT_NOSORT          0x200
-#define COT_PREINSERT       0x400
-#define COT_NEAREST         0x800
-
-#define CFC_KEYWORD         0x001
-#define CFC_FILES           0x002
-#define CFC_WHOLELINE       0x004
+enum { COT_MENUONE = 0x002 };
+enum { COT_ANY_MENU = 0x003 };
+enum { COT_LONGEST = 0x004 };
+enum { COT_NOINSERT = 0x040 };
+enum { COT_NOSELECT = 0x080 };
+enum { COT_FUZZY = 0x100 };
+enum { COT_NOSORT = 0x200 };
+enum { COT_PREINSERT = 0x400 };
+enum { COT_NEAREST = 0x800 };
 
 static long     p_ph;
 static long     p_pw;
@@ -2567,9 +1616,9 @@
 static char_u   *p_dir;
 static char_u   *p_dy;
 static unsigned dy_flags;
-#define DY_LASTLINE             0x001
-#define DY_TRUNCATE             0x002
-#define DY_UHEX                 0x004
+enum { DY_LASTLINE = 0x001 };
+enum { DY_TRUNCATE = 0x002 };
+enum { DY_UHEX = 0x004 };
 static int      p_ed;
 static char_u   *p_ead;
 static char_u   *p_emoji;
@@ -2597,7 +1646,7 @@
 static int      p_gd;
 static char_u   *p_jop;
 static unsigned jop_flags;
-#define JOP_STACK               0x001
+enum { JOP_STACK = 0x001 };
 static int      p_prompt;
 static char_u   *p_hf;
 static long     p_hh;
@@ -2710,28 +1759,27 @@
 static char_u   *p_sws;
 static char_u   *p_swb;
 static unsigned swb_flags;
-#define SWB_USEOPEN             0x001
-#define SWB_USETAB              0x002
-#define SWB_SPLIT               0x004
-#define SWB_NEWTAB              0x008
-#define SWB_VSPLIT              0x010
-#define SWB_USELAST             0x020
+enum { SWB_USEOPEN = 0x001 };
+enum { SWB_USETAB = 0x002 };
+enum { SWB_SPLIT = 0x004 };
+enum { SWB_NEWTAB = 0x008 };
+enum { SWB_VSPLIT = 0x010 };
 
 static char_u   *p_spk;
 
 static char_u   *p_tcl;
 static unsigned tcl_flags;
-#define TCL_LEFT                0x001
-#define TCL_USELAST             0x002
+enum { TCL_LEFT = 0x001 };
+enum { TCL_USELAST = 0x002 };
 static long     p_ts;
 static int      p_tbs;
 static char_u   *p_tc;
 static unsigned tc_flags;
-#define TC_FOLLOWIC             0x01
-#define TC_IGNORE               0x02
-#define TC_MATCH                0x04
-#define TC_FOLLOWSCS            0x08
-#define TC_SMART                0x10
+enum { TC_FOLLOWIC = 0x01 };
+enum { TC_IGNORE = 0x02 };
+enum { TC_MATCH = 0x04 };
+enum { TC_FOLLOWSCS = 0x08 };
+enum { TC_SMART = 0x10 };
 static long     p_tl;
 static int      p_tr;
 static char_u   *p_tags;
@@ -2759,14 +1807,10 @@
 static long     p_ttyscroll;
 static char_u   *p_ttym;
 static unsigned ttym_flags;
-# define TTYM_XTERM             0x01
-# define TTYM_XTERM2            0x02
-# define TTYM_DEC               0x04
-# define TTYM_NETTERM           0x08
-# define TTYM_JSBTERM           0x10
-# define TTYM_PTERM             0x20
-# define TTYM_URXVT             0x40
-# define TTYM_SGR               0x80
+enum { TTYM_XTERM = 0x01 };
+enum { TTYM_XTERM2 = 0x02 };
+enum { TTYM_URXVT = 0x40 };
+enum { TTYM_SGR = 0x80 };
 static long     p_ul;
 static long     p_ur;
 static long     p_uc;
@@ -2774,12 +1818,12 @@
 static int      p_vb;
 static char_u   *p_ve;
 static unsigned ve_flags;
-#define VE_BLOCK        5
-#define VE_INSERT       6
-#define VE_ALL          4
-#define VE_ONEMORE      8
-#define VE_NONE         16
-#define VE_NONEU        32
+enum { VE_BLOCK = 5 };
+enum { VE_INSERT = 6 };
+enum { VE_ALL = 4 };
+enum { VE_ONEMORE = 8 };
+enum { VE_NONE = 16 };
+enum { VE_NONEU = 32 };
 static long     p_verbose;
 static char_u  *p_vfile = (char_u *)"";
 static int      p_warn;
@@ -2902,16 +1946,10 @@
     , WV_COUNT
 };
 
-#define NO_LOCAL_UNDOLEVEL (-123456)
-
-#define ERR_BUFLEN 80
+enum { ERR_BUFLEN = 80 };
 
 // ---------------- end option.h ----------------
 
-// ---------------- begin beval.h ----------------
-
-// ---------------- end beval.h ----------------
-
 // ---------------- begin structs.h ----------------
 
 typedef struct
@@ -2942,8 +1980,6 @@
     void    *ga_data;
 } garray_T;
 
-#define GA_EMPTY    {0, 0, 0, 0, NULL}
-
 typedef signed char     int8_T;
 typedef double          float_T;
 
@@ -2961,8 +1997,8 @@
 typedef struct frame_S          frame_T;
 typedef int                     scid_T;
 typedef struct file_buffer      buf_T;
-#define SCRIPT_VERSION_MAX 4
-#define SCRIPT_VERSION_VIM9 999999
+enum { SCRIPT_VERSION_MAX = 4 };
+enum { SCRIPT_VERSION_VIM9 = 999999 };
 
 typedef struct {
     int         sc_version;
@@ -2976,18 +2012,13 @@
 
 // ---------------- begin regexp.h ----------------
 
-#define _REGEXP_H
-
-#define NSUBEXP  10
-
-#define NFA_MAX_BRACES 20
+enum { NSUBEXP = 10 };
 
-#define NFA_MAX_STATES 100000
-#define NFA_TOO_EXPENSIVE (-1)
+enum { NFA_MAX_STATES = 100000 };
 
-#define     AUTOMATIC_ENGINE    0
-#define     BACKTRACKING_ENGINE 1
-#define     NFA_ENGINE          2
+enum { AUTOMATIC_ENGINE = 0 };
+enum { BACKTRACKING_ENGINE = 1 };
+enum { NFA_ENGINE = 2 };
 
 typedef struct regengine regengine_T;
 
@@ -3078,21 +2109,15 @@
     long        (*regexec_multi)(regmmatch_T *, win_T *, buf_T *, linenr_T, colnr_T, int *);
 };
 
-#define REGSUB_COPY         1
-#define REGSUB_MAGIC        2
-#define REGSUB_BACKSLASH    4
+enum { REGSUB_COPY = 1 };
+enum { REGSUB_MAGIC = 2 };
+enum { REGSUB_BACKSLASH = 4 };
 
 // ---------------- end regexp.h ----------------
 
-# define guicolor_T long
-# define INVALCOLOR ((guicolor_T)0x1ffffff)
-# define CTERMCOLOR ((guicolor_T)0x1fffffe)
-#define COLOR_INVALID(x) ((x) == INVALCOLOR || (x) == CTERMCOLOR)
-
-#define NMARKS          ('z' - 'a' + 1)
-#define EXTRA_MARKS     10
-#define JUMPLISTSIZE    100
-#define TAGSTACKSIZE    20
+enum { EXTRA_MARKS = 10 };
+enum { JUMPLISTSIZE = 100 };
+enum { TAGSTACKSIZE = 20 };
 
 typedef struct filemark
 {
@@ -3118,57 +2143,32 @@
 typedef struct
 {
     char_u      *wo_wcr;
-#define w_p_wcr w_onebuf_opt.wo_wcr
     char_u      *wo_eiw;
-#define w_p_eiw w_onebuf_opt.wo_eiw
     int         wo_list;
-#define w_p_list w_onebuf_opt.wo_list
     char_u      *wo_lcs;
-#define w_p_lcs w_onebuf_opt.wo_lcs
     char_u      *wo_fcs;
-#define w_p_fcs w_onebuf_opt.wo_fcs
     int         wo_nu;
-#define w_p_nu w_onebuf_opt.wo_nu
     int         wo_rnu;
-#define w_p_rnu w_onebuf_opt.wo_rnu
     char_u      *wo_ve;
-#define w_p_ve w_onebuf_opt.wo_ve
     unsigned    wo_ve_flags;
-#define w_ve_flags w_onebuf_opt.wo_ve_flags
     int wo_wfb;
-#define w_p_wfb w_onebuf_opt.wo_wfb
     int         wo_wfh;
-#define w_p_wfh w_onebuf_opt.wo_wfh
     int         wo_wfw;
-#define w_p_wfw w_onebuf_opt.wo_wfw
     long        wo_scr;
-#define w_p_scr w_onebuf_opt.wo_scr
     int         wo_sms;
-#define w_p_sms w_onebuf_opt.wo_sms
     int         wo_scb;
-#define w_p_scb w_onebuf_opt.wo_scb
     int         wo_diff_saved;
-#define w_p_diff_saved w_onebuf_opt.wo_diff_saved
     int         wo_scb_save;
-#define w_p_scb_save w_onebuf_opt.wo_scb_save
     int         wo_wrap;
-#define w_p_wrap w_onebuf_opt.wo_wrap
     int         wo_crb;
-#define w_p_crb w_onebuf_opt.wo_crb
     int         wo_crb_save;
-#define w_p_crb_save w_onebuf_opt.wo_crb_save
     long        wo_siso;
-#define w_p_siso w_onebuf_opt.wo_siso
     long        wo_so;
-#define w_p_so w_onebuf_opt.wo_so
     long        wo_sop;
-#define w_p_sop w_onebuf_opt.wo_sop
 
     long_u      wo_wrap_flags;
-#define w_p_wrap_flags w_onebuf_opt.wo_wrap_flags
 
     char_u      *wo_whl;
-#define w_p_whl w_onebuf_opt.wo_whl
 } winopt_T;
 
 struct wininfo_S
@@ -3233,19 +2233,14 @@
     pos_T       uh_cursor;
     long        uh_cursor_vcol;
     int         uh_flags;
-    pos_T       uh_namedm[NMARKS];
+    pos_T       uh_namedm[ ('z' - 'a' + 1) ];
     visualinfo_T uh_visual;
     time_T      uh_time;
     long        uh_save_nr;
 };
 
-#define UH_CHANGED  0x01
-#define UH_EMPTYBUF 0x02
-
-#define ALIGN_LONG
-#define ALIGN_SIZE (sizeof(long))
-
-#define ALIGN_MASK (ALIGN_SIZE - 1)
+enum { UH_CHANGED = 0x01 };
+enum { UH_EMPTYBUF = 0x02 };
 
 typedef struct m_info minfo_T;
 
@@ -3268,7 +2263,7 @@
     blocknr_T       mhi_key;
 };
 
-#define MHT_INIT_SIZE   64
+enum { MHT_INIT_SIZE = 64 };
 
 typedef struct mf_hashtab_S
 {
@@ -3282,15 +2277,12 @@
 struct block_hdr
 {
     mf_hashitem_T bh_hashitem;
-#define bh_bnum bh_hashitem.mhi_key
 
     bhdr_T      *bh_next;
     bhdr_T      *bh_prev;
     char_u      *bh_data;
     int         bh_page_count;
 
-#define BH_DIRTY    1
-#define BH_LOCKED   2
     char        bh_flags;
 };
 
@@ -3299,7 +2291,6 @@
 struct nr_trans
 {
     mf_hashitem_T nt_hashitem;
-#define nt_old_bnum nt_hashitem.mhi_key
 
     blocknr_T   nt_new_bnum;
 };
@@ -3360,16 +2351,15 @@
     char_u      **xp_files_menu;
     char_u      **xp_files_info;
     char_u      *xp_line;
-#define EXPAND_BUF_LEN 256
     char_u      xp_buf[EXPAND_BUF_LEN];
     int         xp_search_dir;
     pos_T       xp_pre_incsearch_pos;
 } expand_T;
 
-#define XP_BS_NONE      0
-#define XP_BS_ONE       0x1
-#define XP_BS_THREE     0x2
-#define XP_BS_COMMA     0x4
+enum { XP_BS_NONE = 0 };
+enum { XP_BS_ONE = 0x1 };
+enum { XP_BS_THREE = 0x2 };
+enum { XP_BS_COMMA = 0x4 };
 
 typedef struct
 {
@@ -3391,22 +2381,6 @@
 typedef struct
 {
     int         cmod_flags;
-#define CMOD_SANDBOX        0x0001
-#define CMOD_SILENT         0x0002
-#define CMOD_ERRSILENT      0x0004
-#define CMOD_UNSILENT       0x0008
-#define CMOD_NOAUTOCMD      0x0010
-#define CMOD_HIDE           0x0020
-#define CMOD_BROWSE         0x0040
-#define CMOD_CONFIRM        0x0080
-#define CMOD_KEEPALT        0x0100
-#define CMOD_KEEPMARKS      0x0200
-#define CMOD_KEEPJUMPS      0x0400
-#define CMOD_LOCKMARKS      0x0800
-#define CMOD_KEEPPATTERNS   0x1000
-#define CMOD_NOSWAPFILE     0x2000
-#define CMOD_VIM9CMD        0x4000
-#define CMOD_LEGACY         0x8000
 
     int         cmod_split;
     int         cmod_tab;
@@ -3428,7 +2402,7 @@
     MF_DIRTY_YES_NOSYNC,
 } mfdirty_T;
 
-#define MF_SEED_LEN     8
+enum { MF_SEED_LEN = 8 };
 
 struct memfile
 {
@@ -3470,11 +2444,6 @@
     int         ml_stack_top;
     int         ml_stack_size;
 
-#define ML_EMPTY        0x01
-#define ML_LINE_DIRTY   0x02
-#define ML_LOCKED_DIRTY 0x04
-#define ML_LOCKED_POS   0x08
-#define ML_ALLOCATED    0x10
     int         ml_flags;
 
     colnr_T     ml_line_len;
@@ -3488,35 +2457,13 @@
     int         ml_locked_lineadd;
 } memline_T;
 
-#define ML_DEL_MESSAGE      1
-#define ML_DEL_UNDO         2
-#define ML_DEL_NOPROP       4
-
-#define ML_APPEND_NEW       1
-#define ML_APPEND_MARK      2
-#define ML_APPEND_UNDO      4
-#define ML_APPEND_NOPROP    8
-
-#define TP_FLAG_CONT_NEXT       0x1
-#define TP_FLAG_CONT_PREV       0x2
-
-#define TP_FLAG_ALIGN_RIGHT     0x010
-#define TP_FLAG_ALIGN_ABOVE     0x020
-#define TP_FLAG_ALIGN_BELOW     0x040
-
-#define TP_FLAG_WRAP            0x080
-#define TP_FLAG_START_INCL      0x100
-#define TP_FLAG_DELETED         0x200
-#define TP_FLAG_VTEXT_PTR       0x400
-
-#define PROP_COUNT_SIZE sizeof(uint16_t)
-
-#define PROP_TEXT_MIN_CELLS     4
-
-#define PT_FLAG_INS_START_INCL  1
-#define PT_FLAG_INS_END_INCL    2
-#define PT_FLAG_COMBINE         4
-#define PT_FLAG_OVERRIDE        8
+enum { ML_DEL_MESSAGE = 1 };
+enum { ML_DEL_UNDO = 2 };
+enum { ML_DEL_NOPROP = 4 };
+
+enum { ML_APPEND_NEW = 1 };
+enum { ML_APPEND_MARK = 2 };
+enum { ML_APPEND_UNDO = 4 };
 
 typedef struct signgroup_S
 {
@@ -3550,15 +2497,6 @@
     int         ae_fnum;
 } aentry_T;
 
-#define ALIST(win)      (win)->w_alist
-#define GARGLIST        ((aentry_T *)global_alist.al_ga.ga_data)
-#define ARGLIST         ((aentry_T *)ALIST(curwin)->al_ga.ga_data)
-#define WARGLIST(wp)    ((aentry_T *)ALIST(wp)->al_ga.ga_data)
-#define AARGLIST(al)    ((aentry_T *)((al)->al_ga.ga_data))
-#define GARGCOUNT       (global_alist.al_ga.ga_len)
-#define ARGCOUNT        (ALIST(curwin)->al_ga.ga_len)
-#define WARGCOUNT(wp)   (ALIST(wp)->al_ga.ga_len)
-
 typedef struct eslist_elem eslist_T;
 struct eslist_elem
 {
@@ -3566,42 +2504,6 @@
     eslist_T    *next;
 };
 
-#define CSTACK_LEN      50
-
-#define cs_rettv        cs_pend.csp_rv
-#define cs_exception    cs_pend.csp_ex
-
-#define CSF_TRUE        0x0001
-#define CSF_ACTIVE      0x0002
-#define CSF_ELSE        0x0004
-#define CSF_WHILE       0x0008
-#define CSF_FOR 0x0010
-#define CSF_BLOCK       0x0020
-
-#define CSF_TRY 0x0100
-#define CSF_FINALLY     0x0200
-#define CSF_CATCH       0x0400
-#define CSF_THROWN      0x0800
-#define CSF_CAUGHT      0x1000
-#define CSF_FINISHED    0x2000
-#define CSF_SILENT      0x4000
-
-#define CSF_FUNC_DEF    0x8000
-
-#define CSTP_NONE       0
-#define CSTP_ERROR      1
-#define CSTP_INTERRUPT  2
-#define CSTP_THROW      4
-#define CSTP_BREAK      8
-#define CSTP_CONTINUE   16
-#define CSTP_RETURN     24
-#define CSTP_FINISH     32
-
-#define CSL_HAD_LOOP     1
-#define CSL_HAD_ENDLOOP 2
-#define CSL_HAD_CONT     4
-#define CSL_HAD_FINA     8
-
 typedef struct msglist msglist_T;
 struct msglist
 {
@@ -3649,7 +2551,7 @@
     int         estate_did_emsg;
 };
 
-#define MAX_HL_ID       20000
+enum { MAX_HL_ID = 20000 };
 
 typedef struct attr_entry
 {
@@ -3712,12 +2614,12 @@
     time_t      time_set;
 } histentry_T;
 
-#define CONV_NONE               0
-#define CONV_TO_UTF8            1
-#define CONV_9_TO_UTF8          2
-#define CONV_TO_LATIN1          3
-#define CONV_TO_LATIN9          4
-#define CONV_ICONV              5
+enum { CONV_NONE = 0 };
+enum { CONV_TO_UTF8 = 1 };
+enum { CONV_9_TO_UTF8 = 2 };
+enum { CONV_TO_LATIN1 = 3 };
+enum { CONV_TO_LATIN9 = 4 };
+enum { CONV_ICONV = 5 };
 
 typedef struct mapblock mapblock_T;
 struct mapblock
@@ -3750,13 +2652,10 @@
     char_u      *hi_key;
 } hashitem_T;
 
-#define HI_KEY_REMOVED &hash_removed
-#define HASHITEM_EMPTY(hi) ((hi)->hi_key == NULL || (hi)->hi_key == &hash_removed)
+enum { HT_INIT_SIZE = 16 };
 
-#define HT_INIT_SIZE 16
-
-#define HTFLAGS_ERROR   0x01
-#define HTFLAGS_FROZEN  0x02
+enum { HTFLAGS_ERROR = 0x01 };
+enum { HTFLAGS_FROZEN = 0x02 };
 
 typedef struct hashtable_S
 {
@@ -3774,9 +2673,6 @@
 
   typedef long long             varnumber_T;
   typedef unsigned long long    uvarnumber_T;
-#  define VARNUM_MIN            LLONG_MIN
-#  define VARNUM_MAX            LLONG_MAX
-#  define UVARNUM_MAX           ULLONG_MAX
 
 typedef struct {
     char_u      *cb_name;
@@ -3833,20 +2729,7 @@
     type_T          **tt_args;
 };
 
-#define TTFLAG_VARARGS      0x01
-#define TTFLAG_BOOL_OK      0x02
-#define TTFLAG_FLOAT_OK     0x04
-#define TTFLAG_NUMBER_OK    0x08
-#define TTFLAG_STATIC       0x10
-#define TTFLAG_CONST        0x20
-#define TTFLAG_SUPER        0x40
-#define TTFLAG_GENERIC      0x80
-#define TTFLAG_TUPLE_OK     0x100
-
-#define IS_GENERIC_TYPE(type)       ((type->tt_flags & TTFLAG_GENERIC) == TTFLAG_GENERIC)
-
-#define TYPECHK_NUMBER_OK       0x1
-#define TYPECHK_TUPLE_OK        0x2
+enum { TTFLAG_GENERIC = 0x80 };
 
 typedef enum {
     VIM_ACCESS_PRIVATE,
@@ -3854,10 +2737,6 @@
     VIM_ACCESS_ALL
 } omacc_T;
 
-#define OCMFLAG_HAS_TYPE        0x01
-#define OCMFLAG_FINAL           0x02
-#define OCMFLAG_CONST           0x04
-
 typedef enum {
     CLASS_BUILTIN_INVALID,
     CLASS_BUILTIN_STRING,
@@ -3882,10 +2761,8 @@
     int         i2c_is_method;
 };
 
-#define CLASS_INTERFACE     0x1
-#define CLASS_EXTENDED      0x2
-#define CLASS_ABSTRACT      0x4
-#define CLASS_ENUM          0x8
+enum { CLASS_INTERFACE = 0x1 };
+enum { CLASS_ENUM = 0x8 };
 
 struct class_S
 {
@@ -3926,9 +2803,6 @@
     type_T      class_object_type;
 };
 
-#define IS_INTERFACE(cl)        ((cl)->class_flags & CLASS_INTERFACE)
-#define IS_ENUM(cl)             ((cl)->class_flags & CLASS_ENUM)
-
 struct object_S
 {
     class_T     *obj_class;
@@ -3967,12 +2841,7 @@
     }           vval;
 };
 
-#define VAR_SCOPE     1
-#define VAR_DEF_SCOPE 2
-
-#define VAR_LOCKED          1
-#define VAR_FIXED           2
-#define VAR_ITEMS_LOCKED    4
+enum { VAR_FIXED = 2 };
 
 typedef struct listitem_S listitem_T;
 
@@ -4026,7 +2895,7 @@
 };
 typedef struct dictitem_S dictitem_T;
 
-#define DICTITEM16_KEY_LEN 16
+enum { DICTITEM16_KEY_LEN = 16 };
 struct dictitem16_S
 {
     typval_T    di_tv;
@@ -4035,12 +2904,8 @@
 };
 typedef struct dictitem16_S dictitem16_T;
 
-#define DI_FLAGS_RO        0x01
-#define DI_FLAGS_RO_SBX    0x02
-#define DI_FLAGS_FIX       0x04
-#define DI_FLAGS_LOCK      0x08
-#define DI_FLAGS_ALLOC     0x10
-#define DI_FLAGS_RELOAD    0x20
+enum { DI_FLAGS_RO = 0x01 };
+enum { DI_FLAGS_FIX = 0x04 };
 
 struct dictvar_S
 {
@@ -4127,7 +2992,7 @@
     int         lvs_copyID;
 };
 
-#define MAX_LOOP_DEPTH 10
+enum { MAX_LOOP_DEPTH = 10 };
 
 typedef struct outer_S outer_T;
 struct outer_S {
@@ -4279,16 +3144,11 @@
     JIO_OUT
 } job_io_T;
 
-#define CH_PART_FD(part)        ch_part[part].ch_fd
-
 typedef enum {
     PART_SOCK = 0,
-#define CH_SOCK_FD      CH_PART_FD(PART_SOCK)
     PART_COUNT,
 } ch_part_T;
 
-#define INVALID_FD      (-1)
-
 typedef struct {
     sock_T      ch_fd;
 
@@ -4351,72 +3211,17 @@
     int         ch_copyID;
 };
 
-#define JO_MODE             0x0001
-#define JO_IN_MODE          0x0002
-#define JO_OUT_MODE         0x0004
-#define JO_ERR_MODE         0x0008
-#define JO_CALLBACK         0x0010
-#define JO_OUT_CALLBACK     0x0020
-#define JO_ERR_CALLBACK     0x0040
-#define JO_CLOSE_CALLBACK   0x0080
-#define JO_WAITTIME         0x0100
-#define JO_TIMEOUT          0x0200
-#define JO_OUT_TIMEOUT      0x0400
-#define JO_ERR_TIMEOUT      0x0800
-#define JO_PART             0x1000
-#define JO_ID               0x2000
-#define JO_STOPONEXIT       0x4000
-#define JO_EXIT_CB          0x8000
-#define JO_OUT_IO           0x10000
-#define JO_ERR_IO           0x20000
-#define JO_IN_IO            0x40000
-#define JO_OUT_NAME         0x80000
-#define JO_ERR_NAME         0x100000
-#define JO_IN_NAME          0x200000
-#define JO_IN_TOP           0x400000
-#define JO_IN_BOT           0x800000
-#define JO_OUT_BUF          0x1000000
-#define JO_ERR_BUF          0x2000000
-#define JO_IN_BUF           0x4000000
-#define JO_CHANNEL          0x8000000
-#define JO_BLOCK_WRITE      0x10000000
-#define JO_OUT_MODIFIABLE   0x20000000
-#define JO_ERR_MODIFIABLE   0x40000000
-#define JO_ALL              0x7fffffff
-
-#define JO2_OUT_MSG         0x0001
-#define JO2_ERR_MSG         0x0002
-#define JO2_TERM_NAME       0x0004
-#define JO2_TERM_FINISH     0x0008
-#define JO2_ENV             0x0010
-#define JO2_CWD             0x0020
-#define JO2_TERM_ROWS       0x0040
-#define JO2_TERM_COLS       0x0080
-#define JO2_VERTICAL        0x0100
-#define JO2_CURWIN          0x0200
-#define JO2_HIDDEN          0x0400
-#define JO2_TERM_OPENCMD    0x0800
-#define JO2_EOF_CHARS       0x1000
-#define JO2_NORESTORE       0x2000
-#define JO2_TERM_KILL       0x4000
-#define JO2_ANSI_COLORS     0x8000
-#define JO2_TTY_TYPE        0x10000
-#define JO2_BUFNR           0x20000
-#define JO2_TERM_API        0x40000
-#define JO2_TERM_HIGHLIGHT  0x80000
-
-#define JO_MODE_ALL     (JO_MODE + JO_IN_MODE + JO_OUT_MODE + JO_ERR_MODE)
-#define JO_CB_ALL     (JO_CALLBACK + JO_OUT_CALLBACK + JO_ERR_CALLBACK + JO_CLOSE_CALLBACK)
-#define JO_TIMEOUT_ALL  (JO_TIMEOUT + JO_OUT_TIMEOUT + JO_ERR_TIMEOUT)
-
-#define SYNSPL_DEFAULT  0
-#define SYNSPL_TOP      1
-#define SYNSPL_NOTOP    2
-
-#define SYNFLD_START    0
-#define SYNFLD_MINIMUM  1
-
-# define B_SPELL(buf)  (0)
+enum { JO_MODE = 0x0001 };
+enum { JO_IN_MODE = 0x0002 };
+enum { JO_OUT_MODE = 0x0004 };
+enum { JO_ERR_MODE = 0x0008 };
+enum { JO_CALLBACK = 0x0010 };
+enum { JO_OUT_CALLBACK = 0x0020 };
+enum { JO_ERR_CALLBACK = 0x0040 };
+enum { JO_CLOSE_CALLBACK = 0x0080 };
+enum { JO_TIMEOUT = 0x0200 };
+enum { JO_OUT_TIMEOUT = 0x0400 };
+enum { JO_ERR_TIMEOUT = 0x0800 };
 
 typedef struct timer_S timer_T;
 struct timer_S
@@ -4449,7 +3254,6 @@
 
     int         b_changed;
     dictitem16_T b_ct_di;
-#define CHANGEDTICK(buf) ((buf)->b_ct_di.di_tv.vval.v_number)
 
     varnumber_T b_last_changedtick;
     varnumber_T b_last_changedtick_pum;
@@ -4471,7 +3275,7 @@
     off_T       b_orig_size;
     int         b_orig_mode;
 
-    pos_T       b_namedm[NMARKS];
+    pos_T       b_namedm[ ('z' - 'a' + 1) ];
 
     visualinfo_T b_visual;
 
@@ -4519,11 +3323,6 @@
 
     long        b_p_iminsert;
     long        b_p_imsearch;
-#define B_IMODE_USE_INSERT (-1)
-#define B_IMODE_NONE 0
-#define B_IMODE_LMAP 1
-#define B_IMODE_IM 2
-#define B_IMODE_LAST 2
 
     bool        b_p_initialized;
 
@@ -4660,10 +3459,10 @@
 
 };
 
-#define SNAP_HELP_IDX       0
-#define SNAP_AUCMD_IDX      1
-#define SNAP_QUICKFIX_IDX   2
-#define SNAP_COUNT          3
+enum { SNAP_HELP_IDX = 0 };
+enum { SNAP_AUCMD_IDX = 1 };
+enum { SNAP_QUICKFIX_IDX = 2 };
+enum { SNAP_COUNT = 3 };
 
 typedef struct tabpage_S tabpage_T;
 struct tabpage_S
@@ -4708,9 +3507,9 @@
     win_T       *fr_win;
 };
 
-#define FR_LEAF 0
-#define FR_ROW  1
-#define FR_COL  2
+enum { FR_LEAF = 0 };
+enum { FR_ROW = 1 };
+enum { FR_COL = 2 };
 
 typedef struct
 {
@@ -4906,7 +3705,6 @@
 
     winopt_T    w_onebuf_opt;
     winopt_T    w_allbuf_opt;
-#define GLOBAL_WO(p)    ((char *)(p) + sizeof(winopt_T))
 
     long        w_scbind_pos;
 
@@ -4975,8 +3773,8 @@
     char_u      *searchbuf;
 } cmdarg_T;
 
-#define CA_COMMAND_BUSY     1
-#define CA_NO_ADJ_OP_END    2
+enum { CA_COMMAND_BUSY = 1 };
+enum { CA_NO_ADJ_OP_END = 2 };
 
 typedef struct
 {
@@ -4990,8 +3788,6 @@
     int         save_VIsual_active;
 } aco_save_T;
 
-#define PRINT_NUMBER_WIDTH 8
-
 typedef struct
 {
     char_u      *pum_text;
@@ -5023,11 +3819,11 @@
 };
 typedef struct js_reader js_read_T;
 
-#define MAX_ARG_CMDS 10
+enum { MAX_ARG_CMDS = 10 };
 
-#define WIN_HOR     1
-#define WIN_VER     2
-#define WIN_TABS    3
+enum { WIN_HOR = 1 };
+enum { WIN_VER = 2 };
+enum { WIN_TABS = 3 };
 
 typedef struct
 {
@@ -5085,9 +3881,9 @@
     FAIL_POPUP
 } mouse_find_T;
 
-#define DELETION_REGISTER       36
+enum { DELETION_REGISTER = 36 };
 
-#  define NUM_REGISTERS         37
+enum { NUM_REGISTERS = 37 };
 
 struct block_def
 {
@@ -5149,22 +3945,20 @@
     vimconv_T   conv;
 } source_cookie_T;
 
-#define WRITEBUFSIZE    8192
+enum { WRITEBUFSIZE = 8192 };
 
-#define FIO_LATIN1      0x01
-#define FIO_UTF8        0x02
-#define FIO_UCS2        0x04
-#define FIO_UCS4        0x08
-#define FIO_UTF16       0x10
-#define FIO_ENDIAN_L    0x80
-#define FIO_ENCRYPTED   0x1000
-#define FIO_NOCONVERT   0x2000
-#define FIO_UCSBOM      0x4000
-#define FIO_ALL (-1)
+enum { FIO_LATIN1 = 0x01 };
+enum { FIO_UTF8 = 0x02 };
+enum { FIO_UCS2 = 0x04 };
+enum { FIO_UCS4 = 0x08 };
+enum { FIO_UTF16 = 0x10 };
+enum { FIO_ENDIAN_L = 0x80 };
+enum { FIO_NOCONVERT = 0x2000 };
+enum { FIO_UCSBOM = 0x4000 };
 
-#define CONV_RESTLEN 30
+enum { CONV_RESTLEN = 30 };
 
-#define ICONV_MULT 8
+enum { ICONV_MULT = 8 };
 
 typedef enum {
     OPTION_MAGIC_NOT_SET,
@@ -5190,8 +3984,6 @@
     WT_CAST,
 } wherekind_T;
 
-#define WHERE_INIT {NULL, 0, WT_UNKNOWN}
-
 typedef struct {
     int         idx;
     char_u      *str;
@@ -5254,16 +4046,12 @@
     int         spv_has_spell;
 } spellvars_T;
 
-#define STRLEN_LITERAL(s) (sizeof(s "") - 1)
-
 typedef struct
 {
     int     key;
     string_T value;
 } keyvalue_T;
 
-#define KEYVALUE_ENTRY(k, v)     {(k), {((char_u *)v), STRLEN_LITERAL(v)}}
-
 typedef enum
 {
     TERM_SYNC_OUTPUT_ENABLE = 1 << 0,
@@ -5275,41 +4063,6 @@
 // ---------------- end structs.h ----------------
 // ---------------- begin xdiff.h ----------------
 
-#define XDIFF_H
-
-#define XDF_NEED_MINIMAL (1 << 0)
-
-#define XDF_IGNORE_WHITESPACE (1 << 1)
-#define XDF_IGNORE_WHITESPACE_CHANGE (1 << 2)
-#define XDF_IGNORE_WHITESPACE_AT_EOL (1 << 3)
-#define XDF_IGNORE_CR_AT_EOL (1 << 4)
-#define XDF_WHITESPACE_FLAGS (XDF_IGNORE_WHITESPACE |                         XDF_IGNORE_WHITESPACE_CHANGE |                          XDF_IGNORE_WHITESPACE_AT_EOL |                          XDF_IGNORE_CR_AT_EOL)
-
-#define XDF_IGNORE_BLANK_LINES (1 << 7)
-
-#define XDF_PATIENCE_DIFF (1 << 14)
-#define XDF_HISTOGRAM_DIFF (1 << 15)
-#define XDF_DIFF_ALGORITHM_MASK (XDF_PATIENCE_DIFF | XDF_HISTOGRAM_DIFF)
-#define XDF_DIFF_ALG(x) ((x) & XDF_DIFF_ALGORITHM_MASK)
-
-#define XDF_INDENT_HEURISTIC (1 << 23)
-
-#define XDL_EMIT_FUNCNAMES (1 << 0)
-#define XDL_EMIT_NO_HUNK_HDR (1 << 1)
-#define XDL_EMIT_FUNCCONTEXT (1 << 2)
-
-#define XDL_MERGE_MINIMAL 0
-#define XDL_MERGE_EAGER 1
-#define XDL_MERGE_ZEALOUS 2
-#define XDL_MERGE_ZEALOUS_ALNUM 3
-
-#define XDL_MERGE_FAVOR_OURS 1
-#define XDL_MERGE_FAVOR_THEIRS 2
-#define XDL_MERGE_FAVOR_UNION 3
-
-#define XDL_MERGE_DIFF3 1
-#define XDL_MERGE_ZEALOUS_DIFF3 2
-
 typedef struct s_mmfile {
         char *ptr;
         long size;
@@ -5346,11 +4099,6 @@
         xdl_emit_hunk_consume_func_t hunk_func;
 } xdemitconf_t;
 
-#define xdl_malloc(x) lalloc((x), TRUE)
-#define xdl_calloc(n, sz) lalloc_clear(n*sz, TRUE)
-#define xdl_free(ptr) vim_free(ptr)
-#define xdl_realloc(ptr,x) vim_realloc((ptr),(x))
-
 void *xdl_mmfile_first(mmfile_t *mmf, long *size);
 long xdl_mmfile_size(mmfile_t *mmf);
 
@@ -5367,233 +4115,49 @@
         const char *file2;
 } xmparam_t;
 
-#define DEFAULT_CONFLICT_MARKER_SIZE 7
-
 int xdl_merge(mmfile_t *orig, mmfile_t *mf1, mmfile_t *mf2, xmparam_t const *xmp, mmbuffer_t *result);
 
 // ---------------- end xdiff.h ----------------
 
-// ---------------- begin alloc.h ----------------
+enum { MOUSE_LEFT = 0x00 };
+enum { MOUSE_MIDDLE = 0x01 };
+enum { MOUSE_RIGHT = 0x02 };
+enum { MOUSE_RELEASE = 0x03 };
 
-// ---------------- end alloc.h ----------------
+enum { MOUSE_SHIFT = 0x04 };
+enum { MOUSE_ALT = 0x08 };
+enum { MOUSE_CTRL = 0x10 };
 
-#define PROF_NONE       0
-#define PROF_YES        1
-#define PROF_PAUSED     2
-
-#define MOUSE_LEFT      0x00
-#define MOUSE_MIDDLE    0x01
-#define MOUSE_RIGHT     0x02
-#define MOUSE_RELEASE   0x03
-
-#define MOUSE_SHIFT     0x04
-#define MOUSE_ALT       0x08
-#define MOUSE_CTRL      0x10
-
-#define MOUSE_4 0x100
-#define MOUSE_5 0x200
-
-#define MOUSE_X1        0x300
-#define MOUSE_X2        0x400
-
-#define MOUSE_6 0x500
-#define MOUSE_7 0x600
-
-#define MOUSE_MOVE 0x700
-
-#define MOUSE_DRAG_XTERM   0x40
-
-#define MOUSE_DRAG      (0x40 | MOUSE_RELEASE)
-
-#define MOUSEWHEEL_LOW          0x60
-
-#define MOUSESIDEBUTTONS_LOW            0xa0
-
-#define MOUSE_CLICK_MASK        0x03
-
-#define NUM_MOUSE_CLICKS(code)     (((unsigned)((code) & 0xC0) >> 6) + 1)
-
-#define SET_NUM_MOUSE_CLICKS(code, num)     ((code) = ((code) & 0x3f) | ((((num) - 1) & 3) << 6))
-
-#define MOUSE_COLOFF 10000
-
-#define IN_UNKNOWN              0
-#define IN_BUFFER               1
-#define IN_STATUS_LINE          2
-#define IN_SEP_LINE             4
-#define IN_OTHER_WIN            8
-#define CURSOR_MOVED            0x100
-#define MOUSE_FOLD_CLOSE        0x200
-#define MOUSE_FOLD_OPEN         0x400
-#define MOUSE_WINBAR            0x800
-
-#define MOUSE_FOCUS             0x01
-#define MOUSE_MAY_VIS           0x02
-#define MOUSE_DID_MOVE          0x04
-#define MOUSE_SETPOS            0x08
-#define MOUSE_MAY_STOP_VIS      0x10
-#define MOUSE_RELEASED          0x20
-
-# define CHECK_DOUBLE_CLICK 1
-
-#define VALID_PATH              1
-#define VALID_HEAD              2
-
-#define VV_COUNT        0
-#define VV_COUNT1       1
-#define VV_PREVCOUNT    2
-#define VV_ERRMSG       3
-#define VV_WARNINGMSG   4
-#define VV_STATUSMSG    5
-#define VV_SHELL_ERROR  6
-#define VV_THIS_SESSION 7
-#define VV_VERSION      8
-#define VV_LNUM         9
-#define VV_TERMRESPONSE 10
-#define VV_FNAME        11
-#define VV_LANG         12
-#define VV_LC_TIME      13
-#define VV_CTYPE        14
-#define VV_CC_FROM      15
-#define VV_CC_TO        16
-#define VV_FNAME_IN     17
-#define VV_FNAME_OUT    18
-#define VV_FNAME_NEW    19
-#define VV_FNAME_DIFF   20
-#define VV_CMDARG       21
-#define VV_FOLDSTART    22
-#define VV_FOLDEND      23
-#define VV_FOLDDASHES   24
-#define VV_FOLDLEVEL    25
-#define VV_PROGNAME     26
-#define VV_SEND_SERVER  27
-#define VV_DYING        28
-#define VV_EXCEPTION    29
-#define VV_THROWPOINT   30
-#define VV_REG          31
-#define VV_CMDBANG      32
-#define VV_INSERTMODE   33
-#define VV_VAL          34
-#define VV_KEY          35
-#define VV_PROFILING    36
-#define VV_FCS_REASON   37
-#define VV_FCS_CHOICE   38
-#define VV_BEVAL_BUFNR  39
-#define VV_BEVAL_WINNR  40
-#define VV_BEVAL_WINID  41
-#define VV_BEVAL_LNUM   42
-#define VV_BEVAL_COL    43
-#define VV_BEVAL_TEXT   44
-#define VV_SCROLLSTART  45
-#define VV_SWAPNAME     46
-#define VV_SWAPCHOICE   47
-#define VV_SWAPCOMMAND  48
-#define VV_CHAR         49
-#define VV_MOUSE_WIN    50
-#define VV_MOUSE_WINID  51
-#define VV_MOUSE_LNUM   52
-#define VV_MOUSE_COL    53
-#define VV_OP           54
-#define VV_SEARCHFORWARD 55
-#define VV_HLSEARCH     56
-#define VV_OLDFILES     57
-#define VV_WINDOWID     58
-#define VV_PROGPATH     59
-#define VV_COMPLETED_ITEM 60
-#define VV_OPTION_NEW   61
-#define VV_OPTION_OLD   62
-#define VV_OPTION_OLDLOCAL 63
-#define VV_OPTION_OLDGLOBAL 64
-#define VV_OPTION_COMMAND 65
-#define VV_OPTION_TYPE  66
-#define VV_ERRORS       67
-#define VV_FALSE        68
-#define VV_TRUE         69
-#define VV_NONE         70
-#define VV_NULL         71
-#define VV_NUMBERMAX    72
-#define VV_NUMBERMIN    73
-#define VV_NUMBERSIZE   74
-#define VV_VIM_DID_ENTER 75
-#define VV_TESTING      76
-#define VV_TYPE_NUMBER  77
-#define VV_TYPE_STRING  78
-#define VV_TYPE_FUNC    79
-#define VV_TYPE_LIST    80
-#define VV_TYPE_DICT    81
-#define VV_TYPE_FLOAT   82
-#define VV_TYPE_BOOL    83
-#define VV_TYPE_NONE    84
-#define VV_TYPE_JOB     85
-#define VV_TYPE_CHANNEL 86
-#define VV_TYPE_BLOB    87
-#define VV_TYPE_CLASS   88
-#define VV_TYPE_OBJECT  89
-#define VV_TERMRFGRESP  90
-#define VV_TERMRBGRESP  91
-#define VV_TERMU7RESP   92
-#define VV_TERMSTYLERESP 93
-#define VV_TERMBLINKRESP 94
-#define VV_EVENT        95
-#define VV_VERSIONLONG  96
-#define VV_ECHOSPACE    97
-#define VV_ARGV         98
-#define VV_COLLATE      99
-#define VV_EXITING      100
-#define VV_COLORNAMES   101
-#define VV_SIZEOFINT    102
-#define VV_SIZEOFLONG   103
-#define VV_SIZEOFPOINTER 104
-#define VV_MAXCOL       105
-#define VV_PYTHON3_VERSION 106
-#define VV_TYPE_TYPEALIAS 107
-#define VV_TYPE_ENUM      108
-#define VV_TYPE_ENUMVALUE 109
-#define VV_STACKTRACE   110
-#define VV_TYPE_TUPLE   111
-#define VV_WAYLAND_DISPLAY 112
-#define VV_CLIPMETHOD 113
-#define VV_TERMDA1 114
-#define VV_TERMOSC 115
-#define VV_VIM_DID_INIT         116
-#define VV_CLIPPROVIDERS 117
-#define VV_LEN          118
-
-#define VVAL_FALSE      0L
-#define VVAL_TRUE       1L
-#define VVAL_NONE       2L
-#define VVAL_NULL       3L
-
-#define VAR_TYPE_NUMBER     0
-#define VAR_TYPE_STRING     1
-#define VAR_TYPE_FUNC       2
-#define VAR_TYPE_LIST       3
-#define VAR_TYPE_DICT       4
-#define VAR_TYPE_FLOAT      5
-#define VAR_TYPE_BOOL       6
-#define VAR_TYPE_NONE       7
-#define VAR_TYPE_JOB        8
-#define VAR_TYPE_CHANNEL    9
-#define VAR_TYPE_BLOB       10
-#define VAR_TYPE_INSTR      11
-#define VAR_TYPE_CLASS      12
-#define VAR_TYPE_OBJECT     13
-#define VAR_TYPE_TYPEALIAS  14
-#define VAR_TYPE_ENUM       15
-#define VAR_TYPE_ENUMVALUE  16
-#define VAR_TYPE_TUPLE      17
+enum { MOUSE_X1 = 0x300 };
+enum { MOUSE_X2 = 0x400 };
 
-#define DICT_MAXNEST 100
+enum { MOUSE_DRAG_XTERM = 0x40 };
 
-#define TABSTOP_MAX 9999
+enum { MOUSEWHEEL_LOW = 0x60 };
 
-typedef struct stat stat_T;
+enum { MOUSESIDEBUTTONS_LOW = 0xa0 };
+
+enum { MOUSE_CLICK_MASK = 0x03 };
+
+enum { IN_UNKNOWN = 0 };
+enum { IN_BUFFER = 1 };
+enum { IN_STATUS_LINE = 2 };
+enum { IN_SEP_LINE = 4 };
+enum { CURSOR_MOVED = 0x100 };
+
+enum { MOUSE_FOCUS = 0x01 };
+enum { MOUSE_MAY_VIS = 0x02 };
+enum { MOUSE_DID_MOVE = 0x04 };
+enum { MOUSE_SETPOS = 0x08 };
+enum { MOUSE_MAY_STOP_VIS = 0x10 };
+enum { MOUSE_RELEASED = 0x20 };
 
-# define ATTRIBUTE_FORMAT_PRINTF(fmt_idx, arg_idx)     __attribute__((format(printf, fmt_idx, arg_idx)))
+enum { VALID_PATH = 1 };
+enum { VALID_HEAD = 2 };
 
-# define likely(x)      __builtin_expect((x), 1)
-# define unlikely(x)    __builtin_expect((x), 0)
-# define ATTRIBUTE_COLD __attribute__((cold))
+enum { TABSTOP_MAX = 9999 };
+
+typedef struct stat stat_T;
 
 typedef enum {
     PASTE_INSERT,
@@ -5615,8 +4179,6 @@
     ESTACK_SCRIPT,
 } estack_arg_T;
 
-#define ENABLE_STL_MODE_MULTI_NL
-
 typedef enum {
     KEYPROTOCOL_NONE,
     KEYPROTOCOL_MOK2,
@@ -5636,55 +4198,39 @@
 
 typedef int (*opt_expand_cb_T)(optexpand_T *args, int *numMatches, char_u ***matches);
 
-#define ASSIGN_VAR      0
-#define ASSIGN_FINAL    0x01
-#define ASSIGN_CONST    0x02
-#define ASSIGN_NO_DECL  0x04
-#define ASSIGN_DECL     0x08
-#define ASSIGN_UNPACK   0x10
-#define ASSIGN_NO_MEMBER_TYPE 0x20
-#define ASSIGN_FOR_LOOP 0x40
-#define ASSIGN_INIT     0x80
-#define ASSIGN_UPDATE_BLOCK_ID 0x100
-#define ASSIGN_COMPOUND_OP 0x200
-
 // ---------------- begin ex_cmds.h ----------------
 
-#define EX_RANGE        0x001
-#define EX_BANG         0x002
-#define EX_EXTRA        0x004
-#define EX_XFILE        0x008
-#define EX_NOSPC        0x010
-#define EX_DFLALL       0x020
-#define EX_WHOLEFOLD    0x040
-#define EX_NEEDARG      0x080
-#define EX_TRLBAR       0x100
-#define EX_REGSTR       0x200
-#define EX_COUNT        0x400
-#define EX_NOTRLCOM     0x800
-#define EX_ZEROR       0x1000
-#define EX_CTRLV       0x2000
-#define EX_CMDARG      0x4000
-#define EX_BUFNAME     0x8000
-#define EX_BUFUNL     0x10000
-#define EX_ARGOPT     0x20000
-#define EX_SBOXOK     0x40000
-#define EX_CMDWIN     0x80000
-#define EX_MODIFY    0x100000
-#define EX_FLAGS     0x200000
-#define EX_RESTRICT  0x400000
-#define EX_EXPAND    0x800000
-#define EX_LOCK_OK  0x1000000
-#define EX_NONWHITE_OK 0x2000000
-#define EX_KEEPSCRIPT  0x4000000
-#define EX_EXPR_ARG    0x8000000
-#define EX_WHOLE      0x10000000
-#define EX_EXPORT     0x20000000
-#define EX_ARGSPACE   0x40000000
-
-#define EX_FILES (EX_XFILE | EX_EXTRA)
-#define EX_FILE1 (EX_FILES | EX_NOSPC)
-#define EX_WORD1 (EX_EXTRA | EX_NOSPC)
+enum { EX_RANGE = 0x001 };
+enum { EX_BANG = 0x002 };
+enum { EX_EXTRA = 0x004 };
+enum { EX_XFILE = 0x008 };
+enum { EX_NOSPC = 0x010 };
+enum { EX_DFLALL = 0x020 };
+enum { EX_WHOLEFOLD = 0x040 };
+enum { EX_NEEDARG = 0x080 };
+enum { EX_TRLBAR = 0x100 };
+enum { EX_REGSTR = 0x200 };
+enum { EX_COUNT = 0x400 };
+enum { EX_NOTRLCOM = 0x800 };
+enum { EX_ZEROR = 0x1000 };
+enum { EX_CTRLV = 0x2000 };
+enum { EX_CMDARG = 0x4000 };
+enum { EX_BUFNAME = 0x8000 };
+enum { EX_BUFUNL = 0x10000 };
+enum { EX_ARGOPT = 0x20000 };
+enum { EX_SBOXOK = 0x40000 };
+enum { EX_CMDWIN = 0x80000 };
+enum { EX_MODIFY = 0x100000 };
+enum { EX_FLAGS = 0x200000 };
+enum { EX_RESTRICT = 0x400000 };
+enum { EX_EXPAND = 0x800000 };
+enum { EX_LOCK_OK = 0x1000000 };
+enum { EX_NONWHITE_OK = 0x2000000 };
+enum { EX_KEEPSCRIPT = 0x4000000 };
+enum { EX_EXPR_ARG = 0x8000000 };
+enum { EX_WHOLE = 0x10000000 };
+enum { EX_EXPORT = 0x20000000 };
+enum { EX_ARGSPACE = 0x40000000 };
 
 typedef enum {
     ADDR_LINES,
@@ -5703,614 +4249,611 @@
 
 typedef struct exarg exarg_T;
 
-# define EXCMD(a, b, c, d, e)  a
 enum CMD_index
 {
-EXCMD(CMD_append,       "append",       ex_append, EX_BANG|EX_RANGE|EX_ZEROR|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK|EX_MODIFY, ADDR_LINES),
-EXCMD(CMD_abbreviate,   "abbreviate",   ex_abbreviate, EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_abclear,      "abclear",      ex_abclear, EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_aboveleft,    "aboveleft",    ex_wrongmodifier, EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM, ADDR_NONE),
-EXCMD(CMD_abstract,     "abstract",     ex_class, EX_EXTRA|EX_CMDWIN|EX_LOCK_OK|EX_WHOLE|EX_EXPORT, ADDR_NONE),
-EXCMD(CMD_all,          "all",          ex_all, EX_BANG|EX_RANGE|EX_COUNT|EX_TRLBAR, ADDR_OTHER),
-EXCMD(CMD_amenu,        "amenu",        ex_menu, EX_RANGE|EX_ZEROR|EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_OTHER),
-EXCMD(CMD_anoremenu,    "anoremenu",    ex_menu, EX_RANGE|EX_ZEROR|EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_OTHER),
-EXCMD(CMD_args,         "args",         ex_args, EX_BANG|EX_FILES|EX_CMDARG|EX_ARGOPT|EX_TRLBAR, ADDR_NONE),
-EXCMD(CMD_argadd,       "argadd",       ex_argadd, EX_BANG|EX_RANGE|EX_ZEROR|EX_FILES|EX_TRLBAR, ADDR_ARGUMENTS),
-EXCMD(CMD_argdelete,    "argdelete",    ex_argdelete, EX_BANG|EX_RANGE|EX_FILES|EX_TRLBAR, ADDR_ARGUMENTS),
-EXCMD(CMD_argdo,        "argdo",        ex_listdo, EX_BANG|EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM|EX_RANGE|EX_DFLALL|EX_EXPAND, ADDR_ARGUMENTS),
-EXCMD(CMD_argdedupe,    "argdedupe",    ex_argdedupe, EX_TRLBAR, ADDR_NONE),
-EXCMD(CMD_argedit,      "argedit",      ex_argedit, EX_BANG|EX_NEEDARG|EX_RANGE|EX_ZEROR|EX_FILES|EX_CMDARG|EX_ARGOPT|EX_TRLBAR, ADDR_ARGUMENTS),
-EXCMD(CMD_argglobal,    "argglobal",    ex_args, EX_BANG|EX_FILES|EX_CMDARG|EX_ARGOPT|EX_TRLBAR, ADDR_NONE),
-EXCMD(CMD_arglocal,     "arglocal",     ex_args, EX_BANG|EX_FILES|EX_CMDARG|EX_ARGOPT|EX_TRLBAR, ADDR_NONE),
-EXCMD(CMD_argument,     "argument",     ex_argument, EX_BANG|EX_RANGE|EX_COUNT|EX_EXTRA|EX_CMDARG|EX_ARGOPT|EX_TRLBAR, ADDR_ARGUMENTS),
-EXCMD(CMD_ascii,        "ascii",        do_ascii, EX_TRLBAR|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_autocmd,      "autocmd",      ex_autocmd, EX_BANG|EX_EXTRA|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_augroup,      "augroup",      ex_autocmd, EX_BANG|EX_WORD1|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_aunmenu,      "aunmenu",      ex_menu, EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_buffer,       "buffer",       ex_buffer, EX_BANG|EX_RANGE|EX_BUFNAME|EX_BUFUNL|EX_COUNT|EX_EXTRA|EX_CMDARG|EX_TRLBAR, ADDR_BUFFERS),
-EXCMD(CMD_bNext,        "bNext",        ex_bprevious, EX_BANG|EX_RANGE|EX_COUNT|EX_CMDARG|EX_TRLBAR, ADDR_OTHER),
-EXCMD(CMD_ball,         "ball",         ex_buffer_all, EX_RANGE|EX_COUNT|EX_TRLBAR, ADDR_OTHER),
-EXCMD(CMD_badd,         "badd",         ex_edit, EX_NEEDARG|EX_FILE1|EX_CMDARG|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_balt,         "balt",         ex_edit, EX_NEEDARG|EX_FILE1|EX_CMDARG|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_bdelete,      "bdelete",      ex_bunload, EX_BANG|EX_RANGE|EX_BUFNAME|EX_COUNT|EX_EXTRA|EX_TRLBAR, ADDR_BUFFERS),
-EXCMD(CMD_behave,       "behave",       ex_behave, EX_BANG|EX_NEEDARG|EX_WORD1|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_belowright,   "belowright",   ex_wrongmodifier, EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM, ADDR_NONE),
-EXCMD(CMD_bfirst,       "bfirst",       ex_brewind, EX_BANG|EX_RANGE|EX_CMDARG|EX_TRLBAR, ADDR_OTHER),
-EXCMD(CMD_blast,        "blast",        ex_blast, EX_BANG|EX_RANGE|EX_CMDARG|EX_TRLBAR, ADDR_OTHER),
-EXCMD(CMD_bmodified,    "bmodified",    ex_bmodified, EX_BANG|EX_RANGE|EX_COUNT|EX_CMDARG|EX_TRLBAR, ADDR_OTHER),
-EXCMD(CMD_bnext,        "bnext",        ex_bnext, EX_BANG|EX_RANGE|EX_COUNT|EX_CMDARG|EX_TRLBAR, ADDR_OTHER),
-EXCMD(CMD_botright,     "botright",     ex_wrongmodifier, EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM, ADDR_NONE),
-EXCMD(CMD_bprevious,    "bprevious",    ex_bprevious, EX_BANG|EX_RANGE|EX_COUNT|EX_CMDARG|EX_TRLBAR, ADDR_OTHER),
-EXCMD(CMD_brewind,      "brewind",      ex_brewind, EX_BANG|EX_RANGE|EX_CMDARG|EX_TRLBAR, ADDR_OTHER),
-EXCMD(CMD_break,        "break",        ex_break, EX_TRLBAR|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK|EX_WHOLE, ADDR_NONE),
-EXCMD(CMD_breakadd,     "breakadd",     ex_breakadd, EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_breakdel,     "breakdel",     ex_breakdel, EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_breaklist,    "breaklist",    ex_breaklist, EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_browse,       "browse",       ex_wrongmodifier, EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_buffers,      "buffers",      buflist_list, EX_BANG|EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_bufdo,        "bufdo",        ex_listdo, EX_BANG|EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM|EX_RANGE|EX_DFLALL|EX_EXPAND, ADDR_BUFFERS),
-EXCMD(CMD_bunload,      "bunload",      ex_bunload, EX_BANG|EX_RANGE|EX_BUFNAME|EX_COUNT|EX_EXTRA|EX_TRLBAR, ADDR_LOADED_BUFFERS),
-EXCMD(CMD_bwipeout,     "bwipeout",     ex_bunload, EX_BANG|EX_RANGE|EX_BUFNAME|EX_BUFUNL|EX_COUNT|EX_EXTRA|EX_TRLBAR, ADDR_BUFFERS),
-EXCMD(CMD_change,       "change",       ex_change, EX_BANG|EX_WHOLEFOLD|EX_RANGE|EX_COUNT|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK|EX_MODIFY, ADDR_LINES),
-EXCMD(CMD_cNext,        "cNext",        ex_cnext, EX_RANGE|EX_COUNT|EX_TRLBAR|EX_BANG, ADDR_UNSIGNED),
-EXCMD(CMD_cNfile,       "cNfile",       ex_cnext, EX_RANGE|EX_COUNT|EX_TRLBAR|EX_BANG, ADDR_UNSIGNED),
-EXCMD(CMD_cabbrev,      "cabbrev",      ex_abbreviate, EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_cabclear,     "cabclear",     ex_abclear, EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_cabove,       "cabove",       ex_cbelow, EX_RANGE|EX_COUNT|EX_TRLBAR, ADDR_UNSIGNED),
-EXCMD(CMD_caddbuffer,   "caddbuffer",   ex_cbuffer, EX_RANGE|EX_WORD1|EX_TRLBAR, ADDR_LINES),
-EXCMD(CMD_caddexpr,     "caddexpr",     ex_cexpr, EX_NEEDARG|EX_WORD1|EX_NOTRLCOM|EX_EXPR_ARG, ADDR_NONE),
-EXCMD(CMD_caddfile,     "caddfile",     ex_cfile, EX_TRLBAR|EX_FILE1, ADDR_NONE),
-EXCMD(CMD_cafter,       "cafter",       ex_cbelow, EX_RANGE|EX_COUNT|EX_TRLBAR, ADDR_UNSIGNED),
-EXCMD(CMD_call,         "call",         ex_call, EX_RANGE|EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM|EX_EXPR_ARG|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK, ADDR_LINES),
-EXCMD(CMD_catch,        "catch",        ex_catch, EX_EXTRA|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK|EX_WHOLE, ADDR_NONE),
-EXCMD(CMD_cbuffer,      "cbuffer",      ex_cbuffer, EX_BANG|EX_RANGE|EX_WORD1|EX_TRLBAR, ADDR_LINES),
-EXCMD(CMD_cbefore,      "cbefore",      ex_cbelow, EX_RANGE|EX_COUNT|EX_TRLBAR, ADDR_UNSIGNED),
-EXCMD(CMD_cbelow,       "cbelow",       ex_cbelow, EX_RANGE|EX_COUNT|EX_TRLBAR, ADDR_UNSIGNED),
-EXCMD(CMD_cbottom,      "cbottom",      ex_cbottom, EX_TRLBAR, ADDR_NONE),
-EXCMD(CMD_cc,           "cc",           ex_cc, EX_RANGE|EX_COUNT|EX_TRLBAR|EX_BANG, ADDR_QUICKFIX),
-EXCMD(CMD_cclose,       "cclose",       ex_cclose, EX_TRLBAR, ADDR_NONE),
-EXCMD(CMD_cd,           "cd",           ex_cd, EX_BANG|EX_FILE1|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_cdo,          "cdo",          ex_listdo, EX_BANG|EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM|EX_RANGE|EX_DFLALL|EX_EXPAND, ADDR_QUICKFIX_VALID),
-EXCMD(CMD_center,       "center",       ex_align, EX_TRLBAR|EX_RANGE|EX_WHOLEFOLD|EX_EXTRA|EX_CMDWIN|EX_LOCK_OK|EX_MODIFY, ADDR_LINES),
-EXCMD(CMD_cexpr,        "cexpr",        ex_cexpr, EX_NEEDARG|EX_WORD1|EX_NOTRLCOM|EX_EXPR_ARG|EX_BANG, ADDR_NONE),
-EXCMD(CMD_cfile,        "cfile",        ex_cfile, EX_TRLBAR|EX_FILE1|EX_BANG, ADDR_NONE),
-EXCMD(CMD_cfdo,         "cfdo",         ex_listdo, EX_BANG|EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM|EX_RANGE|EX_DFLALL|EX_EXPAND, ADDR_QUICKFIX_VALID),
-EXCMD(CMD_cfirst,       "cfirst",       ex_cc, EX_RANGE|EX_COUNT|EX_TRLBAR|EX_BANG, ADDR_UNSIGNED),
-EXCMD(CMD_cgetfile,     "cgetfile",     ex_cfile, EX_TRLBAR|EX_FILE1, ADDR_NONE),
-EXCMD(CMD_cgetbuffer,   "cgetbuffer",   ex_cbuffer, EX_RANGE|EX_WORD1|EX_TRLBAR, ADDR_LINES),
-EXCMD(CMD_cgetexpr,     "cgetexpr",     ex_cexpr, EX_NEEDARG|EX_WORD1|EX_NOTRLCOM|EX_EXPR_ARG, ADDR_NONE),
-EXCMD(CMD_chdir,        "chdir",        ex_cd, EX_BANG|EX_FILE1|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_changes,      "changes",      ex_changes, EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_checkpath,    "checkpath",    ex_checkpath, EX_TRLBAR|EX_BANG|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_checktime,    "checktime",    ex_checktime, EX_RANGE|EX_BUFNAME|EX_COUNT|EX_EXTRA|EX_TRLBAR, ADDR_OTHER),
-EXCMD(CMD_chistory,     "chistory",     qf_history, EX_RANGE|EX_COUNT|EX_TRLBAR, ADDR_UNSIGNED),
-EXCMD(CMD_clist,        "clist",        qf_list, EX_BANG|EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_clipreset,    "clipreset",    ex_clipreset, EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_clast,        "clast",        ex_cc, EX_RANGE|EX_COUNT|EX_TRLBAR|EX_BANG, ADDR_UNSIGNED),
-EXCMD(CMD_class,        "class",        ex_class, EX_EXTRA|EX_CMDWIN|EX_LOCK_OK|EX_WHOLE|EX_EXPORT, ADDR_NONE),
-EXCMD(CMD_close,        "close",        ex_close, EX_BANG|EX_RANGE|EX_COUNT|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_WINDOWS),
-EXCMD(CMD_clearjumps,   "clearjumps",   ex_clearjumps, EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_cmap,         "cmap",         ex_map, EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_cmapclear,    "cmapclear",    ex_mapclear, EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_cmenu,        "cmenu",        ex_menu, EX_RANGE|EX_ZEROR|EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_OTHER),
-EXCMD(CMD_cnext,        "cnext",        ex_cnext, EX_RANGE|EX_COUNT|EX_TRLBAR|EX_BANG, ADDR_UNSIGNED),
-EXCMD(CMD_cnewer,       "cnewer",       qf_age, EX_RANGE|EX_COUNT|EX_TRLBAR, ADDR_UNSIGNED),
-EXCMD(CMD_cnfile,       "cnfile",       ex_cnext, EX_RANGE|EX_COUNT|EX_TRLBAR|EX_BANG, ADDR_UNSIGNED),
-EXCMD(CMD_cnoremap,     "cnoremap",     ex_map, EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_cnoreabbrev,  "cnoreabbrev",  ex_abbreviate, EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_cnoremenu,    "cnoremenu",    ex_menu, EX_RANGE|EX_ZEROR|EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_OTHER),
-EXCMD(CMD_copy,         "copy",         ex_copymove, EX_RANGE|EX_WHOLEFOLD|EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK|EX_MODIFY, ADDR_LINES),
-EXCMD(CMD_colder,       "colder",       qf_age, EX_RANGE|EX_COUNT|EX_TRLBAR, ADDR_UNSIGNED),
-EXCMD(CMD_colorscheme,  "colorscheme",  ex_colorscheme, EX_WORD1|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_command,      "command",      ex_command, EX_EXTRA|EX_BANG|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_comclear,     "comclear",     ex_comclear, EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_compiler,     "compiler",     ex_compiler, EX_BANG|EX_TRLBAR|EX_WORD1|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_continue,     "continue",     ex_continue, EX_TRLBAR|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK|EX_WHOLE, ADDR_NONE),
-EXCMD(CMD_confirm,      "confirm",      ex_wrongmodifier, EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_const,        "const",        ex_let, EX_EXTRA|EX_BANG|EX_NOTRLCOM|EX_EXPR_ARG|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK|EX_WHOLE|EX_EXPORT, ADDR_NONE),
-EXCMD(CMD_copen,        "copen",        ex_copen, EX_RANGE|EX_COUNT|EX_TRLBAR, ADDR_OTHER),
-EXCMD(CMD_cprevious,    "cprevious",    ex_cnext, EX_RANGE|EX_COUNT|EX_TRLBAR|EX_BANG, ADDR_UNSIGNED),
-EXCMD(CMD_cpfile,       "cpfile",       ex_cnext, EX_RANGE|EX_COUNT|EX_TRLBAR|EX_BANG, ADDR_OTHER),
-EXCMD(CMD_cquit,        "cquit",        ex_cquit, EX_RANGE|EX_COUNT|EX_ZEROR|EX_TRLBAR|EX_BANG, ADDR_UNSIGNED),
-EXCMD(CMD_crewind,      "crewind",      ex_cc, EX_RANGE|EX_COUNT|EX_TRLBAR|EX_BANG, ADDR_UNSIGNED),
-EXCMD(CMD_cscope,       "cscope",       ex_cscope, EX_EXTRA|EX_NOTRLCOM|EX_XFILE, ADDR_NONE),
-EXCMD(CMD_cstag,        "cstag",        ex_cstag, EX_BANG|EX_TRLBAR|EX_WORD1, ADDR_NONE),
-EXCMD(CMD_cunmap,       "cunmap",       ex_unmap, EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_cunabbrev,    "cunabbrev",    ex_abbreviate, EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_cunmenu,      "cunmenu",      ex_menu, EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_cwindow,      "cwindow",      ex_cwindow, EX_RANGE|EX_COUNT|EX_TRLBAR, ADDR_OTHER),
-EXCMD(CMD_delete,       "delete",       ex_operators, EX_RANGE|EX_WHOLEFOLD|EX_REGSTR|EX_COUNT|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK|EX_MODIFY, ADDR_LINES),
-EXCMD(CMD_delmarks,     "delmarks",     ex_delmarks, EX_BANG|EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_debug,        "debug",        ex_debug, EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_debuggreedy,  "debuggreedy",  ex_debuggreedy, EX_RANGE|EX_ZEROR|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_OTHER),
-EXCMD(CMD_def,          "def",          ex_function, EX_EXTRA|EX_BANG|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK|EX_WHOLE|EX_EXPORT, ADDR_NONE),
-EXCMD(CMD_defcompile,   "defcompile",   ex_defcompile, EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK|EX_TRLBAR|EX_EXTRA, ADDR_NONE),
-EXCMD(CMD_defer,        "defer",        ex_call, EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM|EX_EXPR_ARG|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_delcommand,   "delcommand",   ex_delcommand, EX_NEEDARG|EX_WORD1|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_delfunction,  "delfunction",  ex_delfunction, EX_BANG|EX_NEEDARG|EX_WORD1|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_display,      "display",      ex_display, EX_EXTRA|EX_NOTRLCOM|EX_TRLBAR|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_diffupdate,   "diffupdate",   ex_diffupdate, EX_BANG|EX_TRLBAR, ADDR_NONE),
-EXCMD(CMD_diffget,      "diffget",      ex_diffgetput, EX_RANGE|EX_ZEROR|EX_EXTRA|EX_TRLBAR|EX_MODIFY, ADDR_LINES),
-EXCMD(CMD_diffoff,      "diffoff",      ex_diffoff, EX_BANG|EX_TRLBAR, ADDR_NONE),
-EXCMD(CMD_diffpatch,    "diffpatch",    ex_diffpatch, EX_EXTRA|EX_FILE1|EX_TRLBAR|EX_MODIFY, ADDR_NONE),
-EXCMD(CMD_diffput,      "diffput",      ex_diffgetput, EX_RANGE|EX_ZEROR|EX_EXTRA|EX_TRLBAR, ADDR_LINES),
-EXCMD(CMD_diffsplit,    "diffsplit",    ex_diffsplit, EX_EXTRA|EX_FILE1|EX_TRLBAR, ADDR_NONE),
-EXCMD(CMD_diffthis,     "diffthis",     ex_diffthis, EX_TRLBAR, ADDR_NONE),
-EXCMD(CMD_digraphs,     "digraphs",     ex_digraphs, EX_BANG|EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_disassemble,  "disassemble",  ex_disassemble, EX_BANG|EX_EXTRA|EX_NEEDARG|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_djump,        "djump",        ex_findpat, EX_BANG|EX_RANGE|EX_DFLALL|EX_WHOLEFOLD|EX_EXTRA, ADDR_LINES),
-EXCMD(CMD_dlist,        "dlist",        ex_findpat, EX_BANG|EX_RANGE|EX_DFLALL|EX_WHOLEFOLD|EX_EXTRA|EX_CMDWIN|EX_LOCK_OK, ADDR_LINES),
-EXCMD(CMD_doautocmd,    "doautocmd",    ex_doautocmd, EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_doautoall,    "doautoall",    ex_doautoall, EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_drop,         "drop",         ex_drop, EX_BANG|EX_FILES|EX_CMDARG|EX_NEEDARG|EX_ARGOPT|EX_TRLBAR, ADDR_NONE),
-EXCMD(CMD_dsearch,      "dsearch",      ex_findpat, EX_BANG|EX_RANGE|EX_DFLALL|EX_WHOLEFOLD|EX_EXTRA|EX_CMDWIN|EX_LOCK_OK, ADDR_LINES),
-EXCMD(CMD_dsplit,       "dsplit",       ex_findpat, EX_BANG|EX_RANGE|EX_DFLALL|EX_WHOLEFOLD|EX_EXTRA, ADDR_LINES),
-EXCMD(CMD_edit,         "edit",         ex_edit, EX_BANG|EX_FILE1|EX_CMDARG|EX_ARGOPT|EX_TRLBAR, ADDR_NONE),
-EXCMD(CMD_earlier,      "earlier",      ex_later, EX_TRLBAR|EX_EXTRA|EX_NOSPC|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_echo,         "echo",         ex_echo, EX_EXTRA|EX_NOTRLCOM|EX_EXPR_ARG|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_echoerr,      "echoerr",      ex_execute, EX_EXTRA|EX_NOTRLCOM|EX_EXPR_ARG|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_echohl,       "echohl",       ex_echohl, EX_EXTRA|EX_TRLBAR|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_echomsg,      "echomsg",      ex_execute, EX_EXTRA|EX_NOTRLCOM|EX_EXPR_ARG|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_echoconsole,  "echoconsole",  ex_execute, EX_EXTRA|EX_NOTRLCOM|EX_EXPR_ARG|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_echon,        "echon",        ex_echo, EX_EXTRA|EX_NOTRLCOM|EX_EXPR_ARG|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_echowindow,   "echowindow",   ex_execute, EX_RANGE|EX_EXTRA|EX_NOTRLCOM|EX_EXPR_ARG|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK, ADDR_OTHER),
-EXCMD(CMD_else,         "else",         ex_else, EX_TRLBAR|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK|EX_WHOLE, ADDR_NONE),
-EXCMD(CMD_elseif,       "elseif",       ex_else, EX_EXTRA|EX_NOTRLCOM|EX_EXPR_ARG|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK|EX_WHOLE, ADDR_NONE),
-EXCMD(CMD_emenu,        "emenu",        ex_emenu, EX_NEEDARG|EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_RANGE|EX_CMDWIN|EX_LOCK_OK, ADDR_OTHER),
-EXCMD(CMD_endif,        "endif",        ex_endif, EX_TRLBAR|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK|EX_WHOLE, ADDR_NONE),
-EXCMD(CMD_endinterface, "endinterface", ex_wrongmodifier, EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK|EX_WHOLE, ADDR_NONE),
-EXCMD(CMD_endclass,     "endclass",     ex_wrongmodifier, EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK|EX_WHOLE, ADDR_NONE),
-EXCMD(CMD_enddef,       "enddef",       ex_endfunction, EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK|EX_WHOLE, ADDR_NONE),
-EXCMD(CMD_endenum,      "endenum",      ex_wrongmodifier, EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK|EX_WHOLE, ADDR_NONE),
-EXCMD(CMD_endfunction,  "endfunction",  ex_endfunction, EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_endfor,       "endfor",       ex_endwhile, EX_TRLBAR|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK|EX_WHOLE, ADDR_NONE),
-EXCMD(CMD_endtry,       "endtry",       ex_endtry, EX_TRLBAR|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK|EX_WHOLE, ADDR_NONE),
-EXCMD(CMD_endwhile,     "endwhile",     ex_endwhile, EX_TRLBAR|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK|EX_WHOLE, ADDR_NONE),
-EXCMD(CMD_enew,         "enew",         ex_edit, EX_BANG|EX_TRLBAR, ADDR_NONE),
-EXCMD(CMD_enum,         "enum",         ex_class, EX_EXTRA|EX_CMDWIN|EX_LOCK_OK|EX_WHOLE|EX_EXPORT, ADDR_NONE),
-EXCMD(CMD_eval,         "eval",         ex_eval, EX_EXTRA|EX_NOTRLCOM|EX_EXPR_ARG|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_ex,           "ex",           ex_edit, EX_BANG|EX_FILE1|EX_CMDARG|EX_ARGOPT|EX_TRLBAR, ADDR_NONE),
-EXCMD(CMD_execute,      "execute",      ex_execute, EX_EXTRA|EX_NOTRLCOM|EX_EXPR_ARG|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_exit,         "exit",         ex_exit, EX_RANGE|EX_WHOLEFOLD|EX_BANG|EX_FILE1|EX_ARGOPT|EX_DFLALL|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_LINES),
-EXCMD(CMD_export,       "export",       ex_export, EX_EXTRA|EX_NOTRLCOM|EX_EXPR_ARG|EX_CMDWIN|EX_LOCK_OK|EX_WHOLE, ADDR_NONE),
-EXCMD(CMD_exusage,      "exusage",      ex_exusage, EX_TRLBAR, ADDR_NONE),
-EXCMD(CMD_file,         "file",         ex_file, EX_RANGE|EX_ZEROR|EX_BANG|EX_FILE1|EX_TRLBAR, ADDR_OTHER),
-EXCMD(CMD_files,        "files",        buflist_list, EX_BANG|EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_filetype,     "filetype",     ex_filetype, EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_filter,       "filter",       ex_wrongmodifier, EX_BANG|EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM, ADDR_NONE),
-EXCMD(CMD_find,         "find",         ex_find, EX_RANGE|EX_BANG|EX_FILE1|EX_CMDARG|EX_ARGOPT|EX_TRLBAR|EX_NEEDARG, ADDR_OTHER),
-EXCMD(CMD_final,        "final",        ex_let, EX_EXTRA|EX_NOTRLCOM|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK|EX_WHOLE|EX_EXPORT, ADDR_NONE),
-EXCMD(CMD_finally,      "finally",      ex_finally, EX_TRLBAR|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK|EX_WHOLE, ADDR_NONE),
-EXCMD(CMD_finish,       "finish",       ex_finish, EX_TRLBAR|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK|EX_WHOLE, ADDR_NONE),
-EXCMD(CMD_first,        "first",        ex_rewind, EX_EXTRA|EX_BANG|EX_CMDARG|EX_ARGOPT|EX_TRLBAR, ADDR_NONE),
-EXCMD(CMD_fixdel,       "fixdel",       do_fixdel, EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_fold,         "fold",         ex_fold, EX_RANGE|EX_WHOLEFOLD|EX_TRLBAR|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK, ADDR_LINES),
-EXCMD(CMD_foldclose,    "foldclose",    ex_foldopen, EX_RANGE|EX_BANG|EX_WHOLEFOLD|EX_TRLBAR|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK, ADDR_LINES),
-EXCMD(CMD_folddoopen,   "folddoopen",   ex_folddo, EX_RANGE|EX_DFLALL|EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM, ADDR_LINES),
-EXCMD(CMD_folddoclosed, "folddoclosed", ex_folddo, EX_RANGE|EX_DFLALL|EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM, ADDR_LINES),
-EXCMD(CMD_foldopen,     "foldopen",     ex_foldopen, EX_RANGE|EX_BANG|EX_WHOLEFOLD|EX_TRLBAR|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK, ADDR_LINES),
-EXCMD(CMD_for,          "for",          ex_while, EX_EXTRA|EX_NOTRLCOM|EX_EXPR_ARG|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_function,     "function",     ex_function, EX_EXTRA|EX_BANG|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK|EX_EXPORT, ADDR_NONE),
-EXCMD(CMD_global,       "global",       ex_global, EX_RANGE|EX_WHOLEFOLD|EX_BANG|EX_EXTRA|EX_DFLALL|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK|EX_NONWHITE_OK, ADDR_LINES),
-EXCMD(CMD_goto,         "goto",         ex_goto, EX_RANGE|EX_COUNT|EX_TRLBAR|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK, ADDR_OTHER),
-EXCMD(CMD_grep,         "grep",         ex_make, EX_RANGE|EX_BANG|EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM|EX_TRLBAR|EX_XFILE, ADDR_OTHER),
-EXCMD(CMD_grepadd,      "grepadd",      ex_make, EX_RANGE|EX_BANG|EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM|EX_TRLBAR|EX_XFILE, ADDR_OTHER),
-EXCMD(CMD_gui,          "gui",          ex_gui, EX_BANG|EX_FILES|EX_CMDARG|EX_ARGOPT|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_gvim,         "gvim",         ex_gui, EX_BANG|EX_FILES|EX_CMDARG|EX_ARGOPT|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_help,         "help",         ex_help, EX_BANG|EX_EXTRA|EX_NOTRLCOM, ADDR_NONE),
-EXCMD(CMD_helpclose,    "helpclose",    ex_helpclose, EX_TRLBAR, ADDR_NONE),
-EXCMD(CMD_helpfind,     "helpfind",     ex_helpfind, EX_EXTRA|EX_NOTRLCOM, ADDR_NONE),
-EXCMD(CMD_helpgrep,     "helpgrep",     ex_helpgrep, EX_EXTRA|EX_NOTRLCOM|EX_NEEDARG, ADDR_NONE),
-EXCMD(CMD_helptags,     "helptags",     ex_helptags, EX_NEEDARG|EX_FILES|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_hardcopy,     "hardcopy",     ex_hardcopy, EX_RANGE|EX_COUNT|EX_EXTRA|EX_EXPAND|EX_TRLBAR|EX_DFLALL|EX_BANG, ADDR_LINES),
-EXCMD(CMD_highlight,    "highlight",    ex_highlight, EX_BANG|EX_EXTRA|EX_TRLBAR|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_hide,         "hide",         ex_hide, EX_BANG|EX_RANGE|EX_COUNT|EX_EXTRA|EX_TRLBAR, ADDR_WINDOWS),
-EXCMD(CMD_history,      "history",      ex_history, EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_horizontal,   "horizontal",   ex_wrongmodifier, EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM, ADDR_NONE),
-EXCMD(CMD_insert,       "insert",       ex_append, EX_BANG|EX_RANGE|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK|EX_MODIFY, ADDR_LINES),
-EXCMD(CMD_iabbrev,      "iabbrev",      ex_abbreviate, EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_iabclear,     "iabclear",     ex_abclear, EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_if,           "if",           ex_if, EX_EXTRA|EX_NOTRLCOM|EX_EXPR_ARG|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_ijump,        "ijump",        ex_findpat, EX_BANG|EX_RANGE|EX_DFLALL|EX_WHOLEFOLD|EX_EXTRA, ADDR_LINES),
-EXCMD(CMD_ilist,        "ilist",        ex_findpat, EX_BANG|EX_RANGE|EX_DFLALL|EX_WHOLEFOLD|EX_EXTRA|EX_CMDWIN|EX_LOCK_OK, ADDR_LINES),
-EXCMD(CMD_imap,         "imap",         ex_map, EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_imapclear,    "imapclear",    ex_mapclear, EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_imenu,        "imenu",        ex_menu, EX_RANGE|EX_ZEROR|EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_OTHER),
-EXCMD(CMD_import,       "import",       ex_import, EX_EXTRA|EX_NOTRLCOM|EX_CMDWIN|EX_LOCK_OK|EX_WHOLE, ADDR_NONE),
-EXCMD(CMD_inoremap,     "inoremap",     ex_map, EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_inoreabbrev,  "inoreabbrev",  ex_abbreviate, EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_inoremenu,    "inoremenu",    ex_menu, EX_RANGE|EX_ZEROR|EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_OTHER),
-EXCMD(CMD_intro,        "intro",        ex_intro, EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_interface,    "interface",    ex_class, EX_EXTRA|EX_CMDWIN|EX_LOCK_OK|EX_WHOLE|EX_EXPORT, ADDR_NONE),
-EXCMD(CMD_iput,         "iput",         ex_iput, EX_RANGE|EX_WHOLEFOLD|EX_BANG|EX_REGSTR|EX_TRLBAR|EX_ZEROR|EX_CMDWIN|EX_LOCK_OK|EX_MODIFY, ADDR_LINES),
-EXCMD(CMD_isearch,      "isearch",      ex_findpat, EX_BANG|EX_RANGE|EX_DFLALL|EX_WHOLEFOLD|EX_EXTRA|EX_CMDWIN|EX_LOCK_OK, ADDR_LINES),
-EXCMD(CMD_isplit,       "isplit",       ex_findpat, EX_BANG|EX_RANGE|EX_DFLALL|EX_WHOLEFOLD|EX_EXTRA, ADDR_LINES),
-EXCMD(CMD_iunmap,       "iunmap",       ex_unmap, EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_iunabbrev,    "iunabbrev",    ex_abbreviate, EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_iunmenu,      "iunmenu",      ex_menu, EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_join,         "join",         ex_join, EX_BANG|EX_RANGE|EX_WHOLEFOLD|EX_COUNT|EX_FLAGS|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK|EX_MODIFY, ADDR_LINES),
-EXCMD(CMD_jumps,        "jumps",        ex_jumps, EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_k,            "k",            ex_mark, EX_RANGE|EX_WORD1|EX_TRLBAR|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK|EX_NONWHITE_OK, ADDR_LINES),
-EXCMD(CMD_keepmarks,    "keepmarks",    ex_wrongmodifier, EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM, ADDR_NONE),
-EXCMD(CMD_keepjumps,    "keepjumps",    ex_wrongmodifier, EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM, ADDR_NONE),
-EXCMD(CMD_keeppatterns, "keeppatterns", ex_wrongmodifier, EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM, ADDR_NONE),
-EXCMD(CMD_keepalt,      "keepalt",      ex_wrongmodifier, EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM, ADDR_NONE),
-EXCMD(CMD_list,         "list",         ex_print, EX_RANGE|EX_WHOLEFOLD|EX_COUNT|EX_FLAGS|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_LINES),
-EXCMD(CMD_lNext,        "lNext",        ex_cnext, EX_RANGE|EX_COUNT|EX_TRLBAR|EX_BANG, ADDR_UNSIGNED),
-EXCMD(CMD_lNfile,       "lNfile",       ex_cnext, EX_RANGE|EX_COUNT|EX_TRLBAR|EX_BANG, ADDR_UNSIGNED),
-EXCMD(CMD_last,         "last",         ex_last, EX_EXTRA|EX_BANG|EX_CMDARG|EX_ARGOPT|EX_TRLBAR, ADDR_NONE),
-EXCMD(CMD_labove,       "labove",       ex_cbelow, EX_RANGE|EX_COUNT|EX_TRLBAR, ADDR_UNSIGNED),
-EXCMD(CMD_language,     "language",     ex_language, EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_laddexpr,     "laddexpr",     ex_cexpr, EX_NEEDARG|EX_WORD1|EX_NOTRLCOM|EX_EXPR_ARG, ADDR_NONE),
-EXCMD(CMD_laddbuffer,   "laddbuffer",   ex_cbuffer, EX_RANGE|EX_WORD1|EX_TRLBAR, ADDR_LINES),
-EXCMD(CMD_laddfile,     "laddfile",     ex_cfile, EX_TRLBAR|EX_FILE1, ADDR_NONE),
-EXCMD(CMD_lafter,       "lafter",       ex_cbelow, EX_RANGE|EX_COUNT|EX_TRLBAR, ADDR_UNSIGNED),
-EXCMD(CMD_later,        "later",        ex_later, EX_TRLBAR|EX_EXTRA|EX_NOSPC|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_lbuffer,      "lbuffer",      ex_cbuffer, EX_BANG|EX_RANGE|EX_WORD1|EX_TRLBAR, ADDR_LINES),
-EXCMD(CMD_lbefore,      "lbefore",      ex_cbelow, EX_RANGE|EX_COUNT|EX_TRLBAR, ADDR_UNSIGNED),
-EXCMD(CMD_lbelow,       "lbelow",       ex_cbelow, EX_RANGE|EX_COUNT|EX_TRLBAR, ADDR_UNSIGNED),
-EXCMD(CMD_lbottom,      "lbottom",      ex_cbottom, EX_TRLBAR, ADDR_NONE),
-EXCMD(CMD_lcd,          "lcd",          ex_cd, EX_BANG|EX_FILE1|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_lchdir,       "lchdir",       ex_cd, EX_BANG|EX_FILE1|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_lclose,       "lclose",       ex_cclose, EX_RANGE|EX_COUNT|EX_TRLBAR, ADDR_OTHER),
-EXCMD(CMD_lcscope,      "lcscope",      ex_cscope, EX_EXTRA|EX_NOTRLCOM|EX_XFILE, ADDR_NONE),
-EXCMD(CMD_ldo,          "ldo",          ex_listdo, EX_BANG|EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM|EX_RANGE|EX_DFLALL|EX_EXPAND, ADDR_QUICKFIX_VALID),
-EXCMD(CMD_left,         "left",         ex_align, EX_TRLBAR|EX_RANGE|EX_WHOLEFOLD|EX_EXTRA|EX_CMDWIN|EX_LOCK_OK|EX_MODIFY, ADDR_LINES),
-EXCMD(CMD_leftabove,    "leftabove",    ex_wrongmodifier, EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM, ADDR_NONE),
-EXCMD(CMD_let,          "let",          ex_let, EX_EXTRA|EX_NOTRLCOM|EX_EXPR_ARG|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_lexpr,        "lexpr",        ex_cexpr, EX_NEEDARG|EX_WORD1|EX_NOTRLCOM|EX_EXPR_ARG|EX_BANG, ADDR_NONE),
-EXCMD(CMD_legacy,       "legacy",       ex_wrongmodifier, EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_lfile,        "lfile",        ex_cfile, EX_TRLBAR|EX_FILE1|EX_BANG, ADDR_NONE),
-EXCMD(CMD_lfdo,         "lfdo",         ex_listdo, EX_BANG|EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM|EX_RANGE|EX_DFLALL|EX_EXPAND, ADDR_QUICKFIX_VALID),
-EXCMD(CMD_lfirst,       "lfirst",       ex_cc, EX_RANGE|EX_COUNT|EX_TRLBAR|EX_BANG, ADDR_UNSIGNED),
-EXCMD(CMD_lgetfile,     "lgetfile",     ex_cfile, EX_TRLBAR|EX_FILE1, ADDR_NONE),
-EXCMD(CMD_lgetbuffer,   "lgetbuffer",   ex_cbuffer, EX_RANGE|EX_WORD1|EX_TRLBAR, ADDR_LINES),
-EXCMD(CMD_lgetexpr,     "lgetexpr",     ex_cexpr, EX_NEEDARG|EX_WORD1|EX_NOTRLCOM|EX_EXPR_ARG, ADDR_NONE),
-EXCMD(CMD_lgrep,        "lgrep",        ex_make, EX_RANGE|EX_BANG|EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM|EX_TRLBAR|EX_XFILE, ADDR_OTHER),
-EXCMD(CMD_lgrepadd,     "lgrepadd",     ex_make, EX_RANGE|EX_BANG|EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM|EX_TRLBAR|EX_XFILE, ADDR_OTHER),
-EXCMD(CMD_lhelpgrep,    "lhelpgrep",    ex_helpgrep, EX_EXTRA|EX_NOTRLCOM|EX_NEEDARG, ADDR_NONE),
-EXCMD(CMD_lhistory,     "lhistory",     qf_history, EX_RANGE|EX_COUNT|EX_TRLBAR, ADDR_UNSIGNED),
-EXCMD(CMD_ll,           "ll",           ex_cc, EX_RANGE|EX_COUNT|EX_TRLBAR|EX_BANG, ADDR_QUICKFIX),
-EXCMD(CMD_llast,        "llast",        ex_cc, EX_RANGE|EX_COUNT|EX_TRLBAR|EX_BANG, ADDR_UNSIGNED),
-EXCMD(CMD_llist,        "llist",        qf_list, EX_BANG|EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_lmap,         "lmap",         ex_map, EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_lmapclear,    "lmapclear",    ex_mapclear, EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_lmake,        "lmake",        ex_make, EX_BANG|EX_EXTRA|EX_NOTRLCOM|EX_TRLBAR|EX_XFILE, ADDR_NONE),
-EXCMD(CMD_lnoremap,     "lnoremap",     ex_map, EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_lnext,        "lnext",        ex_cnext, EX_RANGE|EX_COUNT|EX_TRLBAR|EX_BANG, ADDR_UNSIGNED),
-EXCMD(CMD_lnewer,       "lnewer",       qf_age, EX_RANGE|EX_COUNT|EX_TRLBAR, ADDR_UNSIGNED),
-EXCMD(CMD_lnfile,       "lnfile",       ex_cnext, EX_RANGE|EX_COUNT|EX_TRLBAR|EX_BANG, ADDR_UNSIGNED),
-EXCMD(CMD_loadview,     "loadview",     ex_loadview, EX_FILE1|EX_TRLBAR, ADDR_NONE),
-EXCMD(CMD_loadkeymap,   "loadkeymap",   ex_loadkeymap, EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_lockmarks,    "lockmarks",    ex_wrongmodifier, EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM, ADDR_NONE),
-EXCMD(CMD_lockvar,      "lockvar",      ex_lockvar, EX_BANG|EX_EXTRA|EX_NEEDARG|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_lolder,       "lolder",       qf_age, EX_RANGE|EX_COUNT|EX_TRLBAR, ADDR_UNSIGNED),
-EXCMD(CMD_lopen,        "lopen",        ex_copen, EX_RANGE|EX_COUNT|EX_TRLBAR, ADDR_OTHER),
-EXCMD(CMD_lprevious,    "lprevious",    ex_cnext, EX_RANGE|EX_COUNT|EX_TRLBAR|EX_BANG, ADDR_UNSIGNED),
-EXCMD(CMD_lpfile,       "lpfile",       ex_cnext, EX_RANGE|EX_COUNT|EX_TRLBAR|EX_BANG, ADDR_OTHER),
-EXCMD(CMD_lrewind,      "lrewind",      ex_cc, EX_RANGE|EX_COUNT|EX_TRLBAR|EX_BANG, ADDR_UNSIGNED),
-EXCMD(CMD_ltag,         "ltag", ex_tag, EX_TRLBAR|EX_BANG|EX_WORD1, ADDR_NONE),
-EXCMD(CMD_lunmap,       "lunmap",       ex_unmap, EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_lua,          "lua",          ex_lua, EX_RANGE|EX_EXTRA|EX_NEEDARG|EX_CMDWIN|EX_LOCK_OK|EX_RESTRICT, ADDR_LINES),
-EXCMD(CMD_luado,        "luado",        ex_luado, EX_RANGE|EX_DFLALL|EX_EXTRA|EX_NEEDARG|EX_CMDWIN|EX_LOCK_OK|EX_RESTRICT, ADDR_LINES),
-EXCMD(CMD_luafile,      "luafile",      ex_luafile, EX_RANGE|EX_FILE1|EX_NEEDARG|EX_CMDWIN|EX_LOCK_OK|EX_RESTRICT, ADDR_LINES),
-EXCMD(CMD_lvimgrep,     "lvimgrep",     ex_vimgrep, EX_RANGE|EX_BANG|EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM|EX_TRLBAR|EX_XFILE|EX_LOCK_OK, ADDR_OTHER),
-EXCMD(CMD_lvimgrepadd,  "lvimgrepadd",  ex_vimgrep, EX_RANGE|EX_BANG|EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM|EX_TRLBAR|EX_XFILE|EX_LOCK_OK, ADDR_OTHER),
-EXCMD(CMD_lwindow,      "lwindow",      ex_cwindow, EX_RANGE|EX_COUNT|EX_TRLBAR, ADDR_OTHER),
-EXCMD(CMD_ls,           "ls",           buflist_list, EX_BANG|EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_move,         "move",         ex_copymove, EX_RANGE|EX_WHOLEFOLD|EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK|EX_MODIFY, ADDR_LINES),
-EXCMD(CMD_mark,         "mark",         ex_mark, EX_RANGE|EX_WORD1|EX_TRLBAR|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK, ADDR_LINES),
-EXCMD(CMD_make,         "make",         ex_make, EX_BANG|EX_EXTRA|EX_NOTRLCOM|EX_TRLBAR|EX_XFILE, ADDR_NONE),
-EXCMD(CMD_map,          "map",          ex_map, EX_BANG|EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_mapclear,     "mapclear",     ex_mapclear, EX_EXTRA|EX_BANG|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_marks,        "marks",        ex_marks, EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_match,        "match",        ex_match, EX_RANGE|EX_EXTRA|EX_CMDWIN|EX_LOCK_OK, ADDR_OTHER),
-EXCMD(CMD_menu,         "menu",         ex_menu, EX_RANGE|EX_ZEROR|EX_BANG|EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_OTHER),
-EXCMD(CMD_menutranslate, "menutranslate", ex_menutranslate, EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_messages,     "messages",     ex_messages, EX_EXTRA|EX_TRLBAR|EX_RANGE|EX_CMDWIN|EX_LOCK_OK, ADDR_OTHER),
-EXCMD(CMD_mkexrc,       "mkexrc",       ex_mkrc, EX_BANG|EX_FILE1|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_mksession,    "mksession",    ex_mkrc, EX_BANG|EX_FILE1|EX_TRLBAR, ADDR_NONE),
-EXCMD(CMD_mkspell,      "mkspell",      ex_mkspell, EX_BANG|EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM|EX_TRLBAR|EX_XFILE, ADDR_NONE),
-EXCMD(CMD_mkvimrc,      "mkvimrc",      ex_mkrc, EX_BANG|EX_FILE1|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_mkview,       "mkview",       ex_mkrc, EX_BANG|EX_FILE1|EX_TRLBAR, ADDR_NONE),
-EXCMD(CMD_mode,         "mode",         ex_mode, EX_WORD1|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_mzscheme,     "mzscheme",     ex_mzscheme, EX_RANGE|EX_EXTRA|EX_DFLALL|EX_NEEDARG|EX_CMDWIN|EX_LOCK_OK|EX_SBOXOK|EX_RESTRICT, ADDR_LINES),
-EXCMD(CMD_mzfile,       "mzfile",       ex_mzfile, EX_RANGE|EX_FILE1|EX_NEEDARG|EX_CMDWIN|EX_LOCK_OK|EX_RESTRICT, ADDR_LINES),
-EXCMD(CMD_next,         "next",         ex_next, EX_RANGE|EX_BANG|EX_FILES|EX_CMDARG|EX_ARGOPT|EX_TRLBAR, ADDR_OTHER),
-EXCMD(CMD_nbkey,        "nbkey",        ex_nbkey, EX_EXTRA|EX_NEEDARG, ADDR_NONE),
-EXCMD(CMD_nbclose,      "nbclose",      ex_nbclose, EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_nbstart,      "nbstart",      ex_nbstart, EX_WORD1|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_new,          "new",          ex_splitview, EX_BANG|EX_FILE1|EX_RANGE|EX_CMDARG|EX_ARGOPT|EX_TRLBAR, ADDR_OTHER),
-EXCMD(CMD_nmap,         "nmap",         ex_map, EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_nmapclear,    "nmapclear",    ex_mapclear, EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_nmenu,        "nmenu",        ex_menu, EX_RANGE|EX_ZEROR|EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_OTHER),
-EXCMD(CMD_nnoremap,     "nnoremap",     ex_map, EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_nnoremenu,    "nnoremenu",    ex_menu, EX_RANGE|EX_ZEROR|EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_OTHER),
-EXCMD(CMD_noremap,      "noremap",      ex_map, EX_BANG|EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_noautocmd,    "noautocmd",    ex_wrongmodifier, EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM, ADDR_NONE),
-EXCMD(CMD_nohlsearch,   "nohlsearch",   ex_nohlsearch, EX_TRLBAR|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_noreabbrev,   "noreabbrev",   ex_abbreviate, EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_noremenu,     "noremenu",     ex_menu, EX_RANGE|EX_ZEROR|EX_BANG|EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_OTHER),
-EXCMD(CMD_noswapfile,   "noswapfile",   ex_wrongmodifier, EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM, ADDR_NONE),
-EXCMD(CMD_normal,       "normal",       ex_normal, EX_RANGE|EX_BANG|EX_EXTRA|EX_NEEDARG|EX_NOTRLCOM|EX_CTRLV|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK, ADDR_LINES),
-EXCMD(CMD_number,       "number",       ex_print, EX_RANGE|EX_WHOLEFOLD|EX_COUNT|EX_FLAGS|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_LINES),
-EXCMD(CMD_nunmap,       "nunmap",       ex_unmap, EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_nunmenu,      "nunmenu",      ex_menu, EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_open,         "open",         ex_open, EX_RANGE|EX_BANG|EX_EXTRA, ADDR_LINES),
-EXCMD(CMD_oldfiles,     "oldfiles",     ex_oldfiles, EX_BANG|EX_TRLBAR|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_omap,         "omap",         ex_map, EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_omapclear,    "omapclear",    ex_mapclear, EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_omenu,        "omenu",        ex_menu, EX_RANGE|EX_ZEROR|EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_OTHER),
-EXCMD(CMD_only,         "only",         ex_only, EX_BANG|EX_RANGE|EX_COUNT|EX_TRLBAR, ADDR_WINDOWS),
-EXCMD(CMD_onoremap,     "onoremap",     ex_map, EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_onoremenu,    "onoremenu",    ex_menu, EX_RANGE|EX_ZEROR|EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_OTHER),
-EXCMD(CMD_options,      "options",      ex_options, EX_TRLBAR, ADDR_NONE),
-EXCMD(CMD_ounmap,       "ounmap",       ex_unmap, EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_ounmenu,      "ounmenu",      ex_menu, EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_ownsyntax,    "ownsyntax",    ex_ownsyntax, EX_EXTRA|EX_NOTRLCOM|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_print,        "print",        ex_print, EX_RANGE|EX_WHOLEFOLD|EX_COUNT|EX_FLAGS|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK|EX_SBOXOK, ADDR_LINES),
-EXCMD(CMD_packadd,      "packadd",      ex_packadd, EX_BANG|EX_FILE1|EX_NEEDARG|EX_TRLBAR|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_packloadall,  "packloadall",  ex_packloadall, EX_BANG|EX_TRLBAR|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_pbuffer,      "pbuffer",      ex_pbuffer, EX_BANG|EX_RANGE|EX_BUFNAME|EX_BUFUNL|EX_COUNT|EX_EXTRA|EX_CMDARG|EX_TRLBAR, ADDR_BUFFERS),
-EXCMD(CMD_pclose,       "pclose",       ex_pclose, EX_BANG|EX_TRLBAR, ADDR_NONE),
-EXCMD(CMD_perl,         "perl",         ex_perl, EX_RANGE|EX_EXTRA|EX_DFLALL|EX_NEEDARG|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK|EX_RESTRICT, ADDR_LINES),
-EXCMD(CMD_perldo,       "perldo",       ex_perldo, EX_RANGE|EX_EXTRA|EX_DFLALL|EX_NEEDARG|EX_CMDWIN|EX_LOCK_OK|EX_RESTRICT, ADDR_LINES),
-EXCMD(CMD_pedit,        "pedit",        ex_pedit, EX_BANG|EX_FILE1|EX_CMDARG|EX_ARGOPT|EX_TRLBAR, ADDR_NONE),
-EXCMD(CMD_pop,          "pop",          ex_tag, EX_RANGE|EX_BANG|EX_COUNT|EX_TRLBAR|EX_ZEROR, ADDR_OTHER),
-EXCMD(CMD_popup,        "popup",        ex_popup, EX_NEEDARG|EX_EXTRA|EX_BANG|EX_TRLBAR|EX_NOTRLCOM|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_ppop,         "ppop",         ex_ptag, EX_RANGE|EX_BANG|EX_COUNT|EX_TRLBAR|EX_ZEROR, ADDR_OTHER),
-EXCMD(CMD_preserve,     "preserve",     ex_preserve, EX_TRLBAR, ADDR_NONE),
-EXCMD(CMD_previous,     "previous",     ex_previous, EX_EXTRA|EX_RANGE|EX_COUNT|EX_BANG|EX_CMDARG|EX_ARGOPT|EX_TRLBAR, ADDR_OTHER),
-EXCMD(CMD_promptfind,   "promptfind",   gui_mch_find_dialog, EX_EXTRA|EX_NOTRLCOM|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_promptrepl,   "promptrepl",   gui_mch_replace_dialog, EX_EXTRA|EX_NOTRLCOM|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_profile,      "profile",      ex_profile, EX_BANG|EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_profdel,      "profdel",      ex_breakdel, EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_psearch,      "psearch",      ex_psearch, EX_BANG|EX_RANGE|EX_WHOLEFOLD|EX_DFLALL|EX_EXTRA, ADDR_LINES),
-EXCMD(CMD_ptag,         "ptag",         ex_ptag, EX_RANGE|EX_BANG|EX_WORD1|EX_TRLBAR|EX_ZEROR, ADDR_OTHER),
-EXCMD(CMD_ptNext,       "ptNext",       ex_ptag, EX_RANGE|EX_BANG|EX_TRLBAR|EX_ZEROR, ADDR_OTHER),
-EXCMD(CMD_ptfirst,      "ptfirst",      ex_ptag, EX_RANGE|EX_BANG|EX_TRLBAR|EX_ZEROR, ADDR_OTHER),
-EXCMD(CMD_ptjump,       "ptjump",       ex_ptag, EX_BANG|EX_TRLBAR|EX_WORD1, ADDR_NONE),
-EXCMD(CMD_ptlast,       "ptlast",       ex_ptag, EX_BANG|EX_TRLBAR, ADDR_NONE),
-EXCMD(CMD_ptnext,       "ptnext",       ex_ptag, EX_RANGE|EX_BANG|EX_TRLBAR|EX_ZEROR, ADDR_OTHER),
-EXCMD(CMD_ptprevious,   "ptprevious",   ex_ptag, EX_RANGE|EX_BANG|EX_TRLBAR|EX_ZEROR, ADDR_OTHER),
-EXCMD(CMD_ptrewind,     "ptrewind",     ex_ptag, EX_RANGE|EX_BANG|EX_TRLBAR|EX_ZEROR, ADDR_OTHER),
-EXCMD(CMD_ptselect,     "ptselect",     ex_ptag, EX_BANG|EX_TRLBAR|EX_WORD1, ADDR_NONE),
-EXCMD(CMD_put,          "put",          ex_put, EX_RANGE|EX_WHOLEFOLD|EX_BANG|EX_REGSTR|EX_TRLBAR|EX_ZEROR|EX_CMDWIN|EX_LOCK_OK|EX_MODIFY, ADDR_LINES),
-EXCMD(CMD_public,       "public",       ex_wrongmodifier, EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK|EX_WHOLE, ADDR_NONE),
-EXCMD(CMD_pwd,          "pwd",          ex_pwd, EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_python,       "python",       ex_python, EX_RANGE|EX_EXTRA|EX_NEEDARG|EX_CMDWIN|EX_LOCK_OK|EX_RESTRICT, ADDR_LINES),
-EXCMD(CMD_pydo,         "pydo",         ex_pydo, EX_RANGE|EX_DFLALL|EX_EXTRA|EX_NEEDARG|EX_CMDWIN|EX_LOCK_OK|EX_RESTRICT, ADDR_LINES),
-EXCMD(CMD_pyfile,       "pyfile",       ex_pyfile, EX_RANGE|EX_FILE1|EX_NEEDARG|EX_CMDWIN|EX_LOCK_OK|EX_RESTRICT, ADDR_LINES),
-EXCMD(CMD_py3,          "py3",          ex_py3, EX_RANGE|EX_EXTRA|EX_NEEDARG|EX_CMDWIN|EX_LOCK_OK|EX_RESTRICT, ADDR_LINES),
-EXCMD(CMD_py3do,        "py3do",        ex_py3do, EX_RANGE|EX_DFLALL|EX_EXTRA|EX_NEEDARG|EX_CMDWIN|EX_LOCK_OK|EX_RESTRICT, ADDR_LINES),
-EXCMD(CMD_python3,      "python3",      ex_py3, EX_RANGE|EX_EXTRA|EX_NEEDARG|EX_CMDWIN|EX_LOCK_OK|EX_RESTRICT, ADDR_LINES),
-EXCMD(CMD_py3file,      "py3file",      ex_py3file, EX_RANGE|EX_FILE1|EX_NEEDARG|EX_CMDWIN|EX_LOCK_OK|EX_RESTRICT, ADDR_LINES),
-EXCMD(CMD_pyx,          "pyx",          ex_pyx, EX_RANGE|EX_EXTRA|EX_NEEDARG|EX_CMDWIN|EX_LOCK_OK|EX_RESTRICT, ADDR_LINES),
-EXCMD(CMD_pyxdo,        "pyxdo",        ex_pyxdo, EX_RANGE|EX_DFLALL|EX_EXTRA|EX_NEEDARG|EX_CMDWIN|EX_LOCK_OK|EX_RESTRICT, ADDR_LINES),
-EXCMD(CMD_pythonx,      "pythonx",      ex_pyx, EX_RANGE|EX_EXTRA|EX_NEEDARG|EX_CMDWIN|EX_LOCK_OK|EX_RESTRICT, ADDR_LINES),
-EXCMD(CMD_pyxfile,      "pyxfile",      ex_pyxfile, EX_RANGE|EX_FILE1|EX_NEEDARG|EX_CMDWIN|EX_LOCK_OK|EX_RESTRICT, ADDR_LINES),
-EXCMD(CMD_quit,         "quit",         ex_quit, EX_BANG|EX_RANGE|EX_COUNT|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_WINDOWS),
-EXCMD(CMD_quitall,      "quitall",      ex_quit_all, EX_BANG|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_qall,         "qall",         ex_quit_all, EX_BANG|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_read,         "read",         ex_read, EX_BANG|EX_RANGE|EX_WHOLEFOLD|EX_FILE1|EX_ARGOPT|EX_TRLBAR|EX_ZEROR|EX_CMDWIN|EX_LOCK_OK|EX_MODIFY, ADDR_LINES),
-EXCMD(CMD_recover,      "recover",      ex_recover, EX_BANG|EX_FILE1|EX_TRLBAR, ADDR_NONE),
-EXCMD(CMD_redo,         "redo",         ex_redo, EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_redir,        "redir",        ex_redir, EX_BANG|EX_FILES|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_redraw,       "redraw",       ex_redraw, EX_BANG|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_redrawstatus, "redrawstatus", ex_redrawstatus, EX_BANG|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_redrawtabline, "redrawtabline", ex_redrawtabline, EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_redrawtabpanel, "redrawtabpanel", ex_redrawtabpanel, EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_registers,    "registers",    ex_display, EX_EXTRA|EX_NOTRLCOM|EX_TRLBAR|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_resize,       "resize",       ex_resize, EX_RANGE|EX_TRLBAR|EX_WORD1|EX_CMDWIN|EX_LOCK_OK, ADDR_OTHER),
-EXCMD(CMD_retab,        "retab",        ex_retab, EX_TRLBAR|EX_RANGE|EX_WHOLEFOLD|EX_DFLALL|EX_BANG|EX_WORD1|EX_CMDWIN|EX_LOCK_OK|EX_MODIFY, ADDR_LINES),
-EXCMD(CMD_return,       "return",       ex_return, EX_EXTRA|EX_NOTRLCOM|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK|EX_WHOLE, ADDR_NONE),
-EXCMD(CMD_rewind,       "rewind",       ex_rewind, EX_EXTRA|EX_BANG|EX_CMDARG|EX_ARGOPT|EX_TRLBAR, ADDR_NONE),
-EXCMD(CMD_right,        "right",        ex_align, EX_TRLBAR|EX_RANGE|EX_WHOLEFOLD|EX_EXTRA|EX_CMDWIN|EX_LOCK_OK|EX_MODIFY, ADDR_LINES),
-EXCMD(CMD_rightbelow,   "rightbelow",   ex_wrongmodifier, EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM, ADDR_NONE),
-EXCMD(CMD_runtime,      "runtime",      ex_runtime, EX_BANG|EX_NEEDARG|EX_FILES|EX_TRLBAR|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_ruby,         "ruby",         ex_ruby, EX_RANGE|EX_EXTRA|EX_NEEDARG|EX_CMDWIN|EX_LOCK_OK|EX_RESTRICT, ADDR_LINES),
-EXCMD(CMD_rubydo,       "rubydo",       ex_rubydo, EX_RANGE|EX_DFLALL|EX_EXTRA|EX_NEEDARG|EX_CMDWIN|EX_LOCK_OK|EX_RESTRICT, ADDR_LINES),
-EXCMD(CMD_rubyfile,     "rubyfile",     ex_rubyfile, EX_RANGE|EX_FILE1|EX_NEEDARG|EX_CMDWIN|EX_LOCK_OK|EX_RESTRICT, ADDR_LINES),
-EXCMD(CMD_rundo,        "rundo",        ex_rundo, EX_NEEDARG|EX_FILE1, ADDR_NONE),
-EXCMD(CMD_rviminfo,     "rviminfo",     ex_viminfo, EX_BANG|EX_FILE1|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_substitute,   "substitute",   ex_substitute, EX_RANGE|EX_WHOLEFOLD|EX_EXTRA|EX_CMDWIN|EX_LOCK_OK|EX_NONWHITE_OK, ADDR_LINES),
-EXCMD(CMD_sNext,        "sNext",        ex_previous, EX_EXTRA|EX_RANGE|EX_COUNT|EX_BANG|EX_CMDARG|EX_ARGOPT|EX_TRLBAR, ADDR_OTHER),
-EXCMD(CMD_sargument,    "sargument",    ex_argument, EX_BANG|EX_RANGE|EX_COUNT|EX_EXTRA|EX_CMDARG|EX_ARGOPT|EX_TRLBAR, ADDR_ARGUMENTS),
-EXCMD(CMD_sall,         "sall",         ex_all, EX_BANG|EX_RANGE|EX_COUNT|EX_TRLBAR, ADDR_OTHER),
-EXCMD(CMD_sandbox,      "sandbox",      ex_wrongmodifier, EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM, ADDR_NONE),
-EXCMD(CMD_saveas,       "saveas",       ex_write, EX_BANG|EX_FILE1|EX_ARGOPT|EX_CMDWIN|EX_LOCK_OK|EX_TRLBAR, ADDR_NONE),
-EXCMD(CMD_sbuffer,      "sbuffer",      ex_buffer, EX_BANG|EX_RANGE|EX_BUFNAME|EX_BUFUNL|EX_COUNT|EX_EXTRA|EX_CMDARG|EX_TRLBAR, ADDR_BUFFERS),
-EXCMD(CMD_sbNext,       "sbNext",       ex_bprevious, EX_RANGE|EX_COUNT|EX_CMDARG|EX_TRLBAR, ADDR_OTHER),
-EXCMD(CMD_sball,        "sball",        ex_buffer_all, EX_RANGE|EX_COUNT|EX_CMDARG|EX_TRLBAR, ADDR_OTHER),
-EXCMD(CMD_sbfirst,      "sbfirst",      ex_brewind, EX_CMDARG|EX_TRLBAR, ADDR_NONE),
-EXCMD(CMD_sblast,       "sblast",       ex_blast, EX_CMDARG|EX_TRLBAR, ADDR_NONE),
-EXCMD(CMD_sbmodified,   "sbmodified",   ex_bmodified, EX_RANGE|EX_COUNT|EX_CMDARG|EX_TRLBAR, ADDR_OTHER),
-EXCMD(CMD_sbnext,       "sbnext",       ex_bnext, EX_RANGE|EX_COUNT|EX_CMDARG|EX_TRLBAR, ADDR_OTHER),
-EXCMD(CMD_sbprevious,   "sbprevious",   ex_bprevious, EX_RANGE|EX_COUNT|EX_CMDARG|EX_TRLBAR, ADDR_OTHER),
-EXCMD(CMD_sbrewind,     "sbrewind",     ex_brewind, EX_CMDARG|EX_TRLBAR, ADDR_NONE),
-EXCMD(CMD_scriptnames,  "scriptnames",  ex_scriptnames, EX_BANG|EX_FILES|EX_RANGE|EX_COUNT|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_OTHER),
-EXCMD(CMD_scriptencoding, "scriptencoding", ex_scriptencoding, EX_WORD1|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_scriptversion, "scriptversion", ex_scriptversion, EX_WORD1|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_scscope,      "scscope",      ex_scscope, EX_EXTRA|EX_NOTRLCOM, ADDR_NONE),
-EXCMD(CMD_set,          "set",          ex_set, EX_BANG|EX_TRLBAR|EX_EXTRA|EX_CMDWIN|EX_LOCK_OK|EX_SBOXOK, ADDR_NONE),
-EXCMD(CMD_setfiletype,  "setfiletype",  ex_setfiletype, EX_TRLBAR|EX_EXTRA|EX_NEEDARG|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_setglobal,    "setglobal",    ex_set, EX_BANG|EX_TRLBAR|EX_EXTRA|EX_CMDWIN|EX_LOCK_OK|EX_SBOXOK, ADDR_NONE),
-EXCMD(CMD_setlocal,     "setlocal",     ex_set, EX_BANG|EX_TRLBAR|EX_EXTRA|EX_CMDWIN|EX_LOCK_OK|EX_SBOXOK, ADDR_NONE),
-EXCMD(CMD_sfind,        "sfind",        ex_splitview, EX_BANG|EX_FILE1|EX_RANGE|EX_CMDARG|EX_ARGOPT|EX_TRLBAR|EX_NEEDARG, ADDR_OTHER),
-EXCMD(CMD_sfirst,       "sfirst",       ex_rewind, EX_EXTRA|EX_BANG|EX_CMDARG|EX_ARGOPT|EX_TRLBAR, ADDR_NONE),
-EXCMD(CMD_shell,        "shell",        ex_shell, EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_simalt,       "simalt",       ex_simalt, EX_NEEDARG|EX_WORD1|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_sign,         "sign",         ex_sign, EX_NEEDARG|EX_RANGE|EX_EXTRA|EX_CMDWIN|EX_LOCK_OK, ADDR_OTHER),
-EXCMD(CMD_silent,       "silent",       ex_wrongmodifier, EX_NEEDARG|EX_EXTRA|EX_BANG|EX_NOTRLCOM|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_sleep,        "sleep",        ex_sleep, EX_BANG|EX_RANGE|EX_COUNT|EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_OTHER),
-EXCMD(CMD_slast,        "slast",        ex_last, EX_EXTRA|EX_BANG|EX_CMDARG|EX_ARGOPT|EX_TRLBAR, ADDR_NONE),
-EXCMD(CMD_smagic,       "smagic",       ex_submagic, EX_RANGE|EX_WHOLEFOLD|EX_EXTRA|EX_CMDWIN|EX_LOCK_OK|EX_NONWHITE_OK, ADDR_LINES),
-EXCMD(CMD_smap,         "smap",         ex_map, EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_smapclear,    "smapclear",    ex_mapclear, EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_smenu,        "smenu",        ex_menu, EX_RANGE|EX_ZEROR|EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_OTHER),
-EXCMD(CMD_snext,        "snext",        ex_next, EX_RANGE|EX_BANG|EX_FILES|EX_CMDARG|EX_ARGOPT|EX_TRLBAR, ADDR_OTHER),
-EXCMD(CMD_snomagic,     "snomagic",     ex_submagic, EX_RANGE|EX_WHOLEFOLD|EX_EXTRA|EX_CMDWIN|EX_LOCK_OK|EX_NONWHITE_OK, ADDR_LINES),
-EXCMD(CMD_snoremap,     "snoremap",     ex_map, EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_snoremenu,    "snoremenu",    ex_menu, EX_RANGE|EX_ZEROR|EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_OTHER),
-EXCMD(CMD_source,       "source",       ex_source, EX_RANGE|EX_DFLALL|EX_BANG|EX_FILE1|EX_TRLBAR|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK, ADDR_LINES),
-EXCMD(CMD_sort,         "sort",         ex_sort, EX_RANGE|EX_DFLALL|EX_WHOLEFOLD|EX_BANG|EX_EXTRA|EX_NOTRLCOM|EX_MODIFY, ADDR_LINES),
-EXCMD(CMD_split,        "split",        ex_splitview, EX_BANG|EX_FILE1|EX_RANGE|EX_CMDARG|EX_ARGOPT|EX_TRLBAR, ADDR_OTHER),
-EXCMD(CMD_spellgood,    "spellgood",    ex_spell, EX_BANG|EX_RANGE|EX_NEEDARG|EX_EXTRA|EX_TRLBAR, ADDR_OTHER),
-EXCMD(CMD_spelldump,    "spelldump",    ex_spelldump, EX_BANG|EX_TRLBAR, ADDR_NONE),
-EXCMD(CMD_spellinfo,    "spellinfo",    ex_spellinfo, EX_TRLBAR, ADDR_NONE),
-EXCMD(CMD_spellrepall,  "spellrepall",  ex_spellrepall, EX_TRLBAR, ADDR_NONE),
-EXCMD(CMD_spellrare,    "spellrare",    ex_spell, EX_BANG|EX_RANGE|EX_NEEDARG|EX_EXTRA|EX_TRLBAR, ADDR_OTHER),
-EXCMD(CMD_spellundo,    "spellundo",    ex_spell, EX_BANG|EX_RANGE|EX_NEEDARG|EX_EXTRA|EX_TRLBAR, ADDR_OTHER),
-EXCMD(CMD_spellwrong,   "spellwrong",   ex_spell, EX_BANG|EX_RANGE|EX_NEEDARG|EX_EXTRA|EX_TRLBAR, ADDR_OTHER),
-EXCMD(CMD_sprevious,    "sprevious",    ex_previous, EX_EXTRA|EX_RANGE|EX_COUNT|EX_BANG|EX_CMDARG|EX_ARGOPT|EX_TRLBAR, ADDR_OTHER),
-EXCMD(CMD_srewind,      "srewind",      ex_rewind, EX_EXTRA|EX_BANG|EX_CMDARG|EX_ARGOPT|EX_TRLBAR, ADDR_NONE),
-EXCMD(CMD_stop,         "stop",         ex_stop, EX_TRLBAR|EX_BANG|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_stag,         "stag",         ex_stag, EX_RANGE|EX_BANG|EX_WORD1|EX_TRLBAR|EX_ZEROR, ADDR_OTHER),
-EXCMD(CMD_startinsert,  "startinsert",  ex_startinsert, EX_BANG|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_startgreplace, "startgreplace", ex_startinsert, EX_BANG|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_startreplace, "startreplace", ex_startinsert, EX_BANG|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_static,       "static",       ex_wrongmodifier, EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK|EX_WHOLE, ADDR_NONE),
-EXCMD(CMD_stopinsert,   "stopinsert",   ex_stopinsert, EX_BANG|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_stjump,       "stjump",       ex_stag, EX_BANG|EX_TRLBAR|EX_WORD1, ADDR_NONE),
-EXCMD(CMD_stselect,     "stselect",     ex_stag, EX_BANG|EX_TRLBAR|EX_WORD1, ADDR_NONE),
-EXCMD(CMD_sunhide,      "sunhide",      ex_buffer_all, EX_RANGE|EX_COUNT|EX_TRLBAR, ADDR_OTHER),
-EXCMD(CMD_sunmap,       "sunmap",       ex_unmap, EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_sunmenu,      "sunmenu",      ex_menu, EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_suspend,      "suspend",      ex_stop, EX_TRLBAR|EX_BANG|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_sview,        "sview",        ex_splitview, EX_BANG|EX_FILE1|EX_RANGE|EX_CMDARG|EX_ARGOPT|EX_TRLBAR, ADDR_OTHER),
-EXCMD(CMD_swapname,     "swapname",     ex_swapname, EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_syntax,       "syntax",       ex_syntax, EX_EXTRA|EX_NOTRLCOM|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_syntime,      "syntime",      ex_syntime, EX_NEEDARG|EX_WORD1|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_syncbind,     "syncbind",     ex_syncbind, EX_TRLBAR, ADDR_NONE),
-EXCMD(CMD_smile,        "smile",        ex_smile, EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK|EX_SBOXOK, ADDR_NONE),
-EXCMD(CMD_t,            "t",            ex_copymove, EX_RANGE|EX_WHOLEFOLD|EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK|EX_MODIFY, ADDR_LINES),
-EXCMD(CMD_tNext,        "tNext",        ex_tag, EX_RANGE|EX_BANG|EX_TRLBAR|EX_ZEROR, ADDR_OTHER),
-EXCMD(CMD_tag,          "tag",          ex_tag, EX_RANGE|EX_BANG|EX_WORD1|EX_TRLBAR|EX_ZEROR, ADDR_OTHER),
-EXCMD(CMD_tags,         "tags",         do_tags, EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_tab,          "tab",          ex_wrongmodifier, EX_RANGE|EX_ZEROR|EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM, ADDR_TABS),
-EXCMD(CMD_tabclose,     "tabclose",     ex_tabclose, EX_BANG|EX_RANGE|EX_ZEROR|EX_EXTRA|EX_NOSPC|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_TABS),
-EXCMD(CMD_tabdo,        "tabdo",        ex_listdo, EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM|EX_RANGE|EX_DFLALL|EX_EXPAND, ADDR_TABS),
-EXCMD(CMD_tabedit,      "tabedit",      ex_splitview, EX_BANG|EX_FILE1|EX_RANGE|EX_ZEROR|EX_CMDARG|EX_ARGOPT|EX_TRLBAR, ADDR_TABS),
-EXCMD(CMD_tabfind,      "tabfind",      ex_splitview, EX_BANG|EX_FILE1|EX_RANGE|EX_ZEROR|EX_CMDARG|EX_ARGOPT|EX_NEEDARG|EX_TRLBAR, ADDR_TABS),
-EXCMD(CMD_tabfirst,     "tabfirst",     ex_tabnext, EX_TRLBAR, ADDR_NONE),
-EXCMD(CMD_tabmove,      "tabmove",      ex_tabmove, EX_RANGE|EX_ZEROR|EX_EXTRA|EX_NOSPC|EX_TRLBAR, ADDR_TABS),
-EXCMD(CMD_tablast,      "tablast",      ex_tabnext, EX_TRLBAR, ADDR_NONE),
-EXCMD(CMD_tabnext,      "tabnext",      ex_tabnext, EX_RANGE|EX_ZEROR|EX_EXTRA|EX_NOSPC|EX_TRLBAR, ADDR_TABS),
-EXCMD(CMD_tabnew,       "tabnew",       ex_splitview, EX_BANG|EX_FILE1|EX_RANGE|EX_ZEROR|EX_CMDARG|EX_ARGOPT|EX_TRLBAR, ADDR_TABS),
-EXCMD(CMD_tabonly,      "tabonly",      ex_tabonly, EX_BANG|EX_RANGE|EX_ZEROR|EX_EXTRA|EX_NOSPC|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_TABS),
-EXCMD(CMD_tabprevious,  "tabprevious",  ex_tabnext, EX_RANGE|EX_ZEROR|EX_EXTRA|EX_NOSPC|EX_TRLBAR, ADDR_TABS_RELATIVE),
-EXCMD(CMD_tabNext,      "tabNext",      ex_tabnext, EX_RANGE|EX_ZEROR|EX_EXTRA|EX_NOSPC|EX_TRLBAR, ADDR_TABS_RELATIVE),
-EXCMD(CMD_tabrewind,    "tabrewind",    ex_tabnext, EX_TRLBAR, ADDR_NONE),
-EXCMD(CMD_tabs,         "tabs",         ex_tabs, EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_tcd,          "tcd",          ex_cd, EX_BANG|EX_FILE1|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_tchdir,       "tchdir",       ex_cd, EX_BANG|EX_FILE1|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_tcl,          "tcl",          ex_tcl, EX_RANGE|EX_EXTRA|EX_NEEDARG|EX_CMDWIN|EX_LOCK_OK|EX_RESTRICT, ADDR_LINES),
-EXCMD(CMD_tcldo,        "tcldo",        ex_tcldo, EX_RANGE|EX_DFLALL|EX_EXTRA|EX_NEEDARG|EX_CMDWIN|EX_LOCK_OK|EX_RESTRICT, ADDR_LINES),
-EXCMD(CMD_tclfile,      "tclfile",      ex_tclfile, EX_RANGE|EX_FILE1|EX_NEEDARG|EX_CMDWIN|EX_LOCK_OK|EX_RESTRICT, ADDR_LINES),
-EXCMD(CMD_tearoff,      "tearoff",      ex_tearoff, EX_NEEDARG|EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_terminal,     "terminal",     ex_terminal, EX_RANGE|EX_BANG|EX_FILES|EX_CMDWIN|EX_LOCK_OK, ADDR_LINES),
-EXCMD(CMD_tfirst,       "tfirst",       ex_tag, EX_RANGE|EX_BANG|EX_TRLBAR|EX_ZEROR, ADDR_OTHER),
-EXCMD(CMD_throw,        "throw",        ex_throw, EX_EXTRA|EX_NEEDARG|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK|EX_WHOLE, ADDR_NONE),
-EXCMD(CMD_this,         "this",         ex_wrongmodifier, EX_EXTRA|EX_NEEDARG|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK|EX_WHOLE, ADDR_NONE),
-EXCMD(CMD_tjump,        "tjump",        ex_tag, EX_BANG|EX_TRLBAR|EX_WORD1, ADDR_NONE),
-EXCMD(CMD_tlast,        "tlast",        ex_tag, EX_BANG|EX_TRLBAR, ADDR_NONE),
-EXCMD(CMD_tlmenu,       "tlmenu",       ex_menu, EX_RANGE|EX_ZEROR|EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_OTHER),
-EXCMD(CMD_tlnoremenu,   "tlnoremenu",   ex_menu, EX_RANGE|EX_ZEROR|EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_OTHER),
-EXCMD(CMD_tlunmenu,     "tlunmenu",     ex_menu, EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_tmenu,        "tmenu",        ex_menu, EX_RANGE|EX_ZEROR|EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_OTHER),
-EXCMD(CMD_tmap,         "tmap",         ex_map, EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_tmapclear,    "tmapclear",    ex_mapclear, EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_tnext,        "tnext",        ex_tag, EX_RANGE|EX_BANG|EX_TRLBAR|EX_ZEROR, ADDR_OTHER),
-EXCMD(CMD_tnoremap,     "tnoremap",     ex_map, EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_topleft,      "topleft",      ex_wrongmodifier, EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM, ADDR_NONE),
-EXCMD(CMD_tprevious,    "tprevious",    ex_tag, EX_RANGE|EX_BANG|EX_TRLBAR|EX_ZEROR, ADDR_OTHER),
-EXCMD(CMD_trewind,      "trewind",      ex_tag, EX_RANGE|EX_BANG|EX_TRLBAR|EX_ZEROR, ADDR_OTHER),
-EXCMD(CMD_try,          "try",          ex_try, EX_TRLBAR|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_tselect,      "tselect",      ex_tag, EX_BANG|EX_TRLBAR|EX_WORD1, ADDR_NONE),
-EXCMD(CMD_tunmenu,      "tunmenu",      ex_menu, EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_tunmap,       "tunmap",       ex_unmap, EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_type,         "type",         ex_type, EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK|EX_WHOLE|EX_EXPORT, ADDR_NONE),
-EXCMD(CMD_undo,         "undo",         ex_undo, EX_RANGE|EX_COUNT|EX_ZEROR|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_OTHER),
-EXCMD(CMD_undojoin,     "undojoin",     ex_undojoin, EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_undolist,     "undolist",     ex_undolist, EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_unabbreviate, "unabbreviate", ex_abbreviate, EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_unhide,       "unhide",       ex_buffer_all, EX_RANGE|EX_COUNT|EX_TRLBAR, ADDR_OTHER),
-EXCMD(CMD_uniq,         "uniq",         ex_uniq, EX_RANGE|EX_DFLALL|EX_WHOLEFOLD|EX_BANG|EX_EXTRA|EX_NOTRLCOM|EX_MODIFY, ADDR_LINES),
-EXCMD(CMD_unlet,        "unlet",        ex_unlet, EX_BANG|EX_EXTRA|EX_NEEDARG|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_unlockvar,    "unlockvar",    ex_lockvar, EX_BANG|EX_EXTRA|EX_NEEDARG|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_unmap,        "unmap",        ex_unmap, EX_BANG|EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_unmenu,       "unmenu",       ex_menu, EX_BANG|EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_unsilent,     "unsilent",     ex_wrongmodifier, EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_update,       "update",       ex_update, EX_RANGE|EX_WHOLEFOLD|EX_BANG|EX_FILE1|EX_ARGOPT|EX_DFLALL|EX_TRLBAR, ADDR_LINES),
-EXCMD(CMD_vglobal,      "vglobal",      ex_global, EX_RANGE|EX_WHOLEFOLD|EX_EXTRA|EX_DFLALL|EX_CMDWIN|EX_LOCK_OK|EX_NONWHITE_OK, ADDR_LINES),
-EXCMD(CMD_var,          "var",          ex_var, EX_EXTRA|EX_NOTRLCOM|EX_EXPR_ARG|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK|EX_WHOLE|EX_EXPORT, ADDR_NONE),
-EXCMD(CMD_version,      "version",      ex_version, EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_verbose,      "verbose",      ex_wrongmodifier, EX_NEEDARG|EX_RANGE|EX_EXTRA|EX_NOTRLCOM|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK, ADDR_OTHER),
-EXCMD(CMD_vertical,     "vertical",     ex_wrongmodifier, EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM, ADDR_NONE),
-EXCMD(CMD_visual,       "visual",       ex_edit, EX_BANG|EX_FILE1|EX_CMDARG|EX_ARGOPT|EX_TRLBAR, ADDR_NONE),
-EXCMD(CMD_view,         "view",         ex_edit, EX_BANG|EX_FILE1|EX_CMDARG|EX_ARGOPT|EX_TRLBAR, ADDR_NONE),
-EXCMD(CMD_vimgrep,      "vimgrep",      ex_vimgrep, EX_RANGE|EX_BANG|EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM|EX_TRLBAR|EX_XFILE|EX_LOCK_OK, ADDR_OTHER),
-EXCMD(CMD_vimgrepadd,   "vimgrepadd",   ex_vimgrep, EX_RANGE|EX_BANG|EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM|EX_TRLBAR|EX_XFILE|EX_LOCK_OK, ADDR_OTHER),
-EXCMD(CMD_vim9cmd,      "vim9cmd",      ex_wrongmodifier, EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_vim9script,   "vim9script",   ex_vim9script, EX_WORD1|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_viusage,      "viusage",      ex_viusage, EX_TRLBAR, ADDR_NONE),
-EXCMD(CMD_vmap,         "vmap",         ex_map, EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_vmapclear,    "vmapclear",    ex_mapclear, EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_vmenu,        "vmenu",        ex_menu, EX_RANGE|EX_ZEROR|EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_OTHER),
-EXCMD(CMD_vnoremap,     "vnoremap",     ex_map, EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_vnew,         "vnew",         ex_splitview, EX_BANG|EX_FILE1|EX_RANGE|EX_CMDARG|EX_ARGOPT|EX_TRLBAR, ADDR_OTHER),
-EXCMD(CMD_vnoremenu,    "vnoremenu",    ex_menu, EX_RANGE|EX_ZEROR|EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_OTHER),
-EXCMD(CMD_vsplit,       "vsplit",       ex_splitview, EX_BANG|EX_FILE1|EX_RANGE|EX_CMDARG|EX_ARGOPT|EX_TRLBAR, ADDR_OTHER),
-EXCMD(CMD_vunmap,       "vunmap",       ex_unmap, EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_vunmenu,      "vunmenu",      ex_menu, EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_write,        "write",        ex_write, EX_RANGE|EX_WHOLEFOLD|EX_BANG|EX_FILE1|EX_ARGOPT|EX_DFLALL|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_LINES),
-EXCMD(CMD_wNext,        "wNext",        ex_wnext, EX_RANGE|EX_WHOLEFOLD|EX_BANG|EX_FILE1|EX_ARGOPT|EX_TRLBAR, ADDR_OTHER),
-EXCMD(CMD_wall,         "wall",         do_wqall, EX_BANG|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_while,        "while",        ex_while, EX_EXTRA|EX_NOTRLCOM|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK|EX_WHOLE, ADDR_NONE),
-EXCMD(CMD_winsize,      "winsize",      ex_winsize, EX_EXTRA|EX_NEEDARG|EX_TRLBAR, ADDR_NONE),
-EXCMD(CMD_wincmd,       "wincmd",       ex_wincmd, EX_NEEDARG|EX_WORD1|EX_RANGE|EX_CMDWIN|EX_LOCK_OK, ADDR_OTHER),
-EXCMD(CMD_windo,        "windo",        ex_listdo, EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM|EX_RANGE|EX_DFLALL|EX_EXPAND, ADDR_WINDOWS),
-EXCMD(CMD_winpos,       "winpos",       ex_winpos, EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_wlrestore,    "wlrestore",    ex_wlrestore, EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK|EX_BANG, ADDR_NONE),
-EXCMD(CMD_wnext,        "wnext",        ex_wnext, EX_RANGE|EX_BANG|EX_FILE1|EX_ARGOPT|EX_TRLBAR, ADDR_OTHER),
-EXCMD(CMD_wprevious,    "wprevious",    ex_wnext, EX_RANGE|EX_BANG|EX_FILE1|EX_ARGOPT|EX_TRLBAR, ADDR_OTHER),
-EXCMD(CMD_wq,           "wq",           ex_exit, EX_RANGE|EX_WHOLEFOLD|EX_BANG|EX_FILE1|EX_ARGOPT|EX_DFLALL|EX_TRLBAR, ADDR_LINES),
-EXCMD(CMD_wqall,        "wqall",        do_wqall, EX_BANG|EX_FILE1|EX_ARGOPT|EX_TRLBAR, ADDR_NONE),
-EXCMD(CMD_wundo,        "wundo",        ex_wundo, EX_BANG|EX_NEEDARG|EX_FILE1, ADDR_NONE),
-EXCMD(CMD_wviminfo,     "wviminfo",     ex_viminfo, EX_BANG|EX_FILE1|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_xit,          "xit",          ex_exit, EX_RANGE|EX_WHOLEFOLD|EX_BANG|EX_FILE1|EX_ARGOPT|EX_DFLALL|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_LINES),
-EXCMD(CMD_xall,         "xall",         do_wqall, EX_BANG|EX_TRLBAR, ADDR_NONE),
-EXCMD(CMD_xmap,         "xmap",         ex_map, EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_xmapclear,    "xmapclear",    ex_mapclear, EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_xmenu,        "xmenu",        ex_menu, EX_RANGE|EX_ZEROR|EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_OTHER),
-EXCMD(CMD_xnoremap,     "xnoremap",     ex_map, EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_xnoremenu,    "xnoremenu",    ex_menu, EX_RANGE|EX_ZEROR|EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_OTHER),
-EXCMD(CMD_xrestore,     "xrestore",     ex_xrestore, EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_xunmap,       "xunmap",       ex_unmap, EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_xunmenu,      "xunmenu",      ex_menu, EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_yank,         "yank",         ex_operators, EX_RANGE|EX_WHOLEFOLD|EX_REGSTR|EX_COUNT|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_LINES),
-EXCMD(CMD_z,            "z",            ex_z, EX_RANGE|EX_WHOLEFOLD|EX_BANG|EX_EXTRA|EX_FLAGS|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_LINES),
-
-EXCMD(CMD_bang,         "!",            ex_bang, EX_RANGE|EX_WHOLEFOLD|EX_BANG|EX_FILES|EX_CMDWIN|EX_LOCK_OK|EX_NONWHITE_OK, ADDR_LINES),
-EXCMD(CMD_pound,        "#",            ex_print, EX_RANGE|EX_WHOLEFOLD|EX_COUNT|EX_FLAGS|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_LINES),
-EXCMD(CMD_and,          "&",            ex_substitute, EX_RANGE|EX_WHOLEFOLD|EX_EXTRA|EX_CMDWIN|EX_LOCK_OK|EX_MODIFY|EX_NONWHITE_OK, ADDR_LINES),
-EXCMD(CMD_star,         "*",            ex_at, EX_RANGE|EX_WHOLEFOLD|EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK|EX_NONWHITE_OK, ADDR_LINES),
-EXCMD(CMD_lshift,       "<",            ex_operators, EX_RANGE|EX_WHOLEFOLD|EX_COUNT|EX_FLAGS|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK|EX_MODIFY, ADDR_LINES),
-EXCMD(CMD_equal,        "=",            ex_equal, EX_RANGE|EX_TRLBAR|EX_DFLALL|EX_FLAGS|EX_CMDWIN|EX_LOCK_OK, ADDR_LINES),
-EXCMD(CMD_rshift,       ">",            ex_operators, EX_RANGE|EX_WHOLEFOLD|EX_COUNT|EX_FLAGS|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK|EX_MODIFY, ADDR_LINES),
-EXCMD(CMD_at,           "@",            ex_at, EX_RANGE|EX_WHOLEFOLD|EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK|EX_NONWHITE_OK, ADDR_LINES),
-EXCMD(CMD_block,        "{",            ex_block, EX_TRLBAR|EX_LOCK_OK|EX_CMDWIN, ADDR_NONE),
-EXCMD(CMD_endblock,     "}",            ex_endblock, EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_tilde,        "~",            ex_substitute, EX_RANGE|EX_WHOLEFOLD|EX_EXTRA|EX_CMDWIN|EX_LOCK_OK|EX_MODIFY|EX_NONWHITE_OK, ADDR_LINES),
-
-EXCMD(CMD_Next,         "Next",         ex_previous, EX_EXTRA|EX_RANGE|EX_COUNT|EX_BANG|EX_CMDARG|EX_ARGOPT|EX_TRLBAR, ADDR_OTHER),
-EXCMD(CMD_Print,        "Print",        ex_print, EX_RANGE|EX_WHOLEFOLD|EX_COUNT|EX_FLAGS|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_LINES),
-EXCMD(CMD_X,            "X",            ex_X, EX_TRLBAR, ADDR_NONE),
+    CMD_append,
+    CMD_abbreviate,
+    CMD_abclear,
+    CMD_aboveleft,
+    CMD_abstract,
+    CMD_all,
+    CMD_amenu,
+    CMD_anoremenu,
+    CMD_args,
+    CMD_argadd,
+    CMD_argdelete,
+    CMD_argdo,
+    CMD_argdedupe,
+    CMD_argedit,
+    CMD_argglobal,
+    CMD_arglocal,
+    CMD_argument,
+    CMD_ascii,
+    CMD_autocmd,
+    CMD_augroup,
+    CMD_aunmenu,
+    CMD_buffer,
+    CMD_bNext,
+    CMD_ball,
+    CMD_badd,
+    CMD_balt,
+    CMD_bdelete,
+    CMD_behave,
+    CMD_belowright,
+    CMD_bfirst,
+    CMD_blast,
+    CMD_bmodified,
+    CMD_bnext,
+    CMD_botright,
+    CMD_bprevious,
+    CMD_brewind,
+    CMD_break,
+    CMD_breakadd,
+    CMD_breakdel,
+    CMD_breaklist,
+    CMD_browse,
+    CMD_buffers,
+    CMD_bufdo,
+    CMD_bunload,
+    CMD_bwipeout,
+    CMD_change,
+    CMD_cNext,
+    CMD_cNfile,
+    CMD_cabbrev,
+    CMD_cabclear,
+    CMD_cabove,
+    CMD_caddbuffer,
+    CMD_caddexpr,
+    CMD_caddfile,
+    CMD_cafter,
+    CMD_call,
+    CMD_catch,
+    CMD_cbuffer,
+    CMD_cbefore,
+    CMD_cbelow,
+    CMD_cbottom,
+    CMD_cc,
+    CMD_cclose,
+    CMD_cd,
+    CMD_cdo,
+    CMD_center,
+    CMD_cexpr,
+    CMD_cfile,
+    CMD_cfdo,
+    CMD_cfirst,
+    CMD_cgetfile,
+    CMD_cgetbuffer,
+    CMD_cgetexpr,
+    CMD_chdir,
+    CMD_changes,
+    CMD_checkpath,
+    CMD_checktime,
+    CMD_chistory,
+    CMD_clist,
+    CMD_clipreset,
+    CMD_clast,
+    CMD_class,
+    CMD_close,
+    CMD_clearjumps,
+    CMD_cmap,
+    CMD_cmapclear,
+    CMD_cmenu,
+    CMD_cnext,
+    CMD_cnewer,
+    CMD_cnfile,
+    CMD_cnoremap,
+    CMD_cnoreabbrev,
+    CMD_cnoremenu,
+    CMD_copy,
+    CMD_colder,
+    CMD_colorscheme,
+    CMD_command,
+    CMD_comclear,
+    CMD_compiler,
+    CMD_continue,
+    CMD_confirm,
+    CMD_const,
+    CMD_copen,
+    CMD_cprevious,
+    CMD_cpfile,
+    CMD_cquit,
+    CMD_crewind,
+    CMD_cscope,
+    CMD_cstag,
+    CMD_cunmap,
+    CMD_cunabbrev,
+    CMD_cunmenu,
+    CMD_cwindow,
+    CMD_delete,
+    CMD_delmarks,
+    CMD_debug,
+    CMD_debuggreedy,
+    CMD_def,
+    CMD_defcompile,
+    CMD_defer,
+    CMD_delcommand,
+    CMD_delfunction,
+    CMD_display,
+    CMD_diffupdate,
+    CMD_diffget,
+    CMD_diffoff,
+    CMD_diffpatch,
+    CMD_diffput,
+    CMD_diffsplit,
+    CMD_diffthis,
+    CMD_digraphs,
+    CMD_disassemble,
+    CMD_djump,
+    CMD_dlist,
+    CMD_doautocmd,
+    CMD_doautoall,
+    CMD_drop,
+    CMD_dsearch,
+    CMD_dsplit,
+    CMD_edit,
+    CMD_earlier,
+    CMD_echo,
+    CMD_echoerr,
+    CMD_echohl,
+    CMD_echomsg,
+    CMD_echoconsole,
+    CMD_echon,
+    CMD_echowindow,
+    CMD_else,
+    CMD_elseif,
+    CMD_emenu,
+    CMD_endif,
+    CMD_endinterface,
+    CMD_endclass,
+    CMD_enddef,
+    CMD_endenum,
+    CMD_endfunction,
+    CMD_endfor,
+    CMD_endtry,
+    CMD_endwhile,
+    CMD_enew,
+    CMD_enum,
+    CMD_eval,
+    CMD_ex,
+    CMD_execute,
+    CMD_exit,
+    CMD_export,
+    CMD_exusage,
+    CMD_file,
+    CMD_files,
+    CMD_filetype,
+    CMD_filter,
+    CMD_find,
+    CMD_final,
+    CMD_finally,
+    CMD_finish,
+    CMD_first,
+    CMD_fixdel,
+    CMD_fold,
+    CMD_foldclose,
+    CMD_folddoopen,
+    CMD_folddoclosed,
+    CMD_foldopen,
+    CMD_for,
+    CMD_function,
+    CMD_global,
+    CMD_goto,
+    CMD_grep,
+    CMD_grepadd,
+    CMD_gui,
+    CMD_gvim,
+    CMD_help,
+    CMD_helpclose,
+    CMD_helpfind,
+    CMD_helpgrep,
+    CMD_helptags,
+    CMD_hardcopy,
+    CMD_highlight,
+    CMD_hide,
+    CMD_history,
+    CMD_horizontal,
+    CMD_insert,
+    CMD_iabbrev,
+    CMD_iabclear,
+    CMD_if,
+    CMD_ijump,
+    CMD_ilist,
+    CMD_imap,
+    CMD_imapclear,
+    CMD_imenu,
+    CMD_import,
+    CMD_inoremap,
+    CMD_inoreabbrev,
+    CMD_inoremenu,
+    CMD_intro,
+    CMD_interface,
+    CMD_iput,
+    CMD_isearch,
+    CMD_isplit,
+    CMD_iunmap,
+    CMD_iunabbrev,
+    CMD_iunmenu,
+    CMD_join,
+    CMD_jumps,
+    CMD_k,
+    CMD_keepmarks,
+    CMD_keepjumps,
+    CMD_keeppatterns,
+    CMD_keepalt,
+    CMD_list,
+    CMD_lNext,
+    CMD_lNfile,
+    CMD_last,
+    CMD_labove,
+    CMD_language,
+    CMD_laddexpr,
+    CMD_laddbuffer,
+    CMD_laddfile,
+    CMD_lafter,
+    CMD_later,
+    CMD_lbuffer,
+    CMD_lbefore,
+    CMD_lbelow,
+    CMD_lbottom,
+    CMD_lcd,
+    CMD_lchdir,
+    CMD_lclose,
+    CMD_lcscope,
+    CMD_ldo,
+    CMD_left,
+    CMD_leftabove,
+    CMD_let,
+    CMD_lexpr,
+    CMD_legacy,
+    CMD_lfile,
+    CMD_lfdo,
+    CMD_lfirst,
+    CMD_lgetfile,
+    CMD_lgetbuffer,
+    CMD_lgetexpr,
+    CMD_lgrep,
+    CMD_lgrepadd,
+    CMD_lhelpgrep,
+    CMD_lhistory,
+    CMD_ll,
+    CMD_llast,
+    CMD_llist,
+    CMD_lmap,
+    CMD_lmapclear,
+    CMD_lmake,
+    CMD_lnoremap,
+    CMD_lnext,
+    CMD_lnewer,
+    CMD_lnfile,
+    CMD_loadview,
+    CMD_loadkeymap,
+    CMD_lockmarks,
+    CMD_lockvar,
+    CMD_lolder,
+    CMD_lopen,
+    CMD_lprevious,
+    CMD_lpfile,
+    CMD_lrewind,
+    CMD_ltag,
+    CMD_lunmap,
+    CMD_lua,
+    CMD_luado,
+    CMD_luafile,
+    CMD_lvimgrep,
+    CMD_lvimgrepadd,
+    CMD_lwindow,
+    CMD_ls,
+    CMD_move,
+    CMD_mark,
+    CMD_make,
+    CMD_map,
+    CMD_mapclear,
+    CMD_marks,
+    CMD_match,
+    CMD_menu,
+    CMD_menutranslate,
+    CMD_messages,
+    CMD_mkexrc,
+    CMD_mksession,
+    CMD_mkspell,
+    CMD_mkvimrc,
+    CMD_mkview,
+    CMD_mode,
+    CMD_mzscheme,
+    CMD_mzfile,
+    CMD_next,
+    CMD_nbkey,
+    CMD_nbclose,
+    CMD_nbstart,
+    CMD_new,
+    CMD_nmap,
+    CMD_nmapclear,
+    CMD_nmenu,
+    CMD_nnoremap,
+    CMD_nnoremenu,
+    CMD_noremap,
+    CMD_noautocmd,
+    CMD_nohlsearch,
+    CMD_noreabbrev,
+    CMD_noremenu,
+    CMD_noswapfile,
+    CMD_normal,
+    CMD_number,
+    CMD_nunmap,
+    CMD_nunmenu,
+    CMD_open,
+    CMD_oldfiles,
+    CMD_omap,
+    CMD_omapclear,
+    CMD_omenu,
+    CMD_only,
+    CMD_onoremap,
+    CMD_onoremenu,
+    CMD_options,
+    CMD_ounmap,
+    CMD_ounmenu,
+    CMD_ownsyntax,
+    CMD_print,
+    CMD_packadd,
+    CMD_packloadall,
+    CMD_pbuffer,
+    CMD_pclose,
+    CMD_perl,
+    CMD_perldo,
+    CMD_pedit,
+    CMD_pop,
+    CMD_popup,
+    CMD_ppop,
+    CMD_preserve,
+    CMD_previous,
+    CMD_promptfind,
+    CMD_promptrepl,
+    CMD_profile,
+    CMD_profdel,
+    CMD_psearch,
+    CMD_ptag,
+    CMD_ptNext,
+    CMD_ptfirst,
+    CMD_ptjump,
+    CMD_ptlast,
+    CMD_ptnext,
+    CMD_ptprevious,
+    CMD_ptrewind,
+    CMD_ptselect,
+    CMD_put,
+    CMD_public,
+    CMD_pwd,
+    CMD_python,
+    CMD_pydo,
+    CMD_pyfile,
+    CMD_py3,
+    CMD_py3do,
+    CMD_python3,
+    CMD_py3file,
+    CMD_pyx,
+    CMD_pyxdo,
+    CMD_pythonx,
+    CMD_pyxfile,
+    CMD_quit,
+    CMD_quitall,
+    CMD_qall,
+    CMD_read,
+    CMD_recover,
+    CMD_redo,
+    CMD_redir,
+    CMD_redraw,
+    CMD_redrawstatus,
+    CMD_redrawtabline,
+    CMD_redrawtabpanel,
+    CMD_registers,
+    CMD_resize,
+    CMD_retab,
+    CMD_return,
+    CMD_rewind,
+    CMD_right,
+    CMD_rightbelow,
+    CMD_runtime,
+    CMD_ruby,
+    CMD_rubydo,
+    CMD_rubyfile,
+    CMD_rundo,
+    CMD_rviminfo,
+    CMD_substitute,
+    CMD_sNext,
+    CMD_sargument,
+    CMD_sall,
+    CMD_sandbox,
+    CMD_saveas,
+    CMD_sbuffer,
+    CMD_sbNext,
+    CMD_sball,
+    CMD_sbfirst,
+    CMD_sblast,
+    CMD_sbmodified,
+    CMD_sbnext,
+    CMD_sbprevious,
+    CMD_sbrewind,
+    CMD_scriptnames,
+    CMD_scriptencoding,
+    CMD_scriptversion,
+    CMD_scscope,
+    CMD_set,
+    CMD_setfiletype,
+    CMD_setglobal,
+    CMD_setlocal,
+    CMD_sfind,
+    CMD_sfirst,
+    CMD_shell,
+    CMD_simalt,
+    CMD_sign,
+    CMD_silent,
+    CMD_sleep,
+    CMD_slast,
+    CMD_smagic,
+    CMD_smap,
+    CMD_smapclear,
+    CMD_smenu,
+    CMD_snext,
+    CMD_snomagic,
+    CMD_snoremap,
+    CMD_snoremenu,
+    CMD_source,
+    CMD_sort,
+    CMD_split,
+    CMD_spellgood,
+    CMD_spelldump,
+    CMD_spellinfo,
+    CMD_spellrepall,
+    CMD_spellrare,
+    CMD_spellundo,
+    CMD_spellwrong,
+    CMD_sprevious,
+    CMD_srewind,
+    CMD_stop,
+    CMD_stag,
+    CMD_startinsert,
+    CMD_startgreplace,
+    CMD_startreplace,
+    CMD_static,
+    CMD_stopinsert,
+    CMD_stjump,
+    CMD_stselect,
+    CMD_sunhide,
+    CMD_sunmap,
+    CMD_sunmenu,
+    CMD_suspend,
+    CMD_sview,
+    CMD_swapname,
+    CMD_syntax,
+    CMD_syntime,
+    CMD_syncbind,
+    CMD_smile,
+    CMD_t,
+    CMD_tNext,
+    CMD_tag,
+    CMD_tags,
+    CMD_tab,
+    CMD_tabclose,
+    CMD_tabdo,
+    CMD_tabedit,
+    CMD_tabfind,
+    CMD_tabfirst,
+    CMD_tabmove,
+    CMD_tablast,
+    CMD_tabnext,
+    CMD_tabnew,
+    CMD_tabonly,
+    CMD_tabprevious,
+    CMD_tabNext,
+    CMD_tabrewind,
+    CMD_tabs,
+    CMD_tcd,
+    CMD_tchdir,
+    CMD_tcl,
+    CMD_tcldo,
+    CMD_tclfile,
+    CMD_tearoff,
+    CMD_terminal,
+    CMD_tfirst,
+    CMD_throw,
+    CMD_this,
+    CMD_tjump,
+    CMD_tlast,
+    CMD_tlmenu,
+    CMD_tlnoremenu,
+    CMD_tlunmenu,
+    CMD_tmenu,
+    CMD_tmap,
+    CMD_tmapclear,
+    CMD_tnext,
+    CMD_tnoremap,
+    CMD_topleft,
+    CMD_tprevious,
+    CMD_trewind,
+    CMD_try,
+    CMD_tselect,
+    CMD_tunmenu,
+    CMD_tunmap,
+    CMD_type,
+    CMD_undo,
+    CMD_undojoin,
+    CMD_undolist,
+    CMD_unabbreviate,
+    CMD_unhide,
+    CMD_uniq,
+    CMD_unlet,
+    CMD_unlockvar,
+    CMD_unmap,
+    CMD_unmenu,
+    CMD_unsilent,
+    CMD_update,
+    CMD_vglobal,
+    CMD_var,
+    CMD_version,
+    CMD_verbose,
+    CMD_vertical,
+    CMD_visual,
+    CMD_view,
+    CMD_vimgrep,
+    CMD_vimgrepadd,
+    CMD_vim9cmd,
+    CMD_vim9script,
+    CMD_viusage,
+    CMD_vmap,
+    CMD_vmapclear,
+    CMD_vmenu,
+    CMD_vnoremap,
+    CMD_vnew,
+    CMD_vnoremenu,
+    CMD_vsplit,
+    CMD_vunmap,
+    CMD_vunmenu,
+    CMD_write,
+    CMD_wNext,
+    CMD_wall,
+    CMD_while,
+    CMD_winsize,
+    CMD_wincmd,
+    CMD_windo,
+    CMD_winpos,
+    CMD_wlrestore,
+    CMD_wnext,
+    CMD_wprevious,
+    CMD_wq,
+    CMD_wqall,
+    CMD_wundo,
+    CMD_wviminfo,
+    CMD_xit,
+    CMD_xall,
+    CMD_xmap,
+    CMD_xmapclear,
+    CMD_xmenu,
+    CMD_xnoremap,
+    CMD_xnoremenu,
+    CMD_xrestore,
+    CMD_xunmap,
+    CMD_xunmenu,
+    CMD_yank,
+    CMD_z,
+
+    CMD_bang,
+    CMD_pound,
+    CMD_and,
+    CMD_star,
+    CMD_lshift,
+    CMD_equal,
+    CMD_rshift,
+    CMD_at,
+    CMD_block,
+    CMD_endblock,
+    CMD_tilde,
+
+    CMD_Next,
+    CMD_Print,
+    CMD_X,
 
-EXCMD(CMD_increment,    "++",           ex_incdec, EX_EXTRA|EX_TRLBAR|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_decrement,    "--",           ex_incdec, EX_EXTRA|EX_TRLBAR|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-
-#undef EXCMD
+    CMD_increment,
+    CMD_decrement,
 
     CMD_SIZE,
     CMD_USER = -1,
@@ -6351,19 +4894,17 @@
     void        *cookie;
 };
 
-# define FORCE_BIN 1
-# define FORCE_NOBIN 2
+enum { FORCE_BIN = 1 };
+enum { FORCE_NOBIN = 2 };
 
-# define EXFLAG_LIST    0x01
-# define EXFLAG_NR      0x02
-# define EXFLAG_PRINT   0x04
+enum { EXFLAG_LIST = 0x01 };
+enum { EXFLAG_NR = 0x02 };
+enum { EXFLAG_PRINT = 0x04 };
 
 // ---------------- end ex_cmds.h ----------------
 // ---------------- begin spell.h ----------------
 
-#define MAXWLEN 254
-
-#define MAXREGIONS 8
+enum { MAXREGIONS = 8 };
 
 typedef int idx_T;
 
@@ -6435,48 +4976,16 @@
     hashtab_T   sl_sounddone;
 };
 
-# define SPL_FNAME_TMPL  "%s.%s.spl"
-# define SPL_FNAME_ADD   ".add."
-# define SPL_FNAME_ASCII ".ascii."
-
-#define WF_REGION   0x01
-#define WF_ONECAP   0x02
-#define WF_ALLCAP   0x04
-#define WF_RARE     0x08
-#define WF_BANNED   0x10
-#define WF_AFX      0x20
-#define WF_FIXCAP   0x40
-#define WF_KEEPCAP  0x80
-
-#define WF_CAPMASK (WF_ONECAP | WF_ALLCAP | WF_KEEPCAP | WF_FIXCAP)
-
-#define WF_HAS_AFF  0x0100
-#define WF_NEEDCOMP 0x0200
-#define WF_NOSUGGEST 0x0400
-#define WF_COMPROOT 0x0800
-#define WF_NOCOMPBEF 0x1000
-#define WF_NOCOMPAFT 0x2000
-
-#define WFP_RARE            0x01
-#define WFP_NC              0x02
-#define WFP_UP              0x04
-#define WFP_COMPPERMIT      0x08
-#define WFP_COMPFORBID      0x10
-
-#define WF_RAREPFX  (WFP_RARE << 24)
-#define WF_PFX_NC   (WFP_NC << 24)
-#define WF_PFX_UP   (WFP_UP << 24)
-#define WF_PFX_COMPPERMIT (WFP_COMPPERMIT << 24)
-#define WF_PFX_COMPFORBID (WFP_COMPFORBID << 24)
-
-#define COMP_CHECKDUP           1
-#define COMP_CHECKREP           2
-#define COMP_CHECKCASE          4
-#define COMP_CHECKTRIPLE        8
-
-#define SP_TRUNCERROR   (-1)
-#define SP_FORMERROR    (-2)
-#define SP_OTHERERROR   (-3)
+enum { WF_ONECAP = 0x02 };
+enum { WF_ALLCAP = 0x04 };
+enum { WF_FIXCAP = 0x40 };
+enum { WF_KEEPCAP = 0x80 };
+
+enum { WFP_RARE = 0x01 };
+enum { WFP_NC = 0x02 };
+enum { WFP_UP = 0x04 };
+enum { WFP_COMPPERMIT = 0x08 };
+enum { WFP_COMPFORBID = 0x10 };
 
 typedef struct langp_S
 {
@@ -6486,28 +4995,10 @@
     int         lp_region;
 } langp_T;
 
-#define LANGP_ENTRY(ga, i)      (((langp_T *)(ga).ga_data) + (i))
-
-#define VIMSUGMAGIC "VIMsug"
-#define VIMSUGMAGICL 6
-#define VIMSUGVERSION 1
-
-# define SPELL_TOFOLD(c) (enc_utf8 && (c) >= 128 ? utf_fold(c)      : (c) < 256 ? (int)spelltab.st_fold[c] : (int)towlower(c))
-
-# define SPELL_TOUPPER(c) (enc_utf8 && (c) >= 128 ? utf_toupper(c)          : (c) < 256 ? (int)spelltab.st_upper[c] : (int)towupper(c))
-
-# define SPELL_ISUPPER(c) (enc_utf8 && (c) >= 128 ? utf_isupper(c)          : (c) < 256 ? spelltab.st_isu[c] : iswupper(c))
-
 // ---------------- end spell.h ----------------
 
 // ---------------- begin proto.h ----------------
 
-#  define Display int
-#  define Widget int
-#  define GdkEvent int
-#  define GdkEventKey int
-#  define XImage int
-
 // ---------------- begin os_unix.pro ----------------
 static sighandler_T mch_signal(int sig, sighandler_T func);
 static int mch_chdir(char *path);
@@ -6523,7 +5014,6 @@
 static int vim_handle_signal(int sig);
 static int mch_check_win(int argc, char **argv);
 static int mch_input_isatty(void);
-void ex_xrestore(exarg_T *eap);
 static int mch_can_restore_title(void);
 static int mch_can_restore_icon(void);
 static void mch_settitle(char_u *title, char_u *icon);
@@ -6567,7 +5057,7 @@
 static int mch_expand_wildcards(int num_pat, char_u **pat, int *num_file, char_u ***file, int flags);
 static int mch_has_exp_wildcard(char_u *p);
 static int mch_has_wildcard(char_u *p);
-int mch_rename(const char *src, const char *dest);
+int  rename(const char *src, const char *dest) ;
 
 // ---------------- end os_unix.pro ----------------
 
@@ -6888,9 +5378,6 @@
 static void ex_history(exarg_T *eap);
 
 // ---------------- end cmdhist.pro ----------------
-// ---------------- begin linematch.pro ----------------
-
-// ---------------- end linematch.pro ----------------
 // ---------------- begin drawline.pro ----------------
 static int win_line(win_T *wp, linenr_T lnum, int startrow, int endrow, int number_only, spellvars_T *spv);
 
@@ -6960,9 +5447,6 @@
 
 // ---------------- end edit.pro ----------------
 // ---------------- begin eval.pro ----------------
-void ex_echo(exarg_T *eap);
-void ex_echohl(exarg_T *eap);
-void ex_execute(exarg_T *eap);
 static char_u *do_string_sub(char_u *str, size_t len, char_u *pat, char_u *sub, typval_T *expr, char_u *flags, size_t *ret_len);
 
 // ---------------- end eval.pro ----------------
@@ -7004,7 +5488,6 @@
 static void ex_drop(exarg_T *eap);
 static char_u *skip_vimgrep_pat(char_u *p, char_u **s, int *flags);
 static char_u *skip_vimgrep_pat_ext(char_u *p, char_u **s, int *flags, char_u **nulp, int *cp);
-void ex_oldfiles(exarg_T *eap);
 
 // ---------------- end ex_cmds.pro ----------------
 // ---------------- begin ex_cmds2.pro ----------------
@@ -7016,10 +5499,6 @@
 static int check_fname(void);
 static int buf_write_all(buf_T *buf, int forceit);
 static void ex_listdo(exarg_T *eap);
-void ex_compiler(exarg_T *eap);
-void ex_pyxfile(exarg_T *eap);
-void ex_pyx(exarg_T *eap);
-void ex_pyxdo(exarg_T *eap);
 static void ex_checktime(exarg_T *eap);
 
 // ---------------- end ex_cmds2.pro ----------------
@@ -7573,25 +6052,25 @@
 
 // ---------------- end memline.pro ----------------
 
-static int smsg(const char *, ...) ATTRIBUTE_COLD ATTRIBUTE_FORMAT_PRINTF(1, 2);
+static int smsg(const char *, ...)  __attribute__((cold))   __attribute__((format(printf, 1, 2))) ;
 
-static int smsg_attr(int, const char *, ...) ATTRIBUTE_FORMAT_PRINTF(2, 3);
+static int smsg_attr(int, const char *, ...)  __attribute__((format(printf, 2, 3))) ;
 
-static int smsg_attr_keep(int, const char *, ...) ATTRIBUTE_FORMAT_PRINTF(2, 3);
+static int smsg_attr_keep(int, const char *, ...)  __attribute__((format(printf, 2, 3))) ;
 
-static int semsg(const char *, ...) ATTRIBUTE_COLD ATTRIBUTE_FORMAT_PRINTF(1, 2);
+static int semsg(const char *, ...)  __attribute__((cold))   __attribute__((format(printf, 1, 2))) ;
 
-static void siemsg(const char *, ...) ATTRIBUTE_COLD ATTRIBUTE_FORMAT_PRINTF(1, 2);
+static void siemsg(const char *, ...)  __attribute__((cold))   __attribute__((format(printf, 1, 2))) ;
 
-static int vim_snprintf_add(char *, size_t, const char *, ...) ATTRIBUTE_FORMAT_PRINTF(3, 4);
+static int vim_snprintf_add(char *, size_t, const char *, ...)  __attribute__((format(printf, 3, 4))) ;
 
-static int vim_snprintf(char *, size_t, const char *, ...) ATTRIBUTE_FORMAT_PRINTF(3, 4);
-static size_t vim_snprintf_safelen(char *, size_t, const char *, ...) ATTRIBUTE_FORMAT_PRINTF(3, 4);
+static int vim_snprintf(char *, size_t, const char *, ...)  __attribute__((format(printf, 3, 4))) ;
+static size_t vim_snprintf_safelen(char *, size_t, const char *, ...)  __attribute__((format(printf, 3, 4))) ;
 
 static int vim_vsnprintf(char *str, size_t str_m, const char *fmt, va_list ap)
-        ATTRIBUTE_FORMAT_PRINTF(3, 0);
+         __attribute__((format(printf, 3, 0))) ;
 static int vim_vsnprintf_typval(char *str, size_t str_m, const char *fmt, va_list ap, typval_T *tvs)
-        ATTRIBUTE_FORMAT_PRINTF(3, 0);
+         __attribute__((format(printf, 3, 0))) ;
 
 // ---------------- begin message.pro ----------------
 static int msg(char *s);
@@ -7642,8 +6121,6 @@
 static void show_sb_text(void);
 static void msg_sb_eol(void);
 static int msg_use_printf(void);
-void mch_errmsg(char *str);
-void mch_msg(char *str);
 static void repeat_message(void);
 static void msg_clr_eos(void);
 static void msg_clr_eos_force(void);
@@ -8284,21 +6761,16 @@
 static int do_in_runtimepath(char_u *name, int flags, void (*callback)(char_u *fname, void *ck), void *cookie);
 static int source_runtime(char_u *name, int flags);
 static int source_in_path(char_u *path, char_u *name, int flags, int *ret_sid);
-void ex_packloadall(exarg_T *eap);
-void ex_packadd(exarg_T *eap);
 static void remove_duplicates(garray_T *gap);
 static int ExpandRTDir(char_u *pat, int flags, int *num_file, char_u ***file, char *dirnames[]);
 static int expand_runtime_cmd(char_u *pat, int *numMatches, char_u ***matches);
 static int ExpandPackAddDir(char_u *pat, int *num_file, char_u ***file);
 static void ex_source(exarg_T *eap);
-void ex_options(exarg_T *eap);
 static int do_source(char_u *fname, int check_other, int is_vimrc, int *ret_sid);
-void ex_scriptnames(exarg_T *eap);
 static char_u *getsourceline(int c, void *cookie, int indent, getline_opt_T options);
 static int sourcing_a_script(exarg_T *eap);
 static void ex_scriptencoding(exarg_T *eap);
 static void ex_scriptversion(exarg_T *eap);
-void ex_finish(exarg_T *eap);
 
 // ---------------- end scriptfile.pro ----------------
 // ---------------- begin screen.pro ----------------
@@ -8347,7 +6819,6 @@
 static int redrawing(void);
 static int messaging(void);
 static void comp_col(void);
-int number_width(win_T *wp);
 static char *set_fillchars_option(win_T *wp, char_u *val, int apply, char *errbuf, size_t errbuflen);
 static char *set_listchars_option(win_T *wp, char_u *val, int apply, char *errbuf, size_t errbuflen);
 static char_u *get_fillchars_name(expand_T *xp, int idx);
@@ -8356,7 +6827,6 @@
 
 // ---------------- end screen.pro ----------------
 // ---------------- begin session.pro ----------------
-void ex_loadview(exarg_T *eap);
 static void ex_mkrc(exarg_T *eap);
 static int put_eol(FILE *fd);
 static int put_line(FILE *fd, char *s);
@@ -8632,9 +7102,6 @@
 static int in_vim9script(void);
 static void ex_vim9script(exarg_T *eap);
 static int vim9_comment_start(char_u *p);
-void ex_incdec(exarg_T *eap);
-void ex_export(exarg_T *eap);
-void ex_import(exarg_T *eap);
 
 // ---------------- end vim9script.pro ----------------
 // ---------------- begin window.pro ----------------
@@ -8737,204 +7204,187 @@
 
 // ---------------- end window.pro ----------------
 
-#  define BalloonEval int
-
 // ---------------- end proto.h ----------------
 
-# define mch_errmsg(str)        fprintf(stderr, "%s", (str))
-# define display_errors()       fflush(stderr)
-# define mch_msg(str)           printf("%s", (str))
-
-# define number_width(x) 7
-
-# define mch_open(n, m, p)      open((n), (m), (p))
-# define mch_fopen(n, p)        fopen((n), (p))
-
 // ---------------- begin globals.h ----------------
 
 static long     Rows
                     = 24L
                     ;
-static long     Columns INIT(= 80);
+static long     Columns  = 80 ;
 
-static schar_T  *ScreenLines INIT(= NULL);
-static sattr_T  *ScreenAttrs INIT(= NULL);
-static colnr_T  *ScreenCols INIT(= NULL);
-static unsigned *LineOffset INIT(= NULL);
-static char_u   *LineWraps INIT(= NULL);
+static schar_T  *ScreenLines  = NULL ;
+static sattr_T  *ScreenAttrs  = NULL ;
+static colnr_T  *ScreenCols  = NULL ;
+static unsigned *LineOffset  = NULL ;
+static char_u   *LineWraps  = NULL ;
 
-static u8char_T *ScreenLinesUC INIT(= NULL);
+static u8char_T *ScreenLinesUC  = NULL ;
 static u8char_T *ScreenLinesC[MAX_MCO];
-static int      Screen_mco INIT(= 0);
+static int      Screen_mco  = 0 ;
 
-static schar_T  *ScreenLines2 INIT(= NULL);
+static schar_T  *ScreenLines2  = NULL ;
 
-static schar_T  *current_ScreenLine INIT(= NULL);
+static schar_T  *current_ScreenLine  = NULL ;
 
-static int      screen_cur_row INIT(= 0);
-static int      screen_cur_col INIT(= 0);
+static int      screen_cur_row  = 0 ;
+static int      screen_cur_col  = 0 ;
 
 static match_T  screen_search_hl;
 
-static linenr_T search_hl_has_cursor_lnum INIT(= 0);
+static linenr_T search_hl_has_cursor_lnum  = 0 ;
 
-static int      no_hlsearch INIT(= FALSE);
+static int      no_hlsearch  = FALSE ;
 
-static int redrawing_for_callback INIT(= 0);
+static int redrawing_for_callback  = 0 ;
 
-static short    *TabPageIdxs INIT(= NULL);
+static short    *TabPageIdxs  = NULL ;
 
-static stl_click_region_T *tabline_stl_click INIT(= NULL);
-static int      tabline_stl_click_count INIT(= 0);
+static stl_click_region_T *tabline_stl_click  = NULL ;
+static int      tabline_stl_click_count  = 0 ;
 
+static long     p_po  = 100 ;
 
-static long     p_po INIT(= 100);
+static int      screen_pum_blend  = 0 ;
 
-static int      screen_pum_blend INIT(= 0);
-
-static sattr_T  *pum_bg_attrs INIT(= NULL);
-static schar_T  *pum_bg_lines INIT(= NULL);
-static u8char_T *pum_bg_linesUC INIT(= NULL);
+static sattr_T  *pum_bg_attrs  = NULL ;
+static schar_T  *pum_bg_lines  = NULL ;
+static u8char_T *pum_bg_linesUC  = NULL ;
 static u8char_T *pum_bg_linesC[MAX_MCO];
-static int      pum_bg_top INIT(= 0);
-static int      pum_bg_bot INIT(= 0);
-static int      pum_bg_cols INIT(= 0);
+static int      pum_bg_top  = 0 ;
+static int      pum_bg_bot  = 0 ;
+static int      pum_bg_cols  = 0 ;
 
-static int      screen_Rows INIT(= 0);
-static int      screen_Columns INIT(= 0);
+static int      screen_Rows  = 0 ;
+static int      screen_Columns  = 0 ;
 
-static int      mod_mask INIT(= 0);
+static int      mod_mask  = 0 ;
 
-static int      vgetc_mod_mask INIT(= 0);
-static int      vgetc_char INIT(= 0);
+static int      vgetc_mod_mask  = 0 ;
+static int      vgetc_char  = 0 ;
 
 static int      cmdline_row;
 static int      cmdline_col_off;
 static int      cmdline_width;
 
-static int      redraw_cmdline INIT(= FALSE);
-static int      redraw_mode INIT(= FALSE);
-static int      clear_cmdline INIT(= FALSE);
-static int      mode_displayed INIT(= FALSE);
-static int      no_win_do_lines_ins INIT(= FALSE);
+static int      redraw_cmdline  = FALSE ;
+static int      redraw_mode  = FALSE ;
+static int      clear_cmdline  = FALSE ;
+static int      mode_displayed  = FALSE ;
+static int      no_win_do_lines_ins  = FALSE ;
 
-static int      exec_from_reg INIT(= FALSE);
+static int      exec_from_reg  = FALSE ;
 
-static int      screen_cleared INIT(= FALSE);
+static int      screen_cleared  = FALSE ;
 
-static colnr_T  dollar_vcol INIT(= -1);
+static colnr_T  dollar_vcol  = -1 ;
 
-static char_u   *edit_submode INIT(= NULL);
-static char_u   *edit_submode_pre INIT(= NULL);
-static char_u   *edit_submode_extra INIT(= NULL);
+static char_u   *edit_submode  = NULL ;
+static char_u   *edit_submode_pre  = NULL ;
+static char_u   *edit_submode_extra  = NULL ;
 static hlf_T    edit_submode_highl;
 
 static int      msg_col;
 static int      msg_row;
 static int      msg_scrolled;
-static int      msg_scrolled_ign INIT(= FALSE);
+static int      msg_scrolled_ign  = FALSE ;
 
-static char_u   *keep_msg INIT(= NULL);
-static int      keep_msg_attr INIT(= 0);
-static int      keep_msg_more INIT(= FALSE);
-static int      need_fileinfo INIT(= FALSE);
-static int      msg_scroll INIT(= FALSE);
-static int      msg_didout INIT(= FALSE);
-static int      msg_didany INIT(= FALSE);
-static int      msg_nowait INIT(= FALSE);
-static int      emsg_off INIT(= 0);
-static int      info_message INIT(= FALSE);
-static int      msg_hist_off INIT(= FALSE);
+static char_u   *keep_msg  = NULL ;
+static int      keep_msg_attr  = 0 ;
+static int      keep_msg_more  = FALSE ;
+static int      need_fileinfo  = FALSE ;
+static int      msg_scroll  = FALSE ;
+static int      msg_didout  = FALSE ;
+static int      msg_didany  = FALSE ;
+static int      msg_nowait  = FALSE ;
+static int      emsg_off  = 0 ;
+static int      info_message  = FALSE ;
+static int      msg_hist_off  = FALSE ;
 static int      did_emsg;
 static int      did_emsg_syntax;
 static int      called_emsg;
 static int      in_echowindow;
-static int      ex_exitval INIT(= 0);
-static int      emsg_on_display INIT(= FALSE);
-static int      rc_did_emsg INIT(= FALSE);
-
-static int      no_wait_return INIT(= 0);
-static int      need_wait_return INIT(= 0);
-static int      did_wait_return INIT(= FALSE);
-static int      need_maketitle INIT(= TRUE);
-
-static int      quit_more INIT(= FALSE);
-static int      newline_on_exit INIT(= FALSE);
-static int      intr_char INIT(= 0);
-static int      ex_keep_indent INIT(= FALSE);
-static int      vgetc_busy INIT(= 0);
-
-static int      didset_vim INIT(= FALSE);
-static int      didset_vimruntime INIT(= FALSE);
-
-static int      lines_left INIT(= -1);
-static int      msg_no_more INIT(= FALSE);
-
-static garray_T exestack INIT5(0, 0, sizeof(estack_T), 50, NULL);
-#define HAVE_SOURCING_INFO  (exestack.ga_data != NULL && exestack.ga_len > 0)
-#define SOURCING_NAME (((estack_T *)exestack.ga_data)[exestack.ga_len - 1].es_name)
-#define SOURCING_LNUM (((estack_T *)exestack.ga_data)[exestack.ga_len - 1].es_lnum)
+static int      ex_exitval  = 0 ;
+static int      emsg_on_display  = FALSE ;
+static int      rc_did_emsg  = FALSE ;
+
+static int      no_wait_return  = 0 ;
+static int      need_wait_return  = 0 ;
+static int      did_wait_return  = FALSE ;
+static int      need_maketitle  = TRUE ;
+
+static int      quit_more  = FALSE ;
+static int      newline_on_exit  = FALSE ;
+static int      intr_char  = 0 ;
+static int      ex_keep_indent  = FALSE ;
+static int      vgetc_busy  = 0 ;
+
+static int      didset_vim  = FALSE ;
+static int      didset_vimruntime  = FALSE ;
+
+static int      lines_left  = -1 ;
+static int      msg_no_more  = FALSE ;
+
+static garray_T exestack  = {0, 0, sizeof(estack_T), 50, NULL} ;
 
-static sctx_T   current_sctx INIT(= {0});
+static sctx_T   current_sctx  = {0} ;
 
 static char_u   hash_removed;
 
-static int      scroll_region INIT(= FALSE);
-static int      t_colors INIT(= 0);
+static int      scroll_region  = FALSE ;
+static int      t_colors  = 0 ;
 
-static int include_none INIT(= 0);
-static int include_default INIT(= 0);
-static int include_link INIT(= 0);
+static int include_none  = 0 ;
+static int include_default  = 0 ;
+static int include_link  = 0 ;
 
-static int      highlight_match INIT(= FALSE);
+static int      highlight_match  = FALSE ;
 static linenr_T search_match_lines;
 static colnr_T  search_match_endcol;
-static linenr_T search_first_line INIT(= 0);
-static linenr_T search_last_line INIT(= MAXLNUM);
+static linenr_T search_first_line  = 0 ;
+static linenr_T search_last_line  =  LONG_MAX  ;
 
-static int      no_smartcase INIT(= FALSE);
+static int      no_smartcase  = FALSE ;
 
-static int      need_check_timestamps INIT(= FALSE);
-static int      did_check_timestamps INIT(= FALSE);
-static int      no_check_timestamps INIT(= 0);
+static int      need_check_timestamps  = FALSE ;
+static int      did_check_timestamps  = FALSE ;
+static int      no_check_timestamps  = 0 ;
 
 static int      highlight_attr[HLF_COUNT];
-static int      cterm_normal_fg_color INIT(= 0);
-static int      cterm_normal_fg_bold INIT(= 0);
-static int      cterm_normal_bg_color INIT(= 0);
-static int      cterm_normal_ul_color INIT(= 0);
-static guicolor_T fallback_fg_rgb INIT(= INVALCOLOR);
-static guicolor_T fallback_bg_rgb INIT(= INVALCOLOR);
-
-static int      autocmd_busy INIT(= FALSE);
-static int      autocmd_no_enter INIT(= FALSE);
-static int      autocmd_no_leave INIT(= FALSE);
-static int      tabpage_move_disallowed INIT(= FALSE);
+static int      cterm_normal_fg_color  = 0 ;
+static int      cterm_normal_fg_bold  = 0 ;
+static int      cterm_normal_bg_color  = 0 ;
+static int      cterm_normal_ul_color  = 0 ;
+static  long  fallback_fg_rgb  =  (( long )0x1ffffff)  ;
+static  long  fallback_bg_rgb  =  (( long )0x1ffffff)  ;
+
+static int      autocmd_busy  = FALSE ;
+static int      autocmd_no_enter  = FALSE ;
+static int      autocmd_no_leave  = FALSE ;
+static int      tabpage_move_disallowed  = FALSE ;
 
-static bufref_T au_new_curbuf INIT3(NULL, 0, 0);
+static bufref_T au_new_curbuf  = {NULL, 0, 0} ;
 
-static buf_T    *au_pending_free_buf INIT(= NULL);
-static win_T    *au_pending_free_win INIT(= NULL);
+static buf_T    *au_pending_free_buf  = NULL ;
+static win_T    *au_pending_free_win  = NULL ;
 
 static int      mouse_row;
 static int      mouse_col;
-static int      mouse_past_bottom INIT(= FALSE);
-static int      mouse_past_eol INIT(= FALSE);
-static int      mouse_dragging INIT(= 0);
+static int      mouse_past_bottom  = FALSE ;
+static int      mouse_past_eol  = FALSE ;
+static int      mouse_dragging  = 0 ;
 
-static int      updating_screen INIT(= FALSE);
+static int      updating_screen  = FALSE ;
 
-static int      redraw_not_allowed INIT(= FALSE);
+static int      redraw_not_allowed  = FALSE ;
 
 static win_T    *firstwin;
 static win_T    *lastwin;
-static win_T    *prevwin INIT(= NULL);
-#define ONE_WINDOW (firstwin == lastwin)
-#define W_NEXT(wp) ((wp)->w_next)
+static win_T    *prevwin  = NULL ;
 
 static win_T    *curwin;
 
-#define AUCMD_WIN_COUNT 5
+enum { AUCMD_WIN_COUNT = 5 };
 
 typedef struct {
     win_T       *auc_win;
@@ -8943,84 +7393,84 @@
 
 static aucmdwin_T aucmd_win[AUCMD_WIN_COUNT];
 
-static int      pum_will_redraw INIT(= FALSE);
+static int      pum_will_redraw  = FALSE ;
 
 static frame_T  *topframe;
 
 static tabpage_T    *first_tabpage;
 static tabpage_T    *curtab;
 static tabpage_T    *lastused_tabpage;
-static int          redraw_tabline INIT(= FALSE);
-static int          redraw_vseps INIT(= FALSE);
+static int          redraw_tabline  = FALSE ;
+static int          redraw_vseps  = FALSE ;
 
-static buf_T    *firstbuf INIT(= NULL);
-static buf_T    *lastbuf INIT(= NULL);
-static buf_T    *curbuf INIT(= NULL);
+static buf_T    *firstbuf  = NULL ;
+static buf_T    *lastbuf  = NULL ;
+static buf_T    *curbuf  = NULL ;
 
-static int      mf_dont_release INIT(= FALSE);
+static int      mf_dont_release  = FALSE ;
 
 static alist_T  global_alist;
-static int      max_alist_id INIT(= 0);
-static int      arg_had_last INIT(= FALSE);
+static int      max_alist_id  = 0 ;
+static int      arg_had_last  = FALSE ;
 
 static int      ru_col;
 static int      sc_col;
 
-static DIR      *vim_tempdir_dp INIT(= NULL);
-static char_u   *vim_tempdir INIT(= NULL);
+static DIR      *vim_tempdir_dp  = NULL ;
+static char_u   *vim_tempdir  = NULL ;
 
-static int      starting INIT(= NO_SCREEN);
-static int      exiting INIT(= FALSE);
-static int      really_exiting INIT(= FALSE);
-static int      v_dying INIT(= 0);
-static int      stdout_isatty INIT(= TRUE);
+static int      starting  = NO_SCREEN ;
+static int      exiting  = FALSE ;
+static int      really_exiting  = FALSE ;
+static int      v_dying  = 0 ;
+static int      stdout_isatty  = TRUE ;
 
-static char     *last_chdir_reason INIT(= NULL);
-static volatile sig_atomic_t full_screen INIT(= FALSE);
+static char     *last_chdir_reason  = NULL ;
+static volatile sig_atomic_t full_screen  = FALSE ;
 
-static int      restricted INIT(= FALSE);
-static int      secure INIT(= FALSE);
+static int      restricted  = FALSE ;
+static int      secure  = FALSE ;
 
-static int      textlock INIT(= 0);
+static int      textlock  = 0 ;
 
-static int      curbuf_lock INIT(= 0);
-static int      allbuf_lock INIT(= 0);
+static int      curbuf_lock  = 0 ;
+static int      allbuf_lock  = 0 ;
 
-static int      silent_mode INIT(= FALSE);
+static int      silent_mode  = FALSE ;
 
 static pos_T    VIsual;
-static int      VIsual_active INIT(= FALSE);
-static int      VIsual_select INIT(= FALSE);
-static int      VIsual_select_reg INIT(= 0);
-static int  VIsual_select_exclu_adj INIT(= FALSE);
-static int      restart_VIsual_select INIT(= 0);
+static int      VIsual_active  = FALSE ;
+static int      VIsual_select  = FALSE ;
+static int      VIsual_select_reg  = 0 ;
+static int  VIsual_select_exclu_adj  = FALSE ;
+static int      restart_VIsual_select  = 0 ;
 static int      VIsual_reselect;
 
-static int      VIsual_mode INIT(= 'v');
+static int      VIsual_mode  = 'v' ;
 
-static int      redo_VIsual_busy INIT(= FALSE);
+static int      redo_VIsual_busy  = FALSE ;
 
-static int      resel_VIsual_mode INIT(= NUL);
+static int      resel_VIsual_mode  = NUL ;
 static linenr_T resel_VIsual_line_count;
 static colnr_T  resel_VIsual_vcol;
 
 static pos_T    where_paste_started;
 
-static int     did_ai INIT(= FALSE);
+static int     did_ai  = FALSE ;
 
-static colnr_T  ai_col INIT(= 0);
+static colnr_T  ai_col  = 0 ;
 
-static int     end_comment_pending INIT(= NUL);
+static int     end_comment_pending  = NUL ;
 
-static int     did_syncbind INIT(= FALSE);
+static int     did_syncbind  = FALSE ;
 
-static int      did_si INIT(= FALSE);
+static int      did_si  = FALSE ;
 
-static int      can_si INIT(= FALSE);
+static int      can_si  = FALSE ;
 
-static int      can_si_back INIT(= FALSE);
+static int      can_si_back  = FALSE ;
 
-static int      old_indent INIT(= 0);
+static int      old_indent  = 0 ;
 
 static pos_T    saved_cursor
                     = {0, 0, 0}
@@ -9030,64 +7480,63 @@
 
 static pos_T    Insstart_orig;
 
-static int      orig_line_count INIT(= 0);
-static int      vr_lines_changed INIT(= 0);
+static int      orig_line_count  = 0 ;
+static int      vr_lines_changed  = 0 ;
 
-#define DBCS_JPN        932
-#define DBCS_JPNU       9932
-#define DBCS_KOR        949
-#define DBCS_KORU       9949
-#define DBCS_CHS        936
-#define DBCS_CHSU       9936
-#define DBCS_CHT        950
-#define DBCS_CHTU       9950
-#define DBCS_2BYTE      1
-#define DBCS_DEBUG      (-1)
-
-static int      enc_dbcs INIT(= 0);
-static int      enc_unicode INIT(= 0);
-static int      enc_utf8 INIT(= FALSE);
-static int      enc_latin1like INIT(= TRUE);
-static int      has_mbyte INIT(= 0);
+enum { DBCS_JPN = 932 };
+enum { DBCS_JPNU = 9932 };
+enum { DBCS_KOR = 949 };
+enum { DBCS_KORU = 9949 };
+enum { DBCS_CHS = 936 };
+enum { DBCS_CHSU = 9936 };
+enum { DBCS_CHT = 950 };
+enum { DBCS_CHTU = 9950 };
+enum { DBCS_2BYTE = 1 };
+
+static int      enc_dbcs  = 0 ;
+static int      enc_unicode  = 0 ;
+static int      enc_utf8  = FALSE ;
+static int      enc_latin1like  = TRUE ;
+static int      has_mbyte  = 0 ;
 
 static char     mb_bytelen_tab[256];
 
 static vimconv_T input_conv;
 static vimconv_T output_conv;
 
-static int (*mb_ptr2len)(char_u *p) INIT(= latin_ptr2len);
+static int (*mb_ptr2len)(char_u *p)  = latin_ptr2len ;
 
-static int (*mb_ptr2len_len)(char_u *p, int size) INIT(= latin_ptr2len_len);
+static int (*mb_ptr2len_len)(char_u *p, int size)  = latin_ptr2len_len ;
 
-static int (*mb_char2len)(int c) INIT(= latin_char2len);
+static int (*mb_char2len)(int c)  = latin_char2len ;
 
-static int (*mb_char2bytes)(int c, char_u *buf) INIT(= latin_char2bytes);
+static int (*mb_char2bytes)(int c, char_u *buf)  = latin_char2bytes ;
 
-static int (*mb_ptr2cells)(char_u *p) INIT(= latin_ptr2cells);
-static int (*mb_ptr2cells_len)(char_u *p, int size) INIT(= latin_ptr2cells_len);
-static int (*mb_char2cells)(int c) INIT(= latin_char2cells);
-static int (*mb_off2cells)(unsigned off, unsigned max_off) INIT(= latin_off2cells);
-static int (*mb_ptr2char)(char_u *p) INIT(= latin_ptr2char);
+static int (*mb_ptr2cells)(char_u *p)  = latin_ptr2cells ;
+static int (*mb_ptr2cells_len)(char_u *p, int size)  = latin_ptr2cells_len ;
+static int (*mb_char2cells)(int c)  = latin_char2cells ;
+static int (*mb_off2cells)(unsigned off, unsigned max_off)  = latin_off2cells ;
+static int (*mb_ptr2char)(char_u *p)  = latin_ptr2char ;
 
-static int (*mb_head_off)(char_u *base, char_u *p) INIT(= latin_head_off);
+static int (*mb_head_off)(char_u *base, char_u *p)  = latin_head_off ;
 
-static int      State INIT(= MODE_NORMAL);
+static int      State  = MODE_NORMAL ;
 
-static int      finish_op INIT(= FALSE);
-static long     opcount INIT(= 0);
-static int      motion_force INIT(= 0);
+static int      finish_op  = FALSE ;
+static long     opcount  = 0 ;
+static int      motion_force  = 0 ;
 
-static int exmode_active INIT(= 0);
+static int exmode_active  = 0 ;
 
-static int pending_exmode_active INIT(= FALSE);
+static int pending_exmode_active  = FALSE ;
 
-static int ex_no_reprint INIT(= FALSE);
+static int ex_no_reprint  = FALSE ;
 
-static int reg_recording INIT(= 0);
-static int reg_executing INIT(= 0);
-static int pending_end_reg_executing INIT(= FALSE);
+static int reg_recording  = 0 ;
+static int reg_executing  = 0 ;
+static int pending_end_reg_executing  = FALSE ;
 
-static int seenModifyOtherKeys INIT(= FALSE);
+static int seenModifyOtherKeys  = FALSE ;
 
 typedef enum {
     MOKS_INITIAL,
@@ -9097,7 +7546,7 @@
     MOKS_AFTER_T_TE,
 } mokstate_T;
 
-static mokstate_T modify_otherkeys_state INIT(= MOKS_INITIAL);
+static mokstate_T modify_otherkeys_state  = MOKS_INITIAL ;
 
 typedef enum {
     KKPS_INITIAL,
@@ -9107,123 +7556,123 @@
     KKPS_AFTER_T_TE,
 } kkpstate_T;
 
-static kkpstate_T kitty_protocol_state INIT(= KKPS_INITIAL);
+static kkpstate_T kitty_protocol_state  = KKPS_INITIAL ;
 
-static int no_mapping INIT(= FALSE);
-static int no_zero_mapping INIT(= 0);
-static int allow_keys INIT(= FALSE);
-static int no_reduce_keys INIT(= FALSE);
-static int no_u_sync INIT(= 0);
+static int no_mapping  = FALSE ;
+static int no_zero_mapping  = 0 ;
+static int allow_keys  = FALSE ;
+static int no_reduce_keys  = FALSE ;
+static int no_u_sync  = 0 ;
 
-static int restart_edit INIT(= 0);
+static int restart_edit  = 0 ;
 static int arrow_used;
-static int      ins_at_eol INIT(= FALSE);
+static int      ins_at_eol  = FALSE ;
 
-static int      no_abbr INIT(= TRUE);
+static int      no_abbr  = TRUE ;
 
-static int      mapped_ctrl_c INIT(= FALSE);
-static int      ctrl_c_interrupts INIT(= TRUE);
+static int      mapped_ctrl_c  = FALSE ;
+static int      ctrl_c_interrupts  = TRUE ;
 
 static cmdmod_T cmdmod;
-static int      sticky_cmdmod_flags INIT(= 0);
+static int      sticky_cmdmod_flags  = 0 ;
 
-static int      msg_silent INIT(= 0);
-static int      emsg_silent INIT(= 0);
-static int      emsg_noredir INIT(= 0);
-static int      cmd_silent INIT(= FALSE);
+static int      msg_silent  = 0 ;
+static int      emsg_silent  = 0 ;
+static int      emsg_noredir  = 0 ;
+static int      cmd_silent  = FALSE ;
 
-static int      in_assert_fails INIT(= FALSE);
+static int      in_assert_fails  = FALSE ;
 
-static int      swap_exists_action INIT(= SEA_NONE);
-static int      swap_exists_did_quit INIT(= FALSE);
+static int      swap_exists_action  = SEA_NONE ;
+static int      swap_exists_did_quit  = FALSE ;
 
 static char_u   *IObuff;
 static char_u   *NameBuff;
 static char     msg_buf[MSG_BUF_LEN];
 
-static int      RedrawingDisabled INIT(= 0);
+static int      RedrawingDisabled  = 0 ;
 
-static int      readonlymode INIT(= FALSE);
-static int      recoverymode INIT(= FALSE);
+static int      readonlymode  = FALSE ;
+static int      recoverymode  = FALSE ;
 
 static typebuf_T typebuf
                     = {NULL, NULL, 0, 0, 0, 0, 0, 0, 0}
                     ;
-static int      typebuf_was_empty INIT(= FALSE);
+static int      typebuf_was_empty  = FALSE ;
 
-static int      ex_normal_busy INIT(= 0);
-static int      ex_normal_lock INIT(= 0);
+static int      ex_normal_busy  = 0 ;
+static int      ex_normal_lock  = 0 ;
 
 static int      stop_insert_mode;
 
 static int      KeyTyped;
 static int      KeyStuffed;
-static int      maptick INIT(= 0);
+static int      maptick  = 0 ;
 
-static int      must_redraw INIT(= 0);
-static int      skip_redraw INIT(= FALSE);
-static int      do_redraw INIT(= FALSE);
+static int      must_redraw  = 0 ;
+static int      skip_redraw  = FALSE ;
+static int      do_redraw  = FALSE ;
 
-static int      need_highlight_changed INIT(= TRUE);
+static int      need_highlight_changed  = TRUE ;
 
-#define NSCRIPT 15
+enum { NSCRIPT = 15 };
 static FILE     *scriptin[NSCRIPT];
-static int      curscript INIT(= 0);
-static FILE     *scriptout  INIT(= NULL);
-static int      read_cmd_fd INIT(= 0);
+static int      curscript  = 0 ;
+static FILE     *scriptout   = NULL ;
+static int      read_cmd_fd  = 0 ;
 
-static volatile sig_atomic_t got_int INIT(= FALSE);
+static volatile sig_atomic_t got_int  = FALSE ;
 
-static volatile sig_atomic_t got_sigusr1 INIT(= FALSE);
+static volatile sig_atomic_t got_sigusr1  = FALSE ;
 
-static int      termcap_active INIT(= FALSE);
-static tmode_T  cur_tmode INIT(= TMODE_COOK);
-static int      bangredo INIT(= FALSE);
+static int      termcap_active  = FALSE ;
+static tmode_T  cur_tmode  = TMODE_COOK ;
+static int      bangredo  = FALSE ;
 static int      searchcmdlen;
 
-static int      did_outofmem_msg INIT(= FALSE);
-static int      did_swapwrite_msg INIT(= FALSE);
-static int      undo_off INIT(= FALSE);
-static int      global_busy INIT(= 0);
-static int      listcmd_busy INIT(= FALSE);
-static int      need_start_insertmode INIT(= FALSE);
-static char_u   *last_cmdline INIT(= NULL);
-static char_u   *repeat_cmdline INIT(= NULL);
-static char_u   *new_last_cmdline INIT(= NULL);
-static char_u   *autocmd_fname INIT(= NULL);
+static int      did_outofmem_msg  = FALSE ;
+static int      did_swapwrite_msg  = FALSE ;
+static int      undo_off  = FALSE ;
+static int      global_busy  = 0 ;
+static int      listcmd_busy  = FALSE ;
+static int      need_start_insertmode  = FALSE ;
+static char_u   *last_cmdline  = NULL ;
+static char_u   *repeat_cmdline  = NULL ;
+static char_u   *new_last_cmdline  = NULL ;
+static char_u   *autocmd_fname  = NULL ;
 static int      autocmd_fname_full;
-static int      autocmd_bufnr INIT(= 0);
-static char_u   *autocmd_match INIT(= NULL);
-static int      aucmd_cmdline_changed_count INIT(= 0);
+static int      autocmd_bufnr  = 0 ;
+static char_u   *autocmd_match  = NULL ;
+static int      aucmd_cmdline_changed_count  = 0 ;
 
-static int      did_cursorhold INIT(= TRUE);
+static int      did_cursorhold  = TRUE ;
 static pos_T    last_cursormoved
                     = {0, 0, 0}
                     ;
 
-static int      postponed_split INIT(= 0);
-static int      postponed_split_flags INIT(= 0);
-static int      postponed_split_tab INIT(= 0);
-static int      g_tag_at_cursor INIT(= FALSE);
+static int      postponed_split  = 0 ;
+static int      postponed_split_flags  = 0 ;
+static int      postponed_split_tab  = 0 ;
+static int      g_tag_at_cursor  = FALSE ;
 
-static int      replace_offset INIT(= 0);
+static int      replace_offset  = 0 ;
 
-static char_u   *escape_chars INIT(= (char_u *)" \t\\\"|");
+static char_u   *escape_chars  = (char_u *)" \t\\\"|" ;
 
-static int      keep_help_flag INIT(= FALSE);
+static int      keep_help_flag  = FALSE ;
 
-static char_u   *empty_option INIT(= (char_u *)"");
+static char_u   *empty_option  = (char_u *)"" ;
 
-static int  redir_off INIT(= FALSE);
-static FILE *redir_fd INIT(= NULL);
+static int  redir_off  = FALSE ;
+static FILE *redir_fd  = NULL ;
 
-static int  save_p_ls INIT(= -1);
-static int  save_p_wmh INIT(= -1);
-static int  wild_menu_showing INIT(= 0);
-#define WM_SHOWN        1
-#define WM_SCROLLED     2
+static int  save_p_ls  = -1 ;
+static int  save_p_wmh  = -1 ;
+static int  wild_menu_showing  = 0 ;
+enum { WM_SHOWN = 1 };
+enum { WM_SCROLLED = 2 };
 
-static char *Version;
+static const char *Version;
 static char *longVersion;
 
 static char_u *default_vim_dir;
@@ -9233,756 +7682,627 @@
 static char_u *compiled_user;
 static char_u *compiled_sys;
 
-static char_u   *homedir INIT(= NULL);
+static char_u   *homedir  = NULL ;
 
-static char_u   *globaldir INIT(= NULL);
+static char_u   *globaldir  = NULL ;
 
-static int      km_stopsel INIT(= FALSE);
-static int      km_startsel INIT(= FALSE);
+static int      km_stopsel  = FALSE ;
+static int      km_startsel  = FALSE ;
 
-static int      cmdwin_type INIT(= 0);
-static int      cmdwin_result INIT(= 0);
-static buf_T    *cmdwin_buf INIT(= NULL);
-static win_T    *cmdwin_win INIT(= NULL);
-
-static char_u no_lines_msg[]    INIT(= N_("--No lines in buffer--"));
-
-static char typename_unknown[]  INIT(= N_("unknown"));
-static char typename_int[]      INIT(= "int");
-static char typename_longint[]  INIT(= "long int");
-static char typename_longlongint[]      INIT(= "long long int");
-static char typename_unsignedint[]      INIT(= "unsigned int");
-static char typename_unsignedlongint[]  INIT(= "unsigned long int");
-static char typename_unsignedlonglongint[]      INIT(= "unsigned long long int");
-static char typename_pointer[]  INIT(= N_("pointer"));
-static char typename_percent[]  INIT(= N_("percent"));
-static char typename_char[] INIT(= "char");
-static char typename_string[]   INIT(= N_("string"));
-static char typename_float[]    INIT(= "float");
+static int      cmdwin_type  = 0 ;
+static int      cmdwin_result  = 0 ;
+static buf_T    *cmdwin_buf  = NULL ;
+static win_T    *cmdwin_win  = NULL ;
+
+static char_u no_lines_msg[]     =  "--No lines in buffer--"  ;
+
+static char typename_unknown[]   =  "unknown"  ;
+static char typename_int[]       = "int" ;
+static char typename_longint[]   = "long int" ;
+static char typename_longlongint[]       = "long long int" ;
+static char typename_unsignedint[]       = "unsigned int" ;
+static char typename_unsignedlongint[]   = "unsigned long int" ;
+static char typename_unsignedlonglongint[]       = "unsigned long long int" ;
+static char typename_pointer[]   =  "pointer"  ;
+static char typename_percent[]   =  "percent"  ;
+static char typename_char[]  = "char" ;
+static char typename_string[]    =  "string"  ;
+static char typename_float[]     = "float" ;
 
 static long     sub_nsubs;
 static linenr_T sub_nlines;
 
 static char_u   wim_flags[4];
 
-static int      term_is_xterm INIT(= FALSE);
+static int      term_is_xterm  = FALSE ;
 
-static int      virtual_op INIT(= MAYBE);
+static int      virtual_op  = MAYBE ;
 
-static char top_bot_msg[]   INIT(= N_("search hit TOP, continuing at BOTTOM"));
-static char bot_top_msg[]   INIT(= N_("search hit BOTTOM, continuing at TOP"));
+static char top_bot_msg[]    =  "search hit TOP, continuing at BOTTOM"  ;
+static char bot_top_msg[]    =  "search hit BOTTOM, continuing at TOP"  ;
 
-static char line_msg[]      INIT(= N_(" line "));
+static char line_msg[]       =  " line "  ;
 
 static int vim_ignored;
 static char *vim_ignoredp;
 
-static optmagic_T magic_overruled INIT(= OPTION_MAGIC_NOT_SET);
+static optmagic_T magic_overruled  = OPTION_MAGIC_NOT_SET ;
 
-static int skip_win_fix_cursor INIT(= FALSE);
-static int skip_win_fix_scroll INIT(= FALSE);
-static int skip_update_topline INIT(= FALSE);
+static int skip_win_fix_cursor  = FALSE ;
+static int skip_win_fix_scroll  = FALSE ;
+static int skip_update_topline  = FALSE ;
 
-#define SHOWCMD_BUFLEN (SHOWCMD_COLS + 1 + 30)
-static char_u showcmd_buf[SHOWCMD_BUFLEN];
+static char_u showcmd_buf[ (SHOWCMD_COLS + 1 + 30) ];
 
-static bool did_warn_clipboard INIT(= FALSE);
+static bool did_warn_clipboard  = FALSE ;
 
-static int allow_osc_key INIT(= 0);
+static int allow_osc_key  = 0 ;
 
-static int silence_w23_w24_msg INIT( = 0);
+static int silence_w23_w24_msg  = 0 ;
 
 // ---------------- end globals.h ----------------
 // ---------------- begin errors.h ----------------
 
-# define PLURAL_MSG(var1, msg1, var2, msg2)     static char var1[] = msg1;     static char var2[] = msg2;
+static char e_interrupted[]  =  "Interrupted"  ;
 
-static char e_interrupted[] INIT(= N_("Interrupted"));
-
-static char e_backslash_should_be_followed_by[] INIT(= N_("E10: \\ should be followed by /, ? or &"));
-static char e_invalid_in_cmdline_window[] INIT(= N_("E11: Invalid in command-line window; :q<CR> closes the window"));
-static char e_command_not_allowed_from_vimrc_in_current_dir_or_tag_search[] INIT(= N_("E12: Command not allowed from exrc/vimrc in current dir or tag search"));
-static char e_file_exists[] INIT(= N_("E13: File exists (add ! to override)"));
-static char e_invalid_range[] INIT(= N_("E16: Invalid range"));
-static char e_str_is_directory[] INIT(= N_("E17: \"%s\" is a directory"));
-static char e_mark_has_invalid_line_number[] INIT(= N_("E19: Mark has invalid line number"));
-static char e_mark_not_set[] INIT(= N_("E20: Mark not set"));
-static char e_cannot_make_changes_modifiable_is_off[] INIT(= N_("E21: Cannot make changes, 'modifiable' is off"));
-static char e_scripts_nested_too_deep[] INIT(= N_("E22: Scripts nested too deep"));
-static char e_no_alternate_file[] INIT(= N_("E23: No alternate file"));
-static char e_no_such_abbreviation[] INIT(= N_("E24: No such abbreviation"));
-static char e_gui_cannot_be_used_not_enabled_at_compile_time[] INIT(= N_("E25: GUI cannot be used: Not enabled at compile time"));
-static char e_hebrew_cannot_be_used_not_enabled_at_compile_time[] INIT(= N_("E26: Hebrew cannot be used: Not enabled at compile time\n"));
-static char e_farsi_support_has_been_removed[] INIT(= N_("E27: Farsi support has been removed\n"));
-static char e_no_such_highlight_group_name_str[] INIT(= N_("E28: No such highlight group name: %s"));
-static char e_no_inserted_text_yet[] INIT(= N_("E29: No inserted text yet"));
-static char e_no_previous_command_line[] INIT(= N_("E30: No previous command line"));
-static char e_no_such_mapping[] INIT(= N_("E31: No such mapping"));
-static char e_no_file_name[] INIT(= N_("E32: No file name"));
-static char e_no_previous_substitute_regular_expression[] INIT(= N_("E33: No previous substitute regular expression"));
-static char e_no_previous_command[] INIT(= N_("E34: No previous command"));
-static char e_no_previous_regular_expression[] INIT(= N_("E35: No previous regular expression"));
-static char e_not_enough_room[] INIT(= N_("E36: Not enough room"));
-static char e_no_write_since_last_change[] INIT(= N_("E37: No write since last change"));
-static char e_no_write_since_last_change_add_bang_to_override[] INIT(= N_("E37: No write since last change (add ! to override)"));
-static char e_null_argument[] INIT(= "E38: Null argument");
-static char e_out_of_memory[] INIT(= N_("E41: Out of memory!"));
-static char e_damaged_match_string[] INIT(= "E43: Damaged match string");
-static char e_corrupted_regexp_program[] INIT(= "E44: Corrupted regexp program");
-static char e_readonly_option_is_set_add_bang_to_override[] INIT(= N_("E45: 'readonly' option is set (add ! to override)"));
-static char e_invalid_scroll_size[] INIT(= N_("E49: Invalid scroll size"));
-static char e_too_many_str_open[] INIT(= N_("E51: Too many %s("));
-static char e_unmatched_str_percent_open[] INIT(= N_("E53: Unmatched %s%%("));
-static char e_unmatched_str_open[] INIT(= N_("E54: Unmatched %s("));
-static char e_unmatched_str_close[] INIT(= N_("E55: Unmatched %s)"));
-static char e_invalid_character_after_str_at[] INIT(= N_("E59: Invalid character after %s@"));
-static char e_too_many_complex_str_curly[] INIT(= N_("E60: Too many complex %s{...}s"));
-static char e_nested_str[] INIT(= N_("E61: Nested %s*"));
-static char e_nested_str_chr[] INIT(= N_("E62: Nested %s%c"));
-static char e_invalid_use_of_underscore[] INIT(= N_("E63: Invalid use of \\_"));
-static char e_str_chr_follows_nothing[] INIT(= N_("E64: %s%c follows nothing"));
-static char e_illegal_back_reference[] INIT(= N_("E65: Illegal back reference"));
-static char e_invalid_character_after_bsl_z[] INIT(= N_("E68: Invalid character after \\z"));
-static char e_missing_sb_after_str[] INIT(= N_("E69: Missing ] after %s%%["));
-static char e_empty_str_brackets[] INIT(= N_("E70: Empty %s%%[]"));
-static char e_invalid_character_after_str[] INIT(= N_("E71: Invalid character after %s%%"));
-static char e_close_error_on_swap_file[] INIT(= N_("E72: Close error on swap file"));
-static char e_tag_stack_empty[] INIT(= N_("E73: Tag stack empty"));
-static char e_command_too_complex[] INIT(= N_("E74: Command too complex"));
-static char e_name_too_long[] INIT(= N_("E75: Name too long"));
-static char e_too_many_brackets[] INIT(= N_("E76: Too many ["));
-static char e_too_many_file_names[] INIT(= N_("E77: Too many file names"));
-static char e_unknown_mark[] INIT(= N_("E78: Unknown mark"));
-static char e_cannot_expand_wildcards[] INIT(= N_("E79: Cannot expand wildcards"));
-static char e_error_while_writing[] INIT(= N_("E80: Error while writing"));
-static char e_cannot_allocate_any_buffer_exiting[] INIT(= N_("E82: Cannot allocate any buffer, exiting..."));
-static char e_cannot_allocate_buffer_using_other_one[] INIT(= N_("E83: Cannot allocate buffer, using other one..."));
-static char e_no_modified_buffer_found[] INIT(= N_("E84: No modified buffer found"));
-static char e_there_is_no_listed_buffer[] INIT(= N_("E85: There is no listed buffer"));
-static char e_buffer_nr_does_not_exist[] INIT(= N_("E86: Buffer %ld does not exist"));
-static char e_cannot_go_beyond_last_buffer[] INIT(= N_("E87: Cannot go beyond last buffer"));
-static char e_cannot_go_before_first_buffer[] INIT(= N_("E88: Cannot go before first buffer"));
-static char e_no_write_since_last_change_for_buffer_nr_add_bang_to_override[] INIT(= N_("E89: No write since last change for buffer %d (add ! to override)"));
-static char e_cannot_unload_last_buffer[] INIT(= N_("E90: Cannot unload last buffer"));
-static char e_shell_option_is_empty[] INIT(= N_("E91: 'shell' option is empty"));
-static char e_buffer_nr_not_found[] INIT(= N_("E92: Buffer %d not found"));
-static char e_more_than_one_match_for_str[] INIT(= N_("E93: More than one match for %s"));
-static char e_no_matching_buffer_for_str[] INIT(= N_("E94: No matching buffer for %s"));
-static char e_buffer_with_this_name_already_exists[] INIT(= N_("E95: Buffer with this name already exists"));
-static char e_cannot_move_range_of_lines_into_itself[] INIT(= N_("E134: Cannot move a range of lines into itself"));
-static char e_filter_autocommands_must_not_change_current_buffer[] INIT(= N_("E135: *Filter* Autocommands must not change current buffer"));
-static char e_file_is_loaded_in_another_buffer[] INIT(= N_("E139: File is loaded in another buffer"));
-static char e_use_bang_to_write_partial_buffer[] INIT(= N_("E140: Use ! to write partial buffer"));
-static char e_no_file_name_for_buffer_nr[] INIT(= N_("E141: No file name for buffer %ld"));
-static char e_file_not_written_writing_is_disabled_by_write_option[] INIT(= N_("E142: File not written: Writing is disabled by 'write' option"));
-static char e_autocommands_unexpectedly_deleted_new_buffer_str[] INIT(= N_("E143: Autocommands unexpectedly deleted new buffer %s"));
-static char e_non_numeric_argument_to_z[] INIT(= N_("E144: Non-numeric argument to :z"));
-static char e_shell_commands_and_some_functionality_not_allowed_in_rvim[] INIT(= N_("E145: Shell commands and some functionality not allowed in rvim"));
-static char e_regular_expressions_cant_be_delimited_by_letters[] INIT(= N_("E146: Regular expressions can't be delimited by letters"));
-static char e_cannot_do_global_recursive_with_range[] INIT(= N_("E147: Cannot do :global recursive with a range"));
-static char e_regular_expression_missing_from_global[] INIT(= N_("E148: Regular expression missing from :global"));
-static char e_sorry_no_help_for_str[] INIT(= N_("E149: Sorry, no help for %s"));
-static char e_not_a_directory_str[] INIT(= N_("E150: Not a directory: %s"));
-static char e_no_match_str_1[] INIT(= N_("E151: No match: %s"));
-static char e_cannot_open_str_for_writing_1[] INIT(= N_("E152: Cannot open %s for writing"));
-static char e_unable_to_open_str_for_reading[] INIT(= N_("E153: Unable to open %s for reading"));
-static char e_duplicate_tag_str_in_file_str_str[] INIT(= N_("E154: Duplicate tag \"%s\" in file %s/%s"));
-static char e_no_write_since_last_change_for_buffer_str[] INIT(= N_("E162: No write since last change for buffer \"%s\""));
-static char e_there_is_only_one_file_to_edit[] INIT(= N_("E163: There is only one file to edit"));
-static char e_cannot_go_before_first_file[] INIT(= N_("E164: Cannot go before first file"));
-static char e_cannot_go_beyond_last_file[] INIT(= N_("E165: Cannot go beyond last file"));
-static char e_cant_open_linked_file_for_writing[] INIT(= N_("E166: Can't open linked file for writing"));
-static char e_scriptencoding_used_outside_of_sourced_file[] INIT(= N_("E167: :scriptencoding used outside of a sourced file"));
-static char e_command_too_recursive[] INIT(= N_("E169: Command too recursive"));
-
-PLURAL_MSG(e_nr_more_file_to_edit, "E173: %d more file to edit", e_nr_more_files_to_edit, "E173: %d more files to edit")
-
-static char e_command_already_exists_add_bang_to_replace_it_str[] INIT(= N_("E174: Command already exists: add ! to replace it: %s"));
-static char e_no_attribute_specified[] INIT(= N_("E175: No attribute specified"));
-static char e_invalid_number_of_arguments[] INIT(= N_("E176: Invalid number of arguments"));
-static char e_count_cannot_be_specified_twice[] INIT(= N_("E177: Count cannot be specified twice"));
-static char e_invalid_default_value_for_count[] INIT(= N_("E178: Invalid default value for count"));
-static char e_argument_required_for_str[] INIT(= N_("E179: Argument required for %s"));
-static char e_invalid_complete_value_str[] INIT(= N_("E180: Invalid complete value: %s"));
-static char e_invalid_address_type_value_str[] INIT(= N_("E180: Invalid address type value: %s"));
-static char e_invalid_attribute_str[] INIT(= N_("E181: Invalid attribute: %s"));
-static char e_invalid_command_name[] INIT(= N_("E182: Invalid command name"));
-static char e_user_defined_commands_must_start_with_an_uppercase_letter[] INIT(= N_("E183: User defined commands must start with an uppercase letter"));
-static char e_no_such_user_defined_command_str[] INIT(= N_("E184: No such user-defined command: %s"));
-static char e_cannot_find_color_scheme_str[] INIT(= N_("E185: Cannot find color scheme '%s'"));
-static char e_no_previous_directory[] INIT(= N_("E186: No previous directory"));
-static char e_directory_unknown[] INIT(= N_("E187: Directory unknown"));
-static char e_obtaining_window_position_not_implemented_for_this_platform[] INIT(= N_("E188: Obtaining window position not implemented for this platform"));
-static char e_str_exists_add_bang_to_override[] INIT(= N_("E189: \"%s\" exists (add ! to override)"));
-static char e_cannot_open_str_for_writing_2[] INIT(= N_("E190: Cannot open \"%s\" for writing"));
-static char e_argument_must_be_letter_or_forward_backward_quote[] INIT(= N_("E191: Argument must be a letter or forward/backward quote"));
-static char e_recursive_use_of_normal_too_deep[] INIT(= N_("E192: Recursive use of :normal too deep"));
-static char e_no_alternate_file_name_to_substitute_for_hash[] INIT(= N_("E194: No alternate file name to substitute for '#'"));
-static char e_no_digraphs_version[] INIT(= N_("E196: No digraphs in this version"));
-static char e_cannot_set_language_to_str[] INIT(= N_("E197: Cannot set language to \"%s\""));
-static char e_active_window_or_buffer_changed_or_deleted[] INIT(= N_("E199: Active window or buffer changed or deleted"));
-static char e_readpre_autocommands_made_file_unreadable[] INIT(= N_("E200: *ReadPre autocommands made the file unreadable"));
-static char e_readpre_autocommands_must_not_change_current_buffer[] INIT(= N_("E201: *ReadPre autocommands must not change current buffer"));
-static char e_autocommands_deleted_or_unloaded_buffer_to_be_written[] INIT(= N_("E203: Autocommands deleted or unloaded buffer to be written"));
-static char e_autocommands_changed_number_of_lines_in_unexpected_way[] INIT(= N_("E204: Autocommand changed number of lines in unexpected way"));
-static char e_patchmode_cant_save_original_file[] INIT(= N_("E205: Patchmode: can't save original file"));
-static char e_patchmode_cant_touch_empty_original_file[] INIT(= N_("E206: Patchmode: can't touch empty original file"));
-static char e_cant_delete_backup_file[] INIT(= N_("E207: Can't delete backup file"));
-static char e_error_writing_to_str[] INIT(= N_("E208: Error writing to \"%s\""));
-static char e_error_closing_str[] INIT(= N_("E209: Error closing \"%s\""));
-static char e_error_reading_str[] INIT(= N_("E210: Error reading \"%s\""));
-static char e_file_str_no_longer_available[] INIT(= N_("E211: File \"%s\" no longer available"));
-static char e_cant_open_file_for_writing[] INIT(= N_("E212: Can't open file for writing"));
-static char e_cannot_convert_add_bang_to_write_without_conversion[] INIT(= N_("E213: Cannot convert (add ! to write without conversion)"));
-static char e_illegal_character_after_star_str[] INIT(= N_("E215: Illegal character after *: %s"));
-static char e_no_such_event_str[] INIT(= N_("E216: No such event: %s"));
-static char e_no_such_group_or_event_str[] INIT(= N_("E216: No such group or event: %s"));
-static char e_cant_execute_autocommands_for_all_events[] INIT(= N_("E217: Can't execute autocommands for ALL events"));
-static char e_autocommand_nesting_too_deep[] INIT(= N_("E218: Autocommand nesting too deep"));
-static char e_missing_open_curly[] INIT(= N_("E219: Missing {."));
-static char e_missing_close_curly[] INIT(= N_("E220: Missing }."));
-static char e_add_to_internal_buffer_that_was_already_read_from[] INIT(= "E222: Add to internal buffer that was already read from");
-static char e_recursive_mapping[] INIT(= N_("E223: Recursive mapping"));
-static char e_global_abbreviation_already_exists_for_str[] INIT(= N_("E224: Global abbreviation already exists for %s"));
-static char e_global_mapping_already_exists_for_str[] INIT(= N_("E225: Global mapping already exists for %s"));
-static char e_abbreviation_already_exists_for_str[] INIT(= N_("E226: Abbreviation already exists for %s"));
-static char e_mapping_already_exists_for_str[] INIT(= N_("E227: Mapping already exists for %s"));
-static char e_makemap_illegal_mode[] INIT(= "E228: makemap: Illegal mode");
-static char e_cant_split_window_while_closing_another[] INIT(= N_("E242: Can't split a window while closing another"));
-static char e_filechangedshell_autocommand_deleted_buffer[] INIT(= N_("E246: FileChangedShell autocommand deleted buffer"));
-static char e_window_layout_changed_unexpectedly[] INIT(= N_("E249: Window layout changed unexpectedly"));
-static char e_cannot_read_from_str_2[] INIT(= N_("E282: Cannot read from \"%s\""));
-static char e_no_marks_matching_str[] INIT(= N_("E283: No marks matching \"%s\""));
-static char e_invalid_count_for_del_bytes_nr[] INIT(= "E292: Invalid count for del_bytes(): %ld");
-static char e_block_was_not_locked[] INIT(= "E293: Block was not locked");
-static char e_seek_error_in_swap_file_read[] INIT(= N_("E294: Seek error in swap file read"));
-static char e_read_error_in_swap_file[] INIT(= N_("E295: Read error in swap file"));
-static char e_seek_error_in_swap_file_write[] INIT(= N_("E296: Seek error in swap file write"));
-static char e_write_error_in_swap_file[] INIT(= N_("E297: Write error in swap file"));
-static char e_didnt_get_block_nr_zero[] INIT(= "E298: Didn't get block nr 0?");
-static char e_didnt_get_block_nr_one[] INIT(= "E298: Didn't get block nr 1?");
-static char e_didnt_get_block_nr_two[] INIT(= "E298: Didn't get block nr 2?");
-static char e_swap_file_already_exists_symlink_attack[] INIT(= N_("E300: Swap file already exists (symlink attack?)"));
-static char e_oops_lost_the_swap_file[] INIT(= N_("E301: Oops, lost the swap file!!!"));
-static char e_could_not_rename_swap_file[] INIT(= N_("E302: Could not rename swap file"));
-static char e_unable_to_open_swap_file_for_str_recovery_impossible[] INIT(= N_("E303: Unable to open swap file for \"%s\", recovery impossible"));
-static char e_ml_upd_block0_didnt_get_block_zero[] INIT(= "E304: ml_upd_block0(): Didn't get block 0??");
-static char e_no_swap_file_found_for_str[] INIT(= N_("E305: No swap file found for %s"));
-static char e_cannot_open_str[] INIT(= N_("E306: Cannot open %s"));
-static char e_str_does_not_look_like_vim_swap_file[] INIT(= N_("E307: %s does not look like a Vim swap file"));
-static char e_warning_original_file_may_have_been_changed[] INIT(= N_("E308: Warning: Original file may have been changed"));
-static char e_unable_to_read_block_one_from_str[] INIT(= N_("E309: Unable to read block 1 from %s"));
-static char e_block_one_id_wrong_str_not_swp_file[] INIT(= N_("E310: Block 1 ID wrong (%s not a .swp file?)"));
-static char e_recovery_interrupted[] INIT(= N_("E311: Recovery Interrupted"));
-static char e_errors_detected_while_recovering_look_for_lines_starting_with_questions[] INIT(= N_("E312: Errors detected while recovering; look for lines starting with ???"));
-static char e_cannot_preserve_there_is_no_swap_file[] INIT(= N_("E313: Cannot preserve, there is no swap file"));
-static char e_preserve_failed[] INIT(= N_("E314: Preserve failed"));
-static char e_ml_get_invalid_lnum_nr[] INIT(= "E315: ml_get: Invalid lnum: %ld");
-static char e_ml_get_cannot_find_line_nr_in_buffer_nr_str[] INIT(= "E316: ml_get: Cannot find line %ld in buffer %d %s");
-static char e_pointer_block_id_wrong[] INIT(= "E317: Pointer block id wrong");
-static char e_pointer_block_id_wrong_two[] INIT(= "E317: Pointer block id wrong 2");
-static char e_pointer_block_id_wrong_three[] INIT(= "E317: Pointer block id wrong 3");
-static char e_pointer_block_id_wrong_four[] INIT(= "E317: Pointer block id wrong 4");
-static char e_updated_too_many_blocks[] INIT(= "E318: Updated too many blocks?");
-static char e_sorry_command_is_not_available_in_this_version[] INIT(= N_("E319: Sorry, the command is not available in this version"));
-static char e_cannot_find_line_nr[] INIT(= "E320: Cannot find line %ld");
-static char e_could_not_reload_str[] INIT(= N_("E321: Could not reload \"%s\""));
-static char e_line_number_out_of_range_nr_past_the_end[] INIT(= "E322: Line number out of range: %ld past the end");
-static char e_line_count_wrong_in_block_nr[] INIT(= "E323: Line count wrong in block %ld");
-static char e_attention[] INIT(= N_("E325: ATTENTION"));
-static char e_too_many_swap_files_found[] INIT(= N_("E326: Too many swap files found"));
-static char e_pattern_too_long[] INIT(= N_("E339: Pattern too long"));
-static char e_internal_error_please_report_a_bug[] INIT(= N_("E340: Internal error; if you can reproduce please report a bug"));
-static char e_internal_error_lalloc_zero[] INIT(= "E341: Internal error: lalloc(0, )");
-static char e_out_of_memory_allocating_nr_bytes[] INIT(= N_("E342: Out of memory!  (allocating %lu bytes)"));
-static char e_invalid_path_number_must_be_at_end_of_path_or_be_followed_by_str[] INIT(= N_("E343: Invalid path: '**[number]' must be at the end of the path or be followed by '%s'."));
-static char e_cant_find_directory_str_in_cdpath[] INIT(= N_("E344: Can't find directory \"%s\" in cdpath"));
-static char e_cant_find_file_str_in_path[] INIT(= N_("E345: Can't find file \"%s\" in path"));
-static char e_no_more_directory_str_found_in_cdpath[] INIT(= N_("E346: No more directory \"%s\" found in cdpath"));
-static char e_no_more_file_str_found_in_path[] INIT(= N_("E347: No more file \"%s\" found in path"));
-static char e_no_string_under_cursor[] INIT(= N_("E348: No string under cursor"));
-static char e_no_identifier_under_cursor[] INIT(= N_("E349: No identifier under cursor"));
-static char e_nothing_in_register_str[] INIT(= N_("E353: Nothing in register %s"));
-static char e_invalid_register_name_str[] INIT(= N_("E354: Invalid register name: '%s'"));
-static char e_unknown_option_str_2[] INIT(= N_("E355: Unknown option: %s"));
-static char e_get_varp_error[] INIT(= "E356: get_varp ERROR");
-static char e_screen_mode_setting_not_supported[] INIT(= N_("E359: Screen mode setting not supported"));
-static char e_pattern_uses_more_memory_than_maxmempattern[] INIT(= N_("E363: Pattern uses more memory than 'maxmempattern'"));
-static char e_no_such_group_str[] INIT(= N_("E367: No such group: \"%s\""));
-static char e_invalid_item_in_str_brackets[] INIT(= N_("E369: Invalid item in %s%%[]"));
-static char e_cannot_write_buftype_option_is_set[] INIT(= N_("E382: Cannot write, 'buftype' option is set"));
-static char e_invalid_search_string_str[] INIT(= N_("E383: Invalid search string: %s"));
-static char e_search_hit_top_without_match_for_str[] INIT(= N_("E384: Search hit TOP without match for: %s"));
-static char e_search_hit_bottom_without_match_for_str[] INIT(= N_("E385: Search hit BOTTOM without match for: %s"));
-static char e_expected_question_or_slash_after_semicolon[] INIT(= N_("E386: Expected '?' or '/'  after ';'"));
-static char e_highlight_group_name_not_found_str[] INIT(= N_("E411: Highlight group not found: %s"));
-static char e_not_enough_arguments_highlight_link_str[] INIT(= N_("E412: Not enough arguments: \":highlight link %s\""));
-static char e_too_many_arguments_highlight_link_str[] INIT(= N_("E413: Too many arguments: \":highlight link %s\""));
-static char e_group_has_settings_highlight_link_ignored[] INIT(= N_("E414: Group has settings, highlight link ignored"));
-static char e_unexpected_equal_sign_str[] INIT(= N_("E415: Unexpected equal sign: %s"));
-static char e_missing_equal_sign_str_2[] INIT(= N_("E416: Missing equal sign: %s"));
-static char e_missing_argument_str[] INIT(= N_("E417: Missing argument: %s"));
-static char e_illegal_value_str[] INIT(= N_("E418: Illegal value: %s"));
-static char e_fg_color_unknown[] INIT(= N_("E419: FG color unknown"));
-static char e_bg_color_unknown[] INIT(= N_("E420: BG color unknown"));
-static char e_color_name_or_number_not_recognized_str[] INIT(= N_("E421: Color name or number not recognized: %s"));
-static char e_terminal_code_too_long_str[] INIT(= N_("E422: Terminal code too long: %s"));
-static char e_illegal_argument_str_3[] INIT(= N_("E423: Illegal argument: %s"));
-static char e_too_many_different_highlighting_attributes_in_use[] INIT(= N_("E424: Too many different highlighting attributes in use"));
-static char e_cannot_go_before_first_matching_tag[] INIT(= N_("E425: Cannot go before first matching tag"));
-static char e_tag_not_found_str[] INIT(= N_("E426: Tag not found: %s"));
-static char e_there_is_only_one_matching_tag[] INIT(= N_("E427: There is only one matching tag"));
-static char e_cannot_go_beyond_last_matching_tag[] INIT(= N_("E428: Cannot go beyond last matching tag"));
-static char e_file_str_does_not_exist[] INIT(= N_("E429: File \"%s\" does not exist"));
-static char e_format_error_in_tags_file_str[] INIT(= N_("E431: Format error in tags file \"%s\""));
-static char e_tags_file_not_sorted_str[] INIT(= N_("E432: Tags file not sorted: %s"));
-static char e_no_tags_file[] INIT(= N_("E433: No tags file"));
-static char e_cannot_find_tag_pattern[] INIT(= N_("E434: Can't find tag pattern"));
-static char e_couldnt_find_tag_just_guessing[] INIT(= N_("E435: Couldn't find tag, just guessing!"));
-static char e_no_str_entry_in_termcap[] INIT(= N_("E436: No \"%s\" entry in termcap"));
-static char e_terminal_capability_cm_required[] INIT(= N_("E437: Terminal capability \"cm\" required"));
-static char e_u_undo_line_numbers_wrong[] INIT(= "E438: u_undo: Line numbers wrong");
-static char e_undo_list_corrupt[] INIT(= "E439: Undo list corrupt");
-static char e_undo_line_missing[] INIT(= "E440: Undo line missing");
-static char e_cant_split_topleft_and_botright_at_the_same_time[] INIT(= N_("E442: Can't split topleft and botright at the same time"));
-static char e_cannot_rotate_when_another_window_is_split[] INIT(= N_("E443: Cannot rotate when another window is split"));
-static char e_cannot_close_last_window[] INIT(= N_("E444: Cannot close last window"));
-static char e_other_window_contains_changes[] INIT(= N_("E445: Other window contains changes"));
-static char e_no_file_name_under_cursor[] INIT(= N_("E446: No file name under cursor"));
-static char e_cant_find_file_str_in_path_2[] INIT(= N_("E447: Can't find file \"%s\" in path"));
-static char e_ul_color_unknown[] INIT(= N_("E453: UL color unknown"));
-static char e_cannot_go_back_to_previous_directory[] INIT(= N_("E459: Cannot go back to previous directory"));
-static char e_could_not_prepare_for_reloading_str[] INIT(= N_("E462: Could not prepare for reloading \"%s\""));
-static char e_ambiguous_use_of_user_defined_command[] INIT(= N_("E464: Ambiguous use of user-defined command"));
-static char e_winsize_requires_two_number_arguments[] INIT(= N_("E465: :winsize requires two number arguments"));
-static char e_winpos_requires_two_number_arguments[] INIT(= N_("E466: :winpos requires two number arguments"));
-static char e_completion_argument_only_allowed_for_custom_completion[] INIT(= N_("E468: Completion argument only allowed for custom completion"));
-static char e_command_aborted[] INIT(= N_("E470: Command aborted"));
-static char e_argument_required[] INIT(= N_("E471: Argument required"));
-static char e_command_failed[] INIT(= N_("E472: Command failed"));
-static char e_internal_error_in_regexp[] INIT(= "E473: Internal error in regexp");
-static char e_invalid_argument[] INIT(= N_("E474: Invalid argument"));
-static char e_invalid_argument_str[] INIT(= N_("E475: Invalid argument: %s"));
-static char e_invalid_value_for_argument_str[] INIT(= N_("E475: Invalid value for argument %s"));
-static char e_invalid_command[] INIT(= N_("E476: Invalid command"));
-static char e_invalid_command_str[] INIT(= N_("E476: Invalid command: %s"));
-static char e_no_bang_allowed[] INIT(= N_("E477: No ! allowed"));
-static char e_dont_panic[] INIT(= N_("E478: Don't panic!"));
-static char e_no_match[] INIT(= N_("E479: No match"));
-static char e_no_match_str_2[] INIT(= N_("E480: No match: %s"));
-static char e_no_range_allowed[] INIT(= N_("E481: No range allowed"));
-static char e_cant_create_file_str[] INIT(= N_("E482: Can't create file %s"));
-static char e_cant_get_temp_file_name[] INIT(= N_("E483: Can't get temp file name"));
-static char e_cant_open_file_str[] INIT(= N_("E484: Can't open file %s"));
-static char e_cant_read_file_str[] INIT(= N_("E485: Can't read file %s"));
-static char e_pattern_not_found_str[] INIT(= N_("E486: Pattern not found: %s"));
-static char e_argument_must_be_positive[] INIT(= N_("E487: Argument must be positive"));
-static char e_trailing_characters[] INIT(= N_("E488: Trailing characters"));
-static char e_trailing_characters_str[] INIT(= N_("E488: Trailing characters: %s"));
-static char e_no_call_stack_to_substitute_for_stack[] INIT(= N_("E489: No call stack to substitute for \"<stack>\""));
-static char e_not_an_editor_command[] INIT(= N_("E492: Not an editor command"));
-static char e_backwards_range_given[] INIT(= N_("E493: Backwards range given"));
-static char e_use_w_or_w_gt_gt[] INIT(= N_("E494: Use w or w>>"));
-static char e_no_autocommand_file_name_to_substitute_for_afile[] INIT(= N_("E495: No autocommand file name to substitute for \"<afile>\""));
-static char e_no_autocommand_buffer_number_to_substitute_for_abuf[] INIT(= N_("E496: No autocommand buffer number to substitute for \"<abuf>\""));
-static char e_no_autocommand_match_name_to_substitute_for_amatch[] INIT(= N_("E497: No autocommand match name to substitute for \"<amatch>\""));
-static char e_no_source_file_name_to_substitute_for_sfile[] INIT(= N_("E498: No :source file name to substitute for \"<sfile>\""));
-static char e_empty_file_name_for_percent_or_hash_only_works_with_ph[] INIT(= N_("E499: Empty file name for '%' or '#', only works with \":p:h\""));
-static char e_evaluates_to_an_empty_string[] INIT(= N_("E500: Evaluates to an empty string"));
-static char e_at_end_of_file[] INIT(= N_("E501: At end-of-file"));
-static char e_is_a_directory[] INIT(= N_("is a directory"));
-static char e_is_not_file_or_writable_device[] INIT(= N_("is not a file or writable device"));
-static char e_str_is_not_file_or_writable_device[] INIT(= N_("E503: \"%s\" is not a file or writable device"));
-static char e_is_read_only_cannot_override_W_in_cpoptions[] INIT(= N_("is read-only (cannot override: \"W\" in 'cpoptions')"));
-static char e_is_read_only_add_bang_to_override[] INIT(= N_("is read-only (add ! to override)"));
-static char e_str_is_read_only_add_bang_to_override[] INIT(= N_("E505: \"%s\" is read-only (add ! to override)"));
-static char e_cant_write_to_backup_file_add_bang_to_override[] INIT(= N_("E506: Can't write to backup file (add ! to override)"));
-static char e_close_error_for_backup_file_add_bang_to_write_anyway[] INIT(= N_("E507: Close error for backup file (add ! to write anyway)"));
-static char e_cant_read_file_for_backup_add_bang_to_write_anyway[] INIT(= N_("E508: Can't read file for backup (add ! to write anyway)"));
-static char e_cannot_create_backup_file_add_bang_to_write_anyway[] INIT(= N_("E509: Cannot create backup file (add ! to override)"));
-static char e_cant_make_backup_file_add_bang_to_write_anyway[] INIT(= N_("E510: Can't make backup file (add ! to write anyway)"));
-static char e_close_failed[] INIT(= N_("E512: Close failed"));
-static char e_write_error_conversion_failed_make_fenc_empty_to_override[] INIT(= N_("E513: Write error, conversion failed (make 'fenc' empty to override)"));
-static char e_write_error_conversion_failed_in_line_nr_make_fenc_empty_to_override[] INIT(= N_("E513: Write error, conversion failed in line %ld (make 'fenc' empty to override)"));
-static char e_write_error_file_system_full[] INIT(= N_("E514: Write error (file system full?)"));
-static char e_no_buffers_were_unloaded[] INIT(= N_("E515: No buffers were unloaded"));
-static char e_no_buffers_were_deleted[] INIT(= N_("E516: No buffers were deleted"));
-static char e_no_buffers_were_wiped_out[] INIT(= N_("E517: No buffers were wiped out"));
-static char e_unknown_option[] INIT(= N_("E518: Unknown option"));
-static char e_option_not_supported[] INIT(= N_("E519: Option not supported"));
-static char e_not_allowed_in_modeline[] INIT(= N_("E520: Not allowed in a modeline"));
-static char e_number_required_after_equal[] INIT(= N_("E521: Number required after ="));
-static char e_number_required_after_str_equal_str[] INIT(= N_("E521: Number required: &%s = '%s'"));
-static char e_not_found_in_termcap[] INIT(= N_("E522: Not found in termcap"));
-static char e_not_allowed_here[] INIT(= N_("E523: Not allowed here"));
-static char e_missing_colon[] INIT(= N_("E524: Missing colon"));
-static char e_zero_length_string[] INIT(= N_("E525: Zero length string"));
-static char e_cannot_set_term_to_empty_string[] INIT(= N_("E529: Cannot set 'term' to empty string"));
-static char e_illegal_character_after_chr[] INIT(= N_("E535: Illegal character after <%c>"));
-static char e_commentstring_must_be_empty_or_contain_str[] INIT(= N_("E537: 'commentstring' must be empty or contain %s"));
-static char e_pattern_found_in_every_line_str[] INIT(= N_("E538: Pattern found in every line: %s"));
-static char e_illegal_character_str[] INIT(= N_("E539: Illegal character <%s>"));
-static char e_syntax_error_in_str_curlies[] INIT(= N_("E554: Syntax error in %s{...}"));
-static char e_at_bottom_of_tag_stack[] INIT(= N_("E555: At bottom of tag stack"));
-static char e_at_top_of_tag_stack[] INIT(= N_("E556: At top of tag stack"));
-static char e_not_allowed_to_change_text_or_change_window[] INIT(= N_("E565: Not allowed to change text or change window"));
-static char e_backupext_and_patchmode_are_equal[] INIT(= N_("E589: 'backupext' and 'patchmode' are equal"));
-static char e_winheight_cannot_be_smaller_than_winminheight[] INIT(= N_("E591: 'winheight' cannot be smaller than 'winminheight'"));
-static char e_winwidth_cannot_be_smaller_than_winminwidth[] INIT(= N_("E592: 'winwidth' cannot be smaller than 'winminwidth'"));
-static char e_need_at_least_nr_lines[] INIT(= N_("E593: Need at least %d lines"));
-static char e_need_at_least_nr_columns[] INIT(= N_("E594: Need at least %d columns"));
-static char e_no_argument_to_delete[] INIT(= N_("E610: No argument to delete"));
-static char e_missing_delimiter_after_search_pattern_str[] INIT(= N_("E654: Missing delimiter after search pattern: %s"));
-static char e_at_start_of_changelist[] INIT(= N_("E662: At start of changelist"));
-static char e_at_end_of_changelist[] INIT(= N_("E663: At end of changelist"));
-static char e_changelist_is_empty[] INIT(= N_("E664: Changelist is empty"));
-static char e_fsync_failed[] INIT(= N_("E667: Fsync failed"));
-static char e_unprintable_character_in_group_name[] INIT(= N_("E669: Unprintable character in group name"));
-static char e_mix_of_help_file_encodings_within_language_str[] INIT(= N_("E670: Mix of help file encodings within a language: %s"));
-static char e_no_matching_autocommands_for_buftype_str_buffer[] INIT(= N_("E676: No matching autocommands for buftype=%s buffer"));
-static char e_invalid_character_after_str_2[] INIT(= N_("E678: Invalid character after %s%%[dxouU]"));
-static char e_buffer_nr_invalid_buffer_number[] INIT(= N_("E680: <buffer=%d>: invalid buffer number"));
-static char e_internal_error_str[] INIT(= N_("E685: Internal error: %s"));
-static char e_cannot_change_directory_buffer_is_modified_add_bang_to_override[] INIT(= N_("E747: Cannot change directory, buffer is modified (add ! to override)"));
-static char e_no_previously_used_register[] INIT(= N_("E748: No previously used register"));
-static char e_empty_buffer[] INIT(= N_("E749: Empty buffer"));
-static char e_too_many_arguments_to_printf[] INIT(= N_("E767: Too many arguments for printf()"));
-static char e_swap_file_exists_str_silent_overrides[] INIT(= N_("E768: Swap file exists: %s (:silent! overrides)"));
-static char e_missing_rsb_after_str_lsb[] INIT(= N_("E769: Missing ] after %s["));
-static char e_symlink_loop_for_str[] INIT(= N_("E773: Symlink loop for \"%s\""));
-static char e_eval_feature_not_available[] INIT(= N_("E775: Eval feature not available"));
-static char e_cannot_close_last_tab_page[] INIT(= N_("E784: Cannot close last tab page"));
-static char e_not_allowed_to_edit_another_buffer_now[] INIT(= N_("E788: Not allowed to edit another buffer now"));
-static char e_undojoin_is_not_allowed_after_undo[] INIT(= N_("E790: undojoin is not allowed after undo"));
-static char e_invalid_id_nr_must_be_greater_than_or_equal_to_one_1[] INIT(= N_("E799: Invalid ID: %d (must be greater than or equal to 1)"));
-static char e_arabic_cannot_be_used_not_enabled_at_compile_time[] INIT(= N_("E800: Arabic cannot be used: Not enabled at compile time\n"));
-static char e_id_already_taken_nr[] INIT(= N_("E801: ID already taken: %d"));
-static char e_invalid_id_nr_must_be_greater_than_or_equal_to_one_2[] INIT(= N_("E802: Invalid ID: %d (must be greater than or equal to 1)"));
-static char e_id_not_found_nr[] INIT(= N_("E803: ID not found: %d"));
-static char e_hashsmall_is_not_available_without_the_eval_feature[] INIT(= N_("E809: #< is not available without the +eval feature"));
-static char e_not_allowed_to_change_buffer_information_now[] INIT(= N_("E811: Not allowed to change buffer information now"));
-static char e_autocommands_changed_buffer_or_buffer_name[] INIT(= N_("E812: Autocommands changed buffer or buffer name"));
-static char e_cannot_close_autocmd_or_popup_window[] INIT(= N_("E813: Cannot close autocmd or popup window"));
-static char e_cannot_close_window_only_autocmd_window_would_remain[] INIT(= N_("E814: Cannot close window, only autocmd window would remain"));
-static char e_undo_number_nr_not_found[] INIT(= N_("E830: Undo number %ld not found"));
-static char e_str_is_encrypted_and_this_version_of_vim_does_not_support_encryption[] INIT(= N_("E833: %s is encrypted and this version of Vim does not support encryption"));
-static char e_conflicts_with_value_of_listchars[] INIT(= N_("E834: Conflicts with value of 'listchars'"));
-static char e_conflicts_with_value_of_fillchars[] INIT(= N_("E835: Conflicts with value of 'fillchars'"));
-static char e_reserved_name_cannot_be_used_for_user_defined_command[] INIT(= N_("E841: Reserved name, cannot be used for user defined command"));
-static char e_no_line_number_to_use_for_slnum[] INIT(= N_("E842: No line number to use for \"<slnum>\""));
-static char e_key_code_not_set[] INIT(= N_("E846: Key code not set"));
-static char e_too_many_highlight_and_syntax_groups[] INIT(= N_("E849: Too many highlight and syntax groups"));
-static char e_invalid_register_name[] INIT(= N_("E850: Invalid register name"));
-static char e_path_too_long_for_completion[] INIT(= N_("E854: Path too long for completion"));
-static char e_autocommands_caused_command_to_abort[] INIT(= N_("E855: Autocommands caused command to abort"));
-static char e_percent_hash_can_only_be_followed_by_zero_one_two_automatic_engine_will_be_used[] INIT(= N_("E864: \\%#= can only be followed by 0, 1, or 2. The automatic engine will be used"));
-static char e_nfa_regexp_end_encountered_prematurely[] INIT(= N_("E865: (NFA) Regexp end encountered prematurely"));
-static char e_nfa_regexp_misplaced_chr[] INIT(= N_("E866: (NFA regexp) Misplaced %c"));
-static char e_nfa_regexp_unknown_operator_z_chr[] INIT(= N_("E867: (NFA regexp) Unknown operator '\\z%c'"));
-static char e_nfa_regexp_unknown_operator_percent_chr[] INIT(= N_("E867: (NFA regexp) Unknown operator '\\%%%c'"));
-static char e_error_building_nfa_with_equivalence_class[] INIT(= N_("E868: Error building NFA with equivalence class!"));
-static char e_nfa_regexp_unknown_operator_at_chr[] INIT(= N_("E869: (NFA regexp) Unknown operator '\\@%c'"));
-static char e_nfa_regexp_error_reading_repetition_limits[] INIT(= N_("E870: (NFA regexp) Error reading repetition limits"));
-static char e_nfa_regexp_cant_have_multi_follow_multi[] INIT(= N_("E871: (NFA regexp) Can't have a multi follow a multi"));
-static char e_nfa_regexp_too_many_parens[] INIT(= N_("E872: (NFA regexp) Too many '('"));
-static char e_nfa_regexp_proper_termination_error[] INIT(= N_("E873: (NFA regexp) proper termination error"));
-static char e_nfa_regexp_could_not_pop_stack[] INIT(= N_("E874: (NFA regexp) Could not pop the stack!"));
-static char e_nfa_regexp_while_converting_from_postfix_to_nfa_too_many_stats_left_on_stack[] INIT(= N_("E875: (NFA regexp) (While converting from postfix to NFA), too many states left on stack"));
-static char e_nfa_regexp_not_enough_space_to_store_whole_nfa[] INIT(= N_("E876: (NFA regexp) Not enough space to store the whole NFA"));
-static char e_nfa_regexp_invalid_character_class_nr[] INIT(= N_("E877: (NFA regexp) Invalid character class: %d"));
-static char e_nfa_regexp_could_not_allocate_memory_for_branch_traversal[] INIT(= N_("E878: (NFA regexp) Could not allocate memory for branch traversal!"));
-static char e_line_count_changed_unexpectedly[] INIT(= N_("E881: Line count changed unexpectedly"));
-static char e_nfa_regexp_cannot_repeat_str[] INIT(= N_("E888: (NFA regexp) cannot repeat %s"));
-static char e_directory_not_found_in_str_str[] INIT(= N_("E919: Directory not found in '%s': \"%s\""));
-static char e_buffer_cannot_be_registered[] INIT(= N_("E931: Buffer cannot be registered"));
-static char e_cannot_delete_current_group[] INIT(= N_("E936: Cannot delete the current group"));
-static char e_attempt_to_delete_buffer_that_is_in_use_str[] INIT(= N_("E937: Attempt to delete a buffer that is in use: %s"));
-static char e_positive_count_required[] INIT(= N_("E939: Positive count required"));
-static char e_command_table_needs_to_be_updated_run_make_cmdidxs[] INIT(= "E943: Command table needs to be updated, run 'make cmdidxs'");
-static char e_reverse_range_in_character_class[] INIT(= N_("E944: Reverse range in character class"));
-static char e_range_too_large_in_character_class[] INIT(= N_("E945: Range too large in character class"));
-static char e_file_changed_while_writing[] INIT(= N_("E949: File changed while writing"));
-static char e_cannot_convert_between_str_and_str[] INIT(= N_("E950: Cannot convert between %s and %s"));
-static char e_percent_value_too_large[] INIT(= N_("E951: \\% value too large"));
-static char e_cannot_use_pattern_recursively[] INIT(= N_("E956: Cannot use pattern recursively"));
-static char e_command_not_allowed_in_rvim[] INIT(= N_("E981: Command not allowed in rvim"));
-static char e_duplicate_argument_str[] INIT(= N_("E983: Duplicate argument: %s"));
-static char e_scriptversion_used_outside_of_sourced_file[] INIT(= N_("E984: :scriptversion used outside of a sourced file"));
-static char e_not_allowed_in_modeline_when_modelineexpr_is_off[] INIT(= N_("E992: Not allowed in a modeline when 'modelineexpr' is off"));
-static char e_scriptversion_not_supported_nr[] INIT(= N_("E999: scriptversion not supported: %d"));
-static char e_missing_rcurly[] INIT(= N_("E1026: Missing }"));
-static char e_cannot_use_scriptversion_after_vim9script[] INIT(= N_("E1040: Cannot use :scriptversion after :vim9script"));
-static char e_colon_required_before_range_str[] INIT(= N_("E1050: Colon required before a range: %s"));
-static char e_yank_register_changed_while_using_it[] INIT(= N_("E1064: Yank register changed while using it"));
-static char e_command_cannot_be_shortened_str[] INIT(= N_("E1065: Command cannot be shortened: %s"));
-static char e_invalid_command_nested_did_you_mean_plusplus_nested[] INIT(= N_("E1078: Invalid command \"nested\", did you mean \"++nested\"?"));
-static char e_command_modifier_without_command[] INIT(= N_("E1082: Command modifier without command"));
-static char e_cmd_mapping_must_end_with_cr_before_second_cmd[] INIT(= N_("E1136: <Cmd> mapping must end with <CR> before second <Cmd>"));
-static char e_cannot_define_autocommands_for_all_events[] INIT(= N_("E1155: Cannot define autocommands for ALL events"));
-static char e_cannot_change_arglist_recursively[] INIT(= N_("E1156: Cannot change the argument list recursively"));
-static char e_cannot_split_window_when_closing_buffer[] INIT(= N_("E1159: Cannot split a window when closing the buffer"));
-static char e_vim9cmd_must_be_followed_by_command[] INIT(= N_("E1164: vim9cmd must be followed by a command"));
-static char e_failed_to_source_defaults[] INIT(= N_("E1187: Failed to source defaults.vim"));
-static char e_regexp_number_after_dot_pos_search_chr[] INIT(= N_("E1204: No Number allowed after .: '\\%%%c'"));
-static char e_no_white_space_allowed_between_option_and[] INIT(= N_("E1205: No white space allowed between option and"));
-static char e_complete_used_without_allowing_arguments[] INIT(= N_("E1208: -complete used without allowing arguments"));
-static char e_cannot_use_bar_to_separate_commands_here_str[] INIT(= N_("E1231: Cannot use a bar to separate commands here: %s"));
-static char e_legacy_must_be_followed_by_command[] INIT(= N_("E1234: legacy must be followed by a command"));
-static char e_no_such_user_defined_command_in_current_buffer_str[] INIT(= N_("E1237: No such user-defined command in current buffer: %s"));
-static char e_resulting_text_too_long[] INIT(= N_("E1240: Resulting text too long"));
-static char e_line_number_out_of_range[] INIT(= N_("E1247: Line number out of range"));
-static char e_highlight_group_name_too_long[] INIT(= N_("E1249: Highlight group name too long"));
-static char e_cmd_mapping_must_end_with_cr[] INIT(= N_("E1255: <Cmd> mapping must end with <CR>"));
-static char e_cannot_use_s_backslash_in_vim9_script[] INIT(= N_("E1270: Cannot use :s\\/sub/ in Vim9 script"));
-static char e_nfa_regexp_missing_value_in_chr[] INIT(= N_("E1273: (NFA regexp) missing value in '\\%%%c'"));
-static char e_no_script_file_name_to_substitute_for_script[] INIT(= N_("E1274: No script file name to substitute for \"<script>\""));
-static char e_atom_engine_must_be_at_start_of_pattern[] INIT(= N_("E1281: Atom '\\%%#=%c' must be at the start of the pattern"));
-static char e_cmdline_window_already_open[] INIT(= N_("E1292: Command-line window is already open"));
-static char e_window_unexpectedly_close_while_searching_for_tags[] INIT(= N_("E1299: Window unexpectedly closed while searching for tags"));
-static char e_cannot_change_mappings_while_listing[] INIT(= N_("E1309: Cannot change mappings while listing"));
-static char e_cannot_change_user_commands_while_listing[] INIT(= N_("E1311: Cannot change user commands while listing"));
-static char e_not_allowed_to_change_window_layout_in_this_autocmd[] INIT(= N_("E1312: Not allowed to change the window layout in this autocmd"));
-static char e_not_allowed_to_add_or_remove_entries_str[] INIT(= N_("E1313: Not allowed to add or remove entries (%s)"));
-static char e_internal_error_shortmess_too_long[] INIT(= "E1336: Internal error: shortmess too long");
-static char e_warning_pointer_block_corrupted[] INIT(= N_("E1364: Warning: Pointer block corrupted"));
-static char e_cannot_mix_positional_and_non_positional_str[] INIT(= N_("E1500: Cannot mix positional and non-positional arguments: %s"));
-static char e_fmt_arg_nr_unused_str[] INIT(= N_("E1501: format argument %d unused in $-style format: %s"));
-static char e_positional_num_field_spec_reused_str_str[] INIT(= N_("E1502: Positional argument %d used as field width reused as different type: %s/%s"));
-static char e_positional_arg_num_type_inconsistent_str_str[] INIT(= N_("E1504: Positional argument %d type used inconsistently: %s/%s"));
-static char e_invalid_format_specifier_str[] INIT(= N_("E1505: Invalid format specifier: %s"));
-static char e_aptypes_is_null_nr_str[] INIT(= "E1507: Internal error: ap_types or ap_types[idx] is NULL: %d: %s");
-static char e_val_too_large[] INIT(= N_("E1510: Value too large: %s"));
-static char e_wrong_number_of_characters_for_field_str[] INIT(= N_("E1511: Wrong number of characters for field \"%s\""));
-static char e_wrong_character_width_for_field_str[] INIT(= N_("E1512: Wrong character width for field \"%s\""));
-static char e_winfixbuf_cannot_go_to_buffer[] INIT(= N_("E1513: Cannot switch buffer. 'winfixbuf' is enabled"));
-static char e_unicode_val_too_large[] INIT(= N_("E1541: Value too large, max Unicode codepoint is U+10FFFF"));
-static char e_cannot_switch_to_a_closing_buffer[] INIT(= N_("E1546: Cannot switch to a closing buffer"));
-static char e_cannot_not_support_redrawtabpanel[] INIT(= N_("E1547: This version of Vim does not support :redrawtabpanel"));
-static char e_osc_response_timed_out[] INIT(= N_("E1568: OSC command response timed out: %.*s"));
-static char e_leadtab_requires_tab[] INIT(= N_("E1572: 'listchars' field \"leadtab\" requires \"tab\" to be specified"));
-static char e_tag_file_entry_must_not_be_url[] INIT(= N_("E1576: Tag file entry must not be a URL"));
-static char e_invalid_format_string_single_percent_s[] INIT(= N_("E1577: Invalid format string, only one \"%s\" is allowed"));
-static char e_completeopt_escape_cannot_be_used_with_nargs_underscore[] INIT(= N_("E1579: -completeopt=escape cannot be used with -nargs=_"));
+static char e_backslash_should_be_followed_by[]  =  "E10: \\ should be followed by /, ? or &"  ;
+static char e_invalid_in_cmdline_window[]  =  "E11: Invalid in command-line window; :q<CR> closes the window"  ;
+static char e_command_not_allowed_from_vimrc_in_current_dir_or_tag_search[]  =  "E12: Command not allowed from exrc/vimrc in current dir or tag search"  ;
+static char e_file_exists[]  =  "E13: File exists (add ! to override)"  ;
+static char e_invalid_range[]  =  "E16: Invalid range"  ;
+static char e_str_is_directory[]  =  "E17: \"%s\" is a directory"  ;
+static char e_mark_has_invalid_line_number[]  =  "E19: Mark has invalid line number"  ;
+static char e_mark_not_set[]  =  "E20: Mark not set"  ;
+static char e_cannot_make_changes_modifiable_is_off[]  =  "E21: Cannot make changes, 'modifiable' is off"  ;
+static char e_scripts_nested_too_deep[]  =  "E22: Scripts nested too deep"  ;
+static char e_no_alternate_file[]  =  "E23: No alternate file"  ;
+static char e_no_such_abbreviation[]  =  "E24: No such abbreviation"  ;
+static char e_gui_cannot_be_used_not_enabled_at_compile_time[]  =  "E25: GUI cannot be used: Not enabled at compile time"  ;
+static char e_hebrew_cannot_be_used_not_enabled_at_compile_time[]  =  "E26: Hebrew cannot be used: Not enabled at compile time\n"  ;
+static char e_farsi_support_has_been_removed[]  =  "E27: Farsi support has been removed\n"  ;
+static char e_no_such_highlight_group_name_str[]  =  "E28: No such highlight group name: %s"  ;
+static char e_no_inserted_text_yet[]  =  "E29: No inserted text yet"  ;
+static char e_no_previous_command_line[]  =  "E30: No previous command line"  ;
+static char e_no_such_mapping[]  =  "E31: No such mapping"  ;
+static char e_no_file_name[]  =  "E32: No file name"  ;
+static char e_no_previous_substitute_regular_expression[]  =  "E33: No previous substitute regular expression"  ;
+static char e_no_previous_command[]  =  "E34: No previous command"  ;
+static char e_no_previous_regular_expression[]  =  "E35: No previous regular expression"  ;
+static char e_not_enough_room[]  =  "E36: Not enough room"  ;
+static char e_no_write_since_last_change[]  =  "E37: No write since last change"  ;
+static char e_no_write_since_last_change_add_bang_to_override[]  =  "E37: No write since last change (add ! to override)"  ;
+static char e_null_argument[]  = "E38: Null argument" ;
+static char e_out_of_memory[]  =  "E41: Out of memory!"  ;
+static char e_damaged_match_string[]  = "E43: Damaged match string" ;
+static char e_corrupted_regexp_program[]  = "E44: Corrupted regexp program" ;
+static char e_readonly_option_is_set_add_bang_to_override[]  =  "E45: 'readonly' option is set (add ! to override)"  ;
+static char e_invalid_scroll_size[]  =  "E49: Invalid scroll size"  ;
+static char e_too_many_str_open[]  =  "E51: Too many %s("  ;
+static char e_unmatched_str_percent_open[]  =  "E53: Unmatched %s%%("  ;
+static char e_unmatched_str_open[]  =  "E54: Unmatched %s("  ;
+static char e_unmatched_str_close[]  =  "E55: Unmatched %s)"  ;
+static char e_invalid_character_after_str_at[]  =  "E59: Invalid character after %s@"  ;
+static char e_too_many_complex_str_curly[]  =  "E60: Too many complex %s{...}s"  ;
+static char e_nested_str[]  =  "E61: Nested %s*"  ;
+static char e_nested_str_chr[]  =  "E62: Nested %s%c"  ;
+static char e_invalid_use_of_underscore[]  =  "E63: Invalid use of \\_"  ;
+static char e_str_chr_follows_nothing[]  =  "E64: %s%c follows nothing"  ;
+static char e_illegal_back_reference[]  =  "E65: Illegal back reference"  ;
+static char e_invalid_character_after_bsl_z[]  =  "E68: Invalid character after \\z"  ;
+static char e_missing_sb_after_str[]  =  "E69: Missing ] after %s%%["  ;
+static char e_empty_str_brackets[]  =  "E70: Empty %s%%[]"  ;
+static char e_invalid_character_after_str[]  =  "E71: Invalid character after %s%%"  ;
+static char e_close_error_on_swap_file[]  =  "E72: Close error on swap file"  ;
+static char e_tag_stack_empty[]  =  "E73: Tag stack empty"  ;
+static char e_command_too_complex[]  =  "E74: Command too complex"  ;
+static char e_name_too_long[]  =  "E75: Name too long"  ;
+static char e_too_many_brackets[]  =  "E76: Too many ["  ;
+static char e_too_many_file_names[]  =  "E77: Too many file names"  ;
+static char e_unknown_mark[]  =  "E78: Unknown mark"  ;
+static char e_cannot_expand_wildcards[]  =  "E79: Cannot expand wildcards"  ;
+static char e_error_while_writing[]  =  "E80: Error while writing"  ;
+static char e_cannot_allocate_any_buffer_exiting[]  =  "E82: Cannot allocate any buffer, exiting..."  ;
+static char e_cannot_allocate_buffer_using_other_one[]  =  "E83: Cannot allocate buffer, using other one..."  ;
+static char e_no_modified_buffer_found[]  =  "E84: No modified buffer found"  ;
+static char e_there_is_no_listed_buffer[]  =  "E85: There is no listed buffer"  ;
+static char e_buffer_nr_does_not_exist[]  =  "E86: Buffer %ld does not exist"  ;
+static char e_cannot_go_beyond_last_buffer[]  =  "E87: Cannot go beyond last buffer"  ;
+static char e_cannot_go_before_first_buffer[]  =  "E88: Cannot go before first buffer"  ;
+static char e_no_write_since_last_change_for_buffer_nr_add_bang_to_override[]  =  "E89: No write since last change for buffer %d (add ! to override)"  ;
+static char e_cannot_unload_last_buffer[]  =  "E90: Cannot unload last buffer"  ;
+static char e_shell_option_is_empty[]  =  "E91: 'shell' option is empty"  ;
+static char e_buffer_nr_not_found[]  =  "E92: Buffer %d not found"  ;
+static char e_more_than_one_match_for_str[]  =  "E93: More than one match for %s"  ;
+static char e_no_matching_buffer_for_str[]  =  "E94: No matching buffer for %s"  ;
+static char e_buffer_with_this_name_already_exists[]  =  "E95: Buffer with this name already exists"  ;
+static char e_cannot_move_range_of_lines_into_itself[]  =  "E134: Cannot move a range of lines into itself"  ;
+static char e_filter_autocommands_must_not_change_current_buffer[]  =  "E135: *Filter* Autocommands must not change current buffer"  ;
+static char e_file_is_loaded_in_another_buffer[]  =  "E139: File is loaded in another buffer"  ;
+static char e_use_bang_to_write_partial_buffer[]  =  "E140: Use ! to write partial buffer"  ;
+static char e_no_file_name_for_buffer_nr[]  =  "E141: No file name for buffer %ld"  ;
+static char e_file_not_written_writing_is_disabled_by_write_option[]  =  "E142: File not written: Writing is disabled by 'write' option"  ;
+static char e_autocommands_unexpectedly_deleted_new_buffer_str[]  =  "E143: Autocommands unexpectedly deleted new buffer %s"  ;
+static char e_non_numeric_argument_to_z[]  =  "E144: Non-numeric argument to :z"  ;
+static char e_shell_commands_and_some_functionality_not_allowed_in_rvim[]  =  "E145: Shell commands and some functionality not allowed in rvim"  ;
+static char e_regular_expressions_cant_be_delimited_by_letters[]  =  "E146: Regular expressions can't be delimited by letters"  ;
+static char e_cannot_do_global_recursive_with_range[]  =  "E147: Cannot do :global recursive with a range"  ;
+static char e_regular_expression_missing_from_global[]  =  "E148: Regular expression missing from :global"  ;
+static char e_sorry_no_help_for_str[]  =  "E149: Sorry, no help for %s"  ;
+static char e_not_a_directory_str[]  =  "E150: Not a directory: %s"  ;
+static char e_no_match_str_1[]  =  "E151: No match: %s"  ;
+static char e_cannot_open_str_for_writing_1[]  =  "E152: Cannot open %s for writing"  ;
+static char e_unable_to_open_str_for_reading[]  =  "E153: Unable to open %s for reading"  ;
+static char e_duplicate_tag_str_in_file_str_str[]  =  "E154: Duplicate tag \"%s\" in file %s/%s"  ;
+static char e_no_write_since_last_change_for_buffer_str[]  =  "E162: No write since last change for buffer \"%s\""  ;
+static char e_there_is_only_one_file_to_edit[]  =  "E163: There is only one file to edit"  ;
+static char e_cannot_go_before_first_file[]  =  "E164: Cannot go before first file"  ;
+static char e_cannot_go_beyond_last_file[]  =  "E165: Cannot go beyond last file"  ;
+static char e_cant_open_linked_file_for_writing[]  =  "E166: Can't open linked file for writing"  ;
+static char e_scriptencoding_used_outside_of_sourced_file[]  =  "E167: :scriptencoding used outside of a sourced file"  ;
+static char e_command_too_recursive[]  =  "E169: Command too recursive"  ;
+
+ static char e_nr_more_file_to_edit[] = "E173: %d more file to edit";
+ static char e_nr_more_files_to_edit[] = "E173: %d more files to edit";
+
+static char e_command_already_exists_add_bang_to_replace_it_str[]  =  "E174: Command already exists: add ! to replace it: %s"  ;
+static char e_no_attribute_specified[]  =  "E175: No attribute specified"  ;
+static char e_invalid_number_of_arguments[]  =  "E176: Invalid number of arguments"  ;
+static char e_count_cannot_be_specified_twice[]  =  "E177: Count cannot be specified twice"  ;
+static char e_invalid_default_value_for_count[]  =  "E178: Invalid default value for count"  ;
+static char e_argument_required_for_str[]  =  "E179: Argument required for %s"  ;
+static char e_invalid_complete_value_str[]  =  "E180: Invalid complete value: %s"  ;
+static char e_invalid_address_type_value_str[]  =  "E180: Invalid address type value: %s"  ;
+static char e_invalid_attribute_str[]  =  "E181: Invalid attribute: %s"  ;
+static char e_invalid_command_name[]  =  "E182: Invalid command name"  ;
+static char e_user_defined_commands_must_start_with_an_uppercase_letter[]  =  "E183: User defined commands must start with an uppercase letter"  ;
+static char e_no_such_user_defined_command_str[]  =  "E184: No such user-defined command: %s"  ;
+static char e_cannot_find_color_scheme_str[]  =  "E185: Cannot find color scheme '%s'"  ;
+static char e_no_previous_directory[]  =  "E186: No previous directory"  ;
+static char e_directory_unknown[]  =  "E187: Directory unknown"  ;
+static char e_obtaining_window_position_not_implemented_for_this_platform[]  =  "E188: Obtaining window position not implemented for this platform"  ;
+static char e_str_exists_add_bang_to_override[]  =  "E189: \"%s\" exists (add ! to override)"  ;
+static char e_cannot_open_str_for_writing_2[]  =  "E190: Cannot open \"%s\" for writing"  ;
+static char e_argument_must_be_letter_or_forward_backward_quote[]  =  "E191: Argument must be a letter or forward/backward quote"  ;
+static char e_recursive_use_of_normal_too_deep[]  =  "E192: Recursive use of :normal too deep"  ;
+static char e_no_alternate_file_name_to_substitute_for_hash[]  =  "E194: No alternate file name to substitute for '#'"  ;
+static char e_no_digraphs_version[]  =  "E196: No digraphs in this version"  ;
+static char e_cannot_set_language_to_str[]  =  "E197: Cannot set language to \"%s\""  ;
+static char e_active_window_or_buffer_changed_or_deleted[]  =  "E199: Active window or buffer changed or deleted"  ;
+static char e_readpre_autocommands_made_file_unreadable[]  =  "E200: *ReadPre autocommands made the file unreadable"  ;
+static char e_readpre_autocommands_must_not_change_current_buffer[]  =  "E201: *ReadPre autocommands must not change current buffer"  ;
+static char e_autocommands_deleted_or_unloaded_buffer_to_be_written[]  =  "E203: Autocommands deleted or unloaded buffer to be written"  ;
+static char e_autocommands_changed_number_of_lines_in_unexpected_way[]  =  "E204: Autocommand changed number of lines in unexpected way"  ;
+static char e_patchmode_cant_save_original_file[]  =  "E205: Patchmode: can't save original file"  ;
+static char e_patchmode_cant_touch_empty_original_file[]  =  "E206: Patchmode: can't touch empty original file"  ;
+static char e_cant_delete_backup_file[]  =  "E207: Can't delete backup file"  ;
+static char e_error_writing_to_str[]  =  "E208: Error writing to \"%s\""  ;
+static char e_error_closing_str[]  =  "E209: Error closing \"%s\""  ;
+static char e_error_reading_str[]  =  "E210: Error reading \"%s\""  ;
+static char e_file_str_no_longer_available[]  =  "E211: File \"%s\" no longer available"  ;
+static char e_cant_open_file_for_writing[]  =  "E212: Can't open file for writing"  ;
+static char e_cannot_convert_add_bang_to_write_without_conversion[]  =  "E213: Cannot convert (add ! to write without conversion)"  ;
+static char e_illegal_character_after_star_str[]  =  "E215: Illegal character after *: %s"  ;
+static char e_no_such_event_str[]  =  "E216: No such event: %s"  ;
+static char e_no_such_group_or_event_str[]  =  "E216: No such group or event: %s"  ;
+static char e_cant_execute_autocommands_for_all_events[]  =  "E217: Can't execute autocommands for ALL events"  ;
+static char e_autocommand_nesting_too_deep[]  =  "E218: Autocommand nesting too deep"  ;
+static char e_missing_open_curly[]  =  "E219: Missing {."  ;
+static char e_missing_close_curly[]  =  "E220: Missing }."  ;
+static char e_add_to_internal_buffer_that_was_already_read_from[]  = "E222: Add to internal buffer that was already read from" ;
+static char e_recursive_mapping[]  =  "E223: Recursive mapping"  ;
+static char e_global_abbreviation_already_exists_for_str[]  =  "E224: Global abbreviation already exists for %s"  ;
+static char e_global_mapping_already_exists_for_str[]  =  "E225: Global mapping already exists for %s"  ;
+static char e_abbreviation_already_exists_for_str[]  =  "E226: Abbreviation already exists for %s"  ;
+static char e_mapping_already_exists_for_str[]  =  "E227: Mapping already exists for %s"  ;
+static char e_makemap_illegal_mode[]  = "E228: makemap: Illegal mode" ;
+static char e_cant_split_window_while_closing_another[]  =  "E242: Can't split a window while closing another"  ;
+static char e_filechangedshell_autocommand_deleted_buffer[]  =  "E246: FileChangedShell autocommand deleted buffer"  ;
+static char e_window_layout_changed_unexpectedly[]  =  "E249: Window layout changed unexpectedly"  ;
+static char e_cannot_read_from_str_2[]  =  "E282: Cannot read from \"%s\""  ;
+static char e_no_marks_matching_str[]  =  "E283: No marks matching \"%s\""  ;
+static char e_invalid_count_for_del_bytes_nr[]  = "E292: Invalid count for del_bytes(): %ld" ;
+static char e_block_was_not_locked[]  = "E293: Block was not locked" ;
+static char e_seek_error_in_swap_file_read[]  =  "E294: Seek error in swap file read"  ;
+static char e_read_error_in_swap_file[]  =  "E295: Read error in swap file"  ;
+static char e_seek_error_in_swap_file_write[]  =  "E296: Seek error in swap file write"  ;
+static char e_write_error_in_swap_file[]  =  "E297: Write error in swap file"  ;
+static char e_didnt_get_block_nr_zero[]  = "E298: Didn't get block nr 0?" ;
+static char e_didnt_get_block_nr_one[]  = "E298: Didn't get block nr 1?" ;
+static char e_didnt_get_block_nr_two[]  = "E298: Didn't get block nr 2?" ;
+static char e_swap_file_already_exists_symlink_attack[]  =  "E300: Swap file already exists (symlink attack?)"  ;
+static char e_oops_lost_the_swap_file[]  =  "E301: Oops, lost the swap file!!!"  ;
+static char e_could_not_rename_swap_file[]  =  "E302: Could not rename swap file"  ;
+static char e_unable_to_open_swap_file_for_str_recovery_impossible[]  =  "E303: Unable to open swap file for \"%s\", recovery impossible"  ;
+static char e_ml_upd_block0_didnt_get_block_zero[]  = "E304: ml_upd_block0(): Didn't get block 0??" ;
+static char e_no_swap_file_found_for_str[]  =  "E305: No swap file found for %s"  ;
+static char e_cannot_open_str[]  =  "E306: Cannot open %s"  ;
+static char e_str_does_not_look_like_vim_swap_file[]  =  "E307: %s does not look like a Vim swap file"  ;
+static char e_warning_original_file_may_have_been_changed[]  =  "E308: Warning: Original file may have been changed"  ;
+static char e_unable_to_read_block_one_from_str[]  =  "E309: Unable to read block 1 from %s"  ;
+static char e_block_one_id_wrong_str_not_swp_file[]  =  "E310: Block 1 ID wrong (%s not a .swp file?)"  ;
+static char e_recovery_interrupted[]  =  "E311: Recovery Interrupted"  ;
+static char e_errors_detected_while_recovering_look_for_lines_starting_with_questions[]  =  "E312: Errors detected while recovering; look for lines starting with ???"  ;
+static char e_cannot_preserve_there_is_no_swap_file[]  =  "E313: Cannot preserve, there is no swap file"  ;
+static char e_preserve_failed[]  =  "E314: Preserve failed"  ;
+static char e_ml_get_invalid_lnum_nr[]  = "E315: ml_get: Invalid lnum: %ld" ;
+static char e_ml_get_cannot_find_line_nr_in_buffer_nr_str[]  = "E316: ml_get: Cannot find line %ld in buffer %d %s" ;
+static char e_pointer_block_id_wrong[]  = "E317: Pointer block id wrong" ;
+static char e_pointer_block_id_wrong_two[]  = "E317: Pointer block id wrong 2" ;
+static char e_pointer_block_id_wrong_three[]  = "E317: Pointer block id wrong 3" ;
+static char e_pointer_block_id_wrong_four[]  = "E317: Pointer block id wrong 4" ;
+static char e_updated_too_many_blocks[]  = "E318: Updated too many blocks?" ;
+static char e_sorry_command_is_not_available_in_this_version[]  =  "E319: Sorry, the command is not available in this version"  ;
+static char e_cannot_find_line_nr[]  = "E320: Cannot find line %ld" ;
+static char e_could_not_reload_str[]  =  "E321: Could not reload \"%s\""  ;
+static char e_line_number_out_of_range_nr_past_the_end[]  = "E322: Line number out of range: %ld past the end" ;
+static char e_line_count_wrong_in_block_nr[]  = "E323: Line count wrong in block %ld" ;
+static char e_attention[]  =  "E325: ATTENTION"  ;
+static char e_too_many_swap_files_found[]  =  "E326: Too many swap files found"  ;
+static char e_pattern_too_long[]  =  "E339: Pattern too long"  ;
+static char e_internal_error_please_report_a_bug[]  =  "E340: Internal error; if you can reproduce please report a bug"  ;
+static char e_internal_error_lalloc_zero[]  = "E341: Internal error: lalloc(0, )" ;
+static char e_out_of_memory_allocating_nr_bytes[]  =  "E342: Out of memory!  (allocating %lu bytes)"  ;
+static char e_invalid_path_number_must_be_at_end_of_path_or_be_followed_by_str[]  =  "E343: Invalid path: '**[number]' must be at the end of the path or be followed by '%s'."  ;
+static char e_cant_find_directory_str_in_cdpath[]  =  "E344: Can't find directory \"%s\" in cdpath"  ;
+static char e_cant_find_file_str_in_path[]  =  "E345: Can't find file \"%s\" in path"  ;
+static char e_no_more_directory_str_found_in_cdpath[]  =  "E346: No more directory \"%s\" found in cdpath"  ;
+static char e_no_more_file_str_found_in_path[]  =  "E347: No more file \"%s\" found in path"  ;
+static char e_no_string_under_cursor[]  =  "E348: No string under cursor"  ;
+static char e_no_identifier_under_cursor[]  =  "E349: No identifier under cursor"  ;
+static char e_nothing_in_register_str[]  =  "E353: Nothing in register %s"  ;
+static char e_invalid_register_name_str[]  =  "E354: Invalid register name: '%s'"  ;
+static char e_unknown_option_str_2[]  =  "E355: Unknown option: %s"  ;
+static char e_get_varp_error[]  = "E356: get_varp ERROR" ;
+static char e_screen_mode_setting_not_supported[]  =  "E359: Screen mode setting not supported"  ;
+static char e_pattern_uses_more_memory_than_maxmempattern[]  =  "E363: Pattern uses more memory than 'maxmempattern'"  ;
+static char e_no_such_group_str[]  =  "E367: No such group: \"%s\""  ;
+static char e_invalid_item_in_str_brackets[]  =  "E369: Invalid item in %s%%[]"  ;
+static char e_cannot_write_buftype_option_is_set[]  =  "E382: Cannot write, 'buftype' option is set"  ;
+static char e_invalid_search_string_str[]  =  "E383: Invalid search string: %s"  ;
+static char e_search_hit_top_without_match_for_str[]  =  "E384: Search hit TOP without match for: %s"  ;
+static char e_search_hit_bottom_without_match_for_str[]  =  "E385: Search hit BOTTOM without match for: %s"  ;
+static char e_expected_question_or_slash_after_semicolon[]  =  "E386: Expected '?' or '/'  after ';'"  ;
+static char e_highlight_group_name_not_found_str[]  =  "E411: Highlight group not found: %s"  ;
+static char e_not_enough_arguments_highlight_link_str[]  =  "E412: Not enough arguments: \":highlight link %s\""  ;
+static char e_too_many_arguments_highlight_link_str[]  =  "E413: Too many arguments: \":highlight link %s\""  ;
+static char e_group_has_settings_highlight_link_ignored[]  =  "E414: Group has settings, highlight link ignored"  ;
+static char e_unexpected_equal_sign_str[]  =  "E415: Unexpected equal sign: %s"  ;
+static char e_missing_equal_sign_str_2[]  =  "E416: Missing equal sign: %s"  ;
+static char e_missing_argument_str[]  =  "E417: Missing argument: %s"  ;
+static char e_illegal_value_str[]  =  "E418: Illegal value: %s"  ;
+static char e_fg_color_unknown[]  =  "E419: FG color unknown"  ;
+static char e_bg_color_unknown[]  =  "E420: BG color unknown"  ;
+static char e_color_name_or_number_not_recognized_str[]  =  "E421: Color name or number not recognized: %s"  ;
+static char e_terminal_code_too_long_str[]  =  "E422: Terminal code too long: %s"  ;
+static char e_illegal_argument_str_3[]  =  "E423: Illegal argument: %s"  ;
+static char e_too_many_different_highlighting_attributes_in_use[]  =  "E424: Too many different highlighting attributes in use"  ;
+static char e_cannot_go_before_first_matching_tag[]  =  "E425: Cannot go before first matching tag"  ;
+static char e_tag_not_found_str[]  =  "E426: Tag not found: %s"  ;
+static char e_there_is_only_one_matching_tag[]  =  "E427: There is only one matching tag"  ;
+static char e_cannot_go_beyond_last_matching_tag[]  =  "E428: Cannot go beyond last matching tag"  ;
+static char e_file_str_does_not_exist[]  =  "E429: File \"%s\" does not exist"  ;
+static char e_format_error_in_tags_file_str[]  =  "E431: Format error in tags file \"%s\""  ;
+static char e_tags_file_not_sorted_str[]  =  "E432: Tags file not sorted: %s"  ;
+static char e_no_tags_file[]  =  "E433: No tags file"  ;
+static char e_cannot_find_tag_pattern[]  =  "E434: Can't find tag pattern"  ;
+static char e_couldnt_find_tag_just_guessing[]  =  "E435: Couldn't find tag, just guessing!"  ;
+static char e_no_str_entry_in_termcap[]  =  "E436: No \"%s\" entry in termcap"  ;
+static char e_terminal_capability_cm_required[]  =  "E437: Terminal capability \"cm\" required"  ;
+static char e_u_undo_line_numbers_wrong[]  = "E438: u_undo: Line numbers wrong" ;
+static char e_undo_list_corrupt[]  = "E439: Undo list corrupt" ;
+static char e_undo_line_missing[]  = "E440: Undo line missing" ;
+static char e_cant_split_topleft_and_botright_at_the_same_time[]  =  "E442: Can't split topleft and botright at the same time"  ;
+static char e_cannot_rotate_when_another_window_is_split[]  =  "E443: Cannot rotate when another window is split"  ;
+static char e_cannot_close_last_window[]  =  "E444: Cannot close last window"  ;
+static char e_other_window_contains_changes[]  =  "E445: Other window contains changes"  ;
+static char e_no_file_name_under_cursor[]  =  "E446: No file name under cursor"  ;
+static char e_cant_find_file_str_in_path_2[]  =  "E447: Can't find file \"%s\" in path"  ;
+static char e_ul_color_unknown[]  =  "E453: UL color unknown"  ;
+static char e_cannot_go_back_to_previous_directory[]  =  "E459: Cannot go back to previous directory"  ;
+static char e_could_not_prepare_for_reloading_str[]  =  "E462: Could not prepare for reloading \"%s\""  ;
+static char e_ambiguous_use_of_user_defined_command[]  =  "E464: Ambiguous use of user-defined command"  ;
+static char e_winsize_requires_two_number_arguments[]  =  "E465: :winsize requires two number arguments"  ;
+static char e_winpos_requires_two_number_arguments[]  =  "E466: :winpos requires two number arguments"  ;
+static char e_completion_argument_only_allowed_for_custom_completion[]  =  "E468: Completion argument only allowed for custom completion"  ;
+static char e_command_aborted[]  =  "E470: Command aborted"  ;
+static char e_argument_required[]  =  "E471: Argument required"  ;
+static char e_command_failed[]  =  "E472: Command failed"  ;
+static char e_internal_error_in_regexp[]  = "E473: Internal error in regexp" ;
+static char e_invalid_argument[]  =  "E474: Invalid argument"  ;
+static char e_invalid_argument_str[]  =  "E475: Invalid argument: %s"  ;
+static char e_invalid_value_for_argument_str[]  =  "E475: Invalid value for argument %s"  ;
+static char e_invalid_command[]  =  "E476: Invalid command"  ;
+static char e_invalid_command_str[]  =  "E476: Invalid command: %s"  ;
+static char e_no_bang_allowed[]  =  "E477: No ! allowed"  ;
+static char e_dont_panic[]  =  "E478: Don't panic!"  ;
+static char e_no_match[]  =  "E479: No match"  ;
+static char e_no_match_str_2[]  =  "E480: No match: %s"  ;
+static char e_no_range_allowed[]  =  "E481: No range allowed"  ;
+static char e_cant_create_file_str[]  =  "E482: Can't create file %s"  ;
+static char e_cant_get_temp_file_name[]  =  "E483: Can't get temp file name"  ;
+static char e_cant_open_file_str[]  =  "E484: Can't open file %s"  ;
+static char e_cant_read_file_str[]  =  "E485: Can't read file %s"  ;
+static char e_pattern_not_found_str[]  =  "E486: Pattern not found: %s"  ;
+static char e_argument_must_be_positive[]  =  "E487: Argument must be positive"  ;
+static char e_trailing_characters[]  =  "E488: Trailing characters"  ;
+static char e_trailing_characters_str[]  =  "E488: Trailing characters: %s"  ;
+static char e_no_call_stack_to_substitute_for_stack[]  =  "E489: No call stack to substitute for \"<stack>\""  ;
+static char e_not_an_editor_command[]  =  "E492: Not an editor command"  ;
+static char e_backwards_range_given[]  =  "E493: Backwards range given"  ;
+static char e_use_w_or_w_gt_gt[]  =  "E494: Use w or w>>"  ;
+static char e_no_autocommand_file_name_to_substitute_for_afile[]  =  "E495: No autocommand file name to substitute for \"<afile>\""  ;
+static char e_no_autocommand_buffer_number_to_substitute_for_abuf[]  =  "E496: No autocommand buffer number to substitute for \"<abuf>\""  ;
+static char e_no_autocommand_match_name_to_substitute_for_amatch[]  =  "E497: No autocommand match name to substitute for \"<amatch>\""  ;
+static char e_no_source_file_name_to_substitute_for_sfile[]  =  "E498: No :source file name to substitute for \"<sfile>\""  ;
+static char e_empty_file_name_for_percent_or_hash_only_works_with_ph[]  =  "E499: Empty file name for '%' or '#', only works with \":p:h\""  ;
+static char e_evaluates_to_an_empty_string[]  =  "E500: Evaluates to an empty string"  ;
+static char e_at_end_of_file[]  =  "E501: At end-of-file"  ;
+static char e_is_a_directory[]  =  "is a directory"  ;
+static char e_is_not_file_or_writable_device[]  =  "is not a file or writable device"  ;
+static char e_str_is_not_file_or_writable_device[]  =  "E503: \"%s\" is not a file or writable device"  ;
+static char e_is_read_only_cannot_override_W_in_cpoptions[]  =  "is read-only (cannot override: \"W\" in 'cpoptions')"  ;
+static char e_is_read_only_add_bang_to_override[]  =  "is read-only (add ! to override)"  ;
+static char e_str_is_read_only_add_bang_to_override[]  =  "E505: \"%s\" is read-only (add ! to override)"  ;
+static char e_cant_write_to_backup_file_add_bang_to_override[]  =  "E506: Can't write to backup file (add ! to override)"  ;
+static char e_close_error_for_backup_file_add_bang_to_write_anyway[]  =  "E507: Close error for backup file (add ! to write anyway)"  ;
+static char e_cant_read_file_for_backup_add_bang_to_write_anyway[]  =  "E508: Can't read file for backup (add ! to write anyway)"  ;
+static char e_cannot_create_backup_file_add_bang_to_write_anyway[]  =  "E509: Cannot create backup file (add ! to override)"  ;
+static char e_cant_make_backup_file_add_bang_to_write_anyway[]  =  "E510: Can't make backup file (add ! to write anyway)"  ;
+static char e_close_failed[]  =  "E512: Close failed"  ;
+static char e_write_error_conversion_failed_make_fenc_empty_to_override[]  =  "E513: Write error, conversion failed (make 'fenc' empty to override)"  ;
+static char e_write_error_conversion_failed_in_line_nr_make_fenc_empty_to_override[]  =  "E513: Write error, conversion failed in line %ld (make 'fenc' empty to override)"  ;
+static char e_write_error_file_system_full[]  =  "E514: Write error (file system full?)"  ;
+static char e_no_buffers_were_unloaded[]  =  "E515: No buffers were unloaded"  ;
+static char e_no_buffers_were_deleted[]  =  "E516: No buffers were deleted"  ;
+static char e_no_buffers_were_wiped_out[]  =  "E517: No buffers were wiped out"  ;
+static char e_unknown_option[]  =  "E518: Unknown option"  ;
+static char e_option_not_supported[]  =  "E519: Option not supported"  ;
+static char e_not_allowed_in_modeline[]  =  "E520: Not allowed in a modeline"  ;
+static char e_number_required_after_equal[]  =  "E521: Number required after ="  ;
+static char e_number_required_after_str_equal_str[]  =  "E521: Number required: &%s = '%s'"  ;
+static char e_not_found_in_termcap[]  =  "E522: Not found in termcap"  ;
+static char e_not_allowed_here[]  =  "E523: Not allowed here"  ;
+static char e_missing_colon[]  =  "E524: Missing colon"  ;
+static char e_zero_length_string[]  =  "E525: Zero length string"  ;
+static char e_cannot_set_term_to_empty_string[]  =  "E529: Cannot set 'term' to empty string"  ;
+static char e_illegal_character_after_chr[]  =  "E535: Illegal character after <%c>"  ;
+static char e_commentstring_must_be_empty_or_contain_str[]  =  "E537: 'commentstring' must be empty or contain %s"  ;
+static char e_pattern_found_in_every_line_str[]  =  "E538: Pattern found in every line: %s"  ;
+static char e_illegal_character_str[]  =  "E539: Illegal character <%s>"  ;
+static char e_syntax_error_in_str_curlies[]  =  "E554: Syntax error in %s{...}"  ;
+static char e_at_bottom_of_tag_stack[]  =  "E555: At bottom of tag stack"  ;
+static char e_at_top_of_tag_stack[]  =  "E556: At top of tag stack"  ;
+static char e_not_allowed_to_change_text_or_change_window[]  =  "E565: Not allowed to change text or change window"  ;
+static char e_backupext_and_patchmode_are_equal[]  =  "E589: 'backupext' and 'patchmode' are equal"  ;
+static char e_winheight_cannot_be_smaller_than_winminheight[]  =  "E591: 'winheight' cannot be smaller than 'winminheight'"  ;
+static char e_winwidth_cannot_be_smaller_than_winminwidth[]  =  "E592: 'winwidth' cannot be smaller than 'winminwidth'"  ;
+static char e_need_at_least_nr_lines[]  =  "E593: Need at least %d lines"  ;
+static char e_need_at_least_nr_columns[]  =  "E594: Need at least %d columns"  ;
+static char e_no_argument_to_delete[]  =  "E610: No argument to delete"  ;
+static char e_missing_delimiter_after_search_pattern_str[]  =  "E654: Missing delimiter after search pattern: %s"  ;
+static char e_at_start_of_changelist[]  =  "E662: At start of changelist"  ;
+static char e_at_end_of_changelist[]  =  "E663: At end of changelist"  ;
+static char e_changelist_is_empty[]  =  "E664: Changelist is empty"  ;
+static char e_fsync_failed[]  =  "E667: Fsync failed"  ;
+static char e_unprintable_character_in_group_name[]  =  "E669: Unprintable character in group name"  ;
+static char e_mix_of_help_file_encodings_within_language_str[]  =  "E670: Mix of help file encodings within a language: %s"  ;
+static char e_no_matching_autocommands_for_buftype_str_buffer[]  =  "E676: No matching autocommands for buftype=%s buffer"  ;
+static char e_invalid_character_after_str_2[]  =  "E678: Invalid character after %s%%[dxouU]"  ;
+static char e_buffer_nr_invalid_buffer_number[]  =  "E680: <buffer=%d>: invalid buffer number"  ;
+static char e_internal_error_str[]  =  "E685: Internal error: %s"  ;
+static char e_cannot_change_directory_buffer_is_modified_add_bang_to_override[]  =  "E747: Cannot change directory, buffer is modified (add ! to override)"  ;
+static char e_no_previously_used_register[]  =  "E748: No previously used register"  ;
+static char e_empty_buffer[]  =  "E749: Empty buffer"  ;
+static char e_too_many_arguments_to_printf[]  =  "E767: Too many arguments for printf()"  ;
+static char e_swap_file_exists_str_silent_overrides[]  =  "E768: Swap file exists: %s (:silent! overrides)"  ;
+static char e_missing_rsb_after_str_lsb[]  =  "E769: Missing ] after %s["  ;
+static char e_symlink_loop_for_str[]  =  "E773: Symlink loop for \"%s\""  ;
+static char e_eval_feature_not_available[]  =  "E775: Eval feature not available"  ;
+static char e_cannot_close_last_tab_page[]  =  "E784: Cannot close last tab page"  ;
+static char e_not_allowed_to_edit_another_buffer_now[]  =  "E788: Not allowed to edit another buffer now"  ;
+static char e_undojoin_is_not_allowed_after_undo[]  =  "E790: undojoin is not allowed after undo"  ;
+static char e_invalid_id_nr_must_be_greater_than_or_equal_to_one_1[]  =  "E799: Invalid ID: %d (must be greater than or equal to 1)"  ;
+static char e_arabic_cannot_be_used_not_enabled_at_compile_time[]  =  "E800: Arabic cannot be used: Not enabled at compile time\n"  ;
+static char e_id_already_taken_nr[]  =  "E801: ID already taken: %d"  ;
+static char e_invalid_id_nr_must_be_greater_than_or_equal_to_one_2[]  =  "E802: Invalid ID: %d (must be greater than or equal to 1)"  ;
+static char e_id_not_found_nr[]  =  "E803: ID not found: %d"  ;
+static char e_hashsmall_is_not_available_without_the_eval_feature[]  =  "E809: #< is not available without the +eval feature"  ;
+static char e_not_allowed_to_change_buffer_information_now[]  =  "E811: Not allowed to change buffer information now"  ;
+static char e_autocommands_changed_buffer_or_buffer_name[]  =  "E812: Autocommands changed buffer or buffer name"  ;
+static char e_cannot_close_autocmd_or_popup_window[]  =  "E813: Cannot close autocmd or popup window"  ;
+static char e_cannot_close_window_only_autocmd_window_would_remain[]  =  "E814: Cannot close window, only autocmd window would remain"  ;
+static char e_undo_number_nr_not_found[]  =  "E830: Undo number %ld not found"  ;
+static char e_str_is_encrypted_and_this_version_of_vim_does_not_support_encryption[]  =  "E833: %s is encrypted and this version of Vim does not support encryption"  ;
+static char e_conflicts_with_value_of_listchars[]  =  "E834: Conflicts with value of 'listchars'"  ;
+static char e_conflicts_with_value_of_fillchars[]  =  "E835: Conflicts with value of 'fillchars'"  ;
+static char e_reserved_name_cannot_be_used_for_user_defined_command[]  =  "E841: Reserved name, cannot be used for user defined command"  ;
+static char e_no_line_number_to_use_for_slnum[]  =  "E842: No line number to use for \"<slnum>\""  ;
+static char e_key_code_not_set[]  =  "E846: Key code not set"  ;
+static char e_too_many_highlight_and_syntax_groups[]  =  "E849: Too many highlight and syntax groups"  ;
+static char e_invalid_register_name[]  =  "E850: Invalid register name"  ;
+static char e_path_too_long_for_completion[]  =  "E854: Path too long for completion"  ;
+static char e_autocommands_caused_command_to_abort[]  =  "E855: Autocommands caused command to abort"  ;
+static char e_percent_hash_can_only_be_followed_by_zero_one_two_automatic_engine_will_be_used[]  =  "E864: \\%#= can only be followed by 0, 1, or 2. The automatic engine will be used"  ;
+static char e_nfa_regexp_end_encountered_prematurely[]  =  "E865: (NFA) Regexp end encountered prematurely"  ;
+static char e_nfa_regexp_misplaced_chr[]  =  "E866: (NFA regexp) Misplaced %c"  ;
+static char e_nfa_regexp_unknown_operator_z_chr[]  =  "E867: (NFA regexp) Unknown operator '\\z%c'"  ;
+static char e_nfa_regexp_unknown_operator_percent_chr[]  =  "E867: (NFA regexp) Unknown operator '\\%%%c'"  ;
+static char e_error_building_nfa_with_equivalence_class[]  =  "E868: Error building NFA with equivalence class!"  ;
+static char e_nfa_regexp_unknown_operator_at_chr[]  =  "E869: (NFA regexp) Unknown operator '\\@%c'"  ;
+static char e_nfa_regexp_error_reading_repetition_limits[]  =  "E870: (NFA regexp) Error reading repetition limits"  ;
+static char e_nfa_regexp_cant_have_multi_follow_multi[]  =  "E871: (NFA regexp) Can't have a multi follow a multi"  ;
+static char e_nfa_regexp_too_many_parens[]  =  "E872: (NFA regexp) Too many '('"  ;
+static char e_nfa_regexp_proper_termination_error[]  =  "E873: (NFA regexp) proper termination error"  ;
+static char e_nfa_regexp_could_not_pop_stack[]  =  "E874: (NFA regexp) Could not pop the stack!"  ;
+static char e_nfa_regexp_while_converting_from_postfix_to_nfa_too_many_stats_left_on_stack[]  =  "E875: (NFA regexp) (While converting from postfix to NFA), too many states left on stack"  ;
+static char e_nfa_regexp_not_enough_space_to_store_whole_nfa[]  =  "E876: (NFA regexp) Not enough space to store the whole NFA"  ;
+static char e_nfa_regexp_invalid_character_class_nr[]  =  "E877: (NFA regexp) Invalid character class: %d"  ;
+static char e_nfa_regexp_could_not_allocate_memory_for_branch_traversal[]  =  "E878: (NFA regexp) Could not allocate memory for branch traversal!"  ;
+static char e_line_count_changed_unexpectedly[]  =  "E881: Line count changed unexpectedly"  ;
+static char e_nfa_regexp_cannot_repeat_str[]  =  "E888: (NFA regexp) cannot repeat %s"  ;
+static char e_directory_not_found_in_str_str[]  =  "E919: Directory not found in '%s': \"%s\""  ;
+static char e_buffer_cannot_be_registered[]  =  "E931: Buffer cannot be registered"  ;
+static char e_cannot_delete_current_group[]  =  "E936: Cannot delete the current group"  ;
+static char e_attempt_to_delete_buffer_that_is_in_use_str[]  =  "E937: Attempt to delete a buffer that is in use: %s"  ;
+static char e_positive_count_required[]  =  "E939: Positive count required"  ;
+static char e_command_table_needs_to_be_updated_run_make_cmdidxs[]  = "E943: Command table needs to be updated, run 'make cmdidxs'" ;
+static char e_reverse_range_in_character_class[]  =  "E944: Reverse range in character class"  ;
+static char e_range_too_large_in_character_class[]  =  "E945: Range too large in character class"  ;
+static char e_file_changed_while_writing[]  =  "E949: File changed while writing"  ;
+static char e_cannot_convert_between_str_and_str[]  =  "E950: Cannot convert between %s and %s"  ;
+static char e_percent_value_too_large[]  =  "E951: \\% value too large"  ;
+static char e_cannot_use_pattern_recursively[]  =  "E956: Cannot use pattern recursively"  ;
+static char e_command_not_allowed_in_rvim[]  =  "E981: Command not allowed in rvim"  ;
+static char e_duplicate_argument_str[]  =  "E983: Duplicate argument: %s"  ;
+static char e_scriptversion_used_outside_of_sourced_file[]  =  "E984: :scriptversion used outside of a sourced file"  ;
+static char e_not_allowed_in_modeline_when_modelineexpr_is_off[]  =  "E992: Not allowed in a modeline when 'modelineexpr' is off"  ;
+static char e_scriptversion_not_supported_nr[]  =  "E999: scriptversion not supported: %d"  ;
+static char e_missing_rcurly[]  =  "E1026: Missing }"  ;
+static char e_cannot_use_scriptversion_after_vim9script[]  =  "E1040: Cannot use :scriptversion after :vim9script"  ;
+static char e_colon_required_before_range_str[]  =  "E1050: Colon required before a range: %s"  ;
+static char e_yank_register_changed_while_using_it[]  =  "E1064: Yank register changed while using it"  ;
+static char e_command_cannot_be_shortened_str[]  =  "E1065: Command cannot be shortened: %s"  ;
+static char e_invalid_command_nested_did_you_mean_plusplus_nested[]  =  "E1078: Invalid command \"nested\", did you mean \"++nested\"?"  ;
+static char e_command_modifier_without_command[]  =  "E1082: Command modifier without command"  ;
+static char e_cmd_mapping_must_end_with_cr_before_second_cmd[]  =  "E1136: <Cmd> mapping must end with <CR> before second <Cmd>"  ;
+static char e_cannot_define_autocommands_for_all_events[]  =  "E1155: Cannot define autocommands for ALL events"  ;
+static char e_cannot_change_arglist_recursively[]  =  "E1156: Cannot change the argument list recursively"  ;
+static char e_cannot_split_window_when_closing_buffer[]  =  "E1159: Cannot split a window when closing the buffer"  ;
+static char e_vim9cmd_must_be_followed_by_command[]  =  "E1164: vim9cmd must be followed by a command"  ;
+static char e_failed_to_source_defaults[]  =  "E1187: Failed to source defaults.vim"  ;
+static char e_regexp_number_after_dot_pos_search_chr[]  =  "E1204: No Number allowed after .: '\\%%%c'"  ;
+static char e_no_white_space_allowed_between_option_and[]  =  "E1205: No white space allowed between option and"  ;
+static char e_complete_used_without_allowing_arguments[]  =  "E1208: -complete used without allowing arguments"  ;
+static char e_cannot_use_bar_to_separate_commands_here_str[]  =  "E1231: Cannot use a bar to separate commands here: %s"  ;
+static char e_legacy_must_be_followed_by_command[]  =  "E1234: legacy must be followed by a command"  ;
+static char e_no_such_user_defined_command_in_current_buffer_str[]  =  "E1237: No such user-defined command in current buffer: %s"  ;
+static char e_resulting_text_too_long[]  =  "E1240: Resulting text too long"  ;
+static char e_line_number_out_of_range[]  =  "E1247: Line number out of range"  ;
+static char e_highlight_group_name_too_long[]  =  "E1249: Highlight group name too long"  ;
+static char e_cmd_mapping_must_end_with_cr[]  =  "E1255: <Cmd> mapping must end with <CR>"  ;
+static char e_cannot_use_s_backslash_in_vim9_script[]  =  "E1270: Cannot use :s\\/sub/ in Vim9 script"  ;
+static char e_nfa_regexp_missing_value_in_chr[]  =  "E1273: (NFA regexp) missing value in '\\%%%c'"  ;
+static char e_no_script_file_name_to_substitute_for_script[]  =  "E1274: No script file name to substitute for \"<script>\""  ;
+static char e_atom_engine_must_be_at_start_of_pattern[]  =  "E1281: Atom '\\%%#=%c' must be at the start of the pattern"  ;
+static char e_cmdline_window_already_open[]  =  "E1292: Command-line window is already open"  ;
+static char e_window_unexpectedly_close_while_searching_for_tags[]  =  "E1299: Window unexpectedly closed while searching for tags"  ;
+static char e_cannot_change_mappings_while_listing[]  =  "E1309: Cannot change mappings while listing"  ;
+static char e_cannot_change_user_commands_while_listing[]  =  "E1311: Cannot change user commands while listing"  ;
+static char e_not_allowed_to_change_window_layout_in_this_autocmd[]  =  "E1312: Not allowed to change the window layout in this autocmd"  ;
+static char e_not_allowed_to_add_or_remove_entries_str[]  =  "E1313: Not allowed to add or remove entries (%s)"  ;
+static char e_internal_error_shortmess_too_long[]  = "E1336: Internal error: shortmess too long" ;
+static char e_warning_pointer_block_corrupted[]  =  "E1364: Warning: Pointer block corrupted"  ;
+static char e_cannot_mix_positional_and_non_positional_str[]  =  "E1500: Cannot mix positional and non-positional arguments: %s"  ;
+static char e_fmt_arg_nr_unused_str[]  =  "E1501: format argument %d unused in $-style format: %s"  ;
+static char e_positional_num_field_spec_reused_str_str[]  =  "E1502: Positional argument %d used as field width reused as different type: %s/%s"  ;
+static char e_positional_arg_num_type_inconsistent_str_str[]  =  "E1504: Positional argument %d type used inconsistently: %s/%s"  ;
+static char e_invalid_format_specifier_str[]  =  "E1505: Invalid format specifier: %s"  ;
+static char e_aptypes_is_null_nr_str[]  = "E1507: Internal error: ap_types or ap_types[idx] is NULL: %d: %s" ;
+static char e_val_too_large[]  =  "E1510: Value too large: %s"  ;
+static char e_wrong_number_of_characters_for_field_str[]  =  "E1511: Wrong number of characters for field \"%s\""  ;
+static char e_wrong_character_width_for_field_str[]  =  "E1512: Wrong character width for field \"%s\""  ;
+static char e_winfixbuf_cannot_go_to_buffer[]  =  "E1513: Cannot switch buffer. 'winfixbuf' is enabled"  ;
+static char e_unicode_val_too_large[]  =  "E1541: Value too large, max Unicode codepoint is U+10FFFF"  ;
+static char e_cannot_switch_to_a_closing_buffer[]  =  "E1546: Cannot switch to a closing buffer"  ;
+static char e_cannot_not_support_redrawtabpanel[]  =  "E1547: This version of Vim does not support :redrawtabpanel"  ;
+static char e_osc_response_timed_out[]  =  "E1568: OSC command response timed out: %.*s"  ;
+static char e_leadtab_requires_tab[]  =  "E1572: 'listchars' field \"leadtab\" requires \"tab\" to be specified"  ;
+static char e_tag_file_entry_must_not_be_url[]  =  "E1576: Tag file entry must not be a URL"  ;
+static char e_invalid_format_string_single_percent_s[]  =  "E1577: Invalid format string, only one \"%s\" is allowed"  ;
+static char e_completeopt_escape_cannot_be_used_with_nargs_underscore[]  =  "E1579: -completeopt=escape cannot be used with -nargs=_"  ;
 
 // ---------------- end errors.h ----------------
 
-# define vim_realloc(ptr, size)  realloc((ptr), (size))
-
-#define MB_BYTE2LEN(b)          mb_bytelen_tab[b]
-#define MB_BYTE2LEN_CHECK(b)    (((b) < 0 || (b) > 255) ? 1 : mb_bytelen_tab[b])
+enum { ENC_8BIT = 0x01 };
+enum { ENC_DBCS = 0x02 };
+enum { ENC_UNICODE = 0x04 };
+
+enum { ENC_ENDIAN_B = 0x10 };
+enum { ENC_ENDIAN_L = 0x20 };
+
+enum { ENC_2BYTE = 0x40 };
+enum { ENC_4BYTE = 0x80 };
+enum { ENC_2WORD = 0x100 };
+
+enum { ENC_LATIN1 = 0x200 };
+enum { ENC_LATIN9 = 0x400 };
+enum { ENC_MACROMAN = 0x800 };
+
+enum { VGR_GLOBAL = 1 };
+enum { VGR_NOJUMP = 2 };
+enum { VGR_FUZZY = 4 };
+
+enum { BAD_REPLACE = '?' };
+
+enum { DOSO_NONE = 0 };
+enum { DOSO_VIMRC = 1 };
+enum { DOSO_GVIMRC = 2 };
+
+enum { BFA_DEL = 1 };
+enum { BFA_WIPE = 2 };
+enum { BFA_KEEP_UNDO = 4 };
+enum { BFA_IGNORE_ABORT = 8 };
+
+enum { MSCR_DOWN = 0 };
+enum { MSCR_UP = 1 };
+
+enum { KEYLEN_REMOVED = 9999 };
+
+enum { DIP_ALL = 0x01 };
+enum { DIP_DIR = 0x02 };
+enum { DIP_ERR = 0x04 };
+enum { DIP_START = 0x08 };
+enum { DIP_OPT = 0x10 };
+enum { DIP_NORTP = 0x20 };
+enum { DIP_NOAFTER = 0x40 };
+enum { DIP_AFTER = 0x80 };
+
+enum { LOWEST_WIN_ID = 1000 };
+
+enum { TFN_QUIET = 0x02 };
+enum { TFN_NO_AUTOLOAD = 0x04 };
+enum { TFN_READ_ONLY = 0x10 };
+enum { TFN_NO_DECL = 0x20 };
+enum { TFN_COMPILING = 0x40 };
+enum { TFN_ASSIGN_WITH_OP = 0x100 };
 
-#define ENC_8BIT        0x01
-#define ENC_DBCS        0x02
-#define ENC_UNICODE     0x04
-
-#define ENC_ENDIAN_B    0x10
-#define ENC_ENDIAN_L    0x20
-
-#define ENC_2BYTE       0x40
-#define ENC_4BYTE       0x80
-#define ENC_2WORD       0x100
-
-#define ENC_LATIN1      0x200
-#define ENC_LATIN9      0x400
-#define ENC_MACROMAN    0x800
-
-#  define ICONV_ERRNO errno
-#  define ICONV_E2BIG  E2BIG
-#  define ICONV_EINVAL EINVAL
-#  define ICONV_EILSEQ EILSEQ
-
-#define SIGN_BYTE 1
-
-# define X_DISPLAY      (Display *)NULL
-
-# define nbdebug(a)
-
-#define SIGNAL_BLOCK    (-1)
-#define SIGNAL_UNBLOCK  (-2)
-
-#define VGR_GLOBAL      1
-#define VGR_NOJUMP      2
-#define VGR_FUZZY       4
-
-#define BAD_REPLACE     '?'
-#define BAD_KEEP        (-1)
-#define BAD_DROP        (-2)
-
-#define DOSO_NONE       0
-#define DOSO_VIMRC      1
-#define DOSO_GVIMRC     2
-
-#define VIF_WANT_INFO       1
-#define VIF_WANT_MARKS      2
-#define VIF_ONLY_CURBUF     4
-#define VIF_FORCEIT         8
-#define VIF_GET_OLDFILES    16
-
-#define BFA_DEL          1
-#define BFA_WIPE         2
-#define BFA_KEEP_UNDO    4
-#define BFA_IGNORE_ABORT 8
-
-#define MSCR_DOWN       0
-#define MSCR_UP         1
-#define MSCR_LEFT       (-1)
-#define MSCR_RIGHT      (-2)
-
-#define KEYLEN_PART_KEY (-1)
-#define KEYLEN_PART_MAP (-2)
-#define KEYLEN_REMOVED  9999
-
-#define FILEINFO_OK          0
-#define FILEINFO_ENC_FAIL    1
-#define FILEINFO_READ_FAIL   2
-#define FILEINFO_INFO_FAIL   3
-
-#define SOPT_BOOL       0x01
-#define SOPT_NUM        0x02
-#define SOPT_STRING     0x04
-#define SOPT_GLOBAL     0x08
-#define SOPT_WIN        0x10
-#define SOPT_BUF        0x20
-#define SOPT_UNSET      0x40
-
-#define SREQ_GLOBAL     0
-#define SREQ_WIN        1
-#define SREQ_BUF        2
-
-#define GREG_NO_EXPR    1
-#define GREG_EXPR_SRC   2
-#define GREG_LIST       4
-
-#define AUTOLOAD_CHAR '#'
-
-# define MAX_OPEN_CHANNELS 0
-
-# define MAX_CLIENT_CHANNELS 0
-
-#define JSON_JS         1
-#define JSON_NO_NONE    2
-#define JSON_NL         4
-
-#define DIP_ALL     0x01
-#define DIP_DIR     0x02
-#define DIP_ERR     0x04
-#define DIP_START   0x08
-#define DIP_OPT     0x10
-#define DIP_NORTP   0x20
-#define DIP_NOAFTER 0x40
-#define DIP_AFTER   0x80
-
-#define LOWEST_WIN_ID 1000
-
-#define COPYID_INC 2
-#define COPYID_MASK (~0x1)
-
-#define TFN_INT         0x01
-#define TFN_QUIET       0x02
-#define TFN_NO_AUTOLOAD 0x04
-#define TFN_NO_DEREF    0x08
-#define TFN_READ_ONLY   0x10
-#define TFN_NO_DECL     0x20
-#define TFN_COMPILING   0x40
-#define TFN_NEW_FUNC    0x80
-#define TFN_ASSIGN_WITH_OP 0x100
-#define TFN_IN_CLASS    0x200
-
-#define GLV_QUIET       TFN_QUIET
-#define GLV_NO_AUTOLOAD TFN_NO_AUTOLOAD
-#define GLV_READ_ONLY   TFN_READ_ONLY
-#define GLV_NO_DECL     TFN_NO_DECL
-#define GLV_COMPILING   TFN_COMPILING
-#define GLV_ASSIGN_WITH_OP TFN_ASSIGN_WITH_OP
-#define GLV_PREFER_FUNC 0x10000
-#define GLV_FOR_LOOP    0x20000
-
-#define DO_NOT_FREE_CNT 99999
-
-#define FLEN_FIXED 40
-
-#define FNE_INCL_BR     1
-#define FNE_CHECK_START 2
-
-# define ELAPSED_TIMEVAL
-# define ELAPSED_INIT(v) gettimeofday(&(v), NULL)
-# define ELAPSED_FUNC(v) elapsed(&(v))
 typedef struct timeval elapsed_T;
 static long elapsed(struct timeval *start_tv);
 
-#define REPLACE_CR_NCHAR    (-1)
-#define REPLACE_NL_NCHAR    (-2)
-
-#define TERM_START_NOJOB        1
-#define TERM_START_FORCEIT      2
-#define TERM_START_SYSTEM       4
-
-#define SAVE_RESTORE_TITLE      1
-#define SAVE_RESTORE_ICON       2
-#define SAVE_RESTORE_BOTH       (SAVE_RESTORE_TITLE | SAVE_RESTORE_ICON)
-
-#define APC_SAVE_FOR_UNDO       1
-#define APC_SUBSTITUTE          2
-#define APC_INDENT              4
-
-#define CLIP_ZINDEX 32000
+enum { SAVE_RESTORE_TITLE = 1 };
+enum { SAVE_RESTORE_ICON = 2 };
 
-#define REPTERM_FROM_PART       1
-#define REPTERM_DO_LT           2
-#define REPTERM_SPECIAL         4
-#define REPTERM_NO_SIMPLIFY     8
+enum { REPTERM_FROM_PART = 1 };
+enum { REPTERM_DO_LT = 2 };
+enum { REPTERM_SPECIAL = 4 };
+enum { REPTERM_NO_SIMPLIFY = 8 };
 
-#define FSK_KEYCODE     0x01
-#define FSK_KEEP_X_KEY  0x02
-#define FSK_IN_STRING   0x04
-#define FSK_SIMPLIFY    0x08
-#define FSK_FROM_PART   0x10
+enum { FSK_KEYCODE = 0x01 };
+enum { FSK_KEEP_X_KEY = 0x02 };
+enum { FSK_IN_STRING = 0x04 };
+enum { FSK_SIMPLIFY = 0x08 };
+enum { FSK_FROM_PART = 0x10 };
 
-#define READDIR_SORT_NONE       0
-#define READDIR_SORT_BYTE       1
-#define READDIR_SORT_IC         2
-#define READDIR_SORT_COLLATE    3
+enum { READDIR_SORT_NONE = 0 };
 
-#define MCH_DELAY_IGNOREINPUT   1
-#define MCH_DELAY_SETTMODE      2
+enum { MCH_DELAY_IGNOREINPUT = 1 };
+enum { MCH_DELAY_SETTMODE = 2 };
 
-#define EVAL_VAR_VERBOSE        1
-#define EVAL_VAR_NOAUTOLOAD     2
-#define EVAL_VAR_IMPORT         4
-#define EVAL_VAR_NO_FUNC        8
+enum { FUZZY_MATCH_MAX_LEN = 1024 };
 
-#define FUZZY_MATCH_MAX_LEN     1024
-#define FUZZY_SCORE_NONE        INT_MIN
+enum { UC_BUFFER = 1 };
+enum { UC_VIM9 = 2 };
 
-#define ETYPE_ARG_UNKNOWN 1
+enum { UCC_ESCAPE = 0x1 };
 
-#define UC_BUFFER       1
-#define UC_VIM9         2
+enum { VSE_NONE = 0 };
+enum { VSE_SHELL = 1 };
+enum { VSE_BUFFER = 2 };
 
-#define UCC_ESCAPE      0x1
-
-#define VSE_NONE        0
-#define VSE_SHELL       1
-#define VSE_BUFFER      2
-
-#define FFED_IS_GLOBAL  1
-#define FFED_NO_GLOBAL  2
-
-#define MAX_LSHIFT_BITS (varnumber_T)((sizeof(uvarnumber_T) * 8) - 1)
-
-#define CF_CLASS        1
-#define CF_INTERFACE    2
-#define CF_ABSTRACT_METHOD      4
-
-#define GETVCOL_END_EXCL_LBR    1
-
-#  define PATH_ESC_WILDCARDS    "*?[{"
+enum { GETVCOL_END_EXCL_LBR = 1 };
 
 // ---------------- end vim.h ----------------
 
-#define KEEP_ROOM (2 * 8192L)
-#define KEEP_ROOM_KB (KEEP_ROOM / 1024L)
-
     void *
 alloc(size_t size)
 {
@@ -9997,12 +8317,11 @@
     p = lalloc(size, TRUE);
     if (p != NULL)
     {
-        (void)vim_memset(p, 0, size);
+        (void) memset((p), (0), (size)) ;
     }
     return p;
 }
 
-
     void *
 lalloc_clear(size_t size, int message)
 {
@@ -10011,7 +8330,7 @@
     p = lalloc(size, message);
     if (p != NULL)
     {
-        (void)vim_memset(p, 0, size);
+        (void) memset((p), (0), (size)) ;
     }
     return p;
 }
@@ -10088,7 +8407,7 @@
 
     if (ret != NULL)
     {
-        mch_memmove(ret, p, len);
+         memmove((char *)(ret), (char *)(p), len) ;
     }
     return ret;
 }
@@ -10150,7 +8469,6 @@
     return OK;
 }
 
-
     int
 ga_grow_inner(garray_T *gap, int n)
 {
@@ -10173,13 +8491,13 @@
         return FAIL;
     }
     new_len = (size_t)gap->ga_itemsize * (gap->ga_len + n);
-    pp = vim_realloc(gap->ga_data, new_len);
+    pp =  realloc((gap->ga_data), (new_len)) ;
     if (pp == NULL)
     {
         return FAIL;
     }
     old_len = (size_t)gap->ga_itemsize * gap->ga_maxlen;
-    vim_memset(pp + old_len, 0, new_len - old_len);
+     memset((pp + old_len), (0), (new_len - old_len)) ;
     gap->ga_maxlen = gap->ga_len + n;
     gap->ga_data = pp;
     return OK;
@@ -10190,13 +8508,13 @@
 {
     int         i;
     int         len = 0;
-    int         sep_len = (int)STRLEN(sep);
+    int         sep_len = (int) strlen((char *)(sep)) ;
     char_u      *s;
     char_u      *p;
 
     for (i = 0; i < gap->ga_len; ++i)
     {
-        len += (int)STRLEN(((char_u **)(gap->ga_data))[i]);
+        len += (int) strlen((char *)(((char_u **)(gap->ga_data))[i])) ;
     }
 
     if (gap->ga_len > 1)
@@ -10216,11 +8534,11 @@
     {
         if (p != s)
         {
-            STRCPY(p, sep);
+             strcpy((char *)(p), (char *)(sep)) ;
             p += sep_len;
         }
-        STRCPY(p, ((char_u **)(gap->ga_data))[i]);
-        p += STRLEN(p);
+         strcpy((char *)(p), (char *)(((char_u **)(gap->ga_data))[i])) ;
+        p +=  strlen((char *)(p)) ;
     }
     return s;
 }
@@ -10264,10 +8582,10 @@
     {
         return;
     }
-    len = (int)STRLEN(s);
+    len = (int) strlen((char *)(s)) ;
     if (ga_grow(gap, len) == OK)
     {
-        mch_memmove((char *)gap->ga_data + gap->ga_len, s, (size_t)len);
+         memmove((char *)((char *)gap->ga_data + gap->ga_len), (char *)(s), (size_t)len) ;
         gap->ga_len += len;
     }
 }
@@ -10281,7 +8599,7 @@
     }
     if (ga_grow(gap, (int)len) == OK)
     {
-        mch_memmove((char *)gap->ga_data + gap->ga_len, s, len);
+         memmove((char *)((char *)gap->ga_data + gap->ga_len), (char *)(s), len) ;
         gap->ga_len += (int)len;
     }
 }
@@ -10312,9 +8630,9 @@
 
 // ==================== arglist.c ====================
 
-#define AL_SET  1
-#define AL_ADD  2
-#define AL_DEL  3
+enum { AL_SET = 1 };
+enum { AL_ADD = 2 };
+enum { AL_DEL = 3 };
 
 static int arglist_locked = FALSE;
 
@@ -10338,7 +8656,7 @@
     }
     while (--al->al_ga.ga_len >= 0)
     {
-        vim_free(AARGLIST(al)[al->al_ga.ga_len].ae_fname);
+        vim_free( ((aentry_T *)((al)->al_ga.ga_data)) [al->al_ga.ga_len].ae_fname);
     }
     ga_clear(&al->al_ga);
 }
@@ -10362,7 +8680,7 @@
     void
 alist_new(void)
 {
-    curwin->w_alist = ALLOC_ONE(alist_T);
+    curwin->w_alist =  (alist_T *)alloc(sizeof(alist_T)) ;
     if (curwin->w_alist == NULL)
     {
         curwin->w_alist = &global_alist;
@@ -10387,7 +8705,7 @@
     }
 
     alist_clear(al);
-    if (GA_GROW_OK(&al->al_ga, count))
+    if (  __builtin_expect(((((&al->al_ga)->ga_maxlen - (&al->al_ga)->ga_len < (count)) ? ga_grow_inner((&al->al_ga), (count)) : OK) == OK), 1)  )
     {
         for (i = 0; i < count; ++i)
         {
@@ -10438,10 +8756,10 @@
     arglist_locked = TRUE;
     ++wp->w_locked;
 
-    AARGLIST(al)[al->al_ga.ga_len].ae_fname = fname;
+     ((aentry_T *)((al)->al_ga.ga_data)) [al->al_ga.ga_len].ae_fname = fname;
     if (set_fnum > 0)
     {
-        AARGLIST(al)[al->al_ga.ga_len].ae_fnum =
+         ((aentry_T *)((al)->al_ga.ga_data)) [al->al_ga.ga_len].ae_fnum =
             buflist_add(fname, BLN_LISTED | (set_fnum == 2 ? BLN_CURBUF : 0));
     }
     ++al->al_ga.ga_len;
@@ -10512,20 +8830,25 @@
     win_T       *win;
     tabpage_T   *tp;
 
-    FOR_ALL_TAB_WINDOWS(tp, win)
+     for ((tp) = first_tabpage; (tp) != NULL; (tp) = (tp)->tp_next)
+     {
+         for ((win) = ((tp) == curtab)            ? firstwin : (tp)->tp_firstwin; (win); (win) = (win)->w_next)
+         {
         if (win->w_alist == curwin->w_alist)
         {
             check_arg_idx(win);
         }
+         }
+     }
 }
 
     static void
 alist_add_list(int         count, char_u      **files, int         after, int         will_edit)
 {
     int         i;
-    int         old_argcount = ARGCOUNT;
+    int         old_argcount =  ( (curwin)->w_alist ->al_ga.ga_len) ;
 
-    if (check_arglist_locked() != FAIL && GA_GROW_OK(&ALIST(curwin)->al_ga, count))
+    if (check_arglist_locked() != FAIL &&   __builtin_expect(((((& (curwin)->w_alist ->al_ga)->ga_maxlen - (& (curwin)->w_alist ->al_ga)->ga_len < (count)) ? ga_grow_inner((& (curwin)->w_alist ->al_ga), (count)) : OK) == OK), 1)  )
     {
         win_T   *wp = curwin;
 
@@ -10533,13 +8856,13 @@
         {
             after = 0;
         }
-        if (after > ARGCOUNT)
+        if (after >  ( (curwin)->w_alist ->al_ga.ga_len) )
         {
-            after = ARGCOUNT;
+            after =  ( (curwin)->w_alist ->al_ga.ga_len) ;
         }
-        if (after < ARGCOUNT)
+        if (after <  ( (curwin)->w_alist ->al_ga.ga_len) )
         {
-            mch_memmove(&(ARGLIST[after + count]), &(ARGLIST[after]), (ARGCOUNT - after) * sizeof(aentry_T));
+             memmove((char *)(&( ((aentry_T *) (curwin)->w_alist ->al_ga.ga_data) [after + count])), (char *)(&( ((aentry_T *) (curwin)->w_alist ->al_ga.ga_data) [after])), ( ( (curwin)->w_alist ->al_ga.ga_len)  - after) * sizeof(aentry_T)) ;
         }
         arglist_locked = TRUE;
         ++wp->w_locked;
@@ -10547,12 +8870,12 @@
         {
             int flags = BLN_LISTED | (will_edit ? BLN_CURBUF : 0);
 
-            ARGLIST[after + i].ae_fname = files[i];
-            ARGLIST[after + i].ae_fnum = buflist_add(files[i], flags);
+             ((aentry_T *) (curwin)->w_alist ->al_ga.ga_data) [after + i].ae_fname = files[i];
+             ((aentry_T *) (curwin)->w_alist ->al_ga.ga_data) [after + i].ae_fnum = buflist_add(files[i], flags);
         }
         arglist_locked = FALSE;
         --wp->w_locked;
-        ALIST(wp)->al_ga.ga_len += count;
+         (wp)->w_alist ->al_ga.ga_len += count;
         if (old_argcount > 0 && wp->w_arg_idx >= after)
         {
             wp->w_arg_idx += count;
@@ -10592,14 +8915,14 @@
         }
 
         didone = FALSE;
-        for (match = 0; match < ARGCOUNT; ++match)
+        for (match = 0; match <  ( (curwin)->w_alist ->al_ga.ga_len) ; ++match)
         {
-            if (vim_regexec(&regmatch, alist_name(&ARGLIST[match]), (colnr_T)0))
+            if (vim_regexec(&regmatch, alist_name(& ((aentry_T *) (curwin)->w_alist ->al_ga.ga_data) [match]), (colnr_T)0))
             {
                 didone = TRUE;
-                vim_free(ARGLIST[match].ae_fname);
-                mch_memmove(ARGLIST + match, ARGLIST + match + 1, (ARGCOUNT - match - 1) * sizeof(aentry_T));
-                --ARGCOUNT;
+                vim_free( ((aentry_T *) (curwin)->w_alist ->al_ga.ga_data) [match].ae_fname);
+                 memmove((char *)( ((aentry_T *) (curwin)->w_alist ->al_ga.ga_data)  + match), (char *)( ((aentry_T *) (curwin)->w_alist ->al_ga.ga_data)  + match + 1), ( ( (curwin)->w_alist ->al_ga.ga_len)  - match - 1) * sizeof(aentry_T)) ;
+                -- ( (curwin)->w_alist ->al_ga.ga_len) ;
                 if (curwin->w_arg_idx > match)
                 {
                     --curwin->w_arg_idx;
@@ -10619,7 +8942,7 @@
 }
 
     static int
-do_arglist(char_u      *str, int         what, int         after UNUSED, int         will_edit)
+do_arglist(char_u      *str, int         what, int         after  __attribute__((unused)) , int         will_edit)
 {
     garray_T    new_ga;
     int         exp_count;
@@ -10668,7 +8991,7 @@
         }
         else
         {
-            alist_set(ALIST(curwin), exp_count, exp_files, will_edit, NULL, 0);
+            alist_set( (curwin)->w_alist , exp_count, exp_files, will_edit, NULL, 0);
         }
     }
 
@@ -10686,16 +9009,16 @@
     int
 editing_arg_idx(win_T *win)
 {
-    return !(win->w_arg_idx >= WARGCOUNT(win) || (win->w_buffer->b_fnum != WARGLIST(win)[win->w_arg_idx].ae_fnum && (win->w_buffer->b_ffname == NULL || !(fullpathcmp(alist_name(&WARGLIST(win)[win->w_arg_idx]), win->w_buffer->b_ffname, TRUE, TRUE) & FPC_SAME))));
+    return !(win->w_arg_idx >=  ( (win)->w_alist ->al_ga.ga_len)  || (win->w_buffer->b_fnum !=  ((aentry_T *) (win)->w_alist ->al_ga.ga_data) [win->w_arg_idx].ae_fnum && (win->w_buffer->b_ffname == NULL || !(fullpathcmp(alist_name(& ((aentry_T *) (win)->w_alist ->al_ga.ga_data) [win->w_arg_idx]), win->w_buffer->b_ffname, TRUE, TRUE) & FPC_SAME))));
 }
 
     void
 check_arg_idx(win_T *win)
 {
-    if (WARGCOUNT(win) > 1 && !editing_arg_idx(win))
+    if ( ( (win)->w_alist ->al_ga.ga_len)  > 1 && !editing_arg_idx(win))
     {
         win->w_arg_idx_invalid = true;
-        if (win->w_arg_idx != WARGCOUNT(win) - 1 && arg_had_last == FALSE && ALIST(win) == &global_alist && GARGCOUNT > 0 && win->w_arg_idx < GARGCOUNT && (win->w_buffer->b_fnum == GARGLIST[GARGCOUNT - 1].ae_fnum || (win->w_buffer->b_ffname != NULL && (fullpathcmp(alist_name(&GARGLIST[GARGCOUNT - 1]), win->w_buffer->b_ffname, TRUE, TRUE) & FPC_SAME))))
+        if (win->w_arg_idx !=  ( (win)->w_alist ->al_ga.ga_len)  - 1 && arg_had_last == FALSE &&  (win)->w_alist  == &global_alist &&  (global_alist.al_ga.ga_len)  > 0 && win->w_arg_idx <  (global_alist.al_ga.ga_len)  && (win->w_buffer->b_fnum ==  ((aentry_T *)global_alist.al_ga.ga_data) [ (global_alist.al_ga.ga_len)  - 1].ae_fnum || (win->w_buffer->b_ffname != NULL && (fullpathcmp(alist_name(& ((aentry_T *)global_alist.al_ga.ga_data) [ (global_alist.al_ga.ga_len)  - 1]), win->w_buffer->b_ffname, TRUE, TRUE) & FPC_SAME))))
         {
             arg_had_last = TRUE;
         }
@@ -10703,7 +9026,7 @@
     else
     {
         win->w_arg_idx_invalid = false;
-        if (win->w_arg_idx == WARGCOUNT(win) - 1 && win->w_alist == &global_alist)
+        if (win->w_arg_idx ==  ( (win)->w_alist ->al_ga.ga_len)  - 1 && win->w_alist == &global_alist)
         {
             arg_had_last = TRUE;
         }
@@ -10721,10 +9044,10 @@
         {
             return;
         }
-        alist_unlink(ALIST(curwin));
+        alist_unlink( (curwin)->w_alist );
         if (eap->cmdidx == CMD_argglobal)
         {
-            ALIST(curwin) = &global_alist;
+             (curwin)->w_alist  = &global_alist;
         }
         else
         {
@@ -10746,12 +9069,12 @@
     {
         char_u **items;
 
-        if (ARGCOUNT <= 0)
+        if ( ( (curwin)->w_alist ->al_ga.ga_len)  <= 0)
         {
             return;
         }
 
-        items = ALLOC_MULT(char_u *, ARGCOUNT);
+        items =  (char_u * *)alloc(sizeof(char_u *) * ( ( (curwin)->w_alist ->al_ga.ga_len) )) ;
         if (items == NULL)
         {
             return;
@@ -10759,11 +9082,11 @@
 
         gotocmdline(TRUE);
 
-        for (i = 0; i < ARGCOUNT; ++i)
+        for (i = 0; i <  ( (curwin)->w_alist ->al_ga.ga_len) ; ++i)
         {
-            items[i] = alist_name(&ARGLIST[i]);
+            items[i] = alist_name(& ((aentry_T *) (curwin)->w_alist ->al_ga.ga_data) [i]);
         }
-        list_in_columns(items, ARGCOUNT, curwin->w_arg_idx);
+        list_in_columns(items,  ( (curwin)->w_alist ->al_ga.ga_len) , curwin->w_arg_idx);
         vim_free(items);
 
         return;
@@ -10773,19 +9096,19 @@
     {
         garray_T        *gap = &curwin->w_alist->al_ga;
 
-        if (GA_GROW_FAILS(gap, GARGCOUNT))
+        if (  __builtin_expect(((((gap)->ga_maxlen - (gap)->ga_len < ( (global_alist.al_ga.ga_len) )) ? ga_grow_inner((gap), ( (global_alist.al_ga.ga_len) )) : OK) == FAIL), 0)  )
         {
             return;
         }
 
-        for (i = 0; i < GARGCOUNT; ++i)
+        for (i = 0; i <  (global_alist.al_ga.ga_len) ; ++i)
         {
-            if (GARGLIST[i].ae_fname != NULL)
+            if ( ((aentry_T *)global_alist.al_ga.ga_data) [i].ae_fname != NULL)
             {
-                AARGLIST(curwin->w_alist)[gap->ga_len].ae_fname =
-                    vim_strsave(GARGLIST[i].ae_fname);
-                AARGLIST(curwin->w_alist)[gap->ga_len].ae_fnum =
-                    GARGLIST[i].ae_fnum;
+                 ((aentry_T *)((curwin->w_alist)->al_ga.ga_data)) [gap->ga_len].ae_fname =
+                    vim_strsave( ((aentry_T *)global_alist.al_ga.ga_data) [i].ae_fname);
+                 ((aentry_T *)((curwin->w_alist)->al_ga.ga_data)) [gap->ga_len].ae_fnum =
+                     ((aentry_T *)global_alist.al_ga.ga_data) [i].ae_fnum;
                 ++gap->ga_len;
             }
         }
@@ -10795,9 +9118,9 @@
     void
 ex_previous(exarg_T *eap)
 {
-    if (curwin->w_arg_idx - (int)eap->line2 >= ARGCOUNT)
+    if (curwin->w_arg_idx - (int)eap->line2 >=  ( (curwin)->w_alist ->al_ga.ga_len) )
     {
-        do_argfile(eap, ARGCOUNT - 1);
+        do_argfile(eap,  ( (curwin)->w_alist ->al_ga.ga_len)  - 1);
     }
     else
     {
@@ -10814,7 +9137,7 @@
     void
 ex_last(exarg_T *eap)
 {
-    do_argfile(eap, ARGCOUNT - 1);
+    do_argfile(eap,  ( (curwin)->w_alist ->al_ga.ga_len)  - 1);
 }
 
     void
@@ -10845,9 +9168,9 @@
     {
         return;
     }
-    if (argn < 0 || argn >= ARGCOUNT)
+    if (argn < 0 || argn >=  ( (curwin)->w_alist ->al_ga.ga_len) )
     {
-        if (ARGCOUNT <= 1)
+        if ( ( (curwin)->w_alist ->al_ga.ga_len)  <= 1)
         {
             emsg(_(e_there_is_only_one_file_to_edit));
         }
@@ -10863,7 +9186,7 @@
         return;
     }
 
-    if (!is_split_cmd && (&ARGLIST[argn])->ae_fnum != curbuf->b_fnum && !check_can_set_curbuf_forceit(eap->forceit))
+    if (!is_split_cmd && (& ((aentry_T *) (curwin)->w_alist ->al_ga.ga_data) [argn])->ae_fnum != curbuf->b_fnum && !check_can_set_curbuf_forceit(eap->forceit))
     {
         return;
     }
@@ -10876,14 +9199,15 @@
         {
             return;
         }
-        RESET_BINDING(curwin);
+         (curwin)-> w_onebuf_opt.wo_scb  = FALSE;
+         (curwin)-> w_onebuf_opt.wo_crb  = FALSE;
     }
     else
     {
         other = TRUE;
         if (buf_hide(curbuf))
         {
-            p = fix_fname(alist_name(&ARGLIST[argn]));
+            p = fix_fname(alist_name(& ((aentry_T *) (curwin)->w_alist ->al_ga.ga_data) [argn]));
             other = otherfile(p);
             vim_free(p);
         }
@@ -10894,12 +9218,12 @@
     }
 
     curwin->w_arg_idx = argn;
-    if (argn == ARGCOUNT - 1 && curwin->w_alist == &global_alist)
+    if (argn ==  ( (curwin)->w_alist ->al_ga.ga_len)  - 1 && curwin->w_alist == &global_alist)
     {
         arg_had_last = TRUE;
     }
 
-    if (do_ecmd(0, alist_name(&ARGLIST[curwin->w_arg_idx]), NULL, eap, ECMD_LAST, (buf_hide(curwin->w_buffer) ? ECMD_HIDE : 0) + (eap->forceit ? ECMD_FORCEIT : 0), curwin) == FAIL)
+    if (do_ecmd(0, alist_name(& ((aentry_T *) (curwin)->w_alist ->al_ga.ga_data) [curwin->w_arg_idx]), NULL, eap,  ((linenr_T)-1) , (buf_hide(curwin->w_buffer) ? ECMD_HIDE : 0) + (eap->forceit ? ECMD_FORCEIT : 0), curwin) == FAIL)
     {
         curwin->w_arg_idx = old_arg_idx;
     }
@@ -10933,35 +9257,35 @@
 }
 
     void
-ex_argdedupe(exarg_T *eap UNUSED)
+ex_argdedupe(exarg_T *eap  __attribute__((unused)) )
 {
     int i;
     int j;
 
-    for (i = 0; i < ARGCOUNT; ++i)
+    for (i = 0; i <  ( (curwin)->w_alist ->al_ga.ga_len) ; ++i)
     {
-        char_u *firstFullname = FullName_save(ARGLIST[i].ae_fname, FALSE);
+        char_u *firstFullname = FullName_save( ((aentry_T *) (curwin)->w_alist ->al_ga.ga_data) [i].ae_fname, FALSE);
         if (firstFullname == NULL)
         {
             return;
         }
 
-        for (j = i + 1; j < ARGCOUNT; ++j)
+        for (j = i + 1; j <  ( (curwin)->w_alist ->al_ga.ga_len) ; ++j)
         {
-            char_u *secondFullname = FullName_save(ARGLIST[j].ae_fname, FALSE);
+            char_u *secondFullname = FullName_save( ((aentry_T *) (curwin)->w_alist ->al_ga.ga_data) [j].ae_fname, FALSE);
             if (secondFullname == NULL)
             {
                 break;
             }
             int areNamesDuplicate =
-                                  fnamecmp(firstFullname, secondFullname) == 0;
+                                   vim_fnamecmp((char_u *)(firstFullname), (char_u *)(secondFullname))  == 0;
             vim_free(secondFullname);
 
             if (areNamesDuplicate)
             {
-                vim_free(ARGLIST[j].ae_fname);
-                mch_memmove(ARGLIST + j, ARGLIST + j + 1, (ARGCOUNT - j - 1) * sizeof(aentry_T));
-                --ARGCOUNT;
+                vim_free( ((aentry_T *) (curwin)->w_alist ->al_ga.ga_data) [j].ae_fname);
+                 memmove((char *)( ((aentry_T *) (curwin)->w_alist ->al_ga.ga_data)  + j), (char *)( ((aentry_T *) (curwin)->w_alist ->al_ga.ga_data)  + j + 1), ( ( (curwin)->w_alist ->al_ga.ga_len)  - j - 1) * sizeof(aentry_T)) ;
+                -- ( (curwin)->w_alist ->al_ga.ga_len) ;
 
                 if (curwin->w_arg_idx == j)
                 {
@@ -10996,7 +9320,7 @@
     {
         i = 0;
     }
-    if (i < ARGCOUNT)
+    if (i <  ( (curwin)->w_alist ->al_ga.ga_len) )
     {
         do_argfile(eap, i);
     }
@@ -11024,16 +9348,16 @@
     {
         if (eap->addr_count == 0)
         {
-            if (curwin->w_arg_idx >= ARGCOUNT)
+            if (curwin->w_arg_idx >=  ( (curwin)->w_alist ->al_ga.ga_len) )
             {
                 emsg(_(e_no_argument_to_delete));
                 return;
             }
             eap->line1 = eap->line2 = curwin->w_arg_idx + 1;
         }
-        else if (eap->line2 > ARGCOUNT)
+        else if (eap->line2 >  ( (curwin)->w_alist ->al_ga.ga_len) )
         {
-            eap->line2 = ARGCOUNT;
+            eap->line2 =  ( (curwin)->w_alist ->al_ga.ga_len) ;
         }
         n = eap->line2 - eap->line1 + 1;
         if (*eap->arg != NUL)
@@ -11052,10 +9376,10 @@
 
         for (i = eap->line1; i <= eap->line2; ++i)
         {
-            vim_free(ARGLIST[i - 1].ae_fname);
+            vim_free( ((aentry_T *) (curwin)->w_alist ->al_ga.ga_data) [i - 1].ae_fname);
         }
-        mch_memmove(ARGLIST + eap->line1 - 1, ARGLIST + eap->line2, (size_t)((ARGCOUNT - eap->line2) * sizeof(aentry_T)));
-        ARGCOUNT -= n;
+         memmove((char *)( ((aentry_T *) (curwin)->w_alist ->al_ga.ga_data)  + eap->line1 - 1), (char *)( ((aentry_T *) (curwin)->w_alist ->al_ga.ga_data)  + eap->line2), (size_t)(( ( (curwin)->w_alist ->al_ga.ga_len)  - eap->line2) * sizeof(aentry_T))) ;
+         ( (curwin)->w_alist ->al_ga.ga_len)  -= n;
         if (curwin->w_arg_idx >= eap->line2)
         {
             curwin->w_arg_idx -= n;
@@ -11070,27 +9394,27 @@
         do_arglist(eap->arg, AL_DEL, 0, FALSE);
     }
 
-    if (ARGCOUNT == 0)
+    if ( ( (curwin)->w_alist ->al_ga.ga_len)  == 0)
     {
         curwin->w_arg_idx = 0;
     }
-    else if (curwin->w_arg_idx >= ARGCOUNT)
+    else if (curwin->w_arg_idx >=  ( (curwin)->w_alist ->al_ga.ga_len) )
     {
-        curwin->w_arg_idx = ARGCOUNT - 1;
+        curwin->w_arg_idx =  ( (curwin)->w_alist ->al_ga.ga_len)  - 1;
     }
 
     maketitle();
 }
 
     char_u *
-get_arglist_name(expand_T *xp UNUSED, int idx)
+get_arglist_name(expand_T *xp  __attribute__((unused)) , int idx)
 {
-    if (idx >= ARGCOUNT)
+    if (idx >=  ( (curwin)->w_alist ->al_ga.ga_len) )
     {
         return NULL;
     }
 
-    return alist_name(&ARGLIST[idx]);
+    return alist_name(& ((aentry_T *) (curwin)->w_alist ->al_ga.ga_data) [idx]);
 }
 
     char_u *
@@ -11154,7 +9478,7 @@
             {
                 for (i = 0; i < aall->opened_len; ++i)
                 {
-                    if (i < aall->alist->al_ga.ga_len && (AARGLIST(aall->alist)[i].ae_fnum == buf->b_fnum || fullpathcmp(alist_name(&AARGLIST(aall->alist)[i]), buf->b_ffname, TRUE, TRUE) & FPC_SAME))
+                    if (i < aall->alist->al_ga.ga_len && ( ((aentry_T *)((aall->alist)->al_ga.ga_data)) [i].ae_fnum == buf->b_fnum || fullpathcmp(alist_name(& ((aentry_T *)((aall->alist)->al_ga.ga_data)) [i]), buf->b_ffname, TRUE, TRUE) & FPC_SAME))
                     {
                         int weight = 1;
 
@@ -11216,7 +9540,7 @@
                             continue;
                         }
                     }
-                    if (ONE_WINDOW && (first_tabpage->tp_next == NULL || !aall->had_tab))
+                    if ( (firstwin == lastwin)  && (first_tabpage->tp_next == NULL || !aall->had_tab))
                     {
                         aall->use_firstwin = TRUE;
                     }
@@ -11257,7 +9581,7 @@
     int         split_ret = OK;
     int         p_ea_save;
 
-    if (aall->keep_tabs && BUFEMPTY() && curbuf->b_nwindows == 1 && curbuf->b_ffname == NULL && !curbuf->b_changed)
+    if (aall->keep_tabs &&  (curbuf->b_ml.ml_line_count == 1 && *ml_get((linenr_T)1) == NUL)  && curbuf->b_nwindows == 1 && curbuf->b_ffname == NULL && !curbuf->b_changed)
     {
         aall->use_firstwin = TRUE;
         tab_drop_empty_window = TRUE;
@@ -11273,7 +9597,7 @@
         {
             if (curwin->w_arg_idx != i)
             {
-                FOR_ALL_WINDOWS(wp)
+                 for ((wp) = firstwin; (wp) != NULL; (wp) = (wp)->w_next) 
                 {
                     if (wp->w_arg_idx == i)
                     {
@@ -11325,7 +9649,7 @@
                 aall->new_curwin = curwin;
                 aall->new_curtab = curtab;
             }
-            (void)do_ecmd(0, alist_name(&AARGLIST(aall->alist)[i]), NULL, NULL, ECMD_ONE, ((buf_hide(curwin->w_buffer) || bufIsChanged(curwin->w_buffer)) ? ECMD_HIDE : 0) + ECMD_OLDBUF, curwin);
+            (void)do_ecmd(0, alist_name(& ((aentry_T *)((aall->alist)->al_ga.ga_data)) [i]), NULL, NULL,  (linenr_T)1 , ((buf_hide(curwin->w_buffer) || bufIsChanged(curwin->w_buffer)) ? ECMD_HIDE : 0) + ECMD_OLDBUF, curwin);
             if (tab_drop_empty_window && i == count - 1)
             {
                 ++autocmd_no_enter;
@@ -11358,7 +9682,7 @@
         emsg(_(e_invalid_in_cmdline_window));
         return;
     }
-    if (ARGCOUNT <= 0)
+    if ( ( (curwin)->w_alist ->al_ga.ga_len)  <= 0)
     {
         return;
     }
@@ -11370,7 +9694,7 @@
     aall.new_curtab = NULL;
     aall.forceit = forceit;
     aall.keep_tabs = keep_tabs;
-    aall.opened_len = ARGCOUNT;
+    aall.opened_len =  ( (curwin)->w_alist ->al_ga.ga_len) ;
     aall.opened = alloc_clear(aall.opened_len);
     if (aall.opened == NULL)
     {
@@ -11456,9 +9780,9 @@
     for (;;)
     {
         len = 0;
-        for (idx = 0; idx < ARGCOUNT; ++idx)
+        for (idx = 0; idx <  ( (curwin)->w_alist ->al_ga.ga_len) ; ++idx)
         {
-            p = alist_name(&ARGLIST[idx]);
+            p = alist_name(& ((aentry_T *) (curwin)->w_alist ->al_ga.ga_data) [idx]);
             if (p == NULL)
             {
                 continue;
@@ -11530,146 +9854,142 @@
     char            last;
 } AutoPat;
 
-#define BUFNEWFILE_INDEX 9
-#define BUFREAD_INDEX 10
+enum { BUFNEWFILE_INDEX = 9 };
+enum { BUFREAD_INDEX = 10 };
 
 static keyvalue_T event_tab[NUM_EVENTS] = {
-    KEYVALUE_ENTRY(-EVENT_BUFADD, "BufAdd"),
-    KEYVALUE_ENTRY(-EVENT_BUFADD, "BufCreate"),
-    KEYVALUE_ENTRY(-EVENT_BUFDELETE, "BufDelete"),
-    KEYVALUE_ENTRY(-EVENT_BUFENTER, "BufEnter"),
-    KEYVALUE_ENTRY(-EVENT_BUFFILEPOST, "BufFilePost"),
-    KEYVALUE_ENTRY(-EVENT_BUFFILEPRE, "BufFilePre"),
-    KEYVALUE_ENTRY(-EVENT_BUFHIDDEN, "BufHidden"),
-    KEYVALUE_ENTRY(-EVENT_BUFLEAVE, "BufLeave"),
-    KEYVALUE_ENTRY(-EVENT_BUFNEW, "BufNew"),
-    KEYVALUE_ENTRY(-EVENT_BUFNEWFILE, "BufNewFile"),
-    KEYVALUE_ENTRY(-EVENT_BUFREADPOST, "BufRead"),
-    KEYVALUE_ENTRY(-EVENT_BUFREADCMD, "BufReadCmd"),
-    KEYVALUE_ENTRY(-EVENT_BUFREADPOST, "BufReadPost"),
-    KEYVALUE_ENTRY(-EVENT_BUFREADPRE, "BufReadPre"),
-    KEYVALUE_ENTRY(-EVENT_BUFUNLOAD, "BufUnload"),
-    KEYVALUE_ENTRY(-EVENT_BUFWINENTER, "BufWinEnter"),
-    KEYVALUE_ENTRY(-EVENT_BUFWINLEAVE, "BufWinLeave"),
-    KEYVALUE_ENTRY(-EVENT_BUFWIPEOUT, "BufWipeout"),
-    KEYVALUE_ENTRY(-EVENT_BUFWRITEPRE, "BufWrite"),
-    KEYVALUE_ENTRY(-EVENT_BUFWRITECMD, "BufWriteCmd"),
-    KEYVALUE_ENTRY(-EVENT_BUFWRITEPOST, "BufWritePost"),
-    KEYVALUE_ENTRY(-EVENT_BUFWRITEPRE, "BufWritePre"),
-    KEYVALUE_ENTRY(EVENT_CMDLINECHANGED, "CmdlineChanged"),
-    KEYVALUE_ENTRY(EVENT_CMDLINEENTER, "CmdlineEnter"),
-    KEYVALUE_ENTRY(EVENT_CMDLINELEAVE, "CmdlineLeave"),
-    KEYVALUE_ENTRY(EVENT_CMDLINELEAVEPRE, "CmdlineLeavePre"),
-    KEYVALUE_ENTRY(EVENT_CMDUNDEFINED, "CmdUndefined"),
-    KEYVALUE_ENTRY(EVENT_CMDWINENTER, "CmdwinEnter"),
-    KEYVALUE_ENTRY(EVENT_CMDWINLEAVE, "CmdwinLeave"),
-    KEYVALUE_ENTRY(EVENT_COLORSCHEME, "ColorScheme"),
-    KEYVALUE_ENTRY(EVENT_COLORSCHEMEPRE, "ColorSchemePre"),
-    KEYVALUE_ENTRY(EVENT_COMPLETECHANGED, "CompleteChanged"),
-    KEYVALUE_ENTRY(EVENT_COMPLETEDONE, "CompleteDone"),
-    KEYVALUE_ENTRY(EVENT_COMPLETEDONEPRE, "CompleteDonePre"),
-    KEYVALUE_ENTRY(-EVENT_CURSORHOLD, "CursorHold"),
-    KEYVALUE_ENTRY(-EVENT_CURSORHOLDI, "CursorHoldI"),
-    KEYVALUE_ENTRY(-EVENT_CURSORMOVED, "CursorMoved"),
-    KEYVALUE_ENTRY(-EVENT_CURSORMOVEDC, "CursorMovedC"),
-    KEYVALUE_ENTRY(-EVENT_CURSORMOVEDI, "CursorMovedI"),
-    KEYVALUE_ENTRY(EVENT_DIFFUPDATED, "DiffUpdated"),
-    KEYVALUE_ENTRY(EVENT_DIRCHANGED, "DirChanged"),
-    KEYVALUE_ENTRY(EVENT_DIRCHANGEDPRE, "DirChangedPre"),
-    KEYVALUE_ENTRY(EVENT_ENCODINGCHANGED, "EncodingChanged"),
-    KEYVALUE_ENTRY(EVENT_EXITPRE, "ExitPre"),
-    KEYVALUE_ENTRY(-EVENT_FILEAPPENDCMD, "FileAppendCmd"),
-    KEYVALUE_ENTRY(-EVENT_FILEAPPENDPOST, "FileAppendPost"),
-    KEYVALUE_ENTRY(-EVENT_FILEAPPENDPRE, "FileAppendPre"),
-    KEYVALUE_ENTRY(-EVENT_FILECHANGEDRO, "FileChangedRO"),
-    KEYVALUE_ENTRY(-EVENT_FILECHANGEDSHELL, "FileChangedShell"),
-    KEYVALUE_ENTRY(-EVENT_FILECHANGEDSHELLPOST, "FileChangedShellPost"),
-    KEYVALUE_ENTRY(EVENT_ENCODINGCHANGED, "FileEncoding"),
-    KEYVALUE_ENTRY(-EVENT_FILEREADCMD, "FileReadCmd"),
-    KEYVALUE_ENTRY(-EVENT_FILEREADPOST, "FileReadPost"),
-    KEYVALUE_ENTRY(-EVENT_FILEREADPRE, "FileReadPre"),
-    KEYVALUE_ENTRY(-EVENT_FILETYPE, "FileType"),
-    KEYVALUE_ENTRY(-EVENT_FILEWRITECMD, "FileWriteCmd"),
-    KEYVALUE_ENTRY(-EVENT_FILEWRITEPOST, "FileWritePost"),
-    KEYVALUE_ENTRY(-EVENT_FILEWRITEPRE, "FileWritePre"),
-    KEYVALUE_ENTRY(-EVENT_FILTERREADPOST, "FilterReadPost"),
-    KEYVALUE_ENTRY(-EVENT_FILTERREADPRE, "FilterReadPre"),
-    KEYVALUE_ENTRY(-EVENT_FILTERWRITEPOST, "FilterWritePost"),
-    KEYVALUE_ENTRY(-EVENT_FILTERWRITEPRE, "FilterWritePre"),
-    KEYVALUE_ENTRY(EVENT_FOCUSGAINED, "FocusGained"),
-    KEYVALUE_ENTRY(EVENT_FOCUSLOST, "FocusLost"),
-    KEYVALUE_ENTRY(EVENT_FUNCUNDEFINED, "FuncUndefined"),
-    KEYVALUE_ENTRY(EVENT_GUIENTER, "GUIEnter"),
-    KEYVALUE_ENTRY(EVENT_GUIFAILED, "GUIFailed"),
-    KEYVALUE_ENTRY(-EVENT_INSERTCHANGE, "InsertChange"),
-    KEYVALUE_ENTRY(-EVENT_INSERTCHARPRE, "InsertCharPre"),
-    KEYVALUE_ENTRY(-EVENT_INSERTENTER, "InsertEnter"),
-    KEYVALUE_ENTRY(-EVENT_INSERTLEAVE, "InsertLeave"),
-    KEYVALUE_ENTRY(-EVENT_INSERTLEAVEPRE, "InsertLeavePre"),
-    KEYVALUE_ENTRY(EVENT_KEYINPUTPRE, "KeyInputPre"),
-    KEYVALUE_ENTRY(EVENT_MENUPOPUP, "MenuPopup"),
-    KEYVALUE_ENTRY(EVENT_MODECHANGED, "ModeChanged"),
-    KEYVALUE_ENTRY(EVENT_OPTIONSET, "OptionSet"),
-    KEYVALUE_ENTRY(EVENT_QUICKFIXCMDPOST, "QuickFixCmdPost"),
-    KEYVALUE_ENTRY(EVENT_QUICKFIXCMDPRE, "QuickFixCmdPre"),
-    KEYVALUE_ENTRY(EVENT_QUITPRE, "QuitPre"),
-    KEYVALUE_ENTRY(EVENT_REMOTEREPLY, "RemoteReply"),
-    KEYVALUE_ENTRY(EVENT_SAFESTATE, "SafeState"),
-    KEYVALUE_ENTRY(EVENT_SAFESTATEAGAIN, "SafeStateAgain"),
-    KEYVALUE_ENTRY(EVENT_SESSIONLOADPOST, "SessionLoadPost"),
-    KEYVALUE_ENTRY(EVENT_SESSIONLOADPRE, "SessionLoadPre"),
-    KEYVALUE_ENTRY(EVENT_SESSIONWRITEPOST, "SessionWritePost"),
-    KEYVALUE_ENTRY(EVENT_SHELLCMDPOST, "ShellCmdPost"),
-    KEYVALUE_ENTRY(-EVENT_SHELLFILTERPOST, "ShellFilterPost"),
-    KEYVALUE_ENTRY(EVENT_SIGUSR1, "SigUSR1"),
-    KEYVALUE_ENTRY(EVENT_SOURCECMD, "SourceCmd"),
-    KEYVALUE_ENTRY(EVENT_SOURCEPOST, "SourcePost"),
-    KEYVALUE_ENTRY(EVENT_SOURCEPRE, "SourcePre"),
-    KEYVALUE_ENTRY(EVENT_SPELLFILEMISSING, "SpellFileMissing"),
-    KEYVALUE_ENTRY(EVENT_STDINREADPOST, "StdinReadPost"),
-    KEYVALUE_ENTRY(EVENT_STDINREADPRE, "StdinReadPre"),
-    KEYVALUE_ENTRY(EVENT_SWAPEXISTS, "SwapExists"),
-    KEYVALUE_ENTRY(EVENT_SYNTAX, "Syntax"),
-    KEYVALUE_ENTRY(EVENT_TABCLOSED, "TabClosed"),
-    KEYVALUE_ENTRY(EVENT_TABCLOSEDPRE, "TabClosedPre"),
-    KEYVALUE_ENTRY(EVENT_TABENTER, "TabEnter"),
-    KEYVALUE_ENTRY(EVENT_TABLEAVE, "TabLeave"),
-    KEYVALUE_ENTRY(EVENT_TABNEW, "TabNew"),
-    KEYVALUE_ENTRY(EVENT_TERMCHANGED, "TermChanged"),
-    KEYVALUE_ENTRY(EVENT_TERMINALOPEN, "TerminalOpen"),
-    KEYVALUE_ENTRY(EVENT_TERMINALWINOPEN, "TerminalWinOpen"),
-    KEYVALUE_ENTRY(EVENT_TERMRESPONSE, "TermResponse"),
-    KEYVALUE_ENTRY(EVENT_TERMRESPONSEALL, "TermResponseAll"),
-    KEYVALUE_ENTRY(-EVENT_TEXTCHANGED, "TextChanged"),
-    KEYVALUE_ENTRY(-EVENT_TEXTCHANGEDI, "TextChangedI"),
-    KEYVALUE_ENTRY(-EVENT_TEXTCHANGEDP, "TextChangedP"),
-    KEYVALUE_ENTRY(-EVENT_TEXTCHANGEDT, "TextChangedT"),
-    KEYVALUE_ENTRY(-EVENT_TEXTPUTPOST, "TextPutPost"),
-    KEYVALUE_ENTRY(-EVENT_TEXTPUTPRE, "TextPutPre"),
-    KEYVALUE_ENTRY(-EVENT_TEXTYANKPOST, "TextYankPost"),
-    KEYVALUE_ENTRY(EVENT_USER, "User"),
-    KEYVALUE_ENTRY(EVENT_VIMENTER, "VimEnter"),
-    KEYVALUE_ENTRY(EVENT_VIMLEAVE, "VimLeave"),
-    KEYVALUE_ENTRY(EVENT_VIMLEAVEPRE, "VimLeavePre"),
-    KEYVALUE_ENTRY(EVENT_VIMRESIZED, "VimResized"),
-    KEYVALUE_ENTRY(EVENT_VIMRESUME, "VimResume"),
-    KEYVALUE_ENTRY(EVENT_VIMSUSPEND, "VimSuspend"),
-    KEYVALUE_ENTRY(-EVENT_WINCLOSED, "WinClosed"),
-    KEYVALUE_ENTRY(-EVENT_WINENTER, "WinEnter"),
-    KEYVALUE_ENTRY(-EVENT_WINLEAVE, "WinLeave"),
-    KEYVALUE_ENTRY(EVENT_WINNEW, "WinNew"),
-    KEYVALUE_ENTRY(EVENT_WINNEWPRE, "WinNewPre"),
-    KEYVALUE_ENTRY(-EVENT_WINRESIZED, "WinResized"),
-    KEYVALUE_ENTRY(-EVENT_WINSCROLLED, "WinScrolled"),
+     {(-EVENT_BUFADD), {((char_u *)"BufAdd"),  (sizeof("BufAdd" "") - 1) }} ,
+     {(-EVENT_BUFADD), {((char_u *)"BufCreate"),  (sizeof("BufCreate" "") - 1) }} ,
+     {(-EVENT_BUFDELETE), {((char_u *)"BufDelete"),  (sizeof("BufDelete" "") - 1) }} ,
+     {(-EVENT_BUFENTER), {((char_u *)"BufEnter"),  (sizeof("BufEnter" "") - 1) }} ,
+     {(-EVENT_BUFFILEPOST), {((char_u *)"BufFilePost"),  (sizeof("BufFilePost" "") - 1) }} ,
+     {(-EVENT_BUFFILEPRE), {((char_u *)"BufFilePre"),  (sizeof("BufFilePre" "") - 1) }} ,
+     {(-EVENT_BUFHIDDEN), {((char_u *)"BufHidden"),  (sizeof("BufHidden" "") - 1) }} ,
+     {(-EVENT_BUFLEAVE), {((char_u *)"BufLeave"),  (sizeof("BufLeave" "") - 1) }} ,
+     {(-EVENT_BUFNEW), {((char_u *)"BufNew"),  (sizeof("BufNew" "") - 1) }} ,
+     {(-EVENT_BUFNEWFILE), {((char_u *)"BufNewFile"),  (sizeof("BufNewFile" "") - 1) }} ,
+     {(-EVENT_BUFREADPOST), {((char_u *)"BufRead"),  (sizeof("BufRead" "") - 1) }} ,
+     {(-EVENT_BUFREADCMD), {((char_u *)"BufReadCmd"),  (sizeof("BufReadCmd" "") - 1) }} ,
+     {(-EVENT_BUFREADPOST), {((char_u *)"BufReadPost"),  (sizeof("BufReadPost" "") - 1) }} ,
+     {(-EVENT_BUFREADPRE), {((char_u *)"BufReadPre"),  (sizeof("BufReadPre" "") - 1) }} ,
+     {(-EVENT_BUFUNLOAD), {((char_u *)"BufUnload"),  (sizeof("BufUnload" "") - 1) }} ,
+     {(-EVENT_BUFWINENTER), {((char_u *)"BufWinEnter"),  (sizeof("BufWinEnter" "") - 1) }} ,
+     {(-EVENT_BUFWINLEAVE), {((char_u *)"BufWinLeave"),  (sizeof("BufWinLeave" "") - 1) }} ,
+     {(-EVENT_BUFWIPEOUT), {((char_u *)"BufWipeout"),  (sizeof("BufWipeout" "") - 1) }} ,
+     {(-EVENT_BUFWRITEPRE), {((char_u *)"BufWrite"),  (sizeof("BufWrite" "") - 1) }} ,
+     {(-EVENT_BUFWRITECMD), {((char_u *)"BufWriteCmd"),  (sizeof("BufWriteCmd" "") - 1) }} ,
+     {(-EVENT_BUFWRITEPOST), {((char_u *)"BufWritePost"),  (sizeof("BufWritePost" "") - 1) }} ,
+     {(-EVENT_BUFWRITEPRE), {((char_u *)"BufWritePre"),  (sizeof("BufWritePre" "") - 1) }} ,
+     {(EVENT_CMDLINECHANGED), {((char_u *)"CmdlineChanged"),  (sizeof("CmdlineChanged" "") - 1) }} ,
+     {(EVENT_CMDLINEENTER), {((char_u *)"CmdlineEnter"),  (sizeof("CmdlineEnter" "") - 1) }} ,
+     {(EVENT_CMDLINELEAVE), {((char_u *)"CmdlineLeave"),  (sizeof("CmdlineLeave" "") - 1) }} ,
+     {(EVENT_CMDLINELEAVEPRE), {((char_u *)"CmdlineLeavePre"),  (sizeof("CmdlineLeavePre" "") - 1) }} ,
+     {(EVENT_CMDUNDEFINED), {((char_u *)"CmdUndefined"),  (sizeof("CmdUndefined" "") - 1) }} ,
+     {(EVENT_CMDWINENTER), {((char_u *)"CmdwinEnter"),  (sizeof("CmdwinEnter" "") - 1) }} ,
+     {(EVENT_CMDWINLEAVE), {((char_u *)"CmdwinLeave"),  (sizeof("CmdwinLeave" "") - 1) }} ,
+     {(EVENT_COLORSCHEME), {((char_u *)"ColorScheme"),  (sizeof("ColorScheme" "") - 1) }} ,
+     {(EVENT_COLORSCHEMEPRE), {((char_u *)"ColorSchemePre"),  (sizeof("ColorSchemePre" "") - 1) }} ,
+     {(EVENT_COMPLETECHANGED), {((char_u *)"CompleteChanged"),  (sizeof("CompleteChanged" "") - 1) }} ,
+     {(EVENT_COMPLETEDONE), {((char_u *)"CompleteDone"),  (sizeof("CompleteDone" "") - 1) }} ,
+     {(EVENT_COMPLETEDONEPRE), {((char_u *)"CompleteDonePre"),  (sizeof("CompleteDonePre" "") - 1) }} ,
+     {(-EVENT_CURSORHOLD), {((char_u *)"CursorHold"),  (sizeof("CursorHold" "") - 1) }} ,
+     {(-EVENT_CURSORHOLDI), {((char_u *)"CursorHoldI"),  (sizeof("CursorHoldI" "") - 1) }} ,
+     {(-EVENT_CURSORMOVED), {((char_u *)"CursorMoved"),  (sizeof("CursorMoved" "") - 1) }} ,
+     {(-EVENT_CURSORMOVEDC), {((char_u *)"CursorMovedC"),  (sizeof("CursorMovedC" "") - 1) }} ,
+     {(-EVENT_CURSORMOVEDI), {((char_u *)"CursorMovedI"),  (sizeof("CursorMovedI" "") - 1) }} ,
+     {(EVENT_DIFFUPDATED), {((char_u *)"DiffUpdated"),  (sizeof("DiffUpdated" "") - 1) }} ,
+     {(EVENT_DIRCHANGED), {((char_u *)"DirChanged"),  (sizeof("DirChanged" "") - 1) }} ,
+     {(EVENT_DIRCHANGEDPRE), {((char_u *)"DirChangedPre"),  (sizeof("DirChangedPre" "") - 1) }} ,
+     {(EVENT_ENCODINGCHANGED), {((char_u *)"EncodingChanged"),  (sizeof("EncodingChanged" "") - 1) }} ,
+     {(EVENT_EXITPRE), {((char_u *)"ExitPre"),  (sizeof("ExitPre" "") - 1) }} ,
+     {(-EVENT_FILEAPPENDCMD), {((char_u *)"FileAppendCmd"),  (sizeof("FileAppendCmd" "") - 1) }} ,
+     {(-EVENT_FILEAPPENDPOST), {((char_u *)"FileAppendPost"),  (sizeof("FileAppendPost" "") - 1) }} ,
+     {(-EVENT_FILEAPPENDPRE), {((char_u *)"FileAppendPre"),  (sizeof("FileAppendPre" "") - 1) }} ,
+     {(-EVENT_FILECHANGEDRO), {((char_u *)"FileChangedRO"),  (sizeof("FileChangedRO" "") - 1) }} ,
+     {(-EVENT_FILECHANGEDSHELL), {((char_u *)"FileChangedShell"),  (sizeof("FileChangedShell" "") - 1) }} ,
+     {(-EVENT_FILECHANGEDSHELLPOST), {((char_u *)"FileChangedShellPost"),  (sizeof("FileChangedShellPost" "") - 1) }} ,
+     {(EVENT_ENCODINGCHANGED), {((char_u *)"FileEncoding"),  (sizeof("FileEncoding" "") - 1) }} ,
+     {(-EVENT_FILEREADCMD), {((char_u *)"FileReadCmd"),  (sizeof("FileReadCmd" "") - 1) }} ,
+     {(-EVENT_FILEREADPOST), {((char_u *)"FileReadPost"),  (sizeof("FileReadPost" "") - 1) }} ,
+     {(-EVENT_FILEREADPRE), {((char_u *)"FileReadPre"),  (sizeof("FileReadPre" "") - 1) }} ,
+     {(-EVENT_FILETYPE), {((char_u *)"FileType"),  (sizeof("FileType" "") - 1) }} ,
+     {(-EVENT_FILEWRITECMD), {((char_u *)"FileWriteCmd"),  (sizeof("FileWriteCmd" "") - 1) }} ,
+     {(-EVENT_FILEWRITEPOST), {((char_u *)"FileWritePost"),  (sizeof("FileWritePost" "") - 1) }} ,
+     {(-EVENT_FILEWRITEPRE), {((char_u *)"FileWritePre"),  (sizeof("FileWritePre" "") - 1) }} ,
+     {(-EVENT_FILTERREADPOST), {((char_u *)"FilterReadPost"),  (sizeof("FilterReadPost" "") - 1) }} ,
+     {(-EVENT_FILTERREADPRE), {((char_u *)"FilterReadPre"),  (sizeof("FilterReadPre" "") - 1) }} ,
+     {(-EVENT_FILTERWRITEPOST), {((char_u *)"FilterWritePost"),  (sizeof("FilterWritePost" "") - 1) }} ,
+     {(-EVENT_FILTERWRITEPRE), {((char_u *)"FilterWritePre"),  (sizeof("FilterWritePre" "") - 1) }} ,
+     {(EVENT_FOCUSGAINED), {((char_u *)"FocusGained"),  (sizeof("FocusGained" "") - 1) }} ,
+     {(EVENT_FOCUSLOST), {((char_u *)"FocusLost"),  (sizeof("FocusLost" "") - 1) }} ,
+     {(EVENT_FUNCUNDEFINED), {((char_u *)"FuncUndefined"),  (sizeof("FuncUndefined" "") - 1) }} ,
+     {(EVENT_GUIENTER), {((char_u *)"GUIEnter"),  (sizeof("GUIEnter" "") - 1) }} ,
+     {(EVENT_GUIFAILED), {((char_u *)"GUIFailed"),  (sizeof("GUIFailed" "") - 1) }} ,
+     {(-EVENT_INSERTCHANGE), {((char_u *)"InsertChange"),  (sizeof("InsertChange" "") - 1) }} ,
+     {(-EVENT_INSERTCHARPRE), {((char_u *)"InsertCharPre"),  (sizeof("InsertCharPre" "") - 1) }} ,
+     {(-EVENT_INSERTENTER), {((char_u *)"InsertEnter"),  (sizeof("InsertEnter" "") - 1) }} ,
+     {(-EVENT_INSERTLEAVE), {((char_u *)"InsertLeave"),  (sizeof("InsertLeave" "") - 1) }} ,
+     {(-EVENT_INSERTLEAVEPRE), {((char_u *)"InsertLeavePre"),  (sizeof("InsertLeavePre" "") - 1) }} ,
+     {(EVENT_KEYINPUTPRE), {((char_u *)"KeyInputPre"),  (sizeof("KeyInputPre" "") - 1) }} ,
+     {(EVENT_MENUPOPUP), {((char_u *)"MenuPopup"),  (sizeof("MenuPopup" "") - 1) }} ,
+     {(EVENT_MODECHANGED), {((char_u *)"ModeChanged"),  (sizeof("ModeChanged" "") - 1) }} ,
+     {(EVENT_OPTIONSET), {((char_u *)"OptionSet"),  (sizeof("OptionSet" "") - 1) }} ,
+     {(EVENT_QUICKFIXCMDPOST), {((char_u *)"QuickFixCmdPost"),  (sizeof("QuickFixCmdPost" "") - 1) }} ,
+     {(EVENT_QUICKFIXCMDPRE), {((char_u *)"QuickFixCmdPre"),  (sizeof("QuickFixCmdPre" "") - 1) }} ,
+     {(EVENT_QUITPRE), {((char_u *)"QuitPre"),  (sizeof("QuitPre" "") - 1) }} ,
+     {(EVENT_REMOTEREPLY), {((char_u *)"RemoteReply"),  (sizeof("RemoteReply" "") - 1) }} ,
+     {(EVENT_SAFESTATE), {((char_u *)"SafeState"),  (sizeof("SafeState" "") - 1) }} ,
+     {(EVENT_SAFESTATEAGAIN), {((char_u *)"SafeStateAgain"),  (sizeof("SafeStateAgain" "") - 1) }} ,
+     {(EVENT_SESSIONLOADPOST), {((char_u *)"SessionLoadPost"),  (sizeof("SessionLoadPost" "") - 1) }} ,
+     {(EVENT_SESSIONLOADPRE), {((char_u *)"SessionLoadPre"),  (sizeof("SessionLoadPre" "") - 1) }} ,
+     {(EVENT_SESSIONWRITEPOST), {((char_u *)"SessionWritePost"),  (sizeof("SessionWritePost" "") - 1) }} ,
+     {(EVENT_SHELLCMDPOST), {((char_u *)"ShellCmdPost"),  (sizeof("ShellCmdPost" "") - 1) }} ,
+     {(-EVENT_SHELLFILTERPOST), {((char_u *)"ShellFilterPost"),  (sizeof("ShellFilterPost" "") - 1) }} ,
+     {(EVENT_SIGUSR1), {((char_u *)"SigUSR1"),  (sizeof("SigUSR1" "") - 1) }} ,
+     {(EVENT_SOURCECMD), {((char_u *)"SourceCmd"),  (sizeof("SourceCmd" "") - 1) }} ,
+     {(EVENT_SOURCEPOST), {((char_u *)"SourcePost"),  (sizeof("SourcePost" "") - 1) }} ,
+     {(EVENT_SOURCEPRE), {((char_u *)"SourcePre"),  (sizeof("SourcePre" "") - 1) }} ,
+     {(EVENT_SPELLFILEMISSING), {((char_u *)"SpellFileMissing"),  (sizeof("SpellFileMissing" "") - 1) }} ,
+     {(EVENT_STDINREADPOST), {((char_u *)"StdinReadPost"),  (sizeof("StdinReadPost" "") - 1) }} ,
+     {(EVENT_STDINREADPRE), {((char_u *)"StdinReadPre"),  (sizeof("StdinReadPre" "") - 1) }} ,
+     {(EVENT_SWAPEXISTS), {((char_u *)"SwapExists"),  (sizeof("SwapExists" "") - 1) }} ,
+     {(EVENT_SYNTAX), {((char_u *)"Syntax"),  (sizeof("Syntax" "") - 1) }} ,
+     {(EVENT_TABCLOSED), {((char_u *)"TabClosed"),  (sizeof("TabClosed" "") - 1) }} ,
+     {(EVENT_TABCLOSEDPRE), {((char_u *)"TabClosedPre"),  (sizeof("TabClosedPre" "") - 1) }} ,
+     {(EVENT_TABENTER), {((char_u *)"TabEnter"),  (sizeof("TabEnter" "") - 1) }} ,
+     {(EVENT_TABLEAVE), {((char_u *)"TabLeave"),  (sizeof("TabLeave" "") - 1) }} ,
+     {(EVENT_TABNEW), {((char_u *)"TabNew"),  (sizeof("TabNew" "") - 1) }} ,
+     {(EVENT_TERMCHANGED), {((char_u *)"TermChanged"),  (sizeof("TermChanged" "") - 1) }} ,
+     {(EVENT_TERMINALOPEN), {((char_u *)"TerminalOpen"),  (sizeof("TerminalOpen" "") - 1) }} ,
+     {(EVENT_TERMINALWINOPEN), {((char_u *)"TerminalWinOpen"),  (sizeof("TerminalWinOpen" "") - 1) }} ,
+     {(EVENT_TERMRESPONSE), {((char_u *)"TermResponse"),  (sizeof("TermResponse" "") - 1) }} ,
+     {(EVENT_TERMRESPONSEALL), {((char_u *)"TermResponseAll"),  (sizeof("TermResponseAll" "") - 1) }} ,
+     {(-EVENT_TEXTCHANGED), {((char_u *)"TextChanged"),  (sizeof("TextChanged" "") - 1) }} ,
+     {(-EVENT_TEXTCHANGEDI), {((char_u *)"TextChangedI"),  (sizeof("TextChangedI" "") - 1) }} ,
+     {(-EVENT_TEXTCHANGEDP), {((char_u *)"TextChangedP"),  (sizeof("TextChangedP" "") - 1) }} ,
+     {(-EVENT_TEXTCHANGEDT), {((char_u *)"TextChangedT"),  (sizeof("TextChangedT" "") - 1) }} ,
+     {(-EVENT_TEXTPUTPOST), {((char_u *)"TextPutPost"),  (sizeof("TextPutPost" "") - 1) }} ,
+     {(-EVENT_TEXTPUTPRE), {((char_u *)"TextPutPre"),  (sizeof("TextPutPre" "") - 1) }} ,
+     {(-EVENT_TEXTYANKPOST), {((char_u *)"TextYankPost"),  (sizeof("TextYankPost" "") - 1) }} ,
+     {(EVENT_USER), {((char_u *)"User"),  (sizeof("User" "") - 1) }} ,
+     {(EVENT_VIMENTER), {((char_u *)"VimEnter"),  (sizeof("VimEnter" "") - 1) }} ,
+     {(EVENT_VIMLEAVE), {((char_u *)"VimLeave"),  (sizeof("VimLeave" "") - 1) }} ,
+     {(EVENT_VIMLEAVEPRE), {((char_u *)"VimLeavePre"),  (sizeof("VimLeavePre" "") - 1) }} ,
+     {(EVENT_VIMRESIZED), {((char_u *)"VimResized"),  (sizeof("VimResized" "") - 1) }} ,
+     {(EVENT_VIMRESUME), {((char_u *)"VimResume"),  (sizeof("VimResume" "") - 1) }} ,
+     {(EVENT_VIMSUSPEND), {((char_u *)"VimSuspend"),  (sizeof("VimSuspend" "") - 1) }} ,
+     {(-EVENT_WINCLOSED), {((char_u *)"WinClosed"),  (sizeof("WinClosed" "") - 1) }} ,
+     {(-EVENT_WINENTER), {((char_u *)"WinEnter"),  (sizeof("WinEnter" "") - 1) }} ,
+     {(-EVENT_WINLEAVE), {((char_u *)"WinLeave"),  (sizeof("WinLeave" "") - 1) }} ,
+     {(EVENT_WINNEW), {((char_u *)"WinNew"),  (sizeof("WinNew" "") - 1) }} ,
+     {(EVENT_WINNEWPRE), {((char_u *)"WinNewPre"),  (sizeof("WinNewPre" "") - 1) }} ,
+     {(-EVENT_WINRESIZED), {((char_u *)"WinResized"),  (sizeof("WinResized" "") - 1) }} ,
+     {(-EVENT_WINSCROLLED), {((char_u *)"WinScrolled"),  (sizeof("WinScrolled" "") - 1) }} ,
 };
 
 static AutoPat *first_autopat[NUM_EVENTS] = { NULL };
 static AutoPat *last_autopat[NUM_EVENTS] = { NULL };
 
-#define AUGROUP_DEFAULT    (-1)
-#define AUGROUP_ERROR      (-2)
-#define AUGROUP_ALL        (-3)
-
 struct AutoPatCmd_S
 {
     AutoPat     *curpat;
@@ -11686,13 +10006,10 @@
 
 static AutoPatCmd_T *active_apc_list = NULL;
 
-#define FOR_ALL_AUTOCMD_PATTERNS(event, ap)     for ((ap) = first_autopat[(int)(event)]; (ap) != NULL; (ap) = (ap)->next)
-
 static garray_T augroups = {0, 0, sizeof(char_u *), 10, NULL};
-#define AUGROUP_NAME(i) (((char_u **)augroups.ga_data)[i])
 static char_u *deleted_augroup = NULL;
 
-static int current_augroup = AUGROUP_DEFAULT;
+static int current_augroup =  (-1) ;
 
 static int au_need_clean = FALSE;
 
@@ -11743,20 +10060,20 @@
     {
         string_T    *event_name;
 
-        if (ap->group != AUGROUP_DEFAULT)
+        if (ap->group !=  (-1) )
         {
-            if (AUGROUP_NAME(ap->group) == NULL)
+            if ( (((char_u **)augroups.ga_data)[ap->group])  == NULL)
             {
-                msg_puts_attr((char *)get_deleted_augroup(), HL_ATTR(HLF_E));
+                msg_puts_attr((char *)get_deleted_augroup(),  highlight_attr[(int)(HLF_E)] );
             }
             else
             {
-                msg_puts_attr((char *)AUGROUP_NAME(ap->group), HL_ATTR(HLF_T));
+                msg_puts_attr((char *) (((char_u **)augroups.ga_data)[ap->group]) ,  highlight_attr[(int)(HLF_T)] );
             }
             msg_puts("  ");
         }
         event_name = event_nr2name(event);
-        msg_puts_attr((char *)event_name->string, HL_ATTR(HLF_T));
+        msg_puts_attr((char *)event_name->string,  highlight_attr[(int)(HLF_T)] );
         last_event = event;
         last_group = ap->group;
         msg_putchar('\n');
@@ -11812,7 +10129,8 @@
     static void
 au_remove_pat(AutoPat *ap)
 {
-    VIM_CLEAR(ap->pat);
+     vim_free(ap->pat);
+     (ap->pat) = NULL;
     ap->buflocal_nr = -1;
     au_need_clean = TRUE;
 }
@@ -11824,14 +10142,16 @@
 
     for (ac = ap->cmds; ac != NULL; ac = ac->next)
     {
-        VIM_CLEAR(ac->cmd);
+         vim_free(ac->cmd);
+         (ac->cmd) = NULL;
     }
     au_need_clean = TRUE;
 }
 
 static void au_del_cmd(AutoCmd *ac)
 {
-    VIM_CLEAR(ac->cmd);
+     vim_free(ac->cmd);
+     (ac->cmd) = NULL;
     au_need_clean = TRUE;
 }
 
@@ -11921,7 +10241,8 @@
 
     for (event = (event_T)0; (int)event < NUM_EVENTS; event = (event_T)((int)event + 1))
     {
-        FOR_ALL_AUTOCMD_PATTERNS(event, ap)
+         for ((ap) = first_autopat[(int)(event)]; (ap) != NULL; (ap) = (ap)->next) 
+         {
             if (ap->buflocal_nr == buf->b_fnum)
             {
                 au_remove_pat(ap);
@@ -11935,6 +10256,7 @@
                     verbose_leave();
                 }
             }
+         }
     }
     au_cleanup();
 }
@@ -11945,27 +10267,27 @@
     int         i;
 
     i = au_find_group(name);
-    if (i != AUGROUP_ERROR)
+    if (i !=  (-2) )
     {
         return i;
     }
 
     for (i = 0; i < augroups.ga_len; ++i)
     {
-        if (AUGROUP_NAME(i) == NULL)
+        if ( (((char_u **)augroups.ga_data)[i])  == NULL)
         {
             break;
         }
     }
     if (i == augroups.ga_len && ga_grow(&augroups, 1) == FAIL)
     {
-        return AUGROUP_ERROR;
+        return  (-2) ;
     }
 
-    AUGROUP_NAME(i) = vim_strsave(name);
-    if (AUGROUP_NAME(i) == NULL)
+     (((char_u **)augroups.ga_data)[i])  = vim_strsave(name);
+    if ( (((char_u **)augroups.ga_data)[i])  == NULL)
     {
-        return AUGROUP_ERROR;
+        return  (-2) ;
     }
     if (i == augroups.ga_len)
     {
@@ -11984,7 +10306,7 @@
     int         in_use = FALSE;
 
     i = au_find_group(name);
-    if (i == AUGROUP_ERROR)
+    if (i ==  (-2) )
     {
         semsg(_(e_no_such_group_str), name);
         return;
@@ -11997,7 +10319,8 @@
 
     for (event = (event_T)0; (int)event < NUM_EVENTS; event = (event_T)((int)event + 1))
     {
-        FOR_ALL_AUTOCMD_PATTERNS(event, ap)
+         for ((ap) = first_autopat[(int)(event)]; (ap) != NULL; (ap) = (ap)->next) 
+         {
             if (ap->group == i && ap->pat != NULL)
             {
                 give_warning((char_u *)_("W19: Deleting augroup that is still in use"), TRUE);
@@ -12005,15 +10328,16 @@
                 event = NUM_EVENTS;
                 break;
             }
+         }
     }
-    vim_free(AUGROUP_NAME(i));
+    vim_free( (((char_u **)augroups.ga_data)[i]) );
     if (in_use)
     {
-        AUGROUP_NAME(i) = get_deleted_augroup();
+         (((char_u **)augroups.ga_data)[i])  = get_deleted_augroup();
     }
     else
     {
-        AUGROUP_NAME(i) = NULL;
+         (((char_u **)augroups.ga_data)[i])  = NULL;
     }
 }
 
@@ -12024,18 +10348,18 @@
 
     for (i = 0; i < augroups.ga_len; ++i)
     {
-        if (AUGROUP_NAME(i) != NULL && AUGROUP_NAME(i) != get_deleted_augroup() && STRCMP(AUGROUP_NAME(i), name) == 0)
+        if ( (((char_u **)augroups.ga_data)[i])  != NULL &&  (((char_u **)augroups.ga_data)[i])  != get_deleted_augroup() &&  strcmp((char *)( (((char_u **)augroups.ga_data)[i]) ), (char *)(name))  == 0)
         {
             return i;
         }
     }
-    return AUGROUP_ERROR;
+    return  (-2) ;
 }
 
     int
 au_has_group(char_u *name)
 {
-    return au_find_group(name) != AUGROUP_ERROR;
+    return au_find_group(name) !=  (-2) ;
 }
 
     void
@@ -12054,14 +10378,14 @@
             au_del_group(arg);
         }
     }
-    else if (STRICMP(arg, "end") == 0)
+    else if ( strcasecmp((char *)(arg), (char *)("end"))  == 0)
     {
-        current_augroup = AUGROUP_DEFAULT;
+        current_augroup =  (-1) ;
     }
     else if (*arg)
     {
         i = au_new_group(arg);
-        if (i != AUGROUP_ERROR)
+        if (i !=  (-2) )
         {
             current_augroup = i;
         }
@@ -12071,9 +10395,9 @@
         msg_start();
         for (i = 0; i < augroups.ga_len; ++i)
         {
-            if (AUGROUP_NAME(i) != NULL)
+            if ( (((char_u **)augroups.ga_data)[i])  != NULL)
             {
-                msg_puts((char *)AUGROUP_NAME(i));
+                msg_puts((char *) (((char_u **)augroups.ga_data)[i]) );
                 msg_puts("  ");
             }
         }
@@ -12085,7 +10409,7 @@
     void
 autocmd_init(void)
 {
-    CLEAR_FIELD(aucmd_win);
+      memset((&(aucmd_win)), (0), (sizeof(aucmd_win)))  ;
 }
 
     int
@@ -12110,7 +10434,7 @@
     static keyvalue_T *bufnewfile = &event_tab[BUFNEWFILE_INDEX];
     static keyvalue_T *bufread = &event_tab[BUFREAD_INDEX];
 
-    for (p = start; *p && !VIM_ISWHITE(*p) && *p != ',' && *p != '|'; ++p)
+    for (p = start; *p && ! ((*p) == ' ' || (*p) == '\t')  && *p != ',' && *p != '|'; ++p)
     {
         ;
     }
@@ -12148,7 +10472,7 @@
     enum {CACHE_SIZE = 12};
     static int cache_tab[CACHE_SIZE];
     static int cache_last_index = -1;
-    static string_T unknown = STR_LITERAL_INIT("Unknown");
+    static string_T unknown =  {(char_u *)("Unknown"),  (sizeof("Unknown" "") - 1) } ;
 
     if (cache_last_index < 0)
     {
@@ -12209,7 +10533,7 @@
 
     if (*arg == '*')
     {
-        if (arg[1] && !VIM_ISWHITE(arg[1]))
+        if (arg[1] && ! ((arg[1]) == ' ' || (arg[1]) == '\t') )
         {
             semsg(_(e_illegal_character_after_star_str), arg);
             return NULL;
@@ -12218,7 +10542,7 @@
     }
     else
     {
-        for (pat = arg; *pat && *pat != '|' && !VIM_ISWHITE(*pat); pat = p)
+        for (pat = arg; *pat && *pat != '|' && ! ((*pat) == ' ' || (*pat) == '\t') ; pat = p)
         {
             if ((int)event_name2nr(pat, &p) >= NUM_EVENTS)
             {
@@ -12251,8 +10575,8 @@
 
     ++emsg_off;
     group = au_get_grouparg(&arg);
-    pat = group == AUGROUP_ERROR
-                          ? NULL : find_end_event(arg, group != AUGROUP_ALL);
+    pat = group ==  (-2) 
+                          ? NULL : find_end_event(arg, group !=  (-3) );
     --emsg_off;
     if (pat == NULL)
     {
@@ -12266,7 +10590,7 @@
     }
 
     cmd = pat;
-    while (*cmd != NUL && (!VIM_ISWHITE(*cmd) || (cmd > pat && *(cmd - 1) == '\\')))
+    while (*cmd != NUL && (! ((*cmd) == ' ' || (*cmd) == '\t')  || (cmd > pat && *(cmd - 1) == '\\')))
     {
         ++cmd;
     }
@@ -12274,15 +10598,15 @@
 
     for (int i = 0; i < 2; ++i)
     {
-        if (STRNCMP(cmd, "++once", 6) == 0 && VIM_ISWHITE(cmd[6]))
+        if ( strncmp((char *)(cmd), (char *)("++once"), (6))  == 0 &&  ((cmd[6]) == ' ' || (cmd[6]) == '\t') )
         {
             cmd = skipwhite(cmd + 6);
         }
-        if (STRNCMP(cmd, "++nested", 8) == 0 && VIM_ISWHITE(cmd[8]))
+        if ( strncmp((char *)(cmd), (char *)("++nested"), (8))  == 0 &&  ((cmd[8]) == ' ' || (cmd[8]) == '\t') )
         {
             cmd = skipwhite(cmd + 8);
         }
-        if (!in_vim9script() && STRNCMP(cmd, "nested", 6) == 0 && VIM_ISWHITE(cmd[6]))
+        if (!in_vim9script() &&  strncmp((char *)(cmd), (char *)("nested"), (6))  == 0 &&  ((cmd[6]) == ' ' || (cmd[6]) == '\t') )
         {
             cmd = skipwhite(cmd + 6);
         }
@@ -12299,7 +10623,7 @@
     {
         int unignore = *ei == '-';
         ei += unignore;
-        if (STRNICMP(ei, "all", 3) == 0 && (ei[3] == NUL || ei[3] == ','))
+        if ( strncasecmp((char *)(ei), (char *)("all"), (3))  == 0 && (ei[3] == NUL || ei[3] == ','))
         {
             ignored = ei == p_ei || (event_tab[event].key <= 0);
             ei += 3 + (ei[3] == ',');
@@ -12327,7 +10651,7 @@
 
     while (*ei)
     {
-        if (STRNICMP(ei, "all", 3) == 0 && (ei[3] == NUL || ei[3] == ','))
+        if ( strncasecmp((char *)(ei), (char *)("all"), (3))  == 0 && (ei[3] == NUL || ei[3] == ','))
         {
             ei += 3 + (ei[3] == ',');
         }
@@ -12365,7 +10689,7 @@
     {
         eap->nextcmd = arg + 1;
         arg = (char_u *)"";
-        group = AUGROUP_ALL;
+        group =  (-3) ;
     }
     else
     {
@@ -12376,7 +10700,7 @@
         }
     }
 
-    pat = find_end_event(arg, group != AUGROUP_ALL);
+    pat = find_end_event(arg, group !=  (-3) );
     if (pat == NULL)
     {
         return;
@@ -12392,7 +10716,7 @@
     else
     {
         cmd = pat;
-        while (*cmd && (!VIM_ISWHITE(*cmd) || cmd[-1] == '\\'))
+        while (*cmd && (! ((*cmd) == ' ' || (*cmd) == '\t')  || cmd[-1] == '\\'))
         {
             cmd++;
         }
@@ -12418,7 +10742,7 @@
                 continue;
             }
 
-            if (STRNCMP(cmd, "++once", 6) == 0 && VIM_ISWHITE(cmd[6]))
+            if ( strncmp((char *)(cmd), (char *)("++once"), (6))  == 0 &&  ((cmd[6]) == ' ' || (cmd[6]) == '\t') )
             {
                 if (once)
                 {
@@ -12429,7 +10753,7 @@
                 cmd = skipwhite(cmd + 6);
             }
 
-            if ((STRNCMP(cmd, "++nested", 8) == 0 && VIM_ISWHITE(cmd[8])))
+            if (( strncmp((char *)(cmd), (char *)("++nested"), (8))  == 0 &&  ((cmd[8]) == ' ' || (cmd[8]) == '\t') ))
             {
                 if (nested)
                 {
@@ -12440,7 +10764,7 @@
                 cmd = skipwhite(cmd + 8);
             }
 
-            if (STRNCMP(cmd, "nested", 6) == 0 && VIM_ISWHITE(cmd[6]))
+            if ( strncmp((char *)(cmd), (char *)("nested"), (6))  == 0 &&  ((cmd[6]) == ' ' || (cmd[6]) == '\t') )
             {
                 if (in_vim9script())
                 {
@@ -12479,7 +10803,7 @@
     }
 
     last_event = (event_T)-1;
-    last_group = AUGROUP_ERROR;
+    last_group =  (-2) ;
     if (*arg == '*' || *arg == NUL || *arg == '|')
     {
         if (*cmd != NUL)
@@ -12499,7 +10823,7 @@
     }
     else
     {
-        while (*arg && *arg != '|' && !VIM_ISWHITE(*arg))
+        while (*arg && *arg != '|' && ! ((*arg) == ' ' || (*arg) == '\t') )
         {
             if (do_autocmd_event(event_name2nr(arg, &arg), pat, once, nested, cmd, forceit, group, flags) == FAIL)
             {
@@ -12523,26 +10847,26 @@
     char_u      *group_name;
     char_u      *p;
     char_u      *arg = *argp;
-    int         group = AUGROUP_ALL;
+    int         group =  (-3) ;
 
-    for (p = arg; *p && !VIM_ISWHITE(*p) && *p != '|'; ++p)
+    for (p = arg; *p && ! ((*p) == ' ' || (*p) == '\t')  && *p != '|'; ++p)
     {
         ;
     }
     if (p <= arg)
     {
-        return AUGROUP_ALL;
+        return  (-3) ;
     }
 
     group_name = vim_strnsave(arg, p - arg);
     if (group_name == NULL)
     {
-        return AUGROUP_ERROR;
+        return  (-2) ;
     }
     group = au_find_group(group_name);
-    if (group == AUGROUP_ERROR)
+    if (group ==  (-2) )
     {
-        group = AUGROUP_ALL;
+        group =  (-3) ;
     }
     else
     {
@@ -12568,7 +10892,7 @@
     int         buflocal_nr;
     char_u      buflocal_pat[25];
 
-    if (group == AUGROUP_ALL)
+    if (group ==  (-3) )
     {
         findgroup = current_augroup;
     }
@@ -12576,11 +10900,11 @@
     {
         findgroup = group;
     }
-    allgroups = (group == AUGROUP_ALL && !forceit && *cmd == NUL);
+    allgroups = (group ==  (-3)  && !forceit && *cmd == NUL);
 
     if (*pat == NUL)
     {
-        FOR_ALL_AUTOCMD_PATTERNS(event, ap)
+         for ((ap) = first_autopat[(int)(event)]; (ap) != NULL; (ap) = (ap)->next) 
         {
             if (forceit)
             {
@@ -12589,7 +10913,7 @@
                     au_remove_pat(ap);
                 }
             }
-            else if (group == AUGROUP_ALL || ap->group == group)
+            else if (group ==  (-3)  || ap->group == group)
             {
                 show_autocmd(ap, event);
             }
@@ -12619,7 +10943,7 @@
         is_buflocal = FALSE;
         buflocal_nr = 0;
 
-        if (patlen >= 8 && STRNCMP(pat, "<buffer", 7) == 0 && pat[patlen - 1] == '>')
+        if (patlen >= 8 &&  strncmp((char *)(pat), (char *)("<buffer"), (7))  == 0 && pat[patlen - 1] == '>')
         {
             is_buflocal = TRUE;
             if (patlen == 8)
@@ -12628,7 +10952,7 @@
             }
             else if (patlen > 9 && pat[7] == '=')
             {
-                if (patlen == 13 && STRNICMP(pat, "<buffer=abuf>", 13) == 0)
+                if (patlen == 13 &&  strncasecmp((char *)(pat), (char *)("<buffer=abuf>"), (13))  == 0)
                 {
                     buflocal_nr = autocmd_bufnr;
                 }
@@ -12643,7 +10967,7 @@
         {
             sprintf((char *)buflocal_pat, "<buffer=%d>", buflocal_nr);
             pat = buflocal_pat;
-            patlen = (int)STRLEN(buflocal_pat);
+            patlen = (int) strlen((char *)(buflocal_pat)) ;
         }
 
         if (!forceit && *cmd != NUL && last_autopat[(int)event] != NULL)
@@ -12658,7 +10982,7 @@
         {
             if (ap->pat != NULL)
             {
-                if ((allgroups || ap->group == findgroup) && ap->patlen == patlen && STRNCMP(pat, ap->pat, patlen) == 0)
+                if ((allgroups || ap->group == findgroup) && ap->patlen == patlen &&  strncmp((char *)(pat), (char *)(ap->pat), (patlen))  == 0)
                 {
                     if (forceit)
                     {
@@ -12694,7 +11018,7 @@
                     return FAIL;
                 }
 
-                ap = ALLOC_ONE(AutoPat);
+                ap =  (AutoPat *)alloc(sizeof(AutoPat)) ;
                 if (ap == NULL)
                 {
                     return FAIL;
@@ -12711,7 +11035,7 @@
                 {
                     tabpage_T *save_curtab = curtab;
                     tabpage_T *tp;
-                    FOR_ALL_TABPAGES(tp)
+                     for ((tp) = first_tabpage; (tp) != NULL; (tp) = (tp)->tp_next) 
                     {
                         unuse_tabpage(curtab);
                         use_tabpage(tp);
@@ -12748,7 +11072,7 @@
                 *prev_ap = ap;
                 last_autopat[(int)event] = ap;
                 ap->next = NULL;
-                if (group == AUGROUP_ALL)
+                if (group ==  (-3) )
                 {
                     ap->group = current_augroup;
                 }
@@ -12763,7 +11087,7 @@
             {
                 prev_ac = &ac->next;
             }
-            ac = ALLOC_ONE(AutoCmd);
+            ac =  (AutoCmd *)alloc(sizeof(AutoCmd)) ;
             if (ac == NULL)
             {
                 return FAIL;
@@ -12815,7 +11139,7 @@
         return FAIL;
     }
 
-    fname = find_end_event(arg, group != AUGROUP_ALL);
+    fname = find_end_event(arg, group !=  (-3) );
     if (fname == NULL)
     {
         return FAIL;
@@ -12823,7 +11147,7 @@
 
     fname = skipwhite(fname);
 
-    while (*arg && !ends_excmd(*arg) && !VIM_ISWHITE(*arg))
+    while (*arg && !ends_excmd(*arg) && ! ((*arg) == ' ' || (*arg) == '\t') )
     {
         if (apply_autocmds_group(event_name2nr(arg, &arg), fname, NULL, TRUE, group, curbuf, NULL))
         {
@@ -12854,7 +11178,7 @@
     int         call_do_modelines = check_nomodeline(&arg);
     int         did_aucmd;
 
-    FOR_ALL_BUFFERS(buf)
+     for ((buf) = firstbuf; (buf) != NULL; (buf) = (buf)->b_next) 
     {
         if (buf->b_ml.ml_mfp == NULL || buf == curbuf)
         {
@@ -12899,7 +11223,7 @@
     int
 check_nomodeline(char_u **argp)
 {
-    if (STRNCMP(*argp, "<nomodeline>", 12) == 0)
+    if ( strncmp((char *)(*argp), (char *)("<nomodeline>"), (12))  == 0)
     {
         *argp = skipwhite(*argp + 12);
         return FALSE;
@@ -12920,11 +11244,13 @@
     }
     else
     {
-        FOR_ALL_WINDOWS(win)
+         for ((win) = firstwin; (win) != NULL; (win) = (win)->w_next) 
+         {
             if (win->w_buffer == buf)
             {
                 break;
             }
+         }
     }
 
     win_T *auc_win = NULL;
@@ -13011,7 +11337,9 @@
             tabpage_T   *tp;
             win_T       *wp;
 
-            FOR_ALL_TAB_WINDOWS(tp, wp)
+             for ((tp) = first_tabpage; (tp) != NULL; (tp) = (tp)->tp_next)
+             {
+                 for ((wp) = ((tp) == curtab)            ? firstwin : (tp)->tp_firstwin; (wp); (wp) = (wp)->w_next)
             {
                 if (wp == awp)
                 {
@@ -13023,6 +11351,7 @@
                     goto win_found;
                 }
             }
+             }
         }
 win_found:
         --curbuf->b_nwindows;
@@ -13103,13 +11432,13 @@
     int
 apply_autocmds(event_T     event, char_u      *fname, char_u      *fname_io, int         force, buf_T       *buf)
 {
-    return apply_autocmds_group(event, fname, fname_io, force, AUGROUP_ALL, buf, NULL);
+    return apply_autocmds_group(event, fname, fname_io, force,  (-3) , buf, NULL);
 }
 
     int
 apply_autocmds_exarg(event_T     event, char_u      *fname, char_u      *fname_io, int         force, buf_T       *buf, exarg_T     *eap)
 {
-    return apply_autocmds_group(event, fname, fname_io, force, AUGROUP_ALL, buf, eap);
+    return apply_autocmds_group(event, fname, fname_io, force,  (-3) , buf, eap);
 }
 
     int
@@ -13117,7 +11446,7 @@
 {
     int         did_cmd;
 
-    did_cmd = apply_autocmds_group(event, fname, fname_io, force, AUGROUP_ALL, buf, NULL);
+    did_cmd = apply_autocmds_group(event, fname, fname_io, force,  (-3) , buf, NULL);
     if (did_cmd)
     {
         *retval = FAIL;
@@ -13128,7 +11457,7 @@
     static int
 has_cursorhold(void)
 {
-    return (first_autopat[(int)(get_real_state() == MODE_NORMAL_BUSY ? EVENT_CURSORHOLD : EVENT_CURSORHOLDI)] != NULL);
+    return (first_autopat[(int)(get_real_state() ==  (0x1000 | MODE_NORMAL)  ? EVENT_CURSORHOLD : EVENT_CURSORHOLDI)] != NULL);
 }
 
     int
@@ -13139,7 +11468,7 @@
     if (!did_cursorhold && has_cursorhold() && reg_recording == 0 && typebuf.tb_len == 0 && !ins_compl_active())
     {
         state = get_real_state();
-        if (state == MODE_NORMAL_BUSY || (state & MODE_INSERT) != 0)
+        if (state ==  (0x1000 | MODE_NORMAL)  || (state & MODE_INSERT) != 0)
         {
             return TRUE;
         }
@@ -13195,7 +11524,6 @@
     return (first_autopat[(int)EVENT_INSERTCHARPRE] != NULL);
 }
 
-
     int
 has_cmdundefined(void)
 {
@@ -13203,7 +11531,7 @@
 }
 
     static int
-apply_autocmds_group(event_T     event, char_u      *fname, char_u      *fname_io, int         force, int         group, buf_T       *buf, exarg_T     *eap UNUSED)
+apply_autocmds_group(event_T     event, char_u      *fname, char_u      *fname_io, int         force, int         group, buf_T       *buf, exarg_T     *eap  __attribute__((unused)) )
 {
     char_u      *sfname = NULL;
     char_u      *tail;
@@ -13224,7 +11552,6 @@
     int         did_save_redobuff = FALSE;
     save_redo_T save_redo;
     int         save_KeyTyped = KeyTyped;
-    ESTACK_CHECK_DECLARATION;
 
     if (event == NUM_EVENTS || first_autopat[(int)event] == NULL || autocmd_blocked > 0)
     {
@@ -13249,7 +11576,7 @@
     int win_ignore = FALSE;
     if (buf == curbuf && event_tab[event].key <= 0)
     {
-        win_ignore = event_ignored(event, curwin->w_p_eiw);
+        win_ignore = event_ignored(event, curwin-> w_onebuf_opt.wo_eiw );
     }
     else if (buf != NULL && event_tab[event].key <= 0 && buf->b_nwindows > 0)
     {
@@ -13257,12 +11584,17 @@
         win_T *wp;
 
         win_ignore = TRUE;
-        FOR_ALL_TAB_WINDOWS(tp, wp)
-            if (wp->w_buffer == buf && !event_ignored(event, wp->w_p_eiw))
+         for ((tp) = first_tabpage; (tp) != NULL; (tp) = (tp)->tp_next)
+         {
+             for ((wp) = ((tp) == curtab)            ? firstwin : (tp)->tp_firstwin; (wp); (wp) = (wp)->w_next)
+             {
+            if (wp->w_buffer == buf && !event_ignored(event, wp-> w_onebuf_opt.wo_eiw ))
             {
                 win_ignore = FALSE;
                 break;
             }
+             }
+         }
     }
     if (win_ignore)
     {
@@ -13384,7 +11716,6 @@
     ++RedrawingDisabled;
 
     estack_push(ETYPE_AUCMD, NULL, 0);
-    ESTACK_CHECK_SETUP;
 
     save_current_sctx = current_sctx;
 
@@ -13410,7 +11741,7 @@
 
     tail = gettail(fname);
 
-    CLEAR_FIELD(patcmd);
+      memset((&(patcmd)), (0), (sizeof(patcmd)))  ;
     patcmd.curpat = first_autopat[(int)event];
     patcmd.group = group;
     patcmd.fname = fname;
@@ -13467,8 +11798,7 @@
     autocmd_busy = save_autocmd_busy;
     filechangeshell_busy = FALSE;
     autocmd_nested = save_autocmd_nested;
-    vim_free(SOURCING_NAME);
-    ESTACK_CHECK_NOW;
+    vim_free( (((estack_T *)exestack.ga_data)[exestack.ga_len - 1].es_name) );
     estack_pop();
     vim_free(autocmd_fname);
     autocmd_fname = save_autocmd_fname;
@@ -13557,14 +11887,15 @@
 
     entry = ((estack_T *)exestack.ga_data) + exestack.ga_len - 1;
 
-    VIM_CLEAR(entry->es_name);
+     vim_free(entry->es_name);
+     (entry->es_name) = NULL;
     entry->es_info.aucmd = NULL;
 
     for (ap = apc->curpat; ap != NULL && !got_int; ap = ap->next)
     {
         apc->curpat = NULL;
 
-        if (ap->pat != NULL && ap->cmds != NULL && (apc->group == AUGROUP_ALL || apc->group == ap->group))
+        if (ap->pat != NULL && ap->cmds != NULL && (apc->group ==  (-3)  || apc->group == ap->group))
         {
             if (ap->buflocal_nr == 0 ? (match_file_pat(NULL, &ap->reg_prog, apc->fname, apc->sfname, apc->tail, ap->allow_dirs)) : ap->buflocal_nr == apc->arg_bufnr)
             {
@@ -13575,7 +11906,7 @@
 
                 event_name = event_nr2name(apc->event);
                 fmt = _("%s Autocommands for \"%s\"");
-                namep = alloc(STRLEN(fmt) + event_name->length + ap->patlen + 1);
+                namep = alloc( strlen((char *)(fmt))  + event_name->length + ap->patlen + 1);
                 if (namep != NULL)
                 {
                     sprintf((char *)namep, fmt, (char *)event_name->string, (char *)ap->pat);
@@ -13612,7 +11943,7 @@
 }
 
     char_u *
-getnextac(int c UNUSED, void *cookie, int indent UNUSED, getline_opt_T options UNUSED)
+getnextac(int c  __attribute__((unused)) , void *cookie, int indent  __attribute__((unused)) , getline_opt_T options  __attribute__((unused)) )
 {
     AutoPatCmd_T    *acp = (AutoPatCmd_T *)cookie;
     char_u          *retval;
@@ -13702,12 +12033,14 @@
         return FALSE;
     }
 
-    FOR_ALL_AUTOCMD_PATTERNS(event, ap)
+     for ((ap) = first_autopat[(int)(event)]; (ap) != NULL; (ap) = (ap)->next) 
+     {
         if (ap->pat != NULL && ap->cmds != NULL && (ap->buflocal_nr == 0 ? match_file_pat(NULL, &ap->reg_prog, fname, sfname, tail, ap->allow_dirs) : buf != NULL && ap->buflocal_nr == buf->b_fnum))
         {
             retval = TRUE;
             break;
         }
+     }
 
     vim_free(fname);
 
@@ -13715,7 +12048,7 @@
 }
 
     char_u *
-get_augroup_name(expand_T *xp UNUSED, int idx)
+get_augroup_name(expand_T *xp  __attribute__((unused)) , int idx)
 {
     if (idx == augroups.ga_len)
     {
@@ -13725,11 +12058,11 @@
     {
         return NULL;
     }
-    if (AUGROUP_NAME(idx) == NULL || AUGROUP_NAME(idx) == get_deleted_augroup())
+    if ( (((char_u **)augroups.ga_data)[idx])  == NULL ||  (((char_u **)augroups.ga_data)[idx])  == get_deleted_augroup())
     {
         return (char_u *)"";
     }
-    return AUGROUP_NAME(idx);
+    return  (((char_u **)augroups.ga_data)[idx]) ;
 }
 
 static int include_groups = FALSE;
@@ -13743,17 +12076,17 @@
     include_groups = FALSE;
     p = arg;
     group = au_get_grouparg(&arg);
-    if (group == AUGROUP_ERROR)
+    if (group ==  (-2) )
     {
         return NULL;
     }
-    if (*arg == NUL && group != AUGROUP_ALL && !VIM_ISWHITE(arg[-1]))
+    if (*arg == NUL && group !=  (-3)  && ! ((arg[-1]) == ' ' || (arg[-1]) == '\t') )
     {
         arg = p;
-        group = AUGROUP_ALL;
+        group =  (-3) ;
     }
 
-    for (p = arg; *p != NUL && !VIM_ISWHITE(*p); ++p)
+    for (p = arg; *p != NUL && ! ((*p) == ' ' || (*p) == '\t') ; ++p)
     {
         if (*p == ',')
         {
@@ -13762,7 +12095,7 @@
     }
     if (*p == NUL)
     {
-        if (group == AUGROUP_ALL)
+        if (group ==  (-3) )
         {
             include_groups = TRUE;
         }
@@ -13772,7 +12105,7 @@
     }
 
     arg = skipwhite(p);
-    while (*arg && (!VIM_ISWHITE(*arg) || arg[-1] == '\\'))
+    while (*arg && (! ((*arg) == ' ' || (*arg) == '\t')  || arg[-1] == '\\'))
     {
         arg++;
     }
@@ -13793,17 +12126,17 @@
 }
 
     char_u *
-get_event_name(expand_T *xp UNUSED, int idx)
+get_event_name(expand_T *xp  __attribute__((unused)) , int idx)
 {
     int i;
 
     if (idx < augroups.ga_len)
     {
-        if (!include_groups || AUGROUP_NAME(idx) == NULL || AUGROUP_NAME(idx) == get_deleted_augroup())
+        if (!include_groups ||  (((char_u **)augroups.ga_data)[idx])  == NULL ||  (((char_u **)augroups.ga_data)[idx])  == get_deleted_augroup())
         {
             return (char_u *)"";
         }
-        return AUGROUP_NAME(idx);
+        return  (((char_u **)augroups.ga_data)[idx]) ;
     }
 
     i = idx - augroups.ga_len;
@@ -13816,7 +12149,7 @@
 }
 
     char_u *
-get_event_name_no_group(expand_T *xp UNUSED, int idx, int win)
+get_event_name_no_group(expand_T *xp  __attribute__((unused)) , int idx, int win)
 {
     if (idx < 0 || idx >= NUM_EVENTS)
     {
@@ -13863,14 +12196,10 @@
 static int      bt_nofileread(buf_T *buf);
 static int      do_buffer_ext(int action, int start, int dir, int count, int flags);
 
-# define dev_T dev_t
-
-#define FOR_ALL_BUFS_FROM_LAST(buf)     for ((buf) = lastbuf; (buf) != NULL; (buf) = (buf)->b_prev)
-
 static int      buf_free_count = 0;
 
 static int      top_file_num = 1;
-static garray_T buf_reuse = GA_EMPTY;
+static garray_T buf_reuse =  {0, 0, 0, 0, NULL} ;
 
     static void
 trigger_undo_ftplugin(buf_T *buf, win_T *win)
@@ -13905,7 +12234,7 @@
     linenr_T    line_count;
 
     line_count = curbuf->b_ml.ml_line_count;
-    retval = readfile(read_stdin ? NULL : curbuf->b_ffname, read_stdin ? NULL : curbuf->b_fname, line_count, (linenr_T)0, (linenr_T)MAXLNUM, eap, flags | READ_BUFFER);
+    retval = readfile(read_stdin ? NULL : curbuf->b_ffname, read_stdin ? NULL : curbuf->b_fname, line_count, (linenr_T)0, (linenr_T) LONG_MAX , eap, flags | READ_BUFFER);
     if (retval == OK)
     {
         while (--line_count >= 0)
@@ -13925,7 +12254,7 @@
 
     if (read_stdin)
     {
-        if (!readonlymode && !BUFEMPTY())
+        if (!readonlymode && ! (curbuf->b_ml.ml_line_count == 1 && *ml_get((linenr_T)1) == NUL) )
         {
             changed();
         }
@@ -13958,11 +12287,13 @@
     if (ml_open(curbuf) == FAIL)
     {
         close_buffer(curwin, curbuf, 0, FALSE, FALSE, FALSE);
-        FOR_ALL_BUFFERS(curbuf)
+         for ((curbuf) = firstbuf; (curbuf) != NULL; (curbuf) = (curbuf)->b_next) 
+         {
             if (curbuf->b_ml.ml_mfp != NULL)
             {
                 break;
             }
+         }
         if (curbuf == NULL)
         {
             emsg(_(e_cannot_allocate_any_buffer_exiting));
@@ -14009,7 +12340,7 @@
         {
             msg_silent = 1;
         }
-        retval = readfile(curbuf->b_ffname, curbuf->b_fname, (linenr_T)0, (linenr_T)0, (linenr_T)MAXLNUM, eap, flags | READ_NEW | (read_fifo ? READ_FIFO : 0));
+        retval = readfile(curbuf->b_ffname, curbuf->b_fname, (linenr_T)0, (linenr_T)0, (linenr_T) LONG_MAX , eap, flags | READ_NEW | (read_fifo ? READ_FIFO : 0));
         if (read_fifo)
         {
             curbuf->b_p_bin = save_bin;
@@ -14029,7 +12360,7 @@
         int     save_bin = curbuf->b_p_bin;
 
         curbuf->b_p_bin = TRUE;
-        retval = readfile(NULL, NULL, (linenr_T)0, (linenr_T)0, (linenr_T)MAXLNUM, NULL, flags | (READ_NEW + READ_STDIN));
+        retval = readfile(NULL, NULL, (linenr_T)0, (linenr_T)0, (linenr_T) LONG_MAX , NULL, flags | (READ_NEW + READ_STDIN));
         curbuf->b_p_bin = save_bin;
         if (retval == OK)
         {
@@ -14058,9 +12389,9 @@
     }
     save_file_ff(curbuf);
 
-    curbuf->b_last_changedtick = CHANGEDTICK(curbuf);
-    curbuf->b_last_changedtick_i = CHANGEDTICK(curbuf);
-    curbuf->b_last_changedtick_pum = CHANGEDTICK(curbuf);
+    curbuf->b_last_changedtick =  ((curbuf)->b_ct_di.di_tv.vval.v_number) ;
+    curbuf->b_last_changedtick_i =  ((curbuf)->b_ct_di.di_tv.vval.v_number) ;
+    curbuf->b_last_changedtick_pum =  ((curbuf)->b_ct_di.di_tv.vval.v_number) ;
 
     if (got_int)
     {
@@ -14121,11 +12452,13 @@
 {
     buf_T       *bp;
 
-    FOR_ALL_BUFS_FROM_LAST(bp)
+     for ((bp) = lastbuf; (bp) != NULL; (bp) = (bp)->b_prev) 
+     {
         if (bp == buf)
         {
             return TRUE;
         }
+     }
     return FALSE;
 }
 
@@ -14146,7 +12479,7 @@
 {
     hashitem_T *hi = hash_find(&buf_hashtab, buf->b_key);
 
-    if (!HASHITEM_EMPTY(hi))
+    if (! ((hi)->hi_key == NULL || (hi)->hi_key == &hash_removed) )
     {
         hash_remove(&buf_hashtab, hi, "close buffer");
     }
@@ -14161,12 +12494,14 @@
     {
         win_T   *wp;
 
-        FOR_ALL_WINDOWS(wp)
+         for ((wp) = firstwin; (wp) != NULL; (wp) = (wp)->w_next) 
+         {
             if (wp->w_buffer == buf)
             {
                 can_unload = FALSE;
                 break;
             }
+         }
     }
     if (can_unload && buf->b_saving)
     {
@@ -14193,9 +12528,7 @@
     int         wipe_buf = (action == DOBUF_WIPE || action == DOBUF_WIPE_REUSE);
     int         del_buf = (action == DOBUF_DEL || wipe_buf);
     int         win_valid = win_valid_any_tab(win);
-    int         closed_popup = win && !win_valid && WIN_IS_POPUP(win);
-
-    CHECK_CURBUF;
+    int         closed_popup = win && !win_valid &&  0 ;
 
     if (buf->b_p_bh[0] == 'd')
     {
@@ -14273,7 +12606,7 @@
 
     if (bt_quickfix(buf) && win_valid && win->w_buffer == buf)
     {
-        win->w_p_wfh = FALSE;
+        win-> w_onebuf_opt.wo_wfh  = FALSE;
     }
 
     hiding_buf = buf->b_nwindows <= 0 || ((win_valid || closed_popup) && win->w_buffer == buf && buf->b_nwindows == 1);
@@ -14305,8 +12638,13 @@
         tabpage_T       *tp;
         win_T           *wp;
 
-        FOR_ALL_TAB_WINDOWS(tp, wp)
+         for ((tp) = first_tabpage; (tp) != NULL; (tp) = (tp)->tp_next)
+         {
+             for ((wp) = ((tp) == curtab)            ? firstwin : (tp)->tp_firstwin; (wp); (wp) = (wp)->w_next)
+             {
             mark_forget_file(wp, buf->b_fnum);
+             }
+         }
 
         if (action == DOBUF_WIPE_REUSE)
         {
@@ -14321,13 +12659,15 @@
         }
         if (buf->b_sfname != buf->b_ffname)
         {
-            VIM_CLEAR(buf->b_sfname);
+             vim_free(buf->b_sfname);
+             (buf->b_sfname) = NULL;
         }
         else
         {
             buf->b_sfname = NULL;
         }
-        VIM_CLEAR(buf->b_ffname);
+         vim_free(buf->b_ffname);
+         (buf->b_ffname) = NULL;
         if (buf->b_prev == NULL)
         {
             firstbuf = buf->b_next;
@@ -14506,7 +12846,8 @@
     uc_clear(&buf->b_ucmds);
     map_clear_mode(buf, MAP_ALL_MODES, TRUE, FALSE);
     map_clear_mode(buf, MAP_ALL_MODES, TRUE, TRUE);
-    VIM_CLEAR(buf->b_start_fenc);
+     vim_free(buf->b_start_fenc);
+     (buf->b_start_fenc) = NULL;
 }
 
     void
@@ -14550,7 +12891,6 @@
     (void)do_buffer_ext(*eap->cmd == 's' ? DOBUF_SPLIT : DOBUF_GOTO, start, dir, count, (eap->forceit ? DOBUF_FORCEIT : 0) | (skip_help_buf ? DOBUF_SKIPHELP : 0));
     if (swap_exists_action == SEA_QUIT && *eap->cmd == 's')
     {
-
         win_close(curwin, TRUE);
         swap_exists_action = save_sea;
         swap_exists_did_quit = TRUE;
@@ -14569,7 +12909,6 @@
 
     if (swap_exists_action == SEA_QUIT)
     {
-
         swap_exists_action = SEA_NONE;
         swap_exists_did_quit = TRUE;
         close_buffer(curwin, curbuf, DOBUF_UNLOAD, FALSE, FALSE, TRUE);
@@ -14599,7 +12938,6 @@
     }
     else if (swap_exists_action == SEA_RECOVER)
     {
-
         msg_scroll = TRUE;
         ml_recover(FALSE);
         msg_puts("\n");
@@ -14630,7 +12968,7 @@
     }
 
     setpcmark();
-    retval = do_ecmd(0, NULL, NULL, NULL, ECMD_ONE, forceit ? ECMD_FORCEIT : 0, curwin);
+    retval = do_ecmd(0, NULL, NULL, NULL,  (linenr_T)1 , forceit ? ECMD_FORCEIT : 0, curwin);
 
     if (buf != curbuf && bufref_valid(&bufref) && buf->b_nwindows == 0)
     {
@@ -14794,17 +13132,19 @@
             end_visual_mode();
         }
 
-        FOR_ALL_BUFFERS(bp)
+         for ((bp) = firstbuf; (bp) != NULL; (bp) = (bp)->b_next) 
+         {
             if (bp->b_p_bl && bp != buf)
             {
                 break;
             }
+         }
         if (bp == NULL && buf == curbuf)
         {
             return empty_curbuf(TRUE, (flags & DOBUF_FORCEIT), action);
         }
 
-        while (buf == curbuf && !(win_locked(curwin) || curwin->w_buffer->b_locked > 0) && (!ONE_WINDOW || first_tabpage->tp_next != NULL))
+        while (buf == curbuf && !(win_locked(curwin) || curwin->w_buffer->b_locked > 0) && (! (firstwin == lastwin)  || first_tabpage->tp_next != NULL))
         {
             if (win_close(curwin, FALSE) == FAIL)
             {
@@ -14919,11 +13259,13 @@
         }
         if (buf == NULL)
         {
-            FOR_ALL_BUFFERS(buf)
+             for ((buf) = firstbuf; (buf) != NULL; (buf) = (buf)->b_next) 
+             {
                 if (buf->b_p_bl && buf != curbuf && !bt_quickfix(buf) && !buf->b_locked_split)
                 {
                     break;
                 }
+             }
         }
         if (buf == NULL)
         {
@@ -14978,7 +13320,8 @@
 
     if (action == DOBUF_SPLIT)
     {
-        RESET_BINDING(curwin);
+         (curwin)-> w_onebuf_opt.wo_scb  = FALSE;
+         (curwin)-> w_onebuf_opt.wo_crb  = FALSE;
     }
 
     return OK;
@@ -15043,7 +13386,7 @@
                 {
                     break;
                 }
-                if (!VIM_ISDIGIT(*arg))
+                if (! ((unsigned)(*arg) - '0' < 10) )
                 {
                     p = skiptowhite_esc(arg);
                     bnr = buflist_findpat(arg, p, command == DOBUF_WIPE || command == DOBUF_WIPE_REUSE, FALSE, FALSE);
@@ -15068,15 +13411,15 @@
         {
             if (command == DOBUF_UNLOAD)
             {
-                STRCPY(IObuff, _(e_no_buffers_were_unloaded));
+                 strcpy((char *)(IObuff), (char *)(_(e_no_buffers_were_unloaded))) ;
             }
             else if (command == DOBUF_DEL)
             {
-                STRCPY(IObuff, _(e_no_buffers_were_deleted));
+                 strcpy((char *)(IObuff), (char *)(_(e_no_buffers_were_deleted))) ;
             }
             else
             {
-                STRCPY(IObuff, _(e_no_buffers_were_wiped_out));
+                 strcpy((char *)(IObuff), (char *)(_(e_no_buffers_were_wiped_out))) ;
             }
             errormsg = (char *)IObuff;
         }
@@ -15217,8 +13560,6 @@
         scroll_cursor_halfway(FALSE, FALSE);
     }
 
-    DO_AUTOCHDIR;
-
     redraw_later(UPD_NOT_VALID);
 }
 
@@ -15235,7 +13576,7 @@
 }
 
     void
-no_write_message_nobang(buf_T *buf UNUSED)
+no_write_message_nobang(buf_T *buf  __attribute__((unused)) )
 {
         emsg(_(e_no_write_since_last_change));
 }
@@ -15243,7 +13584,7 @@
     int
 curbuf_reusable(void)
 {
-    return (curbuf != NULL && curbuf->b_ffname == NULL && curbuf->b_nwindows <= 1 && (curbuf->b_ml.ml_mfp == NULL || BUFEMPTY()) && !bt_quickfix(curbuf) && !curbufIsChanged());
+    return (curbuf != NULL && curbuf->b_ffname == NULL && curbuf->b_nwindows <= 1 && (curbuf->b_ml.ml_mfp == NULL ||  (curbuf->b_ml.ml_line_count == 1 && *ml_get((linenr_T)1) == NUL) ) && !bt_quickfix(curbuf) && !curbufIsChanged());
 }
 
     buf_T *
@@ -15261,9 +13602,9 @@
 
     fname_expand(curbuf, &ffname, &sfname);
 
-    if (sfname == NULL || mch_stat((char *)sfname, &st) < 0)
+    if (sfname == NULL ||  stat(((char *)sfname), (&st))  < 0)
     {
-        st.st_dev = (dev_T)-1;
+        st.st_dev = ( dev_t )-1;
     }
     if (ffname != NULL && !(flags & (BLN_DUMMY | BLN_NEW)) && (buf = buflist_findname_stat(ffname, &st)) != NULL)
     {
@@ -15311,7 +13652,7 @@
     }
     if (buf != curbuf || curbuf == NULL)
     {
-        buf = ALLOC_CLEAR_ONE(buf_T);
+        buf =  (buf_T *)alloc_clear(sizeof(buf_T)) ;
         if (buf == NULL)
         {
             vim_free(ffname);
@@ -15327,19 +13668,21 @@
     }
 
     clear_wininfo(buf);
-    buf->b_wininfo = ALLOC_CLEAR_ONE(wininfo_T);
+    buf->b_wininfo =  (wininfo_T *)alloc_clear(sizeof(wininfo_T)) ;
 
     if ((ffname != NULL && (buf->b_ffname == NULL || buf->b_sfname == NULL)) || buf->b_wininfo == NULL)
     {
         if (buf->b_sfname != buf->b_ffname)
         {
-            VIM_CLEAR(buf->b_sfname);
+             vim_free(buf->b_sfname);
+             (buf->b_sfname) = NULL;
         }
         else
         {
             buf->b_sfname = NULL;
         }
-        VIM_CLEAR(buf->b_ffname);
+         vim_free(buf->b_ffname);
+         (buf->b_ffname) = NULL;
         if (buf != curbuf)
         {
             free_buffer(buf);
@@ -15424,7 +13767,7 @@
     buf->b_wininfo->wi_win = curwin;
 
     buf->b_fname = buf->b_sfname;
-    if (st.st_dev == (dev_T)-1)
+    if (st.st_dev == ( dev_t )-1)
     {
         buf->b_dev_valid = false;
     }
@@ -15503,7 +13846,7 @@
     buf->b_p_ac = -1;
     buf->b_p_ar = -1;
     buf->b_p_fs = -1;
-    buf->b_p_ul = NO_LOCAL_UNDOLEVEL;
+    buf->b_p_ul =  (-123456) ;
     clear_string_option(&buf->b_p_lw);
     clear_string_option(&buf->b_p_bkc);
     clear_string_option(&buf->b_p_menc);
@@ -15556,7 +13899,7 @@
     {
         wp = swbuf_goto_win_with_buf(buf);
 
-        if (wp == NULL && (swb_flags & (SWB_VSPLIT | SWB_SPLIT | SWB_NEWTAB)) && !BUFEMPTY())
+        if (wp == NULL && (swb_flags & (SWB_VSPLIT | SWB_SPLIT | SWB_NEWTAB)) && ! (curbuf->b_ml.ml_line_count == 1 && *ml_get((linenr_T)1) == NUL) )
         {
             if (swb_flags & SWB_NEWTAB)
             {
@@ -15566,13 +13909,14 @@
             {
                 return FAIL;
             }
-            RESET_BINDING(curwin);
+             (curwin)-> w_onebuf_opt.wo_scb  = FALSE;
+             (curwin)-> w_onebuf_opt.wo_crb  = FALSE;
         }
     }
 
     ++RedrawingDisabled;
     int retval = FAIL;
-    if (GETFILE_SUCCESS(getfile(buf->b_fnum, NULL, NULL, (options & GETF_SETMARK), lnum, forceit)))
+    if ( ((getfile(buf->b_fnum, NULL, NULL, (options & GETF_SETMARK), lnum, forceit)) <= 0) )
     {
         if (!p_sol && col != 0)
         {
@@ -15634,9 +13978,9 @@
 {
     stat_T      st;
 
-    if (mch_stat((char *)ffname, &st) < 0)
+    if ( stat(((char *)ffname), (&st))  < 0)
     {
-        st.st_dev = (dev_T)-1;
+        st.st_dev = ( dev_t )-1;
     }
     return buflist_findname_stat(ffname, &st);
 }
@@ -15646,16 +13990,18 @@
 {
     buf_T       *buf;
 
-    FOR_ALL_BUFS_FROM_LAST(buf)
+     for ((buf) = lastbuf; (buf) != NULL; (buf) = (buf)->b_prev) 
+     {
         if ((buf->b_flags & BF_DUMMY) == 0 && !otherfile_buf(buf, ffname, stp))
         {
             return buf;
         }
+     }
     return NULL;
 }
 
     int
-buflist_findpat(char_u      *pattern, char_u      *pattern_end, int         unlisted, int         diffmode UNUSED, int         curtab_only)
+buflist_findpat(char_u      *pattern, char_u      *pattern_end, int         unlisted, int         diffmode  __attribute__((unused)) , int         curtab_only)
 {
     buf_T       *buf;
     int         match = -1;
@@ -15685,7 +14031,7 @@
         {
             return -1;
         }
-        patend = pat + STRLEN(pat) - 1;
+        patend = pat +  strlen((char *)(pat))  - 1;
         toggledollar = (patend > pat && *patend == '$');
 
         find_listed = TRUE;
@@ -15706,7 +14052,7 @@
                 }
                 regmatch.regprog = vim_regcomp(p, magic_isset() ? RE_MAGIC : 0);
 
-                FOR_ALL_BUFS_FROM_LAST(buf)
+                 for ((buf) = lastbuf; (buf) != NULL; (buf) = (buf)->b_prev) 
                 {
                     if (regmatch.regprog == NULL)
                     {
@@ -15719,11 +14065,13 @@
                         {
                             win_T       *wp;
 
-                            FOR_ALL_WINDOWS(wp)
+                             for ((wp) = firstwin; (wp) != NULL; (wp) = (wp)->w_next) 
+                             {
                                 if (wp->w_buffer == buf)
                                 {
                                     break;
                                 }
+                             }
                             if (wp == NULL)
                             {
                                 continue;
@@ -15789,13 +14137,13 @@
     {
         if (*pat == '^' && pat[1] != NUL)
         {
-            int  len = (int)STRLEN(pat);
+            int  len = (int) strlen((char *)(pat)) ;
             patc = alloc(len);
             if (patc == NULL)
             {
                 return FAIL;
             }
-            STRNCPY(patc, pat + 1, len - 1);
+             strncpy((char *)(patc), (char *)(pat + 1), (len - 1)) ;
             patc[len - 1] = NUL;
             to_free = TRUE;
         }
@@ -15813,7 +14161,7 @@
     for (round = 1; round <= 2; ++round)
     {
         count = 0;
-        FOR_ALL_BUFFERS(buf)
+         for ((buf) = firstbuf; (buf) != NULL; (buf) = (buf)->b_next) 
         {
             if (!buf->b_p_bl)
             {
@@ -15835,13 +14183,13 @@
             else
             {
                 p = NULL;
-                if ((score = fuzzy_match_str(buf->b_sfname, pat)) != FUZZY_SCORE_NONE)
+                if ((score = fuzzy_match_str(buf->b_sfname, pat)) !=  INT_MIN )
                 {
                     p = buf->b_sfname;
                 }
                 if (p == NULL)
                 {
-                    if ((score = fuzzy_match_str(buf->b_ffname, pat)) != FUZZY_SCORE_NONE)
+                    if ((score = fuzzy_match_str(buf->b_ffname, pat)) !=  INT_MIN )
                     {
                         p = buf->b_ffname;
                     }
@@ -15896,7 +14244,7 @@
         {
             if (!fuzzy)
             {
-                *file = ALLOC_MULT(char_u *, count);
+                *file =  (char_u * *)alloc(sizeof(char_u *) * (count)) ;
                 if (*file == NULL)
                 {
                     vim_regfree(regmatch.regprog);
@@ -15909,7 +14257,7 @@
             }
             else
             {
-                fuzmatch = ALLOC_MULT(fuzmatch_str_T, count);
+                fuzmatch =  (fuzmatch_str_T *)alloc(sizeof(fuzmatch_str_T) * (count)) ;
                 if (fuzmatch == NULL)
                 {
                     *num_file = 0;
@@ -16000,7 +14348,7 @@
     vim_snprintf((char *)key, sizeof(key), "%x", nr);
     hi = hash_find(&buf_hashtab, key);
 
-    if (!HASHITEM_EMPTY(hi))
+    if (! ((hi)->hi_key == NULL || (hi)->hi_key == &hash_removed) )
     {
         return (buf_T *)(hi->hi_key - ((unsigned)(curbuf->b_key - (char_u *)curbuf)));
     }
@@ -16025,14 +14373,16 @@
 {
     wininfo_T   *wip;
 
-    FOR_ALL_BUF_WININFO(buf, wip)
+     for ((wip) = (buf)->b_wininfo; (wip) != NULL; (wip) = (wip)->wi_next) 
+     {
         if (wip->wi_win == win)
         {
             break;
         }
+     }
     if (wip == NULL)
     {
-        wip = ALLOC_CLEAR_ONE(wininfo_T);
+        wip =  (wininfo_T *)alloc_clear(sizeof(wininfo_T)) ;
         if (wip == NULL)
         {
             return;
@@ -16087,15 +14437,17 @@
 }
 
     static wininfo_T *
-find_wininfo(buf_T       *buf, int         need_options, int         skip_diff_buffer UNUSED)
+find_wininfo(buf_T       *buf, int         need_options, int         skip_diff_buffer  __attribute__((unused)) )
 {
     wininfo_T   *wip;
 
-    FOR_ALL_BUF_WININFO(buf, wip)
+     for ((wip) = (buf)->b_wininfo; (wip) != NULL; (wip) = (wip)->wi_next) 
+     {
         if (wip->wi_win == curwin && (!need_options || wip->wi_optset))
         {
             break;
         }
+     }
 
     if (wip != NULL)
     {
@@ -16179,11 +14531,11 @@
         name = buf_spname(buf);
         if (name != NULL)
         {
-            vim_strncpy(NameBuff, name, MAXPATHL - 1);
+            vim_strncpy(NameBuff, name,  PATH_MAX  - 1);
         }
         else
         {
-            home_replace(buf, buf->b_fname, NameBuff, MAXPATHL, TRUE);
+            home_replace(buf, buf->b_fname, NameBuff,  PATH_MAX , TRUE);
         }
         if (message_filtered(NameBuff))
         {
@@ -16195,16 +14547,16 @@
             ro_char = !buf->b_p_ma ? '-' : (buf->b_p_ro ? '=' : ' ');
 
         msg_putchar('\n');
-        len = (int)vim_snprintf_safelen((char *)IObuff, IOSIZE - 20, "%3d%c%c%c%c%c \"%s\"", buf->b_fnum, buf->b_p_bl ? ' ' : 'u', buf == curbuf ? '%' : (curwin->w_alt_fnum == buf->b_fnum ? '#' : ' '), buf->b_ml.ml_mfp == NULL ? ' ' : (buf->b_nwindows == 0 ? 'h' : 'a'), ro_char, changed_char, NameBuff);
+        len = (int)vim_snprintf_safelen((char *)IObuff,  (1024+1)  - 20, "%3d%c%c%c%c%c \"%s\"", buf->b_fnum, buf->b_p_bl ? ' ' : 'u', buf == curbuf ? '%' : (curwin->w_alt_fnum == buf->b_fnum ? '#' : ' '), buf->b_ml.ml_mfp == NULL ? ' ' : (buf->b_nwindows == 0 ? 'h' : 'a'), ro_char, changed_char, NameBuff);
 
         i = 40 - vim_strsize(IObuff);
         do
         {
             IObuff[len++] = ' ';
         }
-        while (--i > 0 && len < IOSIZE - 18)
+        while (--i > 0 && len <  (1024+1)  - 18)
             ;
-            vim_snprintf((char *)IObuff + len, (size_t)(IOSIZE - len), _("line %ld"), buf == curbuf ? curwin->w_cursor.lnum : (long)buflist_findlnum(buf));
+            vim_snprintf((char *)IObuff + len, (size_t)( (1024+1)  - len), _("line %ld"), buf == curbuf ? curwin->w_cursor.lnum : (long)buflist_findlnum(buf));
         msg_outtrans(IObuff);
         out_flush();
         ui_breakcheck();
@@ -16241,14 +14593,16 @@
     {
         if (buf->b_sfname != buf->b_ffname)
         {
-            VIM_CLEAR(buf->b_sfname);
+             vim_free(buf->b_sfname);
+             (buf->b_sfname) = NULL;
         }
         else
         {
             buf->b_sfname = NULL;
         }
-        VIM_CLEAR(buf->b_ffname);
-        st.st_dev = (dev_T)-1;
+         vim_free(buf->b_ffname);
+         (buf->b_ffname) = NULL;
+        st.st_dev = ( dev_t )-1;
     }
     else
     {
@@ -16258,9 +14612,9 @@
             return FAIL;
         }
 
-        if (mch_stat((char *)ffname, &st) < 0)
+        if ( stat(((char *)ffname), (&st))  < 0)
         {
-            st.st_dev = (dev_T)-1;
+            st.st_dev = ( dev_t )-1;
         }
         if (!(buf->b_flags & BF_DUMMY))
         {
@@ -16272,11 +14626,16 @@
             tabpage_T   *tab;
             int         in_use = FALSE;
 
-            FOR_ALL_TAB_WINDOWS(tab, win)
+             for ((tab) = first_tabpage; (tab) != NULL; (tab) = (tab)->tp_next)
+             {
+                 for ((win) = ((tab) == curtab)            ? firstwin : (tab)->tp_firstwin; (win); (win) = (win)->w_next)
+                 {
                 if (win->w_buffer == obuf)
                 {
                     in_use = TRUE;
                 }
+                 }
+             }
 
             if (obuf->b_ml.ml_mfp != NULL || in_use)
             {
@@ -16305,7 +14664,7 @@
         buf->b_sfname = sfname;
     }
     buf->b_fname = buf->b_sfname;
-    if (st.st_dev == (dev_T)-1)
+    if (st.st_dev == ( dev_t )-1)
     {
         buf->b_dev_valid = false;
     }
@@ -16424,7 +14783,7 @@
     {
         return TRUE;
     }
-    if (fnamecmp(ffname, buf->b_ffname) == 0)
+    if ( vim_fnamecmp((char_u *)(ffname), (char_u *)(buf->b_ffname))  == 0)
     {
         return FALSE;
     }
@@ -16433,9 +14792,9 @@
 
         if (stp == NULL)
         {
-            if (!buf->b_dev_valid || mch_stat((char *)ffname, &st) < 0)
+            if (!buf->b_dev_valid ||  stat(((char *)ffname), (&st))  < 0)
             {
-                st.st_dev = (dev_T)-1;
+                st.st_dev = ( dev_t )-1;
             }
             stp = &st;
         }
@@ -16456,7 +14815,7 @@
 {
     stat_T      st;
 
-    if (buf->b_fname != NULL && mch_stat((char *)buf->b_fname, &st) >= 0)
+    if (buf->b_fname != NULL &&  stat(((char *)buf->b_fname), (&st))  >= 0)
     {
         buf->b_dev_valid = true;
         buf->b_dev = st.st_dev;
@@ -16481,7 +14840,7 @@
     char        *buffer;
     size_t      bufferlen = 0;
 
-    buffer = alloc(IOSIZE);
+    buffer = alloc( (1024+1) );
     if (buffer == NULL)
     {
         return;
@@ -16489,7 +14848,7 @@
 
     if (fullname > 1)
     {
-        bufferlen = vim_snprintf_safelen(buffer, IOSIZE, "buf %d: ", curbuf->b_fnum);
+        bufferlen = vim_snprintf_safelen(buffer,  (1024+1) , "buf %d: ", curbuf->b_fnum);
     }
 
     buffer[bufferlen++] = '"';
@@ -16497,7 +14856,7 @@
     name = buf_spname(curbuf);
     if (name != NULL)
     {
-        bufferlen += vim_snprintf_safelen(buffer + bufferlen, IOSIZE - bufferlen, "%s", name);
+        bufferlen += vim_snprintf_safelen(buffer + bufferlen,  (1024+1)  - bufferlen, "%s", name);
     }
     else
     {
@@ -16509,28 +14868,28 @@
         {
             name = curbuf->b_ffname;
         }
-        bufferlen += home_replace(shorthelp ? curbuf : NULL, name, (char_u *)buffer + bufferlen, IOSIZE - (int)bufferlen, TRUE);
+        bufferlen += home_replace(shorthelp ? curbuf : NULL, name, (char_u *)buffer + bufferlen,  (1024+1)  - (int)bufferlen, TRUE);
     }
 
-    bufferlen += vim_snprintf_safelen(buffer + bufferlen, IOSIZE - bufferlen, "\"%s%s%s%s%s%s", curbufIsChanged() ? (shortmess(SHM_MOD) ?  " [+]" : _(" [Modified]")) : " ", (curbuf->b_flags & BF_NOTEDITED) && !bt_dontwrite(curbuf) ? _("[Not edited]") : "", (curbuf->b_flags & BF_NEW) && !bt_dontwrite(curbuf) ? new_file_message() : "", (curbuf->b_flags & BF_READERR) ? _("[Read errors]") : "", curbuf->b_p_ro ? (shortmess(SHM_RO) ? _("[RO]") : _("[readonly]")) : "", (curbufIsChanged() || (curbuf->b_flags & BF_WRITE_MASK) || curbuf->b_p_ro) ? " " : "");
+    bufferlen += vim_snprintf_safelen(buffer + bufferlen,  (1024+1)  - bufferlen, "\"%s%s%s%s%s%s", curbufIsChanged() ? (shortmess(SHM_MOD) ?  " [+]" : _(" [Modified]")) : " ", (curbuf->b_flags & BF_NOTEDITED) && !bt_dontwrite(curbuf) ? _("[Not edited]") : "", (curbuf->b_flags & BF_NEW) && !bt_dontwrite(curbuf) ? new_file_message() : "", (curbuf->b_flags & BF_READERR) ? _("[Read errors]") : "", curbuf->b_p_ro ? (shortmess(SHM_RO) ? _("[RO]") : _("[readonly]")) : "", (curbufIsChanged() || (curbuf->b_flags &  (BF_NOTEDITED + BF_NEW + BF_READERR) ) || curbuf->b_p_ro) ? " " : "");
 
     if (curbuf->b_ml.ml_flags & ML_EMPTY)
     {
-        bufferlen += vim_snprintf_safelen(buffer + bufferlen, IOSIZE - bufferlen, "%s", _(no_lines_msg));
+        bufferlen += vim_snprintf_safelen(buffer + bufferlen,  (1024+1)  - bufferlen, "%s", _((char *)no_lines_msg));
     }
     else if (p_ru)
     {
-        bufferlen += vim_snprintf_safelen(buffer + bufferlen, IOSIZE - bufferlen, NGETTEXT("%ld line --%d%%--", "%ld lines --%d%%--", curbuf->b_ml.ml_line_count), (long)curbuf->b_ml.ml_line_count, calc_percentage(curwin->w_cursor.lnum, curbuf->b_ml.ml_line_count));
+        bufferlen += vim_snprintf_safelen(buffer + bufferlen,  (1024+1)  - bufferlen, NGETTEXT("%ld line --%d%%--", "%ld lines --%d%%--", curbuf->b_ml.ml_line_count), (long)curbuf->b_ml.ml_line_count, calc_percentage(curwin->w_cursor.lnum, curbuf->b_ml.ml_line_count));
     }
     else
     {
-        bufferlen += vim_snprintf_safelen(buffer + bufferlen, IOSIZE - bufferlen, _("line %ld of %ld --%d%%-- col "), (long)curwin->w_cursor.lnum, (long)curbuf->b_ml.ml_line_count, calc_percentage(curwin->w_cursor.lnum, curbuf->b_ml.ml_line_count));
+        bufferlen += vim_snprintf_safelen(buffer + bufferlen,  (1024+1)  - bufferlen, _("line %ld of %ld --%d%%-- col "), (long)curwin->w_cursor.lnum, (long)curbuf->b_ml.ml_line_count, calc_percentage(curwin->w_cursor.lnum, curbuf->b_ml.ml_line_count));
 
         validate_virtcol();
-        bufferlen += col_print((char_u *)buffer + bufferlen, IOSIZE - bufferlen, (int)curwin->w_cursor.col + 1, (int)curwin->w_virtcol + 1);
+        bufferlen += col_print((char_u *)buffer + bufferlen,  (1024+1)  - bufferlen, (int)curwin->w_cursor.col + 1, (int)curwin->w_virtcol + 1);
     }
 
-    (void)append_arg_number(curwin, (char_u *)buffer + bufferlen, IOSIZE - bufferlen, !shortmess(SHM_FILE));
+    (void)append_arg_number(curwin, (char_u *)buffer + bufferlen,  (1024+1)  - bufferlen, !shortmess(SHM_FILE));
 
     if (dont_truncate)
     {
@@ -16574,7 +14933,7 @@
     char_u      *title_str = NULL;
     char_u      *icon_str = NULL;
     int         mustset;
-    char_u      buf[IOSIZE];
+    char_u      buf[ (1024+1) ];
     size_t      buflen = 0;
 
     if (!redrawing())
@@ -16606,23 +14965,19 @@
         if (*p_titlestring != NUL)
         {
                 title_str = p_titlestring;
-            buflen = STRLEN(title_str);
+            buflen =  strlen((char *)(title_str)) ;
         }
         else
         {
             char_u  *p;
 
-#define SPACE_FOR_FNAME (sizeof(buf) - 100)
-#define SPACE_FOR_DIR   (sizeof(buf) - 20)
-#define SPACE_FOR_ARGNR (sizeof(buf) - 10)
-
             if (curbuf->b_fname == NULL)
             {
-                buflen = vim_snprintf_safelen((char *)buf, SPACE_FOR_FNAME, "%s", _("[No Name]"));
+                buflen = vim_snprintf_safelen((char *)buf,  (sizeof(buf) - 100) , "%s", _("[No Name]"));
             }
             else
             {
-                buflen = vim_snprintf_safelen((char *)buf, SPACE_FOR_FNAME, "%s", ((p = transstr(gettail(curbuf->b_fname))) != NULL) ? p : (char_u *)"");
+                buflen = vim_snprintf_safelen((char *)buf,  (sizeof(buf) - 100) , "%s", ((p = transstr(gettail(curbuf->b_fname))) != NULL) ? p : (char_u *)"");
                 vim_free(p);
             }
 
@@ -16655,31 +15010,30 @@
             {
                 buflen += vim_snprintf_safelen((char *)buf + buflen, sizeof(buf) - buflen, " (");
 
-                home_replace(curbuf, curbuf->b_ffname, buf + buflen, (int)(SPACE_FOR_DIR - buflen), TRUE);
+                home_replace(curbuf, curbuf->b_ffname, buf + buflen, (int)( (sizeof(buf) - 20)  - buflen), TRUE);
 
                 p = gettail_sep(buf + buflen);
                 if (p == buf + buflen)
                 {
-                    buflen += vim_snprintf_safelen((char *)buf + buflen, SPACE_FOR_DIR - buflen, "%s)", _("help"));
+                    buflen += vim_snprintf_safelen((char *)buf + buflen,  (sizeof(buf) - 20)  - buflen, "%s)", _("help"));
                 }
                 else
                 {
-
-                    if (buflen < SPACE_FOR_DIR)
+                    if (buflen <  (sizeof(buf) - 20) )
                     {
                         *p = NUL;
 
-                        buflen += vim_snprintf_safelen((char *)buf + buflen, SPACE_FOR_DIR - buflen, "%s)", ((p = transstr(buf + buflen)) != NULL) ? p : (char_u *)"");
+                        buflen += vim_snprintf_safelen((char *)buf + buflen,  (sizeof(buf) - 20)  - buflen, "%s)", ((p = transstr(buf + buflen)) != NULL) ? p : (char_u *)"");
                         vim_free(p);
                     }
                     else
                     {
-                        buflen += vim_snprintf_safelen((char *)buf + buflen, SPACE_FOR_ARGNR - buflen, "...)");
+                        buflen += vim_snprintf_safelen((char *)buf + buflen,  (sizeof(buf) - 10)  - buflen, "...)");
                     }
                 }
             }
 
-            buflen += append_arg_number(curwin, buf + buflen, SPACE_FOR_ARGNR - buflen, FALSE);
+            buflen += append_arg_number(curwin, buf + buflen,  (sizeof(buf) - 10)  - buflen, FALSE);
 
             buflen += vim_snprintf_safelen((char *)buf + buflen, sizeof(buf) - buflen, " - %s", (char_u *)"VIM");
 
@@ -16711,7 +15065,7 @@
             {
                 name = gettail(curbuf->b_ffname);
             }
-            namelen = (int)STRLEN(name);
+            namelen = (int) strlen((char *)(name)) ;
             if (namelen > 100)
             {
                 namelen -= 100;
@@ -16721,7 +15075,7 @@
                 }
                 name += namelen;
             }
-            STRCPY(buf, name);
+             strcpy((char *)(buf), (char *)(name)) ;
             trans_characters(buf, sizeof(buf));
         }
     }
@@ -16737,7 +15091,7 @@
     static int
 value_changed(char_u *str, char_u **last)
 {
-    if ((str == NULL) != (*last == NULL) || (str != NULL && *last != NULL && STRCMP(str, *last) != 0))
+    if ((str == NULL) != (*last == NULL) || (str != NULL && *last != NULL &&  strcmp((char *)(str), (char *)(*last))  != 0))
     {
         vim_free(*last);
         if (str == NULL)
@@ -16791,7 +15145,7 @@
     static int
 append_arg_number(win_T       *wp, char_u      *buf, size_t      buflen, int         add_file)
 {
-    if (ARGCOUNT <= 1)
+    if ( ( (curwin)->w_alist ->al_ga.ga_len)  <= 1)
     {
         return 0;
     }
@@ -16813,7 +15167,7 @@
             break;
     }
 
-    return (int)vim_snprintf_safelen((char *)buf, buflen, msg, wp->w_arg_idx + 1, ARGCOUNT);
+    return (int)vim_snprintf_safelen((char *)buf, buflen, msg, wp->w_arg_idx + 1,  ( (curwin)->w_alist ->al_ga.ga_len) );
 }
 
     char_u  *
@@ -16823,7 +15177,7 @@
 }
 
     void
-fname_expand(buf_T       *buf UNUSED, char_u      **ffname, char_u      **sfname)
+fname_expand(buf_T       *buf  __attribute__((unused)) , char_u      **ffname, char_u      **sfname)
 {
     if (*ffname == NULL)
     {
@@ -16883,7 +15237,7 @@
         for (wp = firstwin; wp != NULL; wp = wpnext)
         {
             wpnext = wp->w_next;
-            if ((wp->w_buffer->b_nwindows > 1 || ((cmdmod.cmod_split & WSP_VERT) ? wp->w_height + wp->w_status_height < Rows - p_ch - tabline_height() : wp->w_width != Columns) || (had_tab > 0 && wp != firstwin)) && !ONE_WINDOW && !(win_locked(wp) || wp->w_buffer->b_locked > 0) && !win_unlisted(wp))
+            if ((wp->w_buffer->b_nwindows > 1 || ((cmdmod.cmod_split & WSP_VERT) ? wp->w_height + wp->w_status_height < Rows - p_ch - tabline_height() : wp->w_width != Columns) || (had_tab > 0 && wp != firstwin)) && ! (firstwin == lastwin)  && !(win_locked(wp) || wp->w_buffer->b_locked > 0) && !win_unlisted(wp))
             {
                 if (win_close(wp, FALSE) == FAIL)
                 {
@@ -16929,11 +15283,13 @@
         }
         else
         {
-            FOR_ALL_WINDOWS(wp)
+             for ((wp) = firstwin; (wp) != NULL; (wp) = (wp)->w_next) 
+             {
                 if (wp->w_buffer == buf)
                 {
                     break;
                 }
+             }
             if (wp != NULL)
             {
                 win_move_after(wp, curwin);
@@ -16965,7 +15321,6 @@
             }
             if (swap_exists_action == SEA_QUIT)
             {
-
                 win_close(curwin, TRUE);
                 --open_wins;
                 swap_exists_action = SEA_NONE;
@@ -17057,34 +15412,22 @@
 
 // ---------------- begin version.h ----------------
 
-#define VIM_TOSTR_(a)                   #a
-#define VIM_TOSTR(a)                    VIM_TOSTR_(a)
+// Nothing but the preprocessor can build a string literal from a constant, so
+// the version strings are character arrays built from the two numbers instead.
+// The numbers stay the single source of truth and sizeof still gives the
+// length; what an array cannot do is literal concatenation, so the places that
+// pasted "VIM - Vi IMproved " onto it take it through %s.
+enum { VIM_VERSION_MAJOR = 9 };
+enum { VIM_VERSION_MINOR = 2 };
+enum { VIM_VERSION_BUILD = 286 };
+static const char VIM_VERSION_DATE_ONLY[] = "2026 Feb 14";
+
+static const char VIM_VERSION_SHORT[] = {'0' + VIM_VERSION_MAJOR, '.', '0' + VIM_VERSION_MINOR, NUL};
+static const char VIM_VERSION_NODOT[] = {'v', 'i', 'm', '0' + VIM_VERSION_MAJOR, '0' + VIM_VERSION_MINOR, NUL};
+static const char VIM_VERSION_LONG_ONLY[] = {'V', 'I', 'M', ' ', '-', ' ', 'V', 'i', ' ', 'I', 'M', 'p', 'r', 'o', 'v', 'e', 'd', ' ', '0' + VIM_VERSION_MAJOR, '.', '0' + VIM_VERSION_MINOR, NUL};
+enum { VIM_VERSION_100 = VIM_VERSION_MAJOR * 100 + VIM_VERSION_MINOR };
 
-#define VIM_VERSION_MAJOR               9
-#define VIM_VERSION_MINOR               2
-#define VIM_VERSION_BUILD               286
-#define VIM_VERSION_BUILD_BCD           0x11e
-#define VIM_VERSION_DATE_ONLY           "2026 Feb 14"
-
-#define VIM_VERSION_MAJOR_STR           VIM_TOSTR(VIM_VERSION_MAJOR)
-#define VIM_VERSION_MINOR_STR           VIM_TOSTR(VIM_VERSION_MINOR)
-#define VIM_VERSION_100     (VIM_VERSION_MAJOR * 100 + VIM_VERSION_MINOR)
-
-#define VIM_VERSION_BUILD_STR           VIM_TOSTR(VIM_VERSION_BUILD)
-# define VIM_VERSION_PATCHLEVEL         0
-
-# define LEADZERO(x) 000 ## x
-
-#define VIM_VERSION_PATCHLEVEL_STR      VIM_TOSTR(LEADZERO(VIM_VERSION_PATCHLEVEL))
-#define VIM_VERSION_RELEASE             final
-
-#define VIM_VERSION_NODOT     "vim" VIM_VERSION_MAJOR_STR VIM_VERSION_MINOR_STR
-#define VIM_VERSION_SHORT     VIM_VERSION_MAJOR_STR "." VIM_VERSION_MINOR_STR
-#define VIM_VERSION_MEDIUM    VIM_VERSION_SHORT
-#define VIM_VERSION_LONG_ONLY "VIM - Vi IMproved " VIM_VERSION_MEDIUM
-#define VIM_VERSION_LONG_HEAD VIM_VERSION_LONG_ONLY " (" VIM_VERSION_DATE_ONLY
-#define VIM_VERSION_LONG      VIM_VERSION_LONG_HEAD ")"
-#define VIM_VERSION_LONG_DATE VIM_VERSION_LONG_HEAD ", compiled "
+enum { VIM_VERSION_PATCHLEVEL = 0 };
 
 // ---------------- end version.h ----------------
 
@@ -17096,7 +15439,6 @@
     char_u      *e;
     int         prev;
     int         retval = OK;
-    ESTACK_CHECK_DECLARATION;
 
     prev = -1;
     s = ml_get(lnum);
@@ -17105,7 +15447,7 @@
     {
         if (prev == -1 || vim_isspace(prev))
         {
-            if ((prev != -1 && STRNCMP(s, "ex:", (size_t)3) == 0) || STRNCMP(s, "vi:", (size_t)3) == 0)
+            if ((prev != -1 &&  strncmp((char *)(s), (char *)("ex:"), ((size_t)3))  == 0) ||  strncmp((char *)(s), (char *)("vi:"), ((size_t)3))  == 0)
             {
                 break;
             }
@@ -17122,7 +15464,7 @@
                     e = s + 3;
                 }
                 vers = getdigits(&e);
-                if (*e == ':' && (s[0] != 'V' || STRNCMP(skipwhite(e + 1), "set", 3) == 0) && (s[3] == ':' || (VIM_VERSION_100 >= vers && SAFE_isdigit(s[3])) || (VIM_VERSION_100 < vers && s[3] == '<') || (VIM_VERSION_100 > vers && s[3] == '>') || (VIM_VERSION_100 == vers && s[3] == '=')))
+                if (*e == ':' && (s[0] != 'V' ||  strncmp((char *)(skipwhite(e + 1)), (char *)("set"), (3))  == 0) && (s[3] == ':' || (VIM_VERSION_100 >= vers &&  (isdigit ((unsigned char)(s[3]))) ) || (VIM_VERSION_100 < vers && s[3] == '<') || (VIM_VERSION_100 > vers && s[3] == '>') || (VIM_VERSION_100 == vers && s[3] == '=')))
                 {
                     break;
                 }
@@ -17154,7 +15496,6 @@
         line_end = s + len;
 
         estack_push(ETYPE_MODELINE, (char_u *)"modelines", lnum);
-        ESTACK_CHECK_SETUP;
 
         end = FALSE;
         while (end == FALSE)
@@ -17169,7 +15510,7 @@
             {
                 if (e[0] == '\\' && e[1] == ':')
                 {
-                    mch_memmove(e, e + 1, (size_t)(line_end - (e + 1)) + 1);
+                     memmove((char *)(e), (char *)(e + 1), (size_t)(line_end - (e + 1)) + 1) ;
                     --line_end;
                 }
             }
@@ -17178,7 +15519,7 @@
                 end = TRUE;
             }
 
-            if (STRNCMP(s, "set ", (size_t)4) == 0 || STRNCMP(s, "se ", (size_t)3) == 0)
+            if ( strncmp((char *)(s), (char *)("set "), ((size_t)4))  == 0 ||  strncmp((char *)(s), (char *)("se "), ((size_t)3))  == 0)
             {
                 if (*e != ':')
                 {
@@ -17210,7 +15551,6 @@
             s = (e == line_end) ? e : e + 1;
         }
 
-        ESTACK_CHECK_NOW;
         estack_pop();
         vim_free(linecopy);
     }
@@ -17224,13 +15564,13 @@
 }
 
     int
-bt_quickfix(buf_T *buf UNUSED)
+bt_quickfix(buf_T *buf  __attribute__((unused)) )
 {
     return FALSE;
 }
 
     int
-bt_terminal(buf_T *buf UNUSED)
+bt_terminal(buf_T *buf  __attribute__((unused)) )
 {
     return FALSE;
 }
@@ -17294,7 +15634,6 @@
     char_u *
 buf_spname(buf_T *buf)
 {
-
     if (bt_nofilename(buf))
     {
         if (buf->b_fname != NULL)
@@ -17373,14 +15712,14 @@
     }
 
     block_autocmds();
-    if (ml_open(curbuf) == OK && readfile(buf->b_ffname, buf->b_fname, (linenr_T)0, (linenr_T)0, (linenr_T)MAXLNUM, &ea, READ_NEW | READ_DUMMY) == OK)
+    if (ml_open(curbuf) == OK && readfile(buf->b_ffname, buf->b_fname, (linenr_T)0, (linenr_T)0, (linenr_T) LONG_MAX , &ea, READ_NEW | READ_DUMMY) == OK)
     {
         if (buf->b_ml.ml_line_count == curbuf->b_ml.ml_line_count)
         {
             differ = FALSE;
             for (lnum = 1; lnum <= curbuf->b_ml.ml_line_count; ++lnum)
             {
-                if (STRCMP(ml_get_buf(buf, lnum, FALSE), ml_get(lnum)) != 0)
+                if ( strcmp((char *)(ml_get_buf(buf, lnum, FALSE)), (char *)(ml_get(lnum)))  != 0)
                 {
                     differ = TRUE;
                     break;
@@ -17425,7 +15764,7 @@
 
 // ==================== bufwrite.c ====================
 
-#define SMALLBUFSIZE    256
+enum { SMALLBUFSIZE = 256 };
 
 struct bw_info
 {
@@ -17570,7 +15909,7 @@
                     {
                         l = len;
                     }
-                    mch_memmove(ip->bw_rest + ip->bw_restlen, buf, (size_t)l);
+                     memmove((char *)(ip->bw_rest + ip->bw_restlen), (char *)(buf), (size_t)l) ;
                     n = utf_ptr2len_len(ip->bw_rest, ip->bw_restlen + l);
                     if (n > ip->bw_restlen + len)
                     {
@@ -17597,7 +15936,7 @@
                     else
                     {
                         ip->bw_restlen -= n;
-                        mch_memmove(ip->bw_rest, ip->bw_rest + n, (size_t)ip->bw_restlen);
+                         memmove((char *)(ip->bw_rest), (char *)(ip->bw_rest + n), (size_t)ip->bw_restlen) ;
                         n = 0;
                     }
                 }
@@ -17611,7 +15950,7 @@
                             return FAIL;
                         }
                         ip->bw_restlen = len - wlen;
-                        mch_memmove(ip->bw_rest, buf + wlen, (size_t)ip->bw_restlen);
+                         memmove((char *)(ip->bw_rest), (char *)(buf + wlen), (size_t)ip->bw_restlen) ;
                         break;
                     }
                     if (n > 1)
@@ -17671,8 +16010,8 @@
 
                 fromlen = len + ip->bw_restlen;
                 fp = (char *)ip->bw_conv_buf + ip->bw_conv_buflen - fromlen;
-                mch_memmove(fp, ip->bw_rest, (size_t)ip->bw_restlen);
-                mch_memmove(fp + ip->bw_restlen, buf, (size_t)len);
+                 memmove((char *)(fp), (char *)(ip->bw_rest), (size_t)ip->bw_restlen) ;
+                 memmove((char *)(fp + ip->bw_restlen), (char *)(buf), (size_t)len) ;
                 from = fp;
                 tolen = ip->bw_conv_buflen - fromlen;
             }
@@ -17698,7 +16037,7 @@
                 ip->bw_first = FALSE;
             }
 
-            if ((iconv(ip->bw_iconv_fd, (void *)&from, &fromlen, &to, &tolen) == (size_t)-1 && ICONV_ERRNO != ICONV_EINVAL) || fromlen > CONV_RESTLEN)
+            if ((iconv(ip->bw_iconv_fd, (void *)&from, &fromlen, &to, &tolen) == (size_t)-1 &&  errno  !=  EINVAL ) || fromlen > CONV_RESTLEN)
             {
                 ip->bw_conv_error = TRUE;
                 return FAIL;
@@ -17706,7 +16045,7 @@
 
             if (fromlen > 0)
             {
-                mch_memmove(ip->bw_rest, (void *)from, fromlen);
+                 memmove((char *)(ip->bw_rest), (char *)((void *)from), fromlen) ;
             }
             ip->bw_restlen = (int)fromlen;
 
@@ -17731,7 +16070,7 @@
     {
         msg_scroll = TRUE;
         msg_silent = 0;
-        msg_attr(_("WARNING: The file has been changed since reading it!!!"), HL_ATTR(HLF_E));
+        msg_attr(_("WARNING: The file has been changed since reading it!!!"),  highlight_attr[(int)(HLF_E)] );
         if (ask_yesno((char_u *)_("Do you really want to write to it"), TRUE) == 'n')
         {
             return FAIL;
@@ -17847,7 +16186,7 @@
         return FAIL;
     }
 
-    if (STRLEN(fname) >= MAXPATHL)
+    if ( strlen((char *)(fname))  >=  PATH_MAX )
     {
         emsg(_(e_name_too_long));
         return FAIL;
@@ -17877,7 +16216,7 @@
     ffname = fname;
     fname = sfname;
 
-    if (buf->b_ffname != NULL && fnamecmp(ffname, buf->b_ffname) == 0)
+    if (buf->b_ffname != NULL &&  vim_fnamecmp((char_u *)(ffname), (char_u *)(buf->b_ffname))  == 0)
     {
         overwriting = TRUE;
     }
@@ -18032,7 +16371,7 @@
                     }
                     else
                     {
-                        buf->b_flags &= ~BF_WRITE_MASK;
+                        buf->b_flags &= ~ (BF_NOTEDITED + BF_NEW + BF_READERR) ;
                     }
                 }
                 if (reset_changed && buf->b_changed && !append && (overwriting || vim_strchr(p_cpo, CPO_PLUS) != NULL))
@@ -18120,7 +16459,7 @@
     st_old.st_dev = 0;
     st_old.st_ino = 0;
     perm = -1;
-    if (mch_stat((char *)fname, &st_old) < 0)
+    if ( stat(((char *)fname), (&st_old))  < 0)
     {
         newfile = TRUE;
     }
@@ -18202,14 +16541,14 @@
         }
         else if ((bkc & BKC_AUTO))
         {
-            if (st_old.st_nlink > 1 || mch_lstat((char *)fname, &st) < 0 || st.st_dev != st_old.st_dev || st.st_ino != st_old.st_ino)
+            if (st_old.st_nlink > 1 ||  lstat(((char *)fname), (&st))  < 0 || st.st_dev != st_old.st_dev || st.st_ino != st_old.st_ino)
             {
                 backup_copy = TRUE;
             }
             else
             {
                 size_t  dirlen;
-                char_u  tmp_fname[MAXPATHL];
+                char_u  tmp_fname[ PATH_MAX ];
                 int     i;
 
                 dirlen = (size_t)(gettail(fname) - fname);
@@ -18218,9 +16557,9 @@
                 for (i = 4913; ; i += 123)
                 {
                     vim_snprintf((char *)tmp_fname + dirlen, sizeof(tmp_fname) - dirlen, "%d", i);
-                    if (mch_lstat((char *)tmp_fname, &st) < 0)
+                    if ( lstat(((char *)tmp_fname), (&st))  < 0)
                     {
-                        fd = mch_open((char *)tmp_fname, O_CREAT|O_WRONLY|O_EXCL|O_NOFOLLOW, perm);
+                        fd =  open(((char *)tmp_fname), (O_CREAT|O_WRONLY|O_EXCL|O_NOFOLLOW), (perm)) ;
                         if (fd < 0 && errno == EEXIST)
                         {
                             continue;
@@ -18236,12 +16575,12 @@
                 {
                     vim_ignored = fchown(fd, st_old.st_uid, st_old.st_gid);
                     (void)mch_fsetperm(fd, perm);
-                    if (mch_stat((char *)tmp_fname, &st) < 0 || st.st_uid != st_old.st_uid || st.st_gid != st_old.st_gid || (long)st.st_mode != perm)
+                    if ( stat(((char *)tmp_fname), (&st))  < 0 || st.st_uid != st_old.st_uid || st.st_gid != st_old.st_gid || (long)st.st_mode != perm)
                     {
                         backup_copy = TRUE;
                     }
                     close(fd);
-                    mch_remove(tmp_fname);
+                     unlink((char *)(tmp_fname)) ;
                 }
             }
         }
@@ -18250,7 +16589,7 @@
         {
             int lstat_res;
 
-            lstat_res = mch_lstat((char *)fname, &st);
+            lstat_res =  lstat(((char *)fname), (&st)) ;
 
             if ((bkc & BKC_BREAKSYMLINK) && lstat_res == 0 && st.st_ino != st_old.st_ino)
             {
@@ -18272,7 +16611,7 @@
             backup_ext = p_bex;
         }
 
-        if (backup_copy && (fd = mch_open((char *)fname, O_RDONLY | O_EXTRA, 0)) >= 0)
+        if (backup_copy && (fd =  open(((char *)fname), (O_RDONLY | O_EXTRA), (0)) ) >= 0)
         {
             int         bfd;
             char_u      *copybuf;
@@ -18293,8 +16632,8 @@
             dirp = p_bdir;
             while (*dirp)
             {
-                char_u  *p UNUSED;
-                int     copybuf_len UNUSED;
+                char_u  *p  __attribute__((unused)) ;
+                int     copybuf_len  __attribute__((unused)) ;
 
                 st_new.st_ino = 0;
                 st_new.st_dev = 0;
@@ -18333,11 +16672,12 @@
                         goto nobackup;
                     }
 
-                    if (mch_stat((char *)backup, &st_new) >= 0)
+                    if ( stat(((char *)backup), (&st_new))  >= 0)
                     {
                         if (st_new.st_dev == st_old.st_dev && st_new.st_ino == st_old.st_ino)
                         {
-                            VIM_CLEAR(backup);
+                             vim_free(backup);
+                             (backup) = NULL;
                             if (!(buf->b_shortname || buf->b_p_sn))
                             {
                                 buf->b_shortname = true;
@@ -18355,20 +16695,21 @@
                         {
                             char_u      *wp;
 
-                            wp = backup + STRLEN(backup) - 1
-                                                         - STRLEN(backup_ext);
+                            wp = backup +  strlen((char *)(backup))  - 1
+                                                         -  strlen((char *)(backup_ext)) ;
                             if (wp < backup)
                             {
                                 wp = backup;
                             }
                             *wp = 'z';
-                            while (*wp > 'a' && mch_stat((char *)backup, &st_new) >= 0)
+                            while (*wp > 'a' &&  stat(((char *)backup), (&st_new))  >= 0)
                             {
                                 --*wp;
                             }
                             if (*wp == 'a')
                             {
-                                VIM_CLEAR(backup);
+                                 vim_free(backup);
+                                 (backup) = NULL;
                             }
                         }
                     }
@@ -18378,13 +16719,14 @@
 
                 if (backup != NULL)
                 {
-                    mch_remove(backup);
+                     unlink((char *)(backup)) ;
                     umask_save = umask(0);
-                    bfd = mch_open((char *)backup, O_WRONLY|O_CREAT|O_EXTRA|O_EXCL|O_NOFOLLOW, perm & 0777);
+                    bfd =  open(((char *)backup), (O_WRONLY|O_CREAT|O_EXTRA|O_EXCL|O_NOFOLLOW), (perm & 0777)) ;
                     (void)umask(umask_save);
                     if (bfd < 0)
                     {
-                        VIM_CLEAR(backup);
+                         vim_free(backup);
+                         (backup) = NULL;
                     }
                     else
                     {
@@ -18456,9 +16798,9 @@
             dirp = p_bdir;
             while (*dirp)
             {
-                int IObufflen UNUSED;
+                int IObufflen  __attribute__((unused)) ;
 
-                IObufflen = copy_option_part(&dirp, IObuff, IOSIZE, ",");
+                IObufflen = copy_option_part(&dirp, IObuff,  (1024+1) , ",");
 
                 p = IObuff + IObufflen;
                 if (after_pathsep(IObuff, p) && p[-1] == p[-2])
@@ -18487,7 +16829,7 @@
                 {
                     if (!p_bk && mch_getperm(backup) >= 0)
                     {
-                        p = backup + STRLEN(backup) - 1 - STRLEN(backup_ext);
+                        p = backup +  strlen((char *)(backup))  - 1 -  strlen((char *)(backup_ext)) ;
                         if (p < backup)
                         {
                             p = backup;
@@ -18499,19 +16841,20 @@
                         }
                         if (*p == 'a')
                         {
-                            VIM_CLEAR(backup);
+                             vim_free(backup);
+                             (backup) = NULL;
                         }
                     }
                 }
                 if (backup != NULL)
                 {
-
                     if (vim_rename(fname, backup) == 0)
                     {
                         break;
                     }
 
-                    VIM_CLEAR(backup);
+                     vim_free(backup);
+                     (backup) = NULL;
                 }
             }
             if (backup == NULL && !forceit)
@@ -18570,7 +16913,7 @@
 
     converted = need_conversion(fenc);
 
-    if (converted && (enc_utf8 || STRCMP(p_enc, "latin1") == 0))
+    if (converted && (enc_utf8 ||  strcmp((char *)(p_enc), (char *)("latin1"))  == 0))
     {
         wb_flags = get_fio_flags(fenc);
         if (wb_flags & (FIO_UCS2 | FIO_UCS4 | FIO_UTF16 | FIO_UTF8))
@@ -18631,14 +16974,13 @@
         }
         else
         {
-# define TRUNC_ON_OPEN 0
-            while ((fd = mch_open((char *)wfname, O_WRONLY | O_EXTRA | (append ? (forceit ? (O_APPEND | O_CREAT) : O_APPEND) : (O_CREAT | TRUNC_ON_OPEN)), perm < 0 ? 0666 : (perm & 0777))) < 0)
+            while ((fd =  open(((char *)wfname), (O_WRONLY | O_EXTRA | (append ? (forceit ? (O_APPEND | O_CREAT) : O_APPEND) : (O_CREAT | TRUNC_ON_OPEN))), (perm < 0 ? 0666 : (perm & 0777))) ) < 0)
             {
                 if (errmsg == NULL)
                 {
                     stat_T      st;
 
-                    if ((!newfile && st_old.st_nlink > 1) || (mch_lstat((char *)fname, &st) == 0 && (st.st_dev != st_old.st_dev || st.st_ino != st_old.st_ino)))
+                    if ((!newfile && st_old.st_nlink > 1) || ( lstat(((char *)fname), (&st))  == 0 && (st.st_dev != st_old.st_dev || st.st_ino != st_old.st_ino)))
                     {
                         errmsg =
                               (char_u *)_(e_cant_open_linked_file_for_writing);
@@ -18659,7 +17001,7 @@
                             }
                             if (!append)
                             {
-                                mch_remove(wfname);
+                                 unlink((char *)(wfname)) ;
                             }
                             continue;
                         }
@@ -18674,14 +17016,13 @@
                     {
                         if (backup_copy)
                         {
-
-                            if (mch_stat((char *)fname, &st) < 0)
+                            if ( stat(((char *)fname), (&st))  < 0)
                             {
                                 vim_rename(backup, fname);
                             }
-                            if (mch_stat((char *)fname, &st) >= 0)
+                            if ( stat(((char *)fname), (&st))  >= 0)
                             {
-                                mch_remove(backup);
+                                 unlink((char *)(backup)) ;
                             }
                         }
                         else
@@ -18690,7 +17031,7 @@
                         }
                     }
 
-                    if (!newfile && mch_stat((char *)fname, &st) < 0)
+                    if (!newfile &&  stat(((char *)fname), (&st))  < 0)
                     {
                         end = 0;
                     }
@@ -18707,7 +17048,7 @@
             {
                 stat_T  st;
 
-                if (overwriting && (!dobackup || backup_copy) && fname == wfname && perm >= 0 && mch_fstat(fd, &st) == 0 && st.st_ino != st_old.st_ino)
+                if (overwriting && (!dobackup || backup_copy) && fname == wfname && perm >= 0 &&  fstat((fd), (&st))  == 0 && st.st_ino != st_old.st_ino)
                 {
                     close(fd);
                     errmsg = (char_u *)_(e_file_changed_while_writing);
@@ -18872,7 +17213,7 @@
         {
             stat_T      st;
 
-            if (mch_stat((char *)wfname, &st) < 0 || st.st_uid != st_old.st_uid || st.st_gid != st_old.st_gid)
+            if ( stat(((char *)wfname), (&st))  < 0 || st.st_uid != st_old.st_uid || st.st_gid != st_old.st_gid)
             {
                 vim_ignored = fchown(fd, st_old.st_uid, -1);
                 if (fchown(fd, -1, st_old.st_gid) == -1 && perm > 0)
@@ -18948,9 +17289,9 @@
                     msg(_(e_interrupted));
                     out_flush();
                 }
-                if ((fd = mch_open((char *)backup, O_RDONLY | O_EXTRA, 0)) >= 0)
+                if ((fd =  open(((char *)backup), (O_RDONLY | O_EXTRA), (0)) ) >= 0)
                 {
-                    if ((write_info.bw_fd = mch_open((char *)fname, O_WRONLY | O_CREAT | O_TRUNC | O_EXTRA, perm & 0777)) >= 0)
+                    if ((write_info.bw_fd =  open(((char *)fname), (O_WRONLY | O_CREAT | O_TRUNC | O_EXTRA), (perm & 0777)) ) >= 0)
                     {
                         write_info.bw_buf = smallbuf;
                         write_info.bw_flags = FIO_NOCONVERT;
@@ -18990,31 +17331,31 @@
         c = FALSE;
         if (write_info.bw_conv_error)
         {
-            STRCAT(IObuff, _(" CONVERSION ERROR"));
+             strcat((char *)(IObuff), (char *)(_(" CONVERSION ERROR"))) ;
             c = TRUE;
             if (write_info.bw_conv_error_lnum != 0)
             {
-                vim_snprintf_add((char *)IObuff, IOSIZE, _(" in line %ld;"), (long)write_info.bw_conv_error_lnum);
+                vim_snprintf_add((char *)IObuff,  (1024+1) , _(" in line %ld;"), (long)write_info.bw_conv_error_lnum);
             }
         }
         else if (notconverted)
         {
-            STRCAT(IObuff, _("[NOT converted]"));
+             strcat((char *)(IObuff), (char *)(_("[NOT converted]"))) ;
             c = TRUE;
         }
         else if (converted)
         {
-            STRCAT(IObuff, _("[converted]"));
+             strcat((char *)(IObuff), (char *)(_("[converted]"))) ;
             c = TRUE;
         }
         if (device)
         {
-            STRCAT(IObuff, _("[Device]"));
+             strcat((char *)(IObuff), (char *)(_("[Device]"))) ;
             c = TRUE;
         }
         else if (newfile)
         {
-            STRCAT(IObuff, new_file_message());
+             strcat((char *)(IObuff), (char *)(new_file_message())) ;
             c = TRUE;
         }
         if (no_eol)
@@ -19031,11 +17372,11 @@
         {
             if (append)
             {
-                STRCAT(IObuff, shortmess(SHM_WRI) ? _(" [a]") : _(" appended"));
+                 strcat((char *)(IObuff), (char *)(shortmess(SHM_WRI) ? _(" [a]") : _(" appended"))) ;
             }
             else
             {
-                STRCAT(IObuff, shortmess(SHM_WRI) ? _(" [w]") : _(" written"));
+                 strcat((char *)(IObuff), (char *)(shortmess(SHM_WRI) ? _(" [w]") : _(" written"))) ;
             }
         }
 
@@ -19045,9 +17386,9 @@
     if (reset_changed && whole && !append && !write_info.bw_conv_error && (overwriting || vim_strchr(p_cpo, CPO_PLUS) != NULL))
     {
         unchanged(buf, TRUE, FALSE);
-        if (buf->b_last_changedtick + 1 == CHANGEDTICK(buf))
+        if (buf->b_last_changedtick + 1 ==  ((buf)->b_ct_di.di_tv.vval.v_number) )
         {
-            buf->b_last_changedtick = CHANGEDTICK(buf);
+            buf->b_last_changedtick =  ((buf)->b_ct_di.di_tv.vval.v_number) ;
         }
         u_unchanged(buf);
         u_update_save_nr(buf);
@@ -19062,7 +17403,7 @@
         }
         else
         {
-            buf->b_flags &= ~BF_WRITE_MASK;
+            buf->b_flags &= ~ (BF_NOTEDITED + BF_NEW + BF_READERR) ;
         }
     }
 
@@ -19078,10 +17419,11 @@
             {
                 emsg(_(e_patchmode_cant_save_original_file));
             }
-            else if (mch_stat(org, &st) < 0)
+            else if ( stat((org), (&st))  < 0)
             {
                 vim_rename(backup, (char_u *)org);
-                VIM_CLEAR(backup);
+                 vim_free(backup);
+                 (backup) = NULL;
                 set_file_time((char_u *)org, st_old.st_atime, st_old.st_mtime);
             }
         }
@@ -19089,7 +17431,7 @@
         {
             int empty_fd;
 
-            if (org == NULL || (empty_fd = mch_open(org, O_CREAT | O_EXTRA | O_EXCL | O_NOFOLLOW, perm < 0 ? 0666 : (perm & 0777))) < 0)
+            if (org == NULL || (empty_fd =  open((org), (O_CREAT | O_EXTRA | O_EXCL | O_NOFOLLOW), (perm < 0 ? 0666 : (perm & 0777))) ) < 0)
             {
               emsg(_(e_patchmode_cant_touch_empty_original_file));
             }
@@ -19105,7 +17447,7 @@
         }
     }
 
-    if (!p_bk && backup != NULL && !write_info.bw_conv_error && mch_remove(backup) != 0)
+    if (!p_bk && backup != NULL && !write_info.bw_conv_error &&  unlink((char *)(backup))  != 0)
     {
         emsg(_(e_cant_delete_backup_file));
     }
@@ -19134,20 +17476,20 @@
 
     if (errmsg != NULL)
     {
-        int numlen = errnum != NULL ? (int)STRLEN(errnum) : 0;
+        int numlen = errnum != NULL ? (int) strlen((char *)(errnum))  : 0;
 
-        attr = HL_ATTR(HLF_E);
+        attr =  highlight_attr[(int)(HLF_E)] ;
         msg_add_fname(buf, fname);
-        if (STRLEN(IObuff) + STRLEN(errmsg) + numlen >= IOSIZE)
+        if ( strlen((char *)(IObuff))  +  strlen((char *)(errmsg))  + numlen >=  (1024+1) )
         {
-            IObuff[IOSIZE - STRLEN(errmsg) - numlen - 1] = NUL;
+            IObuff[ (1024+1)  -  strlen((char *)(errmsg))  - numlen - 1] = NUL;
         }
         if (errnum != NULL)
         {
-            STRMOVE(IObuff + numlen, IObuff);
-            mch_memmove(IObuff, errnum, (size_t)numlen);
+              memmove((char *)((IObuff + numlen)), (char *)((IObuff)),  strlen((char *)(IObuff))  + 1)  ;
+             memmove((char *)(IObuff), (char *)(errnum), (size_t)numlen) ;
         }
-        STRCAT(IObuff, errmsg);
+         strcat((char *)(IObuff), (char *)(errmsg)) ;
         emsg((char *)IObuff);
         if (errmsg_allocated)
         {
@@ -19160,7 +17502,7 @@
             msg_puts_attr(_("\nWARNING: Original file may be lost or damaged\n"), attr | MSG_HIST);
             msg_puts_attr(_("don't quit the editor until the file is successfully written!"), attr | MSG_HIST);
 
-            if (mch_stat((char *)fname, &st_old) >= 0)
+            if ( stat(((char *)fname), (&st_old))  >= 0)
             {
                 buf_store_time(buf, &st_old, fname);
                 buf->b_mtime_read = buf->b_mtime;
@@ -19211,7 +17553,7 @@
     void
 change_warning(int col)
 {
-    static char *w_readonly = N_("W10: Warning: Changing a readonly file");
+    static char *w_readonly =  "W10: Warning: Changing a readonly file" ;
 
     if (curbuf->b_did_warn || curbufIsChanged() || autocmd_busy || !curbuf->b_p_ro)
     {
@@ -19231,8 +17573,8 @@
     {
         msg_col = col;
     }
-    msg_source(HL_ATTR(HLF_W));
-    msg_puts_attr(_(w_readonly), HL_ATTR(HLF_W) | MSG_HIST);
+    msg_source( highlight_attr[(int)(HLF_W)] );
+    msg_puts_attr(_(w_readonly),  highlight_attr[(int)(HLF_W)]  | MSG_HIST);
     msg_clr_eos();
     (void)msg_end();
     if (msg_silent == 0 && !silent_mode)
@@ -19251,7 +17593,6 @@
     void
 changed(void)
 {
-
     if (!curbuf->b_changed)
     {
         int     save_msg_scroll = msg_scroll;
@@ -19279,7 +17620,7 @@
         }
         changed_internal();
     }
-    ++CHANGEDTICK(curbuf);
+    ++ ((curbuf)->b_ct_di.di_tv.vval.v_number) ;
 
     highlight_match = FALSE;
 }
@@ -19341,22 +17682,28 @@
                 if (curbuf->b_changelistlen == JUMPLISTSIZE)
                 {
                     curbuf->b_changelistlen = JUMPLISTSIZE - 1;
-                    mch_memmove(curbuf->b_changelist, curbuf->b_changelist + 1, sizeof(pos_T) * (JUMPLISTSIZE - 1));
-                    FOR_ALL_TAB_WINDOWS(tp, wp)
+                     memmove((char *)(curbuf->b_changelist), (char *)(curbuf->b_changelist + 1), sizeof(pos_T) * (JUMPLISTSIZE - 1)) ;
+                     for ((tp) = first_tabpage; (tp) != NULL; (tp) = (tp)->tp_next)
+                     {
+                         for ((wp) = ((tp) == curtab)            ? firstwin : (tp)->tp_firstwin; (wp); (wp) = (wp)->w_next)
                     {
                         if (wp->w_buffer == curbuf && wp->w_changelistidx > 0)
                         {
                             --wp->w_changelistidx;
                         }
                     }
+                     }
                 }
-                FOR_ALL_TAB_WINDOWS(tp, wp)
+                 for ((tp) = first_tabpage; (tp) != NULL; (tp) = (tp)->tp_next)
+                 {
+                     for ((wp) = ((tp) == curtab)            ? firstwin : (tp)->tp_firstwin; (wp); (wp) = (wp)->w_next)
                 {
                     if (wp->w_buffer == curbuf && wp->w_changelistidx == curbuf->b_changelistlen)
                     {
                         ++wp->w_changelistidx;
                     }
                 }
+                 }
                 ++curbuf->b_changelistlen;
             }
         }
@@ -19370,7 +17717,9 @@
         check_visual_pos();
     }
 
-    FOR_ALL_TAB_WINDOWS(tp, wp)
+     for ((tp) = first_tabpage; (tp) != NULL; (tp) = (tp)->tp_next)
+     {
+         for ((wp) = ((tp) == curtab)            ? firstwin : (tp)->tp_firstwin; (wp); (wp) = (wp)->w_next)
     {
         if (wp->w_buffer == curbuf)
         {
@@ -19429,7 +17778,7 @@
                 }
             }
 
-            if (wp->w_p_rnu && xtra != 0)
+            if (wp-> w_onebuf_opt.wo_rnu  && xtra != 0)
             {
                 wp->w_last_cursor_lnum_rnu = 0;
             }
@@ -19440,6 +17789,7 @@
             search_hl_has_cursor_lnum += xtra;
         }
     }
+     }
 
     set_must_redraw(UPD_VALID);
 
@@ -19481,9 +17831,8 @@
 }
 
     void
-inserted_bytes(linenr_T lnum, colnr_T col, int added UNUSED)
+inserted_bytes(linenr_T lnum, colnr_T col, int added  __attribute__((unused)) )
 {
-
     changed_bytes(lnum, col);
 }
 
@@ -19496,7 +17845,7 @@
     void
 appended_lines_mark(linenr_T lnum, long count)
 {
-    mark_adjust(lnum + 1, (linenr_T)MAXLNUM, count, 0L);
+    mark_adjust(lnum + 1, (linenr_T) LONG_MAX , count, 0L);
     changed_lines(lnum + 1, 0, lnum + 1, count);
 }
 
@@ -19509,7 +17858,7 @@
     void
 deleted_lines_mark(linenr_T lnum, long count)
 {
-    mark_adjust(lnum, (linenr_T)(lnum + count - 1), (long)MAXLNUM, -count);
+    mark_adjust(lnum, (linenr_T)(lnum + count - 1), (long) LONG_MAX , -count);
     changed_lines(lnum, 0, lnum + count, -count);
 }
 
@@ -19567,11 +17916,11 @@
         check_status(buf);
         redraw_tabline = TRUE;
         need_maketitle = TRUE;
-        ++CHANGEDTICK(buf);
+        ++ ((buf)->b_ct_di.di_tv.vval.v_number) ;
     }
     else if (always_inc_changedtick)
     {
-        ++CHANGEDTICK(buf);
+        ++ ((buf)->b_ct_di.di_tv.vval.v_number) ;
     }
 }
 
@@ -19583,7 +17932,7 @@
     buf->b_start_eol = buf->b_p_eol;
     buf->b_start_bomb = buf->b_p_bomb;
 
-    if (buf->b_start_fenc == NULL || STRCMP(buf->b_start_fenc, buf->b_p_fenc) != 0)
+    if (buf->b_start_fenc == NULL ||  strcmp((char *)(buf->b_start_fenc), (char *)(buf->b_p_fenc))  != 0)
     {
         vim_free(buf->b_start_fenc);
         buf->b_start_fenc = vim_strsave(buf->b_p_fenc);
@@ -19617,13 +17966,13 @@
     {
         return (*buf->b_p_fenc != NUL);
     }
-    return (STRCMP(buf->b_start_fenc, buf->b_p_fenc) != 0);
+    return ( strcmp((char *)(buf->b_start_fenc), (char *)(buf->b_p_fenc))  != 0);
 }
 
     void
 ins_bytes(char_u *p)
 {
-    ins_bytes_len(p, (int)STRLEN(p));
+    ins_bytes_len(p, (int) strlen((char *)(p)) );
 }
 
     void
@@ -19704,10 +18053,10 @@
             colnr_T     vcol;
             int         old_list;
 
-            old_list = curwin->w_p_list;
+            old_list = curwin-> w_onebuf_opt.wo_list ;
             if (old_list && vim_strchr(p_cpo, CPO_LISTWM) == NULL)
             {
-                curwin->w_p_list = FALSE;
+                curwin-> w_onebuf_opt.wo_list  = FALSE;
             }
 
             getvcol(curwin, &curwin->w_cursor, NULL, &vcol, NULL, 0);
@@ -19725,7 +18074,7 @@
                     newlen += vcol - new_vcol;
                 }
             }
-            curwin->w_p_list = old_list;
+            curwin-> w_onebuf_opt.wo_list  = old_list;
         }
         else if (oldp[col] != NUL)
         {
@@ -19754,16 +18103,16 @@
 
     if (col > 0)
     {
-        mch_memmove(newp, oldp, (size_t)col);
+         memmove((char *)(newp), (char *)(oldp), (size_t)col) ;
     }
 
     p = newp + col;
     if (linelen > col + oldlen)
     {
-        mch_memmove(p + newlen, oldp + col + oldlen, (size_t)(linelen - col - oldlen));
+         memmove((char *)(p + newlen), (char *)(oldp + col + oldlen), (size_t)(linelen - col - oldlen)) ;
     }
 
-    mch_memmove(p, buf, charlen);
+     memmove((char *)(p), (char *)(buf), charlen) ;
     i = charlen;
 
     while (i < newlen)
@@ -19818,10 +18167,10 @@
     }
     if (col > 0)
     {
-        mch_memmove(newp, oldp, (size_t)col);
+         memmove((char *)(newp), (char *)(oldp), (size_t)col) ;
     }
-    mch_memmove(newp + col, s, slen);
-    mch_memmove(newp + col + slen, oldp + col, (size_t)(oldlen - col + 1));
+     memmove((char *)(newp + col), (char *)(s), slen) ;
+     memmove((char *)(newp + col + slen), (char *)(oldp + col), (size_t)(oldlen - col + 1)) ;
     ml_replace(lnum, newp, FALSE);
     inserted_bytes(lnum, col, (int)slen);
     curwin->w_cursor.col += (colnr_T)slen;
@@ -19861,7 +18210,7 @@
 }
 
     int
-del_bytes(long        count, int         fixpos_arg, int         use_delcombine UNUSED)
+del_bytes(long        count, int         fixpos_arg, int         use_delcombine  __attribute__((unused)) )
 {
     char_u *oldp;
     char_u *newp;
@@ -19906,7 +18255,7 @@
                 col = n;
                 count = utf_ptr2len(oldp + n);
                 n += count;
-            } while (UTF_COMPOSINGLIKE(oldp + col, oldp + n));
+            } while ( utf_iscomposing(utf_ptr2char(oldp + n)) );
             fixpos = 0;
         }
     }
@@ -19941,9 +18290,9 @@
         {
             return FAIL;
         }
-        mch_memmove(newp, oldp, (size_t)col);
+         memmove((char *)(newp), (char *)(oldp), (size_t)col) ;
     }
-    mch_memmove(newp + col, oldp + col + count, (size_t)movelen);
+     memmove((char *)(newp + col), (char *)(oldp + col + count), (size_t)movelen) ;
     if (alloc_newp)
     {
         ml_replace(lnum, newp, FALSE);
@@ -19960,7 +18309,7 @@
 }
 
     int
-open_line(int         dir, int         flags, int         second_line_indent, int         *did_do_comment UNUSED)
+open_line(int         dir, int         flags, int         second_line_indent, int         *did_do_comment  __attribute__((unused)) )
 {
     char_u      *saved_line;
     char_u      *next_line = NULL;
@@ -20036,7 +18385,7 @@
             p = skipwhite(p_extra);
             first_char = *p;
         }
-        extra_len = (int)STRLEN(p_extra);
+        extra_len = (int) strlen((char *)(p_extra)) ;
         saved_char = *p_extra;
         *p_extra = NUL;
     }
@@ -20123,8 +18472,8 @@
                 }
                 else
                 {
-                    p = ptr + STRLEN(ptr) - 1;
-                    while (p > ptr && VIM_ISWHITE(*p))
+                    p = ptr +  strlen((char *)(ptr))  - 1;
+                    while (p > ptr &&  ((*p) == ' ' || (*p) == '\t') )
                     {
                         --p;
                     }
@@ -20136,7 +18485,7 @@
                         {
                             --p;
                         }
-                        while (p > ptr && VIM_ISWHITE(*p))
+                        while (p > ptr &&  ((*p) == ' ' || (*p) == '\t') )
                         {
                             --p;
                         }
@@ -20170,7 +18519,7 @@
 
                     while ((ptr[0] == '#' || was_backslashed) && curwin->w_cursor.lnum < curbuf->b_ml.ml_line_count)
                     {
-                        if (*ptr && ptr[STRLEN(ptr) - 1] == '\\')
+                        if (*ptr && ptr[ strlen((char *)(ptr))  - 1] == '\\')
                         {
                             was_backslashed = TRUE;
                         }
@@ -20216,7 +18565,7 @@
     end_comment_pending = NUL;
     if (flags & OPENLINE_DO_COM)
     {
-        lead_len = get_leader_len(saved_line, &lead_flags, dir == BACKWARD, TRUE);
+        lead_len = get_leader_len(saved_line, &lead_flags, dir ==  (-1) , TRUE);
         if (lead_len == 0 && curbuf->b_p_cin && do_cindent && dir == FORWARD && (!has_format_option(FO_NO_OPEN_COMS) || (flags & OPENLINE_FORMAT)))
         {
             comment_start = check_linecomment(saved_line);
@@ -20263,7 +18612,7 @@
                 current_flag = *p;
                 if (*p == COM_START)
                 {
-                    if (dir == BACKWARD)
+                    if (dir ==  (-1) )
                     {
                         lead_len = 0;
                         break;
@@ -20302,7 +18651,7 @@
                 {
                     for (p = saved_line + lead_len; *p; ++p)
                     {
-                        if (STRNCMP(p, lead_end, n) == 0)
+                        if ( strncmp((char *)(p), (char *)(lead_end), (n))  == 0)
                         {
                             comment_end = p;
                             lead_len = 0;
@@ -20319,7 +18668,7 @@
                         lead_repl_len = lead_middle_len;
                     }
 
-                    if (!VIM_ISWHITE(saved_line[lead_len - 1]) && ((p_extra != NULL && (int)curwin->w_cursor.col == lead_len) || (p_extra == NULL && saved_line[lead_len] == NUL) || require_blank))
+                    if (! ((saved_line[lead_len - 1]) == ' ' || (saved_line[lead_len - 1]) == '\t')  && ((p_extra != NULL && (int)curwin->w_cursor.col == lead_len) || (p_extra == NULL && saved_line[lead_len] == NUL) || require_blank))
                     {
                         extra_space = TRUE;
                     }
@@ -20366,7 +18715,7 @@
             }
             if (*p == COM_FIRST)
             {
-                if (dir == BACKWARD)
+                if (dir ==  (-1) )
                 {
                     lead_len = 0;
                 }
@@ -20395,7 +18744,7 @@
 
                 for (li = 0; li < comment_start; ++li)
                 {
-                    if (!VIM_ISWHITE(leader[li]))
+                    if (! ((leader[li]) == ' ' || (leader[li]) == '\t') )
                     {
                         leader[li] = ' ';
                     }
@@ -20412,7 +18761,7 @@
                         {
                             c = *p++;
                         }
-                        else if (VIM_ISDIGIT(*p) || *p == '-')
+                        else if ( ((unsigned)(*p) - '0' < 10)  || *p == '-')
                         {
                             off = getdigits(&p);
                         }
@@ -20423,7 +18772,7 @@
                     }
                     if (c == COM_RIGHT)
                     {
-                        for (p = leader + lead_len - 1; p > leader && VIM_ISWHITE(*p); --p)
+                        for (p = leader + lead_len - 1; p > leader &&  ((*p) == ' ' || (*p) == '\t') ; --p)
                         {
                             ;
                         }
@@ -20437,17 +18786,17 @@
 
                             while (old_size < repl_size && p > leader)
                             {
-                                MB_PTR_BACK(leader, p);
+                                 p -= has_mbyte ? ((*mb_head_off)(leader, (p) - 1) + 1) : 1 ;
                                 old_size += ptr2cells(p);
                             }
                             l = lead_repl_len - (int)(endp - p);
                             if (l != 0)
                             {
-                                mch_memmove(endp + l, endp, (size_t)((leader + lead_len) - endp));
+                                 memmove((char *)(endp + l), (char *)(endp), (size_t)((leader + lead_len) - endp)) ;
                             }
                             lead_len += l;
                         }
-                        mch_memmove(p, lead_repl, (size_t)lead_repl_len);
+                         memmove((char *)(p), (char *)(lead_repl), (size_t)lead_repl_len) ;
                         if (p + lead_repl_len > leader + lead_len)
                         {
                             p[lead_repl_len] = NUL;
@@ -20465,11 +18814,11 @@
                                     p[1] = ' ';
                                     --l;
                                 }
-                                mch_memmove(p + 1, p + l + 1, (size_t)((leader + lead_len) - (p + l + 1)));
+                                 memmove((char *)(p + 1), (char *)(p + l + 1), (size_t)((leader + lead_len) - (p + l + 1))) ;
                                 lead_len -= l;
                                 *p = ' ';
                             }
-                            else if (!VIM_ISWHITE(*p))
+                            else if (! ((*p) == ' ' || (*p) == '\t') )
                             {
                                 *p = ' ';
                             }
@@ -20494,20 +18843,20 @@
                             }
                             if (i != lead_repl_len)
                             {
-                                mch_memmove(p + lead_repl_len, p + i, (size_t)(lead_len - i - (p - leader)));
+                                 memmove((char *)(p + lead_repl_len), (char *)(p + i), (size_t)(lead_len - i - (p - leader))) ;
                                 lead_len += lead_repl_len - i;
                             }
                         }
-                        mch_memmove(p, lead_repl, (size_t)lead_repl_len);
+                         memmove((char *)(p), (char *)(lead_repl), (size_t)lead_repl_len) ;
 
                         for (p += lead_repl_len; p < leader + lead_len; ++p)
                         {
-                            if (!VIM_ISWHITE(*p))
+                            if (! ((*p) == ' ' || (*p) == '\t') )
                             {
                                 if (p + 1 < leader + lead_len && p[1] == TAB)
                                 {
                                     --lead_len;
-                                    mch_memmove(p, p + 1, (leader + lead_len) - p);
+                                     memmove((char *)(p), (char *)(p + 1), (leader + lead_len) - p) ;
                                 }
                                 else
                                 {
@@ -20520,7 +18869,7 @@
                                             --l;
                                             *p++ = ' ';
                                         }
-                                        mch_memmove(p + 1, p + l, (leader + lead_len) - p);
+                                         memmove((char *)(p + 1), (char *)(p + l), (leader + lead_len) - p) ;
                                         lead_len -= l - 1;
                                     }
                                     *p = ' ';
@@ -20555,7 +18904,7 @@
                         --off;
                     }
 
-                    if (lead_len > 0 && VIM_ISWHITE(leader[lead_len - 1]))
+                    if (lead_len > 0 &&  ((leader[lead_len - 1]) == ' ' || (leader[lead_len - 1]) == '\t') )
                     {
                         extra_space = FALSE;
                     }
@@ -20572,7 +18921,7 @@
 
                 if (newindent || did_si)
                 {
-                    while (lead_len && VIM_ISWHITE(*leader))
+                    while (lead_len &&  ((*leader) == ' ' || (*leader) == '\t') )
                     {
                         --lead_len;
                         --newcol;
@@ -20603,7 +18952,7 @@
     {
         *p_extra = saved_char;
 
-        if (REPLACE_NORMAL(State))
+        if ( (((State) & REPLACE_FLAG) && !((State) & VREPLACE_FLAG)) )
         {
             replace_push(NUL);
         }
@@ -20611,7 +18960,7 @@
         {
             while ((*p_extra == ' ' || *p_extra == '\t') && (!enc_utf8 || !utf_iscomposing(utf_ptr2char(p_extra + 1))))
             {
-                if (REPLACE_NORMAL(State))
+                if ( (((State) & REPLACE_FLAG) && !((State) & VREPLACE_FLAG)) )
                 {
                     replace_push(*p_extra);
                 }
@@ -20634,16 +18983,16 @@
         {
             int i;
             int padding = second_line_indent
-                                          - (newindent + (int)STRLEN(leader));
+                                          - (newindent + (int) strlen((char *)(leader)) );
 
             for (i = 0; i < padding; i++)
             {
-                STRCAT(leader, " ");
+                 strcat((char *)(leader), (char *)(" ")) ;
                 less_cols--;
                 newcol++;
             }
         }
-        STRCAT(leader, p_extra);
+         strcat((char *)(leader), (char *)(p_extra)) ;
         p_extra = leader;
         did_ai = TRUE;
         less_cols -= lead_len;
@@ -20654,7 +19003,7 @@
     }
 
     old_cursor = curwin->w_cursor;
-    if (dir == BACKWARD)
+    if (dir ==  (-1) )
     {
         --curwin->w_cursor.lnum;
     }
@@ -20664,7 +19013,7 @@
         {
             goto theend;
         }
-        mark_adjust(curwin->w_cursor.lnum + 1, (linenr_T)MAXLNUM, 1L, 0L);
+        mark_adjust(curwin->w_cursor.lnum + 1, (linenr_T) LONG_MAX , 1L, 0L);
         did_append = TRUE;
     }
     else
@@ -20708,7 +19057,7 @@
 
         ai_col = curwin->w_cursor.col;
 
-        if (REPLACE_NORMAL(State))
+        if ( (((State) & REPLACE_FLAG) && !((State) & VREPLACE_FLAG)) )
         {
             for (n = 0; n < (int)curwin->w_cursor.col; ++n)
             {
@@ -20722,7 +19071,7 @@
         }
     }
 
-    if (REPLACE_NORMAL(State))
+    if ( (((State) & REPLACE_FLAG) && !((State) & VREPLACE_FLAG)) )
     {
         while (lead_len-- > 0)
         {
@@ -20907,16 +19256,12 @@
 
 static int    chartab_initialized = FALSE;
 
-#define SET_CHARTAB(buf, c) (buf)->b_chartab[(unsigned)(c) >> 3] |= (1 << ((c) & 0x7))
-#define RESET_CHARTAB(buf, c) (buf)->b_chartab[(unsigned)(c) >> 3] &= ~(1 << ((c) & 0x7))
-#define GET_CHARTAB(buf, c) ((buf)->b_chartab[(unsigned)(c) >> 3] & (1 << ((c) & 0x7)))
-
 static char_u   g_chartab[256];
 
-#define CT_CELL_MASK    0x07
-#define CT_PRINT_CHAR   0x10
-#define CT_ID_CHAR      0x20
-#define CT_FNAME_CHAR   0x40
+enum { CT_CELL_MASK = 0x07 };
+enum { CT_PRINT_CHAR = 0x10 };
+enum { CT_ID_CHAR = 0x20 };
+enum { CT_FNAME_CHAR = 0x40 };
 
 static int in_win_border(win_T *wp, colnr_T vcol);
 
@@ -20954,7 +19299,7 @@
             {
                 g_chartab[c++] = CT_PRINT_CHAR + 1;
             }
-            else if (enc_dbcs != 0 && MB_BYTE2LEN(c) == 2)
+            else if (enc_dbcs != 0 &&  mb_bytelen_tab[c]  == 2)
             {
                 g_chartab[c++] = CT_PRINT_CHAR + 2;
             }
@@ -20966,28 +19311,28 @@
 
         for (c = 1; c < 256; ++c)
         {
-            if ((enc_dbcs != 0 && MB_BYTE2LEN(c) > 1) || (enc_dbcs == DBCS_JPNU && c == 0x8e) || (enc_utf8 && c >= 0xa0))
+            if ((enc_dbcs != 0 &&  mb_bytelen_tab[c]  > 1) || (enc_dbcs == DBCS_JPNU && c == 0x8e) || (enc_utf8 && c >= 0xa0))
             {
                 g_chartab[c] |= CT_FNAME_CHAR;
             }
         }
     }
 
-    CLEAR_FIELD(buf->b_chartab);
+      memset((&(buf->b_chartab)), (0), (sizeof(buf->b_chartab)))  ;
     if (enc_dbcs != 0)
     {
         for (c = 0; c < 256; ++c)
         {
-            if (MB_BYTE2LEN(c) == 2)
+            if ( mb_bytelen_tab[c]  == 2)
             {
-                SET_CHARTAB(buf, c);
+                 (buf)->b_chartab[(unsigned)(c) >> 3] |= (1 << ((c) & 0x7)) ;
             }
         }
     }
 
     if (buf->b_p_lisp)
     {
-        SET_CHARTAB(buf, '-');
+         (buf)->b_chartab[(unsigned)('-') >> 3] |= (1 << (('-') & 0x7)) ;
     }
 
     for (i = global ? 0 : 3; i <= 3; ++i)
@@ -21044,7 +19389,7 @@
             tilde = TRUE;
             ++p;
         }
-        if (VIM_ISDIGIT(*p))
+        if ( ((unsigned)(*p) - '0' < 10) )
         {
             c = getdigits(&p);
         }
@@ -21060,7 +19405,7 @@
         if (*p == '-' && p[1] != NUL)
         {
             ++p;
-            if (VIM_ISDIGIT(*p))
+            if ( ((unsigned)(*p) - '0' < 10) )
             {
                 c2 = getdigits(&p);
             }
@@ -21106,7 +19451,7 @@
 
         while (c <= c2)
         {
-            if (!do_isalpha || MB_ISLOWER(c) || MB_ISUPPER(c))
+            if (!do_isalpha ||  vim_islower(c)  ||  vim_isupper(c) )
             {
                 if (var == p_isi)
                 {
@@ -21121,7 +19466,7 @@
                 }
                 else if (var == p_isp)
                 {
-                    if ((c < ' ' || c > '~') && !(enc_dbcs && MB_BYTE2LEN(c) == 2))
+                    if ((c < ' ' || c > '~') && !(enc_dbcs &&  mb_bytelen_tab[c]  == 2))
                     {
                         if (tilde)
                         {
@@ -21151,11 +19496,11 @@
                 {
                     if (tilde)
                     {
-                        RESET_CHARTAB(buf, c);
+                         (buf)->b_chartab[(unsigned)(c) >> 3] &= ~(1 << ((c) & 0x7)) ;
                     }
                     else
                     {
-                        SET_CHARTAB(buf, c);
+                         (buf)->b_chartab[(unsigned)(c) >> 3] |= (1 << ((c) & 0x7)) ;
                     }
                 }
             }
@@ -21174,7 +19519,7 @@
     char_u      *trs;
     int         trs_len;
 
-    len = (int)STRLEN(buf);
+    len = (int) strlen((char *)(buf)) ;
     room = bufsize - len;
     while (*buf != 0)
     {
@@ -21185,7 +19530,7 @@
         else
         {
             trs = transchar_byte(*buf);
-            trs_len = (int)STRLEN(trs);
+            trs_len = (int) strlen((char *)(trs)) ;
             if (trs_len > 1)
             {
                 room -= trs_len - 1;
@@ -21193,9 +19538,9 @@
                 {
                     return;
                 }
-                mch_memmove(buf + trs_len, buf + 1, (size_t)len);
+                 memmove((char *)(buf + trs_len), (char *)(buf + 1), (size_t)len) ;
             }
-            mch_memmove(buf, trs, (size_t)trs_len);
+             memmove((char *)(buf), (char *)(trs), (size_t)trs_len) ;
             --len;
         }
         buf += trs_len;
@@ -21229,7 +19574,7 @@
                 else
                 {
                     transchar_hex(hexbuf, c);
-                    len += (int)STRLEN(hexbuf);
+                    len += (int) strlen((char *)(hexbuf)) ;
                 }
             }
             else
@@ -21267,22 +19612,22 @@
             c = (*mb_ptr2char)(p);
             if (vim_isprintc(c))
             {
-                mch_memmove(d, p, (size_t)l);
+                 memmove((char *)(d), (char *)(p), (size_t)l) ;
                 d += l;
             }
             else
             {
                 transchar_hex(d, c);
-                d += STRLEN(d);
+                d +=  strlen((char *)(d)) ;
             }
             p += l;
         }
         else
         {
             char_u      *trs = transchar_byte(*p++);
-            size_t      trs_len = STRLEN(trs);
+            size_t      trs_len =  strlen((char *)(trs)) ;
 
-            mch_memmove(d, trs, trs_len);
+             memmove((char *)(d), (char *)(trs), trs_len) ;
             d += trs_len;
         }
     }
@@ -21297,11 +19642,6 @@
     int         i;
     int         len = orglen;
 
-#define GA_CHAR(i)  ((char_u *)ga.ga_data)[i]
-#define GA_PTR(i)   ((char_u *)ga.ga_data + (i))
-#define STR_CHAR(i)  (buf == NULL ? GA_CHAR(i) : buf[i])
-#define STR_PTR(i)   (buf == NULL ? GA_PTR(i) : buf + (i))
-
     if (buf == NULL)
     {
         ga_init2(&ga, 1, 10);
@@ -21309,7 +19649,7 @@
         {
             return NULL;
         }
-        mch_memmove(ga.ga_data, str, (size_t)len);
+         memmove((char *)(ga.ga_data), (char *)(str), (size_t)len) ;
         ga.ga_len = len;
     }
     else
@@ -21318,11 +19658,11 @@
         {
             len = buflen - 1;
         }
-        mch_memmove(buf, str, (size_t)len);
+         memmove((char *)(buf), (char *)(str), (size_t)len) ;
     }
     if (buf == NULL)
     {
-        GA_CHAR(len) = NUL;
+         ((char_u *)ga.ga_data)[len]  = NUL;
     }
     else
     {
@@ -21330,14 +19670,14 @@
     }
 
     i = 0;
-    while (STR_CHAR(i) != NUL)
+    while ( (buf == NULL ?  ((char_u *)ga.ga_data)[i]  : buf[i])  != NUL)
     {
-        if (enc_utf8 || (has_mbyte && MB_BYTE2LEN(STR_CHAR(i)) > 1))
+        if (enc_utf8 || (has_mbyte &&  mb_bytelen_tab[ (buf == NULL ?  ((char_u *)ga.ga_data)[i]  : buf[i]) ]  > 1))
         {
             if (enc_utf8)
             {
-                int     c = utf_ptr2char(STR_PTR(i));
-                int     olen = utf_ptr2len(STR_PTR(i));
+                int     c = utf_ptr2char( (buf == NULL ?  ((char_u *)ga.ga_data + (i))  : buf + (i)) );
+                int     olen = utf_ptr2len( (buf == NULL ?  ((char_u *)ga.ga_data + (i))  : buf + (i)) );
                 int     lc = utf_tolower(c);
 
                 if ((c < 0x80 || olen > 1) && c != lc)
@@ -21358,30 +19698,30 @@
                         {
                             if (buf == NULL)
                             {
-                                STRMOVE(GA_PTR(i) + nlen, GA_PTR(i) + olen);
+                                  memmove((char *)(( ((char_u *)ga.ga_data + (i))  + nlen)), (char *)(( ((char_u *)ga.ga_data + (i))  + olen)),  strlen((char *)( ((char_u *)ga.ga_data + (i))  + olen))  + 1)  ;
                                 ga.ga_len += nlen - olen;
                             }
                             else
                             {
-                                STRMOVE(buf + i + nlen, buf + i + olen);
+                                  memmove((char *)((buf + i + nlen)), (char *)((buf + i + olen)),  strlen((char *)(buf + i + olen))  + 1)  ;
                                 len += nlen - olen;
                             }
                         }
                     }
-                    (void)utf_char2bytes(lc, STR_PTR(i));
+                    (void)utf_char2bytes(lc,  (buf == NULL ?  ((char_u *)ga.ga_data + (i))  : buf + (i)) );
                 }
             }
-            i += (*mb_ptr2len)(STR_PTR(i));
+            i += (*mb_ptr2len)( (buf == NULL ?  ((char_u *)ga.ga_data + (i))  : buf + (i)) );
         }
         else
         {
             if (buf == NULL)
             {
-                GA_CHAR(i) = TOLOWER_LOC(GA_CHAR(i));
+                 ((char_u *)ga.ga_data)[i]  =   (tolower ((unsigned char)(((char_u *)ga.ga_data)[i]))) ;
             }
             else
             {
-                buf[i] = TOLOWER_LOC(buf[i]);
+                buf[i] =   (tolower ((unsigned char)(buf[i]))) ;
             }
             ++i;
         }
@@ -21408,12 +19748,12 @@
     int                 i;
 
     i = 0;
-    if (IS_SPECIAL(c))
+    if ( ((c) < 0) )
     {
         transchar_charbuf[0] = '~';
         transchar_charbuf[1] = '@';
         i = 2;
-        c = K_SECOND(c);
+        c =  ((c) ==  (0x80)  ? KS_SPECIAL : (c) == NUL ? KS_ZERO :  ((-(c)) & 0xff) ) ;
     }
 
     if ((!chartab_initialized && ((c >= ' ' && c <= '~'))) || (c < 256 && vim_isprintc_strict(c)))
@@ -21525,9 +19865,9 @@
     int
 char2cells(int c)
 {
-    if (IS_SPECIAL(c))
+    if ( ((c) < 0) )
     {
-        return char2cells(K_SECOND(c)) + 2;
+        return char2cells( ((c) ==  (0x80)  ? KS_SPECIAL : (c) == NUL ? KS_ZERO :  ((-(c)) & 0xff) ) ) + 2;
     }
     if (c >= 0x80)
     {
@@ -21585,12 +19925,11 @@
     return size;
 }
 
-# define RET_WIN_BUF_CHARTABSIZE(wp, buf, p, col)     if (*(p) == TAB && (!(wp)->w_p_list || wp->w_lcs_chars.tab1))     {       int ts;         ts = (buf)->b_p_ts;     return (int)(ts - (col % ts));     }     else   return ptr2cells(p);
-
     int
 chartabsize(char_u *p, colnr_T col)
 {
-    RET_WIN_BUF_CHARTABSIZE(curwin, curbuf, p, col)
+     if (*(p) == TAB && (!(curwin)-> w_onebuf_opt.wo_list  || curwin->w_lcs_chars.tab1))
+         {       int ts;         ts = (curbuf)->b_p_ts;     return (int)(ts - (col % ts));     }     else   return ptr2cells(p);
 }
 
     int
@@ -21646,7 +19985,7 @@
 linetabsize_eol(win_T *wp, linenr_T lnum)
 {
     return linetabsize(wp, lnum)
-        + ((wp->w_p_list && wp->w_lcs_chars.eol != NUL) ? 1 : 0);
+        + ((wp-> w_onebuf_opt.wo_list  && wp->w_lcs_chars.eol != NUL) ? 1 : 0);
 }
 
     int
@@ -21659,7 +19998,7 @@
 win_linetabsize_cts(chartabsize_T *cts, colnr_T len)
 {
     vimlong_T vcol = cts->cts_vcol;
-    for ( ; *cts->cts_ptr != NUL && (len == MAXCOL || cts->cts_ptr < cts->cts_line + len); MB_PTR_ADV(cts->cts_ptr))
+    for ( ; *cts->cts_ptr != NUL && (len == MAXCOL || cts->cts_ptr < cts->cts_line + len);  cts->cts_ptr += (*mb_ptr2len)(cts->cts_ptr) )
     {
         vcol += win_lbr_chartabsize(cts, NULL, NULL);
         if (vcol > MAXCOL)
@@ -21683,7 +20022,7 @@
     int
 vim_isNormalIDc(int c)
 {
-    return ASCII_ISALNUM(c) || c == '_';
+    return  ( ( ((unsigned)(c) - 'A' < 26)  ||  ((unsigned)(c) - 'a' < 26) )  ||  ((unsigned)(c) - '0' < 10) )  || c == '_';
 }
 
     int
@@ -21707,7 +20046,7 @@
         }
         return FALSE;
     }
-    return (c > 0 && GET_CHARTAB(buf, c) != 0);
+    return (c > 0 &&  ((buf)->b_chartab[(unsigned)(c) >> 3] & (1 << ((c) & 0x7)))  != 0);
 }
 
     int
@@ -21721,7 +20060,7 @@
 {
     int c = *p;
 
-    if (has_mbyte && MB_BYTE2LEN(c) > 1)
+    if (has_mbyte &&  mb_bytelen_tab[c]  > 1)
     {
         c = (*mb_ptr2char)(p);
     }
@@ -21757,7 +20096,7 @@
     int
 vim_isprintc_strict(int c)
 {
-    if (enc_dbcs != 0 && c < 0x100 && MB_BYTE2LEN(c) > 1)
+    if (enc_dbcs != 0 && c < 0x100 &&  mb_bytelen_tab[c]  > 1)
     {
         return FALSE;
     }
@@ -21769,9 +20108,9 @@
 }
 
     void
-init_chartabsize_arg(chartabsize_T   *cts, win_T           *wp, linenr_T        lnum UNUSED, colnr_T         col, char_u          *line, char_u          *ptr)
+init_chartabsize_arg(chartabsize_T   *cts, win_T           *wp, linenr_T        lnum  __attribute__((unused)) , colnr_T         col, char_u          *line, char_u          *ptr)
 {
-    CLEAR_POINTER(cts);
+      memset(((cts)), (0), (sizeof(*(cts))))  ;
     cts->cts_win = wp;
     cts->cts_vcol = col;
     cts->cts_line = line;
@@ -21779,18 +20118,19 @@
 }
 
     void
-clear_chartabsize_arg(chartabsize_T *cts UNUSED)
+clear_chartabsize_arg(chartabsize_T *cts  __attribute__((unused)) )
 {
 }
 
     int
 lbr_chartabsize(chartabsize_T *cts)
 {
-        if (curwin->w_p_wrap)
+        if (curwin-> w_onebuf_opt.wo_wrap )
         {
             return win_nolbr_chartabsize(cts, NULL);
         }
-        RET_WIN_BUF_CHARTABSIZE(curwin, curbuf, cts->cts_ptr, cts->cts_vcol)
+         if (*(cts->cts_ptr) == TAB && (!(curwin)-> w_onebuf_opt.wo_list  || curwin->w_lcs_chars.tab1))
+             {       int ts;         ts = (curbuf)->b_p_ts;     return (int)(ts - (cts->cts_vcol % ts));     }     else   return ptr2cells(cts->cts_ptr);
 }
 
     int
@@ -21799,23 +20139,24 @@
     int         retval;
 
     retval = lbr_chartabsize(cts);
-    MB_PTR_ADV(cts->cts_ptr);
+     cts->cts_ptr += (*mb_ptr2len)(cts->cts_ptr) ;
     return retval;
 }
 
     int
-win_lbr_chartabsize(chartabsize_T   *cts, int             *headp UNUSED, int             *tailp UNUSED)
+win_lbr_chartabsize(chartabsize_T   *cts, int             *headp  __attribute__((unused)) , int             *tailp  __attribute__((unused)) )
 {
     win_T       *wp = cts->cts_win;
     char_u      *s = cts->cts_ptr;
     colnr_T     vcol = cts->cts_vcol;
 
     {
-        if (wp->w_p_wrap)
+        if (wp-> w_onebuf_opt.wo_wrap )
         {
             return win_nolbr_chartabsize(cts, headp);
         }
-        RET_WIN_BUF_CHARTABSIZE(wp, wp->w_buffer, s, vcol)
+         if (*(s) == TAB && (!(wp)-> w_onebuf_opt.wo_list  || wp->w_lcs_chars.tab1))
+             {       int ts;         ts = (wp->w_buffer)->b_p_ts;     return (int)(ts - (vcol % ts));     }     else   return ptr2cells(s);
     }
 
 }
@@ -21828,13 +20169,13 @@
     colnr_T     col = cts->cts_vcol;
     int         n;
 
-    if (*s == TAB && (!wp->w_p_list || wp->w_lcs_chars.tab1))
+    if (*s == TAB && (!wp-> w_onebuf_opt.wo_list  || wp->w_lcs_chars.tab1))
     {
         n = wp->w_buffer->b_p_ts;
         return (int)(n - (col % n));
     }
     n = ptr2cells(s);
-    if (n == 2 && MB_BYTE2LEN(*s) > 1 && in_win_border(wp, col))
+    if (n == 2 &&  mb_bytelen_tab[*s]  > 1 && in_win_border(wp, col))
     {
         if (headp != NULL)
         {
@@ -21891,7 +20232,7 @@
     init_chartabsize_arg(&cts, wp, pos->lnum, 0, line, line);
     cts.cts_max_head_vcol = -1;
 
-    if ((!wp->w_p_list || wp->w_lcs_chars.tab1 != NUL))
+    if ((!wp-> w_onebuf_opt.wo_list  || wp->w_lcs_chars.tab1 != NUL))
     {
         for (;;)
         {
@@ -21919,7 +20260,7 @@
                         incr = g_chartab[c] & CT_CELL_MASK;
                     }
 
-                    if (incr == 2 && wp->w_p_wrap && MB_BYTE2LEN(*ptr) > 1 && in_win_border(wp, vcol))
+                    if (incr == 2 && wp-> w_onebuf_opt.wo_wrap  &&  mb_bytelen_tab[*ptr]  > 1 && in_win_border(wp, vcol))
                     {
                         ++incr;
                         head = 1;
@@ -21985,7 +20326,7 @@
     }
     if (cursor != NULL)
     {
-        if (*ptr == TAB && (State & MODE_NORMAL) && !wp->w_p_list && !virtual_active() && !(VIsual_active && (*p_sel == 'e' || LTOREQ_POS(*pos, VIsual))))
+        if (*ptr == TAB && (State & MODE_NORMAL) && !wp-> w_onebuf_opt.wo_list  && !virtual_active() && !(VIsual_active && (*p_sel == 'e' ||  ( (((*pos).lnum != (VIsual).lnum)               ? (*pos).lnum < (VIsual).lnum                   : (*pos).col != (VIsual).col                        ? (*pos).col < (VIsual).col                     : (*pos).coladd < (VIsual).coladd)  ||  (((*pos).lnum == (VIsual).lnum) && ((*pos).col == (VIsual).col) && ((*pos).coladd == (VIsual).coladd)) ) )))
         {
             *cursor = vcol + incr - 1;
         }
@@ -21999,10 +20340,10 @@
     colnr_T
 getvcol_nolist(pos_T *posp)
 {
-    int         list_save = curwin->w_p_list;
+    int         list_save = curwin-> w_onebuf_opt.wo_list ;
     colnr_T     vcol;
 
-    curwin->w_p_list = FALSE;
+    curwin-> w_onebuf_opt.wo_list  = FALSE;
     if (posp->coladd)
     {
         getvvcol(curwin, posp, NULL, &vcol, NULL, 0);
@@ -22011,7 +20352,7 @@
     {
         getvcol(curwin, posp, NULL, &vcol, NULL, 0);
     }
-    curwin->w_p_list = list_save;
+    curwin-> w_onebuf_opt.wo_list  = list_save;
     return vcol;
 }
 
@@ -22075,7 +20416,7 @@
     colnr_T to1;
     colnr_T to2;
 
-    if (LT_POSP(pos1, pos2))
+    if ( (((pos1)->lnum != (pos2)->lnum)                    ? (pos1)->lnum < (pos2)->lnum                 : (pos1)->col != (pos2)->col                      ? (pos1)->col < (pos2)->col                   : (pos1)->coladd < (pos2)->coladd) )
     {
         getvvcol(wp, pos1, &from1, NULL, &to1, flags);
         getvvcol(wp, pos2, &from2, NULL, &to2, flags);
@@ -22115,7 +20456,7 @@
 {
     char_u      *p = q;
 
-    while (VIM_ISWHITE(*p))
+    while ( ((*p) == ' ' || (*p) == '\t') )
     {
         ++p;
     }
@@ -22139,7 +20480,7 @@
 {
     char_u      *p = q;
 
-    while (VIM_ISDIGIT(*p))
+    while ( ((unsigned)(*p) - '0' < 10) )
     {
         ++p;
     }
@@ -22163,7 +20504,7 @@
 {
     char_u      *p = q;
 
-    while (*p != NUL && !VIM_ISDIGIT(*p))
+    while (*p != NUL && ! ((unsigned)(*p) - '0' < 10) )
     {
         ++p;
     }
@@ -22208,8 +20549,8 @@
     return (c >= '0' && c <= '7');
 }
 
-#define LATIN1LOWER 'l'
-#define LATIN1UPPER 'U'
+enum { LATIN1LOWER = 'l' };
+enum { LATIN1UPPER = 'U' };
 
 static char_u latin1flags[257] = "                                                                 UUUUUUUUUUUUUUUUUUUUUUUUUU      llllllllllllllllllllllllll                                                                     UUUUUUUUUUUUUUUUUUUUUUU UUUUUUUllllllllllllllllllllllll llllllll";
 static char_u latin1upper[257] = "                                 !\"#$%&'()*+,-./0123456789:;<=>?@ABCDEFGHIJKLMNOPQRSTUVWXYZ[\\]^_`ABCDEFGHIJKLMNOPQRSTUVWXYZ{|}~";
@@ -22237,7 +20578,7 @@
             return (latin1flags[c] & LATIN1LOWER) == LATIN1LOWER;
         }
     }
-    return SAFE_islower(c);
+    return  (islower ((unsigned char)(c))) ;
 }
 
     int
@@ -22266,7 +20607,7 @@
             return (latin1flags[c] & LATIN1UPPER) == LATIN1UPPER;
         }
     }
-    return SAFE_isupper(c);
+    return  (isupper ((unsigned char)(c))) ;
 }
 
     int
@@ -22303,9 +20644,9 @@
     }
     if (c < 0x80 && (cmp_flags & CMP_KEEPASCII))
     {
-        return TOUPPER_ASC(c);
+        return  (((c) < 'a' || (c) > 'z') ? (c) : (c) - ('a' - 'A')) ;
     }
-    return TOUPPER_LOC(c);
+    return   (toupper ((unsigned char)(c))) ;
 }
 
     int
@@ -22336,9 +20677,9 @@
     }
     if (c < 0x80 && (cmp_flags & CMP_KEEPASCII))
     {
-        return TOLOWER_ASC(c);
+        return  (((c) < 'A' || (c) > 'Z') ? (c) : (c) + ('a' - 'A')) ;
     }
-    return TOLOWER_LOC(c);
+    return   (tolower ((unsigned char)(c))) ;
 }
 
     char_u *
@@ -22392,7 +20733,7 @@
     {
         ++p;
     }
-    while (VIM_ISDIGIT(*p))
+    while ( ((unsigned)(*p) - '0' < 10) )
     {
         if (retval >= LONG_MAX / 10 - 10)
         {
@@ -22403,7 +20744,7 @@
             retval = retval * 10 - '0' + *p;
         }
         ++p;
-        if (in_vim9script() && *p == '\'' && VIM_ISDIGIT(p[1]))
+        if (in_vim9script() && *p == '\'' &&  ((unsigned)(p[1]) - '0' < 10) )
         {
             ++p;
         }
@@ -22472,7 +20813,7 @@
             pre = 0;
             if (what & STR2NR_OCT)
             {
-                for (n = 1; n != maxlen && VIM_ISDIGIT(ptr[n]); ++n)
+                for (n = 1; n != maxlen &&  ((unsigned)(ptr[n]) - '0' < 10) ; ++n)
                 {
                     if (ptr[n] > '7')
                     {
@@ -22494,13 +20835,13 @@
         }
         while ('0' <= *ptr && *ptr <= '1')
         {
-            if (un <= UVARNUM_MAX / 2)
+            if (un <=  ULLONG_MAX  / 2)
             {
                 un = 2 * un + (uvarnumber_T)(*ptr - '0');
             }
             else
             {
-                un = UVARNUM_MAX;
+                un =  ULLONG_MAX ;
                 if (overflow != NULL)
                 {
                     *overflow = TRUE;
@@ -22529,13 +20870,13 @@
         }
         while ('0' <= *ptr && *ptr <= '7')
         {
-            if (un <= UVARNUM_MAX / 8)
+            if (un <=  ULLONG_MAX  / 8)
             {
                 un = 8 * un + (uvarnumber_T)(*ptr - '0');
             }
             else
             {
-                un = UVARNUM_MAX;
+                un =  ULLONG_MAX ;
                 if (overflow != NULL)
                 {
                     *overflow = TRUE;
@@ -22564,13 +20905,13 @@
         }
         while (vim_isxdigit(*ptr))
         {
-            if (un <= UVARNUM_MAX / 16)
+            if (un <=  ULLONG_MAX  / 16)
             {
                 un = 16 * un + (uvarnumber_T)hex2nr(*ptr);
             }
             else
             {
-                un = UVARNUM_MAX;
+                un =  ULLONG_MAX ;
                 if (overflow != NULL)
                 {
                     *overflow = TRUE;
@@ -22593,17 +20934,17 @@
     }
     else
     {
-        while (VIM_ISDIGIT(*ptr))
+        while ( ((unsigned)(*ptr) - '0' < 10) )
         {
             uvarnumber_T    digit = (uvarnumber_T)(*ptr - '0');
 
-            if (un < UVARNUM_MAX / 10 || (un == UVARNUM_MAX / 10 && digit <= UVARNUM_MAX % 10))
+            if (un <  ULLONG_MAX  / 10 || (un ==  ULLONG_MAX  / 10 && digit <=  ULLONG_MAX  % 10))
             {
                 un = 10 * un + digit;
             }
             else
             {
-                un = UVARNUM_MAX;
+                un =  ULLONG_MAX ;
                 if (overflow != NULL)
                 {
                     *overflow = TRUE;
@@ -22614,7 +20955,7 @@
             {
                 break;
             }
-            if ((what & STR2NR_QUOTE) && *ptr == '\'' && VIM_ISDIGIT(ptr[1]))
+            if ((what & STR2NR_QUOTE) && *ptr == '\'' &&  ((unsigned)(ptr[1]) - '0' < 10) )
             {
                 ++ptr;
                 if (n++ == maxlen)
@@ -22625,7 +20966,7 @@
         }
     }
 
-    if (strict && n - 1 != maxlen && ASCII_ISALNUM(*ptr))
+    if (strict && n - 1 != maxlen &&  ( ( ((unsigned)(*ptr) - 'A' < 26)  ||  ((unsigned)(*ptr) - 'a' < 26) )  ||  ((unsigned)(*ptr) - '0' < 10) ) )
     {
         return;
     }
@@ -22642,9 +20983,9 @@
     {
         if (negative)
         {
-            if (un > VARNUM_MAX)
+            if (un >  LLONG_MAX )
             {
-                *nptr = VARNUM_MIN;
+                *nptr =  LLONG_MIN ;
                 if (overflow != NULL)
                 {
                     *overflow = TRUE;
@@ -22657,9 +20998,9 @@
         }
         else
         {
-            if (un > VARNUM_MAX)
+            if (un >  LLONG_MAX )
             {
-                un = VARNUM_MAX;
+                un =  LLONG_MAX ;
                 if (overflow != NULL)
                 {
                     *overflow = TRUE;
@@ -22711,7 +21052,7 @@
     {
         if (rem_backslash(p))
         {
-            STRMOVE(p, p + 1);
+              memmove((char *)((p)), (char *)((p + 1)),  strlen((char *)(p + 1))  + 1)  ;
         }
     }
 }
@@ -22732,19 +21073,19 @@
 
 // ==================== cindent.c ====================
 
-#define LOOKFOR_INITIAL         0
-#define LOOKFOR_IF              1
-#define LOOKFOR_DO              2
-#define LOOKFOR_CASE            3
-#define LOOKFOR_ANY             4
-#define LOOKFOR_TERM            5
-#define LOOKFOR_UNTERM          6
-#define LOOKFOR_SCOPEDECL       7
-#define LOOKFOR_NOBREAK         8
-#define LOOKFOR_CPP_BASECLASS   9
-#define LOOKFOR_ENUM_OR_INIT    10
-#define LOOKFOR_JS_KEY          11
-#define LOOKFOR_COMMA           12
+enum { LOOKFOR_INITIAL = 0 };
+enum { LOOKFOR_IF = 1 };
+enum { LOOKFOR_DO = 2 };
+enum { LOOKFOR_CASE = 3 };
+enum { LOOKFOR_ANY = 4 };
+enum { LOOKFOR_TERM = 5 };
+enum { LOOKFOR_UNTERM = 6 };
+enum { LOOKFOR_SCOPEDECL = 7 };
+enum { LOOKFOR_NOBREAK = 8 };
+enum { LOOKFOR_CPP_BASECLASS = 9 };
+enum { LOOKFOR_ENUM_OR_INIT = 10 };
+enum { LOOKFOR_JS_KEY = 11 };
+enum { LOOKFOR_COMMA = 12 };
 
     int
 cin_is_cinword(char_u *line)
@@ -22755,7 +21096,7 @@
     int         retval = FALSE;
     int         len;
 
-    cinw_len = (int)STRLEN(curbuf->b_p_cinw) + 1;
+    cinw_len = (int) strlen((char *)(curbuf->b_p_cinw))  + 1;
     cinw_buf = alloc(cinw_len);
     if (cinw_buf == NULL)
     {
@@ -22766,7 +21107,7 @@
     for (cinw = curbuf->b_p_cinw; *cinw; )
     {
         len = copy_option_part(&cinw, cinw_buf, cinw_len, ",");
-        if (STRNCMP(line, cinw_buf, len) == 0 && (!vim_iswordc(line[len]) || !vim_iswordc(line[len - 1])))
+        if ( strncmp((char *)(line), (char *)(cinw_buf), (len))  == 0 && (!vim_iswordc(line[len]) || !vim_iswordc(line[len - 1])))
         {
             retval = TRUE;
             break;
@@ -22833,7 +21174,7 @@
 
                 for (p += 3; *p; ++p)
                 {
-                    if (p[0] == ')' && STRNCMP(p + 1, delim, delim_len) == 0 && p[delim_len + 1] == '"')
+                    if (p[0] == ')' &&  strncmp((char *)(p + 1), (char *)(delim), (delim_len))  == 0 && p[delim_len + 1] == '"')
                     {
                         p += delim_len + 1;
                         break;
@@ -22879,7 +21220,7 @@
             int in_str = FALSE;
 
             p = line;
-            while ((p = vim_strpbrk(p, (char_u *)"\";")) != NULL)
+            while ((p =  (char_u *)strpbrk((char *)(p), (char *)((char_u *)"\";")) ) != NULL)
             {
                 if (*p == '"')
                 {
@@ -23011,7 +21352,7 @@
     }
     rs_pos = find_start_rawstring(curbuf->b_ind_maxcomment);
 
-    if (comment_pos == NULL || (rs_pos != NULL && LT_POS(*rs_pos, *comment_pos)))
+    if (comment_pos == NULL || (rs_pos != NULL &&  (((*rs_pos).lnum != (*comment_pos).lnum)               ? (*rs_pos).lnum < (*comment_pos).lnum                   : (*rs_pos).col != (*comment_pos).col                        ? (*rs_pos).col < (*comment_pos).col                     : (*rs_pos).coladd < (*comment_pos).coladd) ))
     {
         if (is_raw != NULL && rs_pos != NULL)
         {
@@ -23044,7 +21385,7 @@
 
         if (curbuf->b_ind_hash_comment != 0 && s != prev_s && *s == '#')
         {
-            s += STRLEN(s);
+            s +=  strlen((char *)(s)) ;
             break;
         }
         if (*s != '/')
@@ -23054,7 +21395,7 @@
         ++s;
         if (*s == '/')
         {
-            s += STRLEN(s);
+            s +=  strlen((char *)(s)) ;
             break;
         }
         if (*s != '*')
@@ -23182,7 +21523,7 @@
     char_u  *s = cin_skipcomment(p);
     int     found = FALSE;
 
-    cinsd_len = STRLEN(curbuf->b_p_cinsd) + 1;
+    cinsd_len =  strlen((char *)(curbuf->b_p_cinsd))  + 1;
     cinsd_buf = alloc(cinsd_len);
     if (cinsd_buf == NULL)
     {
@@ -23192,7 +21533,7 @@
     for (cinsd = curbuf->b_p_cinsd; *cinsd; )
     {
         len = copy_option_part(&cinsd, cinsd_buf, (int)cinsd_len, ",");
-        if (STRNCMP(s, cinsd_buf, len) == 0)
+        if ( strncmp((char *)(s), (char *)(cinsd_buf), (len))  == 0)
         {
             skip = cin_skipcomment(s + len);
             if (*skip == ':' && skip[1] != ':')
@@ -23225,7 +21566,7 @@
     int         retval = FALSE;
     int         candidate_amount = *amount;
 
-    if (*line != NUL && line[STRLEN(line) - 1] == '\\')
+    if (*line != NUL && line[ strlen((char *)(line))  - 1] == '\\')
     {
         candidate_amount = get_indent_lnum(lnum);
     }
@@ -23243,7 +21584,7 @@
             break;
         }
         line = ml_get(--lnum);
-        if (*line == NUL || line[STRLEN(line) - 1] != '\\')
+        if (*line == NUL || line[ strlen((char *)(line))  - 1] != '\\')
         {
             break;
         }
@@ -23267,7 +21608,7 @@
     {
         p = cin_skipcomment(p + 1);
     }
-    return (STRNCMP(p, "else", 4) == 0 && !vim_isIDc(p[4]));
+    return ( strncmp((char *)(p), (char *)("else"), (4))  == 0 && !vim_isIDc(p[4]));
 }
 
     static int
@@ -23323,15 +21664,15 @@
     static int
 cin_starts_with(char_u *s, char *word)
 {
-    int l = (int)STRLEN(word);
+    int l = (int) strlen((char *)(word)) ;
 
-    return (STRNCMP(s, word, l) == 0 && !vim_isIDc(s[l]));
+    return ( strncmp((char *)(s), (char *)(word), (l))  == 0 && !vim_isIDc(s[l]));
 }
 
     static int
 cin_isdefault(char_u *s)
 {
-    return (STRNCMP(s, "default", 7) == 0 && *(s = cin_skipcomment(s + 7)) == ':' && s[1] != ':');
+    return ( strncmp((char *)(s), (char *)("default"), (7))  == 0 && *(s = cin_skipcomment(s + 7)) == ':' && s[1] != ':');
 }
 
     static int
@@ -23455,12 +21796,12 @@
 {
     char_u      *p = s;
     char_u      *r;
-    int         len = (int)STRLEN(find);
+    int         len = (int) strlen((char *)(find)) ;
 
     while (*p != NUL)
     {
         p = cin_skipcomment(p);
-        if (STRNCMP(p, find, len) == 0)
+        if ( strncmp((char *)(p), (char *)(find), (len))  == 0)
         {
             r = skipwhite(p + len);
             if (cin_nocode(r))
@@ -23505,7 +21846,7 @@
         {
             p = r = cin_skipcomment(p + 1);
         }
-        else if (STRNCMP(p, "return", 6) == 0 && !vim_isIDc(p[6]) && (p == s || (p > s && !vim_isIDc(p[-1]))))
+        else if ( strncmp((char *)(p), (char *)("return"), (6))  == 0 && !vim_isIDc(p[6]) && (p == s || (p > s && !vim_isIDc(p[-1]))))
         {
             p = r = cin_skipcomment(p + 6);
         }
@@ -23574,7 +21915,7 @@
         int i;
         int l;
 
-        for (i = 0; i < (int)ARRAY_LENGTH(skip); ++i)
+        for (i = 0; i < (int) (sizeof(skip) / sizeof((skip)[0])) ; ++i)
         {
             l = (int)strlen(skip[i]);
             if (cin_starts_with(s, skip[i]))
@@ -23598,7 +21939,7 @@
     return cin_is_compound_init(s);
 }
 
-#define FIND_NAMESPACE_LIM 20
+enum { FIND_NAMESPACE_LIM = 20 };
 
     static int
 cin_is_cpp_namespace(char_u *s)
@@ -23609,17 +21950,17 @@
 
     s = cin_skipcomment(s);
 
-    while ((STRNCMP(s, "inline", 6) == 0 || STRNCMP(s, "export", 6) == 0) && (s[6] == NUL || !vim_iswordc(s[6])))
+    while (( strncmp((char *)(s), (char *)("inline"), (6))  == 0 ||  strncmp((char *)(s), (char *)("export"), (6))  == 0) && (s[6] == NUL || !vim_iswordc(s[6])))
     {
         s = cin_skipcomment(skipwhite(s + 6));
     }
 
-    if (STRNCMP(s, "namespace", 9) == 0 && (s[9] == NUL || !vim_iswordc(s[9])))
+    if ( strncmp((char *)(s), (char *)("namespace"), (9))  == 0 && (s[9] == NUL || !vim_iswordc(s[9])))
     {
         p = cin_skipcomment(skipwhite(s + 9));
         while (*p != NUL)
         {
-            if (VIM_ISWHITE(*p))
+            if ( ((*p) == ' ' || (*p) == '\t') )
             {
                 has_name = TRUE;
                 p = cin_skipcomment(skipwhite(p));
@@ -23662,12 +22003,12 @@
     int         has_string_literal = FALSE;
 
     s = cin_skipcomment(s);
-    if (STRNCMP(s, "extern", 6) == 0 && (s[6] == NUL || !vim_iswordc(s[6])))
+    if ( strncmp((char *)(s), (char *)("extern"), (6))  == 0 && (s[6] == NUL || !vim_iswordc(s[6])))
     {
         p = cin_skipcomment(skipwhite(s + 6));
         while (*p != NUL)
         {
-            if (VIM_ISWHITE(*p))
+            if ( ((*p) == ' ' || (*p) == '\t') )
             {
                 p = cin_skipcomment(skipwhite(p));
             }
@@ -23800,23 +22141,23 @@
     line = ml_get_curline();
     p = skipwhite(line);
     len = (int)(skiptowhite(p) - p);
-    if (len == 6 && STRNCMP(p, "static", 6) == 0)
+    if (len == 6 &&  strncmp((char *)(p), (char *)("static"), (6))  == 0)
     {
         p = skipwhite(p + 6);
         len = (int)(skiptowhite(p) - p);
     }
-    if (len == 6 && STRNCMP(p, "struct", 6) == 0)
+    if (len == 6 &&  strncmp((char *)(p), (char *)("struct"), (6))  == 0)
     {
         p = skipwhite(p + 6);
     }
-    else if (len == 4 && STRNCMP(p, "enum", 4) == 0)
+    else if (len == 4 &&  strncmp((char *)(p), (char *)("enum"), (4))  == 0)
     {
         p = skipwhite(p + 4);
     }
-    else if ((len == 8 && STRNCMP(p, "unsigned", 8) == 0) || (len == 6 && STRNCMP(p, "signed", 6) == 0))
+    else if ((len == 8 &&  strncmp((char *)(p), (char *)("unsigned"), (8))  == 0) || (len == 6 &&  strncmp((char *)(p), (char *)("signed"), (6))  == 0))
     {
         s = skipwhite(p + len);
-        if ((STRNCMP(s, "int", 3) == 0 && VIM_ISWHITE(s[3])) || (STRNCMP(s, "long", 4) == 0 && VIM_ISWHITE(s[4])) || (STRNCMP(s, "short", 5) == 0 && VIM_ISWHITE(s[5])) || (STRNCMP(s, "char", 4) == 0 && VIM_ISWHITE(s[4])))
+        if (( strncmp((char *)(s), (char *)("int"), (3))  == 0 &&  ((s[3]) == ' ' || (s[3]) == '\t') ) || ( strncmp((char *)(s), (char *)("long"), (4))  == 0 &&  ((s[4]) == ' ' || (s[4]) == '\t') ) || ( strncmp((char *)(s), (char *)("short"), (5))  == 0 &&  ((s[5]) == ' ' || (s[5]) == '\t') ) || ( strncmp((char *)(s), (char *)("char"), (4))  == 0 &&  ((s[4]) == ' ' || (s[4]) == '\t') ))
         {
             p = s;
         }
@@ -23825,7 +22166,7 @@
     {
         ;
     }
-    if (len == 0 || !VIM_ISWHITE(p[len]) || cin_nocode(p))
+    if (len == 0 || ! ((p[len]) == ' ' || (p[len]) == '\t')  || cin_nocode(p))
     {
         return 0;
     }
@@ -23848,7 +22189,7 @@
     if (lnum > 1)
     {
         line = ml_get(lnum - 1);
-        if (*line != NUL && line[STRLEN(line) - 1] == '\\')
+        if (*line != NUL && line[ strlen((char *)(line))  - 1] == '\\')
         {
             return -1;
         }
@@ -24079,7 +22420,7 @@
         {
             lnum = first_lnum - 1;
             s = ml_get(lnum);
-            if (*s == NUL || s[STRLEN(s) - 1] != '\\')
+            if (*s == NUL || s[ strlen((char *)(s))  - 1] != '\\')
             {
                 retval = TRUE;
             }
@@ -24135,13 +22476,13 @@
     static int
 cin_isif(char_u *p)
 {
-    return (STRNCMP(p, "if", 2) == 0 && !vim_isIDc(p[2]));
+    return ( strncmp((char *)(p), (char *)("if"), (2))  == 0 && !vim_isIDc(p[2]));
 }
 
     static int
 cin_isdo(char_u *p)
 {
-    return (STRNCMP(p, "do", 2) == 0 && !vim_isIDc(p[2]));
+    return ( strncmp((char *)(p), (char *)("do"), (2))  == 0 && !vim_isIDc(p[2]));
 }
 
     static int
@@ -24185,13 +22526,13 @@
     {
         return 0;
     }
-    while (offset > 2 && VIM_ISWHITE(line[offset]))
+    while (offset > 2 &&  ((line[offset]) == ' ' || (line[offset]) == '\t') )
     {
         --offset;
     }
 
     offset -= 1;
-    if (STRNCMP(line + offset, "if", 2) == 0)
+    if ( strncmp((char *)(line + offset), (char *)("if"), (2))  == 0)
     {
         goto probablyFound;
     }
@@ -24199,7 +22540,7 @@
     if (offset >= 1)
     {
         offset -= 1;
-        if (STRNCMP(line + offset, "for", 3) == 0)
+        if ( strncmp((char *)(line + offset), (char *)("for"), (3))  == 0)
         {
             goto probablyFound;
         }
@@ -24207,7 +22548,7 @@
         if (offset >= 2)
         {
             offset -= 2;
-            if (STRNCMP(line + offset, "while", 5) == 0)
+            if ( strncmp((char *)(line + offset), (char *)("while"), (5))  == 0)
             {
                 goto probablyFound;
             }
@@ -24279,7 +22620,7 @@
     static int
 cin_isbreak(char_u *p)
 {
-    return (STRNCMP(p, "break", 5) == 0 && !vim_isIDc(p[5]));
+    return ( strncmp((char *)(p), (char *)("break"), (5))  == 0 && !vim_isIDc(p[5]));
 }
 
     static int
@@ -24390,7 +22731,7 @@
                 s = cin_skipcomment(s + 1);
             }
         }
-        else if ((STRNCMP(s, "class", 5) == 0 && !vim_isIDc(s[5])) || (STRNCMP(s, "struct", 6) == 0 && !vim_isIDc(s[6])))
+        else if (( strncmp((char *)(s), (char *)("class"), (5))  == 0 && !vim_isIDc(s[5])) || ( strncmp((char *)(s), (char *)("struct"), (6))  == 0 && !vim_isIDc(s[6])))
         {
             class_or_struct = TRUE;
             lookfor_ctor_init = FALSE;
@@ -24650,7 +22991,7 @@
         if (*p == '.')
         {
             fraction = atol((char *)++p);
-            while (VIM_ISDIGIT(*p))
+            while ( ((unsigned)(*p) - '0' < 10) )
             {
                 ++p;
                 if (divider)
@@ -24913,9 +23254,6 @@
     pos_T       our_paren_pos;
     char_u      *start;
     int         start_brace;
-#define BRACE_IN_COL0           1
-#define BRACE_AT_START          2
-#define BRACE_AT_END            3
     linenr_T    ourscope;
     char_u      *l;
     char_u      *look;
@@ -24932,7 +23270,7 @@
     int         added_to_amount = 0;
     int         js_cur_has_key = 0;
     linenr_T    raw_string_start = 0;
-    cpp_baseclass_cache_T cache_cpp_baseclass = { FALSE, { MAXLNUM, 0 } };
+    cpp_baseclass_cache_T cache_cpp_baseclass = { FALSE, {  LONG_MAX , 0 } };
 
     int         ind_continuation = curbuf->b_ind_continuation;
 
@@ -24949,7 +23287,7 @@
         return 0;
     }
 
-    if ((State & MODE_INSERT) && curwin->w_cursor.col < (colnr_T)STRLEN(linecopy) && linecopy[curwin->w_cursor.col] == ')')
+    if ((State & MODE_INSERT) && curwin->w_cursor.col < (colnr_T) strlen((char *)(linecopy))  && linecopy[curwin->w_cursor.col] == ')')
     {
         linecopy[curwin->w_cursor.col] = NUL;
     }
@@ -24967,7 +23305,7 @@
         comment_pos = &tryposCopy;
     }
     trypos = find_start_rawstring(curbuf->b_ind_maxcomment);
-    if (trypos != NULL && (comment_pos == NULL || LT_POS(*trypos, *comment_pos)))
+    if (trypos != NULL && (comment_pos == NULL ||  (((*trypos).lnum != (*comment_pos).lnum)               ? (*trypos).lnum < (*comment_pos).lnum                   : (*trypos).col != (*comment_pos).col                        ? (*trypos).col < (*comment_pos).col                     : (*trypos).coladd < (*comment_pos).coladd) ))
     {
         amount = -1;
         goto laterend;
@@ -24976,7 +23314,7 @@
     if (*theline == '#' && (*linecopy == '#' || in_cinkeys('#', ' ', TRUE)))
     {
         char_u *directive = skipwhite(theline + 1);
-        if (curbuf->b_ind_pragma == 0 || STRNCMP(directive, "pragma", 6) != 0)
+        if (curbuf->b_ind_pragma == 0 ||  strncmp((char *)(directive), (char *)("pragma"), (6))  != 0)
         {
             amount = curbuf->b_ind_hash_comment;
             goto theend;
@@ -25047,7 +23385,7 @@
                 {
                     align = *p++;
                 }
-                else if (VIM_ISDIGIT(*p) || *p == '-')
+                else if ( ((unsigned)(*p) - '0' < 10)  || *p == '-')
                 {
                     off = getdigits(&p);
                 }
@@ -25064,34 +23402,34 @@
             lead_end_len = copy_option_part(&p, lead_end, COM_MAX_LEN, ",");
             if (what == COM_START)
             {
-                STRCPY(lead_start, lead_end);
+                 strcpy((char *)(lead_start), (char *)(lead_end)) ;
                 lead_start_len = lead_end_len;
                 start_off = off;
                 start_align = align;
             }
             else if (what == COM_MIDDLE)
             {
-                STRCPY(lead_middle, lead_end);
+                 strcpy((char *)(lead_middle), (char *)(lead_end)) ;
                 lead_middle_len = lead_end_len;
             }
             else if (what == COM_END)
             {
-                if (STRNCMP(theline, lead_middle, lead_middle_len) == 0 && STRNCMP(theline, lead_end, lead_end_len) != 0)
+                if ( strncmp((char *)(theline), (char *)(lead_middle), (lead_middle_len))  == 0 &&  strncmp((char *)(theline), (char *)(lead_end), (lead_end_len))  != 0)
                 {
                     done = TRUE;
                     if (curwin->w_cursor.lnum > 1)
                     {
                         look = skipwhite(ml_get(curwin->w_cursor.lnum - 1));
-                        if (STRNCMP(look, lead_start, lead_start_len) == 0)
+                        if ( strncmp((char *)(look), (char *)(lead_start), (lead_start_len))  == 0)
                         {
                             amount = get_indent_lnum(curwin->w_cursor.lnum - 1);
                         }
-                        else if (STRNCMP(look, lead_middle, lead_middle_len) == 0)
+                        else if ( strncmp((char *)(look), (char *)(lead_middle), (lead_middle_len))  == 0)
                         {
                             amount = get_indent_lnum(curwin->w_cursor.lnum - 1);
                             break;
                         }
-                        else if (STRNCMP(ml_get(comment_pos->lnum) + comment_pos->col, lead_start, lead_start_len) != 0)
+                        else if ( strncmp((char *)(ml_get(comment_pos->lnum) + comment_pos->col), (char *)(lead_start), (lead_start_len))  != 0)
                         {
                             continue;
                         }
@@ -25108,7 +23446,7 @@
                     break;
                 }
 
-                if (STRNCMP(theline, lead_middle, lead_middle_len) != 0 && STRNCMP(theline, lead_end, lead_end_len) == 0)
+                if ( strncmp((char *)(theline), (char *)(lead_middle), (lead_middle_len))  != 0 &&  strncmp((char *)(theline), (char *)(lead_end), (lead_end_len))  == 0)
                 {
                     amount = get_indent_lnum(curwin->w_cursor.lnum - 1);
                     if (off != 0)
@@ -25241,7 +23579,6 @@
 
             if (curbuf->b_ind_if_for_while)
             {
-
                 pos_T       cursor_save = curwin->w_cursor;
                 pos_T       outermost;
                 char_u      *line;
@@ -25322,7 +23659,7 @@
                     else
                     {
                         col = our_paren_pos.col + 1;
-                        while (VIM_ISWHITE(l[col]))
+                        while ( ((l[col]) == ' ' || (l[col]) == '\t') )
                         {
                             col++;
                         }
@@ -25497,7 +23834,6 @@
             }
             else if (start_brace == BRACE_AT_START && lookfor_cpp_namespace)
             {
-
                 lookfor_cpp_namespace = TRUE;
             }
             else
@@ -25906,7 +24242,7 @@
 
                 if (terminated == 0 || (lookfor != LOOKFOR_UNTERM && terminated == ','))
                 {
-                    if (lookfor != LOOKFOR_ENUM_OR_INIT && (*skipwhite(l) == '[' || l[STRLEN(l) - 1] == '['))
+                    if (lookfor != LOOKFOR_ENUM_OR_INIT && (*skipwhite(l) == '[' || l[ strlen((char *)(l))  - 1] == '['))
                     {
                         amount += ind_continuation;
                     }
@@ -25946,7 +24282,7 @@
                         while (curwin->w_cursor.lnum > 1)
                         {
                             l = ml_get(curwin->w_cursor.lnum - 1);
-                            if (*l == NUL || l[STRLEN(l) - 1] != '\\')
+                            if (*l == NUL || l[ strlen((char *)(l))  - 1] != '\\')
                             {
                                 break;
                             }
@@ -26066,7 +24402,7 @@
                             l = ml_get_curline();
                             amount = cur_amount;
 
-                            n = (int)STRLEN(l);
+                            n = (int) strlen((char *)(l)) ;
                             if (curbuf->b_ind_js && terminated == ',' && (*skipwhite(l) == ']' || (n >=2 && l[n - 2] == ']')))
                             {
                                 break;
@@ -26099,7 +24435,7 @@
                             }
                             else
                             {
-                                if (lookfor == LOOKFOR_INITIAL && *l != NUL && l[STRLEN(l) - 1] == '\\')
+                                if (lookfor == LOOKFOR_INITIAL && *l != NUL && l[ strlen((char *)(l))  - 1] == '\\')
                                 {
                                     cont_amount = cin_get_equal_amount(curwin->w_cursor.lnum);
                                 }
@@ -26301,7 +24637,7 @@
         }
 
         n = 0;
-        if (cin_ends_in(l, (char_u *)",") || (*l != NUL && (n = l[STRLEN(l) - 1]) == '\\'))
+        if (cin_ends_in(l, (char_u *)",") || (*l != NUL && (n = l[ strlen((char *)(l))  - 1]) == '\\'))
         {
             if (find_last_paren(l, '(', ')') && (trypos = find_match_paren(curbuf->b_ind_maxparen)) != NULL)
             {
@@ -26311,7 +24647,7 @@
             while (n == 0 && curwin->w_cursor.lnum > 1)
             {
                 l = ml_get(curwin->w_cursor.lnum - 1);
-                if (*l == NUL || l[STRLEN(l) - 1] != '\\')
+                if (*l == NUL || l[ strlen((char *)(l))  - 1] != '\\')
                 {
                     break;
                 }
@@ -26383,7 +24719,7 @@
         if (cin_ends_in(l, (char_u *)";"))
         {
             l = ml_get(curwin->w_cursor.lnum - 1);
-            if (cin_ends_in(l, (char_u *)",") || (*l != NUL && l[STRLEN(l) - 1] == '\\'))
+            if (cin_ends_in(l, (char_u *)",") || (*l != NUL && l[ strlen((char *)(l))  - 1] == '\\'))
             {
                 break;
             }
@@ -26408,7 +24744,7 @@
     if (cur_curpos.lnum > 1)
     {
         l = ml_get(cur_curpos.lnum - 1);
-        if (*l != NUL && l[STRLEN(l) - 1] == '\\')
+        if (*l != NUL && l[ strlen((char *)(l))  - 1] == '\\')
         {
             cur_amount = cin_get_equal_amount(cur_curpos.lnum - 1);
             if (cur_amount > 0)
@@ -26488,7 +24824,7 @@
 
         if (*look == '^' && look[1] >= '?' && look[1] <= '_')
         {
-            if (try_match && keytyped == Ctrl_chr(look[1]))
+            if (try_match && keytyped ==  ( (((look[1]) < 'a' || (look[1]) > 'z') ? (look[1]) : (look[1]) - ('a' - 'A'))  ^ 0x40) )
             {
                 return TRUE;
             }
@@ -26516,7 +24852,7 @@
             if (try_match && keytyped == 'e' && curwin->w_cursor.col >= 4)
             {
                 p = ml_get_curline();
-                if (skipwhite(p) == p + curwin->w_cursor.col - 4 && STRNCMP(p + curwin->w_cursor.col - 4, "else", 4) == 0)
+                if (skipwhite(p) == p + curwin->w_cursor.col - 4 &&  strncmp((char *)(p + curwin->w_cursor.col - 4), (char *)("else"), (4))  == 0)
                 {
                     return TRUE;
                 }
@@ -26588,7 +24924,7 @@
             p = vim_strchr(look, ',');
             if (p == NULL)
             {
-                p = look + STRLEN(look);
+                p = look +  strlen((char *)(look)) ;
             }
             if ((try_match || try_match_word) && curwin->w_cursor.col >= (colnr_T)(p - look))
             {
@@ -26622,17 +24958,17 @@
                             }
                         }
                     }
-                    if (s + (p - look) <= line + curwin->w_cursor.col && (icase ? MB_STRNICMP(s, look, p - look) : STRNCMP(s, look, p - look)) == 0)
+                    if (s + (p - look) <= line + curwin->w_cursor.col && (icase ?  mb_strnicmp((char_u *)(s), (char_u *)(look), (int)(p - look))  :  strncmp((char *)(s), (char *)(look), (p - look)) ) == 0)
                     {
                         match = TRUE;
                     }
                 }
                 else
                 {
-                    if (keytyped == (int)p[-1] || (icase && keytyped < 256 && keytyped >= 0 && TOLOWER_LOC(keytyped) == TOLOWER_LOC((int)p[-1])))
+                    if (keytyped == (int)p[-1] || (icase && keytyped < 256 && keytyped >= 0 &&   (tolower ((unsigned char)(keytyped)))  ==   (tolower ((unsigned char)((int)p[-1]))) ))
                 {
                     line = ml_get_cursor();
-                    if ((curwin->w_cursor.col == (colnr_T)(p - look) || !vim_iswordc(line[-(p - look) - 1])) && (icase ? MB_STRNICMP(line - (p - look), look, p - look) : STRNCMP(line - (p - look), look, p - look)) == 0)
+                    if ((curwin->w_cursor.col == (colnr_T)(p - look) || !vim_iswordc(line[-(p - look) - 1])) && (icase ?  mb_strnicmp((char_u *)(line - (p - look)), (char_u *)(look), (int)(p - look))  :  strncmp((char *)(line - (p - look)), (char *)(look), (p - look)) ) == 0)
                     {
                         match = TRUE;
                     }
@@ -26695,8 +25031,6 @@
 static int compl_selected;
 static string_T cmdline_orig = {NULL, 0};
 
-#define SHOW_MATCH(m) (showtail ? showmatches_gettail(matches[m]) : matches[m])
-
     static int
 cmdline_fuzzy_completion_supported(expand_T *xp)
 {
@@ -26752,7 +25086,7 @@
     {
         return 1;
     }
-    return STRCMP(p1, p2);
+    return  strcmp((char *)(p1), (char *)(p2)) ;
 }
 
     static void
@@ -26855,7 +25189,7 @@
         cmd_showtail = expand_showtail(xp);
     }
 
-    if (xp->xp_context == EXPAND_UNSUCCESSFUL)
+    if (xp->xp_context ==  (-2) )
     {
         beep_flush();
         return OK;
@@ -26929,9 +25263,10 @@
                         break;
                     }
                 }
-                if ((int)STRLEN(p) < j)
+                if ((int) strlen((char *)(p))  < j)
                 {
-                    VIM_CLEAR(p);
+                     vim_free(p);
+                     (p) = NULL;
                 }
             }
         }
@@ -26953,7 +25288,7 @@
 
     if (p != NULL && !got_int && !(options & (WILD_NOSELECT | WILD_NOINSERT)))
     {
-        size_t  plen = STRLEN(p);
+        size_t  plen =  strlen((char *)(p)) ;
         int     difflen;
         int     v = OK;
 
@@ -26966,8 +25301,8 @@
 
         if (v == OK)
         {
-            mch_memmove(&ccline->cmdbuff[ccline->cmdpos + difflen], &ccline->cmdbuff[ccline->cmdpos], (size_t)(ccline->cmdlen - ccline->cmdpos + 1));
-            mch_memmove(&ccline->cmdbuff[i], p, plen);
+             memmove((char *)(&ccline->cmdbuff[ccline->cmdpos + difflen]), (char *)(&ccline->cmdbuff[ccline->cmdpos]), (size_t)(ccline->cmdlen - ccline->cmdpos + 1)) ;
+             memmove((char *)(&ccline->cmdbuff[i]), (char *)(p), plen) ;
             ccline->cmdlen += difflen;
             ccline->cmdpos += difflen;
         }
@@ -27000,10 +25335,10 @@
 {
     int prefix_len;
 
-    cmdline_match_array = ALLOC_MULT(pumitem_T, numMatches);
+    cmdline_match_array =  (pumitem_T *)alloc(sizeof(pumitem_T) * (numMatches)) ;
     if (cmdline_match_array == NULL)
     {
-        return EXPAND_UNSUCCESSFUL;
+        return  (-2) ;
     }
 
     cmdline_match_arraysize = numMatches;
@@ -27011,7 +25346,7 @@
     {
         cmdline_match_array[i].pum_text = (xp->xp_files_abbr != NULL && xp->xp_files_abbr[i] != NULL)
                                         ? xp->xp_files_abbr[i]
-                                        : SHOW_MATCH(i);
+                                        :  (showtail ? showmatches_gettail(matches[i]) : matches[i]) ;
         cmdline_match_array[i].pum_info = xp->xp_files_info != NULL
                                             ? xp->xp_files_info[i] : NULL;
         cmdline_match_array[i].pum_extra = xp->xp_files_menu != NULL
@@ -27031,7 +25366,7 @@
     }
     compl_startcol = cmdline_col_off + MAX(0, compl_startcol - prefix_len);
 
-    return EXPAND_OK;
+    return  (-1) ;
 }
 
     void
@@ -27051,13 +25386,14 @@
 }
 
     void
-cmdline_pum_remove(cmdline_info_T *cclp UNUSED, int defer_redraw)
+cmdline_pum_remove(cmdline_info_T *cclp  __attribute__((unused)) , int defer_redraw)
 {
     int save_KeyTyped = KeyTyped;
 
     term_set_sync_output(TERM_SYNC_OUTPUT_ENABLE);
     pum_undisplay();
-    VIM_CLEAR(cmdline_match_array);
+     vim_free(cmdline_match_array);
+     (cmdline_match_array) = NULL;
     cmdline_match_arraysize = 0;
     if (!defer_redraw)
     {
@@ -27128,7 +25464,7 @@
     {
         s += skip_status_match_char(xp, s);
         len += ptr2cells(s);
-        MB_PTR_ADV(s);
+         s += (*mb_ptr2len)(s) ;
     }
 
     return len;
@@ -27176,7 +25512,7 @@
         match = 0;
         highlight = FALSE;
     }
-    clen = status_match_len(xp, SHOW_MATCH(match)) + 3;
+    clen = status_match_len(xp,  (showtail ? showmatches_gettail(matches[match]) : matches[match]) ) + 3;
     if (match == 0)
     {
         first_match = 0;
@@ -27190,7 +25526,7 @@
     {
         for (i = first_match; i < match; ++i)
         {
-            clen += status_match_len(xp, SHOW_MATCH(i)) + 2;
+            clen += status_match_len(xp,  (showtail ? showmatches_gettail(matches[i]) : matches[i]) ) + 2;
         }
         if (first_match > 0)
         {
@@ -27202,7 +25538,7 @@
             clen = 2;
             for (i = match; i < num_matches; ++i)
             {
-                clen += status_match_len(xp, SHOW_MATCH(i)) + 2;
+                clen += status_match_len(xp,  (showtail ? showmatches_gettail(matches[i]) : matches[i]) ) + 2;
                 if (clen >= topframe->fr_width)
                 {
                     break;
@@ -27218,7 +25554,7 @@
     {
         while (first_match > 0)
         {
-            clen += status_match_len(xp, SHOW_MATCH(first_match - 1)) + 2;
+            clen += status_match_len(xp,  (showtail ? showmatches_gettail(matches[first_match - 1]) : matches[first_match - 1]) ) + 2;
             if (clen >= topframe->fr_width)
             {
                 break;
@@ -27236,13 +25572,13 @@
     }
     else
     {
-        STRCPY(buf, "< ");
+         strcpy((char *)(buf), (char *)("< ")) ;
         len = 2;
     }
     clen = len;
 
     i = first_match;
-    while (clen + status_match_len(xp, SHOW_MATCH(i)) + 2 < topframe->fr_width)
+    while (clen + status_match_len(xp,  (showtail ? showmatches_gettail(matches[i]) : matches[i]) ) + 2 < topframe->fr_width)
     {
         if (i == match)
         {
@@ -27250,21 +25586,21 @@
             selstart_col = clen;
         }
 
-        s = SHOW_MATCH(i);
+        s =  (showtail ? showmatches_gettail(matches[i]) : matches[i]) ;
         for ( ; *s != NUL; ++s)
         {
             s += skip_status_match_char(xp, s);
             clen += ptr2cells(s);
             if (has_mbyte && (l = (*mb_ptr2len)(s)) > 1)
             {
-                STRNCPY(buf + len, s, l);
+                 strncpy((char *)(buf + len), (char *)(s), (l)) ;
                 s += l - 1;
                 len += l;
             }
             else
             {
-                STRCPY(buf + len, transchar_byte(*s));
-                len += (int)STRLEN(buf + len);
+                 strcpy((char *)(buf + len), (char *)(transchar_byte(*s))) ;
+                len += (int) strlen((char *)(buf + len)) ;
             }
         }
         if (i == match)
@@ -27326,7 +25662,7 @@
         if (selstart != NULL && highlight)
         {
             *selend = NUL;
-            screen_puts(selstart, row, firstwin->w_wincol + selstart_col, HL_ATTR(HLF_WM));
+            screen_puts(selstart, row, firstwin->w_wincol + selstart_col,  highlight_attr[(int)(HLF_WM)] );
         }
 
         screen_fill(row, row + 1, firstwin->w_wincol + clen, firstwin->w_wincol + topframe->fr_width, fillchar, fillchar, attr);
@@ -27420,7 +25756,7 @@
         }
         else if (vim_strchr(p_wop, WOP_PUM) != NULL)
         {
-            if (cmdline_pum_create(get_cmdline_info(), xp, xp->xp_files, xp->xp_numfiles, cmd_showtail) == EXPAND_OK)
+            if (cmdline_pum_create(get_cmdline_info(), xp, xp->xp_files, xp->xp_numfiles, cmd_showtail) ==  (-1) )
             {
                 compl_selected = findex;
                 pum_clear();
@@ -27537,7 +25873,7 @@
             }
             if (p_fic && (xp->xp_context == EXPAND_DIRECTORIES || xp->xp_context == EXPAND_FILES || xp->xp_context == EXPAND_SHELLCMD || xp->xp_context == EXPAND_BUFFERS))
             {
-                if (MB_TOLOWER(c0) != MB_TOLOWER(ci))
+                if ( vim_tolower(c0)  !=  vim_tolower(ci) )
                 {
                     break;
                 }
@@ -27623,7 +25959,8 @@
         FreeWild(xp->xp_numfiles, xp->xp_files);
         free_xp_files_extra(xp, xp->xp_numfiles);
         xp->xp_numfiles = -1;
-        VIM_CLEAR(xp->xp_orig);
+         vim_free(xp->xp_orig);
+         (xp->xp_orig) = NULL;
 
         if (cmdline_match_array != NULL)
         {
@@ -27664,17 +26001,17 @@
         if (xp->xp_prefix == XP_PREFIX_NO)
         {
             prefix = "no";
-            ss_size = STRLEN_LITERAL("no") * n;
+            ss_size =  (sizeof("no" "") - 1)  * n;
         }
         else if (xp->xp_prefix == XP_PREFIX_INV)
         {
             prefix = "inv";
-            ss_size = STRLEN_LITERAL("inv") * n;
+            ss_size =  (sizeof("inv" "") - 1)  * n;
         }
 
         for (i = 0; i < xp->xp_numfiles; ++i)
         {
-            ss_size += STRLEN(files[i]) + 1;
+            ss_size +=  strlen((char *)(files[i]))  + 1;
         }
         ++ss_size;
 
@@ -27706,7 +26043,7 @@
     void
 ExpandInit(expand_T *xp)
 {
-    CLEAR_POINTER(xp);
+      memset(((xp)), (0), (sizeof(*(xp))))  ;
     xp->xp_backslash = XP_BS_NONE;
     xp->xp_prefix = XP_PREFIX_NONE;
     xp->xp_numfiles = -1;
@@ -27721,13 +26058,16 @@
         FreeWild(xp->xp_numfiles, xp->xp_files);
         xp->xp_numfiles = -1;
     }
-    VIM_CLEAR(xp->xp_orig);
+     vim_free(xp->xp_orig);
+     (xp->xp_orig) = NULL;
 }
 
     void
 clear_cmdline_orig(void)
 {
-    VIM_CLEAR_STRING(cmdline_orig);
+     vim_free(cmdline_orig.string);
+     (cmdline_orig.string) = NULL;
+     cmdline_orig.length = 0;
 }
 
     static void
@@ -27744,12 +26084,12 @@
     {
         if (xp->xp_context == EXPAND_TAGS_LISTFILES)
         {
-            msg_outtrans_attr(matches[j], HL_ATTR(HLF_D));
-            p = matches[j] + STRLEN(matches[j]) + 1;
+            msg_outtrans_attr(matches[j],  highlight_attr[(int)(HLF_D)] );
+            p = matches[j] +  strlen((char *)(matches[j]))  + 1;
             msg_advance(maxlen + 1);
             msg_puts((char *)p);
             msg_advance(maxlen + 3);
-            msg_outtrans_long_attr(p + 2, HL_ATTR(HLF_D));
+            msg_outtrans_long_attr(p + 2,  highlight_attr[(int)(HLF_D)] );
             break;
         }
         for (i = maxlen - lastlen; --i >= 0; )
@@ -27780,18 +26120,18 @@
             }
             if (showtail)
             {
-                p = SHOW_MATCH(j);
+                p =  (showtail ? showmatches_gettail(matches[j]) : matches[j]) ;
             }
             else
             {
-                home_replace(NULL, matches[j], NameBuff, MAXPATHL, TRUE);
+                home_replace(NULL, matches[j], NameBuff,  PATH_MAX , TRUE);
                 p = NameBuff;
             }
         }
         else
         {
             isdir = FALSE;
-            p = SHOW_MATCH(j);
+            p =  (showtail ? showmatches_gettail(matches[j]) : matches[j]) ;
         }
         lastlen = msg_outtrans_attr(p, isdir ? dir_attr : 0);
     }
@@ -27808,7 +26148,7 @@
 {
     int retval = cmdline_pum_create(ccline, xp, matches, numMatches, showtail);
 
-    if (retval == EXPAND_OK)
+    if (retval ==  (-1) )
     {
         compl_selected = noselect ? -1 : 0;
         pum_clear();
@@ -27838,7 +26178,7 @@
         int retval;
         set_expand_context(xp);
         retval = expand_cmdline(xp, ccline->cmdbuff, ccline->cmdpos, &numMatches, &matches);
-        if (retval != EXPAND_OK)
+        if (retval !=  (-1) )
         {
             return retval;
         }
@@ -27883,12 +26223,12 @@
             int len;
             if (!showtail && (xp->xp_context == EXPAND_FILES || xp->xp_context == EXPAND_SHELLCMD || xp->xp_context == EXPAND_BUFFERS))
             {
-                home_replace(NULL, matches[i], NameBuff, MAXPATHL, TRUE);
+                home_replace(NULL, matches[i], NameBuff,  PATH_MAX , TRUE);
                 len = vim_strsize(NameBuff);
             }
             else
             {
-                len = vim_strsize(SHOW_MATCH(i));
+                len = vim_strsize( (showtail ? showmatches_gettail(matches[i]) : matches[i]) );
             }
             if (len > maxlen)
             {
@@ -27911,14 +26251,14 @@
             lines = (numMatches + columns - 1) / columns;
         }
 
-        attr = HL_ATTR(HLF_D);
+        attr =  highlight_attr[(int)(HLF_D)] ;
 
         if (xp->xp_context == EXPAND_TAGS_LISTFILES)
         {
-            msg_puts_attr(_("tagname"), HL_ATTR(HLF_T));
+            msg_puts_attr(_("tagname"),  highlight_attr[(int)(HLF_T)] );
             msg_clr_eos();
             msg_advance(maxlen - 3);
-            msg_puts_attr(_(" kind file\n"), HL_ATTR(HLF_T));
+            msg_puts_attr(_(" kind file\n"),  highlight_attr[(int)(HLF_T)] );
         }
 
         for (i = 0; i < lines; ++i)
@@ -27952,7 +26292,7 @@
         free_xp_files_extra(xp, numMatches);
     }
 
-    return EXPAND_OK;
+    return  (-1) ;
 }
 
     static char_u *
@@ -27973,7 +26313,7 @@
             t = p;
             had_sep = FALSE;
         }
-        MB_PTR_ADV(p);
+         p += (*mb_ptr2len)(p) ;
     }
     return t;
 }
@@ -28021,7 +26361,6 @@
 
     if (context != EXPAND_FILES && context != EXPAND_FILES_IN_PATH && context != EXPAND_SHELLCMD && context != EXPAND_DIRECTORIES && context != EXPAND_DIRS_IN_CDPATH)
     {
-
         if (context == EXPAND_FINDFUNC || context == EXPAND_HELP || context == EXPAND_COLORS || context == EXPAND_COMPILER || context == EXPAND_OWNSYNTAX || context == EXPAND_FILETYPE || context == EXPAND_KEYMAP || context == EXPAND_PACKADD || context == EXPAND_RUNTIME || ((context == EXPAND_TAGS_LISTFILES || context == EXPAND_TAGS) && fname[0] == '/'))
         {
             retval = vim_strnsave(fname, len);
@@ -28127,7 +26466,7 @@
     if ((ccline->cmdfirstc == '/' || ccline->cmdfirstc == '?') && may_expand_pattern)
     {
         xp->xp_context = EXPAND_PATTERN_IN_BUF;
-        xp->xp_search_dir = (ccline->cmdfirstc == '/') ? FORWARD : BACKWARD;
+        xp->xp_search_dir = (ccline->cmdfirstc == '/') ? FORWARD :  (-1) ;
         xp->xp_pattern = ccline->cmdbuff;
         xp->xp_pattern_len = ccline->cmdpos;
         search_first_line = 0;
@@ -28158,13 +26497,13 @@
     else
     {
         p = cmd;
-        while (ASCII_ISALPHA(*p) || *p == '*')
+        while ( ( ((unsigned)(*p) - 'A' < 26)  ||  ((unsigned)(*p) - 'a' < 26) )  || *p == '*')
         {
             ++p;
         }
-        if (ASCII_ISUPPER(cmd[0]) || STRNCMP("vim9", cmd, 4) == 0)
+        if ( ((unsigned)(cmd[0]) - 'A' < 26)  ||  strncmp((char *)("vim9"), (char *)(cmd), (4))  == 0)
         {
-            while (ASCII_ISALNUM(*p) || *p == '*')
+            while ( ( ( ((unsigned)(*p) - 'A' < 26)  ||  ((unsigned)(*p) - 'a' < 26) )  ||  ((unsigned)(*p) - '0' < 10) )  || *p == '*')
             {
                 ++p;
             }
@@ -28172,7 +26511,7 @@
         if (cmd[0] == 'p' && cmd[1] == 'y' && p == cmd + 2 && *p == '3')
         {
             ++p;
-            while (ASCII_ISALPHA(*p) || *p == '*')
+            while ( ( ((unsigned)(*p) - 'A' < 26)  ||  ((unsigned)(*p) - 'a' < 26) )  || *p == '*')
             {
                 ++p;
             }
@@ -28185,7 +26524,7 @@
 
         if (len == 0)
         {
-            xp->xp_context = EXPAND_UNSUCCESSFUL;
+            xp->xp_context =  (-2) ;
             return NULL;
         }
 
@@ -28193,14 +26532,14 @@
 
         if ((cmd[0] >= 'A' && cmd[0] <= 'Z') || (fuzzy && eap->cmdidx != CMD_bang && *p != NUL))
         {
-            while (ASCII_ISALNUM(*p) || *p == '*')
+            while ( ( ( ((unsigned)(*p) - 'A' < 26)  ||  ((unsigned)(*p) - 'a' < 26) )  ||  ((unsigned)(*p) - '0' < 10) )  || *p == '*')
             {
                 ++p;
             }
         }
     }
 
-    if (*p == NUL && ASCII_ISALNUM(p[-1]))
+    if (*p == NUL &&  ( ( ((unsigned)(p[-1]) - 'A' < 26)  ||  ((unsigned)(p[-1]) - 'a' < 26) )  ||  ((unsigned)(p[-1]) - '0' < 10) ) )
     {
         return NULL;
     }
@@ -28224,7 +26563,7 @@
     }
     if (eap->cmdidx == CMD_SIZE)
     {
-        xp->xp_context = EXPAND_UNSUCCESSFUL;
+        xp->xp_context =  (-2) ;
         return NULL;
     }
 
@@ -28265,7 +26604,7 @@
             }
             in_quote = !in_quote;
         }
-        else if (c == '|' || c == '\n' || c == '"' || (VIM_ISWHITE(c) && (!(eap != NULL && (eap->argt & EX_NOSPC)) || usefilter)))
+        else if (c == '|' || c == '\n' || c == '"' || ( ((c) == ' ' || (c) == '\t')  && (!(eap != NULL && (eap->argt & EX_NOSPC)) || usefilter)))
         {
             len = 0;
             while (*p != NUL)
@@ -28290,7 +26629,7 @@
                 {
                     len = 1;
                 }
-                MB_PTR_ADV(p);
+                 p += (*mb_ptr2len)(p) ;
             }
             if (in_quote)
             {
@@ -28302,7 +26641,7 @@
             }
             p -= len;
         }
-        MB_PTR_ADV(p);
+         p += (*mb_ptr2len)(p) ;
     }
 
     if (bow != NULL && in_quote)
@@ -28515,7 +26854,7 @@
     }
     else
     {
-        if ( STRNCMP(arg, "messages", p - arg) == 0 || STRNCMP(arg, "ctype", p - arg) == 0 || STRNCMP(arg, "time", p - arg) == 0 || STRNCMP(arg, "collate", p - arg) == 0)
+        if (  strncmp((char *)(arg), (char *)("messages"), (p - arg))  == 0 ||  strncmp((char *)(arg), (char *)("ctype"), (p - arg))  == 0 ||  strncmp((char *)(arg), (char *)("time"), (p - arg))  == 0 ||  strncmp((char *)(arg), (char *)("collate"), (p - arg))  == 0)
         {
             xp->xp_context = EXPAND_LOCALES;
             xp->xp_pattern = skipwhite(p);
@@ -28537,9 +26876,8 @@
     EXP_FILETYPECMD_ONOFF,
 } filetype_expand_what;
 
-#define EXPAND_FILETYPECMD_PLUGIN 0x01
-#define EXPAND_FILETYPECMD_INDENT 0x02
-#define EXPAND_FILETYPECMD_ONOFF  0x04
+enum { EXPAND_FILETYPECMD_PLUGIN = 0x01 };
+enum { EXPAND_FILETYPECMD_INDENT = 0x02 };
 
     static char_u *
 set_context_in_filetype_cmd(expand_T *xp, char_u *arg)
@@ -28559,13 +26897,13 @@
 
     for (;;)
     {
-        if (STRNCMP(p, "plugin", 6) == 0 && VIM_ISWHITE(p[6]))
+        if ( strncmp((char *)(p), (char *)("plugin"), (6))  == 0 &&  ((p[6]) == ' ' || (p[6]) == '\t') )
         {
             val |= EXPAND_FILETYPECMD_PLUGIN;
             p = skipwhite(p + 6);
             continue;
         }
-        if (STRNCMP(p, "indent", 6) == 0 && VIM_ISWHITE(p[6]))
+        if ( strncmp((char *)(p), (char *)("indent"), (6))  == 0 &&  ((p[6]) == ' ' || (p[6]) == '\t') )
         {
             val |= EXPAND_FILETYPECMD_INDENT;
             p = skipwhite(p + 6);
@@ -28776,6 +27114,7 @@
             {
                 arg = xp->xp_pattern + 1;
             }
+        __attribute__((fallthrough));
         case CMD_buffer:
         case CMD_sbuffer:
         case CMD_pbuffer:
@@ -28979,7 +27318,7 @@
         ++p;
     }
 
-    if (!IS_USER_CMDIDX(ea.cmdidx))
+    if (! ((int)(ea.cmdidx) < 0) )
     {
         ea.argt = excmd_get_argt(ea.cmdidx);
     }
@@ -28988,12 +27327,12 @@
 
     if ((ea.argt & EX_ARGOPT) || ea.cmdidx == CMD_terminal)
     {
-        while (*arg != NUL && STRNCMP(arg, "++", 2) == 0)
+        while (*arg != NUL &&  strncmp((char *)(arg), (char *)("++"), (2))  == 0)
         {
             p = arg + 2;
             while (*p && !vim_isspace(*p))
             {
-                MB_PTR_ADV(p);
+                 p += (*mb_ptr2len)(p) ;
             }
 
             if (*p == NUL)
@@ -29084,7 +27423,7 @@
                     return NULL;
                 }
             }
-            MB_PTR_ADV(p);
+             p += (*mb_ptr2len)(p) ;
         }
     }
 
@@ -29095,7 +27434,7 @@
 
     p = buff;
     xp->xp_pattern = p;
-    len = (int)STRLEN(buff);
+    len = (int) strlen((char *)(buff)) ;
     while (*p && p < buff + len)
     {
         if (*p == ' ' || *p == TAB)
@@ -29108,7 +27447,7 @@
             {
                 ++p;
             }
-            MB_PTR_ADV(p);
+             p += (*mb_ptr2len)(p) ;
         }
     }
 
@@ -29121,7 +27460,7 @@
 }
 
     void
-set_cmd_context(expand_T    *xp, char_u      *str, int         len, int         col, int         use_ccline UNUSED)
+set_cmd_context(expand_T    *xp, char_u      *str, int         len, int         col, int         use_ccline  __attribute__((unused)) )
 {
     int         old_char = NUL;
     char_u      *nextcomm;
@@ -29153,10 +27492,10 @@
     char_u      *file_str = NULL;
     int         options = WILD_ADD_SLASH|WILD_SILENT;
 
-    if (xp->xp_context == EXPAND_UNSUCCESSFUL)
+    if (xp->xp_context ==  (-2) )
     {
         beep_flush();
-        return EXPAND_UNSUCCESSFUL;
+        return  (-2) ;
     }
     if (xp->xp_context == EXPAND_NOTHING)
     {
@@ -29179,7 +27518,7 @@
     }
     if (file_str == NULL)
     {
-        return EXPAND_UNSUCCESSFUL;
+        return  (-2) ;
     }
 
     if (p_wic)
@@ -29194,7 +27533,7 @@
     }
     vim_free(file_str);
 
-    return EXPAND_OK;
+    return  (-1) ;
 }
 
     static int
@@ -29211,7 +27550,7 @@
 
         free_pat = TRUE;
 
-        pat_len = STRLEN(pat);
+        pat_len =  strlen((char *)(pat)) ;
         pat = vim_strnsave(pat, pat_len);
         if (pat == NULL)
         {
@@ -29231,13 +27570,13 @@
             if (xp->xp_backslash & XP_BS_THREE && *(p + 1) == '\\' && *(p + 2) == '\\' && *(p + 3) == ' ')
             {
                 from = p + 3;
-                mch_memmove(p, from, (size_t)(pat_end - from) + 1);
+                 memmove((char *)(p), (char *)(from), (size_t)(pat_end - from) + 1) ;
                 pat_end -= 3;
             }
             else if (xp->xp_backslash & XP_BS_ONE && *(p + 1) == ' ')
             {
                 from = p + 1;
-                mch_memmove(p, from, (size_t)(pat_end - from) + 1);
+                 memmove((char *)(p), (char *)(from), (size_t)(pat_end - from) + 1) ;
                 --pat_end;
             }
             else if (xp->xp_backslash & XP_BS_COMMA)
@@ -29245,7 +27584,7 @@
                 if (*(p + 1) == '\\' && *(p + 2) == ',')
                 {
                     from = p + 2;
-                    mch_memmove(p, from, (size_t)(pat_end - from) + 1);
+                     memmove((char *)(p), (char *)(from), (size_t)(pat_end - from) + 1) ;
                     pat_end -= 2;
                 }
             }
@@ -29288,7 +27627,7 @@
 }
 
     static char_u *
-get_behave_arg(expand_T *xp UNUSED, int idx)
+get_behave_arg(expand_T *xp  __attribute__((unused)) , int idx)
 {
     if (idx == 0)
     {
@@ -29302,7 +27641,7 @@
 }
 
     static char_u *
-get_filetypecmd_arg(expand_T *xp UNUSED, int idx)
+get_filetypecmd_arg(expand_T *xp  __attribute__((unused)) , int idx)
 {
     if (idx < 0)
     {
@@ -29333,7 +27672,7 @@
 }
 
     static char_u *
-get_retab_arg(expand_T *xp UNUSED, int idx)
+get_retab_arg(expand_T *xp  __attribute__((unused)) , int idx)
 {
     if (idx == 0)
     {
@@ -29343,7 +27682,7 @@
 }
 
     static char_u *
-get_messages_arg(expand_T *xp UNUSED, int idx)
+get_messages_arg(expand_T *xp  __attribute__((unused)) , int idx)
 {
     if (idx == 0)
     {
@@ -29353,7 +27692,7 @@
 }
 
     static char_u *
-get_mapclear_arg(expand_T *xp UNUSED, int idx)
+get_mapclear_arg(expand_T *xp  __attribute__((unused)) , int idx)
 {
     if (idx == 0)
     {
@@ -29398,7 +27737,7 @@
     int i;
     int ret = FAIL;
 
-    for (i = 0; i < (int)ARRAY_LENGTH(tab); ++i)
+    for (i = 0; i < (int) (sizeof(tab) / sizeof((tab)[0])) ; ++i)
     {
         if (xp->xp_context == tab[i].context)
         {
@@ -29525,9 +27864,9 @@
         return expand_pattern_in_buf(pat, xp->xp_search_dir, matches, numMatches);
     }
 
-    if ((xp->xp_context == EXPAND_USER_FUNC || xp->xp_context == EXPAND_DISASSEMBLE) && STRNCMP(pat, "^s:", 3) == 0)
+    if ((xp->xp_context == EXPAND_USER_FUNC || xp->xp_context == EXPAND_DISASSEMBLE) &&  strncmp((char *)(pat), (char *)("^s:"), (3))  == 0)
     {
-        int len = (int)STRLEN(pat) + 20;
+        int len = (int) strlen((char *)(pat))  + 20;
 
         tofree = alloc(len);
         if (tofree == NULL)
@@ -29641,7 +27980,7 @@
             else
             {
                 score = fuzzy_match_str(str, pat);
-                match = (score != FUZZY_SCORE_NONE);
+                match = (score !=  INT_MIN );
             }
         }
         else
@@ -29760,7 +28099,7 @@
     for (int i = 0; i < *numMatches; ++i)
     {
         char_u  *name = (*matches)[i];
-        size_t  namelen = STRLEN(name);
+        size_t  namelen =  strlen((char *)(name)) ;
 
         if (namelen > pathlen)
         {
@@ -29769,9 +28108,9 @@
 
             hash = hash_hash(name + pathlen);
             hi = hash_lookup(ht, name + pathlen, hash);
-            if (HASHITEM_EMPTY(hi))
+            if ( ((hi)->hi_key == NULL || (hi)->hi_key == &hash_removed) )
             {
-                mch_memmove(name, name + pathlen, (size_t)(namelen - pathlen) + 1);
+                 memmove((char *)(name), (char *)(name + pathlen), (size_t)(namelen - pathlen) + 1) ;
                 ((char_u **)gap->ga_data)[gap->ga_len++] = name;
                 hash_add_item(ht, hi, name, hash);
                 name = NULL;
@@ -29797,13 +28136,13 @@
     int         did_curdir = FALSE;
     hashtab_T   found_ht;
 
-    buf = alloc(MAXPATHL);
+    buf = alloc( PATH_MAX );
     if (buf == NULL)
     {
         return FAIL;
     }
 
-    patlen = STRLEN(filepat);
+    patlen =  strlen((char *)(filepat)) ;
     pat = vim_strnsave(filepat, patlen);
     if (pat == NULL)
     {
@@ -29824,7 +28163,7 @@
         p = s + 1;
         if (*p == ' ')
         {
-            mch_memmove(s, p, (size_t)(e - p) + 1);
+             memmove((char *)(s), (char *)(p), (size_t)(e - p) + 1) ;
             --e;
         }
     }
@@ -29874,11 +28213,11 @@
             e = vim_strchr(s, ':');
             if (e == NULL)
             {
-                e = s + STRLEN(s);
+                e = s +  strlen((char *)(s)) ;
             }
 
             pathlen = (size_t)(e - s);
-            if (STRNCMP(s, ".", pathlen) == 0)
+            if ( strncmp((char *)(s), (char *)("."), (pathlen))  == 0)
             {
                 did_curdir = TRUE;
                 flags |= EW_DIR;
@@ -29888,21 +28227,21 @@
                 flags &= ~EW_DIR;
             }
 
-            seplen = !after_pathsep(s, e) ? sizeof(PATHSEP) : 0;
+            seplen = !after_pathsep(s, e) ? sizeof( ((char_u)'/') ) : 0;
         }
 
-        if (pathlen + seplen + patlen + 1 <= MAXPATHL)
+        if (pathlen + seplen + patlen + 1 <=  PATH_MAX )
         {
             if (pathlen > 0)
             {
                 vim_strncpy(buf, s, pathlen);
                 if (seplen > 0)
                 {
-                    STRCPY(buf + pathlen, PATHSEPSTR);
+                     strcpy((char *)(buf + pathlen), (char *)( "/" )) ;
                     pathlen += seplen;
                 }
             }
-            STRCPY(buf + pathlen, pat);
+             strcpy((char *)(buf + pathlen), (char *)(pat)) ;
 
             expand_shellcmd_onedir(buf, pathlen, matches, numMatches, flags, &found_ht, &ga);
         }
@@ -29934,7 +28273,7 @@
     size_t      seplen;
     size_t      filelen;
 
-    buf = alloc(MAXPATHL);
+    buf = alloc( PATH_MAX );
     if (buf == NULL)
     {
         return;
@@ -29943,28 +28282,26 @@
     ExpandInit(&xpc);
     xpc.xp_context = dirs ? EXPAND_DIRECTORIES : EXPAND_FILES;
 
-    filelen = STRLEN(file);
-
-# define TMP_PATHSEPSTR PATHSEPSTR
+    filelen =  strlen((char *)(file)) ;
 
     while (*path != NUL)
     {
-        pathlen = (size_t)copy_option_part(&path, buf, MAXPATHL, ",");
+        pathlen = (size_t)copy_option_part(&path, buf,  PATH_MAX , ",");
         seplen = (*buf != NUL && !after_pathsep(buf, buf + pathlen))
-            ? STRLEN_LITERAL(TMP_PATHSEPSTR)
+            ?  (sizeof(  "/"   "") - 1) 
             : 0;
 
-        if (pathlen + seplen + filelen + 1 <= MAXPATHL)
+        if (pathlen + seplen + filelen + 1 <=  PATH_MAX )
         {
             int     num_p;
             char_u  **p;
 
             if (seplen > 0)
             {
-                STRCPY(buf + pathlen, TMP_PATHSEPSTR);
+                 strcpy((char *)(buf + pathlen), (char *)(  "/"  )) ;
                 pathlen += seplen;
             }
-            STRCPY(buf + pathlen, file);
+             strcpy((char *)(buf + pathlen), (char *)(file)) ;
 
             if (ExpandFromContext(&xpc, buf, &p, &num_p, WILD_SILENT|expand_options) != FAIL && num_p > 0)
             {
@@ -30000,17 +28337,17 @@
     {
         switch (c)
         {
-            case K_UP:
-                c = K_LEFT;
+            case   (-(('k') + ((int)('u') << 8)))  :
+                c =   (-(('k') + ((int)('l') << 8)))  ;
                 break;
-            case K_DOWN:
-                c = K_RIGHT;
+            case   (-(('k') + ((int)('d') << 8)))  :
+                c =   (-(('k') + ((int)('r') << 8)))  ;
                 break;
-            case K_LEFT:
-                c = K_UP;
+            case   (-(('k') + ((int)('l') << 8)))  :
+                c =   (-(('k') + ((int)('u') << 8)))  ;
                 break;
-            case K_RIGHT:
-                c = K_DOWN;
+            case   (-(('k') + ((int)('r') << 8)))  :
+                c =   (-(('k') + ((int)('d') << 8)))  ;
                 break;
             default:
                 break;
@@ -30019,19 +28356,19 @@
 
     if (cmdline_pum_active() || did_wild_list || wild_menu_showing)
     {
-        if (c == K_LEFT)
+        if (c ==   (-(('k') + ((int)('l') << 8)))  )
         {
             c = Ctrl_P;
         }
-        else if (c == K_RIGHT)
+        else if (c ==   (-(('k') + ((int)('r') << 8)))  )
         {
             c = Ctrl_N;
         }
     }
 
-    if (xp->xp_context == EXPAND_MENUNAMES && cclp->cmdpos > 1 && cclp->cmdbuff[cclp->cmdpos - 1] == '.' && cclp->cmdbuff[cclp->cmdpos - 2] != '\\' && (c == '\n' || c == '\r' || c == K_KENTER))
+    if (xp->xp_context == EXPAND_MENUNAMES && cclp->cmdpos > 1 && cclp->cmdbuff[cclp->cmdpos - 1] == '.' && cclp->cmdbuff[cclp->cmdpos - 2] != '\\' && (c == '\n' || c == '\r' || c ==   (-(('K') + ((int)('A') << 8)))  ))
     {
-        c = K_DOWN;
+        c =   (-(('k') + ((int)('d') << 8)))  ;
     }
 
     return c;
@@ -30040,7 +28377,7 @@
     static void
 cmdline_del(cmdline_info_T *cclp, int from)
 {
-    mch_memmove(cclp->cmdbuff + from, cclp->cmdbuff + cclp->cmdpos, (size_t)(cclp->cmdlen - cclp->cmdpos + 1));
+     memmove((char *)(cclp->cmdbuff + from), (char *)(cclp->cmdbuff + cclp->cmdpos), (size_t)(cclp->cmdlen - cclp->cmdpos + 1)) ;
     cclp->cmdlen -= cclp->cmdpos - from;
     cclp->cmdpos = from;
 }
@@ -30051,12 +28388,12 @@
     int         i;
     int         j;
 
-    if (key == K_DOWN && cclp->cmdpos > 0 && cclp->cmdbuff[cclp->cmdpos - 1] == '.')
+    if (key ==   (-(('k') + ((int)('d') << 8)))   && cclp->cmdpos > 0 && cclp->cmdbuff[cclp->cmdpos - 1] == '.')
     {
         key = p_wc;
         KeyTyped = TRUE;
     }
-    else if (key == K_UP)
+    else if (key ==   (-(('k') + ((int)('u') << 8)))  )
     {
         int found = FALSE;
 
@@ -30101,18 +28438,18 @@
     int         j;
     char_u      upseg[5];
 
-    upseg[0] = PATHSEP;
+    upseg[0] =  ((char_u)'/') ;
     upseg[1] = '.';
     upseg[2] = '.';
-    upseg[3] = PATHSEP;
+    upseg[3] =  ((char_u)'/') ;
     upseg[4] = NUL;
 
-    if (key == K_DOWN && cclp->cmdpos > 0 && cclp->cmdbuff[cclp->cmdpos - 1] == PATHSEP && (cclp->cmdpos < 3 || cclp->cmdbuff[cclp->cmdpos - 2] != '.' || cclp->cmdbuff[cclp->cmdpos - 3] != '.'))
+    if (key ==   (-(('k') + ((int)('d') << 8)))   && cclp->cmdpos > 0 && cclp->cmdbuff[cclp->cmdpos - 1] ==  ((char_u)'/')  && (cclp->cmdpos < 3 || cclp->cmdbuff[cclp->cmdpos - 2] != '.' || cclp->cmdbuff[cclp->cmdpos - 3] != '.'))
     {
         key = p_wc;
         KeyTyped = TRUE;
     }
-    else if (STRNCMP(xp->xp_pattern, upseg + 1, 3) == 0 && key == K_DOWN)
+    else if ( strncmp((char *)(xp->xp_pattern), (char *)(upseg + 1), (3))  == 0 && key ==   (-(('k') + ((int)('d') << 8)))  )
     {
         int found = FALSE;
 
@@ -30137,7 +28474,7 @@
             KeyTyped = TRUE;
         }
     }
-    else if (key == K_UP)
+    else if (key ==   (-(('k') + ((int)('u') << 8)))  )
     {
         int found = FALSE;
 
@@ -30167,11 +28504,11 @@
         {
             j = i;
         }
-        else if (STRNCMP(cclp->cmdbuff + j, upseg, 4) == 0)
+        else if ( strncmp((char *)(cclp->cmdbuff + j), (char *)(upseg), (4))  == 0)
         {
             j += 4;
         }
-        else if (STRNCMP(cclp->cmdbuff + j, upseg + 1, 3) == 0 && j == i)
+        else if ( strncmp((char *)(cclp->cmdbuff + j), (char *)(upseg + 1), (3))  == 0 && j == i)
         {
             j += 3;
         }
@@ -30212,7 +28549,7 @@
 }
 
     void
-wildmenu_cleanup(cmdline_info_T *cclp UNUSED)
+wildmenu_cleanup(cmdline_info_T *cclp  __attribute__((unused)) )
 {
     int skt = KeyTyped;
 
@@ -30281,7 +28618,7 @@
     {
         if (exacttext)
         {
-            GA_CONCAT_LITERAL(&ga, "\\n");
+             ga_concat_len((&ga), (char_u *)("\\n"),  (sizeof("\\n" "") - 1) ) ;
         }
         else
         {
@@ -30304,7 +28641,7 @@
             ga_concat_len(&ga, line, linelen);
             if (exacttext)
             {
-                GA_CONCAT_LITERAL(&ga, "\\n");
+                 ga_concat_len((&ga), (char_u *)("\\n"),  (sizeof("\\n" "") - 1) ) ;
             }
             else
             {
@@ -30342,7 +28679,7 @@
     static int
 is_regex_match(char_u *pat, char_u *str)
 {
-    if (STRCMP(pat, str) == 0)
+    if ( strcmp((char *)(pat), (char *)(str))  == 0)
     {
         return TRUE;
     }
@@ -30377,7 +28714,7 @@
 }
 
     static char_u *
-concat_pattern_with_buffer_match(char_u *pat, int pat_len, pos_T *end_match_pos, int lowercase UNUSED)
+concat_pattern_with_buffer_match(char_u *pat, int pat_len, pos_T *end_match_pos, int lowercase  __attribute__((unused)) )
 {
     char_u  *line = ml_get(end_match_pos->lnum);
     char_u  *word_end = find_word_end(line + end_match_pos->col);
@@ -30388,10 +28725,10 @@
     {
         return NULL;
     }
-    mch_memmove(match, pat, pat_len);
+     memmove((char *)(match), (char *)(pat), pat_len) ;
     if (match_len > 0)
     {
-            mch_memmove(match + pat_len, line + end_match_pos->col, match_len);
+             memmove((char *)(match + pat_len), (char *)(line + end_match_pos->col), match_len) ;
     }
     match[pat_len + match_len] = NUL;
     return match;
@@ -30426,9 +28763,9 @@
         return FAIL;
     }
 
-    pat_len = (int)STRLEN(pat);
-    CLEAR_FIELD(cur_match_pos);
-    CLEAR_FIELD(prev_match_pos);
+    pat_len = (int) strlen((char *)(pat)) ;
+      memset((&(cur_match_pos)), (0), (sizeof(cur_match_pos)))  ;
+      memset((&(prev_match_pos)), (0), (sizeof(prev_match_pos)))  ;
     if (has_range)
     {
         cur_match_pos.lnum = search_first_line;
@@ -30463,7 +28800,7 @@
 
         if (compl_started)
         {
-            if ((dir == FORWARD && LTOREQ_POS(cur_match_pos, prev_match_pos)) || (dir == BACKWARD && LTOREQ_POS(prev_match_pos, cur_match_pos)))
+            if ((dir == FORWARD &&  ( (((cur_match_pos).lnum != (prev_match_pos).lnum)               ? (cur_match_pos).lnum < (prev_match_pos).lnum                   : (cur_match_pos).col != (prev_match_pos).col                        ? (cur_match_pos).col < (prev_match_pos).col                     : (cur_match_pos).coladd < (prev_match_pos).coladd)  ||  (((cur_match_pos).lnum == (prev_match_pos).lnum) && ((cur_match_pos).col == (prev_match_pos).col) && ((cur_match_pos).coladd == (prev_match_pos).coladd)) ) ) || (dir ==  (-1)  &&  ( (((prev_match_pos).lnum != (cur_match_pos).lnum)               ? (prev_match_pos).lnum < (cur_match_pos).lnum                   : (prev_match_pos).col != (cur_match_pos).col                        ? (prev_match_pos).col < (cur_match_pos).col                     : (prev_match_pos).coladd < (cur_match_pos).coladd)  ||  (((prev_match_pos).lnum == (cur_match_pos).lnum) && ((prev_match_pos).col == (cur_match_pos).col) && ((prev_match_pos).coladd == (cur_match_pos).coladd)) ) ))
             {
                 if (looped_around)
                 {
@@ -30526,9 +28863,10 @@
 
         for (int i = 0; i < ga.ga_len; ++i)
         {
-            if (STRCMP(match, ((char_u **)ga.ga_data)[i]) == 0)
+            if ( strcmp((char *)(match), (char *)(((char_u **)ga.ga_data)[i]))  == 0)
             {
-                VIM_CLEAR(match);
+                 vim_free(match);
+                 (match) = NULL;
                 break;
             }
         }
@@ -30536,7 +28874,8 @@
         {
             if (ga_grow(&ga, 1) == FAIL)
             {
-                VIM_CLEAR(match);
+                 vim_free(match);
+                 (match) = NULL;
                 goto cleanup;
             }
             ((char_u **)ga.ga_data)[ga.ga_len++] = match;
@@ -30618,11 +28957,11 @@
 };
 
     char_u *
-get_history_arg(expand_T *xp UNUSED, int idx)
+get_history_arg(expand_T *xp  __attribute__((unused)) , int idx)
 {
     char    *short_names = ":=@>?/";
-    int     short_names_count = (int)STRLEN(short_names);
-    int     history_name_count = ARRAY_LENGTH(history_names) - 1;
+    int     short_names_count = (int) strlen((char *)(short_names)) ;
+    int     history_name_count =  (sizeof(history_names) / sizeof((history_names)[0]))  - 1;
 
     if (idx < short_names_count)
     {
@@ -30658,7 +28997,7 @@
     {
         if (newlen > 0)
         {
-            temp = ALLOC_MULT(histentry_T, newlen);
+            temp =  (histentry_T *)alloc(sizeof(histentry_T) * (newlen)) ;
             if (temp == NULL)
             {
                 if (type == 0)
@@ -30769,7 +29108,7 @@
         }
 
         p = history[type][i].hisstr;
-        if (STRCMP(str, p) == 0 && !(writing && history[type][i].viminfo) && (type != HIST_SEARCH || sep == p[history[type][i].hisstrlen + 1]))
+        if ( strcmp((char *)(str), (char *)(p))  == 0 && !(writing && history[type][i].viminfo) && (type != HIST_SEARCH || sep == p[history[type][i].hisstrlen + 1]))
         {
             if (!move_to_front)
             {
@@ -30812,7 +29151,7 @@
 get_histtype(char_u *name)
 {
     int         i;
-    int         len = (int)STRLEN(name);
+    int         len = (int) strlen((char *)(name)) ;
 
     if (len == 0)
     {
@@ -30821,7 +29160,7 @@
 
     for (i = 0; history_names[i] != NULL; ++i)
     {
-        if (STRNICMP(name, history_names[i], len) == 0)
+        if ( strncasecmp((char *)(name), (char *)(history_names[i]), (len))  == 0)
         {
             return i;
         }
@@ -30921,10 +29260,10 @@
         return;
     }
 
-    if (!(VIM_ISDIGIT(*arg) || *arg == '-' || *arg == ','))
+    if (!( ((unsigned)(*arg) - '0' < 10)  || *arg == '-' || *arg == ','))
     {
         end = arg;
-        while (ASCII_ISALPHA(*end) || vim_strchr((char_u *)":=@>/?", *end) != NULL)
+        while ( ( ((unsigned)(*end) - 'A' < 26)  ||  ((unsigned)(*end) - 'a' < 26) )  || vim_strchr((char_u *)":=@>/?", *end) != NULL)
         {
             end++;
         }
@@ -30933,7 +29272,7 @@
         histype1 = get_histtype(arg);
         if (histype1 == -1)
         {
-            if (STRNICMP(arg, "all", STRLEN(arg)) == 0)
+            if ( strncasecmp((char *)(arg), (char *)("all"), ( strlen((char *)(arg)) ))  == 0)
             {
                 histype1 = 0;
                 histype2 = HIST_COUNT-1;
@@ -30970,7 +29309,7 @@
 
     for (; !got_int && histype1 <= histype2; ++histype1)
     {
-        vim_snprintf((char *)IObuff, IOSIZE, "\n      #  %s history", history_names[histype1]);
+        vim_snprintf((char *)IObuff,  (1024+1) , "\n      #  %s history", history_names[histype1]);
         msg_puts_title((char *)IObuff);
         idx = hisidx[histype1];
         hist = history[histype1];
@@ -30997,14 +29336,14 @@
                     int  len;
 
                     msg_putchar('\n');
-                    len = vim_snprintf((char *)IObuff, IOSIZE, "%c%6d  ", i == idx ? '>' : ' ', hist[i].hisnum);
+                    len = vim_snprintf((char *)IObuff,  (1024+1) , "%c%6d  ", i == idx ? '>' : ' ', hist[i].hisnum);
                     if (vim_strsize(hist[i].hisstr) > (int)Columns - 10)
                     {
-                        trunc_string(hist[i].hisstr, IObuff + len, (int)Columns - 10, IOSIZE - (int)len);
+                        trunc_string(hist[i].hisstr, IObuff + len, (int)Columns - 10,  (1024+1)  - (int)len);
                     }
                     else
                     {
-                        STRCPY(IObuff + len, hist[i].hisstr);
+                         strcpy((char *)(IObuff + len), (char *)(hist[i].hisstr)) ;
                     }
                     msg_outtrans(IObuff);
                     out_flush();
@@ -31023,19 +29362,17 @@
     static int
 get_lcs_ext(win_T *wp)
 {
-    if (wp->w_p_wrap)
+    if (wp-> w_onebuf_opt.wo_wrap )
     {
         return NUL;
     }
-    if (wp->w_p_wrap_flags & P_INSECURE)
+    if (wp-> w_onebuf_opt.wo_wrap_flags  & P_INSECURE)
     {
         return '>';
     }
-    return wp->w_p_list ? wp->w_lcs_chars.ext : NUL;
+    return wp-> w_onebuf_opt.wo_list  ? wp->w_lcs_chars.ext : NUL;
 }
 
-# define LINE_ATTR
-
 typedef struct {
     int         draw_state;
 
@@ -31090,45 +29427,38 @@
     int         filler_todo;
 } winlinevars_T;
 
-#define WL_START        0
-#define WL_CMDLINE      (WL_START + 1)
-# define WL_FOLD        WL_CMDLINE
-# define WL_SIGN        WL_FOLD
-#define WL_NR           (WL_SIGN + 1)
-# define WL_BRI         WL_NR
-# define WL_SBR         WL_BRI
-#define WL_LINE         (WL_SBR + 1)
+enum { WL_START = 0 };
 
     static void
-handle_lnum_col(win_T           *wp, winlinevars_T   *wlv, int             sign_present UNUSED, int             num_attr UNUSED)
+handle_lnum_col(win_T           *wp, winlinevars_T   *wlv, int             sign_present  __attribute__((unused)) , int             num_attr  __attribute__((unused)) )
 {
     int has_cpo_n = vim_strchr(p_cpo, CPO_NUMCOL) != NULL;
     int lnum_row = wlv->startrow + wlv->filler_lines
                       ;
 
-    if ((wp->w_p_nu || wp->w_p_rnu) && (wlv->row <= lnum_row || !has_cpo_n) && !((has_cpo_n) && wp->w_skipcol > 0 && wlv->lnum == wp->w_topline))
+    if ((wp-> w_onebuf_opt.wo_nu  || wp-> w_onebuf_opt.wo_rnu ) && (wlv->row <= lnum_row || !has_cpo_n) && !((has_cpo_n) && wp->w_skipcol > 0 && wlv->lnum == wp->w_topline))
     {
         {
-          if (wlv->row == lnum_row && (wp->w_skipcol == 0 || wlv->row > 0 || (wp->w_p_nu && wp->w_p_rnu)))
+          if (wlv->row == lnum_row && (wp->w_skipcol == 0 || wlv->row > 0 || (wp-> w_onebuf_opt.wo_nu  && wp-> w_onebuf_opt.wo_rnu )))
           {
               long num;
               char *fmt = "%*ld ";
 
-              if (wp->w_p_nu && !wp->w_p_rnu)
+              if (wp-> w_onebuf_opt.wo_nu  && !wp-> w_onebuf_opt.wo_rnu )
               {
                   num = (long)wlv->lnum;
               }
               else
               {
                   num = labs((long)get_cursor_rel_lnum(wp, wlv->lnum));
-                  if (num == 0 && wp->w_p_nu && wp->w_p_rnu)
+                  if (num == 0 && wp-> w_onebuf_opt.wo_nu  && wp-> w_onebuf_opt.wo_rnu )
                   {
                       num = wlv->lnum;
                       fmt = "%-*ld ";
                   }
               }
 
-              vim_snprintf((char *)wlv->extra, sizeof(wlv->extra), fmt, number_width(wp), num);
+              vim_snprintf((char *)wlv->extra, sizeof(wlv->extra), fmt,  7 , num);
               if (wp->w_skipcol > 0 && wlv->startrow == 0)
               {
                   for (wlv->p_extra = wlv->extra; *wlv->p_extra == ' '; ++wlv->p_extra)
@@ -31145,15 +29475,15 @@
               wlv->c_extra = ' ';
               wlv->c_final = NUL;
           }
-          wlv->n_extra = number_width(wp) + 1;
-          wlv->char_attr = hl_combine_attr(get_win_attr(wp), HL_ATTR(HLF_N));
-          if (wp->w_p_rnu && wlv->lnum < wp->w_cursor.lnum && HL_ATTR(HLF_LNA) != 0)
+          wlv->n_extra =  7  + 1;
+          wlv->char_attr = hl_combine_attr(get_win_attr(wp),  highlight_attr[(int)(HLF_N)] );
+          if (wp-> w_onebuf_opt.wo_rnu  && wlv->lnum < wp->w_cursor.lnum &&  highlight_attr[(int)(HLF_LNA)]  != 0)
           {
-              wlv->char_attr = hl_combine_attr(get_win_attr(wp), HL_ATTR(HLF_LNA));
+              wlv->char_attr = hl_combine_attr(get_win_attr(wp),  highlight_attr[(int)(HLF_LNA)] );
           }
-          if (wp->w_p_rnu && wlv->lnum > wp->w_cursor.lnum && HL_ATTR(HLF_LNB) != 0)
+          if (wp-> w_onebuf_opt.wo_rnu  && wlv->lnum > wp->w_cursor.lnum &&  highlight_attr[(int)(HLF_LNB)]  != 0)
           {
-              wlv->char_attr = hl_combine_attr(get_win_attr(wp), HL_ATTR(HLF_LNB));
+              wlv->char_attr = hl_combine_attr(get_win_attr(wp),  highlight_attr[(int)(HLF_LNB)] );
           }
         }
     }
@@ -31162,15 +29492,15 @@
     static void
 wlv_screen_line(win_T *wp, winlinevars_T *wlv, int clear_end)
 {
-    if (wlv->row == 0 && wp->w_skipcol > 0 && !(wp->w_p_list && wp->w_lcs_chars.prec != 0))
+    if (wlv->row == 0 && wp->w_skipcol > 0 && !(wp-> w_onebuf_opt.wo_list  && wp->w_lcs_chars.prec != 0))
     {
         int off = (int)(current_ScreenLine - ScreenLines);
         int max_off = off + screen_Columns;
         int skip = 0;
 
-        if (wp->w_p_nu && wp->w_p_rnu)
+        if (wp-> w_onebuf_opt.wo_nu  && wp-> w_onebuf_opt.wo_rnu )
         {
-            while (skip < wp->w_width && VIM_ISDIGIT(ScreenLines[off]))
+            while (skip < wp->w_width &&  ((unsigned)(ScreenLines[off]) - '0' < 10) )
             {
                 ++off;
                 ++skip;
@@ -31188,7 +29518,7 @@
             {
                 ScreenLinesUC[off] = 0;
             }
-            ScreenAttrs[off] = HL_ATTR(HLF_AT);
+            ScreenAttrs[off] =  highlight_attr[(int)(HLF_AT)] ;
             ++off;
         }
     }
@@ -31199,7 +29529,6 @@
     static void
 draw_screen_line(win_T *wp, winlinevars_T *wlv)
 {
-
     wlv->screen_line_flags |= SLF_INC_VCOL;
     wlv_screen_line(wp, wlv, TRUE);
     wlv->screen_line_flags &= ~SLF_INC_VCOL;
@@ -31208,7 +29537,7 @@
 }
 
     static void
-win_line_start(win_T *wp UNUSED, winlinevars_T *wlv, int save_extra)
+win_line_start(win_T *wp  __attribute__((unused)) , winlinevars_T *wlv, int save_extra)
 {
     wlv->col = 0;
     wlv->off = (unsigned)(current_ScreenLine - ScreenLines);
@@ -31258,7 +29587,7 @@
 }
 
     int
-win_line(win_T       *wp, linenr_T    lnum, int         startrow, int         endrow, int         number_only, spellvars_T *spv UNUSED)
+win_line(win_T       *wp, linenr_T    lnum, int         startrow, int         endrow, int         number_only, spellvars_T *spv  __attribute__((unused)) )
 {
     winlinevars_T       wlv;
 
@@ -31305,31 +29634,29 @@
 
     int         match_conc      = 0;
     int         on_last_col     = FALSE;
-# define VCOL_HLC (wlv.vcol - wlv.vcol_off_tp)
 
     if (startrow > endrow)
     {
         return startrow;
     }
 
-    CLEAR_FIELD(wlv);
+      memset((&(wlv)), (0), (sizeof(wlv)))  ;
 
     wlv.lnum = lnum;
     wlv.startrow = startrow;
     wlv.row = startrow;
-    wlv.screen_row = wlv.row + W_WINROW(wp);
+    wlv.screen_row = wlv.row +  (wp->w_winrow) ;
     wlv.fromcol = -10;
     wlv.tocol = MAXCOL;
 
     if (number_only == 0)
     {
-
         if (VIsual_active && wp->w_buffer == curwin->w_buffer)
         {
             pos_T *top;
             pos_T *bot;
 
-            if (LTOREQ_POS(curwin->w_cursor, VIsual))
+            if ( ( (((curwin->w_cursor).lnum != (VIsual).lnum)               ? (curwin->w_cursor).lnum < (VIsual).lnum                   : (curwin->w_cursor).col != (VIsual).col                        ? (curwin->w_cursor).col < (VIsual).col                     : (curwin->w_cursor).coladd < (VIsual).coladd)  ||  (((curwin->w_cursor).lnum == (VIsual).lnum) && ((curwin->w_cursor).col == (VIsual).col) && ((curwin->w_cursor).coladd == (VIsual).coladd)) ) )
             {
                 top = &curwin->w_cursor;
                 bot = &VIsual;
@@ -31404,7 +29731,7 @@
             if (wlv.fromcol >= 0)
             {
                 area_highlighting = TRUE;
-                vi_attr = HL_ATTR(HLF_V);
+                vi_attr =  highlight_attr[(int)(HLF_V)] ;
             }
         }
 
@@ -31433,7 +29760,7 @@
                 wlv.tocol = wlv.fromcol + 1;
             }
             area_highlighting = TRUE;
-            vi_attr = HL_ATTR(HLF_I);
+            vi_attr =  highlight_attr[(int)(HLF_I)] ;
         }
     }
 
@@ -31445,7 +29772,7 @@
     line = ml_get_buf(wp->w_buffer, lnum, FALSE);
     ptr = line;
 
-    if (wp->w_p_list)
+    if (wp-> w_onebuf_opt.wo_list )
     {
         if (wp->w_lcs_chars.space || wp->w_lcs_chars.multispace != NULL || wp->w_lcs_chars.leadmultispace != NULL || wp->w_lcs_chars.trail || wp->w_lcs_chars.lead || wp->w_lcs_chars.nbsp)
         {
@@ -31455,7 +29782,7 @@
         if (wp->w_lcs_chars.trail)
         {
             trailcol = ml_get_buf_len(wp->w_buffer, lnum);
-            while (trailcol > (colnr_T)0 && VIM_ISWHITE(ptr[trailcol - 1]))
+            while (trailcol > (colnr_T)0 &&  ((ptr[trailcol - 1]) == ' ' || (ptr[trailcol - 1]) == '\t') )
             {
                 --trailcol;
             }
@@ -31464,7 +29791,7 @@
         if (wp->w_lcs_chars.lead || wp->w_lcs_chars.leadmultispace != NULL || wp->w_lcs_chars.leadtab1 != NUL)
         {
             leadcol = 0;
-            while (VIM_ISWHITE(ptr[leadcol]))
+            while ( ((ptr[leadcol]) == ' ' || (ptr[leadcol]) == '\t') )
             {
                 ++leadcol;
             }
@@ -31487,7 +29814,7 @@
 
     int skipcol_in_text_prop_above = 0;
 
-    if (wp->w_p_wrap)
+    if (wp-> w_onebuf_opt.wo_wrap )
     {
         v = startrow == 0 ? wp->w_skipcol - skipcol_in_text_prop_above : 0;
     }
@@ -31514,8 +29841,8 @@
             {
                 break;
             }
-            MB_PTR_ADV(cts.cts_ptr);
-            if (wp->w_p_list)
+             cts.cts_ptr += (*mb_ptr2len)(cts.cts_ptr) ;
+            if (wp-> w_onebuf_opt.wo_list )
             {
                 in_multispace = *prev_ptr == ' ' && (*cts.cts_ptr == ' ' || (prev_ptr > line && prev_ptr[-1] == ' '));
                 if (!in_multispace)
@@ -31608,11 +29935,11 @@
     {
         int     has_match_conc = 0;
 
-        if (wlv.draw_state != WL_LINE)
+        if (wlv.draw_state !=  (   (   (WL_START + 1)    + 1)    + 1) )
         {
-            if (wlv.draw_state == WL_CMDLINE - 1 && wlv.n_extra == 0)
+            if (wlv.draw_state ==  (WL_START + 1)  - 1 && wlv.n_extra == 0)
             {
-                wlv.draw_state = WL_CMDLINE;
+                wlv.draw_state =  (WL_START + 1) ;
                 if (wp == cmdwin_win)
                 {
                     wlv.n_extra = 1;
@@ -31620,7 +29947,7 @@
                     if (wlv.row == wlv.startrow)
                     {
                         wlv.c_extra = cmdwin_type;
-                        wlv.char_attr = hl_combine_attr(get_win_attr(wp), HL_ATTR(HLF_AT));
+                        wlv.char_attr = hl_combine_attr(get_win_attr(wp),  highlight_attr[(int)(HLF_AT)] );
                     }
                     else
                     {
@@ -31629,16 +29956,16 @@
                     }
                 }
             }
-            if (wlv.draw_state == WL_NR - 1 && wlv.n_extra == 0)
+            if (wlv.draw_state ==  (   (WL_START + 1)    + 1)  - 1 && wlv.n_extra == 0)
             {
-                wlv.draw_state = WL_NR;
+                wlv.draw_state =  (   (WL_START + 1)    + 1) ;
                 handle_lnum_col(wp, &wlv, sign_present, num_attr);
             }
 
-            if (number_only > 0 && wlv.draw_state == WL_NR && wlv.n_extra == 0)
+            if (number_only > 0 && wlv.draw_state ==  (   (WL_START + 1)    + 1)  && wlv.n_extra == 0)
             {
                 wlv_screen_line(wp, &wlv, FALSE);
-                if ((wlv.row + 1 - wlv.startrow < number_only && (HL_ATTR(HLF_LNA) != 0 || HL_ATTR(HLF_LNB) != 0)))
+                if ((wlv.row + 1 - wlv.startrow < number_only && ( highlight_attr[(int)(HLF_LNA)]  != 0 ||  highlight_attr[(int)(HLF_LNB)]  != 0)))
                 {
                     ++wlv.row;
                     ++wlv.screen_row;
@@ -31655,9 +29982,9 @@
                 }
             }
 
-            if (wlv.draw_state == WL_LINE - 1 && wlv.n_extra == 0)
+            if (wlv.draw_state ==  (   (   (WL_START + 1)    + 1)    + 1)  - 1 && wlv.n_extra == 0)
             {
-                wlv.draw_state = WL_LINE;
+                wlv.draw_state =  (   (   (WL_START + 1)    + 1)    + 1) ;
                 win_line_continue(&wlv);
             }
         }
@@ -31669,9 +29996,8 @@
             break;
         }
 
-        if (wlv.draw_state == WL_LINE && (area_highlighting || extra_check))
+        if (wlv.draw_state ==  (   (   (WL_START + 1)    + 1)    + 1)  && (area_highlighting || extra_check))
         {
-
             int *area_attr_p =
                                                             &area_attr;
 
@@ -31785,7 +30111,7 @@
                     }
                     else
                     {
-                        mb_l = MB_BYTE2LEN(c);
+                        mb_l =  mb_bytelen_tab[c] ;
                         if (mb_l >= wlv.n_extra)
                         {
                             mb_l = 1;
@@ -31806,7 +30132,7 @@
                         mb_c = c;
                         mb_l = 1;
                         mb_utf8 = FALSE;
-                        multi_attr = HL_ATTR(HLF_AT);
+                        multi_attr =  highlight_attr[(int)(HLF_AT)] ;
                         multi_attr = hl_combine_attr(wlv.win_attr, multi_attr);
 
                         ++wlv.n_extra;
@@ -31868,13 +30194,13 @@
                         c = *wlv.p_extra;
                         mb_c = mb_ptr2char_adv(&wlv.p_extra);
                         mb_utf8 = (c >= 0x80);
-                        wlv.n_extra = (int)STRLEN(wlv.p_extra);
+                        wlv.n_extra = (int) strlen((char *)(wlv.p_extra)) ;
                         wlv.c_extra = NUL;
                         wlv.c_final = NUL;
                         if (area_attr == 0 && search_attr == 0)
                         {
                             n_attr = wlv.n_extra + 1;
-                            wlv.extra_attr = hl_combine_attr(wlv.win_attr, HL_ATTR(HLF_8));
+                            wlv.extra_attr = hl_combine_attr(wlv.win_attr,  highlight_attr[(int)(HLF_8)] );
                             saved_attr2 = wlv.char_attr;
                         }
                     }
@@ -31885,7 +30211,7 @@
                 }
                 else
                 {
-                    mb_l = MB_BYTE2LEN(c);
+                    mb_l =  mb_bytelen_tab[c] ;
                     if (mb_l == 0)
                     {
                         mb_l = 1;
@@ -31902,12 +30228,12 @@
                             {
                                 mb_l = 1;
                                 transchar_nonprint(wp->w_buffer, wlv.extra, c);
-                                wlv.n_extra = (int)STRLEN(wlv.extra) - 1;
+                                wlv.n_extra = (int) strlen((char *)(wlv.extra))  - 1;
                             }
                             else
                             {
                                 mb_l = 2;
-                                STRCPY(wlv.extra, "XX");
+                                 strcpy((char *)(wlv.extra), (char *)("XX")) ;
                                 wlv.n_extra = 1;
                             }
                             wlv.p_extra = wlv.extra;
@@ -31917,7 +30243,7 @@
                             if (area_attr == 0 && search_attr == 0)
                             {
                                 n_attr = wlv.n_extra + 1;
-                                wlv.extra_attr = hl_combine_attr(wlv.win_attr, HL_ATTR(HLF_8));
+                                wlv.extra_attr = hl_combine_attr(wlv.win_attr,  highlight_attr[(int)(HLF_8)] );
                                 saved_attr2 = wlv.char_attr;
                             }
                             mb_c = c;
@@ -31930,7 +30256,7 @@
                     mb_c = c;
                     mb_utf8 = FALSE;
                     mb_l = 1;
-                    multi_attr = hl_combine_attr(wlv.win_attr, HL_ATTR(HLF_AT));
+                    multi_attr = hl_combine_attr(wlv.win_attr,  highlight_attr[(int)(HLF_AT)] );
                     --ptr;
                 }
                 else if (*ptr != NUL)
@@ -31947,7 +30273,7 @@
                     if (area_attr == 0 && search_attr == 0)
                     {
                         n_attr = wlv.n_extra + 1;
-                        wlv.extra_attr = hl_combine_attr(wlv.win_attr, HL_ATTR(HLF_AT));
+                        wlv.extra_attr = hl_combine_attr(wlv.win_attr,  highlight_attr[(int)(HLF_AT)] );
                         saved_attr2 = wlv.char_attr;
                     }
                     mb_c = c;
@@ -31960,7 +30286,7 @@
 
             if (extra_check)
             {
-                if (wp->w_p_list)
+                if (wp-> w_onebuf_opt.wo_list )
                 {
                     in_multispace = c == ' ' && (*ptr == ' ' || (prev_ptr > line && prev_ptr[-1] == ' '));
                     if (!in_multispace)
@@ -31969,7 +30295,7 @@
                     }
                 }
 
-                if (wp->w_p_list && ((((c == 160 && mb_l == 1) || (mb_utf8 && ((mb_c == 160 && mb_l == 2) || (mb_c == 0x202f && mb_l == 3)))) && wp->w_lcs_chars.nbsp) || (c == ' ' && mb_l == 1 && (wp->w_lcs_chars.space || (in_multispace && wp->w_lcs_chars.multispace != NULL)) && ptr - line >= leadcol && ptr - line <= trailcol)))
+                if (wp-> w_onebuf_opt.wo_list  && ((((c == 160 && mb_l == 1) || (mb_utf8 && ((mb_c == 160 && mb_l == 2) || (mb_c == 0x202f && mb_l == 3)))) && wp->w_lcs_chars.nbsp) || (c == ' ' && mb_l == 1 && (wp->w_lcs_chars.space || (in_multispace && wp->w_lcs_chars.multispace != NULL)) && ptr - line >= leadcol && ptr - line <= trailcol)))
                 {
                     if (in_multispace && wp->w_lcs_chars.multispace != NULL)
                     {
@@ -31987,7 +30313,7 @@
                     if (area_attr == 0 && search_attr == 0)
                     {
                         n_attr = 1;
-                        wlv.extra_attr = hl_combine_attr(wlv.win_attr, HL_ATTR(HLF_8));
+                        wlv.extra_attr = hl_combine_attr(wlv.win_attr,  highlight_attr[(int)(HLF_8)] );
                         saved_attr2 = wlv.char_attr;
                     }
                     mb_c = c;
@@ -32032,7 +30358,7 @@
                     if (!attr_pri)
                     {
                         n_attr = 1;
-                        wlv.extra_attr = hl_combine_attr(wlv.win_attr, HL_ATTR(HLF_8));
+                        wlv.extra_attr = hl_combine_attr(wlv.win_attr,  highlight_attr[(int)(HLF_8)] );
                         saved_attr2 = wlv.char_attr;
                     }
                     mb_c = c;
@@ -32051,7 +30377,7 @@
 
             if (!vim_isprintc(c) && !(enc_utf8 && c >= 0x80))
             {
-                if (c == TAB && (!wp->w_p_list || wp->w_lcs_chars.tab1))
+                if (c == TAB && (!wp-> w_onebuf_opt.wo_list  || wp->w_lcs_chars.tab1))
                 {
                     int     tab_len = 0;
                     long    vcol_adjusted = wlv.vcol - wlv.vcol_off_tp;
@@ -32059,7 +30385,7 @@
                     int     lcs_tab2 = wp->w_lcs_chars.tab2;
                     int     lcs_tab3 = wp->w_lcs_chars.tab3;
 
-                    if (wp->w_p_list && wp->w_lcs_chars.leadtab1 != NUL && ptr < line + leadcol)
+                    if (wp-> w_onebuf_opt.wo_list  && wp->w_lcs_chars.leadtab1 != NUL && ptr < line + leadcol)
                     {
                         lcs_tab1 = wp->w_lcs_chars.leadtab1;
                         lcs_tab2 = wp->w_lcs_chars.leadtab2;
@@ -32072,14 +30398,14 @@
                         wlv.n_extra = tab_len;
                     }
                     mb_utf8 = FALSE;
-                    if (wp->w_p_list)
+                    if (wp-> w_onebuf_opt.wo_list )
                     {
                         c = (wlv.n_extra == 0 && lcs_tab3)  ? lcs_tab3
                                                             : lcs_tab1;
                             wlv.c_extra = lcs_tab2;
                         wlv.c_final = lcs_tab3;
                         n_attr = tab_len + 1;
-                        wlv.extra_attr = hl_combine_attr(wlv.win_attr, HL_ATTR(HLF_8));
+                        wlv.extra_attr = hl_combine_attr(wlv.win_attr,  highlight_attr[(int)(HLF_8)] );
                         saved_attr2 = wlv.char_attr;
                         mb_c = c;
                         if (enc_utf8 && utf_char2len(c) > 1)
@@ -32096,7 +30422,7 @@
                         c = ' ';
                     }
                 }
-                else if (c == NUL && wlv.n_extra == 0 && (wp->w_p_list || ((wlv.fromcol >= 0 || fromcol_prev >= 0) && wlv.tocol > wlv.vcol && VIsual_mode != Ctrl_V && ((wlv.col < wp->w_width)) && !(noinvcur && lnum == wp->w_cursor.lnum && (colnr_T)wlv.vcol == wp->w_virtcol))) && lcs_eol_one > 0)
+                else if (c == NUL && wlv.n_extra == 0 && (wp-> w_onebuf_opt.wo_list  || ((wlv.fromcol >= 0 || fromcol_prev >= 0) && wlv.tocol > wlv.vcol && VIsual_mode != Ctrl_V && ((wlv.col < wp->w_width)) && !(noinvcur && lnum == wp->w_cursor.lnum && (colnr_T)wlv.vcol == wp->w_virtcol))) && lcs_eol_one > 0)
                 {
                     if (wlv.line_attr == 0)
                     {
@@ -32106,7 +30432,7 @@
                         }
                         wlv.n_extra = 0;
                     }
-                    if (wp->w_p_list && wp->w_lcs_chars.eol > 0)
+                    if (wp-> w_onebuf_opt.wo_list  && wp->w_lcs_chars.eol > 0)
                     {
                         c = wp->w_lcs_chars.eol;
                     }
@@ -32118,7 +30444,7 @@
                     --ptr;
                     if (!attr_pri)
                     {
-                        wlv.extra_attr = hl_combine_attr(wlv.win_attr, HL_ATTR(HLF_AT));
+                        wlv.extra_attr = hl_combine_attr(wlv.win_attr,  highlight_attr[(int)(HLF_AT)] );
                         n_attr = 1;
                     }
                     mb_c = c;
@@ -32149,7 +30475,7 @@
                     if (!attr_pri)
                     {
                         n_attr = wlv.n_extra + 1;
-                        wlv.extra_attr = hl_combine_attr(wlv.win_attr, HL_ATTR(HLF_8));
+                        wlv.extra_attr = hl_combine_attr(wlv.win_attr,  highlight_attr[(int)(HLF_8)] );
 
                         saved_attr2 = wlv.char_attr;
                     }
@@ -32167,7 +30493,7 @@
 
                     ++did_line_attr;
 
-                    if (wlv.line_attr != 0 && wlv.char_attr == search_attr && (did_line_attr > 1 || (wp->w_p_list && wp->w_lcs_chars.eol > 0)))
+                    if (wlv.line_attr != 0 && wlv.char_attr == search_attr && (did_line_attr > 1 || (wp-> w_onebuf_opt.wo_list  && wp->w_lcs_chars.eol > 0)))
                     {
                         wlv.char_attr = wlv.line_attr;
                     }
@@ -32176,7 +30502,7 @@
 
         }
 
-        if (wlv.n_attr_skip == 0 && n_attr > 0 && wlv.draw_state == WL_LINE && (!attr_pri))
+        if (wlv.n_attr_skip == 0 && n_attr > 0 && wlv.draw_state ==  (   (   (WL_START + 1)    + 1)    + 1)  && (!attr_pri))
         {
             if (wlv.line_attr)
             {
@@ -32188,7 +30514,7 @@
             }
         }
 
-        if (lcs_prec_todo != NUL && wp->w_p_list && (wp->w_p_wrap ? (wp->w_skipcol > 0 && wlv.row == 0) : wp->w_leftcol > 0) && wlv.draw_state > WL_NR && skip_cells <= 0 && c != NUL)
+        if (lcs_prec_todo != NUL && wp-> w_onebuf_opt.wo_list  && (wp-> w_onebuf_opt.wo_wrap  ? (wp->w_skipcol > 0 && wlv.row == 0) : wp->w_leftcol > 0) && wlv.draw_state >  (   (WL_START + 1)    + 1)  && skip_cells <= 0 && c != NUL)
         {
             c = wp->w_lcs_chars.prec;
             lcs_prec_todo = NUL;
@@ -32199,7 +30525,7 @@
                 wlv.n_extra = 1;
                 n_attr = 2;
                 wlv.extra_attr =
-                                hl_combine_attr(wlv.win_attr, HL_ATTR(HLF_AT));
+                                hl_combine_attr(wlv.win_attr,  highlight_attr[(int)(HLF_AT)] );
             }
             mb_c = c;
             if (enc_utf8 && utf_char2len(c) > 1)
@@ -32215,7 +30541,7 @@
             if (!attr_pri)
             {
                 saved_attr3 = wlv.char_attr;
-                wlv.char_attr = hl_combine_attr(wlv.win_attr, HL_ATTR(HLF_AT));
+                wlv.char_attr = hl_combine_attr(wlv.win_attr,  highlight_attr[(int)(HLF_AT)] );
                 n_attr3 = 1;
             }
         }
@@ -32277,10 +30603,10 @@
         }
 
         int lcs_ext = get_lcs_ext(wp);
-        if (lcs_ext != NUL && wlv.draw_state == WL_LINE && (wlv.col == wp->w_width - 1) && (*ptr != NUL || lcs_eol_one > 0 || (wlv.n_extra > 0 && (wlv.c_extra != NUL || *wlv.p_extra != NUL))))
+        if (lcs_ext != NUL && wlv.draw_state ==  (   (   (WL_START + 1)    + 1)    + 1)  && (wlv.col == wp->w_width - 1) && (*ptr != NUL || lcs_eol_one > 0 || (wlv.n_extra > 0 && (wlv.c_extra != NUL || *wlv.p_extra != NUL))))
         {
             c = lcs_ext;
-            wlv.char_attr = hl_combine_attr(wlv.win_attr, HL_ATTR(HLF_AT));
+            wlv.char_attr = hl_combine_attr(wlv.win_attr,  highlight_attr[(int)(HLF_AT)] );
             mb_c = c;
             if (enc_utf8 && utf_char2len(c) > 1)
             {
@@ -32294,12 +30620,12 @@
             }
         }
 
-        if (wlv.draw_state == WL_LINE)
+        if (wlv.draw_state ==  (   (   (WL_START + 1)    + 1)    + 1) )
         {
             vcol_prev = wlv.vcol;
         }
 
-        if (wlv.draw_state < WL_LINE || skip_cells <= 0)
+        if (wlv.draw_state <  (   (   (WL_START + 1)    + 1)    + 1)  || skip_cells <= 0)
         {
             ScreenLines[wlv.off] = c;
             if (enc_dbcs == DBCS_JPNU)
@@ -32345,7 +30671,7 @@
                 ScreenAttrs[wlv.off] = wlv.char_attr;
             }
 
-            if (wlv.draw_state > WL_NR)
+            if (wlv.draw_state >  (   (WL_START + 1)    + 1) )
             {
                 ScreenCols[wlv.off] = wlv.vcol;
             }
@@ -32367,7 +30693,7 @@
                     ScreenLines[wlv.off] = mb_c & 0xff;
                 }
 
-                if (wlv.draw_state > WL_NR)
+                if (wlv.draw_state >  (   (WL_START + 1)    + 1) )
                 {
                     ScreenCols[wlv.off] = ++wlv.vcol;
                 }
@@ -32392,23 +30718,23 @@
             --skip_cells;
         }
 
-        if (wlv.draw_state > WL_NR && skipped_cells > 0)
+        if (wlv.draw_state >  (   (WL_START + 1)    + 1)  && skipped_cells > 0)
         {
             wlv.vcol += skipped_cells;
             skipped_cells = 0;
         }
 
-        if (wlv.draw_state > WL_NR)
+        if (wlv.draw_state >  (   (WL_START + 1)    + 1) )
         {
             ++wlv.vcol;
         }
 
-        if (wlv.draw_state > WL_NR && n_attr3 > 0 && --n_attr3 == 0)
+        if (wlv.draw_state >  (   (WL_START + 1)    + 1)  && n_attr3 > 0 && --n_attr3 == 0)
         {
             wlv.char_attr = saved_attr3;
         }
 
-        if (n_attr > 0 && wlv.draw_state == WL_LINE && wlv.n_attr_skip == 0 && --n_attr == 0)
+        if (n_attr > 0 && wlv.draw_state ==  (   (   (WL_START + 1)    + 1)    + 1)  && wlv.n_attr_skip == 0 && --n_attr == 0)
         {
             wlv.char_attr = saved_attr2;
         }
@@ -32417,18 +30743,18 @@
             --wlv.n_attr_skip;
         }
 
-        if (((wlv.col >= wp->w_width)) && (wlv.draw_state != WL_LINE || *ptr != NUL || (wp->w_p_list && wp->w_lcs_chars.eol != NUL && lcs_eol_one != -1) || (wlv.n_extra != 0 && (wlv.c_extra != NUL || *wlv.p_extra != NUL))))
+        if (((wlv.col >= wp->w_width)) && (wlv.draw_state !=  (   (   (WL_START + 1)    + 1)    + 1)  || *ptr != NUL || (wp-> w_onebuf_opt.wo_list  && wp->w_lcs_chars.eol != NUL && lcs_eol_one != -1) || (wlv.n_extra != 0 && (wlv.c_extra != NUL || *wlv.p_extra != NUL))))
         {
             wlv_screen_line(wp, &wlv, TRUE);
             ++wlv.row;
             ++wlv.screen_row;
 
-            if (!wp->w_p_wrap)
+            if (!wp-> w_onebuf_opt.wo_wrap )
             {
                 break;
             }
 
-            if (wlv.draw_state != WL_LINE)
+            if (wlv.draw_state !=  (   (   (WL_START + 1)    + 1)    + 1) )
             {
                 win_draw_end(wp, '@', ' ', TRUE, wlv.row, wp->w_height, HLF_AT);
                 draw_vsep_win(wp, wlv.row);
@@ -32452,7 +30778,7 @@
                         screen_char(LineOffset[wlv.screen_row - 1] + (unsigned)topframe->fr_width - 1, wlv.screen_row - 1, (int)(topframe->fr_width - 1));
                     }
 
-                    if (has_mbyte && MB_BYTE2LEN(ScreenLines[LineOffset[wlv.screen_row - 1] + (topframe->fr_width - 1)]) > 1)
+                    if (has_mbyte &&  mb_bytelen_tab[ScreenLines[LineOffset[wlv.screen_row - 1] + (topframe->fr_width - 1)]]  > 1)
                     {
                         out_char(' ');
                     }
@@ -32561,19 +30887,19 @@
                     type = UPD_NOT_VALID;
                     redraw_as_cleared();
                 }
-                FOR_ALL_WINDOWS(wp)
+                 for ((wp) = firstwin; (wp) != NULL; (wp) = (wp)->w_next) 
                 {
                     if (wp->w_winrow < msg_scrolled)
                     {
-                        if (W_WINROW(wp) + wp->w_height > msg_scrolled && wp->w_redr_type < UPD_REDRAW_TOP && wp->w_lines_valid > 0 && wp->w_topline == wp->w_lines[0].wl_lnum)
+                        if ( (wp->w_winrow)  + wp->w_height > msg_scrolled && wp->w_redr_type < UPD_REDRAW_TOP && wp->w_lines_valid > 0 && wp->w_topline == wp->w_lines[0].wl_lnum)
                         {
-                            wp->w_upd_rows = msg_scrolled - W_WINROW(wp);
+                            wp->w_upd_rows = msg_scrolled -  (wp->w_winrow) ;
                             wp->w_redr_type = UPD_REDRAW_TOP;
                         }
                         else
                         {
                             wp->w_redr_type = UPD_NOT_VALID;
-                            if (W_WINROW(wp) + wp->w_height + wp->w_status_height <= msg_scrolled)
+                            if ( (wp->w_winrow)  + wp->w_height + wp->w_status_height <= msg_scrolled)
                             {
                                 wp->w_redr_status = true;
                             }
@@ -32631,7 +30957,7 @@
 
     did_update_one_window = FALSE;
     screen_search_hl.rm.regprog = NULL;
-    FOR_ALL_WINDOWS(wp)
+     for ((wp) = firstwin; (wp) != NULL; (wp) = (wp)->w_next) 
     {
         override_success = push_highlight_overrides(wp->w_hl, wp->w_hl_len);
 
@@ -32661,17 +30987,21 @@
     if (redraw_vseps)
     {
         redraw_vseps = FALSE;
-        FOR_ALL_WINDOWS(wp)
+         for ((wp) = firstwin; (wp) != NULL; (wp) = (wp)->w_next) 
+         {
             if (wp->w_vsep_width > 0)
             {
                 draw_vsep_win(wp, 0);
             }
+         }
     }
 
     borrow_stl_vsep_hl();
 
-    FOR_ALL_WINDOWS(wp)
+     for ((wp) = firstwin; (wp) != NULL; (wp) = (wp)->w_next) 
+     {
         wp->w_buffer->b_mod_set = false;
+     }
 
     after_updating_screen(TRUE);
 
@@ -32703,11 +31033,11 @@
     int
 statusline_row(win_T *wp)
 {
-    return W_WINROW(wp) + wp->w_height;
+    return  (wp->w_winrow)  + wp->w_height;
 }
 
     void
-win_redr_status(win_T *wp, int ignore_pum UNUSED)
+win_redr_status(win_T *wp, int ignore_pum  __attribute__((unused)) )
 {
     int         row;
     int         fillchar;
@@ -32744,24 +31074,24 @@
 
         get_trans_bufname(wp->w_buffer);
         p = NameBuff;
-        plen = (int)STRLEN(p);
+        plen = (int) strlen((char *)(p)) ;
 
-        if ((bt_help(wp->w_buffer) || bufIsChanged(wp->w_buffer) || wp->w_buffer->b_p_ro) && plen < MAXPATHL - 1)
+        if ((bt_help(wp->w_buffer) || bufIsChanged(wp->w_buffer) || wp->w_buffer->b_p_ro) && plen <  PATH_MAX  - 1)
         {
             *(p + plen++) = ' ';
             *(p + plen) = NUL;
         }
         if (bt_help(wp->w_buffer))
         {
-            plen += vim_snprintf((char *)p + plen, MAXPATHL - plen, "%s", _("[Help]"));
+            plen += vim_snprintf((char *)p + plen,  PATH_MAX  - plen, "%s", _("[Help]"));
         }
         if (bufIsChanged(wp->w_buffer) && !bt_terminal(wp->w_buffer))
         {
-            plen += vim_snprintf((char *)p + plen, MAXPATHL - plen, "%s", "[+]");
+            plen += vim_snprintf((char *)p + plen,  PATH_MAX  - plen, "%s", "[+]");
         }
         if (wp->w_buffer->b_p_ro)
         {
-            plen += vim_snprintf((char *)p + plen, MAXPATHL - plen, "%s", _("[RO]"));
+            plen += vim_snprintf((char *)p + plen,  PATH_MAX  - plen, "%s", _("[RO]"));
         }
 
         this_ru_col = ru_col - (cmdline_width - wp->w_width);
@@ -32801,9 +31131,9 @@
         screen_fill(row, row + 1, plen + wp->w_wincol, this_ru_col + wp->w_wincol, fillchar, fillchar, attr);
         for (i = 1; i < wp->w_status_height; i++)
         {
-            screen_fill(row + i, row + i + 1, wp->w_wincol, W_ENDCOL(wp), fillchar, fillchar, attr);
+            screen_fill(row + i, row + i + 1, wp->w_wincol,  ((wp)->w_wincol + (wp)->w_width) , fillchar, fillchar, attr);
         }
-        if ((NameBufflen = get_keymap_str(wp, (char_u *)"<%s>", NameBuff, MAXPATHL)) > 0 && (this_ru_col - plen) > (NameBufflen + 1))
+        if ((NameBufflen = get_keymap_str(wp, (char_u *)"<%s>", NameBuff,  PATH_MAX )) > 0 && (this_ru_col - plen) > (NameBufflen + 1))
         {
             screen_puts(NameBuff, row, (int)(this_ru_col - NameBufflen - 1 + wp->w_wincol), attr);
         }
@@ -32836,7 +31166,7 @@
             {
                 fillchar = fillchar_vsep(&attr, wp, r);
             }
-            screen_putchar(fillchar, r, W_ENDCOL(wp), attr);
+            screen_putchar(fillchar, r,  ((wp)->w_wincol + (wp)->w_width) , attr);
         }
     }
     busy = FALSE;
@@ -32853,7 +31183,7 @@
         return;
     }
 
-    FOR_ALL_WINDOWS(left)
+     for ((left) = firstwin; (left) != NULL; (left) = (left)->w_next) 
     {
         if (left->w_status_height == 0 || left->w_vsep_width == 0)
         {
@@ -32868,18 +31198,18 @@
         int start = 0;
         int end = 0;
 
-        FOR_ALL_WINDOWS(right)
+         for ((right) = firstwin; (right) != NULL; (right) = (right)->w_next) 
         {
             if (right == left || right->w_status_height == 0)
             {
                 continue;
             }
-            if (right->w_wincol != W_ENDCOL(left) + 1)
+            if (right->w_wincol !=  ((left)->w_wincol + (left)->w_width)  + 1)
             {
                 continue;
             }
-            int l_stl_row = W_WINROW(left) + left->w_height;
-            int r_stl_row = W_WINROW(right) + right->w_height;
+            int l_stl_row =  (left->w_winrow)  + left->w_height;
+            int r_stl_row =  (right->w_winrow)  + right->w_height;
 
             start = l_stl_row > r_stl_row ? l_stl_row : r_stl_row;
             end = l_stl_row + left->w_status_height
@@ -32903,9 +31233,9 @@
             continue;
         }
 
-        int dst_col = W_ENDCOL(left);
+        int dst_col =  ((left)->w_wincol + (left)->w_width) ;
         int src_col = (neighbour == curwin)
-                                ? neighbour->w_wincol : W_ENDCOL(left) - 1;
+                                ? neighbour->w_wincol :  ((left)->w_wincol + (left)->w_width)  - 1;
 
         for (int r = start; r < end; r++)
         {
@@ -32989,7 +31319,6 @@
         int     off;
         int     width;
         colnr_T virtcol;
-#define RULER_BUF_LEN 70
         char_u  buffer[RULER_BUF_LEN];
         int     bufferlen;
         char_u  rel_pos[RULER_BUF_LEN];
@@ -33018,11 +31347,11 @@
         }
 
         virtcol = wp->w_virtcol;
-        if (wp->w_p_list && wp->w_lcs_chars.tab1 == NUL)
+        if (wp-> w_onebuf_opt.wo_list  && wp->w_lcs_chars.tab1 == NUL)
         {
-            wp->w_p_list = FALSE;
+            wp-> w_onebuf_opt.wo_list  = FALSE;
             getvvcol(wp, &wp->w_cursor, NULL, &virtcol, NULL, 0);
-            wp->w_p_list = TRUE;
+            wp-> w_onebuf_opt.wo_list  = TRUE;
         }
 
         bufferlen = vim_snprintf((char *)buffer, RULER_BUF_LEN, _("%ld,"), (wp->w_buffer->b_ml.ml_flags & ML_EMPTY) ? 0L : (long)(wp->w_cursor.lnum));
@@ -33095,7 +31424,7 @@
 }
 
     void
-after_updating_screen(int may_resize_shell UNUSED)
+after_updating_screen(int may_resize_shell  __attribute__((unused)) )
 {
     updating_screen = FALSE;
 
@@ -33151,7 +31480,7 @@
         wp->w_lines_valid = 0;
     }
 
-    if (wp->w_height + WINBAR_HEIGHT(wp) == 0 || (wp->w_frame->fr_height == wp->w_status_height))
+    if (wp->w_height +  0  == 0 || (wp->w_frame->fr_height == wp->w_status_height))
     {
         wp->w_redr_type = 0;
         return;
@@ -33336,7 +31665,6 @@
         }
         else
         {
-
             j = -1;
             row = 0;
             for (i = 0; i < wp->w_lines_valid && i < Rows; i++)
@@ -33662,7 +31990,7 @@
                 top_to_mod = FALSE;
             }
 
-            if (!scrolled_for_mod && mod_bot != MAXLNUM && lnum >= mod_top && lnum < MAX(mod_bot, mod_top + 1) && (!scrolled_down || row >= top_end))
+            if (!scrolled_for_mod && mod_bot !=  LONG_MAX  && lnum >= mod_top && lnum < MAX(mod_bot, mod_top + 1) && (!scrolled_down || row >= top_end))
             {
                 scrolled_for_mod = TRUE;
 
@@ -33714,14 +32042,14 @@
                     {
                         if (row - xtra_rows >= wp->w_height - 2)
                         {
-                            mod_bot = MAXLNUM;
+                            mod_bot =  LONG_MAX ;
                         }
                         else
                         {
                             check_for_delay(FALSE);
                             if (win_del_lines(wp, row, -xtra_rows, FALSE, FALSE, 0) == FAIL)
                             {
-                                mod_bot = MAXLNUM;
+                                mod_bot =  LONG_MAX ;
                             }
                             else
                             {
@@ -33733,14 +32061,14 @@
                     {
                         if (row + xtra_rows >= wp->w_height - 2)
                         {
-                            mod_bot = MAXLNUM;
+                            mod_bot =  LONG_MAX ;
                         }
                         else
                         {
                             check_for_delay(FALSE);
                             if (win_ins_lines(wp, row + old_rows, xtra_rows, FALSE, FALSE) == FAIL)
                             {
-                                mod_bot = MAXLNUM;
+                                mod_bot =  LONG_MAX ;
                             }
                             else if (top_end > row + old_rows)
                             {
@@ -33749,7 +32077,7 @@
                         }
                     }
 
-                    if (mod_bot != MAXLNUM && i != j)
+                    if (mod_bot !=  LONG_MAX  && i != j)
                     {
                         if (j < i)
                         {
@@ -33799,7 +32127,7 @@
                 }
             }
 
-            if (idx < wp->w_lines_valid && wp->w_lines[idx].wl_valid && wp->w_lines[idx].wl_lnum == lnum && lnum > wp->w_topline && !(dy_flags & (DY_LASTLINE | DY_TRUNCATE)) && !WIN_IS_POPUP(wp) && srow + wp->w_lines[idx].wl_size > wp->w_height)
+            if (idx < wp->w_lines_valid && wp->w_lines[idx].wl_valid && wp->w_lines[idx].wl_lnum == lnum && lnum > wp->w_topline && !(dy_flags & (DY_LASTLINE | DY_TRUNCATE)) && ! 0  && srow + wp->w_lines[idx].wl_size > wp->w_height)
             {
                 row = wp->w_height + 1;
             }
@@ -33834,7 +32162,7 @@
         }
         else
         {
-            if ((wp->w_p_nu && mod_top != 0 && lnum >= mod_bot && buf->b_mod_set && buf->b_mod_xlines != 0) || (wp->w_p_rnu && wp->w_last_cursor_lnum_rnu != wp->w_cursor.lnum))
+            if ((wp-> w_onebuf_opt.wo_nu  && mod_top != 0 && lnum >= mod_bot && buf->b_mod_set && buf->b_mod_xlines != 0) || (wp-> w_onebuf_opt.wo_rnu  && wp->w_last_cursor_lnum_rnu != wp->w_cursor.lnum))
             {
                     (void)win_line(wp, lnum, srow, wp->w_height, wp->w_lines[idx].wl_size, &spv);
             }
@@ -33859,7 +32187,7 @@
         }
     }
 
-    wp->w_last_cursor_lnum_rnu = wp->w_p_rnu ? wp->w_cursor.lnum : 0;
+    wp->w_last_cursor_lnum_rnu = wp-> w_onebuf_opt.wo_rnu  ? wp->w_cursor.lnum : 0;
 
     if (idx > wp->w_lines_valid)
     {
@@ -33875,7 +32203,7 @@
         }
         else if (dy_flags & DY_TRUNCATE)
         {
-            int         scr_row = W_WINROW(wp) + wp->w_height - 1;
+            int         scr_row =  (wp->w_winrow)  + wp->w_height - 1;
             int         symbol  = wp->w_fill_chars.lastline;
             int         charlen;
             char_u      fillbuf[12];
@@ -33883,17 +32211,17 @@
             charlen = mb_char2bytes(symbol, &fillbuf[0]);
             mb_char2bytes(symbol, &fillbuf[charlen]);
 
-            screen_puts_len(fillbuf, (wp->w_width > 2 ? 2 : wp->w_width) * charlen, scr_row, wp->w_wincol, HL_ATTR(HLF_AT));
-            screen_fill(scr_row, scr_row + 1, (int)wp->w_wincol + 2, (int)W_ENDCOL(wp), symbol, ' ', HL_ATTR(HLF_AT));
+            screen_puts_len(fillbuf, (wp->w_width > 2 ? 2 : wp->w_width) * charlen, scr_row, wp->w_wincol,  highlight_attr[(int)(HLF_AT)] );
+            screen_fill(scr_row, scr_row + 1, (int)wp->w_wincol + 2, (int) ((wp)->w_wincol + (wp)->w_width) , symbol, ' ',  highlight_attr[(int)(HLF_AT)] );
             set_empty_rows(wp, srow);
             wp->w_botline = lnum;
         }
         else if (dy_flags & DY_LASTLINE)
         {
-            int start_col = (int)W_ENDCOL(wp) - 3;
+            int start_col = (int) ((wp)->w_wincol + (wp)->w_width)  - 3;
             int symbol    = wp->w_fill_chars.lastline;
 
-            screen_fill(W_WINROW(wp) + wp->w_height - 1, W_WINROW(wp) + wp->w_height, (start_col < wp->w_wincol ? wp->w_wincol : start_col), (int)W_ENDCOL(wp), symbol, symbol, HL_ATTR(HLF_AT));
+            screen_fill( (wp->w_winrow)  + wp->w_height - 1,  (wp->w_winrow)  + wp->w_height, (start_col < wp->w_wincol ? wp->w_wincol : start_col), (int) ((wp)->w_wincol + (wp)->w_width) , symbol, symbol,  highlight_attr[(int)(HLF_AT)] );
             set_empty_rows(wp, srow);
             wp->w_botline = lnum;
         }
@@ -33915,7 +32243,7 @@
             wp->w_botline = lnum;
         }
 
-        if (WIN_IS_POPUP(wp))
+        if ( 0 )
         {
             win_draw_end(wp, ' ', ' ', FALSE, row, wp->w_height, HLF_AT);
         }
@@ -33949,11 +32277,13 @@
                 curbuf->b_mod_xlines = j;
             }
             must_redraw = 0;
-            FOR_ALL_WINDOWS(wwp)
+             for ((wwp) = firstwin; (wwp) != NULL; (wwp) = (wwp)->w_next) 
+             {
                 if (wwp->w_redr_type > must_redraw)
                 {
                     must_redraw = wwp->w_redr_type;
                 }
+             }
             recursive = FALSE;
         }
     }
@@ -33984,28 +32314,28 @@
     schar_T     *screenline2 = NULL;
 
     redraw_later(type);
-    if (msg_scrolled || (State != MODE_NORMAL && State != MODE_NORMAL_BUSY) || exiting)
+    if (msg_scrolled || (State != MODE_NORMAL && State !=  (0x1000 | MODE_NORMAL) ) || exiting)
     {
         return ret;
     }
 
     rows = screen_Rows - cmdline_row;
-    screenline = LALLOC_MULT(schar_T, rows * cols);
-    screenattr = LALLOC_MULT(sattr_T, rows * cols);
+    screenline =  (schar_T *)lalloc(sizeof(schar_T) * (rows * cols), FALSE) ;
+    screenattr =  (sattr_T *)lalloc(sizeof(sattr_T) * (rows * cols), FALSE) ;
     if (screenline == NULL || screenattr == NULL)
     {
         ret = 2;
     }
     if (enc_utf8)
     {
-        screenlineUC = LALLOC_MULT(u8char_T, rows * cols);
+        screenlineUC =  (u8char_T *)lalloc(sizeof(u8char_T) * (rows * cols), FALSE) ;
         if (screenlineUC == NULL)
         {
             ret = 2;
         }
         for (i = 0; i < p_mco; ++i)
         {
-            screenlineC[i] = LALLOC_MULT(u8char_T, rows * cols);
+            screenlineC[i] =  (u8char_T *)lalloc(sizeof(u8char_T) * (rows * cols), FALSE) ;
             if (screenlineC[i] == NULL)
             {
                 ret = 2;
@@ -34014,7 +32344,7 @@
     }
     if (enc_dbcs == DBCS_JPNU)
     {
-        screenline2 = LALLOC_MULT(schar_T, rows * cols);
+        screenline2 =  (schar_T *)lalloc(sizeof(schar_T) * (rows * cols), FALSE) ;
         if (screenline2 == NULL)
         {
             ret = 2;
@@ -34025,19 +32355,19 @@
     {
         for (r = 0; r < rows; ++r)
         {
-            mch_memmove(screenline + r * cols, ScreenLines + LineOffset[cmdline_row + r], (size_t)cols * sizeof(schar_T));
-            mch_memmove(screenattr + r * cols, ScreenAttrs + LineOffset[cmdline_row + r], (size_t)cols * sizeof(sattr_T));
+             memmove((char *)(screenline + r * cols), (char *)(ScreenLines + LineOffset[cmdline_row + r]), (size_t)cols * sizeof(schar_T)) ;
+             memmove((char *)(screenattr + r * cols), (char *)(ScreenAttrs + LineOffset[cmdline_row + r]), (size_t)cols * sizeof(sattr_T)) ;
             if (enc_utf8)
             {
-                mch_memmove(screenlineUC + r * cols, ScreenLinesUC + LineOffset[cmdline_row + r], (size_t)cols * sizeof(u8char_T));
+                 memmove((char *)(screenlineUC + r * cols), (char *)(ScreenLinesUC + LineOffset[cmdline_row + r]), (size_t)cols * sizeof(u8char_T)) ;
                 for (i = 0; i < p_mco; ++i)
                 {
-                    mch_memmove(screenlineC[i] + r * cols, ScreenLinesC[i] + LineOffset[cmdline_row + r], (size_t)cols * sizeof(u8char_T));
+                     memmove((char *)(screenlineC[i] + r * cols), (char *)(ScreenLinesC[i] + LineOffset[cmdline_row + r]), (size_t)cols * sizeof(u8char_T)) ;
                 }
             }
             if (enc_dbcs == DBCS_JPNU)
             {
-                mch_memmove(screenline2 + r * cols, ScreenLines2 + LineOffset[cmdline_row + r], (size_t)cols * sizeof(schar_T));
+                 memmove((char *)(screenline2 + r * cols), (char *)(ScreenLines2 + LineOffset[cmdline_row + r]), (size_t)cols * sizeof(schar_T)) ;
             }
         }
 
@@ -34050,19 +32380,19 @@
 
             for (r = 0; r < rows; ++r)
             {
-                mch_memmove(current_ScreenLine, screenline + r * cols, (size_t)cols * sizeof(schar_T));
-                mch_memmove(ScreenAttrs + off, screenattr + r * cols, (size_t)cols * sizeof(sattr_T));
+                 memmove((char *)(current_ScreenLine), (char *)(screenline + r * cols), (size_t)cols * sizeof(schar_T)) ;
+                 memmove((char *)(ScreenAttrs + off), (char *)(screenattr + r * cols), (size_t)cols * sizeof(sattr_T)) ;
                 if (enc_utf8)
                 {
-                    mch_memmove(ScreenLinesUC + off, screenlineUC + r * cols, (size_t)cols * sizeof(u8char_T));
+                     memmove((char *)(ScreenLinesUC + off), (char *)(screenlineUC + r * cols), (size_t)cols * sizeof(u8char_T)) ;
                     for (i = 0; i < p_mco; ++i)
                     {
-                        mch_memmove(ScreenLinesC[i] + off, screenlineC[i] + r * cols, (size_t)cols * sizeof(u8char_T));
+                         memmove((char *)(ScreenLinesC[i] + off), (char *)(screenlineC[i] + r * cols), (size_t)cols * sizeof(u8char_T)) ;
                     }
                 }
                 if (enc_dbcs == DBCS_JPNU)
                 {
-                    mch_memmove(ScreenLines2 + off, screenline2 + r * cols, (size_t)cols * sizeof(schar_T));
+                     memmove((char *)(ScreenLines2 + off), (char *)(screenline2 + r * cols), (size_t)cols * sizeof(schar_T)) ;
                 }
                 screen_line(curwin, cmdline_row + r, 0, cols, cols, -1, 0);
             }
@@ -34099,7 +32429,7 @@
 
     term_set_sync_output(TERM_SYNC_OUTPUT_ENABLE);
 
-    if (State == MODE_HITRETURN || State == MODE_ASKMORE || State == MODE_SETWSIZE || State == MODE_EXTERNCMD || State == MODE_CONFIRM || exmode_active)
+    if (State ==  (0x2000 | MODE_NORMAL)  || State == MODE_ASKMORE || State == MODE_SETWSIZE || State == MODE_EXTERNCMD || State == MODE_CONFIRM || exmode_active)
     {
         if (do_message)
         {
@@ -34179,8 +32509,10 @@
 {
     win_T       *wp;
 
-    FOR_ALL_WINDOWS(wp)
+     for ((wp) = firstwin; (wp) != NULL; (wp) = (wp)->w_next) 
+     {
         redraw_win_later(wp, type);
+     }
     set_must_redraw(type);
 }
 
@@ -34204,7 +32536,7 @@
 {
     win_T       *wp;
 
-    FOR_ALL_WINDOWS(wp)
+     for ((wp) = firstwin; (wp) != NULL; (wp) = (wp)->w_next) 
     {
         if (wp->w_buffer == buf)
         {
@@ -34227,12 +32559,14 @@
 {
     win_T       *wp;
 
-    FOR_ALL_WINDOWS(wp)
+     for ((wp) = firstwin; (wp) != NULL; (wp) = (wp)->w_next) 
+     {
         if (wp->w_status_height)
         {
             wp->w_redr_status = true;
             redraw_later(UPD_VALID);
         }
+     }
     ruler_redraw_lastwin();
 }
 
@@ -34241,12 +32575,14 @@
 {
     win_T       *wp;
 
-    FOR_ALL_WINDOWS(wp)
+     for ((wp) = firstwin; (wp) != NULL; (wp) = (wp)->w_next) 
+     {
         if (wp->w_status_height != 0 && wp->w_buffer == curbuf)
         {
             wp->w_redr_status = true;
             redraw_later(UPD_VALID);
         }
+     }
     if (lastwin->w_buffer == curbuf)
     {
         ruler_redraw_lastwin();
@@ -34258,7 +32594,8 @@
 {
     win_T       *wp;
 
-    FOR_ALL_WINDOWS(wp)
+     for ((wp) = firstwin; (wp) != NULL; (wp) = (wp)->w_next) 
+     {
         if (wp->w_redr_status)
         {
             bool ret = push_highlight_overrides(wp->w_hl, wp->w_hl_len);
@@ -34268,6 +32605,7 @@
                 pop_highlight_overrides();
             }
         }
+     }
     borrow_stl_vsep_hl();
     if (redraw_tabline)
     {
@@ -34285,8 +32623,10 @@
     }
     else if (frp->fr_layout == FR_ROW)
     {
-        FOR_ALL_FRAMES(frp, frp->fr_child)
+         for ((frp) = frp->fr_child; (frp) != NULL; (frp) = (frp)->fr_next) 
+         {
             win_redraw_last_status(frp);
+         }
     }
     else
     {
@@ -34322,13 +32662,12 @@
     }
 }
 
-
 // ==================== edit.c ====================
 
-#define BACKSPACE_CHAR              1
-#define BACKSPACE_WORD              2
-#define BACKSPACE_WORD_NOT_SPACE    3
-#define BACKSPACE_LINE              4
+enum { BACKSPACE_CHAR = 1 };
+enum { BACKSPACE_WORD = 2 };
+enum { BACKSPACE_WORD_NOT_SPACE = 3 };
+enum { BACKSPACE_LINE = 4 };
 
 static int      compl_busy = FALSE;
 
@@ -34380,10 +32719,6 @@
 
 static int      dont_sync_undo = FALSE;
 
-#define TRIGGER_AUTOCOMPLETE()                      do {                                                update_screen(UPD_VALID);          out_flush();                                    ins_compl_enable_autocomplete();                ins_compl_arm_autostart();                      if (!ins_compl_arm_autocomplete_delay())            goto docomplete;                        } while (0)
-
-#define MAY_TRIGGER_AUTOCOMPLETE(c)                                 do {                                                                if (ins_compl_has_autocomplete() && !char_avail()                       && curwin->w_cursor.col > 0)                            {                                                                   (c) = char_before_cursor();                                     if (vim_isprintc(c))                                                TRIGGER_AUTOCOMPLETE();                                 }                                                           } while (0)
-
     int
 edit(int         cmdchar, int         startln, long        count)
 {
@@ -34397,7 +32732,7 @@
     int         line_is_white = FALSE;
     linenr_T    old_topline = 0;
     int         inserted_space = FALSE;
-    int         replaceState = MODE_REPLACE;
+    int         replaceState =  (REPLACE_FLAG | MODE_INSERT) ;
     int         nomove = FALSE;
     int         ins_just_started = TRUE;
 
@@ -34425,7 +32760,7 @@
             highlight_changed();
         }
 
-        if (!EQUAL_POS(curwin->w_cursor, save_cursor) && save_cursor.lnum <= curbuf->b_ml.ml_line_count)
+        if (! (((curwin->w_cursor).lnum == (save_cursor).lnum) && ((curwin->w_cursor).col == (save_cursor).col) && ((curwin->w_cursor).coladd == (save_cursor).coladd))  && save_cursor.lnum <= curbuf->b_ml.ml_line_count)
         {
             int save_state = State;
 
@@ -34466,7 +32801,7 @@
         }
         else
         {
-            if (cmdchar == K_PS)
+            if (cmdchar ==   (-(('P') + ((int)('S') << 8)))  )
             {
                 AppendCharToRedobuff('a');
             }
@@ -34487,12 +32822,12 @@
 
     if (cmdchar == 'R')
     {
-        State = MODE_REPLACE;
+        State =  (REPLACE_FLAG | MODE_INSERT) ;
     }
     else if (cmdchar == 'V' || cmdchar == 'v')
     {
-        State = MODE_VREPLACE;
-        replaceState = MODE_VREPLACE;
+        State =  (REPLACE_FLAG | VREPLACE_FLAG | MODE_INSERT) ;
+        replaceState =  (REPLACE_FLAG | VREPLACE_FLAG | MODE_INSERT) ;
         orig_line_count = curbuf->b_ml.ml_line_count;
         vr_lines_changed = 1;
     }
@@ -34518,9 +32853,9 @@
     clear_showcmd();
     if (!p_ek)
     {
-        MAY_WANT_TO_LOG_THIS;
+          ;
 
-        out_str(T_BD);
+        out_str( ( term_strings[(int)(KS_CBD)] ) );
 
         out_str_t_TE();
     }
@@ -34622,7 +32957,7 @@
 
         msg_scroll = FALSE;
 
-        if (curbuf->b_mod_set && curwin->w_p_wrap && !curwin->w_p_sms && !did_backspace && curwin->w_topline == old_topline && count <= 1)
+        if (curbuf->b_mod_set && curwin-> w_onebuf_opt.wo_wrap  && !curwin-> w_onebuf_opt.wo_sms  && !did_backspace && curwin->w_topline == old_topline && count <= 1)
         {
             mincol = curwin->w_wcol;
             validate_cursor_col();
@@ -34647,12 +32982,12 @@
 
         ins_redraw(TRUE);
 
-        if (curwin->w_p_scb)
+        if (curwin-> w_onebuf_opt.wo_scb )
         {
             do_check_scrollbind(TRUE);
         }
 
-        if (curwin->w_p_crb)
+        if (curwin-> w_onebuf_opt.wo_crb )
         {
             do_check_cursorbind();
         }
@@ -34664,7 +32999,7 @@
 
         may_send_t_RK();
 
-        if (c != K_CURSORHOLD && c != K_COMPLETE_DELAY)
+        if (c !=   (-((KS_EXTRA) + ((int)(KE_CURSORHOLD) << 8)))   && c !=   (-((KS_EXTRA) + ((int)(KE_COMPLETE_DELAY) << 8)))  )
         {
             lastc = c;
         }
@@ -34677,9 +33012,9 @@
         {
             dont_sync_undo = FALSE;
         }
-        if (cmdchar == K_PS)
+        if (cmdchar ==   (-(('P') + ((int)('S') << 8)))  )
         {
-            c = K_PS;
+            c =   (-(('P') + ((int)('S') << 8)))  ;
         }
         else
         {
@@ -34708,7 +33043,7 @@
 
                 if (stop_insert_mode)
                 {
-                    if (c != K_IGNORE && c != K_NOP)
+                    if (c !=   (-((KS_EXTRA) + ((int)(KE_IGNORE) << 8)))   && c !=   (-((KS_EXTRA) + ((int)(KE_NOP) << 8)))  )
                     {
                         vungetc(c);
                     }
@@ -34725,11 +33060,11 @@
                     ins_compl_prep(ESC);
                     goto doESCkey;
                 }
-            } while (c == K_IGNORE || c == K_NOP);
+            } while (c ==   (-((KS_EXTRA) + ((int)(KE_IGNORE) << 8)))   || c ==   (-((KS_EXTRA) + ((int)(KE_NOP) << 8)))  );
         }
 
         did_cursorhold = TRUE;
-        if (c != K_CURSORHOLD && c != K_COMPLETE_DELAY)
+        if (c !=   (-((KS_EXTRA) + ((int)(KE_CURSORHOLD) << 8)))   && c !=   (-((KS_EXTRA) + ((int)(KE_COMPLETE_DELAY) << 8)))  )
         {
             ins_compl_clear_autocomplete_delay();
             ins_compl_disarm_autostart();
@@ -34746,7 +33081,7 @@
 
         if (ins_compl_active() && curwin->w_cursor.col >= ins_compl_col() && ins_compl_has_shown_match() && pum_wanted())
         {
-            if ((c == K_BS || c == Ctrl_H) && curwin->w_cursor.col > ins_compl_col() && (c = ins_compl_bs()) == NUL)
+            if ((c ==   (-(('k') + ((int)('b') << 8)))   || c == Ctrl_H) && curwin->w_cursor.col > ins_compl_col() && (c = ins_compl_bs()) == NUL)
             {
                 continue;
             }
@@ -34765,7 +33100,7 @@
                     continue;
                 }
 
-                if ((c == Ctrl_Y || (ins_compl_enter_selects() && (c == CAR || c == K_KENTER || c == NL))) && stop_arrow() == OK)
+                if ((c == Ctrl_Y || (ins_compl_enter_selects() && (c == CAR || c ==   (-(('K') + ((int)('A') << 8)))   || c == NL))) && stop_arrow() == OK)
                 {
                     ins_compl_delete();
                     if (ins_compl_preinsert_longest() && !ins_compl_is_match_selected())
@@ -34779,7 +33114,7 @@
                         ins_compl_insert(FALSE, FALSE);
                     }
                 }
-                else if (IS_WHITE_NL_OR_NUL(c) && ins_compl_preinsert_effect())
+                else if ( ((c) == ' ' || (c) == '\t' || (c) == '\n' || (c) == NUL)  && ins_compl_preinsert_effect())
                 {
                     ins_compl_delete();
                 }
@@ -34859,10 +33194,11 @@
                 break;
             }
 
+        __attribute__((fallthrough));
         case Ctrl_C:
             if (c == Ctrl_C && cmdwin_type != 0)
             {
-                cmdwin_result = K_IGNORE;
+                cmdwin_result =   (-((KS_EXTRA) + ((int)(KE_IGNORE) << 8)))  ;
                 got_int = FALSE;
                 nomove = TRUE;
                 goto doESCkey;
@@ -34897,9 +33233,9 @@
                 }
                 did_cursorhold = FALSE;
 
-                if (!char_avail() && curbuf->b_last_changedtick_i == CHANGEDTICK(curbuf))
+                if (!char_avail() && curbuf->b_last_changedtick_i ==  ((curbuf)->b_ct_di.di_tv.vval.v_number) )
                 {
-                    curbuf->b_last_changedtick = CHANGEDTICK(curbuf);
+                    curbuf->b_last_changedtick =  ((curbuf)->b_ct_di.di_tv.vval.v_number) ;
                 }
                 return (c == Ctrl_O);
             }
@@ -34928,25 +33264,25 @@
             count = 0;
             goto doESCkey;
 
-        case K_INS:
-        case K_KINS:
+        case   (-(('k') + ((int)('I') << 8)))  :
+        case   (-((KS_EXTRA) + ((int)(KE_KINS) << 8)))  :
             ins_insert(replaceState);
             break;
 
-        case K_SELECT:
+        case   (-((KS_SELECT) + ((int)( ('X') ) << 8)))  :
             break;
 
-        case K_HELP:
-        case K_F1:
-        case K_XF1:
-            stuffcharReadbuff(K_HELP);
+        case   (-(('%') + ((int)('1') << 8)))  :
+        case   (-(('k') + ((int)('1') << 8)))  :
+        case   (-((KS_EXTRA) + ((int)(KE_XF1) << 8)))  :
+            stuffcharReadbuff(  (-(('%') + ((int)('1') << 8)))  );
             if (p_im)
             {
                 need_start_insertmode = TRUE;
             }
             goto doESCkey;
 
-        case K_ZERO:
+        case   (-((KS_ZERO) + ((int)( ('X') ) << 8)))  :
         case NUL:
         case Ctrl_A:
             if (stuff_inserted(NUL, 1L, (c == Ctrl_A)) == FAIL && c != Ctrl_A && !p_im)
@@ -34991,20 +33327,21 @@
             inserted_space = FALSE;
             break;
 
-        case K_DEL:
-        case K_KDEL:
+        case   (-(('k') + ((int)('D') << 8)))  :
+        case   (-((KS_EXTRA) + ((int)(KE_KDEL) << 8)))  :
             ins_del();
             auto_format(FALSE, TRUE);
             break;
 
-        case K_BS:
-        case K_S_BS:
+        case   (-(('k') + ((int)('b') << 8)))  :
+        case   (-((KS_EXTRA) + ((int)(KE_S_BS) << 8)))  :
         case Ctrl_H:
             did_backspace = ins_bs(c, BACKSPACE_CHAR, &inserted_space);
             auto_format(FALSE, TRUE);
             if (did_backspace)
             {
-                MAY_TRIGGER_AUTOCOMPLETE(c);
+                 if (ins_compl_has_autocomplete() && !char_avail()                       && curwin->w_cursor.col > 0)
+                     {                                                                   (c) = char_before_cursor();                                     if (vim_isprintc(c))                                                                                                  update_screen(UPD_VALID);          out_flush();                                    ins_compl_enable_autocomplete();                ins_compl_arm_autostart();                      if (!ins_compl_arm_autocomplete_delay())            goto docomplete;                                                          }
             }
             break;
 
@@ -35013,7 +33350,8 @@
             auto_format(FALSE, TRUE);
             if (did_backspace)
             {
-                MAY_TRIGGER_AUTOCOMPLETE(c);
+                 if (ins_compl_has_autocomplete() && !char_avail()                       && curwin->w_cursor.col > 0)
+                     {                                                                   (c) = char_before_cursor();                                     if (vim_isprintc(c))                                                                                                  update_screen(UPD_VALID);          out_flush();                                    ins_compl_enable_autocomplete();                ins_compl_arm_autostart();                      if (!ins_compl_arm_autocomplete_delay())            goto docomplete;                                                          }
             }
             break;
 
@@ -35023,77 +33361,78 @@
             inserted_space = FALSE;
             if (did_backspace)
             {
-                MAY_TRIGGER_AUTOCOMPLETE(c);
+                 if (ins_compl_has_autocomplete() && !char_avail()                       && curwin->w_cursor.col > 0)
+                     {                                                                   (c) = char_before_cursor();                                     if (vim_isprintc(c))                                                                                                  update_screen(UPD_VALID);          out_flush();                                    ins_compl_enable_autocomplete();                ins_compl_arm_autostart();                      if (!ins_compl_arm_autocomplete_delay())            goto docomplete;                                                          }
             }
             break;
 
-        case K_LEFTMOUSE:
-        case K_LEFTMOUSE_NM:
-        case K_LEFTDRAG:
-        case K_LEFTRELEASE:
-        case K_LEFTRELEASE_NM:
-        case K_MOUSEMOVE:
-        case K_MIDDLEMOUSE:
-        case K_MIDDLEDRAG:
-        case K_MIDDLERELEASE:
-        case K_RIGHTMOUSE:
-        case K_RIGHTDRAG:
-        case K_RIGHTRELEASE:
-        case K_X1MOUSE:
-        case K_X1DRAG:
-        case K_X1RELEASE:
-        case K_X2MOUSE:
-        case K_X2DRAG:
-        case K_X2RELEASE:
+        case   (-((KS_EXTRA) + ((int)(KE_LEFTMOUSE) << 8)))  :
+        case   (-((KS_EXTRA) + ((int)(KE_LEFTMOUSE_NM) << 8)))  :
+        case   (-((KS_EXTRA) + ((int)(KE_LEFTDRAG) << 8)))  :
+        case   (-((KS_EXTRA) + ((int)(KE_LEFTRELEASE) << 8)))  :
+        case   (-((KS_EXTRA) + ((int)(KE_LEFTRELEASE_NM) << 8)))  :
+        case   (-((KS_EXTRA) + ((int)(KE_MOUSEMOVE) << 8)))  :
+        case   (-((KS_EXTRA) + ((int)(KE_MIDDLEMOUSE) << 8)))  :
+        case   (-((KS_EXTRA) + ((int)(KE_MIDDLEDRAG) << 8)))  :
+        case   (-((KS_EXTRA) + ((int)(KE_MIDDLERELEASE) << 8)))  :
+        case   (-((KS_EXTRA) + ((int)(KE_RIGHTMOUSE) << 8)))  :
+        case   (-((KS_EXTRA) + ((int)(KE_RIGHTDRAG) << 8)))  :
+        case   (-((KS_EXTRA) + ((int)(KE_RIGHTRELEASE) << 8)))  :
+        case   (-((KS_EXTRA) + ((int)(KE_X1MOUSE) << 8)))  :
+        case   (-((KS_EXTRA) + ((int)(KE_X1DRAG) << 8)))  :
+        case   (-((KS_EXTRA) + ((int)(KE_X1RELEASE) << 8)))  :
+        case   (-((KS_EXTRA) + ((int)(KE_X2MOUSE) << 8)))  :
+        case   (-((KS_EXTRA) + ((int)(KE_X2DRAG) << 8)))  :
+        case   (-((KS_EXTRA) + ((int)(KE_X2RELEASE) << 8)))  :
             ins_mouse(c);
             break;
 
-        case K_MOUSEDOWN:
+        case   (-((KS_EXTRA) + ((int)(KE_MOUSEDOWN) << 8)))  :
             ins_mousescroll(MSCR_DOWN);
             break;
 
-        case K_MOUSEUP:
+        case   (-((KS_EXTRA) + ((int)(KE_MOUSEUP) << 8)))  :
             ins_mousescroll(MSCR_UP);
             break;
 
-        case K_MOUSELEFT:
-            ins_mousescroll(MSCR_LEFT);
+        case   (-((KS_EXTRA) + ((int)(KE_MOUSELEFT) << 8)))  :
+            ins_mousescroll( (-1) );
             break;
 
-        case K_MOUSERIGHT:
-            ins_mousescroll(MSCR_RIGHT);
+        case   (-((KS_EXTRA) + ((int)(KE_MOUSERIGHT) << 8)))  :
+            ins_mousescroll( (-2) );
             break;
 
-        case K_PS:
+        case   (-(('P') + ((int)('S') << 8)))  :
             bracketed_paste(PASTE_INSERT, FALSE, NULL);
-            if (cmdchar == K_PS)
+            if (cmdchar ==   (-(('P') + ((int)('S') << 8)))  )
             {
                 goto doESCkey;
             }
             break;
-        case K_PE:
+        case   (-(('P') + ((int)('E') << 8)))  :
             break;
 
-        case K_IGNORE:
+        case   (-((KS_EXTRA) + ((int)(KE_IGNORE) << 8)))  :
             break;
 
-        case K_COMMAND:
-        case K_SCRIPT_COMMAND:
+        case   (-((KS_EXTRA) + ((int)(KE_COMMAND) << 8)))  :
+        case   (-((KS_EXTRA) + ((int)(KE_SCRIPT_COMMAND) << 8)))  :
             {
                 bufref_T    save_curbuf;
-                varnumber_T tick = CHANGEDTICK(curbuf);
+                varnumber_T tick =  ((curbuf)->b_ct_di.di_tv.vval.v_number) ;
 
                 set_bufref(&save_curbuf, curbuf);
                 do_cmdkey_command(c, 0);
 
-                if (curbuf->b_u_synced || (bufref_valid(&save_curbuf) && curbuf == save_curbuf.br_buf && tick != CHANGEDTICK(curbuf)))
+                if (curbuf->b_u_synced || (bufref_valid(&save_curbuf) && curbuf == save_curbuf.br_buf && tick !=  ((curbuf)->b_ct_di.di_tv.vval.v_number) ))
                 {
                     ins_need_undo = TRUE;
                 }
             }
             break;
 
-        case K_CURSORHOLD:
+        case   (-((KS_EXTRA) + ((int)(KE_CURSORHOLD) << 8)))  :
             ins_apply_autocmds(EVENT_CURSORHOLDI);
             did_cursorhold = TRUE;
             if (dont_sync_undo == TRUE)
@@ -35102,7 +33441,7 @@
             }
             break;
 
-        case K_COMPLETE_DELAY:
+        case   (-((KS_EXTRA) + ((int)(KE_COMPLETE_DELAY) << 8)))  :
             if (dont_sync_undo == TRUE)
             {
                 dont_sync_undo = MAYBE;
@@ -35121,21 +33460,21 @@
             ins_compl_arm_autostart();
             goto docomplete;
 
-        case K_HOME:
-        case K_KHOME:
-        case K_S_HOME:
-        case K_C_HOME:
+        case   (-(('k') + ((int)('h') << 8)))  :
+        case   (-(('K') + ((int)('1') << 8)))  :
+        case   (-(('#') + ((int)('2') << 8)))  :
+        case   (-((KS_EXTRA) + ((int)(KE_C_HOME) << 8)))  :
             ins_home(c);
             break;
 
-        case K_END:
-        case K_KEND:
-        case K_S_END:
-        case K_C_END:
+        case   (-(('@') + ((int)('7') << 8)))  :
+        case   (-(('K') + ((int)('4') << 8)))  :
+        case   (-(('*') + ((int)('7') << 8)))  :
+        case   (-((KS_EXTRA) + ((int)(KE_C_END) << 8)))  :
             ins_end(c);
             break;
 
-        case K_LEFT:
+        case   (-(('k') + ((int)('l') << 8)))  :
             if (mod_mask & (MOD_MASK_SHIFT|MOD_MASK_CTRL))
             {
                 ins_s_left();
@@ -35146,12 +33485,12 @@
             }
             break;
 
-        case K_S_LEFT:
-        case K_C_LEFT:
+        case   (-(('#') + ((int)('4') << 8)))  :
+        case   (-((KS_EXTRA) + ((int)(KE_C_LEFT) << 8)))  :
             ins_s_left();
             break;
 
-        case K_RIGHT:
+        case   (-(('k') + ((int)('r') << 8)))  :
             if (mod_mask & (MOD_MASK_SHIFT|MOD_MASK_CTRL))
             {
                 ins_s_right();
@@ -35162,12 +33501,12 @@
             }
             break;
 
-        case K_S_RIGHT:
-        case K_C_RIGHT:
+        case   (-(('%') + ((int)('i') << 8)))  :
+        case   (-((KS_EXTRA) + ((int)(KE_C_RIGHT) << 8)))  :
             ins_s_right();
             break;
 
-        case K_UP:
+        case   (-(('k') + ((int)('u') << 8)))  :
             if (pum_visible())
             {
                 goto docomplete;
@@ -35182,9 +33521,9 @@
             }
             break;
 
-        case K_S_UP:
-        case K_PAGEUP:
-        case K_KPAGEUP:
+        case   (-((KS_EXTRA) + ((int)(KE_S_UP) << 8)))  :
+        case   (-(('k') + ((int)('P') << 8)))  :
+        case   (-(('K') + ((int)('3') << 8)))  :
             if (pum_visible())
             {
                 goto docomplete;
@@ -35192,7 +33531,7 @@
             ins_pageup();
             break;
 
-        case K_DOWN:
+        case   (-(('k') + ((int)('d') << 8)))  :
             if (pum_visible())
             {
                 goto docomplete;
@@ -35207,9 +33546,9 @@
             }
             break;
 
-        case K_S_DOWN:
-        case K_PAGEDOWN:
-        case K_KPAGEDOWN:
+        case   (-((KS_EXTRA) + ((int)(KE_S_DOWN) << 8)))  :
+        case   (-(('k') + ((int)('N') << 8)))  :
+        case   (-(('K') + ((int)('5') << 8)))  :
             if (pum_visible())
             {
                 goto docomplete;
@@ -35217,9 +33556,10 @@
             ins_pagedown();
             break;
 
-        case K_S_TAB:
+        case   (-(('k') + ((int)('B') << 8)))  :
             c = TAB;
 
+        __attribute__((fallthrough));
         case TAB:
             inserted_space = FALSE;
             if (ins_tab())
@@ -35229,8 +33569,9 @@
             auto_format(FALSE, TRUE);
             break;
 
-        case K_KENTER:
+        case   (-(('K') + ((int)('A') << 8)))  :
             c = CAR;
+        __attribute__((fallthrough));
         case CAR:
         case NL:
             if (cmdwin_type != 0)
@@ -35297,6 +33638,7 @@
                 goto normalchar;
             }
 
+        __attribute__((fallthrough));
         case Ctrl_P:
         case Ctrl_N:
             if (*curbuf->b_p_cpt == NUL && (ctrl_x_mode_normal() || ctrl_x_mode_whole_line()) && !compl_status_local())
@@ -35351,13 +33693,20 @@
 
             if (ins_compl_has_autocomplete() && !char_avail() && vim_isprintc(c))
             {
-                TRIGGER_AUTOCOMPLETE();
+                 update_screen(UPD_VALID);
+                 out_flush();
+                 ins_compl_enable_autocomplete();
+                 ins_compl_arm_autostart();
+                 if (!ins_compl_arm_autocomplete_delay())
+                 {
+                     goto docomplete;
+                 }
             }
 
             break;
         }
 
-        if (c != K_CURSORHOLD && c != K_COMPLETE_DELAY)
+        if (c !=   (-((KS_EXTRA) + ((int)(KE_CURSORHOLD) << 8)))   && c !=   (-((KS_EXTRA) + ((int)(KE_COMPLETE_DELAY) << 8)))  )
         {
             did_cursorhold = FALSE;
         }
@@ -35396,13 +33745,12 @@
     void
 ins_redraw(int ready)
 {
-
     if (char_avail())
     {
         return;
     }
 
-    if (ready && (has_cursormovedI()) && !EQUAL_POS(last_cursormoved, curwin->w_cursor) && !pum_visible())
+    if (ready && (has_cursormovedI()) && ! (((last_cursormoved).lnum == (curwin->w_cursor).lnum) && ((last_cursormoved).col == (curwin->w_cursor).col) && ((last_cursormoved).coladd == (curwin->w_cursor).coladd))  && !pum_visible())
     {
         if (has_cursormovedI())
         {
@@ -35412,31 +33760,31 @@
         last_cursormoved = curwin->w_cursor;
     }
 
-    if (ready && has_textchangedI() && curbuf->b_last_changedtick_i != CHANGEDTICK(curbuf) && !pum_visible())
+    if (ready && has_textchangedI() && curbuf->b_last_changedtick_i !=  ((curbuf)->b_ct_di.di_tv.vval.v_number)  && !pum_visible())
     {
         aco_save_T      aco;
-        varnumber_T     tick = CHANGEDTICK(curbuf);
+        varnumber_T     tick =  ((curbuf)->b_ct_di.di_tv.vval.v_number) ;
 
         aucmd_prepbuf(&aco, curbuf);
         apply_autocmds(EVENT_TEXTCHANGEDI, NULL, NULL, FALSE, curbuf);
         aucmd_restbuf(&aco);
-        curbuf->b_last_changedtick_i = CHANGEDTICK(curbuf);
-        if (tick != CHANGEDTICK(curbuf))
+        curbuf->b_last_changedtick_i =  ((curbuf)->b_ct_di.di_tv.vval.v_number) ;
+        if (tick !=  ((curbuf)->b_ct_di.di_tv.vval.v_number) )
         {
             u_save(curwin->w_cursor.lnum, (linenr_T)(curwin->w_cursor.lnum + 1));
         }
     }
 
-    if (ready && has_textchangedP() && curbuf->b_last_changedtick_pum != CHANGEDTICK(curbuf) && pum_visible())
+    if (ready && has_textchangedP() && curbuf->b_last_changedtick_pum !=  ((curbuf)->b_ct_di.di_tv.vval.v_number)  && pum_visible())
     {
         aco_save_T      aco;
-        varnumber_T     tick = CHANGEDTICK(curbuf);
+        varnumber_T     tick =  ((curbuf)->b_ct_di.di_tv.vval.v_number) ;
 
         aucmd_prepbuf(&aco, curbuf);
         apply_autocmds(EVENT_TEXTCHANGEDP, NULL, NULL, FALSE, curbuf);
         aucmd_restbuf(&aco);
-        curbuf->b_last_changedtick_pum = CHANGEDTICK(curbuf);
-        if (tick != CHANGEDTICK(curbuf))
+        curbuf->b_last_changedtick_pum =  ((curbuf)->b_ct_di.di_tv.vval.v_number) ;
+        if (tick !=  ((curbuf)->b_ct_di.di_tv.vval.v_number) )
         {
             u_save(curwin->w_cursor.lnum, (linenr_T)(curwin->w_cursor.lnum + 1));
         }
@@ -35475,7 +33823,7 @@
         edit_putchar('^', TRUE);
         did_putchar = TRUE;
     }
-    AppendToRedobuff((char_u *)CTRL_V_STR);
+    AppendToRedobuff((char_u *) "\026" );
 
     add_to_showcmd_c(Ctrl_V);
 
@@ -35512,7 +33860,7 @@
             {
                 ++argidx;
             }
-            else if (VIM_ISDIGIT(p[idx]))
+            else if ( ((unsigned)(p[idx]) - '0' < 10) )
             {
                 arg[argidx] = arg[argidx] * 10 + (p[idx] - '0');
             }
@@ -35537,10 +33885,10 @@
 }
 
 static int  pc_status;
-#define PC_STATUS_UNSET 0
-#define PC_STATUS_RIGHT 1
-#define PC_STATUS_LEFT  2
-#define PC_STATUS_SET   3
+enum { PC_STATUS_UNSET = 0 };
+enum { PC_STATUS_RIGHT = 1 };
+enum { PC_STATUS_LEFT = 2 };
+enum { PC_STATUS_SET = 3 };
 static char_u pc_bytes[MB_MAXBYTES + 1];
 static int  pc_attr;
 static int  pc_row;
@@ -35560,13 +33908,13 @@
     validate_cursor();
     if (highlight)
     {
-        attr = HL_ATTR(HLF_8);
+        attr =  highlight_attr[(int)(HLF_8)] ;
     }
     else
     {
         attr = 0;
     }
-    pc_row = W_WINROW(curwin) + curwin->w_wrow;
+    pc_row =  (curwin->w_winrow)  + curwin->w_wrow;
     pc_col = curwin->w_wincol;
     pc_status = PC_STATUS_UNSET;
     {
@@ -35652,7 +34000,7 @@
 {
     int     i;
 
-    for (i = (int)len - 1; i >= 0 && VIM_ISWHITE(line[i]); i--)
+    for (i = (int)len - 1; i >= 0 &&  ((line[i]) == ' ' || (line[i]) == '\t') ; i--)
     {
         if (State & REPLACE_FLAG)
         {
@@ -35680,7 +34028,7 @@
 }
 
     static int
-del_char_after_col(int limit_col UNUSED)
+del_char_after_col(int limit_col  __attribute__((unused)) )
 {
     if (enc_utf8 && limit_col >= 0)
     {
@@ -35741,7 +34089,7 @@
             break;
         }
 
-        if ((State & MODE_CMDLINE) == 0 && MB_BYTE2LEN_CHECK(nc) == 1)
+        if ((State & MODE_CMDLINE) == 0 &&  (((nc) < 0 || (nc) > 255) ? 1 : mb_bytelen_tab[nc])  == 1)
         {
             add_to_showcmd(nc);
         }
@@ -35777,7 +34125,7 @@
             }
             else
             {
-                if (!VIM_ISDIGIT(nc))
+                if (! ((unsigned)(nc) - '0' < 10) )
                 {
                     break;
                 }
@@ -35814,7 +34162,7 @@
     }
     if (i == 0)
     {
-        if (nc == K_ZERO)
+        if (nc ==   (-((KS_ZERO) + ((int)( ('X') ) << 8)))  )
         {
             cc = '\n';
             nc = 0;
@@ -35851,10 +34199,10 @@
     char_u  *p;
     int     len;
 
-    if (IS_SPECIAL(c) || (mod_mask && allow_modmask))
+    if ( ((c) < 0)  || (mod_mask && allow_modmask))
     {
         p = get_special_key_name(c, mod_mask);
-        len = (int)STRLEN(p);
+        len = (int) strlen((char *)(p)) ;
         c = p[len - 1];
         if (len > 2)
         {
@@ -35874,8 +34222,6 @@
     }
 }
 
-#define ISSPECIAL(c)    ((c) < ' ' || (c) >= DEL || (c) == '0' || (c) == '^')
-
     void
 insertchar(int         c, int         flags, int         second_indent)
 {
@@ -35887,7 +34233,7 @@
     textwidth = comp_textwidth(force_format);
     fo_ins_blank = has_format_option(FO_INS_BLANK);
 
-    if (textwidth > 0 && (force_format || (!VIM_ISWHITE(c) && !((State & REPLACE_FLAG) && !(State & VREPLACE_FLAG) && *ml_get_cursor() != NUL) && (curwin->w_cursor.lnum != Insstart.lnum || ((!has_format_option(FO_INS_LONG) || Insstart_textlen <= (colnr_T)textwidth) && (!fo_ins_blank || Insstart_blank_vcol <= (colnr_T)textwidth))))))
+    if (textwidth > 0 && (force_format || (! ((c) == ' ' || (c) == '\t')  && !((State & REPLACE_FLAG) && !(State & VREPLACE_FLAG) && *ml_get_cursor() != NUL) && (curwin->w_cursor.lnum != Insstart.lnum || ((!has_format_option(FO_INS_LONG) || Insstart_textlen <= (colnr_T)textwidth) && (!fo_ins_blank || Insstart_blank_vcol <= (colnr_T)textwidth))))))
     {
             internal_format(textwidth, second_indent, flags, c == NUL, c);
     }
@@ -35913,7 +34259,7 @@
                 ++p;
             }
             middle_len = copy_option_part(&p, lead_end, COM_MAX_LEN, ",");
-            while (middle_len > 0 && VIM_ISWHITE(lead_end[middle_len - 1]))
+            while (middle_len > 0 &&  ((lead_end[middle_len - 1]) == ' ' || (lead_end[middle_len - 1]) == '\t') )
             {
                 --middle_len;
             }
@@ -35925,7 +34271,7 @@
             end_len = copy_option_part(&p, lead_end, COM_MAX_LEN, ",");
 
             i = curwin->w_cursor.col;
-            while (--i >= 0 && VIM_ISWHITE(line[i]))
+            while (--i >= 0 &&  ((line[i]) == ' ' || (line[i]) == '\t') )
             {
                 ;
             }
@@ -35948,9 +34294,8 @@
     can_si = FALSE;
     can_si_back = FALSE;
 
-    if (       !ISSPECIAL(c) && (!has_mbyte || (*mb_char2len)(c) == 1) && !has_insertcharpre() && vpeekc() != NUL && !(State & REPLACE_FLAG) && !cindent_on())
+    if (       ! ((c) < ' ' || (c) >= DEL || (c) == '0' || (c) == '^')  && (!has_mbyte || (*mb_char2len)(c) == 1) && !has_insertcharpre() && vpeekc() != NUL && !(State & REPLACE_FLAG) && !cindent_on())
     {
-#define INPUT_BUFLEN 100
         char_u          buf[INPUT_BUFLEN + 1];
         int             i;
         colnr_T         virtcol = 0;
@@ -35961,7 +34306,7 @@
         {
             virtcol = get_nolist_virtcol();
         }
-        while (    (c = vpeekc()) != NUL && !ISSPECIAL(c) && (!has_mbyte || MB_BYTE2LEN_CHECK(c) == 1) && i < INPUT_BUFLEN && (textwidth == 0 || (virtcol += byte2cells(buf[i - 1])) < (colnr_T)textwidth) && !(!no_abbr && !vim_iswordc(c) && vim_iswordc(buf[i - 1])))
+        while (    (c = vpeekc()) != NUL && ! ((c) < ' ' || (c) >= DEL || (c) == '0' || (c) == '^')  && (!has_mbyte ||  (((c) < 0 || (c) > 255) ? 1 : mb_bytelen_tab[c])  == 1) && i < INPUT_BUFLEN && (textwidth == 0 || (virtcol += byte2cells(buf[i - 1])) < (colnr_T)textwidth) && !(!no_abbr && !vim_iswordc(c) && vim_iswordc(buf[i - 1])))
         {
             buf[i++] = vgetc();
         }
@@ -36015,7 +34360,7 @@
 {
     char_u      buf[10];
 
-    if (VIM_ISDIGIT(c))
+    if ( ((unsigned)(c) - '0' < 10) )
     {
         vim_snprintf((char *)buf, sizeof(buf), "%03d", c);
         AppendToRedobuff(buf);
@@ -36048,7 +34393,7 @@
 {
     if (!arrow_used && end_change)
     {
-        AppendToRedobuff(ESC_STR);
+        AppendToRedobuff( (char_u *)"\033" );
         stop_insert(end_insert_pos, FALSE, FALSE);
         arrow_used = TRUE;
     }
@@ -36086,7 +34431,7 @@
     {
         if (u_save_cursor() == OK)
         {
-            if (LT_POS(curwin->w_cursor, Insstart))
+            if ( (((curwin->w_cursor).lnum != (Insstart).lnum)               ? (curwin->w_cursor).lnum < (Insstart).lnum                   : (curwin->w_cursor).col != (Insstart).col                        ? (curwin->w_cursor).col < (Insstart).col                     : (curwin->w_cursor).coladd < (Insstart).coladd) )
             {
                 Insstart = curwin->w_cursor;
                 Insstart_textlen = (colnr_T)linetabsize_str(ml_get_curline());
@@ -36131,7 +34476,7 @@
             {
                 dec_cursor();
                 cc = gchar_cursor();
-                if (!VIM_ISWHITE(cc))
+                if (! ((cc) == ' ' || (cc) == '\t') )
                 {
                     curwin->w_cursor = tpos;
                 }
@@ -36139,7 +34484,7 @@
 
             auto_format(TRUE, FALSE);
 
-            if (VIM_ISWHITE(cc))
+            if ( ((cc) == ' ' || (cc) == '\t') )
             {
                 if (gchar_cursor() != NUL)
                 {
@@ -36179,7 +34524,7 @@
                         --curwin->w_cursor.col;
                     }
                     cc = gchar_cursor();
-                    if (!VIM_ISWHITE(cc))
+                    if (! ((cc) == ' ' || (cc) == '\t') )
                     {
                         break;
                     }
@@ -36262,11 +34607,11 @@
     for (i = 0; i < len; ++i)
     {
         c = temp[i];
-        if (c == K_SPECIAL)
+        if (c ==  (0x80) )
         {
-            *s++ = K_SPECIAL;
+            *s++ =  (0x80) ;
             *s++ = KS_SPECIAL;
-            *s++ = KE_FILLER;
+            *s++ =  ('X') ;
         }
         else
         {
@@ -36292,7 +34637,7 @@
         {
             char_u  *ptr;
 
-            for (ptr = ml_get_curline(); VIM_ISWHITE(*ptr) && !((flags & BL_FIX) && ptr[1] == NUL); ++ptr)
+            for (ptr = ml_get_curline();  ((*ptr) == ' ' || (*ptr) == '\t')  && !((flags & BL_FIX) && ptr[1] == NUL); ++ptr)
             {
                 ++curwin->w_cursor.col;
             }
@@ -36515,13 +34860,11 @@
         switch (last)
         {
         case '0':
-#define TEXT_TO_INSERT_0 "\026\060\064\070"
-            stuffReadbuffLen((char_u *)TEXT_TO_INSERT_0, STRLEN_LITERAL(TEXT_TO_INSERT_0));
+            stuffReadbuffLen((char_u *) "\026\060\064\070" ,  (sizeof( "\026\060\064\070"  "") - 1) );
             break;
 
         case '^':
-#define TEXT_TO_INSERT_CARET "\026^"
-            stuffReadbuffLen((char_u *)TEXT_TO_INSERT_CARET, STRLEN_LITERAL(TEXT_TO_INSERT_CARET));
+            stuffReadbuffLen((char_u *) "\026^" ,  (sizeof( "\026^"  "") - 1) );
             break;
 
         default:
@@ -36603,7 +34946,7 @@
     if (replace_stack_len <= replace_stack_nr)
     {
         replace_stack_len += 50;
-        p = ALLOC_MULT(char_u, replace_stack_len);
+        p =  (char_u *)alloc(sizeof(char_u) * (replace_stack_len)) ;
         if (p == NULL)
         {
             replace_stack_len -= 50;
@@ -36611,7 +34954,7 @@
         }
         if (replace_stack != NULL)
         {
-            mch_memmove(p, replace_stack, (size_t)(replace_stack_nr * sizeof(char_u)));
+             memmove((char *)(p), (char *)(replace_stack), (size_t)(replace_stack_nr * sizeof(char_u))) ;
             vim_free(replace_stack);
         }
         replace_stack = p;
@@ -36619,7 +34962,7 @@
     p = replace_stack + replace_stack_nr - replace_offset;
     if (replace_offset)
     {
-        mch_memmove(p + 1, p, (size_t)(replace_offset * sizeof(char_u)));
+         memmove((char *)(p + 1), (char *)(p), (size_t)(replace_offset * sizeof(char_u))) ;
     }
     *p = c;
     ++replace_stack_nr;
@@ -36658,7 +35001,7 @@
         if (replace_stack[i] == NUL && off-- <= 0)
         {
             --replace_stack_nr;
-            mch_memmove(replace_stack + i, replace_stack + i + 1, (size_t)(replace_stack_nr - i));
+             memmove((char *)(replace_stack + i), (char *)(replace_stack + i + 1), (size_t)(replace_stack_nr - i)) ;
             return;
         }
     }
@@ -36687,7 +35030,7 @@
     int         i;
     int         c;
 
-    if (has_mbyte && (n = MB_BYTE2LEN(cc)) > 1)
+    if (has_mbyte && (n =  mb_bytelen_tab[cc] ) > 1)
     {
         buf[0] = cc;
         for (i = 1; i < n; ++i)
@@ -36710,7 +35053,7 @@
             {
                 break;
             }
-            if ((n = MB_BYTE2LEN(c)) == 1)
+            if ((n =  mb_bytelen_tab[c] ) == 1)
             {
                 replace_push(c);
                 break;
@@ -36741,7 +35084,8 @@
     static void
 replace_flush(void)
 {
-    VIM_CLEAR(replace_stack);
+     vim_free(replace_stack);
+     (replace_stack) = NULL;
     replace_stack_len = 0;
     replace_stack_nr = 0;
 }
@@ -36835,13 +35179,13 @@
     ++no_mapping;
     ++allow_keys;
     regname = plain_vgetc();
-    LANGMAP_ADJUST(regname, TRUE);
+      ;
     if (regname == Ctrl_R || regname == Ctrl_O || regname == Ctrl_P)
     {
         literally = regname;
         add_to_showcmd_c(literally);
         regname = plain_vgetc();
-        LANGMAP_ADJUST(regname, TRUE);
+          ;
     }
     --no_mapping;
     --allow_keys;
@@ -36852,7 +35196,7 @@
             AppendCharToRedobuff(literally);
             AppendCharToRedobuff(regname);
 
-            do_put(regname, NULL, BACKWARD, 1L, (literally == Ctrl_P ? PUT_FIXINDENT : 0) | PUT_CURSEND);
+            do_put(regname, NULL,  (-1) , 1L, (literally == Ctrl_P ? PUT_FIXINDENT : 0) | PUT_CURSEND);
         }
         else if (insert_reg(regname, literally) == FAIL)
         {
@@ -36891,13 +35235,13 @@
     --allow_keys;
     switch (c)
     {
-        case K_UP:
+        case   (-(('k') + ((int)('u') << 8)))  :
         case Ctrl_K:
         case 'k':
             ins_up(TRUE);
                   break;
 
-        case K_DOWN:
+        case   (-(('k') + ((int)('d') << 8)))  :
         case Ctrl_J:
         case 'j':
             ins_down(TRUE);
@@ -36962,7 +35306,7 @@
     {
         if (cmdchar != 'r' && cmdchar != 'v')
         {
-            AppendToRedobuff(p_im ? (char_u *)"\014" : ESC_STR);
+            AppendToRedobuff(p_im ? (char_u *)"\014" :  (char_u *)"\033" );
         }
 
         if (*count > 0)
@@ -36984,7 +35328,7 @@
             (void)start_redo_ins();
             if (cmdchar == 'r' || cmdchar == 'v')
             {
-                stuffRedoReadbuff(ESC_STR);
+                stuffRedoReadbuff( (char_u *)"\033" );
             }
             ++RedrawingDisabled;
             disabled_redraw = TRUE;
@@ -37040,7 +35384,7 @@
     setmouse();
     if (!p_ek)
     {
-        MAY_WANT_TO_LOG_THIS;
+          ;
 
         out_str_t_BE();
 
@@ -37068,22 +35412,23 @@
     }
     switch (c)
     {
-        case K_KHOME:
-        case K_KEND:
-        case K_PAGEUP:
-        case K_KPAGEUP:
-        case K_PAGEDOWN:
-        case K_KPAGEDOWN:
+        case   (-(('K') + ((int)('1') << 8)))  :
+        case   (-(('K') + ((int)('4') << 8)))  :
+        case   (-(('k') + ((int)('P') << 8)))  :
+        case   (-(('K') + ((int)('3') << 8)))  :
+        case   (-(('k') + ((int)('N') << 8)))  :
+        case   (-(('K') + ((int)('5') << 8)))  :
             if (!(mod_mask & MOD_MASK_SHIFT))
             {
                 break;
             }
-        case K_S_LEFT:
-        case K_S_RIGHT:
-        case K_S_UP:
-        case K_S_DOWN:
-        case K_S_END:
-        case K_S_HOME:
+        __attribute__((fallthrough));
+        case   (-(('#') + ((int)('4') << 8)))  :
+        case   (-(('%') + ((int)('i') << 8)))  :
+        case   (-((KS_EXTRA) + ((int)(KE_S_UP) << 8)))  :
+        case   (-((KS_EXTRA) + ((int)(KE_S_DOWN) << 8)))  :
+        case   (-(('*') + ((int)('7') << 8)))  :
+        case   (-(('#') + ((int)('2') << 8)))  :
             start_selection();
 
             stuffcharReadbuff(Ctrl_O);
@@ -37091,7 +35436,7 @@
             {
                 char_u      buf[4];
 
-                buf[0] = K_SPECIAL;
+                buf[0] =  (0x80) ;
                 buf[1] = KS_MODIFIER;
                 buf[2] = mod_mask;
                 buf[3] = NUL;
@@ -37116,7 +35461,7 @@
         State = replaceState | (State & MODE_LANGMAP);
     }
     may_trigger_modechanged();
-    AppendCharToRedobuff(K_INS);
+    AppendCharToRedobuff(  (-(('k') + ((int)('I') << 8)))  );
     showmode();
 }
 
@@ -37217,7 +35562,7 @@
     did_si = FALSE;
     can_si = FALSE;
     can_si_back = FALSE;
-    AppendCharToRedobuff(K_DEL);
+    AppendCharToRedobuff(  (-(('k') + ((int)('D') << 8)))  );
 }
 
     static void
@@ -37251,7 +35596,7 @@
     int         cpc[MAX_MCO];
     int         call_fix_indent = FALSE;
 
-    if (       BUFEMPTY() || (((curwin->w_cursor.lnum == 1 && curwin->w_cursor.col == 0) || (!can_bs(BS_START) && ((arrow_used) || (curwin->w_cursor.lnum == Insstart_orig.lnum && curwin->w_cursor.col <= Insstart_orig.col))) || (!can_bs(BS_INDENT) && !arrow_used && ai_col > 0 && curwin->w_cursor.col <= ai_col) || (!can_bs(BS_EOL) && curwin->w_cursor.col == 0))))
+    if (        (curbuf->b_ml.ml_line_count == 1 && *ml_get((linenr_T)1) == NUL)  || (((curwin->w_cursor.lnum == 1 && curwin->w_cursor.col == 0) || (!can_bs(BS_START) && ((arrow_used) || (curwin->w_cursor.lnum == Insstart_orig.lnum && curwin->w_cursor.col <= Insstart_orig.col))) || (!can_bs(BS_INDENT) && !arrow_used && ai_col > 0 && curwin->w_cursor.col <= ai_col) || (!can_bs(BS_EOL) && curwin->w_cursor.col == 0))))
     {
         vim_beep(BO_BS);
         return FALSE;
@@ -37322,11 +35667,11 @@
 
                         if (newp != NULL)
                         {
-                            mch_memmove(newp, ptr, len - 1);
+                             memmove((char *)(newp), (char *)(ptr), len - 1) ;
                             newp[len - 1] = NUL;
                             if (curbuf->b_ml.ml_line_len > len + 1)
                             {
-                                mch_memmove(newp + len, ptr + len + 1, curbuf->b_ml.ml_line_len - len - 1);
+                                 memmove((char *)(newp + len), (char *)(ptr + len + 1), curbuf->b_ml.ml_line_len - len - 1) ;
                             }
 
                             if (curbuf->b_ml.ml_flags & (ML_LINE_DIRTY | ML_ALLOCATED))
@@ -37403,7 +35748,7 @@
 
             while (ptr < cursor_ptr)
             {
-                int     cur_space = VIM_ISWHITE(*ptr);
+                int     cur_space =  ((*ptr) == ' ' || (*ptr) == '\t') ;
 
                 if (!prev_space && cur_space)
                 {
@@ -37411,7 +35756,7 @@
                     space_vcol = vcol;
                 }
                 vcol += chartabsize(ptr, vcol);
-                MB_PTR_ADV(ptr);
+                 ptr += (*mb_ptr2len)(ptr) ;
                 prev_space = cur_space;
             }
 
@@ -37434,7 +35779,7 @@
                     break;
                 }
                 space_vcol += size;
-                MB_PTR_ADV(space_ptr);
+                 space_ptr += (*mb_ptr2len)(space_ptr) ;
             }
             want_col = space_ptr - line;
 
@@ -37558,7 +35903,7 @@
     int         save_allow_keys = allow_keys;
     int         save_paste = p_paste;
 
-    if (end != NULL && STRLEN(end) >= NUMBUFLEN)
+    if (end != NULL &&  strlen((char *)(end))  >= NUMBUFLEN)
     {
         end = NULL;
     }
@@ -37579,7 +35924,7 @@
         {
             c = vgetc();
         }
-        while (c == K_IGNORE || c == K_VER_SCROLLBAR || c == K_HOR_SCROLLBAR)
+        while (c ==   (-((KS_EXTRA) + ((int)(KE_IGNORE) << 8)))   || c ==   (-((KS_VER_SCROLLBAR) + ((int)( ('X') ) << 8)))   || c ==   (-((KS_HOR_SCROLLBAR) + ((int)( ('X') ) << 8)))  )
             ;
 
         if (c == NUL || got_int || (ex_normal_busy > 0 && c == Ctrl_C))
@@ -37596,7 +35941,7 @@
             buf[idx++] = c;
         }
         buf[idx] = NUL;
-        if (end != NULL && STRNCMP(buf, end, idx) == 0)
+        if (end != NULL &&  strncmp((char *)(buf), (char *)(end), (idx))  == 0)
         {
             if (end[idx] == NUL)
             {
@@ -37615,7 +35960,7 @@
                 case PASTE_EX:
                     if (gap != NULL && ga_grow(gap, idx + 1) == OK)
                     {
-                        mch_memmove((char *)gap->ga_data + gap->ga_len, buf, (size_t)idx);
+                         memmove((char *)((char *)gap->ga_data + gap->ga_len), (char *)(buf), (size_t)idx) ;
                         gap->ga_len += idx;
                     }
                     break;
@@ -37624,7 +35969,7 @@
                     if (stop_arrow() == OK)
                     {
                         c = buf[0];
-                        if (idx == 1 && (c == CAR || c == K_KENTER || c == NL))
+                        if (idx == 1 && (c == CAR || c ==   (-(('K') + ((int)('A') << 8)))   || c == NL))
                         {
                             ins_eol(c);
                         }
@@ -37678,7 +36023,7 @@
             start_arrow_with_change(&tpos, end_change);
             if (!end_change)
             {
-                AppendCharToRedobuff(K_LEFT);
+                AppendCharToRedobuff(  (-(('k') + ((int)('l') << 8)))  );
             }
         }
     }
@@ -37704,7 +36049,7 @@
 
     undisplay_dollar();
     tpos = curwin->w_cursor;
-    if (c == K_C_HOME)
+    if (c ==   (-((KS_EXTRA) + ((int)(KE_C_HOME) << 8)))  )
     {
         curwin->w_cursor.lnum = 1;
     }
@@ -37721,7 +36066,7 @@
 
     undisplay_dollar();
     tpos = curwin->w_cursor;
-    if (c == K_C_END)
+    if (c ==   (-((KS_EXTRA) + ((int)(KE_C_END) << 8)))  )
     {
         curwin->w_cursor.lnum = curbuf->b_ml.ml_line_count;
     }
@@ -37741,7 +36086,7 @@
         start_arrow_with_change(&curwin->w_cursor, end_change);
         if (!end_change)
         {
-            AppendCharToRedobuff(K_S_LEFT);
+            AppendCharToRedobuff(  (-(('#') + ((int)('4') << 8)))  );
         }
         (void)bck_word(1L, FALSE, FALSE);
         curwin->w_set_curswant = true;
@@ -37764,7 +36109,7 @@
         start_arrow_with_change(&curwin->w_cursor, end_change);
         if (!end_change)
         {
-            AppendCharToRedobuff(K_RIGHT);
+            AppendCharToRedobuff(  (-(('k') + ((int)('r') << 8)))  );
         }
         curwin->w_set_curswant = true;
         if (virtual_active())
@@ -37808,7 +36153,7 @@
         start_arrow_with_change(&curwin->w_cursor, end_change);
         if (!end_change)
         {
-            AppendCharToRedobuff(K_S_RIGHT);
+            AppendCharToRedobuff(  (-(('%') + ((int)('i') << 8)))  );
         }
         (void)fwd_word(1L, FALSE, 0);
         curwin->w_set_curswant = true;
@@ -37865,7 +36210,7 @@
     }
 
     tpos = curwin->w_cursor;
-    if (pagescroll(BACKWARD, 1L, FALSE) == OK)
+    if (pagescroll( (-1) , 1L, FALSE) == OK)
     {
         start_arrow(&tpos);
         can_cindent = TRUE;
@@ -38011,7 +36356,7 @@
         colnr_T want_vcol;
         colnr_T vcol;
         int             change_col = -1;
-        int             save_list = curwin->w_p_list;
+        int             save_list = curwin-> w_onebuf_opt.wo_list ;
         char_u          *tab = (char_u *)"\t";
         chartabsize_T   cts;
 
@@ -38034,11 +36379,11 @@
 
         if (vim_strchr(p_cpo, CPO_LISTWM) == NULL)
         {
-            curwin->w_p_list = FALSE;
+            curwin-> w_onebuf_opt.wo_list  = FALSE;
         }
 
         fpos = curwin->w_cursor;
-        while (fpos.col > 0 && VIM_ISWHITE(ptr[-1]))
+        while (fpos.col > 0 &&  ((ptr[-1]) == ' ' || (ptr[-1]) == '\t') )
         {
             --fpos.col;
             --ptr;
@@ -38055,7 +36400,7 @@
 
         init_chartabsize_arg(&cts, curwin, 0, vcol, tab, tab);
 
-        while (VIM_ISWHITE(*ptr))
+        while ( ((*ptr) == ' ' || (*ptr) == '\t') )
         {
             i = lbr_chartabsize(&cts);
             if (cts.cts_vcol + i > want_vcol)
@@ -38106,7 +36451,7 @@
             i = cursor->col - fpos.col;
             if (i > 0)
             {
-                    STRMOVE(ptr, ptr + i);
+                      memmove((char *)((ptr)), (char *)((ptr + i)),  strlen((char *)(ptr + i))  + 1)  ;
                 if ((State & REPLACE_FLAG) && !(State & VREPLACE_FLAG))
                 {
                     for (temp = i; --temp >= 0; )
@@ -38129,7 +36474,7 @@
         {
             vim_free(saved_line);
         }
-        curwin->w_p_list = save_list;
+        curwin-> w_onebuf_opt.wo_list  = save_list;
     }
 
     return FALSE;
@@ -38160,7 +36505,7 @@
         coladvance(getviscol());
     }
 
-    AppendToRedobuff(NL_STR);
+    AppendToRedobuff( (char_u *)"\012" );
     i = open_line(FORWARD, has_format_option(FO_RET_COMS) ? OPENLINE_DO_COM : 0, old_indent, NULL);
     old_indent = 0;
     can_cindent = TRUE;
@@ -38234,9 +36579,9 @@
         {
             long        tw_save;
 
-            if (c < 256 && !SAFE_isalnum(c))
+            if (c < 256 && ! (isalnum ((unsigned char)(c))) )
             {
-                AppendToRedobuff((char_u *)CTRL_V_STR);
+                AppendToRedobuff((char_u *) "\026" );
             }
             tw_save = curbuf->b_p_tw;
             curbuf->b_p_tw = -1;
@@ -38256,7 +36601,7 @@
     {
         return 0;
     }
-    if (curwin->w_p_list && vim_strchr(p_cpo, CPO_LISTWM) == NULL)
+    if (curwin-> w_onebuf_opt.wo_list  && vim_strchr(p_cpo, CPO_LISTWM) == NULL)
     {
         return getvcol_nolist(&curwin->w_cursor);
     }
@@ -38279,12 +36624,12 @@
     int
 ins_apply_autocmds(event_T event)
 {
-    varnumber_T tick = CHANGEDTICK(curbuf);
+    varnumber_T tick =  ((curbuf)->b_ct_di.di_tv.vval.v_number) ;
     int r;
 
     r = apply_autocmds(event, NULL, NULL, FALSE, curbuf);
 
-    if (event != EVENT_INSERTLEAVE && tick != CHANGEDTICK(curbuf))
+    if (event != EVENT_INSERTLEAVE && tick !=  ((curbuf)->b_ct_di.di_tv.vval.v_number) )
     {
         u_save(curwin->w_cursor.lnum, (linenr_T)(curwin->w_cursor.lnum + 1));
     }
@@ -38325,7 +36670,7 @@
                 if (zero_width == regmatch.startp[0])
                 {
                     i = mb_ptr2len(tail);
-                    mch_memmove((char_u *)ga.ga_data + ga.ga_len, tail, (size_t)i);
+                     memmove((char *)((char_u *)ga.ga_data + ga.ga_len), (char *)(tail), (size_t)i) ;
                     ga.ga_len += i;
                     tail += i;
                     continue;
@@ -38346,7 +36691,7 @@
             }
 
             i = (int)(regmatch.startp[0] - tail);
-            mch_memmove((char_u *)ga.ga_data + ga.ga_len, tail, (size_t)i);
+             memmove((char *)((char_u *)ga.ga_data + ga.ga_len), (char *)(tail), (size_t)i) ;
             (void)vim_regsub(&regmatch, sub, expr, (char_u *)ga.ga_data + ga.ga_len + i, sublen, REGSUB_COPY | REGSUB_MAGIC);
             ga.ga_len += i + sublen - 1;
             tail = regmatch.endp[0];
@@ -38362,7 +36707,7 @@
 
         if (ga.ga_data != NULL)
         {
-            STRCPY((char *)ga.ga_data + ga.ga_len, tail);
+             strcpy((char *)((char *)ga.ga_data + ga.ga_len), (char *)(tail)) ;
             ga.ga_len += (int)(end - tail);
         }
 
@@ -38406,7 +36751,7 @@
 static void delbuf_msg(char_u *name);
 
     void
-do_ascii(exarg_T *eap UNUSED)
+do_ascii(exarg_T *eap  __attribute__((unused)) )
 {
     int         c;
     int         cval;
@@ -38463,7 +36808,7 @@
         {
             buf2[0] = NUL;
         }
-            vim_snprintf((char *)IObuff, IOSIZE, _("<%s>%s%s  %d,  Hex %02x,  Octal %03o"), transchar(c), buf1, buf2, cval, cval, cval);
+            vim_snprintf((char *)IObuff,  (1024+1) , _("<%s>%s%s  %d,  Hex %02x,  Octal %03o"), transchar(c), buf1, buf2, cval, cval, cval);
         if (enc_utf8)
         {
             c = cc[ci++];
@@ -38476,7 +36821,7 @@
 
     while (has_mbyte && (c >= 0x100 || (enc_utf8 && c >= 0x80)))
     {
-        len = (int)STRLEN(IObuff);
+        len = (int) strlen((char *)(IObuff)) ;
         if (len > 0)
         {
             IObuff[len++] = ' ';
@@ -38487,7 +36832,7 @@
             IObuff[len++] = ' ';
         }
         len += (*mb_char2bytes)(c, IObuff + len);
-            vim_snprintf((char *)IObuff + len, IOSIZE - len, c < 0x10000 ? _("> %d, Hex %04x, Octal %o") : _("> %d, Hex %08x, Octal %o"), c, c, c);
+            vim_snprintf((char *)IObuff + len,  (1024+1)  - len, c < 0x10000 ? _("> %d, Hex %04x, Octal %o") : _("> %d, Hex %08x, Octal %o"), c, c, c);
         if (ci == MAX_MCO)
         {
             break;
@@ -38618,7 +36963,7 @@
 
     first = skipwhite(line);
 
-    for (last = first + STRLEN(first); last > first && VIM_ISWHITE(last[-1]); --last)
+    for (last = first +  strlen((char *)(first)) ; last > first &&  ((last[-1]) == ' ' || (last[-1]) == '\t') ; --last)
     {
         ;
     }
@@ -38670,7 +37015,7 @@
     {
         return strcoll((char *)s1, (char *)s2);
     }
-    return sort_ic ? STRICMP(s1, s2) : STRCMP(s1, s2);
+    return sort_ic ?  strcasecmp((char *)(s1), (char *)(s2))  :  strcmp((char *)(s1), (char *)(s2)) ;
 }
 
     static int
@@ -38709,9 +37054,9 @@
     }
     else
     {
-        STRNCPY(sortbuf1, ml_get(l1.lnum) + l1.st_u.line.start_col_nr, l1.st_u.line.end_col_nr - l1.st_u.line.start_col_nr + 1);
+         strncpy((char *)(sortbuf1), (char *)(ml_get(l1.lnum) + l1.st_u.line.start_col_nr), (l1.st_u.line.end_col_nr - l1.st_u.line.start_col_nr + 1)) ;
         sortbuf1[l1.st_u.line.end_col_nr - l1.st_u.line.start_col_nr] = 0;
-        STRNCPY(sortbuf2, ml_get(l2.lnum) + l2.st_u.line.start_col_nr, l2.st_u.line.end_col_nr - l2.st_u.line.start_col_nr + 1);
+         strncpy((char *)(sortbuf2), (char *)(ml_get(l2.lnum) + l2.st_u.line.start_col_nr), (l2.st_u.line.end_col_nr - l2.st_u.line.start_col_nr + 1)) ;
         sortbuf2[l2.st_u.line.end_col_nr - l2.st_u.line.start_col_nr] = 0;
 
         result = string_compare(sortbuf1, sortbuf2);
@@ -38758,7 +37103,7 @@
     sortbuf1 = NULL;
     sortbuf2 = NULL;
     regmatch.regprog = NULL;
-    nrs = ALLOC_MULT(sorti_T, count);
+    nrs =  (sorti_T *)alloc(sizeof(sorti_T) * (count)) ;
     if (nrs == NULL)
     {
         goto sortend;
@@ -38769,7 +37114,7 @@
 
     for (p = eap->arg; *p != NUL; ++p)
     {
-        if (VIM_ISWHITE(*p))
+        if ( ((*p) == ' ' || (*p) == '\t') )
         {
             ;
         }
@@ -38823,7 +37168,7 @@
             eap->nextcmd = check_nextcmd(p);
             break;
         }
-        else if (!ASCII_ISALPHA(*p) && regmatch.regprog == NULL)
+        else if (! ( ((unsigned)(*p) - 'A' < 26)  ||  ((unsigned)(*p) - 'a' < 26) )  && regmatch.regprog == NULL)
         {
             s = skip_regexp_err(p + 1, *p, TRUE);
             if (s == NULL)
@@ -39001,7 +37346,7 @@
         s = ml_get(get_lnum);
         if (!unique || i == 0 || string_compare(s, sortbuf1) != 0)
         {
-            STRCPY(sortbuf1, s);
+             strcpy((char *)(sortbuf1), (char *)(s)) ;
             if (ml_append(lnum++, sortbuf1, (colnr_T)0, FALSE) == FAIL)
             {
                 break;
@@ -39029,12 +37374,12 @@
     deleted = (long)(count - (lnum - eap->line2));
     if (deleted > 0)
     {
-        mark_adjust(eap->line2 - deleted, eap->line2, (long)MAXLNUM, -deleted);
+        mark_adjust(eap->line2 - deleted, eap->line2, (long) LONG_MAX , -deleted);
         msgmore(-deleted);
     }
     else if (deleted < 0)
     {
-        mark_adjust(eap->line2, MAXLNUM, -deleted, 0L);
+        mark_adjust(eap->line2,  LONG_MAX , -deleted, 0L);
     }
 
     if (change_occurred || deleted != 0)
@@ -39091,7 +37436,7 @@
 
     for (p = eap->arg; *p != NUL; ++p)
     {
-        if (VIM_ISWHITE(*p))
+        if ( ((*p) == ' ' || (*p) == '\t') )
         {
             ;
         }
@@ -39123,7 +37468,7 @@
             eap->nextcmd = check_nextcmd(p);
             break;
         }
-        else if (!ASCII_ISALPHA(*p) && regmatch.regprog == NULL)
+        else if (! ( ((unsigned)(*p) - 'A' < 26)  ||  ((unsigned)(*p) - 'a' < 26) )  && regmatch.regprog == NULL)
         {
             s = skip_regexp_err(p + 1, *p, TRUE);
             if (s == NULL)
@@ -39233,7 +37578,7 @@
             }
             else
             {
-                STRCPY(sortbuf1, &s[start_col]);
+                 strcpy((char *)(sortbuf1), (char *)(&s[start_col])) ;
             }
         }
         else if (keep_only_not_unique)
@@ -39256,7 +37601,7 @@
                     delete_lnum = get_lnum;
                 }
                 match_continue = FALSE;
-                STRCPY(sortbuf1, &s[start_col]);
+                 strcpy((char *)(sortbuf1), (char *)(&s[start_col])) ;
             }
         }
         else
@@ -39280,7 +37625,7 @@
                     delete_lnum = get_lnum;
                 }
                 match_continue = FALSE;
-                STRCPY(sortbuf1, &s[start_col]);
+                 strcpy((char *)(sortbuf1), (char *)(&s[start_col])) ;
             }
         }
 
@@ -39305,7 +37650,7 @@
         }
     }
 
-    mark_adjust(eap->line2 - deleted, eap->line2, (long)MAXLNUM, -deleted);
+    mark_adjust(eap->line2 - deleted, eap->line2, (long) LONG_MAX , -deleted);
     msgmore(-deleted);
 
     if (change_occurred)
@@ -39541,10 +37886,10 @@
     trailarg = skipwhite(arg);
     do
     {
-        len = (int)STRLEN(trailarg) + 1;
+        len = (int) strlen((char *)(trailarg))  + 1;
         if (newcmd != NULL)
         {
-            len += (int)STRLEN(newcmd);
+            len += (int) strlen((char *)(newcmd)) ;
         }
         if (ins_prevcmd)
         {
@@ -39553,7 +37898,7 @@
                 vim_free(newcmd);
                 return;
             }
-            len += (int)STRLEN(prevcmd);
+            len += (int) strlen((char *)(prevcmd)) ;
         }
         if ((t = alloc(len)) == NULL)
         {
@@ -39563,14 +37908,14 @@
         *t = NUL;
         if (newcmd != NULL)
         {
-            STRCAT(t, newcmd);
+             strcat((char *)(t), (char *)(newcmd)) ;
         }
         if (ins_prevcmd)
         {
-            STRCAT(t, prevcmd);
+             strcat((char *)(t), (char *)(prevcmd)) ;
         }
-        p = t + STRLEN(t);
-        STRCAT(t, trailarg);
+        p = t +  strlen((char *)(t)) ;
+         strcat((char *)(t), (char *)(trailarg)) ;
         vim_free(newcmd);
         newcmd = t;
 
@@ -39581,7 +37926,7 @@
             {
                 if (p > newcmd && p[-1] == '\\')
                 {
-                    STRMOVE(p - 1, p);
+                      memmove((char *)((p - 1)), (char *)((p)),  strlen((char *)(p))  + 1)  ;
                 }
                 else
                 {
@@ -39595,7 +37940,7 @@
         }
     } while (trailarg != NULL);
 
-    if (STRLEN(newcmd) > 0)
+    if ( strlen((char *)(newcmd))  > 0)
     {
         vim_free(prevcmd);
         prevcmd = newcmd;
@@ -39632,14 +37977,14 @@
         {
             vim_free(newcmd);
         }
-        newcmd = alloc(STRLEN(prevcmd) + 2 * STRLEN(p_shq) + 1);
+        newcmd = alloc( strlen((char *)(prevcmd))  + 2 *  strlen((char *)(p_shq))  + 1);
         if (newcmd == NULL)
         {
             return;
         }
-        STRCPY(newcmd, p_shq);
-        STRCAT(newcmd, prevcmd);
-        STRCAT(newcmd, p_shq);
+         strcpy((char *)(newcmd), (char *)(p_shq)) ;
+         strcat((char *)(newcmd), (char *)(prevcmd)) ;
+         strcat((char *)(newcmd), (char *)(p_shq)) ;
         free_newcmd = TRUE;
     }
     if (addr_count == 0)
@@ -39755,7 +38100,7 @@
     windgoto((int)Rows - 1, cmdline_col_off);
     cursor_on();
 
-    if (!do_out || STRCMP(p_srr, ">") == 0 || !do_in)
+    if (!do_out ||  strcmp((char *)(p_srr), (char *)(">"))  == 0 || !do_in)
     {
         redraw_later_clear();
     }
@@ -39788,7 +38133,7 @@
     {
         if (otmp != NULL)
         {
-            if (readfile(otmp, NULL, line2, (linenr_T)0, (linenr_T)MAXLNUM, eap, READ_FILTER) != OK)
+            if (readfile(otmp, NULL, line2, (linenr_T)0, (linenr_T) LONG_MAX , eap, READ_FILTER) != OK)
             {
                 {
                     msg_putchar('\n');
@@ -39821,13 +38166,13 @@
                 }
                 else if (save_cmod_flags & CMOD_LOCKMARKS)
                 {
-                    mark_adjust(line2 + 1, (linenr_T)MAXLNUM, linecount - read_linecount, 0L);
+                    mark_adjust(line2 + 1, (linenr_T) LONG_MAX , linecount - read_linecount, 0L);
                     mark_adjust(line1, line2, linecount, 0L);
                 }
                 else
                 {
                     mark_adjust(line1, line1 + read_linecount - 1, linecount, 0L);
-                    mark_adjust(line1 + read_linecount, line2, MAXLNUM, 0L);
+                    mark_adjust(line1 + read_linecount, line2,  LONG_MAX , 0L);
                 }
             }
 
@@ -39895,11 +38240,11 @@
 
     if (itmp != NULL)
     {
-        mch_remove(itmp);
+         unlink((char *)(itmp)) ;
     }
     if (otmp != NULL)
     {
-        mch_remove(otmp);
+         unlink((char *)(otmp)) ;
     }
     vim_free(itmp);
     vim_free(otmp);
@@ -39930,12 +38275,14 @@
 
     if (p_warn && !autocmd_busy && msg_silent == 0)
     {
-        FOR_ALL_BUFFERS(buf)
+         for ((buf) = firstbuf; (buf) != NULL; (buf) = (buf)->b_next) 
+         {
             if (bufIsChangedNotTerm(buf))
             {
                 msg_puts(_("[No write since last change]\n"));
                 break;
             }
+         }
     }
 
     if (!swapping_screen())
@@ -39990,7 +38337,7 @@
 
     }
 
-    display_errors();
+     fflush(stderr) ;
 
     apply_autocmds(EVENT_SHELLCMDPOST, NULL, NULL, FALSE, curbuf);
 }
@@ -40009,22 +38356,22 @@
         return NULL;
     }
 
-    is_fish_shell = fnamecmp(shell_name, "fish") == 0;
+    is_fish_shell =  vim_fnamecmp((char_u *)(shell_name), (char_u *)("fish"))  == 0;
     if (is_fish_shell)
     {
-        len = (long_u)STRLEN(cmd) + 13;
+        len = (long_u) strlen((char *)(cmd))  + 13;
     }
     else
     {
         is_powershell = (shell_name[0] == 'p')
-                        && (fnamecmp(shell_name, "powershell") == 0 || fnamecmp(shell_name, "powershell.exe") == 0 || fnamecmp(shell_name, "pwsh") == 0 || fnamecmp(shell_name, "pwsh.exe") == 0);
+                        && ( vim_fnamecmp((char_u *)(shell_name), (char_u *)("powershell"))  == 0 ||  vim_fnamecmp((char_u *)(shell_name), (char_u *)("powershell.exe"))  == 0 ||  vim_fnamecmp((char_u *)(shell_name), (char_u *)("pwsh"))  == 0 ||  vim_fnamecmp((char_u *)(shell_name), (char_u *)("pwsh.exe"))  == 0);
         if (is_powershell)
         {
-            len = (long_u)STRLEN(cmd) + 7;
+            len = (long_u) strlen((char *)(cmd))  + 7;
         }
         else
         {
-            len = (long_u)STRLEN(cmd) + 3;
+            len = (long_u) strlen((char *)(cmd))  + 3;
         }
     }
 
@@ -40032,16 +38379,16 @@
     {
         if (is_powershell)
         {
-            len += (long_u)STRLEN(itmp) + 17;
+            len += (long_u) strlen((char *)(itmp))  + 17;
         }
         else
         {
-            len += (long_u)STRLEN(itmp) + 9;
+            len += (long_u) strlen((char *)(itmp))  + 9;
         }
     }
     if (otmp != NULL)
     {
-        len += (long_u)STRLEN(otmp) + (long_u)STRLEN(p_srr) + 2;
+        len += (long_u) strlen((char *)(otmp))  + (long_u) strlen((char *)(p_srr))  + 2;
     }
 
     vim_free(shell_name);
@@ -40078,12 +38425,12 @@
         }
         else
         {
-            STRCPY(buf, cmd);
+             strcpy((char *)(buf), (char *)(cmd)) ;
         }
         if (itmp != NULL)
         {
-            STRCAT(buf, " < ");
-            STRCAT(buf, itmp);
+             strcat((char *)(buf), (char *)(" < ")) ;
+             strcat((char *)(buf), (char *)(itmp)) ;
         }
     }
     if (otmp != NULL)
@@ -40100,7 +38447,7 @@
     char_u      *p;
     char_u      *end;
 
-    end = buf + STRLEN(buf);
+    end = buf +  strlen((char *)(buf)) ;
     for (p = opt; (p = vim_strchr(p, '%')) != NULL; ++p)
     {
         if (p[1] == 's')
@@ -40123,12 +38470,12 @@
 }
 
     void
-do_fixdel(exarg_T *eap UNUSED)
+do_fixdel(exarg_T *eap  __attribute__((unused)) )
 {
     char_u  *p;
 
     p = find_termcode((char_u *)"kb");
-    add_termcode((char_u *)"kD", p != NULL && *p == DEL ? (char_u *)CTRL_H_STR : DEL_STR, FALSE);
+    add_termcode((char_u *)"kD", p != NULL && *p == DEL ? (char_u *) "\010"  :  (char_u *)"\177" , FALSE);
 }
 
     void
@@ -40136,10 +38483,10 @@
 {
     char        numbuf[30];
 
-    if (curwin->w_p_nu || use_number)
+    if (curwin-> w_onebuf_opt.wo_nu  || use_number)
     {
-        vim_snprintf(numbuf, sizeof(numbuf), "%*ld ", number_width(curwin), (long)lnum);
-        msg_puts_attr(numbuf, HL_ATTR(HLF_N));
+        vim_snprintf(numbuf, sizeof(numbuf), "%*ld ",  7 , (long)lnum);
+        msg_puts_attr(numbuf,  highlight_attr[(int)(HLF_N)] );
     }
     msg_prt_line(ml_get(lnum), list);
 }
@@ -40206,7 +38553,6 @@
     vim_free(sfname);
     apply_autocmds(EVENT_BUFFILEPOST, NULL, NULL, FALSE, curbuf);
 
-    DO_AUTOCHDIR;
     return OK;
 }
 
@@ -40409,7 +38755,6 @@
 
         if (eap->cmdidx == CMD_saveas || name_was_missing)
         {
-            DO_AUTOCHDIR;
         }
     }
 
@@ -40450,17 +38795,17 @@
                 {
                     return FAIL;
                 }
-                STRCPY(dir, ".");
+                 strcpy((char *)(dir), (char *)(".")) ;
             }
             else
             {
-                dir = alloc(MAXPATHL);
+                dir = alloc( PATH_MAX );
                 if (dir == NULL)
                 {
                     return FAIL;
                 }
                 p = p_dir;
-                copy_option_part(&p, dir, MAXPATHL, ",");
+                copy_option_part(&p, dir,  PATH_MAX , ",");
             }
             swapname = makeswapname(fname, ffname, curbuf, dir);
             vim_free(dir);
@@ -40517,7 +38862,7 @@
         exiting = TRUE;
     }
 
-    FOR_ALL_BUFFERS(buf)
+     for ((buf) = firstbuf; (buf) != NULL; (buf) = (buf)->b_next) 
     {
         if (bufIsChanged(buf) && !bt_dontwrite(buf))
         {
@@ -40578,7 +38923,7 @@
 {
     stat_T      st;
 
-    if (!*forceit && (buf->b_p_ro || (mch_stat((char *)buf->b_ffname, &st) >= 0 && check_file_readonly(buf->b_ffname, 0777))))
+    if (!*forceit && (buf->b_p_ro || ( stat(((char *)buf->b_ffname), (&st))  >= 0 && check_file_readonly(buf->b_ffname, 0777))))
     {
         if (buf->b_p_ro)
         {
@@ -40661,7 +39006,7 @@
     }
     else if (do_ecmd(fnum, ffname, sfname, NULL, lnum, (buf_hide(curbuf) ? ECMD_HIDE : 0) + (forceit ? ECMD_FORCEIT : 0), curwin) == OK)
     {
-        retval = GETFILE_OPEN_OTHER;
+        retval =  (-1) ;
     }
     else
     {
@@ -40694,7 +39039,7 @@
     char_u      *command = NULL;
     int         readfile_flags = 0;
     int         did_inc_redrawing_disabled = FALSE;
-    long        *so_ptr = curwin->w_p_so >= 0 ? &curwin->w_p_so : &p_so;
+    long        *so_ptr = curwin-> w_onebuf_opt.wo_so  >= 0 ? &curwin-> w_onebuf_opt.wo_so  : &p_so;
 
     if (eap != NULL)
     {
@@ -40848,7 +39193,7 @@
             }
         }
 
-        if ((oldbuf && newlnum == ECMD_LASTL) || newlnum == ECMD_LAST)
+        if ((oldbuf && newlnum ==  (linenr_T)0 ) || newlnum ==  ((linenr_T)-1) )
         {
             pos = buflist_findfpos(buf);
             newlnum = pos->lnum;
@@ -40986,7 +39331,7 @@
     if (!other_file && !oldbuf)
     {
         set_last_cursor(curwin);
-        if (newlnum == ECMD_LAST || newlnum == ECMD_LASTL)
+        if (newlnum ==  ((linenr_T)-1)  || newlnum ==  (linenr_T)0 )
         {
             newlnum = curwin->w_cursor.lnum;
             solcol = curwin->w_cursor.col;
@@ -41049,8 +39394,6 @@
     {
         curwin_init();
 
-        DO_AUTOCHDIR;
-
         orig_pos = curwin->w_cursor;
         topline = curwin->w_topline;
         if (!oldbuf)
@@ -41082,7 +39425,7 @@
         }
         check_arg_idx(curwin);
 
-        if (!EQUAL_POS(curwin->w_cursor, orig_pos))
+        if (! (((curwin->w_cursor).lnum == (orig_pos).lnum) && ((curwin->w_cursor).col == (orig_pos).col) && ((curwin->w_cursor).coladd == (orig_pos).coladd)) )
         {
             char_u *text = ml_get_curline();
 
@@ -41278,7 +39621,7 @@
             p = vim_strchr(eap->nextcmd, NL);
             if (p == NULL)
             {
-                p = eap->nextcmd + STRLEN(eap->nextcmd);
+                p = eap->nextcmd +  strlen((char *)(eap->nextcmd)) ;
             }
             theline = vim_strnsave(eap->nextcmd, p - eap->nextcmd);
             if (*p != NUL)
@@ -41296,7 +39639,7 @@
             int save_State = State;
 
             State = MODE_CMDLINE;
-            theline = eap->ea_getline(NUL, eap->cookie, indent, TRUE);
+            theline = eap->ea_getline(NUL, eap->cookie, indent, GETLINE_CONCAT_CONT);
             State = save_State;
         }
         lines_left = Rows - 1;
@@ -41433,13 +39776,13 @@
     {
         bigness = Rows - 1;
     }
-    else if (!ONE_WINDOW)
+    else if (! (firstwin == lastwin) )
     {
         bigness = curwin->w_height - 3;
     }
     else
     {
-        bigness = curwin->w_p_scr * 2;
+        bigness = curwin-> w_onebuf_opt.wo_scr  * 2;
     }
     if (bigness < 1)
     {
@@ -41459,7 +39802,7 @@
 
     if (*x != 0)
     {
-        if (!VIM_ISDIGIT(*x))
+        if (! ((unsigned)(*x) - '0' < 10) )
         {
             emsg(_(e_non_numeric_argument_to_z));
             return;
@@ -41636,7 +39979,7 @@
         {
             ++p;
         }
-        MB_PTR_ADV(p);
+         p += (*mb_ptr2len)(p) ;
     }
     return p;
 }
@@ -41644,7 +39987,7 @@
     static int
 check_regexp_delim(int c)
 {
-    if (SAFE_isalpha(c))
+    if ( (isalpha ((unsigned char)(c))) )
     {
         emsg(_(e_regular_expressions_cant_be_delimited_by_letters));
         return FAIL;
@@ -41700,7 +40043,7 @@
         which_pat = RE_SUBST;
     }
 
-    if (eap->cmd[0] == 's' && *cmd != NUL && !VIM_ISWHITE(*cmd) && vim_strchr((char_u *)"0123456789cegriIp|\"", *cmd) == NULL)
+    if (eap->cmd[0] == 's' && *cmd != NUL && ! ((*cmd) == ' ' || (*cmd) == '\t')  && vim_strchr((char_u *)"0123456789cegriIp|\"", *cmd) == NULL)
     {
         if (check_regexp_delim(*cmd) == FAIL)
         {
@@ -41724,7 +40067,8 @@
             {
                 which_pat = RE_SEARCH;
             }
-            STR_LITERAL_SET(pat, "");
+             (pat).string = (char_u *)("");
+             (pat).length =  (sizeof("" "") - 1) ;
             delimiter = *cmd++;
         }
         else
@@ -41750,7 +40094,7 @@
 
         if (!eap->skip)
         {
-            if (STRCMP(sub, "%") == 0 && vim_strchr(p_cpo, CPO_SUBPERCENT) != NULL)
+            if ( strcmp((char *)(sub), (char *)("%"))  == 0 && vim_strchr(p_cpo, CPO_SUBPERCENT) != NULL)
             {
                 if (old_sub == NULL)
                 {
@@ -41795,7 +40139,7 @@
         endcolumn = (curwin->w_curswant == MAXCOL);
     }
 
-    if (pat.string != NULL && STRCMP(pat.string, "\\n") == 0 && *sub == NUL && (*cmd == NUL || (cmd[1] == NUL && (*cmd == 'g' || *cmd == 'l' || *cmd == 'p' || *cmd == '#'))))
+    if (pat.string != NULL &&  strcmp((char *)(pat.string), (char *)("\\n"))  == 0 && *sub == NUL && (*cmd == NUL || (cmd[1] == NUL && (*cmd == 'g' || *cmd == 'l' || *cmd == 'p' || *cmd == '#'))))
     {
         linenr_T    joined_lines_count;
 
@@ -41926,7 +40270,7 @@
     save_do_ask = subflags.do_ask;
 
     cmd = skipwhite(cmd);
-    if (VIM_ISDIGIT(*cmd))
+    if ( ((unsigned)(*cmd) - '0' < 10) )
     {
         i = getdigits(&cmd);
         if (i <= 0 && !eap->skip && subflags.do_error)
@@ -42062,7 +40406,9 @@
                     lnum += regmatch.startpos[0].lnum;
                     sub_firstlnum += regmatch.startpos[0].lnum;
                     nmatch -= regmatch.startpos[0].lnum;
-                    VIM_CLEAR_STRING(sub_firstline);
+                     vim_free(sub_firstline.string);
+                     (sub_firstline.string) = NULL;
+                     sub_firstline.length = 0;
                 }
 
                 if (lnum > curbuf->b_ml.ml_line_count)
@@ -42129,7 +40475,7 @@
                     State = MODE_CONFIRM;
                     setmouse();
                     curwin->w_cursor.col = regmatch.startpos[0].col;
-                    if (curwin->w_p_crb)
+                    if (curwin-> w_onebuf_opt.wo_crb )
                     {
                         do_check_cursorbind();
                     }
@@ -42157,9 +40503,9 @@
                             }
                             getvcol(curwin, &curwin->w_cursor, NULL, NULL, &ec, 0);
                             curwin->w_cursor.col = regmatch.startpos[0].col;
-                            if (subflags.do_number || curwin->w_p_nu)
+                            if (subflags.do_number || curwin-> w_onebuf_opt.wo_nu )
                             {
-                                int numw = number_width(curwin) + 1;
+                                int numw =  7  + 1;
                                 sc += numw;
                                 ec += numw;
                             }
@@ -42173,7 +40519,7 @@
                                 msg_putchar('^');
                             }
 
-                            resp = getexmodeline('?', NULL, 0, TRUE);
+                            resp = getexmodeline('?', NULL, 0, GETLINE_CONCAT_CONT);
                             if (resp != NULL)
                             {
                                 typed = *resp;
@@ -42205,7 +40551,9 @@
                                     new_line.string = concat_str(new_start.string, sub_firstline.string + copycol);
                                     if (new_line.string == NULL)
                                     {
-                                        VIM_CLEAR_STRING(orig_line);
+                                         vim_free(orig_line.string);
+                                         (orig_line.string) = NULL;
+                                         orig_line.length = 0;
                                     }
                                     else
                                     {
@@ -42243,7 +40591,7 @@
                             i = msg_scroll;
                             msg_scroll = 0;
                             msg_no_more = TRUE;
-                            smsg_attr(HL_ATTR(HLF_R), _("replace with %s (y/n/a/q/l/^E/^Y)?"), sub);
+                            smsg_attr( highlight_attr[(int)(HLF_R)] , _("replace with %s (y/n/a/q/l/^E/^Y)?"), sub);
                             msg_no_more = FALSE;
                             msg_scroll = i;
                             showruler(TRUE);
@@ -42375,13 +40723,13 @@
                             vim_free(new_start.string);
                             goto outofmem;
                         }
-                        mch_memmove(p1, new_start.string, new_start.length + 1);
+                         memmove((char *)(p1), (char *)(new_start.string), new_start.length + 1) ;
                         vim_free(new_start.string);
                         new_start.string = p1;
                     }
                 }
 
-                mch_memmove(new_start.string + new_start.length, sub_firstline.string + copycol, copy_len);
+                 memmove((char *)(new_start.string + new_start.length), (char *)(sub_firstline.string + copycol), copy_len) ;
                 new_start.length += copy_len;
 
                 new_end = new_start.string + new_start.length;
@@ -42445,7 +40793,7 @@
                     if (p1[0] == '\\' && p1[1] != NUL)
                     {
                         n = (size_t)(new_start.length - (p1 - new_start.string));
-                        mch_memmove(p1, p1 + 1, n + 1);
+                         memmove((char *)(p1), (char *)(p1 + 1), n + 1) ;
                         --new_start.length;
                     }
                     else if (*p1 == CAR)
@@ -42456,7 +40804,7 @@
 
                             *p1 = NUL;
                             ml_append(lnum - 1, new_start.string, plen, FALSE);
-                            mark_adjust(lnum + 1, (linenr_T)MAXLNUM, 1L, 0L);
+                            mark_adjust(lnum + 1, (linenr_T) LONG_MAX , 1L, 0L);
                             if (subflags.do_ask)
                             {
                                 appended_lines(lnum - 1, 1L);
@@ -42475,7 +40823,7 @@
                             did_split = true;
                             ++curwin->w_cursor.lnum;
                             n = new_start.length - (size_t)plen;
-                            mch_memmove(new_start.string, p1 + 1, n + 1);
+                             memmove((char *)(new_start.string), (char *)(p1 + 1), n + 1) ;
                             new_start.length = n;
                             p1 = new_start.string - 1;
                         }
@@ -42494,7 +40842,7 @@
                 {
                     if (new_start.string != NULL)
                     {
-                        STRCPY(new_start.string + new_start.length, sub_firstline.string + copycol);
+                         strcpy((char *)(new_start.string + new_start.length), (char *)(sub_firstline.string + copycol)) ;
                         new_start.length += sub_firstline.length - (size_t)copycol;
                         matchcol = (colnr_T)(sub_firstline.length - matchcol);
                         prev_matchcol = (colnr_T)(sub_firstline.length - prev_matchcol);
@@ -42517,11 +40865,11 @@
                             }
                             if (copycol > 0)
                             {
-                                mark_adjust(lnum, lnum + nmatch_tl - 1, (long)MAXLNUM, -nmatch_tl);
+                                mark_adjust(lnum, lnum + nmatch_tl - 1, (long) LONG_MAX , -nmatch_tl);
                             }
                             else
                             {
-                                mark_adjust(lnum - 1, lnum - 1, (long)MAXLNUM, -nmatch_tl);
+                                mark_adjust(lnum - 1, lnum - 1, (long) LONG_MAX , -nmatch_tl);
                             }
                             if (subflags.do_ask)
                             {
@@ -42579,7 +40927,9 @@
                 ++sub_nlines;
             }
             vim_free(new_start.string);
-            VIM_CLEAR_STRING(sub_firstline);
+             vim_free(sub_firstline.string);
+             (sub_firstline.string) = NULL;
+             sub_firstline.length = 0;
         }
 
         line_breakcheck();
@@ -42668,7 +41018,7 @@
 
         if (got_int)
         {
-            STRCPY(msg_buf, _("(Interrupted) "));
+             strcpy((char *)(msg_buf), (char *)(_("(Interrupted) "))) ;
         }
         else
         {
@@ -42785,7 +41135,7 @@
         {
             *cmd++ = NUL;
         }
-        patlen = STRLEN(pat);
+        patlen =  strlen((char *)(pat)) ;
     }
 
     if (search_regcomp(pat, patlen, &used_pat, RE_BOTH, which_pat, SEARCH_HIS, &regmatch) == FAIL)
@@ -42906,7 +41256,7 @@
 }
 
     void
-ex_smile(exarg_T *eap UNUSED)
+ex_smile(exarg_T *eap  __attribute__((unused)) )
 {
     static char *code[] = {
         "\34 \4o\14$\4ox\30 \2o\30$\1ox\25 \2o\36$\1o\11 \1o\1$\3 \2$\1 \1o\1$x\5 \1o\1 \1$\1 \2o\10 \1o\44$\1o\7 \2$\1 \2$\1 \2$\1o\1$x\2 \2o\1 \1$\1 \1$\1 \1\"\1$\6 \1o\11$\4 \15$\4 \11$\1o\7 \3$\1o\2$\1o\1$x\2 \1\"\6$\1o\1$\5 \1o\11$\6 \13$\6 \12$\1o\4 \10$x\4 \7$\4 \13$\6 \13$\6 \27$x\4 \27$\4 \15$\4 \16$\2 \3\"\3$x\5 \1\"\3$\4\"\61$\5 \1\"\3$x\6 \3$\3 \1o\62$\5 \1\"\3$\1ox\5 \1o\2$\1\"\3 \63$\7 \3$\1ox\5 \3$\4 \55$\1\"\1 \1\"\6$",
@@ -42932,7 +41282,7 @@
                 {
                     if (*p == 'o' || *p == '$')
                     {
-                        msg_putchar_attr(*p, HL_ATTR(HLF_L));
+                        msg_putchar_attr(*p,  highlight_attr[(int)(HLF_L)] );
                     }
                     else
                     {
@@ -42960,7 +41310,7 @@
 
     set_arglist(eap->arg);
 
-    if (ARGCOUNT == 0)
+    if ( ( (curwin)->w_alist ->al_ga.ga_len)  == 0)
     {
         return;
     }
@@ -42973,9 +41323,11 @@
         return;
     }
 
-    buf = buflist_findnr(ARGLIST[0].ae_fnum);
+    buf = buflist_findnr( ((aentry_T *) (curwin)->w_alist ->al_ga.ga_data) [0].ae_fnum);
 
-    FOR_ALL_TAB_WINDOWS(tp, wp)
+     for ((tp) = first_tabpage; (tp) != NULL; (tp) = (tp)->tp_next)
+     {
+         for ((wp) = ((tp) == curtab)            ? firstwin : (tp)->tp_firstwin; (wp); (wp) = (wp)->w_next)
     {
         if (wp->w_buffer == buf)
         {
@@ -42996,6 +41348,7 @@
             return;
         }
     }
+     }
 
     if (!buf_hide(curbuf))
     {
@@ -43122,7 +41475,8 @@
     {
         return;
     }
-    FOR_ALL_BUFFERS(buf)
+     for ((buf) = firstbuf; (buf) != NULL; (buf) = (buf)->b_next) 
+     {
         if (bufIsChanged(buf) && !buf->b_p_ro && !bt_dontwrite(buf))
         {
             bufref_T    bufref;
@@ -43136,6 +41490,7 @@
                 buf = firstbuf;
             }
         }
+     }
 }
 
     int
@@ -43196,15 +41551,17 @@
     tabpage_T   *tp;
     win_T       *wp;
 
-    FOR_ALL_BUFFERS(buf)
+     for ((buf) = firstbuf; (buf) != NULL; (buf) = (buf)->b_next) 
+     {
         ++bufcount;
+     }
 
     if (bufcount == 0)
     {
         return FALSE;
     }
 
-    bufnrs = ALLOC_MULT(int, bufcount);
+    bufnrs =  (int *)alloc(sizeof(int) * (bufcount)) ;
     if (bufnrs == NULL)
     {
         return FALSE;
@@ -43212,21 +41569,29 @@
 
     bufnrs[bufnum++] = curbuf->b_fnum;
 
-    FOR_ALL_WINDOWS(wp)
+     for ((wp) = firstwin; (wp) != NULL; (wp) = (wp)->w_next) 
+     {
         if (wp->w_buffer != curbuf)
         {
             add_bufnum(bufnrs, &bufnum, wp->w_buffer->b_fnum);
         }
+     }
 
-    FOR_ALL_TABPAGES(tp)
+     for ((tp) = first_tabpage; (tp) != NULL; (tp) = (tp)->tp_next) 
+     {
         if (tp != curtab)
         {
-            FOR_ALL_WINDOWS_IN_TAB(tp, wp)
+             for ((wp) = ((tp) == NULL || (tp) == curtab)             ? firstwin : (tp)->tp_firstwin; (wp); (wp) = (wp)->w_next) 
+             {
                 add_bufnum(bufnrs, &bufnum, wp->w_buffer->b_fnum);
+             }
         }
+     }
 
-    FOR_ALL_BUFFERS(buf)
+     for ((buf) = firstbuf; (buf) != NULL; (buf) = (buf)->b_next) 
+     {
         add_bufnum(bufnrs, &bufnum, buf->b_fnum);
+     }
 
     for (i = 0; i < bufnum; ++i)
     {
@@ -43272,7 +41637,10 @@
 
     if (buf != curbuf)
     {
-        FOR_ALL_TAB_WINDOWS(tp, wp)
+         for ((tp) = first_tabpage; (tp) != NULL; (tp) = (tp)->tp_next)
+         {
+             for ((wp) = ((tp) == curtab)            ? firstwin : (tp)->tp_firstwin; (wp); (wp) = (wp)->w_next)
+             {
             if (wp->w_buffer == buf)
             {
                 bufref_T bufref;
@@ -43287,6 +41655,8 @@
                 }
                 goto buf_found;
             }
+             }
+         }
     }
 buf_found:
 
@@ -43320,7 +41690,7 @@
     retval = (buf_write(buf, buf->b_ffname, buf->b_fname, (linenr_T)1, buf->b_ml.ml_line_count, NULL, FALSE, forceit, TRUE, FALSE));
     if (curbuf != old_curbuf)
     {
-        msg_source(HL_ATTR(HLF_W));
+        msg_source( highlight_attr[(int)(HLF_W)] );
         msg(_("Warning: Entered other buffer unexpectedly (check autocommands)"));
     }
     return retval;
@@ -43335,7 +41705,7 @@
     buf_T       *buf = curbuf;
     int         next_fnum = 0;
 
-    if (curwin->w_p_wfb && eap->cmdidx != CMD_windo && eap->cmdidx != CMD_tabdo)
+    if (curwin-> w_onebuf_opt.wo_wfb  && eap->cmdidx != CMD_windo && eap->cmdidx != CMD_tabdo)
     {
         if ((eap->cmdidx == CMD_ldo || eap->cmdidx == CMD_lfdo) && !eap->forceit)
         {
@@ -43343,15 +41713,15 @@
             return;
         }
 
-        if (win_valid(prevwin) && !prevwin->w_p_wfb)
+        if (win_valid(prevwin) && !prevwin-> w_onebuf_opt.wo_wfb )
         {
             win_goto(prevwin);
         }
-        if (curwin->w_p_wfb)
+        if (curwin-> w_onebuf_opt.wo_wfb )
         {
             (void)win_split(0, 0);
 
-            if (curwin->w_p_wfb)
+            if (curwin-> w_onebuf_opt.wo_wfb )
             {
                 emsg(_(e_winfixbuf_cannot_go_to_buffer));
                 return;
@@ -43415,7 +41785,7 @@
         {
             if (eap->cmdidx == CMD_argdo)
             {
-                if (i == ARGCOUNT)
+                if (i ==  ( (curwin)->w_alist ->al_ga.ga_len) )
                 {
                     break;
                 }
@@ -43475,11 +41845,13 @@
                 {
                     break;
                 }
-                FOR_ALL_BUFFERS(buf)
+                 for ((buf) = firstbuf; (buf) != NULL; (buf) = (buf)->b_next) 
+                 {
                     if (buf->b_fnum == next_fnum)
                     {
                         break;
                     }
+                 }
                 if (buf == NULL)
                 {
                     break;
@@ -43499,7 +41871,7 @@
             {
                 validate_cursor();
 
-                if (curwin->w_p_scb)
+                if (curwin-> w_onebuf_opt.wo_scb )
                 {
                     do_check_scrollbind(TRUE);
                 }
@@ -43548,15 +41920,11 @@
 
 static int      quitmore = 0;
 static int      ex_pressedreturn = FALSE;
-# define ex_hardcopy    ex_ni
 
 static char_u   *do_one_cmd(char_u **, int, char_u *(*fgetline)(int, void *, int, getline_opt_T), void *cookie);
 static int      if_level = 0;
 static void     append_command(char_u *cmd);
 
-# define ex_emenu               ex_ni
-# define ex_menu                ex_ni
-# define ex_menutranslate       ex_ni
 static void     ex_autocmd(exarg_T *eap);
 static void     ex_doautocmd(exarg_T *eap);
 static void     ex_bunload(exarg_T *eap);
@@ -43569,27 +41937,10 @@
 static void     ex_blast(exarg_T *eap);
 static char_u   *getargcmd(char_u **);
 static int      getargopt(exarg_T *eap);
-# define ex_make                ex_ni
-# define ex_cbuffer             ex_ni
-# define ex_cc                  ex_ni
-# define ex_cnext               ex_ni
-# define ex_cbelow              ex_ni
-# define ex_cfile               ex_ni
-# define qf_list                ex_ni
-# define qf_age                 ex_ni
-# define qf_history             ex_ni
-# define ex_helpgrep            ex_ni
-# define ex_vimgrep             ex_ni
-# define ex_cclose              ex_ni
-# define ex_copen               ex_ni
-# define ex_cwindow             ex_ni
-# define ex_cbottom             ex_ni
-# define ex_cexpr               ex_ni
 
 static linenr_T default_address(exarg_T *eap);
 static void address_default_all(exarg_T *eap);
 static void     get_flags(exarg_T *eap);
-# define HAVE_EX_SCRIPT_NI
 static void     ex_script_ni(exarg_T *eap);
 static char     *invalid_range(exarg_T *eap);
 static void     correct_range(exarg_T *eap);
@@ -43608,14 +41959,9 @@
 static void     ex_tabnext(exarg_T *eap);
 static void     ex_tabmove(exarg_T *eap);
 static void     ex_tabs(exarg_T *eap);
-# define ex_pclose              ex_ni
-# define ex_ptag                ex_ni
-# define ex_pedit               ex_ni
-# define ex_pbuffer             ex_ni
 static void     ex_hide(exarg_T *eap);
 static void     ex_exit(exarg_T *eap);
 static void     ex_print(exarg_T *eap);
-# define ex_goto                ex_ni
 static void     ex_shell(exarg_T *eap);
 static void     ex_preserve(exarg_T *eap);
 static void     ex_recover(exarg_T *eap);
@@ -43624,50 +41970,7 @@
 static void     ex_find(exarg_T *eap);
 static void     ex_open(exarg_T *eap);
 static void     ex_edit(exarg_T *eap);
-# define ex_gui                 ex_nogui
 static void     ex_nogui(exarg_T *eap);
-# define ex_tearoff             ex_ni
-# define ex_popup               ex_ni
-# define ex_simalt              ex_ni
-# define gui_mch_find_dialog    ex_ni
-# define gui_mch_replace_dialog ex_ni
-# define ex_helpfind            ex_ni
-# define ex_cscope              ex_ni
-# define ex_scscope             ex_ni
-# define ex_cstag               ex_ni
-# define ex_syntax              ex_ni
-# define ex_ownsyntax           ex_ni
-# define ex_syntime             ex_ni
-# define ex_spell               ex_ni
-# define ex_mkspell             ex_ni
-# define ex_spelldump           ex_ni
-# define ex_spellinfo           ex_ni
-# define ex_spellrepall         ex_ni
-# define ex_rundo               ex_ni
-# define ex_wundo               ex_ni
-# define ex_lua                 ex_script_ni
-# define ex_luado               ex_ni
-# define ex_luafile             ex_ni
-# define ex_mzscheme            ex_script_ni
-# define ex_mzfile              ex_ni
-# define ex_perl                ex_script_ni
-# define ex_perldo              ex_ni
-# define ex_python              ex_script_ni
-# define ex_pydo                ex_ni
-# define ex_pyfile              ex_ni
-# define ex_py3                 ex_script_ni
-# define ex_py3do               ex_ni
-# define ex_py3file             ex_ni
-# define ex_pyx                 ex_script_ni
-# define ex_pyxdo               ex_ni
-# define ex_pyxfile             ex_ni
-# define ex_tcl                 ex_script_ni
-# define ex_tcldo               ex_ni
-# define ex_tclfile             ex_ni
-# define ex_ruby                ex_script_ni
-# define ex_rubydo              ex_ni
-# define ex_rubyfile            ex_ni
-# define ex_loadkeymap          ex_ni
 static void     ex_swapname(exarg_T *eap);
 static void     ex_syncbind(exarg_T *eap);
 static void     ex_read(exarg_T *eap);
@@ -43696,127 +41999,16 @@
 static void     ex_mark(exarg_T *eap);
 static void     ex_startinsert(exarg_T *eap);
 static void     ex_stopinsert(exarg_T *eap);
-# define ex_findpat             ex_ni
-# define ex_checkpath           ex_ni
-# define ex_psearch             ex_ni
 static void     ex_tag(exarg_T *eap);
 static void     ex_tag_cmd(exarg_T *eap, char_u *name);
-# define ex_block               ex_ni
-# define ex_break               ex_ni
-# define ex_breakadd            ex_ni
-# define ex_breakdel            ex_ni
-# define ex_breaklist           ex_ni
-# define ex_call                ex_ni
-# define ex_catch               ex_ni
-# define ex_class               ex_ni
-# define ex_compiler            ex_ni
-# define ex_continue            ex_ni
-# define ex_debug               ex_ni
-# define ex_debuggreedy         ex_ni
-# define ex_defcompile          ex_ni
-# define ex_delfunction         ex_ni
-# define ex_disassemble         ex_ni
-# define ex_echo                ex_ni
-# define ex_echohl              ex_ni
-# define ex_else                ex_ni
-# define ex_endblock            ex_ni
-# define ex_endfunction         ex_ni
-# define ex_endif               ex_ni
-# define ex_endtry              ex_ni
-# define ex_endwhile            ex_ni
-# define ex_eval                ex_ni
-# define ex_execute             ex_ni
-# define ex_finally             ex_ni
-# define ex_incdec              ex_ni
-# define ex_finish              ex_ni
-# define ex_function            ex_ni
-# define ex_if                  ex_ni
-# define ex_let                 ex_ni
-# define ex_var                 ex_ni
-# define ex_lockvar             ex_ni
-# define ex_oldfiles            ex_ni
-# define ex_options             ex_ni
-# define ex_packadd             ex_ni
-# define ex_packloadall         ex_ni
-# define ex_return              ex_ni
-# define ex_scriptnames         ex_ni
-# define ex_throw               ex_ni
-# define ex_try                 ex_ni
-# define ex_type                ex_ni
-# define ex_unlet               ex_ni
-# define ex_while               ex_ni
-# define ex_import              ex_ni
-# define ex_export              ex_ni
-# define ex_loadview            ex_ni
-# define ex_viminfo             ex_ni
 static void     ex_behave(exarg_T *eap);
 static void     ex_filetype(exarg_T *eap);
 static void     ex_setfiletype(exarg_T *eap);
-# define ex_diffoff             ex_ni
-# define ex_diffpatch           ex_ni
-# define ex_diffgetput          ex_ni
-# define ex_diffsplit           ex_ni
-# define ex_diffthis            ex_ni
-# define ex_diffupdate          ex_ni
 static void     ex_digraphs(exarg_T *eap);
 static void     ex_nohlsearch(exarg_T *eap);
-# define ex_X                   ex_ni
-# define ex_fold                ex_ni
-# define ex_foldopen            ex_ni
-# define ex_folddo              ex_ni
-# define ex_sign                ex_ni
-# define ex_nbclose             ex_ni
-# define ex_nbkey               ex_ni
-# define ex_nbstart             ex_ni
-
-# define ex_profile             ex_ni
-# define ex_terminal            ex_ni
-# define ex_xrestore            ex_ni
-# define ex_wlrestore           ex_ni
-# define ex_clipreset           ex_ni
-# define ex_popupclear          ex_ni
 
-#define DO_DECLARE_EXCMD
 // ---------------- begin ex_cmds.h ----------------
 
-#define EX_RANGE        0x001
-#define EX_BANG         0x002
-#define EX_EXTRA        0x004
-#define EX_XFILE        0x008
-#define EX_NOSPC        0x010
-#define EX_DFLALL       0x020
-#define EX_WHOLEFOLD    0x040
-#define EX_NEEDARG      0x080
-#define EX_TRLBAR       0x100
-#define EX_REGSTR       0x200
-#define EX_COUNT        0x400
-#define EX_NOTRLCOM     0x800
-#define EX_ZEROR       0x1000
-#define EX_CTRLV       0x2000
-#define EX_CMDARG      0x4000
-#define EX_BUFNAME     0x8000
-#define EX_BUFUNL     0x10000
-#define EX_ARGOPT     0x20000
-#define EX_SBOXOK     0x40000
-#define EX_CMDWIN     0x80000
-#define EX_MODIFY    0x100000
-#define EX_FLAGS     0x200000
-#define EX_RESTRICT  0x400000
-#define EX_EXPAND    0x800000
-#define EX_LOCK_OK  0x1000000
-#define EX_NONWHITE_OK 0x2000000
-#define EX_KEEPSCRIPT  0x4000000
-#define EX_EXPR_ARG    0x8000000
-#define EX_WHOLE      0x10000000
-#define EX_EXPORT     0x20000000
-#define EX_ARGSPACE   0x40000000
-
-#define EX_FILES (EX_XFILE | EX_EXTRA)
-#define EX_FILE1 (EX_FILES | EX_NOSPC)
-#define EX_WORD1 (EX_EXTRA | EX_NOSPC)
-
-# define EXCMD(a, b, c, d, e)   {(char_u *)b, STRLEN_LITERAL(b), c, (long_u)(d), e}
-
 typedef void (*ex_func_T) (exarg_T *eap);
 
 static struct cmdname
@@ -43828,614 +42020,619 @@
     cmd_addr_T  cmd_addr_type;
 } cmdnames[] =
 {
-EXCMD(CMD_append,       "append",       ex_append, EX_BANG|EX_RANGE|EX_ZEROR|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK|EX_MODIFY, ADDR_LINES),
-EXCMD(CMD_abbreviate,   "abbreviate",   ex_abbreviate, EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_abclear,      "abclear",      ex_abclear, EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_aboveleft,    "aboveleft",    ex_wrongmodifier, EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM, ADDR_NONE),
-EXCMD(CMD_abstract,     "abstract",     ex_class, EX_EXTRA|EX_CMDWIN|EX_LOCK_OK|EX_WHOLE|EX_EXPORT, ADDR_NONE),
-EXCMD(CMD_all,          "all",          ex_all, EX_BANG|EX_RANGE|EX_COUNT|EX_TRLBAR, ADDR_OTHER),
-EXCMD(CMD_amenu,        "amenu",        ex_menu, EX_RANGE|EX_ZEROR|EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_OTHER),
-EXCMD(CMD_anoremenu,    "anoremenu",    ex_menu, EX_RANGE|EX_ZEROR|EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_OTHER),
-EXCMD(CMD_args,         "args",         ex_args, EX_BANG|EX_FILES|EX_CMDARG|EX_ARGOPT|EX_TRLBAR, ADDR_NONE),
-EXCMD(CMD_argadd,       "argadd",       ex_argadd, EX_BANG|EX_RANGE|EX_ZEROR|EX_FILES|EX_TRLBAR, ADDR_ARGUMENTS),
-EXCMD(CMD_argdelete,    "argdelete",    ex_argdelete, EX_BANG|EX_RANGE|EX_FILES|EX_TRLBAR, ADDR_ARGUMENTS),
-EXCMD(CMD_argdo,        "argdo",        ex_listdo, EX_BANG|EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM|EX_RANGE|EX_DFLALL|EX_EXPAND, ADDR_ARGUMENTS),
-EXCMD(CMD_argdedupe,    "argdedupe",    ex_argdedupe, EX_TRLBAR, ADDR_NONE),
-EXCMD(CMD_argedit,      "argedit",      ex_argedit, EX_BANG|EX_NEEDARG|EX_RANGE|EX_ZEROR|EX_FILES|EX_CMDARG|EX_ARGOPT|EX_TRLBAR, ADDR_ARGUMENTS),
-EXCMD(CMD_argglobal,    "argglobal",    ex_args, EX_BANG|EX_FILES|EX_CMDARG|EX_ARGOPT|EX_TRLBAR, ADDR_NONE),
-EXCMD(CMD_arglocal,     "arglocal",     ex_args, EX_BANG|EX_FILES|EX_CMDARG|EX_ARGOPT|EX_TRLBAR, ADDR_NONE),
-EXCMD(CMD_argument,     "argument",     ex_argument, EX_BANG|EX_RANGE|EX_COUNT|EX_EXTRA|EX_CMDARG|EX_ARGOPT|EX_TRLBAR, ADDR_ARGUMENTS),
-EXCMD(CMD_ascii,        "ascii",        do_ascii, EX_TRLBAR|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_autocmd,      "autocmd",      ex_autocmd, EX_BANG|EX_EXTRA|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_augroup,      "augroup",      ex_autocmd, EX_BANG|EX_WORD1|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_aunmenu,      "aunmenu",      ex_menu, EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_buffer,       "buffer",       ex_buffer, EX_BANG|EX_RANGE|EX_BUFNAME|EX_BUFUNL|EX_COUNT|EX_EXTRA|EX_CMDARG|EX_TRLBAR, ADDR_BUFFERS),
-EXCMD(CMD_bNext,        "bNext",        ex_bprevious, EX_BANG|EX_RANGE|EX_COUNT|EX_CMDARG|EX_TRLBAR, ADDR_OTHER),
-EXCMD(CMD_ball,         "ball",         ex_buffer_all, EX_RANGE|EX_COUNT|EX_TRLBAR, ADDR_OTHER),
-EXCMD(CMD_badd,         "badd",         ex_edit, EX_NEEDARG|EX_FILE1|EX_CMDARG|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_balt,         "balt",         ex_edit, EX_NEEDARG|EX_FILE1|EX_CMDARG|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_bdelete,      "bdelete",      ex_bunload, EX_BANG|EX_RANGE|EX_BUFNAME|EX_COUNT|EX_EXTRA|EX_TRLBAR, ADDR_BUFFERS),
-EXCMD(CMD_behave,       "behave",       ex_behave, EX_BANG|EX_NEEDARG|EX_WORD1|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_belowright,   "belowright",   ex_wrongmodifier, EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM, ADDR_NONE),
-EXCMD(CMD_bfirst,       "bfirst",       ex_brewind, EX_BANG|EX_RANGE|EX_CMDARG|EX_TRLBAR, ADDR_OTHER),
-EXCMD(CMD_blast,        "blast",        ex_blast, EX_BANG|EX_RANGE|EX_CMDARG|EX_TRLBAR, ADDR_OTHER),
-EXCMD(CMD_bmodified,    "bmodified",    ex_bmodified, EX_BANG|EX_RANGE|EX_COUNT|EX_CMDARG|EX_TRLBAR, ADDR_OTHER),
-EXCMD(CMD_bnext,        "bnext",        ex_bnext, EX_BANG|EX_RANGE|EX_COUNT|EX_CMDARG|EX_TRLBAR, ADDR_OTHER),
-EXCMD(CMD_botright,     "botright",     ex_wrongmodifier, EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM, ADDR_NONE),
-EXCMD(CMD_bprevious,    "bprevious",    ex_bprevious, EX_BANG|EX_RANGE|EX_COUNT|EX_CMDARG|EX_TRLBAR, ADDR_OTHER),
-EXCMD(CMD_brewind,      "brewind",      ex_brewind, EX_BANG|EX_RANGE|EX_CMDARG|EX_TRLBAR, ADDR_OTHER),
-EXCMD(CMD_break,        "break",        ex_break, EX_TRLBAR|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK|EX_WHOLE, ADDR_NONE),
-EXCMD(CMD_breakadd,     "breakadd",     ex_breakadd, EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_breakdel,     "breakdel",     ex_breakdel, EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_breaklist,    "breaklist",    ex_breaklist, EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_browse,       "browse",       ex_wrongmodifier, EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_buffers,      "buffers",      buflist_list, EX_BANG|EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_bufdo,        "bufdo",        ex_listdo, EX_BANG|EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM|EX_RANGE|EX_DFLALL|EX_EXPAND, ADDR_BUFFERS),
-EXCMD(CMD_bunload,      "bunload",      ex_bunload, EX_BANG|EX_RANGE|EX_BUFNAME|EX_COUNT|EX_EXTRA|EX_TRLBAR, ADDR_LOADED_BUFFERS),
-EXCMD(CMD_bwipeout,     "bwipeout",     ex_bunload, EX_BANG|EX_RANGE|EX_BUFNAME|EX_BUFUNL|EX_COUNT|EX_EXTRA|EX_TRLBAR, ADDR_BUFFERS),
-EXCMD(CMD_change,       "change",       ex_change, EX_BANG|EX_WHOLEFOLD|EX_RANGE|EX_COUNT|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK|EX_MODIFY, ADDR_LINES),
-EXCMD(CMD_cNext,        "cNext",        ex_cnext, EX_RANGE|EX_COUNT|EX_TRLBAR|EX_BANG, ADDR_UNSIGNED),
-EXCMD(CMD_cNfile,       "cNfile",       ex_cnext, EX_RANGE|EX_COUNT|EX_TRLBAR|EX_BANG, ADDR_UNSIGNED),
-EXCMD(CMD_cabbrev,      "cabbrev",      ex_abbreviate, EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_cabclear,     "cabclear",     ex_abclear, EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_cabove,       "cabove",       ex_cbelow, EX_RANGE|EX_COUNT|EX_TRLBAR, ADDR_UNSIGNED),
-EXCMD(CMD_caddbuffer,   "caddbuffer",   ex_cbuffer, EX_RANGE|EX_WORD1|EX_TRLBAR, ADDR_LINES),
-EXCMD(CMD_caddexpr,     "caddexpr",     ex_cexpr, EX_NEEDARG|EX_WORD1|EX_NOTRLCOM|EX_EXPR_ARG, ADDR_NONE),
-EXCMD(CMD_caddfile,     "caddfile",     ex_cfile, EX_TRLBAR|EX_FILE1, ADDR_NONE),
-EXCMD(CMD_cafter,       "cafter",       ex_cbelow, EX_RANGE|EX_COUNT|EX_TRLBAR, ADDR_UNSIGNED),
-EXCMD(CMD_call,         "call",         ex_call, EX_RANGE|EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM|EX_EXPR_ARG|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK, ADDR_LINES),
-EXCMD(CMD_catch,        "catch",        ex_catch, EX_EXTRA|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK|EX_WHOLE, ADDR_NONE),
-EXCMD(CMD_cbuffer,      "cbuffer",      ex_cbuffer, EX_BANG|EX_RANGE|EX_WORD1|EX_TRLBAR, ADDR_LINES),
-EXCMD(CMD_cbefore,      "cbefore",      ex_cbelow, EX_RANGE|EX_COUNT|EX_TRLBAR, ADDR_UNSIGNED),
-EXCMD(CMD_cbelow,       "cbelow",       ex_cbelow, EX_RANGE|EX_COUNT|EX_TRLBAR, ADDR_UNSIGNED),
-EXCMD(CMD_cbottom,      "cbottom",      ex_cbottom, EX_TRLBAR, ADDR_NONE),
-EXCMD(CMD_cc,           "cc",           ex_cc, EX_RANGE|EX_COUNT|EX_TRLBAR|EX_BANG, ADDR_QUICKFIX),
-EXCMD(CMD_cclose,       "cclose",       ex_cclose, EX_TRLBAR, ADDR_NONE),
-EXCMD(CMD_cd,           "cd",           ex_cd, EX_BANG|EX_FILE1|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_cdo,          "cdo",          ex_listdo, EX_BANG|EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM|EX_RANGE|EX_DFLALL|EX_EXPAND, ADDR_QUICKFIX_VALID),
-EXCMD(CMD_center,       "center",       ex_align, EX_TRLBAR|EX_RANGE|EX_WHOLEFOLD|EX_EXTRA|EX_CMDWIN|EX_LOCK_OK|EX_MODIFY, ADDR_LINES),
-EXCMD(CMD_cexpr,        "cexpr",        ex_cexpr, EX_NEEDARG|EX_WORD1|EX_NOTRLCOM|EX_EXPR_ARG|EX_BANG, ADDR_NONE),
-EXCMD(CMD_cfile,        "cfile",        ex_cfile, EX_TRLBAR|EX_FILE1|EX_BANG, ADDR_NONE),
-EXCMD(CMD_cfdo,         "cfdo",         ex_listdo, EX_BANG|EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM|EX_RANGE|EX_DFLALL|EX_EXPAND, ADDR_QUICKFIX_VALID),
-EXCMD(CMD_cfirst,       "cfirst",       ex_cc, EX_RANGE|EX_COUNT|EX_TRLBAR|EX_BANG, ADDR_UNSIGNED),
-EXCMD(CMD_cgetfile,     "cgetfile",     ex_cfile, EX_TRLBAR|EX_FILE1, ADDR_NONE),
-EXCMD(CMD_cgetbuffer,   "cgetbuffer",   ex_cbuffer, EX_RANGE|EX_WORD1|EX_TRLBAR, ADDR_LINES),
-EXCMD(CMD_cgetexpr,     "cgetexpr",     ex_cexpr, EX_NEEDARG|EX_WORD1|EX_NOTRLCOM|EX_EXPR_ARG, ADDR_NONE),
-EXCMD(CMD_chdir,        "chdir",        ex_cd, EX_BANG|EX_FILE1|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_changes,      "changes",      ex_changes, EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_checkpath,    "checkpath",    ex_checkpath, EX_TRLBAR|EX_BANG|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_checktime,    "checktime",    ex_checktime, EX_RANGE|EX_BUFNAME|EX_COUNT|EX_EXTRA|EX_TRLBAR, ADDR_OTHER),
-EXCMD(CMD_chistory,     "chistory",     qf_history, EX_RANGE|EX_COUNT|EX_TRLBAR, ADDR_UNSIGNED),
-EXCMD(CMD_clist,        "clist",        qf_list, EX_BANG|EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_clipreset,    "clipreset",    ex_clipreset, EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_clast,        "clast",        ex_cc, EX_RANGE|EX_COUNT|EX_TRLBAR|EX_BANG, ADDR_UNSIGNED),
-EXCMD(CMD_class,        "class",        ex_class, EX_EXTRA|EX_CMDWIN|EX_LOCK_OK|EX_WHOLE|EX_EXPORT, ADDR_NONE),
-EXCMD(CMD_close,        "close",        ex_close, EX_BANG|EX_RANGE|EX_COUNT|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_WINDOWS),
-EXCMD(CMD_clearjumps,   "clearjumps",   ex_clearjumps, EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_cmap,         "cmap",         ex_map, EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_cmapclear,    "cmapclear",    ex_mapclear, EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_cmenu,        "cmenu",        ex_menu, EX_RANGE|EX_ZEROR|EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_OTHER),
-EXCMD(CMD_cnext,        "cnext",        ex_cnext, EX_RANGE|EX_COUNT|EX_TRLBAR|EX_BANG, ADDR_UNSIGNED),
-EXCMD(CMD_cnewer,       "cnewer",       qf_age, EX_RANGE|EX_COUNT|EX_TRLBAR, ADDR_UNSIGNED),
-EXCMD(CMD_cnfile,       "cnfile",       ex_cnext, EX_RANGE|EX_COUNT|EX_TRLBAR|EX_BANG, ADDR_UNSIGNED),
-EXCMD(CMD_cnoremap,     "cnoremap",     ex_map, EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_cnoreabbrev,  "cnoreabbrev",  ex_abbreviate, EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_cnoremenu,    "cnoremenu",    ex_menu, EX_RANGE|EX_ZEROR|EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_OTHER),
-EXCMD(CMD_copy,         "copy",         ex_copymove, EX_RANGE|EX_WHOLEFOLD|EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK|EX_MODIFY, ADDR_LINES),
-EXCMD(CMD_colder,       "colder",       qf_age, EX_RANGE|EX_COUNT|EX_TRLBAR, ADDR_UNSIGNED),
-EXCMD(CMD_colorscheme,  "colorscheme",  ex_colorscheme, EX_WORD1|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_command,      "command",      ex_command, EX_EXTRA|EX_BANG|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_comclear,     "comclear",     ex_comclear, EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_compiler,     "compiler",     ex_compiler, EX_BANG|EX_TRLBAR|EX_WORD1|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_continue,     "continue",     ex_continue, EX_TRLBAR|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK|EX_WHOLE, ADDR_NONE),
-EXCMD(CMD_confirm,      "confirm",      ex_wrongmodifier, EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_const,        "const",        ex_let, EX_EXTRA|EX_BANG|EX_NOTRLCOM|EX_EXPR_ARG|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK|EX_WHOLE|EX_EXPORT, ADDR_NONE),
-EXCMD(CMD_copen,        "copen",        ex_copen, EX_RANGE|EX_COUNT|EX_TRLBAR, ADDR_OTHER),
-EXCMD(CMD_cprevious,    "cprevious",    ex_cnext, EX_RANGE|EX_COUNT|EX_TRLBAR|EX_BANG, ADDR_UNSIGNED),
-EXCMD(CMD_cpfile,       "cpfile",       ex_cnext, EX_RANGE|EX_COUNT|EX_TRLBAR|EX_BANG, ADDR_OTHER),
-EXCMD(CMD_cquit,        "cquit",        ex_cquit, EX_RANGE|EX_COUNT|EX_ZEROR|EX_TRLBAR|EX_BANG, ADDR_UNSIGNED),
-EXCMD(CMD_crewind,      "crewind",      ex_cc, EX_RANGE|EX_COUNT|EX_TRLBAR|EX_BANG, ADDR_UNSIGNED),
-EXCMD(CMD_cscope,       "cscope",       ex_cscope, EX_EXTRA|EX_NOTRLCOM|EX_XFILE, ADDR_NONE),
-EXCMD(CMD_cstag,        "cstag",        ex_cstag, EX_BANG|EX_TRLBAR|EX_WORD1, ADDR_NONE),
-EXCMD(CMD_cunmap,       "cunmap",       ex_unmap, EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_cunabbrev,    "cunabbrev",    ex_abbreviate, EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_cunmenu,      "cunmenu",      ex_menu, EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_cwindow,      "cwindow",      ex_cwindow, EX_RANGE|EX_COUNT|EX_TRLBAR, ADDR_OTHER),
-EXCMD(CMD_delete,       "delete",       ex_operators, EX_RANGE|EX_WHOLEFOLD|EX_REGSTR|EX_COUNT|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK|EX_MODIFY, ADDR_LINES),
-EXCMD(CMD_delmarks,     "delmarks",     ex_delmarks, EX_BANG|EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_debug,        "debug",        ex_debug, EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_debuggreedy,  "debuggreedy",  ex_debuggreedy, EX_RANGE|EX_ZEROR|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_OTHER),
-EXCMD(CMD_def,          "def",          ex_function, EX_EXTRA|EX_BANG|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK|EX_WHOLE|EX_EXPORT, ADDR_NONE),
-EXCMD(CMD_defcompile,   "defcompile",   ex_defcompile, EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK|EX_TRLBAR|EX_EXTRA, ADDR_NONE),
-EXCMD(CMD_defer,        "defer",        ex_call, EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM|EX_EXPR_ARG|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_delcommand,   "delcommand",   ex_delcommand, EX_NEEDARG|EX_WORD1|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_delfunction,  "delfunction",  ex_delfunction, EX_BANG|EX_NEEDARG|EX_WORD1|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_display,      "display",      ex_display, EX_EXTRA|EX_NOTRLCOM|EX_TRLBAR|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_diffupdate,   "diffupdate",   ex_diffupdate, EX_BANG|EX_TRLBAR, ADDR_NONE),
-EXCMD(CMD_diffget,      "diffget",      ex_diffgetput, EX_RANGE|EX_ZEROR|EX_EXTRA|EX_TRLBAR|EX_MODIFY, ADDR_LINES),
-EXCMD(CMD_diffoff,      "diffoff",      ex_diffoff, EX_BANG|EX_TRLBAR, ADDR_NONE),
-EXCMD(CMD_diffpatch,    "diffpatch",    ex_diffpatch, EX_EXTRA|EX_FILE1|EX_TRLBAR|EX_MODIFY, ADDR_NONE),
-EXCMD(CMD_diffput,      "diffput",      ex_diffgetput, EX_RANGE|EX_ZEROR|EX_EXTRA|EX_TRLBAR, ADDR_LINES),
-EXCMD(CMD_diffsplit,    "diffsplit",    ex_diffsplit, EX_EXTRA|EX_FILE1|EX_TRLBAR, ADDR_NONE),
-EXCMD(CMD_diffthis,     "diffthis",     ex_diffthis, EX_TRLBAR, ADDR_NONE),
-EXCMD(CMD_digraphs,     "digraphs",     ex_digraphs, EX_BANG|EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_disassemble,  "disassemble",  ex_disassemble, EX_BANG|EX_EXTRA|EX_NEEDARG|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_djump,        "djump",        ex_findpat, EX_BANG|EX_RANGE|EX_DFLALL|EX_WHOLEFOLD|EX_EXTRA, ADDR_LINES),
-EXCMD(CMD_dlist,        "dlist",        ex_findpat, EX_BANG|EX_RANGE|EX_DFLALL|EX_WHOLEFOLD|EX_EXTRA|EX_CMDWIN|EX_LOCK_OK, ADDR_LINES),
-EXCMD(CMD_doautocmd,    "doautocmd",    ex_doautocmd, EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_doautoall,    "doautoall",    ex_doautoall, EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_drop,         "drop",         ex_drop, EX_BANG|EX_FILES|EX_CMDARG|EX_NEEDARG|EX_ARGOPT|EX_TRLBAR, ADDR_NONE),
-EXCMD(CMD_dsearch,      "dsearch",      ex_findpat, EX_BANG|EX_RANGE|EX_DFLALL|EX_WHOLEFOLD|EX_EXTRA|EX_CMDWIN|EX_LOCK_OK, ADDR_LINES),
-EXCMD(CMD_dsplit,       "dsplit",       ex_findpat, EX_BANG|EX_RANGE|EX_DFLALL|EX_WHOLEFOLD|EX_EXTRA, ADDR_LINES),
-EXCMD(CMD_edit,         "edit",         ex_edit, EX_BANG|EX_FILE1|EX_CMDARG|EX_ARGOPT|EX_TRLBAR, ADDR_NONE),
-EXCMD(CMD_earlier,      "earlier",      ex_later, EX_TRLBAR|EX_EXTRA|EX_NOSPC|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_echo,         "echo",         ex_echo, EX_EXTRA|EX_NOTRLCOM|EX_EXPR_ARG|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_echoerr,      "echoerr",      ex_execute, EX_EXTRA|EX_NOTRLCOM|EX_EXPR_ARG|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_echohl,       "echohl",       ex_echohl, EX_EXTRA|EX_TRLBAR|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_echomsg,      "echomsg",      ex_execute, EX_EXTRA|EX_NOTRLCOM|EX_EXPR_ARG|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_echoconsole,  "echoconsole",  ex_execute, EX_EXTRA|EX_NOTRLCOM|EX_EXPR_ARG|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_echon,        "echon",        ex_echo, EX_EXTRA|EX_NOTRLCOM|EX_EXPR_ARG|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_echowindow,   "echowindow",   ex_execute, EX_RANGE|EX_EXTRA|EX_NOTRLCOM|EX_EXPR_ARG|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK, ADDR_OTHER),
-EXCMD(CMD_else,         "else",         ex_else, EX_TRLBAR|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK|EX_WHOLE, ADDR_NONE),
-EXCMD(CMD_elseif,       "elseif",       ex_else, EX_EXTRA|EX_NOTRLCOM|EX_EXPR_ARG|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK|EX_WHOLE, ADDR_NONE),
-EXCMD(CMD_emenu,        "emenu",        ex_emenu, EX_NEEDARG|EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_RANGE|EX_CMDWIN|EX_LOCK_OK, ADDR_OTHER),
-EXCMD(CMD_endif,        "endif",        ex_endif, EX_TRLBAR|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK|EX_WHOLE, ADDR_NONE),
-EXCMD(CMD_endinterface, "endinterface", ex_wrongmodifier, EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK|EX_WHOLE, ADDR_NONE),
-EXCMD(CMD_endclass,     "endclass",     ex_wrongmodifier, EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK|EX_WHOLE, ADDR_NONE),
-EXCMD(CMD_enddef,       "enddef",       ex_endfunction, EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK|EX_WHOLE, ADDR_NONE),
-EXCMD(CMD_endenum,      "endenum",      ex_wrongmodifier, EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK|EX_WHOLE, ADDR_NONE),
-EXCMD(CMD_endfunction,  "endfunction",  ex_endfunction, EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_endfor,       "endfor",       ex_endwhile, EX_TRLBAR|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK|EX_WHOLE, ADDR_NONE),
-EXCMD(CMD_endtry,       "endtry",       ex_endtry, EX_TRLBAR|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK|EX_WHOLE, ADDR_NONE),
-EXCMD(CMD_endwhile,     "endwhile",     ex_endwhile, EX_TRLBAR|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK|EX_WHOLE, ADDR_NONE),
-EXCMD(CMD_enew,         "enew",         ex_edit, EX_BANG|EX_TRLBAR, ADDR_NONE),
-EXCMD(CMD_enum,         "enum",         ex_class, EX_EXTRA|EX_CMDWIN|EX_LOCK_OK|EX_WHOLE|EX_EXPORT, ADDR_NONE),
-EXCMD(CMD_eval,         "eval",         ex_eval, EX_EXTRA|EX_NOTRLCOM|EX_EXPR_ARG|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_ex,           "ex",           ex_edit, EX_BANG|EX_FILE1|EX_CMDARG|EX_ARGOPT|EX_TRLBAR, ADDR_NONE),
-EXCMD(CMD_execute,      "execute",      ex_execute, EX_EXTRA|EX_NOTRLCOM|EX_EXPR_ARG|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_exit,         "exit",         ex_exit, EX_RANGE|EX_WHOLEFOLD|EX_BANG|EX_FILE1|EX_ARGOPT|EX_DFLALL|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_LINES),
-EXCMD(CMD_export,       "export",       ex_export, EX_EXTRA|EX_NOTRLCOM|EX_EXPR_ARG|EX_CMDWIN|EX_LOCK_OK|EX_WHOLE, ADDR_NONE),
-EXCMD(CMD_exusage,      "exusage",      ex_exusage, EX_TRLBAR, ADDR_NONE),
-EXCMD(CMD_file,         "file",         ex_file, EX_RANGE|EX_ZEROR|EX_BANG|EX_FILE1|EX_TRLBAR, ADDR_OTHER),
-EXCMD(CMD_files,        "files",        buflist_list, EX_BANG|EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_filetype,     "filetype",     ex_filetype, EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_filter,       "filter",       ex_wrongmodifier, EX_BANG|EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM, ADDR_NONE),
-EXCMD(CMD_find,         "find",         ex_find, EX_RANGE|EX_BANG|EX_FILE1|EX_CMDARG|EX_ARGOPT|EX_TRLBAR|EX_NEEDARG, ADDR_OTHER),
-EXCMD(CMD_final,        "final",        ex_let, EX_EXTRA|EX_NOTRLCOM|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK|EX_WHOLE|EX_EXPORT, ADDR_NONE),
-EXCMD(CMD_finally,      "finally",      ex_finally, EX_TRLBAR|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK|EX_WHOLE, ADDR_NONE),
-EXCMD(CMD_finish,       "finish",       ex_finish, EX_TRLBAR|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK|EX_WHOLE, ADDR_NONE),
-EXCMD(CMD_first,        "first",        ex_rewind, EX_EXTRA|EX_BANG|EX_CMDARG|EX_ARGOPT|EX_TRLBAR, ADDR_NONE),
-EXCMD(CMD_fixdel,       "fixdel",       do_fixdel, EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_fold,         "fold",         ex_fold, EX_RANGE|EX_WHOLEFOLD|EX_TRLBAR|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK, ADDR_LINES),
-EXCMD(CMD_foldclose,    "foldclose",    ex_foldopen, EX_RANGE|EX_BANG|EX_WHOLEFOLD|EX_TRLBAR|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK, ADDR_LINES),
-EXCMD(CMD_folddoopen,   "folddoopen",   ex_folddo, EX_RANGE|EX_DFLALL|EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM, ADDR_LINES),
-EXCMD(CMD_folddoclosed, "folddoclosed", ex_folddo, EX_RANGE|EX_DFLALL|EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM, ADDR_LINES),
-EXCMD(CMD_foldopen,     "foldopen",     ex_foldopen, EX_RANGE|EX_BANG|EX_WHOLEFOLD|EX_TRLBAR|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK, ADDR_LINES),
-EXCMD(CMD_for,          "for",          ex_while, EX_EXTRA|EX_NOTRLCOM|EX_EXPR_ARG|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_function,     "function",     ex_function, EX_EXTRA|EX_BANG|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK|EX_EXPORT, ADDR_NONE),
-EXCMD(CMD_global,       "global",       ex_global, EX_RANGE|EX_WHOLEFOLD|EX_BANG|EX_EXTRA|EX_DFLALL|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK|EX_NONWHITE_OK, ADDR_LINES),
-EXCMD(CMD_goto,         "goto",         ex_goto, EX_RANGE|EX_COUNT|EX_TRLBAR|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK, ADDR_OTHER),
-EXCMD(CMD_grep,         "grep",         ex_make, EX_RANGE|EX_BANG|EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM|EX_TRLBAR|EX_XFILE, ADDR_OTHER),
-EXCMD(CMD_grepadd,      "grepadd",      ex_make, EX_RANGE|EX_BANG|EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM|EX_TRLBAR|EX_XFILE, ADDR_OTHER),
-EXCMD(CMD_gui,          "gui",          ex_gui, EX_BANG|EX_FILES|EX_CMDARG|EX_ARGOPT|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_gvim,         "gvim",         ex_gui, EX_BANG|EX_FILES|EX_CMDARG|EX_ARGOPT|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_help,         "help",         ex_help, EX_BANG|EX_EXTRA|EX_NOTRLCOM, ADDR_NONE),
-EXCMD(CMD_helpclose,    "helpclose",    ex_helpclose, EX_TRLBAR, ADDR_NONE),
-EXCMD(CMD_helpfind,     "helpfind",     ex_helpfind, EX_EXTRA|EX_NOTRLCOM, ADDR_NONE),
-EXCMD(CMD_helpgrep,     "helpgrep",     ex_helpgrep, EX_EXTRA|EX_NOTRLCOM|EX_NEEDARG, ADDR_NONE),
-EXCMD(CMD_helptags,     "helptags",     ex_helptags, EX_NEEDARG|EX_FILES|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_hardcopy,     "hardcopy",     ex_hardcopy, EX_RANGE|EX_COUNT|EX_EXTRA|EX_EXPAND|EX_TRLBAR|EX_DFLALL|EX_BANG, ADDR_LINES),
-EXCMD(CMD_highlight,    "highlight",    ex_highlight, EX_BANG|EX_EXTRA|EX_TRLBAR|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_hide,         "hide",         ex_hide, EX_BANG|EX_RANGE|EX_COUNT|EX_EXTRA|EX_TRLBAR, ADDR_WINDOWS),
-EXCMD(CMD_history,      "history",      ex_history, EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_horizontal,   "horizontal",   ex_wrongmodifier, EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM, ADDR_NONE),
-EXCMD(CMD_insert,       "insert",       ex_append, EX_BANG|EX_RANGE|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK|EX_MODIFY, ADDR_LINES),
-EXCMD(CMD_iabbrev,      "iabbrev",      ex_abbreviate, EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_iabclear,     "iabclear",     ex_abclear, EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_if,           "if",           ex_if, EX_EXTRA|EX_NOTRLCOM|EX_EXPR_ARG|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_ijump,        "ijump",        ex_findpat, EX_BANG|EX_RANGE|EX_DFLALL|EX_WHOLEFOLD|EX_EXTRA, ADDR_LINES),
-EXCMD(CMD_ilist,        "ilist",        ex_findpat, EX_BANG|EX_RANGE|EX_DFLALL|EX_WHOLEFOLD|EX_EXTRA|EX_CMDWIN|EX_LOCK_OK, ADDR_LINES),
-EXCMD(CMD_imap,         "imap",         ex_map, EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_imapclear,    "imapclear",    ex_mapclear, EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_imenu,        "imenu",        ex_menu, EX_RANGE|EX_ZEROR|EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_OTHER),
-EXCMD(CMD_import,       "import",       ex_import, EX_EXTRA|EX_NOTRLCOM|EX_CMDWIN|EX_LOCK_OK|EX_WHOLE, ADDR_NONE),
-EXCMD(CMD_inoremap,     "inoremap",     ex_map, EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_inoreabbrev,  "inoreabbrev",  ex_abbreviate, EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_inoremenu,    "inoremenu",    ex_menu, EX_RANGE|EX_ZEROR|EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_OTHER),
-EXCMD(CMD_intro,        "intro",        ex_intro, EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_interface,    "interface",    ex_class, EX_EXTRA|EX_CMDWIN|EX_LOCK_OK|EX_WHOLE|EX_EXPORT, ADDR_NONE),
-EXCMD(CMD_iput,         "iput",         ex_iput, EX_RANGE|EX_WHOLEFOLD|EX_BANG|EX_REGSTR|EX_TRLBAR|EX_ZEROR|EX_CMDWIN|EX_LOCK_OK|EX_MODIFY, ADDR_LINES),
-EXCMD(CMD_isearch,      "isearch",      ex_findpat, EX_BANG|EX_RANGE|EX_DFLALL|EX_WHOLEFOLD|EX_EXTRA|EX_CMDWIN|EX_LOCK_OK, ADDR_LINES),
-EXCMD(CMD_isplit,       "isplit",       ex_findpat, EX_BANG|EX_RANGE|EX_DFLALL|EX_WHOLEFOLD|EX_EXTRA, ADDR_LINES),
-EXCMD(CMD_iunmap,       "iunmap",       ex_unmap, EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_iunabbrev,    "iunabbrev",    ex_abbreviate, EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_iunmenu,      "iunmenu",      ex_menu, EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_join,         "join",         ex_join, EX_BANG|EX_RANGE|EX_WHOLEFOLD|EX_COUNT|EX_FLAGS|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK|EX_MODIFY, ADDR_LINES),
-EXCMD(CMD_jumps,        "jumps",        ex_jumps, EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_k,            "k",            ex_mark, EX_RANGE|EX_WORD1|EX_TRLBAR|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK|EX_NONWHITE_OK, ADDR_LINES),
-EXCMD(CMD_keepmarks,    "keepmarks",    ex_wrongmodifier, EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM, ADDR_NONE),
-EXCMD(CMD_keepjumps,    "keepjumps",    ex_wrongmodifier, EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM, ADDR_NONE),
-EXCMD(CMD_keeppatterns, "keeppatterns", ex_wrongmodifier, EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM, ADDR_NONE),
-EXCMD(CMD_keepalt,      "keepalt",      ex_wrongmodifier, EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM, ADDR_NONE),
-EXCMD(CMD_list,         "list",         ex_print, EX_RANGE|EX_WHOLEFOLD|EX_COUNT|EX_FLAGS|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_LINES),
-EXCMD(CMD_lNext,        "lNext",        ex_cnext, EX_RANGE|EX_COUNT|EX_TRLBAR|EX_BANG, ADDR_UNSIGNED),
-EXCMD(CMD_lNfile,       "lNfile",       ex_cnext, EX_RANGE|EX_COUNT|EX_TRLBAR|EX_BANG, ADDR_UNSIGNED),
-EXCMD(CMD_last,         "last",         ex_last, EX_EXTRA|EX_BANG|EX_CMDARG|EX_ARGOPT|EX_TRLBAR, ADDR_NONE),
-EXCMD(CMD_labove,       "labove",       ex_cbelow, EX_RANGE|EX_COUNT|EX_TRLBAR, ADDR_UNSIGNED),
-EXCMD(CMD_language,     "language",     ex_language, EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_laddexpr,     "laddexpr",     ex_cexpr, EX_NEEDARG|EX_WORD1|EX_NOTRLCOM|EX_EXPR_ARG, ADDR_NONE),
-EXCMD(CMD_laddbuffer,   "laddbuffer",   ex_cbuffer, EX_RANGE|EX_WORD1|EX_TRLBAR, ADDR_LINES),
-EXCMD(CMD_laddfile,     "laddfile",     ex_cfile, EX_TRLBAR|EX_FILE1, ADDR_NONE),
-EXCMD(CMD_lafter,       "lafter",       ex_cbelow, EX_RANGE|EX_COUNT|EX_TRLBAR, ADDR_UNSIGNED),
-EXCMD(CMD_later,        "later",        ex_later, EX_TRLBAR|EX_EXTRA|EX_NOSPC|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_lbuffer,      "lbuffer",      ex_cbuffer, EX_BANG|EX_RANGE|EX_WORD1|EX_TRLBAR, ADDR_LINES),
-EXCMD(CMD_lbefore,      "lbefore",      ex_cbelow, EX_RANGE|EX_COUNT|EX_TRLBAR, ADDR_UNSIGNED),
-EXCMD(CMD_lbelow,       "lbelow",       ex_cbelow, EX_RANGE|EX_COUNT|EX_TRLBAR, ADDR_UNSIGNED),
-EXCMD(CMD_lbottom,      "lbottom",      ex_cbottom, EX_TRLBAR, ADDR_NONE),
-EXCMD(CMD_lcd,          "lcd",          ex_cd, EX_BANG|EX_FILE1|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_lchdir,       "lchdir",       ex_cd, EX_BANG|EX_FILE1|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_lclose,       "lclose",       ex_cclose, EX_RANGE|EX_COUNT|EX_TRLBAR, ADDR_OTHER),
-EXCMD(CMD_lcscope,      "lcscope",      ex_cscope, EX_EXTRA|EX_NOTRLCOM|EX_XFILE, ADDR_NONE),
-EXCMD(CMD_ldo,          "ldo",          ex_listdo, EX_BANG|EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM|EX_RANGE|EX_DFLALL|EX_EXPAND, ADDR_QUICKFIX_VALID),
-EXCMD(CMD_left,         "left",         ex_align, EX_TRLBAR|EX_RANGE|EX_WHOLEFOLD|EX_EXTRA|EX_CMDWIN|EX_LOCK_OK|EX_MODIFY, ADDR_LINES),
-EXCMD(CMD_leftabove,    "leftabove",    ex_wrongmodifier, EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM, ADDR_NONE),
-EXCMD(CMD_let,          "let",          ex_let, EX_EXTRA|EX_NOTRLCOM|EX_EXPR_ARG|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_lexpr,        "lexpr",        ex_cexpr, EX_NEEDARG|EX_WORD1|EX_NOTRLCOM|EX_EXPR_ARG|EX_BANG, ADDR_NONE),
-EXCMD(CMD_legacy,       "legacy",       ex_wrongmodifier, EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_lfile,        "lfile",        ex_cfile, EX_TRLBAR|EX_FILE1|EX_BANG, ADDR_NONE),
-EXCMD(CMD_lfdo,         "lfdo",         ex_listdo, EX_BANG|EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM|EX_RANGE|EX_DFLALL|EX_EXPAND, ADDR_QUICKFIX_VALID),
-EXCMD(CMD_lfirst,       "lfirst",       ex_cc, EX_RANGE|EX_COUNT|EX_TRLBAR|EX_BANG, ADDR_UNSIGNED),
-EXCMD(CMD_lgetfile,     "lgetfile",     ex_cfile, EX_TRLBAR|EX_FILE1, ADDR_NONE),
-EXCMD(CMD_lgetbuffer,   "lgetbuffer",   ex_cbuffer, EX_RANGE|EX_WORD1|EX_TRLBAR, ADDR_LINES),
-EXCMD(CMD_lgetexpr,     "lgetexpr",     ex_cexpr, EX_NEEDARG|EX_WORD1|EX_NOTRLCOM|EX_EXPR_ARG, ADDR_NONE),
-EXCMD(CMD_lgrep,        "lgrep",        ex_make, EX_RANGE|EX_BANG|EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM|EX_TRLBAR|EX_XFILE, ADDR_OTHER),
-EXCMD(CMD_lgrepadd,     "lgrepadd",     ex_make, EX_RANGE|EX_BANG|EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM|EX_TRLBAR|EX_XFILE, ADDR_OTHER),
-EXCMD(CMD_lhelpgrep,    "lhelpgrep",    ex_helpgrep, EX_EXTRA|EX_NOTRLCOM|EX_NEEDARG, ADDR_NONE),
-EXCMD(CMD_lhistory,     "lhistory",     qf_history, EX_RANGE|EX_COUNT|EX_TRLBAR, ADDR_UNSIGNED),
-EXCMD(CMD_ll,           "ll",           ex_cc, EX_RANGE|EX_COUNT|EX_TRLBAR|EX_BANG, ADDR_QUICKFIX),
-EXCMD(CMD_llast,        "llast",        ex_cc, EX_RANGE|EX_COUNT|EX_TRLBAR|EX_BANG, ADDR_UNSIGNED),
-EXCMD(CMD_llist,        "llist",        qf_list, EX_BANG|EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_lmap,         "lmap",         ex_map, EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_lmapclear,    "lmapclear",    ex_mapclear, EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_lmake,        "lmake",        ex_make, EX_BANG|EX_EXTRA|EX_NOTRLCOM|EX_TRLBAR|EX_XFILE, ADDR_NONE),
-EXCMD(CMD_lnoremap,     "lnoremap",     ex_map, EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_lnext,        "lnext",        ex_cnext, EX_RANGE|EX_COUNT|EX_TRLBAR|EX_BANG, ADDR_UNSIGNED),
-EXCMD(CMD_lnewer,       "lnewer",       qf_age, EX_RANGE|EX_COUNT|EX_TRLBAR, ADDR_UNSIGNED),
-EXCMD(CMD_lnfile,       "lnfile",       ex_cnext, EX_RANGE|EX_COUNT|EX_TRLBAR|EX_BANG, ADDR_UNSIGNED),
-EXCMD(CMD_loadview,     "loadview",     ex_loadview, EX_FILE1|EX_TRLBAR, ADDR_NONE),
-EXCMD(CMD_loadkeymap,   "loadkeymap",   ex_loadkeymap, EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_lockmarks,    "lockmarks",    ex_wrongmodifier, EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM, ADDR_NONE),
-EXCMD(CMD_lockvar,      "lockvar",      ex_lockvar, EX_BANG|EX_EXTRA|EX_NEEDARG|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_lolder,       "lolder",       qf_age, EX_RANGE|EX_COUNT|EX_TRLBAR, ADDR_UNSIGNED),
-EXCMD(CMD_lopen,        "lopen",        ex_copen, EX_RANGE|EX_COUNT|EX_TRLBAR, ADDR_OTHER),
-EXCMD(CMD_lprevious,    "lprevious",    ex_cnext, EX_RANGE|EX_COUNT|EX_TRLBAR|EX_BANG, ADDR_UNSIGNED),
-EXCMD(CMD_lpfile,       "lpfile",       ex_cnext, EX_RANGE|EX_COUNT|EX_TRLBAR|EX_BANG, ADDR_OTHER),
-EXCMD(CMD_lrewind,      "lrewind",      ex_cc, EX_RANGE|EX_COUNT|EX_TRLBAR|EX_BANG, ADDR_UNSIGNED),
-EXCMD(CMD_ltag,         "ltag", ex_tag, EX_TRLBAR|EX_BANG|EX_WORD1, ADDR_NONE),
-EXCMD(CMD_lunmap,       "lunmap",       ex_unmap, EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_lua,          "lua",          ex_lua, EX_RANGE|EX_EXTRA|EX_NEEDARG|EX_CMDWIN|EX_LOCK_OK|EX_RESTRICT, ADDR_LINES),
-EXCMD(CMD_luado,        "luado",        ex_luado, EX_RANGE|EX_DFLALL|EX_EXTRA|EX_NEEDARG|EX_CMDWIN|EX_LOCK_OK|EX_RESTRICT, ADDR_LINES),
-EXCMD(CMD_luafile,      "luafile",      ex_luafile, EX_RANGE|EX_FILE1|EX_NEEDARG|EX_CMDWIN|EX_LOCK_OK|EX_RESTRICT, ADDR_LINES),
-EXCMD(CMD_lvimgrep,     "lvimgrep",     ex_vimgrep, EX_RANGE|EX_BANG|EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM|EX_TRLBAR|EX_XFILE|EX_LOCK_OK, ADDR_OTHER),
-EXCMD(CMD_lvimgrepadd,  "lvimgrepadd",  ex_vimgrep, EX_RANGE|EX_BANG|EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM|EX_TRLBAR|EX_XFILE|EX_LOCK_OK, ADDR_OTHER),
-EXCMD(CMD_lwindow,      "lwindow",      ex_cwindow, EX_RANGE|EX_COUNT|EX_TRLBAR, ADDR_OTHER),
-EXCMD(CMD_ls,           "ls",           buflist_list, EX_BANG|EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_move,         "move",         ex_copymove, EX_RANGE|EX_WHOLEFOLD|EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK|EX_MODIFY, ADDR_LINES),
-EXCMD(CMD_mark,         "mark",         ex_mark, EX_RANGE|EX_WORD1|EX_TRLBAR|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK, ADDR_LINES),
-EXCMD(CMD_make,         "make",         ex_make, EX_BANG|EX_EXTRA|EX_NOTRLCOM|EX_TRLBAR|EX_XFILE, ADDR_NONE),
-EXCMD(CMD_map,          "map",          ex_map, EX_BANG|EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_mapclear,     "mapclear",     ex_mapclear, EX_EXTRA|EX_BANG|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_marks,        "marks",        ex_marks, EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_match,        "match",        ex_match, EX_RANGE|EX_EXTRA|EX_CMDWIN|EX_LOCK_OK, ADDR_OTHER),
-EXCMD(CMD_menu,         "menu",         ex_menu, EX_RANGE|EX_ZEROR|EX_BANG|EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_OTHER),
-EXCMD(CMD_menutranslate, "menutranslate", ex_menutranslate, EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_messages,     "messages",     ex_messages, EX_EXTRA|EX_TRLBAR|EX_RANGE|EX_CMDWIN|EX_LOCK_OK, ADDR_OTHER),
-EXCMD(CMD_mkexrc,       "mkexrc",       ex_mkrc, EX_BANG|EX_FILE1|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_mksession,    "mksession",    ex_mkrc, EX_BANG|EX_FILE1|EX_TRLBAR, ADDR_NONE),
-EXCMD(CMD_mkspell,      "mkspell",      ex_mkspell, EX_BANG|EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM|EX_TRLBAR|EX_XFILE, ADDR_NONE),
-EXCMD(CMD_mkvimrc,      "mkvimrc",      ex_mkrc, EX_BANG|EX_FILE1|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_mkview,       "mkview",       ex_mkrc, EX_BANG|EX_FILE1|EX_TRLBAR, ADDR_NONE),
-EXCMD(CMD_mode,         "mode",         ex_mode, EX_WORD1|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_mzscheme,     "mzscheme",     ex_mzscheme, EX_RANGE|EX_EXTRA|EX_DFLALL|EX_NEEDARG|EX_CMDWIN|EX_LOCK_OK|EX_SBOXOK|EX_RESTRICT, ADDR_LINES),
-EXCMD(CMD_mzfile,       "mzfile",       ex_mzfile, EX_RANGE|EX_FILE1|EX_NEEDARG|EX_CMDWIN|EX_LOCK_OK|EX_RESTRICT, ADDR_LINES),
-EXCMD(CMD_next,         "next",         ex_next, EX_RANGE|EX_BANG|EX_FILES|EX_CMDARG|EX_ARGOPT|EX_TRLBAR, ADDR_OTHER),
-EXCMD(CMD_nbkey,        "nbkey",        ex_nbkey, EX_EXTRA|EX_NEEDARG, ADDR_NONE),
-EXCMD(CMD_nbclose,      "nbclose",      ex_nbclose, EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_nbstart,      "nbstart",      ex_nbstart, EX_WORD1|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_new,          "new",          ex_splitview, EX_BANG|EX_FILE1|EX_RANGE|EX_CMDARG|EX_ARGOPT|EX_TRLBAR, ADDR_OTHER),
-EXCMD(CMD_nmap,         "nmap",         ex_map, EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_nmapclear,    "nmapclear",    ex_mapclear, EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_nmenu,        "nmenu",        ex_menu, EX_RANGE|EX_ZEROR|EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_OTHER),
-EXCMD(CMD_nnoremap,     "nnoremap",     ex_map, EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_nnoremenu,    "nnoremenu",    ex_menu, EX_RANGE|EX_ZEROR|EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_OTHER),
-EXCMD(CMD_noremap,      "noremap",      ex_map, EX_BANG|EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_noautocmd,    "noautocmd",    ex_wrongmodifier, EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM, ADDR_NONE),
-EXCMD(CMD_nohlsearch,   "nohlsearch",   ex_nohlsearch, EX_TRLBAR|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_noreabbrev,   "noreabbrev",   ex_abbreviate, EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_noremenu,     "noremenu",     ex_menu, EX_RANGE|EX_ZEROR|EX_BANG|EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_OTHER),
-EXCMD(CMD_noswapfile,   "noswapfile",   ex_wrongmodifier, EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM, ADDR_NONE),
-EXCMD(CMD_normal,       "normal",       ex_normal, EX_RANGE|EX_BANG|EX_EXTRA|EX_NEEDARG|EX_NOTRLCOM|EX_CTRLV|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK, ADDR_LINES),
-EXCMD(CMD_number,       "number",       ex_print, EX_RANGE|EX_WHOLEFOLD|EX_COUNT|EX_FLAGS|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_LINES),
-EXCMD(CMD_nunmap,       "nunmap",       ex_unmap, EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_nunmenu,      "nunmenu",      ex_menu, EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_open,         "open",         ex_open, EX_RANGE|EX_BANG|EX_EXTRA, ADDR_LINES),
-EXCMD(CMD_oldfiles,     "oldfiles",     ex_oldfiles, EX_BANG|EX_TRLBAR|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_omap,         "omap",         ex_map, EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_omapclear,    "omapclear",    ex_mapclear, EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_omenu,        "omenu",        ex_menu, EX_RANGE|EX_ZEROR|EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_OTHER),
-EXCMD(CMD_only,         "only",         ex_only, EX_BANG|EX_RANGE|EX_COUNT|EX_TRLBAR, ADDR_WINDOWS),
-EXCMD(CMD_onoremap,     "onoremap",     ex_map, EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_onoremenu,    "onoremenu",    ex_menu, EX_RANGE|EX_ZEROR|EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_OTHER),
-EXCMD(CMD_options,      "options",      ex_options, EX_TRLBAR, ADDR_NONE),
-EXCMD(CMD_ounmap,       "ounmap",       ex_unmap, EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_ounmenu,      "ounmenu",      ex_menu, EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_ownsyntax,    "ownsyntax",    ex_ownsyntax, EX_EXTRA|EX_NOTRLCOM|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_print,        "print",        ex_print, EX_RANGE|EX_WHOLEFOLD|EX_COUNT|EX_FLAGS|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK|EX_SBOXOK, ADDR_LINES),
-EXCMD(CMD_packadd,      "packadd",      ex_packadd, EX_BANG|EX_FILE1|EX_NEEDARG|EX_TRLBAR|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_packloadall,  "packloadall",  ex_packloadall, EX_BANG|EX_TRLBAR|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_pbuffer,      "pbuffer",      ex_pbuffer, EX_BANG|EX_RANGE|EX_BUFNAME|EX_BUFUNL|EX_COUNT|EX_EXTRA|EX_CMDARG|EX_TRLBAR, ADDR_BUFFERS),
-EXCMD(CMD_pclose,       "pclose",       ex_pclose, EX_BANG|EX_TRLBAR, ADDR_NONE),
-EXCMD(CMD_perl,         "perl",         ex_perl, EX_RANGE|EX_EXTRA|EX_DFLALL|EX_NEEDARG|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK|EX_RESTRICT, ADDR_LINES),
-EXCMD(CMD_perldo,       "perldo",       ex_perldo, EX_RANGE|EX_EXTRA|EX_DFLALL|EX_NEEDARG|EX_CMDWIN|EX_LOCK_OK|EX_RESTRICT, ADDR_LINES),
-EXCMD(CMD_pedit,        "pedit",        ex_pedit, EX_BANG|EX_FILE1|EX_CMDARG|EX_ARGOPT|EX_TRLBAR, ADDR_NONE),
-EXCMD(CMD_pop,          "pop",          ex_tag, EX_RANGE|EX_BANG|EX_COUNT|EX_TRLBAR|EX_ZEROR, ADDR_OTHER),
-EXCMD(CMD_popup,        "popup",        ex_popup, EX_NEEDARG|EX_EXTRA|EX_BANG|EX_TRLBAR|EX_NOTRLCOM|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_ppop,         "ppop",         ex_ptag, EX_RANGE|EX_BANG|EX_COUNT|EX_TRLBAR|EX_ZEROR, ADDR_OTHER),
-EXCMD(CMD_preserve,     "preserve",     ex_preserve, EX_TRLBAR, ADDR_NONE),
-EXCMD(CMD_previous,     "previous",     ex_previous, EX_EXTRA|EX_RANGE|EX_COUNT|EX_BANG|EX_CMDARG|EX_ARGOPT|EX_TRLBAR, ADDR_OTHER),
-EXCMD(CMD_promptfind,   "promptfind",   gui_mch_find_dialog, EX_EXTRA|EX_NOTRLCOM|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_promptrepl,   "promptrepl",   gui_mch_replace_dialog, EX_EXTRA|EX_NOTRLCOM|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_profile,      "profile",      ex_profile, EX_BANG|EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_profdel,      "profdel",      ex_breakdel, EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_psearch,      "psearch",      ex_psearch, EX_BANG|EX_RANGE|EX_WHOLEFOLD|EX_DFLALL|EX_EXTRA, ADDR_LINES),
-EXCMD(CMD_ptag,         "ptag",         ex_ptag, EX_RANGE|EX_BANG|EX_WORD1|EX_TRLBAR|EX_ZEROR, ADDR_OTHER),
-EXCMD(CMD_ptNext,       "ptNext",       ex_ptag, EX_RANGE|EX_BANG|EX_TRLBAR|EX_ZEROR, ADDR_OTHER),
-EXCMD(CMD_ptfirst,      "ptfirst",      ex_ptag, EX_RANGE|EX_BANG|EX_TRLBAR|EX_ZEROR, ADDR_OTHER),
-EXCMD(CMD_ptjump,       "ptjump",       ex_ptag, EX_BANG|EX_TRLBAR|EX_WORD1, ADDR_NONE),
-EXCMD(CMD_ptlast,       "ptlast",       ex_ptag, EX_BANG|EX_TRLBAR, ADDR_NONE),
-EXCMD(CMD_ptnext,       "ptnext",       ex_ptag, EX_RANGE|EX_BANG|EX_TRLBAR|EX_ZEROR, ADDR_OTHER),
-EXCMD(CMD_ptprevious,   "ptprevious",   ex_ptag, EX_RANGE|EX_BANG|EX_TRLBAR|EX_ZEROR, ADDR_OTHER),
-EXCMD(CMD_ptrewind,     "ptrewind",     ex_ptag, EX_RANGE|EX_BANG|EX_TRLBAR|EX_ZEROR, ADDR_OTHER),
-EXCMD(CMD_ptselect,     "ptselect",     ex_ptag, EX_BANG|EX_TRLBAR|EX_WORD1, ADDR_NONE),
-EXCMD(CMD_put,          "put",          ex_put, EX_RANGE|EX_WHOLEFOLD|EX_BANG|EX_REGSTR|EX_TRLBAR|EX_ZEROR|EX_CMDWIN|EX_LOCK_OK|EX_MODIFY, ADDR_LINES),
-EXCMD(CMD_public,       "public",       ex_wrongmodifier, EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK|EX_WHOLE, ADDR_NONE),
-EXCMD(CMD_pwd,          "pwd",          ex_pwd, EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_python,       "python",       ex_python, EX_RANGE|EX_EXTRA|EX_NEEDARG|EX_CMDWIN|EX_LOCK_OK|EX_RESTRICT, ADDR_LINES),
-EXCMD(CMD_pydo,         "pydo",         ex_pydo, EX_RANGE|EX_DFLALL|EX_EXTRA|EX_NEEDARG|EX_CMDWIN|EX_LOCK_OK|EX_RESTRICT, ADDR_LINES),
-EXCMD(CMD_pyfile,       "pyfile",       ex_pyfile, EX_RANGE|EX_FILE1|EX_NEEDARG|EX_CMDWIN|EX_LOCK_OK|EX_RESTRICT, ADDR_LINES),
-EXCMD(CMD_py3,          "py3",          ex_py3, EX_RANGE|EX_EXTRA|EX_NEEDARG|EX_CMDWIN|EX_LOCK_OK|EX_RESTRICT, ADDR_LINES),
-EXCMD(CMD_py3do,        "py3do",        ex_py3do, EX_RANGE|EX_DFLALL|EX_EXTRA|EX_NEEDARG|EX_CMDWIN|EX_LOCK_OK|EX_RESTRICT, ADDR_LINES),
-EXCMD(CMD_python3,      "python3",      ex_py3, EX_RANGE|EX_EXTRA|EX_NEEDARG|EX_CMDWIN|EX_LOCK_OK|EX_RESTRICT, ADDR_LINES),
-EXCMD(CMD_py3file,      "py3file",      ex_py3file, EX_RANGE|EX_FILE1|EX_NEEDARG|EX_CMDWIN|EX_LOCK_OK|EX_RESTRICT, ADDR_LINES),
-EXCMD(CMD_pyx,          "pyx",          ex_pyx, EX_RANGE|EX_EXTRA|EX_NEEDARG|EX_CMDWIN|EX_LOCK_OK|EX_RESTRICT, ADDR_LINES),
-EXCMD(CMD_pyxdo,        "pyxdo",        ex_pyxdo, EX_RANGE|EX_DFLALL|EX_EXTRA|EX_NEEDARG|EX_CMDWIN|EX_LOCK_OK|EX_RESTRICT, ADDR_LINES),
-EXCMD(CMD_pythonx,      "pythonx",      ex_pyx, EX_RANGE|EX_EXTRA|EX_NEEDARG|EX_CMDWIN|EX_LOCK_OK|EX_RESTRICT, ADDR_LINES),
-EXCMD(CMD_pyxfile,      "pyxfile",      ex_pyxfile, EX_RANGE|EX_FILE1|EX_NEEDARG|EX_CMDWIN|EX_LOCK_OK|EX_RESTRICT, ADDR_LINES),
-EXCMD(CMD_quit,         "quit",         ex_quit, EX_BANG|EX_RANGE|EX_COUNT|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_WINDOWS),
-EXCMD(CMD_quitall,      "quitall",      ex_quit_all, EX_BANG|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_qall,         "qall",         ex_quit_all, EX_BANG|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_read,         "read",         ex_read, EX_BANG|EX_RANGE|EX_WHOLEFOLD|EX_FILE1|EX_ARGOPT|EX_TRLBAR|EX_ZEROR|EX_CMDWIN|EX_LOCK_OK|EX_MODIFY, ADDR_LINES),
-EXCMD(CMD_recover,      "recover",      ex_recover, EX_BANG|EX_FILE1|EX_TRLBAR, ADDR_NONE),
-EXCMD(CMD_redo,         "redo",         ex_redo, EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_redir,        "redir",        ex_redir, EX_BANG|EX_FILES|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_redraw,       "redraw",       ex_redraw, EX_BANG|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_redrawstatus, "redrawstatus", ex_redrawstatus, EX_BANG|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_redrawtabline, "redrawtabline", ex_redrawtabline, EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_redrawtabpanel, "redrawtabpanel", ex_redrawtabpanel, EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_registers,    "registers",    ex_display, EX_EXTRA|EX_NOTRLCOM|EX_TRLBAR|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_resize,       "resize",       ex_resize, EX_RANGE|EX_TRLBAR|EX_WORD1|EX_CMDWIN|EX_LOCK_OK, ADDR_OTHER),
-EXCMD(CMD_retab,        "retab",        ex_retab, EX_TRLBAR|EX_RANGE|EX_WHOLEFOLD|EX_DFLALL|EX_BANG|EX_WORD1|EX_CMDWIN|EX_LOCK_OK|EX_MODIFY, ADDR_LINES),
-EXCMD(CMD_return,       "return",       ex_return, EX_EXTRA|EX_NOTRLCOM|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK|EX_WHOLE, ADDR_NONE),
-EXCMD(CMD_rewind,       "rewind",       ex_rewind, EX_EXTRA|EX_BANG|EX_CMDARG|EX_ARGOPT|EX_TRLBAR, ADDR_NONE),
-EXCMD(CMD_right,        "right",        ex_align, EX_TRLBAR|EX_RANGE|EX_WHOLEFOLD|EX_EXTRA|EX_CMDWIN|EX_LOCK_OK|EX_MODIFY, ADDR_LINES),
-EXCMD(CMD_rightbelow,   "rightbelow",   ex_wrongmodifier, EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM, ADDR_NONE),
-EXCMD(CMD_runtime,      "runtime",      ex_runtime, EX_BANG|EX_NEEDARG|EX_FILES|EX_TRLBAR|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_ruby,         "ruby",         ex_ruby, EX_RANGE|EX_EXTRA|EX_NEEDARG|EX_CMDWIN|EX_LOCK_OK|EX_RESTRICT, ADDR_LINES),
-EXCMD(CMD_rubydo,       "rubydo",       ex_rubydo, EX_RANGE|EX_DFLALL|EX_EXTRA|EX_NEEDARG|EX_CMDWIN|EX_LOCK_OK|EX_RESTRICT, ADDR_LINES),
-EXCMD(CMD_rubyfile,     "rubyfile",     ex_rubyfile, EX_RANGE|EX_FILE1|EX_NEEDARG|EX_CMDWIN|EX_LOCK_OK|EX_RESTRICT, ADDR_LINES),
-EXCMD(CMD_rundo,        "rundo",        ex_rundo, EX_NEEDARG|EX_FILE1, ADDR_NONE),
-EXCMD(CMD_rviminfo,     "rviminfo",     ex_viminfo, EX_BANG|EX_FILE1|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_substitute,   "substitute",   ex_substitute, EX_RANGE|EX_WHOLEFOLD|EX_EXTRA|EX_CMDWIN|EX_LOCK_OK|EX_NONWHITE_OK, ADDR_LINES),
-EXCMD(CMD_sNext,        "sNext",        ex_previous, EX_EXTRA|EX_RANGE|EX_COUNT|EX_BANG|EX_CMDARG|EX_ARGOPT|EX_TRLBAR, ADDR_OTHER),
-EXCMD(CMD_sargument,    "sargument",    ex_argument, EX_BANG|EX_RANGE|EX_COUNT|EX_EXTRA|EX_CMDARG|EX_ARGOPT|EX_TRLBAR, ADDR_ARGUMENTS),
-EXCMD(CMD_sall,         "sall",         ex_all, EX_BANG|EX_RANGE|EX_COUNT|EX_TRLBAR, ADDR_OTHER),
-EXCMD(CMD_sandbox,      "sandbox",      ex_wrongmodifier, EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM, ADDR_NONE),
-EXCMD(CMD_saveas,       "saveas",       ex_write, EX_BANG|EX_FILE1|EX_ARGOPT|EX_CMDWIN|EX_LOCK_OK|EX_TRLBAR, ADDR_NONE),
-EXCMD(CMD_sbuffer,      "sbuffer",      ex_buffer, EX_BANG|EX_RANGE|EX_BUFNAME|EX_BUFUNL|EX_COUNT|EX_EXTRA|EX_CMDARG|EX_TRLBAR, ADDR_BUFFERS),
-EXCMD(CMD_sbNext,       "sbNext",       ex_bprevious, EX_RANGE|EX_COUNT|EX_CMDARG|EX_TRLBAR, ADDR_OTHER),
-EXCMD(CMD_sball,        "sball",        ex_buffer_all, EX_RANGE|EX_COUNT|EX_CMDARG|EX_TRLBAR, ADDR_OTHER),
-EXCMD(CMD_sbfirst,      "sbfirst",      ex_brewind, EX_CMDARG|EX_TRLBAR, ADDR_NONE),
-EXCMD(CMD_sblast,       "sblast",       ex_blast, EX_CMDARG|EX_TRLBAR, ADDR_NONE),
-EXCMD(CMD_sbmodified,   "sbmodified",   ex_bmodified, EX_RANGE|EX_COUNT|EX_CMDARG|EX_TRLBAR, ADDR_OTHER),
-EXCMD(CMD_sbnext,       "sbnext",       ex_bnext, EX_RANGE|EX_COUNT|EX_CMDARG|EX_TRLBAR, ADDR_OTHER),
-EXCMD(CMD_sbprevious,   "sbprevious",   ex_bprevious, EX_RANGE|EX_COUNT|EX_CMDARG|EX_TRLBAR, ADDR_OTHER),
-EXCMD(CMD_sbrewind,     "sbrewind",     ex_brewind, EX_CMDARG|EX_TRLBAR, ADDR_NONE),
-EXCMD(CMD_scriptnames,  "scriptnames",  ex_scriptnames, EX_BANG|EX_FILES|EX_RANGE|EX_COUNT|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_OTHER),
-EXCMD(CMD_scriptencoding, "scriptencoding", ex_scriptencoding, EX_WORD1|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_scriptversion, "scriptversion", ex_scriptversion, EX_WORD1|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_scscope,      "scscope",      ex_scscope, EX_EXTRA|EX_NOTRLCOM, ADDR_NONE),
-EXCMD(CMD_set,          "set",          ex_set, EX_BANG|EX_TRLBAR|EX_EXTRA|EX_CMDWIN|EX_LOCK_OK|EX_SBOXOK, ADDR_NONE),
-EXCMD(CMD_setfiletype,  "setfiletype",  ex_setfiletype, EX_TRLBAR|EX_EXTRA|EX_NEEDARG|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_setglobal,    "setglobal",    ex_set, EX_BANG|EX_TRLBAR|EX_EXTRA|EX_CMDWIN|EX_LOCK_OK|EX_SBOXOK, ADDR_NONE),
-EXCMD(CMD_setlocal,     "setlocal",     ex_set, EX_BANG|EX_TRLBAR|EX_EXTRA|EX_CMDWIN|EX_LOCK_OK|EX_SBOXOK, ADDR_NONE),
-EXCMD(CMD_sfind,        "sfind",        ex_splitview, EX_BANG|EX_FILE1|EX_RANGE|EX_CMDARG|EX_ARGOPT|EX_TRLBAR|EX_NEEDARG, ADDR_OTHER),
-EXCMD(CMD_sfirst,       "sfirst",       ex_rewind, EX_EXTRA|EX_BANG|EX_CMDARG|EX_ARGOPT|EX_TRLBAR, ADDR_NONE),
-EXCMD(CMD_shell,        "shell",        ex_shell, EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_simalt,       "simalt",       ex_simalt, EX_NEEDARG|EX_WORD1|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_sign,         "sign",         ex_sign, EX_NEEDARG|EX_RANGE|EX_EXTRA|EX_CMDWIN|EX_LOCK_OK, ADDR_OTHER),
-EXCMD(CMD_silent,       "silent",       ex_wrongmodifier, EX_NEEDARG|EX_EXTRA|EX_BANG|EX_NOTRLCOM|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_sleep,        "sleep",        ex_sleep, EX_BANG|EX_RANGE|EX_COUNT|EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_OTHER),
-EXCMD(CMD_slast,        "slast",        ex_last, EX_EXTRA|EX_BANG|EX_CMDARG|EX_ARGOPT|EX_TRLBAR, ADDR_NONE),
-EXCMD(CMD_smagic,       "smagic",       ex_submagic, EX_RANGE|EX_WHOLEFOLD|EX_EXTRA|EX_CMDWIN|EX_LOCK_OK|EX_NONWHITE_OK, ADDR_LINES),
-EXCMD(CMD_smap,         "smap",         ex_map, EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_smapclear,    "smapclear",    ex_mapclear, EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_smenu,        "smenu",        ex_menu, EX_RANGE|EX_ZEROR|EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_OTHER),
-EXCMD(CMD_snext,        "snext",        ex_next, EX_RANGE|EX_BANG|EX_FILES|EX_CMDARG|EX_ARGOPT|EX_TRLBAR, ADDR_OTHER),
-EXCMD(CMD_snomagic,     "snomagic",     ex_submagic, EX_RANGE|EX_WHOLEFOLD|EX_EXTRA|EX_CMDWIN|EX_LOCK_OK|EX_NONWHITE_OK, ADDR_LINES),
-EXCMD(CMD_snoremap,     "snoremap",     ex_map, EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_snoremenu,    "snoremenu",    ex_menu, EX_RANGE|EX_ZEROR|EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_OTHER),
-EXCMD(CMD_source,       "source",       ex_source, EX_RANGE|EX_DFLALL|EX_BANG|EX_FILE1|EX_TRLBAR|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK, ADDR_LINES),
-EXCMD(CMD_sort,         "sort",         ex_sort, EX_RANGE|EX_DFLALL|EX_WHOLEFOLD|EX_BANG|EX_EXTRA|EX_NOTRLCOM|EX_MODIFY, ADDR_LINES),
-EXCMD(CMD_split,        "split",        ex_splitview, EX_BANG|EX_FILE1|EX_RANGE|EX_CMDARG|EX_ARGOPT|EX_TRLBAR, ADDR_OTHER),
-EXCMD(CMD_spellgood,    "spellgood",    ex_spell, EX_BANG|EX_RANGE|EX_NEEDARG|EX_EXTRA|EX_TRLBAR, ADDR_OTHER),
-EXCMD(CMD_spelldump,    "spelldump",    ex_spelldump, EX_BANG|EX_TRLBAR, ADDR_NONE),
-EXCMD(CMD_spellinfo,    "spellinfo",    ex_spellinfo, EX_TRLBAR, ADDR_NONE),
-EXCMD(CMD_spellrepall,  "spellrepall",  ex_spellrepall, EX_TRLBAR, ADDR_NONE),
-EXCMD(CMD_spellrare,    "spellrare",    ex_spell, EX_BANG|EX_RANGE|EX_NEEDARG|EX_EXTRA|EX_TRLBAR, ADDR_OTHER),
-EXCMD(CMD_spellundo,    "spellundo",    ex_spell, EX_BANG|EX_RANGE|EX_NEEDARG|EX_EXTRA|EX_TRLBAR, ADDR_OTHER),
-EXCMD(CMD_spellwrong,   "spellwrong",   ex_spell, EX_BANG|EX_RANGE|EX_NEEDARG|EX_EXTRA|EX_TRLBAR, ADDR_OTHER),
-EXCMD(CMD_sprevious,    "sprevious",    ex_previous, EX_EXTRA|EX_RANGE|EX_COUNT|EX_BANG|EX_CMDARG|EX_ARGOPT|EX_TRLBAR, ADDR_OTHER),
-EXCMD(CMD_srewind,      "srewind",      ex_rewind, EX_EXTRA|EX_BANG|EX_CMDARG|EX_ARGOPT|EX_TRLBAR, ADDR_NONE),
-EXCMD(CMD_stop,         "stop",         ex_stop, EX_TRLBAR|EX_BANG|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_stag,         "stag",         ex_stag, EX_RANGE|EX_BANG|EX_WORD1|EX_TRLBAR|EX_ZEROR, ADDR_OTHER),
-EXCMD(CMD_startinsert,  "startinsert",  ex_startinsert, EX_BANG|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_startgreplace, "startgreplace", ex_startinsert, EX_BANG|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_startreplace, "startreplace", ex_startinsert, EX_BANG|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_static,       "static",       ex_wrongmodifier, EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK|EX_WHOLE, ADDR_NONE),
-EXCMD(CMD_stopinsert,   "stopinsert",   ex_stopinsert, EX_BANG|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_stjump,       "stjump",       ex_stag, EX_BANG|EX_TRLBAR|EX_WORD1, ADDR_NONE),
-EXCMD(CMD_stselect,     "stselect",     ex_stag, EX_BANG|EX_TRLBAR|EX_WORD1, ADDR_NONE),
-EXCMD(CMD_sunhide,      "sunhide",      ex_buffer_all, EX_RANGE|EX_COUNT|EX_TRLBAR, ADDR_OTHER),
-EXCMD(CMD_sunmap,       "sunmap",       ex_unmap, EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_sunmenu,      "sunmenu",      ex_menu, EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_suspend,      "suspend",      ex_stop, EX_TRLBAR|EX_BANG|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_sview,        "sview",        ex_splitview, EX_BANG|EX_FILE1|EX_RANGE|EX_CMDARG|EX_ARGOPT|EX_TRLBAR, ADDR_OTHER),
-EXCMD(CMD_swapname,     "swapname",     ex_swapname, EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_syntax,       "syntax",       ex_syntax, EX_EXTRA|EX_NOTRLCOM|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_syntime,      "syntime",      ex_syntime, EX_NEEDARG|EX_WORD1|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_syncbind,     "syncbind",     ex_syncbind, EX_TRLBAR, ADDR_NONE),
-EXCMD(CMD_smile,        "smile",        ex_smile, EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK|EX_SBOXOK, ADDR_NONE),
-EXCMD(CMD_t,            "t",            ex_copymove, EX_RANGE|EX_WHOLEFOLD|EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK|EX_MODIFY, ADDR_LINES),
-EXCMD(CMD_tNext,        "tNext",        ex_tag, EX_RANGE|EX_BANG|EX_TRLBAR|EX_ZEROR, ADDR_OTHER),
-EXCMD(CMD_tag,          "tag",          ex_tag, EX_RANGE|EX_BANG|EX_WORD1|EX_TRLBAR|EX_ZEROR, ADDR_OTHER),
-EXCMD(CMD_tags,         "tags",         do_tags, EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_tab,          "tab",          ex_wrongmodifier, EX_RANGE|EX_ZEROR|EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM, ADDR_TABS),
-EXCMD(CMD_tabclose,     "tabclose",     ex_tabclose, EX_BANG|EX_RANGE|EX_ZEROR|EX_EXTRA|EX_NOSPC|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_TABS),
-EXCMD(CMD_tabdo,        "tabdo",        ex_listdo, EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM|EX_RANGE|EX_DFLALL|EX_EXPAND, ADDR_TABS),
-EXCMD(CMD_tabedit,      "tabedit",      ex_splitview, EX_BANG|EX_FILE1|EX_RANGE|EX_ZEROR|EX_CMDARG|EX_ARGOPT|EX_TRLBAR, ADDR_TABS),
-EXCMD(CMD_tabfind,      "tabfind",      ex_splitview, EX_BANG|EX_FILE1|EX_RANGE|EX_ZEROR|EX_CMDARG|EX_ARGOPT|EX_NEEDARG|EX_TRLBAR, ADDR_TABS),
-EXCMD(CMD_tabfirst,     "tabfirst",     ex_tabnext, EX_TRLBAR, ADDR_NONE),
-EXCMD(CMD_tabmove,      "tabmove",      ex_tabmove, EX_RANGE|EX_ZEROR|EX_EXTRA|EX_NOSPC|EX_TRLBAR, ADDR_TABS),
-EXCMD(CMD_tablast,      "tablast",      ex_tabnext, EX_TRLBAR, ADDR_NONE),
-EXCMD(CMD_tabnext,      "tabnext",      ex_tabnext, EX_RANGE|EX_ZEROR|EX_EXTRA|EX_NOSPC|EX_TRLBAR, ADDR_TABS),
-EXCMD(CMD_tabnew,       "tabnew",       ex_splitview, EX_BANG|EX_FILE1|EX_RANGE|EX_ZEROR|EX_CMDARG|EX_ARGOPT|EX_TRLBAR, ADDR_TABS),
-EXCMD(CMD_tabonly,      "tabonly",      ex_tabonly, EX_BANG|EX_RANGE|EX_ZEROR|EX_EXTRA|EX_NOSPC|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_TABS),
-EXCMD(CMD_tabprevious,  "tabprevious",  ex_tabnext, EX_RANGE|EX_ZEROR|EX_EXTRA|EX_NOSPC|EX_TRLBAR, ADDR_TABS_RELATIVE),
-EXCMD(CMD_tabNext,      "tabNext",      ex_tabnext, EX_RANGE|EX_ZEROR|EX_EXTRA|EX_NOSPC|EX_TRLBAR, ADDR_TABS_RELATIVE),
-EXCMD(CMD_tabrewind,    "tabrewind",    ex_tabnext, EX_TRLBAR, ADDR_NONE),
-EXCMD(CMD_tabs,         "tabs",         ex_tabs, EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_tcd,          "tcd",          ex_cd, EX_BANG|EX_FILE1|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_tchdir,       "tchdir",       ex_cd, EX_BANG|EX_FILE1|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_tcl,          "tcl",          ex_tcl, EX_RANGE|EX_EXTRA|EX_NEEDARG|EX_CMDWIN|EX_LOCK_OK|EX_RESTRICT, ADDR_LINES),
-EXCMD(CMD_tcldo,        "tcldo",        ex_tcldo, EX_RANGE|EX_DFLALL|EX_EXTRA|EX_NEEDARG|EX_CMDWIN|EX_LOCK_OK|EX_RESTRICT, ADDR_LINES),
-EXCMD(CMD_tclfile,      "tclfile",      ex_tclfile, EX_RANGE|EX_FILE1|EX_NEEDARG|EX_CMDWIN|EX_LOCK_OK|EX_RESTRICT, ADDR_LINES),
-EXCMD(CMD_tearoff,      "tearoff",      ex_tearoff, EX_NEEDARG|EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_terminal,     "terminal",     ex_terminal, EX_RANGE|EX_BANG|EX_FILES|EX_CMDWIN|EX_LOCK_OK, ADDR_LINES),
-EXCMD(CMD_tfirst,       "tfirst",       ex_tag, EX_RANGE|EX_BANG|EX_TRLBAR|EX_ZEROR, ADDR_OTHER),
-EXCMD(CMD_throw,        "throw",        ex_throw, EX_EXTRA|EX_NEEDARG|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK|EX_WHOLE, ADDR_NONE),
-EXCMD(CMD_this,         "this",         ex_wrongmodifier, EX_EXTRA|EX_NEEDARG|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK|EX_WHOLE, ADDR_NONE),
-EXCMD(CMD_tjump,        "tjump",        ex_tag, EX_BANG|EX_TRLBAR|EX_WORD1, ADDR_NONE),
-EXCMD(CMD_tlast,        "tlast",        ex_tag, EX_BANG|EX_TRLBAR, ADDR_NONE),
-EXCMD(CMD_tlmenu,       "tlmenu",       ex_menu, EX_RANGE|EX_ZEROR|EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_OTHER),
-EXCMD(CMD_tlnoremenu,   "tlnoremenu",   ex_menu, EX_RANGE|EX_ZEROR|EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_OTHER),
-EXCMD(CMD_tlunmenu,     "tlunmenu",     ex_menu, EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_tmenu,        "tmenu",        ex_menu, EX_RANGE|EX_ZEROR|EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_OTHER),
-EXCMD(CMD_tmap,         "tmap",         ex_map, EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_tmapclear,    "tmapclear",    ex_mapclear, EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_tnext,        "tnext",        ex_tag, EX_RANGE|EX_BANG|EX_TRLBAR|EX_ZEROR, ADDR_OTHER),
-EXCMD(CMD_tnoremap,     "tnoremap",     ex_map, EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_topleft,      "topleft",      ex_wrongmodifier, EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM, ADDR_NONE),
-EXCMD(CMD_tprevious,    "tprevious",    ex_tag, EX_RANGE|EX_BANG|EX_TRLBAR|EX_ZEROR, ADDR_OTHER),
-EXCMD(CMD_trewind,      "trewind",      ex_tag, EX_RANGE|EX_BANG|EX_TRLBAR|EX_ZEROR, ADDR_OTHER),
-EXCMD(CMD_try,          "try",          ex_try, EX_TRLBAR|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_tselect,      "tselect",      ex_tag, EX_BANG|EX_TRLBAR|EX_WORD1, ADDR_NONE),
-EXCMD(CMD_tunmenu,      "tunmenu",      ex_menu, EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_tunmap,       "tunmap",       ex_unmap, EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_type,         "type",         ex_type, EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK|EX_WHOLE|EX_EXPORT, ADDR_NONE),
-EXCMD(CMD_undo,         "undo",         ex_undo, EX_RANGE|EX_COUNT|EX_ZEROR|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_OTHER),
-EXCMD(CMD_undojoin,     "undojoin",     ex_undojoin, EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_undolist,     "undolist",     ex_undolist, EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_unabbreviate, "unabbreviate", ex_abbreviate, EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_unhide,       "unhide",       ex_buffer_all, EX_RANGE|EX_COUNT|EX_TRLBAR, ADDR_OTHER),
-EXCMD(CMD_uniq,         "uniq",         ex_uniq, EX_RANGE|EX_DFLALL|EX_WHOLEFOLD|EX_BANG|EX_EXTRA|EX_NOTRLCOM|EX_MODIFY, ADDR_LINES),
-EXCMD(CMD_unlet,        "unlet",        ex_unlet, EX_BANG|EX_EXTRA|EX_NEEDARG|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_unlockvar,    "unlockvar",    ex_lockvar, EX_BANG|EX_EXTRA|EX_NEEDARG|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_unmap,        "unmap",        ex_unmap, EX_BANG|EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_unmenu,       "unmenu",       ex_menu, EX_BANG|EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_unsilent,     "unsilent",     ex_wrongmodifier, EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_update,       "update",       ex_update, EX_RANGE|EX_WHOLEFOLD|EX_BANG|EX_FILE1|EX_ARGOPT|EX_DFLALL|EX_TRLBAR, ADDR_LINES),
-EXCMD(CMD_vglobal,      "vglobal",      ex_global, EX_RANGE|EX_WHOLEFOLD|EX_EXTRA|EX_DFLALL|EX_CMDWIN|EX_LOCK_OK|EX_NONWHITE_OK, ADDR_LINES),
-EXCMD(CMD_var,          "var",          ex_var, EX_EXTRA|EX_NOTRLCOM|EX_EXPR_ARG|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK|EX_WHOLE|EX_EXPORT, ADDR_NONE),
-EXCMD(CMD_version,      "version",      ex_version, EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_verbose,      "verbose",      ex_wrongmodifier, EX_NEEDARG|EX_RANGE|EX_EXTRA|EX_NOTRLCOM|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK, ADDR_OTHER),
-EXCMD(CMD_vertical,     "vertical",     ex_wrongmodifier, EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM, ADDR_NONE),
-EXCMD(CMD_visual,       "visual",       ex_edit, EX_BANG|EX_FILE1|EX_CMDARG|EX_ARGOPT|EX_TRLBAR, ADDR_NONE),
-EXCMD(CMD_view,         "view",         ex_edit, EX_BANG|EX_FILE1|EX_CMDARG|EX_ARGOPT|EX_TRLBAR, ADDR_NONE),
-EXCMD(CMD_vimgrep,      "vimgrep",      ex_vimgrep, EX_RANGE|EX_BANG|EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM|EX_TRLBAR|EX_XFILE|EX_LOCK_OK, ADDR_OTHER),
-EXCMD(CMD_vimgrepadd,   "vimgrepadd",   ex_vimgrep, EX_RANGE|EX_BANG|EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM|EX_TRLBAR|EX_XFILE|EX_LOCK_OK, ADDR_OTHER),
-EXCMD(CMD_vim9cmd,      "vim9cmd",      ex_wrongmodifier, EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_vim9script,   "vim9script",   ex_vim9script, EX_WORD1|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_viusage,      "viusage",      ex_viusage, EX_TRLBAR, ADDR_NONE),
-EXCMD(CMD_vmap,         "vmap",         ex_map, EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_vmapclear,    "vmapclear",    ex_mapclear, EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_vmenu,        "vmenu",        ex_menu, EX_RANGE|EX_ZEROR|EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_OTHER),
-EXCMD(CMD_vnoremap,     "vnoremap",     ex_map, EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_vnew,         "vnew",         ex_splitview, EX_BANG|EX_FILE1|EX_RANGE|EX_CMDARG|EX_ARGOPT|EX_TRLBAR, ADDR_OTHER),
-EXCMD(CMD_vnoremenu,    "vnoremenu",    ex_menu, EX_RANGE|EX_ZEROR|EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_OTHER),
-EXCMD(CMD_vsplit,       "vsplit",       ex_splitview, EX_BANG|EX_FILE1|EX_RANGE|EX_CMDARG|EX_ARGOPT|EX_TRLBAR, ADDR_OTHER),
-EXCMD(CMD_vunmap,       "vunmap",       ex_unmap, EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_vunmenu,      "vunmenu",      ex_menu, EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_write,        "write",        ex_write, EX_RANGE|EX_WHOLEFOLD|EX_BANG|EX_FILE1|EX_ARGOPT|EX_DFLALL|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_LINES),
-EXCMD(CMD_wNext,        "wNext",        ex_wnext, EX_RANGE|EX_WHOLEFOLD|EX_BANG|EX_FILE1|EX_ARGOPT|EX_TRLBAR, ADDR_OTHER),
-EXCMD(CMD_wall,         "wall",         do_wqall, EX_BANG|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_while,        "while",        ex_while, EX_EXTRA|EX_NOTRLCOM|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK|EX_WHOLE, ADDR_NONE),
-EXCMD(CMD_winsize,      "winsize",      ex_winsize, EX_EXTRA|EX_NEEDARG|EX_TRLBAR, ADDR_NONE),
-EXCMD(CMD_wincmd,       "wincmd",       ex_wincmd, EX_NEEDARG|EX_WORD1|EX_RANGE|EX_CMDWIN|EX_LOCK_OK, ADDR_OTHER),
-EXCMD(CMD_windo,        "windo",        ex_listdo, EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM|EX_RANGE|EX_DFLALL|EX_EXPAND, ADDR_WINDOWS),
-EXCMD(CMD_winpos,       "winpos",       ex_winpos, EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_wlrestore,    "wlrestore",    ex_wlrestore, EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK|EX_BANG, ADDR_NONE),
-EXCMD(CMD_wnext,        "wnext",        ex_wnext, EX_RANGE|EX_BANG|EX_FILE1|EX_ARGOPT|EX_TRLBAR, ADDR_OTHER),
-EXCMD(CMD_wprevious,    "wprevious",    ex_wnext, EX_RANGE|EX_BANG|EX_FILE1|EX_ARGOPT|EX_TRLBAR, ADDR_OTHER),
-EXCMD(CMD_wq,           "wq",           ex_exit, EX_RANGE|EX_WHOLEFOLD|EX_BANG|EX_FILE1|EX_ARGOPT|EX_DFLALL|EX_TRLBAR, ADDR_LINES),
-EXCMD(CMD_wqall,        "wqall",        do_wqall, EX_BANG|EX_FILE1|EX_ARGOPT|EX_TRLBAR, ADDR_NONE),
-EXCMD(CMD_wundo,        "wundo",        ex_wundo, EX_BANG|EX_NEEDARG|EX_FILE1, ADDR_NONE),
-EXCMD(CMD_wviminfo,     "wviminfo",     ex_viminfo, EX_BANG|EX_FILE1|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_xit,          "xit",          ex_exit, EX_RANGE|EX_WHOLEFOLD|EX_BANG|EX_FILE1|EX_ARGOPT|EX_DFLALL|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_LINES),
-EXCMD(CMD_xall,         "xall",         do_wqall, EX_BANG|EX_TRLBAR, ADDR_NONE),
-EXCMD(CMD_xmap,         "xmap",         ex_map, EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_xmapclear,    "xmapclear",    ex_mapclear, EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_xmenu,        "xmenu",        ex_menu, EX_RANGE|EX_ZEROR|EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_OTHER),
-EXCMD(CMD_xnoremap,     "xnoremap",     ex_map, EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_xnoremenu,    "xnoremenu",    ex_menu, EX_RANGE|EX_ZEROR|EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_OTHER),
-EXCMD(CMD_xrestore,     "xrestore",     ex_xrestore, EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_xunmap,       "xunmap",       ex_unmap, EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_xunmenu,      "xunmenu",      ex_menu, EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_yank,         "yank",         ex_operators, EX_RANGE|EX_WHOLEFOLD|EX_REGSTR|EX_COUNT|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_LINES),
-EXCMD(CMD_z,            "z",            ex_z, EX_RANGE|EX_WHOLEFOLD|EX_BANG|EX_EXTRA|EX_FLAGS|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_LINES),
-
-EXCMD(CMD_bang,         "!",            ex_bang, EX_RANGE|EX_WHOLEFOLD|EX_BANG|EX_FILES|EX_CMDWIN|EX_LOCK_OK|EX_NONWHITE_OK, ADDR_LINES),
-EXCMD(CMD_pound,        "#",            ex_print, EX_RANGE|EX_WHOLEFOLD|EX_COUNT|EX_FLAGS|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_LINES),
-EXCMD(CMD_and,          "&",            ex_substitute, EX_RANGE|EX_WHOLEFOLD|EX_EXTRA|EX_CMDWIN|EX_LOCK_OK|EX_MODIFY|EX_NONWHITE_OK, ADDR_LINES),
-EXCMD(CMD_star,         "*",            ex_at, EX_RANGE|EX_WHOLEFOLD|EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK|EX_NONWHITE_OK, ADDR_LINES),
-EXCMD(CMD_lshift,       "<",            ex_operators, EX_RANGE|EX_WHOLEFOLD|EX_COUNT|EX_FLAGS|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK|EX_MODIFY, ADDR_LINES),
-EXCMD(CMD_equal,        "=",            ex_equal, EX_RANGE|EX_TRLBAR|EX_DFLALL|EX_FLAGS|EX_CMDWIN|EX_LOCK_OK, ADDR_LINES),
-EXCMD(CMD_rshift,       ">",            ex_operators, EX_RANGE|EX_WHOLEFOLD|EX_COUNT|EX_FLAGS|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK|EX_MODIFY, ADDR_LINES),
-EXCMD(CMD_at,           "@",            ex_at, EX_RANGE|EX_WHOLEFOLD|EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK|EX_NONWHITE_OK, ADDR_LINES),
-EXCMD(CMD_block,        "{",            ex_block, EX_TRLBAR|EX_LOCK_OK|EX_CMDWIN, ADDR_NONE),
-EXCMD(CMD_endblock,     "}",            ex_endblock, EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_tilde,        "~",            ex_substitute, EX_RANGE|EX_WHOLEFOLD|EX_EXTRA|EX_CMDWIN|EX_LOCK_OK|EX_MODIFY|EX_NONWHITE_OK, ADDR_LINES),
-
-EXCMD(CMD_Next,         "Next",         ex_previous, EX_EXTRA|EX_RANGE|EX_COUNT|EX_BANG|EX_CMDARG|EX_ARGOPT|EX_TRLBAR, ADDR_OTHER),
-EXCMD(CMD_Print,        "Print",        ex_print, EX_RANGE|EX_WHOLEFOLD|EX_COUNT|EX_FLAGS|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK, ADDR_LINES),
-EXCMD(CMD_X,            "X",            ex_X, EX_TRLBAR, ADDR_NONE),
-
-EXCMD(CMD_increment,    "++",           ex_incdec, EX_EXTRA|EX_TRLBAR|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
-EXCMD(CMD_decrement,    "--",           ex_incdec, EX_EXTRA|EX_TRLBAR|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK, ADDR_NONE),
+    [CMD_append] = {(char_u *)"append", sizeof("append") - 1, ex_append, (long_u)(EX_BANG|EX_RANGE|EX_ZEROR|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK|EX_MODIFY), ADDR_LINES},
+    [CMD_abbreviate] = {(char_u *)"abbreviate", sizeof("abbreviate") - 1, ex_abbreviate, (long_u)(EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_abclear] = {(char_u *)"abclear", sizeof("abclear") - 1, ex_abclear, (long_u)(EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_aboveleft] = {(char_u *)"aboveleft", sizeof("aboveleft") - 1, ex_wrongmodifier, (long_u)(EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM), ADDR_NONE},
+    [CMD_abstract] = {(char_u *)"abstract", sizeof("abstract") - 1,  ex_ni , (long_u)(EX_EXTRA|EX_CMDWIN|EX_LOCK_OK|EX_WHOLE|EX_EXPORT), ADDR_NONE},
+    [CMD_all] = {(char_u *)"all", sizeof("all") - 1, ex_all, (long_u)(EX_BANG|EX_RANGE|EX_COUNT|EX_TRLBAR), ADDR_OTHER},
+    [CMD_amenu] = {(char_u *)"amenu", sizeof("amenu") - 1,  ex_ni , (long_u)(EX_RANGE|EX_ZEROR|EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK), ADDR_OTHER},
+    [CMD_anoremenu] = {(char_u *)"anoremenu", sizeof("anoremenu") - 1,  ex_ni , (long_u)(EX_RANGE|EX_ZEROR|EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK), ADDR_OTHER},
+    [CMD_args] = {(char_u *)"args", sizeof("args") - 1, ex_args, (long_u)(EX_BANG| (EX_XFILE | EX_EXTRA) |EX_CMDARG|EX_ARGOPT|EX_TRLBAR), ADDR_NONE},
+    [CMD_argadd] = {(char_u *)"argadd", sizeof("argadd") - 1, ex_argadd, (long_u)(EX_BANG|EX_RANGE|EX_ZEROR| (EX_XFILE | EX_EXTRA) |EX_TRLBAR), ADDR_ARGUMENTS},
+    [CMD_argdelete] = {(char_u *)"argdelete", sizeof("argdelete") - 1, ex_argdelete, (long_u)(EX_BANG|EX_RANGE| (EX_XFILE | EX_EXTRA) |EX_TRLBAR), ADDR_ARGUMENTS},
+    [CMD_argdo] = {(char_u *)"argdo", sizeof("argdo") - 1, ex_listdo, (long_u)(EX_BANG|EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM|EX_RANGE|EX_DFLALL|EX_EXPAND), ADDR_ARGUMENTS},
+    [CMD_argdedupe] = {(char_u *)"argdedupe", sizeof("argdedupe") - 1, ex_argdedupe, (long_u)(EX_TRLBAR), ADDR_NONE},
+    [CMD_argedit] = {(char_u *)"argedit", sizeof("argedit") - 1, ex_argedit, (long_u)(EX_BANG|EX_NEEDARG|EX_RANGE|EX_ZEROR| (EX_XFILE | EX_EXTRA) |EX_CMDARG|EX_ARGOPT|EX_TRLBAR), ADDR_ARGUMENTS},
+    [CMD_argglobal] = {(char_u *)"argglobal", sizeof("argglobal") - 1, ex_args, (long_u)(EX_BANG| (EX_XFILE | EX_EXTRA) |EX_CMDARG|EX_ARGOPT|EX_TRLBAR), ADDR_NONE},
+    [CMD_arglocal] = {(char_u *)"arglocal", sizeof("arglocal") - 1, ex_args, (long_u)(EX_BANG| (EX_XFILE | EX_EXTRA) |EX_CMDARG|EX_ARGOPT|EX_TRLBAR), ADDR_NONE},
+    [CMD_argument] = {(char_u *)"argument", sizeof("argument") - 1, ex_argument, (long_u)(EX_BANG|EX_RANGE|EX_COUNT|EX_EXTRA|EX_CMDARG|EX_ARGOPT|EX_TRLBAR), ADDR_ARGUMENTS},
+    [CMD_ascii] = {(char_u *)"ascii", sizeof("ascii") - 1, do_ascii, (long_u)(EX_TRLBAR|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_autocmd] = {(char_u *)"autocmd", sizeof("autocmd") - 1, ex_autocmd, (long_u)(EX_BANG|EX_EXTRA|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_augroup] = {(char_u *)"augroup", sizeof("augroup") - 1, ex_autocmd, (long_u)(EX_BANG| (EX_EXTRA | EX_NOSPC) |EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_aunmenu] = {(char_u *)"aunmenu", sizeof("aunmenu") - 1,  ex_ni , (long_u)(EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_buffer] = {(char_u *)"buffer", sizeof("buffer") - 1, ex_buffer, (long_u)(EX_BANG|EX_RANGE|EX_BUFNAME|EX_BUFUNL|EX_COUNT|EX_EXTRA|EX_CMDARG|EX_TRLBAR), ADDR_BUFFERS},
+    [CMD_bNext] = {(char_u *)"bNext", sizeof("bNext") - 1, ex_bprevious, (long_u)(EX_BANG|EX_RANGE|EX_COUNT|EX_CMDARG|EX_TRLBAR), ADDR_OTHER},
+    [CMD_ball] = {(char_u *)"ball", sizeof("ball") - 1, ex_buffer_all, (long_u)(EX_RANGE|EX_COUNT|EX_TRLBAR), ADDR_OTHER},
+    [CMD_badd] = {(char_u *)"badd", sizeof("badd") - 1, ex_edit, (long_u)(EX_NEEDARG| ( (EX_XFILE | EX_EXTRA)  | EX_NOSPC) |EX_CMDARG|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_balt] = {(char_u *)"balt", sizeof("balt") - 1, ex_edit, (long_u)(EX_NEEDARG| ( (EX_XFILE | EX_EXTRA)  | EX_NOSPC) |EX_CMDARG|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_bdelete] = {(char_u *)"bdelete", sizeof("bdelete") - 1, ex_bunload, (long_u)(EX_BANG|EX_RANGE|EX_BUFNAME|EX_COUNT|EX_EXTRA|EX_TRLBAR), ADDR_BUFFERS},
+    [CMD_behave] = {(char_u *)"behave", sizeof("behave") - 1, ex_behave, (long_u)(EX_BANG|EX_NEEDARG| (EX_EXTRA | EX_NOSPC) |EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_belowright] = {(char_u *)"belowright", sizeof("belowright") - 1, ex_wrongmodifier, (long_u)(EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM), ADDR_NONE},
+    [CMD_bfirst] = {(char_u *)"bfirst", sizeof("bfirst") - 1, ex_brewind, (long_u)(EX_BANG|EX_RANGE|EX_CMDARG|EX_TRLBAR), ADDR_OTHER},
+    [CMD_blast] = {(char_u *)"blast", sizeof("blast") - 1, ex_blast, (long_u)(EX_BANG|EX_RANGE|EX_CMDARG|EX_TRLBAR), ADDR_OTHER},
+    [CMD_bmodified] = {(char_u *)"bmodified", sizeof("bmodified") - 1, ex_bmodified, (long_u)(EX_BANG|EX_RANGE|EX_COUNT|EX_CMDARG|EX_TRLBAR), ADDR_OTHER},
+    [CMD_bnext] = {(char_u *)"bnext", sizeof("bnext") - 1, ex_bnext, (long_u)(EX_BANG|EX_RANGE|EX_COUNT|EX_CMDARG|EX_TRLBAR), ADDR_OTHER},
+    [CMD_botright] = {(char_u *)"botright", sizeof("botright") - 1, ex_wrongmodifier, (long_u)(EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM), ADDR_NONE},
+    [CMD_bprevious] = {(char_u *)"bprevious", sizeof("bprevious") - 1, ex_bprevious, (long_u)(EX_BANG|EX_RANGE|EX_COUNT|EX_CMDARG|EX_TRLBAR), ADDR_OTHER},
+    [CMD_brewind] = {(char_u *)"brewind", sizeof("brewind") - 1, ex_brewind, (long_u)(EX_BANG|EX_RANGE|EX_CMDARG|EX_TRLBAR), ADDR_OTHER},
+    [CMD_break] = {(char_u *)"break", sizeof("break") - 1,  ex_ni , (long_u)(EX_TRLBAR|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK|EX_WHOLE), ADDR_NONE},
+    [CMD_breakadd] = {(char_u *)"breakadd", sizeof("breakadd") - 1,  ex_ni , (long_u)(EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_breakdel] = {(char_u *)"breakdel", sizeof("breakdel") - 1,  ex_ni , (long_u)(EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_breaklist] = {(char_u *)"breaklist", sizeof("breaklist") - 1,  ex_ni , (long_u)(EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_browse] = {(char_u *)"browse", sizeof("browse") - 1, ex_wrongmodifier, (long_u)(EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_buffers] = {(char_u *)"buffers", sizeof("buffers") - 1, buflist_list, (long_u)(EX_BANG|EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_bufdo] = {(char_u *)"bufdo", sizeof("bufdo") - 1, ex_listdo, (long_u)(EX_BANG|EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM|EX_RANGE|EX_DFLALL|EX_EXPAND), ADDR_BUFFERS},
+    [CMD_bunload] = {(char_u *)"bunload", sizeof("bunload") - 1, ex_bunload, (long_u)(EX_BANG|EX_RANGE|EX_BUFNAME|EX_COUNT|EX_EXTRA|EX_TRLBAR), ADDR_LOADED_BUFFERS},
+    [CMD_bwipeout] = {(char_u *)"bwipeout", sizeof("bwipeout") - 1, ex_bunload, (long_u)(EX_BANG|EX_RANGE|EX_BUFNAME|EX_BUFUNL|EX_COUNT|EX_EXTRA|EX_TRLBAR), ADDR_BUFFERS},
+    [CMD_change] = {(char_u *)"change", sizeof("change") - 1, ex_change, (long_u)(EX_BANG|EX_WHOLEFOLD|EX_RANGE|EX_COUNT|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK|EX_MODIFY), ADDR_LINES},
+    [CMD_cNext] = {(char_u *)"cNext", sizeof("cNext") - 1,  ex_ni , (long_u)(EX_RANGE|EX_COUNT|EX_TRLBAR|EX_BANG), ADDR_UNSIGNED},
+    [CMD_cNfile] = {(char_u *)"cNfile", sizeof("cNfile") - 1,  ex_ni , (long_u)(EX_RANGE|EX_COUNT|EX_TRLBAR|EX_BANG), ADDR_UNSIGNED},
+    [CMD_cabbrev] = {(char_u *)"cabbrev", sizeof("cabbrev") - 1, ex_abbreviate, (long_u)(EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_cabclear] = {(char_u *)"cabclear", sizeof("cabclear") - 1, ex_abclear, (long_u)(EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_cabove] = {(char_u *)"cabove", sizeof("cabove") - 1,  ex_ni , (long_u)(EX_RANGE|EX_COUNT|EX_TRLBAR), ADDR_UNSIGNED},
+    [CMD_caddbuffer] = {(char_u *)"caddbuffer", sizeof("caddbuffer") - 1,  ex_ni , (long_u)(EX_RANGE| (EX_EXTRA | EX_NOSPC) |EX_TRLBAR), ADDR_LINES},
+    [CMD_caddexpr] = {(char_u *)"caddexpr", sizeof("caddexpr") - 1,  ex_ni , (long_u)(EX_NEEDARG| (EX_EXTRA | EX_NOSPC) |EX_NOTRLCOM|EX_EXPR_ARG), ADDR_NONE},
+    [CMD_caddfile] = {(char_u *)"caddfile", sizeof("caddfile") - 1,  ex_ni , (long_u)(EX_TRLBAR| ( (EX_XFILE | EX_EXTRA)  | EX_NOSPC) ), ADDR_NONE},
+    [CMD_cafter] = {(char_u *)"cafter", sizeof("cafter") - 1,  ex_ni , (long_u)(EX_RANGE|EX_COUNT|EX_TRLBAR), ADDR_UNSIGNED},
+    [CMD_call] = {(char_u *)"call", sizeof("call") - 1,  ex_ni , (long_u)(EX_RANGE|EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM|EX_EXPR_ARG|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK), ADDR_LINES},
+    [CMD_catch] = {(char_u *)"catch", sizeof("catch") - 1,  ex_ni , (long_u)(EX_EXTRA|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK|EX_WHOLE), ADDR_NONE},
+    [CMD_cbuffer] = {(char_u *)"cbuffer", sizeof("cbuffer") - 1,  ex_ni , (long_u)(EX_BANG|EX_RANGE| (EX_EXTRA | EX_NOSPC) |EX_TRLBAR), ADDR_LINES},
+    [CMD_cbefore] = {(char_u *)"cbefore", sizeof("cbefore") - 1,  ex_ni , (long_u)(EX_RANGE|EX_COUNT|EX_TRLBAR), ADDR_UNSIGNED},
+    [CMD_cbelow] = {(char_u *)"cbelow", sizeof("cbelow") - 1,  ex_ni , (long_u)(EX_RANGE|EX_COUNT|EX_TRLBAR), ADDR_UNSIGNED},
+    [CMD_cbottom] = {(char_u *)"cbottom", sizeof("cbottom") - 1,  ex_ni , (long_u)(EX_TRLBAR), ADDR_NONE},
+    [CMD_cc] = {(char_u *)"cc", sizeof("cc") - 1,  ex_ni , (long_u)(EX_RANGE|EX_COUNT|EX_TRLBAR|EX_BANG), ADDR_QUICKFIX},
+    [CMD_cclose] = {(char_u *)"cclose", sizeof("cclose") - 1,  ex_ni , (long_u)(EX_TRLBAR), ADDR_NONE},
+    [CMD_cd] = {(char_u *)"cd", sizeof("cd") - 1, ex_cd, (long_u)(EX_BANG| ( (EX_XFILE | EX_EXTRA)  | EX_NOSPC) |EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_cdo] = {(char_u *)"cdo", sizeof("cdo") - 1, ex_listdo, (long_u)(EX_BANG|EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM|EX_RANGE|EX_DFLALL|EX_EXPAND), ADDR_QUICKFIX_VALID},
+    [CMD_center] = {(char_u *)"center", sizeof("center") - 1, ex_align, (long_u)(EX_TRLBAR|EX_RANGE|EX_WHOLEFOLD|EX_EXTRA|EX_CMDWIN|EX_LOCK_OK|EX_MODIFY), ADDR_LINES},
+    [CMD_cexpr] = {(char_u *)"cexpr", sizeof("cexpr") - 1,  ex_ni , (long_u)(EX_NEEDARG| (EX_EXTRA | EX_NOSPC) |EX_NOTRLCOM|EX_EXPR_ARG|EX_BANG), ADDR_NONE},
+    [CMD_cfile] = {(char_u *)"cfile", sizeof("cfile") - 1,  ex_ni , (long_u)(EX_TRLBAR| ( (EX_XFILE | EX_EXTRA)  | EX_NOSPC) |EX_BANG), ADDR_NONE},
+    [CMD_cfdo] = {(char_u *)"cfdo", sizeof("cfdo") - 1, ex_listdo, (long_u)(EX_BANG|EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM|EX_RANGE|EX_DFLALL|EX_EXPAND), ADDR_QUICKFIX_VALID},
+    [CMD_cfirst] = {(char_u *)"cfirst", sizeof("cfirst") - 1,  ex_ni , (long_u)(EX_RANGE|EX_COUNT|EX_TRLBAR|EX_BANG), ADDR_UNSIGNED},
+    [CMD_cgetfile] = {(char_u *)"cgetfile", sizeof("cgetfile") - 1,  ex_ni , (long_u)(EX_TRLBAR| ( (EX_XFILE | EX_EXTRA)  | EX_NOSPC) ), ADDR_NONE},
+    [CMD_cgetbuffer] = {(char_u *)"cgetbuffer", sizeof("cgetbuffer") - 1,  ex_ni , (long_u)(EX_RANGE| (EX_EXTRA | EX_NOSPC) |EX_TRLBAR), ADDR_LINES},
+    [CMD_cgetexpr] = {(char_u *)"cgetexpr", sizeof("cgetexpr") - 1,  ex_ni , (long_u)(EX_NEEDARG| (EX_EXTRA | EX_NOSPC) |EX_NOTRLCOM|EX_EXPR_ARG), ADDR_NONE},
+    [CMD_chdir] = {(char_u *)"chdir", sizeof("chdir") - 1, ex_cd, (long_u)(EX_BANG| ( (EX_XFILE | EX_EXTRA)  | EX_NOSPC) |EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_changes] = {(char_u *)"changes", sizeof("changes") - 1, ex_changes, (long_u)(EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_checkpath] = {(char_u *)"checkpath", sizeof("checkpath") - 1,  ex_ni , (long_u)(EX_TRLBAR|EX_BANG|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_checktime] = {(char_u *)"checktime", sizeof("checktime") - 1, ex_checktime, (long_u)(EX_RANGE|EX_BUFNAME|EX_COUNT|EX_EXTRA|EX_TRLBAR), ADDR_OTHER},
+    [CMD_chistory] = {(char_u *)"chistory", sizeof("chistory") - 1,  ex_ni , (long_u)(EX_RANGE|EX_COUNT|EX_TRLBAR), ADDR_UNSIGNED},
+    [CMD_clist] = {(char_u *)"clist", sizeof("clist") - 1,  ex_ni , (long_u)(EX_BANG|EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_clipreset] = {(char_u *)"clipreset", sizeof("clipreset") - 1,  ex_ni , (long_u)(EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_clast] = {(char_u *)"clast", sizeof("clast") - 1,  ex_ni , (long_u)(EX_RANGE|EX_COUNT|EX_TRLBAR|EX_BANG), ADDR_UNSIGNED},
+    [CMD_class] = {(char_u *)"class", sizeof("class") - 1,  ex_ni , (long_u)(EX_EXTRA|EX_CMDWIN|EX_LOCK_OK|EX_WHOLE|EX_EXPORT), ADDR_NONE},
+    [CMD_close] = {(char_u *)"close", sizeof("close") - 1, ex_close, (long_u)(EX_BANG|EX_RANGE|EX_COUNT|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK), ADDR_WINDOWS},
+    [CMD_clearjumps] = {(char_u *)"clearjumps", sizeof("clearjumps") - 1, ex_clearjumps, (long_u)(EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_cmap] = {(char_u *)"cmap", sizeof("cmap") - 1, ex_map, (long_u)(EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_cmapclear] = {(char_u *)"cmapclear", sizeof("cmapclear") - 1, ex_mapclear, (long_u)(EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_cmenu] = {(char_u *)"cmenu", sizeof("cmenu") - 1,  ex_ni , (long_u)(EX_RANGE|EX_ZEROR|EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK), ADDR_OTHER},
+    [CMD_cnext] = {(char_u *)"cnext", sizeof("cnext") - 1,  ex_ni , (long_u)(EX_RANGE|EX_COUNT|EX_TRLBAR|EX_BANG), ADDR_UNSIGNED},
+    [CMD_cnewer] = {(char_u *)"cnewer", sizeof("cnewer") - 1,  ex_ni , (long_u)(EX_RANGE|EX_COUNT|EX_TRLBAR), ADDR_UNSIGNED},
+    [CMD_cnfile] = {(char_u *)"cnfile", sizeof("cnfile") - 1,  ex_ni , (long_u)(EX_RANGE|EX_COUNT|EX_TRLBAR|EX_BANG), ADDR_UNSIGNED},
+    [CMD_cnoremap] = {(char_u *)"cnoremap", sizeof("cnoremap") - 1, ex_map, (long_u)(EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_cnoreabbrev] = {(char_u *)"cnoreabbrev", sizeof("cnoreabbrev") - 1, ex_abbreviate, (long_u)(EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_cnoremenu] = {(char_u *)"cnoremenu", sizeof("cnoremenu") - 1,  ex_ni , (long_u)(EX_RANGE|EX_ZEROR|EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK), ADDR_OTHER},
+    [CMD_copy] = {(char_u *)"copy", sizeof("copy") - 1, ex_copymove, (long_u)(EX_RANGE|EX_WHOLEFOLD|EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK|EX_MODIFY), ADDR_LINES},
+    [CMD_colder] = {(char_u *)"colder", sizeof("colder") - 1,  ex_ni , (long_u)(EX_RANGE|EX_COUNT|EX_TRLBAR), ADDR_UNSIGNED},
+    [CMD_colorscheme] = {(char_u *)"colorscheme", sizeof("colorscheme") - 1, ex_colorscheme, (long_u)( (EX_EXTRA | EX_NOSPC) |EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_command] = {(char_u *)"command", sizeof("command") - 1, ex_command, (long_u)(EX_EXTRA|EX_BANG|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_comclear] = {(char_u *)"comclear", sizeof("comclear") - 1, ex_comclear, (long_u)(EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_compiler] = {(char_u *)"compiler", sizeof("compiler") - 1,  ex_ni , (long_u)(EX_BANG|EX_TRLBAR| (EX_EXTRA | EX_NOSPC) |EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_continue] = {(char_u *)"continue", sizeof("continue") - 1,  ex_ni , (long_u)(EX_TRLBAR|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK|EX_WHOLE), ADDR_NONE},
+    [CMD_confirm] = {(char_u *)"confirm", sizeof("confirm") - 1, ex_wrongmodifier, (long_u)(EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_const] = {(char_u *)"const", sizeof("const") - 1,  ex_ni , (long_u)(EX_EXTRA|EX_BANG|EX_NOTRLCOM|EX_EXPR_ARG|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK|EX_WHOLE|EX_EXPORT), ADDR_NONE},
+    [CMD_copen] = {(char_u *)"copen", sizeof("copen") - 1,  ex_ni , (long_u)(EX_RANGE|EX_COUNT|EX_TRLBAR), ADDR_OTHER},
+    [CMD_cprevious] = {(char_u *)"cprevious", sizeof("cprevious") - 1,  ex_ni , (long_u)(EX_RANGE|EX_COUNT|EX_TRLBAR|EX_BANG), ADDR_UNSIGNED},
+    [CMD_cpfile] = {(char_u *)"cpfile", sizeof("cpfile") - 1,  ex_ni , (long_u)(EX_RANGE|EX_COUNT|EX_TRLBAR|EX_BANG), ADDR_OTHER},
+    [CMD_cquit] = {(char_u *)"cquit", sizeof("cquit") - 1, ex_cquit, (long_u)(EX_RANGE|EX_COUNT|EX_ZEROR|EX_TRLBAR|EX_BANG), ADDR_UNSIGNED},
+    [CMD_crewind] = {(char_u *)"crewind", sizeof("crewind") - 1,  ex_ni , (long_u)(EX_RANGE|EX_COUNT|EX_TRLBAR|EX_BANG), ADDR_UNSIGNED},
+    [CMD_cscope] = {(char_u *)"cscope", sizeof("cscope") - 1,  ex_ni , (long_u)(EX_EXTRA|EX_NOTRLCOM|EX_XFILE), ADDR_NONE},
+    [CMD_cstag] = {(char_u *)"cstag", sizeof("cstag") - 1,  ex_ni , (long_u)(EX_BANG|EX_TRLBAR| (EX_EXTRA | EX_NOSPC) ), ADDR_NONE},
+    [CMD_cunmap] = {(char_u *)"cunmap", sizeof("cunmap") - 1, ex_unmap, (long_u)(EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_cunabbrev] = {(char_u *)"cunabbrev", sizeof("cunabbrev") - 1, ex_abbreviate, (long_u)(EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_cunmenu] = {(char_u *)"cunmenu", sizeof("cunmenu") - 1,  ex_ni , (long_u)(EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_cwindow] = {(char_u *)"cwindow", sizeof("cwindow") - 1,  ex_ni , (long_u)(EX_RANGE|EX_COUNT|EX_TRLBAR), ADDR_OTHER},
+    [CMD_delete] = {(char_u *)"delete", sizeof("delete") - 1, ex_operators, (long_u)(EX_RANGE|EX_WHOLEFOLD|EX_REGSTR|EX_COUNT|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK|EX_MODIFY), ADDR_LINES},
+    [CMD_delmarks] = {(char_u *)"delmarks", sizeof("delmarks") - 1, ex_delmarks, (long_u)(EX_BANG|EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_debug] = {(char_u *)"debug", sizeof("debug") - 1,  ex_ni , (long_u)(EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_debuggreedy] = {(char_u *)"debuggreedy", sizeof("debuggreedy") - 1,  ex_ni , (long_u)(EX_RANGE|EX_ZEROR|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK), ADDR_OTHER},
+    [CMD_def] = {(char_u *)"def", sizeof("def") - 1,  ex_ni , (long_u)(EX_EXTRA|EX_BANG|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK|EX_WHOLE|EX_EXPORT), ADDR_NONE},
+    [CMD_defcompile] = {(char_u *)"defcompile", sizeof("defcompile") - 1,  ex_ni , (long_u)(EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK|EX_TRLBAR|EX_EXTRA), ADDR_NONE},
+    [CMD_defer] = {(char_u *)"defer", sizeof("defer") - 1,  ex_ni , (long_u)(EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM|EX_EXPR_ARG|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_delcommand] = {(char_u *)"delcommand", sizeof("delcommand") - 1, ex_delcommand, (long_u)(EX_NEEDARG| (EX_EXTRA | EX_NOSPC) |EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_delfunction] = {(char_u *)"delfunction", sizeof("delfunction") - 1,  ex_ni , (long_u)(EX_BANG|EX_NEEDARG| (EX_EXTRA | EX_NOSPC) |EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_display] = {(char_u *)"display", sizeof("display") - 1, ex_display, (long_u)(EX_EXTRA|EX_NOTRLCOM|EX_TRLBAR|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_diffupdate] = {(char_u *)"diffupdate", sizeof("diffupdate") - 1,  ex_ni , (long_u)(EX_BANG|EX_TRLBAR), ADDR_NONE},
+    [CMD_diffget] = {(char_u *)"diffget", sizeof("diffget") - 1,  ex_ni , (long_u)(EX_RANGE|EX_ZEROR|EX_EXTRA|EX_TRLBAR|EX_MODIFY), ADDR_LINES},
+    [CMD_diffoff] = {(char_u *)"diffoff", sizeof("diffoff") - 1,  ex_ni , (long_u)(EX_BANG|EX_TRLBAR), ADDR_NONE},
+    [CMD_diffpatch] = {(char_u *)"diffpatch", sizeof("diffpatch") - 1,  ex_ni , (long_u)(EX_EXTRA| ( (EX_XFILE | EX_EXTRA)  | EX_NOSPC) |EX_TRLBAR|EX_MODIFY), ADDR_NONE},
+    [CMD_diffput] = {(char_u *)"diffput", sizeof("diffput") - 1,  ex_ni , (long_u)(EX_RANGE|EX_ZEROR|EX_EXTRA|EX_TRLBAR), ADDR_LINES},
+    [CMD_diffsplit] = {(char_u *)"diffsplit", sizeof("diffsplit") - 1,  ex_ni , (long_u)(EX_EXTRA| ( (EX_XFILE | EX_EXTRA)  | EX_NOSPC) |EX_TRLBAR), ADDR_NONE},
+    [CMD_diffthis] = {(char_u *)"diffthis", sizeof("diffthis") - 1,  ex_ni , (long_u)(EX_TRLBAR), ADDR_NONE},
+    [CMD_digraphs] = {(char_u *)"digraphs", sizeof("digraphs") - 1, ex_digraphs, (long_u)(EX_BANG|EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_disassemble] = {(char_u *)"disassemble", sizeof("disassemble") - 1,  ex_ni , (long_u)(EX_BANG|EX_EXTRA|EX_NEEDARG|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_djump] = {(char_u *)"djump", sizeof("djump") - 1,  ex_ni , (long_u)(EX_BANG|EX_RANGE|EX_DFLALL|EX_WHOLEFOLD|EX_EXTRA), ADDR_LINES},
+    [CMD_dlist] = {(char_u *)"dlist", sizeof("dlist") - 1,  ex_ni , (long_u)(EX_BANG|EX_RANGE|EX_DFLALL|EX_WHOLEFOLD|EX_EXTRA|EX_CMDWIN|EX_LOCK_OK), ADDR_LINES},
+    [CMD_doautocmd] = {(char_u *)"doautocmd", sizeof("doautocmd") - 1, ex_doautocmd, (long_u)(EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_doautoall] = {(char_u *)"doautoall", sizeof("doautoall") - 1, ex_doautoall, (long_u)(EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_drop] = {(char_u *)"drop", sizeof("drop") - 1, ex_drop, (long_u)(EX_BANG| (EX_XFILE | EX_EXTRA) |EX_CMDARG|EX_NEEDARG|EX_ARGOPT|EX_TRLBAR), ADDR_NONE},
+    [CMD_dsearch] = {(char_u *)"dsearch", sizeof("dsearch") - 1,  ex_ni , (long_u)(EX_BANG|EX_RANGE|EX_DFLALL|EX_WHOLEFOLD|EX_EXTRA|EX_CMDWIN|EX_LOCK_OK), ADDR_LINES},
+    [CMD_dsplit] = {(char_u *)"dsplit", sizeof("dsplit") - 1,  ex_ni , (long_u)(EX_BANG|EX_RANGE|EX_DFLALL|EX_WHOLEFOLD|EX_EXTRA), ADDR_LINES},
+    [CMD_edit] = {(char_u *)"edit", sizeof("edit") - 1, ex_edit, (long_u)(EX_BANG| ( (EX_XFILE | EX_EXTRA)  | EX_NOSPC) |EX_CMDARG|EX_ARGOPT|EX_TRLBAR), ADDR_NONE},
+    [CMD_earlier] = {(char_u *)"earlier", sizeof("earlier") - 1, ex_later, (long_u)(EX_TRLBAR|EX_EXTRA|EX_NOSPC|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_echo] = {(char_u *)"echo", sizeof("echo") - 1,  ex_ni , (long_u)(EX_EXTRA|EX_NOTRLCOM|EX_EXPR_ARG|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_echoerr] = {(char_u *)"echoerr", sizeof("echoerr") - 1,  ex_ni , (long_u)(EX_EXTRA|EX_NOTRLCOM|EX_EXPR_ARG|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_echohl] = {(char_u *)"echohl", sizeof("echohl") - 1,  ex_ni , (long_u)(EX_EXTRA|EX_TRLBAR|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_echomsg] = {(char_u *)"echomsg", sizeof("echomsg") - 1,  ex_ni , (long_u)(EX_EXTRA|EX_NOTRLCOM|EX_EXPR_ARG|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_echoconsole] = {(char_u *)"echoconsole", sizeof("echoconsole") - 1,  ex_ni , (long_u)(EX_EXTRA|EX_NOTRLCOM|EX_EXPR_ARG|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_echon] = {(char_u *)"echon", sizeof("echon") - 1,  ex_ni , (long_u)(EX_EXTRA|EX_NOTRLCOM|EX_EXPR_ARG|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_echowindow] = {(char_u *)"echowindow", sizeof("echowindow") - 1,  ex_ni , (long_u)(EX_RANGE|EX_EXTRA|EX_NOTRLCOM|EX_EXPR_ARG|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK), ADDR_OTHER},
+    [CMD_else] = {(char_u *)"else", sizeof("else") - 1,  ex_ni , (long_u)(EX_TRLBAR|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK|EX_WHOLE), ADDR_NONE},
+    [CMD_elseif] = {(char_u *)"elseif", sizeof("elseif") - 1,  ex_ni , (long_u)(EX_EXTRA|EX_NOTRLCOM|EX_EXPR_ARG|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK|EX_WHOLE), ADDR_NONE},
+    [CMD_emenu] = {(char_u *)"emenu", sizeof("emenu") - 1,  ex_ni , (long_u)(EX_NEEDARG|EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_RANGE|EX_CMDWIN|EX_LOCK_OK), ADDR_OTHER},
+    [CMD_endif] = {(char_u *)"endif", sizeof("endif") - 1,  ex_ni , (long_u)(EX_TRLBAR|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK|EX_WHOLE), ADDR_NONE},
+    [CMD_endinterface] = {(char_u *)"endinterface", sizeof("endinterface") - 1, ex_wrongmodifier, (long_u)(EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK|EX_WHOLE), ADDR_NONE},
+    [CMD_endclass] = {(char_u *)"endclass", sizeof("endclass") - 1, ex_wrongmodifier, (long_u)(EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK|EX_WHOLE), ADDR_NONE},
+    [CMD_enddef] = {(char_u *)"enddef", sizeof("enddef") - 1,  ex_ni , (long_u)(EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK|EX_WHOLE), ADDR_NONE},
+    [CMD_endenum] = {(char_u *)"endenum", sizeof("endenum") - 1, ex_wrongmodifier, (long_u)(EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK|EX_WHOLE), ADDR_NONE},
+    [CMD_endfunction] = {(char_u *)"endfunction", sizeof("endfunction") - 1,  ex_ni , (long_u)(EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_endfor] = {(char_u *)"endfor", sizeof("endfor") - 1,  ex_ni , (long_u)(EX_TRLBAR|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK|EX_WHOLE), ADDR_NONE},
+    [CMD_endtry] = {(char_u *)"endtry", sizeof("endtry") - 1,  ex_ni , (long_u)(EX_TRLBAR|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK|EX_WHOLE), ADDR_NONE},
+    [CMD_endwhile] = {(char_u *)"endwhile", sizeof("endwhile") - 1,  ex_ni , (long_u)(EX_TRLBAR|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK|EX_WHOLE), ADDR_NONE},
+    [CMD_enew] = {(char_u *)"enew", sizeof("enew") - 1, ex_edit, (long_u)(EX_BANG|EX_TRLBAR), ADDR_NONE},
+    [CMD_enum] = {(char_u *)"enum", sizeof("enum") - 1,  ex_ni , (long_u)(EX_EXTRA|EX_CMDWIN|EX_LOCK_OK|EX_WHOLE|EX_EXPORT), ADDR_NONE},
+    [CMD_eval] = {(char_u *)"eval", sizeof("eval") - 1,  ex_ni , (long_u)(EX_EXTRA|EX_NOTRLCOM|EX_EXPR_ARG|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_ex] = {(char_u *)"ex", sizeof("ex") - 1, ex_edit, (long_u)(EX_BANG| ( (EX_XFILE | EX_EXTRA)  | EX_NOSPC) |EX_CMDARG|EX_ARGOPT|EX_TRLBAR), ADDR_NONE},
+    [CMD_execute] = {(char_u *)"execute", sizeof("execute") - 1,  ex_ni , (long_u)(EX_EXTRA|EX_NOTRLCOM|EX_EXPR_ARG|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_exit] = {(char_u *)"exit", sizeof("exit") - 1, ex_exit, (long_u)(EX_RANGE|EX_WHOLEFOLD|EX_BANG| ( (EX_XFILE | EX_EXTRA)  | EX_NOSPC) |EX_ARGOPT|EX_DFLALL|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK), ADDR_LINES},
+    [CMD_export] = {(char_u *)"export", sizeof("export") - 1,  ex_ni , (long_u)(EX_EXTRA|EX_NOTRLCOM|EX_EXPR_ARG|EX_CMDWIN|EX_LOCK_OK|EX_WHOLE), ADDR_NONE},
+    [CMD_exusage] = {(char_u *)"exusage", sizeof("exusage") - 1, ex_exusage, (long_u)(EX_TRLBAR), ADDR_NONE},
+    [CMD_file] = {(char_u *)"file", sizeof("file") - 1, ex_file, (long_u)(EX_RANGE|EX_ZEROR|EX_BANG| ( (EX_XFILE | EX_EXTRA)  | EX_NOSPC) |EX_TRLBAR), ADDR_OTHER},
+    [CMD_files] = {(char_u *)"files", sizeof("files") - 1, buflist_list, (long_u)(EX_BANG|EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_filetype] = {(char_u *)"filetype", sizeof("filetype") - 1, ex_filetype, (long_u)(EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_filter] = {(char_u *)"filter", sizeof("filter") - 1, ex_wrongmodifier, (long_u)(EX_BANG|EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM), ADDR_NONE},
+    [CMD_find] = {(char_u *)"find", sizeof("find") - 1, ex_find, (long_u)(EX_RANGE|EX_BANG| ( (EX_XFILE | EX_EXTRA)  | EX_NOSPC) |EX_CMDARG|EX_ARGOPT|EX_TRLBAR|EX_NEEDARG), ADDR_OTHER},
+    [CMD_final] = {(char_u *)"final", sizeof("final") - 1,  ex_ni , (long_u)(EX_EXTRA|EX_NOTRLCOM|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK|EX_WHOLE|EX_EXPORT), ADDR_NONE},
+    [CMD_finally] = {(char_u *)"finally", sizeof("finally") - 1,  ex_ni , (long_u)(EX_TRLBAR|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK|EX_WHOLE), ADDR_NONE},
+    [CMD_finish] = {(char_u *)"finish", sizeof("finish") - 1,  ex_ni , (long_u)(EX_TRLBAR|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK|EX_WHOLE), ADDR_NONE},
+    [CMD_first] = {(char_u *)"first", sizeof("first") - 1, ex_rewind, (long_u)(EX_EXTRA|EX_BANG|EX_CMDARG|EX_ARGOPT|EX_TRLBAR), ADDR_NONE},
+    [CMD_fixdel] = {(char_u *)"fixdel", sizeof("fixdel") - 1, do_fixdel, (long_u)(EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_fold] = {(char_u *)"fold", sizeof("fold") - 1,  ex_ni , (long_u)(EX_RANGE|EX_WHOLEFOLD|EX_TRLBAR|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK), ADDR_LINES},
+    [CMD_foldclose] = {(char_u *)"foldclose", sizeof("foldclose") - 1,  ex_ni , (long_u)(EX_RANGE|EX_BANG|EX_WHOLEFOLD|EX_TRLBAR|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK), ADDR_LINES},
+    [CMD_folddoopen] = {(char_u *)"folddoopen", sizeof("folddoopen") - 1,  ex_ni , (long_u)(EX_RANGE|EX_DFLALL|EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM), ADDR_LINES},
+    [CMD_folddoclosed] = {(char_u *)"folddoclosed", sizeof("folddoclosed") - 1,  ex_ni , (long_u)(EX_RANGE|EX_DFLALL|EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM), ADDR_LINES},
+    [CMD_foldopen] = {(char_u *)"foldopen", sizeof("foldopen") - 1,  ex_ni , (long_u)(EX_RANGE|EX_BANG|EX_WHOLEFOLD|EX_TRLBAR|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK), ADDR_LINES},
+    [CMD_for] = {(char_u *)"for", sizeof("for") - 1,  ex_ni , (long_u)(EX_EXTRA|EX_NOTRLCOM|EX_EXPR_ARG|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_function] = {(char_u *)"function", sizeof("function") - 1,  ex_ni , (long_u)(EX_EXTRA|EX_BANG|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK|EX_EXPORT), ADDR_NONE},
+    [CMD_global] = {(char_u *)"global", sizeof("global") - 1, ex_global, (long_u)(EX_RANGE|EX_WHOLEFOLD|EX_BANG|EX_EXTRA|EX_DFLALL|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK|EX_NONWHITE_OK), ADDR_LINES},
+    [CMD_goto] = {(char_u *)"goto", sizeof("goto") - 1,  ex_ni , (long_u)(EX_RANGE|EX_COUNT|EX_TRLBAR|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK), ADDR_OTHER},
+    [CMD_grep] = {(char_u *)"grep", sizeof("grep") - 1,  ex_ni , (long_u)(EX_RANGE|EX_BANG|EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM|EX_TRLBAR|EX_XFILE), ADDR_OTHER},
+    [CMD_grepadd] = {(char_u *)"grepadd", sizeof("grepadd") - 1,  ex_ni , (long_u)(EX_RANGE|EX_BANG|EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM|EX_TRLBAR|EX_XFILE), ADDR_OTHER},
+    [CMD_gui] = {(char_u *)"gui", sizeof("gui") - 1,  ex_nogui , (long_u)(EX_BANG| (EX_XFILE | EX_EXTRA) |EX_CMDARG|EX_ARGOPT|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_gvim] = {(char_u *)"gvim", sizeof("gvim") - 1,  ex_nogui , (long_u)(EX_BANG| (EX_XFILE | EX_EXTRA) |EX_CMDARG|EX_ARGOPT|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_help] = {(char_u *)"help", sizeof("help") - 1, ex_help, (long_u)(EX_BANG|EX_EXTRA|EX_NOTRLCOM), ADDR_NONE},
+    [CMD_helpclose] = {(char_u *)"helpclose", sizeof("helpclose") - 1, ex_helpclose, (long_u)(EX_TRLBAR), ADDR_NONE},
+    [CMD_helpfind] = {(char_u *)"helpfind", sizeof("helpfind") - 1,  ex_ni , (long_u)(EX_EXTRA|EX_NOTRLCOM), ADDR_NONE},
+    [CMD_helpgrep] = {(char_u *)"helpgrep", sizeof("helpgrep") - 1,  ex_ni , (long_u)(EX_EXTRA|EX_NOTRLCOM|EX_NEEDARG), ADDR_NONE},
+    [CMD_helptags] = {(char_u *)"helptags", sizeof("helptags") - 1, ex_helptags, (long_u)(EX_NEEDARG| (EX_XFILE | EX_EXTRA) |EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_hardcopy] = {(char_u *)"hardcopy", sizeof("hardcopy") - 1,  ex_ni , (long_u)(EX_RANGE|EX_COUNT|EX_EXTRA|EX_EXPAND|EX_TRLBAR|EX_DFLALL|EX_BANG), ADDR_LINES},
+    [CMD_highlight] = {(char_u *)"highlight", sizeof("highlight") - 1, ex_highlight, (long_u)(EX_BANG|EX_EXTRA|EX_TRLBAR|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_hide] = {(char_u *)"hide", sizeof("hide") - 1, ex_hide, (long_u)(EX_BANG|EX_RANGE|EX_COUNT|EX_EXTRA|EX_TRLBAR), ADDR_WINDOWS},
+    [CMD_history] = {(char_u *)"history", sizeof("history") - 1, ex_history, (long_u)(EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_horizontal] = {(char_u *)"horizontal", sizeof("horizontal") - 1, ex_wrongmodifier, (long_u)(EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM), ADDR_NONE},
+    [CMD_insert] = {(char_u *)"insert", sizeof("insert") - 1, ex_append, (long_u)(EX_BANG|EX_RANGE|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK|EX_MODIFY), ADDR_LINES},
+    [CMD_iabbrev] = {(char_u *)"iabbrev", sizeof("iabbrev") - 1, ex_abbreviate, (long_u)(EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_iabclear] = {(char_u *)"iabclear", sizeof("iabclear") - 1, ex_abclear, (long_u)(EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_if] = {(char_u *)"if", sizeof("if") - 1,  ex_ni , (long_u)(EX_EXTRA|EX_NOTRLCOM|EX_EXPR_ARG|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_ijump] = {(char_u *)"ijump", sizeof("ijump") - 1,  ex_ni , (long_u)(EX_BANG|EX_RANGE|EX_DFLALL|EX_WHOLEFOLD|EX_EXTRA), ADDR_LINES},
+    [CMD_ilist] = {(char_u *)"ilist", sizeof("ilist") - 1,  ex_ni , (long_u)(EX_BANG|EX_RANGE|EX_DFLALL|EX_WHOLEFOLD|EX_EXTRA|EX_CMDWIN|EX_LOCK_OK), ADDR_LINES},
+    [CMD_imap] = {(char_u *)"imap", sizeof("imap") - 1, ex_map, (long_u)(EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_imapclear] = {(char_u *)"imapclear", sizeof("imapclear") - 1, ex_mapclear, (long_u)(EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_imenu] = {(char_u *)"imenu", sizeof("imenu") - 1,  ex_ni , (long_u)(EX_RANGE|EX_ZEROR|EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK), ADDR_OTHER},
+    [CMD_import] = {(char_u *)"import", sizeof("import") - 1,  ex_ni , (long_u)(EX_EXTRA|EX_NOTRLCOM|EX_CMDWIN|EX_LOCK_OK|EX_WHOLE), ADDR_NONE},
+    [CMD_inoremap] = {(char_u *)"inoremap", sizeof("inoremap") - 1, ex_map, (long_u)(EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_inoreabbrev] = {(char_u *)"inoreabbrev", sizeof("inoreabbrev") - 1, ex_abbreviate, (long_u)(EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_inoremenu] = {(char_u *)"inoremenu", sizeof("inoremenu") - 1,  ex_ni , (long_u)(EX_RANGE|EX_ZEROR|EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK), ADDR_OTHER},
+    [CMD_intro] = {(char_u *)"intro", sizeof("intro") - 1, ex_intro, (long_u)(EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_interface] = {(char_u *)"interface", sizeof("interface") - 1,  ex_ni , (long_u)(EX_EXTRA|EX_CMDWIN|EX_LOCK_OK|EX_WHOLE|EX_EXPORT), ADDR_NONE},
+    [CMD_iput] = {(char_u *)"iput", sizeof("iput") - 1, ex_iput, (long_u)(EX_RANGE|EX_WHOLEFOLD|EX_BANG|EX_REGSTR|EX_TRLBAR|EX_ZEROR|EX_CMDWIN|EX_LOCK_OK|EX_MODIFY), ADDR_LINES},
+    [CMD_isearch] = {(char_u *)"isearch", sizeof("isearch") - 1,  ex_ni , (long_u)(EX_BANG|EX_RANGE|EX_DFLALL|EX_WHOLEFOLD|EX_EXTRA|EX_CMDWIN|EX_LOCK_OK), ADDR_LINES},
+    [CMD_isplit] = {(char_u *)"isplit", sizeof("isplit") - 1,  ex_ni , (long_u)(EX_BANG|EX_RANGE|EX_DFLALL|EX_WHOLEFOLD|EX_EXTRA), ADDR_LINES},
+    [CMD_iunmap] = {(char_u *)"iunmap", sizeof("iunmap") - 1, ex_unmap, (long_u)(EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_iunabbrev] = {(char_u *)"iunabbrev", sizeof("iunabbrev") - 1, ex_abbreviate, (long_u)(EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_iunmenu] = {(char_u *)"iunmenu", sizeof("iunmenu") - 1,  ex_ni , (long_u)(EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_join] = {(char_u *)"join", sizeof("join") - 1, ex_join, (long_u)(EX_BANG|EX_RANGE|EX_WHOLEFOLD|EX_COUNT|EX_FLAGS|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK|EX_MODIFY), ADDR_LINES},
+    [CMD_jumps] = {(char_u *)"jumps", sizeof("jumps") - 1, ex_jumps, (long_u)(EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_k] = {(char_u *)"k", sizeof("k") - 1, ex_mark, (long_u)(EX_RANGE| (EX_EXTRA | EX_NOSPC) |EX_TRLBAR|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK|EX_NONWHITE_OK), ADDR_LINES},
+    [CMD_keepmarks] = {(char_u *)"keepmarks", sizeof("keepmarks") - 1, ex_wrongmodifier, (long_u)(EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM), ADDR_NONE},
+    [CMD_keepjumps] = {(char_u *)"keepjumps", sizeof("keepjumps") - 1, ex_wrongmodifier, (long_u)(EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM), ADDR_NONE},
+    [CMD_keeppatterns] = {(char_u *)"keeppatterns", sizeof("keeppatterns") - 1, ex_wrongmodifier, (long_u)(EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM), ADDR_NONE},
+    [CMD_keepalt] = {(char_u *)"keepalt", sizeof("keepalt") - 1, ex_wrongmodifier, (long_u)(EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM), ADDR_NONE},
+    [CMD_list] = {(char_u *)"list", sizeof("list") - 1, ex_print, (long_u)(EX_RANGE|EX_WHOLEFOLD|EX_COUNT|EX_FLAGS|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK), ADDR_LINES},
+    [CMD_lNext] = {(char_u *)"lNext", sizeof("lNext") - 1,  ex_ni , (long_u)(EX_RANGE|EX_COUNT|EX_TRLBAR|EX_BANG), ADDR_UNSIGNED},
+    [CMD_lNfile] = {(char_u *)"lNfile", sizeof("lNfile") - 1,  ex_ni , (long_u)(EX_RANGE|EX_COUNT|EX_TRLBAR|EX_BANG), ADDR_UNSIGNED},
+    [CMD_last] = {(char_u *)"last", sizeof("last") - 1, ex_last, (long_u)(EX_EXTRA|EX_BANG|EX_CMDARG|EX_ARGOPT|EX_TRLBAR), ADDR_NONE},
+    [CMD_labove] = {(char_u *)"labove", sizeof("labove") - 1,  ex_ni , (long_u)(EX_RANGE|EX_COUNT|EX_TRLBAR), ADDR_UNSIGNED},
+    [CMD_language] = {(char_u *)"language", sizeof("language") - 1, ex_language, (long_u)(EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_laddexpr] = {(char_u *)"laddexpr", sizeof("laddexpr") - 1,  ex_ni , (long_u)(EX_NEEDARG| (EX_EXTRA | EX_NOSPC) |EX_NOTRLCOM|EX_EXPR_ARG), ADDR_NONE},
+    [CMD_laddbuffer] = {(char_u *)"laddbuffer", sizeof("laddbuffer") - 1,  ex_ni , (long_u)(EX_RANGE| (EX_EXTRA | EX_NOSPC) |EX_TRLBAR), ADDR_LINES},
+    [CMD_laddfile] = {(char_u *)"laddfile", sizeof("laddfile") - 1,  ex_ni , (long_u)(EX_TRLBAR| ( (EX_XFILE | EX_EXTRA)  | EX_NOSPC) ), ADDR_NONE},
+    [CMD_lafter] = {(char_u *)"lafter", sizeof("lafter") - 1,  ex_ni , (long_u)(EX_RANGE|EX_COUNT|EX_TRLBAR), ADDR_UNSIGNED},
+    [CMD_later] = {(char_u *)"later", sizeof("later") - 1, ex_later, (long_u)(EX_TRLBAR|EX_EXTRA|EX_NOSPC|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_lbuffer] = {(char_u *)"lbuffer", sizeof("lbuffer") - 1,  ex_ni , (long_u)(EX_BANG|EX_RANGE| (EX_EXTRA | EX_NOSPC) |EX_TRLBAR), ADDR_LINES},
+    [CMD_lbefore] = {(char_u *)"lbefore", sizeof("lbefore") - 1,  ex_ni , (long_u)(EX_RANGE|EX_COUNT|EX_TRLBAR), ADDR_UNSIGNED},
+    [CMD_lbelow] = {(char_u *)"lbelow", sizeof("lbelow") - 1,  ex_ni , (long_u)(EX_RANGE|EX_COUNT|EX_TRLBAR), ADDR_UNSIGNED},
+    [CMD_lbottom] = {(char_u *)"lbottom", sizeof("lbottom") - 1,  ex_ni , (long_u)(EX_TRLBAR), ADDR_NONE},
+    [CMD_lcd] = {(char_u *)"lcd", sizeof("lcd") - 1, ex_cd, (long_u)(EX_BANG| ( (EX_XFILE | EX_EXTRA)  | EX_NOSPC) |EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_lchdir] = {(char_u *)"lchdir", sizeof("lchdir") - 1, ex_cd, (long_u)(EX_BANG| ( (EX_XFILE | EX_EXTRA)  | EX_NOSPC) |EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_lclose] = {(char_u *)"lclose", sizeof("lclose") - 1,  ex_ni , (long_u)(EX_RANGE|EX_COUNT|EX_TRLBAR), ADDR_OTHER},
+    [CMD_lcscope] = {(char_u *)"lcscope", sizeof("lcscope") - 1,  ex_ni , (long_u)(EX_EXTRA|EX_NOTRLCOM|EX_XFILE), ADDR_NONE},
+    [CMD_ldo] = {(char_u *)"ldo", sizeof("ldo") - 1, ex_listdo, (long_u)(EX_BANG|EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM|EX_RANGE|EX_DFLALL|EX_EXPAND), ADDR_QUICKFIX_VALID},
+    [CMD_left] = {(char_u *)"left", sizeof("left") - 1, ex_align, (long_u)(EX_TRLBAR|EX_RANGE|EX_WHOLEFOLD|EX_EXTRA|EX_CMDWIN|EX_LOCK_OK|EX_MODIFY), ADDR_LINES},
+    [CMD_leftabove] = {(char_u *)"leftabove", sizeof("leftabove") - 1, ex_wrongmodifier, (long_u)(EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM), ADDR_NONE},
+    [CMD_let] = {(char_u *)"let", sizeof("let") - 1,  ex_ni , (long_u)(EX_EXTRA|EX_NOTRLCOM|EX_EXPR_ARG|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_lexpr] = {(char_u *)"lexpr", sizeof("lexpr") - 1,  ex_ni , (long_u)(EX_NEEDARG| (EX_EXTRA | EX_NOSPC) |EX_NOTRLCOM|EX_EXPR_ARG|EX_BANG), ADDR_NONE},
+    [CMD_legacy] = {(char_u *)"legacy", sizeof("legacy") - 1, ex_wrongmodifier, (long_u)(EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_lfile] = {(char_u *)"lfile", sizeof("lfile") - 1,  ex_ni , (long_u)(EX_TRLBAR| ( (EX_XFILE | EX_EXTRA)  | EX_NOSPC) |EX_BANG), ADDR_NONE},
+    [CMD_lfdo] = {(char_u *)"lfdo", sizeof("lfdo") - 1, ex_listdo, (long_u)(EX_BANG|EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM|EX_RANGE|EX_DFLALL|EX_EXPAND), ADDR_QUICKFIX_VALID},
+    [CMD_lfirst] = {(char_u *)"lfirst", sizeof("lfirst") - 1,  ex_ni , (long_u)(EX_RANGE|EX_COUNT|EX_TRLBAR|EX_BANG), ADDR_UNSIGNED},
+    [CMD_lgetfile] = {(char_u *)"lgetfile", sizeof("lgetfile") - 1,  ex_ni , (long_u)(EX_TRLBAR| ( (EX_XFILE | EX_EXTRA)  | EX_NOSPC) ), ADDR_NONE},
+    [CMD_lgetbuffer] = {(char_u *)"lgetbuffer", sizeof("lgetbuffer") - 1,  ex_ni , (long_u)(EX_RANGE| (EX_EXTRA | EX_NOSPC) |EX_TRLBAR), ADDR_LINES},
+    [CMD_lgetexpr] = {(char_u *)"lgetexpr", sizeof("lgetexpr") - 1,  ex_ni , (long_u)(EX_NEEDARG| (EX_EXTRA | EX_NOSPC) |EX_NOTRLCOM|EX_EXPR_ARG), ADDR_NONE},
+    [CMD_lgrep] = {(char_u *)"lgrep", sizeof("lgrep") - 1,  ex_ni , (long_u)(EX_RANGE|EX_BANG|EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM|EX_TRLBAR|EX_XFILE), ADDR_OTHER},
+    [CMD_lgrepadd] = {(char_u *)"lgrepadd", sizeof("lgrepadd") - 1,  ex_ni , (long_u)(EX_RANGE|EX_BANG|EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM|EX_TRLBAR|EX_XFILE), ADDR_OTHER},
+    [CMD_lhelpgrep] = {(char_u *)"lhelpgrep", sizeof("lhelpgrep") - 1,  ex_ni , (long_u)(EX_EXTRA|EX_NOTRLCOM|EX_NEEDARG), ADDR_NONE},
+    [CMD_lhistory] = {(char_u *)"lhistory", sizeof("lhistory") - 1,  ex_ni , (long_u)(EX_RANGE|EX_COUNT|EX_TRLBAR), ADDR_UNSIGNED},
+    [CMD_ll] = {(char_u *)"ll", sizeof("ll") - 1,  ex_ni , (long_u)(EX_RANGE|EX_COUNT|EX_TRLBAR|EX_BANG), ADDR_QUICKFIX},
+    [CMD_llast] = {(char_u *)"llast", sizeof("llast") - 1,  ex_ni , (long_u)(EX_RANGE|EX_COUNT|EX_TRLBAR|EX_BANG), ADDR_UNSIGNED},
+    [CMD_llist] = {(char_u *)"llist", sizeof("llist") - 1,  ex_ni , (long_u)(EX_BANG|EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_lmap] = {(char_u *)"lmap", sizeof("lmap") - 1, ex_map, (long_u)(EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_lmapclear] = {(char_u *)"lmapclear", sizeof("lmapclear") - 1, ex_mapclear, (long_u)(EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_lmake] = {(char_u *)"lmake", sizeof("lmake") - 1,  ex_ni , (long_u)(EX_BANG|EX_EXTRA|EX_NOTRLCOM|EX_TRLBAR|EX_XFILE), ADDR_NONE},
+    [CMD_lnoremap] = {(char_u *)"lnoremap", sizeof("lnoremap") - 1, ex_map, (long_u)(EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_lnext] = {(char_u *)"lnext", sizeof("lnext") - 1,  ex_ni , (long_u)(EX_RANGE|EX_COUNT|EX_TRLBAR|EX_BANG), ADDR_UNSIGNED},
+    [CMD_lnewer] = {(char_u *)"lnewer", sizeof("lnewer") - 1,  ex_ni , (long_u)(EX_RANGE|EX_COUNT|EX_TRLBAR), ADDR_UNSIGNED},
+    [CMD_lnfile] = {(char_u *)"lnfile", sizeof("lnfile") - 1,  ex_ni , (long_u)(EX_RANGE|EX_COUNT|EX_TRLBAR|EX_BANG), ADDR_UNSIGNED},
+    [CMD_loadview] = {(char_u *)"loadview", sizeof("loadview") - 1,  ex_ni , (long_u)( ( (EX_XFILE | EX_EXTRA)  | EX_NOSPC) |EX_TRLBAR), ADDR_NONE},
+    [CMD_loadkeymap] = {(char_u *)"loadkeymap", sizeof("loadkeymap") - 1,  ex_ni , (long_u)(EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_lockmarks] = {(char_u *)"lockmarks", sizeof("lockmarks") - 1, ex_wrongmodifier, (long_u)(EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM), ADDR_NONE},
+    [CMD_lockvar] = {(char_u *)"lockvar", sizeof("lockvar") - 1,  ex_ni , (long_u)(EX_BANG|EX_EXTRA|EX_NEEDARG|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_lolder] = {(char_u *)"lolder", sizeof("lolder") - 1,  ex_ni , (long_u)(EX_RANGE|EX_COUNT|EX_TRLBAR), ADDR_UNSIGNED},
+    [CMD_lopen] = {(char_u *)"lopen", sizeof("lopen") - 1,  ex_ni , (long_u)(EX_RANGE|EX_COUNT|EX_TRLBAR), ADDR_OTHER},
+    [CMD_lprevious] = {(char_u *)"lprevious", sizeof("lprevious") - 1,  ex_ni , (long_u)(EX_RANGE|EX_COUNT|EX_TRLBAR|EX_BANG), ADDR_UNSIGNED},
+    [CMD_lpfile] = {(char_u *)"lpfile", sizeof("lpfile") - 1,  ex_ni , (long_u)(EX_RANGE|EX_COUNT|EX_TRLBAR|EX_BANG), ADDR_OTHER},
+    [CMD_lrewind] = {(char_u *)"lrewind", sizeof("lrewind") - 1,  ex_ni , (long_u)(EX_RANGE|EX_COUNT|EX_TRLBAR|EX_BANG), ADDR_UNSIGNED},
+    [CMD_ltag] = {(char_u *)"ltag", sizeof("ltag") - 1, ex_tag, (long_u)(EX_TRLBAR|EX_BANG| (EX_EXTRA | EX_NOSPC) ), ADDR_NONE},
+    [CMD_lunmap] = {(char_u *)"lunmap", sizeof("lunmap") - 1, ex_unmap, (long_u)(EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_lua] = {(char_u *)"lua", sizeof("lua") - 1,  ex_script_ni , (long_u)(EX_RANGE|EX_EXTRA|EX_NEEDARG|EX_CMDWIN|EX_LOCK_OK|EX_RESTRICT), ADDR_LINES},
+    [CMD_luado] = {(char_u *)"luado", sizeof("luado") - 1,  ex_ni , (long_u)(EX_RANGE|EX_DFLALL|EX_EXTRA|EX_NEEDARG|EX_CMDWIN|EX_LOCK_OK|EX_RESTRICT), ADDR_LINES},
+    [CMD_luafile] = {(char_u *)"luafile", sizeof("luafile") - 1,  ex_ni , (long_u)(EX_RANGE| ( (EX_XFILE | EX_EXTRA)  | EX_NOSPC) |EX_NEEDARG|EX_CMDWIN|EX_LOCK_OK|EX_RESTRICT), ADDR_LINES},
+    [CMD_lvimgrep] = {(char_u *)"lvimgrep", sizeof("lvimgrep") - 1,  ex_ni , (long_u)(EX_RANGE|EX_BANG|EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM|EX_TRLBAR|EX_XFILE|EX_LOCK_OK), ADDR_OTHER},
+    [CMD_lvimgrepadd] = {(char_u *)"lvimgrepadd", sizeof("lvimgrepadd") - 1,  ex_ni , (long_u)(EX_RANGE|EX_BANG|EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM|EX_TRLBAR|EX_XFILE|EX_LOCK_OK), ADDR_OTHER},
+    [CMD_lwindow] = {(char_u *)"lwindow", sizeof("lwindow") - 1,  ex_ni , (long_u)(EX_RANGE|EX_COUNT|EX_TRLBAR), ADDR_OTHER},
+    [CMD_ls] = {(char_u *)"ls", sizeof("ls") - 1, buflist_list, (long_u)(EX_BANG|EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_move] = {(char_u *)"move", sizeof("move") - 1, ex_copymove, (long_u)(EX_RANGE|EX_WHOLEFOLD|EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK|EX_MODIFY), ADDR_LINES},
+    [CMD_mark] = {(char_u *)"mark", sizeof("mark") - 1, ex_mark, (long_u)(EX_RANGE| (EX_EXTRA | EX_NOSPC) |EX_TRLBAR|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK), ADDR_LINES},
+    [CMD_make] = {(char_u *)"make", sizeof("make") - 1,  ex_ni , (long_u)(EX_BANG|EX_EXTRA|EX_NOTRLCOM|EX_TRLBAR|EX_XFILE), ADDR_NONE},
+    [CMD_map] = {(char_u *)"map", sizeof("map") - 1, ex_map, (long_u)(EX_BANG|EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_mapclear] = {(char_u *)"mapclear", sizeof("mapclear") - 1, ex_mapclear, (long_u)(EX_EXTRA|EX_BANG|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_marks] = {(char_u *)"marks", sizeof("marks") - 1, ex_marks, (long_u)(EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_match] = {(char_u *)"match", sizeof("match") - 1, ex_match, (long_u)(EX_RANGE|EX_EXTRA|EX_CMDWIN|EX_LOCK_OK), ADDR_OTHER},
+    [CMD_menu] = {(char_u *)"menu", sizeof("menu") - 1,  ex_ni , (long_u)(EX_RANGE|EX_ZEROR|EX_BANG|EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK), ADDR_OTHER},
+    [CMD_menutranslate] = {(char_u *)"menutranslate", sizeof("menutranslate") - 1,  ex_ni , (long_u)(EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_messages] = {(char_u *)"messages", sizeof("messages") - 1, ex_messages, (long_u)(EX_EXTRA|EX_TRLBAR|EX_RANGE|EX_CMDWIN|EX_LOCK_OK), ADDR_OTHER},
+    [CMD_mkexrc] = {(char_u *)"mkexrc", sizeof("mkexrc") - 1, ex_mkrc, (long_u)(EX_BANG| ( (EX_XFILE | EX_EXTRA)  | EX_NOSPC) |EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_mksession] = {(char_u *)"mksession", sizeof("mksession") - 1, ex_mkrc, (long_u)(EX_BANG| ( (EX_XFILE | EX_EXTRA)  | EX_NOSPC) |EX_TRLBAR), ADDR_NONE},
+    [CMD_mkspell] = {(char_u *)"mkspell", sizeof("mkspell") - 1,  ex_ni , (long_u)(EX_BANG|EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM|EX_TRLBAR|EX_XFILE), ADDR_NONE},
+    [CMD_mkvimrc] = {(char_u *)"mkvimrc", sizeof("mkvimrc") - 1, ex_mkrc, (long_u)(EX_BANG| ( (EX_XFILE | EX_EXTRA)  | EX_NOSPC) |EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_mkview] = {(char_u *)"mkview", sizeof("mkview") - 1, ex_mkrc, (long_u)(EX_BANG| ( (EX_XFILE | EX_EXTRA)  | EX_NOSPC) |EX_TRLBAR), ADDR_NONE},
+    [CMD_mode] = {(char_u *)"mode", sizeof("mode") - 1, ex_mode, (long_u)( (EX_EXTRA | EX_NOSPC) |EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_mzscheme] = {(char_u *)"mzscheme", sizeof("mzscheme") - 1,  ex_script_ni , (long_u)(EX_RANGE|EX_EXTRA|EX_DFLALL|EX_NEEDARG|EX_CMDWIN|EX_LOCK_OK|EX_SBOXOK|EX_RESTRICT), ADDR_LINES},
+    [CMD_mzfile] = {(char_u *)"mzfile", sizeof("mzfile") - 1,  ex_ni , (long_u)(EX_RANGE| ( (EX_XFILE | EX_EXTRA)  | EX_NOSPC) |EX_NEEDARG|EX_CMDWIN|EX_LOCK_OK|EX_RESTRICT), ADDR_LINES},
+    [CMD_next] = {(char_u *)"next", sizeof("next") - 1, ex_next, (long_u)(EX_RANGE|EX_BANG| (EX_XFILE | EX_EXTRA) |EX_CMDARG|EX_ARGOPT|EX_TRLBAR), ADDR_OTHER},
+    [CMD_nbkey] = {(char_u *)"nbkey", sizeof("nbkey") - 1,  ex_ni , (long_u)(EX_EXTRA|EX_NEEDARG), ADDR_NONE},
+    [CMD_nbclose] = {(char_u *)"nbclose", sizeof("nbclose") - 1,  ex_ni , (long_u)(EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_nbstart] = {(char_u *)"nbstart", sizeof("nbstart") - 1,  ex_ni , (long_u)( (EX_EXTRA | EX_NOSPC) |EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_new] = {(char_u *)"new", sizeof("new") - 1, ex_splitview, (long_u)(EX_BANG| ( (EX_XFILE | EX_EXTRA)  | EX_NOSPC) |EX_RANGE|EX_CMDARG|EX_ARGOPT|EX_TRLBAR), ADDR_OTHER},
+    [CMD_nmap] = {(char_u *)"nmap", sizeof("nmap") - 1, ex_map, (long_u)(EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_nmapclear] = {(char_u *)"nmapclear", sizeof("nmapclear") - 1, ex_mapclear, (long_u)(EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_nmenu] = {(char_u *)"nmenu", sizeof("nmenu") - 1,  ex_ni , (long_u)(EX_RANGE|EX_ZEROR|EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK), ADDR_OTHER},
+    [CMD_nnoremap] = {(char_u *)"nnoremap", sizeof("nnoremap") - 1, ex_map, (long_u)(EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_nnoremenu] = {(char_u *)"nnoremenu", sizeof("nnoremenu") - 1,  ex_ni , (long_u)(EX_RANGE|EX_ZEROR|EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK), ADDR_OTHER},
+    [CMD_noremap] = {(char_u *)"noremap", sizeof("noremap") - 1, ex_map, (long_u)(EX_BANG|EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_noautocmd] = {(char_u *)"noautocmd", sizeof("noautocmd") - 1, ex_wrongmodifier, (long_u)(EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM), ADDR_NONE},
+    [CMD_nohlsearch] = {(char_u *)"nohlsearch", sizeof("nohlsearch") - 1, ex_nohlsearch, (long_u)(EX_TRLBAR|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_noreabbrev] = {(char_u *)"noreabbrev", sizeof("noreabbrev") - 1, ex_abbreviate, (long_u)(EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_noremenu] = {(char_u *)"noremenu", sizeof("noremenu") - 1,  ex_ni , (long_u)(EX_RANGE|EX_ZEROR|EX_BANG|EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK), ADDR_OTHER},
+    [CMD_noswapfile] = {(char_u *)"noswapfile", sizeof("noswapfile") - 1, ex_wrongmodifier, (long_u)(EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM), ADDR_NONE},
+    [CMD_normal] = {(char_u *)"normal", sizeof("normal") - 1, ex_normal, (long_u)(EX_RANGE|EX_BANG|EX_EXTRA|EX_NEEDARG|EX_NOTRLCOM|EX_CTRLV|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK), ADDR_LINES},
+    [CMD_number] = {(char_u *)"number", sizeof("number") - 1, ex_print, (long_u)(EX_RANGE|EX_WHOLEFOLD|EX_COUNT|EX_FLAGS|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK), ADDR_LINES},
+    [CMD_nunmap] = {(char_u *)"nunmap", sizeof("nunmap") - 1, ex_unmap, (long_u)(EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_nunmenu] = {(char_u *)"nunmenu", sizeof("nunmenu") - 1,  ex_ni , (long_u)(EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_open] = {(char_u *)"open", sizeof("open") - 1, ex_open, (long_u)(EX_RANGE|EX_BANG|EX_EXTRA), ADDR_LINES},
+    [CMD_oldfiles] = {(char_u *)"oldfiles", sizeof("oldfiles") - 1,  ex_ni , (long_u)(EX_BANG|EX_TRLBAR|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_omap] = {(char_u *)"omap", sizeof("omap") - 1, ex_map, (long_u)(EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_omapclear] = {(char_u *)"omapclear", sizeof("omapclear") - 1, ex_mapclear, (long_u)(EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_omenu] = {(char_u *)"omenu", sizeof("omenu") - 1,  ex_ni , (long_u)(EX_RANGE|EX_ZEROR|EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK), ADDR_OTHER},
+    [CMD_only] = {(char_u *)"only", sizeof("only") - 1, ex_only, (long_u)(EX_BANG|EX_RANGE|EX_COUNT|EX_TRLBAR), ADDR_WINDOWS},
+    [CMD_onoremap] = {(char_u *)"onoremap", sizeof("onoremap") - 1, ex_map, (long_u)(EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_onoremenu] = {(char_u *)"onoremenu", sizeof("onoremenu") - 1,  ex_ni , (long_u)(EX_RANGE|EX_ZEROR|EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK), ADDR_OTHER},
+    [CMD_options] = {(char_u *)"options", sizeof("options") - 1,  ex_ni , (long_u)(EX_TRLBAR), ADDR_NONE},
+    [CMD_ounmap] = {(char_u *)"ounmap", sizeof("ounmap") - 1, ex_unmap, (long_u)(EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_ounmenu] = {(char_u *)"ounmenu", sizeof("ounmenu") - 1,  ex_ni , (long_u)(EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_ownsyntax] = {(char_u *)"ownsyntax", sizeof("ownsyntax") - 1,  ex_ni , (long_u)(EX_EXTRA|EX_NOTRLCOM|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_print] = {(char_u *)"print", sizeof("print") - 1, ex_print, (long_u)(EX_RANGE|EX_WHOLEFOLD|EX_COUNT|EX_FLAGS|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK|EX_SBOXOK), ADDR_LINES},
+    [CMD_packadd] = {(char_u *)"packadd", sizeof("packadd") - 1,  ex_ni , (long_u)(EX_BANG| ( (EX_XFILE | EX_EXTRA)  | EX_NOSPC) |EX_NEEDARG|EX_TRLBAR|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_packloadall] = {(char_u *)"packloadall", sizeof("packloadall") - 1,  ex_ni , (long_u)(EX_BANG|EX_TRLBAR|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_pbuffer] = {(char_u *)"pbuffer", sizeof("pbuffer") - 1,  ex_ni , (long_u)(EX_BANG|EX_RANGE|EX_BUFNAME|EX_BUFUNL|EX_COUNT|EX_EXTRA|EX_CMDARG|EX_TRLBAR), ADDR_BUFFERS},
+    [CMD_pclose] = {(char_u *)"pclose", sizeof("pclose") - 1,  ex_ni , (long_u)(EX_BANG|EX_TRLBAR), ADDR_NONE},
+    [CMD_perl] = {(char_u *)"perl", sizeof("perl") - 1,  ex_script_ni , (long_u)(EX_RANGE|EX_EXTRA|EX_DFLALL|EX_NEEDARG|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK|EX_RESTRICT), ADDR_LINES},
+    [CMD_perldo] = {(char_u *)"perldo", sizeof("perldo") - 1,  ex_ni , (long_u)(EX_RANGE|EX_EXTRA|EX_DFLALL|EX_NEEDARG|EX_CMDWIN|EX_LOCK_OK|EX_RESTRICT), ADDR_LINES},
+    [CMD_pedit] = {(char_u *)"pedit", sizeof("pedit") - 1,  ex_ni , (long_u)(EX_BANG| ( (EX_XFILE | EX_EXTRA)  | EX_NOSPC) |EX_CMDARG|EX_ARGOPT|EX_TRLBAR), ADDR_NONE},
+    [CMD_pop] = {(char_u *)"pop", sizeof("pop") - 1, ex_tag, (long_u)(EX_RANGE|EX_BANG|EX_COUNT|EX_TRLBAR|EX_ZEROR), ADDR_OTHER},
+    [CMD_popup] = {(char_u *)"popup", sizeof("popup") - 1,  ex_ni , (long_u)(EX_NEEDARG|EX_EXTRA|EX_BANG|EX_TRLBAR|EX_NOTRLCOM|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_ppop] = {(char_u *)"ppop", sizeof("ppop") - 1,  ex_ni , (long_u)(EX_RANGE|EX_BANG|EX_COUNT|EX_TRLBAR|EX_ZEROR), ADDR_OTHER},
+    [CMD_preserve] = {(char_u *)"preserve", sizeof("preserve") - 1, ex_preserve, (long_u)(EX_TRLBAR), ADDR_NONE},
+    [CMD_previous] = {(char_u *)"previous", sizeof("previous") - 1, ex_previous, (long_u)(EX_EXTRA|EX_RANGE|EX_COUNT|EX_BANG|EX_CMDARG|EX_ARGOPT|EX_TRLBAR), ADDR_OTHER},
+    [CMD_promptfind] = {(char_u *)"promptfind", sizeof("promptfind") - 1,  ex_ni , (long_u)(EX_EXTRA|EX_NOTRLCOM|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_promptrepl] = {(char_u *)"promptrepl", sizeof("promptrepl") - 1,  ex_ni , (long_u)(EX_EXTRA|EX_NOTRLCOM|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_profile] = {(char_u *)"profile", sizeof("profile") - 1,  ex_ni , (long_u)(EX_BANG|EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_profdel] = {(char_u *)"profdel", sizeof("profdel") - 1,  ex_ni , (long_u)(EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_psearch] = {(char_u *)"psearch", sizeof("psearch") - 1,  ex_ni , (long_u)(EX_BANG|EX_RANGE|EX_WHOLEFOLD|EX_DFLALL|EX_EXTRA), ADDR_LINES},
+    [CMD_ptag] = {(char_u *)"ptag", sizeof("ptag") - 1,  ex_ni , (long_u)(EX_RANGE|EX_BANG| (EX_EXTRA | EX_NOSPC) |EX_TRLBAR|EX_ZEROR), ADDR_OTHER},
+    [CMD_ptNext] = {(char_u *)"ptNext", sizeof("ptNext") - 1,  ex_ni , (long_u)(EX_RANGE|EX_BANG|EX_TRLBAR|EX_ZEROR), ADDR_OTHER},
+    [CMD_ptfirst] = {(char_u *)"ptfirst", sizeof("ptfirst") - 1,  ex_ni , (long_u)(EX_RANGE|EX_BANG|EX_TRLBAR|EX_ZEROR), ADDR_OTHER},
+    [CMD_ptjump] = {(char_u *)"ptjump", sizeof("ptjump") - 1,  ex_ni , (long_u)(EX_BANG|EX_TRLBAR| (EX_EXTRA | EX_NOSPC) ), ADDR_NONE},
+    [CMD_ptlast] = {(char_u *)"ptlast", sizeof("ptlast") - 1,  ex_ni , (long_u)(EX_BANG|EX_TRLBAR), ADDR_NONE},
+    [CMD_ptnext] = {(char_u *)"ptnext", sizeof("ptnext") - 1,  ex_ni , (long_u)(EX_RANGE|EX_BANG|EX_TRLBAR|EX_ZEROR), ADDR_OTHER},
+    [CMD_ptprevious] = {(char_u *)"ptprevious", sizeof("ptprevious") - 1,  ex_ni , (long_u)(EX_RANGE|EX_BANG|EX_TRLBAR|EX_ZEROR), ADDR_OTHER},
+    [CMD_ptrewind] = {(char_u *)"ptrewind", sizeof("ptrewind") - 1,  ex_ni , (long_u)(EX_RANGE|EX_BANG|EX_TRLBAR|EX_ZEROR), ADDR_OTHER},
+    [CMD_ptselect] = {(char_u *)"ptselect", sizeof("ptselect") - 1,  ex_ni , (long_u)(EX_BANG|EX_TRLBAR| (EX_EXTRA | EX_NOSPC) ), ADDR_NONE},
+    [CMD_put] = {(char_u *)"put", sizeof("put") - 1, ex_put, (long_u)(EX_RANGE|EX_WHOLEFOLD|EX_BANG|EX_REGSTR|EX_TRLBAR|EX_ZEROR|EX_CMDWIN|EX_LOCK_OK|EX_MODIFY), ADDR_LINES},
+    [CMD_public] = {(char_u *)"public", sizeof("public") - 1, ex_wrongmodifier, (long_u)(EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK|EX_WHOLE), ADDR_NONE},
+    [CMD_pwd] = {(char_u *)"pwd", sizeof("pwd") - 1, ex_pwd, (long_u)(EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_python] = {(char_u *)"python", sizeof("python") - 1,  ex_script_ni , (long_u)(EX_RANGE|EX_EXTRA|EX_NEEDARG|EX_CMDWIN|EX_LOCK_OK|EX_RESTRICT), ADDR_LINES},
+    [CMD_pydo] = {(char_u *)"pydo", sizeof("pydo") - 1,  ex_ni , (long_u)(EX_RANGE|EX_DFLALL|EX_EXTRA|EX_NEEDARG|EX_CMDWIN|EX_LOCK_OK|EX_RESTRICT), ADDR_LINES},
+    [CMD_pyfile] = {(char_u *)"pyfile", sizeof("pyfile") - 1,  ex_ni , (long_u)(EX_RANGE| ( (EX_XFILE | EX_EXTRA)  | EX_NOSPC) |EX_NEEDARG|EX_CMDWIN|EX_LOCK_OK|EX_RESTRICT), ADDR_LINES},
+    [CMD_py3] = {(char_u *)"py3", sizeof("py3") - 1,  ex_script_ni , (long_u)(EX_RANGE|EX_EXTRA|EX_NEEDARG|EX_CMDWIN|EX_LOCK_OK|EX_RESTRICT), ADDR_LINES},
+    [CMD_py3do] = {(char_u *)"py3do", sizeof("py3do") - 1,  ex_ni , (long_u)(EX_RANGE|EX_DFLALL|EX_EXTRA|EX_NEEDARG|EX_CMDWIN|EX_LOCK_OK|EX_RESTRICT), ADDR_LINES},
+    [CMD_python3] = {(char_u *)"python3", sizeof("python3") - 1,  ex_script_ni , (long_u)(EX_RANGE|EX_EXTRA|EX_NEEDARG|EX_CMDWIN|EX_LOCK_OK|EX_RESTRICT), ADDR_LINES},
+    [CMD_py3file] = {(char_u *)"py3file", sizeof("py3file") - 1,  ex_ni , (long_u)(EX_RANGE| ( (EX_XFILE | EX_EXTRA)  | EX_NOSPC) |EX_NEEDARG|EX_CMDWIN|EX_LOCK_OK|EX_RESTRICT), ADDR_LINES},
+    [CMD_pyx] = {(char_u *)"pyx", sizeof("pyx") - 1,  ex_script_ni , (long_u)(EX_RANGE|EX_EXTRA|EX_NEEDARG|EX_CMDWIN|EX_LOCK_OK|EX_RESTRICT), ADDR_LINES},
+    [CMD_pyxdo] = {(char_u *)"pyxdo", sizeof("pyxdo") - 1,  ex_ni , (long_u)(EX_RANGE|EX_DFLALL|EX_EXTRA|EX_NEEDARG|EX_CMDWIN|EX_LOCK_OK|EX_RESTRICT), ADDR_LINES},
+    [CMD_pythonx] = {(char_u *)"pythonx", sizeof("pythonx") - 1,  ex_script_ni , (long_u)(EX_RANGE|EX_EXTRA|EX_NEEDARG|EX_CMDWIN|EX_LOCK_OK|EX_RESTRICT), ADDR_LINES},
+    [CMD_pyxfile] = {(char_u *)"pyxfile", sizeof("pyxfile") - 1,  ex_ni , (long_u)(EX_RANGE| ( (EX_XFILE | EX_EXTRA)  | EX_NOSPC) |EX_NEEDARG|EX_CMDWIN|EX_LOCK_OK|EX_RESTRICT), ADDR_LINES},
+    [CMD_quit] = {(char_u *)"quit", sizeof("quit") - 1, ex_quit, (long_u)(EX_BANG|EX_RANGE|EX_COUNT|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK), ADDR_WINDOWS},
+    [CMD_quitall] = {(char_u *)"quitall", sizeof("quitall") - 1, ex_quit_all, (long_u)(EX_BANG|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_qall] = {(char_u *)"qall", sizeof("qall") - 1, ex_quit_all, (long_u)(EX_BANG|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_read] = {(char_u *)"read", sizeof("read") - 1, ex_read, (long_u)(EX_BANG|EX_RANGE|EX_WHOLEFOLD| ( (EX_XFILE | EX_EXTRA)  | EX_NOSPC) |EX_ARGOPT|EX_TRLBAR|EX_ZEROR|EX_CMDWIN|EX_LOCK_OK|EX_MODIFY), ADDR_LINES},
+    [CMD_recover] = {(char_u *)"recover", sizeof("recover") - 1, ex_recover, (long_u)(EX_BANG| ( (EX_XFILE | EX_EXTRA)  | EX_NOSPC) |EX_TRLBAR), ADDR_NONE},
+    [CMD_redo] = {(char_u *)"redo", sizeof("redo") - 1, ex_redo, (long_u)(EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_redir] = {(char_u *)"redir", sizeof("redir") - 1, ex_redir, (long_u)(EX_BANG| (EX_XFILE | EX_EXTRA) |EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_redraw] = {(char_u *)"redraw", sizeof("redraw") - 1, ex_redraw, (long_u)(EX_BANG|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_redrawstatus] = {(char_u *)"redrawstatus", sizeof("redrawstatus") - 1, ex_redrawstatus, (long_u)(EX_BANG|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_redrawtabline] = {(char_u *)"redrawtabline", sizeof("redrawtabline") - 1, ex_redrawtabline, (long_u)(EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_redrawtabpanel] = {(char_u *)"redrawtabpanel", sizeof("redrawtabpanel") - 1, ex_redrawtabpanel, (long_u)(EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_registers] = {(char_u *)"registers", sizeof("registers") - 1, ex_display, (long_u)(EX_EXTRA|EX_NOTRLCOM|EX_TRLBAR|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_resize] = {(char_u *)"resize", sizeof("resize") - 1, ex_resize, (long_u)(EX_RANGE|EX_TRLBAR| (EX_EXTRA | EX_NOSPC) |EX_CMDWIN|EX_LOCK_OK), ADDR_OTHER},
+    [CMD_retab] = {(char_u *)"retab", sizeof("retab") - 1, ex_retab, (long_u)(EX_TRLBAR|EX_RANGE|EX_WHOLEFOLD|EX_DFLALL|EX_BANG| (EX_EXTRA | EX_NOSPC) |EX_CMDWIN|EX_LOCK_OK|EX_MODIFY), ADDR_LINES},
+    [CMD_return] = {(char_u *)"return", sizeof("return") - 1,  ex_ni , (long_u)(EX_EXTRA|EX_NOTRLCOM|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK|EX_WHOLE), ADDR_NONE},
+    [CMD_rewind] = {(char_u *)"rewind", sizeof("rewind") - 1, ex_rewind, (long_u)(EX_EXTRA|EX_BANG|EX_CMDARG|EX_ARGOPT|EX_TRLBAR), ADDR_NONE},
+    [CMD_right] = {(char_u *)"right", sizeof("right") - 1, ex_align, (long_u)(EX_TRLBAR|EX_RANGE|EX_WHOLEFOLD|EX_EXTRA|EX_CMDWIN|EX_LOCK_OK|EX_MODIFY), ADDR_LINES},
+    [CMD_rightbelow] = {(char_u *)"rightbelow", sizeof("rightbelow") - 1, ex_wrongmodifier, (long_u)(EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM), ADDR_NONE},
+    [CMD_runtime] = {(char_u *)"runtime", sizeof("runtime") - 1, ex_runtime, (long_u)(EX_BANG|EX_NEEDARG| (EX_XFILE | EX_EXTRA) |EX_TRLBAR|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_ruby] = {(char_u *)"ruby", sizeof("ruby") - 1,  ex_script_ni , (long_u)(EX_RANGE|EX_EXTRA|EX_NEEDARG|EX_CMDWIN|EX_LOCK_OK|EX_RESTRICT), ADDR_LINES},
+    [CMD_rubydo] = {(char_u *)"rubydo", sizeof("rubydo") - 1,  ex_ni , (long_u)(EX_RANGE|EX_DFLALL|EX_EXTRA|EX_NEEDARG|EX_CMDWIN|EX_LOCK_OK|EX_RESTRICT), ADDR_LINES},
+    [CMD_rubyfile] = {(char_u *)"rubyfile", sizeof("rubyfile") - 1,  ex_ni , (long_u)(EX_RANGE| ( (EX_XFILE | EX_EXTRA)  | EX_NOSPC) |EX_NEEDARG|EX_CMDWIN|EX_LOCK_OK|EX_RESTRICT), ADDR_LINES},
+    [CMD_rundo] = {(char_u *)"rundo", sizeof("rundo") - 1,  ex_ni , (long_u)(EX_NEEDARG| ( (EX_XFILE | EX_EXTRA)  | EX_NOSPC) ), ADDR_NONE},
+    [CMD_rviminfo] = {(char_u *)"rviminfo", sizeof("rviminfo") - 1,  ex_ni , (long_u)(EX_BANG| ( (EX_XFILE | EX_EXTRA)  | EX_NOSPC) |EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_substitute] = {(char_u *)"substitute", sizeof("substitute") - 1, ex_substitute, (long_u)(EX_RANGE|EX_WHOLEFOLD|EX_EXTRA|EX_CMDWIN|EX_LOCK_OK|EX_NONWHITE_OK), ADDR_LINES},
+    [CMD_sNext] = {(char_u *)"sNext", sizeof("sNext") - 1, ex_previous, (long_u)(EX_EXTRA|EX_RANGE|EX_COUNT|EX_BANG|EX_CMDARG|EX_ARGOPT|EX_TRLBAR), ADDR_OTHER},
+    [CMD_sargument] = {(char_u *)"sargument", sizeof("sargument") - 1, ex_argument, (long_u)(EX_BANG|EX_RANGE|EX_COUNT|EX_EXTRA|EX_CMDARG|EX_ARGOPT|EX_TRLBAR), ADDR_ARGUMENTS},
+    [CMD_sall] = {(char_u *)"sall", sizeof("sall") - 1, ex_all, (long_u)(EX_BANG|EX_RANGE|EX_COUNT|EX_TRLBAR), ADDR_OTHER},
+    [CMD_sandbox] = {(char_u *)"sandbox", sizeof("sandbox") - 1, ex_wrongmodifier, (long_u)(EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM), ADDR_NONE},
+    [CMD_saveas] = {(char_u *)"saveas", sizeof("saveas") - 1, ex_write, (long_u)(EX_BANG| ( (EX_XFILE | EX_EXTRA)  | EX_NOSPC) |EX_ARGOPT|EX_CMDWIN|EX_LOCK_OK|EX_TRLBAR), ADDR_NONE},
+    [CMD_sbuffer] = {(char_u *)"sbuffer", sizeof("sbuffer") - 1, ex_buffer, (long_u)(EX_BANG|EX_RANGE|EX_BUFNAME|EX_BUFUNL|EX_COUNT|EX_EXTRA|EX_CMDARG|EX_TRLBAR), ADDR_BUFFERS},
+    [CMD_sbNext] = {(char_u *)"sbNext", sizeof("sbNext") - 1, ex_bprevious, (long_u)(EX_RANGE|EX_COUNT|EX_CMDARG|EX_TRLBAR), ADDR_OTHER},
+    [CMD_sball] = {(char_u *)"sball", sizeof("sball") - 1, ex_buffer_all, (long_u)(EX_RANGE|EX_COUNT|EX_CMDARG|EX_TRLBAR), ADDR_OTHER},
+    [CMD_sbfirst] = {(char_u *)"sbfirst", sizeof("sbfirst") - 1, ex_brewind, (long_u)(EX_CMDARG|EX_TRLBAR), ADDR_NONE},
+    [CMD_sblast] = {(char_u *)"sblast", sizeof("sblast") - 1, ex_blast, (long_u)(EX_CMDARG|EX_TRLBAR), ADDR_NONE},
+    [CMD_sbmodified] = {(char_u *)"sbmodified", sizeof("sbmodified") - 1, ex_bmodified, (long_u)(EX_RANGE|EX_COUNT|EX_CMDARG|EX_TRLBAR), ADDR_OTHER},
+    [CMD_sbnext] = {(char_u *)"sbnext", sizeof("sbnext") - 1, ex_bnext, (long_u)(EX_RANGE|EX_COUNT|EX_CMDARG|EX_TRLBAR), ADDR_OTHER},
+    [CMD_sbprevious] = {(char_u *)"sbprevious", sizeof("sbprevious") - 1, ex_bprevious, (long_u)(EX_RANGE|EX_COUNT|EX_CMDARG|EX_TRLBAR), ADDR_OTHER},
+    [CMD_sbrewind] = {(char_u *)"sbrewind", sizeof("sbrewind") - 1, ex_brewind, (long_u)(EX_CMDARG|EX_TRLBAR), ADDR_NONE},
+    [CMD_scriptnames] = {(char_u *)"scriptnames", sizeof("scriptnames") - 1,  ex_ni , (long_u)(EX_BANG| (EX_XFILE | EX_EXTRA) |EX_RANGE|EX_COUNT|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK), ADDR_OTHER},
+    [CMD_scriptencoding] = {(char_u *)"scriptencoding", sizeof("scriptencoding") - 1, ex_scriptencoding, (long_u)( (EX_EXTRA | EX_NOSPC) |EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_scriptversion] = {(char_u *)"scriptversion", sizeof("scriptversion") - 1, ex_scriptversion, (long_u)( (EX_EXTRA | EX_NOSPC) |EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_scscope] = {(char_u *)"scscope", sizeof("scscope") - 1,  ex_ni , (long_u)(EX_EXTRA|EX_NOTRLCOM), ADDR_NONE},
+    [CMD_set] = {(char_u *)"set", sizeof("set") - 1, ex_set, (long_u)(EX_BANG|EX_TRLBAR|EX_EXTRA|EX_CMDWIN|EX_LOCK_OK|EX_SBOXOK), ADDR_NONE},
+    [CMD_setfiletype] = {(char_u *)"setfiletype", sizeof("setfiletype") - 1, ex_setfiletype, (long_u)(EX_TRLBAR|EX_EXTRA|EX_NEEDARG|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_setglobal] = {(char_u *)"setglobal", sizeof("setglobal") - 1, ex_set, (long_u)(EX_BANG|EX_TRLBAR|EX_EXTRA|EX_CMDWIN|EX_LOCK_OK|EX_SBOXOK), ADDR_NONE},
+    [CMD_setlocal] = {(char_u *)"setlocal", sizeof("setlocal") - 1, ex_set, (long_u)(EX_BANG|EX_TRLBAR|EX_EXTRA|EX_CMDWIN|EX_LOCK_OK|EX_SBOXOK), ADDR_NONE},
+    [CMD_sfind] = {(char_u *)"sfind", sizeof("sfind") - 1, ex_splitview, (long_u)(EX_BANG| ( (EX_XFILE | EX_EXTRA)  | EX_NOSPC) |EX_RANGE|EX_CMDARG|EX_ARGOPT|EX_TRLBAR|EX_NEEDARG), ADDR_OTHER},
+    [CMD_sfirst] = {(char_u *)"sfirst", sizeof("sfirst") - 1, ex_rewind, (long_u)(EX_EXTRA|EX_BANG|EX_CMDARG|EX_ARGOPT|EX_TRLBAR), ADDR_NONE},
+    [CMD_shell] = {(char_u *)"shell", sizeof("shell") - 1, ex_shell, (long_u)(EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_simalt] = {(char_u *)"simalt", sizeof("simalt") - 1,  ex_ni , (long_u)(EX_NEEDARG| (EX_EXTRA | EX_NOSPC) |EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_sign] = {(char_u *)"sign", sizeof("sign") - 1,  ex_ni , (long_u)(EX_NEEDARG|EX_RANGE|EX_EXTRA|EX_CMDWIN|EX_LOCK_OK), ADDR_OTHER},
+    [CMD_silent] = {(char_u *)"silent", sizeof("silent") - 1, ex_wrongmodifier, (long_u)(EX_NEEDARG|EX_EXTRA|EX_BANG|EX_NOTRLCOM|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_sleep] = {(char_u *)"sleep", sizeof("sleep") - 1, ex_sleep, (long_u)(EX_BANG|EX_RANGE|EX_COUNT|EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK), ADDR_OTHER},
+    [CMD_slast] = {(char_u *)"slast", sizeof("slast") - 1, ex_last, (long_u)(EX_EXTRA|EX_BANG|EX_CMDARG|EX_ARGOPT|EX_TRLBAR), ADDR_NONE},
+    [CMD_smagic] = {(char_u *)"smagic", sizeof("smagic") - 1, ex_submagic, (long_u)(EX_RANGE|EX_WHOLEFOLD|EX_EXTRA|EX_CMDWIN|EX_LOCK_OK|EX_NONWHITE_OK), ADDR_LINES},
+    [CMD_smap] = {(char_u *)"smap", sizeof("smap") - 1, ex_map, (long_u)(EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_smapclear] = {(char_u *)"smapclear", sizeof("smapclear") - 1, ex_mapclear, (long_u)(EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_smenu] = {(char_u *)"smenu", sizeof("smenu") - 1,  ex_ni , (long_u)(EX_RANGE|EX_ZEROR|EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK), ADDR_OTHER},
+    [CMD_snext] = {(char_u *)"snext", sizeof("snext") - 1, ex_next, (long_u)(EX_RANGE|EX_BANG| (EX_XFILE | EX_EXTRA) |EX_CMDARG|EX_ARGOPT|EX_TRLBAR), ADDR_OTHER},
+    [CMD_snomagic] = {(char_u *)"snomagic", sizeof("snomagic") - 1, ex_submagic, (long_u)(EX_RANGE|EX_WHOLEFOLD|EX_EXTRA|EX_CMDWIN|EX_LOCK_OK|EX_NONWHITE_OK), ADDR_LINES},
+    [CMD_snoremap] = {(char_u *)"snoremap", sizeof("snoremap") - 1, ex_map, (long_u)(EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_snoremenu] = {(char_u *)"snoremenu", sizeof("snoremenu") - 1,  ex_ni , (long_u)(EX_RANGE|EX_ZEROR|EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK), ADDR_OTHER},
+    [CMD_source] = {(char_u *)"source", sizeof("source") - 1, ex_source, (long_u)(EX_RANGE|EX_DFLALL|EX_BANG| ( (EX_XFILE | EX_EXTRA)  | EX_NOSPC) |EX_TRLBAR|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK), ADDR_LINES},
+    [CMD_sort] = {(char_u *)"sort", sizeof("sort") - 1, ex_sort, (long_u)(EX_RANGE|EX_DFLALL|EX_WHOLEFOLD|EX_BANG|EX_EXTRA|EX_NOTRLCOM|EX_MODIFY), ADDR_LINES},
+    [CMD_split] = {(char_u *)"split", sizeof("split") - 1, ex_splitview, (long_u)(EX_BANG| ( (EX_XFILE | EX_EXTRA)  | EX_NOSPC) |EX_RANGE|EX_CMDARG|EX_ARGOPT|EX_TRLBAR), ADDR_OTHER},
+    [CMD_spellgood] = {(char_u *)"spellgood", sizeof("spellgood") - 1,  ex_ni , (long_u)(EX_BANG|EX_RANGE|EX_NEEDARG|EX_EXTRA|EX_TRLBAR), ADDR_OTHER},
+    [CMD_spelldump] = {(char_u *)"spelldump", sizeof("spelldump") - 1,  ex_ni , (long_u)(EX_BANG|EX_TRLBAR), ADDR_NONE},
+    [CMD_spellinfo] = {(char_u *)"spellinfo", sizeof("spellinfo") - 1,  ex_ni , (long_u)(EX_TRLBAR), ADDR_NONE},
+    [CMD_spellrepall] = {(char_u *)"spellrepall", sizeof("spellrepall") - 1,  ex_ni , (long_u)(EX_TRLBAR), ADDR_NONE},
+    [CMD_spellrare] = {(char_u *)"spellrare", sizeof("spellrare") - 1,  ex_ni , (long_u)(EX_BANG|EX_RANGE|EX_NEEDARG|EX_EXTRA|EX_TRLBAR), ADDR_OTHER},
+    [CMD_spellundo] = {(char_u *)"spellundo", sizeof("spellundo") - 1,  ex_ni , (long_u)(EX_BANG|EX_RANGE|EX_NEEDARG|EX_EXTRA|EX_TRLBAR), ADDR_OTHER},
+    [CMD_spellwrong] = {(char_u *)"spellwrong", sizeof("spellwrong") - 1,  ex_ni , (long_u)(EX_BANG|EX_RANGE|EX_NEEDARG|EX_EXTRA|EX_TRLBAR), ADDR_OTHER},
+    [CMD_sprevious] = {(char_u *)"sprevious", sizeof("sprevious") - 1, ex_previous, (long_u)(EX_EXTRA|EX_RANGE|EX_COUNT|EX_BANG|EX_CMDARG|EX_ARGOPT|EX_TRLBAR), ADDR_OTHER},
+    [CMD_srewind] = {(char_u *)"srewind", sizeof("srewind") - 1, ex_rewind, (long_u)(EX_EXTRA|EX_BANG|EX_CMDARG|EX_ARGOPT|EX_TRLBAR), ADDR_NONE},
+    [CMD_stop] = {(char_u *)"stop", sizeof("stop") - 1, ex_stop, (long_u)(EX_TRLBAR|EX_BANG|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_stag] = {(char_u *)"stag", sizeof("stag") - 1, ex_stag, (long_u)(EX_RANGE|EX_BANG| (EX_EXTRA | EX_NOSPC) |EX_TRLBAR|EX_ZEROR), ADDR_OTHER},
+    [CMD_startinsert] = {(char_u *)"startinsert", sizeof("startinsert") - 1, ex_startinsert, (long_u)(EX_BANG|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_startgreplace] = {(char_u *)"startgreplace", sizeof("startgreplace") - 1, ex_startinsert, (long_u)(EX_BANG|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_startreplace] = {(char_u *)"startreplace", sizeof("startreplace") - 1, ex_startinsert, (long_u)(EX_BANG|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_static] = {(char_u *)"static", sizeof("static") - 1, ex_wrongmodifier, (long_u)(EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK|EX_WHOLE), ADDR_NONE},
+    [CMD_stopinsert] = {(char_u *)"stopinsert", sizeof("stopinsert") - 1, ex_stopinsert, (long_u)(EX_BANG|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_stjump] = {(char_u *)"stjump", sizeof("stjump") - 1, ex_stag, (long_u)(EX_BANG|EX_TRLBAR| (EX_EXTRA | EX_NOSPC) ), ADDR_NONE},
+    [CMD_stselect] = {(char_u *)"stselect", sizeof("stselect") - 1, ex_stag, (long_u)(EX_BANG|EX_TRLBAR| (EX_EXTRA | EX_NOSPC) ), ADDR_NONE},
+    [CMD_sunhide] = {(char_u *)"sunhide", sizeof("sunhide") - 1, ex_buffer_all, (long_u)(EX_RANGE|EX_COUNT|EX_TRLBAR), ADDR_OTHER},
+    [CMD_sunmap] = {(char_u *)"sunmap", sizeof("sunmap") - 1, ex_unmap, (long_u)(EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_sunmenu] = {(char_u *)"sunmenu", sizeof("sunmenu") - 1,  ex_ni , (long_u)(EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_suspend] = {(char_u *)"suspend", sizeof("suspend") - 1, ex_stop, (long_u)(EX_TRLBAR|EX_BANG|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_sview] = {(char_u *)"sview", sizeof("sview") - 1, ex_splitview, (long_u)(EX_BANG| ( (EX_XFILE | EX_EXTRA)  | EX_NOSPC) |EX_RANGE|EX_CMDARG|EX_ARGOPT|EX_TRLBAR), ADDR_OTHER},
+    [CMD_swapname] = {(char_u *)"swapname", sizeof("swapname") - 1, ex_swapname, (long_u)(EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_syntax] = {(char_u *)"syntax", sizeof("syntax") - 1,  ex_ni , (long_u)(EX_EXTRA|EX_NOTRLCOM|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_syntime] = {(char_u *)"syntime", sizeof("syntime") - 1,  ex_ni , (long_u)(EX_NEEDARG| (EX_EXTRA | EX_NOSPC) |EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_syncbind] = {(char_u *)"syncbind", sizeof("syncbind") - 1, ex_syncbind, (long_u)(EX_TRLBAR), ADDR_NONE},
+    [CMD_smile] = {(char_u *)"smile", sizeof("smile") - 1, ex_smile, (long_u)(EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK|EX_SBOXOK), ADDR_NONE},
+    [CMD_t] = {(char_u *)"t", sizeof("t") - 1, ex_copymove, (long_u)(EX_RANGE|EX_WHOLEFOLD|EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK|EX_MODIFY), ADDR_LINES},
+    [CMD_tNext] = {(char_u *)"tNext", sizeof("tNext") - 1, ex_tag, (long_u)(EX_RANGE|EX_BANG|EX_TRLBAR|EX_ZEROR), ADDR_OTHER},
+    [CMD_tag] = {(char_u *)"tag", sizeof("tag") - 1, ex_tag, (long_u)(EX_RANGE|EX_BANG| (EX_EXTRA | EX_NOSPC) |EX_TRLBAR|EX_ZEROR), ADDR_OTHER},
+    [CMD_tags] = {(char_u *)"tags", sizeof("tags") - 1, do_tags, (long_u)(EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_tab] = {(char_u *)"tab", sizeof("tab") - 1, ex_wrongmodifier, (long_u)(EX_RANGE|EX_ZEROR|EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM), ADDR_TABS},
+    [CMD_tabclose] = {(char_u *)"tabclose", sizeof("tabclose") - 1, ex_tabclose, (long_u)(EX_BANG|EX_RANGE|EX_ZEROR|EX_EXTRA|EX_NOSPC|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK), ADDR_TABS},
+    [CMD_tabdo] = {(char_u *)"tabdo", sizeof("tabdo") - 1, ex_listdo, (long_u)(EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM|EX_RANGE|EX_DFLALL|EX_EXPAND), ADDR_TABS},
+    [CMD_tabedit] = {(char_u *)"tabedit", sizeof("tabedit") - 1, ex_splitview, (long_u)(EX_BANG| ( (EX_XFILE | EX_EXTRA)  | EX_NOSPC) |EX_RANGE|EX_ZEROR|EX_CMDARG|EX_ARGOPT|EX_TRLBAR), ADDR_TABS},
+    [CMD_tabfind] = {(char_u *)"tabfind", sizeof("tabfind") - 1, ex_splitview, (long_u)(EX_BANG| ( (EX_XFILE | EX_EXTRA)  | EX_NOSPC) |EX_RANGE|EX_ZEROR|EX_CMDARG|EX_ARGOPT|EX_NEEDARG|EX_TRLBAR), ADDR_TABS},
+    [CMD_tabfirst] = {(char_u *)"tabfirst", sizeof("tabfirst") - 1, ex_tabnext, (long_u)(EX_TRLBAR), ADDR_NONE},
+    [CMD_tabmove] = {(char_u *)"tabmove", sizeof("tabmove") - 1, ex_tabmove, (long_u)(EX_RANGE|EX_ZEROR|EX_EXTRA|EX_NOSPC|EX_TRLBAR), ADDR_TABS},
+    [CMD_tablast] = {(char_u *)"tablast", sizeof("tablast") - 1, ex_tabnext, (long_u)(EX_TRLBAR), ADDR_NONE},
+    [CMD_tabnext] = {(char_u *)"tabnext", sizeof("tabnext") - 1, ex_tabnext, (long_u)(EX_RANGE|EX_ZEROR|EX_EXTRA|EX_NOSPC|EX_TRLBAR), ADDR_TABS},
+    [CMD_tabnew] = {(char_u *)"tabnew", sizeof("tabnew") - 1, ex_splitview, (long_u)(EX_BANG| ( (EX_XFILE | EX_EXTRA)  | EX_NOSPC) |EX_RANGE|EX_ZEROR|EX_CMDARG|EX_ARGOPT|EX_TRLBAR), ADDR_TABS},
+    [CMD_tabonly] = {(char_u *)"tabonly", sizeof("tabonly") - 1, ex_tabonly, (long_u)(EX_BANG|EX_RANGE|EX_ZEROR|EX_EXTRA|EX_NOSPC|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK), ADDR_TABS},
+    [CMD_tabprevious] = {(char_u *)"tabprevious", sizeof("tabprevious") - 1, ex_tabnext, (long_u)(EX_RANGE|EX_ZEROR|EX_EXTRA|EX_NOSPC|EX_TRLBAR), ADDR_TABS_RELATIVE},
+    [CMD_tabNext] = {(char_u *)"tabNext", sizeof("tabNext") - 1, ex_tabnext, (long_u)(EX_RANGE|EX_ZEROR|EX_EXTRA|EX_NOSPC|EX_TRLBAR), ADDR_TABS_RELATIVE},
+    [CMD_tabrewind] = {(char_u *)"tabrewind", sizeof("tabrewind") - 1, ex_tabnext, (long_u)(EX_TRLBAR), ADDR_NONE},
+    [CMD_tabs] = {(char_u *)"tabs", sizeof("tabs") - 1, ex_tabs, (long_u)(EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_tcd] = {(char_u *)"tcd", sizeof("tcd") - 1, ex_cd, (long_u)(EX_BANG| ( (EX_XFILE | EX_EXTRA)  | EX_NOSPC) |EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_tchdir] = {(char_u *)"tchdir", sizeof("tchdir") - 1, ex_cd, (long_u)(EX_BANG| ( (EX_XFILE | EX_EXTRA)  | EX_NOSPC) |EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_tcl] = {(char_u *)"tcl", sizeof("tcl") - 1,  ex_script_ni , (long_u)(EX_RANGE|EX_EXTRA|EX_NEEDARG|EX_CMDWIN|EX_LOCK_OK|EX_RESTRICT), ADDR_LINES},
+    [CMD_tcldo] = {(char_u *)"tcldo", sizeof("tcldo") - 1,  ex_ni , (long_u)(EX_RANGE|EX_DFLALL|EX_EXTRA|EX_NEEDARG|EX_CMDWIN|EX_LOCK_OK|EX_RESTRICT), ADDR_LINES},
+    [CMD_tclfile] = {(char_u *)"tclfile", sizeof("tclfile") - 1,  ex_ni , (long_u)(EX_RANGE| ( (EX_XFILE | EX_EXTRA)  | EX_NOSPC) |EX_NEEDARG|EX_CMDWIN|EX_LOCK_OK|EX_RESTRICT), ADDR_LINES},
+    [CMD_tearoff] = {(char_u *)"tearoff", sizeof("tearoff") - 1,  ex_ni , (long_u)(EX_NEEDARG|EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_terminal] = {(char_u *)"terminal", sizeof("terminal") - 1,  ex_ni , (long_u)(EX_RANGE|EX_BANG| (EX_XFILE | EX_EXTRA) |EX_CMDWIN|EX_LOCK_OK), ADDR_LINES},
+    [CMD_tfirst] = {(char_u *)"tfirst", sizeof("tfirst") - 1, ex_tag, (long_u)(EX_RANGE|EX_BANG|EX_TRLBAR|EX_ZEROR), ADDR_OTHER},
+    [CMD_throw] = {(char_u *)"throw", sizeof("throw") - 1,  ex_ni , (long_u)(EX_EXTRA|EX_NEEDARG|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK|EX_WHOLE), ADDR_NONE},
+    [CMD_this] = {(char_u *)"this", sizeof("this") - 1, ex_wrongmodifier, (long_u)(EX_EXTRA|EX_NEEDARG|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK|EX_WHOLE), ADDR_NONE},
+    [CMD_tjump] = {(char_u *)"tjump", sizeof("tjump") - 1, ex_tag, (long_u)(EX_BANG|EX_TRLBAR| (EX_EXTRA | EX_NOSPC) ), ADDR_NONE},
+    [CMD_tlast] = {(char_u *)"tlast", sizeof("tlast") - 1, ex_tag, (long_u)(EX_BANG|EX_TRLBAR), ADDR_NONE},
+    [CMD_tlmenu] = {(char_u *)"tlmenu", sizeof("tlmenu") - 1,  ex_ni , (long_u)(EX_RANGE|EX_ZEROR|EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK), ADDR_OTHER},
+    [CMD_tlnoremenu] = {(char_u *)"tlnoremenu", sizeof("tlnoremenu") - 1,  ex_ni , (long_u)(EX_RANGE|EX_ZEROR|EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK), ADDR_OTHER},
+    [CMD_tlunmenu] = {(char_u *)"tlunmenu", sizeof("tlunmenu") - 1,  ex_ni , (long_u)(EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_tmenu] = {(char_u *)"tmenu", sizeof("tmenu") - 1,  ex_ni , (long_u)(EX_RANGE|EX_ZEROR|EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK), ADDR_OTHER},
+    [CMD_tmap] = {(char_u *)"tmap", sizeof("tmap") - 1, ex_map, (long_u)(EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_tmapclear] = {(char_u *)"tmapclear", sizeof("tmapclear") - 1, ex_mapclear, (long_u)(EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_tnext] = {(char_u *)"tnext", sizeof("tnext") - 1, ex_tag, (long_u)(EX_RANGE|EX_BANG|EX_TRLBAR|EX_ZEROR), ADDR_OTHER},
+    [CMD_tnoremap] = {(char_u *)"tnoremap", sizeof("tnoremap") - 1, ex_map, (long_u)(EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_topleft] = {(char_u *)"topleft", sizeof("topleft") - 1, ex_wrongmodifier, (long_u)(EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM), ADDR_NONE},
+    [CMD_tprevious] = {(char_u *)"tprevious", sizeof("tprevious") - 1, ex_tag, (long_u)(EX_RANGE|EX_BANG|EX_TRLBAR|EX_ZEROR), ADDR_OTHER},
+    [CMD_trewind] = {(char_u *)"trewind", sizeof("trewind") - 1, ex_tag, (long_u)(EX_RANGE|EX_BANG|EX_TRLBAR|EX_ZEROR), ADDR_OTHER},
+    [CMD_try] = {(char_u *)"try", sizeof("try") - 1,  ex_ni , (long_u)(EX_TRLBAR|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_tselect] = {(char_u *)"tselect", sizeof("tselect") - 1, ex_tag, (long_u)(EX_BANG|EX_TRLBAR| (EX_EXTRA | EX_NOSPC) ), ADDR_NONE},
+    [CMD_tunmenu] = {(char_u *)"tunmenu", sizeof("tunmenu") - 1,  ex_ni , (long_u)(EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_tunmap] = {(char_u *)"tunmap", sizeof("tunmap") - 1, ex_unmap, (long_u)(EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_type] = {(char_u *)"type", sizeof("type") - 1,  ex_ni , (long_u)(EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK|EX_WHOLE|EX_EXPORT), ADDR_NONE},
+    [CMD_undo] = {(char_u *)"undo", sizeof("undo") - 1, ex_undo, (long_u)(EX_RANGE|EX_COUNT|EX_ZEROR|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK), ADDR_OTHER},
+    [CMD_undojoin] = {(char_u *)"undojoin", sizeof("undojoin") - 1, ex_undojoin, (long_u)(EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_undolist] = {(char_u *)"undolist", sizeof("undolist") - 1, ex_undolist, (long_u)(EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_unabbreviate] = {(char_u *)"unabbreviate", sizeof("unabbreviate") - 1, ex_abbreviate, (long_u)(EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_unhide] = {(char_u *)"unhide", sizeof("unhide") - 1, ex_buffer_all, (long_u)(EX_RANGE|EX_COUNT|EX_TRLBAR), ADDR_OTHER},
+    [CMD_uniq] = {(char_u *)"uniq", sizeof("uniq") - 1, ex_uniq, (long_u)(EX_RANGE|EX_DFLALL|EX_WHOLEFOLD|EX_BANG|EX_EXTRA|EX_NOTRLCOM|EX_MODIFY), ADDR_LINES},
+    [CMD_unlet] = {(char_u *)"unlet", sizeof("unlet") - 1,  ex_ni , (long_u)(EX_BANG|EX_EXTRA|EX_NEEDARG|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_unlockvar] = {(char_u *)"unlockvar", sizeof("unlockvar") - 1,  ex_ni , (long_u)(EX_BANG|EX_EXTRA|EX_NEEDARG|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_unmap] = {(char_u *)"unmap", sizeof("unmap") - 1, ex_unmap, (long_u)(EX_BANG|EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_unmenu] = {(char_u *)"unmenu", sizeof("unmenu") - 1,  ex_ni , (long_u)(EX_BANG|EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_unsilent] = {(char_u *)"unsilent", sizeof("unsilent") - 1, ex_wrongmodifier, (long_u)(EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_update] = {(char_u *)"update", sizeof("update") - 1, ex_update, (long_u)(EX_RANGE|EX_WHOLEFOLD|EX_BANG| ( (EX_XFILE | EX_EXTRA)  | EX_NOSPC) |EX_ARGOPT|EX_DFLALL|EX_TRLBAR), ADDR_LINES},
+    [CMD_vglobal] = {(char_u *)"vglobal", sizeof("vglobal") - 1, ex_global, (long_u)(EX_RANGE|EX_WHOLEFOLD|EX_EXTRA|EX_DFLALL|EX_CMDWIN|EX_LOCK_OK|EX_NONWHITE_OK), ADDR_LINES},
+    [CMD_var] = {(char_u *)"var", sizeof("var") - 1,  ex_ni , (long_u)(EX_EXTRA|EX_NOTRLCOM|EX_EXPR_ARG|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK|EX_WHOLE|EX_EXPORT), ADDR_NONE},
+    [CMD_version] = {(char_u *)"version", sizeof("version") - 1, ex_version, (long_u)(EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_verbose] = {(char_u *)"verbose", sizeof("verbose") - 1, ex_wrongmodifier, (long_u)(EX_NEEDARG|EX_RANGE|EX_EXTRA|EX_NOTRLCOM|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK), ADDR_OTHER},
+    [CMD_vertical] = {(char_u *)"vertical", sizeof("vertical") - 1, ex_wrongmodifier, (long_u)(EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM), ADDR_NONE},
+    [CMD_visual] = {(char_u *)"visual", sizeof("visual") - 1, ex_edit, (long_u)(EX_BANG| ( (EX_XFILE | EX_EXTRA)  | EX_NOSPC) |EX_CMDARG|EX_ARGOPT|EX_TRLBAR), ADDR_NONE},
+    [CMD_view] = {(char_u *)"view", sizeof("view") - 1, ex_edit, (long_u)(EX_BANG| ( (EX_XFILE | EX_EXTRA)  | EX_NOSPC) |EX_CMDARG|EX_ARGOPT|EX_TRLBAR), ADDR_NONE},
+    [CMD_vimgrep] = {(char_u *)"vimgrep", sizeof("vimgrep") - 1,  ex_ni , (long_u)(EX_RANGE|EX_BANG|EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM|EX_TRLBAR|EX_XFILE|EX_LOCK_OK), ADDR_OTHER},
+    [CMD_vimgrepadd] = {(char_u *)"vimgrepadd", sizeof("vimgrepadd") - 1,  ex_ni , (long_u)(EX_RANGE|EX_BANG|EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM|EX_TRLBAR|EX_XFILE|EX_LOCK_OK), ADDR_OTHER},
+    [CMD_vim9cmd] = {(char_u *)"vim9cmd", sizeof("vim9cmd") - 1, ex_wrongmodifier, (long_u)(EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_vim9script] = {(char_u *)"vim9script", sizeof("vim9script") - 1, ex_vim9script, (long_u)( (EX_EXTRA | EX_NOSPC) |EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_viusage] = {(char_u *)"viusage", sizeof("viusage") - 1, ex_viusage, (long_u)(EX_TRLBAR), ADDR_NONE},
+    [CMD_vmap] = {(char_u *)"vmap", sizeof("vmap") - 1, ex_map, (long_u)(EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_vmapclear] = {(char_u *)"vmapclear", sizeof("vmapclear") - 1, ex_mapclear, (long_u)(EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_vmenu] = {(char_u *)"vmenu", sizeof("vmenu") - 1,  ex_ni , (long_u)(EX_RANGE|EX_ZEROR|EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK), ADDR_OTHER},
+    [CMD_vnoremap] = {(char_u *)"vnoremap", sizeof("vnoremap") - 1, ex_map, (long_u)(EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_vnew] = {(char_u *)"vnew", sizeof("vnew") - 1, ex_splitview, (long_u)(EX_BANG| ( (EX_XFILE | EX_EXTRA)  | EX_NOSPC) |EX_RANGE|EX_CMDARG|EX_ARGOPT|EX_TRLBAR), ADDR_OTHER},
+    [CMD_vnoremenu] = {(char_u *)"vnoremenu", sizeof("vnoremenu") - 1,  ex_ni , (long_u)(EX_RANGE|EX_ZEROR|EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK), ADDR_OTHER},
+    [CMD_vsplit] = {(char_u *)"vsplit", sizeof("vsplit") - 1, ex_splitview, (long_u)(EX_BANG| ( (EX_XFILE | EX_EXTRA)  | EX_NOSPC) |EX_RANGE|EX_CMDARG|EX_ARGOPT|EX_TRLBAR), ADDR_OTHER},
+    [CMD_vunmap] = {(char_u *)"vunmap", sizeof("vunmap") - 1, ex_unmap, (long_u)(EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_vunmenu] = {(char_u *)"vunmenu", sizeof("vunmenu") - 1,  ex_ni , (long_u)(EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_write] = {(char_u *)"write", sizeof("write") - 1, ex_write, (long_u)(EX_RANGE|EX_WHOLEFOLD|EX_BANG| ( (EX_XFILE | EX_EXTRA)  | EX_NOSPC) |EX_ARGOPT|EX_DFLALL|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK), ADDR_LINES},
+    [CMD_wNext] = {(char_u *)"wNext", sizeof("wNext") - 1, ex_wnext, (long_u)(EX_RANGE|EX_WHOLEFOLD|EX_BANG| ( (EX_XFILE | EX_EXTRA)  | EX_NOSPC) |EX_ARGOPT|EX_TRLBAR), ADDR_OTHER},
+    [CMD_wall] = {(char_u *)"wall", sizeof("wall") - 1, do_wqall, (long_u)(EX_BANG|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_while] = {(char_u *)"while", sizeof("while") - 1,  ex_ni , (long_u)(EX_EXTRA|EX_NOTRLCOM|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK|EX_WHOLE), ADDR_NONE},
+    [CMD_winsize] = {(char_u *)"winsize", sizeof("winsize") - 1, ex_winsize, (long_u)(EX_EXTRA|EX_NEEDARG|EX_TRLBAR), ADDR_NONE},
+    [CMD_wincmd] = {(char_u *)"wincmd", sizeof("wincmd") - 1, ex_wincmd, (long_u)(EX_NEEDARG| (EX_EXTRA | EX_NOSPC) |EX_RANGE|EX_CMDWIN|EX_LOCK_OK), ADDR_OTHER},
+    [CMD_windo] = {(char_u *)"windo", sizeof("windo") - 1, ex_listdo, (long_u)(EX_NEEDARG|EX_EXTRA|EX_NOTRLCOM|EX_RANGE|EX_DFLALL|EX_EXPAND), ADDR_WINDOWS},
+    [CMD_winpos] = {(char_u *)"winpos", sizeof("winpos") - 1, ex_winpos, (long_u)(EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_wlrestore] = {(char_u *)"wlrestore", sizeof("wlrestore") - 1,  ex_ni , (long_u)(EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK|EX_BANG), ADDR_NONE},
+    [CMD_wnext] = {(char_u *)"wnext", sizeof("wnext") - 1, ex_wnext, (long_u)(EX_RANGE|EX_BANG| ( (EX_XFILE | EX_EXTRA)  | EX_NOSPC) |EX_ARGOPT|EX_TRLBAR), ADDR_OTHER},
+    [CMD_wprevious] = {(char_u *)"wprevious", sizeof("wprevious") - 1, ex_wnext, (long_u)(EX_RANGE|EX_BANG| ( (EX_XFILE | EX_EXTRA)  | EX_NOSPC) |EX_ARGOPT|EX_TRLBAR), ADDR_OTHER},
+    [CMD_wq] = {(char_u *)"wq", sizeof("wq") - 1, ex_exit, (long_u)(EX_RANGE|EX_WHOLEFOLD|EX_BANG| ( (EX_XFILE | EX_EXTRA)  | EX_NOSPC) |EX_ARGOPT|EX_DFLALL|EX_TRLBAR), ADDR_LINES},
+    [CMD_wqall] = {(char_u *)"wqall", sizeof("wqall") - 1, do_wqall, (long_u)(EX_BANG| ( (EX_XFILE | EX_EXTRA)  | EX_NOSPC) |EX_ARGOPT|EX_TRLBAR), ADDR_NONE},
+    [CMD_wundo] = {(char_u *)"wundo", sizeof("wundo") - 1,  ex_ni , (long_u)(EX_BANG|EX_NEEDARG| ( (EX_XFILE | EX_EXTRA)  | EX_NOSPC) ), ADDR_NONE},
+    [CMD_wviminfo] = {(char_u *)"wviminfo", sizeof("wviminfo") - 1,  ex_ni , (long_u)(EX_BANG| ( (EX_XFILE | EX_EXTRA)  | EX_NOSPC) |EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_xit] = {(char_u *)"xit", sizeof("xit") - 1, ex_exit, (long_u)(EX_RANGE|EX_WHOLEFOLD|EX_BANG| ( (EX_XFILE | EX_EXTRA)  | EX_NOSPC) |EX_ARGOPT|EX_DFLALL|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK), ADDR_LINES},
+    [CMD_xall] = {(char_u *)"xall", sizeof("xall") - 1, do_wqall, (long_u)(EX_BANG|EX_TRLBAR), ADDR_NONE},
+    [CMD_xmap] = {(char_u *)"xmap", sizeof("xmap") - 1, ex_map, (long_u)(EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_xmapclear] = {(char_u *)"xmapclear", sizeof("xmapclear") - 1, ex_mapclear, (long_u)(EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_xmenu] = {(char_u *)"xmenu", sizeof("xmenu") - 1,  ex_ni , (long_u)(EX_RANGE|EX_ZEROR|EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK), ADDR_OTHER},
+    [CMD_xnoremap] = {(char_u *)"xnoremap", sizeof("xnoremap") - 1, ex_map, (long_u)(EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_xnoremenu] = {(char_u *)"xnoremenu", sizeof("xnoremenu") - 1,  ex_ni , (long_u)(EX_RANGE|EX_ZEROR|EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK), ADDR_OTHER},
+    [CMD_xrestore] = {(char_u *)"xrestore", sizeof("xrestore") - 1,  ex_ni , (long_u)(EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_xunmap] = {(char_u *)"xunmap", sizeof("xunmap") - 1, ex_unmap, (long_u)(EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_xunmenu] = {(char_u *)"xunmenu", sizeof("xunmenu") - 1,  ex_ni , (long_u)(EX_EXTRA|EX_TRLBAR|EX_NOTRLCOM|EX_CTRLV|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_yank] = {(char_u *)"yank", sizeof("yank") - 1, ex_operators, (long_u)(EX_RANGE|EX_WHOLEFOLD|EX_REGSTR|EX_COUNT|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK), ADDR_LINES},
+    [CMD_z] = {(char_u *)"z", sizeof("z") - 1, ex_z, (long_u)(EX_RANGE|EX_WHOLEFOLD|EX_BANG|EX_EXTRA|EX_FLAGS|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK), ADDR_LINES},
+
+    [CMD_bang] = {(char_u *)"!", sizeof("!") - 1, ex_bang, (long_u)(EX_RANGE|EX_WHOLEFOLD|EX_BANG| (EX_XFILE | EX_EXTRA) |EX_CMDWIN|EX_LOCK_OK|EX_NONWHITE_OK), ADDR_LINES},
+    [CMD_pound] = {(char_u *)"#", sizeof("#") - 1, ex_print, (long_u)(EX_RANGE|EX_WHOLEFOLD|EX_COUNT|EX_FLAGS|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK), ADDR_LINES},
+    [CMD_and] = {(char_u *)"&", sizeof("&") - 1, ex_substitute, (long_u)(EX_RANGE|EX_WHOLEFOLD|EX_EXTRA|EX_CMDWIN|EX_LOCK_OK|EX_MODIFY|EX_NONWHITE_OK), ADDR_LINES},
+    [CMD_star] = {(char_u *)"*", sizeof("*") - 1, ex_at, (long_u)(EX_RANGE|EX_WHOLEFOLD|EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK|EX_NONWHITE_OK), ADDR_LINES},
+    [CMD_lshift] = {(char_u *)"<", sizeof("<") - 1, ex_operators, (long_u)(EX_RANGE|EX_WHOLEFOLD|EX_COUNT|EX_FLAGS|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK|EX_MODIFY), ADDR_LINES},
+    [CMD_equal] = {(char_u *)"=", sizeof("=") - 1, ex_equal, (long_u)(EX_RANGE|EX_TRLBAR|EX_DFLALL|EX_FLAGS|EX_CMDWIN|EX_LOCK_OK), ADDR_LINES},
+    [CMD_rshift] = {(char_u *)">", sizeof(">") - 1, ex_operators, (long_u)(EX_RANGE|EX_WHOLEFOLD|EX_COUNT|EX_FLAGS|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK|EX_MODIFY), ADDR_LINES},
+    [CMD_at] = {(char_u *)"@", sizeof("@") - 1, ex_at, (long_u)(EX_RANGE|EX_WHOLEFOLD|EX_EXTRA|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK|EX_NONWHITE_OK), ADDR_LINES},
+    [CMD_block] = {(char_u *)"{", sizeof("{") - 1,  ex_ni , (long_u)(EX_TRLBAR|EX_LOCK_OK|EX_CMDWIN), ADDR_NONE},
+    [CMD_endblock] = {(char_u *)"}", sizeof("}") - 1,  ex_ni , (long_u)(EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_tilde] = {(char_u *)"~", sizeof("~") - 1, ex_substitute, (long_u)(EX_RANGE|EX_WHOLEFOLD|EX_EXTRA|EX_CMDWIN|EX_LOCK_OK|EX_MODIFY|EX_NONWHITE_OK), ADDR_LINES},
+
+    [CMD_Next] = {(char_u *)"Next", sizeof("Next") - 1, ex_previous, (long_u)(EX_EXTRA|EX_RANGE|EX_COUNT|EX_BANG|EX_CMDARG|EX_ARGOPT|EX_TRLBAR), ADDR_OTHER},
+    [CMD_Print] = {(char_u *)"Print", sizeof("Print") - 1, ex_print, (long_u)(EX_RANGE|EX_WHOLEFOLD|EX_COUNT|EX_FLAGS|EX_TRLBAR|EX_CMDWIN|EX_LOCK_OK), ADDR_LINES},
+    [CMD_X] = {(char_u *)"X", sizeof("X") - 1,  ex_ni , (long_u)(EX_TRLBAR), ADDR_NONE},
 
-#undef EXCMD
+    [CMD_increment] = {(char_u *)"++", sizeof("++") - 1,  ex_ni , (long_u)(EX_EXTRA|EX_TRLBAR|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
+    [CMD_decrement] = {(char_u *)"--", sizeof("--") - 1,  ex_ni , (long_u)(EX_EXTRA|EX_TRLBAR|EX_SBOXOK|EX_CMDWIN|EX_LOCK_OK), ADDR_NONE},
 
 };
 
+// Two lists, kept aligned by the compiler rather than by an X-macro: every row
+// is written [CMD_append] = {...}, so it lands at its own enumerator whatever
+// order the rows are in.  That is stronger than the macro's guarantee of equal
+// *order*.  A dropped last row is caught here; a dropped middle row leaves a
+// zeroed hole, which the 600-command sweep catches.
+static_assert(sizeof(cmdnames) / sizeof(cmdnames[0]) == CMD_SIZE, "cmdnames[] and enum CMD_index have drifted apart");
+
 // ---------------- end ex_cmds.h ----------------
 // ---------------- begin ex_cmdidxs.h ----------------
 static const unsigned short cmdidxs1[26] =
@@ -44528,7 +42725,7 @@
 
     if (curbuf->b_sfname != NULL && *curbuf->b_sfname != NUL)
     {
-        return fnamecmp(ffname, curbuf->b_sfname) != 0;
+        return  vim_fnamecmp((char_u *)(ffname), (char_u *)(curbuf->b_sfname))  != 0;
     }
 
     return otherfile(ffname);
@@ -44574,7 +42771,7 @@
         need_wait_return = FALSE;
         ex_pressedreturn = FALSE;
         ex_no_reprint = FALSE;
-        changedtick = CHANGEDTICK(curbuf);
+        changedtick =  ((curbuf)->b_ct_di.di_tv.vval.v_number) ;
         prev_msg_row = msg_row;
         prev_line = curwin->w_cursor.lnum;
         if (improved)
@@ -44588,7 +42785,7 @@
         }
         lines_left = Rows - 1;
 
-        if ((prev_line != curwin->w_cursor.lnum || changedtick != CHANGEDTICK(curbuf)) && !ex_no_reprint)
+        if ((prev_line != curwin->w_cursor.lnum || changedtick !=  ((curbuf)->b_ct_di.di_tv.vval.v_number) ) && !ex_no_reprint)
         {
             if (curbuf->b_ml.ml_flags & ML_EMPTY)
             {
@@ -44678,8 +42875,6 @@
     int         count = 0;
     int         did_inc_RedrawingDisabled = FALSE;
     int         retval = OK;
-# define cmd_getline fgetline
-# define cmd_cookie cookie
     static int  call_depth = 0;
 
     if (call_depth >= 200)
@@ -44699,7 +42894,6 @@
     next_cmdline = cmdline;
     do
     {
-
         if (next_cmdline == NULL)
         {
             did_emsg = FALSE;
@@ -44762,18 +42956,19 @@
             }
         }
 
-        if ((p_verbose >= 15 && SOURCING_NAME != NULL) || p_verbose >= 16)
+        if ((p_verbose >= 15 &&  (((estack_T *)exestack.ga_data)[exestack.ga_len - 1].es_name)  != NULL) || p_verbose >= 16)
         {
-            msg_verbose_cmd(SOURCING_LNUM, cmdline_copy);
+            msg_verbose_cmd( (((estack_T *)exestack.ga_data)[exestack.ga_len - 1].es_lnum) , cmdline_copy);
         }
 
         ++recursive;
-        next_cmdline = do_one_cmd(&cmdline_copy, flags, cmd_getline, cmd_cookie);
+        next_cmdline = do_one_cmd(&cmdline_copy, flags,  fgetline ,  cookie );
         --recursive;
 
         if (next_cmdline == NULL)
         {
-            VIM_CLEAR(cmdline_copy);
+             vim_free(cmdline_copy);
+             (cmdline_copy) = NULL;
 
             if (getline_equal(fgetline, cookie, getexline) && new_last_cmdline != NULL)
             {
@@ -44784,7 +42979,7 @@
         }
         else
         {
-            STRMOVE(cmdline_copy, next_cmdline);
+              memmove((char *)((cmdline_copy)), (char *)((next_cmdline)),  strlen((char *)(next_cmdline))  + 1)  ;
             next_cmdline = cmdline_copy;
         }
 
@@ -44823,13 +43018,13 @@
 }
 
     int
-getline_equal(char_u      *(*fgetline)(int, void *, int, getline_opt_T), void        *cookie UNUSED, char_u      *(*func)(int, void *, int, getline_opt_T))
+getline_equal(char_u      *(*fgetline)(int, void *, int, getline_opt_T), void        *cookie  __attribute__((unused)) , char_u      *(*func)(int, void *, int, getline_opt_T))
 {
     return fgetline == func;
 }
 
     void *
-getline_cookie(char_u      *(*fgetline)(int, void *, int, getline_opt_T) UNUSED, void        *cookie)
+getline_cookie(char_u      *(*fgetline)(int, void *, int, getline_opt_T)  __attribute__((unused)) , void        *cookie)
 {
     return cookie;
 }
@@ -44889,7 +43084,7 @@
     win_T       *wp;
     int         nr = 0;
 
-    FOR_ALL_WINDOWS(wp)
+     for ((wp) = firstwin; (wp) != NULL; (wp) = (wp)->w_next) 
     {
         ++nr;
         if (wp == win)
@@ -44906,7 +43101,7 @@
     tabpage_T   *tp;
     int         nr = 0;
 
-    FOR_ALL_TABPAGES(tp)
+     for ((tp) = first_tabpage; (tp) != NULL; (tp) = (tp)->tp_next) 
     {
         ++nr;
         if (tp == tab)
@@ -44918,7 +43113,7 @@
 }
 
     static int
-comment_start(char_u *p, int starts_with_colon UNUSED)
+comment_start(char_u *p, int starts_with_colon  __attribute__((unused)) )
 {
     if (in_vim9script())
     {
@@ -44927,11 +43122,6 @@
     return *p == '"';
 }
 
-#define CURRENT_WIN_NR current_win_nr(curwin)
-#define LAST_WIN_NR current_win_nr(NULL)
-#define CURRENT_TAB_NR current_tab_nr(curtab)
-#define LAST_TAB_NR current_tab_nr(NULL)
-
     static char_u *
 do_one_cmd(char_u      **cmdlinep, int         flags, char_u      *(*fgetline)(int, void *, int, getline_opt_T), void        *cookie)
 {
@@ -44951,7 +43141,7 @@
     int         sourcing = flags & DOCMD_VERBOSE;
     int         did_append_cmd = FALSE;
 
-    CLEAR_FIELD(ea);
+      memset((&(ea)), (0), (sizeof(ea)))  ;
     ea.line1 = 1;
     ea.line2 = 1;
 
@@ -44992,7 +43182,7 @@
             {
                 may_have_range = TRUE;
             }
-            if (p < ea.cmd && !VIM_ISWHITE(*p))
+            if (p < ea.cmd && ! ((*p) == ' ' || (*p) == '\t') )
             {
                 break;
             }
@@ -45032,7 +43222,7 @@
 
     ea.cmd = cmd;
 
-    if (!IS_USER_CMDIDX(ea.cmdidx))
+    if (! ((int)(ea.cmdidx) < 0) )
     {
         if (ea.cmdidx != CMD_SIZE)
         {
@@ -45074,12 +43264,12 @@
         goto doend;
     }
 
-    if (p != NULL && ea.cmdidx == CMD_SIZE && !ea.skip && ASCII_ISUPPER(*ea.cmd) && has_cmdundefined())
+    if (p != NULL && ea.cmdidx == CMD_SIZE && !ea.skip &&  ((unsigned)(*ea.cmd) - 'A' < 26)  && has_cmdundefined())
     {
         int ret;
 
         p = ea.cmd;
-        while (ASCII_ISALNUM(*p))
+        while ( ( ( ((unsigned)(*p) - 'A' < 26)  ||  ((unsigned)(*p) - 'a' < 26) )  ||  ((unsigned)(*p) - '0' < 10) ) )
         {
             ++p;
         }
@@ -45097,7 +43287,7 @@
         }
         goto doend;
     }
-    if (*p == '!' && ea.cmd[1] == 0151 && ea.cmd[0] == 78 && !IS_USER_CMDIDX(ea.cmdidx))
+    if (*p == '!' && ea.cmd[1] == 0151 && ea.cmd[0] == 78 && ! ((int)(ea.cmdidx) < 0) )
     {
         errormsg = uc_fun_cmd();
         goto doend;
@@ -45107,7 +43297,7 @@
     {
         if (!ea.skip)
         {
-            STRCPY(IObuff, _(e_not_an_editor_command));
+             strcpy((char *)(IObuff), (char *)(_(e_not_an_editor_command))) ;
             if (!sourcing)
             {
                 if (after_modifier != NULL)
@@ -45126,7 +43316,7 @@
         goto doend;
     }
 
-    ni = (!IS_USER_CMDIDX(ea.cmdidx) && (cmdnames[ea.cmdidx].cmd_func == ex_ni || cmdnames[ea.cmdidx].cmd_func == ex_script_ni));
+    ni = (! ((int)(ea.cmdidx) < 0)  && (cmdnames[ea.cmdidx].cmd_func == ex_ni || cmdnames[ea.cmdidx].cmd_func == ex_script_ni));
 
     if (ea.cmdidx == CMD_if)
     {
@@ -45151,7 +43341,7 @@
         ea.forceit = FALSE;
     }
 
-    if (!IS_USER_CMDIDX(ea.cmdidx))
+    if (! ((int)(ea.cmdidx) < 0) )
     {
         ea.argt = (long)cmdnames[(int)ea.cmdidx].cmd_argt;
     }
@@ -45169,7 +43359,7 @@
             goto doend;
         }
 
-        if (!IS_USER_CMDIDX(ea.cmdidx))
+        if (! ((int)(ea.cmdidx) < 0) )
         {
             if (cmdwin_type != 0 && !(ea.argt & EX_CMDWIN))
             {
@@ -45183,7 +43373,7 @@
             }
         }
 
-        if (!(ea.argt & (EX_CMDWIN | EX_LOCK_OK)) && ea.cmdidx != CMD_checktime && ea.cmdidx != CMD_edit && ea.cmdidx != CMD_file && !IS_USER_CMDIDX(ea.cmdidx) && curbuf_locked())
+        if (!(ea.argt & (EX_CMDWIN | EX_LOCK_OK)) && ea.cmdidx != CMD_checktime && ea.cmdidx != CMD_edit && ea.cmdidx != CMD_file && ! ((int)(ea.cmdidx) < 0)  && curbuf_locked())
         {
             goto doend;
         }
@@ -45319,7 +43509,7 @@
         {
             if (*p == '\\' && p[1] == '\n')
             {
-                STRMOVE(p, p + 1);
+                  memmove((char *)((p)), (char *)((p + 1)),  strlen((char *)(p + 1))  + 1)  ;
             }
             else if (*p == '\n' && !(ea.argt & EX_EXPR_ARG))
             {
@@ -45335,21 +43525,21 @@
         address_default_all(&ea);
     }
 
-    if (       (ea.argt & EX_REGSTR) && *ea.arg != NUL && (!IS_USER_CMDIDX(ea.cmdidx) || *ea.arg != '=') && !((ea.argt & EX_COUNT) && VIM_ISDIGIT(*ea.arg)))
+    if (       (ea.argt & EX_REGSTR) && *ea.arg != NUL && (! ((int)(ea.cmdidx) < 0)  || *ea.arg != '=') && !((ea.argt & EX_COUNT) &&  ((unsigned)(*ea.arg) - '0' < 10) ))
     {
         if (*ea.arg == '*' || *ea.arg == '+')
         {
             errormsg = _(e_invalid_register_name);
             goto doend;
         }
-        if (valid_yank_reg(*ea.arg, (!IS_USER_CMDIDX(ea.cmdidx) && ea.cmdidx != CMD_put && ea.cmdidx != CMD_iput)))
+        if (valid_yank_reg(*ea.arg, (! ((int)(ea.cmdidx) < 0)  && ea.cmdidx != CMD_put && ea.cmdidx != CMD_iput)))
         {
             ea.regname = *ea.arg++;
             ea.arg = skipwhite(ea.arg);
         }
     }
 
-    if ((ea.argt & EX_COUNT) && VIM_ISDIGIT(*ea.arg) && (!(ea.argt & EX_BUFNAME) || *(p = skipdigits(ea.arg + 1)) == NUL || VIM_ISWHITE(*p)))
+    if ((ea.argt & EX_COUNT) &&  ((unsigned)(*ea.arg) - '0' < 10)  && (!(ea.argt & EX_BUFNAME) || *(p = skipdigits(ea.arg + 1)) == NUL ||  ((*p) == ' ' || (*p) == '\t') ))
     {
         n = getdigits_quoted(&ea.arg);
         ea.arg = skipwhite(ea.arg);
@@ -45406,7 +43596,7 @@
         goto doend;
     }
 
-    if ((ea.argt & EX_BUFNAME) && *ea.arg != NUL && ea.addr_count == 0 && !IS_USER_CMDIDX(ea.cmdidx))
+    if ((ea.argt & EX_BUFNAME) && *ea.arg != NUL && ea.addr_count == 0 && ! ((int)(ea.cmdidx) < 0) )
     {
         if (ea.cmdidx == CMD_bdelete || ea.cmdidx == CMD_bwipeout || ea.cmdidx == CMD_bunload)
         {
@@ -45414,8 +43604,8 @@
         }
         else
         {
-            p = ea.arg + STRLEN(ea.arg);
-            while (p > ea.arg && VIM_ISWHITE(p[-1]))
+            p = ea.arg +  strlen((char *)(ea.arg)) ;
+            while (p > ea.arg &&  ((p[-1]) == ' ' || (p[-1]) == '\t') )
             {
                 --p;
             }
@@ -45439,7 +43629,7 @@
         cmdmod.cmod_did_esilent = 0;
     }
 
-    if (IS_USER_CMDIDX(ea.cmdidx))
+    if ( ((int)(ea.cmdidx) < 0) )
     {
         do_ucmd(&ea);
     }
@@ -45465,7 +43655,7 @@
         {
             if (errormsg != (char *)IObuff)
             {
-                STRCPY(IObuff, errormsg);
+                 strcpy((char *)(IObuff), (char *)(errormsg)) ;
                 errormsg = (char *)IObuff;
             }
             append_command(*cmdlinep);
@@ -45558,7 +43748,7 @@
             break;
         }
     }
-    if (i >= len && !ASCII_ISALPHA((*pp)[i]) && (*pp)[i] != '_' && (!noparen || ((*pp)[i] != '(' && (*pp)[i] != '.')))
+    if (i >= len && ! ( ((unsigned)((*pp)[i]) - 'A' < 26)  ||  ((unsigned)((*pp)[i]) - 'a' < 26) )  && (*pp)[i] != '_' && (!noparen || ((*pp)[i] != '(' && (*pp)[i] != '.')))
     {
         *pp = skipwhite(*pp + i);
         return TRUE;
@@ -45588,11 +43778,11 @@
         p = skipwhite(skiptowhite(p));
     }
 
-    if (!ASCII_ISALPHA(*p))
+    if (! ( ((unsigned)(*p) - 'A' < 26)  ||  ((unsigned)(*p) - 'a' < 26) ) )
     {
         return NULL;
     }
-    while (ASCII_ISALNUM(*p))
+    while ( ( ( ((unsigned)(*p) - 'A' < 26)  ||  ((unsigned)(*p) - 'a' < 26) )  ||  ((unsigned)(*p) - '0' < 10) ) )
     {
         ++p;
     }
@@ -45653,10 +43843,10 @@
     int     vim9script = in_vim9script();
     int     has_visual_range = FALSE;
 
-    CLEAR_POINTER(cmod);
+      memset(((cmod)), (0), (sizeof(*(cmod))))  ;
     cmod->cmod_flags = sticky_cmdmod_flags;
 
-    if (STRNCMP(eap->cmd, "'<,'>", 5) == 0)
+    if ( strncmp((char *)(eap->cmd), (char *)("'<,'>"), (5))  == 0)
     {
         eap->cmd += 5;
         cmd_start = eap->cmd;
@@ -45727,7 +43917,7 @@
             char_u *s;
             char_u *n;
 
-            for (s = eap->cmd; ASCII_ISALPHA(*s); ++s)
+            for (s = eap->cmd;  ( ((unsigned)(*s) - 'A' < 26)  ||  ((unsigned)(*s) - 'a' < 26) ) ; ++s)
             {
                 ;
             }
@@ -45915,7 +44105,7 @@
                             break;
                         }
                         cmod->cmod_flags |= CMOD_SILENT;
-                        if (*eap->cmd == '!' && !VIM_ISWHITE(eap->cmd[-1]))
+                        if (*eap->cmd == '!' && ! ((eap->cmd[-1]) == ' ' || (eap->cmd[-1]) == '\t') )
                         {
                             eap->cmd = skipwhite(eap->cmd + 1);
                             cmod->cmod_flags |= CMOD_ERRSILENT;
@@ -45928,13 +44118,13 @@
                             if (!skip_only)
                             {
                                 long tabnr = get_address(eap, &eap->cmd, ADDR_TABS, eap->skip, skip_only, FALSE, 1);
-                                if (tabnr == MAXLNUM)
+                                if (tabnr ==  LONG_MAX )
                                 {
                                     cmod->cmod_tab = tabpage_index(curtab) + 1;
                                 }
                                 else
                                 {
-                                    if (tabnr < 0 || tabnr > LAST_TAB_NR)
+                                    if (tabnr < 0 || tabnr >  current_tab_nr(NULL) )
                                     {
                                         *errormsg = _(e_invalid_range);
                                         return FAIL;
@@ -46002,16 +44192,16 @@
         {
             if (use_plus_cmd)
             {
-                size_t len = STRLEN(cmd_start);
+                size_t len =  strlen((char *)(cmd_start)) ;
 
-                mch_memmove(orig_cmd, cmd_start, len);
-                STRCPY(orig_cmd + len, " *+");
+                 memmove((char *)(orig_cmd), (char *)(cmd_start), len) ;
+                 strcpy((char *)(orig_cmd + len), (char *)(" *+")) ;
             }
             else
             {
-                mch_memmove(cmd_start - 5, cmd_start, eap->cmd - cmd_start);
+                 memmove((char *)(cmd_start - 5), (char *)(cmd_start), eap->cmd - cmd_start) ;
                 eap->cmd -= 5;
-                mch_memmove(eap->cmd - 1, ":'<,'>", 6);
+                 memmove((char *)(eap->cmd - 1), (char *)(":'<,'>"), 6) ;
             }
         }
         else
@@ -46079,7 +44269,7 @@
     if ((cmod->cmod_flags & CMOD_NOAUTOCMD) && cmod->cmod_save_ei == NULL)
     {
         cmod->cmod_save_ei = vim_strsave(p_ei);
-        set_string_option_direct((char_u *)"ei", -1, (char_u *)"all", OPT_FREE, SID_NONE);
+        set_string_option_direct((char_u *)"ei", -1, (char_u *)"all", OPT_FREE,  (-6) );
     }
 }
 
@@ -46094,7 +44284,7 @@
 
     if (cmod->cmod_save_ei != NULL)
     {
-        set_string_option_direct((char_u *)"ei", -1, cmod->cmod_save_ei, OPT_FREE, SID_NONE);
+        set_string_option_direct((char_u *)"ei", -1, cmod->cmod_save_ei, OPT_FREE,  (-6) );
         free_string_option(cmod->cmod_save_ei);
         cmod->cmod_save_ei = NULL;
     }
@@ -46142,7 +44332,7 @@
         {
             goto theend;
         }
-        if (lnum == MAXLNUM)
+        if (lnum ==  LONG_MAX )
         {
             if (*eap->cmd == '%')
             {
@@ -46177,11 +44367,11 @@
                         break;
                     case ADDR_WINDOWS:
                     case ADDR_TABS:
-                        if (IS_USER_CMDIDX(eap->cmdidx))
+                        if ( ((int)(eap->cmdidx) < 0) )
                         {
                             eap->line1 = 1;
                             eap->line2 = eap->addr_type == ADDR_WINDOWS
-                                                  ? LAST_WIN_NR : LAST_TAB_NR;
+                                                  ?  current_win_nr(NULL)  :  current_tab_nr(NULL) ;
                         }
                         else
                         {
@@ -46195,14 +44385,14 @@
                         *errormsg = _(e_invalid_range);
                         goto theend;
                     case ADDR_ARGUMENTS:
-                        if (ARGCOUNT == 0)
+                        if ( ( (curwin)->w_alist ->al_ga.ga_len)  == 0)
                         {
                             eap->line1 = eap->line2 = 0;
                         }
                         else
                         {
                             eap->line1 = 1;
-                            eap->line2 = ARGCOUNT;
+                            eap->line2 =  ( (curwin)->w_alist ->al_ga.ga_len) ;
                         }
                         break;
                     case ADDR_QUICKFIX_VALID:
@@ -46274,7 +44464,7 @@
     if (eap->addr_count == 1)
     {
         eap->line1 = eap->line2;
-        if (lnum == MAXLNUM)
+        if (lnum ==  LONG_MAX )
         {
             eap->addr_count = 0;
         }
@@ -46292,33 +44482,40 @@
     static void
 append_command(char_u *cmd)
 {
-    size_t  len = STRLEN(IObuff);
+    size_t  len =  strlen((char *)(IObuff)) ;
     char_u  *s = cmd;
     char_u  *d;
 
-    if (len > IOSIZE - 100)
+    if (len >  (1024+1)  - 100)
     {
-        d = IObuff + IOSIZE - 100;
+        d = IObuff +  (1024+1)  - 100;
         d -= mb_head_off(IObuff, d);
-        STRCPY(d, "...");
+         strcpy((char *)(d), (char *)("...")) ;
     }
-    STRCAT(IObuff, ": ");
-    d = IObuff + STRLEN(IObuff);
-    while (*s != NUL && d - IObuff + 5 < IOSIZE)
+     strcat((char *)(IObuff), (char *)(": ")) ;
+    d = IObuff +  strlen((char *)(IObuff)) ;
+    while (*s != NUL && d - IObuff + 5 <  (1024+1) )
     {
         if (enc_utf8 ? (s[0] == 0xc2 && s[1] == 0xa0) : *s == 0xa0)
         {
             s += enc_utf8 ? 2 : 1;
-            STRCPY(d, "<a0>");
+             strcpy((char *)(d), (char *)("<a0>")) ;
             d += 4;
         }
-        else if (d - IObuff + (*mb_ptr2len)(s) + 1 >= IOSIZE)
+        else if (d - IObuff + (*mb_ptr2len)(s) + 1 >=  (1024+1) )
         {
             break;
         }
         else
         {
-            MB_COPY_CHAR(s, d);
+             if (has_mbyte)
+             {
+                 mb_copy_char(&(s), &(d));
+             }
+             else
+             {
+                 *(d)++ = *(s)++;
+             }
         }
     }
     *d = NUL;
@@ -46345,7 +44542,7 @@
 }
 
     char_u *
-find_ex_command(exarg_T *eap, int     *full UNUSED, int     (*lookup)(char_u *, size_t, int cmd, cctx_T *) UNUSED, cctx_T  *cctx UNUSED)
+find_ex_command(exarg_T *eap, int     *full  __attribute__((unused)) , int     (*lookup)(char_u *, size_t, int cmd, cctx_T *)  __attribute__((unused)) , cctx_T  *cctx  __attribute__((unused)) )
 {
     int         len;
     char_u      *p;
@@ -46363,21 +44560,21 @@
     }
     else
     {
-        while (ASCII_ISALPHA(*p))
+        while ( ( ((unsigned)(*p) - 'A' < 26)  ||  ((unsigned)(*p) - 'a' < 26) ) )
         {
             ++p;
         }
         if (eap->cmd[0] == 'p' && eap->cmd[1] == 'y')
         {
-            while (ASCII_ISALNUM(*p))
+            while ( ( ( ((unsigned)(*p) - 'A' < 26)  ||  ((unsigned)(*p) - 'a' < 26) )  ||  ((unsigned)(*p) - '0' < 10) ) )
             {
                 ++p;
             }
         }
-        else if (*p == '9' && STRNCMP("vim9", eap->cmd, 4) == 0)
+        else if (*p == '9' &&  strncmp((char *)("vim9"), (char *)(eap->cmd), (4))  == 0)
         {
             ++p;
-            while (ASCII_ISALPHA(*p))
+            while ( ( ((unsigned)(*p) - 'A' < 26)  ||  ((unsigned)(*p) - 'a' < 26) ) )
             {
                 ++p;
             }
@@ -46411,7 +44608,7 @@
             }
         }
 
-        if (ASCII_ISLOWER(eap->cmd[0]))
+        if ( ((unsigned)(eap->cmd[0]) - 'a' < 26) )
         {
             int c1 = eap->cmd[0];
             int c2 = len == 1 ? NUL : eap->cmd[1];
@@ -46422,13 +44619,13 @@
                 getout(1);
             }
 
-            eap->cmdidx = cmdidxs1[CharOrdLow(c1)];
-            if (ASCII_ISLOWER(c2))
+            eap->cmdidx = cmdidxs1[ ((c1) - 'a') ];
+            if ( ((unsigned)(c2) - 'a' < 26) )
             {
-                eap->cmdidx += cmdidxs2[CharOrdLow(c1)][CharOrdLow(c2)];
+                eap->cmdidx += cmdidxs2[ ((c1) - 'a') ][ ((c2) - 'a') ];
             }
         }
-        else if (ASCII_ISUPPER(eap->cmd[0]))
+        else if ( ((unsigned)(eap->cmd[0]) - 'A' < 26) )
         {
             eap->cmdidx = CMD_Next;
         }
@@ -46439,7 +44636,7 @@
 
         for ( ; (int)eap->cmdidx < (int)CMD_SIZE; eap->cmdidx = (cmdidx_T)((int)eap->cmdidx + 1))
         {
-            if (STRNCMP(cmdnames[(int)eap->cmdidx].cmd_name, (char *)eap->cmd, (size_t)len) == 0)
+            if ( strncmp((char *)(cmdnames[(int)eap->cmdidx].cmd_name), (char *)((char *)eap->cmd), ((size_t)len))  == 0)
             {
                 break;
             }
@@ -46465,7 +44662,7 @@
 
         if ((eap->cmdidx == CMD_SIZE || eap->cmdidx == CMD_Print) && *eap->cmd >= 'A' && *eap->cmd <= 'Z')
         {
-            while (ASCII_ISALNUM(*p))
+            while ( ( ( ((unsigned)(*p) - 'A' < 26)  ||  ((unsigned)(*p) - 'a' < 26) )  ||  ((unsigned)(*p) - '0' < 10) ) )
             {
                 ++p;
             }
@@ -46499,7 +44696,7 @@
     {
         for (idx = (cmdidx_T)0; (int)idx < (int)CMD_SIZE; idx = (cmdidx_T)((int)idx + 1))
         {
-            if (STRNCMP(cmdnames[(int)idx].cmd_name, cmd, (size_t)len) == 0)
+            if ( strncmp((char *)(cmdnames[(int)idx].cmd_name), (char *)(cmd), ((size_t)len))  == 0)
             {
                 break;
             }
@@ -46541,12 +44738,12 @@
             while (p > cmd_start)
             {
                 --p;
-                if (!VIM_ISWHITE(*p))
+                if (! ((*p) == ' ' || (*p) == '\t') )
                 {
                     break;
                 }
             }
-            if (cmd > cmd_start && !VIM_ISWHITE(*p) && *p != ',' && *p != ';')
+            if (cmd > cmd_start && ! ((*p) == ' ' || (*p) == '\t')  && *p != ',' && *p != ';')
             {
                 break;
             }
@@ -46621,13 +44818,13 @@
             }
             break;
         case ADDR_WINDOWS:
-            lnum = CURRENT_WIN_NR;
+            lnum =  current_win_nr(curwin) ;
             break;
         case ADDR_ARGUMENTS:
             lnum = curwin->w_arg_idx + 1;
-            if (lnum > ARGCOUNT)
+            if (lnum >  ( (curwin)->w_alist ->al_ga.ga_len) )
             {
-                lnum = ARGCOUNT;
+                lnum =  ( (curwin)->w_alist ->al_ga.ga_len) ;
             }
             break;
         case ADDR_LOADED_BUFFERS:
@@ -46635,7 +44832,7 @@
             lnum = curbuf->b_fnum;
             break;
         case ADDR_TABS:
-            lnum = CURRENT_TAB_NR;
+            lnum =  current_tab_nr(curtab) ;
             break;
         case ADDR_TABS_RELATIVE:
         case ADDR_UNSIGNED:
@@ -46652,7 +44849,7 @@
 }
 
     linenr_T
-get_address(exarg_T     *eap UNUSED, char_u      **ptr, cmd_addr_T  addr_type, int         skip, int         silent, int         to_other_file, int         address_count UNUSED)
+get_address(exarg_T     *eap  __attribute__((unused)) , char_u      **ptr, cmd_addr_T  addr_type, int         skip, int         silent, int         to_other_file, int         address_count  __attribute__((unused)) )
 {
     int         c;
     int         i;
@@ -46664,7 +44861,7 @@
     buf_T       *buf;
 
     cmd = skipwhite(*ptr);
-    lnum = MAXLNUM;
+    lnum =  LONG_MAX ;
     do
     {
         switch (*cmd)
@@ -46678,7 +44875,7 @@
                         lnum = curwin->w_cursor.lnum;
                         break;
                     case ADDR_WINDOWS:
-                        lnum = CURRENT_WIN_NR;
+                        lnum =  current_win_nr(curwin) ;
                         break;
                     case ADDR_ARGUMENTS:
                         lnum = curwin->w_arg_idx + 1;
@@ -46688,7 +44885,7 @@
                         lnum = curbuf->b_fnum;
                         break;
                     case ADDR_TABS:
-                        lnum = CURRENT_TAB_NR;
+                        lnum =  current_tab_nr(curtab) ;
                         break;
                     case ADDR_NONE:
                     case ADDR_TABS_RELATIVE:
@@ -46713,10 +44910,10 @@
                         lnum = curbuf->b_ml.ml_line_count;
                         break;
                     case ADDR_WINDOWS:
-                        lnum = LAST_WIN_NR;
+                        lnum =  current_win_nr(NULL) ;
                         break;
                     case ADDR_ARGUMENTS:
-                        lnum = ARGCOUNT;
+                        lnum =  ( (curwin)->w_alist ->al_ga.ga_len) ;
                         break;
                     case ADDR_LOADED_BUFFERS:
                         buf = lastbuf;
@@ -46734,7 +44931,7 @@
                         lnum = lastbuf->b_fnum;
                         break;
                     case ADDR_TABS:
-                        lnum = LAST_TAB_NR;
+                        lnum =  current_tab_nr(NULL) ;
                         break;
                     case ADDR_NONE:
                     case ADDR_TABS_RELATIVE:
@@ -46809,7 +45006,7 @@
 
                     pos = curwin->w_cursor;
 
-                    if (lnum > 0 && lnum != MAXLNUM)
+                    if (lnum > 0 && lnum !=  LONG_MAX )
                     {
                         curwin->w_cursor.lnum =
                                 lnum > curbuf->b_ml.ml_line_count
@@ -46826,7 +45023,7 @@
                     }
                     searchcmdlen = 0;
                     flags = silent ? SEARCH_KEEP : SEARCH_HIS | SEARCH_MSG;
-                    if (!do_search(NULL, c, c, cmd, STRLEN(cmd), 1L, flags, NULL))
+                    if (!do_search(NULL, c, c, cmd,  strlen((char *)(cmd)) , 1L, flags, NULL))
                     {
                         curwin->w_cursor = pos;
                         cmd = NULL;
@@ -46863,7 +45060,7 @@
 
                 if (!skip)
                 {
-                    if (lnum != MAXLNUM)
+                    if (lnum !=  LONG_MAX )
                     {
                         pos.lnum = lnum;
                     }
@@ -46881,7 +45078,7 @@
                         pos.col = 0;
                     }
                     pos.coladd = 0;
-                    if (searchit(curwin, curbuf, &pos, NULL, *cmd == '?' ? BACKWARD : FORWARD, (char_u *)"", 0, 1L, SEARCH_MSG, i, NULL) != FAIL)
+                    if (searchit(curwin, curbuf, &pos, NULL, *cmd == '?' ?  (-1)  : FORWARD, (char_u *)"", 0, 1L, SEARCH_MSG, i, NULL) != FAIL)
                     {
                         lnum = pos.lnum;
                     }
@@ -46895,7 +45092,7 @@
                 break;
 
             default:
-                if (VIM_ISDIGIT(*cmd))
+                if ( ((unsigned)(*cmd) - '0' < 10) )
                 {
                     lnum = getdigits(&cmd);
                 }
@@ -46904,12 +45101,12 @@
         for (;;)
         {
             cmd = skipwhite(cmd);
-            if (*cmd != '-' && *cmd != '+' && !VIM_ISDIGIT(*cmd))
+            if (*cmd != '-' && *cmd != '+' && ! ((unsigned)(*cmd) - '0' < 10) )
             {
                 break;
             }
 
-            if (lnum == MAXLNUM)
+            if (lnum ==  LONG_MAX )
             {
                 switch (addr_type)
                 {
@@ -46918,7 +45115,7 @@
                         lnum = curwin->w_cursor.lnum;
                         break;
                     case ADDR_WINDOWS:
-                        lnum = CURRENT_WIN_NR;
+                        lnum =  current_win_nr(curwin) ;
                         break;
                     case ADDR_ARGUMENTS:
                         lnum = curwin->w_arg_idx + 1;
@@ -46928,7 +45125,7 @@
                         lnum = curbuf->b_fnum;
                         break;
                     case ADDR_TABS:
-                        lnum = CURRENT_TAB_NR;
+                        lnum =  current_tab_nr(curtab) ;
                         break;
                     case ADDR_TABS_RELATIVE:
                         lnum = 1;
@@ -46944,7 +45141,7 @@
                 }
             }
 
-            if (VIM_ISDIGIT(*cmd))
+            if ( ((unsigned)(*cmd) - '0' < 10) )
             {
                 i = '+';
             }
@@ -46952,14 +45149,14 @@
             {
                 i = *cmd++;
             }
-            if (!VIM_ISDIGIT(*cmd))
+            if (! ((unsigned)(*cmd) - '0' < 10) )
             {
                 n = 1;
             }
             else
             {
                 n = getdigits(&cmd);
-                if (n == MAXLNUM)
+                if (n ==  LONG_MAX )
                 {
                     emsg(_(e_line_number_out_of_range));
                     cmd = NULL;
@@ -47034,22 +45231,22 @@
             eap->line2 = lastbuf->b_fnum;
             break;
         case ADDR_WINDOWS:
-            eap->line2 = LAST_WIN_NR;
+            eap->line2 =  current_win_nr(NULL) ;
             break;
         case ADDR_TABS:
-            eap->line2 = LAST_TAB_NR;
+            eap->line2 =  current_tab_nr(NULL) ;
             break;
         case ADDR_TABS_RELATIVE:
             eap->line2 = 1;
             break;
         case ADDR_ARGUMENTS:
-            if (ARGCOUNT == 0)
+            if ( ( (curwin)->w_alist ->al_ga.ga_len)  == 0)
             {
                 eap->line1 = eap->line2 = 0;
             }
             else
             {
-                eap->line2 = ARGCOUNT;
+                eap->line2 =  ( (curwin)->w_alist ->al_ga.ga_len) ;
             }
             break;
         case ADDR_QUICKFIX_VALID:
@@ -47127,7 +45324,7 @@
                 }
                 break;
             case ADDR_ARGUMENTS:
-                if (eap->line2 > ARGCOUNT + (!ARGCOUNT))
+                if (eap->line2 >  ( (curwin)->w_alist ->al_ga.ga_len)  + (! ( (curwin)->w_alist ->al_ga.ga_len) ))
                 {
                     return _(e_invalid_range);
                 }
@@ -47167,13 +45364,13 @@
                 }
                 break;
             case ADDR_WINDOWS:
-                if (eap->line2 > LAST_WIN_NR)
+                if (eap->line2 >  current_win_nr(NULL) )
                 {
                     return _(e_invalid_range);
                 }
                 break;
             case ADDR_TABS:
-                if (eap->line2 > LAST_TAB_NR)
+                if (eap->line2 >  current_tab_nr(NULL) )
                 {
                     return _(e_invalid_range);
                 }
@@ -47252,13 +45449,12 @@
         if (!eap->usefilter && !escaped && eap->cmdidx != CMD_bang && eap->cmdidx != CMD_grep && eap->cmdidx != CMD_grepadd && eap->cmdidx != CMD_hardcopy && eap->cmdidx != CMD_lgrep && eap->cmdidx != CMD_lgrepadd && eap->cmdidx != CMD_lmake && eap->cmdidx != CMD_make && eap->cmdidx != CMD_terminal)
         {
             char_u      *l;
-# define ESCAPE_CHARS escape_chars
 
             for (l = repl; *l; ++l)
             {
-                if (vim_strchr(ESCAPE_CHARS, *l) != NULL)
+                if (vim_strchr( escape_chars , *l) != NULL)
                 {
-                    l = vim_strsave_escaped(repl, ESCAPE_CHARS);
+                    l = vim_strsave_escaped(repl,  escape_chars );
                     if (l != NULL)
                     {
                         vim_free(repl);
@@ -47269,7 +45465,7 @@
             }
         }
 
-        if ((eap->usefilter || eap->cmdidx == CMD_bang || eap->cmdidx == CMD_terminal) && vim_strpbrk(repl, (char_u *)"!") != NULL)
+        if ((eap->usefilter || eap->cmdidx == CMD_bang || eap->cmdidx == CMD_terminal) &&  (char_u *)strpbrk((char *)(repl), (char *)((char_u *)"!"))  != NULL)
         {
             char_u      *l;
 
@@ -47307,7 +45503,7 @@
                 {
                     if (vim_strchr(eap->arg, '$') != NULL || vim_strchr(eap->arg, '~') != NULL)
                     {
-                        expand_env_esc(eap->arg, NameBuff, MAXPATHL, (char_u *)(" \t" PATH_ESC_WILDCARDS), TRUE, NULL);
+                        expand_env_esc(eap->arg, NameBuff,  PATH_MAX , (char_u *)(" \t"  "*?[{" ), TRUE, NULL);
                         has_wildcards = mch_has_wildcard(NameBuff);
                         p = NameBuff;
                     }
@@ -47336,7 +45532,7 @@
                 }
                 if (p != NULL)
                 {
-                    (void)repl_cmdline(eap, eap->arg, STRLEN(eap->arg), p, cmdlinep);
+                    (void)repl_cmdline(eap, eap->arg,  strlen((char *)(eap->arg)) , p, cmdlinep);
                     if (n == 2)
                     {
                         vim_free(p);
@@ -47357,12 +45553,12 @@
     char_u      *new_cmdline;
     size_t      new_cmdlinelen;
 
-    repllen = STRLEN(repl);
-    taillen = STRLEN(src + srclen);
+    repllen =  strlen((char *)(repl)) ;
+    taillen =  strlen((char *)(src + srclen)) ;
     i = (src - *cmdlinep) + repllen + taillen + 3;
     if (eap->nextcmd != NULL)
     {
-        i += STRLEN(eap->nextcmd);
+        i +=  strlen((char *)(eap->nextcmd)) ;
     }
     if ((new_cmdline = alloc(i)) == NULL)
     {
@@ -47370,17 +45566,17 @@
     }
 
     new_cmdlinelen = src - *cmdlinep;
-    mch_memmove(new_cmdline, *cmdlinep, new_cmdlinelen);
+     memmove((char *)(new_cmdline), (char *)(*cmdlinep), new_cmdlinelen) ;
 
-    mch_memmove(new_cmdline + new_cmdlinelen, repl, repllen);
+     memmove((char *)(new_cmdline + new_cmdlinelen), (char *)(repl), repllen) ;
     new_cmdlinelen += repllen;
-    STRCPY(new_cmdline + new_cmdlinelen, src + srclen);
+     strcpy((char *)(new_cmdline + new_cmdlinelen), (char *)(src + srclen)) ;
     src = new_cmdline + new_cmdlinelen;
 
     if (eap->nextcmd != NULL)
     {
         new_cmdlinelen += taillen + 1;
-        STRCPY(new_cmdline + new_cmdlinelen, eap->nextcmd);
+         strcpy((char *)(new_cmdline + new_cmdlinelen), (char *)(eap->nextcmd)) ;
         eap->nextcmd = new_cmdline + new_cmdlinelen;
     }
     eap->cmd = new_cmdline + (eap->cmd - *cmdlinep);
@@ -47403,7 +45599,7 @@
 
     p = eap->arg;
 
-    for ( ; *p; MB_PTR_ADV(p))
+    for ( ; *p;  p += (*mb_ptr2len)(p) )
     {
         if (*p == Ctrl_V)
         {
@@ -47413,7 +45609,7 @@
             }
             else
             {
-                STRMOVE(p, p + 1);
+                  memmove((char *)((p)), (char *)((p + 1)),  strlen((char *)(p + 1))  + 1)  ;
             }
             if (*p == NUL)
             {
@@ -47421,13 +45617,13 @@
             }
         }
 
-        else if ((*p == '"' && !vim9script && !(eap->argt & EX_NOTRLCOM) && ((eap->cmdidx != CMD_at && eap->cmdidx != CMD_star) || p != eap->arg) && (eap->cmdidx != CMD_redir || p != eap->arg + 1 || p[-1] != '@')) || (*p == '#' && vim9script && !(eap->argt & EX_NOTRLCOM) && p > eap->cmd && VIM_ISWHITE(p[-1])) || (*p == '|' && eap->cmdidx != CMD_append && eap->cmdidx != CMD_change && eap->cmdidx != CMD_insert) || *p == '\n')
+        else if ((*p == '"' && !vim9script && !(eap->argt & EX_NOTRLCOM) && ((eap->cmdidx != CMD_at && eap->cmdidx != CMD_star) || p != eap->arg) && (eap->cmdidx != CMD_redir || p != eap->arg + 1 || p[-1] != '@')) || (*p == '#' && vim9script && !(eap->argt & EX_NOTRLCOM) && p > eap->cmd &&  ((p[-1]) == ' ' || (p[-1]) == '\t') ) || (*p == '|' && eap->cmdidx != CMD_append && eap->cmdidx != CMD_change && eap->cmdidx != CMD_insert) || *p == '\n')
         {
             if ((vim_strchr(p_cpo, CPO_BAR) == NULL || !(eap->argt & EX_CTRLV)) && *(p - 1) == '\\')
             {
                 if (!keep_backslash)
                 {
-                    STRMOVE(p - 1, p);
+                      memmove((char *)((p - 1)), (char *)((p)),  strlen((char *)(p))  + 1)  ;
                     --p;
                 }
             }
@@ -47484,14 +45680,14 @@
         {
             if (rembs)
             {
-                STRMOVE(p, p + 1);
+                  memmove((char *)((p)), (char *)((p + 1)),  strlen((char *)(p + 1))  + 1)  ;
             }
             else
             {
                 ++p;
             }
         }
-        MB_PTR_ADV(p);
+         p += (*mb_ptr2len)(p) ;
     }
     return p;
 }
@@ -47499,15 +45695,15 @@
     int
 get_bad_opt(char_u *p, exarg_T *eap)
 {
-    if (STRICMP(p, "keep") == 0)
+    if ( strcasecmp((char *)(p), (char *)("keep"))  == 0)
     {
-        eap->bad_char = BAD_KEEP;
+        eap->bad_char =  (-1) ;
     }
-    else if (STRICMP(p, "drop") == 0)
+    else if ( strcasecmp((char *)(p), (char *)("drop"))  == 0)
     {
-        eap->bad_char = BAD_DROP;
+        eap->bad_char =  (-2) ;
     }
-    else if (MB_BYTE2LEN(*p) == 1 && p[1] == NUL)
+    else if ( mb_bytelen_tab[*p]  == 1 && p[1] == NUL)
     {
         eap->bad_char = *p;
     }
@@ -47519,7 +45715,7 @@
 }
 
     static char_u *
-get_bad_name(expand_T *xp UNUSED, int idx)
+get_bad_name(expand_T *xp  __attribute__((unused)) , int idx)
 {
     static char *(p_bad_values[]) =
     {
@@ -47528,7 +45724,7 @@
         "drop",
     };
 
-    if (idx < 0 || idx >= (int)ARRAY_LENGTH(p_bad_values))
+    if (idx < 0 || idx >= (int) (sizeof(p_bad_values) / sizeof((p_bad_values)[0])) )
     {
         return NULL;
     }
@@ -47544,7 +45740,7 @@
     int         bad_char_idx;
     char_u      *p;
 
-    if (STRNCMP(arg, "bin", 3) == 0 || STRNCMP(arg, "nobin", 5) == 0)
+    if ( strncmp((char *)(arg), (char *)("bin"), (3))  == 0 ||  strncmp((char *)(arg), (char *)("nobin"), (5))  == 0)
     {
         if (*arg == 'n')
         {
@@ -47563,26 +45759,26 @@
         return OK;
     }
 
-    if (STRNCMP(arg, "edit", 4) == 0)
+    if ( strncmp((char *)(arg), (char *)("edit"), (4))  == 0)
     {
         eap->read_edit = TRUE;
         eap->arg = skipwhite(arg + 4);
         return OK;
     }
 
-    if (STRNCMP(arg, "ff", 2) == 0)
+    if ( strncmp((char *)(arg), (char *)("ff"), (2))  == 0)
     {
         arg += 2;
         pp = &eap->force_ff;
     }
-    else if (STRNCMP(arg, "fileformat", 10) == 0)
+    else if ( strncmp((char *)(arg), (char *)("fileformat"), (10))  == 0)
     {
         arg += 10;
         pp = &eap->force_ff;
     }
-    else if (STRNCMP(arg, "enc", 3) == 0)
+    else if ( strncmp((char *)(arg), (char *)("enc"), (3))  == 0)
     {
-        if (STRNCMP(arg, "encoding", 8) == 0)
+        if ( strncmp((char *)(arg), (char *)("encoding"), (8))  == 0)
         {
             arg += 8;
         }
@@ -47592,7 +45788,7 @@
         }
         pp = &eap->force_enc;
     }
-    else if (STRNCMP(arg, "bad", 3) == 0)
+    else if ( strncmp((char *)(arg), (char *)("bad"), (3))  == 0)
     {
         arg += 3;
         pp = &bad_char_idx;
@@ -47621,7 +45817,7 @@
     {
         for (p = eap->cmd + eap->force_enc; *p != NUL; ++p)
         {
-            *p = TOLOWER_ASC(*p);
+            *p =  (((*p) < 'A' || (*p) > 'Z') ? (*p) : (*p) + ('a' - 'A')) ;
         }
     }
     else
@@ -47636,7 +45832,7 @@
 }
 
     static char_u *
-get_argopt_name(expand_T *xp UNUSED, int idx)
+get_argopt_name(expand_T *xp  __attribute__((unused)) , int idx)
 {
     static char *(p_opt_values[]) =
     {
@@ -47648,7 +45844,7 @@
         "edit",
     };
 
-    if (idx < 0 || idx >= (int)ARRAY_LENGTH(p_opt_values))
+    if (idx < 0 || idx >= (int) (sizeof(p_opt_values) / sizeof((p_opt_values)[0])) )
     {
         return NULL;
     }
@@ -47664,23 +45860,23 @@
         char_u *(*cb)(expand_T *, int) = NULL;
 
         char_u *name_end = xp->xp_pattern - 1;
-        if (name_end - xp->xp_line >= 2 && STRNCMP(name_end - 2, "ff", 2) == 0)
+        if (name_end - xp->xp_line >= 2 &&  strncmp((char *)(name_end - 2), (char *)("ff"), (2))  == 0)
         {
             cb = get_fileformat_name;
         }
-        else if (name_end - xp->xp_line >= 10 && STRNCMP(name_end - 10, "fileformat", 10) == 0)
+        else if (name_end - xp->xp_line >= 10 &&  strncmp((char *)(name_end - 10), (char *)("fileformat"), (10))  == 0)
         {
             cb = get_fileformat_name;
         }
-        else if (name_end - xp->xp_line >= 3 && STRNCMP(name_end - 3, "enc", 3) == 0)
+        else if (name_end - xp->xp_line >= 3 &&  strncmp((char *)(name_end - 3), (char *)("enc"), (3))  == 0)
         {
             cb = get_encoding_name;
         }
-        else if (name_end - xp->xp_line >= 8 && STRNCMP(name_end - 8, "encoding", 8) == 0)
+        else if (name_end - xp->xp_line >= 8 &&  strncmp((char *)(name_end - 8), (char *)("encoding"), (8))  == 0)
         {
             cb = get_encoding_name;
         }
-        else if (name_end - xp->xp_line >= 3 && STRNCMP(name_end - 3, "bad", 3) == 0)
+        else if (name_end - xp->xp_line >= 3 &&  strncmp((char *)(name_end - 3), (char *)("bad"), (3))  == 0)
         {
             cb = get_bad_name;
         }
@@ -47692,9 +45888,9 @@
         return FAIL;
     }
 
-    if (xp->xp_pattern_len == 2 && STRNCMP(xp->xp_pattern, "ff", xp->xp_pattern_len) == 0)
+    if (xp->xp_pattern_len == 2 &&  strncmp((char *)(xp->xp_pattern), (char *)("ff"), (xp->xp_pattern_len))  == 0)
     {
-        *matches = ALLOC_MULT(char_u *, 1);
+        *matches =  (char_u * *)alloc(sizeof(char_u *) * (1)) ;
         if (*matches == NULL)
         {
             return FAIL;
@@ -47817,7 +46013,7 @@
         return;
     }
 
-    goto_buffer(eap, DOBUF_CURRENT, BACKWARD, (int)eap->line2);
+    goto_buffer(eap, DOBUF_CURRENT,  (-1) , (int)eap->line2);
     if (eap->do_ecmd_cmd != NULL)
     {
         do_cmd_argument(eap->do_ecmd_cmd);
@@ -47847,7 +46043,7 @@
         return;
     }
 
-    goto_buffer(eap, DOBUF_LAST, BACKWARD, 0);
+    goto_buffer(eap, DOBUF_LAST,  (-1) , 0);
     if (eap->do_ecmd_cmd != NULL)
     {
         do_cmd_argument(eap->do_ecmd_cmd);
@@ -47867,7 +46063,7 @@
 }
 
     int
-ends_excmd2(char_u *cmd_start UNUSED, char_u *cmd)
+ends_excmd2(char_u *cmd_start  __attribute__((unused)) , char_u *cmd)
 {
     int c = *cmd;
 
@@ -47878,7 +46074,7 @@
     if (in_vim9script())
     {
         return c == '#' && (cmd[1] != '{' || cmd[2] == '{')
-                                 && (cmd == cmd_start || VIM_ISWHITE(cmd[-1]));
+                                 && (cmd == cmd_start ||  ((cmd[-1]) == ' ' || (cmd[-1]) == '\t') );
     }
     return c == '"';
 }
@@ -47941,9 +46137,9 @@
     static int
 check_more(int message, int forceit)
 {
-    int     n = ARGCOUNT - curwin->w_arg_idx - 1;
+    int     n =  ( (curwin)->w_alist ->al_ga.ga_len)  - curwin->w_arg_idx - 1;
 
-    if (!forceit && only_one_window() && ARGCOUNT > 1 && !arg_had_last && n > 0 && quitmore == 0)
+    if (!forceit && only_one_window() &&  ( (curwin)->w_alist ->al_ga.ga_len)  > 1 && !arg_had_last && n > 0 && quitmore == 0)
     {
         if (message)
         {
@@ -47956,13 +46152,13 @@
 }
 
     char_u *
-get_command_name(expand_T *xp UNUSED, int idx)
+get_command_name(expand_T *xp  __attribute__((unused)) , int idx)
 {
     if (idx >= (int)CMD_SIZE)
     {
         return expand_user_command_name(idx);
     }
-    if (STRNCMP(cmdnames[idx].cmd_name, "{", 1) == 0 || STRNCMP(cmdnames[idx].cmd_name, "}", 1) == 0)
+    if ( strncmp((char *)(cmdnames[idx].cmd_name), (char *)("{"), (1))  == 0 ||  strncmp((char *)(cmdnames[idx].cmd_name), (char *)("}"), (1))  == 0)
     {
         return (char_u *)"";
     }
@@ -48075,7 +46271,7 @@
     }
     else
     {
-        if (only_one_window() && (ONE_WINDOW || eap->addr_count == 0))
+        if (only_one_window() && ( (firstwin == lastwin)  || eap->addr_count == 0))
         {
             getout(0);
         }
@@ -48085,7 +46281,7 @@
 }
 
     static void
-ex_cquit(exarg_T *eap UNUSED)
+ex_cquit(exarg_T *eap  __attribute__((unused)) )
 {
     getout(eap->addr_count > 0 ? (int)eap->line2 : EXIT_FAILURE);
 }
@@ -48097,11 +46293,11 @@
     {
         if (eap->forceit)
         {
-            cmdwin_result = K_XF1;
+            cmdwin_result =   (-((KS_EXTRA) + ((int)(KE_XF1) << 8)))  ;
         }
         else
         {
-            cmdwin_result = K_XF2;
+            cmdwin_result =   (-((KS_EXTRA) + ((int)(KE_XF2) << 8)))  ;
         }
         return FAIL;
     }
@@ -48155,7 +46351,7 @@
             }
             else
             {
-                FOR_ALL_WINDOWS(win)
+                 for ((win) = firstwin; (win) != NULL; (win) = (win)->w_next) 
                 {
                     winnr++;
                     if (winnr == eap->line2)
@@ -48236,11 +46432,11 @@
 
         if (relative == 0)
         {
-            if (STRCMP(p, "$") == 0)
+            if ( strcmp((char *)(p), (char *)("$"))  == 0)
             {
-                tab_number = LAST_TAB_NR;
+                tab_number =  current_tab_nr(NULL) ;
             }
-            else if (STRCMP(p, "#") == 0)
+            else if ( strcmp((char *)(p), (char *)("#"))  == 0)
             {
                 if (valid_tabpage(lastused_tabpage))
                 {
@@ -48253,7 +46449,7 @@
                     goto theend;
                 }
             }
-            else if (p == p_save || *p_save == '-' || *p != NUL || tab_number > LAST_TAB_NR)
+            else if (p == p_save || *p_save == '-' || *p != NUL || tab_number >  current_tab_nr(NULL) )
             {
                 eap->errmsg = ex_errmsg(e_invalid_argument_str, eap->arg);
                 goto theend;
@@ -48276,7 +46472,7 @@
                 --tab_number;
             }
         }
-        if (tab_number < unaccept_arg0 || tab_number > LAST_TAB_NR)
+        if (tab_number < unaccept_arg0 || tab_number >  current_tab_nr(NULL) )
         {
             eap->errmsg = ex_errmsg(e_invalid_argument_str, eap->arg);
         }
@@ -48295,7 +46491,7 @@
             {
                 char_u *cmdp = eap->cmd;
 
-                while (--cmdp > *eap->cmdlinep && (VIM_ISWHITE(*cmdp) || VIM_ISDIGIT(*cmdp)))
+                while (--cmdp > *eap->cmdlinep && ( ((*cmdp) == ' ' || (*cmdp) == '\t')  ||  ((unsigned)(*cmdp) - '0' < 10) ))
                 {
                     ;
                 }
@@ -48316,13 +46512,13 @@
         {
         case CMD_tabnext:
             tab_number = tabpage_index(curtab) + 1;
-            if (tab_number > LAST_TAB_NR)
+            if (tab_number >  current_tab_nr(NULL) )
             {
                 tab_number = 1;
             }
             break;
         case CMD_tabmove:
-            tab_number = LAST_TAB_NR;
+            tab_number =  current_tab_nr(NULL) ;
             break;
         default:
             tab_number = tabpage_index(curtab);
@@ -48341,7 +46537,7 @@
 
     if (cmdwin_type != 0)
     {
-        cmdwin_result = K_IGNORE;
+        cmdwin_result =   (-((KS_EXTRA) + ((int)(KE_IGNORE) << 8)))  ;
         return;
     }
 
@@ -48388,7 +46584,7 @@
 
     if (cmdwin_type != 0)
     {
-        cmdwin_result = K_IGNORE;
+        cmdwin_result =   (-((KS_EXTRA) + ((int)(KE_IGNORE) << 8)))  ;
         return;
     }
 
@@ -48412,7 +46608,8 @@
     goto_tabpage(tab_number);
     for (done = 0; done < 1000; ++done)
     {
-        FOR_ALL_TABPAGES(tp)
+         for ((tp) = first_tabpage; (tp) != NULL; (tp) = (tp)->tp_next) 
+         {
             if (tp->tp_topframe != topframe)
             {
                 tabpage_close_other(tp, eap->forceit);
@@ -48422,6 +46619,7 @@
                 }
                 break;
             }
+         }
         if (first_tabpage->tp_next == NULL)
         {
             break;
@@ -48441,11 +46639,11 @@
     curtab->tp_did_tabclosedpre = TRUE;
     tabpage_T *save_curtab = curtab;
 
-    if (!ONE_WINDOW)
+    if (! (firstwin == lastwin) )
     {
         close_others(TRUE, forceit);
     }
-    if (ONE_WINDOW)
+    if ( (firstwin == lastwin) )
     {
         ex_win_close(forceit, curwin, NULL);
     }
@@ -48521,7 +46719,7 @@
 }
 
     static void
-ex_hide(exarg_T *eap UNUSED)
+ex_hide(exarg_T *eap  __attribute__((unused)) )
 {
     if (eap->skip)
     {
@@ -48541,7 +46739,7 @@
         int     winnr = 0;
         win_T   *win;
 
-        FOR_ALL_WINDOWS(win)
+         for ((win) = firstwin; (win) != NULL; (win) = (win)->w_next) 
         {
             winnr++;
             if (winnr == eap->line2)
@@ -48575,7 +46773,7 @@
     out_flush();
     stoptermcap();
     out_flush();
-    mch_restore_title(SAVE_RESTORE_BOTH);
+    mch_restore_title( (SAVE_RESTORE_TITLE | SAVE_RESTORE_ICON) );
     ui_suspend();
     maketitle();
     resettitle();
@@ -48648,13 +46846,13 @@
 }
 
     static void
-ex_shell(exarg_T *eap UNUSED)
+ex_shell(exarg_T *eap  __attribute__((unused)) )
 {
     do_shell(NULL, 0);
 }
 
     static void
-ex_preserve(exarg_T *eap UNUSED)
+ex_preserve(exarg_T *eap  __attribute__((unused)) )
 {
     curbuf->b_flags |= BF_PRESERVED;
     ml_preserve(curbuf, TRUE);
@@ -48701,7 +46899,7 @@
         }
         else
         {
-            fname = find_file_in_path(eap->arg, (int)STRLEN(eap->arg), FNAME_MESS, TRUE, curbuf->b_ffname, &file_to_find, &search_ctx);
+            fname = find_file_in_path(eap->arg, (int) strlen((char *)(eap->arg)) , FNAME_MESS, TRUE, curbuf->b_ffname, &file_to_find, &search_ctx);
             vim_free(file_to_find);
             vim_findfile_cleanup(search_ctx);
         }
@@ -48728,7 +46926,8 @@
     {
         if (*eap->arg != NUL)
         {
-            RESET_BINDING(curwin);
+             (curwin)-> w_onebuf_opt.wo_scb  = FALSE;
+             (curwin)-> w_onebuf_opt.wo_crb  = FALSE;
         }
         else
         {
@@ -48746,7 +46945,7 @@
 {
     exarg_T     ea;
 
-    CLEAR_FIELD(ea);
+      memset((&(ea)), (0), (sizeof(ea)))  ;
     ea.cmdidx = CMD_tabnew;
     ea.cmd = (char_u *)"tabn";
     ea.arg = (char_u *)"";
@@ -48826,7 +47025,7 @@
 }
 
     static void
-ex_tabs(exarg_T *eap UNUSED)
+ex_tabs(exarg_T *eap  __attribute__((unused)) )
 {
     tabpage_T   *tp;
     win_T       *wp;
@@ -48837,8 +47036,8 @@
     for (tp = first_tabpage; tp != NULL && !got_int; tp = tp->tp_next)
     {
         msg_putchar('\n');
-        vim_snprintf((char *)IObuff, IOSIZE, _("Tab page %d"), tabcount++);
-        msg_outtrans_attr(IObuff, HL_ATTR(HLF_T));
+        vim_snprintf((char *)IObuff,  (1024+1) , _("Tab page %d"), tabcount++);
+        msg_outtrans_attr(IObuff,  highlight_attr[(int)(HLF_T)] );
         out_flush();
         ui_breakcheck();
 
@@ -48859,11 +47058,11 @@
             msg_putchar(' ');
             if (buf_spname(wp->w_buffer) != NULL)
             {
-                vim_strncpy(IObuff, buf_spname(wp->w_buffer), IOSIZE - 1);
+                vim_strncpy(IObuff, buf_spname(wp->w_buffer),  (1024+1)  - 1);
             }
             else
             {
-                home_replace(wp->w_buffer, wp->w_buffer->b_fname, IObuff, IOSIZE, TRUE);
+                home_replace(wp->w_buffer, wp->w_buffer->b_fname, IObuff,  (1024+1) , TRUE);
             }
             msg_outtrans(IObuff);
             out_flush();
@@ -48945,7 +47144,7 @@
     }
     else
     {
-        fname = find_file_in_path(eap->arg, (int)STRLEN(eap->arg), FNAME_MESS, TRUE, curbuf->b_ffname, &file_to_find, &search_ctx);
+        fname = find_file_in_path(eap->arg, (int) strlen((char *)(eap->arg)) , FNAME_MESS, TRUE, curbuf->b_ffname, &file_to_find, &search_ctx);
         if (eap->addr_count > 0)
         {
             count = eap->line2;
@@ -48955,7 +47154,8 @@
                 fname = find_file_in_path(NULL, 0, FNAME_MESS, FALSE, curbuf->b_ffname, &file_to_find, &search_ctx);
             }
         }
-        VIM_CLEAR(file_to_find);
+         vim_free(file_to_find);
+         (file_to_find) = NULL;
         vim_findfile_cleanup(search_ctx);
     }
 
@@ -48999,7 +47199,7 @@
             vim_regfree(regmatch.regprog);
             vim_free(line);
         }
-        eap->arg += STRLEN(eap->arg);
+        eap->arg +=  strlen((char *)(eap->arg)) ;
     }
     check_cursor();
 
@@ -49073,7 +47273,7 @@
     if ((eap->cmdidx == CMD_new || eap->cmdidx == CMD_tabnew || eap->cmdidx == CMD_tabedit || eap->cmdidx == CMD_vnew) && *eap->arg == NUL)
     {
         setpcmark();
-        (void)do_ecmd(0, NULL, NULL, eap, ECMD_ONE, ECMD_HIDE + (eap->forceit ? ECMD_FORCEIT : 0), old_curwin == NULL ? curwin : NULL);
+        (void)do_ecmd(0, NULL, NULL, eap,  (linenr_T)1 , ECMD_HIDE + (eap->forceit ? ECMD_FORCEIT : 0), old_curwin == NULL ? curwin : NULL);
     }
     else if ((eap->cmdidx != CMD_split && eap->cmdidx != CMD_vsplit) || *eap->arg != NUL)
     {
@@ -49095,7 +47295,7 @@
         {
             setpcmark();
         }
-        if (do_ecmd(0, (eap->cmdidx == CMD_enew ? NULL : eap->arg), NULL, eap, (*eap->arg == NUL && eap->do_ecmd_lnum == 0 && vim_strchr(p_cpo, CPO_GOTO1) != NULL) ? ECMD_ONE : eap->do_ecmd_lnum, (buf_hide(curbuf) ? ECMD_HIDE : 0) + (eap->forceit ? ECMD_FORCEIT : 0) + (old_curwin != NULL ? ECMD_OLDBUF : 0) + (eap->cmdidx == CMD_badd ? ECMD_ADDBUF : 0) + (eap->cmdidx == CMD_balt ? ECMD_ALTBUF : 0), old_curwin == NULL ? curwin : NULL) == FAIL)
+        if (do_ecmd(0, (eap->cmdidx == CMD_enew ? NULL : eap->arg), NULL, eap, (*eap->arg == NUL && eap->do_ecmd_lnum == 0 && vim_strchr(p_cpo, CPO_GOTO1) != NULL) ?  (linenr_T)1  : eap->do_ecmd_lnum, (buf_hide(curbuf) ? ECMD_HIDE : 0) + (eap->forceit ? ECMD_FORCEIT : 0) + (old_curwin != NULL ? ECMD_OLDBUF : 0) + (eap->cmdidx == CMD_badd ? ECMD_ADDBUF : 0) + (eap->cmdidx == CMD_balt ? ECMD_ALTBUF : 0), old_curwin == NULL ? curwin : NULL) == FAIL)
         {
             if (old_curwin != NULL)
             {
@@ -49142,7 +47342,7 @@
 }
 
     static void
-ex_swapname(exarg_T *eap UNUSED)
+ex_swapname(exarg_T *eap  __attribute__((unused)) )
 {
     if (curbuf->b_ml.ml_mfp == NULL || curbuf->b_ml.ml_mfp->mf_fname == NULL)
     {
@@ -49155,7 +47355,7 @@
 }
 
     static void
-ex_syncbind(exarg_T *eap UNUSED)
+ex_syncbind(exarg_T *eap  __attribute__((unused)) )
 {
     win_T       *wp;
     win_T       *save_curwin = curwin;
@@ -49166,12 +47366,12 @@
 
     setpcmark();
 
-    if (curwin->w_p_scb)
+    if (curwin-> w_onebuf_opt.wo_scb )
     {
         topline = curwin->w_topline;
-        FOR_ALL_WINDOWS(wp)
+         for ((wp) = firstwin; (wp) != NULL; (wp) = (wp)->w_next) 
         {
-            if (wp->w_p_scb && wp->w_buffer)
+            if (wp-> w_onebuf_opt.wo_scb  && wp->w_buffer)
             {
                 y = wp->w_buffer->b_ml.ml_line_count - get_scrolloff_value();
                 if (topline > y)
@@ -49190,9 +47390,9 @@
         topline = 1;
     }
 
-    FOR_ALL_WINDOWS(curwin)
+     for ((curwin) = firstwin; (curwin) != NULL; (curwin) = (curwin)->w_next) 
     {
-        if (curwin->w_p_scb)
+        if (curwin-> w_onebuf_opt.wo_scb )
         {
             curbuf = curwin->w_buffer;
             y = topline - curwin->w_topline;
@@ -49212,7 +47412,7 @@
     }
     curwin = save_curwin;
     curbuf = save_curbuf;
-    if (curwin->w_p_scb)
+    if (curwin-> w_onebuf_opt.wo_scb )
     {
         did_syncbind = TRUE;
         checkpcmark();
@@ -49222,7 +47422,7 @@
 
             ctrl_o[0] = Ctrl_O;
             ctrl_o[1] = 0;
-            ins_typebuf(ctrl_o, REMAP_NONE, 0, TRUE, FALSE);
+            ins_typebuf(ctrl_o,  (-1) , 0, TRUE, FALSE);
         }
     }
 }
@@ -49251,7 +47451,7 @@
             {
                 return;
             }
-            i = readfile(curbuf->b_ffname, curbuf->b_fname, eap->line2, (linenr_T)0, (linenr_T)MAXLNUM, eap, 0);
+            i = readfile(curbuf->b_ffname, curbuf->b_fname, eap->line2, (linenr_T)0, (linenr_T) LONG_MAX , eap, 0);
         }
         else
         {
@@ -49259,7 +47459,7 @@
             {
                 (void)setaltfname(eap->arg, eap->arg, (linenr_T)1);
             }
-            i = readfile(eap->arg, NULL, eap->line2, (linenr_T)0, (linenr_T)MAXLNUM, eap, 0);
+            i = readfile(eap->arg, NULL, eap->line2, (linenr_T)0, (linenr_T) LONG_MAX , eap, 0);
 
         }
     if (i != OK)
@@ -49313,9 +47513,11 @@
 {
     if (scope != CDSCOPE_WINDOW)
     {
-        VIM_CLEAR(curtab->tp_localdir);
+         vim_free(curtab->tp_localdir);
+         (curtab->tp_localdir) = NULL;
     }
-    VIM_CLEAR(curwin->w_localdir);
+     vim_free(curwin->w_localdir);
+     (curwin->w_localdir) = NULL;
     if (scope != CDSCOPE_GLOBAL)
     {
         char_u  *pdir = get_prevdir(scope);
@@ -49325,7 +47527,7 @@
             globaldir = vim_strsave(pdir);
         }
 
-        if (mch_dirname(NameBuff, MAXPATHL) == OK)
+        if (mch_dirname(NameBuff,  PATH_MAX ) == OK)
         {
             if (scope == CDSCOPE_TABPAGE)
             {
@@ -49339,7 +47541,8 @@
     }
     else
     {
-        VIM_CLEAR(globaldir);
+         vim_free(globaldir);
+         (globaldir) = NULL;
     }
 
     last_chdir_reason = NULL;
@@ -49372,7 +47575,7 @@
         return FALSE;
     }
 
-    if (STRCMP(new_dir, "-") == 0)
+    if ( strcmp((char *)(new_dir), (char *)("-"))  == 0)
     {
         pdir = get_prevdir(scope);
         if (pdir == NULL)
@@ -49383,7 +47586,7 @@
         new_dir = pdir;
     }
 
-    if (mch_dirname(NameBuff, MAXPATHL) == OK)
+    if (mch_dirname(NameBuff,  PATH_MAX ) == OK)
     {
         pdir = vim_strsave(NameBuff);
     }
@@ -49394,7 +47597,7 @@
 
     if (*new_dir == NUL)
     {
-        expand_env((char_u *)"$HOME", NameBuff, MAXPATHL);
+        expand_env((char_u *)"$HOME", NameBuff,  PATH_MAX );
         new_dir = NameBuff;
     }
     dir_differs = pdir == NULL
@@ -49476,9 +47679,9 @@
 }
 
     static void
-ex_pwd(exarg_T *eap UNUSED)
+ex_pwd(exarg_T *eap  __attribute__((unused)) )
 {
-    if (mch_dirname(NameBuff, MAXPATHL) == OK)
+    if (mch_dirname(NameBuff,  PATH_MAX ) == OK)
     {
         if (p_verbose > 0)
         {
@@ -49524,7 +47727,7 @@
 
     if (cursor_valid())
     {
-        n = W_WINROW(curwin) + curwin->w_wrow - msg_scrolled;
+        n =  (curwin->w_winrow)  + curwin->w_wrow - msg_scrolled;
         if (n >= 0)
         {
             windgoto(n, curwin->w_wincol + curwin->w_wcol);
@@ -49554,7 +47757,7 @@
     long        wait_now;
     elapsed_T   start_tv;
 
-    ELAPSED_INIT(start_tv);
+     gettimeofday(&(start_tv), NULL) ;
 
     if (hide_cursor)
     {
@@ -49573,7 +47776,7 @@
 
             ui_breakcheck();
 
-        done = ELAPSED_FUNC(start_tv);
+        done =  elapsed(&(start_tv)) ;
     }
 
     if (got_int)
@@ -49595,7 +47798,7 @@
     char_u      *arg = eap->arg;
     char_u      *p;
 
-    if (!SAFE_isdigit(*arg))
+    if (! (isdigit ((unsigned char)(*arg))) )
     {
         semsg(_(e_invalid_argument_str), arg);
         return;
@@ -49737,7 +47940,7 @@
     }
     curwin->w_cursor.lnum = eap->line2;
     check_cursor_col();
-    do_put(eap->regname, NULL, eap->forceit ? BACKWARD : FORWARD, 1L, PUT_LINE|PUT_CURSLINE);
+    do_put(eap->regname, NULL, eap->forceit ?  (-1)  : FORWARD, 1L, PUT_LINE|PUT_CURSLINE);
 }
 
     static void
@@ -49750,7 +47953,7 @@
     }
     curwin->w_cursor.lnum = eap->line2;
     check_cursor_col();
-    do_put(eap->regname, NULL, eap->forceit ? BACKWARD : FORWARD, 1L, PUT_LINE|PUT_CURSLINE |PUT_FIXINDENT);
+    do_put(eap->regname, NULL, eap->forceit ?  (-1)  : FORWARD, 1L, PUT_LINE|PUT_CURSLINE |PUT_FIXINDENT);
 }
 
     static void
@@ -49766,7 +47969,7 @@
     }
     get_flags(eap);
 
-    if (n == MAXLNUM || n < 0 || n > curbuf->b_ml.ml_line_count)
+    if (n ==  LONG_MAX  || n < 0 || n > curbuf->b_ml.ml_line_count)
     {
         emsg(_(e_invalid_range));
         return;
@@ -49883,7 +48086,7 @@
 }
 
     static void
-ex_redo(exarg_T *eap UNUSED)
+ex_redo(exarg_T *eap  __attribute__((unused)) )
 {
     u_redo(1);
 }
@@ -49900,7 +48103,7 @@
     {
         count = 1;
     }
-    else if (SAFE_isdigit(*p))
+    else if ( (isdigit ((unsigned char)(*p))) )
     {
         count = getdigits(&p);
         switch (*p)
@@ -49948,7 +48151,7 @@
     char_u      *fname;
     char_u      *arg = eap->arg;
 
-    if (STRICMP(eap->arg, "END") == 0)
+    if ( strcasecmp((char *)(eap->arg), (char *)("END"))  == 0)
     {
         close_redir();
     }
@@ -50037,7 +48240,7 @@
 }
 
     static void
-ex_redrawstatus(exarg_T *eap UNUSED)
+ex_redrawstatus(exarg_T *eap  __attribute__((unused)) )
 {
     if (eap->forceit)
     {
@@ -50073,7 +48276,7 @@
 }
 
     static void
-ex_redrawtabline(exarg_T *eap UNUSED)
+ex_redrawtabline(exarg_T *eap  __attribute__((unused)) )
 {
     int save_RedrawingDisabled = RedrawingDisabled;
     RedrawingDisabled = 0;
@@ -50089,7 +48292,7 @@
 }
 
     static void
-ex_redrawtabpanel(exarg_T *eap UNUSED)
+ex_redrawtabpanel(exarg_T *eap  __attribute__((unused)) )
 {
     int save_RedrawingDisabled = RedrawingDisabled;
     RedrawingDisabled = 0;
@@ -50130,7 +48333,7 @@
         return NULL;
     }
 
-    if ((fd = mch_fopen((char *)fname, mode)) == NULL)
+    if ((fd =  fopen(((char *)fname), (mode)) ) == NULL)
     {
         semsg(_(e_cannot_open_str_for_writing_2), fname);
     }
@@ -50170,7 +48373,7 @@
 {
     check_cursor();
     update_topline();
-    if (!curwin->w_p_wrap)
+    if (!curwin-> w_onebuf_opt.wo_wrap )
     {
         validate_cursor();
     }
@@ -50246,7 +48449,7 @@
         {
             for (l = (*mb_ptr2len)(p) - 1; l > 0; --l)
             {
-                if (*++p == K_SPECIAL)
+                if (*++p ==  (0x80) )
                 {
                     len += 2;
                 }
@@ -50254,7 +48457,7 @@
         }
         if (len > 0)
         {
-            arg = alloc(STRLEN(eap->arg) + len + 1);
+            arg = alloc( strlen((char *)(eap->arg))  + len + 1);
             if (arg != NULL)
             {
                 len = 0;
@@ -50264,10 +48467,10 @@
                     for (l = (*mb_ptr2len)(p) - 1; l > 0; --l)
                     {
                         arg[len++] = *++p;
-                        if (*p == K_SPECIAL)
+                        if (*p ==  (0x80) )
                         {
                             arg[len++] = KS_SPECIAL;
-                            arg[len++] = KE_FILLER;
+                            arg[len++] =  ('X') ;
                         }
                     }
                     arg[len] = NUL;
@@ -50288,7 +48491,7 @@
                 check_cursor_moved(curwin);
             }
 
-            exec_normal_cmd(arg != NULL ? arg : eap->arg, eap->forceit ? REMAP_NONE : REMAP_YES, FALSE);
+            exec_normal_cmd(arg != NULL ? arg : eap->arg, eap->forceit ?  (-1)  : REMAP_YES, FALSE);
         }
         while (eap->addr_count > 0 && eap->line1 <= eap->line2 && !got_int)
             ;
@@ -50354,7 +48557,7 @@
 }
 
     static void
-ex_stopinsert(exarg_T *eap UNUSED)
+ex_stopinsert(exarg_T *eap  __attribute__((unused)) )
 {
     restart_edit = 0;
     stop_insert_mode = TRUE;
@@ -50369,7 +48572,7 @@
 }
 
     void
-exec_normal(int was_typed, int use_vpeekc, int may_use_terminal_loop UNUSED)
+exec_normal(int was_typed, int use_vpeekc, int may_use_terminal_loop  __attribute__((unused)) )
 {
     oparg_T     oa;
     int         c;
@@ -50468,19 +48671,19 @@
 find_cmdline_var(char_u *src, size_t *usedlen)
 {
     static keyvalue_T spec_str_tab[] = {
-        KEYVALUE_ENTRY(SPEC_SID, "SID>"),
-        KEYVALUE_ENTRY(SPEC_ABUF, "abuf>"),
-        KEYVALUE_ENTRY(SPEC_AFILE, "afile>"),
-        KEYVALUE_ENTRY(SPEC_AMATCH, "amatch>"),
-        KEYVALUE_ENTRY(SPEC_CCWORD, "cWORD>"),
-        KEYVALUE_ENTRY(SPEC_CEXPR, "cexpr>"),
-        KEYVALUE_ENTRY(SPEC_CFILE, "cfile>"),
-        KEYVALUE_ENTRY(SPEC_CWORD, "cword>"),
-        KEYVALUE_ENTRY(SPEC_SCRIPT, "script>"),
-        KEYVALUE_ENTRY(SPEC_SFILE, "sfile>"),
-        KEYVALUE_ENTRY(SPEC_SFLNUM, "sflnum>"),
-        KEYVALUE_ENTRY(SPEC_SLNUM, "slnum>"),
-        KEYVALUE_ENTRY(SPEC_STACK, "stack>")
+         {(SPEC_SID), {((char_u *)"SID>"),  (sizeof("SID>" "") - 1) }} ,
+         {(SPEC_ABUF), {((char_u *)"abuf>"),  (sizeof("abuf>" "") - 1) }} ,
+         {(SPEC_AFILE), {((char_u *)"afile>"),  (sizeof("afile>" "") - 1) }} ,
+         {(SPEC_AMATCH), {((char_u *)"amatch>"),  (sizeof("amatch>" "") - 1) }} ,
+         {(SPEC_CCWORD), {((char_u *)"cWORD>"),  (sizeof("cWORD>" "") - 1) }} ,
+         {(SPEC_CEXPR), {((char_u *)"cexpr>"),  (sizeof("cexpr>" "") - 1) }} ,
+         {(SPEC_CFILE), {((char_u *)"cfile>"),  (sizeof("cfile>" "") - 1) }} ,
+         {(SPEC_CWORD), {((char_u *)"cword>"),  (sizeof("cword>" "") - 1) }} ,
+         {(SPEC_SCRIPT), {((char_u *)"script>"),  (sizeof("script>" "") - 1) }} ,
+         {(SPEC_SFILE), {((char_u *)"sfile>"),  (sizeof("sfile>" "") - 1) }} ,
+         {(SPEC_SFLNUM), {((char_u *)"sflnum>"),  (sizeof("sflnum>" "") - 1) }} ,
+         {(SPEC_SLNUM), {((char_u *)"slnum>"),  (sizeof("slnum>" "") - 1) }} ,
+         {(SPEC_STACK), {((char_u *)"stack>"),  (sizeof("stack>" "") - 1) }} 
     };
     keyvalue_T target;
     keyvalue_T *entry;
@@ -50500,7 +48703,7 @@
         target.value.string = src + 1;
         target.value.length = 0;
 
-        entry = (keyvalue_T *)bsearch(&target, &spec_str_tab, ARRAY_LENGTH(spec_str_tab), sizeof(spec_str_tab[0]), cmp_keyvalue_value_n);
+        entry = (keyvalue_T *)bsearch(&target, &spec_str_tab,  (sizeof(spec_str_tab) / sizeof((spec_str_tab)[0])) , sizeof(spec_str_tab[0]), cmp_keyvalue_value_n);
         if (entry == NULL)
         {
             return -1;
@@ -50547,7 +48750,7 @@
     if (src > srcstart && src[-1] == '\\')
     {
         *usedlen = 0;
-        STRMOVE(src - 1, src);
+          memmove((char *)((src - 1)), (char *)((src)),  strlen((char *)(src))  + 1)  ;
         return NULL;
     }
 
@@ -50577,7 +48780,7 @@
                     else
                     {
                         result = curbuf->b_fname;
-                        tilde_file = STRCMP(result, "~") == 0;
+                        tilde_file =  strcmp((char *)(result), (char *)("~"))  == 0;
                     }
                     break;
                 }
@@ -50630,7 +48833,7 @@
                     }
                     if (lnump != NULL)
                     {
-                        *lnump = ECMD_LAST;
+                        *lnump =  ((linenr_T)-1) ;
                     }
                     if (buf->b_fname == NULL)
                     {
@@ -50640,7 +48843,7 @@
                     else
                     {
                         result = buf->b_fname;
-                        tilde_file = STRCMP(result, "~") == 0;
+                        tilde_file =  strcmp((char *)(result), (char *)("~"))  == 0;
                     }
                 }
                 break;
@@ -50720,18 +48923,18 @@
                 break;
 
         case SPEC_SLNUM:
-                if (SOURCING_NAME == NULL || SOURCING_LNUM == 0)
+                if ( (((estack_T *)exestack.ga_data)[exestack.ga_len - 1].es_name)  == NULL ||  (((estack_T *)exestack.ga_data)[exestack.ga_len - 1].es_lnum)  == 0)
                 {
                     *errormsg = _(e_no_line_number_to_use_for_slnum);
                     return NULL;
                 }
-                sprintf((char *)strbuf, "%ld", SOURCING_LNUM);
+                sprintf((char *)strbuf, "%ld",  (((estack_T *)exestack.ga_data)[exestack.ga_len - 1].es_lnum) );
                 result = strbuf;
                 break;
 
         }
 
-        resultlen = STRLEN(result);
+        resultlen =  strlen((char *)(result)) ;
         if (src[*usedlen] == '<')
         {
             ++*usedlen;
@@ -50787,7 +48990,7 @@
     size_t      srclen;
     char_u      *p;
 
-    resultlen = STRLEN(arg);
+    resultlen =  strlen((char *)(arg)) ;
     result = vim_strnsave(arg, resultlen);
     if (result == NULL)
     {
@@ -50796,7 +48999,7 @@
 
     for (p = result; *p; )
     {
-        if (STRNCMP(p, "<sfile>", 7) != 0)
+        if ( strncmp((char *)(p), (char *)("<sfile>"), (7))  != 0)
         {
             ++p;
         }
@@ -50817,7 +49020,7 @@
                 p += srclen;
                 continue;
             }
-            repllen = STRLEN(repl);
+            repllen =  strlen((char *)(repl)) ;
             resultlen += repllen - srclen;
             newres = alloc(resultlen + 1);
             if (newres == NULL)
@@ -50827,10 +49030,10 @@
                 return NULL;
             }
             len = p - result;
-            mch_memmove(newres, result, len);
-            STRCPY(newres + len, repl);
+             memmove((char *)(newres), (char *)(result), len) ;
+             strcpy((char *)(newres + len), (char *)(repl)) ;
             len += repllen;
-            STRCPY(newres + len, p + srclen);
+             strcpy((char *)(newres + len), (char *)(p + srclen)) ;
             vim_free(repl);
             vim_free(result);
             result = newres;
@@ -50844,14 +49047,14 @@
     static void
 ex_behave(exarg_T *eap)
 {
-    if (STRCMP(eap->arg, "mswin") == 0)
+    if ( strcmp((char *)(eap->arg), (char *)("mswin"))  == 0)
     {
         set_option_value_give_err((char_u *)"selection", 0L, (char_u *)"exclusive", 0);
         set_option_value_give_err((char_u *)"selectmode", 0L, (char_u *)"mouse,key", 0);
         set_option_value_give_err((char_u *)"mousemodel", 0L, (char_u *)"popup", 0);
         set_option_value_give_err((char_u *)"keymodel", 0L, (char_u *)"startsel,stopsel", 0);
     }
-    else if (STRCMP(eap->arg, "xterm") == 0)
+    else if ( strcmp((char *)(eap->arg), (char *)("xterm"))  == 0)
     {
         set_option_value_give_err((char_u *)"selection", 0L, (char_u *)"inclusive", 0);
         set_option_value_give_err((char_u *)"selectmode", 0L, (char_u *)"", 0);
@@ -50883,13 +49086,13 @@
 
     for (;;)
     {
-        if (STRNCMP(arg, "plugin", 6) == 0)
+        if ( strncmp((char *)(arg), (char *)("plugin"), (6))  == 0)
         {
             plugin = TRUE;
             arg = skipwhite(arg + 6);
             continue;
         }
-        if (STRNCMP(arg, "indent", 6) == 0)
+        if ( strncmp((char *)(arg), (char *)("indent"), (6))  == 0)
         {
             indent = TRUE;
             arg = skipwhite(arg + 6);
@@ -50897,20 +49100,20 @@
         }
         break;
     }
-    if (STRCMP(arg, "on") == 0 || STRCMP(arg, "detect") == 0)
+    if ( strcmp((char *)(arg), (char *)("on"))  == 0 ||  strcmp((char *)(arg), (char *)("detect"))  == 0)
     {
         if (*arg == 'o' || !filetype_detect)
         {
-            source_runtime((char_u *)FILETYPE_FILE, DIP_ALL);
+            source_runtime((char_u *) "filetype.vim" , DIP_ALL);
             filetype_detect = TRUE;
             if (plugin)
             {
-                source_runtime((char_u *)FTPLUGIN_FILE, DIP_ALL);
+                source_runtime((char_u *) "ftplugin.vim" , DIP_ALL);
                 filetype_plugin = TRUE;
             }
             if (indent)
             {
-                source_runtime((char_u *)INDENT_FILE, DIP_ALL);
+                source_runtime((char_u *) "indent.vim" , DIP_ALL);
                 filetype_indent = TRUE;
             }
         }
@@ -50920,24 +49123,24 @@
             do_modelines(0);
         }
     }
-    else if (STRCMP(arg, "off") == 0)
+    else if ( strcmp((char *)(arg), (char *)("off"))  == 0)
     {
         if (plugin || indent)
         {
             if (plugin)
             {
-                source_runtime((char_u *)FTPLUGOF_FILE, DIP_ALL);
+                source_runtime((char_u *) "ftplugof.vim" , DIP_ALL);
                 filetype_plugin = FALSE;
             }
             if (indent)
             {
-                source_runtime((char_u *)INDOFF_FILE, DIP_ALL);
+                source_runtime((char_u *) "indoff.vim" , DIP_ALL);
                 filetype_indent = FALSE;
             }
         }
         else
         {
-            source_runtime((char_u *)FTOFF_FILE, DIP_ALL);
+            source_runtime((char_u *) "ftoff.vim" , DIP_ALL);
             filetype_detect = FALSE;
         }
     }
@@ -50956,7 +49159,7 @@
     }
 
     char_u *arg = eap->arg;
-    if (STRNCMP(arg, "FALLBACK ", 9) == 0)
+    if ( strncmp((char *)(arg), (char *)("FALLBACK "), (9))  == 0)
     {
         arg += 9;
     }
@@ -50969,7 +49172,7 @@
 }
 
     static void
-ex_digraphs(exarg_T *eap UNUSED)
+ex_digraphs(exarg_T *eap  __attribute__((unused)) )
 {
     emsg(_(e_no_digraphs_version));
 }
@@ -50981,7 +49184,7 @@
 }
 
     static void
-ex_nohlsearch(exarg_T *eap UNUSED)
+ex_nohlsearch(exarg_T *eap  __attribute__((unused)) )
 {
     set_no_hlsearch(TRUE);
     redraw_all_later(UPD_SOME_VALID);
@@ -51001,10 +49204,10 @@
 
 // ==================== ex_getln.c ====================
 
-#define CMDLINE_NOT_CHANGED     1
-#define CMDLINE_CHANGED         2
-#define GOTO_NORMAL_MODE        3
-#define PROCESS_NEXT_KEY        4
+enum { CMDLINE_NOT_CHANGED = 1 };
+enum { CMDLINE_CHANGED = 2 };
+enum { GOTO_NORMAL_MODE = 3 };
+enum { PROCESS_NEXT_KEY = 4 };
 
 static cmdline_info_T ccline;
 
@@ -51132,7 +49335,9 @@
     is_state->did_incsearch = FALSE;
     is_state->incsearch_postponed = FALSE;
     is_state->magic_overruled_save = magic_overruled;
-    CLEAR_POS(&is_state->match_end);
+     (&is_state->match_end)->lnum = 0;
+     (&is_state->match_end)->col = 0;
+     (&is_state->match_end)->coladd = 0;
     is_state->save_cursor = curwin->w_cursor;
     is_state->search_start = curwin->w_cursor;
     save_viewstate(&is_state->init_viewstate);
@@ -51170,9 +49375,9 @@
     *patlen = ccline.cmdlen;
 
     search_first_line = 0;
-    search_last_line = MAXLNUM;
+    search_last_line =  LONG_MAX ;
 
-    CLEAR_FIELD(ea);
+      memset((&(ea)), (0), (sizeof(ea)))  ;
     ea.line1 = 1;
     ea.line2 = 1;
     ea.cmd = ccline.cmdbuff;
@@ -51187,7 +49392,7 @@
         return FALSE;
     }
 
-    for (p = cmd; ASCII_ISALPHA(*p); ++p)
+    for (p = cmd;  ( ((unsigned)(*p) - 'A' < 26)  ||  ((unsigned)(*p) - 'a' < 26) ) ; ++p)
     {
         ;
     }
@@ -51196,7 +49401,7 @@
         return FALSE;
     }
 
-    if (STRNCMP(cmd, "substitute", p - cmd) == 0 || STRNCMP(cmd, "smagic", p - cmd) == 0 || STRNCMP(cmd, "snomagic", MAX(p - cmd, 3)) == 0 || STRNCMP(cmd, "vglobal", p - cmd) == 0)
+    if ( strncmp((char *)(cmd), (char *)("substitute"), (p - cmd))  == 0 ||  strncmp((char *)(cmd), (char *)("smagic"), (p - cmd))  == 0 ||  strncmp((char *)(cmd), (char *)("snomagic"), (MAX(p - cmd, 3)))  == 0 ||  strncmp((char *)(cmd), (char *)("vglobal"), (p - cmd))  == 0)
     {
         if (*cmd == 's' && cmd[1] == 'm')
         {
@@ -51207,13 +49412,13 @@
             magic_overruled = OPTION_MAGIC_OFF;
         }
     }
-    else if (STRNCMP(cmd, "sort", MAX(p - cmd, 3)) == 0 || STRNCMP(cmd, "uniq", MAX(p - cmd, 3)) == 0)
+    else if ( strncmp((char *)(cmd), (char *)("sort"), (MAX(p - cmd, 3)))  == 0 ||  strncmp((char *)(cmd), (char *)("uniq"), (MAX(p - cmd, 3)))  == 0)
     {
         if (*p == '!')
         {
             p = skipwhite(p + 1);
         }
-        while (ASCII_ISALPHA(*(p = skipwhite(p))))
+        while ( ( ((unsigned)(*(p = skipwhite(p))) - 'A' < 26)  ||  ((unsigned)(*(p = skipwhite(p))) - 'a' < 26) ) )
         {
             ++p;
         }
@@ -51222,7 +49427,7 @@
             return FALSE;
         }
     }
-    else if (STRNCMP(cmd, "vimgrep", MAX(p - cmd, 3)) == 0 || STRNCMP(cmd, "vimgrepadd", MAX(p - cmd, 8)) == 0 || STRNCMP(cmd, "lvimgrep", MAX(p - cmd, 2)) == 0 || STRNCMP(cmd, "lvimgrepadd", MAX(p - cmd, 9)) == 0 || STRNCMP(cmd, "global", p - cmd) == 0)
+    else if ( strncmp((char *)(cmd), (char *)("vimgrep"), (MAX(p - cmd, 3)))  == 0 ||  strncmp((char *)(cmd), (char *)("vimgrepadd"), (MAX(p - cmd, 8)))  == 0 ||  strncmp((char *)(cmd), (char *)("lvimgrep"), (MAX(p - cmd, 2)))  == 0 ||  strncmp((char *)(cmd), (char *)("lvimgrepadd"), (MAX(p - cmd, 9)))  == 0 ||  strncmp((char *)(cmd), (char *)("global"), (p - cmd))  == 0)
     {
         if (*p == '!')
         {
@@ -51305,7 +49510,7 @@
     }
 
     search_first_line = 0;
-    search_last_line = MAXLNUM;
+    search_last_line =  LONG_MAX ;
 
     if (firstc == '/' || firstc == '?')
     {
@@ -51340,7 +49545,7 @@
     }
     else
     {
-        if (!EQUAL_POS(is_state->save_cursor, is_state->search_start))
+        if (! (((is_state->save_cursor).lnum == (is_state->search_start).lnum) && ((is_state->save_cursor).col == (is_state->search_start).col) && ((is_state->save_cursor).coladd == (is_state->search_start).coladd)) )
         {
             curwin->w_cursor = is_state->save_cursor;
             setpcmark();
@@ -51351,7 +49556,7 @@
     highlight_match = FALSE;
 
     search_first_line = 0;
-    search_last_line = MAXLNUM;
+    search_last_line =  LONG_MAX ;
 
     magic_overruled = is_state->magic_overruled_save;
 
@@ -51567,7 +49772,7 @@
     if (c == Ctrl_G)
     {
         t = is_state->match_end;
-        if (LT_POS(is_state->match_start, is_state->match_end))
+        if ( (((is_state->match_start).lnum != (is_state->match_end).lnum)               ? (is_state->match_start).lnum < (is_state->match_end).lnum                   : (is_state->match_start).col != (is_state->match_end).col                        ? (is_state->match_start).col < (is_state->match_end).col                     : (is_state->match_start).coladd < (is_state->match_end).coladd) )
         {
             (void)decl(&t);
         }
@@ -51582,7 +49787,7 @@
         search_flags += SEARCH_KEEP;
     }
     ++emsg_off;
-    i = searchit(curwin, curbuf, &t, NULL, c == Ctrl_G ? FORWARD : BACKWARD, searchstr, searchstrlen, count, search_flags, RE_SEARCH, NULL);
+    i = searchit(curwin, curbuf, &t, NULL, c == Ctrl_G ? FORWARD :  (-1) , searchstr, searchstrlen, count, search_flags, RE_SEARCH, NULL);
     --emsg_off;
     if (dircp != NULL)
     {
@@ -51631,7 +49836,7 @@
             is_state->search_start = t;
             (void)incl(&is_state->search_start);
         }
-        if (LT_POS(t, is_state->search_start) && c == Ctrl_G)
+        if ( (((t).lnum != (is_state->search_start).lnum)               ? (t).lnum < (is_state->search_start).lnum                   : (t).col != (is_state->search_start).col                        ? (t).col < (is_state->search_start).col                     : (t).coladd < (is_state->search_start).coladd)  && c == Ctrl_G)
         {
             is_state->search_start = t;
             if (firstc == '?')
@@ -51689,7 +49894,7 @@
         {
             if (p_ic && p_scs && !pat_has_uppercase(ccline.cmdbuff + skiplen))
             {
-                *c = MB_TOLOWER(*c);
+                *c =  vim_tolower(*c) ;
             }
             if (*c == search_delim || vim_strchr((char_u *)(magic_isset() ? "\\~^$.*[" : "\\^$"), *c) != NULL)
             {
@@ -51717,7 +49922,7 @@
     void
 cmdline_init(void)
 {
-    CLEAR_FIELD(ccline);
+      memset((&(ccline)), (0), (sizeof(ccline)))  ;
 }
 
     static int
@@ -51785,10 +49990,10 @@
         int wim_full = (wim_flags[0] & WIM_FULL);
 
         wim_index = 0;
-        if (c == p_wc || c == p_wcm || c == K_WILD)
+        if (c == p_wc || c == p_wcm || c ==   (-((KS_EXTRA) + ((int)(KE_WILD) << 8)))  )
         {
             options |= WILD_MAY_EXPAND_PATTERN;
-            if (c == K_WILD)
+            if (c ==   (-((KS_EXTRA) + ((int)(KE_WILD) << 8)))  )
             {
                 options |= WILD_FUNC_TRIGGER;
             }
@@ -51915,16 +50120,16 @@
     int         i;
     int         j;
 
-    if (c == K_KDEL)
+    if (c ==   (-((KS_EXTRA) + ((int)(KE_KDEL) << 8)))  )
     {
-        c = K_DEL;
+        c =   (-(('k') + ((int)('D') << 8)))  ;
     }
 
-    if (c == K_DEL && ccline.cmdpos != ccline.cmdlen)
+    if (c ==   (-(('k') + ((int)('D') << 8)))   && ccline.cmdpos != ccline.cmdlen)
     {
         ++ccline.cmdpos;
     }
-    if (has_mbyte && c == K_DEL)
+    if (has_mbyte && c ==   (-(('k') + ((int)('D') << 8)))  )
     {
         ccline.cmdpos += mb_off_next(ccline.cmdbuff, ccline.cmdbuff + ccline.cmdpos);
     }
@@ -52042,7 +50247,7 @@
 }
 
     static int
-cmdline_insert_reg(int *gotesc UNUSED)
+cmdline_insert_reg(int *gotesc  __attribute__((unused)) )
 {
     int         i;
     int         c;
@@ -52080,7 +50285,7 @@
     static void
 cmdline_left_right_mouse(int c, int *ignore_drag_release)
 {
-    if (c == K_LEFTRELEASE || c == K_RIGHTRELEASE)
+    if (c ==   (-((KS_EXTRA) + ((int)(KE_LEFTRELEASE) << 8)))   || c ==   (-((KS_EXTRA) + ((int)(KE_RIGHTRELEASE) << 8)))  )
     {
         *ignore_drag_release = TRUE;
     }
@@ -52138,7 +50343,7 @@
 
     for (;;)
     {
-        if (c == K_UP || c == K_S_UP || c == Ctrl_P || c == K_PAGEUP || c == K_KPAGEUP)
+        if (c ==   (-(('k') + ((int)('u') << 8)))   || c ==   (-((KS_EXTRA) + ((int)(KE_S_UP) << 8)))   || c == Ctrl_P || c ==   (-(('k') + ((int)('P') << 8)))   || c ==   (-(('K') + ((int)('3') << 8)))  )
         {
             if (hiscnt == get_hislen())
             {
@@ -52184,7 +50389,7 @@
             hiscnt = orig_hiscnt;
             break;
         }
-        if ((c != K_UP && c != K_DOWN) || hiscnt == orig_hiscnt || STRNCMP(get_histentry(histype)[hiscnt].hisstr, lookfor, lookforlen) == 0)
+        if ((c !=   (-(('k') + ((int)('u') << 8)))   && c !=   (-(('k') + ((int)('d') << 8)))  ) || hiscnt == orig_hiscnt ||  strncmp((char *)(get_histentry(histype)[hiscnt].hisstr), (char *)(lookfor), (lookforlen))  == 0)
         {
             break;
         }
@@ -52266,7 +50471,7 @@
                 res = GOTO_NORMAL_MODE;
                 goto done;
             }
-            STRCPY(ccline.cmdbuff, p);
+             strcpy((char *)(ccline.cmdbuff), (char *)(p)) ;
             ccline.cmdpos = ccline.cmdlen = (int)plen;
         }
 
@@ -52304,7 +50509,7 @@
 
     if (firstc <= 0)
     {
-        vim_memset(ccline.cmdbuff, ' ', indent);
+         memset((ccline.cmdbuff), (' '), (indent)) ;
         ccline.cmdbuff[indent] = NUL;
         ccline.cmdpos = indent;
         ccline.cmdspos = indent;
@@ -52315,13 +50520,13 @@
 }
 
     char_u *
-getcmdline(int           firstc, long          count, int           indent, getline_opt_T do_concat UNUSED)
+getcmdline(int           firstc, long          count, int           indent, getline_opt_T do_concat  __attribute__((unused)) )
 {
     return getcmdline_int(firstc, count, indent, TRUE);
 }
 
     static char_u *
-getcmdline_int(int         firstc, long        count UNUSED, int         indent, int         clear_ccline)
+getcmdline_int(int         firstc, long        count  __attribute__((unused)) , int         indent, int         clear_ccline)
 {
     static int  depth = 0;
     int         c = 0;
@@ -52361,7 +50566,7 @@
     }
     if (clear_ccline)
     {
-        CLEAR_FIELD(ccline);
+          memset((&(ccline)), (0), (sizeof(ccline)))  ;
     }
 
     init_incsearch_state(&is_state);
@@ -52401,7 +50606,7 @@
 
     if (firstc == '/' || firstc == '?' || firstc == '@')
     {
-        if (curbuf->b_p_imsearch == B_IMODE_USE_INSERT)
+        if (curbuf->b_p_imsearch ==  (-1) )
         {
             b_im_ptr = &curbuf->b_p_iminsert;
         }
@@ -52442,7 +50647,8 @@
         int     prev_cmdpos = ccline.cmdpos;
         int     skip_pum_redraw = FALSE;
 
-        VIM_CLEAR(prev_cmdbuff);
+         vim_free(prev_cmdbuff);
+         (prev_cmdbuff) = NULL;
 
         redir_off = TRUE;
         quit_more = FALSE;
@@ -52465,7 +50671,7 @@
             }
         }
 
-        if (c == K_WILD && firstc != '@')
+        if (c ==   (-((KS_EXTRA) + ((int)(KE_WILD) << 8)))   && firstc != '@')
         {
             skip_pum_redraw = TRUE;
         }
@@ -52474,7 +50680,7 @@
         {
             cursorcmd();
             c = safe_vgetc();
-        } while (c == K_IGNORE || c == K_NOP);
+        } while (c ==   (-((KS_EXTRA) + ((int)(KE_IGNORE) << 8)))   || c ==   (-((KS_EXTRA) + ((int)(KE_NOP) << 8)))  );
 
         if (ccline.cmdbuff_replaced && xpc.xp_numfiles > 0)
         {
@@ -52490,13 +50696,13 @@
         }
         ccline.cmdbuff_replaced = FALSE;
 
-        if (c == K_WILD && did_hist_navigate)
+        if (c ==   (-((KS_EXTRA) + ((int)(KE_WILD) << 8)))   && did_hist_navigate)
         {
             did_hist_navigate = FALSE;
             continue;
         }
 
-        if (c == K_COMMAND || c == K_SCRIPT_COMMAND)
+        if (c ==   (-((KS_EXTRA) + ((int)(KE_COMMAND) << 8)))   || c ==   (-((KS_EXTRA) + ((int)(KE_SCRIPT_COMMAND) << 8)))  )
         {
             int     clen = ccline.cmdlen;
             int     cc_count = aucmd_cmdline_changed_count;
@@ -52521,13 +50727,14 @@
             got_int = FALSE;
         }
 
-        if (lookfor != NULL && c != K_S_DOWN && c != K_S_UP && c != K_DOWN && c != K_UP && c != K_PAGEDOWN && c != K_PAGEUP && c != K_KPAGEDOWN && c != K_KPAGEUP && c != K_LEFT && c != K_RIGHT && (xpc.xp_numfiles > 0 || (c != Ctrl_P && c != Ctrl_N)))
+        if (lookfor != NULL && c !=   (-((KS_EXTRA) + ((int)(KE_S_DOWN) << 8)))   && c !=   (-((KS_EXTRA) + ((int)(KE_S_UP) << 8)))   && c !=   (-(('k') + ((int)('d') << 8)))   && c !=   (-(('k') + ((int)('u') << 8)))   && c !=   (-(('k') + ((int)('N') << 8)))   && c !=   (-(('k') + ((int)('P') << 8)))   && c !=   (-(('K') + ((int)('5') << 8)))   && c !=   (-(('K') + ((int)('3') << 8)))   && c !=   (-(('k') + ((int)('l') << 8)))   && c !=   (-(('k') + ((int)('r') << 8)))   && (xpc.xp_numfiles > 0 || (c != Ctrl_P && c != Ctrl_N)))
         {
-            VIM_CLEAR(lookfor);
+             vim_free(lookfor);
+             (lookfor) = NULL;
             lookforlen = 0;
         }
 
-        if (c != p_wc && c == K_S_TAB && xpc.xp_numfiles > 0)
+        if (c != p_wc && c ==   (-(('k') + ((int)('B') << 8)))   && xpc.xp_numfiles > 0)
         {
             c = Ctrl_P;
         }
@@ -52550,7 +50757,7 @@
             }
         }
 
-        if (KeyTyped && (c == '\n' || c == '\r' || c == K_KENTER || c == ESC || c == intr_char || c == Ctrl_C))
+        if (KeyTyped && (c == '\n' || c == '\r' || c ==   (-(('K') + ((int)('A') << 8)))   || c == ESC || c == intr_char || c == Ctrl_C))
         {
             trigger_cmd_autocmd(cmdline_type, EVENT_CMDLINELEAVEPRE);
             event_cmdlineleavepre_triggered = TRUE;
@@ -52561,15 +50768,15 @@
         }
 
         end_wildmenu = (!key_is_wc && c != Ctrl_N && c != Ctrl_P && c != Ctrl_A && c != Ctrl_L);
-        end_wildmenu = end_wildmenu && (!cmdline_pum_active() || (c != K_PAGEDOWN && c != K_PAGEUP && c != K_KPAGEDOWN && c != K_KPAGEUP && c != K_MOUSEDOWN && c != K_MOUSEUP && c != K_MOUSELEFT && c != K_MOUSERIGHT && !((c == K_S_UP || c == K_S_DOWN) && (mod_mask & MOD_MASK_CTRL))));
+        end_wildmenu = end_wildmenu && (!cmdline_pum_active() || (c !=   (-(('k') + ((int)('N') << 8)))   && c !=   (-(('k') + ((int)('P') << 8)))   && c !=   (-(('K') + ((int)('5') << 8)))   && c !=   (-(('K') + ((int)('3') << 8)))   && c !=   (-((KS_EXTRA) + ((int)(KE_MOUSEDOWN) << 8)))   && c !=   (-((KS_EXTRA) + ((int)(KE_MOUSEUP) << 8)))   && c !=   (-((KS_EXTRA) + ((int)(KE_MOUSELEFT) << 8)))   && c !=   (-((KS_EXTRA) + ((int)(KE_MOUSERIGHT) << 8)))   && !((c ==   (-((KS_EXTRA) + ((int)(KE_S_UP) << 8)))   || c ==   (-((KS_EXTRA) + ((int)(KE_S_DOWN) << 8)))  ) && (mod_mask & MOD_MASK_CTRL))));
 
         if (end_wildmenu)
         {
             if (cmdline_pum_active())
             {
                 skip_pum_redraw = skip_pum_redraw && !key_is_wc
-                    && !VIM_ISWHITE(c)
-                    && (vim_isprintc(c) || c == K_BS || c == Ctrl_H || c == K_DEL || c == K_KDEL || c == Ctrl_W || c == Ctrl_U);
+                    && ! ((c) == ' ' || (c) == '\t') 
+                    && (vim_isprintc(c) || c ==   (-(('k') + ((int)('b') << 8)))   || c == Ctrl_H || c ==   (-(('k') + ((int)('D') << 8)))   || c ==   (-((KS_EXTRA) + ((int)(KE_KDEL) << 8)))   || c == Ctrl_W || c == Ctrl_U);
                 cmdline_pum_remove(&ccline, skip_pum_redraw);
             }
             if (xpc.xp_numfiles != -1)
@@ -52577,7 +50784,7 @@
                 (void)ExpandOne(&xpc, NULL, NULL, 0, WILD_FREE);
             }
             did_wild_list = FALSE;
-            if (!p_wmnu || (c != K_UP && c != K_DOWN))
+            if (!p_wmnu || (c !=   (-(('k') + ((int)('u') << 8)))   && c !=   (-(('k') + ((int)('d') << 8)))  ))
             {
                 xpc.xp_context = EXPAND_NOTHING;
             }
@@ -52608,20 +50815,20 @@
             c = Ctrl_BSL;
         }
 
-        if (c == cedit_key || c == K_CMDWIN)
+        if (c == cedit_key || c ==   (-((KS_EXTRA) + ((int)(KE_CMDWIN) << 8)))  )
         {
-            if ((c == K_CMDWIN || ex_normal_busy == 0) && got_int == FALSE)
+            if ((c ==   (-((KS_EXTRA) + ((int)(KE_CMDWIN) << 8)))   || ex_normal_busy == 0) && got_int == FALSE)
             {
                 c = open_cmdwin();
                 some_key_typed = TRUE;
             }
         }
 
-        if (c == '\n' || c == '\r' || c == K_KENTER || (c == ESC && (!KeyTyped || vim_strchr(p_cpo, CPO_ESC) != NULL)))
+        if (c == '\n' || c == '\r' || c ==   (-(('K') + ((int)('A') << 8)))   || (c == ESC && (!KeyTyped || vim_strchr(p_cpo, CPO_ESC) != NULL)))
         {
             if (exmode_active && c != ESC && ccline.cmdpos == ccline.cmdlen && ccline.cmdpos > 0 && ccline.cmdbuff[ccline.cmdpos - 1] == '\\')
             {
-                if (c == K_KENTER)
+                if (c ==   (-(('K') + ((int)('A') << 8)))  )
                 {
                     c = '\n';
                 }
@@ -52642,14 +50849,14 @@
             }
         }
 
-        if ((c == p_wc && !gotesc && KeyTyped) || c == p_wcm || c == K_WILD)
+        if ((c == p_wc && !gotesc && KeyTyped) || c == p_wcm || c ==   (-((KS_EXTRA) + ((int)(KE_WILD) << 8)))  )
         {
-            if (c == K_WILD)
+            if (c ==   (-((KS_EXTRA) + ((int)(KE_WILD) << 8)))  )
             {
                 ++emsg_silent;
             }
-            res = cmdline_wildchar_complete(c, firstc != '@', &did_wild_list, &wim_index, &xpc, &gotesc, c == K_WILD, &is_state.search_start);
-            if (c == K_WILD)
+            res = cmdline_wildchar_complete(c, firstc != '@', &did_wild_list, &wim_index, &xpc, &gotesc, c ==   (-((KS_EXTRA) + ((int)(KE_WILD) << 8)))  , &is_state.search_start);
+            if (c ==   (-((KS_EXTRA) + ((int)(KE_WILD) << 8)))  )
             {
                 --emsg_silent;
             }
@@ -52657,7 +50864,7 @@
             {
                 goto cmdline_changed;
             }
-            if (c == K_WILD)
+            if (c ==   (-((KS_EXTRA) + ((int)(KE_WILD) << 8)))  )
             {
                 goto cmdline_not_changed;
             }
@@ -52665,7 +50872,7 @@
 
         gotesc = FALSE;
 
-        if (c == K_S_TAB && KeyTyped)
+        if (c ==   (-(('k') + ((int)('B') << 8)))   && KeyTyped)
         {
             if (nextwild(&xpc, WILD_EXPAND_KEEP, 0, firstc != '@') == OK)
             {
@@ -52682,7 +50889,7 @@
             }
         }
 
-        if (c == NUL || c == K_ZERO)
+        if (c == NUL || c ==   (-((KS_ZERO) + ((int)( ('X') ) << 8)))  )
         {
             c = NL;
         }
@@ -52705,10 +50912,10 @@
 
         switch (c)
         {
-        case K_BS:
+        case   (-(('k') + ((int)('b') << 8)))  :
         case Ctrl_H:
-        case K_DEL:
-        case K_KDEL:
+        case   (-(('k') + ((int)('D') << 8)))  :
+        case   (-((KS_EXTRA) + ((int)(KE_KDEL) << 8)))  :
         case Ctrl_W:
             res = cmdline_erase_chars(c, indent, &is_state);
             if (res == CMDLINE_NOT_CHANGED)
@@ -52721,8 +50928,8 @@
             }
             goto cmdline_changed;
 
-        case K_INS:
-        case K_KINS:
+        case   (-(('k') + ((int)('I') << 8)))  :
+        case   (-((KS_EXTRA) + ((int)(KE_KINS) << 8)))  :
                 ccline.overstrike = !ccline.overstrike;
                 may_trigger_modechanged();
                 status_redraw_curbuf();
@@ -52780,9 +50987,9 @@
                 redrawcmd();
                 continue;
 
-        case K_RIGHT:
-        case K_S_RIGHT:
-        case K_C_RIGHT:
+        case   (-(('k') + ((int)('r') << 8)))  :
+        case   (-(('%') + ((int)('i') << 8)))  :
+        case   (-((KS_EXTRA) + ((int)(KE_C_RIGHT) << 8)))  :
                 do
                 {
                     if (ccline.cmdpos >= ccline.cmdlen)
@@ -52804,7 +51011,7 @@
                         ++ccline.cmdpos;
                     }
                 }
-                while ((c == K_S_RIGHT || c == K_C_RIGHT || (mod_mask & (MOD_MASK_SHIFT|MOD_MASK_CTRL))) && ccline.cmdbuff[ccline.cmdpos] != ' ')
+                while ((c ==   (-(('%') + ((int)('i') << 8)))   || c ==   (-((KS_EXTRA) + ((int)(KE_C_RIGHT) << 8)))   || (mod_mask & (MOD_MASK_SHIFT|MOD_MASK_CTRL))) && ccline.cmdbuff[ccline.cmdpos] != ' ')
                     ;
                 if (has_mbyte)
                 {
@@ -52812,9 +51019,9 @@
                 }
                 goto cmdline_not_changed;
 
-        case K_LEFT:
-        case K_S_LEFT:
-        case K_C_LEFT:
+        case   (-(('k') + ((int)('l') << 8)))  :
+        case   (-(('#') + ((int)('4') << 8)))  :
+        case   (-((KS_EXTRA) + ((int)(KE_C_LEFT) << 8)))  :
                 if (ccline.cmdpos == 0)
                 {
                     goto cmdline_not_changed;
@@ -52828,7 +51035,7 @@
                     }
                     ccline.cmdspos -= cmdline_charsize(ccline.cmdpos);
                 }
-                while (ccline.cmdpos > 0 && (c == K_S_LEFT || c == K_C_LEFT || (mod_mask & (MOD_MASK_SHIFT|MOD_MASK_CTRL))) && ccline.cmdbuff[ccline.cmdpos - 1] != ' ')
+                while (ccline.cmdpos > 0 && (c ==   (-(('#') + ((int)('4') << 8)))   || c ==   (-((KS_EXTRA) + ((int)(KE_C_LEFT) << 8)))   || (mod_mask & (MOD_MASK_SHIFT|MOD_MASK_CTRL))) && ccline.cmdbuff[ccline.cmdpos - 1] != ' ')
                     ;
                 if (has_mbyte)
                 {
@@ -52836,14 +51043,14 @@
                 }
                 goto cmdline_not_changed;
 
-        case K_IGNORE:
+        case   (-((KS_EXTRA) + ((int)(KE_IGNORE) << 8)))  :
                 goto cmdline_not_changed;
 
-        case K_MIDDLEDRAG:
-        case K_MIDDLERELEASE:
+        case   (-((KS_EXTRA) + ((int)(KE_MIDDLEDRAG) << 8)))  :
+        case   (-((KS_EXTRA) + ((int)(KE_MIDDLERELEASE) << 8)))  :
                 goto cmdline_not_changed;
 
-        case K_MIDDLEMOUSE:
+        case   (-((KS_EXTRA) + ((int)(KE_MIDDLEMOUSE) << 8)))  :
                     if (!mouse_has(MOUSE_COMMAND))
                     {
                         goto cmdline_not_changed;
@@ -52852,51 +51059,52 @@
                 redrawcmd();
                 goto cmdline_changed;
 
-        case K_LEFTDRAG:
-        case K_LEFTRELEASE:
-        case K_RIGHTDRAG:
-        case K_RIGHTRELEASE:
+        case   (-((KS_EXTRA) + ((int)(KE_LEFTDRAG) << 8)))  :
+        case   (-((KS_EXTRA) + ((int)(KE_LEFTRELEASE) << 8)))  :
+        case   (-((KS_EXTRA) + ((int)(KE_RIGHTDRAG) << 8)))  :
+        case   (-((KS_EXTRA) + ((int)(KE_RIGHTRELEASE) << 8)))  :
                 if (ignore_drag_release)
                 {
                     goto cmdline_not_changed;
                 }
-        case K_LEFTMOUSE:
-        case K_RIGHTMOUSE:
+        __attribute__((fallthrough));
+        case   (-((KS_EXTRA) + ((int)(KE_LEFTMOUSE) << 8)))  :
+        case   (-((KS_EXTRA) + ((int)(KE_RIGHTMOUSE) << 8)))  :
                 cmdline_left_right_mouse(c, &ignore_drag_release);
                 goto cmdline_not_changed;
 
-        case K_MOUSEDOWN:
-        case K_MOUSEUP:
-        case K_MOUSELEFT:
-        case K_MOUSERIGHT:
+        case   (-((KS_EXTRA) + ((int)(KE_MOUSEDOWN) << 8)))  :
+        case   (-((KS_EXTRA) + ((int)(KE_MOUSEUP) << 8)))  :
+        case   (-((KS_EXTRA) + ((int)(KE_MOUSELEFT) << 8)))  :
+        case   (-((KS_EXTRA) + ((int)(KE_MOUSERIGHT) << 8)))  :
                 goto cmdline_not_changed;
 
-        case K_X1MOUSE:
-        case K_X1DRAG:
-        case K_X1RELEASE:
-        case K_X2MOUSE:
-        case K_X2DRAG:
-        case K_X2RELEASE:
-        case K_MOUSEMOVE:
+        case   (-((KS_EXTRA) + ((int)(KE_X1MOUSE) << 8)))  :
+        case   (-((KS_EXTRA) + ((int)(KE_X1DRAG) << 8)))  :
+        case   (-((KS_EXTRA) + ((int)(KE_X1RELEASE) << 8)))  :
+        case   (-((KS_EXTRA) + ((int)(KE_X2MOUSE) << 8)))  :
+        case   (-((KS_EXTRA) + ((int)(KE_X2DRAG) << 8)))  :
+        case   (-((KS_EXTRA) + ((int)(KE_X2RELEASE) << 8)))  :
+        case   (-((KS_EXTRA) + ((int)(KE_MOUSEMOVE) << 8)))  :
                 goto cmdline_not_changed;
 
-        case K_SELECT:
+        case   (-((KS_SELECT) + ((int)( ('X') ) << 8)))  :
                 goto cmdline_not_changed;
 
         case Ctrl_B:
-        case K_HOME:
-        case K_KHOME:
-        case K_S_HOME:
-        case K_C_HOME:
+        case   (-(('k') + ((int)('h') << 8)))  :
+        case   (-(('K') + ((int)('1') << 8)))  :
+        case   (-(('#') + ((int)('2') << 8)))  :
+        case   (-((KS_EXTRA) + ((int)(KE_C_HOME) << 8)))  :
                 ccline.cmdpos = 0;
                 set_cmdspos();
                 goto cmdline_not_changed;
 
         case Ctrl_E:
-        case K_END:
-        case K_KEND:
-        case K_S_END:
-        case K_C_END:
+        case   (-(('@') + ((int)('7') << 8)))  :
+        case   (-(('K') + ((int)('4') << 8)))  :
+        case   (-(('*') + ((int)('7') << 8)))  :
+        case   (-((KS_EXTRA) + ((int)(KE_C_END) << 8)))  :
                 ccline.cmdpos = ccline.cmdlen;
                 set_cmdspos_cursor();
                 goto cmdline_not_changed;
@@ -52938,18 +51146,19 @@
                     }
                     goto cmdline_changed;
                 }
-        case K_UP:
-        case K_DOWN:
-        case K_S_UP:
-        case K_S_DOWN:
-        case K_PAGEUP:
-        case K_KPAGEUP:
-        case K_PAGEDOWN:
-        case K_KPAGEDOWN:
-                if (cmdline_pum_active() && (c == K_PAGEUP || c == K_PAGEDOWN || c == K_KPAGEUP || c == K_KPAGEDOWN))
+        __attribute__((fallthrough));
+        case   (-(('k') + ((int)('u') << 8)))  :
+        case   (-(('k') + ((int)('d') << 8)))  :
+        case   (-((KS_EXTRA) + ((int)(KE_S_UP) << 8)))  :
+        case   (-((KS_EXTRA) + ((int)(KE_S_DOWN) << 8)))  :
+        case   (-(('k') + ((int)('P') << 8)))  :
+        case   (-(('K') + ((int)('3') << 8)))  :
+        case   (-(('k') + ((int)('N') << 8)))  :
+        case   (-(('K') + ((int)('5') << 8)))  :
+                if (cmdline_pum_active() && (c ==   (-(('k') + ((int)('P') << 8)))   || c ==   (-(('k') + ((int)('N') << 8)))   || c ==   (-(('K') + ((int)('3') << 8)))   || c ==   (-(('K') + ((int)('5') << 8)))  ))
                 {
                     wild_type = WILD_PAGEUP;
-                    if (c == K_PAGEDOWN || c == K_KPAGEDOWN)
+                    if (c ==   (-(('k') + ((int)('N') << 8)))   || c ==   (-(('K') + ((int)('5') << 8)))  )
                     {
                         wild_type = WILD_PAGEDOWN;
                     }
@@ -53002,7 +51211,7 @@
 
                 break;
 
-        case K_PS:
+        case   (-(('P') + ((int)('S') << 8)))  :
                 bracketed_paste(PASTE_CMDLINE, FALSE, NULL);
                 goto cmdline_changed;
 
@@ -53012,19 +51221,19 @@
                     gotesc = TRUE;
                     goto returncmd;
                 }
-                if (!IS_SPECIAL(c))
+                if (! ((c) < 0) )
                 {
                     mod_mask = 0x0;
                 }
                 break;
         }
 
-        if (do_abbr && (IS_SPECIAL(c) || !vim_iswordc(c)) && (ccheck_abbr((has_mbyte && c >= 0x100) ? (c + ABBR_OFF) : c) || c == Ctrl_RSB))
+        if (do_abbr && ( ((c) < 0)  || !vim_iswordc(c)) && (ccheck_abbr((has_mbyte && c >= 0x100) ? (c + ABBR_OFF) : c) || c == Ctrl_RSB))
         {
             goto cmdline_changed;
         }
 
-        if (IS_SPECIAL(c) || mod_mask != 0)
+        if ( ((c) < 0)  || mod_mask != 0)
         {
             put_on_cmdline(get_special_key_name(c, mod_mask), -1, TRUE);
         }
@@ -53065,7 +51274,7 @@
         {
             may_do_incsearch_highlighting(firstc, count, &is_state);
         }
-        if (trigger_cmdlinechanged && (ccline.cmdpos != prev_cmdpos || (prev_cmdbuff != NULL && STRCMP(prev_cmdbuff, ccline.cmdbuff) != 0)))
+        if (trigger_cmdlinechanged && (ccline.cmdpos != prev_cmdpos || (prev_cmdbuff != NULL &&  strcmp((char *)(prev_cmdbuff), (char *)(ccline.cmdbuff))  != 0)))
         {
             trigger_cmd_autocmd(cmdline_type, EVENT_CMDLINECHANGED);
         }
@@ -53165,7 +51374,7 @@
 
     for (p = p_wim; *p; ++p)
     {
-        for (i = 0; ASCII_ISALPHA(p[i]); ++i)
+        for (i = 0;  ( ((unsigned)(p[i]) - 'A' < 26)  ||  ((unsigned)(p[i]) - 'a' < 26) ) ; ++i)
         {
             ;
         }
@@ -53173,27 +51382,27 @@
         {
             return FAIL;
         }
-        if (i == 7 && STRNCMP(p, "longest", 7) == 0)
+        if (i == 7 &&  strncmp((char *)(p), (char *)("longest"), (7))  == 0)
         {
             new_wim_flags[idx] |= WIM_LONGEST;
         }
-        else if (i == 4 && STRNCMP(p, "full", 4) == 0)
+        else if (i == 4 &&  strncmp((char *)(p), (char *)("full"), (4))  == 0)
         {
             new_wim_flags[idx] |= WIM_FULL;
         }
-        else if (i == 4 && STRNCMP(p, "list", 4) == 0)
+        else if (i == 4 &&  strncmp((char *)(p), (char *)("list"), (4))  == 0)
         {
             new_wim_flags[idx] |= WIM_LIST;
         }
-        else if (i == 8 && STRNCMP(p, "lastused", 8) == 0)
+        else if (i == 8 &&  strncmp((char *)(p), (char *)("lastused"), (8))  == 0)
         {
             new_wim_flags[idx] |= WIM_BUFLASTUSED;
         }
-        else if (i == 8 && STRNCMP(p, "noselect", 8) == 0)
+        else if (i == 8 &&  strncmp((char *)(p), (char *)("noselect"), (8))  == 0)
         {
             new_wim_flags[idx] |= WIM_NOSELECT;
         }
-        else if (i == 8 && STRNCMP(p, "noinsert", 8) == 0)
+        else if (i == 8 &&  strncmp((char *)(p), (char *)("noinsert"), (8))  == 0)
         {
             new_wim_flags[idx] |= WIM_NOINSERT;
         }
@@ -53356,7 +51565,7 @@
 }
 
     char_u *
-getexline(int         c, void        *cookie UNUSED, int         indent, getline_opt_T options)
+getexline(int         c, void        *cookie  __attribute__((unused)) , int         indent, getline_opt_T options)
 {
     if (exec_from_reg && vpeekc() == ':')
     {
@@ -53366,7 +51575,7 @@
 }
 
     char_u *
-getexmodeline(int         promptc, void        *cookie UNUSED, int         indent, getline_opt_T options UNUSED)
+getexmodeline(int         promptc, void        *cookie  __attribute__((unused)) , int         indent, getline_opt_T options  __attribute__((unused)) )
 {
     garray_T    line_ga;
     char_u      *pend;
@@ -53448,7 +51657,7 @@
             break;
         }
 
-        if (c1 == K_PS)
+        if (c1 ==   (-(('P') + ((int)('S') << 8)))  )
         {
             bracketed_paste(PASTE_EX, FALSE, &line_ga);
             goto redraw;
@@ -53461,7 +51670,7 @@
                 c1 = '\n';
             }
 
-            if (c1 == BS || c1 == K_BS || c1 == DEL || c1 == K_DEL || c1 == K_KDEL)
+            if (c1 == BS || c1 ==   (-(('k') + ((int)('b') << 8)))   || c1 == DEL || c1 ==   (-(('k') + ((int)('D') << 8)))   || c1 ==   (-((KS_EXTRA) + ((int)(KE_KDEL) << 8)))  )
             {
                 if (line_ga.ga_len > 0)
                 {
@@ -53502,7 +51711,7 @@
                     (void)ga_grow(&line_ga, 2);
                     p = (char_u *)line_ga.ga_data;
                     s = skipwhite(p);
-                    mch_memmove(s + 1, s, line_ga.ga_len - (s - p) + 1);
+                     memmove((char *)(s + 1), (char *)(s), line_ga.ga_len - (s - p) + 1) ;
                     *s = ' ';
                     ++line_ga.ga_len;
                 }
@@ -53561,7 +51770,7 @@
                 while (get_indent_str(p, 8, FALSE) > indent)
                 {
                     s = skipwhite(p);
-                    mch_memmove(s - 1, s, line_ga.ga_len - (s - p) + 1);
+                     memmove((char *)(s - 1), (char *)(s), line_ga.ga_len - (s - p) + 1) ;
                     --line_ga.ga_len;
                 }
                 goto add_indent;
@@ -53573,13 +51782,13 @@
                 continue;
             }
 
-            if (IS_SPECIAL(c1))
+            if ( ((c1) < 0) )
             {
                 continue;
             }
         }
 
-        if (IS_SPECIAL(c1))
+        if ( ((c1) < 0) )
         {
             c1 = '?';
         }
@@ -53644,11 +51853,11 @@
     return (char_u *)line_ga.ga_data;
 }
 
-
     static void
 dealloc_cmdbuff(void)
 {
-    VIM_CLEAR(ccline.cmdbuff);
+     vim_free(ccline.cmdbuff);
+     (ccline.cmdbuff) = NULL;
     ccline.cmdlen = ccline.cmdbufflen = 0;
 }
 
@@ -53689,10 +51898,10 @@
         ccline.cmdbufflen = plen;
         return FAIL;
     }
-    mch_memmove(ccline.cmdbuff, p, (size_t)ccline.cmdlen);
+     memmove((char *)(ccline.cmdbuff), (char *)(p), (size_t)ccline.cmdlen) ;
     ccline.cmdbuff[ccline.cmdlen] = NUL;
 
-    if (ccline.xpc != NULL && ccline.xpc->xp_pattern != NULL && ccline.xpc->xp_context != EXPAND_NOTHING && ccline.xpc->xp_context != EXPAND_UNSUCCESSFUL)
+    if (ccline.xpc != NULL && ccline.xpc->xp_pattern != NULL && ccline.xpc->xp_context != EXPAND_NOTHING && ccline.xpc->xp_context !=  (-2) )
     {
         int i = (int)(ccline.xpc->xp_pattern - p);
 
@@ -53767,7 +51976,7 @@
 
     if (len < 0)
     {
-        len = (int)STRLEN(str);
+        len = (int) strlen((char *)(str)) ;
     }
 
     if (ccline.cmdlen + len + 1 >= ccline.cmdbufflen)
@@ -53782,7 +51991,7 @@
     {
         if (!ccline.overstrike)
         {
-            mch_memmove(ccline.cmdbuff + ccline.cmdpos + len, ccline.cmdbuff + ccline.cmdpos, (size_t)(ccline.cmdlen - ccline.cmdpos));
+             memmove((char *)(ccline.cmdbuff + ccline.cmdpos + len), (char *)(ccline.cmdbuff + ccline.cmdpos), (size_t)(ccline.cmdlen - ccline.cmdpos)) ;
             ccline.cmdlen += len;
         }
         else
@@ -53800,7 +52009,7 @@
                 }
                 if (i < ccline.cmdlen)
                 {
-                    mch_memmove(ccline.cmdbuff + ccline.cmdpos + len, ccline.cmdbuff + i, (size_t)(ccline.cmdlen - i));
+                     memmove((char *)(ccline.cmdbuff + ccline.cmdpos + len), (char *)(ccline.cmdbuff + i), (size_t)(ccline.cmdlen - i)) ;
                     ccline.cmdlen += ccline.cmdpos + len - i;
                 }
                 else
@@ -53813,7 +52022,7 @@
                 ccline.cmdlen = ccline.cmdpos + len;
             }
         }
-        mch_memmove(ccline.cmdbuff + ccline.cmdpos, str, (size_t)len);
+         memmove((char *)(ccline.cmdbuff + ccline.cmdpos), (char *)(str), (size_t)len) ;
         ccline.cmdbuff[ccline.cmdlen] = NUL;
 
         if (enc_utf8)
@@ -53904,7 +52113,7 @@
 {
     if (!prev_ccline_used)
     {
-        CLEAR_FIELD(prev_ccline);
+          memset((&(prev_ccline)), (0), (sizeof(prev_ccline)))  ;
         prev_ccline_used = TRUE;
     }
     *ccp = prev_ccline;
@@ -53976,7 +52185,7 @@
                 }
             }
             len = (int)((ccline.cmdbuff + ccline.cmdpos) - w);
-            if (p_ic ? STRNICMP(w, arg, len) == 0 : STRNCMP(w, arg, len) == 0)
+            if (p_ic ?  strncasecmp((char *)(w), (char *)(arg), (len))  == 0 :  strncmp((char *)(w), (char *)(arg), (len))  == 0)
             {
                 p += len;
             }
@@ -54133,7 +52342,7 @@
     }
     else
     {
-        cmdline_row = W_WINROW(lastwin) + lastwin->w_height
+        cmdline_row =  (lastwin->w_winrow)  + lastwin->w_height
                                                     + lastwin->w_status_height;
     }
 }
@@ -54180,7 +52389,7 @@
         return FALSE;
     }
 
-    while (VIM_ISWHITE(ccline.cmdbuff[spos]) && spos < ccline.cmdlen)
+    while ( ((ccline.cmdbuff[spos]) == ' ' || (ccline.cmdbuff[spos]) == '\t')  && spos < ccline.cmdlen)
     {
         spos++;
     }
@@ -54200,7 +52409,7 @@
 vim_strsave_fnameescape(char_u *fname, int what)
 {
     char_u      *p;
-    p = vim_strsave_escaped(fname, what == VSE_SHELL ? SHELL_ESC_CHARS : what == VSE_BUFFER ? BUFFER_ESC_CHARS : PATH_ESC_CHARS);
+    p = vim_strsave_escaped(fname, what == VSE_SHELL ?  ((char_u *)" \t\n*?[{`$\\%#'\"|!<>();&")  : what == VSE_BUFFER ?  ((char_u *)" \t\n*?[`$\\%#'\"|!<")  :  ((char_u *)" \t\n*?[{`$\\%#'\"|!<") );
     if (what == VSE_SHELL && csh_like_shell() && p != NULL)
     {
         char_u      *s;
@@ -54223,14 +52432,14 @@
 {
     char_u      *p;
 
-    p = alloc(STRLEN(*pp) + 2);
+    p = alloc( strlen((char *)(*pp))  + 2);
     if (p == NULL)
     {
         return;
     }
 
     p[0] = '\\';
-    STRCPY(p + 1, *pp);
+     strcpy((char *)(p + 1), (char *)(*pp)) ;
     vim_free(*pp);
     *pp = p;
 }
@@ -54350,7 +52559,7 @@
 }
 
     char *
-did_set_cedit(optset_T *args UNUSED)
+did_set_cedit(optset_T *args  __attribute__((unused)) )
 {
     int n;
 
@@ -54390,7 +52599,7 @@
     if (text_or_buf_locked() || cmdwin_type != 0)
     {
         beep_flush();
-        return K_IGNORE;
+        return   (-((KS_EXTRA) + ((int)(KE_IGNORE) << 8)))  ;
     }
     set_bufref(&old_curbuf, curbuf);
 
@@ -54405,7 +52614,7 @@
     {
         beep_flush();
         ga_clear(&winsizes);
-        return K_IGNORE;
+        return   (-((KS_EXTRA) + ((int)(KE_IGNORE) << 8)))  ;
     }
     if (!win_valid(old_curwin) || curwin == old_curwin || !bufref_valid(&old_curbuf) || old_curwin->w_buffer != old_curbuf.br_buf)
     {
@@ -54418,7 +52627,7 @@
     cmdwin_type = get_cmdline_type();
     cmdwin_win = curwin;
 
-    newbuf_status = do_ecmd(0, NULL, NULL, NULL, ECMD_ONE, ECMD_HIDE, NULL);
+    newbuf_status = do_ecmd(0, NULL, NULL, NULL,  (linenr_T)1 , ECMD_HIDE, NULL);
     cmdwin_valid = win_valid(cmdwin_win);
     if (newbuf_status == FAIL || !cmdwin_valid || curwin != cmdwin_win || !win_valid(old_curwin) || !bufref_valid(&old_curbuf) || old_curwin->w_buffer != old_curbuf.br_buf)
     {
@@ -54446,7 +52655,8 @@
 
     set_option_value_give_err((char_u *)"bt", 0L, (char_u *)"nofile", OPT_LOCAL);
     curbuf->b_p_ma = TRUE;
-    RESET_BINDING(curwin);
+     (curwin)-> w_onebuf_opt.wo_scb  = FALSE;
+     (curwin)-> w_onebuf_opt.wo_crb  = FALSE;
 
     ++curbuf_lock;
 
@@ -54510,7 +52720,7 @@
     trigger_cmd_autocmd(cmdwin_type, EVENT_CMDWINENTER);
     if (restart_edit != 0)
     {
-        stuffcharReadbuff(K_NOP);
+        stuffcharReadbuff(  (-((KS_EXTRA) + ((int)(KE_NOP) << 8)))  );
     }
 
     int save_RedrawingDisabled = RedrawingDisabled;
@@ -54536,12 +52746,12 @@
     {
         dealloc_cmdbuff();
 
-        if (cmdwin_result == K_XF1 || cmdwin_result == K_XF2)
+        if (cmdwin_result ==   (-((KS_EXTRA) + ((int)(KE_XF1) << 8)))   || cmdwin_result ==   (-((KS_EXTRA) + ((int)(KE_XF2) << 8)))  )
         {
             char_u  *p = (char_u *)"qa";
             size_t  plen = 2;
 
-            if (cmdwin_result == K_XF1)
+            if (cmdwin_result ==   (-((KS_EXTRA) + ((int)(KE_XF1) << 8)))  )
             {
                 p = (char_u *)"qa!";
                 plen = 3;
@@ -54608,7 +52818,7 @@
         win_size_restore(&winsizes);
         skip_win_fix_cursor = FALSE;
 
-        if (cmdwin_result == K_IGNORE)
+        if (cmdwin_result ==   (-((KS_EXTRA) + ((int)(KE_IGNORE) << 8)))  )
         {
             update_screen(UPD_VALID);
             set_cmdspos_cursor();
@@ -54633,18 +52843,13 @@
 }
 
     char_u *
-script_get(exarg_T *eap UNUSED, char_u *cmd UNUSED)
+script_get(exarg_T *eap  __attribute__((unused)) , char_u *cmd  __attribute__((unused)) )
 {
     return NULL;
 }
 
-
 // ==================== fileio.c ====================
 
-#define USE_MCH_ACCESS
-
-#define NONASCII_MASK (((long_u)-1 / 0xFF) * 0x80)
-
 static char_u *next_fenc(char_u **pp, int *alloced);
 static linenr_T readfile_linenr(linenr_T linecnt, char_u *p, char_u *endp);
 static char_u *check_for_bom(char_u *p, long size, int *lenp, int flags);
@@ -54662,16 +52867,16 @@
     }
     msg_add_fname(buf, name);
 
-    len = STRLEN(IObuff);
-    if (len > IOSIZE - 100)
+    len =  strlen((char *)(IObuff)) ;
+    if (len >  (1024+1)  - 100)
     {
-        len = IOSIZE - 100;
+        len =  (1024+1)  - 100;
         IObuff[len] = NUL;
     }
 
     if (*s != NUL)
     {
-        STRNCPY(IObuff + len, s, 99);
+         strncpy((char *)(IObuff + len), (char *)(s), (99)) ;
     }
 
     msg_scroll_save = msg_scroll;
@@ -54724,10 +52929,9 @@
     off_T       filesize = 0;
     int         skip_read = FALSE;
     int         split = 0;
-#define UNKNOWN  0x0fffffff
     linenr_T    linecnt;
     int         error = FALSE;
-    int         ff_error = EOL_UNKNOWN;
+    int         ff_error =  (-1) ;
     long        linerest = 0;
     int         perm = 0;
     int         swap_mode = -1;
@@ -54766,7 +52970,7 @@
     char_u      *old_b_fname;
     int         using_b_ffname;
     int         using_b_fname;
-    static char *msg_is_a_directory = N_("is a directory");
+    static char *msg_is_a_directory =  "is a directory" ;
     size_t      fnamelen = 0;
 
     curbuf->b_au_did_filetype = false;
@@ -54843,9 +53047,9 @@
 
     if (fname != NULL && *fname != NUL)
     {
-        fnamelen = STRLEN(fname);
+        fnamelen =  strlen((char *)(fname)) ;
 
-        if (fnamelen >= MAXPATHL)
+        if (fnamelen >=  PATH_MAX )
         {
             filemess(curbuf, fname, (char_u *)_("Illegal file name"), 0);
             msg_end();
@@ -54896,7 +53100,7 @@
 
     if (newfile && !read_stdin && !read_buffer && !read_fifo)
     {
-        if (mch_stat((char *)fname, &st) >= 0)
+        if ( stat(((char *)fname), (&st))  >= 0)
         {
             buf_store_time(curbuf, &st, fname);
             curbuf->b_mtime_read = curbuf->b_mtime;
@@ -54922,11 +53126,11 @@
     }
     else if (!read_buffer)
     {
-        if (!(perm & 0222) || mch_access((char *)fname, W_OK))
+        if (!(perm & 0222) ||  access(((char *)fname), (W_OK)) )
         {
             file_readonly = TRUE;
         }
-        fd = mch_open((char *)fname, O_RDONLY | O_EXTRA, 0);
+        fd =  open(((char *)fname), (O_RDONLY | O_EXTRA), (0)) ;
     }
 
     if (fd < 0)
@@ -55013,7 +53217,7 @@
             {
                 stat_T  swap_st;
 
-                if (mch_stat((char *)swap_fname, &swap_st) >= 0 && st.st_gid != swap_st.st_gid && fchown(curbuf->b_ml.ml_mfp->mf_fd, -1, st.st_gid) == -1)
+                if ( stat(((char *)swap_fname), (&swap_st))  >= 0 && st.st_gid != swap_st.st_gid && fchown(curbuf->b_ml.ml_mfp->mf_fd, -1, st.st_gid) == -1)
                 {
                     swap_mode &= 0600;
                 }
@@ -55079,7 +53283,7 @@
             msg_scroll = m;
         }
 
-        if (!read_stdin && (curbuf != old_curbuf || (using_b_ffname && (old_b_ffname != curbuf->b_ffname)) || (using_b_fname && (old_b_fname != curbuf->b_fname)) || (fd = mch_open((char *)fname, O_RDONLY | O_EXTRA, 0)) < 0))
+        if (!read_stdin && (curbuf != old_curbuf || (using_b_ffname && (old_b_ffname != curbuf->b_ffname)) || (using_b_fname && (old_b_fname != curbuf->b_fname)) || (fd =  open(((char *)fname), (O_RDONLY | O_EXTRA), (0)) ) < 0))
         {
             --no_wait_return;
             msg_scroll = msg_save;
@@ -55104,7 +53308,7 @@
         {
             if (!is_not_a_term())
             {
-                    mch_msg(_("Vim: Reading from stdin...\n"));
+                     printf("%s", (_("Vim: Reading from stdin...\n"))) ;
             }
         }
         else if (!read_buffer)
@@ -55151,10 +53355,10 @@
         if (!c && !read_stdin)
         {
             fc = fname[fnamelen - 1];
-            if (TOLOWER_ASC(fc) == 'x')
+            if ( (((fc) < 'A' || (fc) > 'Z') ? (fc) : (fc) + ('a' - 'A'))  == 'x')
             {
                 len = read_eintr(fd, firstline, 80);
-                vim_lseek(fd, (off_T)0L, SEEK_SET);
+                 lseek (fd, (off_T)0L, SEEK_SET);
                 for (p = firstline; p < firstline + len; ++p)
                 {
                     if (*p >= 0x80)
@@ -55198,7 +53402,7 @@
             read_buf_lnum = 1;
             read_buf_col = 0;
         }
-        else if (read_stdin || vim_lseek(fd, (off_T)0L, SEEK_SET) != 0)
+        else if (read_stdin ||  lseek (fd, (off_T)0L, SEEK_SET) != 0)
         {
             error = TRUE;
             goto failed;
@@ -55237,7 +53441,7 @@
         }
         else
         {
-            fileformat = EOL_UNKNOWN;
+            fileformat =  (-1) ;
         }
     }
 
@@ -55280,8 +53484,9 @@
         }
         if (tmpname != NULL)
         {
-            mch_remove(tmpname);
-            VIM_CLEAR(tmpname);
+             unlink((char *)(tmpname)) ;
+             vim_free(tmpname);
+             (tmpname) = NULL;
         }
     }
 
@@ -55289,12 +53494,12 @@
     converted = need_conversion(fenc);
     if (converted)
     {
-        if (STRCMP(fenc, ENC_UCSBOM) == 0)
+        if ( strcmp((char *)(fenc), (char *)( "ucs-bom" ))  == 0)
         {
             fio_flags = FIO_UCSBOM;
         }
 
-        else if (enc_utf8 || STRCMP(p_enc, "latin1") == 0)
+        else if (enc_utf8 ||  strcmp((char *)(p_enc), (char *)("latin1"))  == 0)
         {
             fio_flags = get_fio_flags(fenc);
         }
@@ -55360,7 +53565,7 @@
                 }
                 if (linerest)
                 {
-                    mch_memmove(new_buffer, ptr - linerest, (size_t)linerest);
+                     memmove((char *)(new_buffer), (char *)(ptr - linerest), (size_t)linerest) ;
                 }
                 vim_free(buffer);
                 buffer = new_buffer;
@@ -55394,7 +53599,7 @@
 
                 if (conv_restlen > 0)
                 {
-                    mch_memmove(ptr, conv_rest, conv_restlen);
+                     memmove((char *)(ptr), (char *)(conv_rest), conv_restlen) ;
                     ptr += conv_restlen;
                     size -= conv_restlen;
                 }
@@ -55473,7 +53678,6 @@
                     }
                     else if (conv_restlen > 0)
                     {
-
                         if (fio_flags != 0 || iconv_fd != (iconv_t)-1)
                         {
                             if (can_retry)
@@ -55491,14 +53695,14 @@
                             illegal_byte = curbuf->b_ml.ml_line_count
                                                                 - linecnt + 1;
                         }
-                        if (bad_char_behavior == BAD_DROP)
+                        if (bad_char_behavior ==  (-2) )
                         {
                             *(ptr - conv_restlen) = NUL;
                             conv_restlen = 0;
                         }
                         else
                         {
-                            if (bad_char_behavior != BAD_KEEP && (fio_flags != 0 || iconv_fd != (iconv_t)-1))
+                            if (bad_char_behavior !=  (-1)  && (fio_flags != 0 || iconv_fd != (iconv_t)-1))
                             {
                                 while (conv_restlen > 0)
                                 {
@@ -55529,13 +53733,13 @@
                 }
                 else
                 {
-                    ccname = check_for_bom(ptr, size, &blen, fio_flags == FIO_UCSBOM ? FIO_ALL : get_fio_flags(fenc));
+                    ccname = check_for_bom(ptr, size, &blen, fio_flags == FIO_UCSBOM ?  (-1)  : get_fio_flags(fenc));
                 }
                 if (ccname != NULL)
                 {
                     filesize += blen;
                     size -= blen;
-                    mch_memmove(ptr, ptr + blen, (size_t)size);
+                     memmove((char *)(ptr), (char *)(ptr + blen), (size_t)size) ;
                     if (set_options)
                     {
                         curbuf->b_p_bomb = TRUE;
@@ -55584,7 +53788,7 @@
                 top = (char *)ptr;
                 to_size = real_size - size;
 
-                while ((iconv(iconv_fd, (void *)&fromp, &from_size, &top, &to_size) == (size_t)-1 && ICONV_ERRNO != ICONV_EINVAL) || from_size > CONV_RESTLEN)
+                while ((iconv(iconv_fd, (void *)&fromp, &from_size, &top, &to_size) == (size_t)-1 &&  errno  !=  EINVAL ) || from_size > CONV_RESTLEN)
                 {
                     if (can_retry)
                     {
@@ -55597,12 +53801,12 @@
 
                     ++fromp;
                     --from_size;
-                    if (bad_char_behavior == BAD_KEEP)
+                    if (bad_char_behavior ==  (-1) )
                     {
                         *top++ = *(fromp - 1);
                         --to_size;
                     }
-                    else if (bad_char_behavior != BAD_DROP)
+                    else if (bad_char_behavior !=  (-2) )
                     {
                         *top++ = bad_char_behavior;
                         --to_size;
@@ -55611,12 +53815,12 @@
 
                 if (from_size > 0)
                 {
-                    mch_memmove(conv_rest, (char_u *)fromp, from_size);
+                     memmove((char *)(conv_rest), (char *)((char_u *)fromp), from_size) ;
                     conv_restlen = (int)from_size;
                 }
 
                 line_start = ptr - linerest;
-                mch_memmove(line_start, buffer, (size_t)linerest);
+                 memmove((char *)(line_start), (char *)(buffer), (size_t)linerest) ;
                 size = (long)((char_u *)top - ptr);
             }
 
@@ -55688,7 +53892,7 @@
                 if (tail != NULL)
                 {
                     conv_restlen = (int)((ptr + size) - tail);
-                    mch_memmove(conv_rest, (char_u *)tail, conv_restlen);
+                     memmove((char *)(conv_rest), (char *)((char_u *)tail), conv_restlen) ;
                     size -= conv_restlen;
                 }
 
@@ -55724,11 +53928,11 @@
                                 {
                                     conv_error = readfile_linenr(linecnt, ptr, p);
                                 }
-                                if (bad_char_behavior == BAD_DROP)
+                                if (bad_char_behavior ==  (-2) )
                                 {
                                     continue;
                                 }
-                                if (bad_char_behavior != BAD_KEEP)
+                                if (bad_char_behavior !=  (-1) )
                                 {
                                     u8c = bad_char_behavior;
                                 }
@@ -55757,11 +53961,11 @@
                                 {
                                     conv_error = readfile_linenr(linecnt, ptr, p);
                                 }
-                                if (bad_char_behavior == BAD_DROP)
+                                if (bad_char_behavior ==  (-2) )
                                 {
                                     continue;
                                 }
-                                if (bad_char_behavior != BAD_KEEP)
+                                if (bad_char_behavior !=  (-1) )
                                 {
                                     u8c = bad_char_behavior;
                                 }
@@ -55806,11 +54010,11 @@
                                 {
                                     conv_error = readfile_linenr(linecnt, ptr, p);
                                 }
-                                if (bad_char_behavior == BAD_DROP)
+                                if (bad_char_behavior ==  (-2) )
                                 {
                                     continue;
                                 }
-                                if (bad_char_behavior != BAD_KEEP)
+                                if (bad_char_behavior !=  (-1) )
                                 {
                                     u8c = bad_char_behavior;
                                 }
@@ -55835,11 +54039,11 @@
                             {
                                 conv_error = readfile_linenr(linecnt, ptr, p);
                             }
-                            if (bad_char_behavior == BAD_DROP)
+                            if (bad_char_behavior ==  (-2) )
                             {
                                 ++dest;
                             }
-                            else if (bad_char_behavior == BAD_KEEP)
+                            else if (bad_char_behavior ==  (-1) )
                             {
                                 *dest = u8c;
                             }
@@ -55860,7 +54064,7 @@
                 }
 
                 line_start = dest - linerest;
-                mch_memmove(line_start, buffer, (size_t)linerest);
+                 memmove((char *)(line_start), (char *)(buffer), (size_t)linerest) ;
                 size = (long)((ptr + real_size) - dest);
                 ptr = dest;
             }
@@ -55879,7 +54083,7 @@
                         {
                             long_u word;
                             memcpy(&word, p, sizeof(long_u));
-                            if (word & NONASCII_MASK)
+                            if (word &  (((long_u)-1 / 0xFF) * 0x80) )
                             {
                                 break;
                             }
@@ -55908,7 +54112,7 @@
                             if (p > ptr)
                             {
                                 conv_restlen = todo;
-                                mch_memmove(conv_rest, p, conv_restlen);
+                                 memmove((char *)(conv_rest), (char *)(p), conv_restlen) ;
                                 size -= conv_restlen;
                                 break;
                             }
@@ -55928,14 +54132,14 @@
                                 illegal_byte = readfile_linenr(linecnt, ptr, p);
                             }
 
-                            if (bad_char_behavior == BAD_DROP)
+                            if (bad_char_behavior ==  (-2) )
                             {
-                                mch_memmove(p, p + 1, todo - 1);
+                                 memmove((char *)(p), (char *)(p + 1), todo - 1) ;
                                 --size;
                             }
                             else
                             {
-                                if (bad_char_behavior != BAD_KEEP)
+                                if (bad_char_behavior !=  (-1) )
                                 {
                                     *p = bad_char_behavior;
                                 }
@@ -55959,7 +54163,7 @@
 
             filesize += size;
 
-            if (fileformat == EOL_UNKNOWN)
+            if (fileformat ==  (-1) )
             {
                 if (try_dos || try_unix)
                 {
@@ -56015,18 +54219,18 @@
                             }
                         }
                     }
-                    else if (fileformat == EOL_UNKNOWN && try_mac == 1)
+                    else if (fileformat ==  (-1)  && try_mac == 1)
                     {
                         fileformat = default_fileformat();
                     }
                 }
 
-                if (fileformat == EOL_UNKNOWN && try_mac)
+                if (fileformat ==  (-1)  && try_mac)
                 {
                     fileformat = EOL_MAC;
                 }
 
-                if (fileformat == EOL_UNKNOWN)
+                if (fileformat ==  (-1) )
                 {
                     fileformat = default_fileformat();
                 }
@@ -56125,7 +54329,7 @@
                         }
                         else if (ff_error != EOL_DOS)
                         {
-                            if (   try_unix && !read_stdin && (read_buffer || vim_lseek(fd, (off_T)0L, SEEK_SET) == 0))
+                            if (   try_unix && !read_stdin && (read_buffer ||  lseek (fd, (off_T)0L, SEEK_SET) == 0))
                             {
                                 fileformat = EOL_UNIX;
                                 if (set_options)
@@ -56240,7 +54444,7 @@
 
     if (tmpname != NULL)
     {
-        mch_remove(tmpname);
+         unlink((char *)(tmpname)) ;
         vim_free(tmpname);
     }
     --no_wait_return;
@@ -56295,20 +54499,20 @@
             msg_add_fname(curbuf, sfname);
             c = FALSE;
 
-            buflen = (int)STRLEN(IObuff);
+            buflen = (int) strlen((char *)(IObuff)) ;
             if (S_ISFIFO(perm))
             {
-                buflen += vim_snprintf((char *)IObuff + buflen, IOSIZE - buflen, _("[fifo]"));
+                buflen += vim_snprintf((char *)IObuff + buflen,  (1024+1)  - buflen, _("[fifo]"));
                 c = TRUE;
             }
             if (S_ISSOCK(perm))
             {
-                buflen += vim_snprintf((char *)IObuff + buflen, IOSIZE - buflen, _("[socket]"));
+                buflen += vim_snprintf((char *)IObuff + buflen,  (1024+1)  - buflen, _("[socket]"));
                 c = TRUE;
             }
             if (curbuf->b_p_ro)
             {
-                buflen += vim_snprintf((char *)IObuff + buflen, IOSIZE - buflen, "%s", shortmess(SHM_RO) ? _("[RO]") : _("[readonly]"));
+                buflen += vim_snprintf((char *)IObuff + buflen,  (1024+1)  - buflen, "%s", shortmess(SHM_RO) ? _("[RO]") : _("[readonly]"));
                 c = TRUE;
             }
             if (read_no_eol_lnum)
@@ -56318,37 +54522,37 @@
             }
             if (ff_error == EOL_DOS)
             {
-                buflen += vim_snprintf((char *)IObuff + buflen, IOSIZE - buflen, _("[CR missing]"));
+                buflen += vim_snprintf((char *)IObuff + buflen,  (1024+1)  - buflen, _("[CR missing]"));
                 c = TRUE;
             }
             if (split)
             {
-                buflen += vim_snprintf((char *)IObuff + buflen, IOSIZE - buflen, _("[long lines split]"));
+                buflen += vim_snprintf((char *)IObuff + buflen,  (1024+1)  - buflen, _("[long lines split]"));
                 c = TRUE;
             }
             if (notconverted)
             {
-                buflen += vim_snprintf((char *)IObuff + buflen, IOSIZE - buflen, _("[NOT converted]"));
+                buflen += vim_snprintf((char *)IObuff + buflen,  (1024+1)  - buflen, _("[NOT converted]"));
                 c = TRUE;
             }
             else if (converted)
             {
-                buflen += vim_snprintf((char *)IObuff + buflen, IOSIZE - buflen, _("[converted]"));
+                buflen += vim_snprintf((char *)IObuff + buflen,  (1024+1)  - buflen, _("[converted]"));
                 c = TRUE;
             }
             if (conv_error != 0)
             {
-                vim_snprintf((char *)IObuff + buflen, IOSIZE - buflen, _("[CONVERSION ERROR in line %ld]"), (long)conv_error);
+                vim_snprintf((char *)IObuff + buflen,  (1024+1)  - buflen, _("[CONVERSION ERROR in line %ld]"), (long)conv_error);
                 c = TRUE;
             }
             else if (illegal_byte > 0)
             {
-                vim_snprintf((char *)IObuff + buflen, IOSIZE - buflen, _("[ILLEGAL BYTE in line %ld]"), (long)illegal_byte);
+                vim_snprintf((char *)IObuff + buflen,  (1024+1)  - buflen, _("[ILLEGAL BYTE in line %ld]"), (long)illegal_byte);
                 c = TRUE;
             }
             else if (error)
             {
-                vim_snprintf((char *)IObuff + buflen, IOSIZE - buflen, _("[READ ERRORS]"));
+                vim_snprintf((char *)IObuff + buflen,  (1024+1)  - buflen, _("[READ ERRORS]"));
                 c = TRUE;
             }
             if (msg_add_fileformat(fileformat))
@@ -56357,7 +54561,8 @@
             }
                 msg_add_lines(c, (long)linecnt, filesize);
 
-            VIM_CLEAR(keep_msg);
+             vim_free(keep_msg);
+             (keep_msg) = NULL;
             msg_scrolled_ign = TRUE;
             {
                 if (msg_col > 0)
@@ -56373,7 +54578,7 @@
             msg_scrolled_ign = FALSE;
         }
 
-        if (newfile && (error || conv_error != 0 || (illegal_byte > 0 && bad_char_behavior != BAD_KEEP)))
+        if (newfile && (error || conv_error != 0 || (illegal_byte > 0 && bad_char_behavior !=  (-1) )))
         {
             curbuf->b_p_ro = TRUE;
         }
@@ -56476,7 +54681,7 @@
     int
 prep_exarg(exarg_T *eap, buf_T *buf)
 {
-    eap->cmd = alloc(15 + (unsigned)STRLEN(buf->b_p_fenc));
+    eap->cmd = alloc(15 + (unsigned) strlen((char *)(buf->b_p_fenc)) );
     if (eap->cmd == NULL)
     {
         return FAIL;
@@ -56550,7 +54755,7 @@
     if (p == NULL)
     {
         r = enc_canonize(*pp);
-        *pp += STRLEN(*pp);
+        *pp +=  strlen((char *)(*pp)) ;
     }
     else
     {
@@ -56576,10 +54781,9 @@
 }
 
     int
-check_file_readonly(char_u      *fname, int         perm UNUSED)
+check_file_readonly(char_u      *fname, int         perm  __attribute__((unused)) )
 {
-
-    return ((perm & 0222) == 0 || mch_access((char *)fname, W_OK));
+    return ((perm & 0222) == 0 ||  access(((char *)fname), (W_OK)) );
 }
 
     int
@@ -56640,8 +54844,8 @@
         fname = (char_u *)"-stdin-";
     }
     IObuff[IObufflen++] = '"';
-    IObufflen += home_replace(buf, fname, IObuff + IObufflen, IOSIZE - 4, TRUE);
-    STRCPY(IObuff + IObufflen, "\" ");
+    IObufflen += home_replace(buf, fname, IObuff + IObufflen,  (1024+1)  - 4, TRUE);
+     strcpy((char *)(IObuff + IObufflen), (char *)("\" ")) ;
 }
 
     int
@@ -56649,12 +54853,12 @@
 {
     if (eol_type == EOL_DOS)
     {
-        STRCAT(IObuff, shortmess(SHM_TEXT) ? _("[dos]") : _("[dos format]"));
+         strcat((char *)(IObuff), (char *)(shortmess(SHM_TEXT) ? _("[dos]") : _("[dos format]"))) ;
         return TRUE;
     }
     if (eol_type == EOL_MAC)
     {
-        STRCAT(IObuff, shortmess(SHM_TEXT) ? _("[mac]") : _("[mac format]"));
+         strcat((char *)(IObuff), (char *)(shortmess(SHM_TEXT) ? _("[mac]") : _("[mac format]"))) ;
         return TRUE;
     }
     return FALSE;
@@ -56663,30 +54867,30 @@
     void
 msg_add_lines(int     insert_space, long    lnum, off_T   nchars)
 {
-    int  len = (int)STRLEN(IObuff);
+    int  len = (int) strlen((char *)(IObuff)) ;
 
     if (shortmess(SHM_LINES))
     {
-        vim_snprintf((char *)IObuff + len, IOSIZE - (size_t)len, _("%s%ldL, %lldB"), insert_space ? " " : "", lnum, (varnumber_T)nchars);
+        vim_snprintf((char *)IObuff + len,  (1024+1)  - (size_t)len, _("%s%ldL, %lldB"), insert_space ? " " : "", lnum, (varnumber_T)nchars);
     }
     else
     {
-        len += vim_snprintf((char *)IObuff + len, IOSIZE - (size_t)len, NGETTEXT("%s%ld line, ", "%s%ld lines, ", lnum), insert_space ? " " : "", lnum);
-        vim_snprintf((char *)IObuff + len, IOSIZE - (size_t)len, NGETTEXT("%lld byte", "%lld bytes", nchars), (varnumber_T)nchars);
+        len += vim_snprintf((char *)IObuff + len,  (1024+1)  - (size_t)len, NGETTEXT("%s%ld line, ", "%s%ld lines, ", lnum), insert_space ? " " : "", lnum);
+        vim_snprintf((char *)IObuff + len,  (1024+1)  - (size_t)len, NGETTEXT("%lld byte", "%lld bytes", nchars), (varnumber_T)nchars);
     }
 }
 
     void
 msg_add_eol(void)
 {
-    STRCAT(IObuff, shortmess(SHM_LAST) ? _("[noeol]") : _("[Incomplete last line]"));
+     strcat((char *)(IObuff), (char *)(shortmess(SHM_LAST) ? _("[noeol]") : _("[Incomplete last line]"))) ;
 }
 
     int
-time_differs(stat_T *st, long mtime, long mtime_ns UNUSED)
+time_differs(stat_T *st, long mtime, long mtime_ns  __attribute__((unused)) )
 {
     return
-        (long)st->ST_MTIM_NSEC != mtime_ns ||
+        (long)st-> st_mtim.tv_nsec  != mtime_ns ||
         (long)st->st_mtime - mtime > 1 || mtime - (long)st->st_mtime > 1
         ;
 }
@@ -56698,7 +54902,7 @@
     int         enc_flags;
     int         fenc_flags;
 
-    if (*fenc == NUL || STRCMP(p_enc, fenc) == 0)
+    if (*fenc == NUL ||  strcmp((char *)(p_enc), (char *)(fenc))  == 0)
     {
         same_encoding = TRUE;
         fenc_flags = 0;
@@ -56769,14 +54973,14 @@
     char        *name = NULL;
     int         len = 2;
 
-    if (p[0] == 0xef && p[1] == 0xbb && size >= 3 && p[2] == 0xbf && (flags == FIO_ALL || flags == FIO_UTF8 || flags == 0))
+    if (p[0] == 0xef && p[1] == 0xbb && size >= 3 && p[2] == 0xbf && (flags ==  (-1)  || flags == FIO_UTF8 || flags == 0))
     {
         name = "utf-8";
         len = 3;
     }
     else if (p[0] == 0xff && p[1] == 0xfe)
     {
-        if (size >= 4 && p[2] == 0 && p[3] == 0 && (flags == FIO_ALL || flags == (FIO_UCS4 | FIO_ENDIAN_L)))
+        if (size >= 4 && p[2] == 0 && p[3] == 0 && (flags ==  (-1)  || flags == (FIO_UCS4 | FIO_ENDIAN_L)))
         {
             name = "ucs-4le";
             len = 4;
@@ -56785,12 +54989,12 @@
         {
             name = "ucs-2le";
         }
-        else if (flags == FIO_ALL || flags == (FIO_UTF16 | FIO_ENDIAN_L))
+        else if (flags ==  (-1)  || flags == (FIO_UTF16 | FIO_ENDIAN_L))
         {
             name = "utf-16le";
         }
     }
-    else if (p[0] == 0xfe && p[1] == 0xff && (flags == FIO_ALL || flags == FIO_UCS2 || flags == FIO_UTF16))
+    else if (p[0] == 0xfe && p[1] == 0xff && (flags ==  (-1)  || flags == FIO_UCS2 || flags == FIO_UTF16))
     {
         if (flags == FIO_UCS2)
         {
@@ -56801,7 +55005,7 @@
             name = "utf-16";
         }
     }
-    else if (size >= 4 && p[0] == 0 && p[1] == 0 && p[2] == 0xfe && p[3] == 0xff && (flags == FIO_ALL || flags == FIO_UCS4))
+    else if (size >= 4 && p[0] == 0 && p[1] == 0 && p[2] == 0xfe && p[3] == 0xff && (flags ==  (-1)  || flags == FIO_UCS4))
     {
         name = "ucs-4";
         len = 4;
@@ -56817,12 +55021,12 @@
     char_u      *dirname;
     char_u      *p = full_path;
 
-    dirname = alloc(MAXPATHL);
+    dirname = alloc( PATH_MAX );
     if (dirname == NULL)
     {
         return full_path;
     }
-    if (mch_dirname(dirname, MAXPATHL) == OK)
+    if (mch_dirname(dirname,  PATH_MAX ) == OK)
     {
         p = shorten_fname(full_path, dirname);
         if (p == NULL || *p == NUL)
@@ -56844,8 +55048,8 @@
     {
         return NULL;
     }
-    len = (int)STRLEN(dir_name);
-    if (fnamencmp(dir_name, full_path, len) == 0)
+    len = (int) strlen((char *)(dir_name)) ;
+    if ( vim_fnamencmp((char_u *)(dir_name), (char_u *)(full_path), (len))  == 0)
     {
         p = full_path + len;
         {
@@ -56880,7 +55084,8 @@
     {
         if (buf->b_sfname != buf->b_ffname)
         {
-            VIM_CLEAR(buf->b_sfname);
+             vim_free(buf->b_sfname);
+             (buf->b_sfname) = NULL;
         }
         p = shorten_fname(buf->b_ffname, dirname);
         if (p != NULL)
@@ -56898,11 +55103,11 @@
     void
 shorten_fnames(int force)
 {
-    char_u      dirname[MAXPATHL];
+    char_u      dirname[ PATH_MAX ];
     buf_T       *buf;
 
-    mch_dirname(dirname, MAXPATHL);
-    FOR_ALL_BUFFERS(buf)
+    mch_dirname(dirname,  PATH_MAX );
+     for ((buf) = firstbuf; (buf) != NULL; (buf) = (buf)->b_next) 
     {
         shorten_buf_fname(buf, dirname, force);
 
@@ -56929,39 +55134,39 @@
     int fnamelen;
     int extlen;
 
-    extlen = (int)STRLEN(ext);
+    extlen = (int) strlen((char *)(ext)) ;
 
     if (fname == NULL || *fname == NUL)
     {
-        retval = alloc(MAXPATHL + extlen + 3);
+        retval = alloc( PATH_MAX  + extlen + 3);
         if (retval == NULL)
         {
             return NULL;
         }
-        if (mch_dirname(retval, MAXPATHL) == FAIL || (fnamelen = (int)STRLEN(retval)) == 0)
+        if (mch_dirname(retval,  PATH_MAX ) == FAIL || (fnamelen = (int) strlen((char *)(retval)) ) == 0)
         {
             vim_free(retval);
             return NULL;
         }
         if (!after_pathsep(retval, retval + fnamelen))
         {
-            retval[fnamelen++] = PATHSEP;
+            retval[fnamelen++] =  ((char_u)'/') ;
             retval[fnamelen] = NUL;
         }
         prepend_dot = FALSE;
     }
     else
     {
-        fnamelen = (int)STRLEN(fname);
+        fnamelen = (int) strlen((char *)(fname)) ;
         retval = alloc(fnamelen + extlen + 3);
         if (retval == NULL)
         {
             return NULL;
         }
-        STRCPY(retval, fname);
+         strcpy((char *)(retval), (char *)(fname)) ;
     }
 
-    for (ptr = retval + fnamelen; ptr > retval; MB_PTR_BACK(retval, ptr))
+    for (ptr = retval + fnamelen; ptr > retval;  ptr -= has_mbyte ? ((*mb_head_off)(retval, (ptr) - 1) + 1) : 1 )
     {
         if (*ext == '.' && shortname)
         {
@@ -56978,9 +55183,9 @@
     }
 
     ptrlen = (size_t)(fnamelen - (ptr - retval));
-    if (ptrlen > (unsigned)BASENAMELEN)
+    if (ptrlen > (unsigned) (MAXNAMLEN - 5) )
     {
-        ptrlen = BASENAMELEN;
+        ptrlen =  (MAXNAMLEN - 5) ;
         ptr[ptrlen] = NUL;
     }
 
@@ -56988,7 +55193,7 @@
 
     if (shortname)
     {
-        if (fname == NULL || *fname == NUL || vim_ispathsep(fname[STRLEN(fname) - 1]))
+        if (fname == NULL || *fname == NUL || vim_ispathsep(fname[ strlen((char *)(fname))  - 1]))
         {
             if (*ext == '.')
             {
@@ -57013,14 +55218,14 @@
         }
     }
 
-    STRCPY(s, ext);
+     strcpy((char *)(s), (char *)(ext)) ;
     if (prepend_dot && !shortname && *(e = gettail(retval)) != '.')
     {
-        mch_memmove(e + 1, e, (size_t)(((fnamelen + extlen) - (e - retval)) + 1));
+         memmove((char *)(e + 1), (char *)(e), (size_t)(((fnamelen + extlen) - (e - retval)) + 1)) ;
         *e = '.';
     }
 
-    if (fname != NULL && STRCMP(fname, retval) == 0)
+    if (fname != NULL &&  strcmp((char *)(fname), (char *)(retval))  == 0)
     {
         while (--s >= ptr)
         {
@@ -57042,7 +55247,6 @@
 vim_fgets(char_u *buf, int size, FILE *fp)
 {
     char        *eof;
-#define FGETS_SIZE 200
     char        tbuf[FGETS_SIZE];
 
     if (size < 2)
@@ -57077,9 +55281,9 @@
     stat_T      st;
     int         use_tmp_file = FALSE;
 
-    if (fnamecmp(from, to) == 0)
+    if ( vim_fnamecmp((char_u *)(from), (char_u *)(to))  == 0)
     {
-        if (p_fic && STRCMP(gettail(from), gettail(to)) != 0)
+        if (p_fic &&  strcmp((char *)(gettail(from)), (char *)(gettail(to)))  != 0)
         {
             use_tmp_file = TRUE;
         }
@@ -57089,7 +55293,7 @@
         }
     }
 
-    if (mch_stat((char *)from, &st) < 0)
+    if ( stat(((char *)from), (&st))  < 0)
     {
         return -1;
     }
@@ -57097,7 +55301,7 @@
     {
         stat_T  st_to;
 
-        if (mch_stat((char *)to, &st_to) >= 0 && st.st_dev == st_to.st_dev && st.st_ino == st_to.st_ino)
+        if ( stat(((char *)to), (&st_to))  >= 0 && st.st_dev == st_to.st_dev && st.st_ino == st_to.st_ino)
         {
             use_tmp_file = TRUE;
         }
@@ -57105,25 +55309,25 @@
 
     if (use_tmp_file)
     {
-        char    tempname[MAXPATHL + 1];
+        char    tempname[ PATH_MAX  + 1];
 
-        if (STRLEN(from) >= MAXPATHL - 5)
+        if ( strlen((char *)(from))  >=  PATH_MAX  - 5)
         {
             return -1;
         }
-        STRCPY(tempname, from);
+         strcpy((char *)(tempname), (char *)(from)) ;
         for (n = 123; n < 99999; ++n)
         {
             sprintf((char *)gettail((char_u *)tempname), "%d", n);
-            if (mch_stat(tempname, &st) < 0)
+            if ( stat((tempname), (&st))  < 0)
             {
-                if (mch_rename((char *)from, tempname) == 0)
+                if ( rename((char *)from, tempname)  == 0)
                 {
-                    if (mch_rename(tempname, (char *)to) == 0)
+                    if ( rename(tempname, (char *)to)  == 0)
                     {
                         return 0;
                     }
-                    (void)mch_rename(tempname, (char *)from);
+                    (void) rename(tempname, (char *)from) ;
                     return -1;
                 }
                 return -1;
@@ -57132,9 +55336,9 @@
         return -1;
     }
 
-    mch_remove(to);
+     unlink((char *)(to)) ;
 
-    if (mch_rename((char *)from, (char *)to) == 0)
+    if ( rename((char *)from, (char *)to)  == 0)
     {
         return 0;
     }
@@ -57145,9 +55349,9 @@
         return -1;
     }
 
-    if (mch_stat((char *)from, &st) >= 0)
+    if ( stat(((char *)from), (&st))  >= 0)
     {
-        mch_remove(from);
+         unlink((char *)(from)) ;
     }
 
     return 0;
@@ -57167,14 +55371,14 @@
     int         ret;
     int         len;
     stat_T      st;
-    char        linkbuf[MAXPATHL + 1];
+    char        linkbuf[ PATH_MAX  + 1];
 
-    ret = mch_lstat((char *)from, &st);
+    ret =  lstat(((char *)from), (&st)) ;
     if (ret >= 0 && S_ISLNK(st.st_mode))
     {
         ret = -1;
 
-        len = readlink((char *)from, linkbuf, MAXPATHL);
+        len = readlink((char *)from, linkbuf,  PATH_MAX );
         if (len > 0)
         {
             linkbuf[len] = NUL;
@@ -57187,14 +55391,14 @@
 
     perm = mch_getperm(from);
     acl = mch_get_acl(from);
-    fd_in = mch_open((char *)from, O_RDONLY|O_EXTRA, 0);
+    fd_in =  open(((char *)from), (O_RDONLY|O_EXTRA), (0)) ;
     if (fd_in == -1)
     {
         mch_free_acl(acl);
         return FAIL;
     }
 
-    fd_out = mch_open((char *)to, O_CREAT|O_EXCL|O_WRONLY|O_EXTRA|O_NOFOLLOW, (int)perm);
+    fd_out =  open(((char *)to), (O_CREAT|O_EXCL|O_WRONLY|O_EXTRA|O_NOFOLLOW), ((int)perm)) ;
     if (fd_out == -1)
     {
         close(fd_in);
@@ -57270,7 +55474,7 @@
         ++no_wait_return;
         did_check_timestamps = TRUE;
         already_warned = FALSE;
-        FOR_ALL_BUFFERS(buf)
+         for ((buf) = firstbuf; (buf) != NULL; (buf) = (buf)->b_next) 
         {
             if (buf->b_nwindows > 0)
             {
@@ -57339,7 +55543,7 @@
 }
 
     int
-buf_check_timestamp(buf_T       *buf, int         focus UNUSED)
+buf_check_timestamp(buf_T       *buf, int         focus  __attribute__((unused)) )
 {
     stat_T      st;
     int         stat_res;
@@ -57365,7 +55569,7 @@
         return 0;
     }
 
-    if (       !(buf->b_flags & BF_NOTEDITED) && buf->b_mtime != 0 && ((stat_res = mch_stat((char *)buf->b_ffname, &st)) < 0 || time_differs(&st, buf->b_mtime, buf->b_mtime_ns) || st.st_size != buf->b_orig_size || (int)st.st_mode != buf->b_orig_mode))
+    if (       !(buf->b_flags & BF_NOTEDITED) && buf->b_mtime != 0 && ((stat_res =  stat(((char *)buf->b_ffname), (&st)) ) < 0 || time_differs(&st, buf->b_mtime, buf->b_mtime_ns) || st.st_size != buf->b_orig_size || (int)st.st_mode != buf->b_orig_mode))
     {
         long prev_b_mtime = buf->b_mtime;
 
@@ -57487,14 +55691,14 @@
             {
                 mesg2 = "";
             }
-            tbufsize = STRLEN(mesg) + STRLEN(path) + 2 + STRLEN(mesg2) + 1;
+            tbufsize =  strlen((char *)(mesg))  +  strlen((char *)(path))  + 2 +  strlen((char *)(mesg2))  + 1;
             tbuf = alloc(tbufsize);
             if (tbuf != NULL)
             {
                 int tbuflen;
 
                 tbuflen = vim_snprintf(tbuf, tbufsize, mesg, path);
-                if (State > MODE_NORMAL_BUSY || (State & MODE_CMDLINE) || already_warned)
+                if (State >  (0x1000 | MODE_NORMAL)  || (State & MODE_CMDLINE) || already_warned)
                 {
                     if (*mesg2 != NUL)
                     {
@@ -57508,10 +55712,10 @@
                     if (!autocmd_busy)
                     {
                         msg_start();
-                        msg_puts_attr(tbuf, HL_ATTR(HLF_E) + MSG_HIST);
+                        msg_puts_attr(tbuf,  highlight_attr[(int)(HLF_E)]  + MSG_HIST);
                         if (*mesg2 != NUL)
                         {
-                            msg_puts_attr(mesg2, HL_ATTR(HLF_W) + MSG_HIST);
+                            msg_puts_attr(mesg2,  highlight_attr[(int)(HLF_W)]  + MSG_HIST);
                         }
                         msg_clr_eos();
                         (void)msg_end();
@@ -57567,7 +55771,7 @@
 
     if (reload_options)
     {
-        CLEAR_FIELD(ea);
+          memset((&(ea)), (0), (sizeof(ea)))  ;
     }
     else
     {
@@ -57586,7 +55790,7 @@
             flags |= READ_KEEP_UNDO;
         }
 
-        if (BUFEMPTY() || saved == FAIL)
+        if ( (curbuf->b_ml.ml_line_count == 1 && *ml_get((linenr_T)1) == NUL)  || saved == FAIL)
         {
             savebuf = NULL;
         }
@@ -57621,12 +55825,12 @@
                 msg_silent = 1;
             }
 
-            if (readfile(buf->b_ffname, buf->b_fname, (linenr_T)0, (linenr_T)0, (linenr_T)MAXLNUM, &ea, flags) != OK)
+            if (readfile(buf->b_ffname, buf->b_fname, (linenr_T)0, (linenr_T)0, (linenr_T) LONG_MAX , &ea, flags) != OK)
             {
                     semsg(_(e_could_not_reload_str), buf->b_fname);
                 if (savebuf != NULL && bufref_valid(&bufref) && buf == curbuf)
                 {
-                    while (!BUFEMPTY())
+                    while (! (curbuf->b_ml.ml_line_count == 1 && *ml_get((linenr_T)1) == NUL) )
                     {
                         if (ml_delete(buf->b_ml.ml_line_count) == FAIL)
                         {
@@ -57682,10 +55886,10 @@
 }
 
     void
-buf_store_time(buf_T *buf, stat_T *st, char_u *fname UNUSED)
+buf_store_time(buf_T *buf, stat_T *st, char_u *fname  __attribute__((unused)) )
 {
     buf->b_mtime = (long)st->st_mtime;
-    buf->b_mtime_ns = (long)st->ST_MTIM_NSEC;
+    buf->b_mtime_ns = (long)st-> st_mtim.tv_nsec ;
     buf->b_orig_size = st->st_size;
     buf->b_orig_mode = (int)st->st_mode;
 }
@@ -57700,7 +55904,7 @@
 }
 
     int
-readdir_core(garray_T    *gap, char_u      *path, int         withattr UNUSED, void        *context, int         (*checkitem)(void *context, void *item), int         sort)
+readdir_core(garray_T    *gap, char_u      *path, int         withattr  __attribute__((unused)) , void        *context, int         (*checkitem)(void *context, void *item), int         sort)
 {
     int                 failed = FALSE;
     char_u              *p;
@@ -57709,8 +55913,6 @@
 
     ga_init2(gap, sizeof(void *), 20);
 
-#  define FREE_ITEM(item)   vim_free(item)
-
     dirp = opendir((char *)path);
     if (dirp == NULL)
     {
@@ -57750,7 +55952,7 @@
 
                 if (r < 0)
                 {
-                    FREE_ITEM(item);
+                     vim_free(item) ;
                     break;
                 }
                 if (r == 0)
@@ -57768,13 +55970,13 @@
                 else
                 {
                     failed = TRUE;
-                    FREE_ITEM(item);
+                     vim_free(item) ;
                     break;
                 }
             }
             else
             {
-                FREE_ITEM(item);
+                 vim_free(item) ;
             }
         }
 
@@ -57805,19 +56007,19 @@
         }
         if (readdir_core(&ga, exp, FALSE, NULL, NULL, READDIR_SORT_NONE) == OK)
         {
-            int len = vim_snprintf((char *)NameBuff, MAXPATHL, "%s/", exp);
+            int len = vim_snprintf((char *)NameBuff,  PATH_MAX , "%s/", exp);
             int i;
 
             for (i = 0; i < ga.ga_len; ++i)
             {
-                vim_snprintf((char *)NameBuff + len, MAXPATHL - len, "%s", ((char_u **)ga.ga_data)[i]);
+                vim_snprintf((char *)NameBuff + len,  PATH_MAX  - len, "%s", ((char_u **)ga.ga_data)[i]);
                 if (delete_recursive(NameBuff) != 0)
                 {
                     result = -1;
                 }
             }
             ga_clear_strings(&ga);
-            if (mch_rmdir(exp) != 0)
+            if ( rmdir((char *)(exp))  != 0)
             {
                 result = -1;
             }
@@ -57830,7 +56032,7 @@
     }
     else
     {
-        result = mch_remove(name) == 0 ? 0 : -1;
+        result =  unlink((char *)(name))  == 0 ? 0 : -1;
     }
 
     return result;
@@ -57893,7 +56095,8 @@
     vim_closetempdir();
     gettail(vim_tempdir)[-1] = NUL;
     delete_recursive(vim_tempdir);
-    VIM_CLEAR(vim_tempdir);
+     vim_free(vim_tempdir);
+     (vim_tempdir) = NULL;
 }
 
     static void
@@ -57902,21 +56105,21 @@
     char_u      *buf;
     size_t      buflen;
 
-    buf = alloc(MAXPATHL + 2);
+    buf = alloc( PATH_MAX  + 2);
     if (buf == NULL)
     {
         return;
     }
 
-    if (vim_FullName(tempdir, buf, MAXPATHL, FALSE) == FAIL)
+    if (vim_FullName(tempdir, buf,  PATH_MAX , FALSE) == FAIL)
     {
-        STRCPY(buf, tempdir);
+         strcpy((char *)(buf), (char *)(tempdir)) ;
     }
-    buflen = STRLEN(buf);
+    buflen =  strlen((char *)(buf)) ;
     if (!after_pathsep(buf, buf + buflen))
     {
-        STRCPY(buf + buflen, PATHSEPSTR);
-        buflen += sizeof(PATHSEP);
+         strcpy((char *)(buf + buflen), (char *)( "/" )) ;
+        buflen += sizeof( ((char_u)'/') );
     }
     vim_tempdir = vim_strnsave(buf, buflen);
     vim_opentempdir();
@@ -57924,22 +56127,23 @@
 }
 
     char_u  *
-vim_tempname(int     extra_char UNUSED, int     keep UNUSED)
+vim_tempname(int     extra_char  __attribute__((unused)) , int     keep  __attribute__((unused)) )
 {
     char_u      itmp[TEMPNAMELEN];
 
-    static char *(tempdirs[]) = {TEMPDIRNAMES};
+    static char *(tempdirs[]) = { "$TMPDIR", "/tmp", ".", "$HOME" };
     int         i;
 
     if (vim_tempdir != NULL && vim_tempdir_gone())
     {
         vim_closetempdir();
-        VIM_CLEAR(vim_tempdir);
+         vim_free(vim_tempdir);
+         (vim_tempdir) = NULL;
     }
 
     if (vim_tempdir == NULL)
     {
-        for (i = 0; i < (int)ARRAY_LENGTH(tempdirs); ++i)
+        for (i = 0; i < (int) (sizeof(tempdirs) / sizeof((tempdirs)[0])) ; ++i)
         {
             size_t      itmplen;
 
@@ -57948,14 +56152,14 @@
             {
                 if (!after_pathsep(itmp, itmp + itmplen))
                 {
-                    STRCPY(itmp + itmplen, PATHSEPSTR);
-                    itmplen += sizeof(PATHSEP);
+                     strcpy((char *)(itmp + itmplen), (char *)( "/" )) ;
+                    itmplen += sizeof( ((char_u)'/') );
                 }
 
                 {
                     mode_t      umask_save = umask(077);
-                    STRCPY(itmp + itmplen, "vXXXXXX");
-                    itmplen += STRLEN_LITERAL("vXXXXXX");
+                     strcpy((char *)(itmp + itmplen), (char *)("vXXXXXX")) ;
+                    itmplen +=  (sizeof("vXXXXXX" "") - 1) ;
                     if (mkdtemp((char *)itmp) != NULL)
                     {
                         vim_settempdir(itmp);
@@ -58015,7 +56219,7 @@
     int
 match_file_list(char_u *list, char_u *sfname, char_u *ffname)
 {
-    char_u      buf[MAXPATHL];
+    char_u      buf[ PATH_MAX ];
     char_u      *tail;
     char_u      *regpat;
     char        allow_dirs;
@@ -58027,7 +56231,7 @@
     p = list;
     while (*p)
     {
-        copy_option_part(&p, buf, MAXPATHL, ",");
+        copy_option_part(&p, buf,  PATH_MAX , ",");
         regpat = file_pat_to_reg_pat(buf, NULL, &allow_dirs, FALSE);
         if (regpat == NULL)
         {
@@ -58044,7 +56248,7 @@
 }
 
     char_u *
-file_pat_to_reg_pat(char_u      *pat, char_u      *pat_end, char        *allow_dirs, int         no_bslash UNUSED)
+file_pat_to_reg_pat(char_u      *pat, char_u      *pat_end, char        *allow_dirs, int         no_bslash  __attribute__((unused)) )
 {
     int         size = 2;
     char_u      *endp;
@@ -58060,7 +56264,7 @@
     }
     if (pat_end == NULL)
     {
-        pat_end = pat + STRLEN(pat);
+        pat_end = pat +  strlen((char *)(pat)) ;
     }
 
     for (p = pat; p < pat_end; p++)
@@ -58218,7 +56422,8 @@
         {
             emsg(_(e_missing_close_curly));
         }
-        VIM_CLEAR(reg_pat);
+         vim_free(reg_pat);
+         (reg_pat) = NULL;
     }
     return reg_pat;
 }
@@ -58230,7 +56435,7 @@
 
     for (;;)
     {
-        ret = vim_read(fd, buf, bufsize);
+        ret =  read((fd), (char *)(buf), (bufsize)) ;
         if (ret >= 0 || errno != EINTR)
         {
             break;
@@ -58247,7 +56452,7 @@
 
     while (ret < (long)bufsize)
     {
-        wlen = vim_write(fd, (char *)buf + ret, bufsize - ret);
+        wlen =  write((fd), (char *)((char *)buf + ret), (bufsize - ret)) ;
         if (wlen < 0)
         {
             if (errno != EINTR)
@@ -58273,7 +56478,7 @@
     char_u *s;
     char_u *p;
     char_u *pbuf;
-    char_u      dirname[MAXPATHL];
+    char_u      dirname[ PATH_MAX ];
     int         c;
     int         has_fullname = 0;
     int         has_homerelative = 0;
@@ -58297,7 +56502,7 @@
             }
         }
 
-        for (p = *fnamep; *p != NUL; MB_PTR_ADV(p))
+        for (p = *fnamep; *p != NUL;  p += (*mb_ptr2len)(p) )
         {
             if (vim_ispathsep(*p) && p[1] == '.' && (p[2] == NUL || vim_ispathsep(p[2]) || (p[2] == '.' && (p[3] == NUL || vim_ispathsep(p[3])))))
             {
@@ -58318,7 +56523,7 @@
 
         if (mch_isdir(*fnamep))
         {
-            *fnamep = vim_strnsave(*fnamep, STRLEN(*fnamep) + 2);
+            *fnamep = vim_strnsave(*fnamep,  strlen((char *)(*fnamep))  + 2);
             vim_free(*bufp);
             *bufp = *fnamep;
             if (*fnamep == NULL)
@@ -58361,23 +56566,23 @@
 
             if (c == '.')
             {
-                mch_dirname(dirname, MAXPATHL);
+                mch_dirname(dirname,  PATH_MAX );
                 if (has_homerelative)
                 {
                     s = vim_strsave(dirname);
                     if (s != NULL)
                     {
-                        dirnamelen = home_replace(NULL, s, dirname, MAXPATHL, TRUE);
+                        dirnamelen = home_replace(NULL, s, dirname,  PATH_MAX , TRUE);
                         vim_free(s);
                     }
                 }
 
                 if (dirnamelen == 0)
                 {
-                    dirnamelen = STRLEN(dirname);
+                    dirnamelen =  strlen((char *)(dirname)) ;
                 }
 
-                if (fnamencmp(p, dirname, dirnamelen) == 0)
+                if ( vim_fnamencmp((char_u *)(p), (char_u *)(dirname), (dirnamelen))  == 0)
                 {
                     p += dirnamelen;
                     if (vim_ispathsep(*p))
@@ -58398,7 +56603,7 @@
             }
             else
             {
-                dirnamelen = home_replace(NULL, p, dirname, MAXPATHL, TRUE);
+                dirnamelen = home_replace(NULL, p, dirname,  PATH_MAX , TRUE);
                 if (*dirname == '~')
                 {
                     s = vim_strnsave(dirname, dirnamelen);
@@ -58416,7 +56621,7 @@
     }
 
     tail = gettail(*fnamep);
-    *fnamelen = STRLEN(*fnamep);
+    *fnamelen =  strlen((char *)(*fnamep)) ;
 
     while (src[*usedlen] == ':' && src[*usedlen + 1] == 'h')
     {
@@ -58425,7 +56630,7 @@
         s = get_past_head(*fnamep);
         while (tail > s && after_pathsep(s, tail))
         {
-            MB_PTR_BACK(*fnamep, tail);
+             tail -= has_mbyte ? ((*mb_head_off)(*fnamep, (tail) - 1) + 1) : 1 ;
         }
         *fnamelen = tail - *fnamep;
         if (*fnamelen == 0)
@@ -58443,7 +56648,7 @@
         {
             while (tail > s && !after_pathsep(s, tail))
             {
-                MB_PTR_BACK(*fnamep, tail);
+                 tail -= has_mbyte ? ((*mb_head_off)(*fnamep, (tail) - 1) + 1) : 1 ;
             }
         }
     }
@@ -58583,7 +56788,7 @@
         }
         vim_free(*bufp);
         *bufp = *fnamep = p;
-        *fnamelen = STRLEN(p);
+        *fnamelen =  strlen((char *)(p)) ;
         *usedlen += 2;
     }
 
@@ -58654,7 +56859,7 @@
 {
     int         fd;
 
-    if (*fname && !mch_isdir(fname) && (fd = mch_open((char *)fname, O_RDONLY | O_NONBLOCK, 0)) >= 0)
+    if (*fname && !mch_isdir(fname) && (fd =  open(((char *)fname), (O_RDONLY | O_NONBLOCK), (0)) ) >= 0)
     {
         close(fd);
         return TRUE;
@@ -58686,10 +56891,10 @@
 
     if (homedir != NULL)
     {
-        dirlen = STRLEN(homedir);
+        dirlen =  strlen((char *)(homedir)) ;
     }
 
-    homedir_env_orig = homedir_env = mch_getenv((char_u *)"HOME");
+    homedir_env_orig = homedir_env =  (char_u *)getenv((char *)((char_u *)"HOME")) ;
     if (homedir_env != NULL && *homedir_env == NUL)
     {
         homedir_env = NULL;
@@ -58701,9 +56906,9 @@
         size_t  flen;
         char_u  *fbuf = NULL;
 
-        flen = STRLEN(homedir_env);
+        flen =  strlen((char *)(homedir_env)) ;
         (void)modify_fname((char_u *)":p", FALSE, &usedlen, &homedir_env, &fbuf, &flen);
-        flen = STRLEN(homedir_env);
+        flen =  strlen((char *)(homedir_env)) ;
         if (flen > 0 && vim_ispathsep(homedir_env[flen - 1]))
         {
             homedir_env[flen - 1] = NUL;
@@ -58712,7 +56917,7 @@
 
     if (homedir_env != NULL)
     {
-        envlen = STRLEN(homedir_env);
+        envlen =  strlen((char *)(homedir_env)) ;
     }
 
     if (!one)
@@ -58726,7 +56931,7 @@
         len = dirlen;
         for (;;)
         {
-            if (   len && fnamencmp(src, p, len) == 0 && (vim_ispathsep(src[len]) || (!one && (src[len] == ',' || src[len] == ' ')) || src[len] == NUL))
+            if (   len &&  vim_fnamencmp((char_u *)(src), (char_u *)(p), (len))  == 0 && (vim_ispathsep(src[len]) || (!one && (src[len] == ',' || src[len] == ' ')) || src[len] == NUL))
             {
                 src += len;
                 if (--dstlen > 0)
@@ -58773,7 +56978,7 @@
     len = 3;
     if (src != NULL)
     {
-        len += (unsigned)STRLEN(src);
+        len += (unsigned) strlen((char *)(src)) ;
     }
     dst = alloc(len);
     if (dst != NULL)
@@ -58786,9 +56991,9 @@
     int
 fullpathcmp(char_u *s1, char_u *s2, int     checkname, int     expandenv)
 {
-    char_u          exp1[MAXPATHL];
-    char_u          full1[MAXPATHL];
-    char_u          full2[MAXPATHL];
+    char_u          exp1[ PATH_MAX ];
+    char_u          full1[ PATH_MAX ];
+    char_u          full2[ PATH_MAX ];
     stat_T st1;
     stat_T st2;
     int r1;
@@ -58796,25 +57001,25 @@
 
     if (expandenv)
     {
-        expand_env(s1, exp1, MAXPATHL);
+        expand_env(s1, exp1,  PATH_MAX );
     }
     else
     {
-        vim_strncpy(exp1, s1, MAXPATHL - 1);
+        vim_strncpy(exp1, s1,  PATH_MAX  - 1);
     }
-    r1 = mch_stat((char *)exp1, &st1);
-    r2 = mch_stat((char *)s2, &st2);
+    r1 =  stat(((char *)exp1), (&st1)) ;
+    r2 =  stat(((char *)s2), (&st2)) ;
     if (r1 != 0 && r2 != 0)
     {
         if (checkname)
         {
-            if (fnamecmp(exp1, s2) == 0)
+            if ( vim_fnamecmp((char_u *)(exp1), (char_u *)(s2))  == 0)
             {
                 return FPC_SAMEX;
             }
-            r1 = vim_FullName(exp1, full1, MAXPATHL, FALSE);
-            r2 = vim_FullName(s2, full2, MAXPATHL, FALSE);
-            if (r1 == OK && r2 == OK && fnamecmp(full1, full2) == 0)
+            r1 = vim_FullName(exp1, full1,  PATH_MAX , FALSE);
+            r2 = vim_FullName(s2, full2,  PATH_MAX , FALSE);
+            if (r1 == OK && r2 == OK &&  vim_fnamecmp((char_u *)(full1), (char_u *)(full2))  == 0)
             {
                 return FPC_SAMEX;
             }
@@ -58848,7 +57053,7 @@
         {
             p1 = p2 + 1;
         }
-        MB_PTR_ADV(p2);
+         p2 += (*mb_ptr2len)(p2) ;
     }
     return p1;
 }
@@ -58873,7 +57078,7 @@
 {
     while (*fname && !vim_ispathsep(*fname))
     {
-        MB_PTR_ADV(fname);
+         fname += (*mb_ptr2len)(fname) ;
     }
     if (*fname)
     {
@@ -58934,9 +57139,9 @@
 {
     if (p_fic)
     {
-        return MB_STRICMP(x, y);
+        return  mb_strnicmp((char_u *)(x), (char_u *)(y), (int)MAXCOL) ;
     }
-    return STRCMP(x, y);
+    return  strcmp((char *)(x), (char *)(y)) ;
 }
 
     int
@@ -58944,29 +57149,29 @@
 {
     if (p_fic)
     {
-        return MB_STRNICMP(x, y, len);
+        return  mb_strnicmp((char_u *)(x), (char_u *)(y), (int)(len)) ;
     }
-    return STRNCMP(x, y, len);
+    return  strncmp((char *)(x), (char *)(y), (len)) ;
 }
 
     char_u  *
 concat_fnames(char_u *fname1, size_t fname1len, char_u *fname2, size_t fname2len, int sep, string_T *ret)
 {
-    ret->string = alloc(fname1len + (sep ? sizeof(PATHSEP) : 0) + fname2len + 1);
+    ret->string = alloc(fname1len + (sep ? sizeof( ((char_u)'/') ) : 0) + fname2len + 1);
     if (ret->string == NULL)
     {
         ret->length = 0;
     }
     else
     {
-        STRCPY(ret->string, fname1);
+         strcpy((char *)(ret->string), (char *)(fname1)) ;
         ret->length = fname1len;
         if (sep && *ret->string != NUL && !after_pathsep(ret->string, ret->string + ret->length))
         {
-            STRCPY(ret->string + ret->length, PATHSEPSTR);
-            ret->length += sizeof(PATHSEP);
+             strcpy((char *)(ret->string + ret->length), (char *)( "/" )) ;
+            ret->length += sizeof( ((char_u)'/') );
         }
-        STRCPY(ret->string + ret->length, fname2);
+         strcpy((char *)(ret->string + ret->length), (char *)(fname2)) ;
         ret->length += fname2len;
     }
 
@@ -58983,10 +57188,10 @@
         return;
     }
 
-    plen = STRLEN(p);
+    plen =  strlen((char *)(p)) ;
     if (!after_pathsep(p, p + plen))
     {
-        STRCPY(p + plen, PATHSEPSTR);
+         strcpy((char *)(p + plen), (char *)( "/" )) ;
     }
 }
 
@@ -59001,13 +57206,13 @@
         return NULL;
     }
 
-    buf = alloc(MAXPATHL);
+    buf = alloc( PATH_MAX );
     if (buf == NULL)
     {
         return NULL;
     }
 
-    if (vim_FullName(fname, buf, MAXPATHL, force) != FAIL)
+    if (vim_FullName(fname, buf,  PATH_MAX , force) != FAIL)
     {
         new_fname = vim_strsave(buf);
     }
@@ -59024,7 +57229,7 @@
 {
     stat_T st;
 
-    if (mch_stat((char *)fname, &st))
+    if ( stat(((char *)fname), (&st)) )
     {
         return FALSE;
     }
@@ -59049,7 +57254,7 @@
         --emsg_off;
         if (eval_pat != NULL)
         {
-            star_follows = STRCMP(exp_pat + usedlen, "*") == 0;
+            star_follows =  strcmp((char *)(exp_pat + usedlen), (char *)("*"))  == 0;
             exp_pat = concat_str(eval_pat, exp_pat + usedlen);
         }
     }
@@ -59063,7 +57268,7 @@
     {
         if (*num_file == 0 && is_cur_alt_file && star_follows)
         {
-            *file = ALLOC_ONE(char_u *);
+            *file =  (char_u * *)alloc(sizeof(char_u *)) ;
             if (*file != NULL)
             {
                 **file = eval_pat;
@@ -59121,7 +57326,8 @@
 
         if (*num_files == 0)
         {
-            VIM_CLEAR(*files);
+             vim_free(*files);
+             (*files) = NULL;
             return FAIL;
         }
     }
@@ -59152,10 +57358,9 @@
     int fnamelen;
     int setsuflen;
     char_u      *setsuf;
-#define MAXSUFLEN 30
     char_u      suf_buf[MAXSUFLEN];
 
-    fnamelen = (int)STRLEN(fname);
+    fnamelen = (int) strlen((char *)(fname)) ;
     setsuflen = 0;
     for (setsuf = p_su; *setsuf; )
     {
@@ -59172,7 +57377,7 @@
         }
         else
         {
-            if (fnamelen >= setsuflen && fnamencmp(suf_buf, fname + fnamelen - setsuflen, (size_t)setsuflen) == 0)
+            if (fnamelen >= setsuflen &&  vim_fnamencmp((char_u *)(suf_buf), (char_u *)(fname + fnamelen - setsuflen), ((size_t)setsuflen))  == 0)
             {
                 break;
             }
@@ -59185,7 +57390,7 @@
     static int
 vim_backtick(char_u *p)
 {
-    return (*p == '`' && *(p + 1) != NUL && *(p + STRLEN(p) - 1) == '`');
+    return (*p == '`' && *(p + 1) != NUL && *(p +  strlen((char *)(p))  - 1) == '`');
 }
 
     static int
@@ -59197,7 +57402,7 @@
     int         cnt = 0;
     int         i;
 
-    cmd = vim_strnsave(pat + 1, STRLEN(pat) - 2);
+    cmd = vim_strnsave(pat + 1,  strlen((char *)(pat))  - 2);
     if (cmd == NULL)
     {
         return -1;
@@ -59274,7 +57479,7 @@
         }
     }
 
-    buflen = STRLEN(path) + MAXPATHL;
+    buflen =  strlen((char *)(path))  +  PATH_MAX ;
     buf = alloc(buflen);
     if (buf == NULL)
     {
@@ -59299,7 +57504,7 @@
             }
             s = p + 1;
         }
-        else if (path_end >= path + wildoff && (vim_strchr((char_u *)"*?[{~$", *path_end) != NULL || (!p_fic && (flags & EW_ICASE) && vim_isalpha(PTR2CHAR(path_end)))))
+        else if (path_end >= path + wildoff && (vim_strchr((char_u *)"*?[{~$", *path_end) != NULL || (!p_fic && (flags & EW_ICASE) && vim_isalpha( (has_mbyte ? mb_ptr2char(path_end) : (int)*(path_end)) ))))
         {
             e = p;
         }
@@ -59307,7 +57512,7 @@
         {
             int charlen = (*mb_ptr2len)(path_end);
 
-            STRNCPY(p, path_end, (size_t)charlen);
+             strncpy((char *)(p), (char *)(path_end), ((size_t)charlen)) ;
             p += charlen;
             path_end += charlen;
         }
@@ -59323,7 +57528,7 @@
     {
         if (rem_backslash(p))
         {
-            STRMOVE(p, p + 1);
+              memmove((char *)((p)), (char *)((p + 1)),  strlen((char *)(p + 1))  + 1)  ;
             --e;
             --s;
         }
@@ -59392,7 +57597,7 @@
                 break;
             }
             len = (size_t)(s - buf);
-            if ((dp->d_name[0] != '.' || starts_with_dot || ((flags & EW_DODOT) && dp->d_name[1] != NUL && (dp->d_name[1] != '.' || dp->d_name[2] != NUL))) && ((regmatch.regprog != NULL && vim_regexec(&regmatch, (char_u *)dp->d_name, (colnr_T)0)) || ((flags & EW_NOTWILD) && fnamencmp(path + len, dp->d_name, e - s) == 0)))
+            if ((dp->d_name[0] != '.' || starts_with_dot || ((flags & EW_DODOT) && dp->d_name[1] != NUL && (dp->d_name[1] != '.' || dp->d_name[2] != NUL))) && ((regmatch.regprog != NULL && vim_regexec(&regmatch, (char_u *)dp->d_name, (colnr_T)0)) || ((flags & EW_NOTWILD) &&  vim_fnamencmp((char_u *)(path + len), (char_u *)(dp->d_name), (e - s))  == 0)))
             {
                 len += vim_snprintf((char *)s, buflen - len, "%s", dp->d_name);
                 if (len + 1 >= buflen)
@@ -59426,7 +57631,7 @@
                     {
                         backslash_halve(buf + len + 1);
                     }
-                    if ((flags & EW_ALLLINKS) ? mch_lstat((char *)buf, &sb) >= 0 : mch_getperm(buf) >= 0)
+                    if ((flags & EW_ALLLINKS) ?  lstat(((char *)buf), (&sb))  >= 0 : mch_getperm(buf) >= 0)
                     {
                         addfile(gap, buf, flags);
                     }
@@ -59451,7 +57656,7 @@
     static int
 has_env_var(char_u *p)
 {
-    for ( ; *p; MB_PTR_ADV(p))
+    for ( ; *p;  p += (*mb_ptr2len)(p) )
     {
         if (*p == '\\' && p[1] != NUL)
         {
@@ -59468,7 +57673,7 @@
     static int
 has_special_wildchar(char_u *p)
 {
-    for ( ; *p; MB_PTR_ADV(p))
+    for ( ; *p;  p += (*mb_ptr2len)(p) )
     {
         if (*p == '\r' || *p == '\n')
         {
@@ -59478,7 +57683,7 @@
         {
             ++p;
         }
-        else if (vim_strchr((char_u *)SPECIAL_WILDCHAR, *p) != NULL)
+        else if (vim_strchr((char_u *) "`'{" , *p) != NULL)
         {
             if (*p == '{' && vim_strchr(p, '}') == NULL)
             {
@@ -59542,7 +57747,7 @@
         {
             if ((has_env_var(p) && !(flags & EW_NOTENV)) || *p == '~')
             {
-                p = expand_env_save_opt(p, TRUE, (char_u *)PATH_ESC_WILDCARDS);
+                p = expand_env_save_opt(p, TRUE, (char_u *) "*?[{" );
                 if (p == NULL)
                 {
                     p = pat[i];
@@ -59623,7 +57828,7 @@
     int         isdir;
     stat_T      sb;
 
-    if (!(flags & EW_NOTFOUND) && ((flags & EW_ALLLINKS) ? mch_lstat((char *)f, &sb) < 0 : mch_getperm(f) < 0))
+    if (!(flags & EW_NOTFOUND) && ((flags & EW_ALLLINKS) ?  lstat(((char *)f), (&sb))  < 0 : mch_getperm(f) < 0))
     {
         return;
     }
@@ -59644,13 +57849,13 @@
         return;
     }
 
-    p = alloc(STRLEN(f) + 1 + isdir);
+    p = alloc( strlen((char *)(f))  + 1 + isdir);
     if (p == NULL)
     {
         return;
     }
 
-    STRCPY(p, f);
+     strcpy((char *)(p), (char *)(f)) ;
     if (isdir && (flags & EW_ADDSLASH))
     {
         add_pathsep(p);
@@ -59684,8 +57889,8 @@
     i = 0;
     for (j = 0; maxlen < 0 || (i < maxlen && j < maxlen);)
     {
-        c1 = PTR2CHAR((char_u *)p + i);
-        c2 = PTR2CHAR((char_u *)q + j);
+        c1 =  (has_mbyte ? mb_ptr2char((char_u *)p + i) : (int)*((char_u *)p + i)) ;
+        c2 =  (has_mbyte ? mb_ptr2char((char_u *)q + j) : (int)*((char_u *)q + j)) ;
 
         if (c1 == NUL)
         {
@@ -59704,7 +57909,7 @@
             break;
         }
 
-        if ((p_fic ? MB_TOUPPER(c1) != MB_TOUPPER(c2) : c1 != c2))
+        if ((p_fic ?  vim_toupper(c1)  !=  vim_toupper(c2)  : c1 != c2))
         {
             if (vim_ispathsep(c1))
             {
@@ -59714,7 +57919,7 @@
             {
                 return 1;
             }
-            return p_fic ? MB_TOUPPER(c1) - MB_TOUPPER(c2)
+            return p_fic ?  vim_toupper(c1)  -  vim_toupper(c2) 
                     : c1 - c2;
         }
 
@@ -59726,8 +57931,8 @@
         return 0;
     }
 
-    c1 = PTR2CHAR((char_u *)s + i);
-    c2 = PTR2CHAR((char_u *)s + i + mb_ptr2len((char_u *)s + i));
+    c1 =  (has_mbyte ? mb_ptr2char((char_u *)s + i) : (int)*((char_u *)s + i)) ;
+    c2 =  (has_mbyte ? mb_ptr2char((char_u *)s + i + mb_ptr2len((char_u *)s + i)) : (int)*((char_u *)s + i + mb_ptr2len((char_u *)s + i))) ;
     if (c2 == NUL && i > 0 && !after_pathsep((char_u *)s, (char_u *)s + i) && c1 == '/')
     {
         return 0;
@@ -59811,8 +58016,6 @@
 
 } ff_visited_list_hdr_T;
 
-#define FF_MAX_STAR_STAR_EXPAND ((char_u)30)
-
 typedef struct ff_search_ctx_T
 {
     ff_stack_T                  *ffsc_stack_ptr;
@@ -59846,7 +58049,7 @@
 static string_T ff_expand_buffer = {NULL, 0};
 
     void *
-vim_findfile_init(char_u      *path, char_u      *filename, size_t      filenamelen, char_u      *stopdirs UNUSED, int         level, int         free_visited, int         find_what, void        *search_ctx_arg, int         tagfile, char_u      *rel_fname)
+vim_findfile_init(char_u      *path, char_u      *filename, size_t      filenamelen, char_u      *stopdirs  __attribute__((unused)) , int         level, int         free_visited, int         find_what, void        *search_ctx_arg, int         tagfile, char_u      *rel_fname)
 {
     char_u              *wc_part;
     ff_stack_T          *sptr;
@@ -59859,7 +58062,7 @@
     }
     else
     {
-        search_ctx = ALLOC_CLEAR_ONE(ff_search_ctx_T);
+        search_ctx =  (ff_search_ctx_T *)alloc_clear(sizeof(ff_search_ctx_T)) ;
         if (search_ctx == NULL)
         {
             goto error_return;
@@ -59891,7 +58094,7 @@
     if (ff_expand_buffer.string == NULL)
     {
         ff_expand_buffer.length = 0;
-        ff_expand_buffer.string = alloc(MAXPATHL);
+        ff_expand_buffer.string = alloc( PATH_MAX );
         if (ff_expand_buffer.string == NULL)
         {
             goto error_return;
@@ -59902,7 +58105,7 @@
     {
         int     len = (int)(gettail(rel_fname) - rel_fname);
 
-        if (!vim_isAbsName(rel_fname) && len + 1 < MAXPATHL)
+        if (!vim_isAbsName(rel_fname) && len + 1 <  PATH_MAX )
         {
             vim_strncpy(ff_expand_buffer.string, rel_fname, len);
             ff_expand_buffer.length = len;
@@ -59912,7 +58115,7 @@
             {
                 goto error_return;
             }
-            search_ctx->ffsc_start_dir.length = STRLEN(search_ctx->ffsc_start_dir.string);
+            search_ctx->ffsc_start_dir.length =  strlen((char *)(search_ctx->ffsc_start_dir.string)) ;
         }
         else
         {
@@ -59931,12 +58134,12 @@
     }
     else if (*path == NUL || !vim_isAbsName(path))
     {
-        if (mch_dirname(ff_expand_buffer.string, MAXPATHL) == FAIL)
+        if (mch_dirname(ff_expand_buffer.string,  PATH_MAX ) == FAIL)
         {
             goto error_return;
         }
 
-        ff_expand_buffer.length = STRLEN(ff_expand_buffer.string);
+        ff_expand_buffer.length =  strlen((char *)(ff_expand_buffer.string)) ;
 
         search_ctx->ffsc_start_dir.length = ff_expand_buffer.length;
         search_ctx->ffsc_start_dir.string = vim_strnsave(ff_expand_buffer.string, search_ctx->ffsc_start_dir.length);
@@ -59957,7 +58160,7 @@
         }
 
         dircount = 1;
-        search_ctx->ffsc_stopdirs_v = ALLOC_ONE(string_T);
+        search_ctx->ffsc_stopdirs_v =  (string_T *)alloc(sizeof(string_T)) ;
 
         if (search_ctx->ffsc_stopdirs_v != NULL)
         {
@@ -59970,7 +58173,7 @@
                 size_t  len;
 
                 helper = walker;
-                ptr = vim_realloc(search_ctx->ffsc_stopdirs_v, (dircount + 1) * sizeof(string_T));
+                ptr =  realloc((search_ctx->ffsc_stopdirs_v), ((dircount + 1) * sizeof(string_T))) ;
                 if (ptr)
                 {
                     search_ctx->ffsc_stopdirs_v = ptr;
@@ -59980,9 +58183,9 @@
                     break;
                 }
                 walker = vim_strchr(walker, ';');
-                len = walker ? (size_t)(walker - helper) : STRLEN(helper);
+                len = walker ? (size_t)(walker - helper) :  strlen((char *)(helper)) ;
 
-                if (*helper != NUL && !vim_isAbsName(helper) && len + 1 < MAXPATHL)
+                if (*helper != NUL && !vim_isAbsName(helper) && len + 1 <  PATH_MAX )
                 {
                     vim_strncpy(ff_expand_buffer.string, helper, len);
                     ff_expand_buffer.length = len;
@@ -59991,7 +58194,7 @@
                     tmp->string = FullName_save(ff_expand_buffer.string, FALSE);
                     if (tmp->string != NULL)
                     {
-                        tmp->length = STRLEN(tmp->string);
+                        tmp->length =  strlen((char *)(tmp->string)) ;
                     }
                 }
                 else
@@ -60036,12 +58239,12 @@
         ff_expand_buffer.length = 0;
         while (*wc_part != NUL)
         {
-            if (ff_expand_buffer.length + 5 >= MAXPATHL)
+            if (ff_expand_buffer.length + 5 >=  PATH_MAX )
             {
                 emsg(_(e_path_too_long_for_completion));
                 break;
             }
-            if (STRNCMP(wc_part, "**", 2) == 0)
+            if ( strncmp((char *)(wc_part), (char *)("**"), (2))  == 0)
             {
                 ff_expand_buffer.string[ff_expand_buffer.length++] = *wc_part++;
                 ff_expand_buffer.string[ff_expand_buffer.length++] = *wc_part++;
@@ -60057,12 +58260,12 @@
                 }
                 else
                 {
-                    ff_expand_buffer.string[ff_expand_buffer.length++] = FF_MAX_STAR_STAR_EXPAND;
+                    ff_expand_buffer.string[ff_expand_buffer.length++] =  ((char_u)30) ;
                 }
                 wc_part = (char_u *)errpt;
                 if (*wc_part != NUL && !vim_ispathsep(*wc_part))
                 {
-                    semsg(_(e_invalid_path_number_must_be_at_end_of_path_or_be_followed_by_str), PATHSEPSTR);
+                    semsg(_(e_invalid_path_number_must_be_at_end_of_path_or_be_followed_by_str),  "/" );
                     goto error_return;
                 }
             }
@@ -60082,7 +58285,7 @@
     }
     else
     {
-        search_ctx->ffsc_fix_path.length = STRLEN(path);
+        search_ctx->ffsc_fix_path.length =  strlen((char *)(path)) ;
         search_ctx->ffsc_fix_path.string = vim_strnsave(path, search_ctx->ffsc_fix_path.length);
         if (search_ctx->ffsc_fix_path.string == NULL)
         {
@@ -60102,14 +58305,14 @@
         search_ctx->ffsc_fix_path.length = 0;
     }
 
-    if (search_ctx->ffsc_start_dir.length + search_ctx->ffsc_fix_path.length + 3 >= MAXPATHL)
+    if (search_ctx->ffsc_start_dir.length + search_ctx->ffsc_fix_path.length + 3 >=  PATH_MAX )
     {
         emsg(_(e_path_too_long_for_completion));
         goto error_return;
     }
 
     add_sep = !after_pathsep(search_ctx->ffsc_start_dir.string, search_ctx->ffsc_start_dir.string + search_ctx->ffsc_start_dir.length);
-    ff_expand_buffer.length = vim_snprintf((char *)ff_expand_buffer.string, MAXPATHL, "%s%s", search_ctx->ffsc_start_dir.string, add_sep ? PATHSEPSTR : "");
+    ff_expand_buffer.length = vim_snprintf((char *)ff_expand_buffer.string,  PATH_MAX , "%s%s", search_ctx->ffsc_start_dir.string, add_sep ?  "/"  : "");
 
     {
         size_t  bufsize = ff_expand_buffer.length + search_ctx->ffsc_fix_path.length + 1;
@@ -60126,7 +58329,7 @@
             if (search_ctx->ffsc_fix_path.length > 0)
             {
                 add_sep = !after_pathsep(search_ctx->ffsc_fix_path.string, search_ctx->ffsc_fix_path.string + search_ctx->ffsc_fix_path.length);
-                ff_expand_buffer.length += vim_snprintf((char *)ff_expand_buffer.string + ff_expand_buffer.length, MAXPATHL - ff_expand_buffer.length, "%s%s", search_ctx->ffsc_fix_path.string, add_sep ? PATHSEPSTR : "");
+                ff_expand_buffer.length += vim_snprintf((char *)ff_expand_buffer.string + ff_expand_buffer.length,  PATH_MAX  - ff_expand_buffer.length, "%s%s", search_ctx->ffsc_fix_path.string, add_sep ?  "/"  : "");
             }
         }
         else
@@ -60137,14 +58340,14 @@
             if (p > search_ctx->ffsc_fix_path.string)
             {
                 len = (int)(p - search_ctx->ffsc_fix_path.string) - 1;
-                if ((len >= 2 && STRNCMP(search_ctx->ffsc_fix_path.string, "..", 2) == 0) && (len == 2 || search_ctx->ffsc_fix_path.string[2] == PATHSEP))
+                if ((len >= 2 &&  strncmp((char *)(search_ctx->ffsc_fix_path.string), (char *)(".."), (2))  == 0) && (len == 2 || search_ctx->ffsc_fix_path.string[2] ==  ((char_u)'/') ))
                 {
                     vim_free(buf);
                     goto error_return;
                 }
 
                 add_sep = !after_pathsep(search_ctx->ffsc_fix_path.string, search_ctx->ffsc_fix_path.string + search_ctx->ffsc_fix_path.length);
-                ff_expand_buffer.length += vim_snprintf((char *)ff_expand_buffer.string + ff_expand_buffer.length, MAXPATHL - ff_expand_buffer.length, "%.*s%s", len, search_ctx->ffsc_fix_path.string, add_sep ? PATHSEPSTR : "");
+                ff_expand_buffer.length += vim_snprintf((char *)ff_expand_buffer.string + ff_expand_buffer.length,  PATH_MAX  - ff_expand_buffer.length, "%.*s%s", len, search_ctx->ffsc_fix_path.string, add_sep ?  "/"  : "");
             }
 
             if (search_ctx->ffsc_wc_path.string != NULL)
@@ -60204,9 +58407,9 @@
         {
             if (r_ptr_end == NULL)
             {
-                r_ptr_end = r_ptr + STRLEN(r_ptr);
+                r_ptr_end = r_ptr +  strlen((char *)(r_ptr)) ;
             }
-            mch_memmove(r_ptr, r_ptr + 1, (size_t)(r_ptr_end - (r_ptr + 1)) + 1);
+             memmove((char *)(r_ptr), (char *)(r_ptr + 1), (size_t)(r_ptr_end - (r_ptr + 1)) + 1) ;
             r_ptr++;
             --r_ptr_end;
         }
@@ -60253,7 +58456,7 @@
 
     search_ctx = (ff_search_ctx_T *)search_ctx_arg;
 
-    if ((file_path.string = alloc(MAXPATHL)) == NULL)
+    if ((file_path.string = alloc( PATH_MAX )) == NULL)
     {
         return NULL;
     }
@@ -60303,10 +58506,10 @@
 
                 if (!vim_isAbsName(stackp->ffs_fix_path.string) && search_ctx->ffsc_start_dir.string)
                 {
-                    if (search_ctx->ffsc_start_dir.length + 1 < MAXPATHL)
+                    if (search_ctx->ffsc_start_dir.length + 1 <  PATH_MAX )
                     {
                         int add_sep = !after_pathsep(search_ctx->ffsc_start_dir.string, search_ctx->ffsc_start_dir.string + search_ctx->ffsc_start_dir.length);
-                        file_path.length = vim_snprintf((char *)file_path.string, MAXPATHL, "%s%s", search_ctx->ffsc_start_dir.string, add_sep ? PATHSEPSTR : "");
+                        file_path.length = vim_snprintf((char *)file_path.string,  PATH_MAX , "%s%s", search_ctx->ffsc_start_dir.string, add_sep ?  "/"  : "");
                     }
                     else
                     {
@@ -60315,10 +58518,10 @@
                     }
                 }
 
-                if (file_path.length + stackp->ffs_fix_path.length + 1 < MAXPATHL)
+                if (file_path.length + stackp->ffs_fix_path.length + 1 <  PATH_MAX )
                 {
                     int add_sep = !after_pathsep(stackp->ffs_fix_path.string, stackp->ffs_fix_path.string + stackp->ffs_fix_path.length);
-                    file_path.length += vim_snprintf((char *)file_path.string + file_path.length, MAXPATHL - file_path.length, "%s%s", stackp->ffs_fix_path.string, add_sep ? PATHSEPSTR : "");
+                    file_path.length += vim_snprintf((char *)file_path.string + file_path.length,  PATH_MAX  - file_path.length, "%s%s", stackp->ffs_fix_path.string, add_sep ?  "/"  : "");
                 }
                 else
                 {
@@ -60330,7 +58533,7 @@
                 rest_of_wildcards.length = stackp->ffs_wc_path.length;
                 if (*rest_of_wildcards.string != NUL)
                 {
-                    if (STRNCMP(rest_of_wildcards.string, "**", 2) == 0)
+                    if ( strncmp((char *)(rest_of_wildcards.string), (char *)("**"), (2))  == 0)
                     {
                         char_u  *p;
 
@@ -60339,7 +58542,7 @@
                         if (*p > 0)
                         {
                             (*p)--;
-                            if (file_path.length + 1 < MAXPATHL)
+                            if (file_path.length + 1 <  PATH_MAX )
                             {
                                 file_path.string[file_path.length++] = '*';
                             }
@@ -60352,7 +58555,7 @@
 
                         if (*p == 0)
                         {
-                            mch_memmove(rest_of_wildcards.string, rest_of_wildcards.string + 3, (size_t)(rest_of_wildcards.length - 3) + 1);
+                             memmove((char *)(rest_of_wildcards.string), (char *)(rest_of_wildcards.string + 3), (size_t)(rest_of_wildcards.length - 3) + 1) ;
                             rest_of_wildcards.length -= 3;
                             stackp->ffs_wc_path.length = rest_of_wildcards.length;
                         }
@@ -60371,7 +58574,7 @@
 
                     while (*rest_of_wildcards.string && !vim_ispathsep(*rest_of_wildcards.string))
                     {
-                        if (file_path.length + 1 < MAXPATHL)
+                        if (file_path.length + 1 <  PATH_MAX )
                         {
                             file_path.string[file_path.length++] = *rest_of_wildcards.string++;
                             --rest_of_wildcards.length;
@@ -60393,7 +58596,7 @@
 
                 if (path_with_url(dirptrs[0]))
                 {
-                    stackp->ffs_filearray = ALLOC_ONE(char_u *);
+                    stackp->ffs_filearray =  (char_u * *)alloc(sizeof(char_u *)) ;
                     if (stackp->ffs_filearray != NULL && (stackp->ffs_filearray[0] = vim_strnsave(dirptrs[0], file_path.length)) != NULL)
                     {
                         stackp->ffs_filearray_size = 1;
@@ -60433,11 +58636,11 @@
                             continue;
                         }
 
-                        len = STRLEN(stackp->ffs_filearray[i]);
-                        if (len + 1 + search_ctx->ffsc_file_to_search.length < MAXPATHL)
+                        len =  strlen((char *)(stackp->ffs_filearray[i])) ;
+                        if (len + 1 + search_ctx->ffsc_file_to_search.length <  PATH_MAX )
                         {
                             int add_sep = !after_pathsep(stackp->ffs_filearray[i], stackp->ffs_filearray[i] + len);
-                            file_path.length = vim_snprintf((char *)file_path.string, MAXPATHL, "%s%s%s", stackp->ffs_filearray[i], add_sep ? PATHSEPSTR : "", search_ctx->ffsc_file_to_search.string);
+                            file_path.length = vim_snprintf((char *)file_path.string,  PATH_MAX , "%s%s%s", stackp->ffs_filearray[i], add_sep ?  "/"  : "", search_ctx->ffsc_file_to_search.string);
                         }
                         else
                         {
@@ -60458,7 +58661,6 @@
                         {
                             if ((path_with_url(file_path.string) || (mch_getperm(file_path.string) >= 0 && (search_ctx->ffsc_find_what == FINDFILE_BOTH || ((search_ctx->ffsc_find_what == FINDFILE_DIR) == mch_isdir(file_path.string))))) && (ff_check_visited(&search_ctx->ffsc_visited_list ->ffvl_visited_list, file_path.string, file_path.length, (char_u *)"", 0) == OK))
                             {
-
                                 stackp->ffs_filearray_cur = i + 1;
                                 ff_push(search_ctx, stackp);
 
@@ -60467,15 +58669,15 @@
                                     file_path.length = simplify_filename(file_path.string);
                                 }
 
-                                if (mch_dirname(ff_expand_buffer.string, MAXPATHL) == OK)
+                                if (mch_dirname(ff_expand_buffer.string,  PATH_MAX ) == OK)
                                 {
                                     char_u  *p;
 
-                                    ff_expand_buffer.length = STRLEN(ff_expand_buffer.string);
+                                    ff_expand_buffer.length =  strlen((char *)(ff_expand_buffer.string)) ;
                                     p = shorten_fname(file_path.string, ff_expand_buffer.string);
                                     if (p != NULL)
                                     {
-                                        mch_memmove(file_path.string, p, (size_t)((file_path.string + file_path.length) - p) + 1);
+                                         memmove((char *)(file_path.string), (char *)(p), (size_t)((file_path.string + file_path.length) - p) + 1) ;
                                         file_path.length -= (p - file_path.string);
                                     }
                                 }
@@ -60486,7 +58688,7 @@
                             {
                                 break;
                             }
-                            file_path.length = len + copy_option_part(&suf, file_path.string + len, (int)(MAXPATHL - len), ",");
+                            file_path.length = len + copy_option_part(&suf, file_path.string + len, (int)( PATH_MAX  - len), ",");
                         }
                     }
                 }
@@ -60499,20 +58701,20 @@
                             continue;
                         }
 
-                        ff_push(search_ctx, ff_create_stack_element(stackp->ffs_filearray[i], STRLEN(stackp->ffs_filearray[i]), rest_of_wildcards.string, rest_of_wildcards.length, stackp->ffs_level - 1, 0));
+                        ff_push(search_ctx, ff_create_stack_element(stackp->ffs_filearray[i],  strlen((char *)(stackp->ffs_filearray[i])) , rest_of_wildcards.string, rest_of_wildcards.length, stackp->ffs_level - 1, 0));
                     }
                 }
                 stackp->ffs_filearray_cur = 0;
                 stackp->ffs_stage = 1;
             }
 
-            if (STRNCMP(stackp->ffs_wc_path.string, "**", 2) == 0)
+            if ( strncmp((char *)(stackp->ffs_wc_path.string), (char *)("**"), (2))  == 0)
             {
                 int i;
 
                 for (i = stackp->ffs_filearray_cur; i < stackp->ffs_filearray_size; ++i)
                 {
-                    if (fnamecmp(stackp->ffs_filearray[i], stackp->ffs_fix_path.string) == 0)
+                    if ( vim_fnamecmp((char_u *)(stackp->ffs_filearray[i]), (char_u *)(stackp->ffs_fix_path.string))  == 0)
                     {
                         continue;
                     }
@@ -60520,7 +58722,7 @@
                     {
                         continue;
                     }
-                    ff_push(search_ctx, ff_create_stack_element(stackp->ffs_filearray[i], STRLEN(stackp->ffs_filearray[i]), stackp->ffs_wc_path.string, stackp->ffs_wc_path.length, stackp->ffs_level - 1, 1));
+                    ff_push(search_ctx, ff_create_stack_element(stackp->ffs_filearray[i],  strlen((char *)(stackp->ffs_filearray[i])) , stackp->ffs_wc_path.string, stackp->ffs_wc_path.length, stackp->ffs_level - 1, 1));
                 }
             }
 
@@ -60556,10 +58758,10 @@
                 break;
             }
 
-            if (search_ctx->ffsc_start_dir.length + 1 + search_ctx->ffsc_fix_path.length < MAXPATHL)
+            if (search_ctx->ffsc_start_dir.length + 1 + search_ctx->ffsc_fix_path.length <  PATH_MAX )
             {
                 int add_sep = !after_pathsep(search_ctx->ffsc_start_dir.string, search_ctx->ffsc_start_dir.string + search_ctx->ffsc_start_dir.length);
-                file_path.length = vim_snprintf((char *)file_path.string, MAXPATHL, "%s%s%s", search_ctx->ffsc_start_dir.string, add_sep ? PATHSEPSTR : "", search_ctx->ffsc_fix_path.string);
+                file_path.length = vim_snprintf((char *)file_path.string,  PATH_MAX , "%s%s%s", search_ctx->ffsc_start_dir.string, add_sep ?  "/"  : "", search_ctx->ffsc_fix_path.string);
             }
             else
             {
@@ -60641,7 +58843,7 @@
         retptr = *list_headp;
         while (retptr != NULL)
         {
-            if (fnamecmp(filename, retptr->ffvl_filename) == 0)
+            if ( vim_fnamecmp((char_u *)(filename), (char_u *)(retptr->ffvl_filename))  == 0)
             {
                 return retptr;
             }
@@ -60649,7 +58851,7 @@
         }
     }
 
-    retptr = ALLOC_ONE(ff_visited_list_hdr_T);
+    retptr =  (ff_visited_list_hdr_T *)alloc(sizeof(ff_visited_list_hdr_T)) ;
     if (retptr == NULL)
     {
         return NULL;
@@ -60691,10 +58893,10 @@
     i = 0;
     for (j = 0; s1[i] != NUL && s2[j] != NUL;)
     {
-        c1 = PTR2CHAR(s1 + i);
-        c2 = PTR2CHAR(s2 + j);
+        c1 =  (has_mbyte ? mb_ptr2char(s1 + i) : (int)*(s1 + i)) ;
+        c2 =  (has_mbyte ? mb_ptr2char(s2 + j) : (int)*(s2 + j)) ;
 
-        if ((p_fic ? MB_TOLOWER(c1) != MB_TOLOWER(c2) : c1 != c2) && (prev1 != '*' || prev2 != '*'))
+        if ((p_fic ?  vim_tolower(c1)  !=  vim_tolower(c2)  : c1 != c2) && (prev1 != '*' || prev2 != '*'))
         {
             return FALSE;
         }
@@ -60724,7 +58926,7 @@
     {
         ff_expand_buffer.string[0] = NUL;
         ff_expand_buffer.length = 0;
-        if (mch_stat((char *)fname, &st) < 0)
+        if ( stat(((char *)fname), (&st))  < 0)
         {
             return FAIL;
         }
@@ -60732,7 +58934,7 @@
 
     for (vp = *visited_list; vp != NULL; vp = vp->ffv_next)
     {
-        if (!url ? (vp->ffv_dev_valid && vp->ffv_dev == st.st_dev && vp->ffv_ino == st.st_ino) : fnamecmp(vp->ffv_fname, ff_expand_buffer.string) == 0)
+        if (!url ? (vp->ffv_dev_valid && vp->ffv_dev == st.st_dev && vp->ffv_ino == st.st_ino) :  vim_fnamecmp((char_u *)(vp->ffv_fname), (char_u *)(ff_expand_buffer.string))  == 0)
         {
             if (ff_wc_equal(vp->ffv_wc_path, wc_path) == TRUE)
             {
@@ -60757,7 +58959,7 @@
     else
     {
         vp->ffv_dev_valid = FALSE;
-        STRCPY(vp->ffv_fname, ff_expand_buffer.string);
+         strcpy((char *)(vp->ffv_fname), (char *)(ff_expand_buffer.string)) ;
     }
     if (wc_path != NULL)
     {
@@ -60779,7 +58981,7 @@
 {
     ff_stack_T  *new;
 
-    new = ALLOC_ONE(ff_stack_T);
+    new =  (ff_stack_T *)alloc(sizeof(ff_stack_T)) ;
     if (new == NULL)
     {
         return NULL;
@@ -60847,8 +59049,12 @@
     static void
 ff_free_stack_element(ff_stack_T *stack_ptr)
 {
-    VIM_CLEAR_STRING(stack_ptr->ffs_fix_path);
-    VIM_CLEAR_STRING(stack_ptr->ffs_wc_path);
+     vim_free(stack_ptr->ffs_fix_path.string);
+     (stack_ptr->ffs_fix_path.string) = NULL;
+     stack_ptr->ffs_fix_path.length = 0;
+     vim_free(stack_ptr->ffs_wc_path.string);
+     (stack_ptr->ffs_wc_path.string) = NULL;
+     stack_ptr->ffs_wc_path.length = 0;
 
     if (stack_ptr->ffs_filearray != NULL)
     {
@@ -60877,13 +59083,22 @@
             vim_free(search_ctx->ffsc_stopdirs_v[i].string);
             i++;
         }
-        VIM_CLEAR(search_ctx->ffsc_stopdirs_v);
+         vim_free(search_ctx->ffsc_stopdirs_v);
+         (search_ctx->ffsc_stopdirs_v) = NULL;
     }
 
-    VIM_CLEAR_STRING(search_ctx->ffsc_file_to_search);
-    VIM_CLEAR_STRING(search_ctx->ffsc_start_dir);
-    VIM_CLEAR_STRING(search_ctx->ffsc_fix_path);
-    VIM_CLEAR_STRING(search_ctx->ffsc_wc_path);
+     vim_free(search_ctx->ffsc_file_to_search.string);
+     (search_ctx->ffsc_file_to_search.string) = NULL;
+     search_ctx->ffsc_file_to_search.length = 0;
+     vim_free(search_ctx->ffsc_start_dir.string);
+     (search_ctx->ffsc_start_dir.string) = NULL;
+     search_ctx->ffsc_start_dir.length = 0;
+     vim_free(search_ctx->ffsc_fix_path.string);
+     (search_ctx->ffsc_fix_path.string) = NULL;
+     search_ctx->ffsc_fix_path.length = 0;
+     vim_free(search_ctx->ffsc_wc_path.string);
+     (search_ctx->ffsc_wc_path.string) = NULL;
+     search_ctx->ffsc_wc_path.length = 0;
     search_ctx->ffsc_level = 0;
 }
 
@@ -60904,7 +59119,7 @@
 
     for (i = 0; stopdirs_v[i].string != NULL; i++)
     {
-        if (fnamencmp(stopdirs_v[i].string, path, path_len) == 0 && ((int)stopdirs_v[i].length <= path_len || vim_ispathsep(stopdirs_v[i].string[path_len])))
+        if ( vim_fnamencmp((char_u *)(stopdirs_v[i].string), (char_u *)(path), (path_len))  == 0 && ((int)stopdirs_v[i].length <= path_len || vim_ispathsep(stopdirs_v[i].string[path_len])))
         {
             return TRUE;
         }
@@ -60946,7 +59161,7 @@
 
         save_char = ptr[len];
         ptr[len] = NUL;
-        file_to_findlen = expand_env_esc(ptr, NameBuff, MAXPATHL, NULL, TRUE, NULL);
+        file_to_findlen = expand_env_esc(ptr, NameBuff,  PATH_MAX , NULL, TRUE, NULL);
         ptr[len] = save_char;
 
         vim_free(*file_to_find);
@@ -60963,7 +59178,7 @@
             {
                 if (ptr[0] == '\\' && ptr[1] == ' ')
                 {
-                    mch_memmove(ptr, ptr + 1, (size_t)((*file_to_find + file_to_findlen) - (ptr + 1)) + 1);
+                     memmove((char *)(ptr), (char *)(ptr + 1), (size_t)((*file_to_find + file_to_findlen) - (ptr + 1)) + 1) ;
                     --file_to_findlen;
                 }
             }
@@ -60989,19 +59204,19 @@
 
             if (rel_fname != NULL)
             {
-                rel_fnamelen = STRLEN(rel_fname);
+                rel_fnamelen =  strlen((char *)(rel_fname)) ;
             }
 
             for (run = 1; run <= 2; ++run)
             {
                 l = (int)file_to_findlen;
-                if (run == 1 && rel_to_curdir && (options & FNAME_REL) && rel_fname != NULL && rel_fnamelen + l < MAXPATHL)
+                if (run == 1 && rel_to_curdir && (options & FNAME_REL) && rel_fname != NULL && rel_fnamelen + l <  PATH_MAX )
                 {
-                    l = vim_snprintf((char *)NameBuff, MAXPATHL, "%.*s%s", (int)(gettail(rel_fname) - rel_fname), rel_fname, *file_to_find);
+                    l = vim_snprintf((char *)NameBuff,  PATH_MAX , "%.*s%s", (int)(gettail(rel_fname) - rel_fname), rel_fname, *file_to_find);
                 }
                 else
                 {
-                    STRCPY(NameBuff, *file_to_find);
+                     strcpy((char *)(NameBuff), (char *)(*file_to_find)) ;
                     run = 2;
                 }
 
@@ -61018,7 +59233,7 @@
                     {
                         break;
                     }
-                    NameBufflen = l + copy_option_part(&suffix, NameBuff + l, MAXPATHL - l, ",");
+                    NameBufflen = l + copy_option_part(&suffix, NameBuff + l,  PATH_MAX  - l, ",");
                 }
             }
         }
@@ -61056,13 +59271,13 @@
                     break;
                 }
 
-                if ((buf = alloc(MAXPATHL)) == NULL)
+                if ((buf = alloc( PATH_MAX )) == NULL)
                 {
                     break;
                 }
 
                 buf[0] = NUL;
-                copy_option_part(&dir, buf, MAXPATHL, " ,");
+                copy_option_part(&dir, buf,  PATH_MAX , " ,");
 
                 r_ptr = vim_findfile_stopdir(buf);
                 *search_ctx = vim_findfile_init(buf, *file_to_find, file_to_findlen, r_ptr, 100, FALSE, find_what, *search_ctx, FALSE, rel_fname);
@@ -61118,7 +59333,7 @@
         {
             return NULL;
         }
-        if (file_lnum != NULL && ptr[len] == ':' && SAFE_isdigit(ptr[len + 1]))
+        if (file_lnum != NULL && ptr[len] == ':' &&  (isdigit ((unsigned char)(ptr[len + 1]))) )
         {
             char_u *p = ptr + len + 1;
 
@@ -61146,7 +59361,7 @@
     ptr = line + col;
     while (*ptr != NUL && !vim_isfilec(*ptr))
     {
-        MB_PTR_ADV(ptr);
+         ptr += (*mb_ptr2len)(ptr) ;
     }
     if (*ptr == NUL)
     {
@@ -61214,16 +59429,16 @@
         size_t  match_textlen = 6;
 
         p = ptr + len;
-        if (STRNCMP(p, match_text, match_textlen) == 0)
+        if ( strncmp((char *)(p), (char *)(match_text), (match_textlen))  == 0)
         {
             p += match_textlen;
         }
         else
         {
             match_text = _(line_msg);
-            match_textlen = STRLEN(match_text);
+            match_textlen =  strlen((char *)(match_text)) ;
 
-            if (STRNCMP(p, match_text, match_textlen) == 0)
+            if ( strncmp((char *)(p), (char *)(match_text), (match_textlen))  == 0)
             {
                 p += match_textlen;
             }
@@ -61234,12 +59449,12 @@
         }
         if (*p != NUL)
         {
-            if (!SAFE_isdigit(*p))
+            if (! (isdigit ((unsigned char)(*p))) )
             {
                 ++p;
             }
             p = skipwhite(p);
-            if (SAFE_isdigit(*p))
+            if ( (isdigit ((unsigned char)(*p))) )
             {
                 *file_lnum = (int)getdigits(&p);
             }
@@ -61318,7 +59533,7 @@
             }
             look_for_sep = TRUE;
         }
-        MB_PTR_ADV(p);
+         p += (*mb_ptr2len)(p) ;
     }
     return dir_end;
 }
@@ -61343,7 +59558,7 @@
         {
             return OK;
         }
-        MB_PTR_BACK(path, *psep);
+         *psep -= has_mbyte ? ((*mb_head_off)(path, (*psep) - 1) + 1) : 1 ;
     }
 
     return FAIL;
@@ -61353,7 +59568,7 @@
 is_unique(char_u *maybe_unique, garray_T *gap, int i)
 {
     int     j;
-    int     candidate_len = (int)STRLEN(maybe_unique);
+    int     candidate_len = (int) strlen((char *)(maybe_unique)) ;
     int     other_path_len;
     char_u  **other_paths = (char_u **)gap->ga_data;
     char_u  *rival;
@@ -61365,14 +59580,14 @@
             continue;
         }
 
-        other_path_len = (int)STRLEN(other_paths[j]);
+        other_path_len = (int) strlen((char *)(other_paths[j])) ;
         if (other_path_len < candidate_len)
         {
             continue;
         }
 
         rival = other_paths[j] + other_path_len - candidate_len;
-        if (fnamecmp(maybe_unique, rival) == 0 && (rival == other_paths[j] || vim_ispathsep(*(rival - 1))))
+        if ( vim_fnamecmp((char_u *)(maybe_unique), (char_u *)(rival))  == 0 && (rival == other_paths[j] || vim_ispathsep(*(rival - 1))))
         {
             return FALSE;
         }
@@ -61389,14 +59604,14 @@
     char_u      *p;
     size_t      curdirlen = 0;
 
-    if ((buf = alloc(MAXPATHL)) == NULL)
+    if ((buf = alloc( PATH_MAX )) == NULL)
     {
         return;
     }
 
     while (*path_option != NUL)
     {
-        buflen = copy_option_part(&path_option, buf, MAXPATHL, " ,");
+        buflen = copy_option_part(&path_option, buf,  PATH_MAX , " ,");
 
         if (vim_strchr(buf, '`') != NULL)
         {
@@ -61413,7 +59628,7 @@
             }
             p = gettail(curbuf->b_ffname);
             plen = (size_t)(p - curbuf->b_ffname);
-            if (plen + buflen >= MAXPATHL)
+            if (plen + buflen >=  PATH_MAX )
             {
                 continue;
             }
@@ -61423,17 +59638,17 @@
             }
             else
             {
-                mch_memmove(buf + plen, buf + 2, (buflen - 2) + 1);
+                 memmove((char *)(buf + plen), (char *)(buf + 2), (buflen - 2) + 1) ;
             }
-            mch_memmove(buf, curbuf->b_ffname, plen);
+             memmove((char *)(buf), (char *)(curbuf->b_ffname), plen) ;
             buflen = simplify_filename(buf);
         }
         else if (buf[0] == NUL)
         {
-            STRCPY(buf, curdir);
+             strcpy((char *)(buf), (char *)(curdir)) ;
             if (curdirlen == 0)
             {
-                curdirlen = STRLEN(curdir);
+                curdirlen =  strlen((char *)(curdir)) ;
             }
             buflen = curdirlen;
         }
@@ -61445,16 +59660,16 @@
         {
             if (curdirlen == 0)
             {
-                curdirlen = STRLEN(curdir);
+                curdirlen =  strlen((char *)(curdir)) ;
             }
-            if (curdirlen + buflen + 3 > MAXPATHL)
+            if (curdirlen + buflen + 3 >  PATH_MAX )
             {
                 continue;
             }
 
-            mch_memmove(buf + curdirlen + 1, buf, buflen + 1);
-            STRCPY(buf, curdir);
-            buf[curdirlen] = PATHSEP;
+             memmove((char *)(buf + curdirlen + 1), (char *)(buf), buflen + 1) ;
+             strcpy((char *)(buf), (char *)(curdir)) ;
+            buf[curdirlen] =  ((char_u)'/') ;
             buflen = simplify_filename(buf);
         }
 
@@ -61501,7 +59716,7 @@
     {
         while (vim_ispathsep(*cutoff))
         {
-            MB_PTR_ADV(cutoff);
+             cutoff += (*mb_ptr2len)(cutoff) ;
         }
     }
 
@@ -61526,14 +59741,14 @@
     remove_duplicates(gap);
     ga_init2(&path_ga, sizeof(char_u *), 1);
 
-    len = (int)STRLEN(pattern);
+    len = (int) strlen((char *)(pattern)) ;
     file_pattern = alloc(len + 2);
     if (file_pattern == NULL)
     {
         return;
     }
     file_pattern[0] = '*';
-    STRCPY(file_pattern + 1, pattern);
+     strcpy((char *)(file_pattern + 1), (char *)(pattern)) ;
     pat = file_pat_to_reg_pat(file_pattern, NULL, NULL, FALSE);
     vim_free(file_pattern);
     if (pat == NULL)
@@ -61549,14 +59764,14 @@
         return;
     }
 
-    if ((curdir = alloc(MAXPATHL)) == NULL)
+    if ((curdir = alloc( PATH_MAX )) == NULL)
     {
         goto theend;
     }
-    mch_dirname(curdir, MAXPATHL);
+    mch_dirname(curdir,  PATH_MAX );
     expand_path_option(curdir, path_option, &path_ga);
 
-    in_curdir = ALLOC_CLEAR_MULT(char_u *, gap->ga_len);
+    in_curdir =  (char_u * *)alloc_clear(sizeof(char_u *) * (gap->ga_len)) ;
     if (in_curdir == NULL)
     {
         goto theend;
@@ -61569,8 +59784,8 @@
         char_u      *dir_end = gettail_dir(path);
         char_u      *path_cutoff;
 
-        len = (int)STRLEN(path);
-        is_in_curdir = fnamencmp(curdir, path, dir_end - path) == 0
+        len = (int) strlen((char *)(path)) ;
+        is_in_curdir =  vim_fnamencmp((char_u *)(curdir), (char_u *)(path), (dir_end - path))  == 0
                                              && curdir[dir_end - path] == NUL;
         if (is_in_curdir)
         {
@@ -61582,7 +59797,7 @@
         if (pattern[0] == '*' && pattern[1] == '*' && vim_ispathsep_nocolon(pattern[2]) && path_cutoff != NULL && vim_regexec(&regmatch, path_cutoff, (colnr_T)0) && is_unique(path_cutoff, gap, i))
         {
             sort_again = TRUE;
-            mch_memmove(path, path_cutoff, STRLEN(path_cutoff) + 1);
+             memmove((char *)(path), (char *)(path_cutoff),  strlen((char *)(path_cutoff))  + 1) ;
         }
         else
         {
@@ -61593,7 +59808,7 @@
                 if (vim_regexec(&regmatch, pathsep_p + 1, (colnr_T)0) && is_unique(pathsep_p + 1, gap, i) && path_cutoff != NULL && pathsep_p + 1 >= path_cutoff)
                 {
                     sort_again = TRUE;
-                    mch_memmove(path, pathsep_p + 1, (size_t)((path + len) - (pathsep_p + 1)) + 1);
+                     memmove((char *)(path), (char *)(pathsep_p + 1), (size_t)((path + len) - (pathsep_p + 1)) + 1) ;
                     break;
                 }
             }
@@ -61604,7 +59819,7 @@
             short_name = shorten_fname(path, curdir);
             if (short_name != NULL && short_name > path + 1)
             {
-                vim_snprintf((char *)path, MAXPATHL, ".%s%s", PATHSEPSTR, short_name);
+                vim_snprintf((char *)path,  PATH_MAX , ".%s%s",  "/" , short_name);
             }
         }
         ui_breakcheck();
@@ -61628,18 +59843,18 @@
         }
         if (is_unique(short_name, gap, i))
         {
-            STRCPY(fnames[i], short_name);
+             strcpy((char *)(fnames[i]), (char *)(short_name)) ;
             continue;
         }
 
-        rel_pathsize = 1 + sizeof(PATHSEP) + STRLEN(short_name) + 1;
+        rel_pathsize = 1 + sizeof( ((char_u)'/') ) +  strlen((char *)(short_name))  + 1;
         rel_path = alloc(rel_pathsize);
         if (rel_path == NULL)
         {
             goto theend;
         }
 
-        vim_snprintf((char *)rel_path, rel_pathsize, ".%s%s", PATHSEPSTR, short_name);
+        vim_snprintf((char *)rel_path, rel_pathsize, ".%s%s",  "/" , short_name);
 
         vim_free(fnames[i]);
         fnames[i] = rel_path;
@@ -61675,11 +59890,11 @@
     int         glob_flags = 0;
     char_u      *path_option = *curbuf->b_p_path == NUL ? p_path : curbuf->b_p_path;
 
-    if ((curdir = alloc(MAXPATHL)) == NULL)
+    if ((curdir = alloc( PATH_MAX )) == NULL)
     {
         return 0;
     }
-    mch_dirname(curdir, MAXPATHL);
+    mch_dirname(curdir,  PATH_MAX );
 
     ga_init2(&path_ga, sizeof(char_u *), 1);
     if (flags & EW_CDPATH)
@@ -61741,10 +59956,10 @@
             ;
     }
     start = p;
-    p_end = p + STRLEN(p);
+    p_end = p +  strlen((char *)(p)) ;
     if (start > filename + 2)
     {
-        mch_memmove(filename + 1, p, (size_t)(p_end - p) + 1);
+         memmove((char *)(filename + 1), (char *)(p), (size_t)(p_end - p) + 1) ;
         p_end -= (size_t)(p - (filename + 1));
         start = p = filename + 1;
     }
@@ -61753,7 +59968,7 @@
     {
         if (vim_ispathsep(*p))
         {
-            mch_memmove(p, p + 1, (size_t)(p_end - (p + 1)) + 1);
+             memmove((char *)(p), (char *)(p + 1), (size_t)(p_end - (p + 1)) + 1) ;
             --p_end;
         }
         else if (p[0] == '.' && (vim_ispathsep(p[1]) || p[1] == NUL))
@@ -61769,7 +59984,7 @@
                 {
                     while (vim_ispathsep(*tail))
                     {
-                        MB_PTR_ADV(tail);
+                         tail += (*mb_ptr2len)(tail) ;
                     }
                 }
                 else if (p > start)
@@ -61777,7 +59992,7 @@
                     --p;
                 }
 
-                mch_memmove(p, tail, (size_t)(p_end - tail) + 1);
+                 memmove((char *)(p), (char *)(tail), (size_t)(p_end - tail) + 1) ;
                 p_end -= (size_t)(tail - p);
             }
         }
@@ -61786,7 +60001,7 @@
             tail = p + 2;
             while (vim_ispathsep(*tail))
             {
-                MB_PTR_ADV(tail);
+                 tail += (*mb_ptr2len)(tail) ;
             }
 
             if (components > 0)
@@ -61799,7 +60014,7 @@
                 {
                     saved_char = p[-1];
                     p[-1] = NUL;
-                    if (mch_lstat((char *)filename, &st) < 0)
+                    if ( lstat(((char *)filename), (&st))  < 0)
                     {
                             do_strip = TRUE;
                     }
@@ -61808,14 +60023,14 @@
                     --p;
                     while (p > start && !after_pathsep(start, p))
                     {
-                        MB_PTR_BACK(start, p);
+                         p -= has_mbyte ? ((*mb_head_off)(start, (p) - 1) + 1) : 1 ;
                     }
 
                     if (!do_strip)
                     {
                         saved_char = *tail;
                         *tail = NUL;
-                        if (mch_stat((char *)filename, &st) >= 0)
+                        if ( stat(((char *)filename), (&st))  >= 0)
                         {
                             do_strip = TRUE;
                         }
@@ -61830,13 +60045,13 @@
 
                             if (p == start && relative)
                             {
-                                (void)mch_stat(".", &new_st);
+                                (void) stat(("."), (&new_st)) ;
                             }
                             else
                             {
                                 saved_char = *p;
                                 *p = NUL;
-                                (void)mch_stat((char *)filename, &new_st);
+                                (void) stat(((char *)filename), (&new_st)) ;
                                 *p = saved_char;
                             }
 
@@ -61867,7 +60082,7 @@
                             --p;
                         }
 
-                        mch_memmove(p, tail, (size_t)(p_end - tail) + 1);
+                         memmove((char *)(p), (char *)(tail), (size_t)(p_end - tail) + 1) ;
                         p_end -= (size_t)(tail - p);
                     }
 
@@ -61876,14 +60091,14 @@
             }
             else if (p == start && !relative)
             {
-                mch_memmove(p, tail, (size_t)(p_end - tail) + 1);
+                 memmove((char *)(p), (char *)(tail), (size_t)(p_end - tail) + 1) ;
                 p_end -= (size_t)(tail - p);
             }
             else
             {
                 if (p == start + 2 && p[-2] == '.')
                 {
-                    mch_memmove(p - 2, p, (size_t)(p_end - p) + 1);
+                     memmove((char *)(p - 2), (char *)(p), (size_t)(p_end - p) + 1) ;
                     p_end -= 2;
                     tail -= 2;
                 }
@@ -61911,9 +60126,7 @@
 static score_t match_positions(char_u *needle, char_u *haystack, int_u *positions);
 static int has_match(char_u *needle, char_u *haystack);
 
-#define SCORE_MAX INFINITY
-#define SCORE_MIN (-INFINITY)
-#define SCORE_SCALE 1000
+enum { SCORE_SCALE = 1000 };
 
     int
 fuzzy_match(char_u      *str, char_u      *pat_arg, int         matchseq, int         *outScore, int_u       *matches, int         maxMatches)
@@ -61951,11 +60164,11 @@
                 break;
             }
             pat = p;
-            while (*p != NUL && !VIM_ISWHITE(PTR2CHAR(p)))
+            while (*p != NUL && ! (( (has_mbyte ? mb_ptr2char(p) : (int)*(p)) ) == ' ' || ( (has_mbyte ? mb_ptr2char(p) : (int)*(p)) ) == '\t') )
             {
                 if (has_mbyte)
                 {
-                    MB_PTR_ADV(p);
+                     p += (*mb_ptr2len)(p) ;
                 }
                 else
                 {
@@ -61968,7 +60181,7 @@
             }
             *p = NUL;
         }
-        pat_chars = MB_CHARLEN(pat);
+        pat_chars =  (has_mbyte ? mb_charlen(pat) : (int) strlen((char *)(pat)) ) ;
         if (pat_chars > maxMatches)
         {
             pat_chars = maxMatches;
@@ -61976,26 +60189,26 @@
         if (numMatches > maxMatches - pat_chars)
         {
             numMatches = 0;
-            *outScore = FUZZY_SCORE_NONE;
+            *outScore =  INT_MIN ;
             break;
         }
 
-        score = FUZZY_SCORE_NONE;
+        score =  INT_MIN ;
         if (has_match(pat, str))
         {
             fzy_score = match_positions(pat, str, matches + numMatches);
-            if (fzy_score != SCORE_MIN)
+            if (fzy_score !=  (-INFINITY) )
             {
-                score = (fzy_score == SCORE_MAX) ? INT_MAX
+                score = (fzy_score ==  INFINITY ) ? INT_MAX
                     : (fzy_score < 0) ? (int)ceil(fzy_score * SCORE_SCALE - 0.5)
                     : (int)floor(fzy_score * SCORE_SCALE + 0.5);
             }
         }
 
-        if (score == FUZZY_SCORE_NONE)
+        if (score ==  INT_MIN )
         {
             numMatches = 0;
-            *outScore = FUZZY_SCORE_NONE;
+            *outScore =  INT_MIN ;
             break;
         }
 
@@ -62087,7 +60300,7 @@
     int
 fuzzy_match_str(char_u *str, char_u *pat)
 {
-    int         score = FUZZY_SCORE_NONE;
+    int         score =  INT_MIN ;
     int_u       matchpos[FUZZY_MATCH_MAX_LEN];
 
     if (str == NULL || pat == NULL)
@@ -62101,9 +60314,9 @@
 }
 
     garray_T *
-fuzzy_match_str_with_pos(char_u *str UNUSED, char_u *pat UNUSED)
+fuzzy_match_str_with_pos(char_u *str  __attribute__((unused)) , char_u *pat  __attribute__((unused)) )
 {
-    int             score = FUZZY_SCORE_NONE;
+    int             score =  INT_MIN ;
     garray_T        *match_positions = NULL;
     int_u           matches[FUZZY_MATCH_MAX_LEN];
     int             j = 0;
@@ -62113,23 +60326,23 @@
         return NULL;
     }
 
-    match_positions = ALLOC_ONE(garray_T);
+    match_positions =  (garray_T *)alloc(sizeof(garray_T)) ;
     if (match_positions == NULL)
     {
         return NULL;
     }
     ga_init2(match_positions, sizeof(int_u), 10);
 
-    if (!fuzzy_match(str, pat, FALSE, &score, matches, FUZZY_MATCH_MAX_LEN) || score == FUZZY_SCORE_NONE)
+    if (!fuzzy_match(str, pat, FALSE, &score, matches, FUZZY_MATCH_MAX_LEN) || score ==  INT_MIN )
     {
         ga_clear(match_positions);
         vim_free(match_positions);
         return NULL;
     }
 
-    for (char_u *p = pat; *p != NUL; MB_PTR_ADV(p))
+    for (char_u *p = pat; *p != NUL;  p += (*mb_ptr2len)(p) )
     {
-        if (!VIM_ISWHITE(PTR2CHAR(p)))
+        if (! (( (has_mbyte ? mb_ptr2char(p) : (int)*(p)) ) == ' ' || ( (has_mbyte ? mb_ptr2char(p) : (int)*(p)) ) == '\t') )
         {
             if (ga_grow(match_positions, 1) == FAIL)
             {
@@ -62179,7 +60392,7 @@
         *score = fuzzy_match_str(start, pat);
         *end = save_end;
 
-        if (*score != FUZZY_SCORE_NONE)
+        if (*score !=  INT_MIN )
         {
             *len = (int)(end - start);
             found = TRUE;
@@ -62194,7 +60407,7 @@
         str = end;
         while (*str != NUL && !vim_iswordp(str))
         {
-            MB_PTR_ADV(str);
+             str += (*mb_ptr2len)(str) ;
         }
     }
 
@@ -62233,8 +60446,7 @@
 
     do
     {
-
-        if (looped_around && (whole_line ? current_pos.lnum == circly_end.lnum : EQUAL_POS(current_pos, circly_end)))
+        if (looped_around && (whole_line ? current_pos.lnum == circly_end.lnum :  (((current_pos).lnum == (circly_end).lnum) && ((current_pos).col == (circly_end).col) && ((current_pos).coladd == (circly_end).coladd)) ))
         {
             break;
         }
@@ -62266,7 +60478,7 @@
                 {
                     char_u *line = *ptr;
                     char_u *p = skipwhite(line);
-                    if (fuzzy_match_str(p, pattern) != FUZZY_SCORE_NONE)
+                    if (fuzzy_match_str(p, pattern) !=  INT_MIN )
                     {
                         found_new_match = TRUE;
                         *pos = current_pos;
@@ -62339,7 +60551,7 @@
         goto theend;
     }
 
-    *matches = ALLOC_MULT(char_u *, count);
+    *matches =  (char_u * *)alloc(sizeof(char_u *) * (count)) ;
     if (*matches == NULL)
     {
         fuzmatch_str_free(fuzmatch, count);
@@ -62365,17 +60577,6 @@
     return OK;
 }
 
-#define MATCH_MAX_LEN FUZZY_MATCH_MAX_LEN
-
-#define SCORE_GAP_LEADING -0.005
-#define SCORE_GAP_TRAILING -0.005
-#define SCORE_GAP_INNER -0.01
-#define SCORE_MATCH_CONSECUTIVE 1.0
-#define SCORE_MATCH_SLASH 0.9
-#define SCORE_MATCH_WORD 0.8
-#define SCORE_MATCH_CAPITAL 0.7
-#define SCORE_MATCH_DOT 0.6
-
     static int
 has_match(char_u *needle, char_u *haystack)
 {
@@ -62395,7 +60596,7 @@
         while (*h_ptr)
         {
             int h_char = mb_ptr2char(h_ptr);
-            if (h_char == n_char || h_char == MB_TOUPPER(n_char))
+            if (h_char == n_char || h_char ==  vim_toupper(n_char) )
             {
                 found = TRUE;
                 h_ptr += mb_ptr2len(h_ptr);
@@ -62419,35 +60620,31 @@
 {
     int needle_len;
     int haystack_len;
-    int lower_needle[MATCH_MAX_LEN];
-    int lower_haystack[MATCH_MAX_LEN];
-    score_t match_bonus[MATCH_MAX_LEN];
+    int lower_needle[ FUZZY_MATCH_MAX_LEN ];
+    int lower_haystack[ FUZZY_MATCH_MAX_LEN ];
+    score_t match_bonus[ FUZZY_MATCH_MAX_LEN ];
 } match_struct;
 
-#define IS_WORD_SEP(c) ((c) == '-' || (c) == '_' || (c) == ' ')
-#define IS_PATH_SEP(c) ((c) == '/')
-#define IS_DOT(c)      ((c) == '.')
-
     static score_t
 compute_bonus_codepoint(int last_c, int c)
 {
-    if (ASCII_ISALNUM(c) || vim_iswordc(c))
+    if ( ( ( ((unsigned)(c) - 'A' < 26)  ||  ((unsigned)(c) - 'a' < 26) )  ||  ((unsigned)(c) - '0' < 10) )  || vim_iswordc(c))
     {
-        if (IS_PATH_SEP(last_c))
+        if ( ((last_c) == '/') )
         {
-            return SCORE_MATCH_SLASH;
+            return  0.9 ;
         }
-        if (IS_WORD_SEP(last_c))
+        if ( ((last_c) == '-' || (last_c) == '_' || (last_c) == ' ') )
         {
-            return SCORE_MATCH_WORD;
+            return  0.8 ;
         }
-        if (IS_DOT(last_c))
+        if ( ((last_c) == '.') )
         {
-            return SCORE_MATCH_DOT;
+            return  0.6 ;
         }
-        if (MB_ISUPPER(c) && MB_ISLOWER(last_c))
+        if ( vim_isupper(c)  &&  vim_islower(last_c) )
         {
-            return SCORE_MATCH_CAPITAL;
+            return  0.7 ;
         }
     }
     return 0;
@@ -62458,24 +60655,24 @@
 {
     int i = 0;
     char_u *p = needle;
-    while (*p != NUL && i < MATCH_MAX_LEN)
+    while (*p != NUL && i <  FUZZY_MATCH_MAX_LEN )
     {
         int c = mb_ptr2char(p);
-        match->lower_needle[i++] = MB_TOLOWER(c);
-        MB_PTR_ADV(p);
+        match->lower_needle[i++] =  vim_tolower(c) ;
+         p += (*mb_ptr2len)(p) ;
     }
     match->needle_len = i;
 
     i = 0;
     p = haystack;
     int prev_c = '/';
-    while (*p != NUL && i < MATCH_MAX_LEN)
+    while (*p != NUL && i <  FUZZY_MATCH_MAX_LEN )
     {
         int c = mb_ptr2char(p);
-        match->lower_haystack[i] = MB_TOLOWER(c);
+        match->lower_haystack[i] =  vim_tolower(c) ;
         match->match_bonus[i] = compute_bonus_codepoint(prev_c, c);
         prev_c = c;
-        MB_PTR_ADV(p);
+         p += (*mb_ptr2len)(p) ;
         i++;
     }
     match->haystack_len = i;
@@ -62492,24 +60689,24 @@
     const int *lower_haystack = match->lower_haystack;
     const score_t *match_bonus = match->match_bonus;
 
-    score_t prev_score = SCORE_MIN;
-    score_t gap_score = i == n - 1 ? SCORE_GAP_TRAILING : SCORE_GAP_INNER;
+    score_t prev_score =  (-INFINITY) ;
+    score_t gap_score = i == n - 1 ?  -0.005  :  -0.01 ;
 
-    score_t prev_M = SCORE_MIN;
-    score_t prev_D = SCORE_MIN;
+    score_t prev_M =  (-INFINITY) ;
+    score_t prev_D =  (-INFINITY) ;
 
     for (int j = 0; j < m; j++)
     {
         if (lower_needle[i] == lower_haystack[j])
         {
-            score_t score = SCORE_MIN;
+            score_t score =  (-INFINITY) ;
             if (!i)
             {
-                score = (j * SCORE_GAP_LEADING) + match_bonus[j];
+                score = (j *  -0.005 ) + match_bonus[j];
             }
             else if (j)
             {
-                score = MAX(prev_M + match_bonus[j], prev_D + SCORE_MATCH_CONSECUTIVE);
+                score = MAX(prev_M + match_bonus[j], prev_D +  1.0 );
             }
             prev_D = last_D[j];
             prev_M = last_M[j];
@@ -62520,7 +60717,7 @@
         {
             prev_D = last_D[j];
             prev_M = last_M[j];
-            curr_D[j] = SCORE_MIN;
+            curr_D[j] =  (-INFINITY) ;
             curr_M[j] = prev_score = prev_score + gap_score;
         }
     }
@@ -62531,7 +60728,7 @@
 {
     if (!needle || !haystack || !*needle)
     {
-        return SCORE_MIN;
+        return  (-INFINITY) ;
     }
 
     match_struct match;
@@ -62540,9 +60737,9 @@
     int n = match.needle_len;
     int m = match.haystack_len;
 
-    if (m > MATCH_MAX_LEN || n > m)
+    if (m >  FUZZY_MATCH_MAX_LEN  || n > m)
     {
-        return SCORE_MIN;
+        return  (-INFINITY) ;
     }
     else if (n == m)
     {
@@ -62564,23 +60761,23 @@
                     positions[i] = i;
                 }
             }
-            return SCORE_MAX;
+            return  INFINITY ;
         }
     }
 
-    if ((size_t)n > (SIZE_MAX / sizeof(score_t)) / MATCH_MAX_LEN / 2)
+    if ((size_t)n > (SIZE_MAX / sizeof(score_t)) /  FUZZY_MATCH_MAX_LEN  / 2)
     {
-        return SCORE_MIN;
+        return  (-INFINITY) ;
     }
 
-    score_t *block = (score_t*)alloc(sizeof(score_t) * MATCH_MAX_LEN * n * 2);
+    score_t *block = (score_t*)alloc(sizeof(score_t) *  FUZZY_MATCH_MAX_LEN  * n * 2);
     if (!block)
     {
-        return SCORE_MIN;
+        return  (-INFINITY) ;
     }
 
-    score_t (*D)[MATCH_MAX_LEN] = (score_t(*)[MATCH_MAX_LEN])block;
-    score_t (*M)[MATCH_MAX_LEN] = (score_t(*)[MATCH_MAX_LEN])(block + MATCH_MAX_LEN * n);
+    score_t (*D)[ FUZZY_MATCH_MAX_LEN ] = (score_t(*)[ FUZZY_MATCH_MAX_LEN ])block;
+    score_t (*M)[ FUZZY_MATCH_MAX_LEN ] = (score_t(*)[ FUZZY_MATCH_MAX_LEN ])(block +  FUZZY_MATCH_MAX_LEN  * n);
 
     match_row(&match, 0, D[0], M[0], D[0], M[0]);
     for (int i = 1; i < n; i++)
@@ -62595,10 +60792,10 @@
         {
             for (; j >= 0; j--)
             {
-                if (D[i][j] != SCORE_MIN && (match_required || D[i][j] == M[i][j]))
+                if (D[i][j] !=  (-INFINITY)  && (match_required || D[i][j] == M[i][j]))
                 {
                     match_required = i && j &&
-                        M[i][j] == D[i - 1][j - 1] + SCORE_MATCH_CONSECUTIVE;
+                        M[i][j] == D[i - 1][j - 1] +  1.0 ;
                     positions[i] = j--;
                     break;
                 }
@@ -62613,7 +60810,7 @@
 
 // ==================== getchar.c ====================
 
-#define MINIMAL_SIZE 20
+enum { MINIMAL_SIZE = 20 };
 
 static buffheader_T redobuff = {{NULL, 0, {NUL}}, NULL, 0, 0, FALSE};
 static buffheader_T old_redobuff = {{NULL, 0, {NUL}}, NULL, 0, 0, FALSE};
@@ -62625,15 +60822,14 @@
 
 static int      KeyNoremap = 0;
 
-#define RM_YES          0
-#define RM_NONE         1
-#define RM_SCRIPT       2
-#define RM_ABBR         4
-#define RM_SIMPLIFIED   8
-
-# define TYPELEN_INIT   (5 * (MAXMAPLEN + 3))
-static char_u   typebuf_init[TYPELEN_INIT];
-static char_u   noremapbuf_init[TYPELEN_INIT];
+enum { RM_YES = 0 };
+enum { RM_NONE = 1 };
+enum { RM_SCRIPT = 2 };
+enum { RM_ABBR = 4 };
+enum { RM_SIMPLIFIED = 8 };
+
+static char_u   typebuf_init[ (5 * (MAXMAPLEN + 3)) ];
+static char_u   noremapbuf_init[ (5 * (MAXMAPLEN + 3)) ];
 
 static size_t   last_recorded_len = 0;
 
@@ -62740,7 +60936,7 @@
 {
     if (slen < 0)
     {
-        slen = (long)STRLEN(s);
+        slen = (long) strlen((char *)(s)) ;
     }
     if (slen == 0)
     {
@@ -62759,7 +60955,7 @@
     }
     else if (buf->bh_index != 0)
     {
-        mch_memmove(buf->bh_first.b_next->b_str, buf->bh_first.b_next->b_str + buf->bh_index, (buf->bh_first.b_next->b_strlen - buf->bh_index) + 1);
+         memmove((char *)(buf->bh_first.b_next->b_str), (char *)(buf->bh_first.b_next->b_str + buf->bh_index), (buf->bh_first.b_next->b_strlen - buf->bh_index) + 1) ;
         buf->bh_first.b_next->b_strlen -= buf->bh_index;
         buf->bh_space += buf->bh_index;
     }
@@ -62836,7 +61032,7 @@
     char_u      temp[4];
     long        templen;
 
-    if (IS_SPECIAL(c))
+    if ( ((c) < 0) )
     {
         len = 1;
     }
@@ -62846,16 +61042,16 @@
     }
     for (i = 0; i < len; ++i)
     {
-        if (!IS_SPECIAL(c))
+        if (! ((c) < 0) )
         {
             c = bytes[i];
         }
 
-        if (IS_SPECIAL(c) || c == K_SPECIAL || c == NUL)
+        if ( ((c) < 0)  || c ==  (0x80)  || c == NUL)
         {
-            temp[0] = K_SPECIAL;
-            temp[1] = K_SECOND(c);
-            temp[2] = K_THIRD(c);
+            temp[0] =  (0x80) ;
+            temp[1] =  ((c) ==  (0x80)  ? KS_SPECIAL : (c) == NUL ? KS_ZERO :  ((-(c)) & 0xff) ) ;
+            temp[2] =  (((c) ==  (0x80)  || (c) == NUL) ?  ('X')  :  (((unsigned)(-(c)) >> 8) & 0xff) ) ;
             temp[3] = NUL;
             templen = 3;
         }
@@ -63123,7 +61319,7 @@
 
     while (*s != NUL)
     {
-        if (*s == K_SPECIAL && s[1] != NUL && s[2] != NUL)
+        if (*s ==  (0x80)  && s[1] != NUL && s[2] != NUL)
         {
             add_buff(&redobuff, s, 3L);
             s += 3;
@@ -63192,7 +61388,7 @@
     while (*arg != NUL)
     {
         start = arg;
-        while ((*arg >= ' ' && *arg < DEL) || (*arg == K_SPECIAL && !literally))
+        while ((*arg >= ' ' && *arg < DEL) || (*arg ==  (0x80)  && !literally))
         {
             ++arg;
         }
@@ -63249,9 +61445,9 @@
     }
     if ((c = *p) != NUL)
     {
-        if (has_mbyte && (c != K_SPECIAL || p[1] == KS_SPECIAL))
+        if (has_mbyte && (c !=  (0x80)  || p[1] == KS_SPECIAL))
         {
-            n = MB_BYTE2LEN_CHECK(c);
+            n =  (((c) < 0 || (c) > 255) ? 1 : mb_bytelen_tab[c]) ;
         }
         else
         {
@@ -63259,9 +61455,9 @@
         }
         for (i = 0; ; ++i)
         {
-            if (c == K_SPECIAL)
+            if (c ==  (0x80) )
             {
-                c = TO_SPECIAL(p[1], p[2]);
+                c =  ((p[1]) == KS_SPECIAL ?  (0x80)  : (p[1]) == KS_ZERO ?   (-((KS_ZERO) + ((int)( ('X') ) << 8)))   :  (-((p[1]) + ((int)(p[2]) << 8))) ) ;
                 p += 2;
             }
             if (*++p == NUL && bp->b_next != NULL)
@@ -63344,7 +61540,7 @@
 
     if (count)
     {
-        while (VIM_ISDIGIT(c))
+        while ( ((unsigned)(c) - '0' < 10) )
         {
             c = read_redo(FALSE, old_redo);
         }
@@ -63373,7 +61569,7 @@
         {
             if (c == 'O' || c == 'o')
             {
-                add_buff(&readbuf2, NL_STR, -1L);
+                add_buff(&readbuf2,  (char_u *)"\012" , -1L);
             }
             break;
         }
@@ -63400,7 +61596,7 @@
 
     typebuf.tb_buf = typebuf_init;
     typebuf.tb_noremap = noremapbuf_init;
-    typebuf.tb_buflen = TYPELEN_INIT;
+    typebuf.tb_buflen =  (5 * (MAXMAPLEN + 3)) ;
     typebuf.tb_len = 0;
     typebuf.tb_off = MAXMAPLEN + 4;
     typebuf.tb_change_cnt = 1;
@@ -63431,17 +61627,17 @@
     }
     state_no_longer_safe("ins_typebuf()");
 
-    addlen = (int)STRLEN(str);
+    addlen = (int) strlen((char *)(str)) ;
 
     if (offset == 0 && addlen <= typebuf.tb_off)
     {
         typebuf.tb_off -= addlen;
-        mch_memmove(typebuf.tb_buf + typebuf.tb_off, str, (size_t)addlen);
+         memmove((char *)(typebuf.tb_buf + typebuf.tb_off), (char *)(str), (size_t)addlen) ;
     }
     else if (typebuf.tb_len == 0 && typebuf.tb_buflen >= addlen + 3 * (MAXMAPLEN + 4))
     {
         typebuf.tb_off = (typebuf.tb_buflen - addlen - 3 * (MAXMAPLEN + 4)) / 2;
-        mch_memmove(typebuf.tb_buf + typebuf.tb_off, str, (size_t)addlen);
+         memmove((char *)(typebuf.tb_buf + typebuf.tb_off), (char *)(str), (size_t)addlen) ;
     }
     else
     {
@@ -63469,17 +61665,17 @@
         }
         typebuf.tb_buflen = newlen;
 
-        mch_memmove(s1 + newoff, typebuf.tb_buf + typebuf.tb_off, (size_t)offset);
-        mch_memmove(s1 + newoff + offset, str, (size_t)addlen);
-        mch_memmove(s1 + newoff + offset + addlen, typebuf.tb_buf + typebuf.tb_off + offset, (size_t)(typebuf.tb_len - offset + 1));
+         memmove((char *)(s1 + newoff), (char *)(typebuf.tb_buf + typebuf.tb_off), (size_t)offset) ;
+         memmove((char *)(s1 + newoff + offset), (char *)(str), (size_t)addlen) ;
+         memmove((char *)(s1 + newoff + offset + addlen), (char *)(typebuf.tb_buf + typebuf.tb_off + offset), (size_t)(typebuf.tb_len - offset + 1)) ;
         if (typebuf.tb_buf != typebuf_init)
         {
             vim_free(typebuf.tb_buf);
         }
         typebuf.tb_buf = s1;
 
-        mch_memmove(s2 + newoff, typebuf.tb_noremap + typebuf.tb_off, (size_t)offset);
-        mch_memmove(s2 + newoff + offset + addlen, typebuf.tb_noremap + typebuf.tb_off + offset, (size_t)(typebuf.tb_len - offset));
+         memmove((char *)(s2 + newoff), (char *)(typebuf.tb_noremap + typebuf.tb_off), (size_t)offset) ;
+         memmove((char *)(s2 + newoff + offset + addlen), (char *)(typebuf.tb_noremap + typebuf.tb_off + offset), (size_t)(typebuf.tb_len - offset)) ;
         if (typebuf.tb_noremap != noremapbuf_init)
         {
             vim_free(typebuf.tb_noremap);
@@ -63490,11 +61686,11 @@
     }
     typebuf.tb_len += addlen;
 
-    if (noremap == REMAP_SCRIPT)
+    if (noremap ==  (-2) )
     {
         val = RM_SCRIPT;
     }
-    else if (noremap == REMAP_SKIP)
+    else if (noremap ==  (-3) )
     {
         val = RM_ABBR;
     }
@@ -63503,7 +61699,7 @@
         val = RM_NONE;
     }
 
-    if (noremap == REMAP_SKIP)
+    if (noremap ==  (-3) )
     {
         nrm = 1;
     }
@@ -63588,12 +61784,12 @@
         i = typebuf.tb_off + offset;
         if (typebuf.tb_off > MAXMAPLEN)
         {
-            mch_memmove(typebuf.tb_buf + MAXMAPLEN, typebuf.tb_buf + typebuf.tb_off, (size_t)offset);
-            mch_memmove(typebuf.tb_noremap + MAXMAPLEN, typebuf.tb_noremap + typebuf.tb_off, (size_t)offset);
+             memmove((char *)(typebuf.tb_buf + MAXMAPLEN), (char *)(typebuf.tb_buf + typebuf.tb_off), (size_t)offset) ;
+             memmove((char *)(typebuf.tb_noremap + MAXMAPLEN), (char *)(typebuf.tb_noremap + typebuf.tb_off), (size_t)offset) ;
             typebuf.tb_off = MAXMAPLEN;
         }
-        mch_memmove(typebuf.tb_buf + typebuf.tb_off + offset, typebuf.tb_buf + i + len, (size_t)(typebuf.tb_len - offset + 1));
-        mch_memmove(typebuf.tb_noremap + typebuf.tb_off + offset, typebuf.tb_noremap + i + len, (size_t)(typebuf.tb_len - offset));
+         memmove((char *)(typebuf.tb_buf + typebuf.tb_off + offset), (char *)(typebuf.tb_buf + i + len), (size_t)(typebuf.tb_len - offset + 1)) ;
+         memmove((char *)(typebuf.tb_noremap + typebuf.tb_off + offset), (char *)(typebuf.tb_noremap + i + len), (size_t)(typebuf.tb_len - offset)) ;
     }
 
     if (typebuf.tb_maplen > offset)
@@ -63657,7 +61853,7 @@
     {
         state->pending_special--;
     }
-    else if (c == K_SPECIAL)
+    else if (c ==  (0x80) )
     {
         state->pending_special = 2;
     }
@@ -63679,13 +61875,13 @@
             {
                 goto ret_false;
             }
-            c = TO_SPECIAL(state->prev_c, c);
-            if (c == K_FOCUSGAINED || c == K_FOCUSLOST || c == K_COMPLETE_DELAY)
+            c =  ((state->prev_c) == KS_SPECIAL ?  (0x80)  : (state->prev_c) == KS_ZERO ?   (-((KS_ZERO) + ((int)( ('X') ) << 8)))   :  (-((state->prev_c) + ((int)(c) << 8))) ) ;
+            if (c ==   (-((KS_EXTRA) + ((int)(KE_FOCUSGAINED) << 8)))   || c ==   (-((KS_EXTRA) + ((int)(KE_FOCUSLOST) << 8)))   || c ==   (-((KS_EXTRA) + ((int)(KE_COMPLETE_DELAY) << 8)))  )
             {
                 state->buflen = 0;
             }
         }
-        state->pending_mbyte = MB_BYTE2LEN_CHECK(c) - 1;
+        state->pending_mbyte =  (((c) < 0 || (c) > 255) ? 1 : mb_bytelen_tab[c])  - 1;
     }
 
     if (state->pending_mbyte > 0)
@@ -63736,7 +61932,7 @@
     void
 gotchars_ignore(void)
 {
-    char_u nop_buf[3] = { K_SPECIAL, KS_EXTRA, KE_IGNORE };
+    char_u nop_buf[3] = {  (0x80) , KS_EXTRA, KE_IGNORE };
     gotchars(nop_buf, 3);
 }
 
@@ -63764,14 +61960,14 @@
     static int
 alloc_typebuf(void)
 {
-    typebuf.tb_buf = alloc(TYPELEN_INIT);
-    typebuf.tb_noremap = alloc(TYPELEN_INIT);
+    typebuf.tb_buf = alloc( (5 * (MAXMAPLEN + 3)) );
+    typebuf.tb_noremap = alloc( (5 * (MAXMAPLEN + 3)) );
     if (typebuf.tb_buf == NULL || typebuf.tb_noremap == NULL)
     {
         free_typebuf();
         return FAIL;
     }
-    typebuf.tb_buflen = TYPELEN_INIT;
+    typebuf.tb_buflen =  (5 * (MAXMAPLEN + 3)) ;
     typebuf.tb_off = MAXMAPLEN + 4;
     typebuf.tb_len = 0;
     typebuf.tb_maplen = 0;
@@ -63793,7 +61989,8 @@
     }
     else
     {
-        VIM_CLEAR(typebuf.tb_buf);
+         vim_free(typebuf.tb_buf);
+         (typebuf.tb_buf) = NULL;
     }
     if (typebuf.tb_noremap == noremapbuf_init)
     {
@@ -63801,7 +61998,8 @@
     }
     else
     {
-        VIM_CLEAR(typebuf.tb_noremap);
+         vim_free(typebuf.tb_noremap);
+         (typebuf.tb_noremap) = NULL;
     }
 }
 
@@ -63853,7 +62051,7 @@
 }
 
     void
-restore_typeahead(tasave_T *tp, int overwrite UNUSED)
+restore_typeahead(tasave_T *tp, int overwrite  __attribute__((unused)) )
 {
     if (tp->typebuf_valid)
     {
@@ -63889,8 +62087,8 @@
     {
         ++curscript;
     }
-    expand_env(name, NameBuff, MAXPATHL);
-    if ((scriptin[curscript] = mch_fopen((char *)NameBuff, READBIN)) == NULL)
+    expand_env(name, NameBuff,  PATH_MAX );
+    if ((scriptin[curscript] =  fopen(((char *)NameBuff), ( "r" )) ) == NULL)
     {
         semsg(_(e_cant_open_file_str), name);
         if (curscript)
@@ -63995,7 +62193,7 @@
             c &= 0x1f;
             if (c == NUL)
             {
-                c = K_ZERO;
+                c =   (-((KS_ZERO) + ((int)( ('X') ) << 8)))  ;
             }
         }
         else if (c == '6')
@@ -64044,7 +62242,7 @@
     state.buflen = 0;
 
     ptr = state.buf;
-    if (ptr[0] == K_SPECIAL && ptr[1] == KS_MODIFIER && ptr[2] != NUL)
+    if (ptr[0] ==  (0x80)  && ptr[1] == KS_MODIFIER && ptr[2] != NUL)
     {
         modifiers = ptr[2];
         ptr += 3;
@@ -64070,7 +62268,7 @@
 
     if (modifiers != 0)
     {
-        add_to_showcmd(K_SPECIAL);
+        add_to_showcmd( (0x80) );
         add_to_showcmd(KS_MODIFIER);
         add_to_showcmd(modifiers);
     }
@@ -64128,7 +62326,7 @@
                 --allow_keys;
             }
 
-            if (c == K_SPECIAL)
+            if (c ==  (0x80) )
             {
                 int         save_allow_keys = allow_keys;
 
@@ -64143,28 +62341,28 @@
                     mod_mask = c;
                     continue;
                 }
-                c = TO_SPECIAL(c2, c);
+                c =  ((c2) == KS_SPECIAL ?  (0x80)  : (c2) == KS_ZERO ?   (-((KS_ZERO) + ((int)( ('X') ) << 8)))   :  (-((c2) + ((int)(c) << 8))) ) ;
 
-                if (allow_osc_key == 0 && c == K_OSC)
+                if (allow_osc_key == 0 && c ==   (-((KS_EXTRA) + ((int)(KE_OSC) << 8)))  )
                 {
                     continue;
                 }
 
-                if (c == K_ESC)
+                if (c ==   (-((KS_EXTRA) + ((int)(KE_ESC) << 8)))  )
                 {
                     c = ESC;
                 }
 
             }
 
-            if (has_mbyte && (n = MB_BYTE2LEN_CHECK(c)) > 1)
+            if (has_mbyte && (n =  (((c) < 0 || (c) > 255) ? 1 : mb_bytelen_tab[c]) ) > 1)
             {
                 ++no_mapping;
                 buf[0] = c;
                 for (i = 1; i < n; ++i)
                 {
                     buf[i] = vgetorpeek(TRUE);
-                    if (buf[i] == K_SPECIAL)
+                    if (buf[i] ==  (0x80) )
                     {
                         c = vgetorpeek(TRUE);
                         if (vgetorpeek(TRUE) == KE_CSI && c == KS_EXTRA)
@@ -64185,101 +62383,101 @@
 
             switch (c)
             {
-                case K_KPLUS:
+                case   (-(('K') + ((int)('6') << 8)))  :
                     c = '+';
                     break;
-                case K_KMINUS:
+                case   (-(('K') + ((int)('7') << 8)))  :
                     c = '-';
                     break;
-                case K_KDIVIDE:
+                case   (-(('K') + ((int)('8') << 8)))  :
                     c = '/';
                     break;
-                case K_KMULTIPLY:
+                case   (-(('K') + ((int)('9') << 8)))  :
                     c = '*';
                     break;
-                case K_KENTER:
+                case   (-(('K') + ((int)('A') << 8)))  :
                     c = CAR;
                     break;
-                case K_KPOINT:
+                case   (-(('K') + ((int)('B') << 8)))  :
                                 c = '.';
                                 break;
-                case K_K0:
+                case   (-(('K') + ((int)('C') << 8)))  :
                     c = '0';
                     break;
-                case K_K1:
+                case   (-(('K') + ((int)('D') << 8)))  :
                     c = '1';
                     break;
-                case K_K2:
+                case   (-(('K') + ((int)('E') << 8)))  :
                     c = '2';
                     break;
-                case K_K3:
+                case   (-(('K') + ((int)('F') << 8)))  :
                     c = '3';
                     break;
-                case K_K4:
+                case   (-(('K') + ((int)('G') << 8)))  :
                     c = '4';
                     break;
-                case K_K5:
+                case   (-(('K') + ((int)('H') << 8)))  :
                     c = '5';
                     break;
-                case K_K6:
+                case   (-(('K') + ((int)('I') << 8)))  :
                     c = '6';
                     break;
-                case K_K7:
+                case   (-(('K') + ((int)('J') << 8)))  :
                     c = '7';
                     break;
-                case K_K8:
+                case   (-(('K') + ((int)('K') << 8)))  :
                     c = '8';
                     break;
-                case K_K9:
+                case   (-(('K') + ((int)('L') << 8)))  :
                     c = '9';
                     break;
 
-                case K_XHOME:
-                case K_ZHOME:
+                case   (-((KS_EXTRA) + ((int)(KE_XHOME) << 8)))  :
+                case   (-((KS_EXTRA) + ((int)(KE_ZHOME) << 8)))  :
                     if (mod_mask == MOD_MASK_SHIFT)
                                 {
-                                    c = K_S_HOME;
+                                    c =   (-(('#') + ((int)('2') << 8)))  ;
                                     mod_mask = 0;
                                 }
                                 else if (mod_mask == MOD_MASK_CTRL)
                                 {
-                                    c = K_C_HOME;
+                                    c =   (-((KS_EXTRA) + ((int)(KE_C_HOME) << 8)))  ;
                                     mod_mask = 0;
                                 }
                                 else
                                 {
-                                    c = K_HOME;
+                                    c =   (-(('k') + ((int)('h') << 8)))  ;
                                 }
                                 break;
-                case K_XEND:
-                case K_ZEND:
+                case   (-((KS_EXTRA) + ((int)(KE_XEND) << 8)))  :
+                case   (-((KS_EXTRA) + ((int)(KE_ZEND) << 8)))  :
                     if (mod_mask == MOD_MASK_SHIFT)
                                 {
-                                    c = K_S_END;
+                                    c =   (-(('*') + ((int)('7') << 8)))  ;
                                     mod_mask = 0;
                                 }
                                 else if (mod_mask == MOD_MASK_CTRL)
                                 {
-                                    c = K_C_END;
+                                    c =   (-((KS_EXTRA) + ((int)(KE_C_END) << 8)))  ;
                                     mod_mask = 0;
                                 }
                                 else
                                 {
-                                    c = K_END;
+                                    c =   (-(('@') + ((int)('7') << 8)))  ;
                                 }
                                 break;
 
-                case K_XUP:
-                    c = K_UP;
+                case   (-((KS_EXTRA) + ((int)(KE_XUP) << 8)))  :
+                    c =   (-(('k') + ((int)('u') << 8)))  ;
                     break;
-                case K_XDOWN:
-                    c = K_DOWN;
+                case   (-((KS_EXTRA) + ((int)(KE_XDOWN) << 8)))  :
+                    c =   (-(('k') + ((int)('d') << 8)))  ;
                     break;
-                case K_XLEFT:
-                    c = K_LEFT;
+                case   (-((KS_EXTRA) + ((int)(KE_XLEFT) << 8)))  :
+                    c =   (-(('k') + ((int)('l') << 8)))  ;
                     break;
-                case K_XRIGHT:
-                    c = K_RIGHT;
+                case   (-((KS_EXTRA) + ((int)(KE_XRIGHT) << 8)))  :
+                    c =   (-(('k') + ((int)('r') << 8)))  ;
                     break;
             }
 
@@ -64289,7 +62487,7 @@
         last_vgetc_recorded_len = last_recorded_len;
     }
 
-    if (c != K_IGNORE)
+    if (c !=   (-((KS_EXTRA) + ((int)(KE_IGNORE) << 8)))  )
     {
         state_no_longer_safe("key typed");
     }
@@ -64319,7 +62517,7 @@
     {
         c = safe_vgetc();
     }
-    while (c == K_IGNORE || c == K_VER_SCROLLBAR || c == K_HOR_SCROLLBAR || c == K_MOUSEMOVE)
+    while (c ==   (-((KS_EXTRA) + ((int)(KE_IGNORE) << 8)))   || c ==   (-((KS_VER_SCROLLBAR) + ((int)( ('X') ) << 8)))   || c ==   (-((KS_HOR_SCROLLBAR) + ((int)( ('X') ) << 8)))   || c ==   (-((KS_EXTRA) + ((int)(KE_MOUSEMOVE) << 8)))  )
         ;
     return c;
 }
@@ -64329,7 +62527,7 @@
 {
     int c = plain_vgetc_nopaste();
 
-    if (c == K_PS)
+    if (c ==   (-(('P') + ((int)('S') << 8)))  )
     {
         c = bracketed_paste(PASTE_ONE_CHAR, FALSE, NULL);
     }
@@ -64384,7 +62582,7 @@
     char_u  *p = typebuf.tb_buf + typebuf.tb_off;
     int     c = *p;
 
-    if (typebuf.tb_len > 3 && (c == K_SPECIAL || c == CSI) && p[1] == KS_MODIFIER && (p[2] & MOD_MASK_CTRL) && !(p[2] & MOD_MASK_SHIFT))
+    if (typebuf.tb_len > 3 && (c ==  (0x80)  || c == CSI) && p[1] == KS_MODIFIER && (p[2] & MOD_MASK_CTRL) && !(p[2] & MOD_MASK_SHIFT))
     {
         c = p[3] & 0x1f;
     }
@@ -64405,7 +62603,7 @@
             break;
         }
         tp = typebuf.tb_buf + typebuf.tb_off + offset;
-        if ((tp[0] == K_SPECIAL || tp[0] == CSI) && tp[1] == KS_MODIFIER)
+        if ((tp[0] ==  (0x80)  || tp[0] == CSI) && tp[1] == KS_MODIFIER)
         {
             int modifier = tp[2];
             int c = tp[3];
@@ -64422,11 +62620,11 @@
                     vgetc_char = c;
                     vgetc_mod_mask = tp[2];
                 }
-                if (IS_SPECIAL(new_c))
+                if ( ((new_c) < 0) )
                 {
-                    new_string[0] = K_SPECIAL;
-                    new_string[1] = K_SECOND(new_c);
-                    new_string[2] = K_THIRD(new_c);
+                    new_string[0] =  (0x80) ;
+                    new_string[1] =  ((new_c) ==  (0x80)  ? KS_SPECIAL : (new_c) == NUL ? KS_ZERO :  ((-(new_c)) & 0xff) ) ;
+                    new_string[2] =  (((new_c) ==  (0x80)  || (new_c) == NUL) ?  ('X')  :  (((unsigned)(-(new_c)) >> 8) & 0xff) ) ;
                     len = 3;
                 }
                 else
@@ -64488,13 +62686,13 @@
     int         is_plug_map = FALSE;
     bool        in_osc = in_osc_sequence();
 
-    if (!in_osc && typebuf.tb_len >= 3 && typebuf.tb_buf[typebuf.tb_off] == K_SPECIAL && typebuf.tb_buf[typebuf.tb_off + 1] == KS_EXTRA && typebuf.tb_buf[typebuf.tb_off + 2] == KE_PLUG)
+    if (!in_osc && typebuf.tb_len >= 3 && typebuf.tb_buf[typebuf.tb_off] ==  (0x80)  && typebuf.tb_buf[typebuf.tb_off + 1] == KS_EXTRA && typebuf.tb_buf[typebuf.tb_off + 2] == KE_PLUG)
     {
         is_plug_map = TRUE;
     }
 
     tb_c1 = typebuf.tb_buf[typebuf.tb_off];
-    if (!in_osc && no_mapping == 0 && is_maphash_valid() && (no_zero_mapping == 0 || tb_c1 != '0') && (typebuf.tb_maplen == 0 || is_plug_map || (p_remap && (typebuf.tb_noremap[typebuf.tb_off] & (RM_NONE|RM_ABBR)) == 0)) && !(p_paste && (State & (MODE_INSERT | MODE_CMDLINE))) && !(State == MODE_HITRETURN && (tb_c1 == CAR || tb_c1 == ' ')) && State != MODE_ASKMORE && State != MODE_CONFIRM && !at_ins_compl_key())
+    if (!in_osc && no_mapping == 0 && is_maphash_valid() && (no_zero_mapping == 0 || tb_c1 != '0') && (typebuf.tb_maplen == 0 || is_plug_map || (p_remap && (typebuf.tb_noremap[typebuf.tb_off] & (RM_NONE|RM_ABBR)) == 0)) && !(p_paste && (State & (MODE_INSERT | MODE_CMDLINE))) && !(State ==  (0x2000 | MODE_NORMAL)  && (tb_c1 == CAR || tb_c1 == ' ')) && State != MODE_ASKMORE && State != MODE_CONFIRM && !at_ins_compl_key())
     {
         mp = get_buf_maphash_list(local_State, tb_c1);
         mp2 = get_maphash_list(local_State, tb_c1);
@@ -64523,7 +62721,7 @@
                     char_u *p1 = mp->m_keys;
                     char_u *p2 = mb_unescape(&p1);
 
-                    if (has_mbyte && p2 != NULL && MB_BYTE2LEN(tb_c1) > mb_ptr2len(p2))
+                    if (has_mbyte && p2 != NULL &&  mb_bytelen_tab[tb_c1]  > mb_ptr2len(p2))
                     {
                         mlen = 0;
                     }
@@ -64536,7 +62734,7 @@
                     int     n;
 
                     s = typebuf.tb_noremap + typebuf.tb_off;
-                    if ((*s & ~RM_SIMPLIFIED) == RM_SCRIPT && (mp->m_keys[0] != K_SPECIAL || mp->m_keys[1] != KS_EXTRA || mp->m_keys[2] != KE_SNR))
+                    if ((*s & ~RM_SIMPLIFIED) == RM_SCRIPT && (mp->m_keys[0] !=  (0x80)  || mp->m_keys[1] != KS_EXTRA || mp->m_keys[2] != KE_SNR))
                     {
                         continue;
                     }
@@ -64557,7 +62755,7 @@
                     {
                         if (!*timedout && !(mp_match != NULL && mp_match->m_nowait))
                         {
-                            keylen = KEYLEN_PART_MAP;
+                            keylen =  (-2) ;
                             break;
                         }
                     }
@@ -64569,18 +62767,17 @@
                 }
                 else
                 {
-
                     if (max_mlen < mlen)
                     {
                         max_mlen = mlen;
-                        want_termcode = mp->m_keys[mlen] == K_SPECIAL;
+                        want_termcode = mp->m_keys[mlen] ==  (0x80) ;
                     }
-                    else if (max_mlen == mlen && mp->m_keys[mlen] == K_SPECIAL)
+                    else if (max_mlen == mlen && mp->m_keys[mlen] ==  (0x80) )
                     {
                         want_termcode = 1;
                     }
 
-                    if (ASCII_ISUPPER(mp->m_keys[mlen]))
+                    if ( ((unsigned)(mp->m_keys[mlen]) - 'A' < 26) )
                     {
                         want_termcode = 1;
                     }
@@ -64588,7 +62785,7 @@
             }
         }
 
-        if (keylen != KEYLEN_PART_MAP && mp_match != NULL)
+        if (keylen !=  (-2)  && mp_match != NULL)
         {
             mp = mp_match;
             keylen = mp_match_len;
@@ -64628,7 +62825,7 @@
         }
         if (mlen == typebuf.tb_len)
         {
-            keylen = KEYLEN_PART_KEY;
+            keylen =  (-1) ;
         }
         else if (max_mlen < mlen)
         {
@@ -64636,7 +62833,7 @@
         }
     }
 
-    if (in_osc || ((mp == NULL || max_mlen + want_termcode > mp_match_len || (mp_match_len == 1 && *mp->m_keys == ESC && !*timedout)) && keylen != KEYLEN_PART_MAP))
+    if (in_osc || ((mp == NULL || max_mlen + want_termcode > mp_match_len || (mp_match_len == 1 && *mp->m_keys == ESC && !*timedout)) && keylen !=  (-2) ))
     {
         int     save_keylen = keylen;
 
@@ -64651,9 +62848,9 @@
                 keylen = 0;
             }
 
-            if (keylen == 0 && save_keylen == KEYLEN_PART_KEY && !*timedout)
+            if (keylen == 0 && save_keylen ==  (-1)  && !*timedout)
             {
-                keylen = KEYLEN_PART_KEY;
+                keylen =  (-1) ;
             }
 
             if (keylen == 0 && !no_reduce_keys)
@@ -64691,7 +62888,7 @@
 
         if (mp == NULL || keylen < 0)
         {
-            keylen = KEYLEN_PART_KEY;
+            keylen =  (-1) ;
         }
         else
         {
@@ -64703,9 +62900,6 @@
     {
         char_u *map_str;
 
-# define save_m_noremap mp->m_noremap
-# define save_m_silent mp->m_silent
-
         if (keylen > typebuf.tb_maplen)
         {
             gotchars(typebuf.tb_buf + typebuf.tb_off + typebuf.tb_maplen, keylen - typebuf.tb_maplen);
@@ -64734,7 +62928,7 @@
         if (VIsual_active && VIsual_select && (mp->m_mode & MODE_VISUAL))
         {
             VIsual_select = FALSE;
-            (void)ins_typebuf(K_SELECT_STRING, REMAP_NONE, 0, TRUE, FALSE);
+            (void)ins_typebuf( (char_u *)"\200\365X" ,  (-1) , 0, TRUE, FALSE);
         }
 
             map_str = mp->m_str;
@@ -64747,19 +62941,19 @@
         {
             int noremap;
 
-            if (save_m_noremap != REMAP_YES)
+            if ( mp->m_noremap  != REMAP_YES)
             {
-                noremap = save_m_noremap;
+                noremap =  mp->m_noremap ;
             }
-            else if (STRNCMP(map_str, mp->m_keys, (size_t)keylen) == 0 || (mp->m_alt != NULL && STRNCMP(map_str, mp->m_alt->m_keys, (size_t)mp->m_alt->m_keylen) == 0))
+            else if ( strncmp((char *)(map_str), (char *)(mp->m_keys), ((size_t)keylen))  == 0 || (mp->m_alt != NULL &&  strncmp((char *)(map_str), (char *)(mp->m_alt->m_keys), ((size_t)mp->m_alt->m_keylen))  == 0))
             {
-                noremap = REMAP_SKIP;
+                noremap =  (-3) ;
             }
             else
             {
                 noremap = REMAP_YES;
             }
-            i = ins_typebuf(map_str, noremap, 0, TRUE, cmd_silent || save_m_silent);
+            i = ins_typebuf(map_str, noremap, 0, TRUE, cmd_silent ||  mp->m_silent );
         }
         *keylenp = keylen;
         if (i == FAIL)
@@ -64935,7 +63129,7 @@
                 c = 0;
                 new_wcol = curwin->w_wcol;
                 new_wrow = curwin->w_wrow;
-                if (       advance && typebuf.tb_len == 1 && typebuf.tb_buf[typebuf.tb_off] == ESC && !no_mapping && kitty_protocol_state != KKPS_ENABLED && ex_normal_busy == 0 && typebuf.tb_maplen == 0 && (State & MODE_INSERT) && (p_timeout || (keylen == KEYLEN_PART_KEY && p_ttimeout)) && (c = inchar(typebuf.tb_buf + typebuf.tb_off + typebuf.tb_len, 3, 25L)) == 0)
+                if (       advance && typebuf.tb_len == 1 && typebuf.tb_buf[typebuf.tb_off] == ESC && !no_mapping && kitty_protocol_state != KKPS_ENABLED && ex_normal_busy == 0 && typebuf.tb_maplen == 0 && (State & MODE_INSERT) && (p_timeout || (keylen ==  (-1)  && p_ttimeout)) && (c = inchar(typebuf.tb_buf + typebuf.tb_off + typebuf.tb_len, 3, 25L)) == 0)
                 {
                     colnr_T     col = 0;
                     char_u      *ptr;
@@ -64962,7 +63156,7 @@
                                 init_chartabsize_arg(&cts, curwin, curwin->w_cursor.lnum, 0, ptr, ptr);
                                 while (cts.cts_ptr < ptr + curwin->w_cursor.col)
                                 {
-                                    if (!VIM_ISWHITE(*cts.cts_ptr))
+                                    if (! ((*cts.cts_ptr) == ' ' || (*cts.cts_ptr) == '\t') )
                                     {
                                         curwin->w_wcol = cts.cts_vcol;
                                     }
@@ -64991,7 +63185,7 @@
                                 col = curwin->w_cursor.col - 1;
                             }
                         }
-                        else if (curwin->w_p_wrap && curwin->w_wrow)
+                        else if (curwin-> w_onebuf_opt.wo_wrap  && curwin->w_wrow)
                         {
                             --curwin->w_wrow;
                             curwin->w_wcol = curwin->w_width - 1;
@@ -65079,7 +63273,7 @@
                 int showing_partial = FALSE;
                 if (typebuf.tb_len > 0 && advance && !exmode_active)
                 {
-                    if (((State & (MODE_NORMAL | MODE_INSERT)) || State == MODE_LANGMAP) && State != MODE_HITRETURN)
+                    if (((State & (MODE_NORMAL | MODE_INSERT)) || State == MODE_LANGMAP) && State !=  (0x2000 | MODE_NORMAL) )
                     {
                         if (State & MODE_INSERT && ptr2cells(typebuf.tb_buf + typebuf.tb_off + typebuf.tb_len - 1) == 1)
                         {
@@ -65118,11 +63312,11 @@
 
                 if (advance)
                 {
-                    if (typebuf.tb_len == 0 || !(p_timeout || (p_ttimeout && keylen == KEYLEN_PART_KEY)))
+                    if (typebuf.tb_len == 0 || !(p_timeout || (p_ttimeout && keylen ==  (-1) )))
                     {
                         wait_time = -1L;
                     }
-                    else if (keylen == KEYLEN_PART_KEY && p_ttm >= 0)
+                    else if (keylen ==  (-1)  && p_ttm >= 0)
                     {
                         wait_time = p_ttm;
                     }
@@ -65185,7 +63379,7 @@
             }
         }
 
-    } while ((c < 0 && c != K_CANCEL) || (advance && c == NUL));
+    } while ((c < 0 && c !=   (-((KS_EXTRA) + ((int)(KE_CANCEL) << 8)))  ) || (advance && c == NUL));
 
     if (advance && p_smd && msg_silent == 0 && (State & MODE_INSERT))
     {
@@ -65236,7 +63430,7 @@
         out_flush_cursor(FALSE, FALSE);
     }
 
-    if (State != MODE_HITRETURN)
+    if (State !=  (0x2000 | MODE_NORMAL) )
     {
         did_outofmem_msg = FALSE;
         did_swapwrite_msg = FALSE;
@@ -65246,7 +63440,6 @@
     script_char = -1;
     while (scriptin[curscript] != NULL && script_char < 0)
     {
-
         if (got_int || (script_char = getc(scriptin[curscript])) < 0)
         {
             closescript();
@@ -65270,12 +63463,11 @@
     {
         if (got_int)
         {
-#define DUM_LEN (MAXMAPLEN * 3 + 3)
-            char_u      dum[DUM_LEN + 1];
+            char_u      dum[ (MAXMAPLEN * 3 + 3)  + 1];
 
             for (;;)
             {
-                len = ui_inchar(dum, DUM_LEN, 0L, 0);
+                len = ui_inchar(dum,  (MAXMAPLEN * 3 + 3) , 0L, 0);
                 if (len == 0 || (len == 1 && dum[0] == Ctrl_C))
                 {
                     break;
@@ -65313,12 +63505,12 @@
 
     for (i = len; --i >= 0; ++p)
     {
-        if (p[0] == NUL || (p[0] == K_SPECIAL && (i < 2 || p[1] != KS_EXTRA || (p[2] != (int)KE_CURSORHOLD && p[2] != (int)KE_COMPLETE_DELAY))))
+        if (p[0] == NUL || (p[0] ==  (0x80)  && (i < 2 || p[1] != KS_EXTRA || (p[2] != (int)KE_CURSORHOLD && p[2] != (int)KE_COMPLETE_DELAY))))
         {
-            mch_memmove(p + 3, p + 1, (size_t)i);
-            p[2] = K_THIRD(p[0]);
-            p[1] = K_SECOND(p[0]);
-            p[0] = K_SPECIAL;
+             memmove((char *)(p + 3), (char *)(p + 1), (size_t)i) ;
+            p[2] =  (((p[0]) ==  (0x80)  || (p[0]) == NUL) ?  ('X')  :  (((unsigned)(-(p[0])) >> 8) & 0xff) ) ;
+            p[1] =  ((p[0]) ==  (0x80)  ? KS_SPECIAL : (p[0]) == NUL ? KS_ZERO :  ((-(p[0])) & 0xff) ) ;
+            p[0] =  (0x80) ;
             p += 2;
             len += 2;
         }
@@ -65334,7 +63526,7 @@
 }
 
     static char_u *
-getcmdkeycmd(int             promptc UNUSED, void            *cookie UNUSED, int             indent UNUSED, getline_opt_T   do_concat UNUSED)
+getcmdkeycmd(int             promptc  __attribute__((unused)) , void            *cookie  __attribute__((unused)) , int             indent  __attribute__((unused)) , getline_opt_T   do_concat  __attribute__((unused)) )
 {
     garray_T    line_ga;
     int         c1 = -1;
@@ -65364,7 +63556,7 @@
 
         c1 = vgetorpeek(TRUE);
 
-        if (c1 == K_SPECIAL)
+        if (c1 ==  (0x80) )
         {
             c1 = vgetorpeek(TRUE);
             c2 = vgetorpeek(TRUE);
@@ -65373,9 +63565,9 @@
                 cmod = c2;
                 continue;
             }
-            c1 = TO_SPECIAL(c1, c2);
+            c1 =  ((c1) == KS_SPECIAL ?  (0x80)  : (c1) == KS_ZERO ?   (-((KS_ZERO) + ((int)( ('X') ) << 8)))   :  (-((c1) + ((int)(c2) << 8))) ) ;
 
-            if (c1 == K_ESC)
+            if (c1 ==   (-((KS_EXTRA) + ((int)(KE_ESC) << 8)))  )
             {
                 c1 = ESC;
             }
@@ -65394,28 +63586,28 @@
         {
             aborted = TRUE;
         }
-        else if (c1 == K_COMMAND || c1 == K_SCRIPT_COMMAND)
+        else if (c1 ==   (-((KS_EXTRA) + ((int)(KE_COMMAND) << 8)))   || c1 ==   (-((KS_EXTRA) + ((int)(KE_SCRIPT_COMMAND) << 8)))  )
         {
             emsg(_(e_cmd_mapping_must_end_with_cr_before_second_cmd));
             aborted = TRUE;
         }
-        else if (c1 == K_SNR)
+        else if (c1 ==   (-((KS_EXTRA) + ((int)(KE_SNR) << 8)))  )
         {
-            GA_CONCAT_LITERAL(&line_ga, "<SNR>");
+             ga_concat_len((&line_ga), (char_u *)("<SNR>"),  (sizeof("<SNR>" "") - 1) ) ;
         }
         else
         {
             if (cmod != 0)
             {
-                ga_append(&line_ga, K_SPECIAL);
+                ga_append(&line_ga,  (0x80) );
                 ga_append(&line_ga, KS_MODIFIER);
                 ga_append(&line_ga, cmod);
             }
-            if (IS_SPECIAL(c1))
+            if ( ((c1) < 0) )
             {
-                ga_append(&line_ga, K_SPECIAL);
-                ga_append(&line_ga, K_SECOND(c1));
-                ga_append(&line_ga, K_THIRD(c1));
+                ga_append(&line_ga,  (0x80) );
+                ga_append(&line_ga,  ((c1) ==  (0x80)  ? KS_SPECIAL : (c1) == NUL ? KS_ZERO :  ((-(c1)) & 0xff) ) );
+                ga_append(&line_ga,  (((c1) ==  (0x80)  || (c1) == NUL) ?  ('X')  :  (((unsigned)(-(c1)) >> 8) & 0xff) ) );
             }
             else
             {
@@ -65437,7 +63629,7 @@
 }
 
     int
-do_cmdkey_command(int key UNUSED, int flags)
+do_cmdkey_command(int key  __attribute__((unused)) , int flags)
 {
     int     res;
 
@@ -65448,14 +63640,14 @@
 
 // ==================== hashtab.c ====================
 
-#define PERTURB_SHIFT 5
+enum { PERTURB_SHIFT = 5 };
 
 static int hash_may_resize(hashtab_T *ht, int minitems);
 
     void
 hash_init(hashtab_T *ht)
 {
-    CLEAR_POINTER(ht);
+      memset(((ht)), (0), (sizeof(*(ht))))  ;
     ht->ht_array = ht->ht_smallarray;
     ht->ht_mask = HT_INIT_SIZE - 1;
 }
@@ -65488,9 +63680,9 @@
     hashitem_T  *hi;
 
     todo = (long)ht->ht_used;
-    FOR_ALL_HASHTAB_ITEMS(ht, hi, todo)
+     for ((hi) = (ht)->ht_array; (todo) > 0; ++(hi)) 
     {
-        if (!HASHITEM_EMPTY(hi))
+        if (! ((hi)->hi_key == NULL || (hi)->hi_key == &hash_removed) )
         {
             vim_free(hi->hi_key - off);
             --todo;
@@ -65520,11 +63712,11 @@
     {
         return hi;
     }
-    if (hi->hi_key == HI_KEY_REMOVED)
+    if (hi->hi_key ==  &hash_removed )
     {
         freeitem = hi;
     }
-    else if (hi->hi_hash == hash && STRCMP(hi->hi_key, key) == 0)
+    else if (hi->hi_hash == hash &&  strcmp((char *)(hi->hi_key), (char *)(key))  == 0)
     {
         return hi;
     }
@@ -65541,11 +63733,11 @@
         {
             return freeitem == NULL ? hi : freeitem;
         }
-        if (hi->hi_hash == hash && hi->hi_key != HI_KEY_REMOVED && STRCMP(hi->hi_key, key) == 0)
+        if (hi->hi_hash == hash && hi->hi_key !=  &hash_removed  &&  strcmp((char *)(hi->hi_key), (char *)(key))  == 0)
         {
             return hi;
         }
-        if (hi->hi_key == HI_KEY_REMOVED && freeitem == NULL)
+        if (hi->hi_key ==  &hash_removed  && freeitem == NULL)
         {
             freeitem = hi;
         }
@@ -65563,7 +63755,7 @@
         return FAIL;
     }
     hi = hash_lookup(ht, key, hash);
-    if (!HASHITEM_EMPTY(hi))
+    if (! ((hi)->hi_key == NULL || (hi)->hi_key == &hash_removed) )
     {
         internal_error("hash_add()");
         return FAIL;
@@ -65600,13 +63792,11 @@
     }
     --ht->ht_used;
     ++ht->ht_changed;
-    hi->hi_key = HI_KEY_REMOVED;
+    hi->hi_key =  &hash_removed ;
     hash_may_resize(ht, 0);
     return OK;
 }
 
-
-
     static int
 hash_may_resize(hashtab_T   *ht, int         minitems)
 {
@@ -65673,14 +63863,14 @@
         newarray = ht->ht_smallarray;
         if (ht->ht_array == newarray)
         {
-            mch_memmove(temparray, newarray, sizeof(temparray));
+             memmove((char *)(temparray), (char *)(newarray), sizeof(temparray)) ;
             oldarray = temparray;
         }
         else
         {
             oldarray = ht->ht_array;
         }
-        CLEAR_FIELD(ht->ht_smallarray);
+          memset((&(ht->ht_smallarray)), (0), (sizeof(ht->ht_smallarray)))  ;
     }
 
     else if (newsize == oldsize && ht->ht_filled * 3 < oldsize * 2)
@@ -65690,7 +63880,7 @@
 
     else
     {
-        newarray = ALLOC_CLEAR_MULT(hashitem_T, newsize);
+        newarray =  (hashitem_T *)alloc_clear(sizeof(hashitem_T) * (newsize)) ;
         if (newarray == NULL)
         {
             if (ht->ht_filled < ht->ht_mask)
@@ -65707,7 +63897,7 @@
     todo = (int)ht->ht_used;
     for (olditem = oldarray; todo > 0; ++olditem)
     {
-        if (!HASHITEM_EMPTY(olditem))
+        if (! ((olditem)->hi_key == NULL || (olditem)->hi_key == &hash_removed) )
         {
             newi = (unsigned)(olditem->hi_hash & newmask);
             newitem = &newarray[newi];
@@ -65814,8 +64004,8 @@
         arg = (char_u *)"";
     }
 
-    p = arg + STRLEN(arg) - 1;
-    while (p > arg && VIM_ISWHITE(*p) && p[-1] != '\\')
+    p = arg +  strlen((char *)(arg))  - 1;
+    while (p > arg &&  ((*p) == ' ' || (*p) == '\t')  && p[-1] != '\\')
     {
         *p-- = NUL;
     }
@@ -65849,11 +64039,13 @@
         }
         else
         {
-            FOR_ALL_WINDOWS(wp)
+             for ((wp) = firstwin; (wp) != NULL; (wp) = (wp)->w_next) 
+             {
                 if (bt_help(wp->w_buffer))
                 {
                     break;
                 }
+             }
         }
         if (wp != NULL && wp->w_buffer->b_nwindows > 0)
         {
@@ -65861,7 +64053,7 @@
         }
         else
         {
-            if ((helpfd = mch_fopen((char *)p_hf, READBIN)) == NULL)
+            if ((helpfd =  fopen(((char *)p_hf), ( "r" )) ) == NULL)
             {
                 smsg(_("Sorry, help file \"%s\" not found"), p_hf);
                 goto erret;
@@ -65884,7 +64076,7 @@
             }
 
             alt_fnum = curbuf->b_fnum;
-            (void)do_ecmd(0, NULL, NULL, NULL, ECMD_LASTL, ECMD_HIDE + ECMD_SET_HELP, NULL);
+            (void)do_ecmd(0, NULL, NULL, NULL,  (linenr_T)0 , ECMD_HIDE + ECMD_SET_HELP, NULL);
             if ((cmdmod.cmod_flags & CMOD_KEEPALT) == 0)
             {
                 curwin->w_alt_fnum = alt_fnum;
@@ -65922,11 +64114,11 @@
 }
 
     void
-ex_helpclose(exarg_T *eap UNUSED)
+ex_helpclose(exarg_T *eap  __attribute__((unused)) )
 {
     win_T *win;
 
-    FOR_ALL_WINDOWS(win)
+     for ((win) = firstwin; (win) != NULL; (win) = (win)->w_next) 
     {
         if (bt_help(win->w_buffer))
         {
@@ -65945,13 +64137,13 @@
     num_letters = 0;
     for (p = matched_string; *p; p++)
     {
-        if (ASCII_ISALNUM(*p))
+        if ( ( ( ((unsigned)(*p) - 'A' < 26)  ||  ((unsigned)(*p) - 'a' < 26) )  ||  ((unsigned)(*p) - '0' < 10) ) )
         {
             num_letters++;
         }
     }
 
-    if (ASCII_ISALNUM(matched_string[offset]) && offset > 0 && ASCII_ISALNUM(matched_string[offset - 1]))
+    if ( ( ( ((unsigned)(matched_string[offset]) - 'A' < 26)  ||  ((unsigned)(matched_string[offset]) - 'a' < 26) )  ||  ((unsigned)(matched_string[offset]) - '0' < 10) )  && offset > 0 &&  ( ( ((unsigned)(matched_string[offset - 1]) - 'A' < 26)  ||  ((unsigned)(matched_string[offset - 1]) - 'a' < 26) )  ||  ((unsigned)(matched_string[offset - 1]) - '0' < 10) ) )
     {
         offset += 10000;
     }
@@ -65967,7 +64159,7 @@
     {
         offset += 100;
     }
-    return (int)(100 * num_letters + STRLEN(matched_string) + offset);
+    return (int)(100 * num_letters +  strlen((char *)(matched_string))  + offset);
 }
 
     static int
@@ -66045,11 +64237,11 @@
     d = IObuff;
     d[0] = NUL;
 
-    if (STRNICMP(arg, "expr-", 5) == 0)
+    if ( strncasecmp((char *)(arg), (char *)("expr-"), (5))  == 0)
     {
-        for (i = (int)ARRAY_LENGTH(expr_table); --i >= 0; )
+        for (i = (int) (sizeof(expr_table) / sizeof((expr_table)[0])) ; --i >= 0; )
         {
-            if (STRCMP(arg + 5, expr_table[i]) == 0)
+            if ( strcmp((char *)(arg + 5), (char *)(expr_table[i]))  == 0)
             {
                 int si = 0;
                 int di = 0;
@@ -66075,9 +64267,9 @@
     {
         for (i = 0; except_tbl[i][0] != NULL; ++i)
         {
-            if (STRCMP(arg, except_tbl[i][0]) == 0)
+            if ( strcmp((char *)(arg), (char *)(except_tbl[i][0]))  == 0)
             {
-                STRCPY(d, except_tbl[i][1]);
+                 strcpy((char *)(d), (char *)(except_tbl[i][1])) ;
                 break;
             }
         }
@@ -66087,10 +64279,10 @@
     {
         if (arg[0] == '\\' && ((arg[1] != NUL && arg[2] == NUL) || (vim_strchr((char_u *)"%_z@", arg[1]) != NULL && arg[2] != NUL)))
         {
-            vim_snprintf((char *)d, IOSIZE, "/\\\\%s", arg + 1);
+            vim_snprintf((char *)d,  (1024+1) , "/\\\\%s", arg + 1);
             if (d[3] == '_' && d[4] == '$')
             {
-                STRCPY(d + 4, "\\$");
+                 strcpy((char *)(d + 4), (char *)("\\$")) ;
             }
         }
         else
@@ -66106,18 +64298,18 @@
           }
           for (s = arg; *s; ++s)
           {
-            if (d - IObuff > IOSIZE - 10)
+            if (d - IObuff >  (1024+1)  - 10)
             {
                 break;
             }
             switch (*s)
             {
                 case '|':
-                    STRCPY(d, "bar");
+                     strcpy((char *)(d), (char *)("bar")) ;
                             d += 3;
                             continue;
                 case '"':
-                    STRCPY(d, "quote");
+                     strcpy((char *)(d), (char *)("quote")) ;
                             d += 5;
                             continue;
                 case '*':
@@ -66133,13 +64325,13 @@
                             break;
             }
 
-            if (*s < ' ' || (*s == '^' && s[1] && (ASCII_ISALPHA(s[1]) || vim_strchr((char_u *)"?@[\\]^", s[1]) != NULL)))
+            if (*s < ' ' || (*s == '^' && s[1] && ( ( ((unsigned)(s[1]) - 'A' < 26)  ||  ((unsigned)(s[1]) - 'a' < 26) )  || vim_strchr((char_u *)"?@[\\]^", s[1]) != NULL)))
             {
                 if (d > IObuff && d[-1] != '_' && d[-1] != '\\')
                 {
                     *d++ = '_';
                 }
-                STRCPY(d, "CTRL-");
+                 strcpy((char *)(d), (char *)("CTRL-")) ;
                 d += 5;
                 if (*s < ' ')
                 {
@@ -66169,9 +64361,9 @@
                 *d++ = '\\';
             }
 
-            if (STRNICMP(s, "CTRL-\\_", 7) == 0)
+            if ( strncasecmp((char *)(s), (char *)("CTRL-\\_"), (7))  == 0)
             {
-                STRCPY(d, "CTRL-\\\\");
+                 strcpy((char *)(d), (char *)("CTRL-\\\\")) ;
                 d += 7;
                 s += 6;
             }
@@ -66198,17 +64390,17 @@
           {
               if (d > IObuff + 2 && d[-1] == '`')
               {
-                  mch_memmove(IObuff, IObuff + 1, STRLEN(IObuff));
+                   memmove((char *)(IObuff), (char *)(IObuff + 1),  strlen((char *)(IObuff)) ) ;
                   d[-2] = NUL;
               }
               else if (d > IObuff + 3 && d[-2] == '`' && d[-1] == ',')
               {
-                  mch_memmove(IObuff, IObuff + 1, STRLEN(IObuff));
+                   memmove((char *)(IObuff), (char *)(IObuff + 1),  strlen((char *)(IObuff)) ) ;
                   d[-3] = NUL;
               }
               else if (d > IObuff + 4 && d[-3] == '`' && d[-2] == '\\' && d[-1] == '.')
               {
-                  mch_memmove(IObuff, IObuff + 1, STRLEN(IObuff));
+                   memmove((char *)(IObuff), (char *)(IObuff + 1),  strlen((char *)(IObuff)) ) ;
                   d[-4] = NUL;
               }
           }
@@ -66241,7 +64433,7 @@
     curbuf->b_help = true;
 
     p = (char_u *)"!-~,^*,^|,^\",192-255";
-    if (STRCMP(curbuf->b_p_isk, p) != 0)
+    if ( strcmp((char *)(curbuf->b_p_isk), (char *)(p))  != 0)
     {
         set_string_option_direct((char_u *)"isk", -1, p, OPT_FREE|OPT_LOCAL, 0);
         check_buf_options(curbuf);
@@ -66249,13 +64441,14 @@
     }
 
     curbuf->b_p_ts = 8;
-    curwin->w_p_list = FALSE;
+    curwin-> w_onebuf_opt.wo_list  = FALSE;
 
     curbuf->b_p_ma = FALSE;
     curbuf->b_p_bin = FALSE;
-    curwin->w_p_nu = 0;
-    curwin->w_p_rnu = 0;
-    RESET_BINDING(curwin);
+    curwin-> w_onebuf_opt.wo_nu  = 0;
+    curwin-> w_onebuf_opt.wo_rnu  = 0;
+     (curwin)-> w_onebuf_opt.wo_scb  = FALSE;
+     (curwin)-> w_onebuf_opt.wo_crb  = FALSE;
 
     set_buflisted(FALSE);
 }
@@ -66272,7 +64465,7 @@
     char_u      *rt;
     int         mustfree;
 
-    if (STRCMP(curbuf->b_p_ft, "help") != 0)
+    if ( strcmp((char *)(curbuf->b_p_ft), (char *)("help"))  != 0)
     {
         ++curbuf_lock;
         set_option_value_give_err((char_u *)"ft", 0L, (char_u *)"help", OPT_LOCAL);
@@ -66284,7 +64477,7 @@
         {
             line = ml_get_buf(curbuf, lnum, FALSE);
             len = ml_get_buf_len(curbuf, lnum);
-            if (in_example && len > 0 && !VIM_ISWHITE(line[0]))
+            if (in_example && len > 0 && ! ((line[0]) == ' ' || (line[0]) == '\t') )
             {
                 if (line[0] == '<')
                 {
@@ -66311,7 +64504,7 @@
     }
 
     fname = gettail(curbuf->b_fname);
-    if (fnamecmp(fname, "help.txt") == 0)
+    if ( vim_fnamecmp((char_u *)(fname), (char_u *)("help.txt"))  == 0)
     {
         for (lnum = 1; lnum < curbuf->b_ml.ml_line_count; ++lnum)
         {
@@ -66326,7 +64519,7 @@
             {
                 int NameBufflen;
 
-                NameBufflen = copy_option_part(&p, NameBuff, MAXPATHL, ",");
+                NameBufflen = copy_option_part(&p, NameBuff,  PATH_MAX , ",");
                 mustfree = FALSE;
                 rt = vim_getenv((char_u *)"VIMRUNTIME", &mustfree);
                 if (rt != NULL && fullpathcmp(rt, NameBuff, FALSE, TRUE) != FPC_SAME)
@@ -66341,10 +64534,10 @@
 
                     if (*NameBuff != NUL && !after_pathsep(NameBuff, NameBuff + NameBufflen))
                     {
-                        STRCPY(NameBuff + NameBufflen, PATHSEPSTR);
-                        NameBufflen += sizeof(PATHSEP);
+                         strcpy((char *)(NameBuff + NameBufflen), (char *)( "/" )) ;
+                        NameBufflen += sizeof( ((char_u)'/') );
                     }
-                    STRCPY(NameBuff + NameBufflen, "doc/*.txt");
+                     strcpy((char *)(NameBuff + NameBufflen), (char *)("doc/*.txt")) ;
                     if (gen_expand_wildcards(1, &NameBuff, &fcount, &fnames, EW_FILE|EW_SILENT) == OK && fcount > 0)
                     {
                         for (fi = 0; fi < fcount; ++fi)
@@ -66353,10 +64546,10 @@
                             {
                                 continue;
                             }
-                            fd = mch_fopen((char *)fnames[fi], "r");
+                            fd =  fopen(((char *)fnames[fi]), ("r")) ;
                             if (fd != NULL)
                             {
-                                vim_fgets(IObuff, IOSIZE, fd);
+                                vim_fgets(IObuff,  (1024+1) , fd);
                                 if (IObuff[0] == '*' && (s = vim_strchr(IObuff + 1, '*')) != NULL)
                                 {
                                     int this_utf = MAYBE;
@@ -66424,13 +64617,13 @@
 }
 
     void
-ex_exusage(exarg_T *eap UNUSED)
+ex_exusage(exarg_T *eap  __attribute__((unused)) )
 {
     do_cmdline_cmd((char_u *)"help ex-cmd-index");
 }
 
     void
-ex_viusage(exarg_T *eap UNUSED)
+ex_viusage(exarg_T *eap  __attribute__((unused)) )
 {
     do_cmdline_cmd((char_u *)"help normal-index");
 }
@@ -66457,10 +64650,10 @@
     int         in_example;
     int         mix = FALSE;
 
-    dirlen = (int)STRLEN(dir);
-    STRCPY(NameBuff, dir);
-    STRCAT(NameBuff, "/**/*");
-    STRCAT(NameBuff, ext);
+    dirlen = (int) strlen((char *)(dir)) ;
+     strcpy((char *)(NameBuff), (char *)(dir)) ;
+     strcat((char *)(NameBuff), (char *)("/**/*")) ;
+     strcat((char *)(NameBuff), (char *)(ext)) ;
     res = gen_expand_wildcards(1, &NameBuff, &filecount, &files, EW_FILE|EW_SILENT);
     if (res == FAIL || filecount == 0)
     {
@@ -66475,10 +64668,10 @@
         return;
     }
 
-    STRCPY(NameBuff, dir);
+     strcpy((char *)(NameBuff), (char *)(dir)) ;
     add_pathsep(NameBuff);
-    STRCAT(NameBuff, tagfname);
-    fd_tags = mch_fopen((char *)NameBuff, "w");
+     strcat((char *)(NameBuff), (char *)(tagfname)) ;
+    fd_tags =  fopen(((char *)NameBuff), ("w")) ;
     if (fd_tags == NULL)
     {
         if (!ignore_writeerr)
@@ -66498,7 +64691,7 @@
         }
         else
         {
-            s = alloc(18 + (unsigned)STRLEN(tagfname));
+            s = alloc(18 + (unsigned) strlen((char *)(tagfname)) );
             if (s == NULL)
             {
                 got_int = TRUE;
@@ -66514,7 +64707,7 @@
 
     for (fi = 0; fi < filecount && !got_int; ++fi)
     {
-        fd = mch_fopen((char *)files[fi], "r");
+        fd =  fopen(((char *)files[fi]), ("r")) ;
         if (fd == NULL)
         {
             semsg(_(e_unable_to_open_str_for_reading), files[fi]);
@@ -66524,7 +64717,7 @@
 
         in_example = FALSE;
         firstline = TRUE;
-        while (!vim_fgets(IObuff, IOSIZE, fd) && !got_int)
+        while (!vim_fgets(IObuff,  (1024+1) , fd) && !got_int)
         {
             if (firstline)
             {
@@ -66592,7 +64785,7 @@
                             got_int = TRUE;
                             break;
                         }
-                        s = alloc(p2 - p1 + STRLEN(fname) + 2);
+                        s = alloc(p2 - p1 +  strlen((char *)(fname))  + 2);
                         if (s == NULL)
                         {
                             got_int = TRUE;
@@ -66607,11 +64800,11 @@
                 }
                 p1 = p2;
             }
-            size_t off = STRLEN(IObuff);
+            size_t off =  strlen((char *)(IObuff)) ;
             if (off >= 2 && IObuff[off - 1] == '\n')
             {
                 off -= 2;
-                while (off > 0 && (ASCII_ISLOWER(IObuff[off]) || VIM_ISDIGIT(IObuff[off])))
+                while (off > 0 && ( ((unsigned)(IObuff[off]) - 'a' < 26)  ||  ((unsigned)(IObuff[off]) - '0' < 10) ))
                 {
                     off--;
                 }
@@ -66644,7 +64837,7 @@
                 if (*p2 == '\t')
                 {
                     *p2 = NUL;
-                    vim_snprintf((char *)NameBuff, MAXPATHL, _(e_duplicate_tag_str_in_file_str_str), ((char_u **)ga.ga_data)[i], dir, p2 + 1);
+                    vim_snprintf((char *)NameBuff,  PATH_MAX , _(e_duplicate_tag_str_in_file_str_str), ((char_u **)ga.ga_data)[i], dir, p2 + 1);
                     emsg((char *)NameBuff);
                     *p2 = '\t';
                     break;
@@ -66662,7 +64855,7 @@
         for (i = 0; i < ga.ga_len; ++i)
         {
             s = ((char_u **)ga.ga_data)[i];
-            if (STRNCMP(s, "help-tags\t", 10) == 0)
+            if ( strncmp((char *)(s), (char *)("help-tags\t"), (10))  == 0)
             {
                 fputs((char *)s, fd_tags);
             }
@@ -66713,13 +64906,13 @@
     char_u      *dirname;
     int         add_help_tags = FALSE;
 
-    if (STRNCMP(eap->arg, "++t", 3) == 0 && VIM_ISWHITE(eap->arg[3]))
+    if ( strncmp((char *)(eap->arg), (char *)("++t"), (3))  == 0 &&  ((eap->arg[3]) == ' ' || (eap->arg[3]) == '\t') )
     {
         add_help_tags = TRUE;
         eap->arg = skipwhite(eap->arg + 3);
     }
 
-    if (STRCMP(eap->arg, "ALL") == 0)
+    if ( strcmp((char *)(eap->arg), (char *)("ALL"))  == 0)
     {
         do_in_path(p_rtp, "", (char_u *)"doc", DIP_ALL + DIP_DIR, helptags_cb, &add_help_tags);
     }
@@ -66742,27 +64935,27 @@
 
 // ==================== highlight.c ====================
 
-#define SG_TERM         1
-#define SG_CTERM        2
-#define SG_GUI          4
-#define SG_LINK         8
+enum { SG_TERM = 1 };
+enum { SG_CTERM = 2 };
+enum { SG_GUI = 4 };
+enum { SG_LINK = 8 };
 
-#define MAX_SYN_NAME    200
+enum { MAX_SYN_NAME = 200 };
 
 static keyvalue_T highlight_tab[] = {
-    KEYVALUE_ENTRY(HL_BOLD, "bold"),
-    KEYVALUE_ENTRY(HL_INVERSE, "inverse"),
-    KEYVALUE_ENTRY(HL_ITALIC, "italic"),
-    KEYVALUE_ENTRY(HL_NOCOMBINE, "nocombine"),
-    KEYVALUE_ENTRY(HL_NORMAL, "NONE"),
-    KEYVALUE_ENTRY(HL_INVERSE, "reverse"),
-    KEYVALUE_ENTRY(HL_STANDOUT, "standout"),
-    KEYVALUE_ENTRY(HL_STRIKETHROUGH, "strikethrough"),
-    KEYVALUE_ENTRY(HL_UNDERCURL, "undercurl"),
-    KEYVALUE_ENTRY(HL_UNDERDASHED, "underdashed"),
-    KEYVALUE_ENTRY(HL_UNDERDOTTED, "underdotted"),
-    KEYVALUE_ENTRY(HL_UNDERDOUBLE, "underdouble"),
-    KEYVALUE_ENTRY(HL_UNDERLINE, "underline")
+     {(HL_BOLD), {((char_u *)"bold"),  (sizeof("bold" "") - 1) }} ,
+     {(HL_INVERSE), {((char_u *)"inverse"),  (sizeof("inverse" "") - 1) }} ,
+     {(HL_ITALIC), {((char_u *)"italic"),  (sizeof("italic" "") - 1) }} ,
+     {(HL_NOCOMBINE), {((char_u *)"nocombine"),  (sizeof("nocombine" "") - 1) }} ,
+     {(HL_NORMAL), {((char_u *)"NONE"),  (sizeof("NONE" "") - 1) }} ,
+     {(HL_INVERSE), {((char_u *)"reverse"),  (sizeof("reverse" "") - 1) }} ,
+     {(HL_STANDOUT), {((char_u *)"standout"),  (sizeof("standout" "") - 1) }} ,
+     {(HL_STRIKETHROUGH), {((char_u *)"strikethrough"),  (sizeof("strikethrough" "") - 1) }} ,
+     {(HL_UNDERCURL), {((char_u *)"undercurl"),  (sizeof("undercurl" "") - 1) }} ,
+     {(HL_UNDERDASHED), {((char_u *)"underdashed"),  (sizeof("underdashed" "") - 1) }} ,
+     {(HL_UNDERDOTTED), {((char_u *)"underdotted"),  (sizeof("underdotted" "") - 1) }} ,
+     {(HL_UNDERDOUBLE), {((char_u *)"underdouble"),  (sizeof("underdouble" "") - 1) }} ,
+     {(HL_UNDERLINE), {((char_u *)"underline"),  (sizeof("underline" "") - 1) }} 
 };
 
 static keyvalue_T *highlight_index_tab[] = {
@@ -66781,9 +64974,7 @@
     &highlight_tab[4]
 };
 
-#define MAX_ATTR_LEN 120
-
-#define ATTR_COMBINE(attr_a, attr_b) ((((attr_b) & HL_NOCOMBINE) ? (attr_b) : (attr_a)) | (attr_b))
+enum { MAX_ATTR_LEN = 120 };
 
 enum {
     BLACK = 0,
@@ -66817,34 +65008,34 @@
 };
 
 static keyvalue_T color_name_tab[] = {
-    KEYVALUE_ENTRY(BLACK, "Black"),
-    KEYVALUE_ENTRY(BLUE, "Blue"),
-    KEYVALUE_ENTRY(BROWN, "Brown"),
-    KEYVALUE_ENTRY(CYAN, "Cyan"),
-    KEYVALUE_ENTRY(DARKBLUE, "DarkBlue"),
-    KEYVALUE_ENTRY(DARKCYAN, "DarkCyan"),
-    KEYVALUE_ENTRY(DARKGRAY, "DarkGray"),
-    KEYVALUE_ENTRY(DARKGREEN, "DarkGreen"),
-    KEYVALUE_ENTRY(DARKGREY, "DarkGrey"),
-    KEYVALUE_ENTRY(DARKMAGENTA, "DarkMagenta"),
-    KEYVALUE_ENTRY(DARKRED, "DarkRed"),
-    KEYVALUE_ENTRY(DARKYELLOW, "DarkYellow"),
-    KEYVALUE_ENTRY(GRAY, "Gray"),
-    KEYVALUE_ENTRY(GREEN, "Green"),
-    KEYVALUE_ENTRY(GREY, "Grey"),
-    KEYVALUE_ENTRY(LIGHTBLUE, "LightBlue"),
-    KEYVALUE_ENTRY(LIGHTCYAN, "LightCyan"),
-    KEYVALUE_ENTRY(LIGHTGRAY, "LightGray"),
-    KEYVALUE_ENTRY(LIGHTGREEN, "LightGreen"),
-    KEYVALUE_ENTRY(LIGHTGREY, "LightGrey"),
-    KEYVALUE_ENTRY(LIGHTMAGENTA, "LightMagenta"),
-    KEYVALUE_ENTRY(LIGHTRED, "LightRed"),
-    KEYVALUE_ENTRY(LIGHTYELLOW, "LightYellow"),
-    KEYVALUE_ENTRY(MAGENTA, "Magenta"),
-    KEYVALUE_ENTRY(NONE, "NONE"),
-    KEYVALUE_ENTRY(RED, "Red"),
-    KEYVALUE_ENTRY(WHITE, "White"),
-    KEYVALUE_ENTRY(YELLOW, "Yellow")
+     {(BLACK), {((char_u *)"Black"),  (sizeof("Black" "") - 1) }} ,
+     {(BLUE), {((char_u *)"Blue"),  (sizeof("Blue" "") - 1) }} ,
+     {(BROWN), {((char_u *)"Brown"),  (sizeof("Brown" "") - 1) }} ,
+     {(CYAN), {((char_u *)"Cyan"),  (sizeof("Cyan" "") - 1) }} ,
+     {(DARKBLUE), {((char_u *)"DarkBlue"),  (sizeof("DarkBlue" "") - 1) }} ,
+     {(DARKCYAN), {((char_u *)"DarkCyan"),  (sizeof("DarkCyan" "") - 1) }} ,
+     {(DARKGRAY), {((char_u *)"DarkGray"),  (sizeof("DarkGray" "") - 1) }} ,
+     {(DARKGREEN), {((char_u *)"DarkGreen"),  (sizeof("DarkGreen" "") - 1) }} ,
+     {(DARKGREY), {((char_u *)"DarkGrey"),  (sizeof("DarkGrey" "") - 1) }} ,
+     {(DARKMAGENTA), {((char_u *)"DarkMagenta"),  (sizeof("DarkMagenta" "") - 1) }} ,
+     {(DARKRED), {((char_u *)"DarkRed"),  (sizeof("DarkRed" "") - 1) }} ,
+     {(DARKYELLOW), {((char_u *)"DarkYellow"),  (sizeof("DarkYellow" "") - 1) }} ,
+     {(GRAY), {((char_u *)"Gray"),  (sizeof("Gray" "") - 1) }} ,
+     {(GREEN), {((char_u *)"Green"),  (sizeof("Green" "") - 1) }} ,
+     {(GREY), {((char_u *)"Grey"),  (sizeof("Grey" "") - 1) }} ,
+     {(LIGHTBLUE), {((char_u *)"LightBlue"),  (sizeof("LightBlue" "") - 1) }} ,
+     {(LIGHTCYAN), {((char_u *)"LightCyan"),  (sizeof("LightCyan" "") - 1) }} ,
+     {(LIGHTGRAY), {((char_u *)"LightGray"),  (sizeof("LightGray" "") - 1) }} ,
+     {(LIGHTGREEN), {((char_u *)"LightGreen"),  (sizeof("LightGreen" "") - 1) }} ,
+     {(LIGHTGREY), {((char_u *)"LightGrey"),  (sizeof("LightGrey" "") - 1) }} ,
+     {(LIGHTMAGENTA), {((char_u *)"LightMagenta"),  (sizeof("LightMagenta" "") - 1) }} ,
+     {(LIGHTRED), {((char_u *)"LightRed"),  (sizeof("LightRed" "") - 1) }} ,
+     {(LIGHTYELLOW), {((char_u *)"LightYellow"),  (sizeof("LightYellow" "") - 1) }} ,
+     {(MAGENTA), {((char_u *)"Magenta"),  (sizeof("Magenta" "") - 1) }} ,
+     {(NONE), {((char_u *)"NONE"),  (sizeof("NONE" "") - 1) }} ,
+     {(RED), {((char_u *)"Red"),  (sizeof("Red" "") - 1) }} ,
+     {(WHITE), {((char_u *)"White"),  (sizeof("White" "") - 1) }} ,
+     {(YELLOW), {((char_u *)"Yellow"),  (sizeof("Yellow" "") - 1) }} 
 };
 
 typedef struct
@@ -66884,24 +65075,18 @@
 
 static int highlight_attr_raw[HLF_COUNT];
 
-static int hl_flags[HLF_COUNT] = HL_FLAGS;
+static int hl_flags[HLF_COUNT] =  {'8', '~', '@', 'd', 'e', 'h', 'i', 'l', 'y', 'm', 'M',                  'n', 'a', 'b', 'N', 'G', 'O', 'r', 's', 'S', 'c', '|', 't', 'v', 'V',                   'w', 'W', 'f', 'F', 'A', 'C', 'D', 'T', 'E', '-', '>',                  'B', 'P', 'R', 'L',             '+', '=', 'k', '<','[', ']', '{', '}', 'x', 'X', 'j', 'H',              'p', 'J', 'Q',                  '*', '#', '_', '!', '.', 'o', 'q',              'z', 'Z', 'g',                  '%', '^', '&', 'I', '('} ;
 
 static garray_T highlight_ga;
-#define HL_TABLE()      ((hl_group_T *)((highlight_ga.ga_data)))
 
 typedef struct {
     int         hn_id;
     char_u      hn_key[1];
 } hlname_T;
 
-#define HLNAME_KEY_OFF  offsetof(hlname_T, hn_key)
-#define HI2HLNAME(hi)   ((hlname_T *)((hi)->hi_key - HLNAME_KEY_OFF))
-
 static hashtab_T highlight_ht;
 static bool      highlight_ht_inited = false;
 
-#define ATTR_OFF (HL_ALL + 1)
-
 static void syn_unadd_group(void);
 static void set_hl_attr(int idx);
 static void highlight_list_one(int id);
@@ -66911,20 +65096,19 @@
 static int hl_has_settings(int idx, int check_link);
 static void highlight_clear(int idx);
 
-# define CENT(a, b) a
 static char *(highlight_init_both[]) = {
-    CENT("ErrorMsg term=standout ctermbg=DarkRed ctermfg=White", "ErrorMsg term=standout ctermbg=DarkRed ctermfg=White guibg=Red guifg=White"),
-    CENT("IncSearch term=reverse,bold,underline cterm=reverse", "IncSearch term=reverse,bold,underline cterm=reverse gui=reverse"),
-    CENT("ModeMsg term=bold cterm=bold", "ModeMsg term=bold cterm=bold gui=bold"),
-    CENT("NonText term=bold ctermfg=Blue", "NonText term=bold ctermfg=Blue gui=bold guifg=Blue"),
-    CENT("StatusLine term=reverse,bold cterm=reverse,bold", "StatusLine term=reverse,bold cterm=reverse,bold gui=reverse,bold"),
-    CENT("StatusLineNC term=reverse cterm=reverse", "StatusLineNC term=reverse cterm=reverse gui=reverse"),
+     "ErrorMsg term=standout ctermbg=DarkRed ctermfg=White" ,
+     "IncSearch term=reverse,bold,underline cterm=reverse" ,
+     "ModeMsg term=bold cterm=bold" ,
+     "NonText term=bold ctermfg=Blue" ,
+     "StatusLine term=reverse,bold cterm=reverse,bold" ,
+     "StatusLineNC term=reverse cterm=reverse" ,
     "default link EndOfBuffer NonText",
-    CENT("VertSplit term=reverse cterm=reverse", "VertSplit term=reverse cterm=reverse gui=reverse"),
+     "VertSplit term=reverse cterm=reverse" ,
     "default link VertSplitNC VertSplit",
-    CENT("PmenuSbar term=NONE ctermbg=Grey", "PmenuSbar term=NONE ctermbg=Grey guibg=Grey"),
-    CENT("TabLineSel term=bold cterm=bold", "TabLineSel term=bold cterm=bold gui=bold"),
-    CENT("TabLineFill term=reverse cterm=reverse", "TabLineFill term=reverse cterm=reverse gui=reverse"),
+     "PmenuSbar term=NONE ctermbg=Grey" ,
+     "TabLineSel term=bold cterm=bold" ,
+     "TabLineFill term=reverse cterm=reverse" ,
     "default link TabPanel TabLine",
     "default link TabPanelSel TabLineSel",
     "default link TabPanelFill TabLineFill",
@@ -66946,49 +65130,49 @@
     "default link MessageWindow WarningMsg",
     "default link PopupNotification WarningMsg",
     "default link PreInsert Added",
-    CENT("Normal cterm=NONE", "Normal gui=NONE"),
+     "Normal cterm=NONE" ,
     NULL
 };
 
 static char *(highlight_init_light[]) = {
-    CENT("Directory term=bold ctermfg=DarkBlue", "Directory term=bold ctermfg=DarkBlue guifg=Blue"),
-    CENT("LineNr term=NONE ctermfg=Brown", "LineNr term=NONE ctermfg=Brown guifg=Brown"),
-    CENT("CursorLineNr term=bold cterm=underline ctermfg=Brown", "CursorLineNr term=bold cterm=underline ctermfg=Brown gui=bold guifg=Brown"),
-    CENT("MoreMsg term=bold ctermfg=DarkGreen", "MoreMsg term=bold ctermfg=DarkGreen gui=bold guifg=SeaGreen"),
-    CENT("Question term=standout ctermfg=DarkGreen", "Question term=standout ctermfg=DarkGreen gui=bold guifg=SeaGreen"),
-    CENT("Search term=reverse ctermbg=Yellow ctermfg=NONE", "Search term=reverse ctermbg=Yellow ctermfg=NONE guibg=Yellow guifg=NONE"),
-    CENT("PmenuThumb term=reverse ctermbg=Black", "PmenuThumb term=reverse ctermbg=Black guibg=Black"),
-    CENT("PmenuShadow term=NONE ctermbg=Black ctermfg=DarkGrey", "PmenuShadow term=NONE ctermbg=Black ctermfg=DarkGrey guibg=Black guifg=DarkGrey"),
-    CENT("Pmenu term=reverse ctermbg=LightMagenta ctermfg=Black", "Pmenu term=reverse ctermbg=LightMagenta ctermfg=Black guibg=LightMagenta"),
-    CENT("PmenuSel term=underline ctermbg=LightGrey ctermfg=Black", "PmenuSel term=underline ctermbg=LightGrey ctermfg=Black guibg=Grey"),
-    CENT("SpecialKey term=NONE ctermfg=DarkBlue", "SpecialKey term=NONE ctermfg=DarkBlue guifg=Blue"),
-    CENT("Title term=bold ctermfg=DarkMagenta", "Title term=bold ctermfg=DarkMagenta gui=bold guifg=Magenta"),
-    CENT("WarningMsg term=standout ctermfg=DarkRed", "WarningMsg term=standout ctermfg=DarkRed guifg=Red"),
-    CENT("WildMenu term=underline ctermbg=Yellow ctermfg=Black", "WildMenu term=underline ctermbg=Yellow ctermfg=Black guibg=Yellow guifg=Black"),
-    CENT("Visual ctermbg=Grey ctermfg=Black", "Visual ctermbg=Grey ctermfg=Black guibg=LightGrey guifg=Black"),
-    CENT("TabLine term=underline cterm=underline ctermfg=black ctermbg=LightGrey", "TabLine term=underline cterm=underline ctermfg=black ctermbg=LightGrey gui=underline guibg=LightGrey"),
-    CENT("MatchParen term=reverse ctermbg=Cyan", "MatchParen term=reverse ctermbg=Cyan guibg=Cyan"),
+     "Directory term=bold ctermfg=DarkBlue" ,
+     "LineNr term=NONE ctermfg=Brown" ,
+     "CursorLineNr term=bold cterm=underline ctermfg=Brown" ,
+     "MoreMsg term=bold ctermfg=DarkGreen" ,
+     "Question term=standout ctermfg=DarkGreen" ,
+     "Search term=reverse ctermbg=Yellow ctermfg=NONE" ,
+     "PmenuThumb term=reverse ctermbg=Black" ,
+     "PmenuShadow term=NONE ctermbg=Black ctermfg=DarkGrey" ,
+     "Pmenu term=reverse ctermbg=LightMagenta ctermfg=Black" ,
+     "PmenuSel term=underline ctermbg=LightGrey ctermfg=Black" ,
+     "SpecialKey term=NONE ctermfg=DarkBlue" ,
+     "Title term=bold ctermfg=DarkMagenta" ,
+     "WarningMsg term=standout ctermfg=DarkRed" ,
+     "WildMenu term=underline ctermbg=Yellow ctermfg=Black" ,
+     "Visual ctermbg=Grey ctermfg=Black" ,
+     "TabLine term=underline cterm=underline ctermfg=black ctermbg=LightGrey" ,
+     "MatchParen term=reverse ctermbg=Cyan" ,
     NULL
 };
 
 static char *(highlight_init_dark[]) = {
-    CENT("Directory term=bold ctermfg=LightCyan", "Directory term=bold ctermfg=LightCyan guifg=Cyan"),
-    CENT("LineNr term=NONE ctermfg=Yellow", "LineNr term=NONE ctermfg=Yellow guifg=Yellow"),
-    CENT("CursorLineNr term=bold cterm=underline ctermfg=Yellow", "CursorLineNr term=bold cterm=underline ctermfg=Yellow gui=bold guifg=Yellow"),
-    CENT("MoreMsg term=bold ctermfg=LightGreen", "MoreMsg term=bold ctermfg=LightGreen gui=bold guifg=SeaGreen"),
-    CENT("Question term=standout ctermfg=LightGreen", "Question term=standout ctermfg=LightGreen gui=bold guifg=Green"),
-    CENT("Search term=reverse ctermbg=Yellow ctermfg=Black", "Search term=reverse ctermbg=Yellow ctermfg=Black guibg=Yellow guifg=Black"),
-    CENT("SpecialKey term=NONE ctermfg=LightBlue", "SpecialKey term=NONE ctermfg=LightBlue guifg=Cyan"),
-    CENT("PmenuThumb term=reverse ctermbg=White", "PmenuThumb term=reverse ctermbg=White guibg=White"),
-    CENT("PmenuShadow term=NONE ctermbg=Black ctermfg=DarkGrey", "PmenuShadow term=NONE ctermbg=Black ctermfg=DarkGrey guibg=Black guifg=DarkGrey"),
-    CENT("Pmenu term=reverse ctermbg=Magenta ctermfg=Black", "Pmenu term=reverse ctermbg=Magenta ctermfg=Black guibg=Magenta"),
-    CENT("PmenuSel term=underline ctermbg=Black ctermfg=DarkGrey", "PmenuSel term=underline ctermbg=Black ctermfg=DarkGrey guibg=DarkGrey"),
-    CENT("Title term=bold ctermfg=LightMagenta", "Title term=bold ctermfg=LightMagenta gui=bold guifg=Magenta"),
-    CENT("WarningMsg term=standout ctermfg=LightRed", "WarningMsg term=standout ctermfg=LightRed guifg=Red"),
-    CENT("WildMenu term=underline ctermbg=Yellow ctermfg=Black", "WildMenu term=underline ctermbg=Yellow ctermfg=Black guibg=Yellow guifg=Black"),
-    CENT("Visual ctermbg=Grey ctermfg=Black", "Visual ctermbg=Grey ctermfg=Black guibg=#575757 guifg=LightGrey"),
-    CENT("TabLine term=underline cterm=underline ctermfg=white ctermbg=DarkGrey", "TabLine term=underline cterm=underline ctermfg=white ctermbg=DarkGrey gui=underline guibg=DarkGrey"),
-    CENT("MatchParen term=reverse ctermbg=DarkCyan", "MatchParen term=reverse ctermbg=DarkCyan guibg=DarkCyan"),
+     "Directory term=bold ctermfg=LightCyan" ,
+     "LineNr term=NONE ctermfg=Yellow" ,
+     "CursorLineNr term=bold cterm=underline ctermfg=Yellow" ,
+     "MoreMsg term=bold ctermfg=LightGreen" ,
+     "Question term=standout ctermfg=LightGreen" ,
+     "Search term=reverse ctermbg=Yellow ctermfg=Black" ,
+     "SpecialKey term=NONE ctermfg=LightBlue" ,
+     "PmenuThumb term=reverse ctermbg=White" ,
+     "PmenuShadow term=NONE ctermbg=Black ctermfg=DarkGrey" ,
+     "Pmenu term=reverse ctermbg=Magenta ctermfg=Black" ,
+     "PmenuSel term=underline ctermbg=Black ctermfg=DarkGrey" ,
+     "Title term=bold ctermfg=LightMagenta" ,
+     "WarningMsg term=standout ctermfg=LightRed" ,
+     "WildMenu term=underline ctermbg=Yellow ctermfg=Black" ,
+     "Visual ctermbg=Grey ctermfg=Black" ,
+     "TabLine term=underline cterm=underline ctermfg=white ctermbg=DarkGrey" ,
+     "MatchParen term=reverse ctermbg=DarkCyan" ,
     NULL
 };
 
@@ -67058,7 +65242,7 @@
     }
 
     recursive = TRUE;
-    buf = alloc(STRLEN(name) + 12);
+    buf = alloc( strlen((char *)(name))  + 12);
     if (buf != NULL)
     {
         apply_autocmds(EVENT_COLORSCHEMEPRE, name, curbuf->b_fname, FALSE, curbuf);
@@ -67133,15 +65317,15 @@
     }
     else if (t_colors == 16 || t_colors == 88 || t_colors >= 256)
     {
-        if (*T_CAF != NUL)
+        if (* ( term_strings[(int)(KS_CAF)] )  != NUL)
         {
-            p = T_CAF;
+            p =  ( term_strings[(int)(KS_CAF)] ) ;
         }
         else
         {
-            p = T_CSF;
+            p =  ( term_strings[(int)(KS_CSF)] ) ;
         }
-        if (*p != NUL && (t_colors > 256 || *(p + STRLEN(p) - 1) == 'm'))
+        if (*p != NUL && (t_colors > 256 || *(p +  strlen((char *)(p))  - 1) == 'm'))
         {
             if (t_colors == 88)
             {
@@ -67168,7 +65352,7 @@
     hl_group_T  *hlgroup = NULL;
 
     from_id = syn_check_group(from_hg, from_len);
-    if (STRNCMP(to_hg, "NONE", 4) == 0)
+    if ( strncmp((char *)(to_hg), (char *)("NONE"), (4))  == 0)
     {
         to_id = 0;
     }
@@ -67179,7 +65363,7 @@
 
     if (from_id > 0)
     {
-        hlgroup = &HL_TABLE()[from_id - 1];
+        hlgroup = & ((hl_group_T *)((highlight_ga.ga_data))) [from_id - 1];
         if (dodefault && (forceit || hlgroup->sg_deflink == 0))
         {
             hlgroup->sg_deflink = to_id;
@@ -67190,7 +65374,7 @@
     {
         if (to_id > 0 && !forceit && !init && hl_has_settings(from_id - 1, dodefault))
         {
-            if (SOURCING_NAME == NULL && !dodefault)
+            if ( (((estack_T *)exestack.ga_data)[exestack.ga_len - 1].es_name)  == NULL && !dodefault)
             {
                 emsg(_(e_group_has_settings_highlight_link_ignored));
             }
@@ -67242,7 +65426,7 @@
     while (arg[off] != NUL)
     {
         target.value.string = arg + off;
-        entry = (keyvalue_T *)bsearch(&target, &highlight_tab, ARRAY_LENGTH(highlight_tab), sizeof(highlight_tab[0]), cmp_keyvalue_value_ni);
+        entry = (keyvalue_T *)bsearch(&target, &highlight_tab,  (sizeof(highlight_tab) / sizeof((highlight_tab)[0])) , sizeof(highlight_tab[0]), cmp_keyvalue_value_ni);
         if (entry == NULL)
         {
             semsg(_(e_illegal_value_str), arg);
@@ -67258,25 +65442,25 @@
     }
     if (*key == 'T')
     {
-        if (!init || !(HL_TABLE()[idx].sg_set & SG_TERM))
+        if (!init || !( ((hl_group_T *)((highlight_ga.ga_data))) [idx].sg_set & SG_TERM))
         {
             if (!init)
             {
-                HL_TABLE()[idx].sg_set |= SG_TERM;
+                 ((hl_group_T *)((highlight_ga.ga_data))) [idx].sg_set |= SG_TERM;
             }
-            HL_TABLE()[idx].sg_term = attr;
+             ((hl_group_T *)((highlight_ga.ga_data))) [idx].sg_term = attr;
         }
     }
     else if (*key == 'C')
     {
-        if (!init || !(HL_TABLE()[idx].sg_set & SG_CTERM))
+        if (!init || !( ((hl_group_T *)((highlight_ga.ga_data))) [idx].sg_set & SG_CTERM))
         {
             if (!init)
             {
-                HL_TABLE()[idx].sg_set |= SG_CTERM;
+                 ((hl_group_T *)((highlight_ga.ga_data))) [idx].sg_set |= SG_CTERM;
             }
-            HL_TABLE()[idx].sg_cterm = attr;
-            HL_TABLE()[idx].sg_cterm_bold = FALSE;
+             ((hl_group_T *)((highlight_ga.ga_data))) [idx].sg_cterm = attr;
+             ((hl_group_T *)((highlight_ga.ga_data))) [idx].sg_cterm_bold = FALSE;
         }
     }
 
@@ -67300,10 +65484,10 @@
     static void
 highlight_set_ctermfg(int idx, int color, int is_normal_group)
 {
-    HL_TABLE()[idx].sg_cterm_fg = color + 1;
+     ((hl_group_T *)((highlight_ga.ga_data))) [idx].sg_cterm_fg = color + 1;
     if (is_normal_group)
     {
-        hl_set_ctermfg_normal_group(color, (HL_TABLE()[idx].sg_cterm & HL_BOLD));
+        hl_set_ctermfg_normal_group(color, ( ((hl_group_T *)((highlight_ga.ga_data))) [idx].sg_cterm & HL_BOLD));
     }
 }
 
@@ -67341,7 +65525,7 @@
     static void
 highlight_set_ctermbg(int idx, int color, int is_normal_group)
 {
-    HL_TABLE()[idx].sg_cterm_bg = color + 1;
+     ((hl_group_T *)((highlight_ga.ga_data))) [idx].sg_cterm_bg = color + 1;
     if (is_normal_group)
     {
         hl_set_ctermbg_normal_group(color);
@@ -67364,7 +65548,7 @@
     static void
 highlight_set_ctermul(int idx, int color, int is_normal_group)
 {
-    HL_TABLE()[idx].sg_cterm_ul = color + 1;
+     ((hl_group_T *)((highlight_ga.ga_data))) [idx].sg_cterm_ul = color + 1;
     if (is_normal_group)
     {
         hl_set_ctermul_normal_group(color);
@@ -67376,21 +65560,21 @@
 {
     int         font;
 
-    if (init && (HL_TABLE()[idx].sg_set & SG_CTERM))
+    if (init && ( ((hl_group_T *)((highlight_ga.ga_data))) [idx].sg_set & SG_CTERM))
     {
         return FALSE;
     }
 
     if (!init)
     {
-        HL_TABLE()[idx].sg_set |= SG_CTERM;
+         ((hl_group_T *)((highlight_ga.ga_data))) [idx].sg_set |= SG_CTERM;
     }
 
-    if (VIM_ISDIGIT(*arg))
+    if ( ((unsigned)(*arg) - '0' < 10) )
     {
         font = atoi((char *)arg);
     }
-    else if (STRICMP(arg, "NONE") == 0)
+    else if ( strcasecmp((char *)(arg), (char *)("NONE"))  == 0)
     {
         font = -1;
     }
@@ -67399,7 +65583,7 @@
         return FALSE;
     }
 
-    HL_TABLE()[idx].sg_cterm_font = font + 1;
+     ((hl_group_T *)((highlight_ga.ga_data))) [idx].sg_cterm_font = font + 1;
     return TRUE;
 }
 
@@ -67408,27 +65592,27 @@
 {
     int         color;
 
-    if (init && (HL_TABLE()[idx].sg_set & SG_CTERM))
+    if (init && ( ((hl_group_T *)((highlight_ga.ga_data))) [idx].sg_set & SG_CTERM))
     {
         return FALSE;
     }
 
     if (!init)
     {
-        HL_TABLE()[idx].sg_set |= SG_CTERM;
+         ((hl_group_T *)((highlight_ga.ga_data))) [idx].sg_set |= SG_CTERM;
     }
 
-    if (key[5] == 'F' && HL_TABLE()[idx].sg_cterm_bold)
+    if (key[5] == 'F' &&  ((hl_group_T *)((highlight_ga.ga_data))) [idx].sg_cterm_bold)
     {
-        HL_TABLE()[idx].sg_cterm &= ~HL_BOLD;
-        HL_TABLE()[idx].sg_cterm_bold = FALSE;
+         ((hl_group_T *)((highlight_ga.ga_data))) [idx].sg_cterm &= ~HL_BOLD;
+         ((hl_group_T *)((highlight_ga.ga_data))) [idx].sg_cterm_bold = FALSE;
     }
 
-    if (VIM_ISDIGIT(*arg))
+    if ( ((unsigned)(*arg) - '0' < 10) )
     {
         color = atoi((char *)arg);
     }
-    else if (STRICMP(arg, "fg") == 0)
+    else if ( strcasecmp((char *)(arg), (char *)("fg"))  == 0)
     {
         if (cterm_normal_fg_color)
         {
@@ -67440,7 +65624,7 @@
             return FALSE;
         }
     }
-    else if (STRICMP(arg, "bg") == 0)
+    else if ( strcasecmp((char *)(arg), (char *)("bg"))  == 0)
     {
         if (cterm_normal_bg_color > 0)
         {
@@ -67452,7 +65636,7 @@
             return FALSE;
         }
     }
-    else if (STRICMP(arg, "ul") == 0)
+    else if ( strcasecmp((char *)(arg), (char *)("ul"))  == 0)
     {
         if (cterm_normal_ul_color > 0)
         {
@@ -67473,7 +65657,7 @@
         target.key = 0;
         target.value.string = arg;
         target.value.length = 0;
-        entry = (keyvalue_T *)bsearch(&target, &color_name_tab, ARRAY_LENGTH(color_name_tab), sizeof(color_name_tab[0]), cmp_keyvalue_value_i);
+        entry = (keyvalue_T *)bsearch(&target, &color_name_tab,  (sizeof(color_name_tab) / sizeof((color_name_tab)[0])) , sizeof(color_name_tab[0]), cmp_keyvalue_value_i);
         if (entry == NULL)
         {
             semsg(_(e_color_name_or_number_not_recognized_str), key_start);
@@ -67484,12 +65668,12 @@
 
         if (bold == TRUE)
         {
-            HL_TABLE()[idx].sg_cterm |= HL_BOLD;
-            HL_TABLE()[idx].sg_cterm_bold = TRUE;
+             ((hl_group_T *)((highlight_ga.ga_data))) [idx].sg_cterm |= HL_BOLD;
+             ((hl_group_T *)((highlight_ga.ga_data))) [idx].sg_cterm_bold = TRUE;
         }
         else if (bold == FALSE)
         {
-            HL_TABLE()[idx].sg_cterm &= ~HL_BOLD;
+             ((hl_group_T *)((highlight_ga.ga_data))) [idx].sg_cterm &= ~HL_BOLD;
         }
     }
 
@@ -67520,10 +65704,10 @@
 
     if (!init)
     {
-        HL_TABLE()[idx].sg_set |= SG_TERM;
+         ((hl_group_T *)((highlight_ga.ga_data))) [idx].sg_set |= SG_TERM;
     }
 
-    if (STRNCMP(arg, "t_", 2) == 0)
+    if ( strncmp((char *)(arg), (char *)("t_"), (2))  == 0)
     {
         off = 0;
         buf[0] = 0;
@@ -67545,12 +65729,12 @@
                 p = (char_u *)"";
             }
 
-            if ((int)(STRLEN(buf) + STRLEN(p)) >= 99)
+            if ((int)( strlen((char *)(buf))  +  strlen((char *)(p)) ) >= 99)
             {
                 semsg(_(e_terminal_code_too_long_str), arg);
                 return FALSE;
             }
-            STRCAT(buf, p);
+             strcat((char *)(buf), (char *)(p)) ;
 
             off += len;
             if (arg[off] == ',')
@@ -67577,7 +65761,7 @@
         buf[off] = NUL;
     }
 
-    if (STRCMP(buf, "NONE") == 0)
+    if ( strcmp((char *)(buf), (char *)("NONE"))  == 0)
     {
         p = NULL;
     }
@@ -67587,13 +65771,13 @@
     }
     if (key[2] == 'A')
     {
-        vim_free(HL_TABLE()[idx].sg_start);
-        HL_TABLE()[idx].sg_start = p;
+        vim_free( ((hl_group_T *)((highlight_ga.ga_data))) [idx].sg_start);
+         ((hl_group_T *)((highlight_ga.ga_data))) [idx].sg_start = p;
     }
     else
     {
-        vim_free(HL_TABLE()[idx].sg_stop);
-        HL_TABLE()[idx].sg_stop = p;
+        vim_free( ((hl_group_T *)((highlight_ga.ga_data))) [idx].sg_stop);
+         ((hl_group_T *)((highlight_ga.ga_data))) [idx].sg_stop = p;
     }
     return TRUE;
 }
@@ -67617,11 +65801,6 @@
     int         dolink = FALSE;
     int         error = FALSE;
     int         is_normal_group = FALSE;
-# define is_titlebar_group 0
-# define is_titlebarnc_group 0
-# define is_menu_group 0
-# define is_tooltip_group 0
-# define is_scrollbar_group 0
 
     if (!init && ends_excmd2(line - 1, line))
     {
@@ -67635,7 +65814,7 @@
     name_end = skiptowhite(line);
     linep = skipwhite(name_end);
 
-    if (STRNCMP(line, "default", name_end - line) == 0)
+    if ( strncmp((char *)(line), (char *)("default"), (name_end - line))  == 0)
     {
         dodefault = TRUE;
         line = linep;
@@ -67643,11 +65822,11 @@
         linep = skipwhite(name_end);
     }
 
-    if (STRNCMP(line, "clear", name_end - line) == 0)
+    if ( strncmp((char *)(line), (char *)("clear"), (name_end - line))  == 0)
     {
         doclear = TRUE;
     }
-    if (STRNCMP(line, "link", name_end - line) == 0)
+    if ( strncmp((char *)(line), (char *)("link"), (name_end - line))  == 0)
     {
         dolink = TRUE;
     }
@@ -67721,9 +65900,9 @@
         return;
     }
 
-    item_before = HL_TABLE()[idx];
+    item_before =  ((hl_group_T *)((highlight_ga.ga_data))) [idx];
 
-    if (STRCMP(HL_TABLE()[idx].sg_name_u, "NORMAL") == 0)
+    if ( strcmp((char *)( ((hl_group_T *)((highlight_ga.ga_data))) [idx].sg_name_u), (char *)("NORMAL"))  == 0)
     {
         is_normal_group = TRUE;
     }
@@ -67733,7 +65912,7 @@
         highlight_clear(idx);
         if (!doclear)
         {
-            HL_TABLE()[idx].sg_set = 0;
+             ((hl_group_T *)((highlight_ga.ga_data))) [idx].sg_set = 0;
         }
     }
 
@@ -67749,7 +65928,7 @@
                 break;
             }
 
-            while (*linep && !VIM_ISWHITE(*linep) && *linep != '=')
+            while (*linep && ! ((*linep) == ' ' || (*linep) == '\t')  && *linep != '=')
             {
                 ++linep;
             }
@@ -67762,13 +65941,13 @@
             }
             linep = skipwhite(linep);
 
-            if (STRCMP(key, "NONE") == 0)
+            if ( strcmp((char *)(key), (char *)("NONE"))  == 0)
             {
-                if (!init || HL_TABLE()[idx].sg_set == 0)
+                if (!init ||  ((hl_group_T *)((highlight_ga.ga_data))) [idx].sg_set == 0)
                 {
                     if (!init)
                     {
-                        HL_TABLE()[idx].sg_set |= SG_TERM+SG_CTERM+SG_GUI;
+                         ((hl_group_T *)((highlight_ga.ga_data))) [idx].sg_set |= SG_TERM+SG_CTERM+SG_GUI;
                     }
                     highlight_clear(idx);
                 }
@@ -67818,7 +65997,7 @@
                 ++linep;
             }
 
-            if (STRCMP(key, "TERM") == 0 || STRCMP(key, "CTERM") == 0 || STRCMP(key, "GUI") == 0)
+            if ( strcmp((char *)(key), (char *)("TERM"))  == 0 ||  strcmp((char *)(key), (char *)("CTERM"))  == 0 ||  strcmp((char *)(key), (char *)("GUI"))  == 0)
             {
                 if (!highlight_set_termgui_attr(idx, key, arg, init))
                 {
@@ -67826,10 +66005,10 @@
                     break;
                 }
             }
-            else if (STRCMP(key, "FONT") == 0)
+            else if ( strcmp((char *)(key), (char *)("FONT"))  == 0)
             {
             }
-            else if (STRCMP(key, "CTERMFG") == 0 || STRCMP(key, "CTERMBG") == 0 || STRCMP(key, "CTERMUL") == 0)
+            else if ( strcmp((char *)(key), (char *)("CTERMFG"))  == 0 ||  strcmp((char *)(key), (char *)("CTERMBG"))  == 0 ||  strcmp((char *)(key), (char *)("CTERMUL"))  == 0)
             {
                 if (!highlight_set_cterm_color(idx, key, key_start, arg, is_normal_group, init))
                 {
@@ -67837,7 +66016,7 @@
                     break;
                 }
             }
-            else if (STRCMP(key, "CTERMFONT") == 0)
+            else if ( strcmp((char *)(key), (char *)("CTERMFONT"))  == 0)
             {
                 if (!highlight_set_cterm_font(idx, arg, init))
                 {
@@ -67845,16 +66024,16 @@
                     break;
                 }
             }
-            else if (STRCMP(key, "GUIFG") == 0)
+            else if ( strcmp((char *)(key), (char *)("GUIFG"))  == 0)
             {
             }
-            else if (STRCMP(key, "GUIBG") == 0)
+            else if ( strcmp((char *)(key), (char *)("GUIBG"))  == 0)
             {
             }
-            else if (STRCMP(key, "GUISP") == 0)
+            else if ( strcmp((char *)(key), (char *)("GUISP"))  == 0)
             {
             }
-            else if (STRCMP(key, "START") == 0 || STRCMP(key, "STOP") == 0)
+            else if ( strcmp((char *)(key), (char *)("START"))  == 0 ||  strcmp((char *)(key), (char *)("STOP"))  == 0)
             {
                 if (!highlight_set_startstop_termcode(idx, key, arg, init))
                 {
@@ -67868,11 +66047,11 @@
                 error = TRUE;
                 break;
             }
-            HL_TABLE()[idx].sg_cleared = FALSE;
+             ((hl_group_T *)((highlight_ga.ga_data))) [idx].sg_cleared = FALSE;
 
-            if (!init || !(HL_TABLE()[idx].sg_set & SG_LINK))
+            if (!init || !( ((hl_group_T *)((highlight_ga.ga_data))) [idx].sg_set & SG_LINK))
             {
-                HL_TABLE()[idx].sg_link = 0;
+                 ((hl_group_T *)((highlight_ga.ga_data))) [idx].sg_link = 0;
             }
 
             linep = skipwhite(linep);
@@ -67887,8 +66066,8 @@
     {
         if (is_normal_group)
         {
-            HL_TABLE()[idx].sg_term_attr = 0;
-            HL_TABLE()[idx].sg_cterm_attr = 0;
+             ((hl_group_T *)((highlight_ga.ga_data))) [idx].sg_term_attr = 0;
+             ((hl_group_T *)((highlight_ga.ga_data))) [idx].sg_cterm_attr = 0;
             resolve_fallback_fg_to_rgb();
             resolve_fallback_bg_to_rgb();
         }
@@ -67901,7 +66080,7 @@
     vim_free(key);
     vim_free(arg);
 
-    if ((did_change || memcmp(&HL_TABLE()[idx], &item_before, sizeof(item_before)) != 0))
+    if ((did_change || memcmp(& ((hl_group_T *)((highlight_ga.ga_data))) [idx], &item_before, sizeof(item_before)) != 0))
     {
         if (!updating_screen)
         {
@@ -67917,43 +66096,41 @@
     cterm_normal_fg_color = 0;
     cterm_normal_fg_bold = 0;
     cterm_normal_bg_color = 0;
-    fallback_fg_rgb = INVALCOLOR;
-    fallback_bg_rgb = INVALCOLOR;
+    fallback_fg_rgb =  (( long )0x1ffffff) ;
+    fallback_bg_rgb =  (( long )0x1ffffff) ;
 }
 
     static int
 hl_has_settings(int idx, int check_link)
 {
-    return HL_TABLE()[idx].sg_cleared == 0
-         && (  HL_TABLE()[idx].sg_term_attr != 0 || HL_TABLE()[idx].sg_cterm_attr != 0 || HL_TABLE()[idx].sg_cterm_fg != 0 || HL_TABLE()[idx].sg_cterm_bg != 0 || HL_TABLE()[idx].sg_cterm_font != 0 || (check_link && (HL_TABLE()[idx].sg_set & SG_LINK)));
+    return  ((hl_group_T *)((highlight_ga.ga_data))) [idx].sg_cleared == 0
+         && (   ((hl_group_T *)((highlight_ga.ga_data))) [idx].sg_term_attr != 0 ||  ((hl_group_T *)((highlight_ga.ga_data))) [idx].sg_cterm_attr != 0 ||  ((hl_group_T *)((highlight_ga.ga_data))) [idx].sg_cterm_fg != 0 ||  ((hl_group_T *)((highlight_ga.ga_data))) [idx].sg_cterm_bg != 0 ||  ((hl_group_T *)((highlight_ga.ga_data))) [idx].sg_cterm_font != 0 || (check_link && ( ((hl_group_T *)((highlight_ga.ga_data))) [idx].sg_set & SG_LINK)));
 }
 
     static void
 highlight_clear(int idx)
 {
-    HL_TABLE()[idx].sg_cleared = TRUE;
+     ((hl_group_T *)((highlight_ga.ga_data))) [idx].sg_cleared = TRUE;
 
-    HL_TABLE()[idx].sg_term = 0;
-    VIM_CLEAR(HL_TABLE()[idx].sg_start);
-    VIM_CLEAR(HL_TABLE()[idx].sg_stop);
-    HL_TABLE()[idx].sg_term_attr = 0;
-    HL_TABLE()[idx].sg_cterm = 0;
-    HL_TABLE()[idx].sg_cterm_bold = FALSE;
-    HL_TABLE()[idx].sg_cterm_fg = 0;
-    HL_TABLE()[idx].sg_cterm_bg = 0;
-    HL_TABLE()[idx].sg_cterm_attr = 0;
-    HL_TABLE()[idx].sg_cterm_font = 0;
-    HL_TABLE()[idx].sg_link = HL_TABLE()[idx].sg_deflink;
+     ((hl_group_T *)((highlight_ga.ga_data))) [idx].sg_term = 0;
+     vim_free( ((hl_group_T *)((highlight_ga.ga_data))) [idx].sg_start);
+     ( ((hl_group_T *)((highlight_ga.ga_data))) [idx].sg_start) = NULL;
+     vim_free( ((hl_group_T *)((highlight_ga.ga_data))) [idx].sg_stop);
+     ( ((hl_group_T *)((highlight_ga.ga_data))) [idx].sg_stop) = NULL;
+     ((hl_group_T *)((highlight_ga.ga_data))) [idx].sg_term_attr = 0;
+     ((hl_group_T *)((highlight_ga.ga_data))) [idx].sg_cterm = 0;
+     ((hl_group_T *)((highlight_ga.ga_data))) [idx].sg_cterm_bold = FALSE;
+     ((hl_group_T *)((highlight_ga.ga_data))) [idx].sg_cterm_fg = 0;
+     ((hl_group_T *)((highlight_ga.ga_data))) [idx].sg_cterm_bg = 0;
+     ((hl_group_T *)((highlight_ga.ga_data))) [idx].sg_cterm_attr = 0;
+     ((hl_group_T *)((highlight_ga.ga_data))) [idx].sg_cterm_font = 0;
+     ((hl_group_T *)((highlight_ga.ga_data))) [idx].sg_link =  ((hl_group_T *)((highlight_ga.ga_data))) [idx].sg_deflink;
 }
 
 static garray_T term_attr_table = {0, 0, 0, 0, NULL};
 
-#define TERM_ATTR_ENTRY(idx) ((attrentry_T *)term_attr_table.ga_data)[idx]
-
 static garray_T cterm_attr_table = {0, 0, 0, 0, NULL};
 
-#define CTERM_ATTR_ENTRY(idx) ((attrentry_T *)cterm_attr_table.ga_data)[idx]
-
     static int
 get_attr_entry(garray_T *table, attrentry_T *aep)
 {
@@ -67967,14 +66144,13 @@
     for (i = 0; i < table->ga_len; ++i)
     {
         taep = &(((attrentry_T *)table->ga_data)[i]);
-        if (       aep->ae_attr == taep->ae_attr && ((table == &term_attr_table && (aep->ae_u.term.start == NULL) == (taep->ae_u.term.start == NULL) && (aep->ae_u.term.start == NULL || STRCMP(aep->ae_u.term.start, taep->ae_u.term.start) == 0) && (aep->ae_u.term.stop == NULL) == (taep->ae_u.term.stop == NULL) && (aep->ae_u.term.stop == NULL || STRCMP(aep->ae_u.term.stop, taep->ae_u.term.stop) == 0)) || (table == &cterm_attr_table && aep->ae_u.cterm.fg_color == taep->ae_u.cterm.fg_color && aep->ae_u.cterm.bg_color == taep->ae_u.cterm.bg_color && aep->ae_u.cterm.ul_color == taep->ae_u.cterm.ul_color && aep->ae_u.cterm.font == taep->ae_u.cterm.font)))
+        if (       aep->ae_attr == taep->ae_attr && ((table == &term_attr_table && (aep->ae_u.term.start == NULL) == (taep->ae_u.term.start == NULL) && (aep->ae_u.term.start == NULL ||  strcmp((char *)(aep->ae_u.term.start), (char *)(taep->ae_u.term.start))  == 0) && (aep->ae_u.term.stop == NULL) == (taep->ae_u.term.stop == NULL) && (aep->ae_u.term.stop == NULL ||  strcmp((char *)(aep->ae_u.term.stop), (char *)(taep->ae_u.term.stop))  == 0)) || (table == &cterm_attr_table && aep->ae_u.cterm.fg_color == taep->ae_u.cterm.fg_color && aep->ae_u.cterm.bg_color == taep->ae_u.cterm.bg_color && aep->ae_u.cterm.ul_color == taep->ae_u.cterm.ul_color && aep->ae_u.cterm.font == taep->ae_u.cterm.font)))
         {
-
-        return i + ATTR_OFF;
+        return i +  (HL_ALL + 1) ;
         }
     }
 
-    if (table->ga_len + ATTR_OFF > MAX_TYPENR)
+    if (table->ga_len +  (HL_ALL + 1)  > MAX_TYPENR)
     {
         if (recursive)
         {
@@ -68001,7 +66177,7 @@
     }
 
     taep = &(((attrentry_T *)table->ga_data)[table->ga_len]);
-    CLEAR_POINTER(taep);
+      memset(((taep)), (0), (sizeof(*(taep))))  ;
     taep->ae_attr = aep->ae_attr;
     if (table == &term_attr_table)
     {
@@ -68030,7 +66206,7 @@
         taep->ae_u.cterm.font = aep->ae_u.cterm.font;
     }
     ++table->ga_len;
-    return (table->ga_len - 1 + ATTR_OFF);
+    return (table->ga_len - 1 +  (HL_ALL + 1) );
 }
 
     void
@@ -68062,10 +66238,10 @@
     }
     if (char_attr <= HL_ALL && prim_attr <= HL_ALL)
     {
-        return ATTR_COMBINE(char_attr, prim_attr);
+        return  ((((prim_attr) & HL_NOCOMBINE) ? (prim_attr) : (char_attr)) | (prim_attr)) ;
     }
 
-    if (IS_CTERM)
+    if ( (t_colors > 1) )
     {
         if (char_attr > HL_ALL)
         {
@@ -68077,7 +66253,7 @@
         }
         else
         {
-            CLEAR_FIELD(new_en);
+              memset((&(new_en)), (0), (sizeof(new_en)))  ;
             if (char_attr <= HL_ALL)
             {
                 new_en.ae_attr = char_attr;
@@ -68086,14 +66262,14 @@
 
         if (prim_attr <= HL_ALL)
         {
-                new_en.ae_attr = ATTR_COMBINE(new_en.ae_attr, prim_attr);
+                new_en.ae_attr =  ((((prim_attr) & HL_NOCOMBINE) ? (prim_attr) : (new_en.ae_attr)) | (prim_attr)) ;
         }
         else
         {
             prim_aep = syn_cterm_attr2entry(prim_attr);
             if (prim_aep != NULL)
             {
-                new_en.ae_attr = ATTR_COMBINE(new_en.ae_attr, prim_aep->ae_attr);
+                new_en.ae_attr =  ((((prim_aep->ae_attr) & HL_NOCOMBINE) ? (prim_aep->ae_attr) : (new_en.ae_attr)) | (prim_aep->ae_attr)) ;
                 if (prim_aep->ae_u.cterm.fg_color > 0)
                 {
                     new_en.ae_u.cterm.fg_color = prim_aep->ae_u.cterm.fg_color;
@@ -68125,7 +66301,7 @@
     }
     else
     {
-        CLEAR_FIELD(new_en);
+          memset((&(new_en)), (0), (sizeof(new_en)))  ;
         if (char_attr <= HL_ALL)
         {
             new_en.ae_attr = char_attr;
@@ -68134,14 +66310,14 @@
 
     if (prim_attr <= HL_ALL)
     {
-        new_en.ae_attr = ATTR_COMBINE(new_en.ae_attr, prim_attr);
+        new_en.ae_attr =  ((((prim_attr) & HL_NOCOMBINE) ? (prim_attr) : (new_en.ae_attr)) | (prim_attr)) ;
     }
     else
     {
         prim_aep = syn_term_attr2entry(prim_attr);
         if (prim_aep != NULL)
         {
-            new_en.ae_attr = ATTR_COMBINE(new_en.ae_attr, prim_aep->ae_attr);
+            new_en.ae_attr =  ((((prim_aep->ae_attr) & HL_NOCOMBINE) ? (prim_aep->ae_attr) : (new_en.ae_attr)) | (prim_aep->ae_attr)) ;
             if (prim_aep->ae_u.term.start != NULL)
             {
                 new_en.ae_u.term.start = prim_aep->ae_u.term.start;
@@ -68225,7 +66401,7 @@
 }
 
     static bool
-resolve_color_to_rgb(int cterm_c, guicolor_T rgb UNUSED, int *r, int *g, int *b)
+resolve_color_to_rgb(int cterm_c,  long  rgb  __attribute__((unused)) , int *r, int *g, int *b)
 {
     if (cterm_c > 0)
     {
@@ -68235,8 +66411,8 @@
     return false;
 }
 
-    static guicolor_T
-resolve_fallback_color(int cterm_c, guicolor_T rgb, guicolor_T default_rgb)
+    static  long 
+resolve_fallback_color(int cterm_c,  long  rgb,  long  default_rgb)
 {
     int red;
     int green;
@@ -68254,19 +66430,19 @@
     static void
 resolve_fallback_fg_to_rgb(void)
 {
-    guicolor_T fgcolor_or_gui_fgcolor = INVALCOLOR;
+     long  fgcolor_or_gui_fgcolor =  (( long )0x1ffffff) ;
     fallback_fg_rgb = resolve_fallback_color(cterm_normal_fg_color, fgcolor_or_gui_fgcolor, (*p_bg == 'l') ? 0x000000 : 0xFFFFFF);
 }
 
     static void
 resolve_fallback_bg_to_rgb(void)
 {
-    guicolor_T bgcolor_or_gui_bgcolor = INVALCOLOR;
+     long  bgcolor_or_gui_bgcolor =  (( long )0x1ffffff) ;
     fallback_bg_rgb = resolve_fallback_color(cterm_normal_bg_color, bgcolor_or_gui_bgcolor, (*p_bg == 'l') ? 0xFFFFFF : 0x000000);
 }
 
     static int
-blend_cterm_colors(int popup_c, guicolor_T popup_rgb, int under_c, guicolor_T under_rgb, int default_rgb, int blend_val)
+blend_cterm_colors(int popup_c,  long  popup_rgb, int under_c,  long  under_rgb, int default_rgb, int blend_val)
 {
     int pr;
     int pg;
@@ -68307,7 +66483,7 @@
 }
 
     int
-hl_blend_attr(int char_attr, int popup_attr, int blend, int blend_fg UNUSED)
+hl_blend_attr(int char_attr, int popup_attr, int blend, int blend_fg  __attribute__((unused)) )
 {
     attrentry_T *char_aep = NULL;
     attrentry_T *popup_aep;
@@ -68319,7 +66495,7 @@
         return char_attr;
     }
 
-    if (IS_CTERM)
+    if ( (t_colors > 1) )
     {
         if (char_attr > HL_ALL)
         {
@@ -68331,7 +66507,7 @@
         }
         else
         {
-            CLEAR_FIELD(new_en);
+              memset((&(new_en)), (0), (sizeof(new_en)))  ;
             if (char_attr <= HL_ALL)
             {
                 new_en.ae_attr = char_attr;
@@ -68340,7 +66516,7 @@
 
         if (popup_attr <= HL_ALL)
         {
-            CLEAR_FIELD(tmp_en);
+              memset((&(tmp_en)), (0), (sizeof(tmp_en)))  ;
             tmp_en.ae_attr = popup_attr;
             popup_aep = &tmp_en;
 
@@ -68353,8 +66529,8 @@
 
         if (popup_aep != NULL)
         {
-            guicolor_T popup_bg_rgb = INVALCOLOR;
-            if (COLOR_INVALID(popup_bg_rgb) && popup_aep->ae_u.cterm.bg_color == 0)
+             long  popup_bg_rgb =  (( long )0x1ffffff) ;
+            if ( ((popup_bg_rgb) ==  (( long )0x1ffffff)  || (popup_bg_rgb) ==  (( long )0x1fffffe) )  && popup_aep->ae_u.cterm.bg_color == 0)
             {
                 popup_bg_rgb = fallback_bg_rgb;
             }
@@ -68382,13 +66558,13 @@
             {
                 int under_fg = (char_aep != NULL)
                     ? char_aep->ae_u.cterm.fg_color : 0;
-                guicolor_T under_fg_rgb = INVALCOLOR;
+                 long  under_fg_rgb =  (( long )0x1ffffff) ;
                 new_en.ae_u.cterm.fg_color = blend_cterm_colors(popup_aep->ae_u.cterm.bg_color, popup_bg_rgb, under_fg, under_fg_rgb, fallback_fg_rgb, blend);
             }
             {
                 int under_bg = (char_aep != NULL)
                     ? char_aep->ae_u.cterm.bg_color : 0;
-                guicolor_T under_bg_rgb = INVALCOLOR;
+                 long  under_bg_rgb =  (( long )0x1ffffff) ;
                 new_en.ae_u.cterm.bg_color = blend_cterm_colors(popup_aep->ae_u.cterm.bg_color, popup_bg_rgb, under_bg, under_bg_rgb, fallback_bg_rgb, blend);
             }
         }
@@ -68405,7 +66581,7 @@
     }
     else
     {
-        CLEAR_FIELD(new_en);
+          memset((&(new_en)), (0), (sizeof(new_en)))  ;
         if (char_attr <= HL_ALL)
         {
             new_en.ae_attr = char_attr;
@@ -68416,14 +66592,14 @@
 }
 
     int
-hl_pum_blend_attr(int char_attr, int popup_attr, int blend UNUSED)
+hl_pum_blend_attr(int char_attr, int popup_attr, int blend  __attribute__((unused)) )
 {
     attrentry_T *char_aep = NULL;
     attrentry_T *popup_aep;
     attrentry_T new_en;
     attrentry_T tmp_en;
 
-    if (IS_CTERM)
+    if ( (t_colors > 1) )
     {
         if (char_attr > HL_ALL)
         {
@@ -68435,7 +66611,7 @@
         }
         else
         {
-            CLEAR_FIELD(new_en);
+              memset((&(new_en)), (0), (sizeof(new_en)))  ;
             if (char_attr <= HL_ALL)
             {
                 new_en.ae_attr = char_attr;
@@ -68444,7 +66620,7 @@
 
         if (popup_attr <= HL_ALL)
         {
-            CLEAR_FIELD(tmp_en);
+              memset((&(tmp_en)), (0), (sizeof(tmp_en)))  ;
             tmp_en.ae_attr = popup_attr;
             popup_aep = &tmp_en;
 
@@ -68457,8 +66633,8 @@
 
         if (popup_aep != NULL)
         {
-            guicolor_T popup_bg_rgb = INVALCOLOR;
-            if (COLOR_INVALID(popup_bg_rgb) && popup_aep->ae_u.cterm.bg_color == 0)
+             long  popup_bg_rgb =  (( long )0x1ffffff) ;
+            if ( ((popup_bg_rgb) ==  (( long )0x1ffffff)  || (popup_bg_rgb) ==  (( long )0x1fffffe) )  && popup_aep->ae_u.cterm.bg_color == 0)
             {
                 popup_bg_rgb = fallback_bg_rgb;
             }
@@ -68466,13 +66642,13 @@
             {
                 int under_fg = (char_aep != NULL)
                     ? char_aep->ae_u.cterm.fg_color : 0;
-                guicolor_T under_fg_rgb = INVALCOLOR;
+                 long  under_fg_rgb =  (( long )0x1ffffff) ;
                 new_en.ae_u.cterm.fg_color = blend_cterm_colors(popup_aep->ae_u.cterm.bg_color, popup_bg_rgb, under_fg, under_fg_rgb, fallback_fg_rgb, blend);
             }
             {
                 int under_bg = (char_aep != NULL)
                     ? char_aep->ae_u.cterm.bg_color : 0;
-                guicolor_T under_bg_rgb = INVALCOLOR;
+                 long  under_bg_rgb =  (( long )0x1ffffff) ;
                 new_en.ae_u.cterm.bg_color = blend_cterm_colors(popup_aep->ae_u.cterm.bg_color, popup_bg_rgb, under_bg, under_bg_rgb, fallback_bg_rgb, blend);
             }
         }
@@ -68489,7 +66665,7 @@
     }
     else
     {
-        CLEAR_FIELD(new_en);
+          memset((&(new_en)), (0), (sizeof(new_en)))  ;
         if (char_attr <= HL_ALL)
         {
             new_en.ae_attr = char_attr;
@@ -68504,7 +66680,7 @@
 {
     attrentry_T *aep;
 
-        if (IS_CTERM)
+        if ( (t_colors > 1) )
         {
             aep = syn_cterm_attr2entry(attr);
         }
@@ -68523,28 +66699,28 @@
     attrentry_T *
 syn_term_attr2entry(int attr)
 {
-    attr -= ATTR_OFF;
+    attr -=  (HL_ALL + 1) ;
     if (attr >= term_attr_table.ga_len)
     {
         return NULL;
     }
-    return &(TERM_ATTR_ENTRY(attr));
+    return &( ((attrentry_T *)term_attr_table.ga_data)[attr] );
 }
 
     attrentry_T *
 syn_cterm_attr2entry(int attr)
 {
-    attr -= ATTR_OFF;
+    attr -=  (HL_ALL + 1) ;
     if (attr >= cterm_attr_table.ga_len)
     {
         return NULL;
     }
-    return &(CTERM_ATTR_ENTRY(attr));
+    return &( ((attrentry_T *)cterm_attr_table.ga_data)[attr] );
 }
 
-#define LIST_ATTR   1
-#define LIST_STRING 2
-#define LIST_INT    3
+enum { LIST_ATTR = 1 };
+enum { LIST_STRING = 2 };
+enum { LIST_INT = 3 };
 
     static void
 highlight_list_one(int id)
@@ -68552,7 +66728,7 @@
     hl_group_T      *sgp;
     int             didh = FALSE;
 
-    sgp = &HL_TABLE()[id - 1];
+    sgp = & ((hl_group_T *)((highlight_ga.ga_data))) [id - 1];
 
     if (message_filtered(sgp->sg_name))
     {
@@ -68573,9 +66749,9 @@
     {
         (void)syn_list_header(didh, 9999, id);
         didh = TRUE;
-        msg_puts_attr("links to", HL_ATTR(HLF_D));
+        msg_puts_attr("links to",  highlight_attr[(int)(HLF_D)] );
         msg_putchar(' ');
-        msg_outtrans(HL_TABLE()[HL_TABLE()[id - 1].sg_link - 1].sg_name);
+        msg_outtrans( ((hl_group_T *)((highlight_ga.ga_data))) [ ((hl_group_T *)((highlight_ga.ga_data))) [id - 1].sg_link - 1].sg_name);
     }
 
     if (!didh)
@@ -68601,16 +66777,16 @@
 
         buf[0] = NUL;
         buflen = 0;
-        for (int i = 0; i < (int)ARRAY_LENGTH(highlight_index_tab); ++i)
+        for (int i = 0; i < (int) (sizeof(highlight_index_tab) / sizeof((highlight_index_tab)[0])) ; ++i)
         {
             if (iarg & highlight_index_tab[i]->key)
             {
                 if (buflen > 0)
                 {
-                    STRCPY(buf + buflen, (char_u *)",");
+                     strcpy((char *)(buf + buflen), (char *)((char_u *)",")) ;
                     ++buflen;
                 }
-                STRCPY(buf + buflen, highlight_index_tab[i]->value.string);
+                 strcpy((char *)(buf + buflen), (char *)(highlight_index_tab[i]->value.string)) ;
                 buflen += highlight_index_tab[i]->value.length;
                 iarg &= ~highlight_index_tab[i]->key;
             }
@@ -68637,14 +66813,14 @@
 
     ts = highlight_arg_to_string(type, iarg, sarg, buf);
 
-    (void)syn_list_header(didh, (int)(vim_strsize(ts) + STRLEN(name) + 1), id);
+    (void)syn_list_header(didh, (int)(vim_strsize(ts) +  strlen((char *)(name))  + 1), id);
     didh = TRUE;
     if (!got_int)
     {
         if (*name != NUL)
         {
-            msg_puts_attr(name, HL_ATTR(HLF_D));
-            msg_puts_attr("=", HL_ATTR(HLF_D));
+            msg_puts_attr(name,  highlight_attr[(int)(HLF_D)] );
+            msg_puts_attr("=",  highlight_attr[(int)(HLF_D)] );
         }
         msg_outtrans(ts);
     }
@@ -68665,7 +66841,7 @@
         {
             return TRUE;
         }
-        msg_outtrans(HL_TABLE()[id - 1].sg_name);
+        msg_outtrans( ((hl_group_T *)((highlight_ga.ga_data))) [id - 1].sg_name);
         name_col = msg_col;
         endcol = 15;
     }
@@ -68713,9 +66889,9 @@
 set_hl_attr(int         idx)
 {
     attrentry_T     at_en;
-    hl_group_T      *sgp = HL_TABLE() + idx;
+    hl_group_T      *sgp =  ((hl_group_T *)((highlight_ga.ga_data)))  + idx;
 
-    if (sgp->sg_name_u != NULL && STRCMP(sgp->sg_name_u, "NORMAL") == 0)
+    if (sgp->sg_name_u != NULL &&  strcmp((char *)(sgp->sg_name_u), (char *)("NORMAL"))  == 0)
     {
         return;
     }
@@ -68773,7 +66949,7 @@
     {
         len = MAX_SYN_NAME;
     }
-    mch_memmove(name_u, name, len);
+     memmove((char *)(name_u), (char *)(name), len) ;
     name_u[len] = NUL;
     vim_strup(name_u);
     if (!highlight_ht_inited)
@@ -68781,17 +66957,17 @@
         return 0;
     }
     hi = hash_find(&highlight_ht, name_u);
-    if (HASHITEM_EMPTY(hi))
+    if ( ((hi)->hi_key == NULL || (hi)->hi_key == &hash_removed) )
     {
         return 0;
     }
-    return HI2HLNAME(hi)->hn_id;
+    return  ((hlname_T *)((hi)->hi_key -  offsetof(hlname_T, hn_key) )) ->hn_id;
 }
 
     int
 syn_name2id(char_u *name)
 {
-    return syn_name2id_len(name, (int)STRLEN(name));
+    return syn_name2id_len(name, (int) strlen((char *)(name)) );
 }
 
     int
@@ -68850,9 +67026,9 @@
             vim_free(name);
             return 0;
         }
-        else if (!ASCII_ISALNUM(*p) && *p != '_' && *p != '.' && *p != '-')
+        else if (! ( ( ((unsigned)(*p) - 'A' < 26)  ||  ((unsigned)(*p) - 'a' < 26) )  ||  ((unsigned)(*p) - '0' < 10) )  && *p != '_' && *p != '.' && *p != '-')
         {
-            msg_source(HL_ATTR(HLF_W));
+            msg_source( highlight_attr[(int)(HLF_W)] );
             msg(_("W18: Invalid character in group name"));
             break;
         }
@@ -68881,7 +67057,7 @@
 
     {
         hlname_T    *hn;
-        int         len = (int)STRLEN(name);
+        int         len = (int) strlen((char *)(name)) ;
 
         hn = alloc(offsetof(hlname_T, hn_key) + len + 1);
         if (hn == NULL)
@@ -68895,9 +67071,9 @@
         name_up = hn->hn_key;
     }
 
-    CLEAR_POINTER(&(HL_TABLE()[highlight_ga.ga_len]));
-    HL_TABLE()[highlight_ga.ga_len].sg_name = name;
-    HL_TABLE()[highlight_ga.ga_len].sg_name_u = name_up;
+      memset(((&( ((hl_group_T *)((highlight_ga.ga_data))) [highlight_ga.ga_len]))), (0), (sizeof(*(&( ((hl_group_T *)((highlight_ga.ga_data))) [highlight_ga.ga_len])))))  ;
+     ((hl_group_T *)((highlight_ga.ga_data))) [highlight_ga.ga_len].sg_name = name;
+     ((hl_group_T *)((highlight_ga.ga_data))) [highlight_ga.ga_len].sg_name_u = name_up;
     hash_add(&highlight_ht, name_up, "highlight");
     ++highlight_ga.ga_len;
 
@@ -68910,13 +67086,13 @@
     hashitem_T *hi;
 
     --highlight_ga.ga_len;
-    hi = hash_find(&highlight_ht, HL_TABLE()[highlight_ga.ga_len].sg_name_u);
-    if (!HASHITEM_EMPTY(hi))
+    hi = hash_find(&highlight_ht,  ((hl_group_T *)((highlight_ga.ga_data))) [highlight_ga.ga_len].sg_name_u);
+    if (! ((hi)->hi_key == NULL || (hi)->hi_key == &hash_removed) )
     {
         hash_remove(&highlight_ht, hi, "highlight");
     }
-    vim_free(HL_TABLE()[highlight_ga.ga_len].sg_name);
-    vim_free(HL_TABLE()[highlight_ga.ga_len].sg_name_u - HLNAME_KEY_OFF);
+    vim_free( ((hl_group_T *)((highlight_ga.ga_data))) [highlight_ga.ga_len].sg_name);
+    vim_free( ((hl_group_T *)((highlight_ga.ga_data))) [highlight_ga.ga_len].sg_name_u -  offsetof(hlname_T, hn_key) );
 }
 
     int
@@ -68926,9 +67102,9 @@
     hl_group_T  *sgp;
 
     hl_id = syn_get_final_id(hl_id);
-    sgp = &HL_TABLE()[hl_id - 1];
+    sgp = & ((hl_group_T *)((highlight_ga.ga_data))) [hl_id - 1];
 
-        if (IS_CTERM)
+        if ( (t_colors > 1) )
         {
             attr = sgp->sg_cterm_attr;
         }
@@ -68955,7 +67131,7 @@
     {
         int tmp = hl_id;
 
-        sgp = &HL_TABLE()[hl_id - 1];
+        sgp = & ((hl_group_T *)((highlight_ga.ga_data))) [hl_id - 1];
         if (sgp->sg_link == 0 || sgp->sg_link > highlight_ga.ga_len)
         {
             break;
@@ -68998,7 +67174,7 @@
     {
         if (i)
         {
-            if (default_hl != NULL && p_hl != NULL && STRCMP(default_hl, p_hl) == 0)
+            if (default_hl != NULL && p_hl != NULL &&  strcmp((char *)(default_hl), (char *)(p_hl))  == 0)
             {
                 continue;
             }
@@ -69032,7 +67208,7 @@
             id = 0;
             for ( ; *p && *p != ','; ++p)
             {
-                if (VIM_ISWHITE(*p))
+                if ( ((*p) == ' ' || (*p) == '\t') )
                 {
                     continue;
                 }
@@ -69086,7 +67262,7 @@
                                 end = vim_strchr(p, ',');
                                 if (end == NULL)
                                 {
-                                    end = p + STRLEN(p);
+                                    end = p +  strlen((char *)(p)) ;
                                 }
                                 id = syn_check_group(p, (int)(end - p));
                                 if (id == 0)
@@ -69108,9 +67284,9 @@
         }
     }
 
-    FOR_ALL_WINDOWS(wp)
+     for ((wp) = firstwin; (wp) != NULL; (wp) = (wp)->w_next) 
     {
-        char *errmsg = update_winhighlight(wp, wp->w_p_whl);
+        char *errmsg = update_winhighlight(wp, wp-> w_onebuf_opt.wo_whl );
 
         if (errmsg != NULL)
         {
@@ -69153,7 +67329,7 @@
     }
 
     include_default = 0;
-    if (STRNCMP("default", arg, p - arg) == 0)
+    if ( strncmp((char *)("default"), (char *)(arg), (p - arg))  == 0)
     {
         arg = skipwhite(p);
         xp->xp_pattern = arg;
@@ -69170,7 +67346,7 @@
         highlight_list();
         expand_group = FALSE;
     }
-    if (STRNCMP("link", arg, p - arg) == 0)
+    if ( strncmp((char *)("link"), (char *)(arg), (p - arg))  == 0)
     {
         xp->xp_pattern = skipwhite(p);
         p = skiptowhite(xp->xp_pattern);
@@ -69182,7 +67358,7 @@
         }
         expand_group = FALSE;
     }
-    else if (STRNCMP("clear", arg, p - arg) == 0)
+    else if ( strncmp((char *)("clear"), (char *)(arg), (p - arg))  == 0)
     {
         xp->xp_pattern = skipwhite(p);
         p = skiptowhite(xp->xp_pattern);
@@ -69222,7 +67398,7 @@
                     expand_hi_include_orig = FALSE;
                 }
 
-                if (STRNCMP(arg, "term=", 5) == 0 || STRNCMP(arg, "cterm=", 6) == 0 || STRNCMP(arg, "gui=", 4) == 0)
+                if ( strncmp((char *)(arg), (char *)("term="), (5))  == 0 ||  strncmp((char *)(arg), (char *)("cterm="), (6))  == 0 ||  strncmp((char *)(arg), (char *)("gui="), (4))  == 0)
                 {
                     char_u *comma = vim_strrchr(p + 1, ',');
                     if (comma != NULL)
@@ -69247,7 +67423,7 @@
 
     for (i = 10; --i >= 0; )
     {
-        highlight_list_two(i, HL_ATTR(HLF_D));
+        highlight_list_two(i,  highlight_attr[(int)(HLF_D)] );
     }
     for (i = 40; --i >= 0; )
     {
@@ -69265,20 +67441,20 @@
 }
 
     char_u *
-get_highlight_name(expand_T *xp UNUSED, int idx)
+get_highlight_name(expand_T *xp  __attribute__((unused)) , int idx)
 {
     return get_highlight_name_ext(xp, idx, TRUE);
 }
 
     char_u *
-get_highlight_name_ext(expand_T *xp UNUSED, int idx, int skip_cleared)
+get_highlight_name_ext(expand_T *xp  __attribute__((unused)) , int idx, int skip_cleared)
 {
     if (idx < 0)
     {
         return NULL;
     }
 
-    if (skip_cleared && idx < highlight_ga.ga_len && HL_TABLE()[idx].sg_cleared)
+    if (skip_cleared && idx < highlight_ga.ga_len &&  ((hl_group_T *)((highlight_ga.ga_data))) [idx].sg_cleared)
     {
         return (char_u *)"";
     }
@@ -69303,11 +67479,11 @@
     {
         return NULL;
     }
-    return HL_TABLE()[idx].sg_name;
+    return  ((hl_group_T *)((highlight_ga.ga_data))) [idx].sg_name;
 }
 
     static char_u *
-get_highlight_attr_name(expand_T *xp UNUSED, int idx)
+get_highlight_attr_name(expand_T *xp  __attribute__((unused)) , int idx)
 {
     if (idx == 0)
     {
@@ -69320,10 +67496,10 @@
             return (char_u*)"";
         }
     }
-    if (idx < (int)ARRAY_LENGTH(highlight_index_tab) + 1)
+    if (idx < (int) (sizeof(highlight_index_tab) / sizeof((highlight_index_tab)[0]))  + 1)
     {
         char_u *value = highlight_index_tab[idx-1]->value.string;
-        if (expand_hi_curvalue != NULL && STRCMP(expand_hi_curvalue, value) == 0)
+        if (expand_hi_curvalue != NULL &&  strcmp((char *)(expand_hi_curvalue), (char *)(value))  == 0)
         {
             return (char_u*)"";
         }
@@ -69333,7 +67509,7 @@
 }
 
     static char_u *
-get_highlight_cterm_color(expand_T *xp UNUSED, int idx)
+get_highlight_cterm_color(expand_T *xp  __attribute__((unused)) , int idx)
 {
     if (idx == 0)
     {
@@ -69354,7 +67530,7 @@
     {
         return (char_u*)"bg";
     }
-    if (idx < (int)ARRAY_LENGTH(color_name_tab) + 3)
+    if (idx < (int) (sizeof(color_name_tab) / sizeof((color_name_tab)[0]))  + 3)
     {
         char_u *value = color_name_tab[idx-3].value.string;
         return value;
@@ -69363,7 +67539,7 @@
 }
 
     static char_u *
-get_highlight_group_key(expand_T *xp UNUSED, int idx)
+get_highlight_group_key(expand_T *xp  __attribute__((unused)) , int idx)
 {
     static char *(p_hi_group_key_values[]) =
     {
@@ -69378,7 +67554,7 @@
         "NONE",
     };
 
-    if (idx < (int)ARRAY_LENGTH(p_hi_group_key_values))
+    if (idx < (int) (sizeof(p_hi_group_key_values) / sizeof((p_hi_group_key_values)[0])) )
     {
         return (char_u*)p_hi_group_key_values[idx];
     }
@@ -69400,11 +67576,11 @@
 
         if (expand_hi_synid != 0)
         {
-            sgp = &HL_TABLE()[expand_hi_synid - 1];
+            sgp = & ((hl_group_T *)((highlight_ga.ga_data))) [expand_hi_synid - 1];
         }
 
         char_u      *name_end = xp->xp_line + expand_hi_equal_col;
-        if (name_end - xp->xp_line >= 5 && STRNCMP(name_end - 5, " term", 5) == 0)
+        if (name_end - xp->xp_line >= 5 &&  strncmp((char *)(name_end - 5), (char *)(" term"), (5))  == 0)
         {
             expandfunc = get_highlight_attr_name;
             if (sgp)
@@ -69413,7 +67589,7 @@
                 iarg = sgp->sg_term;
             }
         }
-        else if (name_end - xp->xp_line >= 6 && STRNCMP(name_end - 6, " cterm", 6) == 0)
+        else if (name_end - xp->xp_line >= 6 &&  strncmp((char *)(name_end - 6), (char *)(" cterm"), (6))  == 0)
         {
             expandfunc = get_highlight_attr_name;
             if (sgp)
@@ -69422,7 +67598,7 @@
                 iarg = sgp->sg_cterm;
             }
         }
-        else if (name_end - xp->xp_line >= 8 && STRNCMP(name_end - 8, " ctermfg", 8) == 0)
+        else if (name_end - xp->xp_line >= 8 &&  strncmp((char *)(name_end - 8), (char *)(" ctermfg"), (8))  == 0)
         {
             expandfunc = get_highlight_cterm_color;
             if (sgp)
@@ -69431,7 +67607,7 @@
                 iarg = sgp->sg_cterm_fg;
             }
         }
-        else if (name_end - xp->xp_line >= 8 && STRNCMP(name_end - 8, " ctermbg", 8) == 0)
+        else if (name_end - xp->xp_line >= 8 &&  strncmp((char *)(name_end - 8), (char *)(" ctermbg"), (8))  == 0)
         {
             expandfunc = get_highlight_cterm_color;
             if (sgp)
@@ -69440,7 +67616,7 @@
                 iarg = sgp->sg_cterm_bg;
             }
         }
-        else if (name_end - xp->xp_line >= 8 && STRNCMP(name_end - 8, " ctermul", 8) == 0)
+        else if (name_end - xp->xp_line >= 8 &&  strncmp((char *)(name_end - 8), (char *)(" ctermul"), (8))  == 0)
         {
             expandfunc = get_highlight_cterm_color;
             if (sgp)
@@ -69582,7 +67758,7 @@
 
     hl_overrides_T *set;
 
-    set = ALLOC_ONE(hl_overrides_T);
+    set =  (hl_overrides_T *)alloc(sizeof(hl_overrides_T)) ;
     if (set == NULL)
     {
         return false;
@@ -69654,7 +67830,7 @@
         return NULL;
     }
 
-    arr = ALLOC_MULT(hl_override_T, num);
+    arr =  (hl_override_T *)alloc(sizeof(hl_override_T) * (num)) ;
     if (arr == NULL)
     {
         *errmsg = e_out_of_memory;
@@ -69700,7 +67876,7 @@
 
         if (tmp == NULL)
         {
-            tolen = (int)STRLEN(p);
+            tolen = (int) strlen((char *)(p)) ;
         }
         else
         {
@@ -69754,7 +67930,7 @@
                     goto fail;
                 }
 
-                if (ids[k] == &fromid && STRCMP(HL_TABLE()[*ids[k] - 1].sg_name_u, "NORMAL") == 0)
+                if (ids[k] == &fromid &&  strcmp((char *)( ((hl_group_T *)((highlight_ga.ga_data))) [*ids[k] - 1].sg_name_u), (char *)("NORMAL"))  == 0)
                 {
                     *ids[k] = -HLF_WIN;
                 }
@@ -69830,7 +68006,7 @@
     char *
 update_wincolor(win_T *wp, char_u *opt)
 {
-    char_u  *str = *opt == NUL ? "" : alloc(sizeof("!(:") + STRLEN(opt));
+    char_u  *str = *opt == NUL ? "" : alloc(sizeof("!(:") +  strlen((char *)(opt)) );
     char    *errmsg;
 
     if (str == NULL)
@@ -69887,7 +68063,7 @@
 }
 
     long
-get_sw_value_col(buf_T *buf, colnr_T col UNUSED, int left UNUSED)
+get_sw_value_col(buf_T *buf, colnr_T col  __attribute__((unused)) , int left  __attribute__((unused)) )
 {
     return buf->b_p_sw ? buf->b_p_sw :
         buf->b_p_ts;
@@ -69968,7 +68144,7 @@
         {
             ind_done = 0;
 
-            while (todo > 0 && VIM_ISWHITE(*p))
+            while (todo > 0 &&  ((*p) == ' ' || (*p) == '\t') )
             {
                 if (*p == TAB)
                 {
@@ -70033,7 +68209,7 @@
         ++ind_len;
     }
 
-    if (!doit && !VIM_ISWHITE(*p) && !(flags & SIN_INSERT))
+    if (!doit && ! ((*p) == ' ' || (*p) == '\t')  && !(flags & SIN_INSERT))
     {
         return FALSE;
     }
@@ -70065,7 +68241,7 @@
             orig_char_len--;
         }
 
-        while (VIM_ISWHITE(*p))
+        while ( ((*p) == ' ' || (*p) == '\t') )
         {
             ++p;
         }
@@ -70089,7 +68265,7 @@
             p = oldline;
             ind_done = 0;
 
-            while (todo > 0 && VIM_ISWHITE(*p))
+            while (todo > 0 &&  ((*p) == ' ' || (*p) == '\t') )
             {
                 if (*p == TAB)
                 {
@@ -70131,7 +68307,7 @@
         *s++ = ' ';
         --todo;
     }
-    mch_memmove(s, p, (size_t)line_len);
+     memmove((char *)(s), (char *)(p), (size_t)line_len) ;
 
     if (!(flags & SIN_UNDO) || u_savesub(curwin->w_cursor.lnum) == OK)
     {
@@ -70214,7 +68390,7 @@
     char_u      *ptr;
     colnr_T     col;
 
-    for (col = 0, ptr = ml_get_curline(); VIM_ISWHITE(*ptr); ++col)
+    for (col = 0, ptr = ml_get_curline();  ((*ptr) == ' ' || (*ptr) == '\t') ; ++col)
     {
         ++ptr;
     }
@@ -70248,7 +68424,6 @@
     {
         for (i = oap->line_count; --i >= 0 && !got_int; )
         {
-
             if (i > 1 && (i % 50 == 0 || i == oap->line_count - 1) && oap->line_count > p_report)
             {
                 smsg(_("%ld lines to indent... "), i);
@@ -70339,7 +68514,7 @@
             i = pos->col;
             if (i > 0)
             {
-                while (--i > 0 && VIM_ISWHITE(ptr[i]))
+                while (--i > 0 &&  ((ptr[i]) == ' ' || (ptr[i]) == '\t') )
                 {
                     ;
                 }
@@ -70424,8 +68599,8 @@
         orig_col = curwin->w_cursor.col;
     }
 
-    save_p_list = curwin->w_p_list;
-    curwin->w_p_list = FALSE;
+    save_p_list = curwin-> w_onebuf_opt.wo_list ;
+    curwin-> w_onebuf_opt.wo_list  = FALSE;
     vc = getvcol_nolist(&curwin->w_cursor);
     vcol = vc;
 
@@ -70491,7 +68666,7 @@
             last_vcol = cts.cts_vcol;
             if (cts.cts_vcol > 0)
             {
-                MB_PTR_ADV(cts.cts_ptr);
+                 cts.cts_ptr += (*mb_ptr2len)(cts.cts_ptr) ;
             }
             if (*cts.cts_ptr == NUL)
             {
@@ -70526,7 +68701,7 @@
         insstart_less = MAXCOL;
     }
 
-    curwin->w_p_list = save_p_list;
+    curwin-> w_onebuf_opt.wo_list  = save_p_list;
 
     if (new_cursor_col <= 0)
     {
@@ -70562,7 +68737,7 @@
         }
     }
 
-    if (REPLACE_NORMAL(State) && start_col >= 0)
+    if ( (((State) & REPLACE_FLAG) && !((State) & VREPLACE_FLAG))  && start_col >= 0)
     {
         while (start_col > (int)curwin->w_cursor.col)
         {
@@ -70628,7 +68803,7 @@
         ind_done = 0;
         s = src;
 
-        while (todo > 0 && VIM_ISWHITE(*s))
+        while (todo > 0 &&  ((*s) == ' ' || (*s) == '\t') )
         {
             if (*s == TAB)
             {
@@ -70700,7 +68875,7 @@
         }
     }
 
-    mch_memmove(p, ml_get_curline(), (size_t)line_len);
+     memmove((char *)(p), (char *)(ml_get_curline()), (size_t)line_len) ;
 
     ml_replace(curwin->w_cursor.lnum, line, FALSE);
 
@@ -70739,11 +68914,11 @@
     linenr_T    last_line = 0;
     int         is_indent_only = 0;
 
-    save_list = curwin->w_p_list;
-    curwin->w_p_list = 0;
+    save_list = curwin-> w_onebuf_opt.wo_list ;
+    curwin-> w_onebuf_opt.wo_list  = 0;
 
     ptr = eap->arg;
-    if (STRNCMP(ptr, "-indentonly", 11) == 0 && IS_WHITE_OR_NUL(ptr[11]))
+    if ( strncmp((char *)(ptr), (char *)("-indentonly"), (11))  == 0 &&  ((ptr[11]) == ' ' || (ptr[11]) == '\t' || (ptr[11]) == NUL) )
     {
         is_indent_only = 1;
         ptr = skipwhite(ptr + 11);
@@ -70785,7 +68960,7 @@
         did_undo = FALSE;
         for (;;)
         {
-            if (VIM_ISWHITE(ptr[col]))
+            if ( ((ptr[col]) == ' ' || (ptr[col]) == '\t') )
             {
                 if (!got_tab && num_spaces == 0)
                 {
@@ -70805,7 +68980,6 @@
             {
                 if (got_tab || (eap->forceit && num_spaces > 1))
                 {
-
                     len = num_spaces = vcol - start_vcol;
                     num_tabs = 0;
                     if (!curbuf->b_p_et)
@@ -70845,9 +69019,9 @@
                         }
                         if (start_col > 0)
                         {
-                            mch_memmove(new_line, ptr, (size_t)start_col);
+                             memmove((char *)(new_line), (char *)(ptr), (size_t)start_col) ;
                         }
-                        mch_memmove(new_line + start_col + len, ptr + col, (size_t)(old_len - col + 1));
+                         memmove((char *)(new_line + start_col + len), (char *)(ptr + col), (size_t)(old_len - col + 1)) ;
                         ptr = new_line + start_col;
                         for (col = 0; col < len; col++)
                         {
@@ -70914,7 +69088,7 @@
         changed_lines(first_line, 0, last_line + 1, 0L);
     }
 
-    curwin->w_p_list = save_list;
+    curwin-> w_onebuf_opt.wo_list  = save_list;
 
     curbuf->b_p_ts = new_ts;
     coladvance(curwin->w_curswant);
@@ -70932,7 +69106,7 @@
     while (*word != NUL)
     {
         len = copy_option_part(&word, buf, LSIZE, ",");
-        if (STRNCMP(buf, p, len) == 0 && IS_WHITE_OR_NUL(p[len]))
+        if ( strncmp((char *)(buf), (char *)(p), (len))  == 0 &&  ((p[len]) == ' ' || (p[len]) == '\t' || (p[len]) == NUL) )
         {
             return TRUE;
         }
@@ -70967,7 +69141,7 @@
     {
         paren = *pos;
         pos = findmatch(NULL, '[');
-        if (pos == NULL || LT_POSP(pos, &paren))
+        if (pos == NULL ||  (((pos)->lnum != (&paren)->lnum)                    ? (pos)->lnum < (&paren)->lnum                 : (pos)->col != (&paren)->col                      ? (pos)->col < (&paren)->col                   : (pos)->coladd < (&paren)->coladd) )
         {
             pos = &paren;
         }
@@ -71080,7 +69254,7 @@
                     firsttry = amount;
 
                     init_chartabsize_arg(&cts, curwin, (colnr_T)(that - line), amount, line, that);
-                    while (VIM_ISWHITE(*cts.cts_ptr))
+                    while ( ((*cts.cts_ptr) == ' ' || (*cts.cts_ptr) == '\t') )
                     {
                         cts.cts_vcol += lbr_chartabsize(&cts);
                         ++cts.cts_ptr;
@@ -71102,7 +69276,7 @@
                         init_chartabsize_arg(&cts, curwin, (colnr_T)(that - line), amount, line, that);
                         if (vi_lisp || (*that != '"' && *that != '\'' && *that != '#' && (*that < '0' || *that > '9')))
                         {
-                            while (*cts.cts_ptr && (!VIM_ISWHITE(*cts.cts_ptr) || quotecount || parencount) && (!((*cts.cts_ptr == '(' || *cts.cts_ptr == '[') && !quotecount && !parencount && vi_lisp)))
+                            while (*cts.cts_ptr && (! ((*cts.cts_ptr) == ' ' || (*cts.cts_ptr) == '\t')  || quotecount || parencount) && (!((*cts.cts_ptr == '(' || *cts.cts_ptr == '[') && !quotecount && !parencount && vi_lisp)))
                             {
                                 if (*cts.cts_ptr == '"')
                                 {
@@ -71123,7 +69297,7 @@
                                 cts.cts_vcol += lbr_chartabsize_adv(&cts);
                             }
                         }
-                        while (VIM_ISWHITE(*cts.cts_ptr))
+                        while ( ((*cts.cts_ptr) == ' ' || (*cts.cts_ptr) == '\t') )
                         {
                             cts.cts_vcol += lbr_chartabsize(&cts);
                             ++cts.cts_ptr;
@@ -71200,51 +69374,44 @@
 
 // ==================== insexpand.c ====================
 
-#define CTRL_X_WANT_IDENT       0x100
-
-#define CTRL_X_NORMAL           0
-#define CTRL_X_NOT_DEFINED_YET  1
-#define CTRL_X_SCROLL           2
-#define CTRL_X_WHOLE_LINE       3
-#define CTRL_X_FILES            4
-#define CTRL_X_TAGS             (5 + CTRL_X_WANT_IDENT)
-#define CTRL_X_PATH_PATTERNS    (6 + CTRL_X_WANT_IDENT)
-#define CTRL_X_PATH_DEFINES     (7 + CTRL_X_WANT_IDENT)
-#define CTRL_X_FINISHED         8
-#define CTRL_X_DICTIONARY       (9 + CTRL_X_WANT_IDENT)
-#define CTRL_X_THESAURUS        (10 + CTRL_X_WANT_IDENT)
-#define CTRL_X_CMDLINE          11
-#define CTRL_X_FUNCTION 12
-#define CTRL_X_OMNI             13
-#define CTRL_X_SPELL            14
-#define CTRL_X_LOCAL_MSG        15
-#define CTRL_X_EVAL             16
-#define CTRL_X_CMDLINE_CTRL_X   17
-#define CTRL_X_REGISTER 18
+enum { CTRL_X_WANT_IDENT = 0x100 };
 
-#define CTRL_X_MSG(i) ctrl_x_msgs[(i) & ~CTRL_X_WANT_IDENT]
+enum { CTRL_X_NORMAL = 0 };
+enum { CTRL_X_NOT_DEFINED_YET = 1 };
+enum { CTRL_X_SCROLL = 2 };
+enum { CTRL_X_WHOLE_LINE = 3 };
+enum { CTRL_X_FILES = 4 };
+enum { CTRL_X_FINISHED = 8 };
+enum { CTRL_X_CMDLINE = 11 };
+enum { CTRL_X_FUNCTION = 12 };
+enum { CTRL_X_OMNI = 13 };
+enum { CTRL_X_SPELL = 14 };
+enum { CTRL_X_LOCAL_MSG = 15 };
+enum { CTRL_X_EVAL = 16 };
+enum { CTRL_X_CMDLINE_CTRL_X = 17 };
+enum { CTRL_X_REGISTER = 18 };
 
 static char *ctrl_x_msgs[] =
 {
-    N_(" Keyword completion (^N^P)"),
-    N_(" ^X mode (^]^D^E^F^I^K^L^N^O^P^Rs^U^V^Y)"),
+     " Keyword completion (^N^P)" ,
+     " ^X mode (^]^D^E^F^I^K^L^N^O^P^Rs^U^V^Y)" ,
     NULL,
-    N_(" Whole line completion (^L^N^P)"),
-    N_(" File name completion (^F^N^P)"),
-    N_(" Tag completion (^]^N^P)"),
-    N_(" Path pattern completion (^N^P)"),
-    N_(" Definition completion (^D^N^P)"),
+     " Whole line completion (^L^N^P)" ,
+     " File name completion (^F^N^P)" ,
+     " Tag completion (^]^N^P)" ,
+     " Path pattern completion (^N^P)" ,
+     " Definition completion (^D^N^P)" ,
     NULL,
-    N_(" Dictionary completion (^K^N^P)"),
-    N_(" Thesaurus completion (^T^N^P)"),
-    N_(" Command-line completion (^V^N^P)"),
-    N_(" User defined completion (^U^N^P)"),
-    N_(" Omni completion (^O^N^P)"),
-    N_(" Spelling suggestion (^S^N^P)"),
-    N_(" Keyword Local completion (^N^P)"),
+     " Dictionary completion (^K^N^P)" ,
+     " Thesaurus completion (^T^N^P)" ,
+     " Command-line completion (^V^N^P)" ,
+     " User defined completion (^U^N^P)" ,
+     " Omni completion (^O^N^P)" ,
+     " Spelling suggestion (^S^N^P)" ,
+     " Keyword Local completion (^N^P)" ,
     NULL,
-    N_(" Command-line completion (^V^N^P)"),
-    N_(" Register completion (^N^P)"),
+     " Command-line completion (^V^N^P)" ,
+     " Register completion (^N^P)" ,
 };
 
 typedef struct compl_S compl_T;
@@ -71265,12 +69432,12 @@
     int         cp_cpt_source_idx;
 };
 
-#define CP_ORIGINAL_TEXT   1
-#define CP_FREE_FNAME       2
-#define CP_CONT_S_IPOS      4
-#define CP_EQUAL            8
-#define CP_ICASE            16
-#define CP_FAST     32
+enum { CP_ORIGINAL_TEXT = 1 };
+enum { CP_FREE_FNAME = 2 };
+enum { CP_CONT_S_IPOS = 4 };
+enum { CP_EQUAL = 8 };
+enum { CP_ICASE = 16 };
+enum { CP_FAST = 32 };
 
 static compl_T    *compl_first_match = NULL;
 static compl_T    *compl_curr_match = NULL;
@@ -71283,9 +69450,6 @@
     char_u  cse_str[1];
 } complstr_T;
 
-#define CSE_OFF         ((int)offsetof(complstr_T, cse_str))
-#define HI2CSE(hi)      ((complstr_T *)((hi)->hi_key - CSE_OFF))
-
 static hashtab_T  compl_strings_ht;
 
     static void
@@ -71303,9 +69467,9 @@
         hash = hash_hash(str);
     }
     hi = hash_lookup(&compl_strings_ht, str, hash);
-    if (HASHITEM_EMPTY(hi))
+    if ( ((hi)->hi_key == NULL || (hi)->hi_key == &hash_removed) )
     {
-        entry = alloc(CSE_OFF + len + 1);
+        entry = alloc( ((int)offsetof(complstr_T, cse_str))  + len + 1);
         if (entry == NULL)
         {
             return;
@@ -71319,7 +69483,7 @@
     }
     else
     {
-        ++HI2CSE(hi)->cse_count;
+        ++ ((complstr_T *)((hi)->hi_key -  ((int)offsetof(complstr_T, cse_str)) )) ->cse_count;
     }
 }
 
@@ -71362,7 +69526,7 @@
 static win_T      *compl_curr_win = NULL;
 static buf_T      *compl_curr_buf = NULL;
 
-#define COMPL_INITIAL_TIMEOUT_MS    80
+enum { COMPL_INITIAL_TIMEOUT_MS = 80 };
 static int        compl_autocomplete = FALSE;
 static bool       compl_autostarted = false;
 static bool       compl_autostart_pending = false;
@@ -71373,19 +69537,17 @@
 static int        compl_from_nonkeyword = FALSE;
 static int        compl_hi_on_autocompl_longest = FALSE;
 
-#define COMPL_MIN_TIMEOUT_MS    5
-#define DECAY_COMPL_TIMEOUT()     do {  if (compl_timeout_ms > COMPL_MIN_TIMEOUT_MS)        compl_timeout_ms /= 2;     } while (0)
+enum { COMPL_MIN_TIMEOUT_MS = 5 };
 
-#define COMPL_FUNC_TIMEOUT_MS           300
-#define COMPL_FUNC_TIMEOUT_NON_KW_MS    1000
+enum { COMPL_FUNC_TIMEOUT_MS = 300 };
+enum { COMPL_FUNC_TIMEOUT_NON_KW_MS = 1000 };
 
 static int        compl_cont_status = 0;
-#define CONT_ADDING     1
-#define CONT_INTRPT     (2 + 4)
-#define CONT_N_ADDS     4
-#define CONT_S_IPOS     8
-#define CONT_SOL        16
-#define CONT_LOCAL      32
+enum { CONT_ADDING = 1 };
+enum { CONT_N_ADDS = 4 };
+enum { CONT_S_IPOS = 8 };
+enum { CONT_SOL = 16 };
+enum { CONT_LOCAL = 32 };
 
 static int        compl_opt_refresh_always = FALSE;
 static int        compl_opt_suppress_empty = FALSE;
@@ -71443,14 +69605,14 @@
     {
         if (compl_cont_status & CONT_N_ADDS)
         {
-            compl_cont_status |= CONT_INTRPT;
+            compl_cont_status |=  (2 + 4) ;
         }
         else
         {
             compl_cont_status = 0;
         }
         ctrl_x_mode = CTRL_X_NOT_DEFINED_YET;
-        edit_submode = (char_u *)_(CTRL_X_MSG(ctrl_x_mode));
+        edit_submode = (char_u *)_( ctrl_x_msgs[(ctrl_x_mode) & ~CTRL_X_WANT_IDENT] );
         edit_submode_pre = NULL;
         showmode();
     }
@@ -71473,15 +69635,15 @@
 static int ctrl_x_mode_files(void)
     { return ctrl_x_mode == CTRL_X_FILES; }
 static int ctrl_x_mode_tags(void)
-    { return ctrl_x_mode == CTRL_X_TAGS; }
+    { return ctrl_x_mode ==  (5 + CTRL_X_WANT_IDENT) ; }
 static int ctrl_x_mode_path_patterns(void)
-    { return ctrl_x_mode == CTRL_X_PATH_PATTERNS; }
+    { return ctrl_x_mode ==  (6 + CTRL_X_WANT_IDENT) ; }
 static int ctrl_x_mode_path_defines(void)
-    { return ctrl_x_mode == CTRL_X_PATH_DEFINES; }
+    { return ctrl_x_mode ==  (7 + CTRL_X_WANT_IDENT) ; }
 static int ctrl_x_mode_dictionary(void)
-    { return ctrl_x_mode == CTRL_X_DICTIONARY; }
+    { return ctrl_x_mode ==  (9 + CTRL_X_WANT_IDENT) ; }
 static int ctrl_x_mode_thesaurus(void)
-    { return ctrl_x_mode == CTRL_X_THESAURUS; }
+    { return ctrl_x_mode ==  (10 + CTRL_X_WANT_IDENT) ; }
 static int ctrl_x_mode_cmdline(void)
     { return ctrl_x_mode == CTRL_X_CMDLINE
                 || ctrl_x_mode == CTRL_X_CMDLINE_CTRL_X;
@@ -71550,7 +69712,7 @@
     static int
 compl_shows_dir_backward(void)
 {
-    return compl_shows_dir == BACKWARD;
+    return compl_shows_dir ==  (-1) ;
 }
 
     int
@@ -71560,7 +69722,7 @@
     {
         ctrl_x_mode = CTRL_X_NORMAL;
         edit_submode = NULL;
-        msg_attr(dict_opt ? _("'dictionary' option is empty") : _("'thesaurus' option is empty"), HL_ATTR(HLF_E));
+        msg_attr(dict_opt ? _("'dictionary' option is empty") : _("'thesaurus' option is empty"),  highlight_attr[(int)(HLF_E)] );
         if (emsg_silent == 0 && !in_assert_fails)
         {
             vim_beep(BO_COMPL);
@@ -71594,11 +69756,11 @@
             return (c == Ctrl_L || c == Ctrl_P || c == Ctrl_N);
         case CTRL_X_FILES:
             return (c == Ctrl_F || c == Ctrl_P || c == Ctrl_N);
-        case CTRL_X_DICTIONARY:
+        case  (9 + CTRL_X_WANT_IDENT) :
             return (c == Ctrl_K || c == Ctrl_P || c == Ctrl_N);
-        case CTRL_X_THESAURUS:
+        case  (10 + CTRL_X_WANT_IDENT) :
             return (c == Ctrl_T || c == Ctrl_P || c == Ctrl_N);
-        case CTRL_X_TAGS:
+        case  (5 + CTRL_X_WANT_IDENT) :
             return (c == Ctrl_RSB || c == Ctrl_P || c == Ctrl_N);
         case CTRL_X_CMDLINE:
             return (c == Ctrl_V || c == Ctrl_Q || c == Ctrl_P || c == Ctrl_N || c == Ctrl_X);
@@ -71664,7 +69826,7 @@
         case CTRL_X_CMDLINE:
         case CTRL_X_CMDLINE_CTRL_X:
         case CTRL_X_OMNI:
-            return vim_isprintc(c) && !VIM_ISWHITE(c);
+            return vim_isprintc(c) && ! ((c) == ' ' || (c) == '\t') ;
 
         case CTRL_X_WHOLE_LINE:
             return vim_isprintc(c);
@@ -71685,7 +69847,7 @@
 
     IObuff[0] = NUL;
 
-    wca = ALLOC_MULT(int, char_len);
+    wca =  (int *)alloc(sizeof(int) * (char_len)) ;
     if (wca == NULL)
     {
         return IObuff;
@@ -71708,14 +69870,14 @@
     for (i = 0; i < min_len; ++i)
     {
         c = has_mbyte ? mb_ptr2char_adv(&p) : *(p++);
-        if (MB_ISLOWER(c))
+        if ( vim_islower(c) )
         {
             has_lower = TRUE;
-            if (MB_ISUPPER(wca[i]))
+            if ( vim_isupper(wca[i]) )
             {
                 for (i = compl_char_len; i < char_len; ++i)
                 {
-                    wca[i] = MB_TOLOWER(wca[i]);
+                    wca[i] =  vim_tolower(wca[i]) ;
                 }
                 break;
             }
@@ -71728,15 +69890,15 @@
         for (i = 0; i < min_len; ++i)
         {
             c = has_mbyte ? mb_ptr2char_adv(&p) : *(p++);
-            if (was_letter && MB_ISUPPER(c) && MB_ISLOWER(wca[i]))
+            if (was_letter &&  vim_isupper(c)  &&  vim_islower(wca[i]) )
             {
                 for (i = compl_char_len; i < char_len; ++i)
                 {
-                    wca[i] = MB_TOUPPER(wca[i]);
+                    wca[i] =  vim_toupper(wca[i]) ;
                 }
                 break;
             }
-            was_letter = MB_ISLOWER(c) || MB_ISUPPER(c);
+            was_letter =  vim_islower(c)  ||  vim_isupper(c) ;
         }
     }
 
@@ -71744,13 +69906,13 @@
     for (i = 0; i < min_len; ++i)
     {
         c = has_mbyte ? mb_ptr2char_adv(&p) : *(p++);
-        if (MB_ISLOWER(c))
+        if ( vim_islower(c) )
         {
-            wca[i] = MB_TOLOWER(wca[i]);
+            wca[i] =  vim_tolower(wca[i]) ;
         }
-        else if (MB_ISUPPER(c))
+        else if ( vim_isupper(c) )
         {
-            wca[i] = MB_TOUPPER(wca[i]);
+            wca[i] =  vim_toupper(wca[i]) ;
         }
     }
 
@@ -71778,15 +69940,15 @@
                 ++gap.ga_len;
             }
         }
-        else if ((p - IObuff) + 6 >= IOSIZE)
+        else if ((p - IObuff) + 6 >=  (1024+1) )
         {
-            if (ga_grow(&gap, IOSIZE) == FAIL)
+            if (ga_grow(&gap,  (1024+1) ) == FAIL)
             {
                 vim_free(wca);
                 return (char_u *)"[failed]";
             }
             *p = NUL;
-            STRCPY(gap.ga_data, IObuff);
+             strcpy((char *)(gap.ga_data), (char *)(IObuff)) ;
             gap.ga_len = (int)(p - IObuff);
         }
         else if (has_mbyte)
@@ -71824,14 +69986,13 @@
 
     if (p_ic && curbuf->b_p_inf && len > 0)
     {
-
         if (has_mbyte)
         {
             p = str;
             char_len = 0;
             while (*p != NUL)
             {
-                MB_PTR_ADV(p);
+                 p += (*mb_ptr2len)(p) ;
                 ++char_len;
             }
         }
@@ -71846,7 +70007,7 @@
             compl_char_len = 0;
             while (*p != NUL)
             {
-                MB_PTR_ADV(p);
+                 p += (*mb_ptr2len)(p) ;
                 ++compl_char_len;
             }
         }
@@ -71901,7 +70062,7 @@
 }
 
     static int
-ins_compl_add(char_u      *str, int         len, char_u      *fname, char_u      **cptext, typval_T    *user_data UNUSED, int         cdir, int         flags_arg, int         adup, int         *user_hl, int         score)
+ins_compl_add(char_u      *str, int         len, char_u      *fname, char_u      **cptext, typval_T    *user_data  __attribute__((unused)) , int         cdir, int         flags_arg, int         adup, int         *user_hl, int         score)
 {
     compl_T *match;
     compl_T *current;
@@ -71926,7 +70087,7 @@
     }
     if (len < 0)
     {
-        len = (int)STRLEN(str);
+        len = (int) strlen((char *)(str)) ;
     }
 
     if (compl_first_match != NULL && !adup && compl_strings_ht.ht_used > 0)
@@ -71937,7 +70098,7 @@
 
         if (len < (int)sizeof(keybuf))
         {
-            mch_memmove(keybuf, str, (size_t)len);
+             memmove((char *)(keybuf), (char *)(str), (size_t)len) ;
             keybuf[len] = NUL;
             key = keybuf;
         }
@@ -71952,14 +70113,14 @@
         }
         str_hash = hash_hash(key);
         hi = hash_lookup(&compl_strings_ht, key, str_hash);
-        if (!HASHITEM_EMPTY(hi))
+        if (! ((hi)->hi_key == NULL || (hi)->hi_key == &hash_removed) )
         {
             if (is_nearest_active() && score > 0)
             {
                 match = compl_first_match;
                 do
                 {
-                    if (!match_at_original_text(match) && STRNCMP(match->cp_str.string, str, len) == 0 && ((int)match->cp_str.length <= len || match->cp_str.string[len] == NUL))
+                    if (!match_at_original_text(match) &&  strncmp((char *)(match->cp_str.string), (char *)(str), (len))  == 0 && ((int)match->cp_str.length <= len || match->cp_str.string[len] == NUL))
                     {
                         if (score < match->cp_score)
                         {
@@ -71977,7 +70138,7 @@
 
     ins_compl_del_pum();
 
-    match = ALLOC_CLEAR_ONE(compl_T);
+    match =  (compl_T *)alloc_clear(sizeof(compl_T)) ;
     if (match == NULL)
     {
         vim_free(new_str);
@@ -71992,7 +70153,7 @@
     match->cp_str.string = new_str;
     match->cp_str.length = len;
 
-    if (fname != NULL && compl_curr_match != NULL && compl_curr_match->cp_fname != NULL && STRCMP(fname, compl_curr_match->cp_fname) == 0)
+    if (fname != NULL && compl_curr_match != NULL && compl_curr_match->cp_fname != NULL &&  strcmp((char *)(fname), (char *)(compl_curr_match->cp_fname))  == 0)
     {
         match->cp_fname = compl_curr_match->cp_fname;
     }
@@ -72026,7 +70187,7 @@
     {
         match->cp_next = match->cp_prev = NULL;
     }
-    else if (cot_fuzzy() && score != FUZZY_SCORE_NONE && compl_get_longest)
+    else if (cot_fuzzy() && score !=  INT_MIN  && compl_get_longest)
     {
         current = compl_first_match->cp_next;
         prev = compl_first_match;
@@ -72102,9 +70263,9 @@
     }
     if (match->cp_flags & CP_ICASE)
     {
-        return STRNICMP(match->cp_str.string, str, (size_t)len) == 0;
+        return  strncasecmp((char *)(match->cp_str.string), (char *)(str), ((size_t)len))  == 0;
     }
-    return STRNCMP(match->cp_str.string, str, (size_t)len) == 0;
+    return  strncmp((char *)(match->cp_str.string), (char *)(str), ((size_t)len))  == 0;
 }
 
     static int
@@ -72126,7 +70287,7 @@
 
     for (int i = 0; i < len; ++i)
     {
-        if (i >= typed && i < longest_end ? TOLOWER_LOC(match->cp_str.string[i]) != TOLOWER_LOC(str[i]) : match->cp_str.string[i] != str[i])
+        if (i >= typed && i < longest_end ?   (tolower ((unsigned char)(match->cp_str.string[i])))  !=   (tolower ((unsigned char)(str[i])))  : match->cp_str.string[i] != str[i])
         {
             return FALSE;
         }
@@ -72139,7 +70300,7 @@
 {
     if (len == -1)
     {
-        len = (int)STRLEN(p);
+        len = (int) strlen((char *)(p)) ;
     }
     ins_bytes_len(p, len);
     compl_ins_end_col = curwin->w_cursor.col;
@@ -72244,14 +70405,14 @@
             c1 = *p;
             c2 = *s;
         }
-        if ((match->cp_flags & CP_ICASE) ? (MB_TOLOWER(c1) != MB_TOLOWER(c2)) : (c1 != c2))
+        if ((match->cp_flags & CP_ICASE) ? ( vim_tolower(c1)  !=  vim_tolower(c2) ) : (c1 != c2))
         {
             break;
         }
         if (has_mbyte)
         {
-            MB_PTR_ADV(p);
-            MB_PTR_ADV(s);
+             p += (*mb_ptr2len)(p) ;
+             s += (*mb_ptr2len)(s) ;
         }
         else
         {
@@ -72285,7 +70446,7 @@
 
     for (int i = 0; i < num_matches && add_r != FAIL; i++)
     {
-        add_r = ins_compl_add(matches[i], -1, NULL, NULL, NULL, dir, CP_FAST | (icase ? CP_ICASE : 0), FALSE, NULL, FUZZY_SCORE_NONE);
+        add_r = ins_compl_add(matches[i], -1, NULL, NULL, NULL, dir, CP_FAST | (icase ? CP_ICASE : 0), FALSE, NULL,  INT_MIN );
         if (add_r == OK)
         {
             dir = FORWARD;
@@ -72362,7 +70523,8 @@
     }
 
     pum_undisplay();
-    VIM_CLEAR(compl_match_array);
+     vim_free(compl_match_array);
+     (compl_match_array) = NULL;
 }
 
     int
@@ -72431,7 +70593,7 @@
 {
     int score_a = ((compl_T*)a)->cp_score;
     int score_b = ((compl_T*)b)->cp_score;
-    if (score_a == FUZZY_SCORE_NONE || score_b == FUZZY_SCORE_NONE)
+    if (score_a ==  INT_MIN  || score_b ==  INT_MIN )
     {
         return 0;
     }
@@ -72454,8 +70616,8 @@
 
     char_u      *line = ml_get(curwin->w_cursor.lnum);
 
-    mch_memmove(dest->string, line + startcol, prepend_len);
-    mch_memmove(dest->string + prepend_len, src->string, src->length);
+     memmove((char *)(dest->string), (char *)(line + startcol), prepend_len) ;
+     memmove((char *)(dest->string + prepend_len), (char *)(src->string), src->length) ;
     dest->string[new_length] = NUL;
     return OK;
 }
@@ -72467,7 +70629,9 @@
 
     if (match == NULL)
     {
-        VIM_CLEAR_STRING(adjusted_leader);
+         vim_free(adjusted_leader.string);
+         (adjusted_leader.string) = NULL;
+         adjusted_leader.length = 0;
         return NULL;
     }
 
@@ -72506,7 +70670,9 @@
             return &adjusted_leader;
         }
 
-        VIM_CLEAR_STRING(adjusted_leader);
+         vim_free(adjusted_leader.string);
+         (adjusted_leader.string) = NULL;
+         adjusted_leader.length = 0;
         if (prepend_startcol_text(&adjusted_leader, &compl_leader, startcol) != OK)
         {
             goto theend;
@@ -72637,7 +70803,7 @@
         shown_match_ok = TRUE;
     }
 
-    if (compl_leader.string != NULL && STRCMP(compl_leader.string, compl_orig_text.string) == 0 && shown_match_ok == FALSE)
+    if (compl_leader.string != NULL &&  strcmp((char *)(compl_leader.string), (char *)(compl_orig_text.string))  == 0 && shown_match_ok == FALSE)
     {
         compl_shown_match = compl_no_select ? compl_first_match
                                             : compl_first_match->cp_next;
@@ -72645,7 +70811,7 @@
 
     if (is_cpt_completion)
     {
-        match_count = ALLOC_CLEAR_MULT(int, cpt_sources_count);
+        match_count =  (int *)alloc_clear(sizeof(int) * (cpt_sources_count)) ;
         if (match_count == NULL)
         {
             return -1;
@@ -72666,7 +70832,7 @@
             compl->cp_flags &= ~CP_ICASE;
         }
 
-        if (!match_at_original_text(compl) && (leader->string == NULL || ins_compl_equal_sc(compl, leader->string, (int)leader->length) || (cot_fuzzy() && compl->cp_score != FUZZY_SCORE_NONE)))
+        if (!match_at_original_text(compl) && (leader->string == NULL || ins_compl_equal_sc(compl, leader->string, (int)leader->length) || (cot_fuzzy() && compl->cp_score !=  INT_MIN )))
         {
             int match_limit_exceeded = FALSE;
             int cur_source = compl->cp_cpt_source_idx;
@@ -72757,7 +70923,7 @@
         cur = 0;
     }
 
-    compl_match_array = ALLOC_CLEAR_MULT(pumitem_T, compl_match_arraysize);
+    compl_match_array =  (pumitem_T *)alloc_clear(sizeof(pumitem_T) * (compl_match_arraysize)) ;
     if (compl_match_array == NULL)
     {
         return -1;
@@ -72848,9 +71014,6 @@
 
 }
 
-#define DICT_FIRST      (1)
-#define DICT_EXACT      (2)
-
     static void
 ins_compl_dictionaries(char_u      *dict_start, char_u      *pat, int         flags, int         thesaurus)
 {
@@ -72889,7 +71052,7 @@
         {
             goto theend;
         }
-        size_t len = STRLEN(pat_esc) + 10;
+        size_t len =  strlen((char *)(pat_esc))  + 10;
         ptr = alloc(len);
         if (ptr == NULL)
         {
@@ -72913,7 +71076,7 @@
     regmatch.rm_ic = ignorecase(pat);
     while (*dict != NUL && !got_int && !compl_interrupted)
     {
-        if (flags == DICT_EXACT)
+        if (flags ==  (2) )
         {
             count = 1;
             files = &dict;
@@ -72930,7 +71093,7 @@
             if (count > 0)
         {
             ins_compl_files(count, files, thesaurus, flags, &regmatch, buf, &dir);
-            if (flags != DICT_EXACT)
+            if (flags !=  (2) )
             {
                 FreeWild(count, files);
             }
@@ -72984,7 +71147,7 @@
 
         if (wstart != skip_word)
         {
-            status = ins_compl_add_infercase(wstart, (int)(ptr - wstart), p_ic, fname, dir, FALSE, FUZZY_SCORE_NONE);
+            status = ins_compl_add_infercase(wstart, (int)(ptr - wstart), p_ic, fname, dir, FALSE,  INT_MIN );
             if (status == FAIL)
             {
                 break;
@@ -73017,12 +71180,12 @@
 
     for (i = 0; i < count && !got_int && !ins_compl_interrupted(); i++)
     {
-        fp = mch_fopen((char *)files[i], "r");
-        if (flags != DICT_EXACT && !shortmess(SHM_COMPLETIONSCAN) && !compl_autocomplete)
+        fp =  fopen(((char *)files[i]), ("r")) ;
+        if (flags !=  (2)  && !shortmess(SHM_COMPLETIONSCAN) && !compl_autocomplete)
         {
             msg_hist_off = TRUE;
-            vim_snprintf((char *)IObuff, IOSIZE, _("Scanning dictionary: %s"), (char *)files[i]);
-            (void)msg_trunc_attr((char *)IObuff, TRUE, HL_ATTR(HLF_R));
+            vim_snprintf((char *)IObuff,  (1024+1) , _("Scanning dictionary: %s"), (char *)files[i]);
+            (void)msg_trunc_attr((char *)IObuff, TRUE,  highlight_attr[(int)(HLF_R)] );
         }
 
         if (fp == NULL)
@@ -73062,7 +71225,7 @@
                     ptr = regmatch->startp[0];
                     ptr = ctrl_x_mode_line_or_eval() ? find_line_end(ptr)
                                                     : find_word_end(ptr);
-                    add_r = ins_compl_add_infercase(regmatch->startp[0], (int)(ptr - regmatch->startp[0]), p_ic, files[i], *dir, FALSE, FUZZY_SCORE_NONE);
+                    add_r = ins_compl_add_infercase(regmatch->startp[0], (int)(ptr - regmatch->startp[0]), p_ic, files[i], *dir, FALSE,  INT_MIN );
                     if (thesaurus)
                     {
                         ptr = buf;
@@ -73140,7 +71303,7 @@
     char_u *
 find_line_end(char_u *ptr)
 {
-    char_u *s = ptr + STRLEN(ptr);
+    char_u *s = ptr +  strlen((char *)(ptr)) ;
     while (s > ptr && (s[-1] == CAR || s[-1] == NL))
     {
         --s;
@@ -73155,9 +71318,9 @@
     {
         hashitem_T *hi = hash_find(&compl_strings_ht, match->cp_str.string);
 
-        if (!HASHITEM_EMPTY(hi))
+        if (! ((hi)->hi_key == NULL || (hi)->hi_key == &hash_removed) )
         {
-            complstr_T *entry = HI2CSE(hi);
+            complstr_T *entry =  ((complstr_T *)((hi)->hi_key -  ((int)offsetof(complstr_T, cse_str)) )) ;
 
             if (--entry->cse_count <= 0)
             {
@@ -73166,7 +71329,9 @@
             }
         }
     }
-    VIM_CLEAR_STRING(match->cp_str);
+     vim_free(match->cp_str.string);
+     (match->cp_str.string) = NULL;
+     match->cp_str.length = 0;
     if (match->cp_flags & CP_FREE_FNAME)
     {
         vim_free(match->cp_fname);
@@ -73183,8 +71348,12 @@
 {
     compl_T *match;
 
-    VIM_CLEAR_STRING(compl_pattern);
-    VIM_CLEAR_STRING(compl_leader);
+     vim_free(compl_pattern.string);
+     (compl_pattern.string) = NULL;
+     compl_pattern.length = 0;
+     vim_free(compl_leader.string);
+     (compl_leader.string) = NULL;
+     compl_leader.length = 0;
 
     if (compl_first_match == NULL)
     {
@@ -73194,7 +71363,7 @@
     ins_compl_del_pum();
     pum_clear();
 
-    hash_clear_all(&compl_strings_ht, CSE_OFF);
+    hash_clear_all(&compl_strings_ht,  ((int)offsetof(complstr_T, cse_str)) );
     hash_init(&compl_strings_ht);
 
     compl_curr_match = compl_first_match;
@@ -73220,10 +71389,16 @@
     compl_longest_end_col = 0;
     compl_curr_win = NULL;
     compl_curr_buf = NULL;
-    VIM_CLEAR_STRING(compl_pattern);
-    VIM_CLEAR_STRING(compl_leader);
+     vim_free(compl_pattern.string);
+     (compl_pattern.string) = NULL;
+     compl_pattern.length = 0;
+     vim_free(compl_leader.string);
+     (compl_leader.string) = NULL;
+     compl_leader.length = 0;
     edit_submode_extra = NULL;
-    VIM_CLEAR_STRING(compl_orig_text);
+     vim_free(compl_orig_text.string);
+     (compl_orig_text.string) = NULL;
+     compl_orig_text.length = 0;
     compl_enter_selects = FALSE;
     cpt_sources_clear();
     compl_autocomplete = FALSE;
@@ -73319,11 +71494,11 @@
 
     line = ml_get_curline();
     p = line + curwin->w_cursor.col;
-    MB_PTR_BACK(line, p);
+     p -= has_mbyte ? ((*mb_head_off)(line, (p) - 1) + 1) : 1 ;
 
     if ((int)(p - line) - (int)compl_col < 0 || ((int)(p - line) - (int)compl_col == 0 && !ctrl_x_mode_omni()) || ctrl_x_mode_eval() || (!can_bs(BS_START) && (int)(p - line) - (int)compl_col - compl_length < 0))
     {
-        return K_BS;
+        return   (-(('k') + ((int)('b') << 8)))  ;
     }
 
     if (curwin->w_cursor.col <= compl_col + compl_length || ins_compl_need_restart())
@@ -73331,13 +71506,15 @@
         ins_compl_restart();
     }
 
-    VIM_CLEAR_STRING(compl_leader);
+     vim_free(compl_leader.string);
+     (compl_leader.string) = NULL;
+     compl_leader.length = 0;
     compl_leader.length = (size_t)((p - line) - compl_col);
     compl_leader.string = vim_strnsave(line + compl_col, compl_leader.length);
     if (compl_leader.string == NULL)
     {
         compl_leader.length = 0;
-        return K_BS;
+        return   (-(('k') + ((int)('b') << 8)))  ;
     }
 
     if (compl_autocomplete && compl_first_match && !ins_compl_has_preinsert())
@@ -73502,7 +71679,9 @@
 
     if (!compl_opt_refresh_always)
     {
-        VIM_CLEAR_STRING(compl_leader);
+         vim_free(compl_leader.string);
+         (compl_leader.string) = NULL;
+         compl_leader.length = 0;
         compl_leader.length = (size_t)(curwin->w_cursor.col - compl_col);
         compl_leader.string = vim_strnsave(ml_get_curline() + compl_col, compl_leader.length);
         if (compl_leader.string == NULL)
@@ -73547,7 +71726,9 @@
         return;
     }
 
-    VIM_CLEAR_STRING(match->cp_str);
+     vim_free(match->cp_str.string);
+     (match->cp_str.string) = NULL;
+     match->cp_str.length = 0;
     match->cp_str.string = p;
     match->cp_str.length = len;
 }
@@ -73587,7 +71768,7 @@
         }
     }
     p += len;
-    c = PTR2CHAR(p);
+    c =  (has_mbyte ? mb_ptr2char(p) : (int)*(p)) ;
     ins_compl_addleader(c);
 }
 
@@ -73619,7 +71800,7 @@
             ctrl_x_mode = CTRL_X_FILES;
             break;
         case Ctrl_K:
-            ctrl_x_mode = CTRL_X_DICTIONARY;
+            ctrl_x_mode =  (9 + CTRL_X_WANT_IDENT) ;
             break;
         case Ctrl_R:
             if (vpeekc() == '=')
@@ -73629,14 +71810,14 @@
             ctrl_x_mode = CTRL_X_REGISTER;
             break;
         case Ctrl_T:
-            ctrl_x_mode = CTRL_X_THESAURUS;
+            ctrl_x_mode =  (10 + CTRL_X_WANT_IDENT) ;
             break;
         case 's':
         case Ctrl_S:
             ctrl_x_mode = CTRL_X_SPELL;
             break;
         case Ctrl_RSB:
-            ctrl_x_mode = CTRL_X_TAGS;
+            ctrl_x_mode =  (5 + CTRL_X_WANT_IDENT) ;
             break;
         case Ctrl_V:
         case Ctrl_Q:
@@ -73650,7 +71831,7 @@
             break;
         case Ctrl_P:
         case Ctrl_N:
-            if (!(compl_cont_status & CONT_INTRPT))
+            if (!(compl_cont_status &  (2 + 4) ))
             {
                 compl_cont_status |= CONT_LOCAL;
             }
@@ -73658,6 +71839,7 @@
             {
                 compl_cont_status &= ~CONT_LOCAL;
             }
+        __attribute__((fallthrough));
         default:
             if (c == Ctrl_X)
             {
@@ -73680,7 +71862,7 @@
 }
 
     static void
-trigger_complete_done_event(int mode UNUSED, string_T *word UNUSED)
+trigger_complete_done_event(int mode  __attribute__((unused)) , string_T *word  __attribute__((unused)) )
 {
     ins_apply_autocmds(EVENT_COMPLETEDONE);
 
@@ -73736,7 +71918,7 @@
         }
     }
 
-    if ((c == Ctrl_Y || (compl_enter_selects && (c == CAR || c == K_KENTER || c == NL))) && pum_visible())
+    if ((c == Ctrl_Y || (compl_enter_selects && (c == CAR || c ==   (-(('K') + ((int)('A') << 8)))   || c == NL))) && pum_visible())
     {
         word.string = vim_strnsave(compl_shown_match->cp_str.string, compl_shown_match->cp_str.length);
         if (word.string == NULL)
@@ -73834,7 +72016,7 @@
         edit_submode_extra = NULL;
     }
 
-    if (c == K_SELECT || c == K_MOUSEDOWN || c == K_MOUSEUP || c == K_MOUSELEFT || c == K_MOUSERIGHT || c == K_MOUSEMOVE || c == K_COMMAND || c == K_SCRIPT_COMMAND)
+    if (c ==   (-((KS_SELECT) + ((int)( ('X') ) << 8)))   || c ==   (-((KS_EXTRA) + ((int)(KE_MOUSEDOWN) << 8)))   || c ==   (-((KS_EXTRA) + ((int)(KE_MOUSEUP) << 8)))   || c ==   (-((KS_EXTRA) + ((int)(KE_MOUSELEFT) << 8)))   || c ==   (-((KS_EXTRA) + ((int)(KE_MOUSERIGHT) << 8)))   || c ==   (-((KS_EXTRA) + ((int)(KE_MOUSEMOVE) << 8)))   || c ==   (-((KS_EXTRA) + ((int)(KE_COMMAND) << 8)))   || c ==   (-((KS_EXTRA) + ((int)(KE_SCRIPT_COMMAND) << 8)))  )
     {
         return retval;
     }
@@ -73932,9 +72114,9 @@
         {
             len -= (*mb_head_off)(p, p + len);
         }
-        for (p += len; *p != NUL; MB_PTR_ADV(p))
+        for (p += len; *p != NUL;  p += (*mb_ptr2len)(p) )
         {
-            AppendCharToRedobuff(K_BS);
+            AppendCharToRedobuff(  (-(('k') + ((int)('b') << 8)))  );
         }
     }
     if (ptr != NULL)
@@ -74020,7 +72202,7 @@
 }
 
     static int
-thesaurus_func_complete(int type UNUSED)
+thesaurus_func_complete(int type  __attribute__((unused)) )
 {
     return FALSE;
 }
@@ -74111,15 +72293,15 @@
                 status = INS_COMPL_CPT_CONT;
                 goto done;
             }
-            compl_type = CTRL_X_DICTIONARY;
+            compl_type =  (9 + CTRL_X_WANT_IDENT) ;
             st->dict = st->ins_buf->b_fname;
-            st->dict_f = DICT_EXACT;
+            st->dict_f =  (2) ;
         }
         if (!shortmess(SHM_COMPLETIONSCAN) && !compl_autocomplete)
         {
             msg_hist_off = TRUE;
-            vim_snprintf((char *)IObuff, IOSIZE, _("Scanning: %s"), st->ins_buf->b_fname == NULL ? buf_spname(st->ins_buf) : st->ins_buf->b_sfname == NULL ? st->ins_buf->b_fname : st->ins_buf->b_sfname);
-            (void)msg_trunc_attr((char *)IObuff, TRUE, HL_ATTR(HLF_R));
+            vim_snprintf((char *)IObuff,  (1024+1) , _("Scanning: %s"), st->ins_buf->b_fname == NULL ? buf_spname(st->ins_buf) : st->ins_buf->b_sfname == NULL ? st->ins_buf->b_fname : st->ins_buf->b_sfname);
+            (void)msg_trunc_attr((char *)IObuff, TRUE,  highlight_attr[(int)(HLF_R)] );
         }
     }
     else if (*st->e_cpt == NUL)
@@ -74138,26 +72320,26 @@
             {
                 if (*st->e_cpt == 'k')
                 {
-                    compl_type = CTRL_X_DICTIONARY;
+                    compl_type =  (9 + CTRL_X_WANT_IDENT) ;
                 }
                 else
                 {
-                    compl_type = CTRL_X_THESAURUS;
+                    compl_type =  (10 + CTRL_X_WANT_IDENT) ;
                 }
                 if (*++st->e_cpt != ',' && *st->e_cpt != NUL)
                 {
                     st->dict = st->e_cpt;
-                    st->dict_f = DICT_FIRST;
+                    st->dict_f =  (1) ;
                 }
             }
             else if (*st->e_cpt == ']' || *st->e_cpt == 't')
             {
-                compl_type = CTRL_X_TAGS;
+                compl_type =  (5 + CTRL_X_WANT_IDENT) ;
                 if (!shortmess(SHM_COMPLETIONSCAN) && !compl_autocomplete)
                 {
                     msg_hist_off = TRUE;
-                    vim_snprintf((char *)IObuff, IOSIZE, _("Scanning tags."));
-                    (void)msg_trunc_attr((char *)IObuff, TRUE, HL_ATTR(HLF_R));
+                    vim_snprintf((char *)IObuff,  (1024+1) , _("Scanning tags."));
+                    (void)msg_trunc_attr((char *)IObuff, TRUE,  highlight_attr[(int)(HLF_R)] );
                 }
             }
             else
@@ -74166,7 +72348,7 @@
             }
         }
 
-        (void)copy_option_part(&st->e_cpt, IObuff, IOSIZE, ",");
+        (void)copy_option_part(&st->e_cpt, IObuff,  (1024+1) , ",");
         *advance_cpt_idx = may_advance_cpt_index(st->e_cpt);
 
         st->found_all = TRUE;
@@ -74184,7 +72366,7 @@
     static void
 get_next_dict_tsr_completion(int compl_type, char_u *dict, int dict_f)
 {
-        ins_compl_dictionaries(dict != NULL ? dict : (compl_type == CTRL_X_THESAURUS ? (*curbuf->b_p_tsr == NUL ? p_tsr : curbuf->b_p_tsr) : (*curbuf->b_p_dict == NUL ? p_dict : curbuf->b_p_dict)), compl_pattern.string, dict != NULL ? dict_f : 0, compl_type == CTRL_X_THESAURUS);
+        ins_compl_dictionaries(dict != NULL ? dict : (compl_type ==  (10 + CTRL_X_WANT_IDENT)  ? (*curbuf->b_p_tsr == NUL ? p_tsr : curbuf->b_p_tsr) : (*curbuf->b_p_dict == NUL ? p_dict : curbuf->b_p_dict)), compl_pattern.string, dict != NULL ? dict_f : 0, compl_type ==  (10 + CTRL_X_WANT_IDENT) );
 }
 
     static void
@@ -74294,13 +72476,13 @@
 
         while (j < prefix_len && *match_ptr != NUL && *prefix_ptr != NUL)
         {
-            if (STRNCMP(prefix_ptr, match_ptr, mb_ptr2len(prefix_ptr)) != 0)
+            if ( strncmp((char *)(prefix_ptr), (char *)(match_ptr), (mb_ptr2len(prefix_ptr)))  != 0)
             {
                 break;
             }
 
-            MB_PTR_ADV(prefix_ptr);
-            MB_PTR_ADV(match_ptr);
+             prefix_ptr += (*mb_ptr2len)(prefix_ptr) ;
+             match_ptr += (*mb_ptr2len)(match_ptr) ;
             j++;
         }
 
@@ -74313,7 +72495,7 @@
     leader = ins_compl_leader();
     leader_len = ins_compl_leader_len();
 
-    if (leader_len > 0 && STRNCMP(prefix, leader, leader_len) != 0)
+    if (leader_len > 0 &&  strncmp((char *)(prefix), (char *)(leader), (leader_len))  != 0)
     {
         goto end;
     }
@@ -74350,14 +72532,16 @@
     int         current_score = 0;
     int         dir = compl_direction;
 
-    char pathsep = PATHSEP;
+    char pathsep =  ((char_u)'/') ;
 
     if (in_fuzzy_collect)
     {
         last_sep = vim_strrchr(leader, pathsep);
         if (last_sep == NULL)
         {
-            VIM_CLEAR_STRING(compl_pattern);
+             vim_free(compl_pattern.string);
+             (compl_pattern.string) = NULL;
+             compl_pattern.length = 0;
             compl_pattern.string = vim_strnsave((char_u *)"*", 1);
             if (compl_pattern.string == NULL)
             {
@@ -74378,7 +72562,9 @@
             if (path_with_wildcard != NULL)
             {
                 vim_snprintf((char *)path_with_wildcard, path_len + 2, "%*.*s*", path_len, path_len, leader);
-                VIM_CLEAR_STRING(compl_pattern);
+                 vim_free(compl_pattern.string);
+                 (compl_pattern.string) = NULL;
+                 compl_pattern.length = 0;
                 compl_pattern.string = path_with_wildcard;
                 compl_pattern.length = path_len + 1;
 
@@ -74404,7 +72590,7 @@
         {
             ptr = matches[i];
             score = fuzzy_match_str(ptr, leader);
-            if (score != FUZZY_SCORE_NONE)
+            if (score !=  INT_MIN )
             {
                 if (ga_grow(&fuzzy_indices, 1) == OK)
                 {
@@ -74470,7 +72656,7 @@
     char_u      **matches;
     int         num_matches;
 
-    if (expand_cmdline(&compl_xp, compl_pattern.string, (int)compl_pattern.length, &num_matches, &matches) == EXPAND_OK)
+    if (expand_cmdline(&compl_xp, compl_pattern.string, (int)compl_pattern.length, &num_matches, &matches) ==  (-1) )
     {
         int             add_r = OK;
         int             dir = compl_direction;
@@ -74496,7 +72682,7 @@
                 cptext[CPT_INFO] = compl_xp.xp_files_info[i];
             }
 
-            add_r = ins_compl_add(matches[i], -1, NULL, cptext, NULL, dir, CP_FAST, FALSE, NULL, FUZZY_SCORE_NONE);
+            add_r = ins_compl_add(matches[i], -1, NULL, cptext, NULL, dir, CP_FAST, FALSE, NULL,  INT_MIN );
             if (add_r == OK)
             {
                 dir = FORWARD;
@@ -74508,7 +72694,7 @@
 }
 
     static void
-get_next_spell_completion(linenr_T lnum UNUSED)
+get_next_spell_completion(linenr_T lnum  __attribute__((unused)) )
 {
 }
 
@@ -74561,7 +72747,7 @@
         {
             if (cur_match_pos->lnum < ins_buf->b_ml.ml_line_count)
             {
-                STRNCPY(IObuff, ptr, len);
+                 strncpy((char *)(IObuff), (char *)(ptr), (len)) ;
                 ptr = ml_get_buf(ins_buf, cur_match_pos->lnum + 1, FALSE);
                 tmp_ptr = ptr = skipwhite(ptr);
                 tmp_ptr = find_word_start(tmp_ptr);
@@ -74579,11 +72765,11 @@
                             IObuff[len++] = ' ';
                         }
                     }
-                    if (tmp_ptr - ptr >= IOSIZE - len)
+                    if (tmp_ptr - ptr >=  (1024+1)  - len)
                     {
-                        tmp_ptr = ptr + IOSIZE - len - 1;
+                        tmp_ptr = ptr +  (1024+1)  - len - 1;
                     }
-                    STRNCPY(IObuff + len, ptr, tmp_ptr - ptr);
+                     strncpy((char *)(IObuff + len), (char *)(ptr), (tmp_ptr - ptr)) ;
                     len += (int)(tmp_ptr - ptr);
                     *cont_s_ipos = TRUE;
                 }
@@ -74613,7 +72799,7 @@
     int         in_fuzzy_collect = !compl_status_adding()
                         && cot_fuzzy() && compl_length > 0;
     char_u      *leader = ins_compl_leader();
-    int         score = FUZZY_SCORE_NONE;
+    int         score =  INT_MIN ;
     int         in_curbuf = st->ins_buf == curbuf;
 
     save_p_scs = p_scs;
@@ -74699,7 +72885,7 @@
         {
             ptr = ins_compl_get_next_word_or_line(st->ins_buf, st->cur_match_pos, &len, &cont_s_ipos);
         }
-        if (ptr == NULL || (ins_compl_has_preinsert() && STRCMP(ptr, ins_compl_leader()) == 0))
+        if (ptr == NULL || (ins_compl_has_preinsert() &&  strcmp((char *)(ptr), (char *)(ins_compl_leader()))  == 0))
         {
             continue;
         }
@@ -74780,15 +72966,15 @@
 
             if (adding_mode)
             {
-                int str_len = (int)STRLEN(str);
+                int str_len = (int) strlen((char *)(str)) ;
                 if (str_len == 0)
                 {
                     continue;
                 }
 
-                if (!compl_orig_text.string || (p_ic ? STRNICMP(str, compl_orig_text.string, compl_orig_text.length) == 0 : STRNCMP(str, compl_orig_text.string, compl_orig_text.length) == 0))
+                if (!compl_orig_text.string || (p_ic ?  strncasecmp((char *)(str), (char *)(compl_orig_text.string), (compl_orig_text.length))  == 0 :  strncmp((char *)(str), (char *)(compl_orig_text.string), (compl_orig_text.length))  == 0))
                 {
-                    if (ins_compl_add_infercase(str, str_len, p_ic, NULL, dir, FALSE, FUZZY_SCORE_NONE) == OK)
+                    if (ins_compl_add_infercase(str, str_len, p_ic, NULL, dir, FALSE,  INT_MIN ) == OK)
                     {
                         dir = FORWARD;
                     }
@@ -74796,7 +72982,7 @@
             }
             else
             {
-                char_u *str_end = str + STRLEN(str);
+                char_u *str_end = str +  strlen((char *)(str)) ;
                 char_u *p = str;
 
                 while (p < str_end && *p != NUL)
@@ -74828,9 +73014,9 @@
                     }
 
                     int len = (int)(word_end - p);
-                    if (len > 0 && (!compl_orig_text.string || (p_ic ? STRNICMP(p, compl_orig_text.string, compl_orig_text.length) == 0 : STRNCMP(p, compl_orig_text.string, compl_orig_text.length) == 0)))
+                    if (len > 0 && (!compl_orig_text.string || (p_ic ?  strncasecmp((char *)(p), (char *)(compl_orig_text.string), (compl_orig_text.length))  == 0 :  strncmp((char *)(p), (char *)(compl_orig_text.string), (compl_orig_text.length))  == 0)))
                     {
-                        if (ins_compl_add_infercase(p, len, p_ic, NULL, dir, FALSE, FUZZY_SCORE_NONE) == OK)
+                        if (ins_compl_add_infercase(p, len, p_ic, NULL, dir, FALSE,  INT_MIN ) == OK)
                         {
                             dir = FORWARD;
                         }
@@ -74864,13 +73050,13 @@
         case -1:
             break;
 
-        case CTRL_X_DICTIONARY:
-        case CTRL_X_THESAURUS:
+        case  (9 + CTRL_X_WANT_IDENT) :
+        case  (10 + CTRL_X_WANT_IDENT) :
             get_next_dict_tsr_completion(type, st->dict, st->dict_f);
             st->dict = NULL;
             break;
 
-        case CTRL_X_TAGS:
+        case  (5 + CTRL_X_WANT_IDENT) :
             get_next_tag_completion();
             break;
 
@@ -74953,11 +73139,11 @@
 }
 
     static void
-compl_source_start_timer(int source_idx UNUSED)
+compl_source_start_timer(int source_idx  __attribute__((unused)) )
 {
     if (compl_autocomplete || p_cto > 0)
     {
-        ELAPSED_INIT(cpt_sources_array[source_idx].compl_start_tv);
+         gettimeofday(&(cpt_sources_array[source_idx].compl_start_tv), NULL) ;
         compl_time_slice_expired = FALSE;
     }
 }
@@ -74990,11 +73176,13 @@
     {
         buf_T *buf;
 
-        FOR_ALL_BUFFERS(buf)
+         for ((buf) = firstbuf; (buf) != NULL; (buf) = (buf)->b_next) 
+         {
             buf->b_scanned = false;
+         }
         if (!st_cleared)
         {
-            CLEAR_FIELD(st);
+              memset((&(st)), (0), (sizeof(st)))  ;
             st_cleared = TRUE;
         }
         st.found_all = FALSE;
@@ -75004,7 +73192,6 @@
         strip_caret_numbers_in_place(st.e_cpt_copy);
         st.e_cpt = st.e_cpt_copy == NULL ? (char_u *)"" : st.e_cpt_copy;
 
-#define LOOKBACK_LINE_COUNT     1000
         if (compl_autocomplete && is_nearest_active())
         {
             start_pos.lnum = MAX(1, start_pos.lnum - LOOKBACK_LINE_COUNT);
@@ -75105,7 +73292,7 @@
         }
         else
         {
-            if (buf_valid(st.ins_buf) && (type == 0 || type == CTRL_X_PATH_PATTERNS))
+            if (buf_valid(st.ins_buf) && (type == 0 || type ==  (6 + CTRL_X_WANT_IDENT) ))
             {
                 st.ins_buf->b_scanned = true;
             }
@@ -75305,7 +73492,7 @@
         return NULL;
     }
 
-    match_count = ALLOC_CLEAR_MULT(int, cpt_sources_count);
+    match_count =  (int *)alloc_clear(sizeof(int) * (cpt_sources_count)) ;
     if (match_count == NULL)
     {
         return NULL;
@@ -75340,10 +73527,10 @@
 
             if (!match_limit_exceeded && (!curbuf_only || (cur_source != -1 && cpt_sources_array[cur_source].cs_flag == '.')))
             {
-                if (first == NULL && STRNCMP(ins_compl_leader(), compl->cp_str.string, ins_compl_leader_len()) == 0)
+                if (first == NULL &&  strncmp((char *)(ins_compl_leader()), (char *)(compl->cp_str.string), (ins_compl_leader_len()))  == 0)
                 {
                     first = compl->cp_str.string;
-                    len = (int)STRLEN(first);
+                    len = (int) strlen((char *)(first)) ;
                 }
                 else if (first != NULL)
                 {
@@ -75353,14 +73540,14 @@
 
                     while (j < len && *s1 != NUL && *s2 != NUL)
                     {
-                        if (MB_BYTE2LEN(*s1) != MB_BYTE2LEN(*s2) || memcmp(s1, s2, MB_BYTE2LEN(*s1)) != 0)
+                        if ( mb_bytelen_tab[*s1]  !=  mb_bytelen_tab[*s2]  || memcmp(s1, s2,  mb_bytelen_tab[*s1] ) != 0)
                         {
                             break;
                         }
 
-                        j += MB_BYTE2LEN(*s1);
-                        MB_PTR_ADV(s1);
-                        MB_PTR_ADV(s2);
+                        j +=  mb_bytelen_tab[*s1] ;
+                         s1 += (*mb_ptr2len)(s1) ;
+                         s2 += (*mb_ptr2len)(s2) ;
                     }
                     len = j;
 
@@ -75378,15 +73565,15 @@
 
     if (len > (int)ins_compl_leader_len())
     {
-        if (len == (int)STRLEN(first))
+        if (len == (int) strlen((char *)(first)) )
         {
             char_u *line = ml_get_curline();
             char_u *p = line + curwin->w_cursor.col;
-            if (p && !IS_WHITE_OR_NUL(*p))
+            if (p && ! ((*p) == ' ' || (*p) == '\t' || (*p) == NUL) )
             {
                 char_u *end = find_word_end(p);
                 int text_len = end - p;
-                if (text_len > 0 && text_len < (len - (int)ins_compl_leader_len()) && STRNCMP(first + len - text_len, p, text_len) == 0)
+                if (text_len > 0 && text_len < (len - (int)ins_compl_leader_len()) &&  strncmp((char *)(first + len - text_len), (char *)(p), (text_len))  == 0)
                 {
                     len -= text_len;
                 }
@@ -75478,19 +73665,19 @@
         return;
     }
 
-    for (s = e = compl_shown_match->cp_fname; *e != NUL; MB_PTR_ADV(e))
+    for (s = e = compl_shown_match->cp_fname; *e != NUL;  e += (*mb_ptr2len)(e) )
     {
         space -= ptr2cells(e);
         while (space < 0)
         {
             space += ptr2cells(s);
-            MB_PTR_ADV(s);
+             s += (*mb_ptr2len)(s) ;
         }
     }
     if (!compl_autocomplete)
     {
         msg_hist_off = TRUE;
-        vim_snprintf((char *)IObuff, IOSIZE, "%s %s%s", lead, s > compl_shown_match->cp_fname ? "<" : "", s);
+        vim_snprintf((char *)IObuff,  (1024+1) , "%s %s%s", lead, s > compl_shown_match->cp_fname ? "<" : "", s);
         msg((char *)IObuff);
         msg_hist_off = FALSE;
         redraw_cmdline = FALSE;
@@ -75603,7 +73790,7 @@
 
         leader = get_leader_for_startcol(compl_shown_match, FALSE);
 
-        if (!match_at_original_text(compl_shown_match) && leader->string != NULL && !ins_compl_equal(compl_shown_match, leader->string, (int)leader->length) && !(cot_fuzzy() && compl_shown_match->cp_score != FUZZY_SCORE_NONE))
+        if (!match_at_original_text(compl_shown_match) && leader->string != NULL && !ins_compl_equal(compl_shown_match, leader->string, (int)leader->length) && !(cot_fuzzy() && compl_shown_match->cp_score !=  INT_MIN ))
         {
             ++todo;
         }
@@ -75739,12 +73926,15 @@
 {
     elapsed_T   *start_tv
         = &cpt_sources_array[cpt_sources_index].compl_start_tv;
-    long        elapsed_ms = ELAPSED_FUNC(*start_tv);
+    long        elapsed_ms =  elapsed(&(*start_tv)) ;
 
     if (elapsed_ms > compl_timeout_ms)
     {
         compl_time_slice_expired = TRUE;
-        DECAY_COMPL_TIMEOUT();
+         if (compl_timeout_ms > COMPL_MIN_TIMEOUT_MS)
+         {
+             compl_timeout_ms /= 2;
+         }
     }
 }
 
@@ -75772,12 +73962,12 @@
         {
             c = safe_vgetc();
             compl_shows_dir = ins_compl_key2dir(c);
-            (void)ins_compl_next(FALSE, ins_compl_key2count(c), c != K_UP && c != K_DOWN);
+            (void)ins_compl_next(FALSE, ins_compl_key2count(c), c !=   (-(('k') + ((int)('u') << 8)))   && c !=   (-(('k') + ((int)('d') << 8)))  );
         }
         else
         {
             c = safe_vgetc();
-            if (c != K_IGNORE)
+            if (c !=   (-((KS_EXTRA) + ((int)(KE_IGNORE) << 8)))  )
             {
                 if (c != Ctrl_R && (KeyTyped || (in_compl_func && reg_executing == 0)))
                 {
@@ -75811,9 +74001,9 @@
     static int
 ins_compl_key2dir(int c)
 {
-    if (c == Ctrl_P || c == Ctrl_L || c == K_PAGEUP || c == K_KPAGEUP || c == K_S_UP || c == K_UP)
+    if (c == Ctrl_P || c == Ctrl_L || c ==   (-(('k') + ((int)('P') << 8)))   || c ==   (-(('K') + ((int)('3') << 8)))   || c ==   (-((KS_EXTRA) + ((int)(KE_S_UP) << 8)))   || c ==   (-(('k') + ((int)('u') << 8)))  )
     {
-        return BACKWARD;
+        return  (-1) ;
     }
     return FORWARD;
 }
@@ -75821,13 +74011,13 @@
     static int
 ins_compl_pum_key(int c)
 {
-    return pum_visible() && (c == K_PAGEUP || c == K_KPAGEUP || c == K_S_UP || c == K_PAGEDOWN || c == K_KPAGEDOWN || c == K_S_DOWN || c == K_UP || c == K_DOWN);
+    return pum_visible() && (c ==   (-(('k') + ((int)('P') << 8)))   || c ==   (-(('K') + ((int)('3') << 8)))   || c ==   (-((KS_EXTRA) + ((int)(KE_S_UP) << 8)))   || c ==   (-(('k') + ((int)('N') << 8)))   || c ==   (-(('K') + ((int)('5') << 8)))   || c ==   (-((KS_EXTRA) + ((int)(KE_S_DOWN) << 8)))   || c ==   (-(('k') + ((int)('u') << 8)))   || c ==   (-(('k') + ((int)('d') << 8)))  );
 }
 
     static int
 ins_compl_key2count(int c)
 {
-    if (ins_compl_pum_key(c) && c != K_UP && c != K_DOWN)
+    if (ins_compl_pum_key(c) && c !=   (-(('k') + ((int)('u') << 8)))   && c !=   (-(('k') + ((int)('d') << 8)))  )
     {
         int h = pum_get_height();
         if (h > 3)
@@ -75844,14 +74034,14 @@
 {
     switch (c)
     {
-        case K_UP:
-        case K_DOWN:
-        case K_PAGEDOWN:
-        case K_KPAGEDOWN:
-        case K_S_DOWN:
-        case K_PAGEUP:
-        case K_KPAGEUP:
-        case K_S_UP:
+        case   (-(('k') + ((int)('u') << 8)))  :
+        case   (-(('k') + ((int)('d') << 8)))  :
+        case   (-(('k') + ((int)('N') << 8)))  :
+        case   (-(('K') + ((int)('5') << 8)))  :
+        case   (-((KS_EXTRA) + ((int)(KE_S_DOWN) << 8)))  :
+        case   (-(('k') + ((int)('P') << 8)))  :
+        case   (-(('K') + ((int)('3') << 8)))  :
+        case   (-((KS_EXTRA) + ((int)(KE_S_UP) << 8)))  :
             return FALSE;
     }
     return TRUE;
@@ -75879,7 +74069,7 @@
                 compl_pattern.length = 0;
                 return FAIL;
             }
-            compl_pattern.length = STRLEN(compl_pattern.string);
+            compl_pattern.length =  strlen((char *)(compl_pattern.string)) ;
         }
         else
         {
@@ -75895,7 +74085,7 @@
     else if (compl_status_adding())
     {
         char_u      *prefix = (char_u *)"\\<";
-        size_t      prefixlen = STRLEN_LITERAL("\\<");
+        size_t      prefixlen =  (sizeof("\\<" "") - 1) ;
         size_t      n;
 
         if (!vim_iswordp(line + compl_col) || (compl_col > 0 && (vim_iswordp(mb_prevptr(line, line + compl_col)))))
@@ -75911,13 +74101,13 @@
             compl_pattern.length = 0;
             return FAIL;
         }
-        STRCPY((char *)compl_pattern.string, prefix);
+         strcpy((char *)((char *)compl_pattern.string), (char *)(prefix)) ;
         (void)quote_meta(compl_pattern.string + prefixlen, line + compl_col, compl_length);
         compl_pattern.length = n - 1;
     }
     else if (--startcol < 0 || !vim_iswordp(mb_prevptr(line, line + startcol + 1)))
     {
-        size_t  len = STRLEN_LITERAL("\\<\\k\\k");
+        size_t  len =  (sizeof("\\<\\k\\k" "") - 1) ;
 
         compl_pattern.string = vim_strnsave((char_u *)"\\<\\k\\k", len);
         if (compl_pattern.string == NULL)
@@ -75967,10 +74157,10 @@
                 compl_pattern.length = 0;
                 return FAIL;
             }
-            STRCPY((char *)compl_pattern.string, "\\<");
+             strcpy((char *)((char *)compl_pattern.string), (char *)("\\<")) ;
             (void)quote_meta(compl_pattern.string + 2, line + compl_col, 1);
-            STRCAT((char *)compl_pattern.string, "\\k");
-            compl_pattern.length = STRLEN(compl_pattern.string);
+             strcat((char *)((char *)compl_pattern.string), (char *)("\\k")) ;
+            compl_pattern.length =  strlen((char *)(compl_pattern.string)) ;
         }
         else
         {
@@ -75982,7 +74172,7 @@
                 compl_pattern.length = 0;
                 return FAIL;
             }
-            STRCPY((char *)compl_pattern.string, "\\<");
+             strcpy((char *)((char *)compl_pattern.string), (char *)("\\<")) ;
             (void)quote_meta(compl_pattern.string + 2, line + compl_col, compl_length);
             compl_pattern.length = n - 1;
         }
@@ -76016,7 +74206,7 @@
             compl_pattern.length = 0;
             return FAIL;
         }
-        compl_pattern.length = STRLEN(compl_pattern.string);
+        compl_pattern.length =  strlen((char *)(compl_pattern.string)) ;
     }
     else
     {
@@ -76039,12 +74229,12 @@
     {
         char_u  *p = line + startcol;
 
-        MB_PTR_BACK(line, p);
-        while (p > line && vim_isfilec(PTR2CHAR(p)))
+         p -= has_mbyte ? ((*mb_head_off)(line, (p) - 1) + 1) : 1 ;
+        while (p > line && vim_isfilec( (has_mbyte ? mb_ptr2char(p) : (int)*(p)) ))
         {
-            MB_PTR_BACK(line, p);
+             p -= has_mbyte ? ((*mb_head_off)(line, (p) - 1) + 1) : 1 ;
         }
-        if (p == line && vim_isfilec(PTR2CHAR(p)))
+        if (p == line && vim_isfilec( (has_mbyte ? mb_ptr2char(p) : (int)*(p)) ))
         {
             startcol = 0;
         }
@@ -76063,7 +74253,7 @@
         return FAIL;
     }
 
-    compl_pattern.length = STRLEN(compl_pattern.string);
+    compl_pattern.length =  strlen((char *)(compl_pattern.string)) ;
 
     return OK;
 }
@@ -76079,7 +74269,7 @@
     }
     compl_pattern.length = curs_col;
     set_cmd_context(&compl_xp, compl_pattern.string, (int)compl_pattern.length, curs_col, FALSE);
-    if (compl_xp.xp_context == EXPAND_UNSUCCESSFUL || compl_xp.xp_context == EXPAND_NOTHING)
+    if (compl_xp.xp_context ==  (-2)  || compl_xp.xp_context == EXPAND_NOTHING)
     {
         compl_col = curs_col;
     }
@@ -76093,7 +74283,7 @@
 }
 
     static int
-get_userdefined_compl_info(colnr_T     curs_col UNUSED, callback_T  *cb UNUSED, int         *startcol UNUSED)
+get_userdefined_compl_info(colnr_T     curs_col  __attribute__((unused)) , callback_T  *cb  __attribute__((unused)) , int         *startcol  __attribute__((unused)) )
 {
     int         ret = FAIL;
 
@@ -76101,7 +74291,7 @@
 }
 
     static int
-get_spell_compl_info(int startcol UNUSED, colnr_T curs_col UNUSED)
+get_spell_compl_info(int startcol  __attribute__((unused)) , colnr_T curs_col  __attribute__((unused)) )
 {
     int         ret = FAIL;
 
@@ -76159,7 +74349,7 @@
     static void
 ins_compl_continue_search(char_u *line)
 {
-    compl_cont_status &= ~CONT_INTRPT;
+    compl_cont_status &= ~ (2 + 4) ;
     if (ctrl_x_mode_normal() || ctrl_x_mode_path_patterns() || ctrl_x_mode_path_defines())
     {
         if (compl_startpos.lnum != curwin->w_cursor.lnum)
@@ -76179,11 +74369,10 @@
             compl_col = compl_startpos.col;
         }
         compl_length = curwin->w_cursor.col - (int)compl_col;
-#define MIN_SPACE 75
-        if (compl_length > (IOSIZE - MIN_SPACE))
+        if (compl_length > ( (1024+1)  - MIN_SPACE))
         {
             compl_cont_status &= ~CONT_SOL;
-            compl_length = (IOSIZE - MIN_SPACE);
+            compl_length = ( (1024+1)  - MIN_SPACE);
             compl_col = curwin->w_cursor.col - compl_length;
         }
         compl_cont_status |= CONT_ADDING | CONT_N_ADDS;
@@ -76227,7 +74416,7 @@
     compl_pending = 0;
     compl_lnum = curwin->w_cursor.lnum;
 
-    if ((compl_cont_status & CONT_INTRPT) == CONT_INTRPT && compl_cont_mode == ctrl_x_mode)
+    if ((compl_cont_status &  (2 + 4) ) ==  (2 + 4)  && compl_cont_mode == ctrl_x_mode)
     {
         ins_compl_continue_search(line);
     }
@@ -76296,23 +74485,29 @@
         }
         else
         {
-            edit_submode = (char_u *)_(CTRL_X_MSG(ctrl_x_mode));
+            edit_submode = (char_u *)_( ctrl_x_msgs[(ctrl_x_mode) & ~CTRL_X_WANT_IDENT] );
         }
     }
 
     ins_compl_fixRedoBufForLeader(NULL);
 
-    VIM_CLEAR_STRING(compl_orig_text);
+     vim_free(compl_orig_text.string);
+     (compl_orig_text.string) = NULL;
+     compl_orig_text.length = 0;
     compl_orig_text.length = (size_t)compl_length;
     compl_orig_text.string = vim_strnsave(line + compl_col, (size_t)compl_length);
     if (p_ic)
     {
         flags |= CP_ICASE;
     }
-    if (compl_orig_text.string == NULL || ins_compl_add(compl_orig_text.string, (int)compl_orig_text.length, NULL, NULL, NULL, 0, flags, FALSE, NULL, FUZZY_SCORE_NONE) != OK)
+    if (compl_orig_text.string == NULL || ins_compl_add(compl_orig_text.string, (int)compl_orig_text.length, NULL, NULL, NULL, 0, flags, FALSE, NULL,  INT_MIN ) != OK)
     {
-        VIM_CLEAR_STRING(compl_pattern);
-        VIM_CLEAR_STRING(compl_orig_text);
+         vim_free(compl_pattern.string);
+         (compl_pattern.string) = NULL;
+         compl_pattern.length = 0;
+         vim_free(compl_orig_text.string);
+         (compl_orig_text.string) = NULL;
+         compl_orig_text.length = 0;
         did_ai = save_did_ai;
         return FAIL;
     }
@@ -76393,7 +74588,7 @@
                 if (!p_smd)
                 {
                     msg_hist_off = TRUE;
-                    msg_attr((char *)edit_submode_extra, edit_submode_highl < HLF_COUNT ? HL_ATTR(edit_submode_highl) : 0);
+                    msg_attr((char *)edit_submode_extra, edit_submode_highl < HLF_COUNT ?  highlight_attr[(int)(edit_submode_highl)]  : 0);
                     msg_hist_off = FALSE;
                 }
             }
@@ -76525,7 +74720,7 @@
 {
     if (p_acl > 0)
     {
-        ELAPSED_INIT(compl_autocomplete_start_tv);
+         gettimeofday(&(compl_autocomplete_start_tv), NULL) ;
         compl_autocomplete_pending = true;
         return true;
     }
@@ -76547,7 +74742,7 @@
     long
 ins_compl_autocomplete_elapsed(void)
 {
-    return ELAPSED_FUNC(compl_autocomplete_start_tv);
+    return  elapsed(&(compl_autocomplete_start_tv)) ;
 }
 
     static void
@@ -76584,16 +74779,19 @@
                 {
                     break;
                 }
+            __attribute__((fallthrough));
             case '~':
                 if (!magic_isset())
                 {
                     break;
                 }
+            __attribute__((fallthrough));
             case '\\':
                 if (ctrl_x_mode_dictionary() || ctrl_x_mode_thesaurus())
                 {
                     break;
                 }
+            __attribute__((fallthrough));
             case '^':
             case '$':
                 m++;
@@ -76635,7 +74833,8 @@
     static void
 cpt_sources_clear(void)
 {
-    VIM_CLEAR(cpt_sources_array);
+     vim_free(cpt_sources_array);
+     (cpt_sources_array) = NULL;
     cpt_sources_index = -1;
     cpt_sources_count = 0;
 }
@@ -76657,7 +74856,7 @@
         return OK;
     }
 
-    cpt_sources_array = ALLOC_CLEAR_MULT(cpt_source_T, count);
+    cpt_sources_array =  (cpt_source_T *)alloc_clear(sizeof(cpt_source_T) * (count)) ;
     if (cpt_sources_array == NULL)
     {
         return FAIL;
@@ -76715,11 +74914,9 @@
 {
 }
 
-
 // ==================== linematch.c ====================
 
-#define LN_MAX_BUFS 8
-#define LN_DECISION_MAX 255
+enum { LN_DECISION_MAX = 255 };
 
 typedef struct diffcmppath_S diffcmppath_T;
 struct diffcmppath_S
@@ -76732,19 +74929,6 @@
     size_t              df_optimal_choice;
 };
 
-
-
-#define MATCH_CHAR_MAX_LEN 800
-
-
-
-
-
-
-
-
-
-
 // ==================== locale.c ====================
 
     void
@@ -76764,32 +74948,31 @@
     char_u      *name;
     int         what = LC_ALL;
     char        *whatstr = "";
-#  define VIM_LC_MESSAGES LC_MESSAGES
 
     name = eap->arg;
 
     p = skiptowhite(eap->arg);
-    if ((*p == NUL || VIM_ISWHITE(*p)) && p - eap->arg >= 3)
+    if ((*p == NUL ||  ((*p) == ' ' || (*p) == '\t') ) && p - eap->arg >= 3)
     {
-        if (STRNICMP(eap->arg, "messages", p - eap->arg) == 0)
+        if ( strncasecmp((char *)(eap->arg), (char *)("messages"), (p - eap->arg))  == 0)
         {
-            what = VIM_LC_MESSAGES;
+            what =  LC_MESSAGES ;
             name = skipwhite(p);
             whatstr = "messages ";
         }
-        else if (STRNICMP(eap->arg, "ctype", p - eap->arg) == 0)
+        else if ( strncasecmp((char *)(eap->arg), (char *)("ctype"), (p - eap->arg))  == 0)
         {
             what = LC_CTYPE;
             name = skipwhite(p);
             whatstr = "ctype ";
         }
-        else if (STRNICMP(eap->arg, "time", p - eap->arg) == 0)
+        else if ( strncasecmp((char *)(eap->arg), (char *)("time"), (p - eap->arg))  == 0)
         {
             what = LC_TIME;
             name = skipwhite(p);
             whatstr = "time ";
         }
-        else if (STRNICMP(eap->arg, "collate", p - eap->arg) == 0)
+        else if ( strncasecmp((char *)(eap->arg), (char *)("collate"), (p - eap->arg))  == 0)
         {
             what = LC_COLLATE;
             name = skipwhite(p);
@@ -76905,7 +75088,7 @@
 }
 
     char_u *
-get_lang_arg(expand_T *xp UNUSED, int idx)
+get_lang_arg(expand_T *xp  __attribute__((unused)) , int idx)
 {
     if (idx == 0)
     {
@@ -76933,7 +75116,7 @@
 }
 
     char_u *
-get_locales(expand_T *xp UNUSED, int idx)
+get_locales(expand_T *xp  __attribute__((unused)) , int idx)
 {
     init_locales();
     if (locales == NULL)
@@ -76952,18 +75135,16 @@
 
 static int              map_locked = 0;
 
-#define MAP_HASH(mode, c1) (((mode) & (MODE_NORMAL | MODE_VISUAL | MODE_SELECT | MODE_OP_PENDING | MODE_TERMINAL)) ? (c1) : ((c1) ^ 0x80))
-
     mapblock_T *
 get_maphash_list(int state, int c)
 {
-    return maphash[MAP_HASH(state, c)];
+    return maphash[ (((state) & (MODE_NORMAL | MODE_VISUAL | MODE_SELECT | MODE_OP_PENDING | MODE_TERMINAL)) ? (c) : ((c) ^ 0x80)) ];
 }
 
     mapblock_T *
 get_buf_maphash_list(int state, int c)
 {
-    return curbuf->b_maphash[MAP_HASH(state, c)];
+    return curbuf->b_maphash[ (((state) & (MODE_NORMAL | MODE_VISUAL | MODE_SELECT | MODE_OP_PENDING | MODE_TERMINAL)) ? (c) : ((c) ^ 0x80)) ];
 }
 
     int
@@ -76980,7 +75161,7 @@
         return;
     }
 
-    CLEAR_FIELD(maphash);
+      memset((&(maphash)), (0), (sizeof(maphash)))  ;
     maphash_valid = TRUE;
 }
 
@@ -77089,7 +75270,7 @@
     if (mapchars != NULL)
     {
         msg_puts((char *)mapchars);
-        len = (int)STRLEN(mapchars);
+        len = (int) strlen((char *)(mapchars)) ;
         vim_free(mapchars);
     }
 
@@ -77105,13 +75286,13 @@
         ++len;
     } while (len < 12);
 
-    if (mp->m_noremap == REMAP_NONE)
+    if (mp->m_noremap ==  (-1) )
     {
-        msg_puts_attr("*", HL_ATTR(HLF_8));
+        msg_puts_attr("*",  highlight_attr[(int)(HLF_8)] );
     }
-    else if (mp->m_noremap == REMAP_SCRIPT)
+    else if (mp->m_noremap ==  (-2) )
     {
-        msg_puts_attr("&", HL_ATTR(HLF_8));
+        msg_puts_attr("&",  highlight_attr[(int)(HLF_8)] );
     }
     else
     {
@@ -77129,7 +75310,7 @@
 
     if (*mp->m_str == NUL)
     {
-        msg_puts_attr("<Nop>", HL_ATTR(HLF_8));
+        msg_puts_attr("<Nop>",  highlight_attr[(int)(HLF_8)] );
     }
     else
     {
@@ -77145,7 +75326,7 @@
     static mapblock_T *
 map_add(mapblock_T  **map_table, mapblock_T  **abbr_table, char_u      *keys, char_u      *rhs, char_u      *orig_rhs, int         noremap, int         nowait, int         silent, int         mode, int         is_abbr, int         simplified)
 {
-    mapblock_T  *mp = ALLOC_CLEAR_ONE(mapblock_T);
+    mapblock_T  *mp =  (mapblock_T *)alloc_clear(sizeof(mapblock_T)) ;
 
     if (mp == NULL)
     {
@@ -77175,7 +75356,7 @@
         vim_free(mp);
         return NULL;
     }
-    mp->m_keylen = (int)STRLEN(mp->m_keys);
+    mp->m_keylen = (int) strlen((char *)(mp->m_keys)) ;
     mp->m_noremap = noremap;
     mp->m_nowait = nowait;
     mp->m_silent = silent;
@@ -77189,7 +75370,7 @@
     }
     else
     {
-        int n = MAP_HASH(mp->m_mode, mp->m_keys[0]);
+        int n =  (((mp->m_mode) & (MODE_NORMAL | MODE_VISUAL | MODE_SELECT | MODE_OP_PENDING | MODE_TERMINAL)) ? (mp->m_keys[0]) : ((mp->m_keys[0]) ^ 0x80)) ;
 
         mp->m_next = map_table[n];
         map_table[n] = mp;
@@ -77290,7 +75471,7 @@
                 else
                 {
                     int n = mp->m_keylen;
-                    if (STRNCMP(mp->m_keys, keys, (size_t)(n < keys_len ? n : keys_len)) == 0)
+                    if ( strncmp((char *)(mp->m_keys), (char *)(keys), ((size_t)(n < keys_len ? n : keys_len)))  == 0)
                     {
                         showmap(mp, TRUE);
                         *did_local = TRUE;
@@ -77346,7 +75527,7 @@
 
     if (maptype == MAPTYPE_NOREMAP)
     {
-        noremap = REMAP_NONE;
+        noremap =  (-1) ;
     }
     else
     {
@@ -77355,7 +75536,7 @@
 
     for (;;)
     {
-        if (STRNCMP(keys, "<buffer>", 8) == 0)
+        if ( strncmp((char *)(keys), (char *)("<buffer>"), (8))  == 0)
         {
             keys = skipwhite(keys + 8);
             map_table = curbuf->b_maphash;
@@ -77363,28 +75544,28 @@
             continue;
         }
 
-        if (STRNCMP(keys, "<nowait>", 8) == 0)
+        if ( strncmp((char *)(keys), (char *)("<nowait>"), (8))  == 0)
         {
             keys = skipwhite(keys + 8);
             nowait = TRUE;
             continue;
         }
 
-        if (STRNCMP(keys, "<silent>", 8) == 0)
+        if ( strncmp((char *)(keys), (char *)("<silent>"), (8))  == 0)
         {
             keys = skipwhite(keys + 8);
             silent = TRUE;
             continue;
         }
 
-        if (STRNCMP(keys, "<special>", 9) == 0)
+        if ( strncmp((char *)(keys), (char *)("<special>"), (9))  == 0)
         {
             keys = skipwhite(keys + 9);
             special = TRUE;
             continue;
         }
 
-        if (STRNCMP(keys, "<unique>", 8) == 0)
+        if ( strncmp((char *)(keys), (char *)("<unique>"), (8))  == 0)
         {
             keys = skipwhite(keys + 8);
             unique = TRUE;
@@ -77397,7 +75578,7 @@
 
     p = keys;
     do_backslash = (vim_strchr(p_cpo, CPO_BSLASH) == NULL);
-    while (*p && (maptype == MAPTYPE_UNMAP || !VIM_ISWHITE(*p)))
+    while (*p && (maptype == MAPTYPE_UNMAP || ! ((*p) == ' ' || (*p) == '\t') ))
     {
         if ((p[0] == Ctrl_V || (do_backslash && p[0] == '\\')) && p[1] != NUL)
         {
@@ -77441,7 +75622,7 @@
     orig_rhs = rhs;
     if (hasarg)
     {
-        if (STRICMP(rhs, "<nop>") == 0)
+        if ( strcasecmp((char *)(rhs), (char *)("<nop>"))  == 0)
         {
             rhs = (char_u *)"";
         }
@@ -77474,7 +75655,7 @@
 
         if (haskey)
         {
-            len = (int)STRLEN(keys);
+            len = (int) strlen((char *)(keys)) ;
             if (len > MAXMAPLEN)
             {
                 retval = 1;
@@ -77491,13 +75672,13 @@
                     char_u      keys_unescaped[MAXMAPLEN + 1];
                     size_t      keys_unescaped_len;
 
-                    mch_memmove(keys_unescaped, keys, (size_t)(len + 1));
+                     memmove((char *)(keys_unescaped), (char *)(keys), (size_t)(len + 1)) ;
                     keys_unescaped_len = vim_unescape_csi(keys_unescaped);
                     p = keys_unescaped;
 
                     first = vim_iswordp(p);
                     last = first;
-                    MB_PTR_ADV(p);
+                     p += (*mb_ptr2len)(p) ;
                     n = 1;
                     while (p < keys_unescaped + keys_unescaped_len)
                     {
@@ -77507,7 +75688,7 @@
                         {
                             same = n - 1;
                         }
-                        MB_PTR_ADV(p);
+                         p += (*mb_ptr2len)(p) ;
                     }
                     if (last && n > 2 && same >= 0 && same < n - 1)
                     {
@@ -77528,7 +75709,7 @@
                 }
                 for (n = 0; n < len; ++n)
                 {
-                    if (VIM_ISWHITE(keys[n]))
+                    if ( ((keys[n]) == ' ' || (keys[n]) == '\t') )
                     {
                         retval = 1;
                         goto theend;
@@ -77565,7 +75746,7 @@
                 }
                 for ( ; mp != NULL && !got_int; mp = mp->m_next)
                 {
-                    if ((mp->m_mode & mode) != 0 && mp->m_keylen == len && STRNCMP(mp->m_keys, keys, (size_t)len) == 0)
+                    if ((mp->m_mode & mode) != 0 && mp->m_keylen == len &&  strncmp((char *)(mp->m_keys), (char *)(keys), ((size_t)len))  == 0)
                     {
                         if (abbrev)
                         {
@@ -77606,7 +75787,6 @@
                 }
                 for (mp = *mpp; mp != NULL && !got_int; mp = *mpp)
                 {
-
                     if ((mp->m_mode & mode) == 0)
                     {
                         mpp = &(mp->m_next);
@@ -77624,7 +75804,7 @@
                     {
                         if (round)
                         {
-                            n = (int)STRLEN(mp->m_str);
+                            n = (int) strlen((char *)(mp->m_str)) ;
                             p = mp->m_str;
                         }
                         else
@@ -77632,7 +75812,7 @@
                             n = mp->m_keylen;
                             p = mp->m_keys;
                         }
-                        if (STRNCMP(p, keys, (size_t)(n < len ? n : len)) == 0)
+                        if ( strncmp((char *)(p), (char *)(keys), ((size_t)(n < len ? n : len)))  == 0)
                         {
                             if (maptype == MAPTYPE_UNMAP)
                             {
@@ -77709,7 +75889,7 @@
                                 continue;
                             }
 
-                            int new_hash = MAP_HASH(mp->m_mode, mp->m_keys[0]);
+                            int new_hash =  (((mp->m_mode) & (MODE_NORMAL | MODE_VISUAL | MODE_SELECT | MODE_OP_PENDING | MODE_TERMINAL)) ? (mp->m_keys[0]) : ((mp->m_keys[0]) ^ 0x80)) ;
                             if (!abbrev && new_hash != hash)
                             {
                                 *mpp = mp->m_next;
@@ -77858,7 +76038,7 @@
     int         mode;
     int         local;
 
-    local = (STRCMP(arg, "<buffer>") == 0);
+    local = ( strcmp((char *)(arg), (char *)("<buffer>"))  == 0);
     if (!local && *arg != NUL)
     {
         emsg(_(e_invalid_argument));
@@ -77934,7 +76114,7 @@
                     map_free(mpp);
                     continue;
                 }
-                new_hash = MAP_HASH(mp->m_mode, mp->m_keys[0]);
+                new_hash =  (((mp->m_mode) & (MODE_NORMAL | MODE_VISUAL | MODE_SELECT | MODE_OP_PENDING | MODE_TERMINAL)) ? (mp->m_keys[0]) : ((mp->m_keys[0]) ^ 0x80)) ;
                 if (!abbr && new_hash != hash)
                 {
                     *mpp = mp->m_next;
@@ -78033,7 +76213,7 @@
     for (; *str; ++str)
     {
         c = *str;
-        if (c == K_SPECIAL && str[1] != NUL && str[2] != NUL)
+        if (c ==  (0x80)  && str[1] != NUL && str[2] != NUL)
         {
             modifiers = 0;
             if (str[1] == KS_MODIFIER)
@@ -78042,21 +76222,21 @@
                 modifiers = *++str;
                 c = *++str;
             }
-            if (c == K_SPECIAL && str[1] != NUL && str[2] != NUL)
+            if (c ==  (0x80)  && str[1] != NUL && str[2] != NUL)
             {
                 if (cpo_special)
                 {
                     ga_clear(&ga);
                     return NULL;
                 }
-                c = TO_SPECIAL(str[1], str[2]);
-                if (c == K_ZERO)
+                c =  ((str[1]) == KS_SPECIAL ?  (0x80)  : (str[1]) == KS_ZERO ?   (-((KS_ZERO) + ((int)( ('X') ) << 8)))   :  (-((str[1]) + ((int)(str[2]) << 8))) ) ;
+                if (c ==   (-((KS_ZERO) + ((int)( ('X') ) << 8)))  )
                 {
                     c = NUL;
                 }
                 str += 2;
             }
-            if (IS_SPECIAL(c) || modifiers)
+            if ( ((c) < 0)  || modifiers)
             {
                 if (cpo_special)
                 {
@@ -78107,28 +76287,28 @@
         expand_buffer = FALSE;
         for (;;)
         {
-            if (STRNCMP(arg, "<buffer>", 8) == 0)
+            if ( strncmp((char *)(arg), (char *)("<buffer>"), (8))  == 0)
             {
                 expand_buffer = TRUE;
                 arg = skipwhite(arg + 8);
                 continue;
             }
-            if (STRNCMP(arg, "<unique>", 8) == 0)
+            if ( strncmp((char *)(arg), (char *)("<unique>"), (8))  == 0)
             {
                 arg = skipwhite(arg + 8);
                 continue;
             }
-            if (STRNCMP(arg, "<nowait>", 8) == 0)
+            if ( strncmp((char *)(arg), (char *)("<nowait>"), (8))  == 0)
             {
                 arg = skipwhite(arg + 8);
                 continue;
             }
-            if (STRNCMP(arg, "<silent>", 8) == 0)
+            if ( strncmp((char *)(arg), (char *)("<silent>"), (8))  == 0)
             {
                 arg = skipwhite(arg + 8);
                 continue;
             }
-            if (STRNCMP(arg, "<special>", 9) == 0)
+            if ( strncmp((char *)(arg), (char *)("<special>"), (9))  == 0)
             {
                 arg = skipwhite(arg + 9);
                 continue;
@@ -78205,7 +76385,7 @@
         else
         {
             score = fuzzy_match_str(p, pat);
-            match = (score != FUZZY_SCORE_NONE);
+            match = (score !=  INT_MIN );
         }
 
         if (!match)
@@ -78270,7 +76450,7 @@
             else
             {
                 score = fuzzy_match_str(p, pat);
-                match = (score != FUZZY_SCORE_NONE);
+                match = (score !=  INT_MIN );
             }
 
             if (!match)
@@ -78338,7 +76518,7 @@
 
         while (ptr2 < ptr3)
         {
-            if (STRCMP(*ptr1, *ptr2) != 0)
+            if ( strcmp((char *)(*ptr1), (char *)(*ptr2))  != 0)
             {
                 *++ptr1 = *ptr2++;
             }
@@ -78454,7 +76634,7 @@
             char_u      *q = mp->m_keys;
             int         match;
 
-            if (vim_strbyte(mp->m_keys, K_SPECIAL) != NULL)
+            if (vim_strbyte(mp->m_keys,  (0x80) ) != NULL)
             {
                 char_u *qe = vim_strsave(mp->m_keys);
 
@@ -78467,7 +76647,7 @@
 
             match =    (mp->m_mode & State)
                     && qlen == len
-                    && STRNCMP(q, ptr, (size_t)len) == 0;
+                    &&  strncmp((char *)(q), (char *)(ptr), ((size_t)len))  == 0;
             if (q != mp->m_keys)
             {
                 vim_free(q);
@@ -78485,11 +76665,11 @@
             j = 0;
             if (c != Ctrl_RSB)
             {
-                if (IS_SPECIAL(c) || c == K_SPECIAL)
+                if ( ((c) < 0)  || c ==  (0x80) )
                 {
-                    tb[j++] = K_SPECIAL;
-                    tb[j++] = K_SECOND(c);
-                    tb[j++] = K_THIRD(c);
+                    tb[j++] =  (0x80) ;
+                    tb[j++] =  ((c) ==  (0x80)  ? KS_SPECIAL : (c) == NUL ? KS_ZERO :  ((-(c)) & 0xff) ) ;
+                    tb[j++] =  (((c) ==  (0x80)  || (c) == NUL) ?  ('X')  :  (((unsigned)(-(c)) >> 8) & 0xff) ) ;
                 }
                 else
                 {
@@ -78511,8 +76691,8 @@
                         escaped = vim_strsave_escape_csi(tb + j);
                         if (escaped != NULL)
                         {
-                            newlen = (int)STRLEN(escaped);
-                            mch_memmove(tb + j, escaped, newlen);
+                            newlen = (int) strlen((char *)(escaped)) ;
+                             memmove((char *)(tb + j), (char *)(escaped), newlen) ;
                             j += newlen;
                             vim_free(escaped);
                         }
@@ -78532,7 +76712,7 @@
             if (s != NULL)
             {
                 (void)ins_typebuf(s, noremap, 0, TRUE, silent);
-                typebuf.tb_no_abbr_cnt += (int)STRLEN(s) + j + 1;
+                typebuf.tb_no_abbr_cnt += (int) strlen((char *)(s))  + j + 1;
             }
 
             tb[0] = Ctrl_H;
@@ -78558,7 +76738,7 @@
     char_u *s;
     char_u *d;
 
-    res = alloc(STRLEN(p) * 4 + 1);
+    res = alloc( strlen((char *)(p))  * 4 + 1);
     if (res == NULL)
     {
         return NULL;
@@ -78567,7 +76747,7 @@
     d = res;
     for (s = p; *s != NUL; )
     {
-        if ((s[0] == K_SPECIAL) && s[1] != NUL && s[2] != NUL)
+        if ((s[0] ==  (0x80) ) && s[1] != NUL && s[2] != NUL)
         {
             *d++ = *s++;
             *d++ = *s++;
@@ -78575,8 +76755,8 @@
         }
         else
         {
-            d = add_char2buf(PTR2CHAR(s), d);
-            s += MB_CPTR2LEN(s);
+            d = add_char2buf( (has_mbyte ? mb_ptr2char(s) : (int)*(s)) , d);
+            s +=  (enc_utf8 ? utf_ptr2len(s) : (*mb_ptr2len)(s)) ;
         }
     }
     *d = NUL;
@@ -78592,12 +76772,12 @@
 
     while (*s != NUL)
     {
-        if (s[0] == K_SPECIAL && s[1] == KS_SPECIAL && s[2] == KE_FILLER)
+        if (s[0] ==  (0x80)  && s[1] == KS_SPECIAL && s[2] ==  ('X') )
         {
-            *d++ = K_SPECIAL;
+            *d++ =  (0x80) ;
             s += 3;
         }
-        else if ((s[0] == K_SPECIAL || s[0] == CSI) && s[1] == KS_EXTRA && s[2] == (int)KE_CSI)
+        else if ((s[0] ==  (0x80)  || s[0] == CSI) && s[1] == KS_EXTRA && s[2] == (int)KE_CSI)
         {
             *d++ = CSI;
             s += 3;
@@ -78660,14 +76840,14 @@
 
             for ( ; mp; mp = mp->m_next)
             {
-                if (mp->m_noremap == REMAP_SCRIPT)
+                if (mp->m_noremap ==  (-2) )
                 {
                     continue;
                 }
 
                 for (p = mp->m_str; *p != NUL; ++p)
                 {
-                    if (p[0] == K_SPECIAL && p[1] == KS_EXTRA && p[2] == (int)KE_SNR)
+                    if (p[0] ==  (0x80)  && p[1] == KS_EXTRA && p[2] == (int)KE_SNR)
                     {
                         break;
                     }
@@ -78782,7 +76962,7 @@
                             {
                                 for (p = (i ? mp->m_str : mp->m_keys); *p; ++p)
                                 {
-                                    if (*p == K_SPECIAL || *p == NL)
+                                    if (*p ==  (0x80)  || *p == NL)
                                     {
                                         did_cpo = TRUE;
                                     }
@@ -78879,7 +77059,7 @@
         }
 
         c = *str;
-        if (c == K_SPECIAL && what != 2)
+        if (c ==  (0x80)  && what != 2)
         {
             modifiers = 0;
             if (str[1] == KS_MODIFIER)
@@ -78899,12 +77079,12 @@
                     --str;
                 }
             }
-            if (c == K_SPECIAL)
+            if (c ==  (0x80) )
             {
-                c = TO_SPECIAL(str[1], str[2]);
+                c =  ((str[1]) == KS_SPECIAL ?  (0x80)  : (str[1]) == KS_ZERO ?   (-((KS_ZERO) + ((int)( ('X') ) << 8)))   :  (-((str[1]) + ((int)(str[2]) << 8))) ) ;
                 str += 2;
             }
-            if (IS_SPECIAL(c) || modifiers)
+            if ( ((c) < 0)  || modifiers)
             {
                 if (fputs((char *)get_special_key_name(c, modifiers), fd) < 0)
                 {
@@ -78933,7 +77113,7 @@
             continue;
         }
 
-        if (what == 2 && (VIM_ISWHITE(c) || c == '"' || c == '\\'))
+        if (what == 2 && ( ((c) == ' ' || (c) == '\t')  || c == '"' || c == '\\'))
         {
             if (putc('\\', fd) < 0)
             {
@@ -78965,11 +77145,9 @@
     int         abbr;
     int         hash;
     buf_T       *bp;
-    ESTACK_CHECK_DECLARATION;
 
     validate_maphash();
     estack_push(ETYPE_INTERNAL, (char_u *)"mappings", 0);
-    ESTACK_CHECK_SETUP;
 
     for (bp = firstbuf; ; bp = bp->b_next)
     {
@@ -79017,7 +77195,7 @@
                         }
                         while (*p)
                         {
-                            if (*p == K_SPECIAL)
+                            if (*p ==  (0x80) )
                             {
                                 ++p;
                                 if (*p < 128)
@@ -79040,7 +77218,6 @@
             break;
         }
     }
-    ESTACK_CHECK_NOW;
     estack_pop();
 }
 
@@ -79061,7 +77238,7 @@
     void
 init_mappings(void)
 {
-    for (int i = 0; i < (int)ARRAY_LENGTH(vimrc_mappings); ++i)
+    for (int i = 0; i < (int) (sizeof(vimrc_mappings) / sizeof((vimrc_mappings)[0])) ; ++i)
     {
         add_map(vimrc_mappings[i].arg, vimrc_mappings[i].mode, FALSE);
     }
@@ -79141,7 +77318,7 @@
 
 // ==================== mark.c ====================
 
-static xfmark_T namedfm[NMARKS + EXTRA_MARKS];
+static xfmark_T namedfm[ ('z' - 'a' + 1)  + EXTRA_MARKS];
 
 static void fname2fnum(xfmark_T *fm);
 static void fmarks_check_one(xfmark_T *fm, char_u *name, buf_T *buf);
@@ -79220,17 +77397,17 @@
         return OK;
     }
 
-    if (ASCII_ISLOWER(c))
+    if ( ((unsigned)(c) - 'a' < 26) )
     {
         i = c - 'a';
         buf->b_namedm[i] = *pos;
         return OK;
     }
-    if (ASCII_ISUPPER(c) || VIM_ISDIGIT(c))
+    if ( ((unsigned)(c) - 'A' < 26)  ||  ((unsigned)(c) - '0' < 10) )
     {
-        if (VIM_ISDIGIT(c))
+        if ( ((unsigned)(c) - '0' < 10) )
         {
-            i = c - '0' + NMARKS;
+            i = c - '0' +  ('z' - 'a' + 1) ;
         }
         else
         {
@@ -79238,7 +77415,8 @@
         }
         namedfm[i].fmark.mark = *pos;
         namedfm[i].fmark.fnum = fnum;
-        VIM_CLEAR(namedfm[i].fname);
+         vim_free(namedfm[i].fname);
+         (namedfm[i].fname) = NULL;
         return OK;
     }
     return FAIL;
@@ -79259,7 +77437,7 @@
                 --wp->w_jumplistidx;
             }
             --wp->w_jumplistlen;
-            mch_memmove(&wp->w_jumplist[i], &wp->w_jumplist[i + 1], (wp->w_jumplistlen - i) * sizeof(wp->w_jumplist[i]));
+             memmove((char *)(&wp->w_jumplist[i]), (char *)(&wp->w_jumplist[i + 1]), (wp->w_jumplistlen - i) * sizeof(wp->w_jumplist[i])) ;
         }
     }
 
@@ -79273,7 +77451,7 @@
                 --wp->w_tagstackidx;
             }
             --wp->w_tagstacklen;
-            mch_memmove(&wp->w_tagstack[i], &wp->w_tagstack[i + 1], (wp->w_tagstacklen - i) * sizeof(wp->w_tagstack[i]));
+             memmove((char *)(&wp->w_tagstack[i]), (char *)(&wp->w_tagstack[i + 1]), (wp->w_tagstacklen - i) * sizeof(wp->w_tagstack[i])) ;
         }
     }
 }
@@ -79320,7 +77498,7 @@
     void
 checkpcmark(void)
 {
-    if (curwin->w_prev_pcmark.lnum != 0 && (EQUAL_POS(curwin->w_pcmark, curwin->w_cursor) || curwin->w_pcmark.lnum == 0))
+    if (curwin->w_prev_pcmark.lnum != 0 && ( (((curwin->w_pcmark).lnum == (curwin->w_cursor).lnum) && ((curwin->w_pcmark).col == (curwin->w_cursor).col) && ((curwin->w_pcmark).coladd == (curwin->w_cursor).coladd))  || curwin->w_pcmark.lnum == 0))
     {
         curwin->w_pcmark = curwin->w_prev_pcmark;
     }
@@ -79486,7 +77664,7 @@
 
         pos = curwin->w_cursor;
         listcmd_busy = TRUE;
-        if (findpar(&oa.inclusive, c == '}' ? FORWARD : BACKWARD, 1L, NUL, FALSE))
+        if (findpar(&oa.inclusive, c == '}' ? FORWARD :  (-1) , 1L, NUL, FALSE))
         {
             pos_copy = curwin->w_cursor;
             posp = &pos_copy;
@@ -79501,7 +77679,7 @@
 
         pos = curwin->w_cursor;
         listcmd_busy = TRUE;
-        if (findsent(c == ')' ? FORWARD : BACKWARD, 1L))
+        if (findsent(c == ')' ? FORWARD :  (-1) , 1L))
         {
             pos_copy = curwin->w_cursor;
             posp = &pos_copy;
@@ -79513,7 +77691,7 @@
     {
         startp = &buf->b_visual.vi_start;
         endp = &buf->b_visual.vi_end;
-        if (((c == '<') == LT_POS(*startp, *endp) || endp->lnum == 0) && startp->lnum != 0)
+        if (((c == '<') ==  (((*startp).lnum != (*endp).lnum)               ? (*startp).lnum < (*endp).lnum                   : (*startp).col != (*endp).col                        ? (*startp).col < (*endp).col                     : (*startp).coladd < (*endp).coladd)  || endp->lnum == 0) && startp->lnum != 0)
         {
             posp = startp;
         }
@@ -79536,15 +77714,15 @@
             pos_copy.coladd = 0;
         }
     }
-    else if (ASCII_ISLOWER(c))
+    else if ( ((unsigned)(c) - 'a' < 26) )
     {
         posp = &(buf->b_namedm[c - 'a']);
     }
-    else if (ASCII_ISUPPER(c) || VIM_ISDIGIT(c))
+    else if ( ((unsigned)(c) - 'A' < 26)  ||  ((unsigned)(c) - '0' < 10) )
     {
-        if (VIM_ISDIGIT(c))
+        if ( ((unsigned)(c) - '0' < 10) )
         {
-            c = c - '0' + NMARKS;
+            c = c - '0' +  ('z' - 'a' + 1) ;
         }
         else
         {
@@ -79593,7 +77771,7 @@
 
     pos = *startpos;
 
-    if (dir == BACKWARD && begin_line)
+    if (dir ==  (-1)  && begin_line)
     {
         pos.col = 0;
     }
@@ -79602,20 +77780,20 @@
         pos.col = MAXCOL;
     }
 
-    for (i = 0; i < NMARKS; i++)
+    for (i = 0; i <  ('z' - 'a' + 1) ; i++)
     {
         if (curbuf->b_namedm[i].lnum > 0)
         {
             if (dir == FORWARD)
             {
-                if ((result == NULL || LT_POS(curbuf->b_namedm[i], *result)) && LT_POS(pos, curbuf->b_namedm[i]))
+                if ((result == NULL ||  (((curbuf->b_namedm[i]).lnum != (*result).lnum)               ? (curbuf->b_namedm[i]).lnum < (*result).lnum                   : (curbuf->b_namedm[i]).col != (*result).col                        ? (curbuf->b_namedm[i]).col < (*result).col                     : (curbuf->b_namedm[i]).coladd < (*result).coladd) ) &&  (((pos).lnum != (curbuf->b_namedm[i]).lnum)               ? (pos).lnum < (curbuf->b_namedm[i]).lnum                   : (pos).col != (curbuf->b_namedm[i]).col                        ? (pos).col < (curbuf->b_namedm[i]).col                     : (pos).coladd < (curbuf->b_namedm[i]).coladd) )
                 {
                     result = &curbuf->b_namedm[i];
                 }
             }
             else
             {
-                if ((result == NULL || LT_POS(*result, curbuf->b_namedm[i])) && LT_POS(curbuf->b_namedm[i], pos))
+                if ((result == NULL ||  (((*result).lnum != (curbuf->b_namedm[i]).lnum)               ? (*result).lnum < (curbuf->b_namedm[i]).lnum                   : (*result).col != (curbuf->b_namedm[i]).col                        ? (*result).col < (curbuf->b_namedm[i]).col                     : (*result).coladd < (curbuf->b_namedm[i]).coladd) ) &&  (((curbuf->b_namedm[i]).lnum != (pos).lnum)               ? (curbuf->b_namedm[i]).lnum < (pos).lnum                   : (curbuf->b_namedm[i]).col != (pos).col                        ? (curbuf->b_namedm[i]).col < (pos).col                     : (curbuf->b_namedm[i]).coladd < (pos).coladd) )
                 {
                     result = &curbuf->b_namedm[i];
                 }
@@ -79640,15 +77818,15 @@
     {
         size_t len;
 
-        len = expand_env((char_u *)"~/", NameBuff, MAXPATHL);
-        vim_strncpy(NameBuff + len, fm->fname + 2, MAXPATHL - len - 1);
+        len = expand_env((char_u *)"~/", NameBuff,  PATH_MAX );
+        vim_strncpy(NameBuff + len, fm->fname + 2,  PATH_MAX  - len - 1);
     }
     else
     {
-        vim_strncpy(NameBuff, fm->fname, MAXPATHL - 1);
+        vim_strncpy(NameBuff, fm->fname,  PATH_MAX  - 1);
     }
 
-    mch_dirname(IObuff, IOSIZE);
+    mch_dirname(IObuff,  (1024+1) );
     p = shorten_fname(NameBuff, IObuff);
 
     (void)buflist_new(NameBuff, p, (linenr_T)1, 0);
@@ -79672,12 +77850,12 @@
         return;
     }
 
-    for (i = 0; i < NMARKS + EXTRA_MARKS; ++i)
+    for (i = 0; i <  ('z' - 'a' + 1)  + EXTRA_MARKS; ++i)
     {
         fmarks_check_one(&namedfm[i], name, buf);
     }
 
-    FOR_ALL_WINDOWS(wp)
+     for ((wp) = firstwin; (wp) != NULL; (wp) = (wp)->w_next) 
     {
         for (i = 0; i < wp->w_jumplistlen; ++i)
         {
@@ -79691,10 +77869,11 @@
     static void
 fmarks_check_one(xfmark_T *fm, char_u *name, buf_T *buf)
 {
-    if (fm->fmark.fnum == 0 && fm->fname != NULL && fnamecmp(name, fm->fname) == 0)
+    if (fm->fmark.fnum == 0 && fm->fname != NULL &&  vim_fnamecmp((char_u *)(name), (char_u *)(fm->fname))  == 0)
     {
         fm->fmark.fnum = buf->b_fnum;
-        VIM_CLEAR(fm->fname);
+         vim_free(fm->fname);
+         (fm->fname) = NULL;
     }
 }
 
@@ -79729,14 +77908,14 @@
 
     if (i == -1)
     {
-        for (i = 0; i < NMARKS + 1; i++)
+        for (i = 0; i <  ('z' - 'a' + 1)  + 1; i++)
         {
             namedfm[i].fmark.mark.lnum = 0;
             namedfm[i].fname = NULL;
         }
     }
 
-    for (i = 0; i < NMARKS; i++)
+    for (i = 0; i <  ('z' - 'a' + 1) ; i++)
     {
         buf->b_namedm[i].lnum = 0;
     }
@@ -79777,7 +77956,7 @@
         return NULL;
     }
     len = 0;
-    for (p = s; *p != NUL; MB_PTR_ADV(p))
+    for (p = s; *p != NUL;  p += (*mb_ptr2len)(p) )
     {
         len += ptr2cells(p);
         if (len >= Columns - lead_len)
@@ -79805,11 +77984,11 @@
     }
 
     show_one_mark('\'', arg, &curwin->w_pcmark, NULL, TRUE);
-    for (i = 0; i < NMARKS; ++i)
+    for (i = 0; i <  ('z' - 'a' + 1) ; ++i)
     {
         show_one_mark(i + 'a', arg, &curbuf->b_namedm[i], NULL, TRUE);
     }
-    for (i = 0; i < NMARKS + EXTRA_MARKS; ++i)
+    for (i = 0; i <  ('z' - 'a' + 1)  + EXTRA_MARKS; ++i)
     {
         if (namedfm[i].fmark.fnum != 0)
         {
@@ -79821,7 +78000,7 @@
         }
         if (name != NULL)
         {
-            show_one_mark(i >= NMARKS ? i - NMARKS + '0' : i + 'A', arg, &namedfm[i].fmark.mark, name, namedfm[i].fmark.fnum == curbuf->b_fnum);
+            show_one_mark(i >=  ('z' - 'a' + 1)  ? i -  ('z' - 'a' + 1)  + '0' : i + 'A', arg, &namedfm[i].fmark.mark, name, namedfm[i].fmark.fnum == curbuf->b_fnum);
             if (namedfm[i].fmark.fnum != 0)
             {
                 vim_free(name);
@@ -79836,7 +78015,7 @@
 
     startp = &curbuf->b_visual.vi_start;
     endp = &curbuf->b_visual.vi_end;
-    if ((LT_POS(*startp, *endp) || endp->lnum == 0) && startp->lnum != 0)
+    if (( (((*startp).lnum != (*endp).lnum)               ? (*startp).lnum < (*endp).lnum                   : (*startp).col != (*endp).col                        ? (*startp).col < (*endp).col                     : (*startp).coladd < (*endp).coladd)  || endp->lnum == 0) && startp->lnum != 0)
     {
         posp = startp;
     }
@@ -79901,7 +78080,7 @@
                 msg_outtrans(IObuff);
                 if (name != NULL)
                 {
-                    msg_outtrans_attr(name, current ? HL_ATTR(HLF_D) : 0);
+                    msg_outtrans_attr(name, current ?  highlight_attr[(int)(HLF_D)]  : 0);
                 }
             }
             out_flush();
@@ -79940,15 +78119,15 @@
     {
         for (p = eap->arg; *p != NUL; ++p)
         {
-            lower = ASCII_ISLOWER(*p);
-            digit = VIM_ISDIGIT(*p);
-            if (lower || digit || ASCII_ISUPPER(*p))
+            lower =  ((unsigned)(*p) - 'a' < 26) ;
+            digit =  ((unsigned)(*p) - '0' < 10) ;
+            if (lower || digit ||  ((unsigned)(*p) - 'A' < 26) )
             {
                 if (p[1] == '-')
                 {
                     from = *p;
                     to = p[2];
-                    if (!(lower ? ASCII_ISLOWER(p[2]) : (digit ? VIM_ISDIGIT(p[2]) : ASCII_ISUPPER(p[2]))) || to < from)
+                    if (!(lower ?  ((unsigned)(p[2]) - 'a' < 26)  : (digit ?  ((unsigned)(p[2]) - '0' < 10)  :  ((unsigned)(p[2]) - 'A' < 26) )) || to < from)
                     {
                         semsg(_(e_invalid_argument_str), p);
                         return;
@@ -79970,7 +78149,7 @@
                     {
                         if (digit)
                         {
-                            n = i - '0' + NMARKS;
+                            n = i - '0' +  ('z' - 'a' + 1) ;
                         }
                         else
                         {
@@ -79978,7 +78157,8 @@
                         }
                         namedfm[n].fmark.mark.lnum = 0;
                         namedfm[n].fmark.fnum = 0;
-                        VIM_CLEAR(namedfm[n].fname);
+                         vim_free(namedfm[n].fname);
+                         (namedfm[n].fname) = NULL;
                     }
                 }
             }
@@ -80019,7 +78199,7 @@
 }
 
     void
-ex_jumps(exarg_T *eap UNUSED)
+ex_jumps(exarg_T *eap  __attribute__((unused)) )
 {
     int         i;
     char_u      *name;
@@ -80051,7 +78231,7 @@
             }
             sprintf((char *)IObuff, "%c %2d %5ld %4d ", i == curwin->w_jumplistidx ? '>' : ' ', i > curwin->w_jumplistidx ? i - curwin->w_jumplistidx : curwin->w_jumplistidx - i, curwin->w_jumplist[i].fmark.mark.lnum, curwin->w_jumplist[i].fmark.mark.col);
             msg_outtrans(IObuff);
-            msg_outtrans_attr(name, curwin->w_jumplist[i].fmark.fnum == curbuf->b_fnum ? HL_ATTR(HLF_D) : 0);
+            msg_outtrans_attr(name, curwin->w_jumplist[i].fmark.fnum == curbuf->b_fnum ?  highlight_attr[(int)(HLF_D)]  : 0);
             vim_free(name);
             ui_breakcheck();
         }
@@ -80064,7 +78244,7 @@
 }
 
     void
-ex_clearjumps(exarg_T *eap UNUSED)
+ex_clearjumps(exarg_T *eap  __attribute__((unused)) )
 {
     free_jumplist(curwin);
     curwin->w_jumplistlen = 0;
@@ -80072,7 +78252,7 @@
 }
 
     void
-ex_changes(exarg_T *eap UNUSED)
+ex_changes(exarg_T *eap  __attribute__((unused)) )
 {
     int         i;
     char_u      *name;
@@ -80095,7 +78275,7 @@
             {
                 break;
             }
-            msg_outtrans_attr(name, HL_ATTR(HLF_D));
+            msg_outtrans_attr(name,  highlight_attr[(int)(HLF_D)] );
             vim_free(name);
             ui_breakcheck();
         }
@@ -80107,12 +78287,6 @@
     }
 }
 
-#define one_adjust(add)     {   lp = add;       if (*lp >= line1 && *lp <= line2)       {           if (amount == MAXLNUM)              *lp = 0;            else                *lp += amount;  }       else if (amount_after && *lp > line2)       *lp += amount_after;     }
-
-#define one_adjust_nodel(add)     {     lp = add;       if (*lp >= line1 && *lp <= line2)       {           if (amount == MAXLNUM)              *lp = line1;        else                *lp += amount;  }       else if (amount_after && *lp > line2)       *lp += amount_after;     }
-
-#define one_adjust_cursor(pp)     {     pos_T *posp = pp;       if (posp->lnum >= line1 && posp->lnum <= line2)         {           if (amount == MAXLNUM)          {           posp->lnum = MAX(line1 - 1, 1);                 posp->col = 0;      }       else               posp->lnum += amount;   }       else if (amount_after && posp->lnum > line2)        posp->lnum += amount_after;     }
-
     void
 mark_adjust(linenr_T    line1, linenr_T    line2, long        amount, long        amount_after)
 {
@@ -80126,7 +78300,7 @@
 }
 
     static void
-mark_adjust_internal(linenr_T    line1, linenr_T    line2, long        amount, long        amount_after, int         adjust_folds UNUSED)
+mark_adjust_internal(linenr_T    line1, linenr_T    line2, long        amount, long        amount_after, int         adjust_folds  __attribute__((unused)) )
 {
     int         i;
     int         fnum = curbuf->b_fnum;
@@ -80143,51 +78317,53 @@
 
     if ((cmdmod.cmod_flags & CMOD_LOCKMARKS) == 0)
     {
-        for (i = 0; i < NMARKS; i++)
+        for (i = 0; i <  ('z' - 'a' + 1) ; i++)
         {
-            one_adjust(&(curbuf->b_namedm[i].lnum));
+             {   lp = &(curbuf->b_namedm[i].lnum);       if (*lp >= line1 && *lp <= line2)       {           if (amount ==  LONG_MAX )              *lp = 0;            else                *lp += amount;  }       else if (amount_after && *lp > line2)       *lp += amount_after;     } ;
             if (namedfm[i].fmark.fnum == fnum)
             {
-                one_adjust_nodel(&(namedfm[i].fmark.mark.lnum));
+                 {     lp = &(namedfm[i].fmark.mark.lnum);       if (*lp >= line1 && *lp <= line2)       {           if (amount ==  LONG_MAX )              *lp = line1;        else                *lp += amount;  }       else if (amount_after && *lp > line2)       *lp += amount_after;     } ;
             }
         }
-        for (i = NMARKS; i < NMARKS + EXTRA_MARKS; i++)
+        for (i =  ('z' - 'a' + 1) ; i <  ('z' - 'a' + 1)  + EXTRA_MARKS; i++)
         {
             if (namedfm[i].fmark.fnum == fnum)
             {
-                one_adjust_nodel(&(namedfm[i].fmark.mark.lnum));
+                 {     lp = &(namedfm[i].fmark.mark.lnum);       if (*lp >= line1 && *lp <= line2)       {           if (amount ==  LONG_MAX )              *lp = line1;        else                *lp += amount;  }       else if (amount_after && *lp > line2)       *lp += amount_after;     } ;
             }
         }
 
-        one_adjust(&(curbuf->b_last_insert.lnum));
+         {   lp = &(curbuf->b_last_insert.lnum);       if (*lp >= line1 && *lp <= line2)       {           if (amount ==  LONG_MAX )              *lp = 0;            else                *lp += amount;  }       else if (amount_after && *lp > line2)       *lp += amount_after;     } ;
 
-        one_adjust(&(curbuf->b_last_change.lnum));
+         {   lp = &(curbuf->b_last_change.lnum);       if (*lp >= line1 && *lp <= line2)       {           if (amount ==  LONG_MAX )              *lp = 0;            else                *lp += amount;  }       else if (amount_after && *lp > line2)       *lp += amount_after;     } ;
 
-        if (!EQUAL_POS(curbuf->b_last_cursor, initpos))
+        if (! (((curbuf->b_last_cursor).lnum == (initpos).lnum) && ((curbuf->b_last_cursor).col == (initpos).col) && ((curbuf->b_last_cursor).coladd == (initpos).coladd)) )
         {
-            one_adjust(&(curbuf->b_last_cursor.lnum));
+             {   lp = &(curbuf->b_last_cursor.lnum);       if (*lp >= line1 && *lp <= line2)       {           if (amount ==  LONG_MAX )              *lp = 0;            else                *lp += amount;  }       else if (amount_after && *lp > line2)       *lp += amount_after;     } ;
         }
 
         for (i = 0; i < curbuf->b_changelistlen; ++i)
         {
-            one_adjust_nodel(&(curbuf->b_changelist[i].lnum));
+             {     lp = &(curbuf->b_changelist[i].lnum);       if (*lp >= line1 && *lp <= line2)       {           if (amount ==  LONG_MAX )              *lp = line1;        else                *lp += amount;  }       else if (amount_after && *lp > line2)       *lp += amount_after;     } ;
         }
 
-        one_adjust_nodel(&(curbuf->b_visual.vi_start.lnum));
-        one_adjust_nodel(&(curbuf->b_visual.vi_end.lnum));
+         {     lp = &(curbuf->b_visual.vi_start.lnum);       if (*lp >= line1 && *lp <= line2)       {           if (amount ==  LONG_MAX )              *lp = line1;        else                *lp += amount;  }       else if (amount_after && *lp > line2)       *lp += amount_after;     } ;
+         {     lp = &(curbuf->b_visual.vi_end.lnum);       if (*lp >= line1 && *lp <= line2)       {           if (amount ==  LONG_MAX )              *lp = line1;        else                *lp += amount;  }       else if (amount_after && *lp > line2)       *lp += amount_after;     } ;
 
     }
 
-    one_adjust(&(curwin->w_pcmark.lnum));
+     {   lp = &(curwin->w_pcmark.lnum);       if (*lp >= line1 && *lp <= line2)       {           if (amount ==  LONG_MAX )              *lp = 0;            else                *lp += amount;  }       else if (amount_after && *lp > line2)       *lp += amount_after;     } ;
 
-    one_adjust(&(curwin->w_prev_pcmark.lnum));
+     {   lp = &(curwin->w_prev_pcmark.lnum);       if (*lp >= line1 && *lp <= line2)       {           if (amount ==  LONG_MAX )              *lp = 0;            else                *lp += amount;  }       else if (amount_after && *lp > line2)       *lp += amount_after;     } ;
 
     if (saved_cursor.lnum != 0)
     {
-        one_adjust_nodel(&(saved_cursor.lnum));
+         {     lp = &(saved_cursor.lnum);       if (*lp >= line1 && *lp <= line2)       {           if (amount ==  LONG_MAX )              *lp = line1;        else                *lp += amount;  }       else if (amount_after && *lp > line2)       *lp += amount_after;     } ;
     }
 
-    FOR_ALL_TAB_WINDOWS(tab, win)
+     for ((tab) = first_tabpage; (tab) != NULL; (tab) = (tab)->tp_next)
+     {
+         for ((win) = ((tab) == curtab)            ? firstwin : (tab)->tp_firstwin; (win); (win) = (win)->w_next)
     {
         if ((cmdmod.cmod_flags & CMOD_LOCKMARKS) == 0)
         {
@@ -80195,7 +78371,7 @@
             {
                 if (win->w_jumplist[i].fmark.fnum == fnum)
                 {
-                    one_adjust_nodel(&(win->w_jumplist[i].fmark.mark.lnum));
+                     {     lp = &(win->w_jumplist[i].fmark.mark.lnum);       if (*lp >= line1 && *lp <= line2)       {           if (amount ==  LONG_MAX )              *lp = line1;        else                *lp += amount;  }       else if (amount_after && *lp > line2)       *lp += amount_after;     } ;
                 }
             }
         }
@@ -80208,22 +78384,22 @@
                 {
                     if (win->w_tagstack[i].fmark.fnum == fnum)
                     {
-                        one_adjust_nodel(&(win->w_tagstack[i].fmark.mark.lnum));
+                         {     lp = &(win->w_tagstack[i].fmark.mark.lnum);       if (*lp >= line1 && *lp <= line2)       {           if (amount ==  LONG_MAX )              *lp = line1;        else                *lp += amount;  }       else if (amount_after && *lp > line2)       *lp += amount_after;     } ;
                     }
                 }
             }
 
             if (win->w_old_cursor_lnum != 0)
             {
-                one_adjust_nodel(&(win->w_old_cursor_lnum));
-                one_adjust_nodel(&(win->w_old_visual_lnum));
+                 {     lp = &(win->w_old_cursor_lnum);       if (*lp >= line1 && *lp <= line2)       {           if (amount ==  LONG_MAX )              *lp = line1;        else                *lp += amount;  }       else if (amount_after && *lp > line2)       *lp += amount_after;     } ;
+                 {     lp = &(win->w_old_visual_lnum);       if (*lp >= line1 && *lp <= line2)       {           if (amount ==  LONG_MAX )              *lp = line1;        else                *lp += amount;  }       else if (amount_after && *lp > line2)       *lp += amount_after;     } ;
             }
 
             if (win != curwin)
             {
                 if (win->w_topline >= line1 && win->w_topline <= line2)
                 {
-                    if (amount == MAXLNUM)
+                    if (amount ==  LONG_MAX )
                     {
                         if (line1 <= 1)
                         {
@@ -80243,18 +78419,17 @@
                 {
                     win->w_topline += amount_after;
                 }
-                one_adjust_cursor(&(win->w_cursor));
+                 {     pos_T *posp = &(win->w_cursor);       if (posp->lnum >= line1 && posp->lnum <= line2)         {           if (amount ==  LONG_MAX )          {           posp->lnum = MAX(line1 - 1, 1);                 posp->col = 0;      }       else               posp->lnum += amount;   }       else if (amount_after && posp->lnum > line2)        posp->lnum += amount_after;     } ;
             }
 
         }
     }
+     }
 
-    FOR_ALL_BUF_WININFO(curbuf, wip)
-        one_adjust_cursor(&(wip->wi_fpos));
+     for ((wip) = (curbuf)->b_wininfo; (wip) != NULL; (wip) = (wip)->wi_next) 
+         {     pos_T *posp = &(wip->wi_fpos);       if (posp->lnum >= line1 && posp->lnum <= line2)         {           if (amount ==  LONG_MAX )          {           posp->lnum = MAX(line1 - 1, 1);                 posp->col = 0;      }       else               posp->lnum += amount;   }       else if (amount_after && posp->lnum > line2)        posp->lnum += amount_after;     } ;
 }
 
-#define col_adjust(pp)     {    posp = pp;      if (posp->lnum == lnum && posp->col >= mincol)  {           posp->lnum += lnum_amount;      if (col_amount < 0 && posp->col <= (colnr_T)-col_amount)            posp->col = 0;      else if (posp->col < spaces_removed)                posp->col = col_amount + spaces_removed;            else                posp->col += col_amount;        }     }
-
     void
 mark_col_adjust(linenr_T    lnum, colnr_T     mincol, long        lnum_amount, long        col_amount, int         spaces_removed)
 {
@@ -80268,47 +78443,47 @@
         return;
     }
 
-    for (i = 0; i < NMARKS; i++)
+    for (i = 0; i <  ('z' - 'a' + 1) ; i++)
     {
-        col_adjust(&(curbuf->b_namedm[i]));
+         {    posp = &(curbuf->b_namedm[i]);      if (posp->lnum == lnum && posp->col >= mincol)  {           posp->lnum += lnum_amount;      if (col_amount < 0 && posp->col <= (colnr_T)-col_amount)            posp->col = 0;      else if (posp->col < spaces_removed)                posp->col = col_amount + spaces_removed;            else                posp->col += col_amount;        }     } ;
         if (namedfm[i].fmark.fnum == fnum)
         {
-            col_adjust(&(namedfm[i].fmark.mark));
+             {    posp = &(namedfm[i].fmark.mark);      if (posp->lnum == lnum && posp->col >= mincol)  {           posp->lnum += lnum_amount;      if (col_amount < 0 && posp->col <= (colnr_T)-col_amount)            posp->col = 0;      else if (posp->col < spaces_removed)                posp->col = col_amount + spaces_removed;            else                posp->col += col_amount;        }     } ;
         }
     }
-    for (i = NMARKS; i < NMARKS + EXTRA_MARKS; i++)
+    for (i =  ('z' - 'a' + 1) ; i <  ('z' - 'a' + 1)  + EXTRA_MARKS; i++)
     {
         if (namedfm[i].fmark.fnum == fnum)
         {
-            col_adjust(&(namedfm[i].fmark.mark));
+             {    posp = &(namedfm[i].fmark.mark);      if (posp->lnum == lnum && posp->col >= mincol)  {           posp->lnum += lnum_amount;      if (col_amount < 0 && posp->col <= (colnr_T)-col_amount)            posp->col = 0;      else if (posp->col < spaces_removed)                posp->col = col_amount + spaces_removed;            else                posp->col += col_amount;        }     } ;
         }
     }
 
-    col_adjust(&(curbuf->b_last_insert));
+     {    posp = &(curbuf->b_last_insert);      if (posp->lnum == lnum && posp->col >= mincol)  {           posp->lnum += lnum_amount;      if (col_amount < 0 && posp->col <= (colnr_T)-col_amount)            posp->col = 0;      else if (posp->col < spaces_removed)                posp->col = col_amount + spaces_removed;            else                posp->col += col_amount;        }     } ;
 
-    col_adjust(&(curbuf->b_last_change));
+     {    posp = &(curbuf->b_last_change);      if (posp->lnum == lnum && posp->col >= mincol)  {           posp->lnum += lnum_amount;      if (col_amount < 0 && posp->col <= (colnr_T)-col_amount)            posp->col = 0;      else if (posp->col < spaces_removed)                posp->col = col_amount + spaces_removed;            else                posp->col += col_amount;        }     } ;
 
     for (i = 0; i < curbuf->b_changelistlen; ++i)
     {
-        col_adjust(&(curbuf->b_changelist[i]));
+         {    posp = &(curbuf->b_changelist[i]);      if (posp->lnum == lnum && posp->col >= mincol)  {           posp->lnum += lnum_amount;      if (col_amount < 0 && posp->col <= (colnr_T)-col_amount)            posp->col = 0;      else if (posp->col < spaces_removed)                posp->col = col_amount + spaces_removed;            else                posp->col += col_amount;        }     } ;
     }
 
-    col_adjust(&(curbuf->b_visual.vi_start));
-    col_adjust(&(curbuf->b_visual.vi_end));
+     {    posp = &(curbuf->b_visual.vi_start);      if (posp->lnum == lnum && posp->col >= mincol)  {           posp->lnum += lnum_amount;      if (col_amount < 0 && posp->col <= (colnr_T)-col_amount)            posp->col = 0;      else if (posp->col < spaces_removed)                posp->col = col_amount + spaces_removed;            else                posp->col += col_amount;        }     } ;
+     {    posp = &(curbuf->b_visual.vi_end);      if (posp->lnum == lnum && posp->col >= mincol)  {           posp->lnum += lnum_amount;      if (col_amount < 0 && posp->col <= (colnr_T)-col_amount)            posp->col = 0;      else if (posp->col < spaces_removed)                posp->col = col_amount + spaces_removed;            else                posp->col += col_amount;        }     } ;
 
-    col_adjust(&(curwin->w_pcmark));
+     {    posp = &(curwin->w_pcmark);      if (posp->lnum == lnum && posp->col >= mincol)  {           posp->lnum += lnum_amount;      if (col_amount < 0 && posp->col <= (colnr_T)-col_amount)            posp->col = 0;      else if (posp->col < spaces_removed)                posp->col = col_amount + spaces_removed;            else                posp->col += col_amount;        }     } ;
 
-    col_adjust(&(curwin->w_prev_pcmark));
+     {    posp = &(curwin->w_prev_pcmark);      if (posp->lnum == lnum && posp->col >= mincol)  {           posp->lnum += lnum_amount;      if (col_amount < 0 && posp->col <= (colnr_T)-col_amount)            posp->col = 0;      else if (posp->col < spaces_removed)                posp->col = col_amount + spaces_removed;            else                posp->col += col_amount;        }     } ;
 
-    col_adjust(&saved_cursor);
+     {    posp = &saved_cursor;      if (posp->lnum == lnum && posp->col >= mincol)  {           posp->lnum += lnum_amount;      if (col_amount < 0 && posp->col <= (colnr_T)-col_amount)            posp->col = 0;      else if (posp->col < spaces_removed)                posp->col = col_amount + spaces_removed;            else                posp->col += col_amount;        }     } ;
 
-    FOR_ALL_WINDOWS(win)
+     for ((win) = firstwin; (win) != NULL; (win) = (win)->w_next) 
     {
         for (i = 0; i < win->w_jumplistlen; ++i)
         {
             if (win->w_jumplist[i].fmark.fnum == fnum)
             {
-                col_adjust(&(win->w_jumplist[i].fmark.mark));
+                 {    posp = &(win->w_jumplist[i].fmark.mark);      if (posp->lnum == lnum && posp->col >= mincol)  {           posp->lnum += lnum_amount;      if (col_amount < 0 && posp->col <= (colnr_T)-col_amount)            posp->col = 0;      else if (posp->col < spaces_removed)                posp->col = col_amount + spaces_removed;            else                posp->col += col_amount;        }     } ;
             }
         }
 
@@ -80318,13 +78493,13 @@
             {
                 if (win->w_tagstack[i].fmark.fnum == fnum)
                 {
-                    col_adjust(&(win->w_tagstack[i].fmark.mark));
+                     {    posp = &(win->w_tagstack[i].fmark.mark);      if (posp->lnum == lnum && posp->col >= mincol)  {           posp->lnum += lnum_amount;      if (col_amount < 0 && posp->col <= (colnr_T)-col_amount)            posp->col = 0;      else if (posp->col < spaces_removed)                posp->col = col_amount + spaces_removed;            else                posp->col += col_amount;        }     } ;
                 }
             }
 
             if (win != curwin)
             {
-                col_adjust(&win->w_cursor);
+                 {    posp = &win->w_cursor;      if (posp->lnum == lnum && posp->col >= mincol)  {           posp->lnum += lnum_amount;      if (col_amount < 0 && posp->col <= (colnr_T)-col_amount)            posp->col = 0;      else if (posp->col < spaces_removed)                posp->col = col_amount + spaces_removed;            else                posp->col += col_amount;        }     } ;
             }
         }
     }
@@ -80431,10 +78606,10 @@
 
 // ==================== match.c ====================
 
-# define SEARCH_HL_PRIORITY 0
+enum { SEARCH_HL_PRIORITY = 0 };
 
     static int
-match_add(win_T       *wp, char_u      *grp, char_u      *pat, int         prio, int         id, list_T      *pos_list, char_u      *conceal_char UNUSED)
+match_add(win_T       *wp, char_u      *grp, char_u      *pat, int         prio, int         id, list_T      *pos_list, char_u      *conceal_char  __attribute__((unused)) )
 {
     matchitem_T *cur;
     matchitem_T *prev;
@@ -80473,7 +78648,7 @@
         }
     }
 
-    if ((hlg_id = syn_namen2id(grp, (int)STRLEN(grp))) == 0)
+    if ((hlg_id = syn_namen2id(grp, (int) strlen((char *)(grp)) )) == 0)
     {
         semsg(_(e_no_such_highlight_group_name_str), grp);
         return -1;
@@ -80484,7 +78659,7 @@
         return -1;
     }
 
-    m = ALLOC_CLEAR_ONE(matchitem_T);
+    m =  (matchitem_T *)alloc_clear(sizeof(matchitem_T)) ;
     if (m == NULL)
     {
         goto fail;
@@ -80594,7 +78769,6 @@
     redraw_win_later(wp, UPD_SOME_VALID);
 }
 
-
     void
 init_search_hl(win_T *wp, match_T *search_hl)
 {
@@ -80617,7 +78791,7 @@
         cur->mit_hl.first_lnum = 0;
         cur = cur->mit_next;
     }
-    search_hl->attr = HL_ATTR(HLF_L);
+    search_hl->attr =  highlight_attr[(int)(HLF_L)] ;
 
     search_hl->buf = wp->w_buffer;
     search_hl->lnum = 0;
@@ -80796,7 +78970,7 @@
     int         n;
 
     cur = wp->w_match_head;
-    shl_flag = WIN_IS_POPUP(wp);
+    shl_flag =  0 ;
     while (cur != NULL || shl_flag == FALSE)
     {
         if (shl_flag == FALSE)
@@ -80870,7 +79044,7 @@
     int         area_highlighting = FALSE;
 
     cur = wp->w_match_head;
-    shl_flag = WIN_IS_POPUP(wp);
+    shl_flag =  0 ;
     while (cur != NULL || shl_flag == FALSE)
     {
         if (shl_flag == FALSE)
@@ -80946,7 +79120,7 @@
 }
 
     int
-update_search_hl(win_T       *wp, linenr_T    lnum, colnr_T     col, char_u      **line, match_T     *search_hl, int         *has_match_conc UNUSED, int         *match_conc UNUSED, int         did_line_attr, int         lcs_eol_one, int         *on_last_col)
+update_search_hl(win_T       *wp, linenr_T    lnum, colnr_T     col, char_u      **line, match_T     *search_hl, int         *has_match_conc  __attribute__((unused)) , int         *match_conc  __attribute__((unused)) , int         did_line_attr, int         lcs_eol_one, int         *on_last_col)
 {
     matchitem_T *cur;
     match_T     *shl;
@@ -80955,7 +79129,7 @@
     int         search_attr = 0;
 
     cur = wp->w_match_head;
-    shl_flag = WIN_IS_POPUP(wp);
+    shl_flag =  0 ;
     while (cur != NULL || shl_flag == FALSE)
     {
         if (shl_flag == FALSE && (cur == NULL || cur->mit_priority > SEARCH_HL_PRIORITY))
@@ -80985,7 +79159,7 @@
                 shl->attr_cur = shl->attr;
                 if (shl == search_hl && shl->has_cursor)
                 {
-                    shl->attr_cur = HL_ATTR(HLF_LC);
+                    shl->attr_cur =  highlight_attr[(int)(HLF_LC)] ;
                     if (shl->attr_cur != shl->attr)
                     {
                         search_hl_has_cursor_lnum = lnum;
@@ -81051,7 +79225,7 @@
     }
 
     cur = wp->w_match_head;
-    shl_flag = WIN_IS_POPUP(wp);
+    shl_flag =  0 ;
     while (cur != NULL || shl_flag == FALSE)
     {
         if (shl_flag == FALSE && (cur == NULL || cur->mit_priority > SEARCH_HL_PRIORITY))
@@ -81073,7 +79247,7 @@
             cur = cur->mit_next;
         }
     }
-    if (*(*line + col) == NUL && (did_line_attr >= 1 || (wp->w_p_list && lcs_eol_one == -1)))
+    if (*(*line + col) == NUL && (did_line_attr >= 1 || (wp-> w_onebuf_opt.wo_list  && lcs_eol_one == -1)))
     {
         search_attr = 0;
     }
@@ -81087,7 +79261,7 @@
     int         prevcol_hl_flag = FALSE;
     matchitem_T *cur;
 
-    if ((long)(wp->w_p_wrap ? wp->w_skipcol : wp->w_leftcol) > prevcol)
+    if ((long)(wp-> w_onebuf_opt.wo_wrap  ? wp->w_skipcol : wp->w_leftcol) > prevcol)
     {
         ++prevcol;
     }
@@ -81120,7 +79294,7 @@
     int         shl_flag;
 
     cur = wp->w_match_head;
-    shl_flag = WIN_IS_POPUP(wp);
+    shl_flag =  0 ;
     while (cur != NULL || shl_flag == FALSE)
     {
         if (shl_flag == FALSE && ((cur != NULL && cur->mit_priority > SEARCH_HL_PRIORITY) || cur == NULL))
@@ -81171,7 +79345,7 @@
     {
         end = eap->arg;
     }
-    else if ((STRNICMP(eap->arg, "none", 4) == 0 && (VIM_ISWHITE(eap->arg[4]) || ends_excmd2(eap->arg, eap->arg + 4))))
+    else if (( strncasecmp((char *)(eap->arg), (char *)("none"), (4))  == 0 && ( ((eap->arg[4]) == ' ' || (eap->arg[4]) == '\t')  || ends_excmd2(eap->arg, eap->arg + 4))))
     {
         end = eap->arg + 4;
     }
@@ -81217,8 +79391,6 @@
 
 // ==================== mbyte.c ====================
 
-# define WINBYTE BYTE
-
 static int dbcs_char2len(int c);
 static int dbcs_char2bytes(int c, char_u *buf);
 static int dbcs_ptr2len(char_u *p);
@@ -81260,132 +79432,70 @@
 {   char *name;         int prop;               int codepage;}
 enc_canon_table[] =
 {
-#define IDX_LATIN_1     0
     {"latin1",          ENC_8BIT + ENC_LATIN1,  1252},
-#define IDX_ISO_2       1
     {"iso-8859-2",      ENC_8BIT,               0},
-#define IDX_ISO_3       2
     {"iso-8859-3",      ENC_8BIT,               0},
-#define IDX_ISO_4       3
     {"iso-8859-4",      ENC_8BIT,               0},
-#define IDX_ISO_5       4
     {"iso-8859-5",      ENC_8BIT,               0},
-#define IDX_ISO_6       5
     {"iso-8859-6",      ENC_8BIT,               0},
-#define IDX_ISO_7       6
     {"iso-8859-7",      ENC_8BIT,               0},
-#define IDX_ISO_8       7
     {"iso-8859-8",      ENC_8BIT,               0},
-#define IDX_ISO_9       8
     {"iso-8859-9",      ENC_8BIT,               0},
-#define IDX_ISO_10      9
     {"iso-8859-10",     ENC_8BIT,               0},
-#define IDX_ISO_11      10
     {"iso-8859-11",     ENC_8BIT,               0},
-#define IDX_ISO_13      11
     {"iso-8859-13",     ENC_8BIT,               0},
-#define IDX_ISO_14      12
     {"iso-8859-14",     ENC_8BIT,               0},
-#define IDX_ISO_15      13
     {"iso-8859-15",     ENC_8BIT + ENC_LATIN9,  0},
-#define IDX_KOI8_R      14
     {"koi8-r",          ENC_8BIT,               0},
-#define IDX_KOI8_U      15
     {"koi8-u",          ENC_8BIT,               0},
-#define IDX_UTF8        16
     {"utf-8",           ENC_UNICODE,            0},
-#define IDX_UCS2        17
     {"ucs-2",           ENC_UNICODE + ENC_ENDIAN_B + ENC_2BYTE, 0},
-#define IDX_UCS2LE      18
     {"ucs-2le",         ENC_UNICODE + ENC_ENDIAN_L + ENC_2BYTE, 0},
-#define IDX_UTF16       19
     {"utf-16",          ENC_UNICODE + ENC_ENDIAN_B + ENC_2WORD, 0},
-#define IDX_UTF16BE     20
     {"utf-16be",                ENC_UNICODE + ENC_ENDIAN_B + ENC_2WORD, 0},
-#define IDX_UTF16LE     21
     {"utf-16le",        ENC_UNICODE + ENC_ENDIAN_L + ENC_2WORD, 0},
-#define IDX_UCS4        22
     {"ucs-4",           ENC_UNICODE + ENC_ENDIAN_B + ENC_4BYTE, 0},
-#define IDX_UCS4LE      23
     {"ucs-4le",         ENC_UNICODE + ENC_ENDIAN_L + ENC_4BYTE, 0},
 
-#define IDX_DEBUG       24
-    {"debug",           ENC_DBCS,               DBCS_DEBUG},
-#define IDX_EUC_JP      25
+    {"debug",           ENC_DBCS,                (-1) },
     {"euc-jp",          ENC_DBCS,               DBCS_JPNU},
-#define IDX_SJIS        26
     {"sjis",            ENC_DBCS,               DBCS_JPN},
-#define IDX_EUC_KR      27
     {"euc-kr",          ENC_DBCS,               DBCS_KORU},
-#define IDX_EUC_CN      28
     {"euc-cn",          ENC_DBCS,               DBCS_CHSU},
-#define IDX_EUC_TW      29
     {"euc-tw",          ENC_DBCS,               DBCS_CHTU},
-#define IDX_BIG5        30
     {"big5",            ENC_DBCS,               DBCS_CHT},
 
-#define IDX_CP437       31
     {"cp437",           ENC_8BIT,               437},
-#define IDX_CP737       32
     {"cp737",           ENC_8BIT,               737},
-#define IDX_CP775       33
     {"cp775",           ENC_8BIT,               775},
-#define IDX_CP850       34
     {"cp850",           ENC_8BIT,               850},
-#define IDX_CP852       35
     {"cp852",           ENC_8BIT,               852},
-#define IDX_CP855       36
     {"cp855",           ENC_8BIT,               855},
-#define IDX_CP857       37
     {"cp857",           ENC_8BIT,               857},
-#define IDX_CP860       38
     {"cp860",           ENC_8BIT,               860},
-#define IDX_CP861       39
     {"cp861",           ENC_8BIT,               861},
-#define IDX_CP862       40
     {"cp862",           ENC_8BIT,               862},
-#define IDX_CP863       41
     {"cp863",           ENC_8BIT,               863},
-#define IDX_CP865       42
     {"cp865",           ENC_8BIT,               865},
-#define IDX_CP866       43
     {"cp866",           ENC_8BIT,               866},
-#define IDX_CP869       44
     {"cp869",           ENC_8BIT,               869},
-#define IDX_CP874       45
     {"cp874",           ENC_8BIT,               874},
-#define IDX_CP932       46
     {"cp932",           ENC_DBCS,               DBCS_JPN},
-#define IDX_CP936       47
     {"cp936",           ENC_DBCS,               DBCS_CHS},
-#define IDX_CP949       48
     {"cp949",           ENC_DBCS,               DBCS_KOR},
-#define IDX_CP950       49
     {"cp950",           ENC_DBCS,               DBCS_CHT},
-#define IDX_CP1250      50
     {"cp1250",          ENC_8BIT,               1250},
-#define IDX_CP1251      51
     {"cp1251",          ENC_8BIT,               1251},
-#define IDX_CP1253      52
     {"cp1253",          ENC_8BIT,               1253},
-#define IDX_CP1254      53
     {"cp1254",          ENC_8BIT,               1254},
-#define IDX_CP1255      54
     {"cp1255",          ENC_8BIT,               1255},
-#define IDX_CP1256      55
     {"cp1256",          ENC_8BIT,               1256},
-#define IDX_CP1257      56
     {"cp1257",          ENC_8BIT,               1257},
-#define IDX_CP1258      57
     {"cp1258",          ENC_8BIT,               1258},
 
-#define IDX_MACROMAN    58
     {"macroman",        ENC_8BIT + ENC_MACROMAN, 0},
-#define IDX_DECMCS      59
     {"dec-mcs",         ENC_8BIT,               0},
-#define IDX_HPROMAN8    60
     {"hp-roman8",       ENC_8BIT,               0},
-#define IDX_COUNT       61
 };
 
 static struct
@@ -81457,8 +79567,6 @@
     {NULL,              0}
 };
 
-# define CP_UTF8 65001
-
     static int
 enc_canon_search(char_u *name)
 {
@@ -81466,7 +79574,7 @@
 
     for (i = 0; i < IDX_COUNT; ++i)
     {
-        if (STRCMP(name, enc_canon_table[i].name) == 0)
+        if ( strcmp((char *)(name), (char *)(enc_canon_table[i].name))  == 0)
         {
             return i;
         }
@@ -81484,11 +79592,11 @@
     {
         return enc_canon_table[i].prop;
     }
-    if (STRNCMP(name, "2byte-", 6) == 0)
+    if ( strncmp((char *)(name), (char *)("2byte-"), (6))  == 0)
     {
         return ENC_DBCS;
     }
-    if (STRNCMP(name, "8bit-", 5) == 0 || STRNCMP(name, "iso-8859-", 9) == 0)
+    if ( strncmp((char *)(name), (char *)("8bit-"), (5))  == 0 ||  strncmp((char *)(name), (char *)("iso-8859-"), (9))  == 0)
     {
         return ENC_8BIT;
     }
@@ -81502,7 +79610,6 @@
     int         idx;
     int         n;
     int         enc_dbcs_new = 0;
-# define LEN_FROM_CONV
     vimconv_T   vimconv;
     char_u      *p;
 
@@ -81518,12 +79625,12 @@
         return NULL;
     }
 
-    else if (STRNCMP(p_enc, "8bit-", 5) == 0 || STRNCMP(p_enc, "iso-8859-", 9) == 0)
+    else if ( strncmp((char *)(p_enc), (char *)("8bit-"), (5))  == 0 ||  strncmp((char *)(p_enc), (char *)("iso-8859-"), (9))  == 0)
     {
         enc_unicode = 0;
         enc_utf8 = FALSE;
     }
-    else if (STRNCMP(p_enc, "2byte-", 6) == 0)
+    else if ( strncmp((char *)(p_enc), (char *)("2byte-"), (6))  == 0)
     {
         enc_dbcs_new = DBCS_2BYTE;
     }
@@ -81569,7 +79676,7 @@
     enc_dbcs = enc_dbcs_new;
     has_mbyte = (enc_dbcs != 0 || enc_utf8);
 
-    enc_latin1like = (enc_utf8 || STRCMP(p_enc, "latin1") == 0 || STRCMP(p_enc, "iso-8859-15") == 0);
+    enc_latin1like = (enc_utf8 ||  strcmp((char *)(p_enc), (char *)("latin1"))  == 0 ||  strcmp((char *)(p_enc), (char *)("iso-8859-15"))  == 0);
 
     if (enc_utf8)
     {
@@ -81615,7 +79722,7 @@
     if (enc_dbcs)
     {
         p = enc_locale();
-        if (p == NULL || STRCMP(p, p_enc) != 0)
+        if (p == NULL ||  strcmp((char *)(p), (char *)(p_enc))  != 0)
         {
             convert_setup(&vimconv, p_enc, (char_u *)"utf-8");
             vimconv.vc_fail = TRUE;
@@ -81713,15 +79820,15 @@
                 }
             }
         }
-        else if (STRCMP(curbuf->b_p_fenc, "utf-8") == 0)
+        else if ( strcmp((char *)(curbuf->b_p_fenc), (char *)("utf-8"))  == 0)
         {
             n = 3;
         }
-        else if (STRNCMP(curbuf->b_p_fenc, "ucs-2", 5) == 0 || STRNCMP(curbuf->b_p_fenc, "utf-16", 6) == 0)
+        else if ( strncmp((char *)(curbuf->b_p_fenc), (char *)("ucs-2"), (5))  == 0 ||  strncmp((char *)(curbuf->b_p_fenc), (char *)("utf-16"), (6))  == 0)
         {
             n = 2;
         }
-        else if (STRNCMP(curbuf->b_p_fenc, "ucs-4", 5) == 0)
+        else if ( strncmp((char *)(curbuf->b_p_fenc), (char *)("ucs-4"), (5))  == 0)
         {
             n = 4;
         }
@@ -81738,9 +79845,9 @@
     int
 mb_get_class_buf(char_u *p, buf_T *buf)
 {
-    if (MB_BYTE2LEN(p[0]) == 1)
+    if ( mb_bytelen_tab[p[0]]  == 1)
     {
-        if (p[0] == NUL || VIM_ISWHITE(p[0]))
+        if (p[0] == NUL ||  ((p[0]) == ' ' || (p[0]) == '\t') )
         {
             return 0;
         }
@@ -81766,7 +79873,6 @@
 {
     switch (enc_dbcs)
     {
-
         case DBCS_JPNU:
         case DBCS_JPN:
             {
@@ -81870,7 +79976,7 @@
 }
 
     int
-latin_char2len(int c UNUSED)
+latin_char2len(int c  __attribute__((unused)) )
 {
     return 1;
 }
@@ -81925,7 +80031,7 @@
         return 0;
     }
 
-    len = MB_BYTE2LEN(*p);
+    len =  mb_bytelen_tab[*p] ;
     if (len == 2 && p[1] == NUL)
     {
         len = 1;
@@ -81956,7 +80062,7 @@
     {
         return 1;
     }
-    len = MB_BYTE2LEN(*p);
+    len =  mb_bytelen_tab[*p] ;
     if (len == 2 && p[1] == NUL)
     {
         len = 1;
@@ -82404,7 +80510,7 @@
 }
 
     int
-latin_ptr2cells(char_u *p UNUSED)
+latin_ptr2cells(char_u *p  __attribute__((unused)) )
 {
     return 1;
 }
@@ -82438,11 +80544,11 @@
     {
         return 1;
     }
-    return MB_BYTE2LEN(*p);
+    return  mb_bytelen_tab[*p] ;
 }
 
     int
-latin_ptr2cells_len(char_u *p UNUSED, int size UNUSED)
+latin_ptr2cells_len(char_u *p  __attribute__((unused)) , int size  __attribute__((unused)) )
 {
     return 1;
 }
@@ -82480,11 +80586,11 @@
     {
         return 1;
     }
-    return MB_BYTE2LEN(*p);
+    return  mb_bytelen_tab[*p] ;
 }
 
     int
-latin_char2cells(int c UNUSED)
+latin_char2cells(int c  __attribute__((unused)) )
 {
     return 1;
 }
@@ -82496,7 +80602,7 @@
     {
         return 1;
     }
-    return MB_BYTE2LEN((unsigned)c >> 8);
+    return  mb_bytelen_tab[(unsigned)c >> 8] ;
 }
 
     int
@@ -82513,7 +80619,7 @@
 }
 
     int
-latin_off2cells(unsigned off UNUSED, unsigned max_off UNUSED)
+latin_off2cells(unsigned off  __attribute__((unused)) , unsigned max_off  __attribute__((unused)) )
 {
     return 1;
 }
@@ -82530,7 +80636,7 @@
     {
         return 1;
     }
-    return MB_BYTE2LEN(ScreenLines[off]);
+    return  mb_bytelen_tab[ScreenLines[off]] ;
 }
 
     int
@@ -82548,7 +80654,7 @@
     static int
 dbcs_ptr2char(char_u *p)
 {
-    if (MB_BYTE2LEN(*p) > 1 && p[1] != NUL)
+    if ( mb_bytelen_tab[*p]  > 1 && p[1] != NUL)
     {
         return (p[0] << 8) + p[1];
     }
@@ -82936,7 +81042,6 @@
     return utf8len_tab[b];
 }
 
-
     int
 utf_ptr2len_len(char_u *p, int size)
 {
@@ -83742,7 +81847,7 @@
     };
 
     int bot = 0;
-    int top = ARRAY_LENGTH(classes) - 1;
+    int top =  (sizeof(classes) / sizeof((classes)[0]))  - 1;
     int mid;
 
     if (c < 0x100)
@@ -84443,7 +82548,7 @@
 {
     if (a < 128 && (cmp_flags & CMP_KEEPASCII))
     {
-        return TOUPPER_ASC(a);
+        return  (((a) < 'a' || (a) > 'z') ? (a) : (a) - ('a' - 'A')) ;
     }
 
     if (!(cmp_flags & CMP_INTERNAL))
@@ -84453,7 +82558,7 @@
 
     if (a < 128)
     {
-        return TOUPPER_LOC(a);
+        return   (toupper ((unsigned char)(a))) ;
     }
 
     return utf_convert(a, toUpper, (int)sizeof(toUpper));
@@ -84470,7 +82575,7 @@
 {
     if (a < 128 && (cmp_flags & CMP_KEEPASCII))
     {
-        return TOLOWER_ASC(a);
+        return  (((a) < 'A' || (a) > 'Z') ? (a) : (a) + ('a' - 'A')) ;
     }
 
     if (!(cmp_flags & CMP_INTERNAL))
@@ -84480,7 +82585,7 @@
 
     if (a < 128)
     {
-        return TOLOWER_LOC(a);
+        return   (tolower ((unsigned char)(a))) ;
     }
 
     return utf_convert(a, toLower, (int)sizeof(toLower));
@@ -84611,7 +82716,7 @@
             {
                 if (s1[i] != s2[i])
                 {
-                    cdiff = MB_TOLOWER(s1[i]) - MB_TOLOWER(s2[i]);
+                    cdiff =  vim_tolower(s1[i])  -  vim_tolower(s2[i]) ;
                     if (cdiff != 0)
                     {
                         return cdiff;
@@ -84624,7 +82729,7 @@
                 {
                     l = n - i;
                 }
-                cdiff = STRNCMP(s1 + i, s2 + i, l);
+                cdiff =  strncmp((char *)(s1 + i), (char *)(s2 + i), (l)) ;
                 if (cdiff != 0)
                 {
                     return cdiff;
@@ -84659,15 +82764,15 @@
         {
             if (i > 0)
             {
-                STRCPY(IObuff + rlen, "+ ");
+                 strcpy((char *)(IObuff + rlen), (char *)("+ ")) ;
                 rlen += 2;
             }
             clen = utf_ptr2len(line + i);
         }
         sprintf((char *)IObuff + rlen, "%02x ", (line[i] == NL) ? NUL : line[i]);
         --clen;
-        rlen += (int)STRLEN(IObuff + rlen);
-        if (rlen > IOSIZE - 20)
+        rlen += (int) strlen((char *)(IObuff + rlen)) ;
+        if (rlen >  (1024+1)  - 20)
         {
             break;
         }
@@ -84677,7 +82782,7 @@
 }
 
     int
-latin_head_off(char_u *base UNUSED, char_u *p UNUSED)
+latin_head_off(char_u *base  __attribute__((unused)) , char_u *p  __attribute__((unused)) )
 {
     return 0;
 }
@@ -84687,7 +82792,7 @@
 {
     char_u      *q;
 
-    if (p <= base || MB_BYTE2LEN(p[-1]) == 1 || *p == NUL)
+    if (p <= base ||  mb_bytelen_tab[p[-1]]  == 1 || *p == NUL)
     {
         return 0;
     }
@@ -84705,7 +82810,7 @@
 {
     char_u      *q;
 
-    if (p <= base || (enc_dbcs == DBCS_JPNU && p[-1] == 0x8e) || MB_BYTE2LEN(p[-1]) == 1 || *p == NUL)
+    if (p <= base || (enc_dbcs == DBCS_JPNU && p[-1] == 0x8e) ||  mb_bytelen_tab[p[-1]]  == 1 || *p == NUL)
     {
         return 0;
     }
@@ -84828,7 +82933,7 @@
     };
 
     int first = 0;
-    int last  = ARRAY_LENGTH(BOL_prohibition_punct) - 1;
+    int last  =  (sizeof(BOL_prohibition_punct) / sizeof((BOL_prohibition_punct)[0]))  - 1;
     int mid   = 0;
 
     while (first < last)
@@ -84879,7 +82984,7 @@
     };
 
     int first = 0;
-    int last  = ARRAY_LENGTH(EOL_prohibition_punct) - 1;
+    int last  =  (sizeof(EOL_prohibition_punct) / sizeof((EOL_prohibition_punct)[0]))  - 1;
     int mid   = 0;
 
     while (first < last)
@@ -84919,7 +83024,7 @@
 {
     int     l = (*mb_ptr2len)(*fp);
 
-    mch_memmove(*tp, *fp, (size_t)l);
+     memmove((char *)(*tp), (char *)(*fp), (size_t)l) ;
     *tp += l;
     *fp += l;
 }
@@ -84968,7 +83073,7 @@
         return i;
     }
 
-    if (enc_dbcs == 0 || p[1] == NUL || MB_BYTE2LEN(*p) == 1)
+    if (enc_dbcs == 0 || p[1] == NUL ||  mb_bytelen_tab[*p]  == 1)
     {
         return 0;
     }
@@ -85081,7 +83186,7 @@
 {
     if (p > line)
     {
-        MB_PTR_BACK(line, p);
+         p -= has_mbyte ? ((*mb_head_off)(line, (p) - 1) + 1) : 1 ;
     }
     return p;
 }
@@ -85105,7 +83210,6 @@
     return count;
 }
 
-
     char_u *
 mb_unescape(char_u **pp)
 {
@@ -85116,17 +83220,17 @@
 
     for (n = 0; str[n] != NUL && m < 4; ++n)
     {
-        if (str[n] == K_SPECIAL && str[n + 1] == KS_SPECIAL && str[n + 2] == KE_FILLER)
+        if (str[n] ==  (0x80)  && str[n + 1] == KS_SPECIAL && str[n + 2] ==  ('X') )
         {
-            buf[m++] = K_SPECIAL;
+            buf[m++] =  (0x80) ;
             n += 2;
         }
-        else if ((str[n] == K_SPECIAL) && str[n + 1] == KS_EXTRA && str[n + 2] == (int)KE_CSI)
+        else if ((str[n] ==  (0x80) ) && str[n + 1] == KS_EXTRA && str[n + 2] == (int)KE_CSI)
         {
             buf[m++] = CSI;
             n += 2;
         }
-        else if (str[n] == K_SPECIAL)
+        else if (str[n] ==  (0x80) )
         {
             break;
         }
@@ -85176,11 +83280,11 @@
     char_u *
 enc_skip(char_u *p)
 {
-    if (STRNCMP(p, "2byte-", 6) == 0)
+    if ( strncmp((char *)(p), (char *)("2byte-"), (6))  == 0)
     {
         return p + 6;
     }
-    if (STRNCMP(p, "8bit-", 5) == 0)
+    if ( strncmp((char *)(p), (char *)("8bit-"), (5))  == 0)
     {
         return p + 5;
     }
@@ -85195,17 +83299,17 @@
     char_u *s;
     int         i;
 
-    if (STRCMP(enc, "default") == 0)
+    if ( strcmp((char *)(enc), (char *)("default"))  == 0)
     {
         r = get_encoding_default();
         if (r == NULL)
         {
-            r = (char_u *)ENC_DFLT;
+            r = (char_u *) "latin1" ;
         }
         return vim_strsave(r);
     }
 
-    r = alloc(STRLEN(enc) + 3);
+    r = alloc( strlen((char *)(enc))  + 3);
     if (r == NULL)
     {
         return NULL;
@@ -85220,40 +83324,40 @@
         }
         else
         {
-            *p++ = TOLOWER_ASC(*s);
+            *p++ =  (((*s) < 'A' || (*s) > 'Z') ? (*s) : (*s) + ('a' - 'A')) ;
         }
     }
     *p = NUL;
 
     p = enc_skip(r);
 
-    if (STRNCMP(p, "microsoft-cp", 12) == 0)
+    if ( strncmp((char *)(p), (char *)("microsoft-cp"), (12))  == 0)
     {
-        STRMOVE(p, p + 10);
+          memmove((char *)((p)), (char *)((p + 10)),  strlen((char *)(p + 10))  + 1)  ;
     }
 
-    if (STRNCMP(p, "iso8859", 7) == 0)
+    if ( strncmp((char *)(p), (char *)("iso8859"), (7))  == 0)
     {
-        STRMOVE(p + 4, p + 3);
+          memmove((char *)((p + 4)), (char *)((p + 3)),  strlen((char *)(p + 3))  + 1)  ;
         p[3] = '-';
     }
 
-    if (STRNCMP(p, "iso-8859", 8) == 0 && SAFE_isdigit(p[8]))
+    if ( strncmp((char *)(p), (char *)("iso-8859"), (8))  == 0 &&  (isdigit ((unsigned char)(p[8]))) )
     {
-        STRMOVE(p + 9, p + 8);
+          memmove((char *)((p + 9)), (char *)((p + 8)),  strlen((char *)(p + 8))  + 1)  ;
         p[8] = '-';
     }
 
-    if (STRNCMP(p, "latin-", 6) == 0)
+    if ( strncmp((char *)(p), (char *)("latin-"), (6))  == 0)
     {
-        STRMOVE(p + 5, p + 6);
+          memmove((char *)((p + 5)), (char *)((p + 6)),  strlen((char *)(p + 6))  + 1)  ;
     }
 
     if (enc_canon_search(p) >= 0)
     {
         if (p != r)
         {
-            STRMOVE(r, p);
+              memmove((char *)((r)), (char *)((p)),  strlen((char *)(p))  + 1)  ;
         }
     }
     else if ((i = enc_alias_search(p)) >= 0)
@@ -85271,7 +83375,7 @@
 
     for (i = 0; enc_alias_table[i].name != NULL; ++i)
     {
-        if (STRCMP(name, enc_alias_table[i].name) == 0)
+        if ( strcmp((char *)(name), (char *)(enc_alias_table[i].name))  == 0)
         {
             return enc_alias_table[i].canon;
         }
@@ -85305,9 +83409,9 @@
 
     if ((p = (char *)vim_strchr((char_u *)s, '.')) != NULL)
     {
-        if (p > s + 2 && STRNICMP(p + 1, "EUC", 3) == 0 && !SAFE_isalnum((int)p[4]) && p[4] != '-' && p[-3] == '_')
+        if (p > s + 2 &&  strncasecmp((char *)(p + 1), (char *)("EUC"), (3))  == 0 && ! (isalnum ((unsigned char)((int)p[4])))  && p[4] != '-' && p[-3] == '_')
         {
-            STRCPY(buf + 10, "euc-");
+             strcpy((char *)(buf + 10), (char *)("euc-")) ;
             buf[14] = p[-2];
             buf[15] = p[-1];
             buf[16] = 0;
@@ -85324,9 +83428,9 @@
         {
             buf[i] = '-';
         }
-        else if (SAFE_isalnum(s[i]))
+        else if ( (isalnum ((unsigned char)(s[i]))) )
         {
-            buf[i] = TOLOWER_ASC(s[i]);
+            buf[i] =  (((s[i]) < 'A' || (s[i]) > 'Z') ? (s[i]) : (s[i]) + ('a' - 'A')) ;
         }
         else
         {
@@ -85358,7 +83462,6 @@
 my_iconv_open(char_u *to, char_u *from)
 {
     iconv_t     fd;
-# define ICONV_TESTLEN 400
     char_u      tobuf[ICONV_TESTLEN];
     char        *p;
     size_t      tolen;
@@ -85408,13 +83511,13 @@
     fromlen = slen;
     for (;;)
     {
-        if (len == 0 || ICONV_ERRNO == ICONV_E2BIG)
+        if (len == 0 ||  errno  ==  E2BIG )
         {
             len = len + fromlen * 2 + 40;
             p = alloc(len);
             if (p != NULL && done > 0)
             {
-                mch_memmove(p, result, done);
+                 memmove((char *)(p), (char *)(result), done) ;
             }
             vim_free(result);
             result = p;
@@ -85432,14 +83535,14 @@
             break;
         }
 
-        if (!vcp->vc_fail && unconvlenp != NULL && (ICONV_ERRNO == ICONV_EINVAL || ICONV_ERRNO == EINVAL))
+        if (!vcp->vc_fail && unconvlenp != NULL && ( errno  ==  EINVAL  ||  errno  == EINVAL))
         {
             *to = NUL;
             *unconvlenp = (int)fromlen;
             break;
         }
 
-        else if (!vcp->vc_fail && (ICONV_ERRNO == ICONV_EILSEQ || ICONV_ERRNO == EILSEQ || ICONV_ERRNO == ICONV_EINVAL || ICONV_ERRNO == EINVAL))
+        else if (!vcp->vc_fail && ( errno  ==  EILSEQ  ||  errno  == EILSEQ ||  errno  ==  EINVAL  ||  errno  == EINVAL))
         {
             *to++ = '?';
             if ((*mb_ptr2cells)((char_u *)from) > 1)
@@ -85461,9 +83564,10 @@
             from += l;
             fromlen -= l;
         }
-        else if (ICONV_ERRNO != ICONV_E2BIG)
+        else if ( errno  !=  E2BIG )
         {
-            VIM_CLEAR(result);
+             vim_free(result);
+             (result) = NULL;
             break;
         }
         done = to - (char *)result;
@@ -85498,7 +83602,7 @@
     vcp->vc_factor = 1;
     vcp->vc_fail = FALSE;
 
-    if (from == NULL || *from == NUL || to == NULL || *to == NUL || STRCMP(from, to) == 0)
+    if (from == NULL || *from == NUL || to == NULL || *to == NUL ||  strcmp((char *)(from), (char *)(to))  == 0)
     {
         return OK;
     }
@@ -85577,11 +83681,11 @@
             *restp = alloc(unconvertlen);
             if (*restp != NULL)
             {
-                mch_memmove(*restp, ptr + len - unconvertlen, unconvertlen);
+                 memmove((char *)(*restp), (char *)(ptr + len - unconvertlen), unconvertlen) ;
             }
             *restlenp = unconvertlen;
         }
-        mch_memmove(ptr, d, dlen);
+         memmove((char *)(ptr), (char *)(d), dlen) ;
     }
     else
     {
@@ -85609,7 +83713,7 @@
 
     if (lenp == NULL)
     {
-        len = (int)STRLEN(ptr);
+        len = (int) strlen((char *)(ptr)) ;
     }
     else
     {
@@ -85808,13 +83912,13 @@
 }
 
     int
-get_cellwidth(int c UNUSED)
+get_cellwidth(int c  __attribute__((unused)) )
 {
     return 0;
 }
 
     char_u *
-get_encoding_name(expand_T *xp UNUSED, int idx)
+get_encoding_name(expand_T *xp  __attribute__((unused)) , int idx)
 {
     if (idx >= (int)(sizeof(enc_canon_table) / sizeof(enc_canon_table[0])))
     {
@@ -85826,11 +83930,7 @@
 
 // ==================== memfile.c ====================
 
-# define STATFS stat
-# define F_BSIZE st_blksize
-# define fstatfs(fd, buf, len, nul) mch_fstat((fd), (buf))
-
-#define MEMFILE_PAGE_SIZE 4096
+enum { MEMFILE_PAGE_SIZE = 4096 };
 
 static long_u   total_mem_used = 0;
 
@@ -85862,10 +83962,9 @@
 {
     memfile_T           *mfp;
     off_T               size;
-# define USE_FSTATFS
-    struct STATFS       stf;
+    struct  stat        stf;
 
-    if ((mfp = ALLOC_ONE(memfile_T)) == NULL)
+    if ((mfp =  (memfile_T *)alloc(sizeof(memfile_T)) ) == NULL)
     {
         return NULL;
     }
@@ -85896,12 +83995,12 @@
     mf_hash_init(&mfp->mf_trans);
     mfp->mf_page_size = MEMFILE_PAGE_SIZE;
 
-    if (mfp->mf_fd >= 0 && fstatfs(mfp->mf_fd, &stf, sizeof(struct statfs), 0) == 0 && stf.F_BSIZE >= MIN_SWAP_PAGE_SIZE && stf.F_BSIZE <= MAX_SWAP_PAGE_SIZE)
+    if (mfp->mf_fd >= 0 &&   fstat(((mfp->mf_fd)), ((&stf)))   == 0 && stf. st_blksize  >= MIN_SWAP_PAGE_SIZE && stf. st_blksize  <= MAX_SWAP_PAGE_SIZE)
     {
-        mfp->mf_page_size = stf.F_BSIZE;
+        mfp->mf_page_size = stf. st_blksize ;
     }
 
-    if (mfp->mf_fd < 0 || (flags & (O_TRUNC|O_EXCL)) || (size = vim_lseek(mfp->mf_fd, (off_T)0L, SEEK_END)) <= 0)
+    if (mfp->mf_fd < 0 || (flags & (O_TRUNC|O_EXCL)) || (size =  lseek (mfp->mf_fd, (off_T)0L, SEEK_END)) <= 0)
     {
         mfp->mf_blocknr_max = 0;
     }
@@ -85965,7 +84064,7 @@
     }
     if (del_file && mfp->mf_fname != NULL)
     {
-        mch_remove(mfp->mf_fname);
+         unlink((char *)(mfp->mf_fname)) ;
     }
     for (hp = mfp->mf_used_first; hp != NULL; hp = nextp)
     {
@@ -86014,9 +84113,11 @@
 
     if (mfp->mf_fname != NULL)
     {
-        mch_remove(mfp->mf_fname);
-        VIM_CLEAR(mfp->mf_fname);
-        VIM_CLEAR(mfp->mf_ffname);
+         unlink((char *)(mfp->mf_fname)) ;
+         vim_free(mfp->mf_fname);
+         (mfp->mf_fname) = NULL;
+         vim_free(mfp->mf_ffname);
+         (mfp->mf_ffname) = NULL;
     }
 }
 
@@ -86045,8 +84146,8 @@
             {
                 return NULL;
             }
-            hp->bh_bnum = freep->bh_bnum;
-            freep->bh_bnum += page_count;
+            hp-> bh_hashitem.mhi_key  = freep-> bh_hashitem.mhi_key ;
+            freep-> bh_hashitem.mhi_key  += page_count;
             freep->bh_page_count -= page_count;
         }
         else if (hp == NULL)
@@ -86061,7 +84162,7 @@
         else
         {
             freep = mf_rem_free(mfp);
-            hp->bh_bnum = freep->bh_bnum;
+            hp-> bh_hashitem.mhi_key  = freep-> bh_hashitem.mhi_key ;
             vim_free(freep);
         }
     }
@@ -86073,12 +84174,12 @@
         }
         if (negative)
         {
-            hp->bh_bnum = mfp->mf_blocknr_min--;
+            hp-> bh_hashitem.mhi_key  = mfp->mf_blocknr_min--;
             mfp->mf_neg_count++;
         }
         else
         {
-            hp->bh_bnum = mfp->mf_blocknr_max;
+            hp-> bh_hashitem.mhi_key  = mfp->mf_blocknr_max;
             mfp->mf_blocknr_max += page_count;
         }
     }
@@ -86088,7 +84189,7 @@
     mf_ins_used(mfp, hp);
     mf_ins_hash(mfp, hp);
 
-    (void)vim_memset((char *)(hp->bh_data), 0, (size_t)mfp->mf_page_size * page_count);
+    (void) memset(((char *)(hp->bh_data)), (0), ((size_t)mfp->mf_page_size * page_count)) ;
 
     return hp;
 }
@@ -86120,7 +84221,7 @@
             return NULL;
         }
 
-        hp->bh_bnum = nr;
+        hp-> bh_hashitem.mhi_key  = nr;
         hp->bh_flags = 0;
         hp->bh_page_count = page_count;
         if (mf_read(mfp, hp) == FAIL)
@@ -86175,7 +84276,7 @@
     vim_free(hp->bh_data);
     mf_rem_hash(mfp, hp);
     mf_rem_used(mfp, hp);
-    if (hp->bh_bnum < 0)
+    if (hp-> bh_hashitem.mhi_key  < 0)
     {
         vim_free(hp);
         mfp->mf_neg_count--;
@@ -86204,9 +84305,9 @@
     status = OK;
     for (hp = mfp->mf_used_last; hp != NULL; hp = hp->bh_prev)
     {
-        if (((flags & MFS_ALL) || hp->bh_bnum >= 0) && (hp->bh_flags & BH_DIRTY) && (status == OK || (hp->bh_bnum >= 0 && hp->bh_bnum < mfp->mf_infile_count)))
+        if (((flags & MFS_ALL) || hp-> bh_hashitem.mhi_key  >= 0) && (hp->bh_flags & BH_DIRTY) && (status == OK || (hp-> bh_hashitem.mhi_key  >= 0 && hp-> bh_hashitem.mhi_key  < mfp->mf_infile_count)))
         {
-            if ((flags & MFS_ZERO) && hp->bh_bnum != 0)
+            if ((flags & MFS_ZERO) && hp-> bh_hashitem.mhi_key  != 0)
             {
                 continue;
             }
@@ -86243,7 +84344,7 @@
 
     if ((flags & MFS_FLUSH) && *p_sws != NUL)
     {
-        if (STRCMP(p_sws, "fsync") == 0)
+        if ( strcmp((char *)(p_sws), (char *)("fsync"))  == 0)
         {
             if (vim_fsync(mfp->mf_fd))
             {
@@ -86268,7 +84369,7 @@
 
     for (hp = mfp->mf_used_last; hp != NULL; hp = hp->bh_prev)
     {
-        if (hp->bh_bnum > 0)
+        if (hp-> bh_hashitem.mhi_key  > 0)
         {
             hp->bh_flags |= BH_DIRTY;
         }
@@ -86351,11 +84452,13 @@
 
     if (mfp->mf_fd < 0 && need_release && p_uc)
     {
-        FOR_ALL_BUFFERS(buf)
+         for ((buf) = firstbuf; (buf) != NULL; (buf) = (buf)->b_next) 
+         {
             if (buf->b_ml.ml_mfp == mfp)
             {
                 break;
             }
+         }
         if (buf != NULL && buf->b_may_swap)
         {
             ml_open_file(buf);
@@ -86389,7 +84492,8 @@
 
     if (hp->bh_page_count != page_count)
     {
-        VIM_CLEAR(hp->bh_data);
+         vim_free(hp->bh_data);
+         (hp->bh_data) = NULL;
         if (page_count > 0)
         {
             hp->bh_data = alloc((size_t)mfp->mf_page_size * page_count);
@@ -86412,7 +84516,7 @@
     bhdr_T      *hp;
     int         retval = FALSE;
 
-    FOR_ALL_BUFFERS(buf)
+     for ((buf) = firstbuf; (buf) != NULL; (buf) = (buf)->b_next) 
     {
         mfp = buf->b_ml.ml_mfp;
         if (mfp != NULL)
@@ -86450,7 +84554,7 @@
 {
     bhdr_T      *hp;
 
-    if ((hp = ALLOC_ONE(bhdr_T)) == NULL)
+    if ((hp =  (bhdr_T *)alloc(sizeof(bhdr_T)) ) == NULL)
     {
         return NULL;
     }
@@ -86501,16 +84605,16 @@
     }
 
     page_size = mfp->mf_page_size;
-    offset = (off_T)page_size * hp->bh_bnum;
+    offset = (off_T)page_size * hp-> bh_hashitem.mhi_key ;
     size = page_size * hp->bh_page_count;
-    if (vim_lseek(mfp->mf_fd, offset, SEEK_SET) != offset)
+    if ( lseek (mfp->mf_fd, offset, SEEK_SET) != offset)
     {
-        PERROR(_(e_seek_error_in_swap_file_read));
+         (void)semsg("%s: %s", (char *)(_(e_seek_error_in_swap_file_read)), strerror(errno)) ;
         return FAIL;
     }
     if ((unsigned)read_eintr(mfp->mf_fd, hp->bh_data, size) != size)
     {
-        PERROR(_(e_read_error_in_swap_file));
+         (void)semsg("%s: %s", (char *)(_(e_read_error_in_swap_file)), strerror(errno)) ;
         return FAIL;
     }
 
@@ -86532,7 +84636,7 @@
         return FAIL;
     }
 
-    if (hp->bh_bnum < 0)
+    if (hp-> bh_hashitem.mhi_key  < 0)
     {
         if (mf_trans_add(mfp, hp) == FAIL)
         {
@@ -86546,7 +84650,7 @@
     {
         int attempt;
 
-        nr = hp->bh_bnum;
+        nr = hp-> bh_hashitem.mhi_key ;
         if (nr > mfp->mf_infile_count)
         {
             nr = mfp->mf_infile_count;
@@ -86572,9 +84676,9 @@
         {
             if (mfp->mf_fd >= 0)
             {
-                if (vim_lseek(mfp->mf_fd, offset, SEEK_SET) != offset)
+                if ( lseek (mfp->mf_fd, offset, SEEK_SET) != offset)
                 {
-                    PERROR(_(e_seek_error_in_swap_file_write));
+                     (void)semsg("%s: %s", (char *)(_(e_seek_error_in_swap_file_write)), strerror(errno)) ;
                     return FAIL;
                 }
                 if (mf_write_block(mfp, hp2 == NULL ? hp : hp2, offset, size) == OK)
@@ -86589,7 +84693,7 @@
                 {
                     close(mfp->mf_fd);
                 }
-                mfp->mf_fd = mch_open_rw((char *)mfp->mf_fname, mfp->mf_flags);
+                mfp->mf_fd =   open((((char *)mfp->mf_fname)), ((mfp->mf_flags)), ((mode_t)0600))  ;
                 mfp->mf_reopen = (mfp->mf_fd < 0);
             }
             if (attempt == 2 || mfp->mf_fd < 0)
@@ -86612,7 +84716,7 @@
         {
             mfp->mf_infile_count = nr + page_count;
         }
-        if (nr == hp->bh_bnum)
+        if (nr == hp-> bh_hashitem.mhi_key )
         {
             break;
         }
@@ -86621,7 +84725,7 @@
 }
 
     static int
-mf_write_block(memfile_T   *mfp, bhdr_T      *hp, off_T       offset UNUSED, unsigned    size)
+mf_write_block(memfile_T   *mfp, bhdr_T      *hp, off_T       offset  __attribute__((unused)) , unsigned    size)
 {
     char_u      *data = hp->bh_data;
     int         result = OK;
@@ -86642,12 +84746,12 @@
     NR_TRANS    *np;
     int         page_count;
 
-    if (hp->bh_bnum >= 0)
+    if (hp-> bh_hashitem.mhi_key  >= 0)
     {
         return OK;
     }
 
-    if ((np = ALLOC_ONE(NR_TRANS)) == NULL)
+    if ((np =  (NR_TRANS *)alloc(sizeof(NR_TRANS)) ) == NULL)
     {
         return FAIL;
     }
@@ -86656,10 +84760,10 @@
     page_count = hp->bh_page_count;
     if (freep != NULL && freep->bh_page_count >= page_count)
     {
-        new_bnum = freep->bh_bnum;
+        new_bnum = freep-> bh_hashitem.mhi_key ;
         if (freep->bh_page_count > page_count)
         {
-            freep->bh_bnum += page_count;
+            freep-> bh_hashitem.mhi_key  += page_count;
             freep->bh_page_count -= page_count;
         }
         else
@@ -86674,11 +84778,11 @@
         mfp->mf_blocknr_max += page_count;
     }
 
-    np->nt_old_bnum = hp->bh_bnum;
+    np-> nt_hashitem.mhi_key  = hp-> bh_hashitem.mhi_key ;
     np->nt_new_bnum = new_bnum;
 
     mf_rem_hash(mfp, hp);
-    hp->bh_bnum = new_bnum;
+    hp-> bh_hashitem.mhi_key  = new_bnum;
     mf_ins_hash(mfp, hp);
 
     mf_hash_add_item(&mfp->mf_trans, (mf_hashitem_T *)np);
@@ -86743,7 +84847,7 @@
 
     mf_set_ffname(mfp);
 
-    if ((flags & O_CREAT) && mch_lstat((char *)mfp->mf_fname, &sb) >= 0)
+    if ((flags & O_CREAT) &&  lstat(((char *)mfp->mf_fname), (&sb))  >= 0)
     {
         mfp->mf_fd = -1;
         emsg(_(e_swap_file_already_exists_symlink_attack));
@@ -86752,13 +84856,15 @@
     {
         flags |= O_EXTRA | O_NOFOLLOW;
         mfp->mf_flags = flags;
-        mfp->mf_fd = mch_open_rw((char *)mfp->mf_fname, flags);
+        mfp->mf_fd =   open((((char *)mfp->mf_fname)), ((flags)), ((mode_t)0600))  ;
     }
 
     if (mfp->mf_fd < 0)
     {
-        VIM_CLEAR(mfp->mf_fname);
-        VIM_CLEAR(mfp->mf_ffname);
+         vim_free(mfp->mf_fname);
+         (mfp->mf_fname) = NULL;
+         vim_free(mfp->mf_ffname);
+         (mfp->mf_ffname) = NULL;
     }
     else
     {
@@ -86771,13 +84877,13 @@
     }
 }
 
-#define MHT_LOG_LOAD_FACTOR 6
-#define MHT_GROWTH_FACTOR   2
+enum { MHT_LOG_LOAD_FACTOR = 6 };
+enum { MHT_GROWTH_FACTOR = 2 };
 
     static void
 mf_hash_init(mf_hashtab_T *mht)
 {
-    CLEAR_POINTER(mht);
+      memset(((mht)), (0), (sizeof(*(mht))))  ;
     mht->mht_buckets = mht->mht_small_buckets;
     mht->mht_mask = MHT_INIT_SIZE - 1;
 }
@@ -86896,8 +85002,7 @@
 
     for (i = 0; i <= mht->mht_mask; i++)
     {
-
-        CLEAR_FIELD(tails);
+          memset((&(tails)), (0), (sizeof(tails)))  ;
 
         for (mhi = mht->mht_buckets[i]; mhi != NULL; mhi = mhi->mhi_next)
         {
@@ -86938,22 +85043,18 @@
 
 // ==================== memline.c ====================
 
-#define CHECK(c, s)     do {   } while (0)
-
 typedef struct block0           ZERO_BL;
 typedef struct pointer_block    PTR_BL;
 typedef struct data_block       DATA_BL;
 typedef struct pointer_entry    PTR_EN;
 
-#define DATA_ID        (('d' << 8) + 'a')
-#define PTR_ID         (('p' << 8) + 't')
-#define BLOCK0_ID0     'b'
-#define BLOCK0_ID1     '0'
-#define BLOCK0_ID1_C0  'c'
-#define BLOCK0_ID1_C1  'C'
-#define BLOCK0_ID1_C2  'd'
-#define BLOCK0_ID1_C3  'S'
-#define BLOCK0_ID1_C4  's'
+enum { BLOCK0_ID0 = 'b' };
+enum { BLOCK0_ID1 = '0' };
+enum { BLOCK0_ID1_C0 = 'c' };
+enum { BLOCK0_ID1_C1 = 'C' };
+enum { BLOCK0_ID1_C2 = 'd' };
+enum { BLOCK0_ID1_C3 = 'S' };
+enum { BLOCK0_ID1_C4 = 's' };
 
 struct pointer_entry
 {
@@ -86971,8 +85072,6 @@
     PTR_EN      pb_pointer[1];
 };
 
-#define PB_COUNT_MAX(mfp) (short_u)(((mfp)->mf_page_size - offsetof(PTR_BL, pb_pointer)) / sizeof(PTR_EN))
-
 struct data_block
 {
     short_u     db_id;
@@ -86983,21 +85082,15 @@
     unsigned    db_index[1];
 };
 
-#define DB_MARKED       ((unsigned)1 << ((sizeof(unsigned) * 8) - 1))
-#define DB_INDEX_MASK   (~DB_MARKED)
-
-#define INDEX_SIZE  (sizeof(unsigned))
-#define HEADER_SIZE (offsetof(DATA_BL, db_index))
-
-#define B0_FNAME_SIZE_ORG       900
-#define B0_FNAME_SIZE_NOCRYPT   898
-#define B0_FNAME_SIZE_CRYPT     890
-#define B0_UNAME_SIZE           40
-#define B0_HNAME_SIZE           40
-#define B0_MAGIC_LONG   0x30313233L
-#define B0_MAGIC_INT    0x20212223L
-#define B0_MAGIC_SHORT  0x10111213L
-#define B0_MAGIC_CHAR   0x55
+enum { B0_FNAME_SIZE_ORG = 900 };
+enum { B0_FNAME_SIZE_NOCRYPT = 898 };
+enum { B0_FNAME_SIZE_CRYPT = 890 };
+enum { B0_UNAME_SIZE = 40 };
+enum { B0_HNAME_SIZE = 40 };
+enum { B0_MAGIC_LONG = 0x30313233L };
+enum { B0_MAGIC_INT = 0x20212223L };
+enum { B0_MAGIC_SHORT = 0x10111213L };
+enum { B0_MAGIC_CHAR = 0x55 };
 
 struct block0
 {
@@ -87016,28 +85109,22 @@
     char_u      b0_magic_char;
 };
 
-#define B0_DIRTY        0x55
-#define b0_dirty        b0_fname[B0_FNAME_SIZE_ORG - 1]
-
-#define b0_flags        b0_fname[B0_FNAME_SIZE_ORG - 2]
-
-#define b0_seed         b0_fname[B0_FNAME_SIZE_ORG - 2 - MF_SEED_LEN]
+enum { B0_DIRTY = 0x55 };
 
-#define B0_FF_MASK      3
+enum { B0_FF_MASK = 3 };
 
-#define B0_SAME_DIR     4
+enum { B0_SAME_DIR = 4 };
 
-#define B0_HAS_FENC     8
+enum { B0_HAS_FENC = 8 };
 
-#define STACK_INCR      5
+enum { STACK_INCR = 5 };
 
 static linenr_T lowest_marked = 0;
 
-#define ML_DELETE       0x11
-#define ML_INSERT       0x12
-#define ML_FIND         0x13
-#define ML_FLUSH        0x02
-#define ML_SIMPLE(x)    ((x) & 0x10)
+enum { ML_DELETE = 0x11 };
+enum { ML_INSERT = 0x12 };
+enum { ML_FIND = 0x13 };
+enum { ML_FLUSH = 0x02 };
 
 typedef enum {
       UB_FNAME = 0
@@ -87106,7 +85193,7 @@
     {
         goto error;
     }
-    if (hp->bh_bnum != 0)
+    if (hp-> bh_hashitem.mhi_key  != 0)
     {
         iemsg(e_didnt_get_block_nr_zero);
         goto error;
@@ -87119,13 +85206,13 @@
     b0p->b0_magic_int = (int)B0_MAGIC_INT;
     b0p->b0_magic_short = (short)B0_MAGIC_SHORT;
     b0p->b0_magic_char = B0_MAGIC_CHAR;
-    mch_memmove(b0p->b0_version, "VIM ", 4);
-    STRNCPY(b0p->b0_version + 4, Version, 6);
+     memmove((char *)(b0p->b0_version), (char *)("VIM "), 4) ;
+     strncpy((char *)(b0p->b0_version + 4), (char *)(Version), (6)) ;
     long_to_char((long)mfp->mf_page_size, b0p->b0_page_size);
 
     {
-        b0p->b0_dirty = buf->b_changed ? B0_DIRTY : 0;
-        b0p->b0_flags = get_fileformat(buf) + 1;
+        b0p-> b0_fname[B0_FNAME_SIZE_ORG - 1]  = buf->b_changed ? B0_DIRTY : 0;
+        b0p-> b0_fname[B0_FNAME_SIZE_ORG - 2]  = get_fileformat(buf) + 1;
         set_b0_fname(b0p, buf);
         (void)get_user_name(b0p->b0_uname, B0_UNAME_SIZE);
         b0p->b0_uname[B0_UNAME_SIZE - 1] = NUL;
@@ -87135,7 +85222,7 @@
     }
 
     mf_put(mfp, hp, TRUE, FALSE);
-    if (!buf->b_help && !B_SPELL(buf))
+    if (!buf->b_help && ! (0) )
     {
         (void)mf_sync(mfp, 0);
     }
@@ -87144,7 +85231,7 @@
     {
         goto error;
     }
-    if (hp->bh_bnum != 1)
+    if (hp-> bh_hashitem.mhi_key  != 1)
     {
         iemsg(e_didnt_get_block_nr_one);
         goto error;
@@ -87161,7 +85248,7 @@
     {
         goto error;
     }
-    if (hp->bh_bnum != 2)
+    if (hp-> bh_hashitem.mhi_key  != 2)
     {
         iemsg(e_didnt_get_block_nr_two);
         goto error;
@@ -87169,7 +85256,7 @@
 
     dp = (DATA_BL *)(hp->bh_data);
     dp->db_index[0] = --dp->db_txt_start;
-    dp->db_free -= 1 + INDEX_SIZE;
+    dp->db_free -= 1 +  (sizeof(unsigned)) ;
     dp->db_line_count = 1;
     *((char_u *)dp + dp->db_txt_start) = NUL;
 
@@ -87223,7 +85310,7 @@
             continue;
         }
 
-        if (fnamecmp(fname, mfp->mf_fname) == 0)
+        if ( vim_fnamecmp((char_u *)(fname), (char_u *)(mfp->mf_fname))  == 0)
         {
             vim_free(fname);
             success = TRUE;
@@ -87250,7 +85337,7 @@
 
     if (mfp->mf_fd == -1)
     {
-        mfp->mf_fd = mch_open((char *)mfp->mf_fname, O_RDWR | O_EXTRA, 0);
+        mfp->mf_fd =  open(((char *)mfp->mf_fname), (O_RDWR | O_EXTRA), (0)) ;
         if (mfp->mf_fd < 0)
         {
             emsg(_(e_oops_lost_the_swap_file));
@@ -87275,11 +85362,13 @@
 {
     buf_T       *buf;
 
-    FOR_ALL_BUFFERS(buf)
+     for ((buf) = firstbuf; (buf) != NULL; (buf) = (buf)->b_next) 
+     {
         if (!buf->b_p_ro || buf->b_changed)
         {
             ml_open_file(buf);
         }
+     }
 }
 
     void
@@ -87376,8 +85465,10 @@
 {
     buf_T       *buf;
 
-    FOR_ALL_BUFFERS(buf)
+     for ((buf) = firstbuf; (buf) != NULL; (buf) = (buf)->b_next) 
+     {
         ml_close(buf, del_file && ((buf->b_flags & BF_PRESERVED) == 0 || vim_strchr(p_cpo, CPO_PRESERVE) == NULL));
+     }
     vim_deltempdir();
 }
 
@@ -87386,11 +85477,13 @@
 {
     buf_T       *buf;
 
-    FOR_ALL_BUFFERS(buf)
+     for ((buf) = firstbuf; (buf) != NULL; (buf) = (buf)->b_next) 
+     {
         if (!bufIsChanged(buf))
         {
             ml_close(buf, TRUE);
         }
+     }
 }
 
     void
@@ -87465,17 +85558,17 @@
         {
             size_t  ulen;
 
-            if (get_user_name(uname, B0_UNAME_SIZE) == FAIL || (ulen = STRLEN(uname)) + flen > B0_FNAME_SIZE_CRYPT - 1)
+            if (get_user_name(uname, B0_UNAME_SIZE) == FAIL || (ulen =  strlen((char *)(uname)) ) + flen > B0_FNAME_SIZE_CRYPT - 1)
             {
                 vim_strncpy(b0p->b0_fname, buf->b_ffname, B0_FNAME_SIZE_CRYPT - 1);
             }
             else
             {
-                mch_memmove(b0p->b0_fname + ulen + 1, b0p->b0_fname + 1, flen);
-                mch_memmove(b0p->b0_fname + 1, uname, ulen);
+                 memmove((char *)(b0p->b0_fname + ulen + 1), (char *)(b0p->b0_fname + 1), flen) ;
+                 memmove((char *)(b0p->b0_fname + 1), (char *)(uname), ulen) ;
             }
         }
-        if (mch_stat((char *)buf->b_ffname, &st) >= 0)
+        if ( stat(((char *)buf->b_ffname), (&st))  >= 0)
         {
             long_to_char((long)st.st_mtime, b0p->b0_mtime);
             long_to_char((long)st.st_ino, b0p->b0_ino);
@@ -87504,11 +85597,11 @@
 {
     if (same_directory(buf->b_ml.ml_mfp->mf_fname, buf->b_ffname))
     {
-        b0p->b0_flags |= B0_SAME_DIR;
+        b0p-> b0_fname[B0_FNAME_SIZE_ORG - 2]  |= B0_SAME_DIR;
     }
     else
     {
-        b0p->b0_flags &= ~B0_SAME_DIR;
+        b0p-> b0_fname[B0_FNAME_SIZE_ORG - 2]  &= ~B0_SAME_DIR;
     }
 }
 
@@ -87518,26 +85611,26 @@
     int         n;
     int         size = B0_FNAME_SIZE_NOCRYPT;
 
-    n = (int)STRLEN(buf->b_p_fenc);
-    if ((int)STRLEN(b0p->b0_fname) + n + 1 > size)
+    n = (int) strlen((char *)(buf->b_p_fenc)) ;
+    if ((int) strlen((char *)(b0p->b0_fname))  + n + 1 > size)
     {
-        b0p->b0_flags &= ~B0_HAS_FENC;
+        b0p-> b0_fname[B0_FNAME_SIZE_ORG - 2]  &= ~B0_HAS_FENC;
     }
     else
     {
-        mch_memmove((char *)b0p->b0_fname + size - n, (char *)buf->b_p_fenc, (size_t)n);
+         memmove((char *)((char *)b0p->b0_fname + size - n), (char *)((char *)buf->b_p_fenc), (size_t)n) ;
         *(b0p->b0_fname + size - n - 1) = NUL;
-        b0p->b0_flags |= B0_HAS_FENC;
+        b0p-> b0_fname[B0_FNAME_SIZE_ORG - 2]  |= B0_HAS_FENC;
     }
 }
 
     static int
-swapfile_process_running(ZERO_BL *b0p, char_u *swap_fname UNUSED)
+swapfile_process_running(ZERO_BL *b0p, char_u *swap_fname  __attribute__((unused)) )
 {
     stat_T          st;
     struct sysinfo  sinfo;
 
-    if (mch_stat((char *)swap_fname, &st) != -1 && sysinfo(&sinfo) == 0 && st.st_mtime < time(NULL) - (time_t)(sinfo.uptime))
+    if ( stat(((char *)swap_fname), (&st))  != -1 && sysinfo(&sinfo) == 0 && st.st_mtime < time(NULL) - (time_t)(sinfo.uptime))
     {
         return FALSE;
     }
@@ -87583,15 +85676,15 @@
 
     recoverymode = TRUE;
     called_from_main = (curbuf->b_ml.ml_mfp == NULL);
-    attr = HL_ATTR(HLF_E);
+    attr =  highlight_attr[(int)(HLF_E)] ;
 
     fname = curbuf->b_fname;
     if (fname == NULL)
     {
         fname = (char_u *)"";
     }
-    len = (int)STRLEN(fname);
-    if (checkext && len >= 4 && STRNICMP(fname + len - 4, ".s", 2) == 0 && vim_strchr((char_u *)"abcdefghijklmnopqrstuvw", TOLOWER_ASC(fname[len - 2])) != NULL && ASCII_ISALPHA(fname[len - 1]))
+    len = (int) strlen((char *)(fname)) ;
+    if (checkext && len >= 4 &&  strncasecmp((char *)(fname + len - 4), (char *)(".s"), (2))  == 0 && vim_strchr((char_u *)"abcdefghijklmnopqrstuvw",  (((fname[len - 2]) < 'A' || (fname[len - 2]) > 'Z') ? (fname[len - 2]) : (fname[len - 2]) + ('a' - 'A')) ) != NULL &&  ( ((unsigned)(fname[len - 1]) - 'A' < 26)  ||  ((unsigned)(fname[len - 1]) - 'a' < 26) ) )
     {
         directly = TRUE;
         fname_used = vim_strsave(fname);
@@ -87633,7 +85726,7 @@
         getout(1);
     }
 
-    buf = ALLOC_ONE(buf_T);
+    buf =  (buf_T *)alloc(sizeof(buf_T)) ;
     if (buf == NULL)
     {
         goto theend;
@@ -87671,7 +85764,7 @@
         goto theend;
     }
     b0p = (ZERO_BL *)(hp->bh_data);
-    if (STRNCMP(b0p->b0_version, "VIM 3.0", 7) == 0)
+    if ( strncmp((char *)(b0p->b0_version), (char *)("VIM 3.0"), (7))  == 0)
     {
         msg_start();
         msg_outtrans_attr(mfp->mf_fname, MSG_HIST);
@@ -87717,7 +85810,7 @@
             msg_end();
             goto theend;
         }
-        if ((size = vim_lseek(mfp->mf_fd, (off_T)0L, SEEK_END)) <= 0)
+        if ((size =  lseek (mfp->mf_fd, (off_T)0L, SEEK_END)) <= 0)
         {
             mfp->mf_blocknr_max = 0;
         }
@@ -87732,7 +85825,7 @@
         {
             goto theend;
         }
-        mch_memmove(p, hp->bh_data, previous_page_size);
+         memmove((char *)(p), (char *)(hp->bh_data), previous_page_size) ;
         vim_free(hp->bh_data);
         hp->bh_data = p;
         b0p = (ZERO_BL *)(hp->bh_data);
@@ -87740,36 +85833,36 @@
 
     if (directly)
     {
-        expand_env(b0p->b0_fname, NameBuff, MAXPATHL);
+        expand_env(b0p->b0_fname, NameBuff,  PATH_MAX );
         if (setfname(curbuf, NameBuff, NULL, TRUE) == FAIL)
         {
             goto theend;
         }
     }
 
-    home_replace(NULL, mfp->mf_fname, NameBuff, MAXPATHL, TRUE);
+    home_replace(NULL, mfp->mf_fname, NameBuff,  PATH_MAX , TRUE);
     smsg(_("Using swap file \"%s\""), NameBuff);
 
     if (buf_spname(curbuf) != NULL)
     {
-        vim_strncpy(NameBuff, buf_spname(curbuf), MAXPATHL - 1);
+        vim_strncpy(NameBuff, buf_spname(curbuf),  PATH_MAX  - 1);
     }
     else
     {
-        home_replace(NULL, curbuf->b_ffname, NameBuff, MAXPATHL, TRUE);
+        home_replace(NULL, curbuf->b_ffname, NameBuff,  PATH_MAX , TRUE);
     }
     smsg(_("Original file \"%s\""), NameBuff);
     msg_putchar('\n');
 
     mtime = char_to_long(b0p->b0_mtime);
-    if (curbuf->b_ffname != NULL && mch_stat((char *)curbuf->b_ffname, &org_stat) != -1 && ((mch_stat((char *)mfp->mf_fname, &swp_stat) != -1 && org_stat.st_mtime > swp_stat.st_mtime) || org_stat.st_mtime != mtime))
+    if (curbuf->b_ffname != NULL &&  stat(((char *)curbuf->b_ffname), (&org_stat))  != -1 && (( stat(((char *)mfp->mf_fname), (&swp_stat))  != -1 && org_stat.st_mtime > swp_stat.st_mtime) || org_stat.st_mtime != mtime))
     {
         emsg(_(e_warning_original_file_may_have_been_changed));
     }
     out_flush();
 
-    b0_ff = (b0p->b0_flags & B0_FF_MASK);
-    if (b0p->b0_flags & B0_HAS_FENC)
+    b0_ff = (b0p-> b0_fname[B0_FNAME_SIZE_ORG - 2]  & B0_FF_MASK);
+    if (b0p-> b0_fname[B0_FNAME_SIZE_ORG - 2]  & B0_HAS_FENC)
     {
         int fnsize = B0_FNAME_SIZE_NOCRYPT;
 
@@ -87790,7 +85883,7 @@
 
     if (curbuf->b_ffname != NULL)
     {
-        orig_file_status = readfile(curbuf->b_ffname, NULL, (linenr_T)0, (linenr_T)0, (linenr_T)MAXLNUM, NULL, READ_NEW);
+        orig_file_status = readfile(curbuf->b_ffname, NULL, (linenr_T)0, (linenr_T)0, (linenr_T) LONG_MAX , NULL, READ_NEW);
     }
 
     if (b0_ff != 0)
@@ -87844,13 +85937,13 @@
         else
         {
             pp = (PTR_BL *)(hp->bh_data);
-            if (pp->pb_id == PTR_ID)
+            if (pp->pb_id ==  (('p' << 8) + 't') )
             {
                 int ptr_block_error = FALSE;
-                if (pp->pb_count_max != PB_COUNT_MAX(mfp))
+                if (pp->pb_count_max !=  (short_u)(((mfp)->mf_page_size - offsetof(PTR_BL, pb_pointer)) / sizeof(PTR_EN)) )
                 {
                     ptr_block_error = TRUE;
-                    pp->pb_count_max = PB_COUNT_MAX(mfp);
+                    pp->pb_count_max =  (short_u)(((mfp)->mf_page_size - offsetof(PTR_BL, pb_pointer)) / sizeof(PTR_EN)) ;
                 }
                 if (pp->pb_count > pp->pb_count_max)
                 {
@@ -87935,7 +86028,7 @@
             else
             {
                 dp = (DATA_BL *)(hp->bh_data);
-                if (dp->db_id != DATA_ID)
+                if (dp->db_id !=  (('d' << 8) + 'a') )
                 {
                     if (bnum == 1)
                     {
@@ -87964,7 +86057,7 @@
                         dp->db_txt_end = page_count * mfp->mf_page_size;
                     }
 
-                    if (dp->db_txt_start < HEADER_SIZE || dp->db_txt_start > dp->db_txt_end)
+                    if (dp->db_txt_start <  (offsetof(DATA_BL, db_index))  || dp->db_txt_start > dp->db_txt_end)
                     {
                         ml_append(lnum++, (char_u *)_("??? block header corrupted"), (colnr_T)0, TRUE);
                         ++error;
@@ -87990,8 +86083,8 @@
                             break;
                         }
 
-                        txt_start = (dp->db_index[i] & DB_INDEX_MASK);
-                        if (txt_start <= (int)HEADER_SIZE || txt_start >= (int)dp->db_txt_end)
+                        txt_start = (dp->db_index[i] &  (~ ((unsigned)1 << ((sizeof(unsigned) * 8) - 1)) ) );
+                        if (txt_start <= (int) (offsetof(DATA_BL, db_index))  || txt_start >= (int)dp->db_txt_end)
                         {
                             ++error;
                             if (did_questions)
@@ -88032,7 +86125,7 @@
         if (!(curbuf->b_ml.ml_line_count == 2 && *ml_get(1) == NUL))
         {
             changed_internal();
-            ++CHANGEDTICK(curbuf);
+            ++ ((curbuf)->b_ct_di.di_tv.vval.v_number) ;
         }
     }
     else
@@ -88040,12 +86133,12 @@
         for (idx = 1; idx <= lnum; ++idx)
         {
             p = vim_strnsave(ml_get(idx), ml_get_len(idx));
-            i = STRCMP(p, ml_get(idx + lnum));
+            i =  strcmp((char *)(p), (char *)(ml_get(idx + lnum))) ;
             vim_free(p);
             if (i != 0)
             {
                 changed_internal();
-                ++CHANGEDTICK(curbuf);
+                ++ ((curbuf)->b_ct_di.di_tv.vval.v_number) ;
                 break;
             }
         }
@@ -88123,7 +86216,7 @@
 }
 
     int
-recover_names(char_u      *fname, int         do_list, list_T      *ret_list UNUSED, int         nr, char_u      **fname_out)
+recover_names(char_u      *fname, int         do_list, list_T      *ret_list  __attribute__((unused)) , int         nr, char_u      **fname_out)
 {
     int         num_names;
     char_u      *(names[6]);
@@ -88134,7 +86227,7 @@
     char_u      *dirp;
     string_T    dir_name;
     char_u      *fname_res = NULL;
-    char_u      fname_buf[MAXPATHL];
+    char_u      fname_buf[ PATH_MAX ];
 
     if (fname != NULL)
     {
@@ -88154,7 +86247,7 @@
         msg_putchar('\n');
     }
 
-    dir_name.string = alloc(STRLEN(p_dir) + 1);
+    dir_name.string = alloc( strlen((char *)(p_dir))  + 1);
     dirp = p_dir;
     while (dir_name.string != NULL && *dirp)
     {
@@ -88164,9 +86257,9 @@
         {
             if (fname == NULL)
             {
-                names[0] = vim_strnsave((char_u *)"*.sw?", STRLEN_LITERAL("*.sw?"));
-                names[1] = vim_strnsave((char_u *)".*.sw?", STRLEN_LITERAL(".*.sw?"));
-                names[2] = vim_strnsave((char_u *)".sw?", STRLEN_LITERAL(".sw?"));
+                names[0] = vim_strnsave((char_u *)"*.sw?",  (sizeof("*.sw?" "") - 1) );
+                names[1] = vim_strnsave((char_u *)".*.sw?",  (sizeof(".*.sw?" "") - 1) );
+                names[2] = vim_strnsave((char_u *)".sw?",  (sizeof(".sw?" "") - 1) );
                 num_names = 3;
             }
             else
@@ -88180,9 +86273,9 @@
             {
                 string_T    ret;
 
-                names[0] = concat_fnames(dir_name.string, dir_name.length, (char_u *)"*.sw?", STRLEN_LITERAL("*.sw?"), TRUE, &ret);
-                names[1] = concat_fnames(dir_name.string, dir_name.length, (char_u *)".*.sw?", STRLEN_LITERAL(".*.sw?"), TRUE, &ret);
-                names[2] = concat_fnames(dir_name.string, dir_name.length, (char_u *)".sw?", STRLEN_LITERAL(".sw?"), TRUE, &ret);
+                names[0] = concat_fnames(dir_name.string, dir_name.length, (char_u *)"*.sw?",  (sizeof("*.sw?" "") - 1) , TRUE, &ret);
+                names[1] = concat_fnames(dir_name.string, dir_name.length, (char_u *)".*.sw?",  (sizeof(".*.sw?" "") - 1) , TRUE, &ret);
+                names[2] = concat_fnames(dir_name.string, dir_name.length, (char_u *)".sw?",  (sizeof(".sw?" "") - 1) , TRUE, &ret);
                 num_names = 3;
             }
             else
@@ -88199,7 +86292,7 @@
                     string_T    ret;
 
                     tail = gettail(fname_res);
-                    tail = concat_fnames(dir_name.string, dir_name.length, tail, STRLEN(tail), TRUE, &ret);
+                    tail = concat_fnames(dir_name.string, dir_name.length, tail,  strlen((char *)(tail)) , TRUE, &ret);
                 }
                 if (tail == NULL)
                 {
@@ -88241,9 +86334,9 @@
             swapname = modname(fname_res, (char_u *)".swp", TRUE);
             if (swapname != NULL)
             {
-                if (mch_stat((char *)swapname, &st) != -1)
+                if ( stat(((char *)swapname), (&st))  != -1)
                 {
-                    files = ALLOC_ONE(char_u *);
+                    files =  (char_u * *)alloc(sizeof(char_u *)) ;
                     if (files != NULL)
                     {
                         files[0] = swapname;
@@ -88353,7 +86446,7 @@
         return NULL;
     }
 
-    for (p = fixed_fname.string; *p != NUL; MB_PTR_ADV(p))
+    for (p = fixed_fname.string; *p != NUL;  p += (*mb_ptr2len)(p) )
     {
         if (vim_ispathsep(*p))
         {
@@ -88378,7 +86471,7 @@
     struct block0   b0;
     char_u          uname[B0_UNAME_SIZE];
 
-    if (mch_stat((char *)fname, &st) != -1)
+    if ( stat(((char *)fname), (&st))  != -1)
     {
         if (mch_get_uname(st.st_uid, uname, B0_UNAME_SIZE) == OK)
         {
@@ -88397,12 +86490,12 @@
         st.st_mtime = 0;
     }
 
-    fd = mch_open((char *)fname, O_RDONLY | O_EXTRA, 0);
+    fd =  open(((char *)fname), (O_RDONLY | O_EXTRA), (0)) ;
     if (fd >= 0)
     {
         if (read_eintr(fd, &b0, sizeof(b0)) == sizeof(b0))
         {
-            if (STRNCMP(b0.b0_version, "VIM 3.0", 7) == 0)
+            if ( strncmp((char *)(b0.b0_version), (char *)("VIM 3.0"), (7))  == 0)
             {
                 msg_puts(_("         [from Vim version 3.0]"));
             }
@@ -88423,7 +86516,7 @@
                 }
 
                 msg_puts(_("\n          modified: "));
-                msg_puts(b0.b0_dirty ? _("YES") : _("no"));
+                msg_puts(b0. b0_fname[B0_FNAME_SIZE_ORG - 1]  ? _("YES") : _("no"));
 
                 if (*(b0.b0_uname) != NUL)
                 {
@@ -88483,12 +86576,12 @@
     struct block0   b0;
     int             ret = TRUE;
 
-    if (mch_stat((char *)fname, &st) == -1)
+    if ( stat(((char *)fname), (&st))  == -1)
     {
         return FALSE;
     }
 
-    fd = mch_open((char *)fname, O_RDONLY | O_EXTRA, 0);
+    fd =  open(((char *)fname), (O_RDONLY | O_EXTRA), (0)) ;
     if (fd < 0)
     {
         return FALSE;
@@ -88504,7 +86597,7 @@
         ret = FALSE;
     }
 
-    if (b0.b0_dirty)
+    if (b0. b0_fname[B0_FNAME_SIZE_ORG - 1] )
     {
         ret = FALSE;
     }
@@ -88520,7 +86613,7 @@
         mch_get_host_name(hostname, B0_HNAME_SIZE);
         hostname[B0_HNAME_SIZE - 1] = NUL;
         b0.b0_hname[B0_HNAME_SIZE - 1] = NUL;
-        if (STRICMP(b0.b0_hname, hostname) != 0)
+        if ( strcasecmp((char *)(b0.b0_hname), (char *)(hostname))  != 0)
         {
             ret = FALSE;
         }
@@ -88559,7 +86652,7 @@
         ++num_names;
     }
 
-    names[num_names] = concat_fnames(path, STRLEN(path), (char_u *)".sw?", STRLEN_LITERAL(".sw?"), FALSE, &ret);
+    names[num_names] = concat_fnames(path,  strlen((char *)(path)) , (char_u *)".sw?",  (sizeof(".sw?" "") - 1) , FALSE, &ret);
     if (names[num_names] == NULL)
     {
         goto end;
@@ -88567,13 +86660,13 @@
     if (num_names >= 1)
     {
         p = names[num_names - 1];
-        i = (int)STRLEN(names[num_names - 1]) - (int)STRLEN(names[num_names]);
+        i = (int) strlen((char *)(names[num_names - 1]))  - (int) strlen((char *)(names[num_names])) ;
         if (i > 0)
         {
             p += i;
         }
 
-        if (STRCMP(p, names[num_names]) != 0)
+        if ( strcmp((char *)(p), (char *)(names[num_names]))  != 0)
         {
             ++num_names;
         }
@@ -88595,12 +86688,12 @@
     }
 
     p = names[num_names];
-    i = STRLEN(names[num_names]) - STRLEN(names[num_names - 1]);
+    i =  strlen((char *)(names[num_names]))  -  strlen((char *)(names[num_names - 1])) ;
     if (i > 0)
     {
         p += i;
     }
-    if (STRCMP(names[num_names - 1], p) == 0)
+    if ( strcmp((char *)(names[num_names - 1]), (char *)(p))  == 0)
     {
         vim_free(names[num_names]);
     }
@@ -88621,7 +86714,7 @@
     buf_T               *buf;
     stat_T              st;
 
-    FOR_ALL_BUFFERS(buf)
+     for ((buf) = firstbuf; (buf) != NULL; (buf) = (buf)->b_next) 
     {
         if (buf->b_ml.ml_mfp == NULL || buf->b_ml.ml_mfp->mf_fname == NULL || buf->b_ml.ml_mfp->mf_fd < 0)
         {
@@ -88632,7 +86725,7 @@
         (void)ml_find_line(buf, (linenr_T)0, ML_FLUSH);
         if (bufIsChanged(buf) && check_file && mf_need_trans(buf->b_ml.ml_mfp) && buf->b_ffname != NULL)
         {
-            if (mch_stat((char *)buf->b_ffname, &st) == -1 || st.st_mtime != buf->b_mtime_read || st.ST_MTIM_NSEC != buf->b_mtime_read_ns || st.st_size != buf->b_orig_size)
+            if ( stat(((char *)buf->b_ffname), (&st))  == -1 || st.st_mtime != buf->b_mtime_read || st. st_mtim.tv_nsec  != buf->b_mtime_read_ns || st.st_size != buf->b_orig_size)
             {
                 ml_preserve(buf, FALSE);
                 did_check_timestamps = FALSE;
@@ -88687,7 +86780,6 @@
                 status = FAIL;
                 goto theend;
             }
-            CHECK(buf->b_ml.ml_locked_low != lnum, "low != lnum");
             lnum = buf->b_ml.ml_locked_high + 1;
         }
         (void)ml_find_line(buf, (linenr_T)0, ML_FLUSH);
@@ -88773,7 +86865,7 @@
 
     if (buf->b_ml.ml_line_textlen <= 0)
     {
-        buf->b_ml.ml_line_textlen = (int)STRLEN(line) + 1;
+        buf->b_ml.ml_line_textlen = (int) strlen((char *)(line))  + 1;
     }
     return buf->b_ml.ml_line_textlen - 1;
 }
@@ -88796,7 +86888,7 @@
         }
         ml_flush_line(buf);
 errorret:
-        STRCPY(questions, "???");
+         strcpy((char *)(questions), (char *)("???")) ;
         buf->b_ml.ml_line_len = 4;
         buf->b_ml.ml_line_textlen = buf->b_ml.ml_line_len;
         buf->b_ml.ml_line_lnum = lnum;
@@ -88838,14 +86930,14 @@
         dp = (DATA_BL *)(hp->bh_data);
 
         idx = lnum - buf->b_ml.ml_locked_low;
-        start = ((dp->db_index[idx]) & DB_INDEX_MASK);
+        start = ((dp->db_index[idx]) &  (~ ((unsigned)1 << ((sizeof(unsigned) * 8) - 1)) ) );
         if (idx == 0)
         {
             end = dp->db_txt_end;
         }
         else
         {
-            end = ((dp->db_index[idx - 1]) & DB_INDEX_MASK);
+            end = ((dp->db_index[idx - 1]) &  (~ ((unsigned)1 << ((sizeof(unsigned) * 8) - 1)) ) );
         }
 
         buf->b_ml.ml_line_ptr = (char_u *)dp + start;
@@ -88901,10 +86993,10 @@
 
     if (len == 0)
     {
-        len = (colnr_T)STRLEN(line) + 1;
+        len = (colnr_T) strlen((char *)(line))  + 1;
     }
 
-    space_needed = len + INDEX_SIZE;
+    space_needed = len +  (sizeof(unsigned)) ;
 
     mfp = buf->b_ml.ml_mfp;
     page_size = mfp->mf_page_size;
@@ -88939,7 +87031,6 @@
 
         db_idx = -1;
         line_count = buf->b_ml.ml_locked_high - buf->b_ml.ml_locked_low;
-        CHECK(buf->b_ml.ml_locked_low != lnum + 1, "locked_low != lnum + 1");
 
         dp = (DATA_BL *)(hp->bh_data);
     }
@@ -88960,9 +87051,9 @@
             }
             else
             {
-                offset = ((dp->db_index[db_idx]) & DB_INDEX_MASK);
+                offset = ((dp->db_index[db_idx]) &  (~ ((unsigned)1 << ((sizeof(unsigned) * 8) - 1)) ) );
             }
-            mch_memmove((char *)dp + dp->db_txt_start, (char *)dp + dp->db_txt_start + len, (size_t)(offset - (dp->db_txt_start + len)));
+             memmove((char *)((char *)dp + dp->db_txt_start), (char *)((char *)dp + dp->db_txt_start + len), (size_t)(offset - (dp->db_txt_start + len))) ;
             for (i = line_count - 1; i > db_idx; --i)
             {
                 dp->db_index[i + 1] = dp->db_index[i] - len;
@@ -88974,10 +87065,10 @@
             dp->db_index[db_idx + 1] = dp->db_txt_start;
         }
 
-        mch_memmove((char *)dp + dp->db_index[db_idx + 1], line, (size_t)len);
+         memmove((char *)((char *)dp + dp->db_index[db_idx + 1]), (char *)(line), (size_t)len) ;
         if (flags & ML_APPEND_MARK)
         {
-            dp->db_index[db_idx + 1] |= DB_MARKED;
+            dp->db_index[db_idx + 1] |=  ((unsigned)1 << ((sizeof(unsigned) * 8) - 1)) ;
         }
 
         buf->b_ml.ml_flags |= ML_LOCKED_DIRTY;
@@ -89024,9 +87115,9 @@
             }
             else
             {
-                data_moved = ((dp->db_index[db_idx]) & DB_INDEX_MASK) -
+                data_moved = ((dp->db_index[db_idx]) &  (~ ((unsigned)1 << ((sizeof(unsigned) * 8) - 1)) ) ) -
                                                             dp->db_txt_start;
-                total_moved = data_moved + lines_moved * INDEX_SIZE;
+                total_moved = data_moved + lines_moved *  (sizeof(unsigned)) ;
                 if ((long)dp->db_free + total_moved >= space_needed)
                 {
                     in_left = TRUE;
@@ -89040,7 +87131,7 @@
             }
         }
 
-        page_count = ((space_needed + HEADER_SIZE) + page_size - 1) / page_size;
+        page_count = ((space_needed +  (offsetof(DATA_BL, db_index)) ) + page_size - 1) / page_size;
         if ((hp_new = ml_new_data(mfp, flags & ML_APPEND_NEW, page_count)) == NULL)
         {
             --(buf->b_ml.ml_locked_lineadd);
@@ -89063,29 +87154,29 @@
         }
         dp_right = (DATA_BL *)(hp_right->bh_data);
         dp_left = (DATA_BL *)(hp_left->bh_data);
-        bnum_left = hp_left->bh_bnum;
-        bnum_right = hp_right->bh_bnum;
+        bnum_left = hp_left-> bh_hashitem.mhi_key ;
+        bnum_right = hp_right-> bh_hashitem.mhi_key ;
         page_count_left = hp_left->bh_page_count;
         page_count_right = hp_right->bh_page_count;
 
         if (!in_left)
         {
             dp_right->db_txt_start -= len;
-            dp_right->db_free -= len + INDEX_SIZE;
+            dp_right->db_free -= len +  (sizeof(unsigned)) ;
             dp_right->db_index[0] = dp_right->db_txt_start;
             if (flags & ML_APPEND_MARK)
             {
-                dp_right->db_index[0] |= DB_MARKED;
+                dp_right->db_index[0] |=  ((unsigned)1 << ((sizeof(unsigned) * 8) - 1)) ;
             }
 
-            mch_memmove((char *)dp_right + dp_right->db_txt_start, line, (size_t)len);
+             memmove((char *)((char *)dp_right + dp_right->db_txt_start), (char *)(line), (size_t)len) ;
             ++line_count_right;
         }
         if (lines_moved)
         {
             dp_right->db_txt_start -= data_moved;
             dp_right->db_free -= total_moved;
-            mch_memmove((char *)dp_right + dp_right->db_txt_start, (char *)dp_left + dp_left->db_txt_start, (size_t)data_moved);
+             memmove((char *)((char *)dp_right + dp_right->db_txt_start), (char *)((char *)dp_left + dp_left->db_txt_start), (size_t)data_moved) ;
             offset = dp_right->db_txt_start - dp_left->db_txt_start;
             dp_left->db_txt_start += data_moved;
             dp_left->db_free += total_moved;
@@ -89102,13 +87193,13 @@
         if (in_left)
         {
             dp_left->db_txt_start -= len;
-            dp_left->db_free -= len + INDEX_SIZE;
+            dp_left->db_free -= len +  (sizeof(unsigned)) ;
             dp_left->db_index[line_count_left] = dp_left->db_txt_start;
             if (flags & ML_APPEND_MARK)
             {
-                dp_left->db_index[line_count_left] |= DB_MARKED;
+                dp_left->db_index[line_count_left] |=  ((unsigned)1 << ((sizeof(unsigned) * 8) - 1)) ;
             }
-            mch_memmove((char *)dp_left + dp_left->db_txt_start, line, (size_t)len);
+             memmove((char *)((char *)dp_left + dp_left->db_txt_start), (char *)(line), (size_t)len) ;
             ++line_count_left;
         }
 
@@ -89155,7 +87246,7 @@
                 goto theend;
             }
             pp = (PTR_BL *)(hp->bh_data);
-            if (pp->pb_id != PTR_ID)
+            if (pp->pb_id !=  (('p' << 8) + 't') )
             {
                 iemsg(e_pointer_block_id_wrong_three);
                 mf_put(mfp, hp, FALSE, FALSE);
@@ -89165,7 +87256,7 @@
             {
                 if (pb_idx + 1 < (int)pp->pb_count)
                 {
-                    mch_memmove(&pp->pb_pointer[pb_idx + 2], &pp->pb_pointer[pb_idx + 1], (size_t)(pp->pb_count - pb_idx - 1) * sizeof(PTR_EN));
+                     memmove((char *)(&pp->pb_pointer[pb_idx + 2]), (char *)(&pp->pb_pointer[pb_idx + 1]), (size_t)(pp->pb_count - pb_idx - 1) * sizeof(PTR_EN)) ;
                 }
                 ++pp->pb_count;
                 pp->pb_pointer[pb_idx].pe_line_count = line_count_left;
@@ -89207,28 +87298,27 @@
                 }
                 pp_new = (PTR_BL *)(hp_new->bh_data);
 
-                if (hp->bh_bnum != 1)
+                if (hp-> bh_hashitem.mhi_key  != 1)
                 {
                     break;
                 }
 
-                mch_memmove(pp_new, pp, (size_t)page_size);
+                 memmove((char *)(pp_new), (char *)(pp), (size_t)page_size) ;
                 pp->pb_count = 1;
-                pp->pb_pointer[0].pe_bnum = hp_new->bh_bnum;
+                pp->pb_pointer[0].pe_bnum = hp_new-> bh_hashitem.mhi_key ;
                 pp->pb_pointer[0].pe_line_count = buf->b_ml.ml_line_count;
                 pp->pb_pointer[0].pe_old_lnum = 1;
                 pp->pb_pointer[0].pe_page_count = 1;
                 mf_put(mfp, hp, TRUE, FALSE);
                 hp = hp_new;
                 pp = pp_new;
-                CHECK(stack_idx != 0, _("stack_idx should be 0"));
                 ip->ip_index = 0;
                 ++stack_idx;
             }
             total_moved = pp->pb_count - pb_idx - 1;
             if (total_moved)
             {
-                mch_memmove(&pp_new->pb_pointer[0], &pp->pb_pointer[pb_idx + 1], (size_t)(total_moved) * sizeof(PTR_EN));
+                 memmove((char *)(&pp_new->pb_pointer[0]), (char *)(&pp->pb_pointer[pb_idx + 1]), (size_t)(total_moved) * sizeof(PTR_EN)) ;
                 pp_new->pb_count = total_moved;
                 pp->pb_count -= total_moved - 1;
                 pp->pb_pointer[pb_idx + 1].pe_bnum = bnum_right;
@@ -89268,8 +87358,8 @@
                 line_count_left += pp->pb_pointer[i].pe_line_count;
             }
 
-            bnum_left = hp->bh_bnum;
-            bnum_right = hp_new->bh_bnum;
+            bnum_left = hp-> bh_hashitem.mhi_key ;
+            bnum_right = hp_new-> bh_hashitem.mhi_key ;
             page_count_left = 1;
             page_count_right = 1;
             mf_put(mfp, hp, TRUE, FALSE);
@@ -89329,7 +87419,7 @@
 
     if (line != NULL)
     {
-        len = (colnr_T)STRLEN(line);
+        len = (colnr_T) strlen((char *)(line)) ;
     }
     return ml_replace_len(lnum, line, len, FALSE, copy);
 }
@@ -89409,7 +87499,7 @@
     {
         if ((flags & ML_DEL_MESSAGE))
         {
-            set_keep_msg((char_u *)_(no_lines_msg), 0);
+            set_keep_msg((char_u *)_((char *)no_lines_msg), 0);
         }
 
         i = ml_replace((linenr_T)1, (char_u *)"", TRUE);
@@ -89436,14 +87526,14 @@
 
     --buf->b_ml.ml_line_count;
 
-    line_start = ((dp->db_index[idx]) & DB_INDEX_MASK);
+    line_start = ((dp->db_index[idx]) &  (~ ((unsigned)1 << ((sizeof(unsigned) * 8) - 1)) ) );
     if (idx == 0)
     {
         line_size = dp->db_txt_end - line_start;
     }
     else
     {
-        line_size = ((dp->db_index[idx - 1]) & DB_INDEX_MASK) - line_start;
+        line_size = ((dp->db_index[idx - 1]) &  (~ ((unsigned)1 << ((sizeof(unsigned) * 8) - 1)) ) ) - line_start;
     }
 
     if (count == 1)
@@ -89461,7 +87551,7 @@
                 goto theend;
             }
             pp = (PTR_BL *)(hp->bh_data);
-            if (pp->pb_id != PTR_ID)
+            if (pp->pb_id !=  (('p' << 8) + 't') )
             {
                 iemsg(e_pointer_block_id_wrong_four);
                 mf_put(mfp, hp, FALSE, FALSE);
@@ -89476,7 +87566,7 @@
             {
                 if (count != idx)
                 {
-                    mch_memmove(&pp->pb_pointer[idx], &pp->pb_pointer[idx + 1], (size_t)(count - idx) * sizeof(PTR_EN));
+                     memmove((char *)(&pp->pb_pointer[idx]), (char *)(&pp->pb_pointer[idx + 1]), (size_t)(count - idx) * sizeof(PTR_EN)) ;
                 }
                 mf_put(mfp, hp, TRUE, FALSE);
 
@@ -89492,19 +87582,18 @@
                 break;
             }
         }
-        CHECK(stack_idx < 0, _("deleted block 1?"));
     }
     else
     {
         text_start = dp->db_txt_start;
-        mch_memmove((char *)dp + text_start + line_size, (char *)dp + text_start, (size_t)(line_start - text_start));
+         memmove((char *)((char *)dp + text_start + line_size), (char *)((char *)dp + text_start), (size_t)(line_start - text_start)) ;
 
         for (i = idx; i < count - 1; ++i)
         {
             dp->db_index[i] = dp->db_index[i + 1] + line_size;
         }
 
-        dp->db_free += line_size + INDEX_SIZE;
+        dp->db_free += line_size +  (sizeof(unsigned)) ;
         dp->db_txt_start += line_size;
         --(dp->db_line_count);
 
@@ -89556,7 +87645,7 @@
     }
 
     dp = (DATA_BL *)(hp->bh_data);
-    dp->db_index[lnum - curbuf->b_ml.ml_locked_low] |= DB_MARKED;
+    dp->db_index[lnum - curbuf->b_ml.ml_locked_low] |=  ((unsigned)1 << ((sizeof(unsigned) * 8) - 1)) ;
     curbuf->b_ml.ml_flags |= ML_LOCKED_DIRTY;
 }
 
@@ -89584,9 +87673,9 @@
 
         for (i = lnum - curbuf->b_ml.ml_locked_low; lnum <= curbuf->b_ml.ml_locked_high; ++i, ++lnum)
         {
-            if ((dp->db_index[i]) & DB_MARKED)
+            if ((dp->db_index[i]) &  ((unsigned)1 << ((sizeof(unsigned) * 8) - 1)) )
             {
-                (dp->db_index[i]) &= DB_INDEX_MASK;
+                (dp->db_index[i]) &=  (~ ((unsigned)1 << ((sizeof(unsigned) * 8) - 1)) ) ;
                 curbuf->b_ml.ml_flags |= ML_LOCKED_DIRTY;
                 lowest_marked = lnum + 1;
                 return lnum;
@@ -89621,9 +87710,9 @@
 
         for (i = lnum - curbuf->b_ml.ml_locked_low; lnum <= curbuf->b_ml.ml_locked_high; ++i, ++lnum)
         {
-            if ((dp->db_index[i]) & DB_MARKED)
+            if ((dp->db_index[i]) &  ((unsigned)1 << ((sizeof(unsigned) * 8) - 1)) )
             {
-                (dp->db_index[i]) &= DB_INDEX_MASK;
+                (dp->db_index[i]) &=  (~ ((unsigned)1 << ((sizeof(unsigned) * 8) - 1)) ) ;
                 curbuf->b_ml.ml_flags |= ML_LOCKED_DIRTY;
             }
         }
@@ -89674,7 +87763,7 @@
         {
             dp = (DATA_BL *)(hp->bh_data);
             idx = lnum - buf->b_ml.ml_locked_low;
-            start = ((dp->db_index[idx]) & DB_INDEX_MASK);
+            start = ((dp->db_index[idx]) &  (~ ((unsigned)1 << ((sizeof(unsigned) * 8) - 1)) ) );
             old_line = (char_u *)dp + start;
             if (idx == 0)
             {
@@ -89682,7 +87771,7 @@
             }
             else
             {
-                old_len = (dp->db_index[idx - 1] & DB_INDEX_MASK) - start;
+                old_len = (dp->db_index[idx - 1] &  (~ ((unsigned)1 << ((sizeof(unsigned) * 8) - 1)) ) ) - start;
             }
             new_len = buf->b_ml.ml_line_len;
             extra = new_len - old_len;
@@ -89692,7 +87781,7 @@
                 count = buf->b_ml.ml_locked_high - buf->b_ml.ml_locked_low + 1;
                 if (extra != 0 && idx < count - 1)
                 {
-                    mch_memmove((char *)dp + dp->db_txt_start - extra, (char *)dp + dp->db_txt_start, (size_t)(start - dp->db_txt_start));
+                     memmove((char *)((char *)dp + dp->db_txt_start - extra), (char *)((char *)dp + dp->db_txt_start), (size_t)(start - dp->db_txt_start)) ;
 
                     for (i = idx + 1; i < count; ++i)
                     {
@@ -89704,12 +87793,12 @@
                 dp->db_free -= extra;
                 dp->db_txt_start -= extra;
 
-                mch_memmove(old_line - extra, new_line, (size_t)new_len);
+                 memmove((char *)(old_line - extra), (char *)(new_line), (size_t)new_len) ;
                 buf->b_ml.ml_flags |= (ML_LOCKED_DIRTY | ML_LOCKED_POS);
             }
             else
             {
-                (void)ml_append_int(buf, lnum, new_line, new_len, ((dp->db_index[idx] & DB_MARKED) ? ML_APPEND_MARK : 0));
+                (void)ml_append_int(buf, lnum, new_line, new_len, ((dp->db_index[idx] &  ((unsigned)1 << ((sizeof(unsigned) * 8) - 1)) ) ? ML_APPEND_MARK : 0));
                 (void)ml_delete_int(buf, lnum, ML_DEL_NOPROP);
             }
         }
@@ -89738,9 +87827,9 @@
     }
 
     dp = (DATA_BL *)(hp->bh_data);
-    dp->db_id = DATA_ID;
+    dp->db_id =  (('d' << 8) + 'a') ;
     dp->db_txt_start = dp->db_txt_end = page_count * mfp->mf_page_size;
-    dp->db_free = dp->db_txt_start - HEADER_SIZE;
+    dp->db_free = dp->db_txt_start -  (offsetof(DATA_BL, db_index)) ;
     dp->db_line_count = 0;
 
     return hp;
@@ -89758,9 +87847,9 @@
     }
 
     pp = (PTR_BL *)(hp->bh_data);
-    pp->pb_id = PTR_ID;
+    pp->pb_id =  (('p' << 8) + 't') ;
     pp->pb_count = 0;
-    pp->pb_count_max = PB_COUNT_MAX(mfp);
+    pp->pb_count_max =  (short_u)(((mfp)->mf_page_size - offsetof(PTR_BL, pb_pointer)) / sizeof(PTR_EN)) ;
 
     return hp;
 }
@@ -89787,7 +87876,7 @@
 
     if (buf->b_ml.ml_locked)
     {
-        if (ML_SIMPLE(action) && buf->b_ml.ml_locked_low <= lnum && buf->b_ml.ml_locked_high >= lnum && !mf_dont_release)
+        if ( ((action) & 0x10)  && buf->b_ml.ml_locked_low <= lnum && buf->b_ml.ml_locked_high >= lnum && !mf_dont_release)
         {
             if (action == ML_INSERT)
             {
@@ -89862,7 +87951,7 @@
         }
 
         dp = (DATA_BL *)(hp->bh_data);
-        if (dp->db_id == DATA_ID)
+        if (dp->db_id ==  (('d' << 8) + 'a') )
         {
             buf->b_ml.ml_locked = hp;
             buf->b_ml.ml_locked_low = low;
@@ -89873,7 +87962,7 @@
         }
 
         pp = (PTR_BL *)(dp);
-        if (pp->pb_id != PTR_ID)
+        if (pp->pb_id !=  (('p' << 8) + 't') )
         {
             iemsg(e_pointer_block_id_wrong);
             goto error_block;
@@ -89893,7 +87982,6 @@
         for (idx = 0; idx < (int)pp->pb_count; ++idx)
         {
             t = pp->pb_pointer[idx].pe_line_count;
-            CHECK(t == 0, _("pe_line_count is zero"));
             if ((low += t) > lnum)
             {
                 ip->ip_index = idx;
@@ -89967,16 +88055,14 @@
 
     if (top == buf->b_ml.ml_stack_size)
     {
-        CHECK(top > 0, _("Stack size increases"));
-
-        newstack = ALLOC_MULT(infoptr_T, buf->b_ml.ml_stack_size + STACK_INCR);
+        newstack =  (infoptr_T *)alloc(sizeof(infoptr_T) * (buf->b_ml.ml_stack_size + STACK_INCR)) ;
         if (newstack == NULL)
         {
             return -1;
         }
         if (top > 0)
         {
-            mch_memmove(newstack, buf->b_ml.ml_stack, (size_t)top * sizeof(infoptr_T));
+             memmove((char *)(newstack), (char *)(buf->b_ml.ml_stack), (size_t)top * sizeof(infoptr_T)) ;
         }
         vim_free(buf->b_ml.ml_stack);
         buf->b_ml.ml_stack = newstack;
@@ -90004,7 +88090,7 @@
             break;
         }
         pp = (PTR_BL *)(hp->bh_data);
-        if (pp->pb_id != PTR_ID)
+        if (pp->pb_id !=  (('p' << 8) + 't') )
         {
             mf_put(mfp, hp, FALSE, FALSE);
             iemsg(e_pointer_block_id_wrong_two);
@@ -90019,7 +88105,7 @@
     int
 resolve_symlink(char_u *fname, char_u *buf)
 {
-    char_u      tmp[MAXPATHL];
+    char_u      tmp[ PATH_MAX ];
     int         ret;
     int         depth = 0;
 
@@ -90028,7 +88114,7 @@
         return FAIL;
     }
 
-    vim_strncpy(tmp, fname, MAXPATHL - 1);
+    vim_strncpy(tmp, fname,  PATH_MAX  - 1);
 
     for (;;)
     {
@@ -90038,7 +88124,7 @@
             return FAIL;
         }
 
-        ret = readlink((char *)tmp, (char *)buf, MAXPATHL - 1);
+        ret = readlink((char *)tmp, (char *)buf,  PATH_MAX  - 1);
         if (ret <= 0)
         {
             if (errno == EINVAL || errno == ENOENT)
@@ -90057,38 +88143,38 @@
 
         if (mch_isFullName(buf))
         {
-            STRCPY(tmp, buf);
+             strcpy((char *)(tmp), (char *)(buf)) ;
         }
         else
         {
             char_u *tail;
 
             tail = gettail(tmp);
-            if (STRLEN(tail) + STRLEN(buf) >= MAXPATHL)
+            if ( strlen((char *)(tail))  +  strlen((char *)(buf))  >=  PATH_MAX )
             {
                 return FAIL;
             }
-            STRCPY(tail, buf);
+             strcpy((char *)(tail), (char *)(buf)) ;
         }
     }
 
-    return vim_FullName(tmp, buf, MAXPATHL, TRUE);
+    return vim_FullName(tmp, buf,  PATH_MAX , TRUE);
 }
 
     char_u *
-makeswapname(char_u      *fname, char_u      *ffname UNUSED, buf_T       *buf, char_u      *dir_name)
+makeswapname(char_u      *fname, char_u      *ffname  __attribute__((unused)) , buf_T       *buf, char_u      *dir_name)
 {
     char_u *r;
     char_u *s;
     char_u      *fname_res = fname;
-    char_u      fname_buf[MAXPATHL];
+    char_u      fname_buf[ PATH_MAX ];
 
     if (resolve_symlink(fname, fname_buf) == OK)
     {
         fname_res = fname_buf;
     }
 
-    int         len = (int)STRLEN(dir_name);
+    int         len = (int) strlen((char *)(dir_name)) ;
 
     s = dir_name + len;
     if (after_pathsep(dir_name, s) && len > 1 && s[-1] == s[-2])
@@ -90120,7 +88206,7 @@
     string_T    retval;
 
     tail.string = gettail(fname);
-    tail.length = STRLEN(tail.string);
+    tail.length =  strlen((char *)(tail.string)) ;
 
     if (dname[0] == '.' && dname[1] == NUL)
     {
@@ -90129,7 +88215,7 @@
     }
     else
     {
-        size_t  dname_len = STRLEN(dname);
+        size_t  dname_len =  strlen((char *)(dname)) ;
 
         if (dname[0] == '.' && vim_ispathsep(dname[1]))
         {
@@ -90181,7 +88267,7 @@
     msg_puts(_("While opening file \""));
     msg_outtrans(buf->b_fname);
     msg_puts("\"\n");
-    if (mch_stat((char *)buf->b_fname, &st) == -1)
+    if ( stat(((char *)buf->b_fname), (&st))  == -1)
     {
         msg_puts(_("      CANNOT BE FOUND"));
     }
@@ -90225,7 +88311,7 @@
     int         r;
     char_u      *buf_fname = buf->b_fname;
 
-    dir_name = alloc(STRLEN(*dirp) + 1);
+    dir_name = alloc( strlen((char *)(*dirp))  + 1);
     if (dir_name == NULL)
     {
         *dirp = NULL;
@@ -90250,9 +88336,10 @@
         {
             break;
         }
-        if ((n = (int)STRLEN(fname)) == 0)
+        if ((n = (int) strlen((char *)(fname)) ) == 0)
         {
-            VIM_CLEAR(fname);
+             vim_free(fname);
+             (fname) = NULL;
             break;
         }
         if (fname[n - 2] == 'w' && fname[n - 1] == 'p' && !(buf->b_p_sn || buf->b_shortname))
@@ -90268,12 +88355,12 @@
             int             same = FALSE;
 
             tail = gettail(buf_fname);
-            if (       vim_strchr(tail, '.') != NULL || STRLEN(tail) > (size_t)8 || *gettail(fname) == '.')
+            if (       vim_strchr(tail, '.') != NULL ||  strlen((char *)(tail))  > (size_t)8 || *gettail(fname) == '.')
             {
                 fname2 = alloc(n + 2);
                 if (fname2 != NULL)
                 {
-                    STRCPY(fname2, fname);
+                     strcpy((char *)(fname2), (char *)(fname)) ;
                     if (vim_strchr(tail, '.') != NULL)
                     {
                         fname2[n - 1] = 'x';
@@ -90287,36 +88374,36 @@
                     {
                         fname2[n - 5] += 1;
                     }
-                    f1 = mch_open((char *)fname, O_RDONLY | O_EXTRA, 0);
+                    f1 =  open(((char *)fname), (O_RDONLY | O_EXTRA), (0)) ;
                     if (f1 < 0)
                     {
-                        f1 = mch_open_rw((char *)fname, O_RDWR|O_CREAT|O_EXCL|O_EXTRA);
+                        f1 =   open((((char *)fname)), ((O_RDWR|O_CREAT|O_EXCL|O_EXTRA)), ((mode_t)0600))  ;
                         created1 = TRUE;
                     }
                     if (f1 >= 0)
                     {
-                        f2 = mch_open((char *)fname2, O_RDONLY | O_EXTRA, 0);
+                        f2 =  open(((char *)fname2), (O_RDONLY | O_EXTRA), (0)) ;
                         if (f2 < 0)
                         {
-                            f2 = mch_open_rw((char *)fname2, O_RDWR|O_CREAT|O_EXCL|O_EXTRA);
+                            f2 =   open((((char *)fname2)), ((O_RDWR|O_CREAT|O_EXCL|O_EXTRA)), ((mode_t)0600))  ;
                             created2 = TRUE;
                         }
                         if (f2 >= 0)
                         {
-                            if (mch_fstat(f1, &s1) != -1 && mch_fstat(f2, &s2) != -1 && s1.st_dev == s2.st_dev && s1.st_ino == s2.st_ino)
+                            if ( fstat((f1), (&s1))  != -1 &&  fstat((f2), (&s2))  != -1 && s1.st_dev == s2.st_dev && s1.st_ino == s2.st_ino)
                             {
                                 same = TRUE;
                             }
                             close(f2);
                             if (created2)
                             {
-                                mch_remove(fname2);
+                                 unlink((char *)(fname2)) ;
                             }
                         }
                         close(f1);
                         if (created1)
                         {
-                            mch_remove(fname);
+                             unlink((char *)(fname)) ;
                         }
                     }
                     vim_free(fname2);
@@ -90334,13 +88421,13 @@
         {
             stat_T      sb;
 
-            if (mch_lstat((char *)fname, &sb) < 0)
+            if ( lstat(((char *)fname), (&sb))  < 0)
             {
                 break;
             }
         }
 
-        if (old_fname != NULL && fnamecmp(fname, old_fname) == 0)
+        if (old_fname != NULL &&  vim_fnamecmp((char_u *)(fname), (char_u *)(old_fname))  == 0)
         {
             break;
         }
@@ -90366,16 +88453,16 @@
                 struct block0   b0;
                 int             differ = FALSE;
 
-                fd = mch_open((char *)fname, O_RDONLY | O_EXTRA, 0);
+                fd =  open(((char *)fname), (O_RDONLY | O_EXTRA), (0)) ;
                 if (fd >= 0)
                 {
                     if (read_eintr(fd, &b0, sizeof(b0)) == sizeof(b0))
                     {
-                        if (b0.b0_flags & B0_SAME_DIR)
+                        if (b0. b0_fname[B0_FNAME_SIZE_ORG - 2]  & B0_SAME_DIR)
                         {
-                            if (fnamecmp(gettail(buf->b_ffname), gettail(b0.b0_fname)) != 0 || !same_directory(fname, buf->b_ffname))
+                            if ( vim_fnamecmp((char_u *)(gettail(buf->b_ffname)), (char_u *)(gettail(b0.b0_fname)))  != 0 || !same_directory(fname, buf->b_ffname))
                             {
-                                expand_env(b0.b0_fname, NameBuff, MAXPATHL);
+                                expand_env(b0.b0_fname, NameBuff,  PATH_MAX );
                                 if (fnamecmp_ino(buf->b_ffname, NameBuff, char_to_long(b0.b0_ino)))
                                 {
                                     differ = TRUE;
@@ -90384,7 +88471,7 @@
                         }
                         else
                         {
-                            expand_env(b0.b0_fname, NameBuff, MAXPATHL);
+                            expand_env(b0.b0_fname, NameBuff,  PATH_MAX );
                             if (fnamecmp_ino(buf->b_ffname, NameBuff, char_to_long(b0.b0_ino)))
                             {
                                 differ = TRUE;
@@ -90399,7 +88486,7 @@
                     sea_choice_T choice = SEA_CHOICE_NONE;
                     stat_T       st;
 
-                    if (mch_stat((char *)buf->b_fname, &st) == 0 && swapfile_unchanged(fname))
+                    if ( stat(((char *)buf->b_fname), (&st))  == 0 && swapfile_unchanged(fname))
                     {
                         choice = SEA_CHOICE_DELETE;
                         if (p_verbose > 0)
@@ -90433,7 +88520,7 @@
                             swap_exists_action = SEA_RECOVER;
                             break;
                         case SEA_CHOICE_DELETE:
-                            mch_remove(fname);
+                             unlink((char *)(fname)) ;
                             break;
                         case SEA_CHOICE_QUIT:
                             swap_exists_action = SEA_QUIT;
@@ -90465,7 +88552,8 @@
             if (fname[n - 2] == 'a')
             {
                 emsg(_(e_too_many_swap_files_found));
-                VIM_CLEAR(fname);
+                 vim_free(fname);
+                 (fname) = NULL;
                 break;
             }
             --fname[n - 2];
@@ -90490,17 +88578,17 @@
     stat_T      st;
     ino_t       ino_c = 0;
     ino_t       ino_s;
-    char_u      buf_c[MAXPATHL];
-    char_u      buf_s[MAXPATHL];
+    char_u      buf_c[ PATH_MAX ];
+    char_u      buf_s[ PATH_MAX ];
     int         retval_c;
     int         retval_s;
 
-    if (mch_stat((char *)fname_c, &st) == 0)
+    if ( stat(((char *)fname_c), (&st))  == 0)
     {
         ino_c = (ino_t)st.st_ino;
     }
 
-    if (mch_stat((char *)fname_s, &st) == 0)
+    if ( stat(((char *)fname_s), (&st))  == 0)
     {
         ino_s = (ino_t)st.st_ino;
     }
@@ -90514,16 +88602,16 @@
         return (ino_c != ino_s);
     }
 
-    retval_c = vim_FullName(fname_c, buf_c, MAXPATHL, TRUE);
-    retval_s = vim_FullName(fname_s, buf_s, MAXPATHL, TRUE);
+    retval_c = vim_FullName(fname_c, buf_c,  PATH_MAX , TRUE);
+    retval_s = vim_FullName(fname_s, buf_s,  PATH_MAX , TRUE);
     if (retval_c == OK && retval_s == OK)
     {
-        return STRCMP(buf_c, buf_s) != 0;
+        return  strcmp((char *)(buf_c), (char *)(buf_s))  != 0;
     }
 
     if (ino_s == 0 && ino_c == 0 && retval_c == FAIL && retval_s == FAIL)
     {
-        return STRCMP(fname_c, fname_s) != 0;
+        return  strcmp((char *)(fname_c), (char *)(fname_s))  != 0;
     }
     return TRUE;
 }
@@ -90568,11 +88656,11 @@
     }
     for (hp = buf->b_ml.ml_mfp->mf_used_last; hp != NULL; hp = hp->bh_prev)
     {
-        if (hp->bh_bnum == 0)
+        if (hp-> bh_hashitem.mhi_key  == 0)
         {
             b0p = (ZERO_BL *)(hp->bh_data);
-            b0p->b0_dirty = buf->b_changed ? B0_DIRTY : 0;
-            b0p->b0_flags = (b0p->b0_flags & ~B0_FF_MASK)
+            b0p-> b0_fname[B0_FNAME_SIZE_ORG - 1]  = buf->b_changed ? B0_DIRTY : 0;
+            b0p-> b0_fname[B0_FNAME_SIZE_ORG - 2]  = (b0p-> b0_fname[B0_FNAME_SIZE_ORG - 2]  & ~B0_FF_MASK)
                                                   | (get_fileformat(buf) + 1);
             add_b0_fenc(b0p, buf);
             hp->bh_flags |= BH_DIRTY;
@@ -90584,8 +88672,6 @@
 
 // ==================== message.c ====================
 
-#define MESSAGE_FILE
-
 static void add_msg_hist(char_u *s, int len, int attr);
 static void check_msg_hist(void);
 static void hit_return_msg(void);
@@ -90615,13 +88701,9 @@
 static int msg_hist_len = 0;
 static int msg_hist_max = 500;
 
-#define MESSAGES_HIT_ENTER      0x001
-#define MESSAGES_WAIT           0x002
-#define MESSAGES_HISTORY        0x004
-
-#define MESSAGES_OPT_HIT_ENTER "hit-enter"
-#define MESSAGES_OPT_WAIT "wait:"
-#define MESSAGES_OPT_HISTORY "history:"
+enum { MESSAGES_HIT_ENTER = 0x001 };
+enum { MESSAGES_WAIT = 0x002 };
+enum { MESSAGES_HISTORY = 0x004 };
 
 static int msg_flags = MESSAGES_HIT_ENTER | MESSAGES_HISTORY;
 static int msg_wait = 0;
@@ -90671,7 +88753,7 @@
     }
     ++entered;
 
-    if ((char_u *)s != keep_msg || (*s != '<' && last_msg_hist != NULL && last_msg_hist->msg != NULL && STRCMP(s, last_msg_hist->msg) != 0))
+    if ((char_u *)s != keep_msg || (*s != '<' && last_msg_hist != NULL && last_msg_hist->msg != NULL &&  strcmp((char *)(s), (char *)(last_msg_hist->msg))  != 0))
     {
         add_msg_hist((char_u *)s, -1, attr);
     }
@@ -90805,7 +88887,7 @@
     }
     else if (enc_utf8)
     {
-        half = i = (int)STRLEN(s);
+        half = i = (int) strlen((char *)(s)) ;
         for (;;)
         {
             do
@@ -90825,7 +88907,7 @@
     }
     else
     {
-        for (i = (int)STRLEN(s); i - 1 >= 0 && len + (n = ptr2cells(s + i - 1)) <= room; --i)
+        for (i = (int) strlen((char *)(s)) ; i - 1 >= 0 && len + (n = ptr2cells(s + i - 1)) <= room; --i)
         {
             len += n;
         }
@@ -90835,7 +88917,7 @@
     {
         if (s != buf)
         {
-            len = STRLEN(s);
+            len =  strlen((char *)(s)) ;
             if (len >= (size_t)buflen)
             {
                 len = buflen - 1;
@@ -90847,19 +88929,19 @@
             }
             else
             {
-                mch_memmove(buf + e, s + e, len);
+                 memmove((char *)(buf + e), (char *)(s + e), len) ;
             }
         }
     }
     else if (e + 3 < buflen)
     {
-        mch_memmove(buf + e, "...", (size_t)3);
-        len = STRLEN(s + i) + 1;
+         memmove((char *)(buf + e), (char *)("..."), (size_t)3) ;
+        len =  strlen((char *)(s + i))  + 1;
         if (len >= (size_t)buflen - e - 3)
         {
             len = buflen - e - 3 - 1;
         }
-        mch_memmove(buf + e + 3, s + i, len);
+         memmove((char *)(buf + e + 3), (char *)(s + i), len) ;
         buf[e + 3 + len - 1] = NUL;
     }
     else
@@ -90881,7 +88963,7 @@
     va_list arglist;
 
     va_start(arglist, s);
-    vim_vsnprintf((char *)IObuff, IOSIZE, s, arglist);
+    vim_vsnprintf((char *)IObuff,  (1024+1) , s, arglist);
     va_end(arglist);
     return msg((char *)IObuff);
 }
@@ -90897,7 +88979,7 @@
     va_list arglist;
 
     va_start(arglist, s);
-    vim_vsnprintf((char *)IObuff, IOSIZE, s, arglist);
+    vim_vsnprintf((char *)IObuff,  (1024+1) , s, arglist);
     va_end(arglist);
     return msg_attr((char *)IObuff, attr);
 }
@@ -90913,7 +88995,7 @@
     va_list arglist;
 
     va_start(arglist, s);
-    vim_vsnprintf((char *)IObuff, IOSIZE, s, arglist);
+    vim_vsnprintf((char *)IObuff,  (1024+1) , s, arglist);
     va_end(arglist);
     return msg_attr_keep((char *)IObuff, attr, TRUE);
 }
@@ -90924,18 +89006,19 @@
     void
 reset_last_sourcing(void)
 {
-    VIM_CLEAR(last_sourcing_name);
+     vim_free(last_sourcing_name);
+     (last_sourcing_name) = NULL;
     last_sourcing_lnum = 0;
 }
 
     static int
 other_sourcing_name(void)
 {
-    if (HAVE_SOURCING_INFO && SOURCING_NAME != NULL)
+    if ( (exestack.ga_data != NULL && exestack.ga_len > 0)  &&  (((estack_T *)exestack.ga_data)[exestack.ga_len - 1].es_name)  != NULL)
     {
         if (last_sourcing_name != NULL)
         {
-            return STRCMP(SOURCING_NAME, last_sourcing_name) != 0;
+            return  strcmp((char *)( (((estack_T *)exestack.ga_data)[exestack.ga_len - 1].es_name) ), (char *)(last_sourcing_name))  != 0;
         }
         return TRUE;
     }
@@ -90948,18 +89031,18 @@
     char_u *Buf;
     char_u *p;
 
-    if (HAVE_SOURCING_INFO && SOURCING_NAME != NULL && other_sourcing_name())
+    if ( (exestack.ga_data != NULL && exestack.ga_len > 0)  &&  (((estack_T *)exestack.ga_data)[exestack.ga_len - 1].es_name)  != NULL && other_sourcing_name())
     {
         char_u      *sname = estack_sfile(ESTACK_NONE);
         char_u      *tofree = sname;
 
         if (sname == NULL)
         {
-            sname = SOURCING_NAME;
+            sname =  (((estack_T *)exestack.ga_data)[exestack.ga_len - 1].es_name) ;
         }
 
             p = (char_u *)_("Error detected while processing %s:");
-        Buf = alloc(STRLEN(sname) + STRLEN(p));
+        Buf = alloc( strlen((char *)(sname))  +  strlen((char *)(p)) );
         if (Buf != NULL)
         {
             sprintf((char *)Buf, (char *)p, sname);
@@ -90976,13 +89059,13 @@
     char_u *Buf;
     char_u *p;
 
-    if (SOURCING_NAME != NULL && (other_sourcing_name() || SOURCING_LNUM != last_sourcing_lnum) && SOURCING_LNUM != 0)
+    if ( (((estack_T *)exestack.ga_data)[exestack.ga_len - 1].es_name)  != NULL && (other_sourcing_name() ||  (((estack_T *)exestack.ga_data)[exestack.ga_len - 1].es_lnum)  != last_sourcing_lnum) &&  (((estack_T *)exestack.ga_data)[exestack.ga_len - 1].es_lnum)  != 0)
     {
         p = (char_u *)_("line %4ld:");
-        Buf = alloc(STRLEN(p) + 20);
+        Buf = alloc( strlen((char *)(p))  + 20);
         if (Buf != NULL)
         {
-            sprintf((char *)Buf, (char *)p, (long)SOURCING_LNUM);
+            sprintf((char *)Buf, (char *)p, (long) (((estack_T *)exestack.ga_data)[exestack.ga_len - 1].es_lnum) );
         }
         return Buf;
     }
@@ -91012,17 +89095,18 @@
     p = get_emsg_lnum();
     if (p != NULL)
     {
-        msg_attr((char *)p, HL_ATTR(HLF_N));
+        msg_attr((char *)p,  highlight_attr[(int)(HLF_N)] );
         vim_free(p);
-        last_sourcing_lnum = SOURCING_LNUM;
+        last_sourcing_lnum =  (((estack_T *)exestack.ga_data)[exestack.ga_len - 1].es_lnum) ;
     }
 
-    if (SOURCING_NAME == NULL || other_sourcing_name())
+    if ( (((estack_T *)exestack.ga_data)[exestack.ga_len - 1].es_name)  == NULL || other_sourcing_name())
     {
-        VIM_CLEAR(last_sourcing_name);
-        if (SOURCING_NAME != NULL)
+         vim_free(last_sourcing_name);
+         (last_sourcing_name) = NULL;
+        if ( (((estack_T *)exestack.ga_data)[exestack.ga_len - 1].es_name)  != NULL)
         {
-            last_sourcing_name = vim_strsave(SOURCING_NAME);
+            last_sourcing_name = vim_strsave( (((estack_T *)exestack.ga_data)[exestack.ga_len - 1].es_name) );
         }
     }
     --no_wait_return;
@@ -91051,7 +89135,6 @@
 
     if (!emsg_off || vim_strchr(p_debug, 't') != NULL)
     {
-
         if (emsg_silent != 0)
         {
             if (emsg_noredir == 0)
@@ -91060,14 +89143,14 @@
                 p = get_emsg_source();
                 if (p != NULL)
                 {
-                    STRCAT(p, "\n");
+                     strcat((char *)(p), (char *)("\n")) ;
                     redir_write(p, -1);
                     vim_free(p);
                 }
                 p = get_emsg_lnum();
                 if (p != NULL)
                 {
-                    STRCAT(p, "\n");
+                     strcat((char *)(p), (char *)("\n")) ;
                     redir_write(p, -1);
                     vim_free(p);
                 }
@@ -91099,7 +89182,7 @@
 
         emsg_on_display = TRUE;
 
-    attr = HL_ATTR(HLF_E);
+    attr =  highlight_attr[(int)(HLF_E)] ;
     if (msg_scrolled != 0)
     {
         need_wait_return = TRUE;
@@ -91141,7 +89224,7 @@
     va_list ap;
 
     va_start(ap, s);
-    vim_vsnprintf((char *)IObuff, IOSIZE, s, ap);
+    vim_vsnprintf((char *)IObuff,  (1024+1) , s, ap);
     va_end(ap);
     return emsg_core((char *)IObuff);
 }
@@ -91178,7 +89261,7 @@
         va_list ap;
 
         va_start(ap, s);
-        vim_vsnprintf((char *)IObuff, IOSIZE, s, ap);
+        vim_vsnprintf((char *)IObuff,  (1024+1) , s, ap);
         va_end(ap);
         emsg_core((char *)IObuff);
     }
@@ -91226,7 +89309,7 @@
     int         room;
 
     room = (int)(Rows - cmdline_row - 1) * cmdline_width + sc_col - 1;
-    if (room > 0 && (force || (shortmess(SHM_TRUNC) && !exmode_active)) && (n = (int)STRLEN(s) - room) > 0)
+    if (room > 0 && (force || (shortmess(SHM_TRUNC) && !exmode_active)) && (n = (int) strlen((char *)(s))  - room) > 0)
     {
         if (has_mbyte)
         {
@@ -91260,7 +89343,7 @@
         return;
     }
 
-    p = ALLOC_ONE(struct msg_hist);
+    p =  (struct msg_hist *)alloc(sizeof(struct msg_hist)) ;
     if (p == NULL)
     {
         return;
@@ -91268,7 +89351,7 @@
 
     if (len < 0)
     {
-        len = (int)STRLEN(s);
+        len = (int) strlen((char *)(s)) ;
     }
     while (len > 0 && *s == '\n')
     {
@@ -91337,20 +89420,20 @@
     p = p_mopt;
     while (*p != NUL)
     {
-        if (STRNCMP(p, MESSAGES_OPT_HIT_ENTER, STRLEN_LITERAL(MESSAGES_OPT_HIT_ENTER)) == 0)
+        if ( strncmp((char *)(p), (char *)( "hit-enter" ), ( (sizeof( "hit-enter"  "") - 1) ))  == 0)
         {
-            p += STRLEN_LITERAL(MESSAGES_OPT_HIT_ENTER);
+            p +=  (sizeof( "hit-enter"  "") - 1) ;
             messages_flags_new |= MESSAGES_HIT_ENTER;
         }
-        else if (STRNCMP(p, MESSAGES_OPT_WAIT, STRLEN_LITERAL(MESSAGES_OPT_WAIT)) == 0 && VIM_ISDIGIT(p[STRLEN_LITERAL(MESSAGES_OPT_WAIT)]))
+        else if ( strncmp((char *)(p), (char *)( "wait:" ), ( (sizeof( "wait:"  "") - 1) ))  == 0 &&  ((unsigned)(p[ (sizeof( "wait:"  "") - 1) ]) - '0' < 10) )
         {
-            p += STRLEN_LITERAL(MESSAGES_OPT_WAIT);
+            p +=  (sizeof( "wait:"  "") - 1) ;
             messages_wait_new = getdigits(&p);
             messages_flags_new |= MESSAGES_WAIT;
         }
-        else if (STRNCMP(p, MESSAGES_OPT_HISTORY, STRLEN_LITERAL(MESSAGES_OPT_HISTORY)) == 0 && VIM_ISDIGIT(p[STRLEN_LITERAL(MESSAGES_OPT_HISTORY)]))
+        else if ( strncmp((char *)(p), (char *)( "history:" ), ( (sizeof( "history:"  "") - 1) ))  == 0 &&  ((unsigned)(p[ (sizeof( "history:"  "") - 1) ]) - '0' < 10) )
         {
-            p += STRLEN_LITERAL(MESSAGES_OPT_HISTORY);
+            p +=  (sizeof( "history:"  "") - 1) ;
             messages_history_new = getdigits(&p);
             messages_flags_new |= MESSAGES_HISTORY;
         }
@@ -91401,7 +89484,7 @@
     char_u          *s;
     int             c = 0;
 
-    if (STRCMP(eap->arg, "clear") == 0)
+    if ( strcmp((char *)(eap->arg), (char *)("clear"))  == 0)
     {
         int keep = eap->addr_count == 0 ? 0 : eap->line2;
 
@@ -91438,10 +89521,10 @@
 
     if (p == first_msg_hist)
     {
-        s = mch_getenv((char_u *)"LANG");
+        s =  (char_u *)getenv((char *)((char_u *)"LANG")) ;
         if (s != NULL && *s != NUL)
         {
-            msg_attr(_("Messages maintainer: The Vim Project"), HL_ATTR(HLF_T));
+            msg_attr(_("Messages maintainer: The Vim Project"),  highlight_attr[(int)(HLF_T)] );
         }
     }
 
@@ -91512,7 +89595,7 @@
     {
         screenalloc(FALSE);
 
-        State = MODE_HITRETURN;
+        State =  (0x2000 | MODE_NORMAL) ;
         setmouse();
         cmdline_row = msg_row;
 
@@ -91548,7 +89631,7 @@
 
                 if (KeyTyped && p_more && !p_cp)
                 {
-                    if (c == 'b' || c == Ctrl_B || c == 'k' || c == 'u' || c == 'g' || c == K_UP || c == K_PAGEUP)
+                    if (c == 'b' || c == Ctrl_B || c == 'k' || c == 'u' || c == 'g' || c ==   (-(('k') + ((int)('u') << 8)))   || c ==   (-(('k') + ((int)('P') << 8)))  )
                     {
                         if (msg_scrolled > Rows)
                         {
@@ -91557,7 +89640,7 @@
                         else
                         {
                             msg_didout = FALSE;
-                            c = K_IGNORE;
+                            c =   (-((KS_EXTRA) + ((int)(KE_IGNORE) << 8)))  ;
                             msg_col =
                                 0;
                         }
@@ -91567,21 +89650,21 @@
                             quit_more = FALSE;
                             got_int = FALSE;
                         }
-                        else if (c != K_IGNORE)
+                        else if (c !=   (-((KS_EXTRA) + ((int)(KE_IGNORE) << 8)))  )
                         {
-                            c = K_IGNORE;
+                            c =   (-((KS_EXTRA) + ((int)(KE_IGNORE) << 8)))  ;
                             hit_return_msg();
                         }
                     }
-                    else if (msg_scrolled > Rows - 2 && (c == 'j' || c == 'd' || c == 'f' || c == Ctrl_F || c == K_DOWN || c == K_PAGEDOWN))
+                    else if (msg_scrolled > Rows - 2 && (c == 'j' || c == 'd' || c == 'f' || c == Ctrl_F || c ==   (-(('k') + ((int)('d') << 8)))   || c ==   (-(('k') + ((int)('N') << 8)))  ))
                     {
-                        c = K_IGNORE;
+                        c =   (-((KS_EXTRA) + ((int)(KE_IGNORE) << 8)))  ;
                     }
                 }
-            } while ((had_got_int && c == Ctrl_C) || c == K_IGNORE || c == K_LEFTDRAG   || c == K_LEFTRELEASE || c == K_MIDDLEDRAG || c == K_MIDDLERELEASE || c == K_RIGHTDRAG  || c == K_RIGHTRELEASE || c == K_MOUSELEFT  || c == K_MOUSERIGHT || c == K_MOUSEDOWN  || c == K_MOUSEUP || c == K_MOUSEMOVE || (!mouse_has(MOUSE_RETURN) && mouse_row < msg_row && (c == K_LEFTMOUSE || c == K_MIDDLEMOUSE || c == K_RIGHTMOUSE || c == K_X1MOUSE || c == K_X2MOUSE)));
+            } while ((had_got_int && c == Ctrl_C) || c ==   (-((KS_EXTRA) + ((int)(KE_IGNORE) << 8)))   || c ==   (-((KS_EXTRA) + ((int)(KE_LEFTDRAG) << 8)))     || c ==   (-((KS_EXTRA) + ((int)(KE_LEFTRELEASE) << 8)))   || c ==   (-((KS_EXTRA) + ((int)(KE_MIDDLEDRAG) << 8)))   || c ==   (-((KS_EXTRA) + ((int)(KE_MIDDLERELEASE) << 8)))   || c ==   (-((KS_EXTRA) + ((int)(KE_RIGHTDRAG) << 8)))    || c ==   (-((KS_EXTRA) + ((int)(KE_RIGHTRELEASE) << 8)))   || c ==   (-((KS_EXTRA) + ((int)(KE_MOUSELEFT) << 8)))    || c ==   (-((KS_EXTRA) + ((int)(KE_MOUSERIGHT) << 8)))   || c ==   (-((KS_EXTRA) + ((int)(KE_MOUSEDOWN) << 8)))    || c ==   (-((KS_EXTRA) + ((int)(KE_MOUSEUP) << 8)))   || c ==   (-((KS_EXTRA) + ((int)(KE_MOUSEMOVE) << 8)))   || (!mouse_has(MOUSE_RETURN) && mouse_row < msg_row && (c ==   (-((KS_EXTRA) + ((int)(KE_LEFTMOUSE) << 8)))   || c ==   (-((KS_EXTRA) + ((int)(KE_MIDDLEMOUSE) << 8)))   || c ==   (-((KS_EXTRA) + ((int)(KE_RIGHTMOUSE) << 8)))   || c ==   (-((KS_EXTRA) + ((int)(KE_X1MOUSE) << 8)))   || c ==   (-((KS_EXTRA) + ((int)(KE_X2MOUSE) << 8)))  )));
             ui_breakcheck();
 
-            if (c == K_LEFTMOUSE || c == K_MIDDLEMOUSE || c == K_RIGHTMOUSE || c == K_X1MOUSE || c == K_X2MOUSE)
+            if (c ==   (-((KS_EXTRA) + ((int)(KE_LEFTMOUSE) << 8)))   || c ==   (-((KS_EXTRA) + ((int)(KE_MIDDLEMOUSE) << 8)))   || c ==   (-((KS_EXTRA) + ((int)(KE_RIGHTMOUSE) << 8)))   || c ==   (-((KS_EXTRA) + ((int)(KE_X1MOUSE) << 8)))   || c ==   (-((KS_EXTRA) + ((int)(KE_X2MOUSE) << 8)))  )
             {
                 (void)jump_to_mouse(MOUSE_SETPOS, NULL, 0);
             }
@@ -91626,7 +89709,8 @@
     reset_last_sourcing();
     if (keep_msg != NULL && vim_strsize(keep_msg) >= (Rows - cmdline_row - 1) * cmdline_width + sc_col)
     {
-        VIM_CLEAR(keep_msg);
+         vim_free(keep_msg);
+         (keep_msg) = NULL;
     }
 
     if (tmpState == MODE_SETWSIZE)
@@ -91656,7 +89740,7 @@
         msg_puts(_("Interrupt: "));
     }
 
-    msg_puts_attr(_("Press ENTER or type command to continue"), HL_ATTR(HLF_R));
+    msg_puts_attr(_("Press ENTER or type command to continue"),  highlight_attr[(int)(HLF_R)] );
     if (!msg_use_printf())
     {
         msg_clr_eos();
@@ -91701,7 +89785,8 @@
 
     if (!msg_silent)
     {
-        VIM_CLEAR(keep_msg);
+         vim_free(keep_msg);
+         (keep_msg) = NULL;
         need_fileinfo = FALSE;
     }
 
@@ -91754,11 +89839,11 @@
 {
     char_u      buf[MB_MAXBYTES + 1];
 
-    if (IS_SPECIAL(c))
+    if ( ((c) < 0) )
     {
-        buf[0] = K_SPECIAL;
-        buf[1] = K_SECOND(c);
-        buf[2] = K_THIRD(c);
+        buf[0] =  (0x80) ;
+        buf[1] =  ((c) ==  (0x80)  ? KS_SPECIAL : (c) == NUL ? KS_ZERO :  ((-(c)) & 0xff) ) ;
+        buf[2] =  (((c) ==  (0x80)  || (c) == NUL) ?  ('X')  :  (((unsigned)(-(c)) >> 8) & 0xff) ) ;
         buf[3] = NUL;
     }
     else
@@ -91805,7 +89890,7 @@
     int
 msg_outtrans_attr(char_u *str, int attr)
 {
-    return msg_outtrans_len_attr(str, (int)STRLEN(str), attr);
+    return msg_outtrans_len_attr(str, (int) strlen((char *)(str)) , attr);
 }
 
     int
@@ -91843,7 +89928,7 @@
 
     if (attr == 0)
     {
-        attr = HL_ATTR(HLF_MSG);
+        attr =  highlight_attr[(int)(HLF_MSG)] ;
     }
 
     if (attr & MSG_HIST)
@@ -91891,7 +89976,7 @@
                     msg_puts_attr_len((char *)plain_start, (int)(str - plain_start), attr);
                 }
                 plain_start = str + mb_l;
-                msg_puts_attr((char *)transchar_buf(NULL, c), attr == 0 ? HL_ATTR(HLF_8) : attr);
+                msg_puts_attr((char *)transchar_buf(NULL, c), attr == 0 ?  highlight_attr[(int)(HLF_8)]  : attr);
                 retval += char2cells(c);
             }
             len -= mb_l - 1;
@@ -91907,8 +89992,8 @@
                     msg_puts_attr_len((char *)plain_start, (int)(str - plain_start), attr);
                 }
                 plain_start = str + 1;
-                msg_puts_attr((char *)s, attr == 0 ? HL_ATTR(HLF_8) : attr);
-                retval += (int)STRLEN(s);
+                msg_puts_attr((char *)s, attr == 0 ?  highlight_attr[(int)(HLF_8)]  : attr);
+                retval += (int) strlen((char *)(s)) ;
             }
             else
             {
@@ -91937,7 +90022,7 @@
     int         attr;
     int         len;
 
-    attr = HL_ATTR(HLF_8);
+    attr =  highlight_attr[(int)(HLF_8)] ;
     while (*str != NUL)
     {
         if ((str == strstart || str[1] == NUL) && *str == ' ')
@@ -91985,7 +90070,7 @@
     }
 
     c = *str;
-    if ((c == K_SPECIAL) && str[1] != NUL && str[2] != NUL)
+    if ((c ==  (0x80) ) && str[1] != NUL && str[2] != NUL)
     {
         if (str[1] == KS_MODIFIER)
         {
@@ -91993,18 +90078,18 @@
             str += 3;
             c = *str;
         }
-        if ((c == K_SPECIAL) && str[1] != NUL && str[2] != NUL)
+        if ((c ==  (0x80) ) && str[1] != NUL && str[2] != NUL)
         {
-            c = TO_SPECIAL(str[1], str[2]);
+            c =  ((str[1]) == KS_SPECIAL ?  (0x80)  : (str[1]) == KS_ZERO ?   (-((KS_ZERO) + ((int)( ('X') ) << 8)))   :  (-((str[1]) + ((int)(str[2]) << 8))) ) ;
             str += 2;
         }
-        if (IS_SPECIAL(c) || modifiers)
+        if ( ((c) < 0)  || modifiers)
         {
             special = TRUE;
         }
     }
 
-    if (has_mbyte && !IS_SPECIAL(c) && MB_BYTE2LEN(c) > 1)
+    if (has_mbyte && ! ((c) < 0)  &&  mb_bytelen_tab[c]  > 1)
     {
         char_u  *p;
 
@@ -92044,10 +90129,10 @@
     while (*sp)
     {
         s = str2special(&sp, FALSE, FALSE);
-        s_len = STRLEN(s);
+        s_len =  strlen((char *)(s)) ;
         if (buf_len + s_len < (size_t)len)
         {
-            STRCPY(buf + buf_len, s);
+             strcpy((char *)(buf + buf_len), (char *)(s)) ;
             buf_len += s_len;
         }
         else
@@ -92075,7 +90160,7 @@
     int         l;
     char_u      buf[MB_MAXBYTES + 1];
 
-    if (curwin->w_p_list)
+    if (curwin-> w_onebuf_opt.wo_list )
     {
         list = TRUE;
     }
@@ -92084,8 +90169,8 @@
     {
         if (curwin->w_lcs_chars.trail)
         {
-            trail = s + STRLEN(s);
-            while (trail > s && VIM_ISWHITE(trail[-1]))
+            trail = s +  strlen((char *)(s)) ;
+            while (trail > s &&  ((trail[-1]) == ' ' || (trail[-1]) == '\t') )
             {
                 --trail;
             }
@@ -92093,7 +90178,7 @@
         if (curwin->w_lcs_chars.lead || curwin->w_lcs_chars.leadmultispace != NULL || curwin->w_lcs_chars.leadtab1 != NUL)
         {
             lead = s;
-            while (VIM_ISWHITE(lead[0]))
+            while ( ((lead[0]) == ' ' || (lead[0]) == '\t') )
             {
                 lead++;
             }
@@ -92132,7 +90217,7 @@
             col += (*mb_ptr2cells)(s);
             if (l >= MB_MAXBYTES)
             {
-                STRCPY(buf, "?");
+                 strcpy((char *)(buf), (char *)("?")) ;
             }
             else if (curwin->w_lcs_chars.nbsp != NUL && list && (mb_ptr2char(s) == 160 || mb_ptr2char(s) == 0x202f))
             {
@@ -92142,7 +90227,7 @@
             }
             else
             {
-                mch_memmove(buf, s, (size_t)l);
+                 memmove((char *)(buf), (char *)(s), (size_t)l) ;
                 buf[l] = NUL;
             }
             msg_puts((char *)buf);
@@ -92186,13 +90271,13 @@
                     c = (n_extra == 0 && lcs_tab3) ? lcs_tab3 : lcs_tab1;
                     c_extra = lcs_tab2;
                     c_final = lcs_tab3;
-                    attr = HL_ATTR(HLF_8);
+                    attr =  highlight_attr[(int)(HLF_8)] ;
                 }
             }
             else if (c == 160 && list && curwin->w_lcs_chars.nbsp != NUL)
             {
                 c = curwin->w_lcs_chars.nbsp;
-                attr = HL_ATTR(HLF_8);
+                attr =  highlight_attr[(int)(HLF_8)] ;
             }
             else if (c == NUL && list && curwin->w_lcs_chars.eol != NUL)
             {
@@ -92201,7 +90286,7 @@
                 c_final = NUL;
                 n_extra = 1;
                 c = curwin->w_lcs_chars.eol;
-                attr = HL_ATTR(HLF_AT);
+                attr =  highlight_attr[(int)(HLF_AT)] ;
                 --s;
             }
             else if (c != NUL && (n = byte2cells(c)) > 1)
@@ -92211,7 +90296,7 @@
                 c_extra = NUL;
                 c_final = NUL;
                 c = *p_extra++;
-                attr = HL_ATTR(HLF_8);
+                attr =  highlight_attr[(int)(HLF_8)] ;
             }
             else if (c == ' ')
             {
@@ -92222,17 +90307,17 @@
                     {
                         multispace_pos = 0;
                     }
-                    attr = HL_ATTR(HLF_8);
+                    attr =  highlight_attr[(int)(HLF_8)] ;
                 }
                 else if (lead != NULL && s <= lead && curwin->w_lcs_chars.lead != NUL)
                 {
                     c = curwin->w_lcs_chars.lead;
-                    attr = HL_ATTR(HLF_8);
+                    attr =  highlight_attr[(int)(HLF_8)] ;
                 }
                 else if (trail != NULL && s > trail)
                 {
                     c = curwin->w_lcs_chars.trail;
-                    attr = HL_ATTR(HLF_8);
+                    attr =  highlight_attr[(int)(HLF_8)] ;
                 }
                 else if (in_multispace && curwin->w_lcs_chars.multispace != NULL)
                 {
@@ -92241,12 +90326,12 @@
                     {
                         multispace_pos = 0;
                     }
-                    attr = HL_ATTR(HLF_8);
+                    attr =  highlight_attr[(int)(HLF_8)] ;
                 }
                 else if (list && curwin->w_lcs_chars.space != NUL)
                 {
                     c = curwin->w_lcs_chars.space;
-                    attr = HL_ATTR(HLF_8);
+                    attr =  highlight_attr[(int)(HLF_8)] ;
                 }
             }
         }
@@ -92271,7 +90356,7 @@
     cw = (*mb_ptr2cells)(s);
     if (cw > 1 && (msg_col == cmdline_width - 1))
     {
-        msg_screen_putchar('>', HL_ATTR(HLF_AT));
+        msg_screen_putchar('>',  highlight_attr[(int)(HLF_AT)] );
         return s;
     }
 
@@ -92296,7 +90381,7 @@
     void
 msg_puts_title(char *s)
 {
-    msg_puts_attr(s, HL_ATTR(HLF_T));
+    msg_puts_attr(s,  highlight_attr[(int)(HLF_T)] );
 }
 
     static void
@@ -92310,7 +90395,7 @@
     {
         slen = (room - 3) / 2;
         msg_outtrans_len_attr(longstr, slen, attr);
-        msg_puts_attr("...", HL_ATTR(HLF_8));
+        msg_puts_attr("...",  highlight_attr[(int)(HLF_8)] );
     }
     msg_outtrans_len_attr(longstr + len - slen, slen, attr);
 }
@@ -92318,7 +90403,7 @@
     void
 msg_outtrans_long_attr(char_u *longstr, int attr)
 {
-    msg_outtrans_long_len_attr(longstr, (int)STRLEN(longstr), attr);
+    msg_outtrans_long_len_attr(longstr, (int) strlen((char *)(longstr)) , attr);
 }
 
     void
@@ -92339,7 +90424,7 @@
 
     if (attr == 0)
     {
-        attr = HL_ATTR(HLF_MSG);
+        attr =  highlight_attr[(int)(HLF_MSG)] ;
     }
 
     if ((attr & MSG_HIST) && maxlen < 0)
@@ -92348,7 +90433,7 @@
         attr &= ~MSG_HIST;
     }
 
-    if (msg_scrolled != 0 && !msg_scrolled_ign && STRCMP(str, "\r") != 0)
+    if (msg_scrolled != 0 && !msg_scrolled_ign &&  strcmp((char *)(str), (char *)("\r"))  != 0)
     {
         need_wait_return = TRUE;
     }
@@ -92366,10 +90451,6 @@
     need_fileinfo = FALSE;
 }
 
-#define PUT_APPEND 0
-#define PUT_TRUNC 1
-#define PUT_BELOW 2
-
     static void
 msg_puts_display(char_u      *str, int         maxlen, int         attr, int         recurse)
 {
@@ -92450,7 +90531,7 @@
                 {
                     --lines_left;
                 }
-            if (p_more && lines_left == 0 && State != MODE_HITRETURN && !msg_no_more && !exmode_active)
+            if (p_more && lines_left == 0 && State !=  (0x2000 | MODE_NORMAL)  && !msg_no_more && !exmode_active)
             {
                 (void)do_more_prompt(NUL);
                 if (quit_more)
@@ -92584,17 +90665,17 @@
     static void
 msg_scroll_up(void)
 {
-    mch_disable_flush();
+      ;
     screen_del_lines(0, 0, 1, (int)Rows, TRUE, 0, NULL);
-    mch_enable_flush();
+      ;
 
     if (!can_clear((char_u *)" "))
     {
-        screen_fill((int)Rows - 1, (int)Rows, cmdline_col_off, cmdline_col_off + cmdline_width, ' ', ' ', HL_ATTR(HLF_MSG));
+        screen_fill((int)Rows - 1, (int)Rows, cmdline_col_off, cmdline_col_off + cmdline_width, ' ', ' ',  highlight_attr[(int)(HLF_MSG)] );
 
         if (ScreenAttrs[LineOffset[Rows - 2] + Columns - 1] == (sattr_T)-1)
         {
-            screen_fill((int)Rows - 2, (int)Rows - 1, (int)Columns - 1, (int)Columns, ' ', ' ', HL_ATTR(HLF_MSG));
+            screen_fill((int)Rows - 2, (int)Rows - 1, (int)Columns - 1, (int)Columns, ' ', ' ',  highlight_attr[(int)(HLF_MSG)] );
         }
     }
 }
@@ -92812,7 +90893,7 @@
 
         if (clear_to_eol)
         {
-            screen_fill(row, row + 1, cmdline_col_off + msg_col, cmdline_col_off + cmdline_width, ' ', ' ', HL_ATTR(HLF_MSG));
+            screen_fill(row, row + 1, cmdline_col_off + msg_col, cmdline_col_off + cmdline_width, ' ', ' ',  highlight_attr[(int)(HLF_MSG)] );
         }
 
         if (mp->sb_eol || mp->sb_next == NULL)
@@ -92875,11 +90956,11 @@
                     buf[n++] = NUL;
                     if (info_message)
                     {
-                        mch_msg((char *)buf);
+                         printf("%s", ((char *)buf)) ;
                     }
                     else
                     {
-                        mch_errmsg((char *)buf);
+                         fprintf(stderr, "%s", ((char *)buf)) ;
                     }
                     vim_free(buf);
                 }
@@ -92913,11 +90994,11 @@
         {
             if (info_message)
             {
-                mch_msg((char *)p);
+                 printf("%s", ((char *)p)) ;
             }
             else
             {
-                mch_errmsg((char *)p);
+                 fprintf(stderr, "%s", ((char *)p)) ;
             }
             vim_free(tofree);
         }
@@ -92940,9 +91021,9 @@
     int         i;
     int         msg_attr;
 
-    msg_attr = HL_ATTR(HLF_MSG);
+    msg_attr =  highlight_attr[(int)(HLF_MSG)] ;
 
-    if (entered || (State == MODE_HITRETURN && typed_char == 0))
+    if (entered || (State ==  (0x2000 | MODE_NORMAL)  && typed_char == 0))
     {
         return FALSE;
     }
@@ -92979,16 +91060,16 @@
         switch (c)
         {
         case BS:
-        case K_BS:
+        case   (-(('k') + ((int)('b') << 8)))  :
         case 'k':
-        case K_UP:
+        case   (-(('k') + ((int)('u') << 8)))  :
             toscroll = -1;
             break;
 
         case CAR:
         case NL:
         case 'j':
-        case K_DOWN:
+        case   (-(('k') + ((int)('d') << 8)))  :
             toscroll = 1;
             break;
 
@@ -93002,15 +91083,15 @@
 
         case 'b':
         case Ctrl_B:
-        case K_PAGEUP:
+        case   (-(('k') + ((int)('P') << 8)))  :
             toscroll = -(Rows - 1);
             break;
 
         case ' ':
         case 'f':
         case Ctrl_F:
-        case K_PAGEDOWN:
-        case K_LEFTMOUSE:
+        case   (-(('k') + ((int)('N') << 8)))  :
+        case   (-((KS_EXTRA) + ((int)(KE_LEFTMOUSE) << 8)))  :
             toscroll = Rows - 1;
             break;
 
@@ -93030,6 +91111,7 @@
                 skip_redraw = TRUE;
                 need_wait_return = FALSE;
             }
+        __attribute__((fallthrough));
         case 'q':
         case Ctrl_C:
         case ESC:
@@ -93161,7 +91243,7 @@
     int         attr;
     char_u      *s = (char_u *)_("-- More --");
 
-    attr = HL_ATTR(HLF_M);
+    attr =  highlight_attr[(int)(HLF_M)] ;
     screen_puts(s, (int)Rows - 1, cmdline_col_off, attr);
     if (full)
     {
@@ -93181,7 +91263,7 @@
     {
         windgoto(msg_row, cmdline_col_off + msg_col);
     }
-    else if (State == MODE_HITRETURN || State == MODE_SETWSIZE)
+    else if (State ==  (0x2000 | MODE_NORMAL)  || State == MODE_SETWSIZE)
     {
         if (msg_row == Rows - 1)
         {
@@ -93229,13 +91311,13 @@
     {
         if (full_screen)
         {
-            if (*T_CD)
+            if (* ( term_strings[(int)(KS_CD)] ) )
             {
-                out_str(T_CD);
+                out_str( ( term_strings[(int)(KS_CD)] ) );
             }
-            else if (*T_CE)
+            else if (* ( term_strings[(int)(KS_CE)] ) )
             {
-                out_str(T_CE);
+                out_str( ( term_strings[(int)(KS_CE)] ) );
             }
         }
     }
@@ -93243,7 +91325,7 @@
     {
         int msg_attr;
 
-        msg_attr = HL_ATTR(HLF_MSG);
+        msg_attr =  highlight_attr[(int)(HLF_MSG)] ;
 
         {
             screen_fill(msg_row, msg_row + 1, cmdline_col_off + msg_col, cmdline_col_off + cmdline_width, ' ', ' ', msg_attr);
@@ -93423,7 +91505,7 @@
     {
         verbose_did_open = TRUE;
 
-        verbose_fd = mch_fopen((char *)p_vfile, "a");
+        verbose_fd =  fopen(((char *)p_vfile), ("a")) ;
         if (verbose_fd == NULL)
         {
             semsg(_(e_cant_open_file_str), p_vfile);
@@ -93449,10 +91531,11 @@
 
     ++no_wait_return;
 
-    VIM_CLEAR(keep_msg);
+     vim_free(keep_msg);
+     (keep_msg) = NULL;
     if (hl)
     {
-        keep_msg_attr = HL_ATTR(HLF_W);
+        keep_msg_attr =  highlight_attr[(int)(HLF_W)] ;
     }
     else
     {
@@ -93462,9 +91545,9 @@
     if (with_source)
     {
         msg_start();
-        msg_source(HL_ATTR(HLF_W));
+        msg_source( highlight_attr[(int)(HLF_W)] );
         msg_puts(" ");
-        msg_puts_attr((char *)message, HL_ATTR(HLF_W) | MSG_HIST);
+        msg_puts_attr((char *)message,  highlight_attr[(int)(HLF_W)]  | MSG_HIST);
         msg_clr_eos();
         (void)msg_end();
     }
@@ -93510,8 +91593,8 @@
 
 // ==================== misc1.c ====================
 
-#define URL_SLASH       1
-#define URL_BACKSLASH   2
+enum { URL_SLASH = 1 };
+enum { URL_BACKSLASH = 2 };
 
 static garray_T ga_users;
 
@@ -93531,7 +91614,7 @@
     char_u      *saved_flags = NULL;
 
     result = i = 0;
-    while (VIM_ISWHITE(line[i]))
+    while ( ((line[i]) == ' ' || (line[i]) == '\t') )
     {
         ++i;
     }
@@ -93569,13 +91652,13 @@
                 continue;
             }
 
-            if (VIM_ISWHITE(string[0]))
+            if ( ((string[0]) == ' ' || (string[0]) == '\t') )
             {
-                if (i == 0 || !VIM_ISWHITE(line[i - 1]))
+                if (i == 0 || ! ((line[i - 1]) == ' ' || (line[i - 1]) == '\t') )
                 {
                     continue;
                 }
-                while (VIM_ISWHITE(string[0]))
+                while ( ((string[0]) == ' ' || (string[0]) == '\t') )
                 {
                     ++string;
                 }
@@ -93589,7 +91672,7 @@
                 continue;
             }
 
-            if (vim_strchr(part_buf, COM_BLANK) != NULL && !VIM_ISWHITE(line[i + j]) && line[i + j] != NUL)
+            if (vim_strchr(part_buf, COM_BLANK) != NULL && ! ((line[i + j]) == ' ' || (line[i + j]) == '\t')  && line[i + j] != NUL)
             {
                 continue;
             }
@@ -93633,7 +91716,7 @@
 
         result = i;
 
-        while (VIM_ISWHITE(line[i]))
+        while ( ((line[i]) == ' ' || (line[i]) == '\t') )
         {
             ++i;
         }
@@ -93666,7 +91749,7 @@
     int         found_one;
     char_u      part_buf[COM_MAX_LEN];
 
-    i = (int)STRLEN(line);
+    i = (int) strlen((char *)(line)) ;
     while (--i >= lower_check_bound)
     {
         found_one = FALSE;
@@ -93683,13 +91766,13 @@
             *string++ = NUL;
             com_leader = string;
 
-            if (VIM_ISWHITE(string[0]))
+            if ( ((string[0]) == ' ' || (string[0]) == '\t') )
             {
-                if (i == 0 || !VIM_ISWHITE(line[i - 1]))
+                if (i == 0 || ! ((line[i - 1]) == ' ' || (line[i - 1]) == '\t') )
                 {
                     continue;
                 }
-                while (VIM_ISWHITE(*string))
+                while ( ((*string) == ' ' || (*string) == '\t') )
                 {
                     ++string;
                 }
@@ -93703,14 +91786,14 @@
                 continue;
             }
 
-            if (vim_strchr(part_buf, COM_BLANK) != NULL && !VIM_ISWHITE(line[i + j]) && line[i + j] != NUL)
+            if (vim_strchr(part_buf, COM_BLANK) != NULL && ! ((line[i + j]) == ' ' || (line[i + j]) == '\t')  && line[i + j] != NUL)
             {
                 continue;
             }
 
             if (vim_strchr(part_buf, COM_MIDDLE) != NULL)
             {
-                for (j = 0; VIM_ISWHITE(line[j]) && j <= i; j++)
+                for (j = 0;  ((line[j]) == ' ' || (line[j]) == '\t')  && j <= i; j++)
                 {
                     ;
                 }
@@ -93746,11 +91829,11 @@
 
             lower_check_bound = i;
 
-            while (VIM_ISWHITE(*com_leader))
+            while ( ((*com_leader) == ' ' || (*com_leader) == '\t') )
             {
                 ++com_leader;
             }
-            len1 = (int)STRLEN(com_leader);
+            len1 = (int) strlen((char *)(com_leader)) ;
 
             for (list = curbuf->b_p_com; *list; )
             {
@@ -93763,11 +91846,11 @@
                 }
                 string = vim_strchr(part_buf2, ':');
                 ++string;
-                while (VIM_ISWHITE(*string))
+                while ( ((*string) == ' ' || (*string) == '\t') )
                 {
                     ++string;
                 }
-                len2 = (int)STRLEN(string);
+                len2 = (int) strlen((char *)(string)) ;
                 if (len2 == 0)
                 {
                     continue;
@@ -93776,7 +91859,7 @@
                 for (off = (len2 > i ? i : len2); off > 0 && off + len1 > len2;)
                 {
                     --off;
-                    if (STRNCMP(string + off, com_leader, len2 - off) == 0)
+                    if ( strncmp((char *)(string + off), (char *)(com_leader), (len2 - off))  == 0)
                     {
                         if (i - off < lower_check_bound)
                         {
@@ -93806,7 +91889,7 @@
         return 1;
     }
 
-    if (!wp->w_p_wrap)
+    if (!wp-> w_onebuf_opt.wo_wrap )
     {
         lines = 1
         ;
@@ -93841,7 +91924,7 @@
     clear_chartabsize_arg(&cts);
     col = (int)cts.cts_vcol;
 
-    if (wp->w_p_list && wp->w_lcs_chars.eol != NUL)
+    if (wp-> w_onebuf_opt.wo_list  && wp->w_lcs_chars.eol != NUL)
     {
         col += 1;
     }
@@ -93869,7 +91952,7 @@
     char_u      *line;
     chartabsize_T cts;
 
-    if (!wp->w_p_wrap)
+    if (!wp-> w_onebuf_opt.wo_wrap )
     {
         return lines + 1;
     }
@@ -93885,11 +91968,11 @@
     while (*cts.cts_ptr != NUL && cts.cts_ptr < line + column)
     {
         cts.cts_vcol += win_lbr_chartabsize(&cts, NULL, NULL);
-        MB_PTR_ADV(cts.cts_ptr);
+         cts.cts_ptr += (*mb_ptr2len)(cts.cts_ptr) ;
     }
 
     col = cts.cts_vcol;
-    if (*cts.cts_ptr == TAB && (State & MODE_NORMAL) && (!wp->w_p_list || wp->w_lcs_chars.tab1))
+    if (*cts.cts_ptr == TAB && (State & MODE_NORMAL) && (!wp-> w_onebuf_opt.wo_list  || wp->w_lcs_chars.tab1))
     {
         col += win_lbr_chartabsize(&cts, NULL, NULL) - 1;
     }
@@ -94001,12 +92084,14 @@
 {
     win_T       *wp;
 
-    FOR_ALL_WINDOWS(wp)
+     for ((wp) = firstwin; (wp) != NULL; (wp) = (wp)->w_next) 
+     {
         if (wp->w_buffer == buf && wp->w_status_height)
         {
             wp->w_redr_status = true;
             set_must_redraw(UPD_VALID);
         }
+     }
 }
 
     int
@@ -94027,7 +92112,7 @@
 
     while (r != 'y' && r != 'n')
     {
-        smsg_attr(HL_ATTR(HLF_R), "%s (y/n)?", str);
+        smsg_attr( highlight_attr[(int)(HLF_R)] , "%s (y/n)?", str);
         if (direct)
         {
             r = get_keystroke();
@@ -94080,7 +92165,7 @@
             char_u  *t_buf = buf;
 
             buflen += 100;
-            buf = vim_realloc(buf, buflen);
+            buf =  realloc((buf), (buflen)) ;
             if (buf == NULL)
             {
                 vim_free(t_buf);
@@ -94112,7 +92197,7 @@
 
         if (n == KEYLEN_REMOVED)
         {
-            if (must_redraw != 0 && !need_wait_return && (State & (MODE_CMDLINE | MODE_HITRETURN | MODE_ASKMORE)) == 0)
+            if (must_redraw != 0 && !need_wait_return && (State & (MODE_CMDLINE |  (0x2000 | MODE_NORMAL)  | MODE_ASKMORE)) == 0)
             {
                 update_screen(0);
                 setcursor();
@@ -94129,10 +92214,10 @@
         }
 
         n = buf[0];
-        if (n == K_SPECIAL)
+        if (n ==  (0x80) )
         {
-            n = TO_SPECIAL(buf[1], buf[2]);
-            if (buf[1] == KS_MODIFIER || n == K_IGNORE || (is_mouse_key(n) && n != K_LEFTMOUSE))
+            n =  ((buf[1]) == KS_SPECIAL ?  (0x80)  : (buf[1]) == KS_ZERO ?   (-((KS_ZERO) + ((int)( ('X') ) << 8)))   :  (-((buf[1]) + ((int)(buf[2]) << 8))) ) ;
+            if (buf[1] == KS_MODIFIER || n ==   (-((KS_EXTRA) + ((int)(KE_IGNORE) << 8)))   || (is_mouse_key(n) && n !=   (-((KS_EXTRA) + ((int)(KE_LEFTMOUSE) << 8)))  ))
             {
                 if (buf[1] == KS_MODIFIER)
                 {
@@ -94141,7 +92226,7 @@
                 len -= 3;
                 if (len > 0)
                 {
-                    mch_memmove(buf, buf + 3, (size_t)len);
+                     memmove((char *)(buf), (char *)(buf + 3), (size_t)len) ;
                 }
                 continue;
             }
@@ -94149,7 +92234,7 @@
         }
         if (has_mbyte)
         {
-            if (MB_BYTE2LEN(n) > len)
+            if ( mb_bytelen_tab[n]  > len)
             {
                 continue;
             }
@@ -94203,7 +92288,7 @@
     {
         windgoto(msg_row, cmdline_col_off + msg_col);
         c = safe_vgetc();
-        if (VIM_ISDIGIT(c))
+        if ( ((unsigned)(c) - '0' < 10) )
         {
             if (vim_append_digit_int(&n, c - '0') == FAIL)
             {
@@ -94212,7 +92297,7 @@
             msg_putchar(c);
             ++typed;
         }
-        else if (c == K_DEL || c == K_KDEL || c == K_BS || c == Ctrl_H)
+        else if (c ==   (-(('k') + ((int)('D') << 8)))   || c ==   (-((KS_EXTRA) + ((int)(KE_KDEL) << 8)))   || c ==   (-(('k') + ((int)('b') << 8)))   || c == Ctrl_H)
         {
             if (typed > 0)
             {
@@ -94221,7 +92306,7 @@
             }
             n /= 10;
         }
-        else if (mouse_used != NULL && c == K_LEFTMOUSE)
+        else if (mouse_used != NULL && c ==   (-((KS_EXTRA) + ((int)(KE_LEFTMOUSE) << 8)))  )
         {
             *mouse_used = TRUE;
             n = mouse_row + 1;
@@ -94355,7 +92440,6 @@
     void
 vim_beep(unsigned val)
 {
-
     if (emsg_silent != 0 || in_assert_fails)
     {
         return;
@@ -94366,13 +92450,13 @@
         static int              did_init = FALSE;
         static elapsed_T        start_tv;
 
-        if (!did_init || ELAPSED_FUNC(start_tv) > 500)
+        if (!did_init ||  elapsed(&(start_tv))  > 500)
         {
             did_init = TRUE;
-            ELAPSED_INIT(start_tv);
+             gettimeofday(&(start_tv), NULL) ;
             if (p_vb)
             {
-                out_str_cf(T_VB);
+                out_str_cf( ( term_strings[(int)(KS_VB)] ) );
             }
             else
             {
@@ -94383,8 +92467,8 @@
 
     if (vim_strchr(p_debug, 'e') != NULL)
     {
-        msg_source(HL_ATTR(HLF_W));
-        msg_attr(_("Beep!"), HL_ATTR(HLF_W));
+        msg_source( highlight_attr[(int)(HLF_W)] );
+        msg_attr(_("Beep!"),  highlight_attr[(int)(HLF_W)] );
     }
 }
 
@@ -94393,15 +92477,16 @@
 {
     char_u  *var;
 
-    VIM_CLEAR(homedir);
+     vim_free(homedir);
+     (homedir) = NULL;
 
-    var = mch_getenv((char_u *)"HOME");
+    var =  (char_u *)getenv((char *)((char_u *)"HOME")) ;
 
     if (var != NULL)
     {
-        if (mch_dirname(NameBuff, MAXPATHL) == OK && mch_chdir((char *)NameBuff) == 0)
+        if (mch_dirname(NameBuff,  PATH_MAX ) == OK && mch_chdir((char *)NameBuff) == 0)
         {
-            if (!mch_chdir((char *)var) && mch_dirname(IObuff, IOSIZE) == OK)
+            if (!mch_chdir((char *)var) && mch_dirname(IObuff,  (1024+1) ) == OK)
             {
                 var = IObuff;
             }
@@ -94425,10 +92510,10 @@
 {
     char_u      *p;
 
-    p = alloc(MAXPATHL);
+    p = alloc( PATH_MAX );
     if (p != NULL)
     {
-        expand_env_esc(src, p, MAXPATHL, esc_chars, one, NULL);
+        expand_env_esc(src, p,  PATH_MAX , esc_chars, one, NULL);
     }
     return p;
 }
@@ -94454,7 +92539,7 @@
 
     if (startstr != NULL)
     {
-        startstr_len = (int)STRLEN(startstr);
+        startstr_len = (int) strlen((char *)(startstr)) ;
     }
 
     src = skipwhite(srcp);
@@ -94535,7 +92620,7 @@
 
             }
 
-            if (esc_chars != NULL && var != NULL && vim_strpbrk(var, esc_chars) != NULL)
+            if (esc_chars != NULL && var != NULL &&  (char_u *)strpbrk((char *)(var), (char *)(esc_chars))  != NULL)
             {
                 char_u  *p = vim_strsave_escaped(var, esc_chars);
 
@@ -94552,11 +92637,11 @@
 
             if (var != NULL && *var != NUL)
             {
-                c = (int)STRLEN(var);
+                c = (int) strlen((char *)(var)) ;
 
-                if (c + STRLEN(tail) + 1 < (unsigned)dstlen)
+                if (c +  strlen((char *)(tail))  + 1 < (unsigned)dstlen)
                 {
-                    STRCPY(dst, var);
+                     strcpy((char *)(dst), (char *)(var)) ;
                     dstlen -= c;
                     if (after_pathsep(dst, dst + c) && vim_ispathsep(*tail))
                     {
@@ -94590,7 +92675,7 @@
                 *dst++ = *src++;
                 --dstlen;
 
-                if (startstr != NULL && src - startstr_len >= srcp && STRNCMP(src - startstr_len, startstr, startstr_len) == 0)
+                if (startstr != NULL && src - startstr_len >= srcp &&  strncmp((char *)(src - startstr_len), (char *)(startstr), (startstr_len))  == 0)
                 {
                     at_start = TRUE;
                 }
@@ -94606,10 +92691,10 @@
     static char_u *
 remove_tail(char_u *p, char_u *pend, char_u *name)
 {
-    int         len = (int)STRLEN(name) + 1;
+    int         len = (int) strlen((char *)(name))  + 1;
     char_u      *newend = pend - len;
 
-    if (newend >= p && fnamencmp(newend, name, len - 1) == 0 && (newend == p || after_pathsep(p, newend)))
+    if (newend >= p &&  vim_fnamencmp((char_u *)(newend), (char_u *)(name), (len - 1))  == 0 && (newend == p || after_pathsep(p, newend)))
     {
         return newend;
     }
@@ -94626,19 +92711,19 @@
     {
         return NULL;
     }
-    vimdir_len = STRLEN(vimdir);
-    concat_fnames(vimdir, vimdir_len, (char_u *)VIM_VERSION_NODOT, STRLEN_LITERAL(VIM_VERSION_NODOT), TRUE, &p);
+    vimdir_len =  strlen((char *)(vimdir)) ;
+    concat_fnames(vimdir, vimdir_len, (char_u *)VIM_VERSION_NODOT, sizeof(VIM_VERSION_NODOT) - 1, TRUE, &p);
     if (p.string != NULL && mch_isdir(p.string))
     {
         return p.string;
     }
     vim_free(p.string);
-    concat_fnames(vimdir, vimdir_len, (char_u *)RUNTIME_DIRNAME, STRLEN_LITERAL(RUNTIME_DIRNAME), TRUE, &p);
+    concat_fnames(vimdir, vimdir_len, (char_u *) "runtime" ,  (sizeof( "runtime"  "") - 1) , TRUE, &p);
     if (p.string != NULL && mch_isdir(p.string))
     {
         string_T    fname;
 
-        concat_fnames(p.string, p.length, (char_u *)"defaults.vim", STRLEN_LITERAL("defaults.vim"), TRUE, &fname);
+        concat_fnames(p.string, p.length, (char_u *)"defaults.vim",  (sizeof("defaults.vim" "") - 1) , TRUE, &fname);
 
         if (fname.string != NULL)
         {
@@ -94661,7 +92746,7 @@
     char_u      *p = NULL;
     char_u      *pend;
     int         vimruntime;
-    p = mch_getenv(name);
+    p =  (char_u *)getenv((char *)(name)) ;
     if (p != NULL && *p == NUL)
     {
         p = NULL;
@@ -94672,15 +92757,15 @@
         return p;
     }
 
-    vimruntime = (STRCMP(name, "VIMRUNTIME") == 0);
-    if (!vimruntime && STRCMP(name, "VIM") != 0)
+    vimruntime = ( strcmp((char *)(name), (char *)("VIMRUNTIME"))  == 0);
+    if (!vimruntime &&  strcmp((char *)(name), (char *)("VIM"))  != 0)
     {
         return NULL;
     }
 
     if (vimruntime && *default_vimruntime_dir == NUL)
     {
-        p = mch_getenv((char_u *)"VIM");
+        p =  (char_u *)getenv((char *)((char_u *)"VIM")) ;
         if (p != NULL && *p == NUL)
         {
             p = NULL;
@@ -94694,7 +92779,7 @@
             }
             else
             {
-                p = mch_getenv((char_u *)"VIM");
+                p =  (char_u *)getenv((char *)((char_u *)"VIM")) ;
             }
         }
     }
@@ -94716,7 +92801,7 @@
 
             if (!vimruntime)
             {
-                pend = remove_tail(p, pend, (char_u *)RUNTIME_DIRNAME);
+                pend = remove_tail(p, pend, (char_u *) "runtime" );
                 pend = remove_tail(p, pend, (char_u *)VIM_VERSION_NODOT);
             }
 
@@ -94729,7 +92814,8 @@
 
             if (p != NULL && !mch_isdir(p))
             {
-                VIM_CLEAR(p);
+                 vim_free(p);
+                 (p) = NULL;
             }
             else
             {
@@ -94786,11 +92872,11 @@
 {
     vim_unsetenv(var);
 
-    if (STRICMP(var, "VIM") == 0)
+    if ( strcasecmp((char *)(var), (char *)("VIM"))  == 0)
     {
         didset_vim = FALSE;
     }
-    else if (STRICMP(var, "VIMRUNTIME") == 0)
+    else if ( strcasecmp((char *)(var), (char *)("VIMRUNTIME"))  == 0)
     {
         didset_vimruntime = FALSE;
     }
@@ -94799,11 +92885,11 @@
     void
 vim_setenv(char_u *name, char_u *val)
 {
-    mch_setenv((char *)name, (char *)val, 1);
+     setenv((char *)name, (char *)val, 1) ;
 }
 
     char_u *
-get_env_name(expand_T    *xp UNUSED, int         idx)
+get_env_name(expand_T    *xp  __attribute__((unused)) , int         idx)
 {
     extern char         **environ;
     char_u              *str;
@@ -94868,7 +92954,7 @@
         endpwent();
     }
     {
-        char_u  *user_env = mch_getenv((char_u *)"USER");
+        char_u  *user_env =  (char_u *)getenv((char *)((char_u *)"USER")) ;
 
         if (user_env != NULL && *user_env != NUL)
         {
@@ -94878,7 +92964,7 @@
             {
                 char_u  *local_user = ((char_u **)ga_users.ga_data)[i];
 
-                if (STRCMP(local_user, user_env) == 0)
+                if ( strcmp((char *)(local_user), (char *)(user_env))  == 0)
                 {
                     break;
                 }
@@ -94898,7 +92984,7 @@
 }
 
     char_u*
-get_users(expand_T *xp UNUSED, int idx)
+get_users(expand_T *xp  __attribute__((unused)) , int idx)
 {
     init_users();
     if (idx < ga_users.ga_len)
@@ -94912,17 +92998,17 @@
 match_user(char_u *name)
 {
     int i;
-    int n = (int)STRLEN(name);
+    int n = (int) strlen((char *)(name)) ;
     int result = 0;
 
     init_users();
     for (i = 0; i < ga_users.ga_len; i++)
     {
-        if (STRCMP(((char_u **)ga_users.ga_data)[i], name) == 0)
+        if ( strcmp((char *)(((char_u **)ga_users.ga_data)[i]), (char *)(name))  == 0)
         {
             return 2;
         }
-        if (STRNCMP(((char_u **)ga_users.ga_data)[i], name, n) == 0)
+        if ( strncmp((char *)(((char_u **)ga_users.ga_data)[i]), (char *)(name), (n))  == 0)
         {
             result = 1;
         }
@@ -94964,11 +93050,11 @@
 
     ml_close_notmod();
 
-    FOR_ALL_BUFFERS(buf)
+     for ((buf) = firstbuf; (buf) != NULL; (buf) = (buf)->b_next) 
     {
         if (buf->b_ml.ml_mfp != NULL && buf->b_ml.ml_mfp->mf_fname != NULL)
         {
-            OUT_STR("Vim: preserving files...\r\n");
+             out_str((char_u *)("Vim: preserving files...\r\n")) ;
             screen_start();
             out_flush();
             ml_sync_all(FALSE, FALSE);
@@ -94978,12 +93064,12 @@
 
     ml_close_all(FALSE);
 
-    OUT_STR("Vim: Finished.\r\n");
+     out_str((char_u *)("Vim: Finished.\r\n")) ;
 
     getout(1);
 }
 
-# define BREAKCHECK_SKIP 1000
+enum { BREAKCHECK_SKIP = 1000 };
 
 static int      breakcheck_count = 0;
 
@@ -95040,7 +93126,7 @@
 
     vim_free(command);
 
-    fd = mch_fopen((char *)tempname, READBIN);
+    fd =  fopen(((char *)tempname), ( "r" )) ;
 
     if (fd == NULL || fseek(fd, 0L, SEEK_END) == -1 || (len = ftell(fd)) == -1 || fseek(fd, 0L, SEEK_SET) == -1)
     {
@@ -95065,7 +93151,8 @@
     if (i != len)
     {
         semsg(_(e_cant_read_file_str), tempname);
-        VIM_CLEAR(buffer);
+         vim_free(buffer);
+         (buffer) = NULL;
     }
     else if (ret_len == NULL)
     {
@@ -95085,7 +93172,7 @@
     }
 
 done:
-    mch_remove(tempname);
+     unlink((char *)(tempname)) ;
     vim_free(tempname);
     return buffer;
 }
@@ -95112,7 +93199,7 @@
         char_u *p2;
 
         p1 = p_sh;
-        for (p2 = p_sh; p2 < p; MB_PTR_ADV(p2))
+        for (p2 = p_sh; p2 < p;  p2 += (*mb_ptr2len)(p2) )
         {
             if (vim_ispathsep(*p2))
             {
@@ -95127,11 +93214,11 @@
     int
 path_is_url(char_u *p)
 {
-    if (STRNCMP(p, "://", (size_t)3) == 0)
+    if ( strncmp((char *)(p), (char *)("://"), ((size_t)3))  == 0)
     {
         return URL_SLASH;
     }
-    else if (STRNCMP(p, ":\\\\", (size_t)3) == 0)
+    else if ( strncmp((char *)(p), (char *)(":\\\\"), ((size_t)3))  == 0)
     {
         return URL_BACKSLASH;
     }
@@ -95143,12 +93230,12 @@
 {
     char_u *p;
 
-    if (!ASCII_ISALPHA(*fname))
+    if (! ( ((unsigned)(*fname) - 'A' < 26)  ||  ((unsigned)(*fname) - 'a' < 26) ) )
     {
         return 0;
     }
 
-    for (p = fname + 1; (ASCII_ISALPHA(*p) || (*p == '-')); ++p)
+    for (p = fname + 1; ( ( ((unsigned)(*p) - 'A' < 26)  ||  ((unsigned)(*p) - 'a' < 26) )  || (*p == '-')); ++p)
     {
         ;
     }
@@ -95304,7 +93391,7 @@
         int             width = curwin->w_width - win_col_off(curwin);
         chartabsize_T   cts;
 
-        if (finetune && curwin->w_p_wrap && curwin->w_width != 0 && wcol >= (colnr_T)width && width > 0)
+        if (finetune && curwin-> w_onebuf_opt.wo_wrap  && curwin->w_width != 0 && wcol >= (colnr_T)width && width > 0)
         {
             csize = linetabsize_eol(curwin, pos->lnum);
             if (csize > 0)
@@ -95336,7 +93423,6 @@
 
         if (virtual_active() && addspaces && wcol >= 0 && ((col != wcol && col != wcol + 1) || csize > 1))
         {
-
             if (line[idx] == NUL)
             {
                 int     correct = wcol - col;
@@ -95822,14 +93908,14 @@
     {MOD_MASK_META,             MOD_MASK_META,          (char_u)'T'},
     {MOD_MASK_CTRL,             MOD_MASK_CTRL,          (char_u)'C'},
     {MOD_MASK_SHIFT,            MOD_MASK_SHIFT,         (char_u)'S'},
-    {MOD_MASK_MULTI_CLICK,      MOD_MASK_2CLICK,        (char_u)'2'},
-    {MOD_MASK_MULTI_CLICK,      MOD_MASK_3CLICK,        (char_u)'3'},
-    {MOD_MASK_MULTI_CLICK,      MOD_MASK_4CLICK,        (char_u)'4'},
+    { (MOD_MASK_2CLICK|MOD_MASK_3CLICK|MOD_MASK_4CLICK) ,      MOD_MASK_2CLICK,        (char_u)'2'},
+    { (MOD_MASK_2CLICK|MOD_MASK_3CLICK|MOD_MASK_4CLICK) ,      MOD_MASK_3CLICK,        (char_u)'3'},
+    { (MOD_MASK_2CLICK|MOD_MASK_3CLICK|MOD_MASK_4CLICK) ,      MOD_MASK_4CLICK,        (char_u)'4'},
     {MOD_MASK_ALT,              MOD_MASK_ALT,           (char_u)'A'},
     {0, 0, NUL}
 };
 
-#define MOD_KEYS_ENTRY_SIZE 5
+enum { MOD_KEYS_ENTRY_SIZE = 5 };
 
 static char_u modifier_keys_table[] =
 {
@@ -95926,173 +94012,173 @@
     int         is_alt;
 } key_names_table[] =
 {
-    {TRUE, K_BS, STR_LITERAL_INIT("BackSpace"), TRUE},
-    {TRUE, '|', STR_LITERAL_INIT("Bar"), FALSE},
-    {TRUE, K_BS, STR_LITERAL_INIT("BS"), FALSE},
-    {TRUE, '\\', STR_LITERAL_INIT("Bslash"), FALSE},
-    {TRUE, K_COMMAND, STR_LITERAL_INIT("Cmd"), FALSE},
-    {TRUE, CAR, STR_LITERAL_INIT("CR"), FALSE},
-    {TRUE, CSI, STR_LITERAL_INIT("CSI"), FALSE},
-    {TRUE, K_CURSORHOLD, STR_LITERAL_INIT("CursorHold"), FALSE},
+    {TRUE,   (-(('k') + ((int)('b') << 8)))  ,  {(char_u *)("BackSpace"),  (sizeof("BackSpace" "") - 1) } , TRUE},
+    {TRUE, '|',  {(char_u *)("Bar"),  (sizeof("Bar" "") - 1) } , FALSE},
+    {TRUE,   (-(('k') + ((int)('b') << 8)))  ,  {(char_u *)("BS"),  (sizeof("BS" "") - 1) } , FALSE},
+    {TRUE, '\\',  {(char_u *)("Bslash"),  (sizeof("Bslash" "") - 1) } , FALSE},
+    {TRUE,   (-((KS_EXTRA) + ((int)(KE_COMMAND) << 8)))  ,  {(char_u *)("Cmd"),  (sizeof("Cmd" "") - 1) } , FALSE},
+    {TRUE, CAR,  {(char_u *)("CR"),  (sizeof("CR" "") - 1) } , FALSE},
+    {TRUE, CSI,  {(char_u *)("CSI"),  (sizeof("CSI" "") - 1) } , FALSE},
+    {TRUE,   (-((KS_EXTRA) + ((int)(KE_CURSORHOLD) << 8)))  ,  {(char_u *)("CursorHold"),  (sizeof("CursorHold" "") - 1) } , FALSE},
     {
     FALSE,
-        K_DEC_MOUSE, STR_LITERAL_INIT("DecMouse"), FALSE},
-    {TRUE, K_DEL, STR_LITERAL_INIT("Del"), FALSE},
-    {TRUE, K_DEL, STR_LITERAL_INIT("Delete"), TRUE},
-    {TRUE, K_DOWN, STR_LITERAL_INIT("Down"), FALSE},
-    {TRUE, K_DROP, STR_LITERAL_INIT("Drop"), FALSE},
-    {TRUE, K_END, STR_LITERAL_INIT("End"), FALSE},
-    {TRUE, CAR, STR_LITERAL_INIT("Enter"), TRUE},
-    {TRUE, ESC, STR_LITERAL_INIT("Esc"), FALSE},
-
-    {TRUE, K_F1, STR_LITERAL_INIT("F1"), FALSE},
-    {TRUE, K_F10, STR_LITERAL_INIT("F10"), FALSE},
-    {TRUE, K_F11, STR_LITERAL_INIT("F11"), FALSE},
-    {TRUE, K_F12, STR_LITERAL_INIT("F12"), FALSE},
-    {TRUE, K_F13, STR_LITERAL_INIT("F13"), FALSE},
-    {TRUE, K_F14, STR_LITERAL_INIT("F14"), FALSE},
-    {TRUE, K_F15, STR_LITERAL_INIT("F15"), FALSE},
-    {TRUE, K_F16, STR_LITERAL_INIT("F16"), FALSE},
-    {TRUE, K_F17, STR_LITERAL_INIT("F17"), FALSE},
-    {TRUE, K_F18, STR_LITERAL_INIT("F18"), FALSE},
-    {TRUE, K_F19, STR_LITERAL_INIT("F19"), FALSE},
-
-    {TRUE, K_F2, STR_LITERAL_INIT("F2"), FALSE},
-    {TRUE, K_F20, STR_LITERAL_INIT("F20"), FALSE},
-    {TRUE, K_F21, STR_LITERAL_INIT("F21"), FALSE},
-    {TRUE, K_F22, STR_LITERAL_INIT("F22"), FALSE},
-    {TRUE, K_F23, STR_LITERAL_INIT("F23"), FALSE},
-    {TRUE, K_F24, STR_LITERAL_INIT("F24"), FALSE},
-    {TRUE, K_F25, STR_LITERAL_INIT("F25"), FALSE},
-    {TRUE, K_F26, STR_LITERAL_INIT("F26"), FALSE},
-    {TRUE, K_F27, STR_LITERAL_INIT("F27"), FALSE},
-    {TRUE, K_F28, STR_LITERAL_INIT("F28"), FALSE},
-    {TRUE, K_F29, STR_LITERAL_INIT("F29"), FALSE},
-
-    {TRUE, K_F3, STR_LITERAL_INIT("F3"), FALSE},
-    {TRUE, K_F30, STR_LITERAL_INIT("F30"), FALSE},
-    {TRUE, K_F31, STR_LITERAL_INIT("F31"), FALSE},
-    {TRUE, K_F32, STR_LITERAL_INIT("F32"), FALSE},
-    {TRUE, K_F33, STR_LITERAL_INIT("F33"), FALSE},
-    {TRUE, K_F34, STR_LITERAL_INIT("F34"), FALSE},
-    {TRUE, K_F35, STR_LITERAL_INIT("F35"), FALSE},
-    {TRUE, K_F36, STR_LITERAL_INIT("F36"), FALSE},
-    {TRUE, K_F37, STR_LITERAL_INIT("F37"), FALSE},
-
-    {TRUE, K_F4, STR_LITERAL_INIT("F4"), FALSE},
-    {TRUE, K_F5, STR_LITERAL_INIT("F5"), FALSE},
-    {TRUE, K_F6, STR_LITERAL_INIT("F6"), FALSE},
-    {TRUE, K_F7, STR_LITERAL_INIT("F7"), FALSE},
-    {TRUE, K_F8, STR_LITERAL_INIT("F8"), FALSE},
-    {TRUE, K_F9, STR_LITERAL_INIT("F9"), FALSE},
-
-    {TRUE, K_FOCUSGAINED, STR_LITERAL_INIT("FocusGained"), FALSE},
-    {TRUE, K_FOCUSLOST, STR_LITERAL_INIT("FocusLost"), FALSE},
-    {TRUE, K_HELP, STR_LITERAL_INIT("Help"), FALSE},
-    {TRUE, K_HOME, STR_LITERAL_INIT("Home"), FALSE},
-    {TRUE, K_IGNORE, STR_LITERAL_INIT("Ignore"), FALSE},
-    {TRUE, K_INS, STR_LITERAL_INIT("Ins"), TRUE},
-    {TRUE, K_INS, STR_LITERAL_INIT("Insert"), FALSE},
+          (-((KS_DEC_MOUSE) + ((int)( ('X') ) << 8)))  ,  {(char_u *)("DecMouse"),  (sizeof("DecMouse" "") - 1) } , FALSE},
+    {TRUE,   (-(('k') + ((int)('D') << 8)))  ,  {(char_u *)("Del"),  (sizeof("Del" "") - 1) } , FALSE},
+    {TRUE,   (-(('k') + ((int)('D') << 8)))  ,  {(char_u *)("Delete"),  (sizeof("Delete" "") - 1) } , TRUE},
+    {TRUE,   (-(('k') + ((int)('d') << 8)))  ,  {(char_u *)("Down"),  (sizeof("Down" "") - 1) } , FALSE},
+    {TRUE,   (-((KS_EXTRA) + ((int)(KE_DROP) << 8)))  ,  {(char_u *)("Drop"),  (sizeof("Drop" "") - 1) } , FALSE},
+    {TRUE,   (-(('@') + ((int)('7') << 8)))  ,  {(char_u *)("End"),  (sizeof("End" "") - 1) } , FALSE},
+    {TRUE, CAR,  {(char_u *)("Enter"),  (sizeof("Enter" "") - 1) } , TRUE},
+    {TRUE, ESC,  {(char_u *)("Esc"),  (sizeof("Esc" "") - 1) } , FALSE},
+
+    {TRUE,   (-(('k') + ((int)('1') << 8)))  ,  {(char_u *)("F1"),  (sizeof("F1" "") - 1) } , FALSE},
+    {TRUE,   (-(('k') + ((int)(';') << 8)))  ,  {(char_u *)("F10"),  (sizeof("F10" "") - 1) } , FALSE},
+    {TRUE,   (-(('F') + ((int)('1') << 8)))  ,  {(char_u *)("F11"),  (sizeof("F11" "") - 1) } , FALSE},
+    {TRUE,   (-(('F') + ((int)('2') << 8)))  ,  {(char_u *)("F12"),  (sizeof("F12" "") - 1) } , FALSE},
+    {TRUE,   (-(('F') + ((int)('3') << 8)))  ,  {(char_u *)("F13"),  (sizeof("F13" "") - 1) } , FALSE},
+    {TRUE,   (-(('F') + ((int)('4') << 8)))  ,  {(char_u *)("F14"),  (sizeof("F14" "") - 1) } , FALSE},
+    {TRUE,   (-(('F') + ((int)('5') << 8)))  ,  {(char_u *)("F15"),  (sizeof("F15" "") - 1) } , FALSE},
+    {TRUE,   (-(('F') + ((int)('6') << 8)))  ,  {(char_u *)("F16"),  (sizeof("F16" "") - 1) } , FALSE},
+    {TRUE,   (-(('F') + ((int)('7') << 8)))  ,  {(char_u *)("F17"),  (sizeof("F17" "") - 1) } , FALSE},
+    {TRUE,   (-(('F') + ((int)('8') << 8)))  ,  {(char_u *)("F18"),  (sizeof("F18" "") - 1) } , FALSE},
+    {TRUE,   (-(('F') + ((int)('9') << 8)))  ,  {(char_u *)("F19"),  (sizeof("F19" "") - 1) } , FALSE},
+
+    {TRUE,   (-(('k') + ((int)('2') << 8)))  ,  {(char_u *)("F2"),  (sizeof("F2" "") - 1) } , FALSE},
+    {TRUE,   (-(('F') + ((int)('A') << 8)))  ,  {(char_u *)("F20"),  (sizeof("F20" "") - 1) } , FALSE},
+    {TRUE,   (-(('F') + ((int)('B') << 8)))  ,  {(char_u *)("F21"),  (sizeof("F21" "") - 1) } , FALSE},
+    {TRUE,   (-(('F') + ((int)('C') << 8)))  ,  {(char_u *)("F22"),  (sizeof("F22" "") - 1) } , FALSE},
+    {TRUE,   (-(('F') + ((int)('D') << 8)))  ,  {(char_u *)("F23"),  (sizeof("F23" "") - 1) } , FALSE},
+    {TRUE,   (-(('F') + ((int)('E') << 8)))  ,  {(char_u *)("F24"),  (sizeof("F24" "") - 1) } , FALSE},
+    {TRUE,   (-(('F') + ((int)('F') << 8)))  ,  {(char_u *)("F25"),  (sizeof("F25" "") - 1) } , FALSE},
+    {TRUE,   (-(('F') + ((int)('G') << 8)))  ,  {(char_u *)("F26"),  (sizeof("F26" "") - 1) } , FALSE},
+    {TRUE,   (-(('F') + ((int)('H') << 8)))  ,  {(char_u *)("F27"),  (sizeof("F27" "") - 1) } , FALSE},
+    {TRUE,   (-(('F') + ((int)('I') << 8)))  ,  {(char_u *)("F28"),  (sizeof("F28" "") - 1) } , FALSE},
+    {TRUE,   (-(('F') + ((int)('J') << 8)))  ,  {(char_u *)("F29"),  (sizeof("F29" "") - 1) } , FALSE},
+
+    {TRUE,   (-(('k') + ((int)('3') << 8)))  ,  {(char_u *)("F3"),  (sizeof("F3" "") - 1) } , FALSE},
+    {TRUE,   (-(('F') + ((int)('K') << 8)))  ,  {(char_u *)("F30"),  (sizeof("F30" "") - 1) } , FALSE},
+    {TRUE,   (-(('F') + ((int)('L') << 8)))  ,  {(char_u *)("F31"),  (sizeof("F31" "") - 1) } , FALSE},
+    {TRUE,   (-(('F') + ((int)('M') << 8)))  ,  {(char_u *)("F32"),  (sizeof("F32" "") - 1) } , FALSE},
+    {TRUE,   (-(('F') + ((int)('N') << 8)))  ,  {(char_u *)("F33"),  (sizeof("F33" "") - 1) } , FALSE},
+    {TRUE,   (-(('F') + ((int)('O') << 8)))  ,  {(char_u *)("F34"),  (sizeof("F34" "") - 1) } , FALSE},
+    {TRUE,   (-(('F') + ((int)('P') << 8)))  ,  {(char_u *)("F35"),  (sizeof("F35" "") - 1) } , FALSE},
+    {TRUE,   (-(('F') + ((int)('Q') << 8)))  ,  {(char_u *)("F36"),  (sizeof("F36" "") - 1) } , FALSE},
+    {TRUE,   (-(('F') + ((int)('R') << 8)))  ,  {(char_u *)("F37"),  (sizeof("F37" "") - 1) } , FALSE},
+
+    {TRUE,   (-(('k') + ((int)('4') << 8)))  ,  {(char_u *)("F4"),  (sizeof("F4" "") - 1) } , FALSE},
+    {TRUE,   (-(('k') + ((int)('5') << 8)))  ,  {(char_u *)("F5"),  (sizeof("F5" "") - 1) } , FALSE},
+    {TRUE,   (-(('k') + ((int)('6') << 8)))  ,  {(char_u *)("F6"),  (sizeof("F6" "") - 1) } , FALSE},
+    {TRUE,   (-(('k') + ((int)('7') << 8)))  ,  {(char_u *)("F7"),  (sizeof("F7" "") - 1) } , FALSE},
+    {TRUE,   (-(('k') + ((int)('8') << 8)))  ,  {(char_u *)("F8"),  (sizeof("F8" "") - 1) } , FALSE},
+    {TRUE,   (-(('k') + ((int)('9') << 8)))  ,  {(char_u *)("F9"),  (sizeof("F9" "") - 1) } , FALSE},
+
+    {TRUE,   (-((KS_EXTRA) + ((int)(KE_FOCUSGAINED) << 8)))  ,  {(char_u *)("FocusGained"),  (sizeof("FocusGained" "") - 1) } , FALSE},
+    {TRUE,   (-((KS_EXTRA) + ((int)(KE_FOCUSLOST) << 8)))  ,  {(char_u *)("FocusLost"),  (sizeof("FocusLost" "") - 1) } , FALSE},
+    {TRUE,   (-(('%') + ((int)('1') << 8)))  ,  {(char_u *)("Help"),  (sizeof("Help" "") - 1) } , FALSE},
+    {TRUE,   (-(('k') + ((int)('h') << 8)))  ,  {(char_u *)("Home"),  (sizeof("Home" "") - 1) } , FALSE},
+    {TRUE,   (-((KS_EXTRA) + ((int)(KE_IGNORE) << 8)))  ,  {(char_u *)("Ignore"),  (sizeof("Ignore" "") - 1) } , FALSE},
+    {TRUE,   (-(('k') + ((int)('I') << 8)))  ,  {(char_u *)("Ins"),  (sizeof("Ins" "") - 1) } , TRUE},
+    {TRUE,   (-(('k') + ((int)('I') << 8)))  ,  {(char_u *)("Insert"),  (sizeof("Insert" "") - 1) } , FALSE},
     {
     FALSE,
-        K_JSBTERM_MOUSE, STR_LITERAL_INIT("JsbMouse"), FALSE},
-    {TRUE, K_K0, STR_LITERAL_INIT("k0"), FALSE},
-    {TRUE, K_K1, STR_LITERAL_INIT("k1"), FALSE},
-    {TRUE, K_K2, STR_LITERAL_INIT("k2"), FALSE},
-    {TRUE, K_K3, STR_LITERAL_INIT("k3"), FALSE},
-    {TRUE, K_K4, STR_LITERAL_INIT("k4"), FALSE},
-    {TRUE, K_K5, STR_LITERAL_INIT("k5"), FALSE},
-    {TRUE, K_K6, STR_LITERAL_INIT("k6"), FALSE},
-    {TRUE, K_K7, STR_LITERAL_INIT("k7"), FALSE},
-    {TRUE, K_K8, STR_LITERAL_INIT("k8"), FALSE},
-    {TRUE, K_K9, STR_LITERAL_INIT("k9"), FALSE},
-
-    {TRUE, K_KDEL, STR_LITERAL_INIT("kDel"), FALSE},
-    {TRUE, K_KDIVIDE, STR_LITERAL_INIT("kDivide"), FALSE},
-    {TRUE, K_KEND, STR_LITERAL_INIT("kEnd"), FALSE},
-    {TRUE, K_KENTER, STR_LITERAL_INIT("kEnter"), FALSE},
-    {TRUE, K_KHOME, STR_LITERAL_INIT("kHome"), FALSE},
-    {TRUE, K_KINS, STR_LITERAL_INIT("kInsert"), FALSE},
-    {TRUE, K_KMINUS, STR_LITERAL_INIT("kMinus"), FALSE},
-    {TRUE, K_KMULTIPLY, STR_LITERAL_INIT("kMultiply"), FALSE},
-    {TRUE, K_KPAGEDOWN, STR_LITERAL_INIT("kPageDown"), FALSE},
-    {TRUE, K_KPAGEUP, STR_LITERAL_INIT("kPageUp"), FALSE},
-    {TRUE, K_KPLUS, STR_LITERAL_INIT("kPlus"), FALSE},
-    {TRUE, K_KPOINT, STR_LITERAL_INIT("kPoint"), FALSE},
-    {TRUE, K_LEFT, STR_LITERAL_INIT("Left"), FALSE},
-    {TRUE, K_LEFTDRAG, STR_LITERAL_INIT("LeftDrag"), FALSE},
-    {TRUE, K_LEFTMOUSE, STR_LITERAL_INIT("LeftMouse"), FALSE},
-    {TRUE, K_LEFTMOUSE_NM, STR_LITERAL_INIT("LeftMouseNM"), FALSE},
-    {TRUE, K_LEFTRELEASE, STR_LITERAL_INIT("LeftRelease"), FALSE},
-    {TRUE, K_LEFTRELEASE_NM, STR_LITERAL_INIT("LeftReleaseNM"), FALSE},
-    {TRUE, NL, STR_LITERAL_INIT("LF"), TRUE},
-    {TRUE, NL, STR_LITERAL_INIT("LineFeed"), TRUE},
-    {TRUE, '<', STR_LITERAL_INIT("lt"), FALSE},
-    {TRUE, K_MIDDLEDRAG, STR_LITERAL_INIT("MiddleDrag"), FALSE},
-    {TRUE, K_MIDDLEMOUSE, STR_LITERAL_INIT("MiddleMouse"), FALSE},
-    {TRUE, K_MIDDLERELEASE, STR_LITERAL_INIT("MiddleRelease"), FALSE},
-    {TRUE, K_MOUSE, STR_LITERAL_INIT("Mouse"), FALSE},
-    {TRUE, K_MOUSEDOWN, STR_LITERAL_INIT("MouseDown"), TRUE},
-    {TRUE, K_MOUSEMOVE, STR_LITERAL_INIT("MouseMove"), FALSE},
-    {TRUE, K_MOUSEUP, STR_LITERAL_INIT("MouseUp"), TRUE},
+          (-((KS_JSBTERM_MOUSE) + ((int)( ('X') ) << 8)))  ,  {(char_u *)("JsbMouse"),  (sizeof("JsbMouse" "") - 1) } , FALSE},
+    {TRUE,   (-(('K') + ((int)('C') << 8)))  ,  {(char_u *)("k0"),  (sizeof("k0" "") - 1) } , FALSE},
+    {TRUE,   (-(('K') + ((int)('D') << 8)))  ,  {(char_u *)("k1"),  (sizeof("k1" "") - 1) } , FALSE},
+    {TRUE,   (-(('K') + ((int)('E') << 8)))  ,  {(char_u *)("k2"),  (sizeof("k2" "") - 1) } , FALSE},
+    {TRUE,   (-(('K') + ((int)('F') << 8)))  ,  {(char_u *)("k3"),  (sizeof("k3" "") - 1) } , FALSE},
+    {TRUE,   (-(('K') + ((int)('G') << 8)))  ,  {(char_u *)("k4"),  (sizeof("k4" "") - 1) } , FALSE},
+    {TRUE,   (-(('K') + ((int)('H') << 8)))  ,  {(char_u *)("k5"),  (sizeof("k5" "") - 1) } , FALSE},
+    {TRUE,   (-(('K') + ((int)('I') << 8)))  ,  {(char_u *)("k6"),  (sizeof("k6" "") - 1) } , FALSE},
+    {TRUE,   (-(('K') + ((int)('J') << 8)))  ,  {(char_u *)("k7"),  (sizeof("k7" "") - 1) } , FALSE},
+    {TRUE,   (-(('K') + ((int)('K') << 8)))  ,  {(char_u *)("k8"),  (sizeof("k8" "") - 1) } , FALSE},
+    {TRUE,   (-(('K') + ((int)('L') << 8)))  ,  {(char_u *)("k9"),  (sizeof("k9" "") - 1) } , FALSE},
+
+    {TRUE,   (-((KS_EXTRA) + ((int)(KE_KDEL) << 8)))  ,  {(char_u *)("kDel"),  (sizeof("kDel" "") - 1) } , FALSE},
+    {TRUE,   (-(('K') + ((int)('8') << 8)))  ,  {(char_u *)("kDivide"),  (sizeof("kDivide" "") - 1) } , FALSE},
+    {TRUE,   (-(('K') + ((int)('4') << 8)))  ,  {(char_u *)("kEnd"),  (sizeof("kEnd" "") - 1) } , FALSE},
+    {TRUE,   (-(('K') + ((int)('A') << 8)))  ,  {(char_u *)("kEnter"),  (sizeof("kEnter" "") - 1) } , FALSE},
+    {TRUE,   (-(('K') + ((int)('1') << 8)))  ,  {(char_u *)("kHome"),  (sizeof("kHome" "") - 1) } , FALSE},
+    {TRUE,   (-((KS_EXTRA) + ((int)(KE_KINS) << 8)))  ,  {(char_u *)("kInsert"),  (sizeof("kInsert" "") - 1) } , FALSE},
+    {TRUE,   (-(('K') + ((int)('7') << 8)))  ,  {(char_u *)("kMinus"),  (sizeof("kMinus" "") - 1) } , FALSE},
+    {TRUE,   (-(('K') + ((int)('9') << 8)))  ,  {(char_u *)("kMultiply"),  (sizeof("kMultiply" "") - 1) } , FALSE},
+    {TRUE,   (-(('K') + ((int)('5') << 8)))  ,  {(char_u *)("kPageDown"),  (sizeof("kPageDown" "") - 1) } , FALSE},
+    {TRUE,   (-(('K') + ((int)('3') << 8)))  ,  {(char_u *)("kPageUp"),  (sizeof("kPageUp" "") - 1) } , FALSE},
+    {TRUE,   (-(('K') + ((int)('6') << 8)))  ,  {(char_u *)("kPlus"),  (sizeof("kPlus" "") - 1) } , FALSE},
+    {TRUE,   (-(('K') + ((int)('B') << 8)))  ,  {(char_u *)("kPoint"),  (sizeof("kPoint" "") - 1) } , FALSE},
+    {TRUE,   (-(('k') + ((int)('l') << 8)))  ,  {(char_u *)("Left"),  (sizeof("Left" "") - 1) } , FALSE},
+    {TRUE,   (-((KS_EXTRA) + ((int)(KE_LEFTDRAG) << 8)))  ,  {(char_u *)("LeftDrag"),  (sizeof("LeftDrag" "") - 1) } , FALSE},
+    {TRUE,   (-((KS_EXTRA) + ((int)(KE_LEFTMOUSE) << 8)))  ,  {(char_u *)("LeftMouse"),  (sizeof("LeftMouse" "") - 1) } , FALSE},
+    {TRUE,   (-((KS_EXTRA) + ((int)(KE_LEFTMOUSE_NM) << 8)))  ,  {(char_u *)("LeftMouseNM"),  (sizeof("LeftMouseNM" "") - 1) } , FALSE},
+    {TRUE,   (-((KS_EXTRA) + ((int)(KE_LEFTRELEASE) << 8)))  ,  {(char_u *)("LeftRelease"),  (sizeof("LeftRelease" "") - 1) } , FALSE},
+    {TRUE,   (-((KS_EXTRA) + ((int)(KE_LEFTRELEASE_NM) << 8)))  ,  {(char_u *)("LeftReleaseNM"),  (sizeof("LeftReleaseNM" "") - 1) } , FALSE},
+    {TRUE, NL,  {(char_u *)("LF"),  (sizeof("LF" "") - 1) } , TRUE},
+    {TRUE, NL,  {(char_u *)("LineFeed"),  (sizeof("LineFeed" "") - 1) } , TRUE},
+    {TRUE, '<',  {(char_u *)("lt"),  (sizeof("lt" "") - 1) } , FALSE},
+    {TRUE,   (-((KS_EXTRA) + ((int)(KE_MIDDLEDRAG) << 8)))  ,  {(char_u *)("MiddleDrag"),  (sizeof("MiddleDrag" "") - 1) } , FALSE},
+    {TRUE,   (-((KS_EXTRA) + ((int)(KE_MIDDLEMOUSE) << 8)))  ,  {(char_u *)("MiddleMouse"),  (sizeof("MiddleMouse" "") - 1) } , FALSE},
+    {TRUE,   (-((KS_EXTRA) + ((int)(KE_MIDDLERELEASE) << 8)))  ,  {(char_u *)("MiddleRelease"),  (sizeof("MiddleRelease" "") - 1) } , FALSE},
+    {TRUE,   (-((KS_MOUSE) + ((int)( ('X') ) << 8)))  ,  {(char_u *)("Mouse"),  (sizeof("Mouse" "") - 1) } , FALSE},
+    {TRUE,   (-((KS_EXTRA) + ((int)(KE_MOUSEDOWN) << 8)))  ,  {(char_u *)("MouseDown"),  (sizeof("MouseDown" "") - 1) } , TRUE},
+    {TRUE,   (-((KS_EXTRA) + ((int)(KE_MOUSEMOVE) << 8)))  ,  {(char_u *)("MouseMove"),  (sizeof("MouseMove" "") - 1) } , FALSE},
+    {TRUE,   (-((KS_EXTRA) + ((int)(KE_MOUSEUP) << 8)))  ,  {(char_u *)("MouseUp"),  (sizeof("MouseUp" "") - 1) } , TRUE},
     {
     FALSE,
-        K_NETTERM_MOUSE, STR_LITERAL_INIT("NetMouse"), FALSE},
-    {TRUE, NL, STR_LITERAL_INIT("NewLine"), TRUE},
-    {TRUE, NL, STR_LITERAL_INIT("NL"), FALSE},
-    {TRUE, K_ZERO, STR_LITERAL_INIT("Nul"), FALSE},
-    {TRUE, OSC, STR_LITERAL_INIT("OSC"), FALSE},
-    {TRUE, K_PAGEDOWN, STR_LITERAL_INIT("PageDown"), FALSE},
-    {TRUE, K_PAGEUP, STR_LITERAL_INIT("PageUp"), FALSE},
-    {TRUE, K_PE, STR_LITERAL_INIT("PasteEnd"), FALSE},
-    {TRUE, K_PS, STR_LITERAL_INIT("PasteStart"), FALSE},
-    {TRUE, K_PLUG, STR_LITERAL_INIT("Plug"), FALSE},
+          (-((KS_NETTERM_MOUSE) + ((int)( ('X') ) << 8)))  ,  {(char_u *)("NetMouse"),  (sizeof("NetMouse" "") - 1) } , FALSE},
+    {TRUE, NL,  {(char_u *)("NewLine"),  (sizeof("NewLine" "") - 1) } , TRUE},
+    {TRUE, NL,  {(char_u *)("NL"),  (sizeof("NL" "") - 1) } , FALSE},
+    {TRUE,   (-((KS_ZERO) + ((int)( ('X') ) << 8)))  ,  {(char_u *)("Nul"),  (sizeof("Nul" "") - 1) } , FALSE},
+    {TRUE, OSC,  {(char_u *)("OSC"),  (sizeof("OSC" "") - 1) } , FALSE},
+    {TRUE,   (-(('k') + ((int)('N') << 8)))  ,  {(char_u *)("PageDown"),  (sizeof("PageDown" "") - 1) } , FALSE},
+    {TRUE,   (-(('k') + ((int)('P') << 8)))  ,  {(char_u *)("PageUp"),  (sizeof("PageUp" "") - 1) } , FALSE},
+    {TRUE,   (-(('P') + ((int)('E') << 8)))  ,  {(char_u *)("PasteEnd"),  (sizeof("PasteEnd" "") - 1) } , FALSE},
+    {TRUE,   (-(('P') + ((int)('S') << 8)))  ,  {(char_u *)("PasteStart"),  (sizeof("PasteStart" "") - 1) } , FALSE},
+    {TRUE,   (-((KS_EXTRA) + ((int)(KE_PLUG) << 8)))  ,  {(char_u *)("Plug"),  (sizeof("Plug" "") - 1) } , FALSE},
     {
     FALSE,
-        K_PTERM_MOUSE, STR_LITERAL_INIT("PtermMouse"), FALSE},
-    {TRUE, CAR, STR_LITERAL_INIT("Return"), TRUE},
-    {TRUE, K_RIGHT, STR_LITERAL_INIT("Right"), FALSE},
-    {TRUE, K_RIGHTDRAG, STR_LITERAL_INIT("RightDrag"), FALSE},
-    {TRUE, K_RIGHTMOUSE, STR_LITERAL_INIT("RightMouse"), FALSE},
-    {TRUE, K_RIGHTRELEASE, STR_LITERAL_INIT("RightRelease"), FALSE},
-    {TRUE, K_SCRIPT_COMMAND, STR_LITERAL_INIT("ScriptCmd"), FALSE},
-    {TRUE, K_MOUSEUP, STR_LITERAL_INIT("ScrollWheelDown"), FALSE},
-    {TRUE, K_MOUSERIGHT, STR_LITERAL_INIT("ScrollWheelLeft"), FALSE},
-    {TRUE, K_MOUSELEFT, STR_LITERAL_INIT("ScrollWheelRight"), FALSE},
-    {TRUE, K_MOUSEDOWN, STR_LITERAL_INIT("ScrollWheelUp"), FALSE},
-    {TRUE, K_SGR_MOUSE, STR_LITERAL_INIT("SgrMouse"), FALSE},
-    {TRUE, K_SGR_MOUSERELEASE, STR_LITERAL_INIT("SgrMouseRelease"), FALSE},
+          (-((KS_PTERM_MOUSE) + ((int)( ('X') ) << 8)))  ,  {(char_u *)("PtermMouse"),  (sizeof("PtermMouse" "") - 1) } , FALSE},
+    {TRUE, CAR,  {(char_u *)("Return"),  (sizeof("Return" "") - 1) } , TRUE},
+    {TRUE,   (-(('k') + ((int)('r') << 8)))  ,  {(char_u *)("Right"),  (sizeof("Right" "") - 1) } , FALSE},
+    {TRUE,   (-((KS_EXTRA) + ((int)(KE_RIGHTDRAG) << 8)))  ,  {(char_u *)("RightDrag"),  (sizeof("RightDrag" "") - 1) } , FALSE},
+    {TRUE,   (-((KS_EXTRA) + ((int)(KE_RIGHTMOUSE) << 8)))  ,  {(char_u *)("RightMouse"),  (sizeof("RightMouse" "") - 1) } , FALSE},
+    {TRUE,   (-((KS_EXTRA) + ((int)(KE_RIGHTRELEASE) << 8)))  ,  {(char_u *)("RightRelease"),  (sizeof("RightRelease" "") - 1) } , FALSE},
+    {TRUE,   (-((KS_EXTRA) + ((int)(KE_SCRIPT_COMMAND) << 8)))  ,  {(char_u *)("ScriptCmd"),  (sizeof("ScriptCmd" "") - 1) } , FALSE},
+    {TRUE,   (-((KS_EXTRA) + ((int)(KE_MOUSEUP) << 8)))  ,  {(char_u *)("ScrollWheelDown"),  (sizeof("ScrollWheelDown" "") - 1) } , FALSE},
+    {TRUE,   (-((KS_EXTRA) + ((int)(KE_MOUSERIGHT) << 8)))  ,  {(char_u *)("ScrollWheelLeft"),  (sizeof("ScrollWheelLeft" "") - 1) } , FALSE},
+    {TRUE,   (-((KS_EXTRA) + ((int)(KE_MOUSELEFT) << 8)))  ,  {(char_u *)("ScrollWheelRight"),  (sizeof("ScrollWheelRight" "") - 1) } , FALSE},
+    {TRUE,   (-((KS_EXTRA) + ((int)(KE_MOUSEDOWN) << 8)))  ,  {(char_u *)("ScrollWheelUp"),  (sizeof("ScrollWheelUp" "") - 1) } , FALSE},
+    {TRUE,   (-((KS_SGR_MOUSE) + ((int)( ('X') ) << 8)))  ,  {(char_u *)("SgrMouse"),  (sizeof("SgrMouse" "") - 1) } , FALSE},
+    {TRUE,   (-((KS_SGR_MOUSE_RELEASE) + ((int)( ('X') ) << 8)))  ,  {(char_u *)("SgrMouseRelease"),  (sizeof("SgrMouseRelease" "") - 1) } , FALSE},
     {
     FALSE,
-        K_SNR, STR_LITERAL_INIT("SNR"), FALSE},
-    {TRUE, ' ', STR_LITERAL_INIT("Space"), FALSE},
-    {TRUE, TAB, STR_LITERAL_INIT("Tab"), FALSE},
-    {TRUE, K_TAB, STR_LITERAL_INIT("Tab"), FALSE},
-    {TRUE, K_UNDO, STR_LITERAL_INIT("Undo"), FALSE},
-    {TRUE, K_UP, STR_LITERAL_INIT("Up"), FALSE},
+          (-((KS_EXTRA) + ((int)(KE_SNR) << 8)))  ,  {(char_u *)("SNR"),  (sizeof("SNR" "") - 1) } , FALSE},
+    {TRUE, ' ',  {(char_u *)("Space"),  (sizeof("Space" "") - 1) } , FALSE},
+    {TRUE, TAB,  {(char_u *)("Tab"),  (sizeof("Tab" "") - 1) } , FALSE},
+    {TRUE,   (-((KS_EXTRA) + ((int)(KE_TAB) << 8)))  ,  {(char_u *)("Tab"),  (sizeof("Tab" "") - 1) } , FALSE},
+    {TRUE,   (-(('&') + ((int)('8') << 8)))  ,  {(char_u *)("Undo"),  (sizeof("Undo" "") - 1) } , FALSE},
+    {TRUE,   (-(('k') + ((int)('u') << 8)))  ,  {(char_u *)("Up"),  (sizeof("Up" "") - 1) } , FALSE},
     {
     FALSE,
-        K_URXVT_MOUSE, STR_LITERAL_INIT("UrxvtMouse"), FALSE},
-    {TRUE, K_X1DRAG, STR_LITERAL_INIT("X1Drag"), FALSE},
-    {TRUE, K_X1MOUSE, STR_LITERAL_INIT("X1Mouse"), FALSE},
-    {TRUE, K_X1RELEASE, STR_LITERAL_INIT("X1Release"), FALSE},
-    {TRUE, K_X2DRAG, STR_LITERAL_INIT("X2Drag"), FALSE},
-    {TRUE, K_X2MOUSE, STR_LITERAL_INIT("X2Mouse"), FALSE},
-    {TRUE, K_X2RELEASE, STR_LITERAL_INIT("X2Release"), FALSE},
-    {TRUE, K_CSI, STR_LITERAL_INIT("xCSI"), FALSE},
-    {TRUE, K_XDOWN, STR_LITERAL_INIT("xDown"), FALSE},
-    {TRUE, K_XEND, STR_LITERAL_INIT("xEnd"), FALSE},
-    {TRUE, K_XF1, STR_LITERAL_INIT("xF1"), FALSE},
-    {TRUE, K_XF2, STR_LITERAL_INIT("xF2"), FALSE},
-    {TRUE, K_XF3, STR_LITERAL_INIT("xF3"), FALSE},
-    {TRUE, K_XF4, STR_LITERAL_INIT("xF4"), FALSE},
-    {TRUE, K_XHOME, STR_LITERAL_INIT("xHome"), FALSE},
-    {TRUE, K_XLEFT, STR_LITERAL_INIT("xLeft"), FALSE},
-    {TRUE, K_OSC, STR_LITERAL_INIT("xOSC"), FALSE},
-    {TRUE, K_XRIGHT, STR_LITERAL_INIT("xRight"), FALSE},
-    {TRUE, K_XUP, STR_LITERAL_INIT("xUp"), FALSE},
-    {TRUE, K_ZEND, STR_LITERAL_INIT("zEnd"), FALSE},
-    {TRUE, K_ZHOME, STR_LITERAL_INIT("zHome"), FALSE}
+          (-((KS_URXVT_MOUSE) + ((int)( ('X') ) << 8)))  ,  {(char_u *)("UrxvtMouse"),  (sizeof("UrxvtMouse" "") - 1) } , FALSE},
+    {TRUE,   (-((KS_EXTRA) + ((int)(KE_X1DRAG) << 8)))  ,  {(char_u *)("X1Drag"),  (sizeof("X1Drag" "") - 1) } , FALSE},
+    {TRUE,   (-((KS_EXTRA) + ((int)(KE_X1MOUSE) << 8)))  ,  {(char_u *)("X1Mouse"),  (sizeof("X1Mouse" "") - 1) } , FALSE},
+    {TRUE,   (-((KS_EXTRA) + ((int)(KE_X1RELEASE) << 8)))  ,  {(char_u *)("X1Release"),  (sizeof("X1Release" "") - 1) } , FALSE},
+    {TRUE,   (-((KS_EXTRA) + ((int)(KE_X2DRAG) << 8)))  ,  {(char_u *)("X2Drag"),  (sizeof("X2Drag" "") - 1) } , FALSE},
+    {TRUE,   (-((KS_EXTRA) + ((int)(KE_X2MOUSE) << 8)))  ,  {(char_u *)("X2Mouse"),  (sizeof("X2Mouse" "") - 1) } , FALSE},
+    {TRUE,   (-((KS_EXTRA) + ((int)(KE_X2RELEASE) << 8)))  ,  {(char_u *)("X2Release"),  (sizeof("X2Release" "") - 1) } , FALSE},
+    {TRUE,   (-((KS_EXTRA) + ((int)(KE_CSI) << 8)))  ,  {(char_u *)("xCSI"),  (sizeof("xCSI" "") - 1) } , FALSE},
+    {TRUE,   (-((KS_EXTRA) + ((int)(KE_XDOWN) << 8)))  ,  {(char_u *)("xDown"),  (sizeof("xDown" "") - 1) } , FALSE},
+    {TRUE,   (-((KS_EXTRA) + ((int)(KE_XEND) << 8)))  ,  {(char_u *)("xEnd"),  (sizeof("xEnd" "") - 1) } , FALSE},
+    {TRUE,   (-((KS_EXTRA) + ((int)(KE_XF1) << 8)))  ,  {(char_u *)("xF1"),  (sizeof("xF1" "") - 1) } , FALSE},
+    {TRUE,   (-((KS_EXTRA) + ((int)(KE_XF2) << 8)))  ,  {(char_u *)("xF2"),  (sizeof("xF2" "") - 1) } , FALSE},
+    {TRUE,   (-((KS_EXTRA) + ((int)(KE_XF3) << 8)))  ,  {(char_u *)("xF3"),  (sizeof("xF3" "") - 1) } , FALSE},
+    {TRUE,   (-((KS_EXTRA) + ((int)(KE_XF4) << 8)))  ,  {(char_u *)("xF4"),  (sizeof("xF4" "") - 1) } , FALSE},
+    {TRUE,   (-((KS_EXTRA) + ((int)(KE_XHOME) << 8)))  ,  {(char_u *)("xHome"),  (sizeof("xHome" "") - 1) } , FALSE},
+    {TRUE,   (-((KS_EXTRA) + ((int)(KE_XLEFT) << 8)))  ,  {(char_u *)("xLeft"),  (sizeof("xLeft" "") - 1) } , FALSE},
+    {TRUE,   (-((KS_EXTRA) + ((int)(KE_OSC) << 8)))  ,  {(char_u *)("xOSC"),  (sizeof("xOSC" "") - 1) } , FALSE},
+    {TRUE,   (-((KS_EXTRA) + ((int)(KE_XRIGHT) << 8)))  ,  {(char_u *)("xRight"),  (sizeof("xRight" "") - 1) } , FALSE},
+    {TRUE,   (-((KS_EXTRA) + ((int)(KE_XUP) << 8)))  ,  {(char_u *)("xUp"),  (sizeof("xUp" "") - 1) } , FALSE},
+    {TRUE,   (-((KS_EXTRA) + ((int)(KE_ZEND) << 8)))  ,  {(char_u *)("zEnd"),  (sizeof("zEnd" "") - 1) } , FALSE},
+    {TRUE,   (-((KS_EXTRA) + ((int)(KE_ZHOME) << 8)))  ,  {(char_u *)("zHome"),  (sizeof("zHome" "") - 1) } , FALSE}
 };
 
     static int
@@ -96100,7 +94186,7 @@
 {
     int     i;
 
-    c = TOUPPER_ASC(c);
+    c =  (((c) < 'a' || (c) > 'z') ? (c) : (c) - ('a' - 'A')) ;
     for (i = 0; mod_mask_table[i].mod_mask != 0; i++)
     {
         if (c == mod_mask_table[i].name)
@@ -96126,16 +94212,16 @@
     if (key == TAB && (*modifiers & MOD_MASK_SHIFT))
     {
         *modifiers &= ~MOD_MASK_SHIFT;
-        return K_S_TAB;
+        return   (-(('k') + ((int)('B') << 8)))  ;
     }
-    key0 = KEY2TERMCAP0(key);
-    key1 = KEY2TERMCAP1(key);
+    key0 =  ((-(key)) & 0xff) ;
+    key1 =  (((unsigned)(-(key)) >> 8) & 0xff) ;
     for (i = 0; modifier_keys_table[i] != NUL; i += MOD_KEYS_ENTRY_SIZE)
     {
         if (key0 == modifier_keys_table[i + 3] && key1 == modifier_keys_table[i + 4] && (*modifiers & modifier_keys_table[i]))
         {
             *modifiers &= ~modifier_keys_table[i];
-            return TERMCAP2KEY(modifier_keys_table[i + 1], modifier_keys_table[i + 2]);
+            return  (-((modifier_keys_table[i + 1]) + ((int)(modifier_keys_table[i + 2]) << 8))) ;
         }
     }
     return key;
@@ -96146,38 +94232,38 @@
 {
     switch (key)
     {
-        case K_XUP:
-            return K_UP;
-        case K_XDOWN:
-            return K_DOWN;
-        case K_XLEFT:
-            return K_LEFT;
-        case K_XRIGHT:
-            return K_RIGHT;
-        case K_XHOME:
-            return K_HOME;
-        case K_ZHOME:
-            return K_HOME;
-        case K_XEND:
-            return K_END;
-        case K_ZEND:
-            return K_END;
-        case K_XF1:
-            return K_F1;
-        case K_XF2:
-            return K_F2;
-        case K_XF3:
-            return K_F3;
-        case K_XF4:
-            return K_F4;
-        case K_S_XF1:
-            return K_S_F1;
-        case K_S_XF2:
-            return K_S_F2;
-        case K_S_XF3:
-            return K_S_F3;
-        case K_S_XF4:
-            return K_S_F4;
+        case   (-((KS_EXTRA) + ((int)(KE_XUP) << 8)))  :
+            return   (-(('k') + ((int)('u') << 8)))  ;
+        case   (-((KS_EXTRA) + ((int)(KE_XDOWN) << 8)))  :
+            return   (-(('k') + ((int)('d') << 8)))  ;
+        case   (-((KS_EXTRA) + ((int)(KE_XLEFT) << 8)))  :
+            return   (-(('k') + ((int)('l') << 8)))  ;
+        case   (-((KS_EXTRA) + ((int)(KE_XRIGHT) << 8)))  :
+            return   (-(('k') + ((int)('r') << 8)))  ;
+        case   (-((KS_EXTRA) + ((int)(KE_XHOME) << 8)))  :
+            return   (-(('k') + ((int)('h') << 8)))  ;
+        case   (-((KS_EXTRA) + ((int)(KE_ZHOME) << 8)))  :
+            return   (-(('k') + ((int)('h') << 8)))  ;
+        case   (-((KS_EXTRA) + ((int)(KE_XEND) << 8)))  :
+            return   (-(('@') + ((int)('7') << 8)))  ;
+        case   (-((KS_EXTRA) + ((int)(KE_ZEND) << 8)))  :
+            return   (-(('@') + ((int)('7') << 8)))  ;
+        case   (-((KS_EXTRA) + ((int)(KE_XF1) << 8)))  :
+            return   (-(('k') + ((int)('1') << 8)))  ;
+        case   (-((KS_EXTRA) + ((int)(KE_XF2) << 8)))  :
+            return   (-(('k') + ((int)('2') << 8)))  ;
+        case   (-((KS_EXTRA) + ((int)(KE_XF3) << 8)))  :
+            return   (-(('k') + ((int)('3') << 8)))  ;
+        case   (-((KS_EXTRA) + ((int)(KE_XF4) << 8)))  :
+            return   (-(('k') + ((int)('4') << 8)))  ;
+        case   (-((KS_EXTRA) + ((int)(KE_S_XF1) << 8)))  :
+            return   (-((KS_EXTRA) + ((int)(KE_S_F1) << 8)))  ;
+        case   (-((KS_EXTRA) + ((int)(KE_S_XF2) << 8)))  :
+            return   (-((KS_EXTRA) + ((int)(KE_S_F2) << 8)))  ;
+        case   (-((KS_EXTRA) + ((int)(KE_S_XF3) << 8)))  :
+            return   (-((KS_EXTRA) + ((int)(KE_S_F3) << 8)))  ;
+        case   (-((KS_EXTRA) + ((int)(KE_S_XF4) << 8)))  :
+            return   (-((KS_EXTRA) + ((int)(KE_S_F4) << 8)))  ;
     }
     return key;
 }
@@ -96194,19 +94280,19 @@
     string[0] = '<';
     idx = 1;
 
-    if (IS_SPECIAL(c) && KEY2TERMCAP0(c) == KS_KEY)
+    if ( ((c) < 0)  &&  ((-(c)) & 0xff)  == KS_KEY)
     {
-        c = KEY2TERMCAP1(c);
+        c =  (((unsigned)(-(c)) >> 8) & 0xff) ;
     }
 
-    if (IS_SPECIAL(c))
+    if ( ((c) < 0) )
     {
         for (i = 0; modifier_keys_table[i] != 0; i += MOD_KEYS_ENTRY_SIZE)
         {
-            if (       KEY2TERMCAP0(c) == (int)modifier_keys_table[i + 1] && (int)KEY2TERMCAP1(c) == (int)modifier_keys_table[i + 2])
+            if (        ((-(c)) & 0xff)  == (int)modifier_keys_table[i + 1] && (int) (((unsigned)(-(c)) >> 8) & 0xff)  == (int)modifier_keys_table[i + 2])
             {
                 modifiers |= modifier_keys_table[i];
-                c = TERMCAP2KEY(modifier_keys_table[i + 3], modifier_keys_table[i + 4]);
+                c =  (-((modifier_keys_table[i + 3]) + ((int)(modifier_keys_table[i + 4]) << 8))) ;
                 break;
             }
         }
@@ -96240,12 +94326,12 @@
 
     if (table_idx < 0)
     {
-        if (IS_SPECIAL(c))
+        if ( ((c) < 0) )
         {
             string[idx++] = 't';
             string[idx++] = '_';
-            string[idx++] = KEY2TERMCAP0(c);
-            string[idx++] = KEY2TERMCAP1(c);
+            string[idx++] =  ((-(c)) & 0xff) ;
+            string[idx++] =  (((unsigned)(-(c)) >> 8) & 0xff) ;
         }
         else
         {
@@ -96276,7 +94362,7 @@
 
         if (s->length + idx + 2 <= MAX_KEY_NAME_LEN)
         {
-            STRCPY(string + idx, s->string);
+             strcpy((char *)(string + idx), (char *)(s->string)) ;
             idx += (int)s->length;
         }
     }
@@ -96308,16 +94394,16 @@
 
     if (modifiers != 0)
     {
-        dst[dlen++] = K_SPECIAL;
+        dst[dlen++] =  (0x80) ;
         dst[dlen++] = KS_MODIFIER;
         dst[dlen++] = modifiers;
     }
 
-    if (IS_SPECIAL(key))
+    if ( ((key) < 0) )
     {
-        dst[dlen++] = K_SPECIAL;
-        dst[dlen++] = KEY2TERMCAP0(key);
-        dst[dlen++] = KEY2TERMCAP1(key);
+        dst[dlen++] =  (0x80) ;
+        dst[dlen++] =  ((-(key)) & 0xff) ;
+        dst[dlen++] =  (((unsigned)(-(key)) >> 8) & 0xff) ;
     }
     else if (escape_ks)
     {
@@ -96389,9 +94475,9 @@
         {
             bp += 3;
         }
-        else if (STRNICMP(bp, "char-", 5) == 0)
+        else if ( strncasecmp((char *)(bp), (char *)("char-"), (5))  == 0)
         {
-            vim_str2nr(bp + 5, NULL, &l, STR2NR_ALL, NULL, NULL, 0, TRUE, NULL);
+            vim_str2nr(bp + 5, NULL, &l,  (STR2NR_BIN + STR2NR_OCT + STR2NR_HEX + STR2NR_OOCT) , NULL, NULL, 0, TRUE, NULL);
             if (l == 0)
             {
                 emsg(_(e_invalid_argument));
@@ -96422,9 +94508,9 @@
 
         if (bp >= last_dash)
         {
-            if (STRNICMP(last_dash + 1, "char-", 5) == 0 && VIM_ISDIGIT(last_dash[6]))
+            if ( strncasecmp((char *)(last_dash + 1), (char *)("char-"), (5))  == 0 &&  ((unsigned)(last_dash[6]) - '0' < 10) )
             {
-                vim_str2nr(last_dash + 6, NULL, &l, STR2NR_ALL, NULL, &n, 0, TRUE, NULL);
+                vim_str2nr(last_dash + 6, NULL, &l,  (STR2NR_BIN + STR2NR_OCT + STR2NR_HEX + STR2NR_OOCT) , NULL, &n, 0, TRUE, NULL);
                 if (l == 0)
                 {
                     emsg(_(e_invalid_argument));
@@ -96450,7 +94536,7 @@
                 }
                 if (modifiers != 0 && last_dash[l + off] == '>')
                 {
-                    key = PTR2CHAR(last_dash + off);
+                    key =  (has_mbyte ? mb_ptr2char(last_dash + off) : (int)*(last_dash + off)) ;
                 }
                 else
                 {
@@ -96468,11 +94554,11 @@
 
                 if ((flags & FSK_KEYCODE) == 0)
                 {
-                    if (key == K_BS)
+                    if (key ==   (-(('k') + ((int)('b') << 8)))  )
                     {
                         key = BS;
                     }
-                    else if (key == K_DEL || key == K_KDEL)
+                    else if (key ==   (-(('k') + ((int)('D') << 8)))   || key ==   (-((KS_EXTRA) + ((int)(KE_KDEL) << 8)))  )
                     {
                         key = DEL;
                     }
@@ -96488,11 +94574,11 @@
                     }
                     else
                     {
-                        key = K_ESC;
+                        key =   (-((KS_EXTRA) + ((int)(KE_ESC) << 8)))  ;
                     }
                 }
 
-                if (!IS_SPECIAL(key))
+                if (! ((key) < 0) )
                 {
                     key = extract_modifiers(key, &modifiers, flags & FSK_SIMPLIFY, did_simplify);
                 }
@@ -96514,9 +94600,9 @@
         return key;
     }
 
-    if (ASCII_ISALPHA(key))
+    if ( ( ((unsigned)(key) - 'A' < 26)  ||  ((unsigned)(key) - 'a' < 26) ) )
     {
-        return no_reduce_keys == 0 ? TOUPPER_ASC(key) : key;
+        return no_reduce_keys == 0 ?  (((key) < 'a' || (key) > 'z') ? (key) : (key) - ('a' - 'A'))  : key;
     }
     if (key == '2')
     {
@@ -96560,27 +94646,27 @@
 {
     int modifiers = *modp;
 
-    if ((modifiers & MOD_MASK_SHIFT) && ASCII_ISALPHA(key))
+    if ((modifiers & MOD_MASK_SHIFT) &&  ( ((unsigned)(key) - 'A' < 26)  ||  ((unsigned)(key) - 'a' < 26) ) )
     {
-        key = TOUPPER_ASC(key);
+        key =  (((key) < 'a' || (key) > 'z') ? (key) : (key) - ('a' - 'A')) ;
         if (simplify || modifiers == MOD_MASK_SHIFT || modifiers == (MOD_MASK_SHIFT | MOD_MASK_ALT) || modifiers == (MOD_MASK_SHIFT | MOD_MASK_META))
         {
             modifiers &= ~MOD_MASK_SHIFT;
         }
     }
 
-    if ((modifiers & MOD_MASK_CTRL) && ASCII_ISALPHA(key))
+    if ((modifiers & MOD_MASK_CTRL) &&  ( ((unsigned)(key) - 'A' < 26)  ||  ((unsigned)(key) - 'a' < 26) ) )
     {
-        key = TOUPPER_ASC(key);
+        key =  (((key) < 'a' || (key) > 'z') ? (key) : (key) - ('a' - 'A')) ;
     }
 
-    if (simplify && (modifiers & MOD_MASK_CTRL) && ((key >= '?' && key <= '_') || ASCII_ISALPHA(key)))
+    if (simplify && (modifiers & MOD_MASK_CTRL) && ((key >= '?' && key <= '_') ||  ( ((unsigned)(key) - 'A' < 26)  ||  ((unsigned)(key) - 'a' < 26) ) ))
     {
-        key = Ctrl_chr(key);
+        key =  ( (((key) < 'a' || (key) > 'z') ? (key) : (key) - ('a' - 'A'))  ^ 0x40) ;
         modifiers &= ~MOD_MASK_CTRL;
         if (key == NUL)
         {
-            key = K_ZERO;
+            key =   (-((KS_ZERO) + ((int)( ('X') ) << 8)))  ;
         }
         if (did_simplify != NULL)
         {
@@ -96607,7 +94693,7 @@
 {
     int     i;
 
-    for (i = 0; i < (int)ARRAY_LENGTH(key_names_table); i++)
+    for (i = 0; i < (int) (sizeof(key_names_table) / sizeof((key_names_table)[0])) ; i++)
     {
         if (c == key_names_table[i].key && !key_names_table[i].is_alt)
         {
@@ -96632,7 +94718,7 @@
 
     while (vim_isNormalIDc(*p1) && *p2 != NUL)
     {
-        if ((result = TOLOWER_ASC(*p1) - TOLOWER_ASC(*p2)) != 0)
+        if ((result =  (((*p1) < 'A' || (*p1) > 'Z') ? (*p1) : (*p1) + ('a' - 'A'))  -  (((*p2) < 'A' || (*p2) > 'Z') ? (*p2) : (*p2) + ('a' - 'A')) ) != 0)
         {
             break;
         }
@@ -96670,7 +94756,7 @@
         string[2] = NUL;
         if (add_termcap_entry(string, FALSE) == OK)
         {
-            return TERMCAP2KEY(name[2], name[3]);
+            return  (-((name[2]) + ((int)(name[3]) << 8))) ;
         }
     }
     else
@@ -96683,11 +94769,11 @@
         target.name.string = name;
         target.name.length = 0;
 
-        entry = (struct key_name_entry *)bsearch(&target, &key_names_table, ARRAY_LENGTH(key_names_table), sizeof(key_names_table[0]), cmp_key_name_entry);
+        entry = (struct key_name_entry *)bsearch(&target, &key_names_table,  (sizeof(key_names_table) / sizeof((key_names_table)[0])) , sizeof(key_names_table[0]), cmp_key_name_entry);
         if (entry != NULL && entry->enabled)
         {
             int key = entry->key;
-            return key == K_TAB ? TAB : key;
+            return key ==   (-((KS_EXTRA) + ((int)(KE_TAB) << 8)))   ? TAB : key;
         }
     }
 
@@ -96697,7 +94783,7 @@
     char_u *
 get_key_name(int i)
 {
-    if (i < 0 || i >= (int)ARRAY_LENGTH(key_names_table))
+    if (i < 0 || i >= (int) (sizeof(key_names_table) / sizeof((key_names_table)[0])) )
     {
         return NULL;
     }
@@ -96757,15 +94843,15 @@
     switch (t)
     {
     case EOL_DOS:
-        p = FF_DOS;
+        p =  "dos" ;
         curbuf->b_p_tx = TRUE;
         break;
     case EOL_UNIX:
-        p = FF_UNIX;
+        p =  "unix" ;
         curbuf->b_p_tx = FALSE;
         break;
     case EOL_MAC:
-        p = FF_MAC;
+        p =  "mac" ;
         curbuf->b_p_tx = FALSE;
         break;
     }
@@ -96833,7 +94919,7 @@
                     ecmd = cmd;
                 }
             }
-            ncmdsize = STRLEN(ecmd) + STRLEN(p_sxq) * 2 + 1;
+            ncmdsize =  strlen((char *)(ecmd))  +  strlen((char *)(p_sxq))  * 2 + 1;
             ncmd = alloc(ncmdsize);
             if (ncmd != NULL)
             {
@@ -96887,7 +94973,7 @@
     int
 same_directory(char_u *f1, char_u *f2)
 {
-    char_u      ffname[MAXPATHL];
+    char_u      ffname[ PATH_MAX ];
     char_u      *t1;
     char_u      *t2;
 
@@ -96896,7 +94982,7 @@
         return FALSE;
     }
 
-    (void)vim_FullName(f1, ffname, MAXPATHL, FALSE);
+    (void)vim_FullName(f1, ffname,  PATH_MAX , FALSE);
     t1 = gettail_sep(ffname);
     t2 = gettail_sep(f2);
     return (t1 - ffname == t2 - f2 && pathcmp((char *)ffname, (char *)f2, (int)(t1 - ffname)) == 0);
@@ -96910,7 +94996,7 @@
     char_u      *file_to_find = NULL;
     char        *search_ctx = NULL;
 
-    dir_name = find_directory_in_path(new_dir, (int)STRLEN(new_dir), FNAME_MESS, curbuf->b_ffname, &file_to_find, &search_ctx);
+    dir_name = find_directory_in_path(new_dir, (int) strlen((char *)(new_dir)) , FNAME_MESS, curbuf->b_ffname, &file_to_find, &search_ctx);
     vim_free(file_to_find);
     vim_findfile_cleanup(search_ctx);
     if (dir_name == NULL)
@@ -97021,7 +95107,7 @@
                 }
             }
 
-            *argv = ALLOC_MULT(char *, *argc + 4);
+            *argv =  (char * *)alloc(sizeof(char *) * (*argc + 4)) ;
             if (*argv == NULL)
             {
                 return FAIL;
@@ -97031,16 +95117,13 @@
     return OK;
 }
 
-
-
-
     int
 cmp_keyvalue_value_n(const void *a, const void *b)
 {
     keyvalue_T *kv1 = (keyvalue_T *)a;
     keyvalue_T *kv2 = (keyvalue_T *)b;
 
-    return STRNCMP(kv1->value.string, kv2->value.string, MAX(kv1->value.length, kv2->value.length));
+    return  strncmp((char *)(kv1->value.string), (char *)(kv2->value.string), (MAX(kv1->value.length, kv2->value.length))) ;
 }
 
     int
@@ -97049,7 +95132,7 @@
     keyvalue_T *kv1 = (keyvalue_T *)a;
     keyvalue_T *kv2 = (keyvalue_T *)b;
 
-    return STRICMP(kv1->value.string, kv2->value.string);
+    return  strcasecmp((char *)(kv1->value.string), (char *)(kv2->value.string)) ;
 }
 
     int
@@ -97185,14 +95268,6 @@
     return head;
 }
 
-
-
-#define BASE64_ENCODED_LEN(len) ((((len) + 2) / 3) * 4 + 1)
-
-
-
-
-
 // ==================== mouse.c ====================
 
 static long mouse_hor_step = 6;
@@ -97201,8 +95276,6 @@
 static int stl_click_handler(win_T *wp, int mrow, int mcol, int which_button, int mods);
 static int stl_click_handler_regions(stl_click_region_T *regions, int region_count, int winid, char_u *area_name, int mrow, int mcol, int which_button, int mods);
 
-
-
     static long
 time_diff_ms(struct timeval *t1, struct timeval *t2)
 {
@@ -97215,7 +95288,7 @@
 {
     int         c;
 
-    if (has_mbyte && MB_BYTE2LEN(p[0]) > 1)
+    if (has_mbyte &&  mb_bytelen_tab[p[0]]  > 1)
     {
         return mb_get_class(p);
     }
@@ -97338,7 +95411,7 @@
 
     for (;;)
     {
-        which_button = get_mouse_button(KEY2TERMCAP1(c), &is_click, &is_drag);
+        which_button = get_mouse_button( (((unsigned)(-(c)) >> 8) & 0xff) , &is_click, &is_drag);
         if (is_drag)
         {
             if (!KeyStuffed && vpeekc() != NUL)
@@ -97360,7 +95433,7 @@
         break;
     }
 
-    if (c == K_MOUSEMOVE)
+    if (c ==   (-((KS_EXTRA) + ((int)(KE_MOUSEMOVE) << 8)))  )
     {
         return FALSE;
     }
@@ -97409,7 +95482,7 @@
         return FALSE;
     }
 
-    if ((mod_mask & (MOD_MASK_SHIFT | MOD_MASK_CTRL | MOD_MASK_ALT | MOD_MASK_META)) && (!is_click || (mod_mask & MOD_MASK_MULTI_CLICK) || which_button == MOUSE_MIDDLE) && !((mod_mask & (MOD_MASK_SHIFT|MOD_MASK_ALT)) && mouse_model_popup() && which_button == MOUSE_LEFT) && !((mod_mask & MOD_MASK_ALT) && !mouse_model_popup() && which_button == MOUSE_RIGHT))
+    if ((mod_mask & (MOD_MASK_SHIFT | MOD_MASK_CTRL | MOD_MASK_ALT | MOD_MASK_META)) && (!is_click || (mod_mask &  (MOD_MASK_2CLICK|MOD_MASK_3CLICK|MOD_MASK_4CLICK) ) || which_button == MOUSE_MIDDLE) && !((mod_mask & (MOD_MASK_SHIFT|MOD_MASK_ALT)) && mouse_model_popup() && which_button == MOUSE_LEFT) && !((mod_mask & MOD_MASK_ALT) && !mouse_model_popup() && which_button == MOUSE_RIGHT))
     {
         return FALSE;
     }
@@ -97448,7 +95521,7 @@
                 else
                 {
                     stuffcharReadbuff('y');
-                    stuffcharReadbuff(K_MIDDLEMOUSE);
+                    stuffcharReadbuff(  (-((KS_EXTRA) + ((int)(KE_MIDDLEMOUSE) << 8)))  );
                 }
                 do_always = TRUE;
                 return FALSE;
@@ -97474,7 +95547,7 @@
                 }
                 else
                 {
-                    do_put(regname, NULL, BACKWARD, 1L, fixindent | PUT_CURSEND);
+                    do_put(regname, NULL,  (-1) , 1L, fixindent | PUT_CURSEND);
 
                     AppendCharToRedobuff(Ctrl_R);
                     AppendCharToRedobuff(fixindent ? Ctrl_P : Ctrl_O);
@@ -97540,7 +95613,7 @@
             c1 = tp_label.nr;
             if (c1 >= 0)
             {
-                if ((mod_mask & MOD_MASK_MULTI_CLICK) == MOD_MASK_2CLICK && !tp_label.is_panel)
+                if ((mod_mask &  (MOD_MASK_2CLICK|MOD_MASK_3CLICK|MOD_MASK_4CLICK) ) == MOD_MASK_2CLICK && !tp_label.is_panel)
                 {
                     end_visual_mode_keep_button();
                     tabpage_new();
@@ -97623,7 +95696,7 @@
         {
             if (is_click && VIsual_active)
             {
-                if (LT_POS(curwin->w_cursor, VIsual))
+                if ( (((curwin->w_cursor).lnum != (VIsual).lnum)               ? (curwin->w_cursor).lnum < (VIsual).lnum                   : (curwin->w_cursor).col != (VIsual).col                        ? (curwin->w_cursor).col < (VIsual).col                     : (curwin->w_cursor).coladd < (VIsual).coladd) )
                 {
                     start_visual = curwin->w_cursor;
                     end_visual = VIsual;
@@ -97718,11 +95791,11 @@
         }
         else
         {
-            if (LT_POS(curwin->w_cursor, start_visual))
+            if ( (((curwin->w_cursor).lnum != (start_visual).lnum)               ? (curwin->w_cursor).lnum < (start_visual).lnum                   : (curwin->w_cursor).col != (start_visual).col                        ? (curwin->w_cursor).col < (start_visual).col                     : (curwin->w_cursor).coladd < (start_visual).coladd) )
             {
                 VIsual = end_visual;
             }
-            else if (LT_POS(end_visual, curwin->w_cursor))
+            else if ( (((end_visual).lnum != (curwin->w_cursor).lnum)               ? (end_visual).lnum < (curwin->w_cursor).lnum                   : (end_visual).col != (curwin->w_cursor).col                        ? (end_visual).col < (curwin->w_cursor).col                     : (end_visual).coladd < (curwin->w_cursor).coladd) )
             {
                 VIsual = start_visual;
             }
@@ -97789,7 +95862,7 @@
 
         if (fixindent)
         {
-            c1 = (dir == BACKWARD) ? '[' : ']';
+            c1 = (dir ==  (-1) ) ? '[' : ']';
             c2 = 'p';
         }
         else
@@ -97806,7 +95879,7 @@
         do_put(regname, NULL, dir, count, fixindent | PUT_CURSEND);
     }
 
-    else if ((mod_mask & MOD_MASK_CTRL) || (curbuf->b_help && (mod_mask & MOD_MASK_MULTI_CLICK) == MOD_MASK_2CLICK))
+    else if ((mod_mask & MOD_MASK_CTRL) || (curbuf->b_help && (mod_mask &  (MOD_MASK_2CLICK|MOD_MASK_3CLICK|MOD_MASK_4CLICK) ) == MOD_MASK_2CLICK))
     {
         if (State & MODE_INSERT)
         {
@@ -97838,7 +95911,7 @@
     else if (in_sep_line)
     {
     }
-    else if ((mod_mask & MOD_MASK_MULTI_CLICK) && (State & (MODE_NORMAL | MODE_INSERT)) && mouse_has(MOUSE_VISUAL))
+    else if ((mod_mask &  (MOD_MASK_2CLICK|MOD_MASK_3CLICK|MOD_MASK_4CLICK) ) && (State & (MODE_NORMAL | MODE_INSERT)) && mouse_has(MOUSE_VISUAL))
     {
         if (is_click || !VIsual_active)
         {
@@ -97856,7 +95929,7 @@
                 may_start_select('o');
                 setmouse();
             }
-            if ((mod_mask & MOD_MASK_MULTI_CLICK) == MOD_MASK_2CLICK)
+            if ((mod_mask &  (MOD_MASK_2CLICK|MOD_MASK_3CLICK|MOD_MASK_4CLICK) ) == MOD_MASK_2CLICK)
             {
                 if (mod_mask & MOD_MASK_ALT)
                 {
@@ -97867,16 +95940,16 @@
                     VIsual_mode = 'v';
                 }
             }
-            else if ((mod_mask & MOD_MASK_MULTI_CLICK) == MOD_MASK_3CLICK)
+            else if ((mod_mask &  (MOD_MASK_2CLICK|MOD_MASK_3CLICK|MOD_MASK_4CLICK) ) == MOD_MASK_3CLICK)
             {
                 VIsual_mode = 'V';
             }
-            else if ((mod_mask & MOD_MASK_MULTI_CLICK) == MOD_MASK_4CLICK)
+            else if ((mod_mask &  (MOD_MASK_2CLICK|MOD_MASK_3CLICK|MOD_MASK_4CLICK) ) == MOD_MASK_4CLICK)
             {
                 VIsual_mode = Ctrl_V;
             }
         }
-        if ((mod_mask & MOD_MASK_MULTI_CLICK) == MOD_MASK_2CLICK)
+        if ((mod_mask &  (MOD_MASK_2CLICK|MOD_MASK_3CLICK|MOD_MASK_4CLICK) ) == MOD_MASK_2CLICK)
         {
             pos_T       *pos = NULL;
             int         gc;
@@ -97884,7 +95957,7 @@
             if (is_click)
             {
                 end_visual = curwin->w_cursor;
-                while (gc = gchar_pos(&end_visual), VIM_ISWHITE(gc))
+                while (gc = gchar_pos(&end_visual),  ((gc) == ' ' || (gc) == '\t') )
                 {
                     inc(&end_visual);
                 }
@@ -97892,7 +95965,7 @@
                 {
                     oap->motion_type = MCHAR;
                 }
-                if (oap != NULL && VIsual_mode == 'v' && !vim_iswordc(gchar_pos(&end_visual)) && EQUAL_POS(curwin->w_cursor, VIsual) && (pos = findmatch(oap, NUL)) != NULL)
+                if (oap != NULL && VIsual_mode == 'v' && !vim_iswordc(gchar_pos(&end_visual)) &&  (((curwin->w_cursor).lnum == (VIsual).lnum) && ((curwin->w_cursor).col == (VIsual).col) && ((curwin->w_cursor).coladd == (VIsual).coladd))  && (pos = findmatch(oap, NUL)) != NULL)
                 {
                     curwin->w_cursor = *pos;
                     if (oap->motion_type == MLINE)
@@ -97901,7 +95974,7 @@
                     }
                     else if (*p_sel == 'e')
                     {
-                        if (LT_POS(curwin->w_cursor, VIsual))
+                        if ( (((curwin->w_cursor).lnum != (VIsual).lnum)               ? (curwin->w_cursor).lnum < (VIsual).lnum                   : (curwin->w_cursor).col != (VIsual).col                        ? (curwin->w_cursor).col < (VIsual).col                     : (curwin->w_cursor).coladd < (VIsual).coladd) )
                         {
                             ++VIsual.col;
                         }
@@ -97915,7 +95988,7 @@
 
             if (pos == NULL && (is_click || is_drag))
             {
-                if (LT_POS(curwin->w_cursor, orig_cursor))
+                if ( (((curwin->w_cursor).lnum != (orig_cursor).lnum)               ? (curwin->w_cursor).lnum < (orig_cursor).lnum                   : (curwin->w_cursor).col != (orig_cursor).col                        ? (curwin->w_cursor).col < (orig_cursor).col                     : (curwin->w_cursor).coladd < (orig_cursor).coladd) )
                 {
                     find_start_of_word(&curwin->w_cursor);
                     find_end_of_word(&VIsual);
@@ -97971,7 +96044,7 @@
 
     undisplay_dollar();
     tpos = curwin->w_cursor;
-    if (do_mouse(NULL, c, BACKWARD, 1L, 0))
+    if (do_mouse(NULL, c,  (-1) , 1L, 0))
     {
         win_T   *new_curwin = curwin;
 
@@ -98001,7 +96074,7 @@
     {
         if (!(State & MODE_INSERT) && (mouse_vert_step < 0 || shift_or_ctrl))
         {
-            pagescroll(cap->arg == MSCR_UP ? FORWARD : BACKWARD, 1L, FALSE);
+            pagescroll(cap->arg == MSCR_UP ? FORWARD :  (-1) , 1L, FALSE);
         }
         else
         {
@@ -98031,7 +96104,7 @@
         long step = (mouse_hor_step < 0 || shift_or_ctrl)
                                             ? curwin->w_width : mouse_hor_step;
         long leftcol = curwin->w_leftcol
-                                     + (cap->arg == MSCR_RIGHT ? -step : step);
+                                     + (cap->arg ==  (-2)  ? -step : step);
         if (leftcol < 0)
         {
             leftcol = 0;
@@ -98046,7 +96119,7 @@
 {
     cmdarg_T    cap;
     oparg_T     oa;
-    CLEAR_FIELD(cap);
+      memset((&(cap)), (0), (sizeof(cap)))  ;
     clear_oparg(&oa);
     cap.oap = &oa;
     cap.arg = dir;
@@ -98054,16 +96127,16 @@
     switch (dir)
     {
         case MSCR_UP:
-            cap.cmdchar = K_MOUSEUP;
+            cap.cmdchar =   (-((KS_EXTRA) + ((int)(KE_MOUSEUP) << 8)))  ;
             break;
         case MSCR_DOWN:
-            cap.cmdchar = K_MOUSEDOWN;
+            cap.cmdchar =   (-((KS_EXTRA) + ((int)(KE_MOUSEDOWN) << 8)))  ;
             break;
-        case MSCR_LEFT:
-            cap.cmdchar = K_MOUSELEFT;
+        case  (-1) :
+            cap.cmdchar =   (-((KS_EXTRA) + ((int)(KE_MOUSELEFT) << 8)))  ;
             break;
-        case MSCR_RIGHT:
-            cap.cmdchar = K_MOUSERIGHT;
+        case  (-2) :
+            cap.cmdchar =   (-((KS_EXTRA) + ((int)(KE_MOUSERIGHT) << 8)))  ;
             break;
         default:
             siemsg("Invalid ins_mousescroll() argument: %d", dir);
@@ -98111,7 +96184,7 @@
         ins_compl_show_pum();
     }
 
-    if (!EQUAL_POS(curwin->w_cursor, orig_cursor))
+    if (! (((curwin->w_cursor).lnum == (orig_cursor).lnum) && ((curwin->w_cursor).col == (orig_cursor).col) && ((curwin->w_cursor).coladd == (orig_cursor).coladd)) )
     {
         start_arrow(&orig_cursor);
         set_can_cindent(TRUE);
@@ -98121,28 +96194,28 @@
     int
 is_mouse_key(int c)
 {
-    return c == K_LEFTMOUSE
-        || c == K_LEFTMOUSE_NM
-        || c == K_LEFTDRAG
-        || c == K_LEFTRELEASE
-        || c == K_LEFTRELEASE_NM
-        || c == K_MOUSEMOVE
-        || c == K_MIDDLEMOUSE
-        || c == K_MIDDLEDRAG
-        || c == K_MIDDLERELEASE
-        || c == K_RIGHTMOUSE
-        || c == K_RIGHTDRAG
-        || c == K_RIGHTRELEASE
-        || c == K_MOUSEDOWN
-        || c == K_MOUSEUP
-        || c == K_MOUSELEFT
-        || c == K_MOUSERIGHT
-        || c == K_X1MOUSE
-        || c == K_X1DRAG
-        || c == K_X1RELEASE
-        || c == K_X2MOUSE
-        || c == K_X2DRAG
-        || c == K_X2RELEASE;
+    return c ==   (-((KS_EXTRA) + ((int)(KE_LEFTMOUSE) << 8)))  
+        || c ==   (-((KS_EXTRA) + ((int)(KE_LEFTMOUSE_NM) << 8)))  
+        || c ==   (-((KS_EXTRA) + ((int)(KE_LEFTDRAG) << 8)))  
+        || c ==   (-((KS_EXTRA) + ((int)(KE_LEFTRELEASE) << 8)))  
+        || c ==   (-((KS_EXTRA) + ((int)(KE_LEFTRELEASE_NM) << 8)))  
+        || c ==   (-((KS_EXTRA) + ((int)(KE_MOUSEMOVE) << 8)))  
+        || c ==   (-((KS_EXTRA) + ((int)(KE_MIDDLEMOUSE) << 8)))  
+        || c ==   (-((KS_EXTRA) + ((int)(KE_MIDDLEDRAG) << 8)))  
+        || c ==   (-((KS_EXTRA) + ((int)(KE_MIDDLERELEASE) << 8)))  
+        || c ==   (-((KS_EXTRA) + ((int)(KE_RIGHTMOUSE) << 8)))  
+        || c ==   (-((KS_EXTRA) + ((int)(KE_RIGHTDRAG) << 8)))  
+        || c ==   (-((KS_EXTRA) + ((int)(KE_RIGHTRELEASE) << 8)))  
+        || c ==   (-((KS_EXTRA) + ((int)(KE_MOUSEDOWN) << 8)))  
+        || c ==   (-((KS_EXTRA) + ((int)(KE_MOUSEUP) << 8)))  
+        || c ==   (-((KS_EXTRA) + ((int)(KE_MOUSELEFT) << 8)))  
+        || c ==   (-((KS_EXTRA) + ((int)(KE_MOUSERIGHT) << 8)))  
+        || c ==   (-((KS_EXTRA) + ((int)(KE_X1MOUSE) << 8)))  
+        || c ==   (-((KS_EXTRA) + ((int)(KE_X1DRAG) << 8)))  
+        || c ==   (-((KS_EXTRA) + ((int)(KE_X1RELEASE) << 8)))  
+        || c ==   (-((KS_EXTRA) + ((int)(KE_X2MOUSE) << 8)))  
+        || c ==   (-((KS_EXTRA) + ((int)(KE_X2DRAG) << 8)))  
+        || c ==   (-((KS_EXTRA) + ((int)(KE_X2RELEASE) << 8)))  ;
 }
 
 static struct mousetable
@@ -98205,15 +96278,9 @@
     return (int)KE_IGNORE;
 }
 
-#define HMT_NORMAL      1
-#define HMT_NETTERM     2
-#define HMT_DEC 4
-#define HMT_JSBTERM     8
-#define HMT_PTERM       16
-#define HMT_URXVT       32
-#define HMT_GPM 64
-#define HMT_SGR 128
-#define HMT_SGR_REL     256
+enum { HMT_NORMAL = 1 };
+enum { HMT_SGR = 128 };
+enum { HMT_SGR_REL = 256 };
 static int has_mouse_termcode = 0;
 
     void
@@ -98222,7 +96289,7 @@
     char_u      name[2];
 
     name[0] = n;
-    name[1] = KE_FILLER;
+    name[1] =  ('X') ;
     add_termcode(name, s, FALSE);
     if (n == KS_SGR_MOUSE)
     {
@@ -98244,7 +96311,7 @@
     char_u      name[2];
 
     name[0] = n;
-    name[1] = KE_FILLER;
+    name[1] =  ('X') ;
     del_termcode(name);
     if (n == KS_SGR_MOUSE)
     {
@@ -98280,7 +96347,7 @@
     {
         checkfor = MOUSE_VISUAL;
     }
-    else if (State == MODE_HITRETURN || State == MODE_ASKMORE || State == MODE_SETWSIZE)
+    else if (State ==  (0x2000 | MODE_NORMAL)  || State == MODE_ASKMORE || State == MODE_SETWSIZE)
     {
         checkfor = MOUSE_RETURN;
     }
@@ -98321,7 +96388,7 @@
         switch (*p)
         {
             case 'a':
-                if (vim_strchr((char_u *)MOUSE_A, c) != NULL)
+                if (vim_strchr((char_u *) "nvich" , c) != NULL)
                 {
                           return TRUE;
                 }
@@ -98535,7 +96602,7 @@
     {
         if (dragwin != NULL)
         {
-            count = row - W_WINROW(dragwin) - dragwin->w_height + 1
+            count = row -  (dragwin->w_winrow)  - dragwin->w_height + 1
                                                              - on_status_line;
             win_drag_status_line(dragwin, count);
             did_drag |= count;
@@ -98561,7 +96628,7 @@
             redraw_curbuf_later(UPD_INVERTED);
         }
 
-        row -= W_WINROW(curwin);
+        row -=  (curwin->w_winrow) ;
         col -= curwin->w_wincol;
 
         if (row < 0)
@@ -98613,7 +96680,7 @@
         }
     }
 
-    if (prev_row >= W_WINROW(curwin) && prev_row < W_WINROW(curwin) + curwin->w_height && prev_col >= curwin->w_wincol && prev_col < W_ENDCOL(curwin) && ScreenLines != NULL)
+    if (prev_row >=  (curwin->w_winrow)  && prev_row <  (curwin->w_winrow)  + curwin->w_height && prev_col >= curwin->w_wincol && prev_col <  ((curwin)->w_wincol + (curwin)->w_width)  && ScreenLines != NULL)
     {
         int off = LineOffset[prev_row] + prev_col;
 
@@ -98674,7 +96741,7 @@
     int
 do_mousescroll_horiz(long_u leftcol)
 {
-    if (curwin->w_p_wrap)
+    if (curwin-> w_onebuf_opt.wo_wrap )
     {
         return FALSE;
     }
@@ -98722,7 +96789,7 @@
     void
 nv_mouse(cmdarg_T *cap)
 {
-    (void)do_mouse(cap->oap, cap->cmdchar, BACKWARD, cap->count1, 0);
+    (void)do_mouse(cap->oap, cap->cmdchar,  (-1) , cap->count1, 0);
 }
 
 static int      held_button = MOUSE_RELEASE;
@@ -98774,7 +96841,7 @@
             *slen += num_bytes;
 
                 j = get_termcode_len(idx);
-            if (STRNCMP(tp, tp + *slen, (size_t)j) == 0 && tp[*slen + j] == mouse_code && tp[*slen + j + 1] != NUL && tp[*slen + j + 2] != NUL)
+            if ( strncmp((char *)(tp), (char *)(tp + *slen), ((size_t)j))  == 0 && tp[*slen + j] == mouse_code && tp[*slen + j + 1] != NUL && tp[*slen + j + 2] != NUL)
             {
                 *slen += j;
             }
@@ -98838,7 +96905,7 @@
         {
             if (use_xterm_mouse() > 1 && mouse_code >= 0x80)
             {
-                mouse_code = MOUSE_DRAG;
+                mouse_code =  (0x40 | MOUSE_RELEASE) ;
             }
             else
             {
@@ -98855,7 +96922,7 @@
         {
             if (mouse_code & MOUSE_DRAG_XTERM)
             {
-                mouse_code |= MOUSE_DRAG;
+                mouse_code |=  (0x40 | MOUSE_RELEASE) ;
             }
         }
     }
@@ -98868,7 +96935,7 @@
 
     if (current_button == MOUSE_RELEASE && wheel_code == 0)
     {
-        if ((mouse_code & MOUSE_DRAG) == MOUSE_DRAG)
+        if ((mouse_code &  (0x40 | MOUSE_RELEASE) ) ==  (0x40 | MOUSE_RELEASE) )
         {
             is_drag = TRUE;
         }
@@ -99066,7 +97133,7 @@
         }
     }
 
-    if (!win->w_p_wrap)
+    if (!win-> w_onebuf_opt.wo_wrap )
     {
         col += win->w_leftcol;
     }
@@ -99084,7 +97151,7 @@
 }
 
     win_T *
-mouse_find_win(int *rowp, int *colp, mouse_find_T popup UNUSED)
+mouse_find_win(int *rowp, int *colp, mouse_find_T popup  __attribute__((unused)) )
 {
     frame_T     *fp;
     win_T       *wp;
@@ -99127,11 +97194,13 @@
             }
         }
     }
-    FOR_ALL_WINDOWS(wp)
+     for ((wp) = firstwin; (wp) != NULL; (wp) = (wp)->w_next) 
+     {
         if (wp == fp->fr_win)
         {
             return wp;
         }
+     }
     return NULL;
 }
 
@@ -99232,7 +97301,7 @@
     static void
 redraw_for_cursorline(win_T *wp)
 {
-    if ((wp->w_p_rnu) && (wp->w_valid & VALID_CROW) == 0 && !pum_visible())
+    if ((wp-> w_onebuf_opt.wo_rnu ) && (wp->w_valid & VALID_CROW) == 0 && !pum_visible())
     {
         redraw_win_later(wp, UPD_VALID);
     }
@@ -99252,7 +97321,7 @@
     {
         extra2 = win_col_off(wp) - win_col_off2(wp);
     }
-    if (wp->w_p_list && wp->w_lcs_chars.prec)
+    if (wp-> w_onebuf_opt.wo_list  && wp->w_lcs_chars.prec)
     {
         return 1;
     }
@@ -99325,7 +97394,7 @@
     int         n;
     int         check_topline = FALSE;
     int         check_botline = FALSE;
-    long        *so_ptr = curwin->w_p_so >= 0 ? &curwin->w_p_so : &p_so;
+    long        *so_ptr = curwin-> w_onebuf_opt.wo_so  >= 0 ? &curwin-> w_onebuf_opt.wo_so  : &p_so;
     int         save_so = *so_ptr;
     bool        eof_pressure;
 
@@ -99357,7 +97426,7 @@
 
     linenr_T old_topline = curwin->w_topline;
 
-    if (BUFEMPTY())
+    if ( (curbuf->b_ml.ml_line_count == 1 && *ml_get((linenr_T)1) == NUL) )
     {
         if (curwin->w_topline != 1)
         {
@@ -99494,7 +97563,7 @@
         dollar_vcol = -1;
         redraw_later(UPD_VALID);
 
-        if (!curwin->w_p_sms)
+        if (!curwin-> w_onebuf_opt.wo_sms )
         {
             reset_skipcol();
         }
@@ -99609,11 +97678,9 @@
     redraw_win_later(wp, UPD_NOT_VALID);
 }
 
-
     void
 set_topline(win_T *wp, linenr_T lnum)
 {
-
     wp->w_botline += lnum - wp->w_topline;
     if (wp->w_botline > wp->w_buffer->b_ml.ml_line_count + 1)
     {
@@ -99817,7 +97884,7 @@
     col += off;
     width = curwin->w_width - off + curwin_col_off2();
 
-    if (curwin->w_p_wrap && col >= (colnr_T)curwin->w_width && width > 0)
+    if (curwin-> w_onebuf_opt.wo_wrap  && col >= (colnr_T)curwin->w_width && width > 0)
     {
         col -= ((col - curwin->w_width) / width + 1) * width;
     }
@@ -99837,7 +97904,7 @@
     int
 win_col_off(win_T *wp)
 {
-    return (((wp->w_p_nu || wp->w_p_rnu) ? number_width(wp) + 1 : 0) + (wp != cmdwin_win ? 0 : 1));
+    return (((wp-> w_onebuf_opt.wo_nu  || wp-> w_onebuf_opt.wo_rnu ) ?  7  + 1 : 0) + (wp != cmdwin_win ? 0 : 1));
 }
 
     int
@@ -99849,9 +97916,9 @@
     int
 win_col_off2(win_T *wp)
 {
-    if ((wp->w_p_nu || wp->w_p_rnu) && vim_strchr(p_cpo, CPO_NUMCOL) != NULL)
+    if ((wp-> w_onebuf_opt.wo_nu  || wp-> w_onebuf_opt.wo_rnu ) && vim_strchr(p_cpo, CPO_NUMCOL) != NULL)
     {
-        return number_width(wp) + 1;
+        return  7  + 1;
     }
     return 0;
 }
@@ -99905,7 +97972,7 @@
     if (width1 <= 0)
     {
         curwin->w_wcol = curwin->w_width - 1;
-        if (curwin->w_p_wrap)
+        if (curwin-> w_onebuf_opt.wo_wrap )
         {
             curwin->w_wrow = curwin->w_height - 1;
         }
@@ -99914,7 +97981,7 @@
             curwin->w_wrow = curwin->w_height - 1 - curwin->w_empty_rows;
         }
     }
-    else if (curwin->w_p_wrap && curwin->w_width != 0)
+    else if (curwin-> w_onebuf_opt.wo_wrap  && curwin->w_width != 0)
     {
         width2 = width1 + curwin_col_off2();
 
@@ -100100,7 +98167,7 @@
             win_del_lines(curwin, 0, -extra, FALSE, FALSE, 0);
         }
     }
-    else if (!curwin->w_p_sms)
+    else if (!curwin-> w_onebuf_opt.wo_sms )
     {
         curwin->w_skipcol = 0;
     }
@@ -100117,7 +98184,7 @@
 
 static void cursor_correct_sms(void)
 {
-    if (!curwin->w_p_sms || !curwin->w_p_wrap || curwin->w_cursor.lnum != curwin->w_topline)
+    if (!curwin-> w_onebuf_opt.wo_sms  || !curwin-> w_onebuf_opt.wo_wrap  || curwin->w_cursor.lnum != curwin->w_topline)
     {
         return;
     }
@@ -100251,12 +98318,12 @@
 }
 
     void
-scrolldown(long        line_count, int         byfold UNUSED)
+scrolldown(long        line_count, int         byfold  __attribute__((unused)) )
 {
     long        done = 0;
     int         wrow;
     int         moved = FALSE;
-    int         do_sms = curwin->w_p_wrap && curwin->w_p_sms;
+    int         do_sms = curwin-> w_onebuf_opt.wo_wrap  && curwin-> w_onebuf_opt.wo_sms ;
     int         width1 = 0;
     int         width2 = 0;
 
@@ -100309,7 +98376,7 @@
                 }
                 else
                 {
-                    done += PLINES_NOFILL(curwin->w_topline);
+                    done +=  plines(curwin->w_topline) ;
                 }
             }
         }
@@ -100320,7 +98387,7 @@
     curwin->w_cline_row += done;
 
     wrow = curwin->w_wrow;
-    if (curwin->w_p_wrap && curwin->w_width != 0)
+    if (curwin-> w_onebuf_opt.wo_wrap  && curwin->w_width != 0)
     {
         validate_virtcol();
         validate_cheight();
@@ -100345,9 +98412,9 @@
 }
 
     void
-scrollup(long        line_count, int         byfold UNUSED)
+scrollup(long        line_count, int         byfold  __attribute__((unused)) )
 {
-    int         do_sms = curwin->w_p_wrap && curwin->w_p_sms;
+    int         do_sms = curwin-> w_onebuf_opt.wo_wrap  && curwin-> w_onebuf_opt.wo_sms ;
 
     if (do_sms)
     {
@@ -100435,7 +98502,7 @@
     void
 adjust_skipcol(void)
 {
-    if (!curwin->w_p_wrap || !curwin->w_p_sms || curwin->w_cursor.lnum != curwin->w_topline)
+    if (!curwin-> w_onebuf_opt.wo_wrap  || !curwin-> w_onebuf_opt.wo_sms  || curwin->w_cursor.lnum != curwin->w_topline)
     {
         return;
     }
@@ -100534,7 +98601,7 @@
 
     end_row = curwin->w_wrow;
     end_row += plines(curwin->w_topline - 1);
-    if (curwin->w_p_wrap && curwin->w_width != 0)
+    if (curwin-> w_onebuf_opt.wo_wrap  && curwin->w_width != 0)
     {
         validate_cheight();
         validate_virtcol();
@@ -100562,7 +98629,7 @@
     validate_cursor();
 
     start_row = curwin->w_wrow - plines(curwin->w_topline);
-    if (curwin->w_p_wrap && curwin->w_width != 0)
+    if (curwin-> w_onebuf_opt.wo_wrap  && curwin->w_width != 0)
     {
         validate_virtcol();
         start_row -= curwin->w_virtcol / curwin->w_width;
@@ -100588,7 +98655,7 @@
         }
         else
         {
-            lp->height = PLINES_WIN_NOFILL(curwin, lp->lnum, winheight);
+            lp->height =  plines_win((curwin), (lp->lnum), (winheight)) ;
         }
     }
 }
@@ -100610,7 +98677,7 @@
         }
         else
         {
-            lp->height = PLINES_NOFILL(lp->lnum);
+            lp->height =  plines(lp->lnum) ;
         }
     }
 }
@@ -100649,7 +98716,7 @@
 
     while (top > 0)
     {
-            i = PLINES_NOFILL(top);
+            i =  plines(top) ;
         if (top < curwin->w_topline)
         {
             scrolled += i;
@@ -100755,7 +98822,7 @@
     int         old_empty_rows = curwin->w_empty_rows;
     linenr_T    cln;
     long        so = get_scrolloff_value();
-    int         do_sms = curwin->w_p_wrap && curwin->w_p_sms;
+    int         do_sms = curwin-> w_onebuf_opt.wo_wrap  && curwin-> w_onebuf_opt.wo_sms ;
 
     cln = curwin->w_cursor.lnum;
     if (set_topbot)
@@ -100990,7 +99057,7 @@
     topline = loff.lnum;
 
     int want_height;
-    int do_sms = curwin->w_p_wrap && curwin->w_p_sms;
+    int do_sms = curwin-> w_onebuf_opt.wo_wrap  && curwin-> w_onebuf_opt.wo_sms ;
     if (do_sms)
     {
         if (atend)
@@ -101160,7 +99227,7 @@
         }
         if (above < above_wanted && (above < below || below >= below_wanted))
         {
-                above += PLINES_NOFILL(topline);
+                above +=  plines(topline) ;
             ++topline;
         }
     }
@@ -101197,7 +99264,7 @@
     int     min_height = curwin->w_height - 2;
 
     validate_botline();
-    if ((dir == BACKWARD && curwin->w_topline == 1) || (dir == FORWARD && curwin->w_botline > curbuf->b_ml.ml_line_count))
+    if ((dir ==  (-1)  && curwin->w_topline == 1) || (dir == FORWARD && curwin->w_botline > curbuf->b_ml.ml_line_count))
     {
         return min_height + 2;
     }
@@ -101260,17 +99327,17 @@
 
 static int scroll_with_sms(int dir, long count, long *curscount)
 {
-    int         prev_sms = curwin->w_p_sms;
+    int         prev_sms = curwin-> w_onebuf_opt.wo_sms ;
     colnr_T     prev_skipcol = curwin->w_skipcol;
     linenr_T    prev_topline = curwin->w_topline;
 
-    curwin->w_p_sms = TRUE;
+    curwin-> w_onebuf_opt.wo_sms  = TRUE;
     scroll_redraw(dir == FORWARD, count);
 
     if (!prev_sms && curwin->w_skipcol > 0)
     {
         int fixdir = dir;
-        if (labs(curwin->w_topline - prev_topline) > (dir == BACKWARD))
+        if (labs(curwin->w_topline - prev_topline) > (dir ==  (-1) ))
         {
             fixdir = dir * -1;
         }
@@ -101285,7 +99352,7 @@
         scroll_redraw(fixdir == FORWARD, count);
         *curscount += count * (fixdir == dir ? 1 : -1);
     }
-    curwin->w_p_sms = prev_sms;
+    curwin-> w_onebuf_opt.wo_sms  = prev_sms;
 
     return curwin->w_topline != prev_topline
         || curwin->w_skipcol != prev_skipcol;
@@ -101307,9 +99374,9 @@
     {
         if (count)
         {
-            curwin->w_p_scr = MIN(curwin->w_height, count);
+            curwin-> w_onebuf_opt.wo_scr  = MIN(curwin->w_height, count);
         }
-        count = MIN(curwin->w_height, curwin->w_p_scr);
+        count = MIN(curwin->w_height, curwin-> w_onebuf_opt.wo_scr );
 
         long curscount = count;
         if (dir == FORWARD && (curwin->w_topline + curwin->w_height + count > buflen))
@@ -101333,7 +99400,7 @@
             curwin->w_curswant = prev_curswant;
         }
 
-        if (curwin->w_p_wrap)
+        if (curwin-> w_onebuf_opt.wo_wrap )
         {
             nv_screengo(&oa, dir, curscount);
         }
@@ -101348,7 +99415,7 @@
     }
     else
     {
-        count *= ((ONE_WINDOW && p_window > 0 && p_window < Rows - 1) ? MAX(1, p_window - 2) : get_scroll_overlap(dir));
+        count *= (( (firstwin == lastwin)  && p_window > 0 && p_window < Rows - 1) ? MAX(1, p_window - 2) : get_scroll_overlap(dir));
         did_move = scroll_with_sms(dir, count, &count);
 
         if (did_move)
@@ -101371,7 +99438,7 @@
     {
         beep_flush();
     }
-    else if (!curwin->w_p_sms)
+    else if (!curwin-> w_onebuf_opt.wo_sms )
     {
         beginline(BL_SOL | BL_FIX);
     }
@@ -101391,13 +99458,13 @@
     static varnumber_T  prev_changedtick = 0;
     static pos_T        prev_cursor = {0, 0, 0};
 
-    if (curwin == prev_curwin && curbuf == prev_curbuf && CHANGEDTICK(curbuf) == prev_changedtick && EQUAL_POS(curwin->w_cursor, prev_cursor))
+    if (curwin == prev_curwin && curbuf == prev_curbuf &&  ((curbuf)->b_ct_di.di_tv.vval.v_number)  == prev_changedtick &&  (((curwin->w_cursor).lnum == (prev_cursor).lnum) && ((curwin->w_cursor).col == (prev_cursor).col) && ((curwin->w_cursor).coladd == (prev_cursor).coladd)) )
     {
         return;
     }
     prev_curwin = curwin;
     prev_curbuf = curbuf;
-    prev_changedtick = CHANGEDTICK(curbuf);
+    prev_changedtick =  ((curbuf)->b_ct_di.di_tv.vval.v_number) ;
     prev_cursor = curwin->w_cursor;
 
     linenr_T    line = curwin->w_cursor.lnum;
@@ -101411,10 +99478,10 @@
     int         old_VIsual_active = VIsual_active;
 
     VIsual_select = VIsual_active = 0;
-    FOR_ALL_WINDOWS(curwin)
+     for ((curwin) = firstwin; (curwin) != NULL; (curwin) = (curwin)->w_next) 
     {
         curbuf = curwin->w_buffer;
-        if (curwin != old_curwin && curwin->w_p_crb)
+        if (curwin != old_curwin && curwin-> w_onebuf_opt.wo_crb )
         {
                 curwin->w_cursor.lnum = line;
             curwin->w_cursor.col = col;
@@ -101426,7 +99493,7 @@
             restart_edit = 'a';
             check_cursor();
 
-            if (!curwin->w_p_scb)
+            if (!curwin-> w_onebuf_opt.wo_scb )
             {
                 validate_cursor();
             }
@@ -101438,7 +99505,7 @@
             }
             redraw_later(UPD_VALID);
 
-            if (!curwin->w_p_scb)
+            if (!curwin-> w_onebuf_opt.wo_scb )
             {
                 update_topline();
             }
@@ -101538,34 +99605,19 @@
 static void     nv_open(cmdarg_T *cap);
 static void     nv_cursorhold(cmdarg_T *cap);
 
-#define DO_DECLARE_NVCMD
 // ---------------- begin nv_cmds.h ----------------
 
-# define NVCMD(a, b, c, d)      {a, b, c, d}
-
-#  define NV_VER_SCROLLBAR nv_error
-#  define NV_HOR_SCROLLBAR nv_error
-
-#  define NV_TABLINE    nv_error
-#  define NV_TABMENU    nv_error
-
-#  define NV_NBCMD      nv_error
-
-#  define NV_DROP               nv_error
-
 typedef void (*nv_func_T)(cmdarg_T *cap);
 
-# define NV_NCH     0x01
-# define NV_NCH_NOP  (0x02|NV_NCH)
-# define NV_NCH_ALW  (0x04|NV_NCH)
-# define NV_LANG            0x08
-
-# define NV_SS      0x10
-# define NV_SSS     0x20
-# define NV_STS     0x40
-# define NV_RL      0x80
-# define NV_KEEPREG  0x100
-# define NV_NCW     0x200
+enum { NV_NCH = 0x01 };
+enum { NV_LANG = 0x08 };
+
+enum { NV_SS = 0x10 };
+enum { NV_SSS = 0x20 };
+enum { NV_STS = 0x40 };
+enum { NV_RL = 0x80 };
+enum { NV_KEEPREG = 0x100 };
+enum { NV_NCW = 0x200 };
 
 static const struct nv_cmd
 {
@@ -101576,205 +99628,203 @@
 } nv_cmds[] =
 
 {
-    NVCMD(NUL,          nv_error,       0,                      0),
-    NVCMD(Ctrl_A,       nv_addsub,      0,                      0),
-    NVCMD(Ctrl_B,       nv_page,        NV_STS,                 BACKWARD),
-    NVCMD(Ctrl_C,       nv_esc,         0,                      TRUE),
-    NVCMD(Ctrl_D,       nv_halfpage,    0,                      0),
-    NVCMD(Ctrl_E,       nv_scroll_line, 0,                      TRUE),
-    NVCMD(Ctrl_F,       nv_page,        NV_STS,                 FORWARD),
-    NVCMD(Ctrl_G,       nv_ctrlg,       0,                      0),
-    NVCMD(Ctrl_H,       nv_ctrlh,       0,                      0),
-    NVCMD(Ctrl_I,       nv_pcmark,      0,                      0),
-    NVCMD(NL,           nv_down,        0,                      FALSE),
-    NVCMD(Ctrl_K,       nv_error,       0,                      0),
-    NVCMD(Ctrl_L,       nv_clear,       0,                      0),
-    NVCMD(CAR,          nv_down,        0,                      TRUE),
-    NVCMD(Ctrl_N,       nv_down,        NV_STS,                 FALSE),
-    NVCMD(Ctrl_O,       nv_ctrlo,       0,                      0),
-    NVCMD(Ctrl_P,       nv_up,          NV_STS,                 FALSE),
-    NVCMD(Ctrl_Q,       nv_visual,      0,                      FALSE),
-    NVCMD(Ctrl_R,       nv_redo_or_register, 0,                 0),
-    NVCMD(Ctrl_S,       nv_ignore,      0,                      0),
-    NVCMD(Ctrl_T,       nv_tagpop,      NV_NCW,                 0),
-    NVCMD(Ctrl_U,       nv_halfpage,    0,                      0),
-    NVCMD(Ctrl_V,       nv_visual,      0,                      FALSE),
-    NVCMD(Ctrl_W,       nv_window,      0,                      0),
-    NVCMD(Ctrl_X,       nv_addsub,      0,                      0),
-    NVCMD(Ctrl_Y,       nv_scroll_line, 0,                      FALSE),
-    NVCMD(Ctrl_Z,       nv_suspend,     0,                      0),
-    NVCMD(ESC,          nv_esc,         0,                      FALSE),
-    NVCMD(Ctrl_BSL,     nv_normal,      NV_NCH_ALW,             0),
-    NVCMD(Ctrl_RSB,     nv_ident,       NV_NCW,                 0),
-    NVCMD(Ctrl_HAT,     nv_hat,         NV_NCW,                 0),
-    NVCMD(Ctrl__,       nv_error,       0,                      0),
-    NVCMD(' ',          nv_right,       0,                      0),
-    NVCMD('!',          nv_operator,    0,                      0),
-    NVCMD('"',          nv_regname,     NV_NCH_NOP|NV_KEEPREG,  0),
-    NVCMD('#',          nv_ident,       0,                      0),
-    NVCMD('$',          nv_dollar,      0,                      0),
-    NVCMD('%',          nv_percent,     0,                      0),
-    NVCMD('&',          nv_optrans,     0,                      0),
-    NVCMD('\'',         nv_gomark,      NV_NCH_ALW,             TRUE),
-    NVCMD('(',          nv_brace,       0,                      BACKWARD),
-    NVCMD(')',          nv_brace,       0,                      FORWARD),
-    NVCMD('*',          nv_ident,       0,                      0),
-    NVCMD('+',          nv_down,        0,                      TRUE),
-    NVCMD(',',          nv_csearch,     0,                      TRUE),
-    NVCMD('-',          nv_up,          0,                      TRUE),
-    NVCMD('.',          nv_dot,         NV_KEEPREG,             0),
-    NVCMD('/',          nv_search,      0,                      FALSE),
-    NVCMD('0',          nv_beginline,   0,                      0),
-    NVCMD('1',          nv_ignore,      0,                      0),
-    NVCMD('2',          nv_ignore,      0,                      0),
-    NVCMD('3',          nv_ignore,      0,                      0),
-    NVCMD('4',          nv_ignore,      0,                      0),
-    NVCMD('5',          nv_ignore,      0,                      0),
-    NVCMD('6',          nv_ignore,      0,                      0),
-    NVCMD('7',          nv_ignore,      0,                      0),
-    NVCMD('8',          nv_ignore,      0,                      0),
-    NVCMD('9',          nv_ignore,      0,                      0),
-    NVCMD(':',          nv_colon,       0,                      0),
-    NVCMD(';',          nv_csearch,     0,                      FALSE),
-    NVCMD('<',          nv_operator,    NV_RL,                  0),
-    NVCMD('=',          nv_operator,    0,                      0),
-    NVCMD('>',          nv_operator,    NV_RL,                  0),
-    NVCMD('?',          nv_search,      0,                      FALSE),
-    NVCMD('@',          nv_at,          NV_NCH_NOP,             FALSE),
-    NVCMD('A',          nv_edit,        0,                      0),
-    NVCMD('B',          nv_bck_word,    0,                      1),
-    NVCMD('C',          nv_abbrev,      NV_KEEPREG,             0),
-    NVCMD('D',          nv_abbrev,      NV_KEEPREG,             0),
-    NVCMD('E',          nv_wordcmd,     0,                      TRUE),
-    NVCMD('F',          nv_csearch,     NV_NCH_ALW|NV_LANG,     BACKWARD),
-    NVCMD('G',          nv_goto,        0,                      TRUE),
-    NVCMD('H',          nv_scroll,      0,                      0),
-    NVCMD('I',          nv_edit,        0,                      0),
-    NVCMD('J',          nv_join,        0,                      0),
-    NVCMD('K',          nv_ident,       0,                      0),
-    NVCMD('L',          nv_scroll,      0,                      0),
-    NVCMD('M',          nv_scroll,      0,                      0),
-    NVCMD('N',          nv_next,        0,                      SEARCH_REV),
-    NVCMD('O',          nv_open,        0,                      0),
-    NVCMD('P',          nv_put,         0,                      0),
-    NVCMD('Q',          nv_exmode,      NV_NCW,                 0),
-    NVCMD('R',          nv_Replace,     0,                      FALSE),
-    NVCMD('S',          nv_subst,       NV_KEEPREG,             0),
-    NVCMD('T',          nv_csearch,     NV_NCH_ALW|NV_LANG,     BACKWARD),
-    NVCMD('U',          nv_Undo,        0,                      0),
-    NVCMD('V',          nv_visual,      0,                      FALSE),
-    NVCMD('W',          nv_wordcmd,     0,                      TRUE),
-    NVCMD('X',          nv_abbrev,      NV_KEEPREG,             0),
-    NVCMD('Y',          nv_abbrev,      NV_KEEPREG,             0),
-    NVCMD('Z',          nv_Zet,         NV_NCH_NOP|NV_NCW,      0),
-    NVCMD('[',          nv_brackets,    NV_NCH_ALW,             BACKWARD),
-    NVCMD('\\',         nv_error,       0,                      0),
-    NVCMD(']',          nv_brackets,    NV_NCH_ALW,             FORWARD),
-    NVCMD('^',          nv_beginline,   0,                  BL_WHITE | BL_FIX),
-    NVCMD('_',          nv_lineop,      0,                      0),
-    NVCMD('`',          nv_gomark,      NV_NCH_ALW,             FALSE),
-    NVCMD('a',          nv_edit,        NV_NCH,                 0),
-    NVCMD('b',          nv_bck_word,    0,                      0),
-    NVCMD('c',          nv_operator,    0,                      0),
-    NVCMD('d',          nv_operator,    0,                      0),
-    NVCMD('e',          nv_wordcmd,     0,                      FALSE),
-    NVCMD('f',          nv_csearch,     NV_NCH_ALW|NV_LANG,     FORWARD),
-    NVCMD('g',          nv_g_cmd,       NV_NCH_ALW,             FALSE),
-    NVCMD('h',          nv_left,        NV_RL,                  0),
-    NVCMD('i',          nv_edit,        NV_NCH,                 0),
-    NVCMD('j',          nv_down,        0,                      FALSE),
-    NVCMD('k',          nv_up,          0,                      FALSE),
-    NVCMD('l',          nv_right,       NV_RL,                  0),
-    NVCMD('m',          nv_mark,        NV_NCH_NOP,             0),
-    NVCMD('n',          nv_next,        0,                      0),
-    NVCMD('o',          nv_open,        0,                      0),
-    NVCMD('p',          nv_put,         0,                      0),
-    NVCMD('q',          nv_record,      NV_NCH,                 0),
-    NVCMD('r',          nv_replace,     NV_NCH_NOP|NV_LANG,     0),
-    NVCMD('s',          nv_subst,       NV_KEEPREG,             0),
-    NVCMD('t',          nv_csearch,     NV_NCH_ALW|NV_LANG,     FORWARD),
-    NVCMD('u',          nv_undo,        0,                      0),
-    NVCMD('v',          nv_visual,      0,                      FALSE),
-    NVCMD('w',          nv_wordcmd,     0,                      FALSE),
-    NVCMD('x',          nv_abbrev,      NV_KEEPREG,             0),
-    NVCMD('y',          nv_operator,    0,                      0),
-    NVCMD('z',          nv_zet,         NV_NCH_ALW,             0),
-    NVCMD('{',          nv_findpar,     0,                      BACKWARD),
-    NVCMD('|',          nv_pipe,        0,                      0),
-    NVCMD('}',          nv_findpar,     0,                      FORWARD),
-    NVCMD('~',          nv_tilde,       0,                      0),
-
-    NVCMD(POUND,        nv_ident,       0,                      0),
-    NVCMD(K_MOUSEUP,    nv_mousescroll, 0,                      MSCR_UP),
-    NVCMD(K_MOUSEDOWN,  nv_mousescroll, 0,                      MSCR_DOWN),
-    NVCMD(K_MOUSELEFT,  nv_mousescroll, 0,                      MSCR_LEFT),
-    NVCMD(K_MOUSERIGHT, nv_mousescroll, 0,                      MSCR_RIGHT),
-    NVCMD(K_LEFTMOUSE,  nv_mouse,       0,                      0),
-    NVCMD(K_LEFTMOUSE_NM, nv_mouse,     0,                      0),
-    NVCMD(K_LEFTDRAG,   nv_mouse,       0,                      0),
-    NVCMD(K_LEFTRELEASE, nv_mouse,      0,                      0),
-    NVCMD(K_LEFTRELEASE_NM, nv_mouse,   0,                      0),
-    NVCMD(K_MOUSEMOVE,  nv_mouse,       0,                      0),
-    NVCMD(K_MIDDLEMOUSE, nv_mouse,      0,                      0),
-    NVCMD(K_MIDDLEDRAG, nv_mouse,       0,                      0),
-    NVCMD(K_MIDDLERELEASE, nv_mouse,    0,                      0),
-    NVCMD(K_RIGHTMOUSE, nv_mouse,       0,                      0),
-    NVCMD(K_RIGHTDRAG,  nv_mouse,       0,                      0),
-    NVCMD(K_RIGHTRELEASE, nv_mouse,     0,                      0),
-    NVCMD(K_X1MOUSE,    nv_mouse,       0,                      0),
-    NVCMD(K_X1DRAG,     nv_mouse,       0,                      0),
-    NVCMD(K_X1RELEASE,  nv_mouse,       0,                      0),
-    NVCMD(K_X2MOUSE,    nv_mouse,       0,                      0),
-    NVCMD(K_X2DRAG,     nv_mouse,       0,                      0),
-    NVCMD(K_X2RELEASE,  nv_mouse,       0,                      0),
-    NVCMD(K_IGNORE,     nv_ignore,      NV_KEEPREG,             0),
-    NVCMD(K_NOP,        nv_nop,         0,                      0),
-    NVCMD(K_INS,        nv_edit,        0,                      0),
-    NVCMD(K_KINS,       nv_edit,        0,                      0),
-    NVCMD(K_BS,         nv_ctrlh,       0,                      0),
-    NVCMD(K_UP,         nv_up,          NV_SSS|NV_STS,          FALSE),
-    NVCMD(K_S_UP,       nv_page,        NV_SS,                  BACKWARD),
-    NVCMD(K_DOWN,       nv_down,        NV_SSS|NV_STS,          FALSE),
-    NVCMD(K_S_DOWN,     nv_page,        NV_SS,                  FORWARD),
-    NVCMD(K_LEFT,       nv_left,        NV_SSS|NV_STS|NV_RL,    0),
-    NVCMD(K_S_LEFT,     nv_bck_word,    NV_SS|NV_RL,            0),
-    NVCMD(K_C_LEFT,     nv_bck_word,    NV_SSS|NV_RL|NV_STS,    1),
-    NVCMD(K_RIGHT,      nv_right,       NV_SSS|NV_STS|NV_RL,    0),
-    NVCMD(K_S_RIGHT,    nv_wordcmd,     NV_SS|NV_RL,            FALSE),
-    NVCMD(K_C_RIGHT,    nv_wordcmd,     NV_SSS|NV_RL|NV_STS,    TRUE),
-    NVCMD(K_PAGEUP,     nv_page,        NV_SSS|NV_STS,          BACKWARD),
-    NVCMD(K_KPAGEUP,    nv_page,        NV_SSS|NV_STS,          BACKWARD),
-    NVCMD(K_PAGEDOWN,   nv_page,        NV_SSS|NV_STS,          FORWARD),
-    NVCMD(K_KPAGEDOWN,  nv_page,        NV_SSS|NV_STS,          FORWARD),
-    NVCMD(K_END,        nv_end,         NV_SSS|NV_STS,          FALSE),
-    NVCMD(K_KEND,       nv_end,         NV_SSS|NV_STS,          FALSE),
-    NVCMD(K_S_END,      nv_end,         NV_SS,                  FALSE),
-    NVCMD(K_C_END,      nv_end,         NV_SSS|NV_STS,          TRUE),
-    NVCMD(K_HOME,       nv_home,        NV_SSS|NV_STS,          0),
-    NVCMD(K_KHOME,      nv_home,        NV_SSS|NV_STS,          0),
-    NVCMD(K_S_HOME,     nv_home,        NV_SS,                  0),
-    NVCMD(K_C_HOME,     nv_goto,        NV_SSS|NV_STS,          FALSE),
-    NVCMD(K_DEL,        nv_abbrev,      0,                      0),
-    NVCMD(K_KDEL,       nv_abbrev,      0,                      0),
-    NVCMD(K_UNDO,       nv_kundo,       0,                      0),
-    NVCMD(K_HELP,       nv_help,        NV_NCW,                 0),
-    NVCMD(K_F1,         nv_help,        NV_NCW,                 0),
-    NVCMD(K_XF1,        nv_help,        NV_NCW,                 0),
-    NVCMD(K_SELECT,     nv_select,      0,                      0),
-    NVCMD(K_VER_SCROLLBAR, NV_VER_SCROLLBAR, 0,                 0),
-    NVCMD(K_HOR_SCROLLBAR, NV_HOR_SCROLLBAR, 0,                 0),
-    NVCMD(K_TABLINE,    NV_TABLINE,     0,                      0),
-    NVCMD(K_TABMENU,    NV_TABMENU,     0,                      0),
-    NVCMD(K_F21,        NV_NBCMD,       NV_NCH_ALW,             0),
-    NVCMD(K_DROP,       NV_DROP,        NV_STS,                 0),
-    NVCMD(K_CURSORHOLD, nv_cursorhold,  NV_KEEPREG,             0),
-    NVCMD(K_PS,         nv_edit,        0,                      0),
-    NVCMD(K_COMMAND,    nv_colon,       0,                      0),
-    NVCMD(K_SCRIPT_COMMAND, nv_colon,   0,                      0),
+     {NUL, nv_error, 0, 0} ,
+     {Ctrl_A, nv_addsub, 0, 0} ,
+     {Ctrl_B, nv_page, NV_STS,  (-1) } ,
+     {Ctrl_C, nv_esc, 0, TRUE} ,
+     {Ctrl_D, nv_halfpage, 0, 0} ,
+     {Ctrl_E, nv_scroll_line, 0, TRUE} ,
+     {Ctrl_F, nv_page, NV_STS, FORWARD} ,
+     {Ctrl_G, nv_ctrlg, 0, 0} ,
+     {Ctrl_H, nv_ctrlh, 0, 0} ,
+     {Ctrl_I, nv_pcmark, 0, 0} ,
+     {NL, nv_down, 0, FALSE} ,
+     {Ctrl_K, nv_error, 0, 0} ,
+     {Ctrl_L, nv_clear, 0, 0} ,
+     {CAR, nv_down, 0, TRUE} ,
+     {Ctrl_N, nv_down, NV_STS, FALSE} ,
+     {Ctrl_O, nv_ctrlo, 0, 0} ,
+     {Ctrl_P, nv_up, NV_STS, FALSE} ,
+     {Ctrl_Q, nv_visual, 0, FALSE} ,
+     {Ctrl_R, nv_redo_or_register, 0, 0} ,
+     {Ctrl_S, nv_ignore, 0, 0} ,
+     {Ctrl_T, nv_tagpop, NV_NCW, 0} ,
+     {Ctrl_U, nv_halfpage, 0, 0} ,
+     {Ctrl_V, nv_visual, 0, FALSE} ,
+     {Ctrl_W, nv_window, 0, 0} ,
+     {Ctrl_X, nv_addsub, 0, 0} ,
+     {Ctrl_Y, nv_scroll_line, 0, FALSE} ,
+     {Ctrl_Z, nv_suspend, 0, 0} ,
+     {ESC, nv_esc, 0, FALSE} ,
+     {Ctrl_BSL, nv_normal,  (0x04|NV_NCH) , 0} ,
+     {Ctrl_RSB, nv_ident, NV_NCW, 0} ,
+     {Ctrl_HAT, nv_hat, NV_NCW, 0} ,
+     {Ctrl__, nv_error, 0, 0} ,
+     {' ', nv_right, 0, 0} ,
+     {'!', nv_operator, 0, 0} ,
+     {'"', nv_regname,  (0x02|NV_NCH) |NV_KEEPREG, 0} ,
+     {'#', nv_ident, 0, 0} ,
+     {'$', nv_dollar, 0, 0} ,
+     {'%', nv_percent, 0, 0} ,
+     {'&', nv_optrans, 0, 0} ,
+     {'\'', nv_gomark,  (0x04|NV_NCH) , TRUE} ,
+     {'(', nv_brace, 0,  (-1) } ,
+     {')', nv_brace, 0, FORWARD} ,
+     {'*', nv_ident, 0, 0} ,
+     {'+', nv_down, 0, TRUE} ,
+     {',', nv_csearch, 0, TRUE} ,
+     {'-', nv_up, 0, TRUE} ,
+     {'.', nv_dot, NV_KEEPREG, 0} ,
+     {'/', nv_search, 0, FALSE} ,
+     {'0', nv_beginline, 0, 0} ,
+     {'1', nv_ignore, 0, 0} ,
+     {'2', nv_ignore, 0, 0} ,
+     {'3', nv_ignore, 0, 0} ,
+     {'4', nv_ignore, 0, 0} ,
+     {'5', nv_ignore, 0, 0} ,
+     {'6', nv_ignore, 0, 0} ,
+     {'7', nv_ignore, 0, 0} ,
+     {'8', nv_ignore, 0, 0} ,
+     {'9', nv_ignore, 0, 0} ,
+     {':', nv_colon, 0, 0} ,
+     {';', nv_csearch, 0, FALSE} ,
+     {'<', nv_operator, NV_RL, 0} ,
+     {'=', nv_operator, 0, 0} ,
+     {'>', nv_operator, NV_RL, 0} ,
+     {'?', nv_search, 0, FALSE} ,
+     {'@', nv_at,  (0x02|NV_NCH) , FALSE} ,
+     {'A', nv_edit, 0, 0} ,
+     {'B', nv_bck_word, 0, 1} ,
+     {'C', nv_abbrev, NV_KEEPREG, 0} ,
+     {'D', nv_abbrev, NV_KEEPREG, 0} ,
+     {'E', nv_wordcmd, 0, TRUE} ,
+     {'F', nv_csearch,  (0x04|NV_NCH) |NV_LANG,  (-1) } ,
+     {'G', nv_goto, 0, TRUE} ,
+     {'H', nv_scroll, 0, 0} ,
+     {'I', nv_edit, 0, 0} ,
+     {'J', nv_join, 0, 0} ,
+     {'K', nv_ident, 0, 0} ,
+     {'L', nv_scroll, 0, 0} ,
+     {'M', nv_scroll, 0, 0} ,
+     {'N', nv_next, 0, SEARCH_REV} ,
+     {'O', nv_open, 0, 0} ,
+     {'P', nv_put, 0, 0} ,
+     {'Q', nv_exmode, NV_NCW, 0} ,
+     {'R', nv_Replace, 0, FALSE} ,
+     {'S', nv_subst, NV_KEEPREG, 0} ,
+     {'T', nv_csearch,  (0x04|NV_NCH) |NV_LANG,  (-1) } ,
+     {'U', nv_Undo, 0, 0} ,
+     {'V', nv_visual, 0, FALSE} ,
+     {'W', nv_wordcmd, 0, TRUE} ,
+     {'X', nv_abbrev, NV_KEEPREG, 0} ,
+     {'Y', nv_abbrev, NV_KEEPREG, 0} ,
+     {'Z', nv_Zet,  (0x02|NV_NCH) |NV_NCW, 0} ,
+     {'[', nv_brackets,  (0x04|NV_NCH) ,  (-1) } ,
+     {'\\', nv_error, 0, 0} ,
+     {']', nv_brackets,  (0x04|NV_NCH) , FORWARD} ,
+     {'^', nv_beginline, 0, BL_WHITE | BL_FIX} ,
+     {'_', nv_lineop, 0, 0} ,
+     {'`', nv_gomark,  (0x04|NV_NCH) , FALSE} ,
+     {'a', nv_edit, NV_NCH, 0} ,
+     {'b', nv_bck_word, 0, 0} ,
+     {'c', nv_operator, 0, 0} ,
+     {'d', nv_operator, 0, 0} ,
+     {'e', nv_wordcmd, 0, FALSE} ,
+     {'f', nv_csearch,  (0x04|NV_NCH) |NV_LANG, FORWARD} ,
+     {'g', nv_g_cmd,  (0x04|NV_NCH) , FALSE} ,
+     {'h', nv_left, NV_RL, 0} ,
+     {'i', nv_edit, NV_NCH, 0} ,
+     {'j', nv_down, 0, FALSE} ,
+     {'k', nv_up, 0, FALSE} ,
+     {'l', nv_right, NV_RL, 0} ,
+     {'m', nv_mark,  (0x02|NV_NCH) , 0} ,
+     {'n', nv_next, 0, 0} ,
+     {'o', nv_open, 0, 0} ,
+     {'p', nv_put, 0, 0} ,
+     {'q', nv_record, NV_NCH, 0} ,
+     {'r', nv_replace,  (0x02|NV_NCH) |NV_LANG, 0} ,
+     {'s', nv_subst, NV_KEEPREG, 0} ,
+     {'t', nv_csearch,  (0x04|NV_NCH) |NV_LANG, FORWARD} ,
+     {'u', nv_undo, 0, 0} ,
+     {'v', nv_visual, 0, FALSE} ,
+     {'w', nv_wordcmd, 0, FALSE} ,
+     {'x', nv_abbrev, NV_KEEPREG, 0} ,
+     {'y', nv_operator, 0, 0} ,
+     {'z', nv_zet,  (0x04|NV_NCH) , 0} ,
+     {'{', nv_findpar, 0,  (-1) } ,
+     {'|', nv_pipe, 0, 0} ,
+     {'}', nv_findpar, 0, FORWARD} ,
+     {'~', nv_tilde, 0, 0} ,
+
+     {POUND, nv_ident, 0, 0} ,
+     {  (-((KS_EXTRA) + ((int)(KE_MOUSEUP) << 8)))  , nv_mousescroll, 0, MSCR_UP} ,
+     {  (-((KS_EXTRA) + ((int)(KE_MOUSEDOWN) << 8)))  , nv_mousescroll, 0, MSCR_DOWN} ,
+     {  (-((KS_EXTRA) + ((int)(KE_MOUSELEFT) << 8)))  , nv_mousescroll, 0,  (-1) } ,
+     {  (-((KS_EXTRA) + ((int)(KE_MOUSERIGHT) << 8)))  , nv_mousescroll, 0,  (-2) } ,
+     {  (-((KS_EXTRA) + ((int)(KE_LEFTMOUSE) << 8)))  , nv_mouse, 0, 0} ,
+     {  (-((KS_EXTRA) + ((int)(KE_LEFTMOUSE_NM) << 8)))  , nv_mouse, 0, 0} ,
+     {  (-((KS_EXTRA) + ((int)(KE_LEFTDRAG) << 8)))  , nv_mouse, 0, 0} ,
+     {  (-((KS_EXTRA) + ((int)(KE_LEFTRELEASE) << 8)))  , nv_mouse, 0, 0} ,
+     {  (-((KS_EXTRA) + ((int)(KE_LEFTRELEASE_NM) << 8)))  , nv_mouse, 0, 0} ,
+     {  (-((KS_EXTRA) + ((int)(KE_MOUSEMOVE) << 8)))  , nv_mouse, 0, 0} ,
+     {  (-((KS_EXTRA) + ((int)(KE_MIDDLEMOUSE) << 8)))  , nv_mouse, 0, 0} ,
+     {  (-((KS_EXTRA) + ((int)(KE_MIDDLEDRAG) << 8)))  , nv_mouse, 0, 0} ,
+     {  (-((KS_EXTRA) + ((int)(KE_MIDDLERELEASE) << 8)))  , nv_mouse, 0, 0} ,
+     {  (-((KS_EXTRA) + ((int)(KE_RIGHTMOUSE) << 8)))  , nv_mouse, 0, 0} ,
+     {  (-((KS_EXTRA) + ((int)(KE_RIGHTDRAG) << 8)))  , nv_mouse, 0, 0} ,
+     {  (-((KS_EXTRA) + ((int)(KE_RIGHTRELEASE) << 8)))  , nv_mouse, 0, 0} ,
+     {  (-((KS_EXTRA) + ((int)(KE_X1MOUSE) << 8)))  , nv_mouse, 0, 0} ,
+     {  (-((KS_EXTRA) + ((int)(KE_X1DRAG) << 8)))  , nv_mouse, 0, 0} ,
+     {  (-((KS_EXTRA) + ((int)(KE_X1RELEASE) << 8)))  , nv_mouse, 0, 0} ,
+     {  (-((KS_EXTRA) + ((int)(KE_X2MOUSE) << 8)))  , nv_mouse, 0, 0} ,
+     {  (-((KS_EXTRA) + ((int)(KE_X2DRAG) << 8)))  , nv_mouse, 0, 0} ,
+     {  (-((KS_EXTRA) + ((int)(KE_X2RELEASE) << 8)))  , nv_mouse, 0, 0} ,
+     {  (-((KS_EXTRA) + ((int)(KE_IGNORE) << 8)))  , nv_ignore, NV_KEEPREG, 0} ,
+     {  (-((KS_EXTRA) + ((int)(KE_NOP) << 8)))  , nv_nop, 0, 0} ,
+     {  (-(('k') + ((int)('I') << 8)))  , nv_edit, 0, 0} ,
+     {  (-((KS_EXTRA) + ((int)(KE_KINS) << 8)))  , nv_edit, 0, 0} ,
+     {  (-(('k') + ((int)('b') << 8)))  , nv_ctrlh, 0, 0} ,
+     {  (-(('k') + ((int)('u') << 8)))  , nv_up, NV_SSS|NV_STS, FALSE} ,
+     {  (-((KS_EXTRA) + ((int)(KE_S_UP) << 8)))  , nv_page, NV_SS,  (-1) } ,
+     {  (-(('k') + ((int)('d') << 8)))  , nv_down, NV_SSS|NV_STS, FALSE} ,
+     {  (-((KS_EXTRA) + ((int)(KE_S_DOWN) << 8)))  , nv_page, NV_SS, FORWARD} ,
+     {  (-(('k') + ((int)('l') << 8)))  , nv_left, NV_SSS|NV_STS|NV_RL, 0} ,
+     {  (-(('#') + ((int)('4') << 8)))  , nv_bck_word, NV_SS|NV_RL, 0} ,
+     {  (-((KS_EXTRA) + ((int)(KE_C_LEFT) << 8)))  , nv_bck_word, NV_SSS|NV_RL|NV_STS, 1} ,
+     {  (-(('k') + ((int)('r') << 8)))  , nv_right, NV_SSS|NV_STS|NV_RL, 0} ,
+     {  (-(('%') + ((int)('i') << 8)))  , nv_wordcmd, NV_SS|NV_RL, FALSE} ,
+     {  (-((KS_EXTRA) + ((int)(KE_C_RIGHT) << 8)))  , nv_wordcmd, NV_SSS|NV_RL|NV_STS, TRUE} ,
+     {  (-(('k') + ((int)('P') << 8)))  , nv_page, NV_SSS|NV_STS,  (-1) } ,
+     {  (-(('K') + ((int)('3') << 8)))  , nv_page, NV_SSS|NV_STS,  (-1) } ,
+     {  (-(('k') + ((int)('N') << 8)))  , nv_page, NV_SSS|NV_STS, FORWARD} ,
+     {  (-(('K') + ((int)('5') << 8)))  , nv_page, NV_SSS|NV_STS, FORWARD} ,
+     {  (-(('@') + ((int)('7') << 8)))  , nv_end, NV_SSS|NV_STS, FALSE} ,
+     {  (-(('K') + ((int)('4') << 8)))  , nv_end, NV_SSS|NV_STS, FALSE} ,
+     {  (-(('*') + ((int)('7') << 8)))  , nv_end, NV_SS, FALSE} ,
+     {  (-((KS_EXTRA) + ((int)(KE_C_END) << 8)))  , nv_end, NV_SSS|NV_STS, TRUE} ,
+     {  (-(('k') + ((int)('h') << 8)))  , nv_home, NV_SSS|NV_STS, 0} ,
+     {  (-(('K') + ((int)('1') << 8)))  , nv_home, NV_SSS|NV_STS, 0} ,
+     {  (-(('#') + ((int)('2') << 8)))  , nv_home, NV_SS, 0} ,
+     {  (-((KS_EXTRA) + ((int)(KE_C_HOME) << 8)))  , nv_goto, NV_SSS|NV_STS, FALSE} ,
+     {  (-(('k') + ((int)('D') << 8)))  , nv_abbrev, 0, 0} ,
+     {  (-((KS_EXTRA) + ((int)(KE_KDEL) << 8)))  , nv_abbrev, 0, 0} ,
+     {  (-(('&') + ((int)('8') << 8)))  , nv_kundo, 0, 0} ,
+     {  (-(('%') + ((int)('1') << 8)))  , nv_help, NV_NCW, 0} ,
+     {  (-(('k') + ((int)('1') << 8)))  , nv_help, NV_NCW, 0} ,
+     {  (-((KS_EXTRA) + ((int)(KE_XF1) << 8)))  , nv_help, NV_NCW, 0} ,
+     {  (-((KS_SELECT) + ((int)( ('X') ) << 8)))  , nv_select, 0, 0} ,
+     {  (-((KS_VER_SCROLLBAR) + ((int)( ('X') ) << 8)))  ,  nv_error , 0, 0} ,
+     {  (-((KS_HOR_SCROLLBAR) + ((int)( ('X') ) << 8)))  ,  nv_error , 0, 0} ,
+     {  (-((KS_TABLINE) + ((int)( ('X') ) << 8)))  ,  nv_error , 0, 0} ,
+     {  (-((KS_TABMENU) + ((int)( ('X') ) << 8)))  ,  nv_error , 0, 0} ,
+     {  (-(('F') + ((int)('B') << 8)))  ,  nv_error ,  (0x04|NV_NCH) , 0} ,
+     {  (-((KS_EXTRA) + ((int)(KE_DROP) << 8)))  ,  nv_error , NV_STS, 0} ,
+     {  (-((KS_EXTRA) + ((int)(KE_CURSORHOLD) << 8)))  , nv_cursorhold, NV_KEEPREG, 0} ,
+     {  (-(('P') + ((int)('S') << 8)))  , nv_edit, 0, 0} ,
+     {  (-((KS_EXTRA) + ((int)(KE_COMMAND) << 8)))  , nv_colon, 0, 0} ,
+     {  (-((KS_EXTRA) + ((int)(KE_SCRIPT_COMMAND) << 8)))  , nv_colon, 0, 0} ,
 };
 
-#define NV_CMDS_SIZE ARRAY_LENGTH(nv_cmds)
-
 // ---------------- end nv_cmds.h ----------------
 
 // ---------------- begin nv_cmdidxs.h ----------------
@@ -102006,7 +100056,7 @@
     }
 
     bot = nv_max_linear + 1;
-    top = NV_CMDS_SIZE - 1;
+    top =   (sizeof(nv_cmds) / sizeof((nv_cmds)[0]))   - 1;
     idx = -1;
     while (bot <= top)
     {
@@ -102070,14 +100120,14 @@
 }
 
     static int
-normal_cmd_get_count(cmdarg_T        *cap, int             c, int             toplevel UNUSED, int             set_prevcount UNUSED, int             *ctrl_w, int             *need_flushbuf UNUSED)
+normal_cmd_get_count(cmdarg_T        *cap, int             c, int             toplevel  __attribute__((unused)) , int             set_prevcount  __attribute__((unused)) , int             *ctrl_w, int             *need_flushbuf  __attribute__((unused)) )
 {
 getcount:
     if (!(VIsual_active && VIsual_select))
     {
-        while ((c >= '1' && c <= '9') || (cap->count0 != 0 && (c == K_DEL || c == K_KDEL || c == '0')))
+        while ((c >= '1' && c <= '9') || (cap->count0 != 0 && (c ==   (-(('k') + ((int)('D') << 8)))   || c ==   (-((KS_EXTRA) + ((int)(KE_KDEL) << 8)))   || c == '0')))
         {
-            if (c == K_DEL || c == K_KDEL)
+            if (c ==   (-(('k') + ((int)('D') << 8)))   || c ==   (-((KS_EXTRA) + ((int)(KE_KDEL) << 8)))  )
             {
                 cap->count0 /= 10;
                 del_from_showcmd(4);
@@ -102097,7 +100147,7 @@
             }
             ++no_zero_mapping;
             c = plain_vgetc();
-            LANGMAP_ADJUST(c, TRUE);
+              ;
             --no_zero_mapping;
             if (*ctrl_w)
             {
@@ -102115,7 +100165,7 @@
             ++no_mapping;
             ++allow_keys;
             c = plain_vgetc();
-            LANGMAP_ADJUST(c, TRUE);
+              ;
             --no_mapping;
             --allow_keys;
             *need_flushbuf |= add_to_showcmd(c);
@@ -102123,7 +100173,7 @@
         }
     }
 
-    if (c == K_CURSORHOLD)
+    if (c ==   (-((KS_EXTRA) + ((int)(KE_CURSORHOLD) << 8)))  )
     {
         cap->oap->prev_opcount = cap->opcount;
         cap->oap->prev_count0 = cap->count0;
@@ -102156,11 +100206,11 @@
     static int
 normal_cmd_needs_more_chars(cmdarg_T *cap, short_u cmd_flags)
 {
-    return ((cmd_flags & NV_NCH) && (((cmd_flags & NV_NCH_NOP) == NV_NCH_NOP && cap->oap->op_type == OP_NOP) || (cmd_flags & NV_NCH_ALW) == NV_NCH_ALW || (cap->cmdchar == 'q' && cap->oap->op_type == OP_NOP && reg_recording == 0 && reg_executing == 0) || ((cap->cmdchar == 'a' || cap->cmdchar == 'i') && (cap->oap->op_type != OP_NOP || VIsual_active))));
+    return ((cmd_flags & NV_NCH) && (((cmd_flags &  (0x02|NV_NCH) ) ==  (0x02|NV_NCH)  && cap->oap->op_type == OP_NOP) || (cmd_flags &  (0x04|NV_NCH) ) ==  (0x04|NV_NCH)  || (cap->cmdchar == 'q' && cap->oap->op_type == OP_NOP && reg_recording == 0 && reg_executing == 0) || ((cap->cmdchar == 'a' || cap->cmdchar == 'i') && (cap->oap->op_type != OP_NOP || VIsual_active))));
 }
 
     static int
-normal_cmd_get_more_chars(int         idx_arg, cmdarg_T    *cap, int         *need_flushbuf UNUSED)
+normal_cmd_get_more_chars(int         idx_arg, cmdarg_T    *cap, int         *need_flushbuf  __attribute__((unused)) )
 {
     int         idx = idx_arg;
     int         c;
@@ -102176,7 +100226,7 @@
     if (cap->cmdchar == 'g')
     {
         cap->nchar = plain_vgetc();
-        LANGMAP_ADJUST(cap->nchar, TRUE);
+          ;
         *need_flushbuf |= add_to_showcmd(cap->nchar);
         if (cap->nchar == 'r' || cap->nchar == '\'' || cap->nchar == '`' || cap->nchar == Ctrl_BSL)
         {
@@ -102209,7 +100259,7 @@
     {
         if (repl)
         {
-            State = MODE_REPLACE;
+            State =  (REPLACE_FLAG | MODE_INSERT) ;
         }
         if (lang && curbuf->b_p_iminsert == B_IMODE_LMAP)
         {
@@ -102217,7 +100267,7 @@
             --allow_keys;
             if (repl)
             {
-                State = MODE_LREPLACE;
+                State =  (REPLACE_FLAG | MODE_LANGMAP) ;
             }
             else
             {
@@ -102227,9 +100277,9 @@
         }
         if ((State & MODE_INSERT) && !p_ek)
         {
-            MAY_WANT_TO_LOG_THIS;
+              ;
 
-            out_str(T_BD);
+            out_str( ( term_strings[(int)(KS_CBD)] ) );
             out_str_t_TE();
         }
 
@@ -102237,7 +100287,7 @@
 
         if ((State & MODE_INSERT) && !p_ek)
         {
-            MAY_WANT_TO_LOG_THIS;
+              ;
 
             out_str_t_BE();
             out_str_t_TI();
@@ -102247,15 +100297,14 @@
         {
             ++no_mapping;
             ++allow_keys;
-            State = MODE_NORMAL_BUSY;
+            State =  (0x1000 | MODE_NORMAL) ;
         }
-        State = MODE_NORMAL_BUSY;
+        State =  (0x1000 | MODE_NORMAL) ;
         *need_flushbuf |= add_to_showcmd(*cp);
 
         if (!lit)
         {
-
-            LANGMAP_ADJUST(*cp, !lang);
+              ;
         }
 
         if (cp == &cap->extra_char && cap->nchar == Ctrl_BSL && (cap->extra_char == Ctrl_N || cap->extra_char == Ctrl_G))
@@ -102296,7 +100345,7 @@
         if (enc_utf8 && lang)
         {
             --no_mapping;
-            while ((c = vpeekc()) > 0 && (c >= 0x100 || MB_BYTE2LEN(vpeekc()) > 1))
+            while ((c = vpeekc()) > 0 && (c >= 0x100 ||  mb_bytelen_tab[vpeekc()]  > 1))
             {
                 c = plain_vgetc();
                 if (!utf_iscomposing(c))
@@ -102373,7 +100422,7 @@
 }
 
     void
-normal_cmd(oparg_T     *oap, int         toplevel UNUSED)
+normal_cmd(oparg_T     *oap, int         toplevel  __attribute__((unused)) )
 {
     cmdarg_T    ca;
     int         c;
@@ -102387,7 +100436,7 @@
     int         set_prevcount = FALSE;
     int         save_did_cursorhold = did_cursorhold;
 
-    CLEAR_FIELD(ca);
+      memset((&(ca)), (0), (sizeof(ca)))  ;
     ca.oap = oap;
 
     ca.opcount = opcount;
@@ -102410,10 +100459,10 @@
 
     mapped_len = typebuf_maplen();
 
-    State = MODE_NORMAL_BUSY;
+    State =  (0x1000 | MODE_NORMAL) ;
 
     c = safe_vgetc();
-    LANGMAP_ADJUST(c, get_real_state() != MODE_SELECT);
+      ;
 
     if (restart_edit == 0)
     {
@@ -102426,10 +100475,10 @@
 
     if (c == NUL)
     {
-        c = K_ZERO;
+        c =   (-((KS_ZERO) + ((int)( ('X') ) << 8)))  ;
     }
 
-    if (VIsual_active && VIsual_select && (vim_isprintc(c) || c == NL || c == CAR || c == K_KENTER))
+    if (VIsual_active && VIsual_select && (vim_isprintc(c) || c == NL || c == CAR || c ==   (-(('K') + ((int)('A') << 8)))  ))
     {
         int len;
 
@@ -102519,7 +100568,7 @@
         out_flush();
     }
 
-    if (ca.cmdchar != K_IGNORE)
+    if (ca.cmdchar !=   (-((KS_EXTRA) + ((int)(KE_IGNORE) << 8)))  )
     {
         if (ex_normal_busy)
         {
@@ -102543,7 +100592,7 @@
         goto normal_end;
     }
 
-    if (ca.cmdchar != K_IGNORE)
+    if (ca.cmdchar !=   (-((KS_EXTRA) + ((int)(KE_IGNORE) << 8)))  )
     {
         msg_didout = FALSE;
         msg_col = 0;
@@ -102579,7 +100628,7 @@
         old_mapped_len = typebuf_maplen();
     }
 
-    if (ca.cmdchar != K_IGNORE && ca.cmdchar != K_MOUSEMOVE)
+    if (ca.cmdchar !=   (-((KS_EXTRA) + ((int)(KE_IGNORE) << 8)))   && ca.cmdchar !=   (-((KS_EXTRA) + ((int)(KE_MOUSEMOVE) << 8)))  )
     {
         do_pending_operator(&ca, old_col, FALSE);
     }
@@ -102599,7 +100648,7 @@
         may_trigger_modechanged();
     }
 
-    if (oap->op_type == OP_NOP && oap->regname == 0 && ca.cmdchar != K_CURSORHOLD)
+    if (oap->op_type == OP_NOP && oap->regname == 0 && ca.cmdchar !=   (-((KS_EXTRA) + ((int)(KE_CURSORHOLD) << 8)))  )
     {
         clear_showcmd();
     }
@@ -102612,13 +100661,13 @@
         mb_adjust_cursor();
     }
 
-    if (curwin->w_p_scb && toplevel)
+    if (curwin-> w_onebuf_opt.wo_scb  && toplevel)
     {
         validate_cursor();
         do_check_scrollbind(TRUE);
     }
 
-    if (curwin->w_p_crb && toplevel)
+    if (curwin-> w_onebuf_opt.wo_crb  && toplevel)
     {
         validate_cursor();
         do_check_cursorbind();
@@ -102655,7 +100704,7 @@
 
     if (full_screen)
     {
-        if (!did_check && HL_ATTR(HLF_V) == 0)
+        if (!did_check &&  highlight_attr[(int)(HLF_V)]  == 0)
         {
             msg(_("Warning: terminal cannot highlight"));
         }
@@ -102674,7 +100723,6 @@
     void
 end_visual_mode_keep_button(void)
 {
-
     VIsual_active = FALSE;
     setmouse();
     mouse_dragging = 0;
@@ -102728,13 +100776,13 @@
     static int
 find_is_eval_item(char_u      *ptr, int         *colp, int         *bnp, int         dir)
 {
-    if ((*ptr == ']' && dir == BACKWARD) || (*ptr == '[' && dir == FORWARD))
+    if ((*ptr == ']' && dir ==  (-1) ) || (*ptr == '[' && dir == FORWARD))
     {
         ++*bnp;
     }
     if (*bnp > 0)
     {
-        if ((*ptr == '[' && dir == BACKWARD) || (*ptr == ']' && dir == FORWARD))
+        if ((*ptr == '[' && dir ==  (-1) ) || (*ptr == ']' && dir == FORWARD))
         {
             --*bnp;
         }
@@ -102746,7 +100794,7 @@
         return TRUE;
     }
 
-    if (ptr[dir == BACKWARD ? 0 : 1] == '>' && ptr[dir == BACKWARD ? -1 : 0] == '-')
+    if (ptr[dir ==  (-1)  ? 0 : 1] == '>' && ptr[dir ==  (-1)  ? -1 : 0] == '-')
     {
         *colp += dir;
         return TRUE;
@@ -102793,7 +100841,7 @@
         }
         else
         {
-            while (ptr[col] != NUL && (i == 0 ? !vim_iswordc(ptr[col]) : VIM_ISWHITE(ptr[col])) && (!(find_type & FIND_EVAL) || ptr[col] != ']'))
+            while (ptr[col] != NUL && (i == 0 ? !vim_iswordc(ptr[col]) :  ((ptr[col]) == ' ' || (ptr[col]) == '\t') ) && (!(find_type & FIND_EVAL) || ptr[col] != ']'))
             {
                 ++col;
             }
@@ -102815,7 +100863,7 @@
             {
                 prevcol = col - 1 - (*mb_head_off)(ptr, ptr + col - 1);
                 prev_class = mb_get_class(ptr + prevcol);
-                if (this_class != prev_class && (i == 0 || prev_class == 0 || (find_type & FIND_IDENT)) && (!(find_type & FIND_EVAL) || prevcol == 0 || !find_is_eval_item(ptr + prevcol, &prevcol, &bn, BACKWARD)))
+                if (this_class != prev_class && (i == 0 || prev_class == 0 || (find_type & FIND_IDENT)) && (!(find_type & FIND_EVAL) || prevcol == 0 || !find_is_eval_item(ptr + prevcol, &prevcol, &bn,  (-1) )))
                 {
                     break;
                 }
@@ -102833,7 +100881,7 @@
         }
         else
         {
-            while (col > 0 && ((i == 0 ? vim_iswordc(ptr[col - 1]) : (!VIM_ISWHITE(ptr[col - 1]) && (!(find_type & FIND_IDENT) || !vim_iswordc(ptr[col - 1])))) || ((find_type & FIND_EVAL) && col > 1 && find_is_eval_item(ptr + col - 1, &col, &bn, BACKWARD))))
+            while (col > 0 && ((i == 0 ? vim_iswordc(ptr[col - 1]) : (! ((ptr[col - 1]) == ' ' || (ptr[col - 1]) == '\t')  && (!(find_type & FIND_IDENT) || !vim_iswordc(ptr[col - 1])))) || ((find_type & FIND_EVAL) && col > 1 && find_is_eval_item(ptr + col - 1, &col, &bn,  (-1) ))))
             {
                 --col;
             }
@@ -102880,7 +100928,7 @@
     }
     else
     {
-        while ((i == 0 ? vim_iswordc(ptr[col]) : (ptr[col] != NUL && !VIM_ISWHITE(ptr[col]))) || ((find_type & FIND_EVAL) && col <= (int)startcol && find_is_eval_item(ptr + col, &col, &bn, FORWARD)))
+        while ((i == 0 ? vim_iswordc(ptr[col]) : (ptr[col] != NUL && ! ((ptr[col]) == ' ' || (ptr[col]) == '\t') )) || ((find_type & FIND_EVAL) && col <= (int)startcol && find_is_eval_item(ptr + col, &col, &bn, FORWARD)))
         {
             ++col;
         }
@@ -102985,23 +101033,23 @@
 {
     switch (cap->cmdchar)
     {
-        case K_S_RIGHT:
-            cap->cmdchar = K_RIGHT;
+        case   (-(('%') + ((int)('i') << 8)))  :
+            cap->cmdchar =   (-(('k') + ((int)('r') << 8)))  ;
             break;
-        case K_S_LEFT:
-            cap->cmdchar = K_LEFT;
+        case   (-(('#') + ((int)('4') << 8)))  :
+            cap->cmdchar =   (-(('k') + ((int)('l') << 8)))  ;
             break;
-        case K_S_UP:
-            cap->cmdchar = K_UP;
+        case   (-((KS_EXTRA) + ((int)(KE_S_UP) << 8)))  :
+            cap->cmdchar =   (-(('k') + ((int)('u') << 8)))  ;
             break;
-        case K_S_DOWN:
-            cap->cmdchar = K_DOWN;
+        case   (-((KS_EXTRA) + ((int)(KE_S_DOWN) << 8)))  :
+            cap->cmdchar =   (-(('k') + ((int)('d') << 8)))  ;
             break;
-        case K_S_HOME:
-            cap->cmdchar = K_HOME;
+        case   (-(('#') + ((int)('2') << 8)))  :
+            cap->cmdchar =   (-(('k') + ((int)('h') << 8)))  ;
             break;
-        case K_S_END:
-            cap->cmdchar = K_END;
+        case   (-(('*') + ((int)('7') << 8)))  :
+            cap->cmdchar =   (-(('@') + ((int)('7') << 8)))  ;
             break;
     }
     cap->cmdchar = simplify_key(cap->cmdchar, &mod_mask);
@@ -103020,7 +101068,7 @@
     }
 }
 
-static char_u   old_showcmd_buf[SHOWCMD_BUFLEN];
+static char_u   old_showcmd_buf[ (SHOWCMD_COLS + 1 + 30) ];
 static int      showcmd_is_clear = TRUE;
 static int      showcmd_visual = FALSE;
 
@@ -103036,7 +101084,7 @@
 
     if (VIsual_active && stuff_empty() && typebuf.tb_len == 0 && !using_script())
     {
-        int             cursor_bot = LT_POS(VIsual, curwin->w_cursor);
+        int             cursor_bot =  (((VIsual).lnum != (curwin->w_cursor).lnum)               ? (VIsual).lnum < (curwin->w_cursor).lnum                   : (VIsual).col != (curwin->w_cursor).col                        ? (VIsual).col < (curwin->w_cursor).col                     : (VIsual).coladd < (curwin->w_cursor).coladd) ;
         long            lines;
         colnr_T leftcol;
         colnr_T rightcol;
@@ -103058,11 +101106,11 @@
         if (VIsual_mode == Ctrl_V)
         {
             getvcols(curwin, &curwin->w_cursor, &VIsual, &leftcol, &rightcol, GETVCOL_END_EXCL_LBR);
-            vim_snprintf((char *)showcmd_buf, SHOWCMD_BUFLEN, "%ldx%ld", lines, (long)(rightcol - leftcol + 1));
+            vim_snprintf((char *)showcmd_buf,  (SHOWCMD_COLS + 1 + 30) , "%ldx%ld", lines, (long)(rightcol - leftcol + 1));
         }
         else if (VIsual_mode == 'V' || VIsual.lnum != curwin->w_cursor.lnum)
         {
-            vim_snprintf((char *)showcmd_buf, SHOWCMD_BUFLEN, "%ld", lines);
+            vim_snprintf((char *)showcmd_buf,  (SHOWCMD_COLS + 1 + 30) , "%ld", lines);
         }
         else
         {
@@ -103097,11 +101145,11 @@
             }
             if (bytes == chars)
             {
-                vim_snprintf((char *)showcmd_buf, SHOWCMD_BUFLEN, "%d", chars);
+                vim_snprintf((char *)showcmd_buf,  (SHOWCMD_COLS + 1 + 30) , "%d", chars);
             }
             else
             {
-                vim_snprintf((char *)showcmd_buf, SHOWCMD_BUFLEN, "%d-%d", chars, bytes);
+                vim_snprintf((char *)showcmd_buf,  (SHOWCMD_COLS + 1 + 30) , "%d-%d", chars, bytes);
             }
         }
         showcmd_buf[SHOWCMD_COLS] = NUL;
@@ -103132,13 +101180,13 @@
     char_u      mbyte_buf[MB_MAXBYTES];
     static int  ignore[] =
     {
-        K_IGNORE, K_PS,
-        K_LEFTMOUSE, K_LEFTDRAG, K_LEFTRELEASE, K_MOUSEMOVE,
-        K_MIDDLEMOUSE, K_MIDDLEDRAG, K_MIDDLERELEASE,
-        K_RIGHTMOUSE, K_RIGHTDRAG, K_RIGHTRELEASE,
-        K_MOUSEDOWN, K_MOUSEUP, K_MOUSELEFT, K_MOUSERIGHT,
-        K_X1MOUSE, K_X1DRAG, K_X1RELEASE, K_X2MOUSE, K_X2DRAG, K_X2RELEASE,
-        K_CURSORHOLD, K_COMMAND, K_SCRIPT_COMMAND,
+          (-((KS_EXTRA) + ((int)(KE_IGNORE) << 8)))  ,   (-(('P') + ((int)('S') << 8)))  ,
+          (-((KS_EXTRA) + ((int)(KE_LEFTMOUSE) << 8)))  ,   (-((KS_EXTRA) + ((int)(KE_LEFTDRAG) << 8)))  ,   (-((KS_EXTRA) + ((int)(KE_LEFTRELEASE) << 8)))  ,   (-((KS_EXTRA) + ((int)(KE_MOUSEMOVE) << 8)))  ,
+          (-((KS_EXTRA) + ((int)(KE_MIDDLEMOUSE) << 8)))  ,   (-((KS_EXTRA) + ((int)(KE_MIDDLEDRAG) << 8)))  ,   (-((KS_EXTRA) + ((int)(KE_MIDDLERELEASE) << 8)))  ,
+          (-((KS_EXTRA) + ((int)(KE_RIGHTMOUSE) << 8)))  ,   (-((KS_EXTRA) + ((int)(KE_RIGHTDRAG) << 8)))  ,   (-((KS_EXTRA) + ((int)(KE_RIGHTRELEASE) << 8)))  ,
+          (-((KS_EXTRA) + ((int)(KE_MOUSEDOWN) << 8)))  ,   (-((KS_EXTRA) + ((int)(KE_MOUSEUP) << 8)))  ,   (-((KS_EXTRA) + ((int)(KE_MOUSELEFT) << 8)))  ,   (-((KS_EXTRA) + ((int)(KE_MOUSERIGHT) << 8)))  ,
+          (-((KS_EXTRA) + ((int)(KE_X1MOUSE) << 8)))  ,   (-((KS_EXTRA) + ((int)(KE_X1DRAG) << 8)))  ,   (-((KS_EXTRA) + ((int)(KE_X1RELEASE) << 8)))  ,   (-((KS_EXTRA) + ((int)(KE_X2MOUSE) << 8)))  ,   (-((KS_EXTRA) + ((int)(KE_X2DRAG) << 8)))  ,   (-((KS_EXTRA) + ((int)(KE_X2RELEASE) << 8)))  ,
+          (-((KS_EXTRA) + ((int)(KE_CURSORHOLD) << 8)))  ,   (-((KS_EXTRA) + ((int)(KE_COMMAND) << 8)))  ,   (-((KS_EXTRA) + ((int)(KE_SCRIPT_COMMAND) << 8)))  ,
         0
     };
 
@@ -103153,7 +101201,7 @@
         showcmd_visual = FALSE;
     }
 
-    if (IS_SPECIAL(c))
+    if ( ((c) < 0) )
     {
         for (i = 0; ignore[i] != 0; ++i)
         {
@@ -103169,7 +101217,7 @@
         p = transchar(c);
         if (*p == ' ')
         {
-            STRCPY(p, "<20>");
+             strcpy((char *)(p), (char *)("<20>")) ;
         }
     }
     else
@@ -103177,14 +101225,14 @@
         mbyte_buf[(*mb_char2bytes)(c, mbyte_buf)] = NUL;
         p = mbyte_buf;
     }
-    old_len = (int)STRLEN(showcmd_buf);
-    extra_len = (int)STRLEN(p);
+    old_len = (int) strlen((char *)(showcmd_buf)) ;
+    extra_len = (int) strlen((char *)(p)) ;
     overflow = old_len + extra_len - SHOWCMD_COLS;
     if (overflow > 0)
     {
-        mch_memmove(showcmd_buf, showcmd_buf + overflow, old_len - overflow + 1);
+         memmove((char *)(showcmd_buf), (char *)(showcmd_buf + overflow), old_len - overflow + 1) ;
     }
-    STRCAT(showcmd_buf, p);
+     strcat((char *)(showcmd_buf), (char *)(p)) ;
 
     if (char_avail())
     {
@@ -103215,7 +101263,7 @@
         return;
     }
 
-    old_len = (int)STRLEN(showcmd_buf);
+    old_len = (int) strlen((char *)(showcmd_buf)) ;
     if (len > old_len)
     {
         len = old_len;
@@ -103233,7 +101281,7 @@
 {
     if (p_sc)
     {
-        STRCPY(old_showcmd_buf, showcmd_buf);
+         strcpy((char *)(old_showcmd_buf), (char *)(showcmd_buf)) ;
     }
 }
 
@@ -103245,7 +101293,7 @@
         return;
     }
 
-    STRCPY(showcmd_buf, old_showcmd_buf);
+     strcpy((char *)(showcmd_buf), (char *)(old_showcmd_buf)) ;
 
     display_showcmd();
 }
@@ -103307,7 +101355,7 @@
     static buf_T        *old_buf = NULL;
     static colnr_T      old_leftcol = 0;
 
-    if (check && curwin->w_p_scb)
+    if (check && curwin-> w_onebuf_opt.wo_scb )
     {
         if (did_syncbind)
         {
@@ -103350,10 +101398,10 @@
     want_hor = (vim_strchr(p_sbo, 'h') && (leftcol_diff || topline_diff != 0));
 
     VIsual_select = VIsual_active = 0;
-    FOR_ALL_WINDOWS(curwin)
+     for ((curwin) = firstwin; (curwin) != NULL; (curwin) = (curwin)->w_next) 
     {
         curbuf = curwin->w_buffer;
-        if (curwin == old_curwin || !curwin->w_p_scb)
+        if (curwin == old_curwin || !curwin-> w_onebuf_opt.wo_scb )
         {
             continue;
         }
@@ -103407,7 +101455,7 @@
 }
 
     static void
-nv_nop(cmdarg_T *cap UNUSED)
+nv_nop(cmdarg_T *cap  __attribute__((unused)) )
 {
 }
 
@@ -103456,7 +101504,7 @@
 
     if (mod_mask & MOD_MASK_CTRL)
     {
-        if (cap->arg == BACKWARD)
+        if (cap->arg ==  (-1) )
         {
             goto_tabpage(-(int)cap->count1);
         }
@@ -103564,7 +101612,7 @@
     p_ws = FALSE;
     p_scs = FALSE;
 
-    if (!locally || !findpar(&incll, BACKWARD, 1L, '{', FALSE))
+    if (!locally || !findpar(&incll,  (-1) , 1L, '{', FALSE))
     {
         setpcmark();
         curwin->w_cursor.lnum = 1;
@@ -103580,7 +101628,9 @@
     }
     curwin->w_cursor.col = 0;
 
-    CLEAR_POS(&found_pos);
+     (&found_pos)->lnum = 0;
+     (&found_pos)->col = 0;
+     (&found_pos)->coladd = 0;
     for (;;)
     {
         t = searchit(curwin, curbuf, &curwin->w_cursor, NULL, FORWARD, pat, patlen, 1L, searchflags, RE_LAST, NULL);
@@ -103638,7 +101688,9 @@
 
         if (!valid)
         {
-            CLEAR_POS(&found_pos);
+             (&found_pos)->lnum = 0;
+             (&found_pos)->col = 0;
+             (&found_pos)->coladd = 0;
         }
         else
         {
@@ -103668,7 +101720,6 @@
     int
 nv_screengo(oparg_T *oap, int dir, long dist)
 {
-
     int         linelen = linetabsize_no_outer(curwin, curwin->w_cursor.lnum);
 
     int         retval = OK;
@@ -103728,7 +101779,7 @@
 
       while (dist--)
       {
-        if (dir == BACKWARD)
+        if (dir ==  (-1) )
         {
             if ((long)curwin->w_curswant >= width1)
             {
@@ -103793,7 +101844,7 @@
         coladvance(curwin->w_curswant);
     }
 
-    if (curwin->w_cursor.col > 0 && curwin->w_p_wrap)
+    if (curwin->w_cursor.col > 0 && curwin-> w_onebuf_opt.wo_wrap )
     {
         colnr_T virtcol;
         int     c;
@@ -103848,16 +101899,16 @@
         ++no_mapping;
         ++allow_keys;
         nchar = plain_vgetc();
-        LANGMAP_ADJUST(nchar, TRUE);
+          ;
         --no_mapping;
         --allow_keys;
         (void)add_to_showcmd(nchar);
 
-        if (nchar == K_DEL || nchar == K_KDEL)
+        if (nchar ==   (-(('k') + ((int)('D') << 8)))   || nchar ==   (-((KS_EXTRA) + ((int)(KE_KDEL) << 8)))  )
         {
             n /= 10;
         }
-        else if (VIM_ISDIGIT(nchar))
+        else if ( ((unsigned)(nchar) - '0' < 10) )
         {
             if (vim_append_digit_long(&n, nchar - '0') == FAIL)
             {
@@ -103870,7 +101921,7 @@
             win_setheight((int)n);
             break;
         }
-        else if (nchar == 'l' || nchar == 'h' || nchar == K_LEFT || nchar == K_RIGHT)
+        else if (nchar == 'l' || nchar == 'h' || nchar ==   (-(('k') + ((int)('l') << 8)))   || nchar ==   (-(('k') + ((int)('r') << 8)))  )
         {
             cap->count1 = n ? n * cap->count1 : cap->count1;
             *nchar_arg = nchar;
@@ -103894,7 +101945,7 @@
     int         nchar = cap->nchar;
     long        siso = get_sidescrolloff_value();
 
-    if (VIM_ISDIGIT(nchar) && !nv_z_get_count(cap, &nchar))
+    if ( ((unsigned)(nchar) - '0' < 10)  && !nv_z_get_count(cap, &nchar))
     {
             return;
     }
@@ -103933,11 +101984,13 @@
                         curwin->w_cursor.lnum = curwin->w_botline;
                     }
                 }
+    __attribute__((fallthrough));
     case NL:
     case CAR:
-    case K_KENTER:
+    case   (-(('K') + ((int)('A') << 8)))  :
                 beginline(BL_WHITE | BL_FIX);
 
+    __attribute__((fallthrough));
     case 't':
         scroll_cursor_top(0, TRUE);
                 redraw_later(UPD_VALID);
@@ -103947,6 +102000,7 @@
     case '.':
         beginline(BL_WHITE | BL_FIX);
 
+    __attribute__((fallthrough));
     case 'z':
         scroll_cursor_halfway(TRUE, FALSE);
                 redraw_later(UPD_VALID);
@@ -103967,9 +102021,11 @@
                 {
                     curwin->w_cursor.lnum = curwin->w_topline - 1;
                 }
+    __attribute__((fallthrough));
     case '-':
                 beginline(BL_WHITE | BL_FIX);
 
+    __attribute__((fallthrough));
     case 'b':
         scroll_cursor_bot(0, TRUE);
                 redraw_later(UPD_VALID);
@@ -103979,9 +102035,10 @@
     case 'H':
                 cap->count1 *= curwin->w_width / 2;
 
+    __attribute__((fallthrough));
     case 'h':
-    case K_LEFT:
-                if (!curwin->w_p_wrap)
+    case   (-(('k') + ((int)('l') << 8)))  :
+                if (!curwin-> w_onebuf_opt.wo_wrap )
                 {
                     (void)set_leftcol((colnr_T)cap->count1 > curwin->w_leftcol ? 0 : curwin->w_leftcol - (colnr_T)cap->count1);
                 }
@@ -103990,16 +102047,17 @@
     case 'L':
         cap->count1 *= curwin->w_width / 2;
 
+    __attribute__((fallthrough));
     case 'l':
-    case K_RIGHT:
-                if (!curwin->w_p_wrap)
+    case   (-(('k') + ((int)('r') << 8)))  :
+                if (!curwin-> w_onebuf_opt.wo_wrap )
                 {
                     (void)set_leftcol(curwin->w_leftcol + (colnr_T)cap->count1);
                 }
                 break;
 
     case 's':
-        if (!curwin->w_p_wrap)
+        if (!curwin-> w_onebuf_opt.wo_wrap )
                 {
                     getvcol(curwin, &curwin->w_cursor, &col, NULL, NULL, 0);
                     if ((long)col > siso)
@@ -104019,7 +102077,7 @@
                 break;
 
     case 'e':
-        if (!curwin->w_p_wrap)
+        if (!curwin-> w_onebuf_opt.wo_wrap )
                 {
                     getvcol(curwin, &curwin->w_cursor, NULL, NULL, &col, 0);
                     n = curwin->w_width - curwin_col_off();
@@ -104075,8 +102133,8 @@
 {
     int old_p_im;
     int cmd_result;
-    int is_cmdkey = cap->cmdchar == K_COMMAND
-                                           || cap->cmdchar == K_SCRIPT_COMMAND;
+    int is_cmdkey = cap->cmdchar ==   (-((KS_EXTRA) + ((int)(KE_COMMAND) << 8)))  
+                                           || cap->cmdchar ==   (-((KS_EXTRA) + ((int)(KE_SCRIPT_COMMAND) << 8)))  ;
     int flags;
 
     if (VIsual_active && !is_cmdkey)
@@ -104235,7 +102293,7 @@
     cmdarg_T    ca;
 
     clear_oparg(&oa);
-    CLEAR_FIELD(ca);
+      memset((&(ca)), (0), (sizeof(ca)))  ;
     ca.oap = &oa;
     ca.cmdchar = c1;
     ca.nchar = c2;
@@ -104251,8 +102309,8 @@
 
     if (kp_help)
     {
-        STRCPY(buf, "he! ");
-        *buflen = STRLEN_LITERAL("he! ");
+         strcpy((char *)(buf), (char *)("he! ")) ;
+        *buflen =  (sizeof("he! " "") - 1) ;
         return n;
     }
 
@@ -104282,8 +102340,8 @@
         return 0;
     }
 
-    isman = (STRCMP(kp, "man") == 0);
-    isman_s = (STRCMP(kp, "man -s") == 0);
+    isman = ( strcmp((char *)(kp), (char *)("man"))  == 0);
+    isman_s = ( strcmp((char *)(kp), (char *)("man -s"))  == 0);
     if (cap->count0 != 0 && !(isman || isman_s))
     {
         *buflen = vim_snprintf((char *)buf, bufsize, ".,.+%ld! ", cap->count0 - 1);
@@ -104363,14 +102421,14 @@
     }
 
     kp = (*curbuf->b_p_kp == NUL ? p_kp : curbuf->b_p_kp);
-    kp_help = (*kp == NUL || STRCMP(kp, ":he") == 0 || STRCMP(kp, ":help") == 0);
+    kp_help = (*kp == NUL ||  strcmp((char *)(kp), (char *)(":he"))  == 0 ||  strcmp((char *)(kp), (char *)(":help"))  == 0);
     if (kp_help && *skipwhite(ptr) == NUL)
     {
         emsg(_(e_no_identifier_under_cursor));
         return;
     }
     kp_ex = (*kp == ':');
-    bufsize = (size_t)(n * 2 + 30 + STRLEN(kp));
+    bufsize = (size_t)(n * 2 + 30 +  strlen((char *)(kp)) );
     buf = alloc(bufsize);
     if (buf == NULL)
     {
@@ -104388,8 +102446,8 @@
 
             if (!g_cmd && vim_iswordp(ptr))
             {
-                STRCPY(buf, "\\<");
-                buflen = STRLEN_LITERAL("\\<");
+                 strcpy((char *)(buf), (char *)("\\<")) ;
+                buflen =  (sizeof("\\<" "") - 1) ;
             }
             no_smartcase = TRUE;
             break;
@@ -104405,8 +102463,8 @@
         case ']':
             tag_cmd = TRUE;
             {
-                STRCPY(buf, "ts ");
-                buflen = STRLEN_LITERAL("ts ");
+                 strcpy((char *)(buf), (char *)("ts ")) ;
+                buflen =  (sizeof("ts " "") - 1) ;
             }
             break;
 
@@ -104414,20 +102472,20 @@
             tag_cmd = TRUE;
             if (curbuf->b_help)
             {
-                STRCPY(buf, "he! ");
-                buflen = STRLEN_LITERAL("he! ");
+                 strcpy((char *)(buf), (char *)("he! ")) ;
+                buflen =  (sizeof("he! " "") - 1) ;
             }
             else
             {
                 if (g_cmd)
                 {
-                    STRCPY(buf, "tj ");
-                    buflen = STRLEN_LITERAL("tj ");
+                     strcpy((char *)(buf), (char *)("tj ")) ;
+                    buflen =  (sizeof("tj " "") - 1) ;
                 }
                 else if (cap->count0 == 0)
                 {
-                    STRCPY(buf, "ta ");
-                    buflen = STRLEN_LITERAL("ta ");
+                     strcpy((char *)(buf), (char *)("ta ")) ;
+                    buflen =  (sizeof("ta " "") - 1) ;
                 }
                 else
                 {
@@ -104455,8 +102513,8 @@
             vim_free(buf);
             return;
         }
-        plen = STRLEN(p);
-        newbuf = vim_realloc(buf, buflen + plen + 1);
+        plen =  strlen((char *)(p)) ;
+        newbuf =  realloc((buf), (buflen + plen + 1)) ;
         if (newbuf == NULL)
         {
             vim_free(buf);
@@ -104464,7 +102522,7 @@
             return;
         }
         buf = newbuf;
-        STRCPY(buf + buflen, p);
+         strcpy((char *)(buf + buflen), (char *)(p)) ;
         buflen += plen;
         vim_free(p);
     }
@@ -104480,7 +102538,7 @@
         }
         else if (tag_cmd)
         {
-            if (STRCMP(curbuf->b_p_ft, "help") == 0)
+            if ( strcmp((char *)(curbuf->b_p_ft), (char *)("help"))  == 0)
             {
                 aux_ptr = (char_u *)"";
             }
@@ -104522,8 +102580,8 @@
     {
         if (!g_cmd && (has_mbyte ? vim_iswordp(mb_prevptr(ml_get_curline(), ptr)) : vim_iswordc(ptr[-1])))
         {
-            STRCPY(buf + buflen, "\\>");
-            buflen += STRLEN_LITERAL("\\>");
+             strcpy((char *)(buf + buflen), (char *)("\\>")) ;
+            buflen +=  (sizeof("\\>" "") - 1) ;
         }
 
         init_history();
@@ -104563,7 +102621,7 @@
     }
     else
     {
-        if (LT_POS(curwin->w_cursor, VIsual))
+        if ( (((curwin->w_cursor).lnum != (VIsual).lnum)               ? (curwin->w_cursor).lnum < (VIsual).lnum                   : (curwin->w_cursor).col != (VIsual).col                        ? (curwin->w_cursor).col < (VIsual).col                     : (curwin->w_cursor).coladd < (VIsual).coladd) )
         {
             *pp = ml_get_pos(&curwin->w_cursor);
             *lenp = VIsual.col - curwin->w_cursor.col + 1;
@@ -104691,9 +102749,9 @@
     {
         if ((!past_line && oneright() == FAIL) || (past_line && *ml_get_cursor() == NUL))
         {
-            if (       ((cap->cmdchar == ' ' && vim_strchr(p_ww, 's') != NULL) || (cap->cmdchar == 'l' && vim_strchr(p_ww, 'l') != NULL) || (cap->cmdchar == K_RIGHT && vim_strchr(p_ww, '>') != NULL)) && curwin->w_cursor.lnum < curbuf->b_ml.ml_line_count)
+            if (       ((cap->cmdchar == ' ' && vim_strchr(p_ww, 's') != NULL) || (cap->cmdchar == 'l' && vim_strchr(p_ww, 'l') != NULL) || (cap->cmdchar ==   (-(('k') + ((int)('r') << 8)))   && vim_strchr(p_ww, '>') != NULL)) && curwin->w_cursor.lnum < curbuf->b_ml.ml_line_count)
             {
-                if (       cap->oap->op_type != OP_NOP && !cap->oap->inclusive && !LINEEMPTY(curwin->w_cursor.lnum))
+                if (       cap->oap->op_type != OP_NOP && !cap->oap->inclusive && ! (*ml_get(curwin->w_cursor.lnum) == NUL) )
                 {
                     cap->oap->inclusive = TRUE;
                 }
@@ -104716,7 +102774,7 @@
             }
             else
             {
-                if (!LINEEMPTY(curwin->w_cursor.lnum))
+                if (! (*ml_get(curwin->w_cursor.lnum) == NUL) )
                 {
                     cap->oap->inclusive = TRUE;
                 }
@@ -104766,13 +102824,13 @@
     {
         if (oneleft() == FAIL)
         {
-            if (       (((cap->cmdchar == K_BS || cap->cmdchar == Ctrl_H) && vim_strchr(p_ww, 'b') != NULL) || (cap->cmdchar == 'h' && vim_strchr(p_ww, 'h') != NULL) || (cap->cmdchar == K_LEFT && vim_strchr(p_ww, '<') != NULL)) && curwin->w_cursor.lnum > 1)
+            if (       (((cap->cmdchar ==   (-(('k') + ((int)('b') << 8)))   || cap->cmdchar == Ctrl_H) && vim_strchr(p_ww, 'b') != NULL) || (cap->cmdchar == 'h' && vim_strchr(p_ww, 'h') != NULL) || (cap->cmdchar ==   (-(('k') + ((int)('l') << 8)))   && vim_strchr(p_ww, '<') != NULL)) && curwin->w_cursor.lnum > 1)
             {
                 --(curwin->w_cursor.lnum);
                 coladvance((colnr_T)MAXCOL);
                 curwin->w_set_curswant = true;
 
-                if (       (cap->oap->op_type == OP_DELETE || cap->oap->op_type == OP_CHANGE) && !LINEEMPTY(curwin->w_cursor.lnum))
+                if (       (cap->oap->op_type == OP_DELETE || cap->oap->op_type == OP_CHANGE) && ! (*ml_get(curwin->w_cursor.lnum) == NUL) )
                 {
                     char_u *cp = ml_get_cursor();
 
@@ -104805,7 +102863,7 @@
 {
     if (mod_mask & MOD_MASK_SHIFT)
     {
-        cap->arg = BACKWARD;
+        cap->arg =  (-1) ;
         nv_page(cap);
         return;
     }
@@ -104875,7 +102933,7 @@
             (void)autowrite(curbuf, FALSE);
         }
         setpcmark();
-        if (do_ecmd(0, ptr, NULL, NULL, ECMD_LAST, buf_hide(curbuf) ? ECMD_HIDE : 0, curwin) == OK && cap->nchar == 'F' && lnum >= 0)
+        if (do_ecmd(0, ptr, NULL, NULL,  ((linenr_T)-1) , buf_hide(curbuf) ? ECMD_HIDE : 0, curwin) == OK && cap->nchar == 'F' && lnum >= 0)
         {
             curwin->w_cursor.lnum = lnum;
             check_cursor_lnum();
@@ -104938,7 +102996,7 @@
         return;
     }
 
-    (void)normal_search(cap, cap->cmdchar, cap->searchbuf, STRLEN(cap->searchbuf), (cap->arg || !EQUAL_POS(save_cursor, curwin->w_cursor)) ? 0 : SEARCH_MARK, NULL);
+    (void)normal_search(cap, cap->cmdchar, cap->searchbuf,  strlen((char *)(cap->searchbuf)) , (cap->arg || ! (((save_cursor).lnum == (curwin->w_cursor).lnum) && ((save_cursor).col == (curwin->w_cursor).col) && ((save_cursor).coladd == (curwin->w_cursor).coladd)) ) ? 0 : SEARCH_MARK, NULL);
 }
 
     static void
@@ -104948,7 +103006,7 @@
     int     wrapped = FALSE;
     int     i = normal_search(cap, 0, NULL, 0, SEARCH_MARK | cap->arg, &wrapped);
 
-    if (i == 1 && !wrapped && EQUAL_POS(old, curwin->w_cursor))
+    if (i == 1 && !wrapped &&  (((old).lnum == (curwin->w_cursor).lnum) && ((old).col == (curwin->w_cursor).col) && ((old).coladd == (curwin->w_cursor).coladd)) )
     {
         cap->count1 += 1;
         (void)normal_search(cap, 0, NULL, 0, SEARCH_MARK | cap->arg, NULL);
@@ -104973,7 +103031,7 @@
     cap->oap->use_reg_one = TRUE;
     curwin->w_set_curswant = true;
 
-    CLEAR_FIELD(sia);
+      memset((&(sia)), (0), (sizeof(sia)))  ;
     i = do_search(cap->oap, dir, dir, pat, patlen, cap->count1, opt | SEARCH_OPT | SEARCH_ECHO | SEARCH_MSG, &sia);
     if (wrapped != NULL)
     {
@@ -104991,7 +103049,7 @@
         }
         curwin->w_cursor.coladd = 0;
     }
-    if (!EQUAL_POS(curwin->w_cursor, prev_cursor) && p_hls && !no_hlsearch)
+    if (! (((curwin->w_cursor).lnum == (prev_cursor).lnum) && ((curwin->w_cursor).col == (prev_cursor).col) && ((curwin->w_cursor).coladd == (prev_cursor).coladd))  && p_hls && !no_hlsearch)
     {
         redraw_later(UPD_SOME_VALID);
     }
@@ -105023,7 +103081,7 @@
     }
 
     cap->oap->motion_type = MCHAR;
-    if (IS_SPECIAL(cap->nchar) || searchc(cap, t_cmd) == FAIL)
+    if ( ((cap->nchar) < 0)  || searchc(cap, t_cmd) == FAIL)
     {
         clearopbeep(cap->oap);
         if (cursor_dec)
@@ -105196,7 +103254,6 @@
     }
     else
     {
-
     if (  (cap->cmdchar == '[' && vim_strchr((char_u *)"{(*/#mM", cap->nchar) != NULL) || (cap->cmdchar == ']' && vim_strchr((char_u *)"})*/#mM", cap->nchar) != NULL))
     {
         nv_bracket_block(cap, &old_pos);
@@ -105238,7 +103295,7 @@
         for (n = cap->count1; n > 0; --n)
         {
             prev_pos = *pos;
-            pos = getnextmark(pos, cap->cmdchar == '[' ? BACKWARD : FORWARD, cap->nchar == '\'');
+            pos = getnextmark(pos, cap->cmdchar == '[' ?  (-1)  : FORWARD, cap->nchar == '\'');
             if (pos == NULL)
             {
                 break;
@@ -105251,9 +103308,9 @@
         nv_cursormark(cap, cap->nchar == '\'', pos);
     }
 
-    else if (cap->nchar >= K_RIGHTRELEASE && cap->nchar <= K_LEFTMOUSE)
+    else if (cap->nchar >=   (-((KS_EXTRA) + ((int)(KE_RIGHTRELEASE) << 8)))   && cap->nchar <=   (-((KS_EXTRA) + ((int)(KE_LEFTMOUSE) << 8)))  )
     {
-        (void)do_mouse(cap->oap, cap->nchar, (cap->cmdchar == ']') ? FORWARD : BACKWARD, cap->count1, PUT_FIXINDENT);
+        (void)do_mouse(cap->oap, cap->nchar, (cap->cmdchar == ']') ? FORWARD :  (-1) , cap->count1, PUT_FIXINDENT);
     }
 
     else
@@ -105450,7 +103507,7 @@
         had_ctrl_v = NUL;
     }
 
-    if (IS_SPECIAL(cap->nchar))
+    if ( ((cap->nchar) < 0) )
     {
         clearopbeep(cap->oap);
         return;
@@ -105466,11 +103523,11 @@
         {
             if (cap->nchar == CAR)
             {
-                cap->nchar = REPLACE_CR_NCHAR;
+                cap->nchar =  (-1) ;
             }
             else if (cap->nchar == NL)
             {
-                cap->nchar = REPLACE_NL_NCHAR;
+                cap->nchar =  (-2) ;
             }
         }
         nv_operator(cap);
@@ -105542,7 +103599,7 @@
 
             for (n = cap->count1; n > 0; --n)
             {
-                State = MODE_REPLACE;
+                State =  (REPLACE_FLAG | MODE_INSERT) ;
                 if (cap->nchar == Ctrl_E || cap->nchar == Ctrl_Y)
                 {
                     int c = ins_copychar(curwin->w_cursor.lnum + (cap->nchar == Ctrl_Y ? -1 : 1));
@@ -105741,7 +103798,7 @@
         return;
     }
 
-    if (LINEEMPTY(curwin->w_cursor.lnum) && vim_strchr(p_ww, '~') == NULL)
+    if ( (*ml_get(curwin->w_cursor.lnum) == NUL)  && vim_strchr(p_ww, '~') == NULL)
     {
         clearopbeep(cap->oap);
         return;
@@ -105832,7 +103889,7 @@
 {
     static char_u trans[] = "YyDdCcxdXdAAIIrr";
 
-    if (SAFE_isupper(cap->cmdchar))
+    if ( (isupper ((unsigned char)(cap->cmdchar))) )
     {
         if (VIsual_mode != Ctrl_V)
         {
@@ -105870,7 +103927,7 @@
     static void
 nv_abbrev(cmdarg_T *cap)
 {
-    if (cap->cmdchar == K_DEL || cap->cmdchar == K_KDEL)
+    if (cap->cmdchar ==   (-(('k') + ((int)('D') << 8)))   || cap->cmdchar ==   (-((KS_EXTRA) + ((int)(KE_KDEL) << 8)))  )
     {
         cap->cmdchar = 'x';
     }
@@ -106174,7 +104231,6 @@
     static void
 n_start_visual_mode(int c)
 {
-
     VIsual_mode = c;
     VIsual_active = TRUE;
     VIsual_reselect = TRUE;
@@ -106297,7 +104353,7 @@
 
     cap->oap->motion_type = MCHAR;
     cap->oap->inclusive = FALSE;
-    if (curwin->w_p_wrap && curwin->w_width != 0)
+    if (curwin-> w_onebuf_opt.wo_wrap  && curwin->w_width != 0)
     {
         int     width1 = curwin->w_width - curwin_col_off();
         int     width2 = width1 + curwin_col_off2();
@@ -106327,7 +104383,7 @@
     }
     if (cap->nchar == 'm')
     {
-        i += (curwin->w_width - curwin_col_off() + ((curwin->w_p_wrap && i > 0) ? curwin_col_off2() : 0)) / 2;
+        i += (curwin->w_width - curwin_col_off() + ((curwin-> w_onebuf_opt.wo_wrap  && i > 0) ? curwin_col_off2() : 0)) / 2;
     }
     coladvance((colnr_T)i);
     if (flag)
@@ -106336,7 +104392,7 @@
         {
             i = gchar_cursor();
         }
-        while (VIM_ISWHITE(i) && oneright() == OK)
+        while ( ((i) == ' ' || (i) == '\t')  && oneright() == OK)
             ;
         curwin->w_valid &= ~VALID_WCOL;
     }
@@ -106365,7 +104421,7 @@
         --curwin->w_cursor.col;
     }
 
-    while (curwin->w_cursor.col > 0 && VIM_ISWHITE(ptr[curwin->w_cursor.col]))
+    while (curwin->w_cursor.col > 0 &&  ((ptr[curwin->w_cursor.col]) == ' ' || (ptr[curwin->w_cursor.col]) == '\t') )
     {
         --curwin->w_cursor.col;
     }
@@ -106381,14 +104437,14 @@
     int         col_off = curwin_col_off();
     int         flag = FALSE;
 
-    if (cap->nchar == K_END || cap->nchar == K_KEND)
+    if (cap->nchar ==   (-(('@') + ((int)('7') << 8)))   || cap->nchar ==   (-(('K') + ((int)('4') << 8)))  )
     {
         flag = TRUE;
     }
 
     oap->motion_type = MCHAR;
     oap->inclusive = TRUE;
-    if (curwin->w_p_wrap && curwin->w_width != 0)
+    if (curwin-> w_onebuf_opt.wo_wrap  && curwin->w_width != 0)
     {
         curwin->w_curswant = MAXCOL;
         if (cap->count1 == 1)
@@ -106409,7 +104465,7 @@
             coladvance((colnr_T)i);
 
             update_curswant_force();
-            if (curwin->w_cursor.col > 0 && curwin->w_p_wrap)
+            if (curwin->w_cursor.col > 0 && curwin-> w_onebuf_opt.wo_wrap )
             {
                 if (curwin->w_virtcol > (colnr_T)i)
                 {
@@ -106451,7 +104507,7 @@
         {
             i = gchar_cursor();
         }
-        while (IS_WHITE_OR_NUL(i) && oneleft() == OK)
+        while ( ((i) == ' ' || (i) == '\t' || (i) == NUL)  && oneleft() == OK)
             ;
         curwin->w_valid &= ~VALID_WCOL;
     }
@@ -106524,8 +104580,9 @@
         VIsual_reselect = FALSE;
         break;
 
-    case K_BS:
+    case   (-(('k') + ((int)('b') << 8)))  :
         cap->nchar = Ctrl_H;
+    __attribute__((fallthrough));
     case 'h':
     case 'H':
     case Ctrl_H:
@@ -106543,8 +104600,8 @@
         break;
 
     case 'j':
-    case K_DOWN:
-        if (!curwin->w_p_wrap)
+    case   (-(('k') + ((int)('d') << 8)))  :
+        if (!curwin-> w_onebuf_opt.wo_wrap )
         {
             oap->motion_type = MLINE;
             i = cursor_down(cap->count1, oap->op_type == OP_NOP);
@@ -106560,15 +104617,15 @@
         break;
 
     case 'k':
-    case K_UP:
-        if (!curwin->w_p_wrap)
+    case   (-(('k') + ((int)('u') << 8)))  :
+        if (!curwin-> w_onebuf_opt.wo_wrap )
         {
             oap->motion_type = MLINE;
             i = cursor_up(cap->count1, oap->op_type == OP_NOP);
         }
         else
         {
-            i = nv_screengo(oap, BACKWARD, cap->count1);
+            i = nv_screengo(oap,  (-1) , cap->count1);
         }
         if (i == FAIL)
         {
@@ -106583,8 +104640,8 @@
     case '^':
     case '0':
     case 'm':
-    case K_HOME:
-    case K_KHOME:
+    case   (-(('k') + ((int)('h') << 8)))  :
+    case   (-(('K') + ((int)('1') << 8)))  :
         nv_g_home_m_cmd(cap);
         break;
 
@@ -106610,8 +104667,8 @@
         break;
 
     case '$':
-    case K_END:
-    case K_KEND:
+    case   (-(('@') + ((int)('7') << 8)))  :
+    case   (-(('K') + ((int)('4') << 8)))  :
         nv_g_dollar_cmd(cap);
         break;
 
@@ -106657,6 +104714,7 @@
 
     case '\'':
         cap->arg = TRUE;
+    __attribute__((fallthrough));
     case '`':
         nv_gomark(cap);
         break;
@@ -106692,6 +104750,7 @@
     case 'q':
     case 'w':
         oap->cursor_start = curwin->w_cursor;
+    __attribute__((fallthrough));
     case '~':
     case 'u':
     case 'U':
@@ -106705,27 +104764,27 @@
         nv_gd(oap, cap->nchar, (int)cap->count0);
         break;
 
-    case K_MIDDLEMOUSE:
-    case K_MIDDLEDRAG:
-    case K_MIDDLERELEASE:
-    case K_LEFTMOUSE:
-    case K_LEFTDRAG:
-    case K_LEFTRELEASE:
-    case K_MOUSEMOVE:
-    case K_RIGHTMOUSE:
-    case K_RIGHTDRAG:
-    case K_RIGHTRELEASE:
-    case K_X1MOUSE:
-    case K_X1DRAG:
-    case K_X1RELEASE:
-    case K_X2MOUSE:
-    case K_X2DRAG:
-    case K_X2RELEASE:
+    case   (-((KS_EXTRA) + ((int)(KE_MIDDLEMOUSE) << 8)))  :
+    case   (-((KS_EXTRA) + ((int)(KE_MIDDLEDRAG) << 8)))  :
+    case   (-((KS_EXTRA) + ((int)(KE_MIDDLERELEASE) << 8)))  :
+    case   (-((KS_EXTRA) + ((int)(KE_LEFTMOUSE) << 8)))  :
+    case   (-((KS_EXTRA) + ((int)(KE_LEFTDRAG) << 8)))  :
+    case   (-((KS_EXTRA) + ((int)(KE_LEFTRELEASE) << 8)))  :
+    case   (-((KS_EXTRA) + ((int)(KE_MOUSEMOVE) << 8)))  :
+    case   (-((KS_EXTRA) + ((int)(KE_RIGHTMOUSE) << 8)))  :
+    case   (-((KS_EXTRA) + ((int)(KE_RIGHTDRAG) << 8)))  :
+    case   (-((KS_EXTRA) + ((int)(KE_RIGHTRELEASE) << 8)))  :
+    case   (-((KS_EXTRA) + ((int)(KE_X1MOUSE) << 8)))  :
+    case   (-((KS_EXTRA) + ((int)(KE_X1DRAG) << 8)))  :
+    case   (-((KS_EXTRA) + ((int)(KE_X1RELEASE) << 8)))  :
+    case   (-((KS_EXTRA) + ((int)(KE_X2MOUSE) << 8)))  :
+    case   (-((KS_EXTRA) + ((int)(KE_X2DRAG) << 8)))  :
+    case   (-((KS_EXTRA) + ((int)(KE_X2RELEASE) << 8)))  :
         mod_mask = MOD_MASK_CTRL;
-        (void)do_mouse(oap, cap->nchar, BACKWARD, cap->count1, 0);
+        (void)do_mouse(oap, cap->nchar,  (-1) , cap->count1, 0);
         break;
 
-    case K_IGNORE:
+    case   (-((KS_EXTRA) + ((int)(KE_IGNORE) << 8)))  :
         break;
 
     case 'p':
@@ -106786,14 +104845,13 @@
     static void
 n_opencmd(cmdarg_T *cap)
 {
-
     if (checkclearopq(cap->oap))
     {
         return;
     }
 
-    curbuf->b_last_changedtick_i = CHANGEDTICK(curbuf);
-    if (u_save((linenr_T)(curwin->w_cursor.lnum - (cap->cmdchar == 'O' ? 1 : 0)), (linenr_T)(curwin->w_cursor.lnum + (cap->cmdchar == 'o' ? 1 : 0))) == OK && open_line(cap->cmdchar == 'O' ? BACKWARD : FORWARD, has_format_option(FO_OPEN_COMS) ? OPENLINE_DO_COM : 0, 0, NULL) == OK)
+    curbuf->b_last_changedtick_i =  ((curbuf)->b_ct_di.di_tv.vval.v_number) ;
+    if (u_save((linenr_T)(curwin->w_cursor.lnum - (cap->cmdchar == 'O' ? 1 : 0)), (linenr_T)(curwin->w_cursor.lnum + (cap->cmdchar == 'o' ? 1 : 0))) == OK && open_line(cap->cmdchar == 'O' ?  (-1)  : FORWARD, has_format_option(FO_OPEN_COMS) ? OPENLINE_DO_COM : 0, 0, NULL) == OK)
     {
         if (vim_strchr(p_cpo, CPO_HASH) != NULL)
         {
@@ -106826,7 +104884,7 @@
         ++no_mapping;
         ++allow_keys;
         reg = plain_vgetc();
-        LANGMAP_ADJUST(reg, TRUE);
+          ;
         --no_mapping;
         --allow_keys;
 
@@ -106987,7 +105045,7 @@
         n = gchar_cursor();
         if (n != NUL)
         {
-            if (VIM_ISWHITE(n))
+            if ( ((n) == ' ' || (n) == '\t') )
             {
                 if (cap->count1 == 1 && vim_strchr(p_cpo, CPO_CW) != NULL)
                 {
@@ -107019,7 +105077,7 @@
         n = fwd_word(cap->count1, cap->arg, cap->oap->op_type != OP_NOP);
     }
 
-    if (LT_POS(startpos, curwin->w_cursor))
+    if ( (((startpos).lnum != (curwin->w_cursor).lnum)               ? (startpos).lnum < (curwin->w_cursor).lnum                   : (startpos).col != (curwin->w_cursor).col                        ? (startpos).col < (curwin->w_cursor).col                     : (startpos).coladd < (curwin->w_cursor).coladd) )
     {
         adjust_cursor(cap->oap);
     }
@@ -107060,7 +105118,7 @@
     static void
 adjust_for_sel(cmdarg_T *cap)
 {
-    if (VIsual_active && cap->oap->inclusive && *p_sel == 'e' && gchar_cursor() != NUL && LT_POS(VIsual, curwin->w_cursor))
+    if (VIsual_active && cap->oap->inclusive && *p_sel == 'e' && gchar_cursor() != NUL &&  (((VIsual).lnum != (curwin->w_cursor).lnum)               ? (VIsual).lnum < (curwin->w_cursor).lnum                   : (VIsual).col != (curwin->w_cursor).col                        ? (VIsual).col < (curwin->w_cursor).col                     : (VIsual).coladd < (curwin->w_cursor).coladd) )
     {
         if (has_mbyte)
         {
@@ -107078,9 +105136,9 @@
     int
 unadjust_for_sel(void)
 {
-    if (*p_sel == 'e' && !EQUAL_POS(VIsual, curwin->w_cursor))
+    if (*p_sel == 'e' && ! (((VIsual).lnum == (curwin->w_cursor).lnum) && ((VIsual).col == (curwin->w_cursor).col) && ((VIsual).coladd == (curwin->w_cursor).coladd)) )
     {
-        return unadjust_for_sel_inner(LT_POS(VIsual, curwin->w_cursor) ? &curwin->w_cursor : &VIsual);
+        return unadjust_for_sel_inner( (((VIsual).lnum != (curwin->w_cursor).lnum)               ? (VIsual).lnum < (curwin->w_cursor).lnum                   : (VIsual).col != (curwin->w_cursor).col                        ? (VIsual).col < (curwin->w_cursor).col                     : (VIsual).coladd < (curwin->w_cursor).coladd)  ? &curwin->w_cursor : &VIsual);
     }
     return FALSE;
 }
@@ -107214,7 +105272,7 @@
 
                 if (out_redir)
                 {
-                    mch_errmsg(ms);
+                     fprintf(stderr, "%s", (ms)) ;
                 }
                 else
                 {
@@ -107246,14 +105304,14 @@
         }
         if (cmdwin_type != 0)
         {
-            cmdwin_result = K_IGNORE;
+            cmdwin_result =   (-((KS_EXTRA) + ((int)(KE_IGNORE) << 8)))  ;
             got_int = FALSE;
             return;
         }
     }
     else if (cmdwin_type != 0 && ex_normal_busy && typebuf_was_empty)
     {
-        cmdwin_result = K_IGNORE;
+        cmdwin_result =   (-((KS_EXTRA) + ((int)(KE_IGNORE) << 8)))  ;
         return;
     }
 
@@ -107290,14 +105348,14 @@
     }
     else
     {
-        curwin->w_cursor.col += (colnr_T)STRLEN(ml_get_cursor());
+        curwin->w_cursor.col += (colnr_T) strlen((char *)(ml_get_cursor())) ;
     }
 }
 
     static void
 nv_edit(cmdarg_T *cap)
 {
-    if (cap->cmdchar == K_INS || cap->cmdchar == K_KINS)
+    if (cap->cmdchar ==   (-(('k') + ((int)('I') << 8)))   || cap->cmdchar ==   (-((KS_EXTRA) + ((int)(KE_KINS) << 8)))  )
     {
         cap->cmdchar = 'i';
     }
@@ -107315,12 +105373,12 @@
     {
         emsg(_(e_cannot_make_changes_modifiable_is_off));
         clearop(cap->oap);
-        if (cap->cmdchar == K_PS)
+        if (cap->cmdchar ==   (-(('P') + ((int)('S') << 8)))  )
         {
             bracketed_paste(PASTE_INSERT, TRUE, NULL);
         }
     }
-    else if (cap->cmdchar == K_PS && VIsual_active)
+    else if (cap->cmdchar ==   (-(('P') + ((int)('S') << 8)))   && VIsual_active)
     {
         pos_T old_pos = curwin->w_cursor;
         pos_T old_visual = VIsual;
@@ -107339,7 +105397,7 @@
         cap->nchar = NUL;
         nv_operator(cap);
         do_pending_operator(cap, 0, FALSE);
-        cap->cmdchar = K_PS;
+        cap->cmdchar =   (-(('P') + ((int)('S') << 8)))  ;
 
         if (*ml_get_cursor() != NUL)
         {
@@ -107381,12 +105439,13 @@
                 }
                 break;
 
-            case K_PS:
+            case   (-(('P') + ((int)('S') << 8)))  :
                 if (curwin->w_cursor.col == 0)
                 {
                     break;
                 }
 
+            __attribute__((fallthrough));
             case 'a':
                 if (virtual_active() && (curwin->w_cursor.coladd > 0 || *ml_get_cursor() == NUL || *ml_get_cursor() == TAB))
                 {
@@ -107410,7 +105469,7 @@
 
         invoke_edit(cap, FALSE, cap->cmdchar, FALSE);
     }
-    else if (cap->cmdchar == K_PS)
+    else if (cap->cmdchar ==   (-(('P') + ((int)('S') << 8)))  )
     {
         bracketed_paste(PASTE_INSERT, TRUE, NULL);
     }
@@ -107434,7 +105493,7 @@
 
     if (cap->cmdchar != 'O' && cap->cmdchar != 'o')
     {
-        curbuf->b_last_changedtick_i = CHANGEDTICK(curbuf);
+        curbuf->b_last_changedtick_i =  ((curbuf)->b_ct_di.di_tv.vval.v_number) ;
     }
     if (edit(cmd, startln, cap->count1))
     {
@@ -107541,7 +105600,7 @@
             return;
         }
         stuffcharReadbuff(cap->nchar);
-        stuffcharReadbuff(K_CMDWIN);
+        stuffcharReadbuff(  (-((KS_EXTRA) + ((int)(KE_CMDWIN) << 8)))  );
     }
     else
     {
@@ -107573,7 +105632,7 @@
     static void
 nv_halfpage(cmdarg_T *cap)
 {
-    int dir = cap->cmdchar == Ctrl_D ? FORWARD : BACKWARD;
+    int dir = cap->cmdchar == Ctrl_D ? FORWARD :  (-1) ;
     if (!checkclearop(cap->oap))
     {
         pagescroll(dir, cap->count0, TRUE);
@@ -107640,12 +105699,12 @@
     if (fix_indent)
     {
         dir = (cap->cmdchar == ']' && cap->nchar == 'p')
-            ? FORWARD : BACKWARD;
+            ? FORWARD :  (-1) ;
         flags |= PUT_FIXINDENT;
     }
     else
     {
-        dir = (cap->cmdchar == 'P' || ((cap->cmdchar == 'g' || cap->cmdchar == 'z') && cap->nchar == 'P')) ? BACKWARD : FORWARD;
+        dir = (cap->cmdchar == 'P' || ((cap->cmdchar == 'g' || cap->cmdchar == 'z') && cap->nchar == 'P')) ?  (-1)  : FORWARD;
     }
     prep_redo_cmd(cap);
     if (cap->cmdchar == 'g')
@@ -107662,7 +105721,7 @@
         was_visual = TRUE;
         regname = cap->oap->regname;
         keep_registers = cap->cmdchar == 'P';
-        if (regname == 0 || regname == '"' || VIM_ISDIGIT(regname) || regname == '-')
+        if (regname == 0 || regname == '"' ||  ((unsigned)(regname) - '0' < 10)  || regname == '-')
         {
             reg1 = get_register(regname, TRUE);
         }
@@ -107696,7 +105755,7 @@
         {
             flags |= PUT_LINE_FORWARD;
         }
-        dir = BACKWARD;
+        dir =  (-1) ;
         if ((VIsual_mode != 'V' && curwin->w_cursor.col < curbuf->b_op_start.col) || (VIsual_mode == 'V' && curwin->w_cursor.lnum < curbuf->b_op_start.lnum))
         {
             dir = FORWARD;
@@ -107761,10 +105820,9 @@
 static void     mb_adjust_opend(oparg_T *oap);
 static int      do_addsub(int op_type, pos_T *pos, int length, linenr_T Prenum1);
 static void     pbyte(pos_T lp, int c);
-#define PBYTE(lp, c) pbyte(lp, c)
 
-#define OPF_LINES  1
-#define OPF_CHANGE 2
+enum { OPF_LINES = 1 };
+enum { OPF_CHANGE = 2 };
 
 static const char opchars[][3] =
 {
@@ -107831,7 +105889,7 @@
         {
             break;
         }
-        if (i == (int)ARRAY_LENGTH(opchars) - 1)
+        if (i == (int) (sizeof(opchars) / sizeof((opchars)[0]))  - 1)
         {
             internal_error("get_op_type()");
             break;
@@ -107928,7 +105986,7 @@
         }
         msg_line_single = NGETTEXT("%ld line %sed %d time", "%ld line %sed %d times", amount);
         msg_line_plural = NGETTEXT("%ld lines %sed %d time", "%ld lines %sed %d times", amount);
-        vim_snprintf((char *)IObuff, IOSIZE, NGETTEXT(msg_line_single, msg_line_plural, oap->line_count), oap->line_count, op, amount);
+        vim_snprintf((char *)IObuff,  (1024+1) , NGETTEXT(msg_line_single, msg_line_plural, oap->line_count), oap->line_count, op, amount);
         msg_attr_keep((char *)IObuff, 0, TRUE);
     }
 
@@ -108082,7 +106140,7 @@
         }
 
         init_chartabsize_arg(&cts, curwin, curwin->w_cursor.lnum, bd.start_vcol, bd.textstart, bd.textstart);
-        for ( ; VIM_ISWHITE(*cts.cts_ptr); )
+        for ( ;  ((*cts.cts_ptr) == ' ' || (*cts.cts_ptr) == '\t') ; )
         {
             incr = lbr_chartabsize_adv(&cts);
             total += incr;
@@ -108112,12 +106170,12 @@
         {
             return;
         }
-        mch_memmove(newp, oldp, (size_t)bd.textcol);
+         memmove((char *)(newp), (char *)(oldp), (size_t)bd.textcol) ;
         newlen = bd.textcol;
-        vim_memset(newp + newlen, TAB, (size_t)tabs);
+         memset((newp + newlen), (TAB), ((size_t)tabs)) ;
         newlen += tabs;
-        vim_memset(newp + newlen, ' ', (size_t)spaces);
-        STRCPY(newp + newlen + spaces, bd.textstart);
+         memset((newp + newlen), (' '), ((size_t)spaces)) ;
+         strcpy((char *)(newp + newlen + spaces), (char *)(bd.textstart)) ;
     }
     else
     {
@@ -108134,13 +106192,13 @@
 
         if (bd.startspaces)
         {
-            MB_PTR_ADV(non_white);
+             non_white += (*mb_ptr2len)(non_white) ;
         }
 
         non_white_col = bd.start_vcol;
 
         init_chartabsize_arg(&cts, curwin, curwin->w_cursor.lnum, non_white_col, bd.textstart, non_white);
-        while (VIM_ISWHITE(*cts.cts_ptr))
+        while ( ((*cts.cts_ptr) == ' ' || (*cts.cts_ptr) == '\t') )
         {
             incr = lbr_chartabsize_adv(&cts);
             cts.cts_vcol += incr;
@@ -108170,7 +106228,7 @@
                 break;
             }
             cts.cts_vcol += incr;
-            MB_PTR_ADV(cts.cts_ptr);
+             cts.cts_ptr += (*mb_ptr2len)(cts.cts_ptr) ;
         }
         verbatim_copy_width = cts.cts_vcol;
         verbatim_copy_end = cts.cts_ptr;
@@ -108186,10 +106244,10 @@
         {
             return;
         }
-        mch_memmove(newp, oldp, fixedlen);
+         memmove((char *)(newp), (char *)(oldp), fixedlen) ;
         newlen = fixedlen;
-        vim_memset(newp + newlen, ' ', (size_t)fill);
-        STRCPY(newp + newlen + fill, non_white);
+         memset((newp + newlen), (' '), ((size_t)fill)) ;
+         strcpy((char *)(newp + newlen + fill), (char *)(non_white)) ;
     }
     ml_replace(curwin->w_cursor.lnum, newp, FALSE);
 
@@ -108280,20 +106338,20 @@
             continue;
         }
 
-        mch_memmove(newp, oldp, (size_t)offset);
+         memmove((char *)(newp), (char *)(oldp), (size_t)offset) ;
         oldp += offset;
 
-        vim_memset(newp + offset, ' ', (size_t)spaces);
+         memset((newp + offset), (' '), ((size_t)spaces)) ;
         startcol = offset + spaces;
 
-        mch_memmove(newp + startcol, s, slen);
+         memmove((char *)(newp + startcol), (char *)(s), slen) ;
         offset += (int)slen;
 
         if (spaces > 0 && !bdp->is_short)
         {
             if (*oldp == TAB)
             {
-                vim_memset(newp + offset + spaces, ' ', (size_t)(ts_val - spaces));
+                 memset((newp + offset + spaces), (' '), ((size_t)(ts_val - spaces))) ;
                 oldp++;
                 count++;
             }
@@ -108307,7 +106365,7 @@
         {
             offset += count;
         }
-        STRCPY(newp + offset, oldp);
+         strcpy((char *)(newp + offset), (char *)(oldp)) ;
 
         ml_replace(lnum, newp, FALSE);
 
@@ -108482,9 +106540,9 @@
             {
                 continue;
             }
-            mch_memmove(newp, oldp, (size_t)bd.textcol);
-            vim_memset(newp + bd.textcol, ' ', (size_t)(bd.startspaces + bd.endspaces));
-            STRCPY(newp + bd.textcol + bd.startspaces + bd.endspaces, oldp + bd.textcol + bd.textlen);
+             memmove((char *)(newp), (char *)(oldp), (size_t)bd.textcol) ;
+             memset((newp + bd.textcol), (' '), ((size_t)(bd.startspaces + bd.endspaces))) ;
+             strcpy((char *)(newp + bd.textcol + bd.startspaces + bd.endspaces), (char *)(oldp + bd.textcol + bd.textlen)) ;
             ml_replace(lnum, newp, FALSE);
 
         }
@@ -108682,7 +106740,7 @@
 {
     int n = State;
 
-    State = MODE_REPLACE;
+    State =  (REPLACE_FLAG | MODE_INSERT) ;
     ins_char(c);
     State = n;
     dec_cursor();
@@ -108707,12 +106765,12 @@
         return OK;
     }
 
-    if (c == REPLACE_CR_NCHAR)
+    if (c ==  (-1) )
     {
         had_ctrl_v_cr = TRUE;
         c = CAR;
     }
-    else if (c == REPLACE_NL_NCHAR)
+    else if (c ==  (-2) )
     {
         had_ctrl_v_cr = TRUE;
         c = NL;
@@ -108782,10 +106840,10 @@
             {
                 continue;
             }
-            vim_memset(newp, NUL, (size_t)(oldlen + 1 + n));
-            mch_memmove(newp, oldp, (size_t)bd.textcol);
+             memset((newp), (NUL), ((size_t)(oldlen + 1 + n))) ;
+             memmove((char *)(newp), (char *)(oldp), (size_t)bd.textcol) ;
             newlen = bd.textcol;
-            vim_memset(newp + newlen, ' ', (size_t)bd.startspaces);
+             memset((newp + newlen), (' '), ((size_t)bd.startspaces)) ;
             newlen += bd.startspaces;
             if (had_ctrl_v_cr || (c != '\r' && c != '\n'))
             {
@@ -108798,13 +106856,13 @@
                 }
                 else
                 {
-                    vim_memset(newp + newlen, c, (size_t)numc);
+                     memset((newp + newlen), (c), ((size_t)numc)) ;
                     newlen += numc;
                 }
                 if (!bd.is_short)
                 {
-                    vim_memset(newp + newlen, ' ', (size_t)bd.endspaces);
-                    STRCPY(newp + newlen + bd.endspaces, oldp + bd.textcol + bd.textlen);
+                     memset((newp + newlen), (' '), ((size_t)bd.endspaces)) ;
+                     strcpy((char *)(newp + newlen + bd.endspaces), (char *)(oldp + bd.textcol + bd.textlen)) ;
                 }
             }
             else
@@ -108812,7 +106870,7 @@
                 after_p = alloc(oldlen + 1 + n - newlen);
                 if (after_p != NULL)
                 {
-                    STRCPY(after_p, oldp + bd.textcol + bd.textlen);
+                     strcpy((char *)(after_p), (char *)(oldp + bd.textcol + bd.textlen)) ;
                 }
             }
 
@@ -108843,7 +106901,7 @@
             dec(&(oap->end));
         }
 
-        while (LTOREQ_POS(curwin->w_cursor, oap->end))
+        while ( ( (((curwin->w_cursor).lnum != (oap->end).lnum)               ? (curwin->w_cursor).lnum < (oap->end).lnum                   : (curwin->w_cursor).col != (oap->end).col                        ? (curwin->w_cursor).col < (oap->end).col                     : (curwin->w_cursor).coladd < (oap->end).coladd)  ||  (((curwin->w_cursor).lnum == (oap->end).lnum) && ((curwin->w_cursor).col == (oap->end).col) && ((curwin->w_cursor).coladd == (oap->end).coladd)) ) )
         {
             int done = FALSE;
 
@@ -108880,7 +106938,7 @@
                     }
                     if (gchar_cursor() != NUL)
                     {
-                        PBYTE(curwin->w_cursor, c);
+                         pbyte(curwin->w_cursor, c) ;
                         done = TRUE;
                     }
                 }
@@ -108904,7 +106962,7 @@
                     }
                     else
                     {
-                        PBYTE(curwin->w_cursor, c);
+                         pbyte(curwin->w_cursor, c) ;
                     }
                     if (inc(&curwin->w_cursor) == -1)
                     {
@@ -108991,7 +107049,7 @@
             for (;;)
             {
                 did_change |= swapchars(oap->op_type, &pos, pos.lnum == oap->end.lnum ? oap->end.col + 1 : ml_get_pos_len(&pos));
-                if (LTOREQ_POS(oap->end, pos) || inc(&pos) == -1)
+                if ( ( (((oap->end).lnum != (pos).lnum)               ? (oap->end).lnum < (pos).lnum                   : (oap->end).col != (pos).col                        ? (oap->end).col < (pos).col                     : (oap->end).coladd < (pos).coladd)  ||  (((oap->end).lnum == (pos).lnum) && ((oap->end).col == (pos).col) && ((oap->end).coladd == (pos).coladd)) )  || inc(&pos) == -1)
                 {
                     break;
                 }
@@ -109059,7 +107117,7 @@
         return FALSE;
     }
 
-    if ((op_type == OP_UPPER || op_type == OP_NOP || op_type == OP_TILDE) && c == 0xdf && (enc_latin1like || STRCMP(p_enc, "iso-8859-2") == 0))
+    if ((op_type == OP_UPPER || op_type == OP_NOP || op_type == OP_TILDE) && c == 0xdf && (enc_latin1like ||  strcmp((char *)(p_enc), (char *)("iso-8859-2"))  == 0))
     {
         pos_T   sp = curwin->w_cursor;
 
@@ -109075,26 +107133,26 @@
         return FALSE;
     }
     nc = c;
-    if (MB_ISLOWER(c))
+    if ( vim_islower(c) )
     {
         if (op_type == OP_ROT13)
         {
-            nc = ROT13(c, 'a');
+            nc =  (((((c) - ('a')) + 13) % 26) + ('a')) ;
         }
         else if (op_type != OP_LOWER)
         {
-            nc = MB_TOUPPER(c);
+            nc =  vim_toupper(c) ;
         }
     }
-    else if (MB_ISUPPER(c))
+    else if ( vim_isupper(c) )
     {
         if (op_type == OP_ROT13)
         {
-            nc = ROT13(c, 'A');
+            nc =  (((((c) - ('A')) + 13) % 26) + ('A')) ;
         }
         else if (op_type != OP_UPPER)
         {
-            nc = MB_TOLOWER(c);
+            nc =  vim_tolower(c) ;
         }
     }
     if (nc != c)
@@ -109110,7 +107168,7 @@
         }
         else
         {
-            PBYTE(*pos, nc);
+             pbyte(*pos, nc) ;
         }
         return TRUE;
     }
@@ -109139,20 +107197,20 @@
     {
         if (curwin->w_cursor.coladd > 0)
         {
-            int         old_ve_flags = curwin->w_ve_flags;
+            int         old_ve_flags = curwin-> w_onebuf_opt.wo_ve_flags ;
 
             if (u_save_cursor() == FAIL)
             {
                 return;
             }
 
-            curwin->w_ve_flags = VE_ALL;
+            curwin-> w_onebuf_opt.wo_ve_flags  = VE_ALL;
             coladvance_force(oap->op_type == OP_APPEND ? oap->end_vcol + 1 : getviscol());
             if (oap->op_type == OP_APPEND)
             {
                 --curwin->w_cursor.col;
             }
-            curwin->w_ve_flags = old_ve_flags;
+            curwin-> w_onebuf_opt.wo_ve_flags  = old_ve_flags;
         }
         block_prep(oap, &bd, oap->start.lnum, TRUE);
         ind_pre_col = (colnr_T)getwhitecols_curline();
@@ -109191,7 +107249,7 @@
             curwin->w_cursor = oap->end;
             check_cursor_col();
 
-            if (!LINEEMPTY(curwin->w_cursor.lnum) && oap->start_vcol != oap->end_vcol)
+            if (! (*ml_get(curwin->w_cursor.lnum) == NUL)  && oap->start_vcol != oap->end_vcol)
             {
                 inc_cursor();
             }
@@ -109202,7 +107260,7 @@
     start_insert = curwin->w_cursor;
     (void)edit(NUL, FALSE, (linenr_T)count1);
 
-    if (t1.lnum == curbuf->b_op_start_orig.lnum && LT_POS(curbuf->b_op_start_orig, t1))
+    if (t1.lnum == curbuf->b_op_start_orig.lnum &&  (((curbuf->b_op_start_orig).lnum != (t1).lnum)               ? (curbuf->b_op_start_orig).lnum < (t1).lnum                   : (curbuf->b_op_start_orig).col != (t1).col                        ? (curbuf->b_op_start_orig).col < (t1).col                     : (curbuf->b_op_start_orig).coladd < (t1).coladd) )
     {
         oap->start = curbuf->b_op_start_orig;
     }
@@ -109358,7 +107416,7 @@
         return FALSE;
     }
 
-    if ((l > curwin->w_cursor.col) && !LINEEMPTY(curwin->w_cursor.lnum) && !virtual_op)
+    if ((l > curwin->w_cursor.col) && ! (*ml_get(curwin->w_cursor.lnum) == NUL)  && !virtual_op)
     {
         inc_cursor();
     }
@@ -109429,12 +107487,12 @@
                         {
                             continue;
                         }
-                        mch_memmove(newp, oldp, (size_t)bd.textcol);
+                         memmove((char *)(newp), (char *)(oldp), (size_t)bd.textcol) ;
                         newlen = bd.textcol;
-                        vim_memset(newp + newlen, ' ', (size_t)vpos.coladd);
+                         memset((newp + newlen), (' '), ((size_t)vpos.coladd)) ;
                         newlen += vpos.coladd;
-                        mch_memmove(newp + newlen, ins_text, ins_len);
-                        STRCPY(newp + newlen + ins_len, oldp + bd.textcol);
+                         memmove((char *)(newp + newlen), (char *)(ins_text), ins_len) ;
+                         strcpy((char *)(newp + newlen + ins_len), (char *)(oldp + bd.textcol)) ;
                         ml_replace(linenr, newp, FALSE);
                     }
                 }
@@ -109527,7 +107585,7 @@
 }
 
     int
-do_join(long    count, int     insert_space, int     save_undo, int     use_formatoptions UNUSED, int     setmark)
+do_join(long    count, int     insert_space, int     save_undo, int     use_formatoptions  __attribute__((unused)) , int     setmark)
 {
     char_u      *curr = NULL;
     char_u      *curr_start = NULL;
@@ -109573,13 +107631,12 @@
         if (t == 0 && setmark && (cmdmod.cmod_flags & CMOD_LOCKMARKS) == 0)
         {
             curwin->w_buffer->b_op_start.lnum = curwin->w_cursor.lnum;
-            curwin->w_buffer->b_op_start.col  = (colnr_T)STRLEN(curr);
+            curwin->w_buffer->b_op_start.col  = (colnr_T) strlen((char *)(curr)) ;
         }
         if (remove_comments)
         {
             if (t > 0 && prev_was_comment)
             {
-
                 char_u *new_curr = skip_comment(curr, TRUE, insert_space, &prev_was_comment);
                 comments[t] = (int)(new_curr - curr);
                 curr = new_curr;
@@ -109609,7 +107666,7 @@
                 }
             }
         }
-        currsize = (int)STRLEN(curr);
+        currsize = (int) strlen((char *)(curr)) ;
         sumsize += currsize + spaces[t];
         endcurr1 = endcurr2 = NUL;
         if (insert_space && currsize > 0)
@@ -109617,11 +107674,11 @@
             if (has_mbyte)
             {
                 cend = curr + currsize;
-                MB_PTR_BACK(curr, cend);
+                 cend -= has_mbyte ? ((*mb_head_off)(curr, (cend) - 1) + 1) : 1 ;
                 endcurr1 = (*mb_ptr2char)(cend);
                 if (cend > curr)
                 {
-                    MB_PTR_BACK(curr, cend);
+                     cend -= has_mbyte ? ((*mb_head_off)(curr, (cend) - 1) + 1) : 1 ;
                     endcurr2 = (*mb_ptr2char)(cend);
                 }
             }
@@ -109659,12 +107716,12 @@
         int spaces_removed;
 
         cend -= currsize;
-        mch_memmove(cend, curr, (size_t)currsize);
+         memmove((char *)(cend), (char *)(curr), (size_t)currsize) ;
 
         if (spaces[t] > 0)
         {
             cend -= spaces[t];
-            vim_memset(cend, ' ', (size_t)(spaces[t]));
+             memset((cend), (' '), ((size_t)(spaces[t]))) ;
         }
 
         spaces_removed = (curr - curr_start) - spaces[t];
@@ -109683,7 +107740,7 @@
         {
             curr = skipwhite(curr);
         }
-        currsize = (int)STRLEN(curr);
+        currsize = (int) strlen((char *)(curr)) ;
     }
 
     ml_replace_len(curwin->w_cursor.lnum, newp, (colnr_T)newp_len, TRUE, FALSE);
@@ -109746,7 +107803,7 @@
     {
         incr = lbr_chartabsize(&cts);
         cts.cts_vcol += incr;
-        if (VIM_ISWHITE(*cts.cts_ptr))
+        if ( ((*cts.cts_ptr) == ' ' || (*cts.cts_ptr) == '\t') )
         {
             bdp->pre_whitesp += incr;
             bdp->pre_whitesp_c++;
@@ -109757,7 +107814,7 @@
             bdp->pre_whitesp_c = 0;
         }
         prev_pstart = cts.cts_ptr;
-        MB_PTR_ADV(cts.cts_ptr);
+         cts.cts_ptr += (*mb_ptr2len)(cts.cts_ptr) ;
     }
     bdp->start_vcol = cts.cts_vcol;
     pstart = cts.cts_ptr;
@@ -110119,7 +108176,6 @@
 
         if (       do_bin && do_hex && ! ((col > 0 && (ptr[col] == 'X' || ptr[col] == 'x') && ptr[col - 1] == '0' && (!has_mbyte || !(*mb_head_off)(ptr, ptr + col - 1)) && vim_isxdigit(ptr[col + 1]))))
         {
-
             col = pos->col;
 
             while (col > 0 && vim_isdigit(ptr[col]))
@@ -110144,12 +108200,12 @@
         {
             col = pos->col;
 
-            while (ptr[col] != NUL && !vim_isdigit(ptr[col]) && !(do_alpha && ASCII_ISALPHA(ptr[col])))
+            while (ptr[col] != NUL && !vim_isdigit(ptr[col]) && !(do_alpha &&  ( ((unsigned)(ptr[col]) - 'A' < 26)  ||  ((unsigned)(ptr[col]) - 'a' < 26) ) ))
             {
                 col += mb_ptr2len(ptr + col);
             }
 
-            while (col > 0 && vim_isdigit(ptr[col - 1]) && !(do_alpha && ASCII_ISALPHA(ptr[col])))
+            while (col > 0 && vim_isdigit(ptr[col - 1]) && !(do_alpha &&  ( ((unsigned)(ptr[col]) - 'A' < 26)  ||  ((unsigned)(ptr[col]) - 'a' < 26) ) ))
             {
                 --col;
                 if (has_mbyte)
@@ -110162,7 +108218,7 @@
 
     if (visual)
     {
-        while (ptr[col] != NUL && length > 0 && !vim_isdigit(ptr[col]) && !(do_alpha && ASCII_ISALPHA(ptr[col])))
+        while (ptr[col] != NUL && length > 0 && !vim_isdigit(ptr[col]) && !(do_alpha &&  ( ((unsigned)(ptr[col]) - 'A' < 26)  ||  ((unsigned)(ptr[col]) - 'a' < 26) ) ))
         {
             int mb_len = mb_ptr2len(ptr + col);
 
@@ -110177,7 +108233,7 @@
 
         if (col > pos->col && ptr[col - 1] == '-' && (!has_mbyte || !(*mb_head_off)(ptr, ptr + col - 1)) && !do_unsigned)
         {
-            if (do_blank && col >= 2 && !VIM_ISWHITE(ptr[col - 2]))
+            if (do_blank && col >= 2 && ! ((ptr[col - 2]) == ' ' || (ptr[col - 2]) == '\t') )
             {
                 blank_unsigned = TRUE;
             }
@@ -110190,19 +108246,19 @@
     }
 
     firstdigit = ptr[col];
-    if (!VIM_ISDIGIT(firstdigit) && !(do_alpha && ASCII_ISALPHA(firstdigit)))
+    if (! ((unsigned)(firstdigit) - '0' < 10)  && !(do_alpha &&  ( ((unsigned)(firstdigit) - 'A' < 26)  ||  ((unsigned)(firstdigit) - 'a' < 26) ) ))
     {
         beep_flush();
         goto theend;
     }
 
-    if (do_alpha && ASCII_ISALPHA(firstdigit))
+    if (do_alpha &&  ( ((unsigned)(firstdigit) - 'A' < 26)  ||  ((unsigned)(firstdigit) - 'a' < 26) ) )
     {
         if (op_type == OP_NR_SUB)
         {
-            if (CharOrd(firstdigit) < Prenum1)
+            if ( ((firstdigit) < 'a' ? (firstdigit) - 'A' : (firstdigit) - 'a')  < Prenum1)
             {
-                if (SAFE_isupper(firstdigit))
+                if ( (isupper ((unsigned char)(firstdigit))) )
                 {
                     firstdigit = 'A';
                 }
@@ -110218,9 +108274,9 @@
         }
         else
         {
-            if (26 - CharOrd(firstdigit) - 1 < Prenum1)
+            if (26 -  ((firstdigit) < 'a' ? (firstdigit) - 'A' : (firstdigit) - 'a')  - 1 < Prenum1)
             {
-                if (SAFE_isupper(firstdigit))
+                if ( (isupper ((unsigned char)(firstdigit))) )
                 {
                     firstdigit = 'Z';
                 }
@@ -110256,7 +108312,7 @@
 
         if (col > 0 && ptr[col - 1] == '-' && (!has_mbyte || !(*mb_head_off)(ptr, ptr + col - 1)) && !visual && !do_unsigned)
         {
-            if (do_blank && col >= 2 && !VIM_ISWHITE(ptr[col - 2]))
+            if (do_blank && col >= 2 && ! ((ptr[col - 2]) == ' ' || (ptr[col - 2]) == '\t') )
             {
                 blank_unsigned = TRUE;
             }
@@ -110363,9 +108419,9 @@
         save_pos = curwin->w_cursor;
         for (i = 0; i < todel; ++i)
         {
-            if (c < 0x100 && SAFE_isalpha(c))
+            if (c < 0x100 &&  (isalpha ((unsigned char)(c))) )
             {
-                if (SAFE_isupper(c))
+                if ( (isupper ((unsigned char)(c))) )
                 {
                     hexupper = TRUE;
                 }
@@ -110448,7 +108504,7 @@
         *ptr = NUL;
         buf1len = (int)(ptr - buf1);
 
-        STRCPY(buf1 + buf1len, buf2);
+         strcpy((char *)(buf1 + buf1len), (char *)(buf2)) ;
         buf1len += buf2len;
 
         save_pos = curwin->w_cursor;
@@ -110510,7 +108566,7 @@
     void
 clear_oparg(oparg_T *oap)
 {
-    CLEAR_POINTER(oap);
+      memset(((oap)), (0), (sizeof(*(oap))))  ;
 }
 
     static varnumber_T
@@ -110580,7 +108636,7 @@
     {
         if (dict == NULL)
         {
-            msg(_(no_lines_msg));
+            msg(_((char *)no_lines_msg));
             return;
         }
     }
@@ -110597,7 +108653,7 @@
 
         if (VIsual_active)
         {
-            if (LT_POS(VIsual, curwin->w_cursor))
+            if ( (((VIsual).lnum != (curwin->w_cursor).lnum)               ? (VIsual).lnum < (curwin->w_cursor).lnum                   : (VIsual).col != (curwin->w_cursor).col                        ? (VIsual).col < (curwin->w_cursor).col                     : (VIsual).coladd < (curwin->w_cursor).coladd) )
             {
                 min_pos = VIsual;
                 max_pos = curwin->w_cursor;
@@ -110677,7 +108733,7 @@
                 if (s != NULL)
                 {
                     byte_count_cursor += line_count_info(s, &word_count_cursor, &char_count_cursor, len, eol_size);
-                    if (lnum == curbuf->b_ml.ml_line_count && !curbuf->b_p_eol && (curbuf->b_p_bin || !curbuf->b_p_fixeol) && (long)STRLEN(s) < len)
+                    if (lnum == curbuf->b_ml.ml_line_count && !curbuf->b_p_eol && (curbuf->b_p_bin || !curbuf->b_p_fixeol) && (long) strlen((char *)(s))  < len)
                     {
                         byte_count_cursor -= eol_size;
                     }
@@ -110717,11 +108773,11 @@
 
                 if (char_count_cursor == byte_count_cursor && char_count == byte_count)
                 {
-                    vim_snprintf((char *)IObuff, IOSIZE, _("Selected %s%ld of %ld Lines; %lld of %lld Words; %lld of %lld Bytes"), buf1, line_count_selected, (long)curbuf->b_ml.ml_line_count, word_count_cursor, word_count, byte_count_cursor, byte_count);
+                    vim_snprintf((char *)IObuff,  (1024+1) , _("Selected %s%ld of %ld Lines; %lld of %lld Words; %lld of %lld Bytes"), buf1, line_count_selected, (long)curbuf->b_ml.ml_line_count, word_count_cursor, word_count, byte_count_cursor, byte_count);
                 }
                 else
                 {
-                    vim_snprintf((char *)IObuff, IOSIZE, _("Selected %s%ld of %ld Lines; %lld of %lld Words; %lld of %lld Chars; %lld of %lld Bytes"), buf1, line_count_selected, (long)curbuf->b_ml.ml_line_count, word_count_cursor, word_count, char_count_cursor, char_count, byte_count_cursor, byte_count);
+                    vim_snprintf((char *)IObuff,  (1024+1) , _("Selected %s%ld of %ld Lines; %lld of %lld Words; %lld of %lld Chars; %lld of %lld Bytes"), buf1, line_count_selected, (long)curbuf->b_ml.ml_line_count, word_count_cursor, word_count, char_count_cursor, char_count, byte_count_cursor, byte_count);
                 }
             }
             else
@@ -110733,11 +108789,11 @@
 
                 if (char_count_cursor == byte_count_cursor && char_count == byte_count)
                 {
-                    vim_snprintf((char *)IObuff, IOSIZE, _("Col %s of %s; Line %ld of %ld; Word %lld of %lld; Byte %lld of %lld"), (char *)buf1, (char *)buf2, (long)curwin->w_cursor.lnum, (long)curbuf->b_ml.ml_line_count, word_count_cursor, word_count, byte_count_cursor, byte_count);
+                    vim_snprintf((char *)IObuff,  (1024+1) , _("Col %s of %s; Line %ld of %ld; Word %lld of %lld; Byte %lld of %lld"), (char *)buf1, (char *)buf2, (long)curwin->w_cursor.lnum, (long)curbuf->b_ml.ml_line_count, word_count_cursor, word_count, byte_count_cursor, byte_count);
                 }
                 else
                 {
-                    vim_snprintf((char *)IObuff, IOSIZE, _("Col %s of %s; Line %ld of %ld; Word %lld of %lld; Char %lld of %lld; Byte %lld of %lld"), (char *)buf1, (char *)buf2, (long)curwin->w_cursor.lnum, (long)curbuf->b_ml.ml_line_count, word_count_cursor, word_count, char_count_cursor, char_count, byte_count_cursor, byte_count);
+                    vim_snprintf((char *)IObuff,  (1024+1) , _("Col %s of %s; Line %ld of %ld; Word %lld of %lld; Char %lld of %lld; Byte %lld of %lld"), (char *)buf1, (char *)buf2, (long)curwin->w_cursor.lnum, (long)curbuf->b_ml.ml_line_count, word_count_cursor, word_count, char_count_cursor, char_count, byte_count_cursor, byte_count);
                 }
             }
         }
@@ -110745,9 +108801,9 @@
         bom_count = bomb_size();
         if (dict == NULL && bom_count > 0)
         {
-            size_t len = STRLEN(IObuff);
+            size_t len =  strlen((char *)(IObuff)) ;
 
-            vim_snprintf((char *)IObuff + len, IOSIZE - len, _("(+%lld for BOM)"), bom_count);
+            vim_snprintf((char *)IObuff + len,  (1024+1)  - len, _("(+%lld for BOM)"), bom_count);
         }
         if (dict == NULL)
         {
@@ -110838,7 +108894,7 @@
 static callback_T opfunc_cb;
 
     char *
-did_set_operatorfunc(optset_T *args UNUSED)
+did_set_operatorfunc(optset_T *args  __attribute__((unused)) )
 {
     if (option_set_callback_func(p_opfunc, &opfunc_cb) == FAIL)
     {
@@ -110849,7 +108905,7 @@
 }
 
     static void
-op_function(oparg_T *oap UNUSED)
+op_function(oparg_T *oap  __attribute__((unused)) )
 {
     emsg(_(e_eval_feature_not_available));
 }
@@ -110933,8 +108989,8 @@
 is_ex_cmdchar(cmdarg_T *cap)
 {
     return cap->cmdchar == ':'
-        || cap->cmdchar == K_COMMAND
-        || cap->cmdchar == K_SCRIPT_COMMAND;
+        || cap->cmdchar ==   (-((KS_EXTRA) + ((int)(KE_COMMAND) << 8)))  
+        || cap->cmdchar ==   (-((KS_EXTRA) + ((int)(KE_SCRIPT_COMMAND) << 8)))  ;
 }
 
     void
@@ -110993,7 +109049,7 @@
                 {
                     AppendToRedobuffLit(cap->searchbuf, -1);
                 }
-                AppendToRedobuff(NL_STR);
+                AppendToRedobuff( (char_u *)"\012" );
             }
             else if (is_ex_cmdchar(cap))
             {
@@ -111011,8 +109067,9 @@
                     {
                         AppendToRedobuffSpec(repeat_cmdline);
                     }
-                    AppendToRedobuff(NL_STR);
-                    VIM_CLEAR(repeat_cmdline);
+                    AppendToRedobuff( (char_u *)"\012" );
+                     vim_free(repeat_cmdline);
+                     (repeat_cmdline) = NULL;
                 }
             }
         }
@@ -111070,7 +109127,7 @@
 
             if (VIsual_select && VIsual_mode == 'V' && cap->oap->op_type != OP_DELETE)
             {
-                if (LT_POS(VIsual, curwin->w_cursor))
+                if ( (((VIsual).lnum != (curwin->w_cursor).lnum)               ? (VIsual).lnum < (curwin->w_cursor).lnum                   : (VIsual).col != (curwin->w_cursor).col                        ? (VIsual).col < (curwin->w_cursor).col                     : (VIsual).coladd < (curwin->w_cursor).coladd) )
                 {
                     VIsual.col = 0;
                     curwin->w_cursor.col = ml_get_len(curwin->w_cursor.lnum);
@@ -111095,7 +109152,7 @@
             }
         }
 
-        if (LT_POS(oap->start, curwin->w_cursor))
+        if ( (((oap->start).lnum != (curwin->w_cursor).lnum)               ? (oap->start).lnum < (curwin->w_cursor).lnum                   : (oap->start).col != (curwin->w_cursor).col                        ? (oap->start).col < (curwin->w_cursor).col                     : (oap->start).coladd < (curwin->w_cursor).coladd) )
         {
             oap->end = curwin->w_cursor;
             curwin->w_cursor = oap->start;
@@ -111158,11 +109215,11 @@
                     int extra_opchar = get_extra_op_char(oap->op_type);
                     int nchar = oap->op_type == OP_REPLACE ? cap->nchar : NUL;
 
-                    if (nchar == REPLACE_CR_NCHAR)
+                    if (nchar ==  (-1) )
                     {
                         nchar = CAR;
                     }
-                    else if (nchar == REPLACE_NL_NCHAR)
+                    else if (nchar ==  (-2) )
                     {
                         nchar = NL;
                     }
@@ -111237,7 +109294,7 @@
         }
         curwin->w_set_curswant = true;
 
-        oap->empty = (oap->motion_type == MCHAR && (!oap->inclusive || (oap->op_type == OP_YANK && gchar_pos(&oap->end) == NUL)) && EQUAL_POS(oap->start, oap->end) && !(virtual_op && oap->start.coladd != oap->end.coladd));
+        oap->empty = (oap->motion_type == MCHAR && (!oap->inclusive || (oap->op_type == OP_YANK && gchar_pos(&oap->end) == NUL)) &&  (((oap->start).lnum == (oap->end).lnum) && ((oap->start).col == (oap->end).col) && ((oap->start).coladd == (oap->end).coladd))  && !(virtual_op && oap->start.coladd != oap->end.coladd));
         empty_region_error = (oap->empty && vim_strchr(p_cpo, CPO_EMPTYREGION) != NULL);
 
         if (oap->is_VIsual && (oap->empty || !curbuf->b_p_ma))
@@ -111346,7 +109403,7 @@
                     restart_edit_save = 0;
                 }
                 restart_edit = 0;
-                curbuf->b_last_changedtick_i = CHANGEDTICK(curbuf);
+                curbuf->b_last_changedtick_i =  ((curbuf)->b_ct_di.di_tv.vval.v_number) ;
 
                 if (op_change(oap))
                 {
@@ -111369,6 +109426,7 @@
                 bangredo = TRUE;
             }
 
+        __attribute__((fallthrough));
         case OP_INDENT:
         case OP_COLON:
 
@@ -111440,7 +109498,7 @@
             {
                 restart_edit_save = restart_edit;
                 restart_edit = 0;
-                curbuf->b_last_changedtick_i = CHANGEDTICK(curbuf);
+                curbuf->b_last_changedtick_i =  ((curbuf)->b_ct_di.di_tv.vval.v_number) ;
 
                 op_insert(oap, cap->count1);
 
@@ -111523,102 +109581,9 @@
 
 // ---------------- begin optiondefs.h ----------------
 
-#define PV_BOTH 0x1000
-#define PV_WIN  0x2000
-#define PV_BUF  0x4000
-#define PV_MASK 0x0fff
-#define OPT_WIN(x)  (idopt_T)(PV_WIN + (int)(x))
-#define OPT_BUF(x)  (idopt_T)(PV_BUF + (int)(x))
-#define OPT_BOTH(x) (idopt_T)(PV_BOTH + (int)(x))
-
-#define PV_AC           OPT_BOTH(OPT_BUF(BV_AC))
-#define PV_AI           OPT_BUF(BV_AI)
-#define PV_AR           OPT_BOTH(OPT_BUF(BV_AR))
-#define PV_BKC          OPT_BOTH(OPT_BUF(BV_BKC))
-#define PV_BH           OPT_BUF(BV_BH)
-#define PV_BT           OPT_BUF(BV_BT)
-#define PV_BIN          OPT_BUF(BV_BIN)
-#define PV_BL           OPT_BUF(BV_BL)
-#define PV_BOMB         OPT_BUF(BV_BOMB)
-#define PV_CI           OPT_BUF(BV_CI)
-#define PV_CIN          OPT_BUF(BV_CIN)
-#define PV_CINK         OPT_BUF(BV_CINK)
-#define PV_CINO         OPT_BUF(BV_CINO)
-#define PV_CINSD        OPT_BUF(BV_CINSD)
-#define PV_CINW         OPT_BUF(BV_CINW)
-#define PV_CM           OPT_BOTH(OPT_BUF(BV_CM))
-#define PV_CMS          OPT_BUF(BV_CMS)
-#define PV_COM          OPT_BUF(BV_COM)
-#define PV_COT          OPT_BOTH(OPT_BUF(BV_COT))
-#define PV_CPT          OPT_BUF(BV_CPT)
-#define PV_DICT         OPT_BOTH(OPT_BUF(BV_DICT))
-#define PV_TSR          OPT_BOTH(OPT_BUF(BV_TSR))
-#define PV_FFU          OPT_BOTH(OPT_BUF(BV_FFU))
-#define PV_CSL          OPT_BUF(BV_CSL)
-#define PV_EOF          OPT_BUF(BV_EOF)
-#define PV_EOL          OPT_BUF(BV_EOL)
-#define PV_FIXEOL       OPT_BUF(BV_FIXEOL)
-#define PV_EP           OPT_BOTH(OPT_BUF(BV_EP))
-#define PV_ET           OPT_BUF(BV_ET)
-#define PV_FENC         OPT_BUF(BV_FENC)
-#define PV_FP           OPT_BOTH(OPT_BUF(BV_FP))
-#define PV_FF           OPT_BUF(BV_FF)
-#define PV_FLP          OPT_BUF(BV_FLP)
-#define PV_FO           OPT_BUF(BV_FO)
-# define PV_FS          OPT_BOTH(OPT_BUF(BV_FS))
-#define PV_FT           OPT_BUF(BV_FT)
-#define PV_IMI          OPT_BUF(BV_IMI)
-#define PV_IMS          OPT_BUF(BV_IMS)
-#define PV_INF          OPT_BUF(BV_INF)
-#define PV_ISK          OPT_BUF(BV_ISK)
-#define PV_KP           OPT_BOTH(OPT_BUF(BV_KP))
-#define PV_LISP         OPT_BUF(BV_LISP)
-#define PV_LOP          OPT_BUF(BV_LOP)
-#define PV_LW           OPT_BOTH(OPT_BUF(BV_LW))
-#define PV_MENC         OPT_BOTH(OPT_BUF(BV_MENC))
-#define PV_MA           OPT_BUF(BV_MA)
-#define PV_ML           OPT_BUF(BV_ML)
-#define PV_MOD          OPT_BUF(BV_MOD)
-#define PV_MPS          OPT_BUF(BV_MPS)
-#define PV_NF           OPT_BUF(BV_NF)
-#define PV_PATH         OPT_BOTH(OPT_BUF(BV_PATH))
-#define PV_PI           OPT_BUF(BV_PI)
-#define PV_QE           OPT_BUF(BV_QE)
-#define PV_RO           OPT_BUF(BV_RO)
-#define PV_SI           OPT_BUF(BV_SI)
-#define PV_SN           OPT_BUF(BV_SN)
-#define PV_STS          OPT_BUF(BV_STS)
-#define PV_SUA          OPT_BUF(BV_SUA)
-#define PV_SW           OPT_BUF(BV_SW)
-#define PV_SWF          OPT_BUF(BV_SWF)
-#define PV_TAGS         OPT_BOTH(OPT_BUF(BV_TAGS))
-#define PV_TC           OPT_BOTH(OPT_BUF(BV_TC))
-#define PV_TS           OPT_BUF(BV_TS)
-#define PV_TW           OPT_BUF(BV_TW)
-#define PV_TX           OPT_BUF(BV_TX)
-#define PV_WM           OPT_BUF(BV_WM)
-
-#define PV_LIST         OPT_WIN(WV_LIST)
-#define PV_WCR          OPT_WIN(WV_WCR)
-#define PV_EIW          OPT_WIN(WV_EIW)
-#define PV_LCS          OPT_BOTH(OPT_WIN(WV_LCS))
-#define PV_FCS          OPT_BOTH(OPT_WIN(WV_FCS))
-#define PV_NU           OPT_WIN(WV_NU)
-#define PV_RNU          OPT_WIN(WV_RNU)
-#define PV_VE           OPT_BOTH(OPT_WIN(WV_VE))
-#define PV_SCBIND       OPT_WIN(WV_SCBIND)
-#define PV_SCROLL       OPT_WIN(WV_SCROLL)
-#define PV_SMS          OPT_WIN(WV_SMS)
-#define PV_SISO         OPT_BOTH(OPT_WIN(WV_SISO))
-#define PV_SO           OPT_BOTH(OPT_WIN(WV_SO))
-#define PV_SOP          OPT_BOTH(OPT_WIN(WV_SOP))
-#define PV_UL           OPT_BOTH(OPT_BUF(BV_UL))
-#define PV_WFB          OPT_WIN(WV_WFB)
-#define PV_WFH          OPT_WIN(WV_WFH)
-#define PV_WFW          OPT_WIN(WV_WFW)
-#define PV_WHL          OPT_WIN(WV_WHL)
-#define PV_WRAP         OPT_WIN(WV_WRAP)
-#define PV_CRBIND       OPT_WIN(WV_CRBIND)
+enum { PV_BOTH = 0x1000 };
+enum { PV_WIN = 0x2000 };
+enum { PV_BUF = 0x4000 };
 
 typedef enum
 {
@@ -111626,8 +109591,6 @@
     PV_MAXVAL = 0xffff
 } idopt_T;
 
-#define VAR_WIN ((char_u *)-1)
-
 static int      p_et_nobin;
 static int      p_ml_nobin;
 static long     p_tw_nobin;
@@ -111651,21 +109614,12 @@
     opt_expand_cb_T     opt_expand_cb;
 
     char_u      *def_val[2];
-# define SCTX_INIT
 };
 
-#define VI_DEFAULT  0
-#define VIM_DEFAULT 1
-
-#define ISK_LATIN1  (char_u *)"@,48-57,_,192-255"
+enum { VI_DEFAULT = 0 };
+enum { VIM_DEFAULT = 1 };
 
-# define ISP_LATIN1 (char_u *)"@,161-255"
-
-#define HIGHLIGHT_INIT "8:SpecialKey,~:EndOfBuffer,@:NonText,d:Directory,e:ErrorMsg,i:IncSearch,l:Search,y:CurSearch,m:MoreMsg,M:ModeMsg,n:LineNr,a:LineNrAbove,b:LineNrBelow,N:CursorLineNr,G:CursorLineSign,O:CursorLineFold,r:Question,s:StatusLine,S:StatusLineNC,c:VertSplit,|:VertSplitNC,t:Title,v:Visual,V:VisualNOS,w:WarningMsg,W:WildMenu,f:Folded,F:FoldColumn,A:DiffAdd,C:DiffChange,D:DiffDelete,T:DiffText,E:DiffTextAdd,>:SignColumn,-:Conceal,B:SpellBad,P:SpellCap,R:SpellRare,L:SpellLocal,+:Pmenu,=:PmenuSel,k:PmenuMatch,<:PmenuMatchSel,[:PmenuKind,]:PmenuKindSel,{:PmenuExtra,}:PmenuExtraSel,x:PmenuSbar,X:PmenuThumb,j:PmenuBorder,H:PmenuShadow,p:Popup,J:PopupBorder,Q:PopupTitle,*:TabLine,#:TabLineSel,_:TabLineFill,!:CursorColumn,.:CursorLine,o:ColorColumn,q:QuickFixLine,z:StatusLineTerm,Z:StatusLineTermNC,g:MsgArea,h:ComplMatchIns,%:TabPanel,^:TabPanelSel,&:TabPanelFill,I:PreInsert"
-
-# define DEFAULT_PYTHON_VER     0
-
-#define INDENTKEYS_DEFAULT (char_u *)"0{,0},0),0],:,0#,!^F,o,O,e"
+enum { DEFAULT_PYTHON_VER = 0 };
 
 static struct vimoption options[] =
 {
@@ -111674,1776 +109628,1772 @@
                             NULL, NULL,
                             {
                             (char_u *)224L,
-                                            (char_u *)0L} SCTX_INIT},
+                                            (char_u *)0L}   },
     {"allowrevins", "ari",  P_BOOL|P_VI_DEF|P_VIM,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
-                            {(char_u *)FALSE, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)FALSE, (char_u *)0L}   },
     {"altkeymap",   "akm",  P_BOOL|P_VI_DEF,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
-                            {(char_u *)FALSE, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)FALSE, (char_u *)0L}   },
     {"ambiwidth",  "ambw",  P_STRING|P_VI_DEF|P_RCLR,
                             (char_u *)&p_ambw, PV_NONE, did_set_ambiwidth, expand_set_ambiwidth,
                             {(char_u *)"single", (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"antialias",   "anti", P_BOOL|P_VI_DEF|P_VIM|P_RCLR,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)FALSE, (char_u *)FALSE}
-                            SCTX_INIT},
+                              },
     {"arabic",      "arab", P_BOOL|P_VI_DEF|P_VIM|P_CURSWANT,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
-                            {(char_u *)FALSE, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)FALSE, (char_u *)0L}   },
     {"arabicshape", "arshape", P_BOOL|P_VI_DEF|P_VIM|P_RCLR,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
-                            {(char_u *)TRUE, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)TRUE, (char_u *)0L}   },
     {"autochdir",  "acd",   P_BOOL|P_VI_DEF,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)0L, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"autocomplete",  "ac", P_BOOL|P_VI_DEF,
-                            (char_u *)&p_ac, PV_AC, NULL,
-                            NULL, {(char_u *)FALSE, (char_u *)0L} SCTX_INIT},
+                            (char_u *)&p_ac,   (idopt_T)(PV_BOTH + (int)( (idopt_T)(PV_BUF + (int)(BV_AC)) ))  , NULL,
+                            NULL, {(char_u *)FALSE, (char_u *)0L}   },
     {"autocompletedelay", "acl", P_NUM|P_VI_DEF,
                             (char_u *)&p_acl, PV_NONE, NULL, NULL,
-                            {(char_u *)0L, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)0L, (char_u *)0L}   },
     {"autocompletetimeout", "act", P_NUM|P_VI_DEF,
                             (char_u *)&p_act, PV_NONE, NULL, NULL,
-                            {(char_u *)80L, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)80L, (char_u *)0L}   },
     {"autoindent",  "ai",   P_BOOL|P_VI_DEF,
-                            (char_u *)&p_ai, PV_AI, NULL, NULL,
-                            {(char_u *)TRUE, (char_u *)0L} SCTX_INIT},
+                            (char_u *)&p_ai,   (idopt_T)(PV_BUF + (int)(BV_AI))  , NULL, NULL,
+                            {(char_u *)TRUE, (char_u *)0L}   },
     {"autoprint",   "ap",   P_BOOL|P_VI_DEF,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
-                            {(char_u *)FALSE, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)FALSE, (char_u *)0L}   },
     {"autoread",    "ar",   P_BOOL|P_VI_DEF,
-                            (char_u *)&p_ar, PV_AR, NULL, NULL,
-                            {(char_u *)FALSE, (char_u *)0L} SCTX_INIT},
+                            (char_u *)&p_ar,   (idopt_T)(PV_BOTH + (int)( (idopt_T)(PV_BUF + (int)(BV_AR)) ))  , NULL, NULL,
+                            {(char_u *)FALSE, (char_u *)0L}   },
     {"autoshelldir",  "asd",   P_BOOL|P_VI_DEF,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)0L, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"autowrite",   "aw",   P_BOOL|P_VI_DEF,
                             (char_u *)&p_aw, PV_NONE, NULL, NULL,
-                            {(char_u *)FALSE, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)FALSE, (char_u *)0L}   },
     {"autowriteall","awa",  P_BOOL|P_VI_DEF,
                             (char_u *)&p_awa, PV_NONE, NULL, NULL,
-                            {(char_u *)FALSE, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)FALSE, (char_u *)0L}   },
     {"background",  "bg",   P_STRING|P_VI_DEF|P_RCLR|P_HLONLY,
                             (char_u *)&p_bg, PV_NONE, did_set_background, expand_set_background,
                             {
                             (char_u *)"light",
-                                            (char_u *)0L} SCTX_INIT},
+                                            (char_u *)0L}   },
     {"backspace",   "bs",   P_STRING|P_VIM|P_ONECOMMA|P_NODUP,
                             (char_u *)&p_bs, PV_NONE, did_set_backspace, expand_set_backspace,
-                            {(char_u *)"", (char_u *)"indent,eol,start"} SCTX_INIT},
+                            {(char_u *)"", (char_u *)"indent,eol,start"}   },
     {"backup",      "bk",   P_BOOL|P_VI_DEF|P_VIM,
                             (char_u *)&p_bk, PV_NONE, NULL, NULL,
-                            {(char_u *)FALSE, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)FALSE, (char_u *)0L}   },
     {"backupcopy",  "bkc",  P_STRING|P_VIM|P_ONECOMMA|P_NODUP,
-                            (char_u *)&p_bkc, PV_BKC, did_set_backupcopy, expand_set_backupcopy,
+                            (char_u *)&p_bkc,   (idopt_T)(PV_BOTH + (int)( (idopt_T)(PV_BUF + (int)(BV_BKC)) ))  , did_set_backupcopy, expand_set_backupcopy,
                             {(char_u *)"yes", (char_u *)"auto"}
-                            SCTX_INIT},
+                              },
     {"backupdir",   "bdir", P_STRING|P_EXPAND|P_VI_DEF|P_ONECOMMA
                                                             |P_NODUP|P_SECURE,
                             (char_u *)&p_bdir, PV_NONE, NULL, NULL,
-                            {(char_u *)DFLT_BDIR, (char_u *)0L} SCTX_INIT},
+                            {(char_u *) ".,~/tmp,~/" , (char_u *)0L}   },
     {"backupext",   "bex",  P_STRING|P_VI_DEF|P_NFNAME,
                             (char_u *)&p_bex, PV_NONE,
                             did_set_backupext_or_patchmode, NULL,
                             {
                             (char_u *)"~",
-                                            (char_u *)0L} SCTX_INIT},
+                                            (char_u *)0L}   },
     {"backupskip",  "bsk",  P_STRING|P_VI_DEF|P_ONECOMMA|P_NODUP,
                             (char_u *)&p_bsk, PV_NONE, NULL, NULL,
                             {(char_u *)"", (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"balloondelay","bdlay",P_NUM|P_VI_DEF,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)0L, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"ballooneval", "beval",P_BOOL|P_VI_DEF|P_NO_MKRC,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)0L, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"balloonevalterm", "bevalterm",P_BOOL|P_VI_DEF|P_NO_MKRC,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)0L, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"balloonexpr", "bexpr", P_STRING|P_ALLOCED|P_VI_DEF|P_VIM|P_MLE,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)0L, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"beautify",    "bf",   P_BOOL|P_VI_DEF,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
-                            {(char_u *)FALSE, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)FALSE, (char_u *)0L}   },
     {"belloff",      "bo",  P_STRING|P_VI_DEF|P_COMMA|P_NODUP,
                             (char_u *)&p_bo, PV_NONE, did_set_belloff, expand_set_belloff,
-                            {(char_u *)"", (char_u *)0L} SCTX_INIT},
+                            {(char_u *)"", (char_u *)0L}   },
     {"binary",      "bin",  P_BOOL|P_VI_DEF|P_RSTAT,
-                            (char_u *)&p_bin, PV_BIN, did_set_binary, NULL,
-                            {(char_u *)FALSE, (char_u *)0L} SCTX_INIT},
+                            (char_u *)&p_bin,   (idopt_T)(PV_BUF + (int)(BV_BIN))  , did_set_binary, NULL,
+                            {(char_u *)FALSE, (char_u *)0L}   },
     {"bioskey",     "biosk",P_BOOL|P_VI_DEF,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
-                            {(char_u *)TRUE, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)TRUE, (char_u *)0L}   },
     {"bomb",        NULL,   P_BOOL|P_NO_MKRC|P_VI_DEF|P_RSTAT,
-                            (char_u *)&p_bomb, PV_BOMB,
+                            (char_u *)&p_bomb,   (idopt_T)(PV_BUF + (int)(BV_BOMB))  ,
                             did_set_eof_eol_fixeol_bomb, NULL,
-                            {(char_u *)FALSE, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)FALSE, (char_u *)0L}   },
     {"breakat",     "brk",  P_STRING|P_VI_DEF|P_RALL|P_FLAGLIST,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)0L, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"breakindent",   "bri",  P_BOOL|P_VI_DEF|P_VIM|P_RWIN,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)0L, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"breakindentopt", "briopt", P_STRING|P_ALLOCED|P_VI_DEF|P_RBUF
                                                   |P_ONECOMMA|P_NODUP,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)"", (char_u *)NULL}
-                            SCTX_INIT},
+                              },
     {"browsedir",   "bsdir",P_STRING|P_VI_DEF,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)0L, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"bufhidden",   "bh",   P_STRING|P_ALLOCED|P_VI_DEF|P_NOGLOB,
-                            (char_u *)&p_bh, PV_BH, did_set_bufhidden, expand_set_bufhidden,
+                            (char_u *)&p_bh,   (idopt_T)(PV_BUF + (int)(BV_BH))  , did_set_bufhidden, expand_set_bufhidden,
                             {(char_u *)"", (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"buflisted",   "bl",   P_BOOL|P_VI_DEF|P_NOGLOB,
-                            (char_u *)&p_bl, PV_BL, did_set_buflisted, NULL,
+                            (char_u *)&p_bl,   (idopt_T)(PV_BUF + (int)(BV_BL))  , did_set_buflisted, NULL,
                             {(char_u *)1L, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"buftype",     "bt",   P_STRING|P_ALLOCED|P_VI_DEF|P_NOGLOB,
-                            (char_u *)&p_bt, PV_BT, did_set_buftype, expand_set_buftype,
+                            (char_u *)&p_bt,   (idopt_T)(PV_BUF + (int)(BV_BT))  , did_set_buftype, expand_set_buftype,
                             {(char_u *)"", (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"casemap",     "cmp",   P_STRING|P_VI_DEF|P_ONECOMMA|P_NODUP,
                             (char_u *)&p_cmp, PV_NONE, did_set_casemap, expand_set_casemap,
                             {(char_u *)"internal,keepascii", (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"cdhome",      "cdh",  P_BOOL|P_VI_DEF|P_VIM|P_SECURE,
                             (char_u *)&p_cdh, PV_NONE, NULL, NULL,
                             {(char_u *)FALSE, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"cdpath",      "cd",   P_STRING|P_EXPAND|P_VI_DEF|P_SECURE|P_COMMA|P_NODUP,
                             (char_u *)&p_cdpath, PV_NONE, NULL, NULL,
                             {(char_u *)",,", (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"cedit",       NULL,   P_STRING,
                             (char_u *)&p_cedit, PV_NONE, did_set_cedit, NULL,
-                            {(char_u *)"", (char_u *)CTRL_F_STR}
-                            SCTX_INIT},
+                            {(char_u *)"", (char_u *) "\006" }
+                              },
     {"charconvert",  "ccv", P_STRING|P_VI_DEF|P_SECURE,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)0L, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"chistory",    "chi",  P_NUM|P_VI_DEF,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)0L, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"cindent",     "cin",  P_BOOL|P_VI_DEF|P_VIM,
-                            (char_u *)&p_cin, PV_CIN, NULL, NULL,
-                            {(char_u *)FALSE, (char_u *)0L} SCTX_INIT},
+                            (char_u *)&p_cin,   (idopt_T)(PV_BUF + (int)(BV_CIN))  , NULL, NULL,
+                            {(char_u *)FALSE, (char_u *)0L}   },
     {"cinkeys",     "cink", P_STRING|P_ALLOCED|P_VI_DEF|P_ONECOMMA|P_NODUP,
-                            (char_u *)&p_cink, PV_CINK, NULL, NULL,
-                            {INDENTKEYS_DEFAULT, (char_u *)0L} SCTX_INIT},
+                            (char_u *)&p_cink,   (idopt_T)(PV_BUF + (int)(BV_CINK))  , NULL, NULL,
+                            { (char_u *)"0{,0},0),0],:,0#,!^F,o,O,e" , (char_u *)0L}   },
     {"cinoptions",  "cino", P_STRING|P_ALLOCED|P_VI_DEF|P_ONECOMMA|P_NODUP,
-                            (char_u *)&p_cino, PV_CINO, did_set_cinoptions, NULL,
-                            {(char_u *)"", (char_u *)0L} SCTX_INIT},
+                            (char_u *)&p_cino,   (idopt_T)(PV_BUF + (int)(BV_CINO))  , did_set_cinoptions, NULL,
+                            {(char_u *)"", (char_u *)0L}   },
     {"cinscopedecls", "cinsd", P_STRING|P_ALLOCED|P_VI_DEF|P_ONECOMMA|P_NODUP,
-                            (char_u *)&p_cinsd, PV_CINSD, NULL, NULL,
+                            (char_u *)&p_cinsd,   (idopt_T)(PV_BUF + (int)(BV_CINSD))  , NULL, NULL,
                             {(char_u *)"public,protected,private", (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"cinwords",    "cinw", P_STRING|P_ALLOCED|P_VI_DEF|P_ONECOMMA|P_NODUP,
-                            (char_u *)&p_cinw, PV_CINW, NULL, NULL,
+                            (char_u *)&p_cinw,   (idopt_T)(PV_BUF + (int)(BV_CINW))  , NULL, NULL,
                             {(char_u *)"if,else,while,do,for,switch",
                                 (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"clipboard",   "cb",   P_STRING|P_VI_DEF|P_ONECOMMA|P_NODUP,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)"", (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"clipmethod", "cpm",   P_STRING|P_VI_DEF|P_ONECOMMA|P_NODUP,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)NULL, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"cmdheight",   "ch",   P_NUM|P_VI_DEF|P_RALL,
                             (char_u *)&p_ch, PV_NONE, did_set_cmdheight, NULL,
-                            {(char_u *)1L, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)1L, (char_u *)0L}   },
     {"cmdwinheight", "cwh", P_NUM|P_VI_DEF,
                             (char_u *)&p_cwh, PV_NONE, NULL, NULL,
-                            {(char_u *)7L, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)7L, (char_u *)0L}   },
     {"colorcolumn", "cc",   P_STRING|P_VI_DEF|P_ONECOMMA|P_NODUP|P_RWIN|P_HLONLY,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
-                            {(char_u *)"", (char_u *)0L} SCTX_INIT},
+                            {(char_u *)"", (char_u *)0L}   },
     {"columns",     "co",   P_NUM|P_NODEFAULT|P_NO_MKRC|P_VI_DEF|P_RCLR,
                             (char_u *)&Columns, PV_NONE, NULL, NULL,
-                            {(char_u *)80L, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)80L, (char_u *)0L}   },
     {"comments",    "com",  P_STRING|P_ALLOCED|P_VI_DEF|P_ONECOMMA|P_NODUP,
-                            (char_u *)&p_com, PV_COM, did_set_comments, NULL,
+                            (char_u *)&p_com,   (idopt_T)(PV_BUF + (int)(BV_COM))  , did_set_comments, NULL,
                             {(char_u *)"s1:/*,mb:*,ex:*/,://,b:#,:%,:XCOMM,n:>,fb:-",
                                 (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"commentstring", "cms", P_STRING|P_ALLOCED|P_VI_DEF,
-                            (char_u *)&p_cms, PV_CMS, did_set_commentstring, NULL,
+                            (char_u *)&p_cms,   (idopt_T)(PV_BUF + (int)(BV_CMS))  , did_set_commentstring, NULL,
                             {(char_u *)"/* %s */", (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"compatible",  "cp",   P_BOOL|P_RALL,
                             (char_u *)&p_cp, PV_NONE, did_set_compatible, NULL,
-                            {(char_u *)FALSE, (char_u *)FALSE} SCTX_INIT},
+                            {(char_u *)FALSE, (char_u *)FALSE}   },
     {"complete",    "cpt",  P_STRING|P_ALLOCED|P_VI_DEF|P_ONECOMMA|P_NODUP|P_MLE,
-                            (char_u *)&p_cpt, PV_CPT, did_set_complete, expand_set_complete,
+                            (char_u *)&p_cpt,   (idopt_T)(PV_BUF + (int)(BV_CPT))  , did_set_complete, expand_set_complete,
                             {(char_u *)".,w,b,u,t,i", (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"completefunc", "cfu", P_STRING|P_ALLOCED|P_VI_DEF|P_SECURE|P_FUNC,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)0L, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"completefuzzycollect", "cfc", P_STRING|P_VI_DEF|P_ONECOMMA|P_NODUP,
                             (char_u *)&p_cfc, PV_NONE, did_set_completefuzzycollect, expand_set_completefuzzycollect,
                             {(char_u *)"", (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"completeitemalign", "cia", P_STRING|P_VI_DEF|P_ONECOMMA|P_NODUP,
                             (char_u *)&p_cia, PV_NONE, did_set_completeitemalign, NULL,
                             {(char_u *)"abbr,kind,menu", (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"completeopt",   "cot",  P_STRING|P_VI_DEF|P_ONECOMMA|P_NODUP,
-                            (char_u *)&p_cot, PV_COT, did_set_completeopt, expand_set_completeopt,
+                            (char_u *)&p_cot,   (idopt_T)(PV_BOTH + (int)( (idopt_T)(PV_BUF + (int)(BV_COT)) ))  , did_set_completeopt, expand_set_completeopt,
                             {(char_u *)"menu,preview", (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"completepopup", "cpp", P_STRING|P_VI_DEF|P_COMMA|P_NODUP|P_COLON,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)NULL, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"completeslash",   "csl",  P_STRING|P_VI_DEF|P_VIM,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)0L, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"completetimeout", "cto", P_NUM|P_VI_DEF,
                             (char_u *)&p_cto, PV_NONE, NULL, NULL,
-                            {(char_u *)0L, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)0L, (char_u *)0L}   },
     {"concealcursor","cocu", P_STRING|P_ALLOCED|P_RWIN|P_VI_DEF|P_FLAGLIST,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)NULL, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"conceallevel","cole", P_NUM|P_RWIN|P_VI_DEF,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)0L, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"confirm",     "cf",   P_BOOL|P_VI_DEF,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
-                            {(char_u *)FALSE, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)FALSE, (char_u *)0L}   },
     {"conskey",     "consk",P_BOOL|P_VI_DEF,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
-                            {(char_u *)FALSE, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)FALSE, (char_u *)0L}   },
     {"copyindent",  "ci",   P_BOOL|P_VI_DEF|P_VIM,
-                            (char_u *)&p_ci, PV_CI, NULL, NULL,
-                            {(char_u *)FALSE, (char_u *)0L} SCTX_INIT},
+                            (char_u *)&p_ci,   (idopt_T)(PV_BUF + (int)(BV_CI))  , NULL, NULL,
+                            {(char_u *)FALSE, (char_u *)0L}   },
     {"cpoptions",   "cpo",  P_STRING|P_VIM|P_RALL|P_FLAGLIST,
                             (char_u *)&p_cpo, PV_NONE, did_set_cpoptions, expand_set_cpoptions,
-                            {(char_u *)CPO_VI, (char_u *)CPO_VIM}
-                            SCTX_INIT},
+                            {(char_u *) "aAbBcCdDeEfFgHiIjJkKlLmMnoOpPqrRsStuvwWxXyZz$!%*-+<>;" , (char_u *) "aABceFsz" }
+                              },
     {"cryptmethod", "cm",   P_STRING|P_ALLOCED|P_VI_DEF,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)0L, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"cscopepathcomp", "cspc", P_NUM|P_VI_DEF|P_VIM,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
-                            {(char_u *)0L, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)0L, (char_u *)0L}   },
     {"cscopeprg",   "csprg", P_STRING|P_EXPAND|P_VI_DEF|P_SECURE,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)0L, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"cscopequickfix", "csqf", P_STRING|P_VI_DEF|P_ONECOMMA|P_NODUP,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)0L, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"cscoperelative", "csre", P_BOOL|P_VI_DEF|P_VIM,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
-                            {(char_u *)0L, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)0L, (char_u *)0L}   },
     {"cscopetag",   "cst",  P_BOOL|P_VI_DEF|P_VIM,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
-                            {(char_u *)0L, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)0L, (char_u *)0L}   },
     {"cscopetagorder", "csto", P_NUM|P_VI_DEF|P_VIM,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
-                            {(char_u *)0L, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)0L, (char_u *)0L}   },
     {"cscopeverbose", "csverb", P_BOOL|P_VI_DEF|P_VIM,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
-                            {(char_u *)0L, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)0L, (char_u *)0L}   },
     {"cursorbind",  "crb",  P_BOOL|P_VI_DEF,
-                            (char_u *)VAR_WIN, PV_CRBIND, NULL, NULL,
-                            {(char_u *)FALSE, (char_u *)0L} SCTX_INIT},
+                            (char_u *) ((char_u *)-1) ,   (idopt_T)(PV_WIN + (int)(WV_CRBIND))  , NULL, NULL,
+                            {(char_u *)FALSE, (char_u *)0L}   },
     {"cursorcolumn", "cuc", P_BOOL|P_VI_DEF|P_RWIN|P_HLONLY,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
-                            {(char_u *)FALSE, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)FALSE, (char_u *)0L}   },
     {"cursorline",   "cul", P_BOOL|P_VI_DEF|P_RWIN|P_HLONLY,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
-                            {(char_u *)FALSE, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)FALSE, (char_u *)0L}   },
     {"cursorlineopt", "culopt", P_STRING|P_VI_DEF|P_RWIN|P_HLONLY|P_ONECOMMA|P_NODUP,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
-                            {(char_u *)"both", (char_u *)0L} SCTX_INIT},
+                            {(char_u *)"both", (char_u *)0L}   },
     {"debug",       NULL,   P_STRING|P_VI_DEF,
                             (char_u *)&p_debug, PV_NONE, did_set_debug, expand_set_debug,
-                            {(char_u *)"", (char_u *)0L} SCTX_INIT},
+                            {(char_u *)"", (char_u *)0L}   },
     {"define",      "def",  P_STRING|P_ALLOCED|P_VI_DEF,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)NULL, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"delcombine", "deco",  P_BOOL|P_VI_DEF|P_VIM,
                             (char_u *)&p_deco, PV_NONE, NULL, NULL,
-                            {(char_u *)FALSE, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)FALSE, (char_u *)0L}   },
     {"dictionary",  "dict", P_STRING|P_EXPAND|P_VI_DEF|P_ONECOMMA|P_NODUP|P_NDNAME,
-                            (char_u *)&p_dict, PV_DICT, NULL, NULL,
-                            {(char_u *)"", (char_u *)0L} SCTX_INIT},
+                            (char_u *)&p_dict,   (idopt_T)(PV_BOTH + (int)( (idopt_T)(PV_BUF + (int)(BV_DICT)) ))  , NULL, NULL,
+                            {(char_u *)"", (char_u *)0L}   },
     {"diff",        NULL,   P_BOOL|P_VI_DEF|P_RWIN|P_NOGLOB,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
-                            {(char_u *)FALSE, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)FALSE, (char_u *)0L}   },
     {"diffanchors", "dia",  P_STRING|P_VI_DEF|P_ONECOMMA,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
-                            {(char_u *)"", (char_u *)NULL} SCTX_INIT},
+                            {(char_u *)"", (char_u *)NULL}   },
     {"diffexpr",    "dex",  P_STRING|P_VI_DEF|P_SECURE|P_CURSWANT,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)0L, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"diffopt",     "dip",  P_STRING|P_ALLOCED|P_VI_DEF|P_RWIN|P_ONECOMMA|P_COLON
                                                                      |P_NODUP,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)"", (char_u *)NULL}
-                            SCTX_INIT},
+                              },
     {"digraph",     "dg",   P_BOOL|P_VI_DEF|P_VIM,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
-                            {(char_u *)FALSE, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)FALSE, (char_u *)0L}   },
     {"directory",   "dir",  P_STRING|P_EXPAND|P_VI_DEF|P_ONECOMMA
                                                             |P_NODUP|P_SECURE,
                             (char_u *)&p_dir, PV_NONE, NULL, NULL,
-                            {(char_u *)DFLT_DIR, (char_u *)0L} SCTX_INIT},
+                            {(char_u *) ".,~/tmp,/var/tmp,/tmp" , (char_u *)0L}   },
     {"display",     "dy",   P_STRING|P_VI_DEF|P_ONECOMMA|P_RALL|P_NODUP,
                             (char_u *)&p_dy, PV_NONE, did_set_display, expand_set_display,
-                            {(char_u *)"", (char_u *)0L} SCTX_INIT},
+                            {(char_u *)"", (char_u *)0L}   },
     {"eadirection", "ead",  P_STRING|P_VI_DEF,
                             (char_u *)&p_ead, PV_NONE, did_set_eadirection, expand_set_eadirection,
                             {(char_u *)"both", (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"edcompatible","ed",   P_BOOL|P_VI_DEF,
                             (char_u *)&p_ed, PV_NONE, NULL, NULL,
-                            {(char_u *)FALSE, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)FALSE, (char_u *)0L}   },
     {"emoji",      "emo",   P_BOOL|P_VI_DEF|P_RCLR,
                             (char_u *)&p_emoji, PV_NONE, did_set_ambiwidth, NULL,
                             {(char_u *)TRUE, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"encoding",    "enc",  P_STRING|P_VI_DEF|P_RCLR|P_NO_ML,
                             (char_u *)&p_enc, PV_NONE, did_set_encoding, expand_set_encoding,
-                            {(char_u *)ENC_DFLT, (char_u *)0L}
-                            SCTX_INIT},
+                            {(char_u *) "latin1" , (char_u *)0L}
+                              },
     {"endoffile",   "eof",  P_BOOL|P_NO_MKRC|P_VI_DEF|P_RSTAT,
-                            (char_u *)&p_eof, PV_EOF,
+                            (char_u *)&p_eof,   (idopt_T)(PV_BUF + (int)(BV_EOF))  ,
                             did_set_eof_eol_fixeol_bomb, NULL,
-                            {(char_u *)FALSE, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)FALSE, (char_u *)0L}   },
     {"endofline",   "eol",  P_BOOL|P_NO_MKRC|P_VI_DEF|P_RSTAT,
-                            (char_u *)&p_eol, PV_EOL,
+                            (char_u *)&p_eol,   (idopt_T)(PV_BUF + (int)(BV_EOL))  ,
                             did_set_eof_eol_fixeol_bomb, NULL,
-                            {(char_u *)TRUE, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)TRUE, (char_u *)0L}   },
     {"equalalways", "ea",   P_BOOL|P_VI_DEF|P_RALL,
                             (char_u *)&p_ea, PV_NONE, did_set_equalalways, NULL,
-                            {(char_u *)TRUE, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)TRUE, (char_u *)0L}   },
     {"equalprg",    "ep",   P_STRING|P_EXPAND|P_VI_DEF|P_SECURE,
-                            (char_u *)&p_ep, PV_EP, NULL, NULL,
-                            {(char_u *)"", (char_u *)0L} SCTX_INIT},
+                            (char_u *)&p_ep,   (idopt_T)(PV_BOTH + (int)( (idopt_T)(PV_BUF + (int)(BV_EP)) ))  , NULL, NULL,
+                            {(char_u *)"", (char_u *)0L}   },
     {"errorbells",  "eb",   P_BOOL|P_VI_DEF,
                             (char_u *)&p_eb, PV_NONE, NULL, NULL,
-                            {(char_u *)FALSE, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)FALSE, (char_u *)0L}   },
     {"errorfile",   "ef",   P_STRING|P_EXPAND|P_VI_DEF|P_SECURE,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)NULL, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"errorformat", "efm",  P_STRING|P_VI_DEF|P_ONECOMMA|P_NODUP,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)NULL, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"esckeys",     "ek",   P_BOOL|P_VIM,
                             (char_u *)&p_ek, PV_NONE, NULL, NULL,
-                            {(char_u *)FALSE, (char_u *)TRUE} SCTX_INIT},
+                            {(char_u *)FALSE, (char_u *)TRUE}   },
     {"eventignore", "ei",   P_STRING|P_VI_DEF|P_ONECOMMA|P_NODUP,
                             (char_u *)&p_ei, PV_NONE, did_set_eventignore, expand_set_eventignore,
-                            {(char_u *)"", (char_u *)0L} SCTX_INIT},
+                            {(char_u *)"", (char_u *)0L}   },
     {"eventignorewin", "eiw",   P_STRING|P_VI_DEF|P_ONECOMMA|P_NODUP,
-                            (char_u *)VAR_WIN, PV_EIW, did_set_eventignore, expand_set_eventignore,
-                            {(char_u *)"", (char_u *)0L} SCTX_INIT},
+                            (char_u *) ((char_u *)-1) ,   (idopt_T)(PV_WIN + (int)(WV_EIW))  , did_set_eventignore, expand_set_eventignore,
+                            {(char_u *)"", (char_u *)0L}   },
     {"expandtab",   "et",   P_BOOL|P_VI_DEF|P_VIM,
-                            (char_u *)&p_et, PV_ET, NULL, NULL,
-                            {(char_u *)TRUE, (char_u *)0L} SCTX_INIT},
+                            (char_u *)&p_et,   (idopt_T)(PV_BUF + (int)(BV_ET))  , NULL, NULL,
+                            {(char_u *)TRUE, (char_u *)0L}   },
     {"exrc",        "ex",   P_BOOL|P_VI_DEF|P_SECURE,
                             (char_u *)&p_exrc, PV_NONE, NULL, NULL,
-                            {(char_u *)FALSE, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)FALSE, (char_u *)0L}   },
     {"fileencoding","fenc", P_STRING|P_ALLOCED|P_VI_DEF|P_RSTAT|P_RBUF
                                                                    |P_NO_MKRC,
-                            (char_u *)&p_fenc, PV_FENC, did_set_encoding, expand_set_encoding,
+                            (char_u *)&p_fenc,   (idopt_T)(PV_BUF + (int)(BV_FENC))  , did_set_encoding, expand_set_encoding,
                             {(char_u *)"", (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"fileencodings","fencs", P_STRING|P_VI_DEF|P_ONECOMMA,
                             (char_u *)&p_fencs, PV_NONE, NULL, expand_set_encoding,
                             {(char_u *)"ucs-bom", (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"fileformat",  "ff",   P_STRING|P_ALLOCED|P_VI_DEF|P_RSTAT|P_NO_MKRC
                                                                   |P_CURSWANT,
-                            (char_u *)&p_ff, PV_FF, did_set_fileformat, expand_set_fileformat,
-                            {(char_u *)DFLT_FF, (char_u *)0L} SCTX_INIT},
+                            (char_u *)&p_ff,   (idopt_T)(PV_BUF + (int)(BV_FF))  , did_set_fileformat, expand_set_fileformat,
+                            {(char_u *) "unix" , (char_u *)0L}   },
     {"fileformats", "ffs",  P_STRING|P_VIM|P_ONECOMMA|P_NODUP,
                             (char_u *)&p_ffs, PV_NONE, did_set_fileformats, expand_set_fileformat,
-                            {(char_u *)DFLT_FFS_VI, (char_u *)DFLT_FFS_VIM}
-                            SCTX_INIT},
+                            {(char_u *) "" , (char_u *) "unix,dos" }
+                              },
     {"fileignorecase", "fic", P_BOOL|P_VI_DEF,
                             (char_u *)&p_fic, PV_NONE, NULL, NULL,
                             {
                                     (char_u *)FALSE,
-                                        (char_u *)0L} SCTX_INIT},
+                                        (char_u *)0L}   },
     {"filetype",    "ft",   P_STRING|P_ALLOCED|P_VI_DEF|P_NOGLOB|P_NFNAME,
-                            (char_u *)&p_ft, PV_FT,
+                            (char_u *)&p_ft,   (idopt_T)(PV_BUF + (int)(BV_FT))  ,
                             did_set_filetype_or_syntax, NULL,
                             {(char_u *)"", (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"fillchars",   "fcs",  P_STRING|P_VI_DEF|P_RALL|P_ONECOMMA|P_NODUP|P_COLON,
-                            (char_u *)&p_fcs, PV_FCS, did_set_chars_option, expand_set_chars_option,
+                            (char_u *)&p_fcs,   (idopt_T)(PV_BOTH + (int)( (idopt_T)(PV_WIN + (int)(WV_FCS)) ))  , did_set_chars_option, expand_set_chars_option,
                             {(char_u *)"vert:|,fold:-,eob:~,lastline:@",
                                                                   (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"findfunc", "ffu",     P_STRING|P_ALLOCED|P_VI_DEF|P_SECURE|P_FUNC,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)0L, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"fixendofline",  "fixeol", P_BOOL|P_VI_DEF|P_RSTAT,
-                            (char_u *)&p_fixeol, PV_FIXEOL,
+                            (char_u *)&p_fixeol,   (idopt_T)(PV_BUF + (int)(BV_FIXEOL))  ,
                             did_set_eof_eol_fixeol_bomb, NULL,
-                            {(char_u *)TRUE, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)TRUE, (char_u *)0L}   },
     {"fkmap",       "fk",   P_BOOL|P_VI_DEF,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
-                            {(char_u *)FALSE, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)FALSE, (char_u *)0L}   },
     {"flash",       "fl",   P_BOOL|P_VI_DEF,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
-                            {(char_u *)FALSE, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)FALSE, (char_u *)0L}   },
     {"foldclose",   "fcl",  P_STRING|P_VI_DEF|P_ONECOMMA|P_NODUP|P_RWIN,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)NULL, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"foldcolumn",  "fdc",  P_NUM|P_VI_DEF|P_RWIN,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)NULL, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"foldenable",  "fen",  P_BOOL|P_VI_DEF|P_RWIN,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)NULL, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"foldexpr",    "fde",  P_STRING|P_ALLOCED|P_VIM|P_VI_DEF|P_RWIN|P_MLE,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)NULL, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"foldignore",  "fdi",  P_STRING|P_ALLOCED|P_VIM|P_VI_DEF|P_RWIN,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)NULL, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"foldlevel",   "fdl",  P_NUM|P_VI_DEF|P_RWIN,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)NULL, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"foldlevelstart","fdls", P_NUM|P_VI_DEF|P_CURSWANT,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)NULL, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"foldmarker",  "fmr",  P_STRING|P_ALLOCED|P_VIM|P_VI_DEF|
                                                     P_RWIN|P_ONECOMMA|P_NODUP,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)NULL, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"foldmethod",  "fdm",  P_STRING|P_ALLOCED|P_VIM|P_VI_DEF|P_RWIN,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)NULL, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"foldminlines","fml",  P_NUM|P_VI_DEF|P_RWIN,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)NULL, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"foldnestmax", "fdn",  P_NUM|P_VI_DEF|P_RWIN,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)NULL, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"foldopen",    "fdo",  P_STRING|P_VI_DEF|P_ONECOMMA|P_NODUP|P_CURSWANT,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)NULL, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"foldtext",    "fdt",  P_STRING|P_ALLOCED|P_VIM|P_VI_DEF|P_RWIN|P_MLE,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)NULL, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"formatexpr", "fex",   P_STRING|P_ALLOCED|P_VI_DEF|P_VIM|P_MLE,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)0L, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"formatlistpat","flp", P_STRING|P_ALLOCED|P_VI_DEF,
-                            (char_u *)&p_flp, PV_FLP, NULL, NULL,
+                            (char_u *)&p_flp,   (idopt_T)(PV_BUF + (int)(BV_FLP))  , NULL, NULL,
                             {(char_u *)"^\\s*\\d\\+[\\]:.)}\\t ]\\s*",
-                                                 (char_u *)0L} SCTX_INIT},
+                                                 (char_u *)0L}   },
     {"formatoptions","fo",  P_STRING|P_ALLOCED|P_VIM|P_FLAGLIST,
-                            (char_u *)&p_fo, PV_FO, did_set_formatoptions, expand_set_formatoptions,
-                            {(char_u *)DFLT_FO_VI, (char_u *)DFLT_FO_VIM}
-                            SCTX_INIT},
+                            (char_u *)&p_fo,   (idopt_T)(PV_BUF + (int)(BV_FO))  , did_set_formatoptions, expand_set_formatoptions,
+                            {(char_u *) "vt" , (char_u *) "tcq" }
+                              },
     {"formatprg",   "fp",   P_STRING|P_EXPAND|P_VI_DEF|P_SECURE,
-                            (char_u *)&p_fp, PV_FP, NULL, NULL,
-                            {(char_u *)"", (char_u *)0L} SCTX_INIT},
+                            (char_u *)&p_fp,   (idopt_T)(PV_BOTH + (int)( (idopt_T)(PV_BUF + (int)(BV_FP)) ))  , NULL, NULL,
+                            {(char_u *)"", (char_u *)0L}   },
     {"fsync",       "fs",   P_BOOL|P_SECURE|P_VI_DEF,
-                            (char_u *)&p_fs, PV_FS, NULL, NULL,
+                            (char_u *)&p_fs,   (idopt_T)(PV_BOTH + (int)( (idopt_T)(PV_BUF + (int)(BV_FS)) ))  , NULL, NULL,
                             {(char_u *)TRUE, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"gdefault",    "gd",   P_BOOL|P_VI_DEF|P_VIM,
                             (char_u *)&p_gd, PV_NONE, NULL, NULL,
-                            {(char_u *)FALSE, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)FALSE, (char_u *)0L}   },
     {"graphic",     "gr",   P_BOOL|P_VI_DEF,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
-                            {(char_u *)FALSE, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)FALSE, (char_u *)0L}   },
     {"grepformat",  "gfm",  P_STRING|P_VI_DEF|P_ONECOMMA|P_NODUP,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)NULL, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"grepprg",     "gp",   P_STRING|P_EXPAND|P_VI_DEF|P_SECURE,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)NULL, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"guicursor",    "gcr", P_STRING|P_VI_DEF|P_ONECOMMA|P_NODUP,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)NULL, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"guifont",     "gfn",  P_STRING|P_VI_DEF|P_RCLR|P_ONECOMMA|P_NODUP
                                 |P_COLON
                                 ,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)NULL, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"guifontset",  "gfs",  P_STRING|P_VI_DEF|P_RCLR|P_ONECOMMA,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)NULL, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"guifontwide", "gfw",  P_STRING|P_VI_DEF|P_RCLR|P_ONECOMMA|P_NODUP
                                 |P_COLON
                                 ,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)NULL, (char_u *)0L}
-                            SCTX_INIT},
+                              },
 
     {"guiheadroom", "ghr",  P_NUM|P_VI_DEF,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
-                            {(char_u *)50L, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)50L, (char_u *)0L}   },
 
     {"guiligatures", "gli", P_STRING|P_VI_DEF|P_RCLR|P_ONECOMMA|P_NODUP,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)NULL, (char_u *)0L}
-                            SCTX_INIT},
+                              },
 
     {"guioptions",  "go",   P_STRING|P_VI_DEF|P_RALL|P_FLAGLIST,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)NULL, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"guipty",      NULL,   P_BOOL|P_VI_DEF,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
-                            {(char_u *)TRUE, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)TRUE, (char_u *)0L}   },
     {"guitablabel",  "gtl", P_STRING|P_VI_DEF|P_RWIN|P_MLE,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)NULL, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"guitabtooltip",  "gtt", P_STRING|P_VI_DEF|P_RWIN|P_MLE,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)NULL, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"hardtabs",    "ht",   P_NUM|P_VI_DEF,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
-                            {(char_u *)0L, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)0L, (char_u *)0L}   },
     {"helpfile",    "hf",   P_STRING|P_EXPAND|P_VI_DEF|P_SECURE,
                             (char_u *)&p_hf, PV_NONE, did_set_helpfile, NULL,
-                            {(char_u *)DFLT_HELPFILE, (char_u *)0L}
-                            SCTX_INIT},
+                            {(char_u *) "$VIMRUNTIME/doc/help.txt" , (char_u *)0L}
+                              },
     {"helpheight",  "hh",   P_NUM|P_VI_DEF,
                             (char_u *)&p_hh, PV_NONE,
                             did_set_winheight_helpheight, NULL,
-                            {(char_u *)20L, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)20L, (char_u *)0L}   },
     {"helplang",    "hlg",  P_STRING|P_VI_DEF|P_ONECOMMA,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)0L, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"hidden",      "hid",  P_BOOL|P_VI_DEF,
                             (char_u *)&p_hid, PV_NONE, NULL, NULL,
-                            {(char_u *)FALSE, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)FALSE, (char_u *)0L}   },
     {"highlight",   "hl",   P_STRING|P_VI_DEF|P_RCLR|P_ONECOMMA|P_NODUP|P_COLON,
                             (char_u *)&p_hl, PV_NONE, did_set_highlight, expand_set_highlight,
-                            {(char_u *)HIGHLIGHT_INIT, (char_u *)0L}
-                            SCTX_INIT},
+                            {(char_u *) "8:SpecialKey,~:EndOfBuffer,@:NonText,d:Directory,e:ErrorMsg,i:IncSearch,l:Search,y:CurSearch,m:MoreMsg,M:ModeMsg,n:LineNr,a:LineNrAbove,b:LineNrBelow,N:CursorLineNr,G:CursorLineSign,O:CursorLineFold,r:Question,s:StatusLine,S:StatusLineNC,c:VertSplit,|:VertSplitNC,t:Title,v:Visual,V:VisualNOS,w:WarningMsg,W:WildMenu,f:Folded,F:FoldColumn,A:DiffAdd,C:DiffChange,D:DiffDelete,T:DiffText,E:DiffTextAdd,>:SignColumn,-:Conceal,B:SpellBad,P:SpellCap,R:SpellRare,L:SpellLocal,+:Pmenu,=:PmenuSel,k:PmenuMatch,<:PmenuMatchSel,[:PmenuKind,]:PmenuKindSel,{:PmenuExtra,}:PmenuExtraSel,x:PmenuSbar,X:PmenuThumb,j:PmenuBorder,H:PmenuShadow,p:Popup,J:PopupBorder,Q:PopupTitle,*:TabLine,#:TabLineSel,_:TabLineFill,!:CursorColumn,.:CursorLine,o:ColorColumn,q:QuickFixLine,z:StatusLineTerm,Z:StatusLineTermNC,g:MsgArea,h:ComplMatchIns,%:TabPanel,^:TabPanelSel,&:TabPanelFill,I:PreInsert" , (char_u *)0L}
+                              },
     {"history",     "hi",   P_NUM|P_VIM,
                             (char_u *)&p_hi, PV_NONE, NULL, NULL,
-                            {(char_u *)9999L, (char_u *)9999L} SCTX_INIT},
+                            {(char_u *)9999L, (char_u *)9999L}   },
     {"hkmap",       "hk",   P_BOOL|P_VI_DEF|P_VIM,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
-                            {(char_u *)FALSE, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)FALSE, (char_u *)0L}   },
     {"hkmapp",      "hkp",  P_BOOL|P_VI_DEF|P_VIM,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
-                            {(char_u *)FALSE, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)FALSE, (char_u *)0L}   },
     {"hlsearch",    "hls",  P_BOOL|P_VI_DEF|P_VIM|P_RALL|P_HLONLY,
                             (char_u *)&p_hls, PV_NONE, did_set_hlsearch, NULL,
-                            {(char_u *)TRUE, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)TRUE, (char_u *)0L}   },
     {"icon",        NULL,   P_BOOL|P_VI_DEF,
                             (char_u *)&p_icon, PV_NONE, did_set_title_icon, NULL,
-                            {(char_u *)FALSE, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)FALSE, (char_u *)0L}   },
     {"iconstring",  NULL,   P_STRING|P_VI_DEF|P_MLE,
                             (char_u *)&p_iconstring, PV_NONE,
                             did_set_iconstring, NULL,
-                            {(char_u *)"", (char_u *)0L} SCTX_INIT},
+                            {(char_u *)"", (char_u *)0L}   },
     {"ignorecase",  "ic",   P_BOOL|P_VI_DEF,
                             (char_u *)&p_ic, PV_NONE, did_set_ignorecase, NULL,
-                            {(char_u *)FALSE, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)FALSE, (char_u *)0L}   },
     {"imactivatefunc","imaf",P_STRING|P_VI_DEF|P_SECURE|P_FUNC,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)NULL, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"imactivatekey","imak",P_STRING|P_VI_DEF,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
-                            {(char_u *)"", (char_u *)0L} SCTX_INIT},
+                            {(char_u *)"", (char_u *)0L}   },
     {"imcmdline",   "imc",  P_BOOL|P_VI_DEF,
                             (char_u *)&p_imcmdline, PV_NONE, NULL, NULL,
-                            {(char_u *)FALSE, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)FALSE, (char_u *)0L}   },
     {"imdisable",   "imd",  P_BOOL|P_VI_DEF,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)FALSE, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"iminsert",    "imi",  P_NUM|P_VI_DEF,
-                            (char_u *)&p_iminsert, PV_IMI, did_set_iminsert, NULL,
+                            (char_u *)&p_iminsert,   (idopt_T)(PV_BUF + (int)(BV_IMI))  , did_set_iminsert, NULL,
                             {(char_u *)B_IMODE_NONE, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"imsearch",    "ims",  P_NUM|P_VI_DEF,
-                            (char_u *)&p_imsearch, PV_IMS, did_set_imsearch, NULL,
-                            {(char_u *)B_IMODE_USE_INSERT, (char_u *)0L}
-                            SCTX_INIT},
+                            (char_u *)&p_imsearch,   (idopt_T)(PV_BUF + (int)(BV_IMS))  , did_set_imsearch, NULL,
+                            {(char_u *) (-1) , (char_u *)0L}
+                              },
     {"imstatusfunc","imsf",P_STRING|P_VI_DEF|P_SECURE|P_FUNC,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)NULL, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"imstyle",     "imst", P_NUM|P_VI_DEF|P_SECURE,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)0L, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"include",     "inc",  P_STRING|P_ALLOCED|P_VI_DEF,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)0L, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"includeexpr", "inex", P_STRING|P_ALLOCED|P_VI_DEF|P_MLE,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)0L, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"incsearch",   "is",   P_BOOL|P_VI_DEF|P_VIM,
                             (char_u *)&p_is, PV_NONE, NULL, NULL,
-                            {(char_u *)FALSE, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)FALSE, (char_u *)0L}   },
     {"indentexpr", "inde",  P_STRING|P_ALLOCED|P_VI_DEF|P_VIM|P_MLE,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)0L, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"indentkeys", "indk",  P_STRING|P_ALLOCED|P_VI_DEF|P_ONECOMMA|P_NODUP,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)0L, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"infercase",   "inf",  P_BOOL|P_VI_DEF,
-                            (char_u *)&p_inf, PV_INF, NULL, NULL,
-                            {(char_u *)FALSE, (char_u *)0L} SCTX_INIT},
+                            (char_u *)&p_inf,   (idopt_T)(PV_BUF + (int)(BV_INF))  , NULL, NULL,
+                            {(char_u *)FALSE, (char_u *)0L}   },
     {"insertmode",  "im",   P_BOOL|P_VI_DEF|P_VIM,
                             (char_u *)&p_im, PV_NONE, did_set_insertmode, NULL,
-                            {(char_u *)FALSE, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)FALSE, (char_u *)0L}   },
     {"isfname",     "isf",  P_STRING|P_VI_DEF|P_COMMA|P_NODUP,
                             (char_u *)&p_isf, PV_NONE, did_set_isopt, NULL,
                             {
                             (char_u *)"@,48-57,/,.,-,_,+,,,#,$,%,~,=",
-                                (char_u *)0L} SCTX_INIT},
+                                (char_u *)0L}   },
     {"isident",     "isi",  P_STRING|P_VI_DEF|P_COMMA|P_NODUP,
                             (char_u *)&p_isi, PV_NONE, did_set_isopt, NULL,
                             {
                             (char_u *)"@,48-57,_,192-255",
-                                (char_u *)0L} SCTX_INIT},
+                                (char_u *)0L}   },
     {"iskeyword",   "isk",  P_STRING|P_ALLOCED|P_VIM|P_COMMA|P_NODUP,
-                            (char_u *)&p_isk, PV_ISK, did_set_iskeyword, NULL,
+                            (char_u *)&p_isk,   (idopt_T)(PV_BUF + (int)(BV_ISK))  , did_set_iskeyword, NULL,
                             {
                                 (char_u *)"@,48-57,_",
-                                ISK_LATIN1
-                            } SCTX_INIT},
+                                 (char_u *)"@,48-57,_,192-255" 
+                            }   },
     {"isprint",     "isp",  P_STRING|P_VI_DEF|P_RALL|P_COMMA|P_NODUP,
                             (char_u *)&p_isp, PV_NONE, did_set_isopt, NULL,
                             {
-                            ISP_LATIN1,
-                                (char_u *)0L} SCTX_INIT},
+                             (char_u *)"@,161-255" ,
+                                (char_u *)0L}   },
     {"joinspaces",  "js",   P_BOOL|P_VI_DEF|P_VIM,
                             (char_u *)&p_js, PV_NONE, NULL, NULL,
-                            {(char_u *)FALSE, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)FALSE, (char_u *)0L}   },
     {"jumpoptions", "jop",  P_STRING|P_VI_DEF|P_VIM|P_ONECOMMA|P_NODUP,
                             (char_u *)&p_jop, PV_NONE, did_set_jumpoptions, expand_set_jumpoptions,
-                            {(char_u *)"", (char_u *)0L} SCTX_INIT},
+                            {(char_u *)"", (char_u *)0L}   },
     {"key",         NULL,   P_STRING|P_ALLOCED|P_VI_DEF|P_NO_MKRC|P_NO_CMD_EXPAND,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)0L, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"keymap",      "kmp",  P_STRING|P_ALLOCED|P_VI_DEF|P_RBUF|P_RSTAT|P_NFNAME|P_PRI_MKRC,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)"", (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"keymodel",    "km",   P_STRING|P_VI_DEF|P_ONECOMMA|P_NODUP,
                             (char_u *)&p_km, PV_NONE, did_set_keymodel, expand_set_keymodel,
-                            {(char_u *)"startsel", (char_u *)0L} SCTX_INIT},
+                            {(char_u *)"startsel", (char_u *)0L}   },
     {"keyprotocol", "kpc",  P_STRING|P_VI_DEF|P_ONECOMMA|P_NODUP|P_COLON,
                             (char_u *)&p_kpc, PV_NONE, did_set_keyprotocol, expand_set_keyprotocol,
                             {(char_u *)"kitty:kitty,foot:kitty,ghostty:kitty,wezterm:kitty,xterm:mok2", (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"keywordprg",  "kp",   P_STRING|P_EXPAND|P_VI_DEF|P_SECURE,
-                            (char_u *)&p_kp, PV_KP, NULL, NULL,
+                            (char_u *)&p_kp,   (idopt_T)(PV_BOTH + (int)( (idopt_T)(PV_BUF + (int)(BV_KP)) ))  , NULL, NULL,
                             {
                             (char_u *)"man",
-                                (char_u *)0L} SCTX_INIT},
+                                (char_u *)0L}   },
     {"langmap",     "lmap", P_STRING|P_VI_DEF|P_ONECOMMA|P_NODUP|P_SECURE,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)NULL, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"langmenu",    "lm",   P_STRING|P_VI_DEF|P_NFNAME,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
-                            {(char_u *)"", (char_u *)0L} SCTX_INIT},
+                            {(char_u *)"", (char_u *)0L}   },
     {"langnoremap",  "lnr",   P_BOOL|P_VI_DEF,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
-                            {(char_u *)FALSE, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)FALSE, (char_u *)0L}   },
     {"langremap",  "lrm",   P_BOOL|P_VI_DEF,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
-                            {(char_u *)TRUE, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)TRUE, (char_u *)0L}   },
     {"laststatus",  "ls",   P_NUM|P_VI_DEF|P_RALL,
                             (char_u *)&p_ls, PV_NONE, did_set_laststatus, NULL,
-                            {(char_u *)1L, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)1L, (char_u *)0L}   },
     {"lazyredraw",  "lz",   P_BOOL|P_VI_DEF,
                             (char_u *)&p_lz, PV_NONE, NULL, NULL,
-                            {(char_u *)TRUE, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)TRUE, (char_u *)0L}   },
     {"lhistory", "lhi",     P_NUM|P_VI_DEF,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)0L, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"linebreak",   "lbr",  P_BOOL|P_VI_DEF|P_RWIN,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
-                            {(char_u *)FALSE, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)FALSE, (char_u *)0L}   },
     {"lines",       NULL,   P_NUM|P_NODEFAULT|P_NO_MKRC|P_VI_DEF|P_RCLR,
                             (char_u *)&Rows, PV_NONE, NULL, NULL,
                             {
                             (char_u *)24L,
-                                            (char_u *)0L} SCTX_INIT},
+                                            (char_u *)0L}   },
     {"linespace",   "lsp",  P_NUM|P_VI_DEF|P_RCLR,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)0L, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"lisp",        NULL,   P_BOOL|P_VI_DEF,
-                            (char_u *)&p_lisp, PV_LISP, did_set_lisp, NULL,
-                            {(char_u *)FALSE, (char_u *)0L} SCTX_INIT},
+                            (char_u *)&p_lisp,   (idopt_T)(PV_BUF + (int)(BV_LISP))  , did_set_lisp, NULL,
+                            {(char_u *)FALSE, (char_u *)0L}   },
     {"lispoptions", "lop",  P_STRING|P_ALLOCED|P_VI_DEF|P_ONECOMMA|P_NODUP,
-                            (char_u *)&p_lop, PV_LOP, did_set_lispoptions, expand_set_lispoptions,
-                            {(char_u *)"", (char_u *)0L} SCTX_INIT},
+                            (char_u *)&p_lop,   (idopt_T)(PV_BUF + (int)(BV_LOP))  , did_set_lispoptions, expand_set_lispoptions,
+                            {(char_u *)"", (char_u *)0L}   },
     {"lispwords",   "lw",   P_STRING|P_VI_DEF|P_ONECOMMA|P_NODUP,
-                            (char_u *)&p_lispwords, PV_LW, NULL, NULL,
-                            {(char_u *)LISPWORD_VALUE, (char_u *)0L} SCTX_INIT},
+                            (char_u *)&p_lispwords,   (idopt_T)(PV_BOTH + (int)( (idopt_T)(PV_BUF + (int)(BV_LW)) ))  , NULL, NULL,
+                            {(char_u *) "defun,define,defmacro,set!,lambda,if,case,let,flet,let*,letrec,do,do*,define-syntax,let-syntax,letrec-syntax,destructuring-bind,defpackage,defparameter,defstruct,deftype,defvar,do-all-symbols,do-external-symbols,do-symbols,dolist,dotimes,ecase,etypecase,eval-when,labels,macrolet,multiple-value-bind,multiple-value-call,multiple-value-prog1,multiple-value-setq,prog1,progv,typecase,unless,unwind-protect,when,with-input-from-string,with-open-file,with-open-stream,with-output-to-string,with-package-iterator,define-condition,handler-bind,handler-case,restart-bind,restart-case,with-simple-restart,store-value,use-value,muffle-warning,abort,continue,with-slots,with-slots*,with-accessors,with-accessors*,defclass,defmethod,print-unreadable-object" , (char_u *)0L}   },
     {"list",        NULL,   P_BOOL|P_VI_DEF|P_RWIN,
-                            (char_u *)VAR_WIN, PV_LIST, NULL, NULL,
-                            {(char_u *)FALSE, (char_u *)0L} SCTX_INIT},
+                            (char_u *) ((char_u *)-1) ,   (idopt_T)(PV_WIN + (int)(WV_LIST))  , NULL, NULL,
+                            {(char_u *)FALSE, (char_u *)0L}   },
     {"listchars",   "lcs",  P_STRING|P_VI_DEF|P_RALL|P_ONECOMMA|P_NODUP|P_COLON,
-                            (char_u *)&p_lcs, PV_LCS, did_set_chars_option, expand_set_chars_option,
-                            {(char_u *)"eol:$", (char_u *)0L} SCTX_INIT},
+                            (char_u *)&p_lcs,   (idopt_T)(PV_BOTH + (int)( (idopt_T)(PV_WIN + (int)(WV_LCS)) ))  , did_set_chars_option, expand_set_chars_option,
+                            {(char_u *)"eol:$", (char_u *)0L}   },
     {"loadplugins", "lpl",  P_BOOL|P_VI_DEF,
                             (char_u *)&p_lpl, PV_NONE, NULL, NULL,
-                            {(char_u *)TRUE, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)TRUE, (char_u *)0L}   },
     {"luadll",      NULL,   P_STRING|P_EXPAND|P_VI_DEF|P_SECURE,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)"", (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"macatsui",    NULL,   P_BOOL|P_VI_DEF|P_RCLR,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)"", (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"magic",       NULL,   P_BOOL|P_VI_DEF,
                             (char_u *)&p_magic, PV_NONE, NULL, NULL,
-                            {(char_u *)TRUE, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)TRUE, (char_u *)0L}   },
     {"makeef",      "mef",  P_STRING|P_EXPAND|P_VI_DEF|P_SECURE,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)NULL, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"makeencoding","menc", P_STRING|P_VI_DEF,
-                            (char_u *)&p_menc, PV_MENC, did_set_encoding, expand_set_encoding,
+                            (char_u *)&p_menc,   (idopt_T)(PV_BOTH + (int)( (idopt_T)(PV_BUF + (int)(BV_MENC)) ))  , did_set_encoding, expand_set_encoding,
                             {(char_u *)"", (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"makeprg",     "mp",   P_STRING|P_EXPAND|P_VI_DEF|P_SECURE,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)NULL, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"matchpairs",  "mps",  P_STRING|P_ALLOCED|P_VI_DEF|P_ONECOMMA|P_NODUP,
-                            (char_u *)&p_mps, PV_MPS, did_set_matchpairs, NULL,
+                            (char_u *)&p_mps,   (idopt_T)(PV_BUF + (int)(BV_MPS))  , did_set_matchpairs, NULL,
                             {(char_u *)"(:),{:},[:]", (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"matchtime",   "mat",  P_NUM|P_VI_DEF,
                             (char_u *)&p_mat, PV_NONE, NULL, NULL,
-                            {(char_u *)5L, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)5L, (char_u *)0L}   },
     {"maxcombine",  "mco",  P_NUM|P_VI_DEF|P_CURSWANT,
                             (char_u *)&p_mco, PV_NONE, did_set_maxcombine, NULL,
-                            {(char_u *)2, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)2, (char_u *)0L}   },
     {"maxfuncdepth", "mfd", P_NUM|P_VI_DEF,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
-                            {(char_u *)100L, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)100L, (char_u *)0L}   },
     {"maxmapdepth", "mmd",  P_NUM|P_VI_DEF,
                             (char_u *)&p_mmd, PV_NONE, NULL, NULL,
-                            {(char_u *)1000L, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)1000L, (char_u *)0L}   },
     {"maxmem",      "mm",   P_NUM|P_VI_DEF,
                             (char_u *)&p_mm, PV_NONE, NULL, NULL,
-                            {(char_u *)DFLT_MAXMEM, (char_u *)0L}
-                            SCTX_INIT},
+                            {(char_u *) (5*1024) , (char_u *)0L}
+                              },
     {"maxmempattern","mmp", P_NUM|P_VI_DEF,
                             (char_u *)&p_mmp, PV_NONE, NULL, NULL,
-                            {(char_u *)1000L, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)1000L, (char_u *)0L}   },
     {"maxmemtot",   "mmt",  P_NUM|P_VI_DEF,
                             (char_u *)&p_mmt, PV_NONE, NULL, NULL,
-                            {(char_u *)DFLT_MAXMEMTOT, (char_u *)0L}
-                            SCTX_INIT},
+                            {(char_u *) (10*1024) , (char_u *)0L}
+                              },
     {"maxsearchcount", "msc", P_NUM|P_VI_DEF,
                             (char_u *)&p_msc, PV_NONE, did_set_maxsearchcount, NULL,
-                            {(char_u *)99L, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)99L, (char_u *)0L}   },
     {"menuitems",   "mis",  P_NUM|P_VI_DEF,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
-                            {(char_u *)25L, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)25L, (char_u *)0L}   },
     {"mesg",        NULL,   P_BOOL|P_VI_DEF,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
-                            {(char_u *)FALSE, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)FALSE, (char_u *)0L}   },
     {"messagesopt","mopt",  P_STRING|P_ALLOCED|P_VI_DEF|P_ONECOMMA|P_COLON|P_NODUP,
                             (char_u *)&p_mopt, PV_NONE, did_set_messagesopt, expand_set_messagesopt,
-                            {(char_u *)"hit-enter,history:500", (char_u *)NULL} SCTX_INIT},
+                            {(char_u *)"hit-enter,history:500", (char_u *)NULL}   },
     {"mkspellmem",  "msm",  P_STRING|P_VI_DEF|P_EXPAND|P_SECURE,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)0L, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"modeline",    "ml",   P_BOOL|P_VIM,
-                            (char_u *)&p_ml, PV_ML, NULL, NULL,
-                            {(char_u *)FALSE, (char_u *)TRUE} SCTX_INIT},
+                            (char_u *)&p_ml,   (idopt_T)(PV_BUF + (int)(BV_ML))  , NULL, NULL,
+                            {(char_u *)FALSE, (char_u *)TRUE}   },
     {"modelineexpr", "mle",  P_BOOL|P_VI_DEF|P_SECURE,
                             (char_u *)&p_mle, PV_NONE, NULL, NULL,
-                            {(char_u *)FALSE, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)FALSE, (char_u *)0L}   },
     {"modelines",   "mls",  P_NUM|P_VI_DEF,
                             (char_u *)&p_mls, PV_NONE, NULL, NULL,
-                            {(char_u *)5L, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)5L, (char_u *)0L}   },
     {"modelinestrict", "mlst", P_BOOL|P_VI_DEF|P_SECURE,
                             (char_u *)&p_mlstr, PV_NONE, NULL, NULL,
-                            {(char_u *)TRUE, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)TRUE, (char_u *)0L}   },
     {"modifiable",  "ma",   P_BOOL|P_VI_DEF|P_NOGLOB,
-                            (char_u *)&p_ma, PV_MA, did_set_modifiable, NULL,
-                            {(char_u *)TRUE, (char_u *)0L} SCTX_INIT},
+                            (char_u *)&p_ma,   (idopt_T)(PV_BUF + (int)(BV_MA))  , did_set_modifiable, NULL,
+                            {(char_u *)TRUE, (char_u *)0L}   },
     {"modified",    "mod",  P_BOOL|P_NO_MKRC|P_VI_DEF|P_RSTAT,
-                            (char_u *)&p_mod, PV_MOD, did_set_modified, NULL,
-                            {(char_u *)FALSE, (char_u *)0L} SCTX_INIT},
+                            (char_u *)&p_mod,   (idopt_T)(PV_BUF + (int)(BV_MOD))  , did_set_modified, NULL,
+                            {(char_u *)FALSE, (char_u *)0L}   },
     {"more",        NULL,   P_BOOL|P_VIM,
                             (char_u *)&p_more, PV_NONE, NULL, NULL,
-                            {(char_u *)FALSE, (char_u *)TRUE} SCTX_INIT},
+                            {(char_u *)FALSE, (char_u *)TRUE}   },
     {"mouse",       NULL,   P_STRING|P_VI_DEF|P_FLAGLIST,
                             (char_u *)&p_mouse, PV_NONE, did_set_mouse, expand_set_mouse,
                             {
                                 (char_u *)"",
-                                (char_u *)0L} SCTX_INIT},
+                                (char_u *)0L}   },
     {"mousefocus",   "mousef", P_BOOL|P_VI_DEF,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
-                            {(char_u *)FALSE, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)FALSE, (char_u *)0L}   },
     {"mousehide",   "mh",   P_BOOL|P_VI_DEF,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
-                            {(char_u *)TRUE, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)TRUE, (char_u *)0L}   },
     {"mousemodel",  "mousem", P_STRING|P_VI_DEF,
                             (char_u *)&p_mousem, PV_NONE, did_set_mousemodel, expand_set_mousemodel,
                             {
                                 (char_u *)"extend",
-                                (char_u *)0L} SCTX_INIT},
+                                (char_u *)0L}   },
     {"mousemoveevent",   "mousemev",   P_BOOL|P_VI_DEF,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
-                            {(char_u *)FALSE, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)FALSE, (char_u *)0L}   },
     {"mouseshape",  "mouses",  P_STRING|P_VI_DEF|P_ONECOMMA|P_NODUP,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)NULL, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"mousetime",   "mouset",   P_NUM|P_VI_DEF,
                             (char_u *)&p_mouset, PV_NONE, NULL, NULL,
-                            {(char_u *)500L, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)500L, (char_u *)0L}   },
     {"mzquantum",  "mzq",   P_NUM,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
-                            {(char_u *)100L, (char_u *)100L} SCTX_INIT},
+                            {(char_u *)100L, (char_u *)100L}   },
     {"mzschemedll", NULL,   P_STRING|P_EXPAND|P_VI_DEF|P_SECURE,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)"", (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"mzschemegcdll", NULL, P_STRING|P_EXPAND|P_VI_DEF|P_SECURE,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)"", (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"novice",      NULL,   P_BOOL|P_VI_DEF,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
-                            {(char_u *)FALSE, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)FALSE, (char_u *)0L}   },
     {"nrformats",   "nf",   P_STRING|P_ALLOCED|P_VI_DEF|P_ONECOMMA|P_NODUP,
-                            (char_u *)&p_nf, PV_NF, did_set_nrformats, expand_set_nrformats,
+                            (char_u *)&p_nf,   (idopt_T)(PV_BUF + (int)(BV_NF))  , did_set_nrformats, expand_set_nrformats,
                             {(char_u *)"bin,octal,hex", (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"number",      "nu",   P_BOOL|P_VI_DEF|P_RWIN,
-                            (char_u *)VAR_WIN, PV_NU,
+                            (char_u *) ((char_u *)-1) ,   (idopt_T)(PV_WIN + (int)(WV_NU))  ,
                             did_set_number_relativenumber, NULL,
-                            {(char_u *)FALSE, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)FALSE, (char_u *)0L}   },
     {"numberwidth", "nuw",  P_NUM|P_RWIN|P_VIM,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
-                            {(char_u *)8L, (char_u *)4L} SCTX_INIT},
+                            {(char_u *)8L, (char_u *)4L}   },
     {"omnifunc",    "ofu",  P_STRING|P_ALLOCED|P_VI_DEF|P_SECURE|P_FUNC,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)0L, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"open",        NULL,   P_BOOL|P_VI_DEF,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
-                            {(char_u *)FALSE, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)FALSE, (char_u *)0L}   },
     {"opendevice",  "odev", P_BOOL|P_VI_DEF,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)FALSE, (char_u *)FALSE}
-                            SCTX_INIT},
+                              },
     {"operatorfunc", "opfunc", P_STRING|P_VI_DEF|P_SECURE|P_FUNC,
                             (char_u *)&p_opfunc, PV_NONE,
                             did_set_operatorfunc, NULL,
-                            {(char_u *)"", (char_u *)0L} SCTX_INIT},
+                            {(char_u *)"", (char_u *)0L}   },
     {"optimize",    "opt",  P_BOOL|P_VI_DEF,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
-                            {(char_u *)FALSE, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)FALSE, (char_u *)0L}   },
     {"osctimeoutlen", "ost", P_NUM|P_VI_DEF,
                             (char_u *)&p_ost, PV_NONE, did_set_osctimeoutlen, NULL,
-                            {(char_u *)1000, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)1000, (char_u *)0L}   },
     {"osfiletype",  "oft",  P_STRING|P_ALLOCED|P_VI_DEF,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
-                            {(char_u *)0L, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)0L, (char_u *)0L}   },
     {"packpath",    "pp",   P_STRING|P_VI_DEF|P_EXPAND|P_ONECOMMA|P_NODUP
                                                                     |P_SECURE,
                             (char_u *)&p_pp, PV_NONE, NULL, NULL,
-                            {(char_u *)DFLT_RUNTIMEPATH, (char_u *)0L}
-                            SCTX_INIT},
+                            {(char_u *) "~/.vim,$VIM/vimfiles,$VIMRUNTIME,$VIM/vimfiles/after,~/.vim/after" , (char_u *)0L}
+                              },
     {"paragraphs",  "para", P_STRING|P_VI_DEF,
                             (char_u *)&p_para, PV_NONE, NULL, NULL,
                             {(char_u *)"IPLPPPQPP TPHPLIPpLpItpplpipbp",
-                                (char_u *)0L} SCTX_INIT},
+                                (char_u *)0L}   },
     {"paste",       NULL,   P_BOOL|P_VI_DEF|P_PRI_MKRC,
                             (char_u *)&p_paste, PV_NONE, did_set_paste, NULL,
-                            {(char_u *)FALSE, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)FALSE, (char_u *)0L}   },
     {"pastetoggle", "pt",   P_STRING|P_VI_DEF,
                             (char_u *)&p_pt, PV_NONE, did_set_pastetoggle, NULL,
-                            {(char_u *)"", (char_u *)0L} SCTX_INIT},
+                            {(char_u *)"", (char_u *)0L}   },
     {"patchexpr",   "pex",  P_STRING|P_VI_DEF|P_SECURE,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)0L, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"patchmode",   "pm",   P_STRING|P_VI_DEF|P_NFNAME,
                             (char_u *)&p_pm, PV_NONE,
                             did_set_backupext_or_patchmode, NULL,
-                            {(char_u *)"", (char_u *)0L} SCTX_INIT},
+                            {(char_u *)"", (char_u *)0L}   },
     {"path",        "pa",   P_STRING|P_EXPAND|P_VI_DEF|P_COMMA|P_NODUP,
-                            (char_u *)&p_path, PV_PATH, NULL, NULL,
+                            (char_u *)&p_path,   (idopt_T)(PV_BOTH + (int)( (idopt_T)(PV_BUF + (int)(BV_PATH)) ))  , NULL, NULL,
                             {
                             (char_u *)".,/usr/include,,",
-                                (char_u *)0L} SCTX_INIT},
+                                (char_u *)0L}   },
     {"perldll",     NULL,   P_STRING|P_EXPAND|P_VI_DEF|P_SECURE,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)0L, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"preserveindent", "pi", P_BOOL|P_VI_DEF|P_VIM,
-                            (char_u *)&p_pi, PV_PI, NULL, NULL,
-                            {(char_u *)FALSE, (char_u *)0L} SCTX_INIT},
+                            (char_u *)&p_pi,   (idopt_T)(PV_BUF + (int)(BV_PI))  , NULL, NULL,
+                            {(char_u *)FALSE, (char_u *)0L}   },
     {"previewheight", "pvh", P_NUM|P_VI_DEF,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
-                            {(char_u *)12L, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)12L, (char_u *)0L}   },
     {"previewpopup", "pvp", P_STRING|P_VI_DEF|P_COMMA|P_NODUP|P_COLON,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)NULL, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"previewwindow", "pvw", P_BOOL|P_VI_DEF|P_RSTAT|P_NOGLOB,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
-                            {(char_u *)FALSE, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)FALSE, (char_u *)0L}   },
     {"printdevice", "pdev", P_STRING|P_VI_DEF|P_SECURE,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)NULL, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"printencoding", "penc", P_STRING|P_VI_DEF,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)NULL, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"printexpr", "pexpr",  P_STRING|P_VI_DEF|P_SECURE,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)NULL, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"printfont", "pfn",    P_STRING|P_VI_DEF,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)NULL, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"printheader", "pheader",  P_STRING|P_VI_DEF|P_GETTEXT|P_MLE,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)NULL, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"printmbcharset", "pmbcs",  P_STRING|P_VI_DEF,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)NULL, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"printmbfont", "pmbfn",  P_STRING|P_VI_DEF,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)NULL, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"printoptions", "popt", P_STRING|P_VI_DEF|P_ONECOMMA|P_NODUP,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)NULL, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"prompt",      NULL,   P_BOOL|P_VI_DEF,
                             (char_u *)&p_prompt, PV_NONE, NULL, NULL,
-                            {(char_u *)TRUE, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)TRUE, (char_u *)0L}   },
     {"pumborder",   "pb",   P_STRING|P_VI_DEF|P_COMMA|P_NODUP|P_COLON|P_SECURE,
                             (char_u *)&p_pb, PV_NONE, did_set_pumborder, expand_set_pumborder,
-                            {(char_u *)"", (char_u *)NULL} SCTX_INIT},
+                            {(char_u *)"", (char_u *)NULL}   },
     {"pumheight",   "ph",   P_NUM|P_VI_DEF,
                             (char_u *)&p_ph, PV_NONE, NULL, NULL,
-                            {(char_u *)0L, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)0L, (char_u *)0L}   },
     {"pummaxwidth", "pmw",   P_NUM|P_VI_DEF,
                             (char_u *)&p_pmw, PV_NONE, NULL, NULL,
-                            {(char_u *)0L, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)0L, (char_u *)0L}   },
     {"pumopt",      NULL,   P_STRING|P_VI_DEF|P_COMMA|P_NODUP|P_COLON,
                             (char_u *)&p_pumopt, PV_NONE,
                             did_set_pumopt, expand_set_pumopt,
-                            {(char_u *)"", (char_u *)NULL} SCTX_INIT},
+                            {(char_u *)"", (char_u *)NULL}   },
     {"pumwidth",    "pw",   P_NUM|P_VI_DEF,
                             (char_u *)&p_pw, PV_NONE, NULL, NULL,
-                            {(char_u *)15L, (char_u *)15L} SCTX_INIT},
+                            {(char_u *)15L, (char_u *)15L}   },
     {"pythondll",   NULL,   P_STRING|P_EXPAND|P_VI_DEF|P_SECURE,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)NULL, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"pythonhome",  NULL,   P_STRING|P_EXPAND|P_VI_DEF|P_SECURE,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)NULL, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"pythonthreedll",  NULL,   P_STRING|P_EXPAND|P_VI_DEF|P_SECURE,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)NULL, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"pythonthreehome", NULL,   P_STRING|P_EXPAND|P_VI_DEF|P_SECURE,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)NULL, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"pyxversion", "pyx",   P_NUM|P_VI_DEF|P_SECURE,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)DEFAULT_PYTHON_VER, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"quickfixtextfunc", "qftf", P_STRING|P_ALLOCED|P_VI_DEF|P_VIM|P_SECURE|P_FUNC,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)NULL, (char_u *)NULL}
-                            SCTX_INIT},
+                              },
     {"quoteescape", "qe",   P_STRING|P_ALLOCED|P_VI_DEF,
-                            (char_u *)&p_qe, PV_QE, NULL, NULL,
-                            {(char_u *)"\\", (char_u *)0L} SCTX_INIT},
+                            (char_u *)&p_qe,   (idopt_T)(PV_BUF + (int)(BV_QE))  , NULL, NULL,
+                            {(char_u *)"\\", (char_u *)0L}   },
     {"readonly",    "ro",   P_BOOL|P_VI_DEF|P_RSTAT|P_NOGLOB,
-                            (char_u *)&p_ro, PV_RO, did_set_readonly, NULL,
-                            {(char_u *)FALSE, (char_u *)0L} SCTX_INIT},
+                            (char_u *)&p_ro,   (idopt_T)(PV_BUF + (int)(BV_RO))  , did_set_readonly, NULL,
+                            {(char_u *)FALSE, (char_u *)0L}   },
     {"redraw",      NULL,   P_BOOL|P_VI_DEF,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
-                            {(char_u *)FALSE, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)FALSE, (char_u *)0L}   },
     {"redrawtime",  "rdt",  P_NUM|P_VI_DEF,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
-                            {(char_u *)2000L, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)2000L, (char_u *)0L}   },
     {"regexpengine", "re",  P_NUM|P_VI_DEF,
                             (char_u *)&p_re, PV_NONE, NULL, NULL,
-                            {(char_u *)1L, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)1L, (char_u *)0L}   },
     {"relativenumber", "rnu", P_BOOL|P_VI_DEF|P_RWIN,
-                            (char_u *)VAR_WIN, PV_RNU,
+                            (char_u *) ((char_u *)-1) ,   (idopt_T)(PV_WIN + (int)(WV_RNU))  ,
                             did_set_number_relativenumber, NULL,
-                            {(char_u *)FALSE, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)FALSE, (char_u *)0L}   },
     {"remap",       NULL,   P_BOOL|P_VI_DEF,
                             (char_u *)&p_remap, PV_NONE, NULL, NULL,
-                            {(char_u *)TRUE, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)TRUE, (char_u *)0L}   },
     {"renderoptions", "rop", P_STRING|P_ONECOMMA|P_RCLR|P_VI_DEF,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)NULL, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"report",      NULL,   P_NUM|P_VI_DEF,
                             (char_u *)&p_report, PV_NONE, NULL, NULL,
-                            {(char_u *)2L, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)2L, (char_u *)0L}   },
     {"restorescreen", "rs", P_BOOL|P_VI_DEF,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
-                            {(char_u *)TRUE, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)TRUE, (char_u *)0L}   },
     {"revins",      "ri",   P_BOOL|P_VI_DEF|P_VIM,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
-                            {(char_u *)FALSE, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)FALSE, (char_u *)0L}   },
     {"rightleft",   "rl",   P_BOOL|P_VI_DEF|P_RWIN,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
-                            {(char_u *)FALSE, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)FALSE, (char_u *)0L}   },
     {"rightleftcmd", "rlc", P_STRING|P_ALLOCED|P_VI_DEF|P_RWIN,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)NULL, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"rubydll",     NULL,   P_STRING|P_EXPAND|P_VI_DEF|P_SECURE,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)NULL, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"ruler",       "ru",   P_BOOL|P_VIM|P_RSTAT,
                             (char_u *)&p_ru, PV_NONE, NULL, NULL,
-                            {(char_u *)TRUE, (char_u *)TRUE} SCTX_INIT},
+                            {(char_u *)TRUE, (char_u *)TRUE}   },
     {"rulerformat", "ruf",  P_STRING|P_VI_DEF|P_ALLOCED|P_RSTAT|P_MLE,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
-                            {(char_u *)"", (char_u *)0L} SCTX_INIT},
+                            {(char_u *)"", (char_u *)0L}   },
     {"runtimepath", "rtp",  P_STRING|P_VI_DEF|P_EXPAND|P_ONECOMMA|P_NODUP
                                                                     |P_SECURE,
                             (char_u *)&p_rtp, PV_NONE, NULL, NULL,
-                            {(char_u *)DFLT_RUNTIMEPATH, (char_u *)0L}
-                            SCTX_INIT},
+                            {(char_u *) "~/.vim,$VIM/vimfiles,$VIMRUNTIME,$VIM/vimfiles/after,~/.vim/after" , (char_u *)0L}
+                              },
     {"scroll",      "scr",  P_NUM|P_NO_MKRC|P_VI_DEF,
-                            (char_u *)VAR_WIN, PV_SCROLL, NULL, NULL,
-                            {(char_u *)0L, (char_u *)0L} SCTX_INIT},
+                            (char_u *) ((char_u *)-1) ,   (idopt_T)(PV_WIN + (int)(WV_SCROLL))  , NULL, NULL,
+                            {(char_u *)0L, (char_u *)0L}   },
     {"scrollbind",  "scb",  P_BOOL|P_VI_DEF,
-                            (char_u *)VAR_WIN, PV_SCBIND, did_set_scrollbind, NULL,
-                            {(char_u *)FALSE, (char_u *)0L} SCTX_INIT},
+                            (char_u *) ((char_u *)-1) ,   (idopt_T)(PV_WIN + (int)(WV_SCBIND))  , did_set_scrollbind, NULL,
+                            {(char_u *)FALSE, (char_u *)0L}   },
     {"scrollfocus", "scf",  P_BOOL|P_VI_DEF,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
-                            {(char_u *)FALSE, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)FALSE, (char_u *)0L}   },
     {"scrolljump",  "sj",   P_NUM|P_VI_DEF|P_VIM,
                             (char_u *)&p_sj, PV_NONE, NULL, NULL,
-                            {(char_u *)1L, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)1L, (char_u *)0L}   },
     {"scrolloff",   "so",   P_NUM|P_VI_DEF|P_VIM|P_RALL,
-                            (char_u *)&p_so, PV_SO, NULL, NULL,
-                            {(char_u *)1L, (char_u *)0L} SCTX_INIT},
+                            (char_u *)&p_so,   (idopt_T)(PV_BOTH + (int)( (idopt_T)(PV_WIN + (int)(WV_SO)) ))  , NULL, NULL,
+                            {(char_u *)1L, (char_u *)0L}   },
     {"scrolloffpad", "sop", P_NUM|P_VI_DEF|P_VIM|P_RALL,
-                                (char_u *)&p_sop, PV_SOP, NULL, NULL,
-                                {(char_u *)0L, (char_u *)0L} SCTX_INIT},
+                                (char_u *)&p_sop,   (idopt_T)(PV_BOTH + (int)( (idopt_T)(PV_WIN + (int)(WV_SOP)) ))  , NULL, NULL,
+                                {(char_u *)0L, (char_u *)0L}   },
     {"scrollopt",   "sbo",  P_STRING|P_VI_DEF|P_ONECOMMA|P_NODUP,
                             (char_u *)&p_sbo, PV_NONE, did_set_scrollopt, expand_set_scrollopt,
                             {(char_u *)"ver,jump", (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"sections",    "sect", P_STRING|P_VI_DEF,
                             (char_u *)&p_sections, PV_NONE, NULL, NULL,
                             {(char_u *)"SHNHH HUnhsh", (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"secure",      NULL,   P_BOOL|P_VI_DEF|P_SECURE,
                             (char_u *)&p_secure, PV_NONE, NULL, NULL,
-                            {(char_u *)FALSE, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)FALSE, (char_u *)0L}   },
     {"selection",   "sel",  P_STRING|P_VI_DEF,
                             (char_u *)&p_sel, PV_NONE, did_set_selection, expand_set_selection,
                             {(char_u *)"inclusive", (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"selectmode",  "slm",  P_STRING|P_VI_DEF|P_ONECOMMA|P_NODUP,
                             (char_u *)&p_slm, PV_NONE, did_set_selectmode, expand_set_selectmode,
-                            {(char_u *)"", (char_u *)0L} SCTX_INIT},
+                            {(char_u *)"", (char_u *)0L}   },
     {"sessionoptions", "ssop", P_STRING|P_VI_DEF|P_ONECOMMA|P_NODUP,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)0L, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"shell",       "sh",   P_STRING|P_EXPAND|P_VI_DEF|P_SECURE,
                             (char_u *)&p_sh, PV_NONE, NULL, NULL,
                             {
                             (char_u *)"sh",
-                                (char_u *)0L} SCTX_INIT},
+                                (char_u *)0L}   },
     {"shellcmdflag","shcf", P_STRING|P_VI_DEF|P_SECURE,
                             (char_u *)&p_shcf, PV_NONE, NULL, NULL,
                             {
                             (char_u *)"-c",
-                                (char_u *)0L} SCTX_INIT},
+                                (char_u *)0L}   },
     {"shellpipe",   "sp",   P_STRING|P_VI_DEF|P_SECURE,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)0L, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"shellquote",  "shq",  P_STRING|P_VI_DEF|P_SECURE,
                             (char_u *)&p_shq, PV_NONE, NULL, NULL,
-                            {(char_u *)"", (char_u *)0L} SCTX_INIT},
+                            {(char_u *)"", (char_u *)0L}   },
     {"shellredir",  "srr",  P_STRING|P_VI_DEF|P_SECURE,
                             (char_u *)&p_srr, PV_NONE, did_set_shellpipe_redir, NULL,
-                            {(char_u *)">", (char_u *)0L} SCTX_INIT},
+                            {(char_u *)">", (char_u *)0L}   },
     {"shellslash",  "ssl",   P_BOOL|P_VI_DEF,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
-                            {(char_u *)FALSE, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)FALSE, (char_u *)0L}   },
     {"shelltemp",   "stmp", P_BOOL,
                             (char_u *)&p_stmp, PV_NONE, NULL, NULL,
-                            {(char_u *)FALSE, (char_u *)TRUE} SCTX_INIT},
+                            {(char_u *)FALSE, (char_u *)TRUE}   },
     {"shelltype",   "st",   P_NUM|P_VI_DEF,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
-                            {(char_u *)0L, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)0L, (char_u *)0L}   },
     {"shellxescape", "sxe", P_STRING|P_VI_DEF|P_SECURE,
                             (char_u *)&p_sxe, PV_NONE, NULL, NULL,
                             {
                             (char_u *)"",
-                                (char_u *)0L} SCTX_INIT},
+                                (char_u *)0L}   },
     {"shellxquote", "sxq",  P_STRING|P_VI_DEF|P_SECURE,
                             (char_u *)&p_sxq, PV_NONE, NULL, NULL,
                             {
                             (char_u *)"",
-                                (char_u *)0L} SCTX_INIT},
+                                (char_u *)0L}   },
     {"shiftround",  "sr",   P_BOOL|P_VI_DEF|P_VIM,
                             (char_u *)&p_sr, PV_NONE, NULL, NULL,
-                            {(char_u *)TRUE, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)TRUE, (char_u *)0L}   },
     {"shiftwidth",  "sw",   P_NUM|P_VI_DEF,
-                            (char_u *)&p_sw, PV_SW,
+                            (char_u *)&p_sw,   (idopt_T)(PV_BUF + (int)(BV_SW))  ,
                             did_set_shiftwidth_tabstop, NULL,
-                            {(char_u *)4L, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)4L, (char_u *)0L}   },
     {"shortmess",   "shm",  P_STRING|P_VIM|P_FLAGLIST,
                             (char_u *)&p_shm, PV_NONE, did_set_shortmess, expand_set_shortmess,
                             {(char_u *)"S", (char_u *)"filnxtToOS"}
-                            SCTX_INIT},
+                              },
     {"shortname",   "sn",   P_BOOL|P_VI_DEF,
-                            (char_u *)&p_sn, PV_SN, NULL, NULL,
-                            {(char_u *)FALSE, (char_u *)0L} SCTX_INIT},
+                            (char_u *)&p_sn,   (idopt_T)(PV_BUF + (int)(BV_SN))  , NULL, NULL,
+                            {(char_u *)FALSE, (char_u *)0L}   },
     {"showbreak",   "sbr",  P_STRING|P_VI_DEF|P_RALL,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
-                            {(char_u *)"", (char_u *)0L} SCTX_INIT},
+                            {(char_u *)"", (char_u *)0L}   },
     {"showcmd",     "sc",   P_BOOL|P_VIM,
                             (char_u *)&p_sc, PV_NONE, NULL, NULL,
-                            {(char_u *)FALSE, (char_u *)TRUE} SCTX_INIT},
+                            {(char_u *)FALSE, (char_u *)TRUE}   },
     {"showcmdloc",  "sloc", P_STRING|P_RSTAT,
                             (char_u *)&p_sloc, PV_NONE, did_set_showcmdloc, expand_set_showcmdloc,
-                            {(char_u *)"last", (char_u *)"last"} SCTX_INIT},
+                            {(char_u *)"last", (char_u *)"last"}   },
     {"showfulltag", "sft",  P_BOOL|P_VI_DEF,
                             (char_u *)&p_sft, PV_NONE, NULL, NULL,
-                            {(char_u *)FALSE, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)FALSE, (char_u *)0L}   },
     {"showmatch",   "sm",   P_BOOL|P_VI_DEF,
                             (char_u *)&p_sm, PV_NONE, NULL, NULL,
-                            {(char_u *)FALSE, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)FALSE, (char_u *)0L}   },
     {"showmode",    "smd",  P_BOOL|P_VIM,
                             (char_u *)&p_smd, PV_NONE, NULL, NULL,
-                            {(char_u *)FALSE, (char_u *)TRUE} SCTX_INIT},
+                            {(char_u *)FALSE, (char_u *)TRUE}   },
     {"showtabline", "stal", P_NUM|P_VI_DEF|P_RALL,
                             (char_u *)&p_stal, PV_NONE, did_set_showtabline, NULL,
-                            {(char_u *)1L, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)1L, (char_u *)0L}   },
     {"sidescroll",  "ss",   P_NUM|P_VI_DEF,
                             (char_u *)&p_ss, PV_NONE, NULL, NULL,
-                            {(char_u *)0L, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)0L, (char_u *)0L}   },
     {"sidescrolloff", "siso", P_NUM|P_VI_DEF|P_VIM|P_RBUF,
-                            (char_u *)&p_siso, PV_SISO, NULL, NULL,
-                            {(char_u *)0L, (char_u *)0L} SCTX_INIT},
+                            (char_u *)&p_siso,   (idopt_T)(PV_BOTH + (int)( (idopt_T)(PV_WIN + (int)(WV_SISO)) ))  , NULL, NULL,
+                            {(char_u *)0L, (char_u *)0L}   },
     {"signcolumn",   "scl",  P_STRING|P_ALLOCED|P_VI_DEF|P_RWIN,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)NULL, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"slowopen",    "slow", P_BOOL|P_VI_DEF,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
-                            {(char_u *)FALSE, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)FALSE, (char_u *)0L}   },
     {"smartcase",   "scs",  P_BOOL|P_VI_DEF|P_VIM,
                             (char_u *)&p_scs, PV_NONE, NULL, NULL,
-                            {(char_u *)FALSE, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)FALSE, (char_u *)0L}   },
     {"smartindent", "si",   P_BOOL|P_VI_DEF|P_VIM,
-                            (char_u *)&p_si, PV_SI, NULL, NULL,
-                            {(char_u *)TRUE, (char_u *)0L} SCTX_INIT},
+                            (char_u *)&p_si,   (idopt_T)(PV_BUF + (int)(BV_SI))  , NULL, NULL,
+                            {(char_u *)TRUE, (char_u *)0L}   },
     {"smarttab",    "sta",  P_BOOL|P_VI_DEF|P_VIM,
                             (char_u *)&p_sta, PV_NONE, NULL, NULL,
-                            {(char_u *)TRUE, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)TRUE, (char_u *)0L}   },
     {"smoothscroll", "sms", P_BOOL|P_VI_DEF|P_RWIN,
-                            (char_u *)VAR_WIN, PV_SMS, did_set_smoothscroll, NULL,
-                            {(char_u *)FALSE, (char_u *)0L} SCTX_INIT},
+                            (char_u *) ((char_u *)-1) ,   (idopt_T)(PV_WIN + (int)(WV_SMS))  , did_set_smoothscroll, NULL,
+                            {(char_u *)FALSE, (char_u *)0L}   },
     {"softtabstop", "sts",  P_NUM|P_VI_DEF|P_VIM,
-                            (char_u *)&p_sts, PV_STS, NULL, NULL,
-                            {(char_u *)4L, (char_u *)0L} SCTX_INIT},
+                            (char_u *)&p_sts,   (idopt_T)(PV_BUF + (int)(BV_STS))  , NULL, NULL,
+                            {(char_u *)4L, (char_u *)0L}   },
     {"sourceany",   NULL,   P_BOOL|P_VI_DEF,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
-                            {(char_u *)FALSE, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)FALSE, (char_u *)0L}   },
     {"spell",       NULL,   P_BOOL|P_VI_DEF|P_RWIN|P_HLONLY,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
-                            {(char_u *)FALSE, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)FALSE, (char_u *)0L}   },
     {"spellcapcheck", "spc", P_STRING|P_ALLOCED|P_VI_DEF|P_RBUF|P_HLONLY,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)0L, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"spellfile",   "spf",  P_STRING|P_EXPAND|P_ALLOCED|P_VI_DEF|P_SECURE
                                                                   |P_ONECOMMA,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)0L, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"spelllang",   "spl",  P_STRING|P_ALLOCED|P_VI_DEF|P_ONECOMMA
                                                      |P_RBUF|P_HLONLY|P_EXPAND,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)0L, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"spelloptions", "spo",  P_STRING|P_ALLOCED|P_VI_DEF
                                            |P_ONECOMMA|P_NODUP|P_RBUF|P_HLONLY,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)0L, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"spellsuggest", "sps", P_STRING|P_VI_DEF|P_EXPAND|P_SECURE|P_ONECOMMA,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)0L, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"splitbelow",  "sb",   P_BOOL|P_VI_DEF,
                             (char_u *)&p_sb, PV_NONE, NULL, NULL,
-                            {(char_u *)FALSE, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)FALSE, (char_u *)0L}   },
     {"splitkeep",   "spk",  P_STRING,
                             (char_u *)&p_spk, PV_NONE, did_set_splitkeep, expand_set_splitkeep,
-                            {(char_u *)"cursor", (char_u *)"cursor"} SCTX_INIT},
+                            {(char_u *)"cursor", (char_u *)"cursor"}   },
     {"splitright",  "spr",  P_BOOL|P_VI_DEF,
                             (char_u *)&p_spr, PV_NONE, NULL, NULL,
-                            {(char_u *)FALSE, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)FALSE, (char_u *)0L}   },
     {"startofline", "sol",  P_BOOL|P_VI_DEF|P_VIM,
                             (char_u *)&p_sol, PV_NONE, NULL, NULL,
-                            {(char_u *)TRUE, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)TRUE, (char_u *)0L}   },
     {"statusline"  ,"stl",  P_STRING|P_VI_DEF|P_ALLOCED|P_RSTAT|P_MLE,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
-                            {(char_u *)"", (char_u *)0L} SCTX_INIT},
+                            {(char_u *)"", (char_u *)0L}   },
     {"statuslineopt"  ,"stlo",  P_STRING|P_VI_DEF|P_ALLOCED|P_RSTAT|P_MLE
                             |P_ONECOMMA|P_COLON|P_NODUP,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
-                            {(char_u *)"", (char_u *)0L} SCTX_INIT},
+                            {(char_u *)"", (char_u *)0L}   },
     {"suffixes",    "su",   P_STRING|P_VI_DEF|P_ONECOMMA|P_NODUP,
                             (char_u *)&p_su, PV_NONE, NULL, NULL,
                             {(char_u *)".bak,~,.o,.h,.info,.swp,.obj",
-                                (char_u *)0L} SCTX_INIT},
+                                (char_u *)0L}   },
     {"suffixesadd", "sua",  P_STRING|P_VI_DEF|P_ALLOCED|P_ONECOMMA|P_NODUP,
-                            (char_u *)&p_sua, PV_SUA, NULL, NULL,
+                            (char_u *)&p_sua,   (idopt_T)(PV_BUF + (int)(BV_SUA))  , NULL, NULL,
                             {(char_u *)"", (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"swapfile",    "swf",  P_BOOL|P_VI_DEF|P_RSTAT,
-                            (char_u *)&p_swf, PV_SWF, did_set_swapfile, NULL,
-                            {(char_u *)TRUE, (char_u *)0L} SCTX_INIT},
+                            (char_u *)&p_swf,   (idopt_T)(PV_BUF + (int)(BV_SWF))  , did_set_swapfile, NULL,
+                            {(char_u *)TRUE, (char_u *)0L}   },
     {"swapsync",    "sws",  P_STRING|P_VI_DEF,
                             (char_u *)&p_sws, PV_NONE, did_set_swapsync, expand_set_swapsync,
-                            {(char_u *)"fsync", (char_u *)0L} SCTX_INIT},
+                            {(char_u *)"fsync", (char_u *)0L}   },
     {"switchbuf",   "swb",  P_STRING|P_VI_DEF|P_ONECOMMA|P_NODUP,
                             (char_u *)&p_swb, PV_NONE, did_set_switchbuf, expand_set_switchbuf,
-                            {(char_u *)"", (char_u *)0L} SCTX_INIT},
+                            {(char_u *)"", (char_u *)0L}   },
     {"synmaxcol",   "smc",  P_NUM|P_VI_DEF|P_RBUF,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)0L, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"syntax",      "syn",  P_STRING|P_ALLOCED|P_VI_DEF|P_NOGLOB|P_NFNAME,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)0L, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"tabclose",    "tcl",  P_STRING|P_VI_DEF|P_ONECOMMA|P_NODUP,
                             (char_u *)&p_tcl, PV_NONE, did_set_tabclose, expand_set_tabclose,
-                            {(char_u *)"", (char_u *)0L} SCTX_INIT},
+                            {(char_u *)"", (char_u *)0L}   },
     {"tabline",     "tal",  P_STRING|P_VI_DEF|P_RALL|P_MLE,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
-                            {(char_u *)"", (char_u *)0L} SCTX_INIT},
+                            {(char_u *)"", (char_u *)0L}   },
     {"tabpagemax",  "tpm",  P_NUM|P_VI_DEF,
                             (char_u *)&p_tpm, PV_NONE, NULL, NULL,
-                            {(char_u *)10L, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)10L, (char_u *)0L}   },
     {"tabstop",     "ts",   P_NUM|P_VI_DEF|P_RBUF,
-                            (char_u *)&p_ts, PV_TS,
+                            (char_u *)&p_ts,   (idopt_T)(PV_BUF + (int)(BV_TS))  ,
                             did_set_shiftwidth_tabstop, NULL,
-                            {(char_u *)4L, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)4L, (char_u *)0L}   },
     {"tagbsearch",  "tbs",   P_BOOL|P_VI_DEF,
                             (char_u *)&p_tbs, PV_NONE, NULL, NULL,
                             {(char_u *)TRUE, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"tagcase",     "tc",   P_STRING|P_VIM,
-                            (char_u *)&p_tc, PV_TC, did_set_tagcase, expand_set_tagcase,
-                            {(char_u *)"followic", (char_u *)"followic"} SCTX_INIT},
+                            (char_u *)&p_tc,   (idopt_T)(PV_BOTH + (int)( (idopt_T)(PV_BUF + (int)(BV_TC)) ))  , did_set_tagcase, expand_set_tagcase,
+                            {(char_u *)"followic", (char_u *)"followic"}   },
     {"tagfunc",    "tfu",   P_STRING|P_ALLOCED|P_VI_DEF|P_SECURE|P_FUNC,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)0L, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"taglength",   "tl",   P_NUM|P_VI_DEF,
                             (char_u *)&p_tl, PV_NONE, NULL, NULL,
-                            {(char_u *)0L, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)0L, (char_u *)0L}   },
     {"tagrelative", "tr",   P_BOOL|P_VIM,
                             (char_u *)&p_tr, PV_NONE, NULL, NULL,
-                            {(char_u *)FALSE, (char_u *)TRUE} SCTX_INIT},
+                            {(char_u *)FALSE, (char_u *)TRUE}   },
     {"tags",        "tag",  P_STRING|P_EXPAND|P_VI_DEF|P_ONECOMMA|P_NODUP,
-                            (char_u *)&p_tags, PV_TAGS, NULL, NULL,
+                            (char_u *)&p_tags,   (idopt_T)(PV_BOTH + (int)( (idopt_T)(PV_BUF + (int)(BV_TAGS)) ))  , NULL, NULL,
                             {
                             (char_u *)"./tags,tags",
-                                (char_u *)0L} SCTX_INIT},
+                                (char_u *)0L}   },
     {"tagsecure",   "tsc",   P_BOOL|P_VI_DEF|P_SECURE,
                             (char_u *)&p_tagsecure, PV_NONE, NULL, NULL,
-                            {(char_u *)TRUE, (char_u *)TRUE} SCTX_INIT},
+                            {(char_u *)TRUE, (char_u *)TRUE}   },
     {"tagstack",    "tgst", P_BOOL|P_VI_DEF,
                             (char_u *)&p_tgst, PV_NONE, NULL, NULL,
-                            {(char_u *)TRUE, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)TRUE, (char_u *)0L}   },
     {"tcldll",      NULL,   P_STRING|P_EXPAND|P_VI_DEF|P_SECURE,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)0L, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"term",        NULL,   P_STRING|P_EXPAND|P_NODEFAULT|P_NO_MKRC|P_VI_DEF|P_RALL,
-                            (char_u *)&T_NAME, PV_NONE, did_set_term, NULL,
-                            {(char_u *)"", (char_u *)0L} SCTX_INIT},
+                            (char_u *)& ( term_strings[(int)(KS_NAME)] ) , PV_NONE, did_set_term, NULL,
+                            {(char_u *)"", (char_u *)0L}   },
     {"termbidi", "tbidi",   P_BOOL|P_VI_DEF,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
-                            {(char_u *)FALSE, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)FALSE, (char_u *)0L}   },
     {"termencoding", "tenc", P_STRING|P_VI_DEF|P_RCLR,
                             (char_u *)&p_tenc, PV_NONE, did_set_encoding, expand_set_encoding,
                             {(char_u *)"", (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"termguicolors", "tgc", P_BOOL|P_VI_DEF|P_VIM|P_RCLR,
                             (char_u*)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)FALSE, (char_u *)FALSE}
-                            SCTX_INIT},
+                              },
     {"termresize", "trz", P_STRING|P_VI_DEF,
                             (char_u *)&p_trz, PV_NONE, did_set_termresize, expand_set_termresize,
                             {(char_u *)"", (char_u *)0}
-                            SCTX_INIT},
+                              },
     {"termsync", "tsy",     P_BOOL|P_VI_DEF,
                             (char_u *)&p_tsy, PV_NONE, did_set_termsync, NULL,
-                            {(char_u *)FALSE, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)FALSE, (char_u *)0L}   },
     {"termwinkey", "twk",   P_STRING|P_ALLOCED|P_RWIN|P_VI_DEF,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)NULL, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"termwinscroll", "twsl", P_NUM|P_VI_DEF|P_VIM|P_RBUF,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)NULL, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"termwinsize", "tws",  P_STRING|P_ALLOCED|P_RWIN|P_VI_DEF,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)NULL, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"termwintype", "twt",  P_STRING|P_ALLOCED|P_VI_DEF,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)NULL, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"terse",       NULL,   P_BOOL|P_VI_DEF,
                             (char_u *)&p_terse, PV_NONE, did_set_terse, NULL,
-                            {(char_u *)FALSE, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)FALSE, (char_u *)0L}   },
     {"textauto",    "ta",   P_BOOL|P_VIM,
                             (char_u *)&p_ta, PV_NONE, did_set_textauto, NULL,
-                            {(char_u *)DFLT_TEXTAUTO, (char_u *)TRUE}
-                            SCTX_INIT},
+                            {(char_u *) FALSE , (char_u *)TRUE}
+                              },
     {"textmode",    "tx",   P_BOOL|P_VI_DEF|P_NO_MKRC,
-                            (char_u *)&p_tx, PV_TX, did_set_textmode, NULL,
+                            (char_u *)&p_tx,   (idopt_T)(PV_BUF + (int)(BV_TX))  , did_set_textmode, NULL,
                             {
                             (char_u *)FALSE,
-                                (char_u *)0L} SCTX_INIT},
+                                (char_u *)0L}   },
     {"textwidth",   "tw",   P_NUM|P_VI_DEF|P_VIM|P_RBUF|P_HLONLY,
-                            (char_u *)&p_tw, PV_TW, did_set_textwidth, NULL,
-                            {(char_u *)0L, (char_u *)0L} SCTX_INIT},
+                            (char_u *)&p_tw,   (idopt_T)(PV_BUF + (int)(BV_TW))  , did_set_textwidth, NULL,
+                            {(char_u *)0L, (char_u *)0L}   },
     {"thesaurus",   "tsr",  P_STRING|P_EXPAND|P_VI_DEF|P_ONECOMMA|P_NODUP|P_NDNAME,
-                            (char_u *)&p_tsr, PV_TSR, NULL, NULL,
-                            {(char_u *)"", (char_u *)0L} SCTX_INIT},
+                            (char_u *)&p_tsr,   (idopt_T)(PV_BOTH + (int)( (idopt_T)(PV_BUF + (int)(BV_TSR)) ))  , NULL, NULL,
+                            {(char_u *)"", (char_u *)0L}   },
     {"thesaurusfunc", "tsrfu",  P_STRING|P_ALLOCED|P_VI_DEF|P_SECURE|P_FUNC,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)0L, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"tildeop",     "top",  P_BOOL|P_VI_DEF|P_VIM,
                             (char_u *)&p_to, PV_NONE, NULL, NULL,
-                            {(char_u *)FALSE, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)FALSE, (char_u *)0L}   },
     {"timeout",     "to",   P_BOOL|P_VI_DEF,
                             (char_u *)&p_timeout, PV_NONE, NULL, NULL,
-                            {(char_u *)TRUE, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)TRUE, (char_u *)0L}   },
     {"timeoutlen",  "tm",   P_NUM|P_VI_DEF,
                             (char_u *)&p_tm, PV_NONE, NULL, NULL,
-                            {(char_u *)1000L, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)1000L, (char_u *)0L}   },
     {"title",       NULL,   P_BOOL|P_VI_DEF,
                             (char_u *)&p_title, PV_NONE, did_set_title_icon, NULL,
-                            {(char_u *)FALSE, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)FALSE, (char_u *)0L}   },
     {"titlelen",    NULL,   P_NUM|P_VI_DEF,
                             (char_u *)&p_titlelen, PV_NONE, did_set_titlelen, NULL,
-                            {(char_u *)85L, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)85L, (char_u *)0L}   },
     {"titleold",    NULL,   P_STRING|P_VI_DEF|P_GETTEXT|P_SECURE|P_NO_MKRC,
                             (char_u *)&p_titleold, PV_NONE, NULL, NULL,
-                            {(char_u *)N_("Thanks for flying Vim"),
+                            {(char_u *) "Thanks for flying Vim" ,
                                                                (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"titlestring", NULL,   P_STRING|P_VI_DEF|P_MLE,
                             (char_u *)&p_titlestring, PV_NONE,
                             did_set_titlestring, NULL,
-                            {(char_u *)"", (char_u *)0L} SCTX_INIT},
+                            {(char_u *)"", (char_u *)0L}   },
     {"toolbar",     "tb",   P_STRING|P_ONECOMMA|P_VI_DEF|P_NODUP,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)0L, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"toolbariconsize", "tbis", P_STRING|P_VI_DEF,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)0L, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"ttimeout",    NULL,   P_BOOL|P_VI_DEF|P_VIM,
                             (char_u *)&p_ttimeout, PV_NONE, NULL, NULL,
-                            {(char_u *)FALSE, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)FALSE, (char_u *)0L}   },
     {"ttimeoutlen", "ttm",  P_NUM|P_VI_DEF,
                             (char_u *)&p_ttm, PV_NONE, NULL, NULL,
-                            {(char_u *)-1L, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)-1L, (char_u *)0L}   },
     {"ttybuiltin",  "tbi",  P_BOOL|P_VI_DEF,
                             (char_u *)&p_tbi, PV_NONE, NULL, NULL,
-                            {(char_u *)TRUE, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)TRUE, (char_u *)0L}   },
     {"ttyfast",     "tf",   P_BOOL|P_NO_MKRC|P_VI_DEF,
                             (char_u *)&p_tf, PV_NONE, NULL, NULL,
-                            {(char_u *)TRUE, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)TRUE, (char_u *)0L}   },
     {"ttymouse",    "ttym", P_STRING|P_NODEFAULT|P_NO_MKRC|P_VI_DEF,
                             (char_u *)&p_ttym, PV_NONE, did_set_ttymouse, expand_set_ttymouse,
-                            {(char_u *)"", (char_u *)0L} SCTX_INIT},
+                            {(char_u *)"", (char_u *)0L}   },
     {"ttyscroll",   "tsl",  P_NUM|P_VI_DEF,
                             (char_u *)&p_ttyscroll, PV_NONE, NULL, NULL,
-                            {(char_u *)999L, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)999L, (char_u *)0L}   },
     {"ttytype",     "tty",  P_STRING|P_EXPAND|P_NODEFAULT|P_NO_MKRC|P_VI_DEF|P_RALL,
-                            (char_u *)&T_NAME, PV_NONE, did_set_term, NULL,
-                            {(char_u *)"", (char_u *)0L} SCTX_INIT},
+                            (char_u *)& ( term_strings[(int)(KS_NAME)] ) , PV_NONE, did_set_term, NULL,
+                            {(char_u *)"", (char_u *)0L}   },
     {"undodir",     "udir", P_STRING|P_EXPAND|P_ONECOMMA|P_NODUP|P_SECURE
                                                                     |P_VI_DEF,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)0L, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"undofile",    "udf",  P_BOOL|P_VI_DEF|P_VIM,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
-                            {(char_u *)FALSE, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)FALSE, (char_u *)0L}   },
     {"undolevels",  "ul",   P_NUM|P_VI_DEF,
-                            (char_u *)&p_ul, PV_UL, did_set_undolevels, NULL,
+                            (char_u *)&p_ul,   (idopt_T)(PV_BOTH + (int)( (idopt_T)(PV_BUF + (int)(BV_UL)) ))  , did_set_undolevels, NULL,
                             {
                             (char_u *)9999L,
-                                (char_u *)0L} SCTX_INIT},
+                                (char_u *)0L}   },
     {"undoreload",  "ur",   P_NUM|P_VI_DEF,
                             (char_u *)&p_ur, PV_NONE, NULL, NULL,
-                            { (char_u *)10000L, (char_u *)0L} SCTX_INIT},
+                            { (char_u *)10000L, (char_u *)0L}   },
     {"updatecount", "uc",   P_NUM|P_VI_DEF,
                             (char_u *)&p_uc, PV_NONE, did_set_updatecount, NULL,
-                            {(char_u *)200L, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)200L, (char_u *)0L}   },
     {"updatetime",  "ut",   P_NUM|P_VI_DEF,
                             (char_u *)&p_ut, PV_NONE, NULL, NULL,
-                            {(char_u *)4000L, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)4000L, (char_u *)0L}   },
     {"varsofttabstop", "vsts",  P_STRING|P_VI_DEF|P_VIM|P_COMMA,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)"", (char_u *)NULL}
-                            SCTX_INIT},
+                              },
     {"vartabstop",  "vts",  P_STRING|P_VI_DEF|P_VIM|P_RBUF|P_COMMA,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)"", (char_u *)NULL}
-                            SCTX_INIT},
+                              },
     {"verbose",     "vbs",  P_NUM|P_VI_DEF,
                             (char_u *)&p_verbose, PV_NONE, NULL, NULL,
-                            {(char_u *)0L, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)0L, (char_u *)0L}   },
     {"verbosefile", "vfile", P_STRING|P_EXPAND|P_VI_DEF|P_SECURE,
                             (char_u *)&p_vfile, PV_NONE, did_set_verbosefile, NULL,
-                            {(char_u *)"", (char_u *)0L} SCTX_INIT},
+                            {(char_u *)"", (char_u *)0L}   },
     {"viewdir",     "vdir", P_STRING|P_EXPAND|P_VI_DEF|P_SECURE,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)0L, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"viewoptions", "vop",  P_STRING|P_VI_DEF|P_ONECOMMA|P_NODUP,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)0L, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"viminfo",     "vi",   P_STRING|P_ONECOMMA|P_NODUP|P_SECURE,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)0L, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"viminfofile", "vif",  P_STRING|P_EXPAND|P_ONECOMMA|P_NODUP
                                                             |P_SECURE|P_VI_DEF,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)0L, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"virtualedit", "ve",   P_STRING|P_ONECOMMA|P_NODUP|P_VI_DEF
                                                             |P_VIM|P_CURSWANT,
-                            (char_u *)&p_ve, PV_VE, did_set_virtualedit, expand_set_virtualedit,
+                            (char_u *)&p_ve,   (idopt_T)(PV_BOTH + (int)( (idopt_T)(PV_WIN + (int)(WV_VE)) ))  , did_set_virtualedit, expand_set_virtualedit,
                             {(char_u *)"", (char_u *)""}
-                            SCTX_INIT},
+                              },
     {"visualbell",  "vb",   P_BOOL|P_VI_DEF,
                             (char_u *)&p_vb, PV_NONE, NULL, NULL,
-                            {(char_u *)FALSE, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)FALSE, (char_u *)0L}   },
     {"w1200",       NULL,   P_NUM|P_VI_DEF,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
-                            {(char_u *)0L, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)0L, (char_u *)0L}   },
     {"w300",        NULL,   P_NUM|P_VI_DEF,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
-                            {(char_u *)0L, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)0L, (char_u *)0L}   },
     {"w9600",       NULL,   P_NUM|P_VI_DEF,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
-                            {(char_u *)0L, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)0L, (char_u *)0L}   },
     {"warn",        NULL,   P_BOOL|P_VI_DEF,
                             (char_u *)&p_warn, PV_NONE, NULL, NULL,
-                            {(char_u *)TRUE, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)TRUE, (char_u *)0L}   },
     {"weirdinvert", "wiv",  P_BOOL|P_VI_DEF|P_RCLR,
                             (char_u *)&p_wiv, PV_NONE, did_set_weirdinvert, NULL,
-                            {(char_u *)FALSE, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)FALSE, (char_u *)0L}   },
     {"whichwrap",   "ww",   P_STRING|P_VIM|P_ONECOMMA|P_FLAGLIST,
                             (char_u *)&p_ww, PV_NONE, did_set_whichwrap, expand_set_whichwrap,
-                            {(char_u *)"", (char_u *)"b,s"} SCTX_INIT},
+                            {(char_u *)"", (char_u *)"b,s"}   },
     {"wildchar",    "wc",   P_NUM|P_VIM,
                             (char_u *)&p_wc, PV_NONE, did_set_wildchar, NULL,
                             {(char_u *)(long)Ctrl_E, (char_u *)(long)TAB}
-                            SCTX_INIT},
+                              },
     {"wildcharm",   "wcm",  P_NUM|P_VI_DEF,
                             (char_u *)&p_wcm, PV_NONE, did_set_wildchar, NULL,
-                            {(char_u *)0L, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)0L, (char_u *)0L}   },
     {"wildignore",  "wig",  P_STRING|P_VI_DEF|P_ONECOMMA|P_NODUP,
                             (char_u *)&p_wig, PV_NONE, NULL, NULL,
-                            {(char_u *)"", (char_u *)0L} SCTX_INIT},
+                            {(char_u *)"", (char_u *)0L}   },
     {"wildignorecase", "wic", P_BOOL|P_VI_DEF,
                             (char_u *)&p_wic, PV_NONE, NULL, NULL,
-                            {(char_u *)FALSE, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)FALSE, (char_u *)0L}   },
     {"wildmenu",    "wmnu", P_BOOL|P_VI_DEF,
                             (char_u *)&p_wmnu, PV_NONE, NULL, NULL,
-                            {(char_u *)TRUE, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)TRUE, (char_u *)0L}   },
     {"wildmode",    "wim",  P_STRING|P_VI_DEF|P_ONECOMMA|P_NODUP|P_COLON,
                             (char_u *)&p_wim, PV_NONE, did_set_wildmode, expand_set_wildmode,
-                            {(char_u *)"full", (char_u *)0L} SCTX_INIT},
+                            {(char_u *)"full", (char_u *)0L}   },
     {"wildoptions", "wop",  P_STRING|P_VI_DEF|P_ONECOMMA|P_NODUP,
                             (char_u *)&p_wop, PV_NONE, did_set_wildoptions, expand_set_wildoptions,
                             {(char_u *)"", (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"winaltkeys",  "wak",  P_STRING|P_VI_DEF,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)NULL, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"wincolor", "wcr",     P_STRING|P_ALLOCED|P_VI_DEF|P_RWIN,
-                            (char_u *)VAR_WIN, PV_WCR, did_set_wincolor, expand_set_wincolor,
+                            (char_u *) ((char_u *)-1) ,   (idopt_T)(PV_WIN + (int)(WV_WCR))  , did_set_wincolor, expand_set_wincolor,
                             {(char_u *)"", (char_u *)NULL}
-                            SCTX_INIT},
+                              },
     {"window",      "wi",   P_NUM|P_VI_DEF,
                             (char_u *)&p_window, PV_NONE, did_set_window, NULL,
-                            {(char_u *)0L, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)0L, (char_u *)0L}   },
     {"winfixbuf", "wfb",    P_BOOL|P_VI_DEF,
-                            (char_u *)VAR_WIN, PV_WFB, NULL, NULL,
-                            {(char_u *)FALSE, (char_u *)0L} SCTX_INIT},
+                            (char_u *) ((char_u *)-1) ,   (idopt_T)(PV_WIN + (int)(WV_WFB))  , NULL, NULL,
+                            {(char_u *)FALSE, (char_u *)0L}   },
     {"winfixheight", "wfh", P_BOOL|P_VI_DEF|P_RSTAT,
-                            (char_u *)VAR_WIN, PV_WFH, NULL, NULL,
-                            {(char_u *)FALSE, (char_u *)0L} SCTX_INIT},
+                            (char_u *) ((char_u *)-1) ,   (idopt_T)(PV_WIN + (int)(WV_WFH))  , NULL, NULL,
+                            {(char_u *)FALSE, (char_u *)0L}   },
     {"winfixwidth", "wfw",  P_BOOL|P_VI_DEF|P_RSTAT,
-                            (char_u *)VAR_WIN, PV_WFW, NULL, NULL,
-                            {(char_u *)FALSE, (char_u *)0L} SCTX_INIT},
+                            (char_u *) ((char_u *)-1) ,   (idopt_T)(PV_WIN + (int)(WV_WFW))  , NULL, NULL,
+                            {(char_u *)FALSE, (char_u *)0L}   },
     {"winheight",   "wh",   P_NUM|P_VI_DEF,
                             (char_u *)&p_wh, PV_NONE,
                             did_set_winheight_helpheight, NULL,
-                            {(char_u *)1L, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)1L, (char_u *)0L}   },
     {"winhighlight", "whl", P_STRING|P_VI_DEF|P_RALL|P_ONECOMMA|P_NODUP|P_COLON,
-                            (char_u *)VAR_WIN, PV_WHL, did_set_winhighlight, expand_set_winhighlight,
-                            {(char_u *)"", (char_u *)NULL} SCTX_INIT},
+                            (char_u *) ((char_u *)-1) ,   (idopt_T)(PV_WIN + (int)(WV_WHL))  , did_set_winhighlight, expand_set_winhighlight,
+                            {(char_u *)"", (char_u *)NULL}   },
     {"winminheight", "wmh", P_NUM|P_VI_DEF,
                             (char_u *)&p_wmh, PV_NONE, did_set_winminheight, NULL,
-                            {(char_u *)1L, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)1L, (char_u *)0L}   },
     {"winminwidth", "wmw",  P_NUM|P_VI_DEF,
                             (char_u *)&p_wmw, PV_NONE, did_set_winminwidth, NULL,
-                            {(char_u *)1L, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)1L, (char_u *)0L}   },
     {"winptydll", NULL,     P_STRING|P_EXPAND|P_VI_DEF|P_SECURE,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)0L, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"winwidth",   "wiw",   P_NUM|P_VI_DEF,
                             (char_u *)&p_wiw, PV_NONE, did_set_winwidth, NULL,
-                            {(char_u *)20L, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)20L, (char_u *)0L}   },
     {"wlseat",      "wse",  P_STRING|P_VI_DEF,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)NULL, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"wlsteal",     "wst",  P_BOOL|P_VI_DEF,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)NULL, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"wltimeoutlen", "wtm", P_NUM|P_VI_DEF,
                             (char_u *)NULL, PV_NONE, NULL, NULL,
                             {(char_u *)NULL, (char_u *)0L}
-                            SCTX_INIT},
+                              },
     {"wrap",        NULL,   P_BOOL|P_VI_DEF|P_RWIN,
-                            (char_u *)VAR_WIN, PV_WRAP, did_set_wrap, NULL,
-                            {(char_u *)TRUE, (char_u *)0L} SCTX_INIT},
+                            (char_u *) ((char_u *)-1) ,   (idopt_T)(PV_WIN + (int)(WV_WRAP))  , did_set_wrap, NULL,
+                            {(char_u *)TRUE, (char_u *)0L}   },
     {"wrapmargin",  "wm",   P_NUM|P_VI_DEF,
-                            (char_u *)&p_wm, PV_WM, NULL, NULL,
-                            {(char_u *)0L, (char_u *)0L} SCTX_INIT},
+                            (char_u *)&p_wm,   (idopt_T)(PV_BUF + (int)(BV_WM))  , NULL, NULL,
+                            {(char_u *)0L, (char_u *)0L}   },
     {"wrapscan",    "ws",   P_BOOL|P_VI_DEF,
                             (char_u *)&p_ws, PV_NONE, NULL, NULL,
-                            {(char_u *)TRUE, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)TRUE, (char_u *)0L}   },
     {"write",       NULL,   P_BOOL|P_VI_DEF,
                             (char_u *)&p_write, PV_NONE, NULL, NULL,
-                            {(char_u *)TRUE, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)TRUE, (char_u *)0L}   },
     {"writeany",    "wa",   P_BOOL|P_VI_DEF,
                             (char_u *)&p_wa, PV_NONE, NULL, NULL,
-                            {(char_u *)FALSE, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)FALSE, (char_u *)0L}   },
     {"writebackup", "wb",   P_BOOL|P_VI_DEF|P_VIM,
                             (char_u *)&p_wb, PV_NONE, NULL, NULL,
                             {
                             (char_u *)TRUE,
-                                (char_u *)0L} SCTX_INIT},
+                                (char_u *)0L}   },
     {"writedelay",  "wd",   P_NUM|P_VI_DEF,
                             (char_u *)&p_wd, PV_NONE, NULL, NULL,
-                            {(char_u *)0L, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)0L, (char_u *)0L}   },
     {"xtermcodes",  NULL,   P_BOOL|P_VI_DEF,
                             (char_u *)&p_xtermcodes, PV_NONE, NULL, NULL,
-                            {(char_u *)TRUE, (char_u *)0L} SCTX_INIT},
+                            {(char_u *)TRUE, (char_u *)0L}   },
 
-#define p_term(sss, vvv)                            {sss, NULL, P_STRING|P_VI_DEF|P_RALL|P_SECURE,                          (char_u *)&vvv, PV_NONE, did_set_term_option, NULL,                             {(char_u *)"", (char_u *)0L} SCTX_INIT},
+     {"t_AB", NULL, P_STRING|P_VI_DEF|P_RALL|P_SECURE,                          (char_u *)& ( term_strings[(int)(KS_CAB)] ) , PV_NONE, did_set_term_option, NULL,                             {(char_u *)"", (char_u *)0L}   }, 
+     {"t_AF", NULL, P_STRING|P_VI_DEF|P_RALL|P_SECURE,                          (char_u *)& ( term_strings[(int)(KS_CAF)] ) , PV_NONE, did_set_term_option, NULL,                             {(char_u *)"", (char_u *)0L}   }, 
+     {"t_AU", NULL, P_STRING|P_VI_DEF|P_RALL|P_SECURE,                          (char_u *)& ( term_strings[(int)(KS_CAU)] ) , PV_NONE, did_set_term_option, NULL,                             {(char_u *)"", (char_u *)0L}   }, 
+     {"t_AL", NULL, P_STRING|P_VI_DEF|P_RALL|P_SECURE,                          (char_u *)& ( term_strings[(int)(KS_CAL)] ) , PV_NONE, did_set_term_option, NULL,                             {(char_u *)"", (char_u *)0L}   }, 
+     {"t_al", NULL, P_STRING|P_VI_DEF|P_RALL|P_SECURE,                          (char_u *)& ( term_strings[(int)(KS_AL)] ) , PV_NONE, did_set_term_option, NULL,                             {(char_u *)"", (char_u *)0L}   }, 
+     {"t_bc", NULL, P_STRING|P_VI_DEF|P_RALL|P_SECURE,                          (char_u *)& ( term_strings[(int)(KS_BC)] ) , PV_NONE, did_set_term_option, NULL,                             {(char_u *)"", (char_u *)0L}   }, 
+     {"t_BE", NULL, P_STRING|P_VI_DEF|P_RALL|P_SECURE,                          (char_u *)& ( term_strings[(int)(KS_CBE)] ) , PV_NONE, did_set_term_option, NULL,                             {(char_u *)"", (char_u *)0L}   }, 
+     {"t_BD", NULL, P_STRING|P_VI_DEF|P_RALL|P_SECURE,                          (char_u *)& ( term_strings[(int)(KS_CBD)] ) , PV_NONE, did_set_term_option, NULL,                             {(char_u *)"", (char_u *)0L}   }, 
+     {"t_cd", NULL, P_STRING|P_VI_DEF|P_RALL|P_SECURE,                          (char_u *)& ( term_strings[(int)(KS_CD)] ) , PV_NONE, did_set_term_option, NULL,                             {(char_u *)"", (char_u *)0L}   }, 
+     {"t_ce", NULL, P_STRING|P_VI_DEF|P_RALL|P_SECURE,                          (char_u *)& ( term_strings[(int)(KS_CE)] ) , PV_NONE, did_set_term_option, NULL,                             {(char_u *)"", (char_u *)0L}   }, 
+     {"t_Ce", NULL, P_STRING|P_VI_DEF|P_RALL|P_SECURE,                          (char_u *)& ( term_strings[(int)(KS_UCE)] ) , PV_NONE, did_set_term_option, NULL,                             {(char_u *)"", (char_u *)0L}   }, 
+     {"t_CF", NULL, P_STRING|P_VI_DEF|P_RALL|P_SECURE,                          (char_u *)& ( term_strings[(int)(KS_CF)] ) , PV_NONE, did_set_term_option, NULL,                             {(char_u *)"", (char_u *)0L}   }, 
+     {"t_cl", NULL, P_STRING|P_VI_DEF|P_RALL|P_SECURE,                          (char_u *)& ( term_strings[(int)(KS_CL)] ) , PV_NONE, did_set_term_option, NULL,                             {(char_u *)"", (char_u *)0L}   }, 
+     {"t_cm", NULL, P_STRING|P_VI_DEF|P_RALL|P_SECURE,                          (char_u *)& ( term_strings[(int)(KS_CM)] ) , PV_NONE, did_set_term_option, NULL,                             {(char_u *)"", (char_u *)0L}   }, 
+     {"t_Co", NULL, P_STRING|P_VI_DEF|P_RALL|P_SECURE,                          (char_u *)& ( term_strings[(int)(KS_CCO)] ) , PV_NONE, did_set_term_option, NULL,                             {(char_u *)"", (char_u *)0L}   }, 
+     {"t_CS", NULL, P_STRING|P_VI_DEF|P_RALL|P_SECURE,                          (char_u *)& ( term_strings[(int)(KS_CCS)] ) , PV_NONE, did_set_term_option, NULL,                             {(char_u *)"", (char_u *)0L}   }, 
+     {"t_Cs", NULL, P_STRING|P_VI_DEF|P_RALL|P_SECURE,                          (char_u *)& ( term_strings[(int)(KS_UCS)] ) , PV_NONE, did_set_term_option, NULL,                             {(char_u *)"", (char_u *)0L}   }, 
+     {"t_cs", NULL, P_STRING|P_VI_DEF|P_RALL|P_SECURE,                          (char_u *)& ( term_strings[(int)(KS_CS)] ) , PV_NONE, did_set_term_option, NULL,                             {(char_u *)"", (char_u *)0L}   }, 
+     {"t_CV", NULL, P_STRING|P_VI_DEF|P_RALL|P_SECURE,                          (char_u *)& ( term_strings[(int)(KS_CSV)] ) , PV_NONE, did_set_term_option, NULL,                             {(char_u *)"", (char_u *)0L}   }, 
+     {"t_da", NULL, P_STRING|P_VI_DEF|P_RALL|P_SECURE,                          (char_u *)& ( term_strings[(int)(KS_DA)] ) , PV_NONE, did_set_term_option, NULL,                             {(char_u *)"", (char_u *)0L}   }, 
+     {"t_db", NULL, P_STRING|P_VI_DEF|P_RALL|P_SECURE,                          (char_u *)& ( term_strings[(int)(KS_DB)] ) , PV_NONE, did_set_term_option, NULL,                             {(char_u *)"", (char_u *)0L}   }, 
+     {"t_DL", NULL, P_STRING|P_VI_DEF|P_RALL|P_SECURE,                          (char_u *)& ( term_strings[(int)(KS_CDL)] ) , PV_NONE, did_set_term_option, NULL,                             {(char_u *)"", (char_u *)0L}   }, 
+     {"t_dl", NULL, P_STRING|P_VI_DEF|P_RALL|P_SECURE,                          (char_u *)& ( term_strings[(int)(KS_DL)] ) , PV_NONE, did_set_term_option, NULL,                             {(char_u *)"", (char_u *)0L}   }, 
+     {"t_ds", NULL, P_STRING|P_VI_DEF|P_RALL|P_SECURE,                          (char_u *)& ( term_strings[(int)(KS_DS)] ) , PV_NONE, did_set_term_option, NULL,                             {(char_u *)"", (char_u *)0L}   }, 
+     {"t_Ds", NULL, P_STRING|P_VI_DEF|P_RALL|P_SECURE,                          (char_u *)& ( term_strings[(int)(KS_CDS)] ) , PV_NONE, did_set_term_option, NULL,                             {(char_u *)"", (char_u *)0L}   }, 
+     {"t_EC", NULL, P_STRING|P_VI_DEF|P_RALL|P_SECURE,                          (char_u *)& ( term_strings[(int)(KS_CEC)] ) , PV_NONE, did_set_term_option, NULL,                             {(char_u *)"", (char_u *)0L}   }, 
+     {"t_EI", NULL, P_STRING|P_VI_DEF|P_RALL|P_SECURE,                          (char_u *)& ( term_strings[(int)(KS_CEI)] ) , PV_NONE, did_set_term_option, NULL,                             {(char_u *)"", (char_u *)0L}   }, 
+     {"t_fs", NULL, P_STRING|P_VI_DEF|P_RALL|P_SECURE,                          (char_u *)& ( term_strings[(int)(KS_FS)] ) , PV_NONE, did_set_term_option, NULL,                             {(char_u *)"", (char_u *)0L}   }, 
+     {"t_fd", NULL, P_STRING|P_VI_DEF|P_RALL|P_SECURE,                          (char_u *)& ( term_strings[(int)(KS_FD)] ) , PV_NONE, did_set_term_option, NULL,                             {(char_u *)"", (char_u *)0L}   }, 
+     {"t_fe", NULL, P_STRING|P_VI_DEF|P_RALL|P_SECURE,                          (char_u *)& ( term_strings[(int)(KS_FE)] ) , PV_NONE, did_set_term_option, NULL,                             {(char_u *)"", (char_u *)0L}   }, 
+     {"t_GP", NULL, P_STRING|P_VI_DEF|P_RALL|P_SECURE,                          (char_u *)& ( term_strings[(int)(KS_CGP)] ) , PV_NONE, did_set_term_option, NULL,                             {(char_u *)"", (char_u *)0L}   }, 
+     {"t_IE", NULL, P_STRING|P_VI_DEF|P_RALL|P_SECURE,                          (char_u *)& ( term_strings[(int)(KS_CIE)] ) , PV_NONE, did_set_term_option, NULL,                             {(char_u *)"", (char_u *)0L}   }, 
+     {"t_IS", NULL, P_STRING|P_VI_DEF|P_RALL|P_SECURE,                          (char_u *)& ( term_strings[(int)(KS_CIS)] ) , PV_NONE, did_set_term_option, NULL,                             {(char_u *)"", (char_u *)0L}   }, 
+     {"t_ke", NULL, P_STRING|P_VI_DEF|P_RALL|P_SECURE,                          (char_u *)& ( term_strings[(int)(KS_KE)] ) , PV_NONE, did_set_term_option, NULL,                             {(char_u *)"", (char_u *)0L}   }, 
+     {"t_ks", NULL, P_STRING|P_VI_DEF|P_RALL|P_SECURE,                          (char_u *)& ( term_strings[(int)(KS_KS)] ) , PV_NONE, did_set_term_option, NULL,                             {(char_u *)"", (char_u *)0L}   }, 
+     {"t_le", NULL, P_STRING|P_VI_DEF|P_RALL|P_SECURE,                          (char_u *)& ( term_strings[(int)(KS_LE)] ) , PV_NONE, did_set_term_option, NULL,                             {(char_u *)"", (char_u *)0L}   }, 
+     {"t_mb", NULL, P_STRING|P_VI_DEF|P_RALL|P_SECURE,                          (char_u *)& ( term_strings[(int)(KS_MB)] ) , PV_NONE, did_set_term_option, NULL,                             {(char_u *)"", (char_u *)0L}   }, 
+     {"t_md", NULL, P_STRING|P_VI_DEF|P_RALL|P_SECURE,                          (char_u *)& ( term_strings[(int)(KS_MD)] ) , PV_NONE, did_set_term_option, NULL,                             {(char_u *)"", (char_u *)0L}   }, 
+     {"t_me", NULL, P_STRING|P_VI_DEF|P_RALL|P_SECURE,                          (char_u *)& ( term_strings[(int)(KS_ME)] ) , PV_NONE, did_set_term_option, NULL,                             {(char_u *)"", (char_u *)0L}   }, 
+     {"t_mr", NULL, P_STRING|P_VI_DEF|P_RALL|P_SECURE,                          (char_u *)& ( term_strings[(int)(KS_MR)] ) , PV_NONE, did_set_term_option, NULL,                             {(char_u *)"", (char_u *)0L}   }, 
+     {"t_ms", NULL, P_STRING|P_VI_DEF|P_RALL|P_SECURE,                          (char_u *)& ( term_strings[(int)(KS_MS)] ) , PV_NONE, did_set_term_option, NULL,                             {(char_u *)"", (char_u *)0L}   }, 
+     {"t_nd", NULL, P_STRING|P_VI_DEF|P_RALL|P_SECURE,                          (char_u *)& ( term_strings[(int)(KS_ND)] ) , PV_NONE, did_set_term_option, NULL,                             {(char_u *)"", (char_u *)0L}   }, 
+     {"t_op", NULL, P_STRING|P_VI_DEF|P_RALL|P_SECURE,                          (char_u *)& ( term_strings[(int)(KS_OP)] ) , PV_NONE, did_set_term_option, NULL,                             {(char_u *)"", (char_u *)0L}   }, 
+     {"t_RF", NULL, P_STRING|P_VI_DEF|P_RALL|P_SECURE,                          (char_u *)& ( term_strings[(int)(KS_RFG)] ) , PV_NONE, did_set_term_option, NULL,                             {(char_u *)"", (char_u *)0L}   }, 
+     {"t_RB", NULL, P_STRING|P_VI_DEF|P_RALL|P_SECURE,                          (char_u *)& ( term_strings[(int)(KS_RBG)] ) , PV_NONE, did_set_term_option, NULL,                             {(char_u *)"", (char_u *)0L}   }, 
+     {"t_RC", NULL, P_STRING|P_VI_DEF|P_RALL|P_SECURE,                          (char_u *)& ( term_strings[(int)(KS_CRC)] ) , PV_NONE, did_set_term_option, NULL,                             {(char_u *)"", (char_u *)0L}   }, 
+     {"t_RI", NULL, P_STRING|P_VI_DEF|P_RALL|P_SECURE,                          (char_u *)& ( term_strings[(int)(KS_CRI)] ) , PV_NONE, did_set_term_option, NULL,                             {(char_u *)"", (char_u *)0L}   }, 
+     {"t_Ri", NULL, P_STRING|P_VI_DEF|P_RALL|P_SECURE,                          (char_u *)& ( term_strings[(int)(KS_SRI)] ) , PV_NONE, did_set_term_option, NULL,                             {(char_u *)"", (char_u *)0L}   }, 
+     {"t_RK", NULL, P_STRING|P_VI_DEF|P_RALL|P_SECURE,                          (char_u *)& ( term_strings[(int)(KS_CRK)] ) , PV_NONE, did_set_term_option, NULL,                             {(char_u *)"", (char_u *)0L}   }, 
+     {"t_RS", NULL, P_STRING|P_VI_DEF|P_RALL|P_SECURE,                          (char_u *)& ( term_strings[(int)(KS_CRS)] ) , PV_NONE, did_set_term_option, NULL,                             {(char_u *)"", (char_u *)0L}   }, 
+     {"t_RT", NULL, P_STRING|P_VI_DEF|P_RALL|P_SECURE,                          (char_u *)& ( term_strings[(int)(KS_CRT)] ) , PV_NONE, did_set_term_option, NULL,                             {(char_u *)"", (char_u *)0L}   }, 
+     {"t_RV", NULL, P_STRING|P_VI_DEF|P_RALL|P_SECURE,                          (char_u *)& ( term_strings[(int)(KS_CRV)] ) , PV_NONE, did_set_term_option, NULL,                             {(char_u *)"", (char_u *)0L}   }, 
+     {"t_Sb", NULL, P_STRING|P_VI_DEF|P_RALL|P_SECURE,                          (char_u *)& ( term_strings[(int)(KS_CSB)] ) , PV_NONE, did_set_term_option, NULL,                             {(char_u *)"", (char_u *)0L}   }, 
+     {"t_SC", NULL, P_STRING|P_VI_DEF|P_RALL|P_SECURE,                          (char_u *)& ( term_strings[(int)(KS_CSC)] ) , PV_NONE, did_set_term_option, NULL,                             {(char_u *)"", (char_u *)0L}   }, 
+     {"t_se", NULL, P_STRING|P_VI_DEF|P_RALL|P_SECURE,                          (char_u *)& ( term_strings[(int)(KS_SE)] ) , PV_NONE, did_set_term_option, NULL,                             {(char_u *)"", (char_u *)0L}   }, 
+     {"t_Sf", NULL, P_STRING|P_VI_DEF|P_RALL|P_SECURE,                          (char_u *)& ( term_strings[(int)(KS_CSF)] ) , PV_NONE, did_set_term_option, NULL,                             {(char_u *)"", (char_u *)0L}   }, 
+     {"t_SH", NULL, P_STRING|P_VI_DEF|P_RALL|P_SECURE,                          (char_u *)& ( term_strings[(int)(KS_CSH)] ) , PV_NONE, did_set_term_option, NULL,                             {(char_u *)"", (char_u *)0L}   }, 
+     {"t_SI", NULL, P_STRING|P_VI_DEF|P_RALL|P_SECURE,                          (char_u *)& ( term_strings[(int)(KS_CSI)] ) , PV_NONE, did_set_term_option, NULL,                             {(char_u *)"", (char_u *)0L}   }, 
+     {"t_Si", NULL, P_STRING|P_VI_DEF|P_RALL|P_SECURE,                          (char_u *)& ( term_strings[(int)(KS_SSI)] ) , PV_NONE, did_set_term_option, NULL,                             {(char_u *)"", (char_u *)0L}   }, 
+     {"t_so", NULL, P_STRING|P_VI_DEF|P_RALL|P_SECURE,                          (char_u *)& ( term_strings[(int)(KS_SO)] ) , PV_NONE, did_set_term_option, NULL,                             {(char_u *)"", (char_u *)0L}   }, 
+     {"t_SR", NULL, P_STRING|P_VI_DEF|P_RALL|P_SECURE,                          (char_u *)& ( term_strings[(int)(KS_CSR)] ) , PV_NONE, did_set_term_option, NULL,                             {(char_u *)"", (char_u *)0L}   }, 
+     {"t_sr", NULL, P_STRING|P_VI_DEF|P_RALL|P_SECURE,                          (char_u *)& ( term_strings[(int)(KS_SR)] ) , PV_NONE, did_set_term_option, NULL,                             {(char_u *)"", (char_u *)0L}   }, 
+     {"t_ST", NULL, P_STRING|P_VI_DEF|P_RALL|P_SECURE,                          (char_u *)& ( term_strings[(int)(KS_CST)] ) , PV_NONE, did_set_term_option, NULL,                             {(char_u *)"", (char_u *)0L}   }, 
+     {"t_Te", NULL, P_STRING|P_VI_DEF|P_RALL|P_SECURE,                          (char_u *)& ( term_strings[(int)(KS_STE)] ) , PV_NONE, did_set_term_option, NULL,                             {(char_u *)"", (char_u *)0L}   }, 
+     {"t_te", NULL, P_STRING|P_VI_DEF|P_RALL|P_SECURE,                          (char_u *)& ( term_strings[(int)(KS_TE)] ) , PV_NONE, did_set_term_option, NULL,                             {(char_u *)"", (char_u *)0L}   }, 
+     {"t_TE", NULL, P_STRING|P_VI_DEF|P_RALL|P_SECURE,                          (char_u *)& ( term_strings[(int)(KS_CTE)] ) , PV_NONE, did_set_term_option, NULL,                             {(char_u *)"", (char_u *)0L}   }, 
+     {"t_ti", NULL, P_STRING|P_VI_DEF|P_RALL|P_SECURE,                          (char_u *)& ( term_strings[(int)(KS_TI)] ) , PV_NONE, did_set_term_option, NULL,                             {(char_u *)"", (char_u *)0L}   }, 
+     {"t_TI", NULL, P_STRING|P_VI_DEF|P_RALL|P_SECURE,                          (char_u *)& ( term_strings[(int)(KS_CTI)] ) , PV_NONE, did_set_term_option, NULL,                             {(char_u *)"", (char_u *)0L}   }, 
+     {"t_Ts", NULL, P_STRING|P_VI_DEF|P_RALL|P_SECURE,                          (char_u *)& ( term_strings[(int)(KS_STS)] ) , PV_NONE, did_set_term_option, NULL,                             {(char_u *)"", (char_u *)0L}   }, 
+     {"t_ts", NULL, P_STRING|P_VI_DEF|P_RALL|P_SECURE,                          (char_u *)& ( term_strings[(int)(KS_TS)] ) , PV_NONE, did_set_term_option, NULL,                             {(char_u *)"", (char_u *)0L}   }, 
+     {"t_u7", NULL, P_STRING|P_VI_DEF|P_RALL|P_SECURE,                          (char_u *)& ( term_strings[(int)(KS_U7)] ) , PV_NONE, did_set_term_option, NULL,                             {(char_u *)"", (char_u *)0L}   }, 
+     {"t_ue", NULL, P_STRING|P_VI_DEF|P_RALL|P_SECURE,                          (char_u *)& ( term_strings[(int)(KS_UE)] ) , PV_NONE, did_set_term_option, NULL,                             {(char_u *)"", (char_u *)0L}   }, 
+     {"t_us", NULL, P_STRING|P_VI_DEF|P_RALL|P_SECURE,                          (char_u *)& ( term_strings[(int)(KS_US)] ) , PV_NONE, did_set_term_option, NULL,                             {(char_u *)"", (char_u *)0L}   }, 
+     {"t_Us", NULL, P_STRING|P_VI_DEF|P_RALL|P_SECURE,                          (char_u *)& ( term_strings[(int)(KS_USS)] ) , PV_NONE, did_set_term_option, NULL,                             {(char_u *)"", (char_u *)0L}   }, 
+     {"t_ut", NULL, P_STRING|P_VI_DEF|P_RALL|P_SECURE,                          (char_u *)& ( term_strings[(int)(KS_UT)] ) , PV_NONE, did_set_term_option, NULL,                             {(char_u *)"", (char_u *)0L}   }, 
+     {"t_vb", NULL, P_STRING|P_VI_DEF|P_RALL|P_SECURE,                          (char_u *)& ( term_strings[(int)(KS_VB)] ) , PV_NONE, did_set_term_option, NULL,                             {(char_u *)"", (char_u *)0L}   }, 
+     {"t_ve", NULL, P_STRING|P_VI_DEF|P_RALL|P_SECURE,                          (char_u *)& ( term_strings[(int)(KS_VE)] ) , PV_NONE, did_set_term_option, NULL,                             {(char_u *)"", (char_u *)0L}   }, 
+     {"t_vi", NULL, P_STRING|P_VI_DEF|P_RALL|P_SECURE,                          (char_u *)& ( term_strings[(int)(KS_VI)] ) , PV_NONE, did_set_term_option, NULL,                             {(char_u *)"", (char_u *)0L}   }, 
+     {"t_VS", NULL, P_STRING|P_VI_DEF|P_RALL|P_SECURE,                          (char_u *)& ( term_strings[(int)(KS_CVS)] ) , PV_NONE, did_set_term_option, NULL,                             {(char_u *)"", (char_u *)0L}   }, 
+     {"t_vs", NULL, P_STRING|P_VI_DEF|P_RALL|P_SECURE,                          (char_u *)& ( term_strings[(int)(KS_VS)] ) , PV_NONE, did_set_term_option, NULL,                             {(char_u *)"", (char_u *)0L}   }, 
+     {"t_WP", NULL, P_STRING|P_VI_DEF|P_RALL|P_SECURE,                          (char_u *)& ( term_strings[(int)(KS_CWP)] ) , PV_NONE, did_set_term_option, NULL,                             {(char_u *)"", (char_u *)0L}   }, 
+     {"t_WS", NULL, P_STRING|P_VI_DEF|P_RALL|P_SECURE,                          (char_u *)& ( term_strings[(int)(KS_CWS)] ) , PV_NONE, did_set_term_option, NULL,                             {(char_u *)"", (char_u *)0L}   }, 
+     {"t_XM", NULL, P_STRING|P_VI_DEF|P_RALL|P_SECURE,                          (char_u *)& ( term_strings[(int)(KS_CXM)] ) , PV_NONE, did_set_term_option, NULL,                             {(char_u *)"", (char_u *)0L}   }, 
+     {"t_xn", NULL, P_STRING|P_VI_DEF|P_RALL|P_SECURE,                          (char_u *)& ( term_strings[(int)(KS_XN)] ) , PV_NONE, did_set_term_option, NULL,                             {(char_u *)"", (char_u *)0L}   }, 
+     {"t_xs", NULL, P_STRING|P_VI_DEF|P_RALL|P_SECURE,                          (char_u *)& ( term_strings[(int)(KS_XS)] ) , PV_NONE, did_set_term_option, NULL,                             {(char_u *)"", (char_u *)0L}   }, 
+     {"t_ZH", NULL, P_STRING|P_VI_DEF|P_RALL|P_SECURE,                          (char_u *)& ( term_strings[(int)(KS_CZH)] ) , PV_NONE, did_set_term_option, NULL,                             {(char_u *)"", (char_u *)0L}   }, 
+     {"t_ZR", NULL, P_STRING|P_VI_DEF|P_RALL|P_SECURE,                          (char_u *)& ( term_strings[(int)(KS_CZR)] ) , PV_NONE, did_set_term_option, NULL,                             {(char_u *)"", (char_u *)0L}   }, 
+     {"t_8f", NULL, P_STRING|P_VI_DEF|P_RALL|P_SECURE,                          (char_u *)& ( term_strings[(int)(KS_8F)] ) , PV_NONE, did_set_term_option, NULL,                             {(char_u *)"", (char_u *)0L}   }, 
+     {"t_8b", NULL, P_STRING|P_VI_DEF|P_RALL|P_SECURE,                          (char_u *)& ( term_strings[(int)(KS_8B)] ) , PV_NONE, did_set_term_option, NULL,                             {(char_u *)"", (char_u *)0L}   }, 
+     {"t_8u", NULL, P_STRING|P_VI_DEF|P_RALL|P_SECURE,                          (char_u *)& ( term_strings[(int)(KS_8U)] ) , PV_NONE, did_set_term_option, NULL,                             {(char_u *)"", (char_u *)0L}   }, 
+     {"t_xo", NULL, P_STRING|P_VI_DEF|P_RALL|P_SECURE,                          (char_u *)& ( term_strings[(int)(KS_XON)] ) , PV_NONE, did_set_term_option, NULL,                             {(char_u *)"", (char_u *)0L}   }, 
+     {"t_BS", NULL, P_STRING|P_VI_DEF|P_RALL|P_SECURE,                          (char_u *)& ( term_strings[(int)(KS_BSU)] ) , PV_NONE, did_set_term_option, NULL,                             {(char_u *)"", (char_u *)0L}   }, 
+     {"t_ES", NULL, P_STRING|P_VI_DEF|P_RALL|P_SECURE,                          (char_u *)& ( term_strings[(int)(KS_ESU)] ) , PV_NONE, did_set_term_option, NULL,                             {(char_u *)"", (char_u *)0L}   }, 
 
-    p_term("t_AB", T_CAB)
-    p_term("t_AF", T_CAF)
-    p_term("t_AU", T_CAU)
-    p_term("t_AL", T_CAL)
-    p_term("t_al", T_AL)
-    p_term("t_bc", T_BC)
-    p_term("t_BE", T_BE)
-    p_term("t_BD", T_BD)
-    p_term("t_cd", T_CD)
-    p_term("t_ce", T_CE)
-    p_term("t_Ce", T_UCE)
-    p_term("t_CF", T_CFO)
-    p_term("t_cl", T_CL)
-    p_term("t_cm", T_CM)
-    p_term("t_Co", T_CCO)
-    p_term("t_CS", T_CCS)
-    p_term("t_Cs", T_UCS)
-    p_term("t_cs", T_CS)
-    p_term("t_CV", T_CSV)
-    p_term("t_da", T_DA)
-    p_term("t_db", T_DB)
-    p_term("t_DL", T_CDL)
-    p_term("t_dl", T_DL)
-    p_term("t_ds", T_DS)
-    p_term("t_Ds", T_CDS)
-    p_term("t_EC", T_CEC)
-    p_term("t_EI", T_CEI)
-    p_term("t_fs", T_FS)
-    p_term("t_fd", T_FD)
-    p_term("t_fe", T_FE)
-    p_term("t_GP", T_CGP)
-    p_term("t_IE", T_CIE)
-    p_term("t_IS", T_CIS)
-    p_term("t_ke", T_KE)
-    p_term("t_ks", T_KS)
-    p_term("t_le", T_LE)
-    p_term("t_mb", T_MB)
-    p_term("t_md", T_MD)
-    p_term("t_me", T_ME)
-    p_term("t_mr", T_MR)
-    p_term("t_ms", T_MS)
-    p_term("t_nd", T_ND)
-    p_term("t_op", T_OP)
-    p_term("t_RF", T_RFG)
-    p_term("t_RB", T_RBG)
-    p_term("t_RC", T_CRC)
-    p_term("t_RI", T_CRI)
-    p_term("t_Ri", T_SRI)
-    p_term("t_RK", T_CRK)
-    p_term("t_RS", T_CRS)
-    p_term("t_RT", T_CRT)
-    p_term("t_RV", T_CRV)
-    p_term("t_Sb", T_CSB)
-    p_term("t_SC", T_CSC)
-    p_term("t_se", T_SE)
-    p_term("t_Sf", T_CSF)
-    p_term("t_SH", T_CSH)
-    p_term("t_SI", T_CSI)
-    p_term("t_Si", T_SSI)
-    p_term("t_so", T_SO)
-    p_term("t_SR", T_CSR)
-    p_term("t_sr", T_SR)
-    p_term("t_ST", T_CST)
-    p_term("t_Te", T_STE)
-    p_term("t_te", T_TE)
-    p_term("t_TE", T_CTE)
-    p_term("t_ti", T_TI)
-    p_term("t_TI", T_CTI)
-    p_term("t_Ts", T_STS)
-    p_term("t_ts", T_TS)
-    p_term("t_u7", T_U7)
-    p_term("t_ue", T_UE)
-    p_term("t_us", T_US)
-    p_term("t_Us", T_USS)
-    p_term("t_ut", T_UT)
-    p_term("t_vb", T_VB)
-    p_term("t_ve", T_VE)
-    p_term("t_vi", T_VI)
-    p_term("t_VS", T_CVS)
-    p_term("t_vs", T_VS)
-    p_term("t_WP", T_CWP)
-    p_term("t_WS", T_CWS)
-    p_term("t_XM", T_CXM)
-    p_term("t_xn", T_XN)
-    p_term("t_xs", T_XS)
-    p_term("t_ZH", T_CZH)
-    p_term("t_ZR", T_CZR)
-    p_term("t_8f", T_8F)
-    p_term("t_8b", T_8B)
-    p_term("t_8u", T_8U)
-    p_term("t_xo", T_XON)
-    p_term("t_BS", T_BSU)
-    p_term("t_ES", T_ESU)
-
-    {NULL, NULL, 0, NULL, PV_NONE, NULL, NULL, {NULL, NULL} SCTX_INIT}
+    {NULL, NULL, 0, NULL, PV_NONE, NULL, NULL, {NULL, NULL}   }
 };
 
-#define OPTION_COUNT ARRAY_LENGTH(options)
-
 // ---------------- end optiondefs.h ----------------
 
 static void set_options_default(int opt_flags);
@@ -113452,7 +111402,6 @@
 static char_u *option_expand(int opt_idx, char_u *val);
 static void didset_options(void);
 static void didset_options2(void);
-# define insecure_flag(wp, opt_idx, opt_flags) (&options[opt_idx].flags)
 static char *set_bool_option(int opt_idx, char_u *varp, int value, int opt_flags);
 static char *set_num_option(int opt_idx, char_u *varp, long value, char *errbuf, size_t errbuflen, int opt_flags);
 static int find_key_option(char_u *arg_arg, int has_lt);
@@ -113477,7 +111426,7 @@
 {
     char_u      *p;
 
-    if (((p = mch_getenv((char_u *)"SHELL")) != NULL && *p != NUL))
+    if (((p =  (char_u *)getenv((char *)((char_u *)"SHELL")) ) != NULL && *p != NUL))
     {
         set_string_default_esc("sh", p, TRUE);
     }
@@ -113496,13 +111445,13 @@
     opt_idx = findoption((char_u *)"backupskip");
 
     ga_init2(&ga, 1, 100);
-    for (i = 0; i < (int)ARRAY_LENGTH(names); ++i)
+    for (i = 0; i < (int) (sizeof(names) / sizeof((names)[0])) ; ++i)
     {
         int             mustfree = FALSE;
         if (*names[i] == NUL)
         {
             p = (char_u *)"/tmp";
-            plen = (int)STRLEN_LITERAL("/tmp");
+            plen = (int) (sizeof("/tmp" "") - 1) ;
         }
         else
         {
@@ -113517,7 +111466,7 @@
 
             if (plen == 0)
             {
-                plen = (int)STRLEN(p);
+                plen = (int) strlen((char *)(p)) ;
                 if (after_pathsep(p, p + plen))
                 {
                     has_trailing_path_sep = TRUE;
@@ -113531,7 +111480,7 @@
                 size_t  itemseplen = (ga.ga_len == 0) ? 0 : 1;
                 size_t  itemlen;
 
-                itemlen = vim_snprintf((char *)item, itemsize, "%s%s*", p, (has_trailing_path_sep) ? "" : PATHSEPSTR);
+                itemlen = vim_snprintf((char *)item, itemsize, "%s%s*", p, (has_trailing_path_sep) ? "" :  "/" );
 
                 if (find_dup_item(ga.ga_data, item, itemlen, options[opt_idx].flags) == NULL && ga_grow(&ga, (int)(itemseplen + itemlen + 1)) == OK)
                 {
@@ -113591,7 +111540,7 @@
         return;
     }
 
-    buf = alloc((STRLEN(cdpath) << 1) + 2);
+    buf = alloc(( strlen((char *)(cdpath))  << 1) + 2);
     if (buf != NULL)
     {
         buf[0] = ',';
@@ -113644,7 +111593,7 @@
     {
         return;
     }
-    if (fnamecmp(p, "nologin") == 0 || fnamecmp(p, "false") == 0)
+    if ( vim_fnamecmp((char_u *)(p), (char_u *)("nologin"))  == 0 ||  vim_fnamecmp((char_u *)(p), (char_u *)("false"))  == 0)
     {
         restricted = TRUE;
     }
@@ -113659,16 +111608,16 @@
     opt_idx = findoption((char_u *)"runtimepath");
     if (opt_idx >= 0)
     {
-        options[opt_idx].def_val[VI_DEFAULT] = (char_u *)CLEAN_RUNTIMEPATH;
-        p_rtp = (char_u *)CLEAN_RUNTIMEPATH;
+        options[opt_idx].def_val[VI_DEFAULT] = (char_u *) "$VIM/vimfiles,$VIMRUNTIME,$VIM/vimfiles/after" ;
+        p_rtp = (char_u *) "$VIM/vimfiles,$VIMRUNTIME,$VIM/vimfiles/after" ;
     }
     opt_idx = findoption((char_u *)"packpath");
     if (opt_idx < 0)
     {
         return;
     }
-    options[opt_idx].def_val[VI_DEFAULT] = (char_u *)CLEAN_RUNTIMEPATH;
-    p_pp = (char_u *)CLEAN_RUNTIMEPATH;
+    options[opt_idx].def_val[VI_DEFAULT] = (char_u *) "$VIM/vimfiles,$VIMRUNTIME,$VIM/vimfiles/after" ;
+    p_pp = (char_u *) "$VIM/vimfiles,$VIMRUNTIME,$VIM/vimfiles/after" ;
 }
 
     static void
@@ -113684,25 +111633,25 @@
     string_T    vimrc_xdg = {NULL, 0};
 
     (void)init_chartab();
-    vimrc1 = expand_env_save((char_u *)USR_VIMRC_FILE);
-    vimrc2 = expand_env_save((char_u *)USR_VIMRC_FILE2);
+    vimrc1 = expand_env_save((char_u *) "$HOME/.vimrc" );
+    vimrc2 = expand_env_save((char_u *) "~/.vim/vimrc" );
 
-    xdg_dir = mch_getenv("XDG_CONFIG_HOME");
+    xdg_dir =  (char_u *)getenv((char *)("XDG_CONFIG_HOME")) ;
     if (!xdg_dir)
     {
         xdg_dir = expand_env_save((char_u *)"~/.config");
         should_free_xdg_dir = TRUE;
         has_xdg_env = FALSE;
     }
-    concat_fnames(xdg_dir, STRLEN(xdg_dir), (char_u *)"vim/vimrc", STRLEN_LITERAL("vim/vimrc"), TRUE, &vimrc_xdg);
+    concat_fnames(xdg_dir,  strlen((char *)(xdg_dir)) , (char_u *)"vim/vimrc",  (sizeof("vim/vimrc" "") - 1) , TRUE, &vimrc_xdg);
 
     if (file_is_readable(vimrc1) || file_is_readable(vimrc2) || !file_is_readable(vimrc_xdg.string))
     {
         goto theend;
     }
 
-    xdg_rtp = has_xdg_env ? (char_u *)XDG_RUNTIMEPATH
-        : (char_u *)XDG_RUNTIMEPATH_FB;
+    xdg_rtp = has_xdg_env ? (char_u *) "$XDG_CONFIG_HOME/vim,$VIM/vimfiles,$VIMRUNTIME,$VIM/vimfiles/after,$XDG_CONFIG_HOME/vim/after" 
+        : (char_u *) "~/.config/vim,$VIM/vimfiles,$VIMRUNTIME,$VIM/vimfiles/after,~/.config/vim/after" ;
 
     if ((opt_idx = findoption((char_u *)"runtimepath")) < 0)
     {
@@ -113778,9 +111727,9 @@
 
     char_u *save_enc = p_enc;
     p_enc = p;
-    if (STRCMP(p_enc, "gb18030") == 0)
+    if ( strcmp((char *)(p_enc), (char *)("gb18030"))  == 0)
     {
-        p_enc = vim_strnsave((char_u *)"cp936", STRLEN_LITERAL("cp936"));
+        p_enc = vim_strnsave((char_u *)"cp936",  (sizeof("cp936" "") - 1) );
         vim_free(p);
     }
     if (mb_init() == NULL)
@@ -113803,13 +111752,12 @@
     void
 set_init_1(int clean_arg)
 {
-
     p_cp = FALSE;
 
-    if (mch_getenv((char_u *)"VIM_POSIX") != NULL)
+    if ( (char_u *)getenv((char *)((char_u *)"VIM_POSIX"))  != NULL)
     {
-        set_string_default("cpo", (char_u *)CPO_ALL);
-        set_string_default("shm", (char_u *)SHM_POSIX);
+        set_string_default("cpo", (char_u *) "aAbBcCdDeEfFgHiIjJkKlLmMnoOpPqrRsStuvwWxXyZz$!%*-+<>#{|&/\\.;~" );
+        set_string_default("shm", (char_u *) "AS" );
     }
 
     set_init_default_shell();
@@ -113832,7 +111780,7 @@
     curbuf->b_p_ac = -1;
     curbuf->b_p_ar = -1;
     curbuf->b_p_fs = -1;
-    curbuf->b_p_ul = NO_LOCAL_UNDOLEVEL;
+    curbuf->b_p_ul =  (-123456) ;
     check_buf_options(curbuf);
     check_win_options(curwin);
     check_options();
@@ -113895,7 +111843,7 @@
         }
         else if (flags & P_NUM)
         {
-            if (options[opt_idx].indir == PV_SCROLL)
+            if (options[opt_idx].indir ==   (idopt_T)(PV_WIN + (int)(WV_SCROLL))  )
             {
                 win_comp_scroll(curwin);
             }
@@ -113903,7 +111851,7 @@
             {
                 long def_val = (long)(long_i)options[opt_idx].def_val[dvi];
 
-                if ((long *)varp == &curwin->w_p_so || (long *)varp == &curwin->w_p_siso || (long *)varp == &curwin->w_p_sop)
+                if ((long *)varp == &curwin-> w_onebuf_opt.wo_so  || (long *)varp == &curwin-> w_onebuf_opt.wo_siso  || (long *)varp == &curwin-> w_onebuf_opt.wo_sop )
                 {
                     *(long *)varp = -1;
                 }
@@ -113921,7 +111869,7 @@
         else
         {
             *(int *)varp = (int)(long)(long_i)options[opt_idx].def_val[dvi];
-            if (options[opt_idx].indir == PV_ML && getuid() == ROOT_UID)
+            if (options[opt_idx].indir ==   (idopt_T)(PV_BUF + (int)(BV_ML))   && getuid() == ROOT_UID)
             {
                 *(int *)varp = FALSE;
             }
@@ -113932,11 +111880,11 @@
             }
         }
 
-        flagsp = insecure_flag(curwin, opt_idx, opt_flags);
+        flagsp =  (&options[opt_idx].flags) ;
         *flagsp = *flagsp & ~P_INSECURE;
         if (both)
         {
-            flagsp = insecure_flag(curwin, opt_idx, OPT_LOCAL);
+            flagsp =  (&options[opt_idx].flags) ;
             *flagsp = *flagsp & ~P_INSECURE;
         }
     }
@@ -113958,8 +111906,13 @@
         }
     }
 
-    FOR_ALL_TAB_WINDOWS(tp, wp)
+     for ((tp) = first_tabpage; (tp) != NULL; (tp) = (tp)->tp_next)
+     {
+         for ((wp) = ((tp) == curtab)            ? firstwin : (tp)->tp_firstwin; (wp); (wp) = (wp)->w_next)
+         {
         win_comp_scroll(wp);
+         }
+     }
     parse_cino(curbuf);
 }
 
@@ -114016,7 +111969,7 @@
 
     for (s = origval; *s != NUL; ++s)
     {
-        if ((!(flags & P_COMMA) || s == origval || (s[-1] == ',' && !(bs & 1))) && STRNCMP(s, newval, newvallen) == 0 && (!(flags & P_COMMA) || s[newvallen] == ',' || s[newvallen] == NUL))
+        if ((!(flags & P_COMMA) || s == origval || (s[-1] == ',' && !(bs & 1))) &&  strncmp((char *)(s), (char *)(newval), (newvallen))  == 0 && (!(flags & P_COMMA) || s[newvallen] == ',' || s[newvallen] == NUL))
         {
             return s;
         }
@@ -114090,7 +112043,7 @@
     p = get_isolated_shell_name();
     if (p != NULL)
     {
-        if (       fnamecmp(p, "csh") == 0 || fnamecmp(p, "tcsh") == 0)
+        if (        vim_fnamecmp((char_u *)(p), (char_u *)("csh"))  == 0 ||  vim_fnamecmp((char_u *)(p), (char_u *)("tcsh"))  == 0)
         {
             if (do_srr)
             {
@@ -114100,7 +112053,7 @@
         }
         else
         {
-            if (       fnamecmp(p, "sh") == 0 || fnamecmp(p, "ksh") == 0 || fnamecmp(p, "mksh") == 0 || fnamecmp(p, "pdksh") == 0 || fnamecmp(p, "zsh") == 0 || fnamecmp(p, "zsh-beta") == 0 || fnamecmp(p, "bash") == 0 || fnamecmp(p, "fish") == 0 || fnamecmp(p, "ash") == 0 || fnamecmp(p, "dash") == 0 || fnamecmp(p, "pwsh") == 0)
+            if (        vim_fnamecmp((char_u *)(p), (char_u *)("sh"))  == 0 ||  vim_fnamecmp((char_u *)(p), (char_u *)("ksh"))  == 0 ||  vim_fnamecmp((char_u *)(p), (char_u *)("mksh"))  == 0 ||  vim_fnamecmp((char_u *)(p), (char_u *)("pdksh"))  == 0 ||  vim_fnamecmp((char_u *)(p), (char_u *)("zsh"))  == 0 ||  vim_fnamecmp((char_u *)(p), (char_u *)("zsh-beta"))  == 0 ||  vim_fnamecmp((char_u *)(p), (char_u *)("bash"))  == 0 ||  vim_fnamecmp((char_u *)(p), (char_u *)("fish"))  == 0 ||  vim_fnamecmp((char_u *)(p), (char_u *)("ash"))  == 0 ||  vim_fnamecmp((char_u *)(p), (char_u *)("dash"))  == 0 ||  vim_fnamecmp((char_u *)(p), (char_u *)("pwsh"))  == 0)
             {
                 if (do_srr)
                 {
@@ -114112,7 +112065,7 @@
         vim_free(p);
     }
 
-    if (BUFEMPTY())
+    if ( (curbuf->b_ml.ml_line_count == 1 && *ml_get((linenr_T)1) == NUL) )
     {
         int idx_ffs = findoption((char_u *)"ffs");
 
@@ -114183,12 +112136,12 @@
     int         prefix = PREFIX_NONE;
     char_u      *arg = *argp;
 
-    if (STRNCMP(arg, "no", 2) == 0 && STRNCMP(arg, "novice", 6) != 0)
+    if ( strncmp((char *)(arg), (char *)("no"), (2))  == 0 &&  strncmp((char *)(arg), (char *)("novice"), (6))  != 0)
     {
         prefix = PREFIX_NO;
         arg += 2;
     }
-    else if (STRNCMP(arg, "inv", 3) == 0)
+    else if ( strncmp((char *)(arg), (char *)("inv"), (3))  == 0)
     {
         prefix = PREFIX_INV;
         arg += 3;
@@ -114247,7 +112200,7 @@
         }
         else
         {
-            while (ASCII_ISALNUM(arg[len]) || arg[len] == '_')
+            while ( ( ( ((unsigned)(arg[len]) - 'A' < 26)  ||  ((unsigned)(arg[len]) - 'a' < 26) )  ||  ((unsigned)(arg[len]) - '0' < 10) )  || arg[len] == '_')
             {
                 ++len;
             }
@@ -114324,7 +112277,7 @@
 {
     for (int i = 0; modeline_whitelist[i] != NULL; i++)
     {
-        if (STRCMP(name, modeline_whitelist[i]) == 0)
+        if ( strcmp((char *)(name), (char *)(modeline_whitelist[i]))  == 0)
         {
             return true;
         }
@@ -114335,12 +112288,12 @@
     static int
 validate_opt_idx(int opt_idx, int opt_flags, long_u flags, char **errmsg, set_prefix_T prefix)
 {
-    if ((opt_flags & OPT_WINONLY) && (opt_idx < 0 || options[opt_idx].var != VAR_WIN))
+    if ((opt_flags & OPT_WINONLY) && (opt_idx < 0 || options[opt_idx].var !=  ((char_u *)-1) ))
     {
         return FAIL;
     }
 
-    if ((opt_flags & OPT_NOWIN) && opt_idx >= 0 && options[opt_idx].var == VAR_WIN)
+    if ((opt_flags & OPT_NOWIN) && opt_idx >= 0 && options[opt_idx].var ==  ((char_u *)-1) )
     {
         return FAIL;
     }
@@ -114359,7 +112312,7 @@
         }
         if (p_mlstr && opt_idx >= 0)
         {
-            if (options[opt_idx].indir == PV_ML && prefix == PREFIX_NO)
+            if (options[opt_idx].indir ==   (idopt_T)(PV_BUF + (int)(BV_ML))   && prefix == PREFIX_NO)
             {
                 return OK;
             }
@@ -114416,13 +112369,13 @@
             *(char_u **)varp = empty_option;
             break;
         case 1:
-            *(char_u **)varp = vim_strnsave((char_u *)"indent,eol", STRLEN_LITERAL("indent,eol"));
+            *(char_u **)varp = vim_strnsave((char_u *)"indent,eol",  (sizeof("indent,eol" "") - 1) );
             break;
         case 2:
-            *(char_u **)varp = vim_strnsave((char_u *)"indent,eol,start", STRLEN_LITERAL("indent,eol,start"));
+            *(char_u **)varp = vim_strnsave((char_u *)"indent,eol,start",  (sizeof("indent,eol,start" "") - 1) );
             break;
         case 3:
-            *(char_u **)varp = vim_strnsave((char_u *)"indent,eol,nostop", STRLEN_LITERAL("indent,eol,nostop"));
+            *(char_u **)varp = vim_strnsave((char_u *)"indent,eol,nostop",  (sizeof("indent,eol,nostop" "") - 1) );
             break;
     }
     vim_free(*oldval_p);
@@ -114450,27 +112403,27 @@
     int i = getdigits(argp);
     if (i & 1)
     {
-        STRCPY(whichwrap, "b,");
+         strcpy((char *)(whichwrap), (char *)("b,")) ;
         len += 2;
     }
     if (i & 2)
     {
-        STRCPY(whichwrap + len, "s,");
+         strcpy((char *)(whichwrap + len), (char *)("s,")) ;
         len += 2;
     }
     if (i & 4)
     {
-        STRCPY(whichwrap + len, "h,l,");
+         strcpy((char *)(whichwrap + len), (char *)("h,l,")) ;
         len += 4;
     }
     if (i & 8)
     {
-        STRCPY(whichwrap + len, "<,>,");
+         strcpy((char *)(whichwrap + len), (char *)("<,>,")) ;
         len += 4;
     }
     if (i & 16)
     {
-        STRCPY(whichwrap + len, "[,],");
+         strcpy((char *)(whichwrap + len), (char *)("[,],")) ;
         len += 4;
     }
     if (*whichwrap != NUL)
@@ -114482,17 +112435,17 @@
 }
 
     static char_u *
-stropt_copy_value(char_u      *origval, char_u      **argp, set_op_T    op, int         flags UNUSED)
+stropt_copy_value(char_u      *origval, char_u      **argp, set_op_T    op, int         flags  __attribute__((unused)) )
 {
     char_u      *arg = *argp;
     unsigned    newlen;
     char_u      *newval;
     char_u      *s = NULL;
 
-    newlen = (unsigned)STRLEN(arg) + 1;
+    newlen = (unsigned) strlen((char *)(arg))  + 1;
     if (op != OP_NONE)
     {
-        newlen += (unsigned)STRLEN(origval) + 1;
+        newlen += (unsigned) strlen((char *)(origval))  + 1;
     }
     newval = alloc(newlen);
     if (newval == NULL)
@@ -114501,7 +112454,7 @@
     }
     s = newval;
 
-    while (*arg != NUL && !VIM_ISWHITE(*arg))
+    while (*arg != NUL && ! ((*arg) == ' ' || (*arg) == '\t') )
     {
         int i;
 
@@ -114511,7 +112464,7 @@
         }
         if (has_mbyte && (i = (*mb_ptr2len)(arg)) > 1)
         {
-            mch_memmove(s, arg, (size_t)i);
+             memmove((char *)(s), (char *)(arg), (size_t)i) ;
             arg += i;
             s += i;
         }
@@ -114536,10 +112489,10 @@
     }
 
     vim_free(newval);
-    unsigned newlen = (unsigned)STRLEN(s) + 1;
+    unsigned newlen = (unsigned) strlen((char *)(s))  + 1;
     if (op != OP_NONE)
     {
-        newlen += (unsigned)STRLEN(origval) + 1;
+        newlen += (unsigned) strlen((char *)(origval))  + 1;
     }
 
     newval = alloc(newlen);
@@ -114548,7 +112501,7 @@
         return NULL;
     }
 
-    STRCPY(newval, s);
+     strcpy((char *)(newval), (char *)(s)) ;
 
     return newval;
 }
@@ -114561,18 +112514,18 @@
     int comma = ((flags & P_COMMA) && *origval != NUL && *newval != NUL);
     if (op == OP_ADDING)
     {
-        len = (int)STRLEN(origval);
+        len = (int) strlen((char *)(origval)) ;
         if (comma && len > 1 && (flags & P_ONECOMMA) == P_ONECOMMA && origval[len - 1] == ',' && origval[len - 2] != '\\')
         {
             len--;
         }
-        mch_memmove(newval + len + comma, newval, STRLEN(newval) + 1);
-        mch_memmove(newval, origval, (size_t)len);
+         memmove((char *)(newval + len + comma), (char *)(newval),  strlen((char *)(newval))  + 1) ;
+         memmove((char *)(newval), (char *)(origval), (size_t)len) ;
     }
     else
     {
-        len = (int)STRLEN(newval);
-        STRMOVE(newval + len + comma, origval);
+        len = (int) strlen((char *)(newval)) ;
+          memmove((char *)((newval + len + comma)), (char *)((origval)),  strlen((char *)(origval))  + 1)  ;
     }
     if (comma)
     {
@@ -114583,7 +112536,7 @@
     static void
 stropt_remove_val(char_u      *origval, char_u      *newval, int         flags, char_u      *strval, int         len)
 {
-    STRCPY(newval, origval);
+     strcpy((char *)(newval), (char *)(origval)) ;
     if (*strval)
     {
         if (flags & P_COMMA)
@@ -114601,7 +112554,7 @@
                 ++len;
             }
         }
-        STRMOVE(newval + (strval - origval), strval + len);
+          memmove((char *)((newval + (strval - origval))), (char *)((strval + len)),  strlen((char *)(strval + len))  + 1)  ;
     }
 }
 
@@ -114612,12 +112565,12 @@
 
     while (*p != NUL)
     {
-        if ((p == src || *(p - 1) == ',') && STRNCMP(p, key, keylen) == 0)
+        if ((p == src || *(p - 1) == ',') &&  strncmp((char *)(p), (char *)(key), (keylen))  == 0)
         {
             char_u *end = vim_strchr(p, ',');
             if (end == NULL)
             {
-                end = p + STRLEN(p);
+                end = p +  strlen((char *)(p)) ;
             }
             *itemlenp = (int)(end - p);
             return p;
@@ -114632,11 +112585,11 @@
 {
     if (item[itemlen] == ',')
     {
-        STRMOVE(item, item + itemlen + 1);
+          memmove((char *)((item)), (char *)((item + itemlen + 1)),  strlen((char *)(item + itemlen + 1))  + 1)  ;
     }
     else if (item > str && *(item - 1) == ',')
     {
-        STRMOVE(item - 1, item + itemlen);
+          memmove((char *)((item - 1)), (char *)((item + itemlen)),  strlen((char *)(item + itemlen))  + 1)  ;
     }
     else
     {
@@ -114673,24 +112626,24 @@
     static void
 append_item(char_u *str, char_u *item, int item_len)
 {
-    int len = (int)STRLEN(str);
+    int len = (int) strlen((char *)(str)) ;
 
     if (len > 0)
     {
         str[len++] = ',';
     }
-    mch_memmove(str + len, item, (size_t)item_len);
+     memmove((char *)(str + len), (char *)(item), (size_t)item_len) ;
     str[len + item_len] = NUL;
 }
 
     static void
 prepend_item(char_u *str, char_u *item, int item_len)
 {
-    int len = (int)STRLEN(str);
+    int len = (int) strlen((char *)(str)) ;
     int comma = (len > 0) ? 1 : 0;
 
-    mch_memmove(str + item_len + comma, str, (size_t)len + 1);
-    mch_memmove(str, item, (size_t)item_len);
+     memmove((char *)(str + item_len + comma), (char *)(str), (size_t)len + 1) ;
+     memmove((char *)(str), (char *)(item), (size_t)item_len) ;
     if (comma)
     {
         str[item_len] = ',';
@@ -114698,7 +112651,7 @@
 }
 
     static bool
-stropt_handle_keymatch(char_u      *origval, char_u      *newval, set_op_T    op, int         flags UNUSED)
+stropt_handle_keymatch(char_u      *origval, char_u      *newval, set_op_T    op, int         flags  __attribute__((unused)) )
 {
     char_u  *p;
     char_u  *item_start;
@@ -114714,14 +112667,14 @@
         return false;
     }
 
-    STRCPY(newval, origval);
+     strcpy((char *)(newval), (char *)(origval)) ;
 
     item_start = newval_copy;
     for (;;)
     {
         p = vim_strchr(item_start, ',');
         int item_len = (p == NULL)
-                            ? (int)STRLEN(item_start) : (int)(p - item_start);
+                            ? (int) strlen((char *)(item_start))  : (int)(p - item_start);
 
         if (item_len > 0)
         {
@@ -114736,7 +112689,7 @@
                     char_u *found = find_key_item(newval, item_start, keylen, &old_itemlen);
                     if (found != NULL)
                     {
-                        if (old_itemlen == item_len && STRNCMP(found, item_start, item_len) == 0)
+                        if (old_itemlen == item_len &&  strncmp((char *)(found), (char *)(item_start), (item_len))  == 0)
                         {
                             remove_key_item(newval, item_start, keylen, found);
                         }
@@ -114821,7 +112774,7 @@
         {
             if (*s != ',' && *(s + 1) == ',' && vim_strchr(s + 2, *s) != NULL)
             {
-                STRMOVE(s, s + 2);
+                  memmove((char *)((s)), (char *)((s + 2)),  strlen((char *)(s + 2))  + 1)  ;
                 continue;
             }
         }
@@ -114829,7 +112782,7 @@
         {
             if ((!(flags & P_COMMA) || *s != ',') && vim_strchr(s + 1, *s) != NULL)
             {
-                STRMOVE(s, s + 1);
+                  memmove((char *)((s)), (char *)((s + 1)),  strlen((char *)(s + 1))  + 1)  ;
                 continue;
             }
         }
@@ -114868,11 +112821,11 @@
             save_arg = arg;
             arg = (char_u *)":help";
         }
-        else if (varp == (char_u *)&p_bs && VIM_ISDIGIT(**(char_u **)varp))
+        else if (varp == (char_u *)&p_bs &&  ((unsigned)(**(char_u **)varp) - '0' < 10) )
         {
             opt_backspace_nr2str(varp, &origval, &origval_l, &origval_g, &oldval);
         }
-        else if (varp == (char_u *)&p_ww && VIM_ISDIGIT(*arg))
+        else if (varp == (char_u *)&p_ww &&  ((unsigned)(*arg) - '0' < 10) )
         {
             char_u *t = opt_whichwrap_nr2str(&arg, whichwrap);
             save_arg = arg;
@@ -114907,18 +112860,18 @@
             int len = 0;
             if (op == OP_REMOVING || (flags & P_NODUP))
             {
-                len = (int)STRLEN(newval);
+                len = (int) strlen((char *)(newval)) ;
                 s = find_dup_item(origval, newval, len, flags);
 
                 if ((op == OP_ADDING || op == OP_PREPENDING) && s != NULL)
                 {
                     op = OP_NONE;
-                    STRCPY(newval, origval);
+                     strcpy((char *)(newval), (char *)(origval)) ;
                 }
 
                 if (s == NULL)
                 {
-                    s = origval + (int)STRLEN(origval);
+                    s = origval + (int) strlen((char *)(origval)) ;
                 }
             }
 
@@ -115001,7 +112954,7 @@
     }
 
     {
-        long_u  *p = insecure_flag(curwin, opt_idx, opt_flags);
+        long_u  *p =  (&options[opt_idx].flags) ;
         int     secure_saved = secure;
 
         if ((opt_flags & OPT_MODELINE) || (op != OP_NONE && (*p & P_INSECURE)))
@@ -115062,7 +113015,7 @@
     }
     else
     {
-        if (nextchar != NUL && !VIM_ISWHITE(afterchar))
+        if (nextchar != NUL && ! ((afterchar) == ' ' || (afterchar) == '\t') )
         {
             return e_trailing_characters;
         }
@@ -115100,9 +113053,9 @@
     {
         if ((long *)varp == &curbuf->b_p_ul && opt_flags == OPT_LOCAL)
         {
-            value = NO_LOCAL_UNDOLEVEL;
+            value =  (-123456) ;
         }
-        else if (opt_flags == OPT_LOCAL && ((long *)varp == &curwin->w_p_siso || (long *)varp == &curwin->w_p_so || (long *)varp == &curwin->w_p_sop))
+        else if (opt_flags == OPT_LOCAL && ((long *)varp == &curwin-> w_onebuf_opt.wo_siso  || (long *)varp == &curwin-> w_onebuf_opt.wo_so  || (long *)varp == &curwin-> w_onebuf_opt.wo_sop ))
         {
             value = -1;
         }
@@ -115111,7 +113064,7 @@
             value = *(long *)get_varp_scope(&(options[opt_idx]), OPT_GLOBAL);
         }
     }
-    else if (((long *)varp == &p_wc || (long *)varp == &p_wcm) && (*arg == '<' || *arg == '^' || (*arg != NUL && (!arg[1] || VIM_ISWHITE(arg[1])) && !VIM_ISDIGIT(*arg))))
+    else if (((long *)varp == &p_wc || (long *)varp == &p_wcm) && (*arg == '<' || *arg == '^' || (*arg != NUL && (!arg[1] ||  ((arg[1]) == ' ' || (arg[1]) == '\t') ) && ! ((unsigned)(*arg) - '0' < 10) )))
     {
         value = string_to_key(arg, FALSE);
         if (value == 0)
@@ -115120,10 +113073,10 @@
             goto skip;
         }
     }
-    else if (*arg == '-' || VIM_ISDIGIT(*arg))
+    else if (*arg == '-' ||  ((unsigned)(*arg) - '0' < 10) )
     {
-        vim_str2nr(arg, NULL, &i, STR2NR_ALL, &value, NULL, 0, TRUE, NULL);
-        if (i == 0 || (arg[i] != NUL && !VIM_ISWHITE(arg[i])))
+        vim_str2nr(arg, NULL, &i,  (STR2NR_BIN + STR2NR_OCT + STR2NR_HEX + STR2NR_OOCT) , &value, NULL, 0, TRUE, NULL);
+        if (i == 0 || (arg[i] != NUL && ! ((arg[i]) == ' ' || (arg[i]) == '\t') ))
         {
             errmsg = e_number_required_after_equal;
             goto skip;
@@ -115148,7 +113101,7 @@
         value = *(long *)varp - value;
     }
 
-    if ((long *)varp == &curwin->w_p_sop && value < -1)
+    if ((long *)varp == &curwin-> w_onebuf_opt.wo_sop  && value < -1)
     {
         errmsg = e_invalid_argument;
         goto skip;
@@ -115177,7 +113130,7 @@
     else
     {
         ++arg;
-        for (p = arg; *p && !VIM_ISWHITE(*p); ++p)
+        for (p = arg; *p && ! ((*p) == ' ' || (*p) == '\t') ; ++p)
         {
             if (*p == '\\' && p[1] != NUL)
             {
@@ -115303,7 +113256,7 @@
     }
     else
     {
-        while (VIM_ISWHITE(arg[len]))
+        while ( ((arg[len]) == ' ' || (arg[len]) == '\t') )
         {
             ++len;
         }
@@ -115350,8 +113303,8 @@
         varp = NULL;
         if (key < 0)
         {
-            key_name[0] = KEY2TERMCAP0(key);
-            key_name[1] = KEY2TERMCAP1(key);
+            key_name[0] =  ((-(key)) & 0xff) ;
+            key_name[1] =  (((unsigned)(-(key)) >> 8) & 0xff) ;
         }
         else
         {
@@ -115382,7 +113335,7 @@
                 arg += 2;
             }
         }
-        if (vim_strchr((char_u *)"?!&<", nextchar) != NULL && arg[1] != NUL && !VIM_ISWHITE(arg[1]))
+        if (vim_strchr((char_u *)"?!&<", nextchar) != NULL && arg[1] != NUL && ! ((arg[1]) == ' ' || (arg[1]) == '\t') )
         {
             errmsg = e_trailing_characters;
             goto skip;
@@ -115419,7 +113372,7 @@
                 (void)show_one_termcode(key_name, p, TRUE);
             }
         }
-        if (nextchar != '?' && nextchar != NUL && !VIM_ISWHITE(afterchar))
+        if (nextchar != '?' && nextchar != NUL && ! ((afterchar) == ' ' || (afterchar) == '\t') )
         {
             errmsg = e_trailing_characters;
         }
@@ -115450,7 +113403,7 @@
 
     while (*arg != NUL)
     {
-        if (STRNCMP(arg, "all", 3) == 0 && !ASCII_ISALPHA(arg[3]) && !(opt_flags & OPT_MODELINE))
+        if ( strncmp((char *)(arg), (char *)("all"), (3))  == 0 && ! ( ((unsigned)(arg[3]) - 'A' < 26)  ||  ((unsigned)(arg[3]) - 'a' < 26) )  && !(opt_flags & OPT_MODELINE))
         {
             arg += 3;
             if (*arg == '&')
@@ -115467,7 +113420,7 @@
                 did_show = TRUE;
             }
         }
-        else if (STRNCMP(arg, "termcap", 7) == 0 && !(opt_flags & OPT_MODELINE))
+        else if ( strncmp((char *)(arg), (char *)("termcap"), (7))  == 0 && !(opt_flags & OPT_MODELINE))
         {
             showoptions(2, opt_flags);
             show_termcodes(opt_flags);
@@ -115489,7 +113442,7 @@
 
             for (i = 0; i < 2 ; ++i)
             {
-                while (*arg != NUL && !VIM_ISWHITE(*arg))
+                while (*arg != NUL && ! ((*arg) == ' ' || (*arg) == '\t') )
                 {
                     if (*arg++ == '\\' && *arg != NUL)
                     {
@@ -115505,14 +113458,14 @@
 
             if (errmsg != NULL)
             {
-                i = vim_snprintf((char *)IObuff, IOSIZE, "%s", (char_u *)_(errmsg)) + 2;
-                if (i + (arg - startarg) < IOSIZE)
+                i = vim_snprintf((char *)IObuff,  (1024+1) , "%s", (char_u *)_(errmsg)) + 2;
+                if (i + (arg - startarg) <  (1024+1) )
                 {
-                    STRCPY(IObuff + i - 2, ": ");
-                    mch_memmove(IObuff + i, startarg, (arg - startarg));
+                     strcpy((char *)(IObuff + i - 2), (char *)(": ")) ;
+                     memmove((char *)(IObuff + i), (char *)(startarg), (arg - startarg)) ;
                     IObuff[i + (arg - startarg)] = NUL;
                 }
-                trans_characters(IObuff, IOSIZE);
+                trans_characters(IObuff,  (1024+1) );
 
                 ++no_wait_return;
                 emsg((char *)IObuff);
@@ -115549,10 +113502,10 @@
 
     options[opt_idx].flags |= P_WAS_SET;
 
-    flagsp = insecure_flag(curwin, opt_idx, opt_flags);
+    flagsp =  (&options[opt_idx].flags) ;
     if (both)
     {
-        flagsp_local = insecure_flag(curwin, opt_idx, OPT_LOCAL);
+        flagsp_local =  (&options[opt_idx].flags) ;
     }
     if (!value_checked && (secure || (opt_flags & OPT_MODELINE)))
     {
@@ -115581,16 +113534,16 @@
     }
     if (*arg == '^' && arg[1])
     {
-        int key = Ctrl_chr(arg[1]);
+        int key =  ( (((arg[1]) < 'a' || (arg[1]) > 'z') ? (arg[1]) : (arg[1]) - ('a' - 'A'))  ^ 0x40) ;
         if (key == 0)
         {
-            key = K_ZERO;
+            key =   (-((KS_ZERO) + ((int)( ('X') ) << 8)))  ;
         }
         return key;
     }
     if (multi_byte)
     {
-        return PTR2CHAR(arg);
+        return  (has_mbyte ? mb_ptr2char(arg) : (int)*(arg)) ;
     }
     return *arg;
 }
@@ -115670,7 +113623,7 @@
         return NULL;
     }
 
-    if (val != NULL && STRLEN(val) > MAXPATHL)
+    if (val != NULL &&  strlen((char *)(val))  >  PATH_MAX )
     {
         return NULL;
     }
@@ -115683,8 +113636,8 @@
     char_u ** var = (char_u **)options[opt_idx].var;
     int esc = var == &p_tags || var == &p_path;
 
-    expand_env_esc(val, NameBuff, MAXPATHL, esc ? (char_u *)" \t" : NULL, FALSE, NULL);
-    if (STRCMP(NameBuff, val) == 0)
+    expand_env_esc(val, NameBuff,  PATH_MAX , esc ? (char_u *)" \t" : NULL, FALSE, NULL);
+    if ( strcmp((char *)(NameBuff), (char *)(val))  == 0)
     {
         return NULL;
     }
@@ -115710,9 +113663,9 @@
 
     check_opt_wim();
 
-    (void)set_listchars_option(curwin, curwin->w_p_lcs, TRUE, NULL, 0);
+    (void)set_listchars_option(curwin, curwin-> w_onebuf_opt.wo_lcs , TRUE, NULL, 0);
 
-    (void)set_fillchars_option(curwin, curwin->w_p_fcs, TRUE, NULL, 0);
+    (void)set_fillchars_option(curwin, curwin-> w_onebuf_opt.wo_fcs , TRUE, NULL, 0);
 
 }
 
@@ -115771,7 +113724,7 @@
 
     for (s = val; *s != NUL; ++s)
     {
-        if (!ASCII_ISALNUM(*s) && vim_strchr((char_u *)allowed, *s) == NULL)
+        if (! ( ( ((unsigned)(*s) - 'A' < 26)  ||  ((unsigned)(*s) - 'a' < 26) )  ||  ((unsigned)(*s) - '0' < 10) )  && vim_strchr((char_u *)allowed, *s) == NULL)
         {
             return FALSE;
         }
@@ -115823,14 +113776,14 @@
 }
 
     char *
-did_set_compatible(optset_T *args UNUSED)
+did_set_compatible(optset_T *args  __attribute__((unused)) )
 {
     compatible_set();
     return NULL;
 }
 
     char *
-did_set_eof_eol_fixeol_bomb(optset_T *args UNUSED)
+did_set_eof_eol_fixeol_bomb(optset_T *args  __attribute__((unused)) )
 {
     redraw_titles();
     return NULL;
@@ -115848,14 +113801,14 @@
 }
 
     char *
-did_set_hlsearch(optset_T *args UNUSED)
+did_set_hlsearch(optset_T *args  __attribute__((unused)) )
 {
     set_no_hlsearch(FALSE);
     return NULL;
 }
 
     char *
-did_set_ignorecase(optset_T *args UNUSED)
+did_set_ignorecase(optset_T *args  __attribute__((unused)) )
 {
     if (p_hls)
     {
@@ -115865,7 +113818,7 @@
 }
 
     char *
-did_set_iminsert(optset_T *args UNUSED)
+did_set_iminsert(optset_T *args  __attribute__((unused)) )
 {
     char *errmsg = NULL;
 
@@ -115884,7 +113837,7 @@
 }
 
     char *
-did_set_imsearch(optset_T *args UNUSED)
+did_set_imsearch(optset_T *args  __attribute__((unused)) )
 {
     char *errmsg = NULL;
 
@@ -115924,21 +113877,21 @@
 }
 
     char *
-did_set_laststatus(optset_T *args UNUSED)
+did_set_laststatus(optset_T *args  __attribute__((unused)) )
 {
     last_status(FALSE);
     return NULL;
 }
 
     char *
-did_set_lisp(optset_T *args UNUSED)
+did_set_lisp(optset_T *args  __attribute__((unused)) )
 {
     (void)buf_init_chartab(curbuf, FALSE);
     return NULL;
 }
 
     char *
-did_set_maxcombine(optset_T *args UNUSED)
+did_set_maxcombine(optset_T *args  __attribute__((unused)) )
 {
     if (p_mco > MAX_MCO)
     {
@@ -115953,9 +113906,8 @@
 }
 
     char *
-did_set_modifiable(optset_T *args UNUSED)
+did_set_modifiable(optset_T *args  __attribute__((unused)) )
 {
-
     redraw_titles();
 
     return NULL;
@@ -115974,7 +113926,7 @@
 }
 
     char *
-did_set_number_relativenumber(optset_T *args UNUSED)
+did_set_number_relativenumber(optset_T *args  __attribute__((unused)) )
 {
     return NULL;
 }
@@ -115992,7 +113944,7 @@
 }
 
     char *
-did_set_paste(optset_T *args UNUSED)
+did_set_paste(optset_T *args  __attribute__((unused)) )
 {
     static int  old_p_paste = FALSE;
     static int  save_sm = 0;
@@ -116004,7 +113956,7 @@
     {
         if (!old_p_paste)
         {
-            FOR_ALL_BUFFERS(buf)
+             for ((buf) = firstbuf; (buf) != NULL; (buf) = (buf)->b_next) 
             {
                 buf->b_p_tw_nopaste = buf->b_p_tw;
                 buf->b_p_wm_nopaste = buf->b_p_wm;
@@ -116023,7 +113975,7 @@
             p_wm_nopaste = p_wm;
         }
 
-        FOR_ALL_BUFFERS(buf)
+         for ((buf) = firstbuf; (buf) != NULL; (buf) = (buf)->b_next) 
         {
             buf->b_p_tw = 0;
             buf->b_p_wm = 0;
@@ -116048,7 +114000,7 @@
 
     else if (old_p_paste)
     {
-        FOR_ALL_BUFFERS(buf)
+         for ((buf) = firstbuf; (buf) != NULL; (buf) = (buf)->b_next) 
         {
             buf->b_p_tw = buf->b_p_tw_nopaste;
             buf->b_p_wm = buf->b_p_wm_nopaste;
@@ -116095,9 +114047,9 @@
 }
 
     char *
-did_set_scrollbind(optset_T *args UNUSED)
+did_set_scrollbind(optset_T *args  __attribute__((unused)) )
 {
-    if (!curwin->w_p_scb)
+    if (!curwin-> w_onebuf_opt.wo_scb )
     {
         return NULL;
     }
@@ -116108,10 +114060,9 @@
 }
 
     char *
-did_set_maxsearchcount(optset_T *args UNUSED)
+did_set_maxsearchcount(optset_T *args  __attribute__((unused)) )
 {
     char        *errmsg = NULL;
-#define MAX_SEARCH_COUNT 9999
 
     if (p_msc <= 0)
     {
@@ -116177,16 +114128,16 @@
 }
 
     char *
-did_set_showtabline(optset_T *args UNUSED)
+did_set_showtabline(optset_T *args  __attribute__((unused)) )
 {
     shell_new_rows();
     return NULL;
 }
 
     char *
-did_set_smoothscroll(optset_T *args UNUSED)
+did_set_smoothscroll(optset_T *args  __attribute__((unused)) )
 {
-    if (!curwin->w_p_sms)
+    if (!curwin-> w_onebuf_opt.wo_sms )
     {
         curwin->w_skipcol = 0;
     }
@@ -116195,7 +114146,7 @@
 }
 
     char *
-did_set_swapfile(optset_T *args UNUSED)
+did_set_swapfile(optset_T *args  __attribute__((unused)) )
 {
     if (curbuf->b_p_swf && p_uc)
     {
@@ -116209,7 +114160,7 @@
 }
 
     char *
-did_set_termsync(optset_T *args UNUSED)
+did_set_termsync(optset_T *args  __attribute__((unused)) )
 {
     if (!p_tsy)
     {
@@ -116219,7 +114170,7 @@
 }
 
     char *
-did_set_terse(optset_T *args UNUSED)
+did_set_terse(optset_T *args  __attribute__((unused)) )
 {
     char_u      *p;
 
@@ -116227,13 +114178,13 @@
 
     if (p_terse && p == NULL)
     {
-        STRCPY(IObuff, p_shm);
-        STRCAT(IObuff, "s");
+         strcpy((char *)(IObuff), (char *)(p_shm)) ;
+         strcat((char *)(IObuff), (char *)("s")) ;
         set_string_option_direct((char_u *)"shm", -1, IObuff, OPT_FREE, 0);
     }
     else if (!p_terse && p != NULL)
     {
-        STRMOVE(p, p + 1);
+          memmove((char *)((p)), (char *)((p + 1)),  strlen((char *)(p + 1))  + 1)  ;
     }
     return NULL;
 }
@@ -116241,7 +114192,7 @@
     char *
 did_set_textauto(optset_T *args)
 {
-    set_string_option_direct((char_u *)"ffs", -1, p_ta ? (char_u *)DFLT_FFS_VIM : (char_u *)"", OPT_FREE | args->os_flags, 0);
+    set_string_option_direct((char_u *)"ffs", -1, p_ta ? (char_u *) "unix,dos"  : (char_u *)"", OPT_FREE | args->os_flags, 0);
 
     return NULL;
 }
@@ -116255,7 +114206,7 @@
 }
 
     char *
-did_set_textwidth(optset_T *args UNUSED)
+did_set_textwidth(optset_T *args  __attribute__((unused)) )
 {
     char *errmsg = NULL;
 
@@ -116274,7 +114225,7 @@
 }
 
     char *
-did_set_title_icon(optset_T *args UNUSED)
+did_set_title_icon(optset_T *args  __attribute__((unused)) )
 {
     did_set_title();
     return NULL;
@@ -116302,7 +114253,6 @@
     static void
 update_global_undolevels(long value, long old_value)
 {
-
     p_ul = old_value;
     u_sync(TRUE);
     p_ul = value;
@@ -116357,13 +114307,13 @@
 {
     if (p_wiv && !args->os_oldval.boolean)
     {
-        T_XS = (char_u *)"y";
+         ( term_strings[(int)(KS_XS)] )  = (char_u *)"y";
     }
     else if (!p_wiv && args->os_oldval.boolean)
     {
-        T_XS = empty_option;
+         ( term_strings[(int)(KS_XS)] )  = empty_option;
     }
-    p_wiv = (*T_XS != NUL);
+    p_wiv = (* ( term_strings[(int)(KS_XS)] )  != NUL);
 
     return NULL;
 }
@@ -116373,7 +114323,7 @@
 {
     long c = *(long *)args->os_varp;
 
-    if (c == Ctrl_C || c == '\n' || c == '\r' || c == K_KENTER)
+    if (c == Ctrl_C || c == '\n' || c == '\r' || c ==   (-(('K') + ((int)('A') << 8)))  )
     {
         return e_invalid_argument;
     }
@@ -116382,7 +114332,7 @@
 }
 
     char *
-did_set_window(optset_T *args UNUSED)
+did_set_window(optset_T *args  __attribute__((unused)) )
 {
     if (p_window < 1)
     {
@@ -116417,7 +114367,7 @@
         p_hh = 0;
     }
 
-    if (!ONE_WINDOW)
+    if (! (firstwin == lastwin) )
     {
         if (pp == &p_wh && curwin->w_height < p_wh)
         {
@@ -116433,7 +114383,7 @@
 }
 
     char *
-did_set_winminheight(optset_T *args UNUSED)
+did_set_winminheight(optset_T *args  __attribute__((unused)) )
 {
     char *errmsg = NULL;
 
@@ -116453,7 +114403,7 @@
 }
 
     char *
-did_set_winminwidth(optset_T *args UNUSED)
+did_set_winminwidth(optset_T *args  __attribute__((unused)) )
 {
     char *errmsg = NULL;
 
@@ -116473,7 +114423,7 @@
 }
 
     char *
-did_set_winwidth(optset_T *args UNUSED)
+did_set_winwidth(optset_T *args  __attribute__((unused)) )
 {
     char *errmsg = NULL;
 
@@ -116488,7 +114438,7 @@
         p_wiw = p_wmw;
     }
 
-    if (!ONE_WINDOW && curwin->w_width < p_wiw)
+    if (! (firstwin == lastwin)  && curwin->w_width < p_wiw)
     {
         win_setwidth((int)p_wiw);
     }
@@ -116497,9 +114447,9 @@
 }
 
     char *
-did_set_wrap(optset_T *args UNUSED)
+did_set_wrap(optset_T *args  __attribute__((unused)) )
 {
-    if (curwin->w_p_wrap)
+    if (curwin-> w_onebuf_opt.wo_wrap )
     {
         curwin->w_leftcol = 0;
     }
@@ -116533,7 +114483,7 @@
     {
         optset_T args;
 
-        CLEAR_FIELD(args);
+          memset((&(args)), (0), (sizeof(args)))  ;
         args.os_varp = varp;
         args.os_flags = opt_flags;
         args.os_oldval.boolean = old_value;
@@ -116615,23 +114565,23 @@
         errmsg = e_argument_must_be_positive;
         p_tm = 0;
     }
-    if ((curwin->w_p_scr <= 0 || (curwin->w_p_scr > curwin->w_height && curwin->w_height > 0)) && full_screen)
+    if ((curwin-> w_onebuf_opt.wo_scr  <= 0 || (curwin-> w_onebuf_opt.wo_scr  > curwin->w_height && curwin->w_height > 0)) && full_screen)
     {
-        if (pp == &(curwin->w_p_scr))
+        if (pp == &(curwin-> w_onebuf_opt.wo_scr ))
         {
-            if (curwin->w_p_scr != 0)
+            if (curwin-> w_onebuf_opt.wo_scr  != 0)
             {
                 errmsg = e_invalid_scroll_size;
             }
             win_comp_scroll(curwin);
         }
-        else if (curwin->w_p_scr <= 0)
+        else if (curwin-> w_onebuf_opt.wo_scr  <= 0)
         {
-            curwin->w_p_scr = 1;
+            curwin-> w_onebuf_opt.wo_scr  = 1;
         }
         else
         {
-            curwin->w_p_scr = curwin->w_height;
+            curwin-> w_onebuf_opt.wo_scr  = curwin->w_height;
         }
     }
     if (p_hi < 0)
@@ -116725,7 +114675,7 @@
     {
         optset_T args;
 
-        CLEAR_FIELD(args);
+          memset((&(args)), (0), (sizeof(args)))  ;
         args.os_varp = varp;
         args.os_flags = opt_flags;
         args.os_oldval.number = old_value;
@@ -116822,7 +114772,7 @@
                 }
                 else
                 {
-                    quick_tab[CharOrdLow(s[0])] = opt_idx;
+                    quick_tab[ ((s[0]) - 'a') ] = opt_idx;
                 }
             }
             p = s;
@@ -116841,11 +114791,11 @@
     }
     else
     {
-        opt_idx = quick_tab[CharOrdLow(arg[0])];
+        opt_idx = quick_tab[ ((arg[0]) - 'a') ];
     }
     for (; (s = options[opt_idx].fullname) != NULL && s[0] == arg[0]; opt_idx++)
     {
-        if (STRCMP(arg, s) == 0)
+        if ( strcmp((char *)(arg), (char *)(s))  == 0)
         {
             break;
         }
@@ -116856,11 +114806,11 @@
     }
     if (s == NULL && !is_term_opt)
     {
-        opt_idx = quick_tab[CharOrdLow(arg[0])];
+        opt_idx = quick_tab[ ((arg[0]) - 'a') ];
         for ( ; options[opt_idx].fullname != NULL; opt_idx++)
         {
             s = options[opt_idx].shortname;
-            if (s != NULL && STRCMP(arg, s) == 0)
+            if (s != NULL &&  strcmp((char *)(arg), (char *)(s))  == 0)
             {
                 break;
             }
@@ -116901,7 +114851,7 @@
     int
 is_window_local_option(int opt_idx)
 {
-    return options[opt_idx].var == VAR_WIN;
+    return options[opt_idx].var ==  ((char_u *)-1) ;
 }
 
     int
@@ -116924,14 +114874,14 @@
     {
         int key;
 
-        if (STRLEN(name) == 4 && name[0] == 't' && name[1] == '_' && (key = find_key_option(name, FALSE)) != 0)
+        if ( strlen((char *)(name))  == 4 && name[0] == 't' && name[1] == '_' && (key = find_key_option(name, FALSE)) != 0)
         {
             char_u key_name[2];
 
             if (key < 0)
             {
-                key_name[0] = KEY2TERMCAP0(key);
-                key_name[1] = KEY2TERMCAP1(key);
+                key_name[0] =  ((-(key)) & 0xff) ;
+                key_name[1] =  (((unsigned)(-(key)) >> 8) & 0xff) ;
             }
             else
             {
@@ -117059,7 +115009,7 @@
     {
         if (!has_lt || arg[4] == '>')
         {
-            key = TERMCAP2KEY(arg[2], arg[3]);
+            key =  (-((arg[2]) + ((int)(arg[3]) << 8))) ;
         }
     }
     else if (has_lt)
@@ -117091,10 +115041,7 @@
     int                 i;
     int                 len;
 
-#define INC 20
-#define GAP 3
-
-    items = ALLOC_MULT(struct vimoption *, OPTION_COUNT);
+    items =  (struct vimoption * *)alloc(sizeof(struct vimoption *) * (  (sizeof(options) / sizeof((options)[0]))  )) ;
     if (items == NULL)
     {
         return;
@@ -117153,7 +115100,7 @@
                 else
                 {
                     option_value2string(p, opt_flags);
-                    len = (int)STRLEN(p->fullname) + vim_strsize(NameBuff) + 1;
+                    len = (int) strlen((char *)(p->fullname))  + vim_strsize(NameBuff) + 1;
                 }
                 if ((len <= INC - GAP && run == 1) || (len > INC - GAP && run == 2))
                 {
@@ -117214,7 +115161,7 @@
     {
         return (*(int *)varp == (int)(long)(long_i)p->def_val[dvi]);
     }
-    return (STRCMP(*(char_u **)varp, p->def_val[dvi]) == 0);
+    return ( strcmp((char *)(*(char_u **)varp), (char *)(p->def_val[dvi]))  == 0);
 }
 
     static void
@@ -117293,7 +115240,7 @@
             round = 2;
             if (p->indir != PV_NONE)
             {
-                if (p->var == VAR_WIN)
+                if (p->var ==  ((char_u *)-1) )
                 {
                     if (!(opt_flags & OPT_LOCAL))
                     {
@@ -117349,7 +115296,7 @@
 
                     legacy = false;
 
-                    if (p->indir == PV_FT)
+                    if (p->indir ==   (idopt_T)(PV_BUF + (int)(BV_FT))  )
                     {
                         if (fprintf(fd, "if &%s != '%s'", p->fullname, *(char_u **)(varp)) < 0 || put_eol(fd) < 0)
                         {
@@ -117407,7 +115354,7 @@
         }
         else if ((flags & P_EXPAND) != 0)
         {
-            int  size = (int)STRLEN(*valuep) + 1;
+            int  size = (int) strlen((char *)(*valuep))  + 1;
 
             buf = alloc(size);
             if (buf == NULL)
@@ -117416,7 +115363,7 @@
             }
             home_replace(NULL, *valuep, buf, size, FALSE);
 
-            if (size >= MAXPATHL && (flags & P_COMMA) != 0 && vim_strchr(*valuep, ',') != NULL)
+            if (size >=  PATH_MAX  && (flags & P_COMMA) != 0 && vim_strchr(*valuep, ',') != NULL)
             {
                 part = alloc(size);
                 if (part == NULL)
@@ -117518,7 +115465,7 @@
 clear_termoptions(void)
 {
     mch_setmouse(FALSE);
-    mch_restore_title(SAVE_RESTORE_BOTH);
+    mch_restore_title( (SAVE_RESTORE_TITLE | SAVE_RESTORE_ICON) );
     stoptermcap();
 
     free_termoptions();
@@ -117610,9 +115557,9 @@
 {
     if ((scope & OPT_GLOBAL) && p->indir != PV_NONE)
     {
-        if (p->var == VAR_WIN)
+        if (p->var ==  ((char_u *)-1) )
         {
-            return (char_u *)GLOBAL_WO(get_varp(p));
+            return (char_u *) ((char *)(get_varp(p)) + sizeof(winopt_T)) ;
         }
         return p->var;
     }
@@ -117620,50 +115567,50 @@
     {
         switch ((int)p->indir)
         {
-            case PV_FP:
+            case   (idopt_T)(PV_BOTH + (int)( (idopt_T)(PV_BUF + (int)(BV_FP)) ))  :
                 return (char_u *)&(curbuf->b_p_fp);
-            case PV_FS:
+            case   (idopt_T)(PV_BOTH + (int)( (idopt_T)(PV_BUF + (int)(BV_FS)) ))  :
                 return (char_u *)&(curbuf->b_p_fs);
-            case PV_EP:
+            case   (idopt_T)(PV_BOTH + (int)( (idopt_T)(PV_BUF + (int)(BV_EP)) ))  :
                 return (char_u *)&(curbuf->b_p_ep);
-            case PV_KP:
+            case   (idopt_T)(PV_BOTH + (int)( (idopt_T)(PV_BUF + (int)(BV_KP)) ))  :
                 return (char_u *)&(curbuf->b_p_kp);
-            case PV_PATH:
+            case   (idopt_T)(PV_BOTH + (int)( (idopt_T)(PV_BUF + (int)(BV_PATH)) ))  :
                 return (char_u *)&(curbuf->b_p_path);
-            case PV_AC:
+            case   (idopt_T)(PV_BOTH + (int)( (idopt_T)(PV_BUF + (int)(BV_AC)) ))  :
                 return (char_u *)&(curbuf->b_p_ac);
-            case PV_AR:
+            case   (idopt_T)(PV_BOTH + (int)( (idopt_T)(PV_BUF + (int)(BV_AR)) ))  :
                 return (char_u *)&(curbuf->b_p_ar);
-            case PV_TAGS:
+            case   (idopt_T)(PV_BOTH + (int)( (idopt_T)(PV_BUF + (int)(BV_TAGS)) ))  :
                 return (char_u *)&(curbuf->b_p_tags);
-            case PV_TC:
+            case   (idopt_T)(PV_BOTH + (int)( (idopt_T)(PV_BUF + (int)(BV_TC)) ))  :
                 return (char_u *)&(curbuf->b_p_tc);
-            case PV_SISO:
-                return (char_u *)&(curwin->w_p_siso);
-            case PV_SO:
-                return (char_u *)&(curwin->w_p_so);
-            case PV_SOP:
-                return (char_u *)&(curwin->w_p_sop);
-            case PV_COT:
+            case   (idopt_T)(PV_BOTH + (int)( (idopt_T)(PV_WIN + (int)(WV_SISO)) ))  :
+                return (char_u *)&(curwin-> w_onebuf_opt.wo_siso );
+            case   (idopt_T)(PV_BOTH + (int)( (idopt_T)(PV_WIN + (int)(WV_SO)) ))  :
+                return (char_u *)&(curwin-> w_onebuf_opt.wo_so );
+            case   (idopt_T)(PV_BOTH + (int)( (idopt_T)(PV_WIN + (int)(WV_SOP)) ))  :
+                return (char_u *)&(curwin-> w_onebuf_opt.wo_sop );
+            case   (idopt_T)(PV_BOTH + (int)( (idopt_T)(PV_BUF + (int)(BV_COT)) ))  :
                 return (char_u *)&(curbuf->b_p_cot);
-            case PV_DICT:
+            case   (idopt_T)(PV_BOTH + (int)( (idopt_T)(PV_BUF + (int)(BV_DICT)) ))  :
                 return (char_u *)&(curbuf->b_p_dict);
-            case PV_TSR:
+            case   (idopt_T)(PV_BOTH + (int)( (idopt_T)(PV_BUF + (int)(BV_TSR)) ))  :
                 return (char_u *)&(curbuf->b_p_tsr);
-            case PV_UL:
+            case   (idopt_T)(PV_BOTH + (int)( (idopt_T)(PV_BUF + (int)(BV_UL)) ))  :
                 return (char_u *)&(curbuf->b_p_ul);
-            case PV_LW:
+            case   (idopt_T)(PV_BOTH + (int)( (idopt_T)(PV_BUF + (int)(BV_LW)) ))  :
                 return (char_u *)&(curbuf->b_p_lw);
-            case PV_BKC:
+            case   (idopt_T)(PV_BOTH + (int)( (idopt_T)(PV_BUF + (int)(BV_BKC)) ))  :
                 return (char_u *)&(curbuf->b_p_bkc);
-            case PV_MENC:
+            case   (idopt_T)(PV_BOTH + (int)( (idopt_T)(PV_BUF + (int)(BV_MENC)) ))  :
                 return (char_u *)&(curbuf->b_p_menc);
-            case PV_LCS:
-                return (char_u *)&(curwin->w_p_lcs);
-            case PV_FCS:
-                return (char_u *)&(curwin->w_p_fcs);
-            case PV_VE:
-                return (char_u *)&(curwin->w_p_ve);
+            case   (idopt_T)(PV_BOTH + (int)( (idopt_T)(PV_WIN + (int)(WV_LCS)) ))  :
+                return (char_u *)&(curwin-> w_onebuf_opt.wo_lcs );
+            case   (idopt_T)(PV_BOTH + (int)( (idopt_T)(PV_WIN + (int)(WV_FCS)) ))  :
+                return (char_u *)&(curwin-> w_onebuf_opt.wo_fcs );
+            case   (idopt_T)(PV_BOTH + (int)( (idopt_T)(PV_WIN + (int)(WV_VE)) ))  :
+                return (char_u *)&(curwin-> w_onebuf_opt.wo_ve );
 
         }
         return NULL;
@@ -117690,196 +115637,196 @@
         case PV_NONE:
             return p->var;
 
-        case PV_EP:
+        case   (idopt_T)(PV_BOTH + (int)( (idopt_T)(PV_BUF + (int)(BV_EP)) ))  :
             return *curbuf->b_p_ep != NUL
                                     ? (char_u *)&curbuf->b_p_ep : p->var;
-        case PV_KP:
+        case   (idopt_T)(PV_BOTH + (int)( (idopt_T)(PV_BUF + (int)(BV_KP)) ))  :
             return *curbuf->b_p_kp != NUL
                                     ? (char_u *)&curbuf->b_p_kp : p->var;
-        case PV_PATH:
+        case   (idopt_T)(PV_BOTH + (int)( (idopt_T)(PV_BUF + (int)(BV_PATH)) ))  :
             return *curbuf->b_p_path != NUL
                                     ? (char_u *)&(curbuf->b_p_path) : p->var;
-        case PV_AC:
+        case   (idopt_T)(PV_BOTH + (int)( (idopt_T)(PV_BUF + (int)(BV_AC)) ))  :
             return curbuf->b_p_ac >= 0
                                     ? (char_u *)&(curbuf->b_p_ac) : p->var;
-        case PV_AR:
+        case   (idopt_T)(PV_BOTH + (int)( (idopt_T)(PV_BUF + (int)(BV_AR)) ))  :
             return curbuf->b_p_ar >= 0
                                     ? (char_u *)&(curbuf->b_p_ar) : p->var;
-        case PV_TAGS:
+        case   (idopt_T)(PV_BOTH + (int)( (idopt_T)(PV_BUF + (int)(BV_TAGS)) ))  :
             return *curbuf->b_p_tags != NUL
                                     ? (char_u *)&(curbuf->b_p_tags) : p->var;
-        case PV_TC:
+        case   (idopt_T)(PV_BOTH + (int)( (idopt_T)(PV_BUF + (int)(BV_TC)) ))  :
             return *curbuf->b_p_tc != NUL
                                     ? (char_u *)&(curbuf->b_p_tc) : p->var;
-        case PV_BKC:
+        case   (idopt_T)(PV_BOTH + (int)( (idopt_T)(PV_BUF + (int)(BV_BKC)) ))  :
             return *curbuf->b_p_bkc != NUL
                                     ? (char_u *)&(curbuf->b_p_bkc) : p->var;
-        case PV_SISO:
-            return curwin->w_p_siso >= 0
-                                    ? (char_u *)&(curwin->w_p_siso) : p->var;
-        case PV_SO:
-            return curwin->w_p_so >= 0
-                                    ? (char_u *)&(curwin->w_p_so) : p->var;
-        case PV_SOP:
-            return curwin->w_p_sop != -1
-                                    ? (char_u *)&(curwin->w_p_sop) : p->var;
-        case PV_COT:
+        case   (idopt_T)(PV_BOTH + (int)( (idopt_T)(PV_WIN + (int)(WV_SISO)) ))  :
+            return curwin-> w_onebuf_opt.wo_siso  >= 0
+                                    ? (char_u *)&(curwin-> w_onebuf_opt.wo_siso ) : p->var;
+        case   (idopt_T)(PV_BOTH + (int)( (idopt_T)(PV_WIN + (int)(WV_SO)) ))  :
+            return curwin-> w_onebuf_opt.wo_so  >= 0
+                                    ? (char_u *)&(curwin-> w_onebuf_opt.wo_so ) : p->var;
+        case   (idopt_T)(PV_BOTH + (int)( (idopt_T)(PV_WIN + (int)(WV_SOP)) ))  :
+            return curwin-> w_onebuf_opt.wo_sop  != -1
+                                    ? (char_u *)&(curwin-> w_onebuf_opt.wo_sop ) : p->var;
+        case   (idopt_T)(PV_BOTH + (int)( (idopt_T)(PV_BUF + (int)(BV_COT)) ))  :
             return *curbuf->b_p_cot != NUL
                                     ? (char_u *)&(curbuf->b_p_cot) : p->var;
-        case PV_DICT:
+        case   (idopt_T)(PV_BOTH + (int)( (idopt_T)(PV_BUF + (int)(BV_DICT)) ))  :
             return *curbuf->b_p_dict != NUL
                                     ? (char_u *)&(curbuf->b_p_dict) : p->var;
-        case PV_TSR:
+        case   (idopt_T)(PV_BOTH + (int)( (idopt_T)(PV_BUF + (int)(BV_TSR)) ))  :
             return *curbuf->b_p_tsr != NUL
                                     ? (char_u *)&(curbuf->b_p_tsr) : p->var;
-        case PV_FP:
+        case   (idopt_T)(PV_BOTH + (int)( (idopt_T)(PV_BUF + (int)(BV_FP)) ))  :
             return *curbuf->b_p_fp != NUL
                                     ? (char_u *)&(curbuf->b_p_fp) : p->var;
-        case PV_FS:
+        case   (idopt_T)(PV_BOTH + (int)( (idopt_T)(PV_BUF + (int)(BV_FS)) ))  :
             return curbuf->b_p_fs >= 0
                                     ? (char_u *)&(curbuf->b_p_fs) : p->var;
-        case PV_UL:
-            return curbuf->b_p_ul != NO_LOCAL_UNDOLEVEL
+        case   (idopt_T)(PV_BOTH + (int)( (idopt_T)(PV_BUF + (int)(BV_UL)) ))  :
+            return curbuf->b_p_ul !=  (-123456) 
                                     ? (char_u *)&(curbuf->b_p_ul) : p->var;
-        case PV_LW:
+        case   (idopt_T)(PV_BOTH + (int)( (idopt_T)(PV_BUF + (int)(BV_LW)) ))  :
             return *curbuf->b_p_lw != NUL
                                     ? (char_u *)&(curbuf->b_p_lw) : p->var;
-        case PV_MENC:
+        case   (idopt_T)(PV_BOTH + (int)( (idopt_T)(PV_BUF + (int)(BV_MENC)) ))  :
             return *curbuf->b_p_menc != NUL
                                     ? (char_u *)&(curbuf->b_p_menc) : p->var;
-        case PV_LIST:
-            return (char_u *)&(curwin->w_p_list);
-        case PV_LCS:
-            return *curwin->w_p_lcs != NUL
-                                    ? (char_u *)&(curwin->w_p_lcs) : p->var;
-        case PV_FCS:
-            return *curwin->w_p_fcs != NUL
-                                    ? (char_u *)&(curwin->w_p_fcs) : p->var;
-        case PV_VE:
-            return *curwin->w_p_ve != NUL
-                                    ? (char_u *)&(curwin->w_p_ve) : p->var;
-        case PV_EIW:
-            return (char_u *)&(curwin->w_p_eiw);
-        case PV_NU:
-            return (char_u *)&(curwin->w_p_nu);
-        case PV_RNU:
-            return (char_u *)&(curwin->w_p_rnu);
-        case PV_WFB:
-            return (char_u *)&(curwin->w_p_wfb);
-        case PV_WFH:
-            return (char_u *)&(curwin->w_p_wfh);
-        case PV_WFW:
-            return (char_u *)&(curwin->w_p_wfw);
-        case PV_SCROLL:
-            return (char_u *)&(curwin->w_p_scr);
-        case PV_SMS:
-            return (char_u *)&(curwin->w_p_sms);
-        case PV_WRAP:
-            return (char_u *)&(curwin->w_p_wrap);
-        case PV_WCR:
-            return (char_u *)&(curwin->w_p_wcr);
-        case PV_SCBIND:
-            return (char_u *)&(curwin->w_p_scb);
-        case PV_CRBIND:
-            return (char_u *)&(curwin->w_p_crb);
-        case PV_WHL:
-            return (char_u *)&(curwin->w_p_whl);
+        case   (idopt_T)(PV_WIN + (int)(WV_LIST))  :
+            return (char_u *)&(curwin-> w_onebuf_opt.wo_list );
+        case   (idopt_T)(PV_BOTH + (int)( (idopt_T)(PV_WIN + (int)(WV_LCS)) ))  :
+            return *curwin-> w_onebuf_opt.wo_lcs  != NUL
+                                    ? (char_u *)&(curwin-> w_onebuf_opt.wo_lcs ) : p->var;
+        case   (idopt_T)(PV_BOTH + (int)( (idopt_T)(PV_WIN + (int)(WV_FCS)) ))  :
+            return *curwin-> w_onebuf_opt.wo_fcs  != NUL
+                                    ? (char_u *)&(curwin-> w_onebuf_opt.wo_fcs ) : p->var;
+        case   (idopt_T)(PV_BOTH + (int)( (idopt_T)(PV_WIN + (int)(WV_VE)) ))  :
+            return *curwin-> w_onebuf_opt.wo_ve  != NUL
+                                    ? (char_u *)&(curwin-> w_onebuf_opt.wo_ve ) : p->var;
+        case   (idopt_T)(PV_WIN + (int)(WV_EIW))  :
+            return (char_u *)&(curwin-> w_onebuf_opt.wo_eiw );
+        case   (idopt_T)(PV_WIN + (int)(WV_NU))  :
+            return (char_u *)&(curwin-> w_onebuf_opt.wo_nu );
+        case   (idopt_T)(PV_WIN + (int)(WV_RNU))  :
+            return (char_u *)&(curwin-> w_onebuf_opt.wo_rnu );
+        case   (idopt_T)(PV_WIN + (int)(WV_WFB))  :
+            return (char_u *)&(curwin-> w_onebuf_opt.wo_wfb );
+        case   (idopt_T)(PV_WIN + (int)(WV_WFH))  :
+            return (char_u *)&(curwin-> w_onebuf_opt.wo_wfh );
+        case   (idopt_T)(PV_WIN + (int)(WV_WFW))  :
+            return (char_u *)&(curwin-> w_onebuf_opt.wo_wfw );
+        case   (idopt_T)(PV_WIN + (int)(WV_SCROLL))  :
+            return (char_u *)&(curwin-> w_onebuf_opt.wo_scr );
+        case   (idopt_T)(PV_WIN + (int)(WV_SMS))  :
+            return (char_u *)&(curwin-> w_onebuf_opt.wo_sms );
+        case   (idopt_T)(PV_WIN + (int)(WV_WRAP))  :
+            return (char_u *)&(curwin-> w_onebuf_opt.wo_wrap );
+        case   (idopt_T)(PV_WIN + (int)(WV_WCR))  :
+            return (char_u *)&(curwin-> w_onebuf_opt.wo_wcr );
+        case   (idopt_T)(PV_WIN + (int)(WV_SCBIND))  :
+            return (char_u *)&(curwin-> w_onebuf_opt.wo_scb );
+        case   (idopt_T)(PV_WIN + (int)(WV_CRBIND))  :
+            return (char_u *)&(curwin-> w_onebuf_opt.wo_crb );
+        case   (idopt_T)(PV_WIN + (int)(WV_WHL))  :
+            return (char_u *)&(curwin-> w_onebuf_opt.wo_whl );
 
-        case PV_AI:
+        case   (idopt_T)(PV_BUF + (int)(BV_AI))  :
             return (char_u *)&(curbuf->b_p_ai);
-        case PV_BIN:
+        case   (idopt_T)(PV_BUF + (int)(BV_BIN))  :
             return (char_u *)&(curbuf->b_p_bin);
-        case PV_BOMB:
+        case   (idopt_T)(PV_BUF + (int)(BV_BOMB))  :
             return (char_u *)&(curbuf->b_p_bomb);
-        case PV_BH:
+        case   (idopt_T)(PV_BUF + (int)(BV_BH))  :
             return (char_u *)&(curbuf->b_p_bh);
-        case PV_BT:
+        case   (idopt_T)(PV_BUF + (int)(BV_BT))  :
             return (char_u *)&(curbuf->b_p_bt);
-        case PV_BL:
+        case   (idopt_T)(PV_BUF + (int)(BV_BL))  :
             return (char_u *)&(curbuf->b_p_bl);
-        case PV_CI:
+        case   (idopt_T)(PV_BUF + (int)(BV_CI))  :
             return (char_u *)&(curbuf->b_p_ci);
-        case PV_CIN:
+        case   (idopt_T)(PV_BUF + (int)(BV_CIN))  :
             return (char_u *)&(curbuf->b_p_cin);
-        case PV_CINK:
+        case   (idopt_T)(PV_BUF + (int)(BV_CINK))  :
             return (char_u *)&(curbuf->b_p_cink);
-        case PV_CINO:
+        case   (idopt_T)(PV_BUF + (int)(BV_CINO))  :
             return (char_u *)&(curbuf->b_p_cino);
-        case PV_CINSD:
+        case   (idopt_T)(PV_BUF + (int)(BV_CINSD))  :
             return (char_u *)&(curbuf->b_p_cinsd);
-        case PV_CINW:
+        case   (idopt_T)(PV_BUF + (int)(BV_CINW))  :
             return (char_u *)&(curbuf->b_p_cinw);
-        case PV_COM:
+        case   (idopt_T)(PV_BUF + (int)(BV_COM))  :
             return (char_u *)&(curbuf->b_p_com);
-        case PV_CMS:
+        case   (idopt_T)(PV_BUF + (int)(BV_CMS))  :
             return (char_u *)&(curbuf->b_p_cms);
-        case PV_CPT:
+        case   (idopt_T)(PV_BUF + (int)(BV_CPT))  :
             return (char_u *)&(curbuf->b_p_cpt);
-        case PV_EOF:
+        case   (idopt_T)(PV_BUF + (int)(BV_EOF))  :
             return (char_u *)&(curbuf->b_p_eof);
-        case PV_EOL:
+        case   (idopt_T)(PV_BUF + (int)(BV_EOL))  :
             return (char_u *)&(curbuf->b_p_eol);
-        case PV_FIXEOL:
+        case   (idopt_T)(PV_BUF + (int)(BV_FIXEOL))  :
             return (char_u *)&(curbuf->b_p_fixeol);
-        case PV_ET:
+        case   (idopt_T)(PV_BUF + (int)(BV_ET))  :
             return (char_u *)&(curbuf->b_p_et);
-        case PV_FENC:
+        case   (idopt_T)(PV_BUF + (int)(BV_FENC))  :
             return (char_u *)&(curbuf->b_p_fenc);
-        case PV_FF:
+        case   (idopt_T)(PV_BUF + (int)(BV_FF))  :
             return (char_u *)&(curbuf->b_p_ff);
-        case PV_FT:
+        case   (idopt_T)(PV_BUF + (int)(BV_FT))  :
             return (char_u *)&(curbuf->b_p_ft);
-        case PV_FO:
+        case   (idopt_T)(PV_BUF + (int)(BV_FO))  :
             return (char_u *)&(curbuf->b_p_fo);
-        case PV_FLP:
+        case   (idopt_T)(PV_BUF + (int)(BV_FLP))  :
             return (char_u *)&(curbuf->b_p_flp);
-        case PV_IMI:
+        case   (idopt_T)(PV_BUF + (int)(BV_IMI))  :
             return (char_u *)&(curbuf->b_p_iminsert);
-        case PV_IMS:
+        case   (idopt_T)(PV_BUF + (int)(BV_IMS))  :
             return (char_u *)&(curbuf->b_p_imsearch);
-        case PV_INF:
+        case   (idopt_T)(PV_BUF + (int)(BV_INF))  :
             return (char_u *)&(curbuf->b_p_inf);
-        case PV_ISK:
+        case   (idopt_T)(PV_BUF + (int)(BV_ISK))  :
             return (char_u *)&(curbuf->b_p_isk);
-        case PV_LISP:
+        case   (idopt_T)(PV_BUF + (int)(BV_LISP))  :
             return (char_u *)&(curbuf->b_p_lisp);
-        case PV_LOP:
+        case   (idopt_T)(PV_BUF + (int)(BV_LOP))  :
             return (char_u *)&(curbuf->b_p_lop);
-        case PV_ML:
+        case   (idopt_T)(PV_BUF + (int)(BV_ML))  :
             return (char_u *)&(curbuf->b_p_ml);
-        case PV_MPS:
+        case   (idopt_T)(PV_BUF + (int)(BV_MPS))  :
             return (char_u *)&(curbuf->b_p_mps);
-        case PV_MA:
+        case   (idopt_T)(PV_BUF + (int)(BV_MA))  :
             return (char_u *)&(curbuf->b_p_ma);
-        case PV_MOD:
+        case   (idopt_T)(PV_BUF + (int)(BV_MOD))  :
             return (char_u *)&(curbuf->b_changed);
-        case PV_NF:
+        case   (idopt_T)(PV_BUF + (int)(BV_NF))  :
             return (char_u *)&(curbuf->b_p_nf);
-        case PV_PI:
+        case   (idopt_T)(PV_BUF + (int)(BV_PI))  :
             return (char_u *)&(curbuf->b_p_pi);
-        case PV_QE:
+        case   (idopt_T)(PV_BUF + (int)(BV_QE))  :
             return (char_u *)&(curbuf->b_p_qe);
-        case PV_RO:
+        case   (idopt_T)(PV_BUF + (int)(BV_RO))  :
             return (char_u *)&(curbuf->b_p_ro);
-        case PV_SI:
+        case   (idopt_T)(PV_BUF + (int)(BV_SI))  :
             return (char_u *)&(curbuf->b_p_si);
-        case PV_SN:
+        case   (idopt_T)(PV_BUF + (int)(BV_SN))  :
             return (char_u *)&(curbuf->b_p_sn);
-        case PV_STS:
+        case   (idopt_T)(PV_BUF + (int)(BV_STS))  :
             return (char_u *)&(curbuf->b_p_sts);
-        case PV_SUA:
+        case   (idopt_T)(PV_BUF + (int)(BV_SUA))  :
             return (char_u *)&(curbuf->b_p_sua);
-        case PV_SWF:
+        case   (idopt_T)(PV_BUF + (int)(BV_SWF))  :
             return (char_u *)&(curbuf->b_p_swf);
-        case PV_SW:
+        case   (idopt_T)(PV_BUF + (int)(BV_SW))  :
             return (char_u *)&(curbuf->b_p_sw);
-        case PV_TS:
+        case   (idopt_T)(PV_BUF + (int)(BV_TS))  :
             return (char_u *)&(curbuf->b_p_ts);
-        case PV_TW:
+        case   (idopt_T)(PV_BUF + (int)(BV_TW))  :
             return (char_u *)&(curbuf->b_p_tw);
-        case PV_TX:
+        case   (idopt_T)(PV_BUF + (int)(BV_TX))  :
             return (char_u *)&(curbuf->b_p_tx);
-        case PV_WM:
+        case   (idopt_T)(PV_BUF + (int)(BV_WM))  :
             return (char_u *)&(curbuf->b_p_wm);
         default:
             iemsg(e_get_varp_error);
@@ -117926,14 +115873,14 @@
     void
 after_copy_winopt(win_T *wp)
 {
-    char *errmsg = update_winhighlight(wp, wp->w_p_whl);
+    char *errmsg = update_winhighlight(wp, wp-> w_onebuf_opt.wo_whl );
 
     if (errmsg != NULL)
     {
         emsg(_(errmsg));
     }
 
-    if (wp->w_p_wrap)
+    if (wp-> w_onebuf_opt.wo_wrap )
     {
         wp->w_leftcol = 0;
     }
@@ -117941,8 +115888,8 @@
     {
         wp->w_skipcol = 0;
     }
-    set_listchars_option(wp, wp->w_p_lcs, TRUE, NULL, 0);
-    set_fillchars_option(wp, wp->w_p_fcs, TRUE, NULL, 0);
+    set_listchars_option(wp, wp-> w_onebuf_opt.wo_lcs , TRUE, NULL, 0);
+    set_fillchars_option(wp, wp-> w_onebuf_opt.wo_fcs , TRUE, NULL, 0);
 }
 
     static char_u *
@@ -117991,7 +115938,7 @@
 }
 
     static void
-check_winopt(winopt_T *wop UNUSED)
+check_winopt(winopt_T *wop  __attribute__((unused)) )
 {
     check_string_option(&wop->wo_eiw);
     check_string_option(&wop->wo_wcr);
@@ -118002,7 +115949,7 @@
 }
 
     void
-clear_winopt(winopt_T *wop UNUSED)
+clear_winopt(winopt_T *wop  __attribute__((unused)) )
 {
     clear_string_option(&wop->wo_eiw);
     clear_string_option(&wop->wo_wcr);
@@ -118012,9 +115959,6 @@
     clear_string_option(&wop->wo_whl);
 }
 
-# define COPY_OPT_SCTX(buf, bv)
-# define COPY_OPT_INSECURE(flagsfield, bv)
-
     void
 buf_copy_options(buf_T *buf, int flags)
 {
@@ -118048,13 +115992,13 @@
                 switch (*p_ffs)
                 {
                     case 'm':
-                        buf->b_p_ff = vim_strsave((char_u *)FF_MAC);
+                        buf->b_p_ff = vim_strsave((char_u *) "mac" );
                         break;
                     case 'd':
-                        buf->b_p_ff = vim_strsave((char_u *)FF_DOS);
+                        buf->b_p_ff = vim_strsave((char_u *) "dos" );
                         break;
                     case 'u':
-                        buf->b_p_ff = vim_strsave((char_u *)FF_UNIX);
+                        buf->b_p_ff = vim_strsave((char_u *) "unix" );
                         break;
                     default:
                         buf->b_p_ff = vim_strsave(p_ff);
@@ -118072,33 +116016,33 @@
             }
 
             buf->b_p_ai = p_ai;
-            COPY_OPT_SCTX(buf, BV_AI);
+              ;
             buf->b_p_ai_nopaste = p_ai_nopaste;
             buf->b_p_sw = p_sw;
-            COPY_OPT_SCTX(buf, BV_SW);
+              ;
             buf->b_p_tw = p_tw;
-            COPY_OPT_SCTX(buf, BV_TW);
+              ;
             buf->b_p_tw_nopaste = p_tw_nopaste;
             buf->b_p_tw_nobin = p_tw_nobin;
             buf->b_p_wm = p_wm;
-            COPY_OPT_SCTX(buf, BV_WM);
+              ;
             buf->b_p_wm_nopaste = p_wm_nopaste;
             buf->b_p_wm_nobin = p_wm_nobin;
             buf->b_p_bin = p_bin;
-            COPY_OPT_SCTX(buf, BV_BIN);
+              ;
             buf->b_p_bomb = p_bomb;
-            COPY_OPT_SCTX(buf, BV_BOMB);
+              ;
             buf->b_p_fixeol = p_fixeol;
-            COPY_OPT_SCTX(buf, BV_FIXEOL);
+              ;
             buf->b_p_et = p_et;
-            COPY_OPT_SCTX(buf, BV_ET);
+              ;
             buf->b_p_et_nobin = p_et_nobin;
             buf->b_p_et_nopaste = p_et_nopaste;
             buf->b_p_ml = p_ml;
-            COPY_OPT_SCTX(buf, BV_ML);
+              ;
             buf->b_p_ml_nobin = p_ml_nobin;
             buf->b_p_inf = p_inf;
-            COPY_OPT_SCTX(buf, BV_INF);
+              ;
             if (cmdmod.cmod_flags & CMOD_NOSWAPFILE)
             {
                 buf->b_p_swf = FALSE;
@@ -118106,62 +116050,62 @@
             else
             {
                 buf->b_p_swf = p_swf;
-                COPY_OPT_SCTX(buf, BV_SWF);
+                  ;
             }
             buf->b_p_cpt = vim_strsave(p_cpt);
-            COPY_OPT_SCTX(buf, BV_CPT);
+              ;
             buf->b_p_sts = p_sts;
-            COPY_OPT_SCTX(buf, BV_STS);
+              ;
             buf->b_p_sts_nopaste = p_sts_nopaste;
             buf->b_p_sn = p_sn;
-            COPY_OPT_SCTX(buf, BV_SN);
+              ;
             buf->b_p_com = vim_strsave(p_com);
-            COPY_OPT_SCTX(buf, BV_COM);
+              ;
             buf->b_p_cms = vim_strsave(p_cms);
-            COPY_OPT_SCTX(buf, BV_CMS);
+              ;
             buf->b_p_fo = vim_strsave(p_fo);
-            COPY_OPT_SCTX(buf, BV_FO);
+              ;
             buf->b_p_flp = vim_strsave(p_flp);
-            COPY_OPT_SCTX(buf, BV_FLP);
+              ;
             buf->b_p_nf = vim_strsave(p_nf);
-            COPY_OPT_SCTX(buf, BV_NF);
+              ;
             buf->b_p_mps = vim_strsave(p_mps);
-            COPY_OPT_SCTX(buf, BV_MPS);
+              ;
             buf->b_p_si = p_si;
-            COPY_OPT_SCTX(buf, BV_SI);
+              ;
             buf->b_p_ci = p_ci;
-            COPY_OPT_SCTX(buf, BV_CI);
+              ;
 
             buf->b_p_cin = p_cin;
-            COPY_OPT_SCTX(buf, BV_CIN);
+              ;
             buf->b_p_cink = vim_strsave(p_cink);
-            COPY_OPT_SCTX(buf, BV_CINK);
+              ;
             buf->b_p_cino = vim_strsave(p_cino);
-            COPY_OPT_SCTX(buf, BV_CINO);
+              ;
             buf->b_p_cinsd = vim_strsave(p_cinsd);
-            COPY_OPT_SCTX(buf, BV_CINSD);
+              ;
             buf->b_p_lop = vim_strsave(p_lop);
-            COPY_OPT_SCTX(buf, BV_LOP);
+              ;
 
             buf->b_p_ft = empty_option;
             buf->b_p_pi = p_pi;
-            COPY_OPT_SCTX(buf, BV_PI);
+              ;
             buf->b_p_cinw = vim_strsave(p_cinw);
-            COPY_OPT_SCTX(buf, BV_CINW);
+              ;
             buf->b_p_lisp = p_lisp;
-            COPY_OPT_SCTX(buf, BV_LISP);
+              ;
             buf->b_p_fp = empty_option;
             buf->b_p_sua = vim_strsave(p_sua);
-            COPY_OPT_SCTX(buf, BV_SUA);
+              ;
             buf->b_p_iminsert = p_iminsert;
-            COPY_OPT_SCTX(buf, BV_IMI);
+              ;
             buf->b_p_imsearch = p_imsearch;
-            COPY_OPT_SCTX(buf, BV_IMS);
+              ;
 
             buf->b_p_ac = -1;
             buf->b_p_ar = -1;
             buf->b_p_fs = -1;
-            buf->b_p_ul = NO_LOCAL_UNDOLEVEL;
+            buf->b_p_ul =  (-123456) ;
             buf->b_p_bkc = empty_option;
             buf->b_bkc_flags = 0;
             buf->b_p_ep = empty_option;
@@ -118175,7 +116119,7 @@
             buf->b_p_dict = empty_option;
             buf->b_p_tsr = empty_option;
             buf->b_p_qe = vim_strsave(p_qe);
-            COPY_OPT_SCTX(buf, BV_QE);
+              ;
             buf->b_p_lw = empty_option;
             buf->b_p_menc = empty_option;
 
@@ -118186,17 +116130,17 @@
             else
             {
                 buf->b_p_isk = vim_strsave(p_isk);
-                COPY_OPT_SCTX(buf, BV_ISK);
+                  ;
                 did_isk = TRUE;
                 buf->b_p_ts = p_ts;
-                COPY_OPT_SCTX(buf, BV_TS);
+                  ;
                 buf->b_help = false;
                 if (buf->b_p_bt[0] == 'h')
                 {
                     clear_string_option(&buf->b_p_bt);
                 }
                 buf->b_p_ma = p_ma;
-                COPY_OPT_SCTX(buf, BV_MA);
+                  ;
             }
         }
 
@@ -118265,7 +116209,7 @@
         xp->xp_pattern = arg;
         return;
     }
-    argend = arg + STRLEN(arg);
+    argend = arg +  strlen((char *)(arg)) ;
     p = argend - 1;
     if (*p == ' ' && *(p - 1) != '\\')
     {
@@ -118289,13 +116233,13 @@
         }
         --p;
     }
-    if (STRNCMP(p, "no", 2) == 0 && STRNCMP(p, "novice", 6) != 0)
+    if ( strncmp((char *)(p), (char *)("no"), (2))  == 0 &&  strncmp((char *)(p), (char *)("novice"), (6))  != 0)
     {
         xp->xp_context = EXPAND_BOOL_SETTINGS;
         xp->xp_prefix = XP_PREFIX_NO;
         p += 2;
     }
-    else if (STRNCMP(p, "inv", 3) == 0)
+    else if ( strncmp((char *)(p), (char *)("inv"), (3))  == 0)
     {
         xp->xp_context = EXPAND_BOOL_SETTINGS;
         xp->xp_prefix = XP_PREFIX_INV;
@@ -118319,8 +116263,8 @@
         }
         nextchar = *++p;
         is_term_option = TRUE;
-        expand_option_name[2] = KEY2TERMCAP0(key);
-        expand_option_name[3] = KEY2TERMCAP1(key);
+        expand_option_name[2] =  ((-(key)) & 0xff) ;
+        expand_option_name[3] =  (((unsigned)(-(key)) >> 8) & 0xff) ;
     }
     else
     {
@@ -118342,7 +116286,7 @@
         }
         else
         {
-            while (ASCII_ISALNUM(*p) || *p == '_' || *p == '*')
+            while ( ( ( ((unsigned)(*p) - 'A' < 26)  ||  ((unsigned)(*p) - 'a' < 26) )  ||  ((unsigned)(*p) - '0' < 10) )  || *p == '_' || *p == '*')
             {
                 p++;
             }
@@ -118384,7 +116328,7 @@
     }
     if ((nextchar != '=' && nextchar != ':') || xp->xp_context == EXPAND_BOOL_SETTINGS)
     {
-        xp->xp_context = EXPAND_UNSUCCESSFUL;
+        xp->xp_context =  (-2) ;
         return;
     }
 
@@ -118401,7 +116345,7 @@
     {
         if (options[opt_idx].flags & P_NO_CMD_EXPAND)
         {
-            xp->xp_context=EXPAND_UNSUCCESSFUL;
+            xp->xp_context= (-2) ;
             return;
         }
     }
@@ -118518,7 +116462,7 @@
         int score;
 
         score = fuzzy_match_str(str, fuzzystr);
-        if (score != FUZZY_SCORE_NONE)
+        if (score !=  INT_MIN )
         {
             if (!test_only)
             {
@@ -118558,7 +116502,7 @@
         regmatch->rm_ic = ic;
         if (xp->xp_context != EXPAND_BOOL_SETTINGS)
         {
-            for (match = 0; match < (int)ARRAY_LENGTH(names); ++match)
+            for (match = 0; match < (int) (sizeof(names) / sizeof((names)[0])) ; ++match)
             {
                 if (match_str((char_u *)names[match], regmatch, *matches, count, (loop == 0), fuzzy, fuzzystr, fuzmatch))
                 {
@@ -118653,7 +116597,7 @@
         {
             for (opt_idx = 0; (str = get_termcode(opt_idx)) != NULL; opt_idx++)
             {
-                if (!SAFE_isprint(str[0]) || !SAFE_isprint(str[1]))
+                if (! (isprint ((unsigned char)(str[0])))  || ! (isprint ((unsigned char)(str[1]))) )
                 {
                     continue;
                 }
@@ -118703,8 +116647,8 @@
             for (opt_idx = 0; (str = get_key_name(opt_idx)) != NULL; opt_idx++)
             {
                 name_buf[0] = '<';
-                STRCPY(name_buf + 1, str);
-                STRCAT(name_buf, ">");
+                 strcpy((char *)(name_buf + 1), (char *)(str)) ;
+                 strcat((char *)(name_buf), (char *)(">")) ;
 
                 if (match_str(name_buf, regmatch, *matches, count, (loop == 0), fuzzy, fuzzystr, fuzmatch))
                 {
@@ -118735,7 +116679,7 @@
             }
             if (!fuzzy)
             {
-                *matches = ALLOC_MULT(char_u *, *numMatches);
+                *matches =  (char_u * *)alloc(sizeof(char_u *) * (*numMatches)) ;
                 if (*matches == NULL)
                 {
                     *matches = (char_u **)"";
@@ -118744,7 +116688,7 @@
             }
             else
             {
-                fuzmatch = ALLOC_MULT(fuzmatch_str_T, *numMatches);
+                fuzmatch =  (fuzmatch_str_T *)alloc(sizeof(fuzmatch_str_T) * (*numMatches)) ;
                 if (fuzmatch == NULL)
                 {
                     *matches = (char_u **)"";
@@ -118783,7 +116727,7 @@
     char_u  *buf;
 
     *numMatches = 0;
-    *matches = ALLOC_MULT(char_u *, 1);
+    *matches =  (char_u * *)alloc(sizeof(char_u *) * (1)) ;
     if (*matches == NULL)
     {
         return FAIL;
@@ -118811,7 +116755,8 @@
     buf = escape_option_str_cmdline(var);
     if (buf == NULL)
     {
-        VIM_CLEAR(*matches);
+         vim_free(*matches);
+         (*matches) = NULL;
         return FAIL;
     }
 
@@ -118946,19 +116891,18 @@
     }
     else if (option_flags & P_FLAGLIST)
     {
-
         if (*xp->xp_pattern != NUL)
         {
             return FAIL;
         }
 
-        size_t num_flags = STRLEN(option_val);
+        size_t num_flags =  strlen((char *)(option_val)) ;
         if (num_flags == 0)
         {
             return FAIL;
         }
 
-        *matches = ALLOC_MULT(char_u *, num_flags + 1);
+        *matches =  (char_u * *)alloc(sizeof(char_u *) * (num_flags + 1)) ;
         if (*matches == NULL)
         {
             return FAIL;
@@ -118970,7 +116914,8 @@
         p = vim_strnsave(option_val, num_flags);
         if (p == NULL)
         {
-            VIM_CLEAR(*matches);
+             vim_free(*matches);
+             (*matches) = NULL;
             return FAIL;
         }
         (*matches)[count++] = p;
@@ -119008,11 +116953,11 @@
 
         if (wc_use_keyname(varp, &wc))
         {
-            STRCPY(NameBuff, get_special_key_name((int)wc, 0));
+             strcpy((char *)(NameBuff), (char *)(get_special_key_name((int)wc, 0))) ;
         }
         else if (wc != 0)
         {
-            STRCPY(NameBuff, transchar((int)wc));
+             strcpy((char *)(NameBuff), (char *)(transchar((int)wc))) ;
         }
         else
         {
@@ -119028,15 +116973,15 @@
         }
         else if (opp->flags & P_EXPAND)
         {
-            home_replace(NULL, varp, NameBuff, MAXPATHL, FALSE);
+            home_replace(NULL, varp, NameBuff,  PATH_MAX , FALSE);
         }
         else if ((char_u **)opp->var == &p_pt)
         {
-            str2specialbuf(p_pt, NameBuff, MAXPATHL);
+            str2specialbuf(p_pt, NameBuff,  PATH_MAX );
         }
         else
         {
-            vim_strncpy(NameBuff, varp, MAXPATHL - 1);
+            vim_strncpy(NameBuff, varp,  PATH_MAX  - 1);
         }
     }
 }
@@ -119047,7 +116992,7 @@
     if (((long *)varp == &p_wc) || ((long *)varp == &p_wcm))
     {
         *wcp = *(long *)varp;
-        if (IS_SPECIAL(*wcp) || find_special_key_in_table((int)*wcp) >= 0)
+        if ( ((*wcp) < 0)  || find_special_key_in_table((int)*wcp) >= 0)
         {
             return TRUE;
         }
@@ -119059,7 +117004,7 @@
 shortmess(int x)
 {
     return p_shm != NULL &&
-            (   vim_strchr(p_shm, x) != NULL || (vim_strchr(p_shm, 'a') != NULL && vim_strchr((char_u *)SHM_A, x) != NULL));
+            (   vim_strchr(p_shm, x) != NULL || (vim_strchr(p_shm, 'a') != NULL && vim_strchr((char_u *) "rmfixlnw" , x) != NULL));
 }
 
     void
@@ -119187,19 +117132,19 @@
     long
 get_scrolloff_value(void)
 {
-    return curwin->w_p_so < 0 ? p_so : curwin->w_p_so;
+    return curwin-> w_onebuf_opt.wo_so  < 0 ? p_so : curwin-> w_onebuf_opt.wo_so ;
 }
 
     long
 get_scrolloffpad_value(void)
 {
-    return curwin->w_p_sop == -1 ? p_sop : curwin->w_p_sop;
+    return curwin-> w_onebuf_opt.wo_sop  == -1 ? p_sop : curwin-> w_onebuf_opt.wo_sop ;
 }
 
     long
 get_sidescrolloff_value(void)
 {
-    return curwin->w_p_siso < 0 ? p_siso : curwin->w_p_siso;
+    return curwin-> w_onebuf_opt.wo_siso  < 0 ? p_siso : curwin-> w_onebuf_opt.wo_siso ;
 }
 
     unsigned int
@@ -119211,7 +117156,7 @@
     unsigned int
 get_ve_flags(void)
 {
-    return (curwin->w_ve_flags ? curwin->w_ve_flags : ve_flags)
+    return (curwin-> w_onebuf_opt.wo_ve_flags  ? curwin-> w_onebuf_opt.wo_ve_flags  : ve_flags)
             & ~(VE_NONE | VE_NONEU);
 }
 
@@ -119231,7 +117176,7 @@
 }
 
     int
-option_set_callback_func(char_u *optval UNUSED, callback_T *optcb UNUSED)
+option_set_callback_func(char_u *optval  __attribute__((unused)) , callback_T *optcb  __attribute__((unused)) )
 {
     return FAIL;
 }
@@ -119250,7 +117195,7 @@
                                  "showmatch", "operator", "register", "shell",
                                  "spell", "term", "wildmode", NULL};
 static char *(p_nf_values[]) = {"bin", "octal", "hex", "alpha", "unsigned", "blank", NULL};
-static char *(p_ff_values[]) = {FF_UNIX, FF_DOS, FF_MAC, NULL};
+static char *(p_ff_values[]) = { "unix" ,  "dos" ,  "mac" , NULL};
 static char *(p_cmp_values[]) = {"internal", "keepascii", NULL};
 static char *(p_dy_values[]) = {"lastline", "truncate", "uhex", NULL};
 static char *(p_jop_values[]) = {"stack", NULL};
@@ -119394,7 +117339,7 @@
 
     if (is_window_local_option(opt_idx))
     {
-        p = (char_u **)GLOBAL_WO(varp);
+        p = (char_u **) ((char *)(varp) + sizeof(winopt_T)) ;
     }
     else
     {
@@ -119408,7 +117353,7 @@
 }
 
     void
-set_string_option_direct(char_u      *name, int         opt_idx, char_u      *val, int         opt_flags, int         set_sid UNUSED)
+set_string_option_direct(char_u      *name, int         opt_idx, char_u      *val, int         opt_flags, int         set_sid  __attribute__((unused)) )
 {
     char_u      *s;
     char_u      **varp;
@@ -119513,7 +117458,7 @@
     static int
 check_illegal_path_names(int opt_idx, char_u **varp)
 {
-    return (((get_option_flags(opt_idx) & P_NFNAME) && vim_strpbrk(*varp, (char_u *)(secure ? "/\\*?[|;&<>\r\n" : "/\\*?[<>\r\n")) != NULL) || ((get_option_flags(opt_idx) & P_NDNAME) && vim_strpbrk(*varp, (char_u *)"*?[|;&<>\r\n") != NULL));
+    return (((get_option_flags(opt_idx) & P_NFNAME) &&  (char_u *)strpbrk((char *)(*varp), (char *)((char_u *)(secure ? "/\\*?[|;&<>\r\n" : "/\\*?[<>\r\n")))  != NULL) || ((get_option_flags(opt_idx) & P_NDNAME) &&  (char_u *)strpbrk((char *)(*varp), (char *)((char_u *)"*?[|;&<>\r\n"))  != NULL));
 }
 
     static char *
@@ -119557,7 +117502,7 @@
     int         include_orig_val = args->oe_include_orig_val;
     char_u      *option_val = args->oe_opt_value;
 
-    *matches = ALLOC_MULT(char_u *, numValues + 1);
+    *matches =  (char_u * *)alloc(sizeof(char_u *) * (numValues + 1)) ;
     if (*matches == NULL)
     {
         return FAIL;
@@ -119570,7 +117515,8 @@
         p = vim_strsave(option_val);
         if (p == NULL)
         {
-            VIM_CLEAR(*matches);
+             vim_free(*matches);
+             (*matches) = NULL;
             return FAIL;
         }
         (*matches)[count++] = p;
@@ -119580,7 +117526,7 @@
     {
         if (include_orig_val && *option_val != NUL)
         {
-            if (STRCMP((char_u*)*val, option_val) == 0)
+            if ( strcmp((char *)((char_u*)*val), (char *)(option_val))  == 0)
             {
                 continue;
             }
@@ -119592,7 +117538,8 @@
             {
                 if (count == 0)
                 {
-                    VIM_CLEAR(*matches);
+                     vim_free(*matches);
+                     (*matches) = NULL;
                     return FAIL;
                 }
                 else
@@ -119605,7 +117552,8 @@
     }
     if (count == 0)
     {
-        VIM_CLEAR(*matches);
+         vim_free(*matches);
+         (*matches) = NULL;
         return FAIL;
     }
     *numMatches = count;
@@ -119657,9 +117605,9 @@
     int     append = args->oe_append;
     int     include_orig_val = args->oe_include_orig_val && (*option_val != NUL);
 
-    size_t num_flags = STRLEN(flags);
+    size_t num_flags =  strlen((char *)(flags)) ;
 
-    *matches = ALLOC_MULT(char_u *, num_flags + 1);
+    *matches =  (char_u * *)alloc(sizeof(char_u *) * (num_flags + 1)) ;
     if (*matches == NULL)
     {
         return FAIL;
@@ -119672,7 +117620,8 @@
         p = vim_strsave(option_val);
         if (p == NULL)
         {
-            VIM_CLEAR(*matches);
+             vim_free(*matches);
+             (*matches) = NULL;
             return FAIL;
         }
         (*matches)[count++] = p;
@@ -119696,7 +117645,8 @@
             {
                 if (count == 0)
                 {
-                    VIM_CLEAR(*matches);
+                     vim_free(*matches);
+                     (*matches) = NULL;
                     return FAIL;
                 }
                 else
@@ -119710,7 +117660,8 @@
 
     if (count == 0)
     {
-        VIM_CLEAR(*matches);
+         vim_free(*matches);
+         (*matches) = NULL;
         return FAIL;
     }
     *numMatches = count;
@@ -119718,7 +117669,7 @@
 }
 
     char *
-did_set_ambiwidth(optset_T *args UNUSED)
+did_set_ambiwidth(optset_T *args  __attribute__((unused)) )
 {
     if (check_opt_strings(p_ambw, p_ambw_values, FALSE) != OK)
     {
@@ -119728,8 +117679,6 @@
     return check_chars_options();
 }
 
-#define completing_value_for_subopt(args, name)           completing_value_for_subopt_len(args, name, (int)STRLEN_LITERAL(name))
-
     static bool
 completing_value_for_subopt_len(optexpand_T *args, char *name, int len)
 {
@@ -119745,13 +117694,13 @@
         return false;
     }
 
-    return STRNCMP(colon - len, name, len) == 0;
+    return  strncmp((char *)(colon - len), (char *)(name), (len))  == 0;
 }
 
     int
 expand_set_ambiwidth(optexpand_T *args, int *numMatches, char_u ***matches)
 {
-    return expand_set_opt_string(args, p_ambw_values, ARRAY_LENGTH(p_ambw_values) - 1, numMatches, matches);
+    return expand_set_opt_string(args, p_ambw_values,  (sizeof(p_ambw_values) / sizeof((p_ambw_values)[0]))  - 1, numMatches, matches);
 }
 
     char *
@@ -119775,13 +117724,13 @@
     int
 expand_set_background(optexpand_T *args, int *numMatches, char_u ***matches)
 {
-    return expand_set_opt_string(args, p_bg_values, ARRAY_LENGTH(p_bg_values) - 1, numMatches, matches);
+    return expand_set_opt_string(args, p_bg_values,  (sizeof(p_bg_values) / sizeof((p_bg_values)[0]))  - 1, numMatches, matches);
 }
 
     char *
-did_set_backspace(optset_T *args UNUSED)
+did_set_backspace(optset_T *args  __attribute__((unused)) )
 {
-    if (VIM_ISDIGIT(*p_bs))
+    if ( ((unsigned)(*p_bs) - '0' < 10) )
     {
         if (*p_bs > '3' || p_bs[1] != NUL)
         {
@@ -119799,7 +117748,7 @@
     int
 expand_set_backspace(optexpand_T *args, int *numMatches, char_u ***matches)
 {
-    return expand_set_opt_string(args, p_bs_values, ARRAY_LENGTH(p_bs_values) - 1, numMatches, matches);
+    return expand_set_opt_string(args, p_bs_values,  (sizeof(p_bs_values) / sizeof((p_bs_values)[0]))  - 1, numMatches, matches);
 }
 
     char *
@@ -119842,13 +117791,13 @@
     int
 expand_set_backupcopy(optexpand_T *args, int *numMatches, char_u ***matches)
 {
-    return expand_set_opt_string(args, p_bkc_values, ARRAY_LENGTH(p_bkc_values) - 1, numMatches, matches);
+    return expand_set_opt_string(args, p_bkc_values,  (sizeof(p_bkc_values) / sizeof((p_bkc_values)[0]))  - 1, numMatches, matches);
 }
 
     char *
-did_set_backupext_or_patchmode(optset_T *args UNUSED)
+did_set_backupext_or_patchmode(optset_T *args  __attribute__((unused)) )
 {
-    if (STRCMP(*p_bex == '.' ? p_bex + 1 : p_bex, *p_pm == '.' ? p_pm + 1 : p_pm) == 0)
+    if ( strcmp((char *)(*p_bex == '.' ? p_bex + 1 : p_bex), (char *)(*p_pm == '.' ? p_pm + 1 : p_pm))  == 0)
     {
         return e_backupext_and_patchmode_are_equal;
     }
@@ -119857,7 +117806,7 @@
 }
 
     char *
-did_set_belloff(optset_T *args UNUSED)
+did_set_belloff(optset_T *args  __attribute__((unused)) )
 {
     return did_set_opt_flags(p_bo, p_bo_values, &bo_flags, TRUE);
 }
@@ -119865,11 +117814,11 @@
     int
 expand_set_belloff(optexpand_T *args, int *numMatches, char_u ***matches)
 {
-    return expand_set_opt_string(args, p_bo_values, ARRAY_LENGTH(p_bo_values) - 1, numMatches, matches);
+    return expand_set_opt_string(args, p_bo_values,  (sizeof(p_bo_values) / sizeof((p_bo_values)[0]))  - 1, numMatches, matches);
 }
 
     char *
-did_set_bufhidden(optset_T *args UNUSED)
+did_set_bufhidden(optset_T *args  __attribute__((unused)) )
 {
     return did_set_opt_strings(curbuf->b_p_bh, p_bufhidden_values, FALSE);
 }
@@ -119877,11 +117826,11 @@
     int
 expand_set_bufhidden(optexpand_T *args, int *numMatches, char_u ***matches)
 {
-    return expand_set_opt_string(args, p_bufhidden_values, ARRAY_LENGTH(p_bufhidden_values) - 1, numMatches, matches);
+    return expand_set_opt_string(args, p_bufhidden_values,  (sizeof(p_bufhidden_values) / sizeof((p_bufhidden_values)[0]))  - 1, numMatches, matches);
 }
 
     char *
-did_set_buftype(optset_T *args UNUSED)
+did_set_buftype(optset_T *args  __attribute__((unused)) )
 {
     if (check_opt_strings(curbuf->b_p_bt, p_buftype_values, FALSE) != OK)
     {
@@ -119902,11 +117851,11 @@
     int
 expand_set_buftype(optexpand_T *args, int *numMatches, char_u ***matches)
 {
-    return expand_set_opt_string(args, p_buftype_values, ARRAY_LENGTH(p_buftype_values) - 1, numMatches, matches);
+    return expand_set_opt_string(args, p_buftype_values,  (sizeof(p_buftype_values) / sizeof((p_buftype_values)[0]))  - 1, numMatches, matches);
 }
 
     char *
-did_set_casemap(optset_T *args UNUSED)
+did_set_casemap(optset_T *args  __attribute__((unused)) )
 {
     return did_set_opt_flags(p_cmp, p_cmp_values, &cmp_flags, TRUE);
 }
@@ -119914,14 +117863,14 @@
     int
 expand_set_casemap(optexpand_T *args, int *numMatches, char_u ***matches)
 {
-    return expand_set_opt_string(args, p_cmp_values, ARRAY_LENGTH(p_cmp_values) - 1, numMatches, matches);
+    return expand_set_opt_string(args, p_cmp_values,  (sizeof(p_cmp_values) / sizeof((p_cmp_values)[0]))  - 1, numMatches, matches);
 }
 
     static char *
 did_set_global_listfillchars(char_u *val, int opt_lcs, int opt_flags, char *errbuf, size_t errbuflen)
 {
     char        *errmsg = NULL;
-    char_u      **local_ptr = opt_lcs ? &curwin->w_p_lcs : &curwin->w_p_fcs;
+    char_u      **local_ptr = opt_lcs ? &curwin-> w_onebuf_opt.wo_lcs  : &curwin-> w_onebuf_opt.wo_fcs ;
 
     if (opt_lcs)
     {
@@ -119943,23 +117892,26 @@
     {
         clear_string_option(local_ptr);
     }
-    FOR_ALL_TAB_WINDOWS(tp, wp)
+     for ((tp) = first_tabpage; (tp) != NULL; (tp) = (tp)->tp_next)
+     {
+         for ((wp) = ((tp) == curtab)            ? firstwin : (tp)->tp_firstwin; (wp); (wp) = (wp)->w_next)
     {
         if (opt_lcs)
         {
-            if (*wp->w_p_lcs == NUL)
+            if (*wp-> w_onebuf_opt.wo_lcs  == NUL)
             {
-                (void)set_listchars_option(wp, wp->w_p_lcs, TRUE, NULL, 0);
+                (void)set_listchars_option(wp, wp-> w_onebuf_opt.wo_lcs , TRUE, NULL, 0);
             }
         }
         else
         {
-            if (*wp->w_p_fcs == NUL)
+            if (*wp-> w_onebuf_opt.wo_fcs  == NUL)
             {
-                (void)set_fillchars_option(wp, wp->w_p_fcs, TRUE, NULL, 0);
+                (void)set_fillchars_option(wp, wp-> w_onebuf_opt.wo_fcs , TRUE, NULL, 0);
             }
         }
     }
+     }
 
     redraw_all_later(UPD_NOT_VALID);
 
@@ -119976,11 +117928,11 @@
     {
         errmsg = did_set_global_listfillchars(*varp, varp == &p_lcs, args->os_flags, args->os_errbuf, args->os_errbuflen);
     }
-    else if (varp == &curwin->w_p_lcs)
+    else if (varp == &curwin-> w_onebuf_opt.wo_lcs )
     {
         errmsg = set_listchars_option(curwin, *varp, TRUE, args->os_errbuf, args->os_errbuflen);
     }
-    else if (varp == &curwin->w_p_fcs)
+    else if (varp == &curwin-> w_onebuf_opt.wo_fcs )
     {
         errmsg = set_fillchars_option(curwin, *varp, TRUE, args->os_errbuf, args->os_errbuflen);
     }
@@ -119992,12 +117944,12 @@
 expand_set_chars_option(optexpand_T *args, int *numMatches, char_u ***matches)
 {
     char_u **varp = (char_u **)args->oe_varp;
-    int is_lcs = (varp == &p_lcs || varp == &curwin->w_p_lcs);
+    int is_lcs = (varp == &p_lcs || varp == &curwin-> w_onebuf_opt.wo_lcs );
     return expand_set_opt_generic(args, is_lcs ? get_listchars_name : get_fillchars_name, numMatches, matches);
 }
 
     char *
-did_set_cinoptions(optset_T *args UNUSED)
+did_set_cinoptions(optset_T *args  __attribute__((unused)) )
 {
     parse_cino(curbuf);
 
@@ -120015,7 +117967,7 @@
     {
         while (*s && *s != ':')
         {
-            if (vim_strchr((char_u *)COM_ALL, *s) == NULL && !VIM_ISDIGIT(*s) && *s != '-')
+            if (vim_strchr((char_u *) "nbsmexflrO" , *s) == NULL && ! ((unsigned)(*s) - '0' < 10)  && *s != '-')
             {
                 errmsg = illegal_char(args->os_errbuf, args->os_errbuflen, *s);
                 break;
@@ -120074,7 +118026,7 @@
 
     for (p = *varp; *p; )
     {
-        vim_memset(buffer, 0, LSIZE);
+         memset((buffer), (0), (LSIZE)) ;
         buf_ptr = buffer;
         escape = 0;
 
@@ -120144,11 +118096,11 @@
     static char *(p_cpt_values[]) = {
         ".", "w", "b", "u", "k", "kspell", "s", "i", "d", "]", "t", "U", "F", "o",
         NULL};
-    return expand_set_opt_string(args, p_cpt_values, ARRAY_LENGTH(p_cpt_values) - 1, numMatches, matches);
+    return expand_set_opt_string(args, p_cpt_values,  (sizeof(p_cpt_values) / sizeof((p_cpt_values)[0]))  - 1, numMatches, matches);
 }
 
     char *
-did_set_completeopt(optset_T *args UNUSED)
+did_set_completeopt(optset_T *args  __attribute__((unused)) )
 {
     char_u      *cot = p_cot;
     unsigned    *flags = &cot_flags;
@@ -120174,11 +118126,11 @@
     int
 expand_set_completeopt(optexpand_T *args, int *numMatches, char_u ***matches)
 {
-    return expand_set_opt_string(args, p_cot_values, ARRAY_LENGTH(p_cot_values) - 1, numMatches, matches);
+    return expand_set_opt_string(args, p_cot_values,  (sizeof(p_cot_values) / sizeof((p_cot_values)[0]))  - 1, numMatches, matches);
 }
 
     char *
-did_set_completefuzzycollect(optset_T *args UNUSED)
+did_set_completefuzzycollect(optset_T *args  __attribute__((unused)) )
 {
     if (opt_strings_flags(p_cfc, p_cfc_values, &cfc_flags, TRUE) != OK)
     {
@@ -120190,11 +118142,11 @@
     int
 expand_set_completefuzzycollect(optexpand_T *args, int *numMatches, char_u ***matches)
 {
-    return expand_set_opt_string(args, p_cfc_values, ARRAY_LENGTH(p_cfc_values) - 1, numMatches, matches);
+    return expand_set_opt_string(args, p_cfc_values,  (sizeof(p_cfc_values) / sizeof((p_cfc_values)[0]))  - 1, numMatches, matches);
 }
 
     char *
-did_set_completeitemalign(optset_T *args UNUSED)
+did_set_completeitemalign(optset_T *args  __attribute__((unused)) )
 {
     char_u      *p = p_cia;
     unsigned    new_cia_flags = 0;
@@ -120210,7 +118162,7 @@
             return e_invalid_argument;
         }
 
-        if (STRCMP(buf, "abbr") == 0)
+        if ( strcmp((char *)(buf), (char *)("abbr"))  == 0)
         {
             if (seen[CPT_ABBR])
             {
@@ -120220,7 +118172,7 @@
             seen[CPT_ABBR] = TRUE;
             count++;
         }
-        else if (STRCMP(buf, "kind") == 0)
+        else if ( strcmp((char *)(buf), (char *)("kind"))  == 0)
         {
             if (seen[CPT_KIND])
             {
@@ -120230,7 +118182,7 @@
             seen[CPT_KIND] = TRUE;
             count++;
         }
-        else if (STRCMP(buf, "menu") == 0)
+        else if ( strcmp((char *)(buf), (char *)("menu"))  == 0)
         {
             if (seen[CPT_MENU])
             {
@@ -120259,17 +118211,17 @@
 {
     char_u      **varp = (char_u **)args->os_varp;
 
-    return did_set_option_listflag(*varp, (char_u *)CPO_ALL, args->os_errbuf, args->os_errbuflen);
+    return did_set_option_listflag(*varp, (char_u *) "aAbBcCdDeEfFgHiIjJkKlLmMnoOpPqrRsStuvwWxXyZz$!%*-+<>#{|&/\\.;~" , args->os_errbuf, args->os_errbuflen);
 }
 
     int
 expand_set_cpoptions(optexpand_T *args, int *numMatches, char_u ***matches)
 {
-    return expand_set_opt_listflag(args, (char_u*)CPO_ALL, numMatches, matches);
+    return expand_set_opt_listflag(args, (char_u*) "aAbBcCdDeEfFgHiIjJkKlLmMnoOpPqrRsStuvwWxXyZz$!%*-+<>#{|&/\\.;~" , numMatches, matches);
 }
 
     char *
-did_set_debug(optset_T *args UNUSED)
+did_set_debug(optset_T *args  __attribute__((unused)) )
 {
     return did_set_opt_strings(p_debug, p_debug_values, TRUE);
 }
@@ -120277,11 +118229,11 @@
     int
 expand_set_debug(optexpand_T *args, int *numMatches, char_u ***matches)
 {
-    return expand_set_opt_string(args, p_debug_values, ARRAY_LENGTH(p_debug_values) - 1, numMatches, matches);
+    return expand_set_opt_string(args, p_debug_values,  (sizeof(p_debug_values) / sizeof((p_debug_values)[0]))  - 1, numMatches, matches);
 }
 
     char *
-did_set_display(optset_T *args UNUSED)
+did_set_display(optset_T *args  __attribute__((unused)) )
 {
     if (opt_strings_flags(p_dy, p_dy_values, &dy_flags, TRUE) != OK)
     {
@@ -120295,11 +118247,11 @@
     int
 expand_set_display(optexpand_T *args, int *numMatches, char_u ***matches)
 {
-    return expand_set_opt_string(args, p_dy_values, ARRAY_LENGTH(p_dy_values) - 1, numMatches, matches);
+    return expand_set_opt_string(args, p_dy_values,  (sizeof(p_dy_values) / sizeof((p_dy_values)[0]))  - 1, numMatches, matches);
 }
 
     char *
-did_set_eadirection(optset_T *args UNUSED)
+did_set_eadirection(optset_T *args  __attribute__((unused)) )
 {
     return did_set_opt_strings(p_ead, p_ead_values, FALSE);
 }
@@ -120307,7 +118259,7 @@
     int
 expand_set_eadirection(optexpand_T *args, int *numMatches, char_u ***matches)
 {
-    return expand_set_opt_string(args, p_ead_values, ARRAY_LENGTH(p_ead_values) - 1, numMatches, matches);
+    return expand_set_opt_string(args, p_ead_values,  (sizeof(p_ead_values) / sizeof((p_ead_values)[0]))  - 1, numMatches, matches);
 }
 
     char *
@@ -120353,7 +118305,6 @@
 
     if (errmsg == NULL)
     {
-
         if (((varp == &p_enc && *p_tenc != NUL) || varp == &p_tenc))
         {
             if (convert_setup(&input_conv, p_tenc, p_enc) == FAIL || convert_setup(&output_conv, p_enc, p_tenc) == FAIL)
@@ -120385,7 +118336,7 @@
         return e_invalid_argument;
     }
 
-    if (oldval == NULL || STRCMP(oldval, *varp) == 0)
+    if (oldval == NULL ||  strcmp((char *)(oldval), (char *)(*varp))  == 0)
     {
         return NULL;
     }
@@ -120477,13 +118428,13 @@
     int
 expand_set_fileformat(optexpand_T *args, int *numMatches, char_u ***matches)
 {
-    return expand_set_opt_string(args, p_ff_values, ARRAY_LENGTH(p_ff_values) - 1, numMatches, matches);
+    return expand_set_opt_string(args, p_ff_values,  (sizeof(p_ff_values) / sizeof((p_ff_values)[0]))  - 1, numMatches, matches);
 }
 
     char_u *
-get_fileformat_name(expand_T *xp UNUSED, int idx)
+get_fileformat_name(expand_T *xp  __attribute__((unused)) , int idx)
 {
-    if (idx >= (int)ARRAY_LENGTH(p_ff_values))
+    if (idx >= (int) (sizeof(p_ff_values) / sizeof((p_ff_values)[0])) )
     {
         return NULL;
     }
@@ -120492,7 +118443,7 @@
 }
 
     char *
-did_set_fileformats(optset_T *args UNUSED)
+did_set_fileformats(optset_T *args  __attribute__((unused)) )
 {
     if (check_opt_strings(p_ffs, p_ff_values, TRUE) != OK)
     {
@@ -120521,7 +118472,7 @@
         return e_invalid_argument;
     }
 
-    args->os_value_changed = STRCMP(args->os_oldval.string, *varp) != 0;
+    args->os_value_changed =  strcmp((char *)(args->os_oldval.string), (char *)(*varp))  != 0;
 
     args->os_value_checked = TRUE;
 
@@ -120533,17 +118484,17 @@
 {
     char_u      **varp = (char_u **)args->os_varp;
 
-    return did_set_option_listflag(*varp, (char_u *)FO_ALL, args->os_errbuf, args->os_errbuflen);
+    return did_set_option_listflag(*varp, (char_u *) "tcro/q2vlb1mMBn,aw]jp" , args->os_errbuf, args->os_errbuflen);
 }
 
     int
 expand_set_formatoptions(optexpand_T *args, int *numMatches, char_u ***matches)
 {
-    return expand_set_opt_listflag(args, (char_u*)FO_ALL, numMatches, matches);
+    return expand_set_opt_listflag(args, (char_u*) "tcro/q2vlb1mMBn,aw]jp" , numMatches, matches);
 }
 
     char *
-did_set_helpfile(optset_T *args UNUSED)
+did_set_helpfile(optset_T *args  __attribute__((unused)) )
 {
     if (didset_vim)
     {
@@ -120557,7 +118508,7 @@
 }
 
     char *
-did_set_highlight(optset_T *args UNUSED)
+did_set_highlight(optset_T *args  __attribute__((unused)) )
 {
     if (highlight_changed() == FAIL)
     {
@@ -120571,11 +118522,11 @@
 expand_hl_occasions(optexpand_T *args, int *numMatches, char_u ***matches, char_u prefix)
 {
     char_u          *p;
-    static char_u   hl_flags[HLF_COUNT] = HL_FLAGS;
+    static char_u   hl_flags[HLF_COUNT] =  {'8', '~', '@', 'd', 'e', 'h', 'i', 'l', 'y', 'm', 'M',                  'n', 'a', 'b', 'N', 'G', 'O', 'r', 's', 'S', 'c', '|', 't', 'v', 'V',                   'w', 'W', 'f', 'F', 'A', 'C', 'D', 'T', 'E', '-', '>',                  'B', 'P', 'R', 'L',             '+', '=', 'k', '<','[', ']', '{', '}', 'x', 'X', 'j', 'H',              'p', 'J', 'Q',                  '*', '#', '_', '!', '.', 'o', 'q',              'z', 'Z', 'g',                  '%', '^', '&', 'I', '('} ;
     size_t          i;
     int             count = 0;
 
-    *matches = ALLOC_MULT(char_u *, HLF_COUNT + 1);
+    *matches =  (char_u * *)alloc(sizeof(char_u *) * (HLF_COUNT + 1)) ;
     if (*matches == NULL)
     {
         return FAIL;
@@ -120586,7 +118537,8 @@
         p = vim_strsave(args->oe_opt_value);
         if (p == NULL)
         {
-            VIM_CLEAR(*matches);
+             vim_free(*matches);
+             (*matches) = NULL;
             return FAIL;
         }
         (*matches)[count++] = p;
@@ -120599,7 +118551,8 @@
         {
             if (count == 0)
             {
-                VIM_CLEAR(*matches);
+                 vim_free(*matches);
+                 (*matches) = NULL;
                 return FAIL;
             }
             else
@@ -120620,7 +118573,8 @@
 
     if (count == 0)
     {
-        VIM_CLEAR(*matches);
+         vim_free(*matches);
+         (*matches) = NULL;
         return FAIL;
     }
     *numMatches = count;
@@ -120647,9 +118601,9 @@
 
     static char_u p_hl_mode_values[] =
         {':', 'b', 'i', '-', 'n', 'r', 's', 'u', 'c', '2', 'd', '=', 't'};
-    size_t num_hl_modes = ARRAY_LENGTH(p_hl_mode_values);
+    size_t num_hl_modes =  (sizeof(p_hl_mode_values) / sizeof((p_hl_mode_values)[0])) ;
 
-    *matches = ALLOC_MULT(char_u *, num_hl_modes);
+    *matches =  (char_u * *)alloc(sizeof(char_u *) * (num_hl_modes)) ;
     if (*matches == NULL)
     {
         return FAIL;
@@ -120675,7 +118629,8 @@
         {
             if (i == 0)
             {
-                VIM_CLEAR(*matches);
+                 vim_free(*matches);
+                 (*matches) = NULL;
                 return FAIL;
             }
             else
@@ -120689,7 +118644,8 @@
     }
     if (count == 0)
     {
-        VIM_CLEAR(*matches);
+         vim_free(*matches);
+         (*matches) = NULL;
         return FAIL;
     }
     *numMatches = count;
@@ -120698,7 +118654,7 @@
 }
 
     static char *
-parse_titleiconstring(optset_T *args UNUSED, int flagval UNUSED)
+parse_titleiconstring(optset_T *args  __attribute__((unused)) , int flagval  __attribute__((unused)) )
 {
     did_set_title();
 
@@ -120746,7 +118702,7 @@
 }
 
     char *
-did_set_jumpoptions(optset_T *args UNUSED)
+did_set_jumpoptions(optset_T *args  __attribute__((unused)) )
 {
     if (opt_strings_flags(p_jop, p_jop_values, &jop_flags, TRUE) != OK)
     {
@@ -120759,11 +118715,11 @@
     int
 expand_set_jumpoptions(optexpand_T *args, int *numMatches, char_u ***matches)
 {
-    return expand_set_opt_string(args, p_jop_values, ARRAY_LENGTH(p_jop_values) - 1, numMatches, matches);
+    return expand_set_opt_string(args, p_jop_values,  (sizeof(p_jop_values) / sizeof((p_jop_values)[0]))  - 1, numMatches, matches);
 }
 
     char *
-did_set_keymodel(optset_T *args UNUSED)
+did_set_keymodel(optset_T *args  __attribute__((unused)) )
 {
     if (check_opt_strings(p_km, p_km_values, TRUE) != OK)
     {
@@ -120778,13 +118734,13 @@
     int
 expand_set_keymodel(optexpand_T *args, int *numMatches, char_u ***matches)
 {
-    return expand_set_opt_string(args, p_km_values, ARRAY_LENGTH(p_km_values) - 1, numMatches, matches);
+    return expand_set_opt_string(args, p_km_values,  (sizeof(p_km_values) / sizeof((p_km_values)[0]))  - 1, numMatches, matches);
 }
 
     char *
-did_set_keyprotocol(optset_T *args UNUSED)
+did_set_keyprotocol(optset_T *args  __attribute__((unused)) )
 {
-    char_u *term = T_NAME;
+    char_u *term =  ( term_strings[(int)(KS_NAME)] ) ;
     keyprot_T kpc = match_keyprotocol(term);
     if (kpc == KEYPROTOCOL_FAIL)
     {
@@ -120802,7 +118758,7 @@
     expand_T *xp = args->oe_xp;
     if (xp->xp_pattern > args->oe_set_arg && *(xp->xp_pattern-1) == ':')
     {
-        return expand_set_opt_string(args, p_kpc_protocol_values, ARRAY_LENGTH(p_kpc_protocol_values) - 1, numMatches, matches);
+        return expand_set_opt_string(args, p_kpc_protocol_values,  (sizeof(p_kpc_protocol_values) / sizeof((p_kpc_protocol_values)[0]))  - 1, numMatches, matches);
     }
     static char *(empty[]) = {NULL};
     return expand_set_opt_string(args, empty, 0, numMatches, matches);
@@ -120813,7 +118769,7 @@
 {
     char_u      **varp = (char_u **)args->os_varp;
 
-    if (**varp != NUL && STRCMP(*varp, "expr:0") != 0 && STRCMP(*varp, "expr:1") != 0)
+    if (**varp != NUL &&  strcmp((char *)(*varp), (char *)("expr:0"))  != 0 &&  strcmp((char *)(*varp), (char *)("expr:1"))  != 0)
     {
         return e_invalid_argument;
     }
@@ -120825,7 +118781,7 @@
 expand_set_lispoptions(optexpand_T *args, int *numMatches, char_u ***matches)
 {
     static char *(p_lop_values[]) = {"expr:0", "expr:1", NULL};
-    return expand_set_opt_string(args, p_lop_values, ARRAY_LENGTH(p_lop_values) - 1, numMatches, matches);
+    return expand_set_opt_string(args, p_lop_values,  (sizeof(p_lop_values) / sizeof((p_lop_values)[0]))  - 1, numMatches, matches);
 }
 
     char *
@@ -120880,7 +118836,7 @@
 }
 
     char *
-did_set_messagesopt(optset_T *args UNUSED)
+did_set_messagesopt(optset_T *args  __attribute__((unused)) )
 {
     if (messagesopt_changed() == FAIL)
     {
@@ -120894,7 +118850,7 @@
 expand_set_messagesopt(optexpand_T *args, int *numMatches, char_u ***matches)
 {
     static char *(p_mopt_values[]) = {"hit-enter", "wait:", "history:", NULL};
-    return expand_set_opt_string(args, p_mopt_values, ARRAY_LENGTH(p_mopt_values) - 1, numMatches, matches);
+    return expand_set_opt_string(args, p_mopt_values,  (sizeof(p_mopt_values) / sizeof((p_mopt_values)[0]))  - 1, numMatches, matches);
 }
 
     char *
@@ -120903,7 +118859,7 @@
     char_u      **varp = (char_u **)args->os_varp;
     char        *retval;
 
-    retval = did_set_option_listflag(*varp, (char_u *)MOUSE_ALL, args->os_errbuf, args->os_errbuflen);
+    retval = did_set_option_listflag(*varp, (char_u *) "anvichr" , args->os_errbuf, args->os_errbuflen);
     if (retval == NULL)
     {
         redraw_tabline = TRUE;
@@ -120918,11 +118874,11 @@
     int
 expand_set_mouse(optexpand_T *args, int *numMatches, char_u ***matches)
 {
-    return expand_set_opt_listflag(args, (char_u*)MOUSE_ALL, numMatches, matches);
+    return expand_set_opt_listflag(args, (char_u*) "anvichr" , numMatches, matches);
 }
 
     char *
-did_set_mousemodel(optset_T *args UNUSED)
+did_set_mousemodel(optset_T *args  __attribute__((unused)) )
 {
     if (check_opt_strings(p_mousem, p_mousem_values, FALSE) != OK)
     {
@@ -120935,7 +118891,7 @@
     int
 expand_set_mousemodel(optexpand_T *args, int *numMatches, char_u ***matches)
 {
-    return expand_set_opt_string(args, p_mousem_values, ARRAY_LENGTH(p_mousem_values) - 1, numMatches, matches);
+    return expand_set_opt_string(args, p_mousem_values,  (sizeof(p_mousem_values) / sizeof((p_mousem_values)[0]))  - 1, numMatches, matches);
 }
 
     char *
@@ -120949,11 +118905,11 @@
     int
 expand_set_nrformats(optexpand_T *args, int *numMatches, char_u ***matches)
 {
-    return expand_set_opt_string(args, p_nf_values, ARRAY_LENGTH(p_nf_values) - 1, numMatches, matches);
+    return expand_set_opt_string(args, p_nf_values,  (sizeof(p_nf_values) / sizeof((p_nf_values)[0]))  - 1, numMatches, matches);
 }
 
     char *
-did_set_pastetoggle(optset_T *args UNUSED)
+did_set_pastetoggle(optset_T *args  __attribute__((unused)) )
 {
     char_u      *p;
 
@@ -120970,8 +118926,6 @@
     return NULL;
 }
 
-#define PUM_BORDER_CLEAR()       do {                           pum_set_border(FALSE);  pum_set_shadow(FALSE);  pum_set_margin(FALSE);     } while (0)
-
     static int
 parse_pumopt_border(char_u *val, int len)
 {
@@ -120984,23 +118938,23 @@
         return FAIL;
     }
 
-    if (can_use_box_chars && STRCMP(token, "single") == 0)
+    if (can_use_box_chars &&  strcmp((char *)(token), (char *)("single"))  == 0)
     {
         pum_set_border_chars(0x2500, 0x2502, 0x2500, 0x2502, 0x250c, 0x2510, 0x2518, 0x2514);
     }
-    else if (can_use_box_chars && STRCMP(token, "double") == 0)
+    else if (can_use_box_chars &&  strcmp((char *)(token), (char *)("double"))  == 0)
     {
         pum_set_border_chars(0x2550, 0x2551, 0x2550, 0x2551, 0x2554, 0x2557, 0x255D, 0x255A);
     }
-    else if (can_use_box_chars && STRCMP(token, "round") == 0)
+    else if (can_use_box_chars &&  strcmp((char *)(token), (char *)("round"))  == 0)
     {
         pum_set_border_chars(0x2500, 0x2502, 0x2500, 0x2502, 0x256d, 0x256e, 0x256f, 0x2570);
     }
-    else if (STRCMP(token, "ascii") == 0)
+    else if ( strcmp((char *)(token), (char *)("ascii"))  == 0)
     {
         pum_set_border_chars('-', '|', '-', '|', '+', '+', '+', '+');
     }
-    else if (STRNCMP(token, "custom:", 7) == 0)
+    else if ( strncmp((char *)(token), (char *)("custom:"), (7))  == 0)
     {
         char_u  *q = token + 7;
         int     out[8];
@@ -121050,7 +119004,9 @@
     int     have_border = FALSE;
     int     have_margin = FALSE;
 
-    PUM_BORDER_CLEAR();
+     pum_set_border(FALSE);
+     pum_set_shadow(FALSE);
+     pum_set_margin(FALSE);
     p_ph = 0;
     p_pw = 15;
     p_pmw = 0;
@@ -121072,10 +119028,10 @@
         }
         else
         {
-            len = (int)STRLEN(p);
+            len = (int) strlen((char *)(p)) ;
         }
 
-        if (STRNCMP(p, "border:", 7) == 0)
+        if ( strncmp((char *)(p), (char *)("border:"), (7))  == 0)
         {
             if (have_border)
             {
@@ -121087,7 +119043,7 @@
                 goto error;
             }
         }
-        else if (STRNCMP(p, "height:", 7) == 0)
+        else if ( strncmp((char *)(p), (char *)("height:"), (7))  == 0)
         {
             long n = atol((char *)p + 7);
             if (n < 0)
@@ -121096,7 +119052,7 @@
             }
             p_ph = n;
         }
-        else if (STRNCMP(p, "width:", 6) == 0)
+        else if ( strncmp((char *)(p), (char *)("width:"), (6))  == 0)
         {
             long n = atol((char *)p + 6);
             if (n < 0)
@@ -121105,7 +119061,7 @@
             }
             p_pw = n;
         }
-        else if (STRNCMP(p, "maxwidth:", 9) == 0)
+        else if ( strncmp((char *)(p), (char *)("maxwidth:"), (9))  == 0)
         {
             long n = atol((char *)p + 9);
             if (n < 0)
@@ -121114,7 +119070,7 @@
             }
             p_pmw = n;
         }
-        else if (STRNCMP(p, "opacity:", 8) == 0)
+        else if ( strncmp((char *)(p), (char *)("opacity:"), (8))  == 0)
         {
             long n = atol((char *)p + 8);
             if (n < 0 || n > 100)
@@ -121123,11 +119079,11 @@
             }
             p_po = n;
         }
-        else if (len == 6 && STRNCMP(p, "shadow", 6) == 0)
+        else if (len == 6 &&  strncmp((char *)(p), (char *)("shadow"), (6))  == 0)
         {
             pum_set_shadow(TRUE);
         }
-        else if (len == 6 && STRNCMP(p, "margin", 6) == 0)
+        else if (len == 6 &&  strncmp((char *)(p), (char *)("margin"), (6))  == 0)
         {
             have_margin = TRUE;
             pum_set_margin(TRUE);
@@ -121157,7 +119113,9 @@
     return NULL;
 
 error:
-    PUM_BORDER_CLEAR();
+     pum_set_border(FALSE);
+     pum_set_shadow(FALSE);
+     pum_set_margin(FALSE);
     p_ph = 0;
     p_pw = 15;
     p_pmw = 0;
@@ -121166,10 +119124,10 @@
 }
 
     static char_u *
-get_pum_border_style(expand_T *xp UNUSED, int idx)
+get_pum_border_style(expand_T *xp  __attribute__((unused)) , int idx)
 {
     static char *styles[] = {"ascii", "custom:", "single", "double", "round"};
-    return idx < ((enc_utf8 && *p_ambw == 's') ? (int)ARRAY_LENGTH(styles) : 2)
+    return idx < ((enc_utf8 && *p_ambw == 's') ? (int) (sizeof(styles) / sizeof((styles)[0]))  : 2)
             ? (char_u *)styles[idx] : NULL;
 }
 
@@ -121180,7 +119138,7 @@
 
     if (xp->xp_pattern > args->oe_set_arg && *(xp->xp_pattern-1) == ':')
     {
-        if (completing_value_for_subopt(args, "border"))
+        if ( completing_value_for_subopt_len(args, "border", (int) (sizeof("border" "") - 1) ) )
         {
             return expand_set_opt_generic(args, get_pum_border_style, numMatches, matches);
         }
@@ -121189,7 +119147,7 @@
 
     static char *(p_pumopt_values[]) = {"border:", "height:", "width:",
         "maxwidth:", "opacity:", "shadow", "margin", NULL};
-    return expand_set_opt_string(args, p_pumopt_values, ARRAY_LENGTH(p_pumopt_values) - 1, numMatches, matches);
+    return expand_set_opt_string(args, p_pumopt_values,  (sizeof(p_pumopt_values) / sizeof((p_pumopt_values)[0]))  - 1, numMatches, matches);
 }
 
     char *
@@ -121201,7 +119159,9 @@
     int     have_border = FALSE;
     int     have_margin = FALSE;
 
-    PUM_BORDER_CLEAR();
+     pum_set_border(FALSE);
+     pum_set_shadow(FALSE);
+     pum_set_margin(FALSE);
 
     if (*varp == NULL || **varp == NUL)
     {
@@ -121217,14 +119177,14 @@
         }
         else
         {
-            len = (int)STRLEN(p);
+            len = (int) strlen((char *)(p)) ;
         }
 
-        if (STRNCMP(p, "shadow", len) == 0 && len == 6)
+        if ( strncmp((char *)(p), (char *)("shadow"), (len))  == 0 && len == 6)
         {
             pum_set_shadow(TRUE);
         }
-        else if (STRNCMP(p, "margin", len) == 0 && len == 6)
+        else if ( strncmp((char *)(p), (char *)("margin"), (len))  == 0 && len == 6)
         {
             have_margin = TRUE;
             pum_set_margin(TRUE);
@@ -121260,7 +119220,9 @@
     return NULL;
 
 error:
-    PUM_BORDER_CLEAR();
+     pum_set_border(FALSE);
+     pum_set_shadow(FALSE);
+     pum_set_margin(FALSE);
     pum_set_border_chars(0, 0, 0, 0, 0, 0, 0, 0);
     return e_invalid_argument;
 }
@@ -121280,7 +119242,7 @@
 }
 
     char *
-did_set_scrollopt(optset_T *args UNUSED)
+did_set_scrollopt(optset_T *args  __attribute__((unused)) )
 {
     return did_set_opt_strings(p_sbo, p_scbopt_values, TRUE);
 }
@@ -121288,12 +119250,11 @@
     int
 expand_set_scrollopt(optexpand_T *args, int *numMatches, char_u ***matches)
 {
-    return expand_set_opt_string(args, p_scbopt_values, ARRAY_LENGTH(p_scbopt_values) - 1, numMatches, matches);
+    return expand_set_opt_string(args, p_scbopt_values,  (sizeof(p_scbopt_values) / sizeof((p_scbopt_values)[0]))  - 1, numMatches, matches);
 }
 
-
     char *
-did_set_selection(optset_T *args UNUSED)
+did_set_selection(optset_T *args  __attribute__((unused)) )
 {
     if (*p_sel == NUL || check_opt_strings(p_sel, p_sel_values, FALSE) != OK)
     {
@@ -121306,11 +119267,11 @@
     int
 expand_set_selection(optexpand_T *args, int *numMatches, char_u ***matches)
 {
-    return expand_set_opt_string(args, p_sel_values, ARRAY_LENGTH(p_sel_values) - 1, numMatches, matches);
+    return expand_set_opt_string(args, p_sel_values,  (sizeof(p_sel_values) / sizeof((p_sel_values)[0]))  - 1, numMatches, matches);
 }
 
     char *
-did_set_selectmode(optset_T *args UNUSED)
+did_set_selectmode(optset_T *args  __attribute__((unused)) )
 {
     return did_set_opt_strings(p_slm, p_slm_values, TRUE);
 }
@@ -121318,7 +119279,7 @@
     int
 expand_set_selectmode(optexpand_T *args, int *numMatches, char_u ***matches)
 {
-    return expand_set_opt_string(args, p_slm_values, ARRAY_LENGTH(p_slm_values) - 1, numMatches, matches);
+    return expand_set_opt_string(args, p_slm_values,  (sizeof(p_slm_values) / sizeof((p_slm_values)[0]))  - 1, numMatches, matches);
 }
 
     char *
@@ -121366,17 +119327,17 @@
 {
     char_u      **varp = (char_u **)args->os_varp;
 
-    return did_set_option_listflag(*varp, (char_u *)SHM_ALL, args->os_errbuf, args->os_errbuflen);
+    return did_set_option_listflag(*varp, (char_u *) "rmfixlnwaWtToOsAIcCqFSu" , args->os_errbuf, args->os_errbuflen);
 }
 
     int
 expand_set_shortmess(optexpand_T *args, int *numMatches, char_u ***matches)
 {
-    return expand_set_opt_listflag(args, (char_u*)SHM_ALL, numMatches, matches);
+    return expand_set_opt_listflag(args, (char_u*) "rmfixlnwaWtToOsAIcCqFSu" , numMatches, matches);
 }
 
     char *
-did_set_showcmdloc(optset_T *args UNUSED)
+did_set_showcmdloc(optset_T *args  __attribute__((unused)) )
 {
     char        *errmsg = did_set_opt_strings(p_sloc, p_sloc_values, FALSE);
 
@@ -121391,27 +119352,32 @@
     int
 expand_set_showcmdloc(optexpand_T *args, int *numMatches, char_u ***matches)
 {
-    return expand_set_opt_string(args, p_sloc_values, ARRAY_LENGTH(p_sloc_values) - 1, numMatches, matches);
+    return expand_set_opt_string(args, p_sloc_values,  (sizeof(p_sloc_values) / sizeof((p_sloc_values)[0]))  - 1, numMatches, matches);
 }
 
     char *
-did_set_splitkeep(optset_T *args UNUSED)
+did_set_splitkeep(optset_T *args  __attribute__((unused)) )
 {
     win_T       *wp;
     tabpage_T   *tp;
-    FOR_ALL_TAB_WINDOWS(tp, wp)
+     for ((tp) = first_tabpage; (tp) != NULL; (tp) = (tp)->tp_next)
+     {
+         for ((wp) = ((tp) == curtab)            ? firstwin : (tp)->tp_firstwin; (wp); (wp) = (wp)->w_next)
+         {
         wp->w_prev_height = wp->w_height;
+         }
+     }
     return did_set_opt_strings(p_spk, p_spk_values, FALSE);
 }
 
     int
 expand_set_splitkeep(optexpand_T *args, int *numMatches, char_u ***matches)
 {
-    return expand_set_opt_string(args, p_spk_values, ARRAY_LENGTH(p_spk_values) - 1, numMatches, matches);
+    return expand_set_opt_string(args, p_spk_values,  (sizeof(p_spk_values) / sizeof((p_spk_values)[0]))  - 1, numMatches, matches);
 }
 
     char *
-did_set_swapsync(optset_T *args UNUSED)
+did_set_swapsync(optset_T *args  __attribute__((unused)) )
 {
     return did_set_opt_strings(p_sws, p_sws_values, FALSE);
 }
@@ -121419,11 +119385,11 @@
     int
 expand_set_swapsync(optexpand_T *args, int *numMatches, char_u ***matches)
 {
-    return expand_set_opt_string(args, p_sws_values, ARRAY_LENGTH(p_sws_values) - 1, numMatches, matches);
+    return expand_set_opt_string(args, p_sws_values,  (sizeof(p_sws_values) / sizeof((p_sws_values)[0]))  - 1, numMatches, matches);
 }
 
     char *
-did_set_switchbuf(optset_T *args UNUSED)
+did_set_switchbuf(optset_T *args  __attribute__((unused)) )
 {
     return did_set_opt_flags(p_swb, p_swb_values, &swb_flags, TRUE);
 }
@@ -121431,11 +119397,11 @@
     int
 expand_set_switchbuf(optexpand_T *args, int *numMatches, char_u ***matches)
 {
-    return expand_set_opt_string(args, p_swb_values, ARRAY_LENGTH(p_swb_values) - 1, numMatches, matches);
+    return expand_set_opt_string(args, p_swb_values,  (sizeof(p_swb_values) / sizeof((p_swb_values)[0]))  - 1, numMatches, matches);
 }
 
     char *
-did_set_tabclose(optset_T *args UNUSED)
+did_set_tabclose(optset_T *args  __attribute__((unused)) )
 {
     return did_set_opt_flags(p_tcl, p_tcl_values, &tcl_flags, TRUE);
 }
@@ -121443,7 +119409,7 @@
     int
 expand_set_tabclose(optexpand_T *args, int *numMatches, char_u ***matches)
 {
-    return expand_set_opt_string(args, p_tcl_values, ARRAY_LENGTH(p_tcl_values) - 1, numMatches, matches);
+    return expand_set_opt_string(args, p_tcl_values,  (sizeof(p_tcl_values) / sizeof((p_tcl_values)[0]))  - 1, numMatches, matches);
 }
 
     char *
@@ -121478,17 +119444,17 @@
     int
 expand_set_tagcase(optexpand_T *args, int *numMatches, char_u ***matches)
 {
-    return expand_set_opt_string(args, p_tc_values, ARRAY_LENGTH(p_tc_values) - 1, numMatches, matches);
+    return expand_set_opt_string(args, p_tc_values,  (sizeof(p_tc_values) / sizeof((p_tc_values)[0]))  - 1, numMatches, matches);
 }
 
     char *
-did_set_term(optset_T *args UNUSED)
+did_set_term(optset_T *args  __attribute__((unused)) )
 {
-    if (T_NAME[0] == NUL)
+    if ( ( term_strings[(int)(KS_NAME)] ) [0] == NUL)
     {
         return e_cannot_set_term_to_empty_string;
     }
-    if (set_termname(T_NAME) == FAIL)
+    if (set_termname( ( term_strings[(int)(KS_NAME)] ) ) == FAIL)
     {
         return e_not_found_in_termcap;
     }
@@ -121508,42 +119474,42 @@
         return NULL;
     }
 
-    if (varp == &T_CCO)
+    if (varp == & ( term_strings[(int)(KS_CCO)] ) )
     {
-        int colors = atoi((char *)T_CCO);
+        int colors = atoi((char *) ( term_strings[(int)(KS_CCO)] ) );
 
         if (colors != t_colors)
         {
             t_colors = colors;
             if (t_colors <= 1)
             {
-                vim_free(T_CCO);
-                T_CCO = empty_option;
+                vim_free( ( term_strings[(int)(KS_CCO)] ) );
+                 ( term_strings[(int)(KS_CCO)] )  = empty_option;
             }
             init_highlight(TRUE, FALSE);
         }
     }
     ttest(FALSE);
-    if (varp == &T_ME)
+    if (varp == & ( term_strings[(int)(KS_ME)] ) )
     {
-        out_str(T_ME);
+        out_str( ( term_strings[(int)(KS_ME)] ) );
         redraw_later(UPD_CLEAR);
     }
-    if (varp == &T_BE && termcap_active)
+    if (varp == & ( term_strings[(int)(KS_CBE)] )  && termcap_active)
     {
-        MAY_WANT_TO_LOG_THIS;
+          ;
 
-        if (*T_BE == NUL)
+        if (* ( term_strings[(int)(KS_CBE)] )  == NUL)
         {
-            out_str(T_BD);
+            out_str( ( term_strings[(int)(KS_CBD)] ) );
         }
         else
         {
-            out_str(T_BE);
+            out_str( ( term_strings[(int)(KS_CBE)] ) );
         }
     }
 
-    if (varp == &T_BSU || varp == &T_ESU)
+    if (varp == & ( term_strings[(int)(KS_BSU)] )  || varp == & ( term_strings[(int)(KS_ESU)] ) )
     {
         term_set_sync_output(TERM_SYNC_OUTPUT_OFF);
     }
@@ -121552,13 +119518,13 @@
 }
 
     char *
-did_set_termresize(optset_T *args UNUSED)
+did_set_termresize(optset_T *args  __attribute__((unused)) )
 {
-    if (*p_trz == NUL || STRCMP(p_trz, "inband") == 0)
+    if (*p_trz == NUL ||  strcmp((char *)(p_trz), (char *)("inband"))  == 0)
     {
         term_set_win_resize(true);
     }
-    else if (STRCMP(p_trz, "sigwinch") == 0)
+    else if ( strcmp((char *)(p_trz), (char *)("sigwinch"))  == 0)
     {
         term_set_win_resize(false);
     }
@@ -121573,7 +119539,7 @@
     int
 expand_set_termresize(optexpand_T *args, int *numMatches, char_u ***matches)
 {
-    return expand_set_opt_string(args, p_trz_values, ARRAY_LENGTH(p_trz_values) - 1, numMatches, matches);
+    return expand_set_opt_string(args, p_trz_values,  (sizeof(p_trz_values) / sizeof((p_trz_values)[0]))  - 1, numMatches, matches);
 }
 
     char *
@@ -121585,7 +119551,7 @@
 }
 
     char *
-did_set_ttymouse(optset_T *args UNUSED)
+did_set_ttymouse(optset_T *args  __attribute__((unused)) )
 {
     char *errmsg = NULL;
 
@@ -121609,11 +119575,11 @@
     int
 expand_set_ttymouse(optexpand_T *args, int *numMatches, char_u ***matches)
 {
-    return expand_set_opt_string(args, p_ttym_values, ARRAY_LENGTH(p_ttym_values) - 1, numMatches, matches);
+    return expand_set_opt_string(args, p_ttym_values,  (sizeof(p_ttym_values) / sizeof((p_ttym_values)[0]))  - 1, numMatches, matches);
 }
 
     char *
-did_set_verbosefile(optset_T *args UNUSED)
+did_set_verbosefile(optset_T *args  __attribute__((unused)) )
 {
     verbose_stop();
     if (*p_vfile != NUL && verbose_open() == FAIL)
@@ -121632,8 +119598,8 @@
 
     if (args->os_flags & OPT_LOCAL)
     {
-        ve = curwin->w_p_ve;
-        flags = &curwin->w_ve_flags;
+        ve = curwin-> w_onebuf_opt.wo_ve ;
+        flags = &curwin-> w_onebuf_opt.wo_ve_flags ;
     }
 
     if ((args->os_flags & OPT_LOCAL) && *ve == NUL)
@@ -121646,7 +119612,7 @@
         {
             return e_invalid_argument;
         }
-        else if (STRCMP(ve, args->os_oldval.string) != 0)
+        else if ( strcmp((char *)(ve), (char *)(args->os_oldval.string))  != 0)
         {
             validate_virtcol();
             coladvance(curwin->w_virtcol);
@@ -121659,7 +119625,7 @@
     int
 expand_set_virtualedit(optexpand_T *args, int *numMatches, char_u ***matches)
 {
-    return expand_set_opt_string(args, p_ve_values, ARRAY_LENGTH(p_ve_values) - 1, numMatches, matches);
+    return expand_set_opt_string(args, p_ve_values,  (sizeof(p_ve_values) / sizeof((p_ve_values)[0]))  - 1, numMatches, matches);
 }
 
     char *
@@ -121667,17 +119633,17 @@
 {
     char_u      **varp = (char_u **)args->os_varp;
 
-    return did_set_option_listflag(*varp, (char_u *)(WW_ALL ","), args->os_errbuf, args->os_errbuflen);
+    return did_set_option_listflag(*varp, (char_u *)( "bshl<>[]~"  ","), args->os_errbuf, args->os_errbuflen);
 }
 
     int
 expand_set_whichwrap(optexpand_T *args, int *numMatches, char_u ***matches)
 {
-    return expand_set_opt_listflag(args, (char_u*)WW_ALL, numMatches, matches);
+    return expand_set_opt_listflag(args, (char_u*) "bshl<>[]~" , numMatches, matches);
 }
 
     char *
-did_set_wildmode(optset_T *args UNUSED)
+did_set_wildmode(optset_T *args  __attribute__((unused)) )
 {
     if (check_opt_wim() == FAIL)
     {
@@ -121689,11 +119655,11 @@
     int
 expand_set_wildmode(optexpand_T *args, int *numMatches, char_u ***matches)
 {
-    return expand_set_opt_string(args, p_wim_values, ARRAY_LENGTH(p_wim_values) - 1, numMatches, matches);
+    return expand_set_opt_string(args, p_wim_values,  (sizeof(p_wim_values) / sizeof((p_wim_values)[0]))  - 1, numMatches, matches);
 }
 
     char *
-did_set_wildoptions(optset_T *args UNUSED)
+did_set_wildoptions(optset_T *args  __attribute__((unused)) )
 {
     return did_set_opt_strings(p_wop, p_wop_values, TRUE);
 }
@@ -121701,11 +119667,11 @@
     int
 expand_set_wildoptions(optexpand_T *args, int *numMatches, char_u ***matches)
 {
-    return expand_set_opt_string(args, p_wop_values, ARRAY_LENGTH(p_wop_values) - 1, numMatches, matches);
+    return expand_set_opt_string(args, p_wop_values,  (sizeof(p_wop_values) / sizeof((p_wop_values)[0]))  - 1, numMatches, matches);
 }
 
     char *
-did_set_wincolor(optset_T *args UNUSED)
+did_set_wincolor(optset_T *args  __attribute__((unused)) )
 {
     update_wincolor(curwin, args->os_newval.string);
     return NULL;
@@ -121724,7 +119690,6 @@
 
     if ((xp->xp_pattern > args->oe_set_arg && *(xp->xp_pattern-1) == ':') || xp->xp_pattern == args->oe_set_arg || *(xp->xp_pattern-1) == ',')
     {
-
         if (*xp->xp_pattern == '!')
         {
             return expand_hl_occasions(args, numMatches, matches, '!');
@@ -121735,7 +119700,8 @@
         }
     }
 
-    VIM_CLEAR(*matches);
+     vim_free(*matches);
+     (*matches) = NULL;
     return FAIL;
 }
 
@@ -121778,7 +119744,7 @@
     opt_did_set_cb_T did_set_cb = get_option_did_set_cb(opt_idx);
     optset_T    args;
 
-    if (varp == &T_NAME)
+    if (varp == & ( term_strings[(int)(KS_NAME)] ) )
     {
         opt_idx = findoption((char_u *)"term");
         if (opt_idx >= 0)
@@ -121788,7 +119754,7 @@
         }
     }
 
-    CLEAR_FIELD(args);
+      memset((&(args)), (0), (sizeof(args)))  ;
 
     if ((secure) && (get_option_flags(opt_idx) & P_SECURE))
     {
@@ -121904,8 +119870,8 @@
                 return FAIL;
             }
 
-            len = (int)STRLEN(values[i]);
-            if (STRNCMP(values[i], val, len) == 0 && ((list && val[len] == ',') || val[len] == NUL))
+            len = (int) strlen((char *)(values[i])) ;
+            if ( strncmp((char *)(values[i]), (char *)(val), (len))  == 0 && ((list && val[len] == ',') || val[len] == NUL))
             {
                 val += len + (val[len] == ',');
                 new_flags |= (1 << i);
@@ -121930,7 +119896,7 @@
     void
 save_clear_shm_value(void)
 {
-    if (STRLEN(p_shm) >= SHM_LEN)
+    if ( strlen((char *)(p_shm))  >= SHM_LEN)
     {
         iemsg(e_internal_error_shortmess_too_long);
         return;
@@ -121938,7 +119904,7 @@
 
     if (++set_shm_recursive == 1)
     {
-        STRCPY(shm_buf, p_shm);
+         strcpy((char *)(shm_buf), (char *)(p_shm)) ;
         set_option_value_give_err((char_u *)"shm", 0L, (char_u *)"", 0);
     }
 }
@@ -121949,7 +119915,7 @@
     if (--set_shm_recursive == 0)
     {
         set_option_value_give_err((char_u *)"shm", 0L, shm_buf, 0);
-        vim_memset(shm_buf, 0, SHM_LEN);
+         memset((shm_buf), (0), (SHM_LEN)) ;
     }
 }
 
@@ -121961,21 +119927,21 @@
     char_u      *q = p_rtp;
     string_T    buf;
 
-    buf.string = alloc(MAXPATHL);
+    buf.string = alloc( PATH_MAX );
     if (buf.string == NULL)
     {
         return;
     }
 
-    buf.length = (size_t)copy_option_part(&q, buf.string, MAXPATHL, ",");
+    buf.length = (size_t)copy_option_part(&q, buf.string,  PATH_MAX , ",");
 
     p = vim_getenv((char_u *)"MYVIMDIR", &dofree);
 
-    if (p == NULL || STRCMP(p, buf.string) != 0)
+    if (p == NULL ||  strcmp((char *)(p), (char *)(buf.string))  != 0)
     {
         if (*buf.string != NUL && !after_pathsep(buf.string, buf.string + buf.length))
         {
-            STRCPY(buf.string + buf.length, PATHSEPSTR);
+             strcpy((char *)(buf.string + buf.length), (char *)( "/" )) ;
         }
         vim_setenv((char_u *)"MYVIMDIR", buf.string);
     }
@@ -121988,10 +119954,6 @@
 
 // ==================== os_unix.c ====================
 
-// ---------------- begin os_unixx.h ----------------
-
-// ---------------- end os_unixx.h ----------------
-
 static int ignore_sigtstp = FALSE;
 
 static int get_x11_title(int);
@@ -122011,20 +119973,17 @@
 
 static void handle_resize(void);
 
-static void sig_winch SIGPROTOARG;
-static void sig_tstp SIGPROTOARG;
+static void sig_winch  (int) ;
+static void sig_tstp  (int) ;
 static volatile sig_atomic_t in_mch_suspend = FALSE;
-static void catch_sigint SIGPROTOARG;
-static void catch_sigusr1 SIGPROTOARG;
-static void catch_sigpwr SIGPROTOARG;
-static void deathtrap SIGPROTOARG;
+static void catch_sigint  (int) ;
+static void catch_sigusr1  (int) ;
+static void catch_sigpwr  (int) ;
+static void deathtrap  (int) ;
 
 static void catch_int_signal(void);
 static void set_signals(void);
 static void catch_signals(void (*func_deadly)(int), void (*func_other)(int));
-# define SIGSET_DECL(set)       sigset_t set;
-# define BLOCK_SIGNALS(set)     block_signals(set)
-# define UNBLOCK_SIGNALS(set)   unblock_signals(set)
 static int  have_wildcard(int, char_u **);
 static int  have_dollars(int, char_u **);
 
@@ -122135,8 +120094,6 @@
     return chdir(path);
 }
 
-# define NEW_TTY_SYSTEM
-
     void
 mch_write(char_u *s, int len)
 {
@@ -122181,7 +120138,7 @@
 }
 
     long_u
-mch_total_mem(int special UNUSED)
+mch_total_mem(int special  __attribute__((unused)) )
 {
     long_u      mem = 0;
     long_u      shiftright = 10;
@@ -122306,14 +120263,14 @@
 }
 
     static void
-sig_winch SIGDEFARG(sigarg)
+sig_winch  (int sigarg  __attribute__((unused)) ) 
 {
     mch_signal(SIGWINCH, sig_winch);
     do_resize = TRUE;
 }
 
     static void
-sig_tstp SIGDEFARG(sigarg)
+sig_tstp  (int sigarg  __attribute__((unused)) ) 
 {
     if (in_mch_suspend)
     {
@@ -122329,28 +120286,28 @@
 }
 
     static void
-catch_sigint SIGDEFARG(sigarg)
+catch_sigint  (int sigarg  __attribute__((unused)) ) 
 {
     mch_signal(SIGINT, catch_sigint);
     got_int = TRUE;
 }
 
     static void
-catch_sigusr1 SIGDEFARG(sigarg)
+catch_sigusr1  (int sigarg  __attribute__((unused)) ) 
 {
     mch_signal(SIGUSR1, catch_sigusr1);
     got_sigusr1 = TRUE;
 }
 
     static void
-catch_sigpwr SIGDEFARG(sigarg)
+catch_sigpwr  (int sigarg  __attribute__((unused)) ) 
 {
     mch_signal(SIGPWR, catch_sigpwr);
     ml_sync_all(FALSE, FALSE);
 }
 
     static void
-deathtrap SIGDEFARG(sigarg)
+deathtrap  (int sigarg  __attribute__((unused)) ) 
 {
     static int  entered = 0;
     int         i;
@@ -122393,7 +120350,7 @@
     }
     if (entered == 2)
     {
-        OUT_STR("Vim: Double signal, exiting\n");
+         out_str((char_u *)("Vim: Double signal, exiting\n")) ;
         out_flush();
         getout(1);
     }
@@ -122414,13 +120371,13 @@
     did_check_timestamps = FALSE;
 }
 
-static void sigcont_handler SIGPROTOARG;
+static void sigcont_handler  (int) ;
 
 static volatile sig_atomic_t sigcont_received;
-static void sigcont_handler SIGPROTOARG;
+static void sigcont_handler  (int) ;
 
     static void
-sigcont_handler SIGDEFARG(sigarg)
+sigcont_handler  (int sigarg  __attribute__((unused)) ) 
 {
     if (in_mch_suspend)
     {
@@ -122574,11 +120531,11 @@
 
     switch (sig)
     {
-        case SIGNAL_BLOCK:
+        case  (-1) :
             blocked = TRUE;
                              break;
 
-        case SIGNAL_UNBLOCK:
+        case  (-2) :
             blocked = FALSE;
                              if (got_signal != 0)
                              {
@@ -122603,7 +120560,7 @@
 }
 
     int
-mch_check_win(int argc UNUSED, char **argv UNUSED)
+mch_check_win(int argc  __attribute__((unused)) , char **argv  __attribute__((unused)) )
 {
     if (isatty(1))
     {
@@ -122623,7 +120580,7 @@
 }
 
     static int
-get_x11_title(int test_only UNUSED)
+get_x11_title(int test_only  __attribute__((unused)) )
 {
     return FALSE;
 }
@@ -122633,13 +120590,13 @@
 {
     if (!test_only)
     {
-        if (STRNCMP(T_NAME, "builtin_", 8) == 0)
+        if ( strncmp((char *)( ( term_strings[(int)(KS_NAME)] ) ), (char *)("builtin_"), (8))  == 0)
         {
-            oldicon = vim_strsave(T_NAME + 8);
+            oldicon = vim_strsave( ( term_strings[(int)(KS_NAME)] )  + 8);
         }
         else
         {
-            oldicon = vim_strsave(T_NAME);
+            oldicon = vim_strsave( ( term_strings[(int)(KS_NAME)] ) );
         }
     }
     return FALSE;
@@ -122663,7 +120620,7 @@
     int         type = 0;
     static int  recursive = 0;
 
-    if (T_NAME == NULL)
+    if ( ( term_strings[(int)(KS_NAME)] )  == NULL)
     {
         return;
     }
@@ -122678,37 +120635,38 @@
     }
     ++recursive;
 
-    if ((type || *T_TS != NUL) && title != NULL)
+    if ((type || * ( term_strings[(int)(KS_TS)] )  != NUL) && title != NULL)
     {
         if (oldtitle_outdated)
         {
             oldtitle_outdated = FALSE;
-            VIM_CLEAR(oldtitle);
+             vim_free(oldtitle);
+             (oldtitle) = NULL;
         }
         if (oldtitle == NULL)
         {
             (void)get_x11_title(FALSE);
         }
 
-        if (*T_TS != NUL)
+        if (* ( term_strings[(int)(KS_TS)] )  != NUL)
         {
             term_settitle(title);
         }
         unix_did_set_title = TRUE;
     }
 
-    if ((type || *T_CIS != NUL) && icon != NULL)
+    if ((type || * ( term_strings[(int)(KS_CIS)] )  != NUL) && icon != NULL)
     {
         if (oldicon == NULL)
         {
             get_x11_icon(FALSE);
         }
 
-        if (*T_CIS != NUL)
+        if (* ( term_strings[(int)(KS_CIS)] )  != NUL)
         {
-            out_str(T_CIS);
+            out_str( ( term_strings[(int)(KS_CIS)] ) );
             out_str_nf(icon);
-            out_str(T_CIE);
+            out_str( ( term_strings[(int)(KS_CIE)] ) );
             out_flush();
         }
         did_set_icon = TRUE;
@@ -122749,13 +120707,13 @@
     {
         return FALSE;
     }
-    return ((STRNICMP(name, "xterm", 5) == 0 && STRNICMP(name, "xterm-kitty", 11) != 0) || STRNICMP(name, "nxterm", 6) == 0 || STRNICMP(name, "kterm", 5) == 0 || STRNICMP(name, "mlterm", 6) == 0 || STRNICMP(name, "rxvt", 4) == 0 || STRNICMP(name, "screen.xterm", 12) == 0 || STRCMP(name, "builtin_xterm") == 0);
+    return (( strncasecmp((char *)(name), (char *)("xterm"), (5))  == 0 &&  strncasecmp((char *)(name), (char *)("xterm-kitty"), (11))  != 0) ||  strncasecmp((char *)(name), (char *)("nxterm"), (6))  == 0 ||  strncasecmp((char *)(name), (char *)("kterm"), (5))  == 0 ||  strncasecmp((char *)(name), (char *)("mlterm"), (6))  == 0 ||  strncasecmp((char *)(name), (char *)("rxvt"), (4))  == 0 ||  strncasecmp((char *)(name), (char *)("screen.xterm"), (12))  == 0 ||  strcmp((char *)(name), (char *)("builtin_xterm"))  == 0);
 }
 
     int
 use_xterm_like_mouse(char_u *name)
 {
-    return (name != NULL && (term_is_xterm || STRNICMP(name, "screen", 6) == 0 || STRNICMP(name, "tmux", 4) == 0 || STRNICMP(name, "gnome", 5) == 0 || STRICMP(name, "st") == 0 || STRNICMP(name, "st-", 3) == 0 || STRNICMP(name, "stterm", 6) == 0));
+    return (name != NULL && (term_is_xterm ||  strncasecmp((char *)(name), (char *)("screen"), (6))  == 0 ||  strncasecmp((char *)(name), (char *)("tmux"), (4))  == 0 ||  strncasecmp((char *)(name), (char *)("gnome"), (5))  == 0 ||  strcasecmp((char *)(name), (char *)("st"))  == 0 ||  strncasecmp((char *)(name), (char *)("st-"), (3))  == 0 ||  strncasecmp((char *)(name), (char *)("stterm"), (6))  == 0));
 }
 
     int
@@ -122780,7 +120738,6 @@
     return 0;
 }
 
-
     int
 mch_get_user_name(char_u *s, int len)
 {
@@ -122841,7 +120798,7 @@
 {
     if (getcwd((char *)buf, len) == NULL)
     {
-        STRCPY(buf, strerror(errno));
+         strcpy((char *)(buf), (char *)(strerror(errno))) ;
         return FAIL;
     }
     return OK;
@@ -122853,19 +120810,19 @@
     int         buflen = 0;
     int         fd = -1;
     static int  dont_fchdir = FALSE;
-    char_u      olddir[MAXPATHL];
+    char_u      olddir[ PATH_MAX ];
     char_u      *p;
     int         retval = OK;
 
     if ((force || !mch_isFullName(fname)) && ((p = vim_strrchr(fname, '/')) == NULL || p != fname))
     {
-        if (p == NULL && STRCMP(fname, "..") == 0)
+        if (p == NULL &&  strcmp((char *)(fname), (char *)(".."))  == 0)
         {
             p = fname + 2;
         }
         if (p != NULL)
         {
-            if (STRCMP(p, "/..") == 0)
+            if ( strcmp((char *)(p), (char *)("/.."))  == 0)
             {
                 p += 3;
             }
@@ -122881,7 +120838,7 @@
                 }
             }
 
-            if (fd < 0 && (mch_dirname(olddir, MAXPATHL) == FAIL || mch_chdir((char *)olddir) != 0))
+            if (fd < 0 && (mch_dirname(olddir,  PATH_MAX ) == FAIL || mch_chdir((char *)olddir) != 0))
             {
                 p = NULL;
                 retval = FAIL;
@@ -122951,31 +120908,31 @@
             close(fd);
         }
 
-        buflen = (int)STRLEN(buf);
+        buflen = (int) strlen((char *)(buf)) ;
         if (buflen >= len - 1)
         {
             retval = FAIL;
         }
-        else if (buflen > 0 && buf[buflen - 1] != PATHSEP && *fname != NUL && STRCMP(fname, ".") != 0)
+        else if (buflen > 0 && buf[buflen - 1] !=  ((char_u)'/')  && *fname != NUL &&  strcmp((char *)(fname), (char *)("."))  != 0)
         {
-            STRCPY(buf + buflen, PATHSEPSTR);
-            buflen += sizeof(PATHSEP);
+             strcpy((char *)(buf + buflen), (char *)( "/" )) ;
+            buflen += sizeof( ((char_u)'/') );
         }
     }
 
     if (buflen == 0)
     {
-        buflen = (int)STRLEN(buf);
+        buflen = (int) strlen((char *)(buf)) ;
     }
 
-    if (retval == FAIL || (int)(buflen + STRLEN(fname)) >= len)
+    if (retval == FAIL || (int)(buflen +  strlen((char *)(fname)) ) >= len)
     {
         return FAIL;
     }
 
-    if (STRCMP(fname, ".") != 0)
+    if ( strcmp((char *)(fname), (char *)("."))  != 0)
     {
-        STRCPY(buf + buflen, fname);
+         strcpy((char *)(buf + buflen), (char *)(fname)) ;
     }
 
     return OK;
@@ -123012,14 +120969,14 @@
 }
 
     vim_acl_T
-mch_get_acl(char_u *fname UNUSED)
+mch_get_acl(char_u *fname  __attribute__((unused)) )
 {
     vim_acl_T   ret = NULL;
     return ret;
 }
 
     void
-mch_set_acl(char_u *fname UNUSED, vim_acl_T aclent)
+mch_set_acl(char_u *fname  __attribute__((unused)) , vim_acl_T aclent)
 {
     if (aclent == NULL)
     {
@@ -123037,7 +120994,7 @@
 }
 
     void
-mch_hide(char_u *name UNUSED)
+mch_hide(char_u *name  __attribute__((unused)) )
 {
 }
 
@@ -123066,7 +121023,7 @@
     {
         return FALSE;
     }
-    if (mch_lstat((char *)name, &statb))
+    if ( lstat(((char *)name), (&statb)) )
     {
         return FALSE;
     }
@@ -123082,7 +121039,7 @@
     {
         return 0;
     }
-    return S_ISREG(st.st_mode) && mch_access((char *)name, X_OK) == 0;
+    return S_ISREG(st.st_mode) &&  access(((char *)name), (X_OK))  == 0;
 }
 
     int
@@ -123122,8 +121079,8 @@
     {
         return -1;
     }
-    p_end = p + STRLEN(p);
-    bufsize = STRLEN(name) + (size_t)(p_end - p) + 2;
+    p_end = p +  strlen((char *)(p)) ;
+    bufsize =  strlen((char *)(name))  + (size_t)(p_end - p) + 2;
     buf = alloc(bufsize);
     if (buf == NULL)
     {
@@ -123141,9 +121098,9 @@
         if (elen <= 1)
         {
             p = (char_u *)"./";
-            elen = STRLEN_LITERAL("./");
+            elen =  (sizeof("./" "") - 1) ;
         }
-        buflen = vim_snprintf((char *)buf, bufsize, "%.*s%s%s", (int)elen, p, (after_pathsep(p, p + elen)) ? "" : PATHSEPSTR, name);
+        buflen = vim_snprintf((char *)buf, bufsize, "%.*s%s%s", (int)elen, p, (after_pathsep(p, p + elen)) ? "" :  "/" , name);
         retval = executable_file(buf);
         if (retval == 1)
         {
@@ -123195,7 +121152,6 @@
     void
 mch_early_init(void)
 {
-
     signal_stack = alloc(get_signal_stack_size());
     init_signal_stack();
 
@@ -123214,11 +121170,11 @@
         {
             if (info_message)
             {
-                mch_msg("\n");
+                 printf("%s", ("\n")) ;
             }
             else
             {
-                mch_errmsg("\r\n");
+                 fprintf(stderr, "%s", ("\r\n")) ;
             }
         }
         else
@@ -123243,8 +121199,8 @@
         settmode(TMODE_COOK);
         if (!is_not_a_term())
         {
-            mch_restore_title(SAVE_RESTORE_BOTH);
-            term_pop_title(SAVE_RESTORE_BOTH);
+            mch_restore_title( (SAVE_RESTORE_TITLE | SAVE_RESTORE_ICON) );
+            term_pop_title( (SAVE_RESTORE_TITLE | SAVE_RESTORE_ICON) );
         }
 
         if (swapping_screen() && !newline_on_exit)
@@ -123327,7 +121283,7 @@
     tnew = told;
     if (tmode == TMODE_RAW)
     {
-        tnew.c_iflag &= ~(ICRNL | (T_XON == NULL || *T_XON == NUL ? IXON : 0));
+        tnew.c_iflag &= ~(ICRNL | ( ( term_strings[(int)(KS_XON)] )  == NULL || * ( term_strings[(int)(KS_XON)] )  == NUL ? IXON : 0));
         tnew.c_lflag &= ~(ICANON | ECHO | ISIG | ECHOE | IEXTEN);
         tnew.c_oflag &= ~(ONLCR | XTABS);
         tnew.c_cc[VMIN] = 1;
@@ -123420,7 +121376,7 @@
 
     xterm_mouse_vers = use_xterm_mouse();
 
-    if (T_CXM != NULL && *T_CXM != NUL)
+    if ( ( term_strings[(int)(KS_CXM)] )  != NULL && * ( term_strings[(int)(KS_CXM)] )  != NUL)
     {
         term_enable_mouse(on);
     }
@@ -123450,7 +121406,7 @@
 {
     if (use_xterm_mouse())
     {
-        set_mouse_termcode(KS_MOUSE, (char_u *)(term_is_8bit(T_NAME) ? "\233M" : "\033[M"));
+        set_mouse_termcode(KS_MOUSE, (char_u *)(term_is_8bit( ( term_strings[(int)(KS_NAME)] ) ) ? "\233M" : "\033[M"));
         if (*p_mouse != NUL)
         {
             mch_setmouse(FALSE);
@@ -123464,9 +121420,9 @@
 
     if (use_xterm_mouse() == 4)
     {
-        set_mouse_termcode(KS_SGR_MOUSE, (char_u *)(term_is_8bit(T_NAME) ? "\233<*M" : "\033[<*M"));
+        set_mouse_termcode(KS_SGR_MOUSE, (char_u *)(term_is_8bit( ( term_strings[(int)(KS_NAME)] ) ) ? "\233<*M" : "\033[<*M"));
 
-        set_mouse_termcode(KS_SGR_MOUSE_RELEASE, (char_u *)(term_is_8bit(T_NAME) ? "\233<*m" : "\033[<*m"));
+        set_mouse_termcode(KS_SGR_MOUSE_RELEASE, (char_u *)(term_is_8bit( ( term_strings[(int)(KS_NAME)] ) ) ? "\233<*m" : "\033[<*m"));
 
         if (*p_mouse != NUL)
         {
@@ -123526,12 +121482,10 @@
     return OK;
 }
 
-
-
     void
 mch_set_shellsize(void)
 {
-    if (*T_CWS)
+    if (* ( term_strings[(int)(KS_CWS)] ) )
     {
         term_set_winsize((int)Rows, (int)Columns);
         out_flush();
@@ -123571,7 +121525,7 @@
 }
 
     static void
-set_child_environment(long    rows, long    columns, char    *term, int     is_terminal UNUSED)
+set_child_environment(long    rows, long    columns, char    *term, int     is_terminal  __attribute__((unused)) )
 {
     char        envbuf[50];
 
@@ -123593,7 +121547,7 @@
 }
 
     static void
-may_send_sigint(int c UNUSED, pid_t pid UNUSED, pid_t wpid UNUSED)
+may_send_sigint(int c  __attribute__((unused)) , pid_t pid  __attribute__((unused)) , pid_t wpid  __attribute__((unused)) )
 {
     if (c == Ctrl_C || c == intr_char)
     {
@@ -123633,7 +121587,7 @@
             argv[argc++] = (char *)extra_shell_arg;
         }
 
-        *shcf_tofree = alloc(STRLEN(p_shcf) + 1);
+        *shcf_tofree = alloc( strlen((char *)(p_shcf))  + 1);
         if (*shcf_tofree == NULL)
         {
             return FAIL;
@@ -123657,8 +121611,7 @@
     return OK;
 }
 
-# define EXEC_FAILED 122
-# define OPEN_NULL_FAILED 123
+enum { EXEC_FAILED = 122 };
 
     static int
 mch_call_shell_fork(char_u      *cmd, int         options)
@@ -123717,12 +121670,12 @@
 
     if (!pipe_error)
     {
-        SIGSET_DECL(curset)
-        BLOCK_SIGNALS(&curset);
+         sigset_t curset; 
+         block_signals(&curset) ;
         pid = fork();
         if (pid == -1)
         {
-            UNBLOCK_SIGNALS(&curset);
+             unblock_signals(&curset) ;
 
             msg_puts(_("\nCannot fork\n"));
             if ((options & (SHELL_READ|SHELL_WRITE)))
@@ -123738,7 +121691,7 @@
         else if (pid == 0)
         {
             reset_signals();
-            UNBLOCK_SIGNALS(&curset);
+             unblock_signals(&curset) ;
 
             if (!show_shell_mess || (options & SHELL_EXPAND))
             {
@@ -123760,7 +121713,6 @@
             }
             else if ((options & (SHELL_READ|SHELL_WRITE)))
             {
-
                 if (p_stmp)
                 {
                     (void)setsid();
@@ -123789,10 +121741,9 @@
         {
             catch_signals(SIG_IGN, SIG_ERR);
             catch_int_signal();
-            UNBLOCK_SIGNALS(&curset);
+             unblock_signals(&curset) ;
             if ((options & (SHELL_READ|SHELL_WRITE)))
             {
-# define BUFLEN 100
                 char_u      buffer[BUFLEN + 1];
                 int         buffer_off = 0;
                 char_u      ta_buf[BUFLEN + 1];
@@ -123885,7 +121836,7 @@
                 }
 
                 noread_cnt = 0;
-                ELAPSED_INIT(start_tv);
+                 gettimeofday(&(start_tv), NULL) ;
                 for (;;)
                 {
                     len = 0;
@@ -123894,7 +121845,7 @@
                       if (ta_len == 0)
                       {
                           noread_cnt = 0;
-                          ELAPSED_INIT(start_tv);
+                           gettimeofday(&(start_tv), NULL) ;
                           len = ui_inchar(ta_buf, BUFLEN, 10L, 0);
                       }
                       if (ta_len > 0 || len > 0)
@@ -123948,7 +121899,7 @@
                             if (len > 0)
                             {
                                 ta_len -= len;
-                                mch_memmove(ta_buf, ta_buf + len, ta_len);
+                                 memmove((char *)(ta_buf), (char *)(ta_buf + len), ta_len) ;
                             }
                         }
                       }
@@ -124002,12 +121953,12 @@
 
                             for (p = buffer; p < buffer + len; p += l)
                             {
-                                l = MB_CPTR2LEN(p);
+                                l =  (enc_utf8 ? utf_ptr2len(p) : (*mb_ptr2len)(p)) ;
                                 if (l == 0)
                                 {
                                     l = 1;
                                 }
-                                else if (MB_BYTE2LEN(*p) != l)
+                                else if ( mb_bytelen_tab[*p]  != l)
                                 {
                                     break;
                                 }
@@ -124031,7 +121982,7 @@
                             {
                                 *p = c;
                                 buffer_off = (buffer + len) - p;
-                                mch_memmove(buffer, p, buffer_off);
+                                 memmove((char *)(buffer), (char *)(p), buffer_off) ;
                                 continue;
                             }
                             buffer_off = 0;
@@ -124052,7 +122003,7 @@
 
                         if (wait_pid == 0)
                         {
-                            long        msec = ELAPSED_FUNC(start_tv);
+                            long        msec =  elapsed(&(start_tv)) ;
 
                             if (msec > 2000)
                             {
@@ -124206,13 +122157,12 @@
 }
 
     static int
-RealWaitForChar(int fd, long msec, int *check_for_gpm UNUSED, int *interrupted)
+RealWaitForChar(int fd, long msec, int *check_for_gpm  __attribute__((unused)) , int *interrupted)
 {
     int         ret;
     int         result;
 
     {
-
         struct timeval  tv;
         struct timeval  *tvp;
         static fd_set rfds;
@@ -124245,7 +122195,7 @@
             *interrupted = FALSE;
         }
 
-        ret = select(maxfd + 1, SELECT_TYPE_ARG234 &rfds, SELECT_TYPE_ARG234 &wfds, SELECT_TYPE_ARG234 &efds, tvp);
+        ret = select(maxfd + 1,  (fd_set *)  &rfds,  (fd_set *)  &wfds,  (fd_set *)  &efds, tvp);
         result = ret > 0 && FD_ISSET(fd, &rfds);
         if (result)
         {
@@ -124286,8 +122236,6 @@
     return unix_expandpath(gap, path, 0, flags, FALSE);
 }
 
-# define SHELL_SPECIAL (char_u *)"\t \"&'$;<>()\\|\n"
-
     int
 mch_expand_wildcards(int            num_pat, char_u       **pat, int           *num_file, char_u      ***file, int            flags)
 {
@@ -124304,18 +122252,12 @@
     size_t      commandlen;
     FILE        *fd;
     char_u      *buffer;
-# define STYLE_ECHO     0
-# define STYLE_GLOB     1
-# define STYLE_VIMGLOB  2
-# define STYLE_PRINT    3
-# define STYLE_BT       4
-# define STYLE_GLOBSTAR 5
     int         shell_style = STYLE_ECHO;
     int         check_spaces;
     static int  did_find_nul = FALSE;
     int         ampersand = FALSE;
-    static string_T sh_vimglob_func = STR_LITERAL_INIT("vimglob() { while [ $# -ge 1 ]; do echo \"$1\"; shift; done }; vimglob >");
-    static string_T sh_globstar_opt = STR_LITERAL_INIT("[[ ${BASH_VERSINFO[0]} -ge 4 ]] && shopt -s globstar; ");
+    static string_T sh_vimglob_func =  {(char_u *)("vimglob() { while [ $# -ge 1 ]; do echo \"$1\"; shift; done }; vimglob >"),  (sizeof("vimglob() { while [ $# -ge 1 ]; do echo \"$1\"; shift; done }; vimglob >" "") - 1) } ;
+    static string_T sh_globstar_opt =  {(char_u *)("[[ ${BASH_VERSINFO[0]} -ge 4 ]] && shopt -s globstar; "),  (sizeof("[[ ${BASH_VERSINFO[0]} -ge 4 ]] && shopt -s globstar; " "") - 1) } ;
 
     *num_file = 0;
     *file = NULL;
@@ -124342,17 +122284,17 @@
         return FAIL;
     }
 
-    if (num_pat == 1 && *pat[0] == '`' && (len = STRLEN(pat[0])) > 2 && *(pat[0] + len - 1) == '`')
+    if (num_pat == 1 && *pat[0] == '`' && (len =  strlen((char *)(pat[0])) ) > 2 && *(pat[0] + len - 1) == '`')
     {
         shell_style = STYLE_BT;
     }
-    else if ((len = STRLEN(p_sh)) >= 3)
+    else if ((len =  strlen((char *)(p_sh)) ) >= 3)
     {
-        if (STRCMP(p_sh + len - 3, "csh") == 0)
+        if ( strcmp((char *)(p_sh + len - 3), (char *)("csh"))  == 0)
         {
             shell_style = STYLE_GLOB;
         }
-        else if (STRCMP(p_sh + len - 3, "zsh") == 0)
+        else if ( strcmp((char *)(p_sh + len - 3), (char *)("zsh"))  == 0)
         {
             shell_style = STYLE_PRINT;
         }
@@ -124369,7 +122311,7 @@
         }
     }
 
-    tempnamelen = STRLEN(tempname);
+    tempnamelen =  strlen((char *)(tempname)) ;
     len = tempnamelen + 29;
     if (shell_style == STYLE_VIMGLOB)
     {
@@ -124385,7 +122327,7 @@
         ++len;
         for (j = 0; pat[i][j] != NUL; ++j)
         {
-            if (vim_strchr(SHELL_SPECIAL, pat[i][j]) != NULL)
+            if (vim_strchr( (char_u *)"\t \"&'$;<>()\\|\n" , pat[i][j]) != NULL)
             {
                 ++len;
             }
@@ -124411,7 +122353,7 @@
         else
         {
             --p;
-            while (p > command && VIM_ISWHITE(*p))
+            while (p > command &&  ((*p) == ' ' || (*p) == '\t') )
             {
                 --p;
             }
@@ -124421,7 +122363,7 @@
             }
 
             --p;
-            while (p > command && VIM_ISWHITE(*p))
+            while (p > command &&  ((*p) == ' ' || (*p) == '\t') )
             {
                 --p;
             }
@@ -124469,13 +122411,13 @@
                 }
                 else if (pat[i][j] == '\\' && pat[i][j + 1] != NUL)
                 {
-                    if (intick || vim_strchr(SHELL_SPECIAL, pat[i][j + 1]) != NULL || pat[i][j + 1] == '`')
+                    if (intick || vim_strchr( (char_u *)"\t \"&'$;<>()\\|\n" , pat[i][j + 1]) != NULL || pat[i][j + 1] == '`')
                     {
                         *p++ = '\\';
                     }
                     ++j;
                 }
-                else if (!intick && ((flags & EW_KEEPDOLLAR) == 0 || pat[i][j] != '$') && vim_strchr(SHELL_SPECIAL, pat[i][j]) != NULL)
+                else if (!intick && ((flags & EW_KEEPDOLLAR) == 0 || pat[i][j] != '$') && vim_strchr( (char_u *)"\t \"&'$;<>()\\|\n" , pat[i][j]) != NULL)
                 {
                     *p++ = '\\';
                 }
@@ -124492,7 +122434,7 @@
     }
     if (ampersand)
     {
-        STRCPY(command + commandlen, "&");
+         strcpy((char *)(command + commandlen), (char *)("&")) ;
     }
 
     if (shell_style == STYLE_PRINT)
@@ -124518,7 +122460,7 @@
 
     if (i != 0)
     {
-        mch_remove(tempname);
+         unlink((char *)(tempname)) ;
         vim_free(tempname);
         if (!(flags & EW_SILENT))
         {
@@ -124537,7 +122479,7 @@
         goto notfound;
     }
 
-    fd = fopen((char *)tempname, READBIN);
+    fd = fopen((char *)tempname,  "r" );
     if (fd == NULL)
     {
         if (!(flags & EW_SILENT))
@@ -124561,7 +122503,7 @@
     }
     if (buffer == NULL)
     {
-        mch_remove(tempname);
+         unlink((char *)(tempname)) ;
         vim_free(tempname);
         fclose(fd);
         return FAIL;
@@ -124569,7 +122511,7 @@
     len = llen;
     i = fread((char *)buffer, 1, len, fd);
     fclose(fd);
-    mch_remove(tempname);
+     unlink((char *)(tempname)) ;
     if (i != (int)len)
     {
         semsg(_(e_cant_read_file_str), tempname);
@@ -124615,7 +122557,7 @@
         if (shell_style == STYLE_PRINT && !did_find_nul)
         {
             buffer[len] = NUL;
-            if (len && (int)STRLEN(buffer) < (int)len)
+            if (len && (int) strlen((char *)(buffer))  < (int)len)
             {
                 did_find_nul = TRUE;
             }
@@ -124653,7 +122595,7 @@
         goto notfound;
     }
     *num_file = i;
-    *file = ALLOC_MULT(char_u *, i);
+    *file =  (char_u * *)alloc(sizeof(char_u *) * (i)) ;
     if (*file == NULL)
     {
         vim_free(buffer);
@@ -124709,10 +122651,10 @@
             continue;
         }
 
-        p = alloc(STRLEN((*file)[i]) + 1 + dir);
+        p = alloc( strlen((char *)((*file)[i]))  + 1 + dir);
         if (p)
         {
-            STRCPY(p, (*file)[i]);
+             strcpy((char *)(p), (char *)((*file)[i])) ;
             if (dir)
             {
                 add_pathsep(p);
@@ -124725,7 +122667,8 @@
 
     if (*num_file == 0)
     {
-        VIM_CLEAR(*file);
+         vim_free(*file);
+         (*file) = NULL;
         goto notfound;
     }
 
@@ -124745,7 +122688,7 @@
     int         i;
     char_u      *s;
 
-    *file = ALLOC_MULT(char_u *, num_pat);
+    *file =  (char_u * *)alloc(sizeof(char_u *) * (num_pat)) ;
     if (*file == NULL)
     {
         return FAIL;
@@ -124766,7 +122709,7 @@
     int
 mch_has_exp_wildcard(char_u *p)
 {
-    for ( ; *p; MB_PTR_ADV(p))
+    for ( ; *p;  p += (*mb_ptr2len)(p) )
     {
         if (*p == '\\' && p[1] != NUL)
         {
@@ -124786,7 +122729,7 @@
     int
 mch_has_wildcard(char_u *p)
 {
-    for ( ; *p; MB_PTR_ADV(p))
+    for ( ; *p;  p += (*mb_ptr2len)(p) )
     {
         if (*p == '\\' && p[1] != NUL)
         {
@@ -124833,7 +122776,6 @@
     return FALSE;
 }
 
-
 // ==================== pathdef.c ====================
 static char_u *default_vim_dir = (char_u *)"/usr/local/share/vim";
 static char_u *default_vimruntime_dir = (char_u *)"";
@@ -124891,13 +122833,24 @@
 static void pum_draw_shadow(void);
 static void compute_margins(int *right_margin, int *left_margin, int *left_padding);
 
-#define PUM_DEF_HEIGHT 10
+enum { PUM_DEF_HEIGHT = 10 };
 
-#define DEFINE_PUM_SETTER(feature)      void     pum_set_##feature(int enable)     {    pum_##feature = enable ? 1 : 0;     }
+// Token pasting is the one thing C has no answer to, so the three functions
+// this macro defined are written out.
+void pum_set_border(int enable)
+{
+    pum_border = enable ? 1 : 0;
+}
 
-DEFINE_PUM_SETTER(border)
-DEFINE_PUM_SETTER(shadow)
-DEFINE_PUM_SETTER(margin)
+void pum_set_shadow(int enable)
+{
+    pum_shadow = enable ? 1 : 0;
+}
+
+void pum_set_margin(int enable)
+{
+    pum_margin = enable ? 1 : 0;
+}
 
     static void
 pum_compute_size(void)
@@ -125085,7 +123038,7 @@
         }
         else
         {
-            pum_win_row = curwin->w_wrow + W_WINROW(curwin);
+            pum_win_row = curwin->w_wrow +  (curwin->w_winrow) ;
         }
         pum_win_height = curwin->w_height;
         pum_win_col = curwin->w_wincol;
@@ -125216,7 +123169,7 @@
         return NULL;
     }
 
-    attrs = ALLOC_MULT(int, vim_strsize(text));
+    attrs =  (int *)alloc(sizeof(int) * (vim_strsize(text))) ;
     if (attrs == NULL)
     {
         return NULL;
@@ -125224,7 +123177,7 @@
 
     in_fuzzy = (State & MODE_CMDLINE) ? cmdline_compl_is_fuzzy()
                                           : (get_cot_flags() & COT_FUZZY) != 0;
-    leader_len = STRLEN(leader);
+    leader_len =  strlen((char *)(leader)) ;
 
     if (in_fuzzy)
     {
@@ -125254,7 +123207,7 @@
         }
         else
         {
-            if (matched_len < 0 && MB_STRNICMP(ptr, leader, leader_len) == 0)
+            if (matched_len < 0 &&  mb_strnicmp((char_u *)(ptr), (char_u *)(leader), (int)(leader_len))  == 0)
             {
                 matched_len = (int)leader_len;
             }
@@ -125279,7 +123232,7 @@
         }
         cell_idx += char_cells;
 
-        MB_PTR_ADV(ptr);
+         ptr += (*mb_ptr2len)(ptr) ;
         char_pos++;
     }
 
@@ -125292,7 +123245,7 @@
 }
 
     static void
-pum_screen_puts_with_attrs(int         row, int         col, int         cells UNUSED, char_u      *text, int         textlen, int         *attrs)
+pum_screen_puts_with_attrs(int         row, int         col, int         cells  __attribute__((unused)) , char_u      *text, int         textlen, int         *attrs)
 {
     int         col_start = col;
     char_u      *ptr = text;
@@ -125354,7 +123307,7 @@
         return col;
     }
 
-    size = (int)STRLEN(st);
+    size = (int) strlen((char *)(st)) ;
     cells = (*mb_string2cells)(st, size);
     truncated = width_limit - totwidth < cells + pad;
 
@@ -125380,7 +123333,7 @@
             st_end = st + size;
             while (st_end > st && cells > remaining)
             {
-                MB_PTR_BACK(st, st_end);
+                 st_end -= has_mbyte ? ((*mb_head_off)(st, (st_end) - 1) + 1) : 1 ;
                 cells -= has_mbyte ? (*mb_ptr2cells)(st_end) : 1;
             }
             size = st_end - st;
@@ -125413,7 +123366,8 @@
         screen_putchar(trunc, row, col + cells + over_cell, trunc_attr);
     }
 
-    VIM_CLEAR(st);
+     vim_free(st);
+     (st) = NULL;
     return col + width;
 }
 
@@ -125427,7 +123381,7 @@
     int     w;
     int     selected = idx == pum_selected;
 
-    for ( ; ; MB_PTR_ADV(p))
+    for ( ; ;  p += (*mb_ptr2len)(p) )
     {
         if (s == NULL)
         {
@@ -125462,7 +123416,8 @@
 
         if (attrs != NULL)
         {
-            VIM_CLEAR(attrs);
+             vim_free(attrs);
+             (attrs) = NULL;
         }
 
         if (*p != TAB)
@@ -125524,12 +123479,16 @@
 pum_free_bg(void)
 {
     int k;
-    VIM_CLEAR(pum_bg_attrs);
-    VIM_CLEAR(pum_bg_lines);
-    VIM_CLEAR(pum_bg_linesUC);
+     vim_free(pum_bg_attrs);
+     (pum_bg_attrs) = NULL;
+     vim_free(pum_bg_lines);
+     (pum_bg_lines) = NULL;
+     vim_free(pum_bg_linesUC);
+     (pum_bg_linesUC) = NULL;
     for (k = 0; k < MAX_MCO; ++k)
     {
-        VIM_CLEAR(pum_bg_linesC[k]);
+         vim_free(pum_bg_linesC[k]);
+         (pum_bg_linesC[k]) = NULL;
     }
     pum_bg_top = 0;
     pum_bg_bot = 0;
@@ -125622,14 +123581,14 @@
                 if (save_top < save_bot)
                 {
                     save_ncells = (save_bot - save_top) * screen_Columns;
-                    pum_bg_attrs = LALLOC_MULT(sattr_T, save_ncells);
-                    pum_bg_lines = LALLOC_MULT(schar_T, save_ncells);
+                    pum_bg_attrs =  (sattr_T *)lalloc(sizeof(sattr_T) * (save_ncells), FALSE) ;
+                    pum_bg_lines =  (schar_T *)lalloc(sizeof(schar_T) * (save_ncells), FALSE) ;
                     if (enc_utf8)
                     {
-                        pum_bg_linesUC = LALLOC_MULT(u8char_T, save_ncells);
+                        pum_bg_linesUC =  (u8char_T *)lalloc(sizeof(u8char_T) * (save_ncells), FALSE) ;
                         for (k = 0; k < MAX_MCO; ++k)
                         {
-                            pum_bg_linesC[k] = LALLOC_MULT(u8char_T, save_ncells);
+                            pum_bg_linesC[k] =  (u8char_T *)lalloc(sizeof(u8char_T) * (save_ncells), FALSE) ;
                         }
                     }
                     if (pum_bg_attrs != NULL && pum_bg_lines != NULL)
@@ -125639,16 +123598,16 @@
                             int soff = (r - save_top) * screen_Columns;
                             int loff = LineOffset[r];
 
-                            mch_memmove(pum_bg_attrs + soff, ScreenAttrs + loff, screen_Columns * sizeof(sattr_T));
-                            mch_memmove(pum_bg_lines + soff, ScreenLines + loff, screen_Columns * sizeof(schar_T));
+                             memmove((char *)(pum_bg_attrs + soff), (char *)(ScreenAttrs + loff), screen_Columns * sizeof(sattr_T)) ;
+                             memmove((char *)(pum_bg_lines + soff), (char *)(ScreenLines + loff), screen_Columns * sizeof(schar_T)) ;
                             if (enc_utf8 && pum_bg_linesUC != NULL && ScreenLinesUC != NULL)
                             {
-                                mch_memmove(pum_bg_linesUC + soff, ScreenLinesUC + loff, screen_Columns * sizeof(u8char_T));
+                                 memmove((char *)(pum_bg_linesUC + soff), (char *)(ScreenLinesUC + loff), screen_Columns * sizeof(u8char_T)) ;
                                 for (k = 0; k < MAX_MCO; ++k)
                                 {
                                     if (pum_bg_linesC[k] != NULL && ScreenLinesC[k] != NULL)
                                     {
-                                        mch_memmove(pum_bg_linesC[k] + soff, ScreenLinesC[k] + loff, screen_Columns * sizeof(u8char_T));
+                                         memmove((char *)(pum_bg_linesC[k] + soff), (char *)(ScreenLinesC[k] + loff), screen_Columns * sizeof(u8char_T)) ;
                                     }
                                 }
                             }
@@ -125771,7 +123730,7 @@
 }
 
     static int
-pum_set_selected(int n, int repeat UNUSED)
+pum_set_selected(int n, int repeat  __attribute__((unused)) )
 {
     int     resized = FALSE;
     int     context = pum_height / 2;
@@ -125864,7 +123823,7 @@
 {
     int     row = (State & MODE_CMDLINE)
                         ? cmdline_row
-                        : curwin->w_wrow + W_WINROW(curwin);
+                        : curwin->w_wrow +  (curwin->w_winrow) ;
 
     return pum_window != curwin
             || (pum_win_row == row && pum_win_height == curwin->w_height && pum_win_col == curwin->w_wincol && pum_win_width == curwin->w_width);
@@ -126085,16 +124044,12 @@
 
 // ==================== regexp.c ====================
 
-#define Magic(x)        ((int)(x) - 256)
-#define un_Magic(x)     ((x) + 256)
-#define is_Magic(x)     ((x) < 0)
-
     static int
 no_Magic(int x)
 {
-    if (is_Magic(x))
+    if ( ((x) < 0) )
     {
-        return un_Magic(x);
+        return  ((x) + 256) ;
     }
     return x;
 }
@@ -126102,45 +124057,33 @@
     static int
 toggle_Magic(int x)
 {
-    if (is_Magic(x))
+    if ( ((x) < 0) )
     {
-        return un_Magic(x);
+        return  ((x) + 256) ;
     }
-    return Magic(x);
+    return  ((int)(x) - 256) ;
 }
 
-#define REGMAGIC        0234
-
-#define UCHARAT(p)      ((int)*(char_u *)(p))
-
-#define EMSG_RET_NULL(m) return (emsg((m)), rc_did_emsg = TRUE, (void *)NULL)
-#define IEMSG_RET_NULL(m) return (iemsg((m)), rc_did_emsg = TRUE, (void *)NULL)
-#define EMSG_RET_FAIL(m) return (emsg((m)), rc_did_emsg = TRUE, FAIL)
-#define EMSG2_RET_NULL(m, c) return (semsg((const char *)(m), (c) ? "" : "\\"), rc_did_emsg = TRUE, (void *)NULL)
-#define EMSG3_RET_NULL(m, c, a) return (semsg((const char *)(m), (c) ? "" : "\\", (a)), rc_did_emsg = TRUE, (void *)NULL)
-#define EMSG2_RET_FAIL(m, c) return (semsg((const char *)(m), (c) ? "" : "\\"), rc_did_emsg = TRUE, FAIL)
-#define EMSG_ONE_RET_NULL EMSG2_RET_NULL(_(e_invalid_item_in_str_brackets), reg_magic == MAGIC_ALL)
-
-#define MAX_LIMIT       (32767L << 16L)
+enum { REGMAGIC = 0234 };
 
-#define NOT_MULTI       0
-#define MULTI_ONE       1
-#define MULTI_MULT      2
+enum { NOT_MULTI = 0 };
+enum { MULTI_ONE = 1 };
+enum { MULTI_MULT = 2 };
 
-#define RA_FAIL         1
-#define RA_CONT         2
-#define RA_BREAK        3
-#define RA_MATCH        4
-#define RA_NOMATCH      5
+enum { RA_FAIL = 1 };
+enum { RA_CONT = 2 };
+enum { RA_BREAK = 3 };
+enum { RA_MATCH = 4 };
+enum { RA_NOMATCH = 5 };
 
     static int
 re_multi_type(int c)
 {
-    if (c == Magic('@') || c == Magic('=') || c == Magic('?'))
+    if (c ==  ((int)('@') - 256)  || c ==  ((int)('=') - 256)  || c ==  ((int)('?') - 256) )
     {
         return MULTI_ONE;
     }
-    if (c == Magic('*') || c == Magic('+') || c == Magic('{'))
+    if (c ==  ((int)('*') - 256)  || c ==  ((int)('+') - 256)  || c ==  ((int)('{') - 256) )
     {
         return MULTI_MULT;
     }
@@ -126199,28 +124142,28 @@
 {
     static keyvalue_T char_class_tab[] =
     {
-        KEYVALUE_ENTRY(CLASS_ALNUM, "alnum:]"),
-        KEYVALUE_ENTRY(CLASS_ALPHA, "alpha:]"),
-        KEYVALUE_ENTRY(CLASS_BACKSPACE, "backspace:]"),
-        KEYVALUE_ENTRY(CLASS_BLANK, "blank:]"),
-        KEYVALUE_ENTRY(CLASS_CNTRL, "cntrl:]"),
-        KEYVALUE_ENTRY(CLASS_DIGIT, "digit:]"),
-        KEYVALUE_ENTRY(CLASS_ESCAPE, "escape:]"),
-        KEYVALUE_ENTRY(CLASS_FNAME, "fname:]"),
-        KEYVALUE_ENTRY(CLASS_GRAPH, "graph:]"),
-        KEYVALUE_ENTRY(CLASS_IDENT, "ident:]"),
-        KEYVALUE_ENTRY(CLASS_KEYWORD, "keyword:]"),
-        KEYVALUE_ENTRY(CLASS_LOWER, "lower:]"),
-        KEYVALUE_ENTRY(CLASS_PRINT, "print:]"),
-        KEYVALUE_ENTRY(CLASS_PUNCT, "punct:]"),
-        KEYVALUE_ENTRY(CLASS_RETURN, "return:]"),
-        KEYVALUE_ENTRY(CLASS_SPACE, "space:]"),
-        KEYVALUE_ENTRY(CLASS_TAB, "tab:]"),
-        KEYVALUE_ENTRY(CLASS_UPPER, "upper:]"),
-        KEYVALUE_ENTRY(CLASS_XDIGIT, "xdigit:]")
+         {(CLASS_ALNUM), {((char_u *)"alnum:]"),  (sizeof("alnum:]" "") - 1) }} ,
+         {(CLASS_ALPHA), {((char_u *)"alpha:]"),  (sizeof("alpha:]" "") - 1) }} ,
+         {(CLASS_BACKSPACE), {((char_u *)"backspace:]"),  (sizeof("backspace:]" "") - 1) }} ,
+         {(CLASS_BLANK), {((char_u *)"blank:]"),  (sizeof("blank:]" "") - 1) }} ,
+         {(CLASS_CNTRL), {((char_u *)"cntrl:]"),  (sizeof("cntrl:]" "") - 1) }} ,
+         {(CLASS_DIGIT), {((char_u *)"digit:]"),  (sizeof("digit:]" "") - 1) }} ,
+         {(CLASS_ESCAPE), {((char_u *)"escape:]"),  (sizeof("escape:]" "") - 1) }} ,
+         {(CLASS_FNAME), {((char_u *)"fname:]"),  (sizeof("fname:]" "") - 1) }} ,
+         {(CLASS_GRAPH), {((char_u *)"graph:]"),  (sizeof("graph:]" "") - 1) }} ,
+         {(CLASS_IDENT), {((char_u *)"ident:]"),  (sizeof("ident:]" "") - 1) }} ,
+         {(CLASS_KEYWORD), {((char_u *)"keyword:]"),  (sizeof("keyword:]" "") - 1) }} ,
+         {(CLASS_LOWER), {((char_u *)"lower:]"),  (sizeof("lower:]" "") - 1) }} ,
+         {(CLASS_PRINT), {((char_u *)"print:]"),  (sizeof("print:]" "") - 1) }} ,
+         {(CLASS_PUNCT), {((char_u *)"punct:]"),  (sizeof("punct:]" "") - 1) }} ,
+         {(CLASS_RETURN), {((char_u *)"return:]"),  (sizeof("return:]" "") - 1) }} ,
+         {(CLASS_SPACE), {((char_u *)"space:]"),  (sizeof("space:]" "") - 1) }} ,
+         {(CLASS_TAB), {((char_u *)"tab:]"),  (sizeof("tab:]" "") - 1) }} ,
+         {(CLASS_UPPER), {((char_u *)"upper:]"),  (sizeof("upper:]" "") - 1) }} ,
+         {(CLASS_XDIGIT), {((char_u *)"xdigit:]"),  (sizeof("xdigit:]" "") - 1) }} 
     };
 
-    if ((*pp)[1] == ':' && ASCII_ISLOWER((*pp)[2]) && ASCII_ISLOWER((*pp)[3]) && ASCII_ISLOWER((*pp)[4]))
+    if ((*pp)[1] == ':' &&  ((unsigned)((*pp)[2]) - 'a' < 26)  &&  ((unsigned)((*pp)[3]) - 'a' < 26)  &&  ((unsigned)((*pp)[4]) - 'a' < 26) )
     {
         keyvalue_T target;
         keyvalue_T *entry;
@@ -126236,7 +124179,7 @@
         }
         else
         {
-            entry = (keyvalue_T *)bsearch(&target, &char_class_tab, ARRAY_LENGTH(char_class_tab), sizeof(char_class_tab[0]), cmp_keyvalue_value_n);
+            entry = (keyvalue_T *)bsearch(&target, &char_class_tab,  (sizeof(char_class_tab) / sizeof((char_class_tab)[0])) , sizeof(char_class_tab[0]), cmp_keyvalue_value_n);
         }
         if (entry != NULL)
         {
@@ -126250,15 +124193,15 @@
 
 static short    class_tab[256];
 
-#define     RI_DIGIT    0x01
-#define     RI_HEX      0x02
-#define     RI_OCTAL    0x04
-#define     RI_WORD     0x08
-#define     RI_HEAD     0x10
-#define     RI_ALPHA    0x20
-#define     RI_LOWER    0x40
-#define     RI_UPPER    0x80
-#define     RI_WHITE    0x100
+enum { RI_DIGIT = 0x01 };
+enum { RI_HEX = 0x02 };
+enum { RI_OCTAL = 0x04 };
+enum { RI_WORD = 0x08 };
+enum { RI_HEAD = 0x10 };
+enum { RI_ALPHA = 0x20 };
+enum { RI_LOWER = 0x40 };
+enum { RI_UPPER = 0x80 };
+enum { RI_WHITE = 0x100 };
 
     static void
 init_class_tab(void)
@@ -126311,21 +124254,11 @@
     done = TRUE;
 }
 
-#define ri_digit(c)     ((c) < 0x100 && (class_tab[c] & RI_DIGIT))
-#define ri_hex(c)       ((c) < 0x100 && (class_tab[c] & RI_HEX))
-#define ri_octal(c)     ((c) < 0x100 && (class_tab[c] & RI_OCTAL))
-#define ri_word(c)      ((c) < 0x100 && (class_tab[c] & RI_WORD))
-#define ri_head(c)      ((c) < 0x100 && (class_tab[c] & RI_HEAD))
-#define ri_alpha(c)     ((c) < 0x100 && (class_tab[c] & RI_ALPHA))
-#define ri_lower(c)     ((c) < 0x100 && (class_tab[c] & RI_LOWER))
-#define ri_upper(c)     ((c) < 0x100 && (class_tab[c] & RI_UPPER))
-#define ri_white(c)     ((c) < 0x100 && (class_tab[c] & RI_WHITE))
-
-#define RF_ICASE    1
-#define RF_NOICASE  2
-#define RF_HASNL    4
-#define RF_ICOMBINE 8
-#define RF_LOOKBH   16
+enum { RF_ICASE = 1 };
+enum { RF_NOICASE = 2 };
+enum { RF_HASNL = 4 };
+enum { RF_ICOMBINE = 8 };
+enum { RF_LOOKBH = 16 };
 
 static char_u   *regparse;
 static int      regnpar;
@@ -126353,12 +124286,11 @@
 static int      prevprevchr;
 static int      nextchr;
 
-#define REG_NOPAREN     0
-#define REG_PAREN       1
-#define REG_ZPAREN      2
-#define REG_NPAREN      3
+enum { REG_NOPAREN = 0 };
+enum { REG_PAREN = 1 };
+enum { REG_NPAREN = 3 };
 
-#define REG_MAX_PAREN_DEPTH     1000
+enum { REG_MAX_PAREN_DEPTH = 1000 };
 
 typedef struct
 {
@@ -126494,7 +124426,7 @@
                 ++p;
                 if (*p != ']' && *p != NUL)
                 {
-                    MB_PTR_ADV(p);
+                     p += (*mb_ptr2len)(p) ;
                 }
             }
         else if (*p == '\\' && !reg_cpo_bsl && (vim_strchr(REGEXP_INRANGE, p[1]) != NULL || (!reg_cpo_lit && vim_strchr(REGEXP_ABBR, p[1]) != NULL)))
@@ -126554,7 +124486,7 @@
     }
     get_cpo_flags();
 
-    for (; p[0] != NUL; MB_PTR_ADV(p))
+    for (; p[0] != NUL;  p += (*mb_ptr2len)(p) )
     {
         if (p[0] == dirc)
         {
@@ -126574,7 +124506,7 @@
             {
                 if (startplen == 0)
                 {
-                    startplen = STRLEN(startp);
+                    startplen =  strlen((char *)(startp)) ;
                 }
                 if (*newp == NULL)
                 {
@@ -126591,7 +124523,7 @@
                 }
                 if (*newp != NULL)
                 {
-                    mch_memmove(p, p + 1, startplen - ((p + 1) - startp) + 1);
+                     memmove((char *)(p), (char *)(p + 1), startplen - ((p + 1) - startp) + 1) ;
                 }
                 else
                 {
@@ -126678,7 +124610,7 @@
         case '~':
             if (reg_magic >= MAGIC_ON)
             {
-                curchr = Magic(curchr);
+                curchr =  ((int)(curchr) - 256) ;
             }
             break;
         case '(':
@@ -126705,19 +124637,19 @@
         case '/':
             if (reg_magic == MAGIC_ALL)
             {
-                curchr = Magic(curchr);
+                curchr =  ((int)(curchr) - 256) ;
             }
             break;
         case '*':
-            if (reg_magic >= MAGIC_ON && !at_start && !(prev_at_start && prevchr == Magic('^')) && (after_slash || (prevchr != Magic('(') && prevchr != Magic('&') && prevchr != Magic('|'))))
+            if (reg_magic >= MAGIC_ON && !at_start && !(prev_at_start && prevchr ==  ((int)('^') - 256) ) && (after_slash || (prevchr !=  ((int)('(') - 256)  && prevchr !=  ((int)('&') - 256)  && prevchr !=  ((int)('|') - 256) )))
             {
-                curchr = Magic('*');
+                curchr =  ((int)('*') - 256) ;
             }
             break;
         case '^':
-            if (reg_magic >= MAGIC_OFF && (at_start || reg_magic == MAGIC_ALL || prevchr == Magic('(') || prevchr == Magic('|') || prevchr == Magic('&') || prevchr == Magic('n') || (no_Magic(prevchr) == '(' && prevprevchr == Magic('%'))))
+            if (reg_magic >= MAGIC_OFF && (at_start || reg_magic == MAGIC_ALL || prevchr ==  ((int)('(') - 256)  || prevchr ==  ((int)('|') - 256)  || prevchr ==  ((int)('&') - 256)  || prevchr ==  ((int)('n') - 256)  || (no_Magic(prevchr) == '(' && prevprevchr ==  ((int)('%') - 256) )))
             {
-                curchr = Magic('^');
+                curchr =  ((int)('^') - 256) ;
                 at_start = TRUE;
                 prev_at_start = FALSE;
             }
@@ -126742,7 +124674,7 @@
                 }
                 if (p[0] == NUL || (p[0] == '\\' && (p[1] == '|' || p[1] == '&' || p[1] == ')' || p[1] == 'n')) || (is_magic_all && (p[0] == '|' || p[0] == '&' || p[0] == ')')) || reg_magic == MAGIC_ALL)
                 {
-                    curchr = Magic('$');
+                    curchr =  ((int)('$') - 256) ;
                 }
             }
             break;
@@ -126968,16 +124900,16 @@
         }
         else
         {
-            *maxval = MAX_LIMIT;
+            *maxval =  (32767L << 16L) ;
         }
     }
-    else if (VIM_ISDIGIT(*first_char))
+    else if ( ((unsigned)(*first_char) - '0' < 10) )
     {
         *maxval = *minval;
     }
     else
     {
-        *maxval = MAX_LIMIT;
+        *maxval =  (32767L << 16L) ;
     }
     if (*regparse == '\\')
     {
@@ -126985,7 +124917,7 @@
     }
     if (*regparse != '}')
     {
-        EMSG2_RET_FAIL(_(e_syntax_error_in_str_curlies), reg_magic == MAGIC_ALL);
+         return (semsg((const char *)(_(e_syntax_error_in_str_curlies)), (reg_magic == MAGIC_ALL) ? "" : "\\"), rc_did_emsg = TRUE, FAIL) ;
     }
 
     if ((!reverse && *minval > *maxval) || (reverse && *minval < *maxval))
@@ -127125,8 +125057,6 @@
     return length;
 }
 
-#define REG_MULTI       (rex.reg_match == NULL)
-
     static int
 reg_prev_class(void)
 {
@@ -127153,14 +125083,14 @@
     colnr_T     cols;
     colnr_T     curswant;
 
-    if (rex.reg_buf != curbuf || VIsual.lnum == 0 || !REG_MULTI)
+    if (rex.reg_buf != curbuf || VIsual.lnum == 0 || ! (rex.reg_match == NULL) )
     {
         return FALSE;
     }
 
     if (VIsual_active)
     {
-        if (LT_POS(VIsual, wp->w_cursor))
+        if ( (((VIsual).lnum != (wp->w_cursor).lnum)               ? (VIsual).lnum < (wp->w_cursor).lnum                   : (VIsual).col != (wp->w_cursor).col                        ? (VIsual).col < (wp->w_cursor).col                     : (VIsual).coladd < (wp->w_cursor).coladd) )
         {
             top = VIsual;
             bot = wp->w_cursor;
@@ -127175,7 +125105,7 @@
     }
     else
     {
-        if (LT_POS(curbuf->b_visual.vi_start, curbuf->b_visual.vi_end))
+        if ( (((curbuf->b_visual.vi_start).lnum != (curbuf->b_visual.vi_end).lnum)               ? (curbuf->b_visual.vi_start).lnum < (curbuf->b_visual.vi_end).lnum                   : (curbuf->b_visual.vi_start).col != (curbuf->b_visual.vi_end).col                        ? (curbuf->b_visual.vi_start).col < (curbuf->b_visual.vi_end).col                     : (curbuf->b_visual.vi_start).coladd < (curbuf->b_visual.vi_end).coladd) )
         {
             top = curbuf->b_visual.vi_start;
             bot = curbuf->b_visual.vi_end;
@@ -127240,13 +125170,13 @@
 {
     regprog_T   *prog;
 
-    prog = REG_MULTI ? rex.reg_mmatch->regprog : rex.reg_match->regprog;
+    prog =  (rex.reg_match == NULL)  ? rex.reg_mmatch->regprog : rex.reg_match->regprog;
     if (prog->engine == &nfa_regengine)
     {
         return FALSE;
     }
 
-    if (UCHARAT(((bt_regprog_T *)prog)->program) != REGMAGIC)
+    if ( ((int)*(char_u *)(((bt_regprog_T *)prog)->program))  != REGMAGIC)
     {
         iemsg(e_corrupted_regexp_program);
         return TRUE;
@@ -127262,15 +125192,15 @@
         return;
     }
 
-    if (REG_MULTI)
+    if ( (rex.reg_match == NULL) )
     {
-        vim_memset(rex.reg_startpos, 0xff, sizeof(lpos_T) * NSUBEXP);
-        vim_memset(rex.reg_endpos, 0xff, sizeof(lpos_T) * NSUBEXP);
+         memset((rex.reg_startpos), (0xff), (sizeof(lpos_T) * NSUBEXP)) ;
+         memset((rex.reg_endpos), (0xff), (sizeof(lpos_T) * NSUBEXP)) ;
     }
     else
     {
-        vim_memset(rex.reg_startp, 0, sizeof(char_u *) * NSUBEXP);
-        vim_memset(rex.reg_endp, 0, sizeof(char_u *) * NSUBEXP);
+         memset((rex.reg_startp), (0), (sizeof(char_u *) * NSUBEXP)) ;
+         memset((rex.reg_endp), (0), (sizeof(char_u *) * NSUBEXP)) ;
     }
     rex.need_clear_subexpr = FALSE;
 }
@@ -127299,7 +125229,7 @@
     {
         if (rex.line != reg_tofree)
         {
-            len = (int)STRLEN(rex.line);
+            len = (int) strlen((char *)(rex.line)) ;
             if (reg_tofree == NULL || len >= (int)reg_tofreelen)
             {
                 len += 50;
@@ -127311,7 +125241,7 @@
                 }
                 reg_tofreelen = len;
             }
-            STRCPY(reg_tofree, rex.line);
+             strcpy((char *)(reg_tofree), (char *)(rex.line)) ;
             rex.input = reg_tofree + (rex.input - rex.line);
             rex.line = reg_tofree;
         }
@@ -127326,7 +125256,7 @@
             len = (int)reg_getline_len(clnum) - ccol;
         }
 
-        if ((!rex.reg_ic && cstrncmp(p + ccol, rex.input, &len) != 0) || (rex.reg_ic && MB_STRNICMP(p + ccol, rex.input, len) != 0))
+        if ((!rex.reg_ic && cstrncmp(p + ccol, rex.input, &len) != 0) || (rex.reg_ic &&  mb_strnicmp((char_u *)(p + ccol), (char_u *)(rex.input), (int)(len))  != 0))
         {
             return RA_NOMATCH;
         }
@@ -127457,7 +125387,7 @@
 
     if (!rex.reg_ic)
     {
-        result = STRNCMP(s1, s2, *n);
+        result =  strncmp((char *)(s1), (char *)(s2), (*n)) ;
     }
     else if (enc_utf8)
     {
@@ -127467,18 +125397,18 @@
         while (n1 > 0 && *p != NUL)
         {
             n1 -= mb_ptr2len(s1);
-            MB_PTR_ADV(p);
+             p += (*mb_ptr2len)(p) ;
             n2++;
         }
         p = s2;
         while (n2-- > 0 && *p != NUL)
         {
-            MB_PTR_ADV(p);
+             p += (*mb_ptr2len)(p) ;
         }
 
         n2 = p - s2;
 
-        result = MB_STRNICMP2(s1, s2, *n, n2);
+        result =  mb_strnicmp2((char_u *)(s1), (char_u *)(s2), (*n), (n2)) ;
         if (result == 0 && n2 < *n)
         {
             *n = n2;
@@ -127486,7 +125416,7 @@
     }
     else
     {
-        result = MB_STRNICMP(s1, s2, *n);
+        result =  mb_strnicmp((char_u *)(s1), (char_u *)(s2), (int)(*n)) ;
     }
 
     if (result != 0 && enc_utf8 && rex.reg_icombine)
@@ -127548,14 +125478,14 @@
     }
     else
     {
-        if (MB_ISUPPER(c))
+        if ( vim_isupper(c) )
         {
-            cc = MB_TOLOWER(c);
+            cc =  vim_tolower(c) ;
             lc = cc;
         }
-        else if (MB_ISLOWER(c))
+        else if ( vim_islower(c) )
         {
-            cc = MB_TOUPPER(c);
+            cc =  vim_toupper(c) ;
             lc = c;
         }
         else
@@ -127603,13 +125533,13 @@
     static void
 do_upper(int *d, int c)
 {
-    *d = MB_TOUPPER(c);
+    *d =  vim_toupper(c) ;
 }
 
     static void
 do_lower(int *d, int c)
 {
-    *d = MB_TOLOWER(c);
+    *d =  vim_tolower(c) ;
 }
 
     char_u *
@@ -127632,7 +125562,7 @@
 
     for (p = newsub; *p; ++p)
     {
-        if (STRNCMP(p, tilde, tildelen) == 0)
+        if ( strncmp((char *)(p), (char *)(tilde), (tildelen))  == 0)
         {
             size_t prefixlen = p - newsub;
             char_u *postfix = p + tildelen;
@@ -127641,7 +125571,7 @@
 
             if (newsublen == 0)
             {
-                newsublen = STRLEN(newsub);
+                newsublen =  strlen((char *)(newsub)) ;
             }
             newsublen -= tildelen;
             postfixlen = newsublen - prefixlen;
@@ -127666,9 +125596,9 @@
                     break;
                 }
 
-                mch_memmove(tmpsub, newsub, prefixlen);
-                mch_memmove(tmpsub + prefixlen, reg_prev_sub, reg_prev_sublen);
-                STRCPY(tmpsub + prefixlen + reg_prev_sublen, postfix);
+                 memmove((char *)(tmpsub), (char *)(newsub), prefixlen) ;
+                 memmove((char *)(tmpsub + prefixlen), (char *)(reg_prev_sub), reg_prev_sublen) ;
+                 strcpy((char *)(tmpsub + prefixlen + reg_prev_sublen), (char *)(postfix)) ;
 
                 if (newsub != source)
                 {
@@ -127680,7 +125610,7 @@
             }
             else
             {
-                mch_memmove(p, postfix, postfixlen + 1);
+                 memmove((char *)(p), (char *)(postfix), postfixlen + 1) ;
             }
 
             --p;
@@ -127710,7 +125640,8 @@
     newsublen = p - newsub;
     if (newsublen == 0)
     {
-        VIM_CLEAR(reg_prev_sub);
+         vim_free(reg_prev_sub);
+         (reg_prev_sub) = NULL;
     }
     else
     {
@@ -127863,7 +125794,7 @@
         }
         if (no < 0)
         {
-            if (c == K_SPECIAL && src[0] != NUL && src[1] != NUL)
+            if (c ==  (0x80)  && src[0] != NUL && src[1] != NUL)
             {
                 if (copy)
                 {
@@ -127969,7 +125900,7 @@
                                 iemsg("vim_regsub_both(): not enough space");
                                 return 0;
                             }
-                            mch_memmove(dst + 1, src - 1 + clen, (size_t)(totlen - clen));
+                             memmove((char *)(dst + 1), (char *)(src - 1 + clen), (size_t)(totlen - clen)) ;
                         }
                         dst += totlen - clen;
                     }
@@ -127989,7 +125920,7 @@
         }
         else
         {
-            if (REG_MULTI)
+            if ( (rex.reg_match == NULL) )
             {
                 clnum = rex.reg_mmatch->startpos[no].lnum;
                 if (clnum < 0 || rex.reg_mmatch->endpos[no].lnum < 0)
@@ -128028,7 +125959,7 @@
                 {
                     if (len == 0)
                     {
-                        if (REG_MULTI)
+                        if ( (rex.reg_match == NULL) )
                         {
                             if (rex.reg_mmatch->endpos[no].lnum == clnum)
                             {
@@ -128183,107 +126114,104 @@
 
 // ==================== regexp_bt.c ====================
 
-#define END             0
-#define BOL             1
-#define EOL             2
-#define BRANCH          3
-#define BACK            4
-#define EXACTLY         5
-#define NOTHING         6
-#define STAR            7
-#define PLUS            8
-#define MATCH           9
-#define NOMATCH         10
-#define BEHIND          11
-#define NOBEHIND        12
-#define SUBPAT          13
-#define BRACE_SIMPLE    14
-#define BOW             15
-#define EOW             16
-#define BRACE_LIMITS    17
-#define NEWL            18
-#define BHPOS           19
-
-#define ADD_NL          30
-#define FIRST_NL        ANY + ADD_NL
-#define ANY             20
-#define ANYOF           21
-#define ANYBUT          22
-#define IDENT           23
-#define SIDENT          24
-#define KWORD           25
-#define SKWORD          26
-#define FNAME           27
-#define SFNAME          28
-#define PRINT           29
-#define SPRINT          30
-#define RE_WHITE           31
-#define NWHITE          32
-#define DIGIT           33
-#define NDIGIT          34
-#define HEX             35
-#define NHEX            36
-#define OCTAL           37
-#define NOCTAL          38
-#define WORD            39
-#define NWORD           40
-#define HEAD            41
-#define NHEAD           42
-#define ALPHA           43
-#define NALPHA          44
-#define LOWER           45
-#define NLOWER          46
-#define UPPER           47
-#define NUPPER          48
-#define LAST_NL         NUPPER + ADD_NL
-#define WITH_NL(op)     ((op) >= FIRST_NL && (op) <= LAST_NL)
-
-#define MOPEN           80
-#define MCLOSE          90
-#define BACKREF         100
-
-#define BRACE_COMPLEX   140
-
-#define NOPEN           150
-#define NCLOSE          151
-
-#define DELIM_NL        4
-#define F_PCLOSE        180
-#define F_QCLOSE        181
-#define F_RCLOSE        182
-#define F_ACLOSE        183
-#define F_PCLOSE_NL     184
-#define F_QCLOSE_NL     185
-#define F_RCLOSE_NL     186
-#define F_ACLOSE_NL     187
-#define T_PCLOSE        190
-#define T_QCLOSE        191
-#define T_RCLOSE        192
-#define T_ACLOSE        193
-#define T_PCLOSE_NL     194
-#define T_QCLOSE_NL     195
-#define T_RCLOSE_NL     196
-#define T_ACLOSE_NL     197
-
-#define MULTIBYTECODE   200
-#define RE_BOF          201
-#define RE_EOF          202
-#define CURSOR          203
-
-#define RE_LNUM         204
-#define RE_COL          205
-#define RE_VCOL         206
-
-#define RE_MARK         207
-#define RE_VISUAL       208
-#define RE_COMPOSING    209
-
-#define HASWIDTH        0x1
-#define SIMPLE          0x2
-#define SPSTART         0x4
-#define HASNL           0x8
-#define HASLOOKBH       0x10
-#define WORST           0
+enum { END = 0 };
+enum { BOL = 1 };
+enum { EOL = 2 };
+enum { BRANCH = 3 };
+enum { BACK = 4 };
+enum { EXACTLY = 5 };
+enum { NOTHING = 6 };
+enum { STAR = 7 };
+enum { PLUS = 8 };
+enum { MATCH = 9 };
+enum { NOMATCH = 10 };
+enum { BEHIND = 11 };
+enum { NOBEHIND = 12 };
+enum { SUBPAT = 13 };
+enum { BRACE_SIMPLE = 14 };
+enum { BOW = 15 };
+enum { EOW = 16 };
+enum { BRACE_LIMITS = 17 };
+enum { NEWL = 18 };
+enum { BHPOS = 19 };
+
+enum { ADD_NL = 30 };
+enum { ANY = 20 };
+enum { ANYOF = 21 };
+enum { ANYBUT = 22 };
+enum { IDENT = 23 };
+enum { SIDENT = 24 };
+enum { KWORD = 25 };
+enum { SKWORD = 26 };
+enum { FNAME = 27 };
+enum { SFNAME = 28 };
+enum { PRINT = 29 };
+enum { SPRINT = 30 };
+enum { RE_WHITE = 31 };
+enum { NWHITE = 32 };
+enum { DIGIT = 33 };
+enum { NDIGIT = 34 };
+enum { HEX = 35 };
+enum { NHEX = 36 };
+enum { OCTAL = 37 };
+enum { NOCTAL = 38 };
+enum { WORD = 39 };
+enum { NWORD = 40 };
+enum { HEAD = 41 };
+enum { NHEAD = 42 };
+enum { ALPHA = 43 };
+enum { NALPHA = 44 };
+enum { LOWER = 45 };
+enum { NLOWER = 46 };
+enum { UPPER = 47 };
+enum { NUPPER = 48 };
+
+enum { MOPEN = 80 };
+enum { MCLOSE = 90 };
+enum { BACKREF = 100 };
+
+enum { BRACE_COMPLEX = 140 };
+
+enum { NOPEN = 150 };
+enum { NCLOSE = 151 };
+
+enum { DELIM_NL = 4 };
+enum { F_PCLOSE = 180 };
+enum { F_QCLOSE = 181 };
+enum { F_RCLOSE = 182 };
+enum { F_ACLOSE = 183 };
+enum { F_PCLOSE_NL = 184 };
+enum { F_QCLOSE_NL = 185 };
+enum { F_RCLOSE_NL = 186 };
+enum { F_ACLOSE_NL = 187 };
+enum { T_PCLOSE = 190 };
+enum { T_QCLOSE = 191 };
+enum { T_RCLOSE = 192 };
+enum { T_ACLOSE = 193 };
+enum { T_PCLOSE_NL = 194 };
+enum { T_QCLOSE_NL = 195 };
+enum { T_RCLOSE_NL = 196 };
+enum { T_ACLOSE_NL = 197 };
+
+enum { MULTIBYTECODE = 200 };
+enum { RE_BOF = 201 };
+enum { RE_EOF = 202 };
+enum { CURSOR = 203 };
+
+enum { RE_LNUM = 204 };
+enum { RE_COL = 205 };
+enum { RE_VCOL = 206 };
+
+enum { RE_MARK = 207 };
+enum { RE_VISUAL = 208 };
+enum { RE_COMPOSING = 209 };
+
+enum { HASWIDTH = 0x1 };
+enum { SIMPLE = 0x2 };
+enum { SPSTART = 0x4 };
+enum { HASNL = 0x8 };
+enum { HASLOOKBH = 0x10 };
+enum { WORST = 0 };
 
 static int      num_complex_braces;
 static char_u   *regcode;
@@ -128307,8 +126235,6 @@
     UPPER, NUPPER
 };
 
-#define JUST_CALC_SIZE  ((char_u *) -1)
-
 typedef enum regstate_E
 {
     RS_NOPEN = 0
@@ -128385,15 +126311,8 @@
 
 static regsave_T behind_pos;
 
-#define REGSTACK_INITIAL        2048
-#define BACKPOS_INITIAL         64
-
-#define OP(p)           ((int)*(p))
-#define NEXT(p)         (((*((p) + 1) & 0377) << 8) + (*((p) + 2) & 0377))
-#define OPERAND(p)      ((p) + 3)
-#define OPERAND_MIN(p)  (((long)(p)[3] << 24) + ((long)(p)[4] << 16)                    + ((long)(p)[5] << 8) + (long)(p)[6])
-#define OPERAND_MAX(p)  OPERAND_MIN((p) + 4)
-#define OPERAND_CMP(p)  (p)[7]
+enum { REGSTACK_INITIAL = 2048 };
+enum { BACKPOS_INITIAL = 64 };
 
 static char_u *reg(int paren, int *flagp);
 
@@ -128417,7 +126336,7 @@
 
     num_complex_braces = 0;
     regnpar = 1;
-    CLEAR_FIELD(had_endbrace);
+      memset((&(had_endbrace)), (0), (sizeof(had_endbrace)))  ;
     regsize = 0L;
     reg_toolong = FALSE;
     bt_reg_parse_depth = 0;
@@ -128434,7 +126353,7 @@
     static void
 regc(int b)
 {
-    if (regcode == JUST_CALC_SIZE)
+    if (regcode ==  ((char_u *) -1) )
     {
         regsize++;
     }
@@ -128451,7 +126370,7 @@
     {
         return;
     }
-    if (regcode == JUST_CALC_SIZE)
+    if (regcode ==  ((char_u *) -1) )
     {
         regsize += (*mb_char2len)(c);
     }
@@ -128464,7 +126383,7 @@
     static void
 reg_equi_class(int c)
 {
-    if (enc_utf8 || STRCMP(p_enc, "latin1") == 0 || STRCMP(p_enc, "iso-8859-15") == 0)
+    if (enc_utf8 ||  strcmp((char *)(p_enc), (char *)("latin1"))  == 0 ||  strcmp((char *)(p_enc), (char *)("iso-8859-15"))  == 0)
     {
         switch (c)
         {
@@ -129865,7 +127784,7 @@
     char_u  *ret;
 
     ret = regcode;
-    if (ret == JUST_CALC_SIZE)
+    if (ret ==  ((char_u *) -1) )
     {
         regsize += 3;
     }
@@ -129893,18 +127812,18 @@
 {
     int     offset;
 
-    if (p == JUST_CALC_SIZE || reg_toolong)
+    if (p ==  ((char_u *) -1)  || reg_toolong)
     {
         return NULL;
     }
 
-    offset = NEXT(p);
+    offset =  (((*((p) + 1) & 0377) << 8) + (*((p) + 2) & 0377)) ;
     if (offset == 0)
     {
         return NULL;
     }
 
-    if (OP(p) == BACK)
+    if ( ((int)*(p))  == BACK)
     {
         return p - offset;
     }
@@ -129921,7 +127840,7 @@
     char_u      *temp;
     int         offset;
 
-    if (p == JUST_CALC_SIZE)
+    if (p ==  ((char_u *) -1) )
     {
         return;
     }
@@ -129937,7 +127856,7 @@
         scan = temp;
     }
 
-    if (OP(scan) == BACK)
+    if ( ((int)*(scan))  == BACK)
     {
         offset = (int)(scan - val);
     }
@@ -129959,11 +127878,11 @@
     static void
 regoptail(char_u *p, char_u *val)
 {
-    if (p == NULL || p == JUST_CALC_SIZE || (OP(p) != BRANCH && (OP(p) < BRACE_COMPLEX || OP(p) > BRACE_COMPLEX + 9)))
+    if (p == NULL || p ==  ((char_u *) -1)  || ( ((int)*(p))  != BRANCH && ( ((int)*(p))  < BRACE_COMPLEX ||  ((int)*(p))  > BRACE_COMPLEX + 9)))
     {
         return;
     }
-    regtail(OPERAND(p), val);
+    regtail( ((p) + 3) , val);
 }
 
     static void
@@ -129973,7 +127892,7 @@
     char_u      *dst;
     char_u      *place;
 
-    if (regcode == JUST_CALC_SIZE)
+    if (regcode ==  ((char_u *) -1) )
     {
         regsize += 3;
         return;
@@ -129999,7 +127918,7 @@
     char_u      *dst;
     char_u      *place;
 
-    if (regcode == JUST_CALC_SIZE)
+    if (regcode ==  ((char_u *) -1) )
     {
         regsize += 7;
         return;
@@ -130026,7 +127945,7 @@
     char_u      *dst;
     char_u      *place;
 
-    if (regcode == JUST_CALC_SIZE)
+    if (regcode ==  ((char_u *) -1) )
     {
         regsize += 11;
         return;
@@ -130088,23 +128007,23 @@
     c = getchr();
     switch (c)
     {
-      case Magic('^'):
+      case  ((int)('^') - 256) :
         ret = regnode(BOL);
         break;
 
-      case Magic('$'):
+      case  ((int)('$') - 256) :
         ret = regnode(EOL);
         break;
 
-      case Magic('<'):
+      case  ((int)('<') - 256) :
         ret = regnode(BOW);
         break;
 
-      case Magic('>'):
+      case  ((int)('>') - 256) :
         ret = regnode(EOW);
         break;
 
-      case Magic('_'):
+      case  ((int)('_') - 256) :
         c = no_Magic(getchr());
         if (c == '%')
         {
@@ -130114,7 +128033,7 @@
             {
                 goto delimiter_atom;
             }
-            EMSG2_RET_NULL(_(e_invalid_character_after_str), reg_magic == MAGIC_ALL);
+             return (semsg((const char *)(_(e_invalid_character_after_str)), (reg_magic == MAGIC_ALL) ? "" : "\\"), rc_did_emsg = TRUE, (void *)NULL) ;
         }
         if (c == '^')
         {
@@ -130135,40 +128054,41 @@
             goto collection;
         }
 
-      case Magic('.'):
-      case Magic('i'):
-      case Magic('I'):
-      case Magic('k'):
-      case Magic('K'):
-      case Magic('f'):
-      case Magic('F'):
-      case Magic('p'):
-      case Magic('P'):
-      case Magic('s'):
-      case Magic('S'):
-      case Magic('d'):
-      case Magic('D'):
-      case Magic('x'):
-      case Magic('X'):
-      case Magic('o'):
-      case Magic('O'):
-      case Magic('w'):
-      case Magic('W'):
-      case Magic('h'):
-      case Magic('H'):
-      case Magic('a'):
-      case Magic('A'):
-      case Magic('l'):
-      case Magic('L'):
-      case Magic('u'):
-      case Magic('U'):
+      __attribute__((fallthrough));
+      case  ((int)('.') - 256) :
+      case  ((int)('i') - 256) :
+      case  ((int)('I') - 256) :
+      case  ((int)('k') - 256) :
+      case  ((int)('K') - 256) :
+      case  ((int)('f') - 256) :
+      case  ((int)('F') - 256) :
+      case  ((int)('p') - 256) :
+      case  ((int)('P') - 256) :
+      case  ((int)('s') - 256) :
+      case  ((int)('S') - 256) :
+      case  ((int)('d') - 256) :
+      case  ((int)('D') - 256) :
+      case  ((int)('x') - 256) :
+      case  ((int)('X') - 256) :
+      case  ((int)('o') - 256) :
+      case  ((int)('O') - 256) :
+      case  ((int)('w') - 256) :
+      case  ((int)('W') - 256) :
+      case  ((int)('h') - 256) :
+      case  ((int)('H') - 256) :
+      case  ((int)('a') - 256) :
+      case  ((int)('A') - 256) :
+      case  ((int)('l') - 256) :
+      case  ((int)('L') - 256) :
+      case  ((int)('u') - 256) :
+      case  ((int)('U') - 256) :
         p = vim_strchr(classchars, no_Magic(c));
         if (p == NULL)
         {
-            EMSG_RET_NULL(_(e_invalid_use_of_underscore));
+             return (emsg((_(e_invalid_use_of_underscore))), rc_did_emsg = TRUE, (void *)NULL) ;
         }
 
-        if (enc_utf8 && c == Magic('.') && utf_iscomposing(peekchr()))
+        if (enc_utf8 && c ==  ((int)('.') - 256)  && utf_iscomposing(peekchr()))
         {
             c = getchr();
             goto do_multibyte;
@@ -130177,7 +128097,7 @@
         *flagp |= HASWIDTH | SIMPLE;
         break;
 
-      case Magic('n'):
+      case  ((int)('n') - 256) :
         if (reg_string)
         {
             ret = regnode(EXACTLY);
@@ -130192,10 +128112,10 @@
         }
         break;
 
-      case Magic('('):
+      case  ((int)('(') - 256) :
         if (one_exactly)
         {
-            EMSG_ONE_RET_NULL;
+              return (semsg((const char *)(_(e_invalid_item_in_str_brackets)), (reg_magic == MAGIC_ALL) ? "" : "\\"), rc_did_emsg = TRUE, (void *)NULL)  ;
         }
         ret = reg(REG_PAREN, &flags);
         if (ret == NULL)
@@ -130206,25 +128126,25 @@
         break;
 
       case NUL:
-      case Magic('|'):
-      case Magic('&'):
-      case Magic(')'):
+      case  ((int)('|') - 256) :
+      case  ((int)('&') - 256) :
+      case  ((int)(')') - 256) :
         if (one_exactly)
         {
-            EMSG_ONE_RET_NULL;
+              return (semsg((const char *)(_(e_invalid_item_in_str_brackets)), (reg_magic == MAGIC_ALL) ? "" : "\\"), rc_did_emsg = TRUE, (void *)NULL)  ;
         }
-        IEMSG_RET_NULL(e_internal_error_in_regexp);
+         return (iemsg((e_internal_error_in_regexp)), rc_did_emsg = TRUE, (void *)NULL) ;
 
-      case Magic('='):
-      case Magic('?'):
-      case Magic('+'):
-      case Magic('@'):
-      case Magic('{'):
-      case Magic('*'):
+      case  ((int)('=') - 256) :
+      case  ((int)('?') - 256) :
+      case  ((int)('+') - 256) :
+      case  ((int)('@') - 256) :
+      case  ((int)('{') - 256) :
+      case  ((int)('*') - 256) :
         c = no_Magic(c);
-        EMSG3_RET_NULL(_(e_str_chr_follows_nothing), (c == '*' ? reg_magic >= MAGIC_ON : reg_magic == MAGIC_ALL), c);
+         return (semsg((const char *)(_(e_str_chr_follows_nothing)), ((c == '*' ? reg_magic >= MAGIC_ON : reg_magic == MAGIC_ALL)) ? "" : "\\", (c)), rc_did_emsg = TRUE, (void *)NULL) ;
 
-      case Magic('~'):
+      case  ((int)('~') - 256) :
             if (reg_prev_sub != NULL)
             {
                 char_u      *lp;
@@ -130247,23 +128167,23 @@
             }
             else
             {
-                EMSG_RET_NULL(_(e_no_previous_substitute_regular_expression));
+                 return (emsg((_(e_no_previous_substitute_regular_expression))), rc_did_emsg = TRUE, (void *)NULL) ;
             }
             break;
 
-      case Magic('1'):
-      case Magic('2'):
-      case Magic('3'):
-      case Magic('4'):
-      case Magic('5'):
-      case Magic('6'):
-      case Magic('7'):
-      case Magic('8'):
-      case Magic('9'):
+      case  ((int)('1') - 256) :
+      case  ((int)('2') - 256) :
+      case  ((int)('3') - 256) :
+      case  ((int)('4') - 256) :
+      case  ((int)('5') - 256) :
+      case  ((int)('6') - 256) :
+      case  ((int)('7') - 256) :
+      case  ((int)('8') - 256) :
+      case  ((int)('9') - 256) :
             {
                 int                 refnum;
 
-                refnum = c - Magic('0');
+                refnum = c -  ((int)('0') - 256) ;
                 if (!seen_endbrace(refnum))
                 {
                     return NULL;
@@ -130272,12 +128192,11 @@
             }
             break;
 
-      case Magic('z'):
+      case  ((int)('z') - 256) :
         {
             c = no_Magic(getchr());
             switch (c)
             {
-
                 case 's':
                     ret = regnode(MOPEN + 0);
                           if (re_mult_next("\\zs") == FAIL)
@@ -130295,12 +128214,12 @@
                           break;
 
                 default:
-                    EMSG_RET_NULL(_(e_invalid_character_after_bsl_z));
+                     return (emsg((_(e_invalid_character_after_bsl_z))), rc_did_emsg = TRUE, (void *)NULL) ;
             }
         }
         break;
 
-      case Magic('%'):
+      case  ((int)('%') - 256) :
         {
             c = no_Magic(getchr());
             switch (c)
@@ -130308,7 +128227,7 @@
                 case '(':
                     if (one_exactly)
                     {
-                        EMSG_ONE_RET_NULL;
+                          return (semsg((const char *)(_(e_invalid_item_in_str_brackets)), (reg_magic == MAGIC_ALL) ? "" : "\\"), rc_did_emsg = TRUE, (void *)NULL)  ;
                     }
                     ret = reg(REG_NPAREN, &flags);
                     if (ret == NULL)
@@ -130346,7 +128265,7 @@
                 case '[':
                           if (one_exactly)
                           {
-                              EMSG_ONE_RET_NULL;
+                                return (semsg((const char *)(_(e_invalid_item_in_str_brackets)), (reg_magic == MAGIC_ALL) ? "" : "\\"), rc_did_emsg = TRUE, (void *)NULL)  ;
                           }
                           {
                               char_u    *lastbranch;
@@ -130358,7 +128277,7 @@
                               {
                                   if (c == NUL)
                                   {
-                                      EMSG2_RET_NULL(_(e_missing_sb_after_str), reg_magic == MAGIC_ALL);
+                                       return (semsg((const char *)(_(e_missing_sb_after_str)), (reg_magic == MAGIC_ALL) ? "" : "\\"), rc_did_emsg = TRUE, (void *)NULL) ;
                                   }
                                   br = regnode(BRANCH);
                                   if (ret == NULL)
@@ -130385,24 +128304,24 @@
                               }
                               if (ret == NULL)
                               {
-                                  EMSG2_RET_NULL(_(e_empty_str_brackets), reg_magic == MAGIC_ALL);
+                                   return (semsg((const char *)(_(e_empty_str_brackets)), (reg_magic == MAGIC_ALL) ? "" : "\\"), rc_did_emsg = TRUE, (void *)NULL) ;
                               }
                               lastbranch = regnode(BRANCH);
                               br = regnode(NOTHING);
-                              if (ret != JUST_CALC_SIZE)
+                              if (ret !=  ((char_u *) -1) )
                               {
                                   regtail(lastnode, br);
                                   regtail(lastbranch, br);
                                   for (br = ret; br != lastnode; )
                                   {
-                                      if (OP(br) == BRANCH)
+                                      if ( ((int)*(br))  == BRANCH)
                                       {
                                           regtail(br, lastbranch);
                                           if (reg_toolong)
                                           {
                                               return NULL;
                                           }
-                                          br = OPERAND(br);
+                                          br =  ((br) + 3) ;
                                       }
                                       else
                                       {
@@ -130446,7 +128365,7 @@
 
                               if (i < 0 || i > INT_MAX)
                               {
-                                  EMSG2_RET_NULL(_(e_invalid_character_after_str_2), reg_magic == MAGIC_ALL);
+                                   return (semsg((const char *)(_(e_invalid_character_after_str_2)), (reg_magic == MAGIC_ALL) ? "" : "\\"), rc_did_emsg = TRUE, (void *)NULL) ;
                               }
                               if (use_multibytecode(i))
                               {
@@ -130502,7 +128421,7 @@
                                 idx = 3;
                                 break;
                             default:
-                                EMSG2_RET_NULL(_(e_invalid_character_after_str), reg_magic == MAGIC_ALL);
+                                 return (semsg((const char *)(_(e_invalid_character_after_str)), (reg_magic == MAGIC_ALL) ? "" : "\\"), rc_did_emsg = TRUE, (void *)NULL) ;
                         }
                         if (delim_nl)
                         {
@@ -130519,12 +128438,13 @@
                     break;
 
                 case '>':
-                    if (!VIM_ISDIGIT(*regparse) && *regparse != '\'' && *regparse != '.')
+                    if (! ((unsigned)(*regparse) - '0' < 10)  && *regparse != '\'' && *regparse != '.')
                     {
                         goto delimiter_atom;
                     }
+                __attribute__((fallthrough));
                 default:
-                          if (VIM_ISDIGIT(c) || c == '<' || c == '>' || c == '\'' || c == '.')
+                          if ( ((unsigned)(c) - '0' < 10)  || c == '<' || c == '>' || c == '\'' || c == '.')
                           {
                               long_u    n = 0;
                               int       cmp;
@@ -130541,7 +128461,7 @@
                                   cur = TRUE;
                                   c = getchr();
                               }
-                              while (VIM_ISDIGIT(c))
+                              while ( ((unsigned)(c) - '0' < 10) )
                               {
                                   got_digit = TRUE;
                                   n = n * 10 + (c - '0');
@@ -130551,7 +128471,7 @@
                               {
                                   c = getchr();
                                   ret = regnode(RE_MARK);
-                                  if (ret == JUST_CALC_SIZE)
+                                  if (ret ==  ((char_u *) -1) )
                                   {
                                       regsize += 2;
                                   }
@@ -130603,7 +128523,7 @@
                                       }
                                       ret = regnode(RE_VCOL);
                                   }
-                                  if (ret == JUST_CALC_SIZE)
+                                  if (ret ==  ((char_u *) -1) )
                                   {
                                       regsize += 5;
                                   }
@@ -130616,12 +128536,12 @@
                               }
                           }
 
-                          EMSG2_RET_NULL(_(e_invalid_character_after_str), reg_magic == MAGIC_ALL);
+                           return (semsg((const char *)(_(e_invalid_character_after_str)), (reg_magic == MAGIC_ALL) ? "" : "\\"), rc_did_emsg = TRUE, (void *)NULL) ;
             }
         }
         break;
 
-      case Magic('['):
+      case  ((int)('[') - 256) :
 collection:
         {
             char_u      *lp;
@@ -130684,13 +128604,13 @@
 
                             if (startc > endc)
                             {
-                                EMSG_RET_NULL(_(e_reverse_range_in_character_class));
+                                 return (emsg((_(e_reverse_range_in_character_class))), rc_did_emsg = TRUE, (void *)NULL) ;
                             }
                             if (has_mbyte && ((*mb_char2len)(startc) > 1 || (*mb_char2len)(endc) > 1))
                             {
                                 if (endc > startc + 256)
                                 {
-                                    EMSG_RET_NULL(_(e_range_too_large_in_character_class));
+                                     return (emsg((_(e_range_too_large_in_character_class))), rc_did_emsg = TRUE, (void *)NULL) ;
                                 }
                                 while (++startc <= endc)
                                 {
@@ -130712,7 +128632,7 @@
                         regparse++;
                         if (*regparse == 'n')
                         {
-                            if (ret != JUST_CALC_SIZE)
+                            if (ret !=  ((char_u *) -1) )
                             {
                                 if (*ret == ANYOF)
                                 {
@@ -130728,7 +128648,7 @@
                             startc = coll_get_char();
                             if (startc == INT_MAX)
                             {
-                                EMSG_RET_NULL(_(e_unicode_val_too_large));
+                                 return (emsg((_(e_unicode_val_too_large))), rc_did_emsg = TRUE, (void *)NULL) ;
                             }
                             if (startc == 0)
                             {
@@ -130804,7 +128724,7 @@
                             case CLASS_DIGIT:
                                 for (cu = 1; cu <= 127; cu++)
                                 {
-                                    if (VIM_ISDIGIT(cu))
+                                    if ( ((unsigned)(cu) - '0' < 10) )
                                     {
                                         regmbc(cu);
                                     }
@@ -130822,7 +128742,7 @@
                             case CLASS_LOWER:
                                 for (cu = 1; cu <= 255; cu++)
                                 {
-                                    if (MB_ISLOWER(cu) && cu != 170 && cu != 186)
+                                    if ( vim_islower(cu)  && cu != 170 && cu != 186)
                                     {
                                         regmbc(cu);
                                     }
@@ -130856,7 +128776,7 @@
                             case CLASS_UPPER:
                                 for (cu = 1; cu <= 255; cu++)
                                 {
-                                    if (MB_ISUPPER(cu))
+                                    if ( vim_isupper(cu) )
                                     {
                                         regmbc(cu);
                                     }
@@ -130940,7 +128860,7 @@
                 prevchr_len = 1;
                 if (*regparse != ']')
                 {
-                    EMSG_RET_NULL(_(e_too_many_brackets));
+                     return (emsg((_(e_too_many_brackets))), rc_did_emsg = TRUE, (void *)NULL) ;
                 }
                 skipchr();
                 *flagp |= HASWIDTH | SIMPLE;
@@ -130948,10 +128868,11 @@
             }
             else if (reg_strict)
             {
-                EMSG2_RET_NULL(_(e_missing_rsb_after_str_lsb), reg_magic > MAGIC_OFF);
+                 return (semsg((const char *)(_(e_missing_rsb_after_str_lsb)), (reg_magic > MAGIC_OFF) ? "" : "\\"), rc_did_emsg = TRUE, (void *)NULL) ;
             }
         }
 
+      __attribute__((fallthrough));
       default:
         {
             int         len;
@@ -130967,7 +128888,7 @@
 
             ret = regnode(EXACTLY);
 
-            for (len = 0; c != NUL && (len == 0 || (re_multi_type(peekchr()) == NOT_MULTI && !one_exactly && !is_Magic(c))); ++len)
+            for (len = 0; c != NUL && (len == 0 || (re_multi_type(peekchr()) == NOT_MULTI && !one_exactly && ! ((c) < 0) )); ++len)
             {
                 c = no_Magic(c);
                 if (has_mbyte)
@@ -130980,7 +128901,7 @@
                         for (;;)
                         {
                             l = utf_ptr2len(regparse);
-                            if (!UTF_COMPOSINGLIKE(regparse, regparse + l))
+                            if (! utf_iscomposing(utf_ptr2char(regparse + l)) )
                             {
                                 break;
                             }
@@ -131037,7 +128958,7 @@
     skipchr();
     switch (op)
     {
-        case Magic('*'):
+        case  ((int)('*') - 256) :
             if (flags & SIMPLE)
             {
                 reginsert(STAR, ret);
@@ -131052,7 +128973,7 @@
             }
             break;
 
-        case Magic('+'):
+        case  ((int)('+') - 256) :
             if (flags & SIMPLE)
             {
                 reginsert(PLUS, ret);
@@ -131068,7 +128989,7 @@
             *flagp = (WORST | HASWIDTH | (flags & (HASNL | HASLOOKBH)));
             break;
 
-        case Magic('@'):
+        case  ((int)('@') - 256) :
             {
                 int     lop = END;
                 long    nr;
@@ -131098,7 +129019,7 @@
                 }
                 if (lop == END)
                 {
-                    EMSG2_RET_NULL(_(e_invalid_character_after_str_at), reg_magic == MAGIC_ALL);
+                     return (semsg((const char *)(_(e_invalid_character_after_str_at)), (reg_magic == MAGIC_ALL) ? "" : "\\"), rc_did_emsg = TRUE, (void *)NULL) ;
                 }
                 if (lop == BEHIND || lop == NOBEHIND)
                 {
@@ -131121,8 +129042,8 @@
                 break;
             }
 
-        case Magic('?'):
-        case Magic('='):
+        case  ((int)('?') - 256) :
+        case  ((int)('=') - 256) :
             reginsert(BRANCH, ret);
             regtail(ret, regnode(BRANCH));
             next = regnode(NOTHING);
@@ -131130,7 +129051,7 @@
             regoptail(ret, next);
             break;
 
-        case Magic('{'):
+        case  ((int)('{') - 256) :
             if (!read_limits(&minval, &maxval))
             {
                 return NULL;
@@ -131144,7 +129065,7 @@
             {
                 if (num_complex_braces >= 10)
                 {
-                    EMSG2_RET_NULL(_(e_too_many_complex_str_curly), reg_magic == MAGIC_ALL);
+                     return (semsg((const char *)(_(e_too_many_complex_str_curly)), (reg_magic == MAGIC_ALL) ? "" : "\\"), rc_did_emsg = TRUE, (void *)NULL) ;
                 }
                 reginsert(BRACE_COMPLEX + num_complex_braces, ret);
                 regoptail(ret, regnode(BACK));
@@ -131160,11 +129081,11 @@
     }
     if (re_multi_type(peekchr()) != NOT_MULTI)
     {
-        if (peekchr() == Magic('*'))
+        if (peekchr() ==  ((int)('*') - 256) )
         {
-            EMSG2_RET_NULL(_(e_nested_str), reg_magic >= MAGIC_ON);
+             return (semsg((const char *)(_(e_nested_str)), (reg_magic >= MAGIC_ON) ? "" : "\\"), rc_did_emsg = TRUE, (void *)NULL) ;
         }
-        EMSG3_RET_NULL(_(e_nested_str_chr), reg_magic == MAGIC_ALL, no_Magic(peekchr()));
+         return (semsg((const char *)(_(e_nested_str_chr)), (reg_magic == MAGIC_ALL) ? "" : "\\", (no_Magic(peekchr()))), rc_did_emsg = TRUE, (void *)NULL) ;
     }
 
     return ret;
@@ -131186,39 +129107,39 @@
         switch (peekchr())
         {
             case NUL:
-            case Magic('|'):
-            case Magic('&'):
-            case Magic(')'):
+            case  ((int)('|') - 256) :
+            case  ((int)('&') - 256) :
+            case  ((int)(')') - 256) :
                             cont = FALSE;
                             break;
-            case Magic('Z'):
+            case  ((int)('Z') - 256) :
                             regflags |= RF_ICOMBINE;
                             skipchr_keepstart();
                             break;
-            case Magic('c'):
+            case  ((int)('c') - 256) :
                             regflags |= RF_ICASE;
                             skipchr_keepstart();
                             break;
-            case Magic('C'):
+            case  ((int)('C') - 256) :
                             regflags |= RF_NOICASE;
                             skipchr_keepstart();
                             break;
-            case Magic('v'):
+            case  ((int)('v') - 256) :
                             reg_magic = MAGIC_ALL;
                             skipchr_keepstart();
                             curchr = -1;
                             break;
-            case Magic('m'):
+            case  ((int)('m') - 256) :
                             reg_magic = MAGIC_ON;
                             skipchr_keepstart();
                             curchr = -1;
                             break;
-            case Magic('M'):
+            case  ((int)('M') - 256) :
                             reg_magic = MAGIC_OFF;
                             skipchr_keepstart();
                             curchr = -1;
                             break;
-            case Magic('V'):
+            case  ((int)('V') - 256) :
                             reg_magic = MAGIC_NONE;
                             skipchr_keepstart();
                             curchr = -1;
@@ -131277,7 +129198,7 @@
         {
             regtail(chain, latest);
         }
-        if (peekchr() != Magic('&'))
+        if (peekchr() !=  ((int)('&') - 256) )
         {
             break;
         }
@@ -131309,7 +129230,7 @@
     {
         if (regnpar >= NSUBEXP)
         {
-            EMSG2_RET_NULL(_(e_too_many_str_open), reg_magic == MAGIC_ALL);
+             return (semsg((const char *)(_(e_too_many_str_open)), (reg_magic == MAGIC_ALL) ? "" : "\\"), rc_did_emsg = TRUE, (void *)NULL) ;
         }
         parno = regnpar;
         ++regnpar;
@@ -131326,7 +129247,7 @@
 
     if (bt_reg_parse_depth >= REG_MAX_PAREN_DEPTH)
     {
-        EMSG_RET_NULL(_(e_command_too_complex));
+         return (emsg((_(e_command_too_complex))), rc_did_emsg = TRUE, (void *)NULL) ;
     }
     ++bt_reg_parse_depth;
 
@@ -131349,7 +129270,7 @@
         *flagp &= ~HASWIDTH;
     }
     *flagp |= flags & (SPSTART | HASNL | HASLOOKBH);
-    while (peekchr() == Magic('|'))
+    while (peekchr() ==  ((int)('|') - 256) )
     {
         skipchr();
         br = regbranch(&flags);
@@ -131374,7 +129295,7 @@
         regoptail(br, ender);
     }
 
-    if (paren != REG_NOPAREN && getchr() != Magic(')'))
+    if (paren != REG_NOPAREN && getchr() !=  ((int)(')') - 256) )
     {
         if (paren == REG_NPAREN)
         {
@@ -131393,7 +129314,7 @@
     }
     else if (paren == REG_NOPAREN && peekchr() != NUL)
     {
-        if (curchr == Magic(')'))
+        if (curchr ==  ((int)(')') - 256) )
         {
             semsg(_(e_unmatched_str_close), reg_magic == MAGIC_ALL ? "" : "\\");
             rc_did_emsg = TRUE;
@@ -131429,13 +129350,13 @@
 
     if (expr == NULL)
     {
-        IEMSG_RET_NULL(e_null_argument);
+         return (iemsg((e_null_argument)), rc_did_emsg = TRUE, (void *)NULL) ;
     }
 
     init_class_tab();
 
     regcomp_start(expr, re_flags);
-    regcode = JUST_CALC_SIZE;
+    regcode =  ((char_u *) -1) ;
     regc(REGMAGIC);
     if (reg(REG_NOPAREN, &flags) == NULL)
     {
@@ -131457,7 +129378,7 @@
         vim_free(r);
         if (reg_toolong)
         {
-            EMSG_RET_NULL(_(e_pattern_too_long));
+             return (emsg((_(e_pattern_too_long))), rc_did_emsg = TRUE, (void *)NULL) ;
         }
         return NULL;
     }
@@ -131476,40 +129397,40 @@
         r->regflags |= RF_LOOKBH;
     }
     scan = r->program + 1;
-    if (OP(regnext(scan)) == END)
+    if ( ((int)*(regnext(scan)))  == END)
     {
-        scan = OPERAND(scan);
+        scan =  ((scan) + 3) ;
 
-        if (OP(scan) == BOL || OP(scan) == RE_BOF)
+        if ( ((int)*(scan))  == BOL ||  ((int)*(scan))  == RE_BOF)
         {
             r->reganch++;
             scan = regnext(scan);
         }
 
-        if (OP(scan) == EXACTLY)
+        if ( ((int)*(scan))  == EXACTLY)
         {
             if (has_mbyte)
             {
-                r->regstart = (*mb_ptr2char)(OPERAND(scan));
+                r->regstart = (*mb_ptr2char)( ((scan) + 3) );
             }
             else
             {
-                r->regstart = *OPERAND(scan);
+                r->regstart = * ((scan) + 3) ;
             }
         }
-        else if ((OP(scan) == BOW || OP(scan) == EOW || OP(scan) == NOTHING || OP(scan) == MOPEN + 0 || OP(scan) == NOPEN || OP(scan) == MCLOSE + 0 || OP(scan) == NCLOSE) && OP(regnext(scan)) == EXACTLY)
+        else if (( ((int)*(scan))  == BOW ||  ((int)*(scan))  == EOW ||  ((int)*(scan))  == NOTHING ||  ((int)*(scan))  == MOPEN + 0 ||  ((int)*(scan))  == NOPEN ||  ((int)*(scan))  == MCLOSE + 0 ||  ((int)*(scan))  == NCLOSE) &&  ((int)*(regnext(scan)))  == EXACTLY)
         {
             if (has_mbyte)
             {
-                r->regstart = (*mb_ptr2char)(OPERAND(regnext(scan)));
+                r->regstart = (*mb_ptr2char)( ((regnext(scan)) + 3) );
             }
             else
             {
-                r->regstart = *OPERAND(regnext(scan));
+                r->regstart = * ((regnext(scan)) + 3) ;
             }
         }
 
-        if ((flags & SPSTART || OP(scan) == BOW || OP(scan) == EOW) && !(flags & HASNL))
+        if ((flags & SPSTART ||  ((int)*(scan))  == BOW ||  ((int)*(scan))  == EOW) && !(flags & HASNL))
         {
             size_t  scanlen;
 
@@ -131517,12 +129438,12 @@
             len = 0;
             for (; scan != NULL; scan = regnext(scan))
             {
-                if (OP(scan) == EXACTLY)
+                if ( ((int)*(scan))  == EXACTLY)
                 {
-                    scanlen = STRLEN(OPERAND(scan));
+                    scanlen =  strlen((char *)( ((scan) + 3) )) ;
                     if (scanlen >= (size_t)len)
                     {
-                        longest = OPERAND(scan);
+                        longest =  ((scan) + 3) ;
                         len = (int)scanlen;
                     }
                 }
@@ -131576,15 +129497,13 @@
     vim_free(prog);
 }
 
-#define ADVANCE_REGINPUT() MB_PTR_ADV(rex.input)
-
 static long     bl_minval;
 static long     bl_maxval;
 
     static void
 reg_save(regsave_T *save, garray_T *gap)
 {
-    if (REG_MULTI)
+    if ( (rex.reg_match == NULL) )
     {
         save->rs_u.pos.col = (colnr_T)(rex.input - rex.line);
         save->rs_u.pos.lnum = rex.lnum;
@@ -131599,7 +129518,7 @@
     static void
 reg_restore(regsave_T *save, garray_T *gap)
 {
-    if (REG_MULTI)
+    if ( (rex.reg_match == NULL) )
     {
         if (rex.lnum != save->rs_u.pos.lnum)
         {
@@ -131618,7 +129537,7 @@
     static int
 reg_save_equal(regsave_T *save)
 {
-    if (REG_MULTI)
+    if ( (rex.reg_match == NULL) )
     {
         return rex.lnum == save->rs_u.pos.lnum
                                   && rex.input == rex.line + save->rs_u.pos.col;
@@ -131626,10 +129545,6 @@
     return rex.input == save->rs_u.ptr;
 }
 
-#define save_se(savep, posp, pp)     REG_MULTI ? save_se_multi((savep), (posp)) : save_se_one((savep), (pp))
-
-#define restore_se(savep, posp, pp) {     if (REG_MULTI)        *(posp) = (savep)->se_u.pos;     else   *(pp) = (savep)->se_u.ptr; }
-
     static void
 save_se_multi(save_se_T *savep, lpos_T *posp)
 {
@@ -131655,8 +129570,8 @@
     int         testval = 0;
 
     scan = rex.input;
-    opnd = OPERAND(p);
-    switch (OP(p))
+    opnd =  ((p) + 3) ;
+    switch ( ((int)*(p)) )
     {
       case ANY:
       case ANY + ADD_NL:
@@ -131665,9 +129580,9 @@
             while (*scan != NUL && count < maxcount)
             {
                 ++count;
-                MB_PTR_ADV(scan);
+                 scan += (*mb_ptr2len)(scan) ;
             }
-            if (!REG_MULTI || !WITH_NL(OP(p)) || rex.lnum > rex.reg_maxline || rex.reg_line_lbr || count == maxcount)
+            if (! (rex.reg_match == NULL)  || ! (( ((int)*(p)) ) >=  ANY + ADD_NL  && ( ((int)*(p)) ) <=  NUPPER + ADD_NL )  || rex.lnum > rex.reg_maxline || rex.reg_line_lbr || count == maxcount)
             {
                 break;
             }
@@ -131688,13 +129603,13 @@
       case SIDENT + ADD_NL:
         while (count < maxcount)
         {
-            if (vim_isIDc(PTR2CHAR(scan)) && (testval || !VIM_ISDIGIT(*scan)))
+            if (vim_isIDc( (has_mbyte ? mb_ptr2char(scan) : (int)*(scan)) ) && (testval || ! ((unsigned)(*scan) - '0' < 10) ))
             {
-                MB_PTR_ADV(scan);
+                 scan += (*mb_ptr2len)(scan) ;
             }
             else if (*scan == NUL)
             {
-                if (!REG_MULTI || !WITH_NL(OP(p)) || rex.lnum > rex.reg_maxline || rex.reg_line_lbr)
+                if (! (rex.reg_match == NULL)  || ! (( ((int)*(p)) ) >=  ANY + ADD_NL  && ( ((int)*(p)) ) <=  NUPPER + ADD_NL )  || rex.lnum > rex.reg_maxline || rex.reg_line_lbr)
                 {
                     break;
                 }
@@ -131705,7 +129620,7 @@
                     break;
                 }
             }
-            else if (rex.reg_line_lbr && *scan == '\n' && WITH_NL(OP(p)))
+            else if (rex.reg_line_lbr && *scan == '\n' &&  (( ((int)*(p)) ) >=  ANY + ADD_NL  && ( ((int)*(p)) ) <=  NUPPER + ADD_NL ) )
             {
                 ++scan;
             }
@@ -131724,13 +129639,13 @@
       case SKWORD + ADD_NL:
         while (count < maxcount)
         {
-            if (vim_iswordp_buf(scan, rex.reg_buf) && (testval || !VIM_ISDIGIT(*scan)))
+            if (vim_iswordp_buf(scan, rex.reg_buf) && (testval || ! ((unsigned)(*scan) - '0' < 10) ))
             {
-                MB_PTR_ADV(scan);
+                 scan += (*mb_ptr2len)(scan) ;
             }
             else if (*scan == NUL)
             {
-                if (!REG_MULTI || !WITH_NL(OP(p)) || rex.lnum > rex.reg_maxline || rex.reg_line_lbr)
+                if (! (rex.reg_match == NULL)  || ! (( ((int)*(p)) ) >=  ANY + ADD_NL  && ( ((int)*(p)) ) <=  NUPPER + ADD_NL )  || rex.lnum > rex.reg_maxline || rex.reg_line_lbr)
                 {
                     break;
                 }
@@ -131741,7 +129656,7 @@
                     break;
                 }
             }
-            else if (rex.reg_line_lbr && *scan == '\n' && WITH_NL(OP(p)))
+            else if (rex.reg_line_lbr && *scan == '\n' &&  (( ((int)*(p)) ) >=  ANY + ADD_NL  && ( ((int)*(p)) ) <=  NUPPER + ADD_NL ) )
             {
                 ++scan;
             }
@@ -131760,13 +129675,13 @@
       case SFNAME + ADD_NL:
         while (count < maxcount)
         {
-            if (vim_isfilec(PTR2CHAR(scan)) && (testval || !VIM_ISDIGIT(*scan)))
+            if (vim_isfilec( (has_mbyte ? mb_ptr2char(scan) : (int)*(scan)) ) && (testval || ! ((unsigned)(*scan) - '0' < 10) ))
             {
-                MB_PTR_ADV(scan);
+                 scan += (*mb_ptr2len)(scan) ;
             }
             else if (*scan == NUL)
             {
-                if (!REG_MULTI || !WITH_NL(OP(p)) || rex.lnum > rex.reg_maxline || rex.reg_line_lbr)
+                if (! (rex.reg_match == NULL)  || ! (( ((int)*(p)) ) >=  ANY + ADD_NL  && ( ((int)*(p)) ) <=  NUPPER + ADD_NL )  || rex.lnum > rex.reg_maxline || rex.reg_line_lbr)
                 {
                     break;
                 }
@@ -131777,7 +129692,7 @@
                     break;
                 }
             }
-            else if (rex.reg_line_lbr && *scan == '\n' && WITH_NL(OP(p)))
+            else if (rex.reg_line_lbr && *scan == '\n' &&  (( ((int)*(p)) ) >=  ANY + ADD_NL  && ( ((int)*(p)) ) <=  NUPPER + ADD_NL ) )
             {
                 ++scan;
             }
@@ -131798,7 +129713,7 @@
         {
             if (*scan == NUL)
             {
-                if (!REG_MULTI || !WITH_NL(OP(p)) || rex.lnum > rex.reg_maxline || rex.reg_line_lbr)
+                if (! (rex.reg_match == NULL)  || ! (( ((int)*(p)) ) >=  ANY + ADD_NL  && ( ((int)*(p)) ) <=  NUPPER + ADD_NL )  || rex.lnum > rex.reg_maxline || rex.reg_line_lbr)
                 {
                     break;
                 }
@@ -131809,11 +129724,11 @@
                     break;
                 }
             }
-            else if (vim_isprintc(PTR2CHAR(scan)) == 1 && (testval || !VIM_ISDIGIT(*scan)))
+            else if (vim_isprintc( (has_mbyte ? mb_ptr2char(scan) : (int)*(scan)) ) == 1 && (testval || ! ((unsigned)(*scan) - '0' < 10) ))
             {
-                MB_PTR_ADV(scan);
+                 scan += (*mb_ptr2len)(scan) ;
             }
-            else if (rex.reg_line_lbr && *scan == '\n' && WITH_NL(OP(p)))
+            else if (rex.reg_line_lbr && *scan == '\n' &&  (( ((int)*(p)) ) >=  ANY + ADD_NL  && ( ((int)*(p)) ) <=  NUPPER + ADD_NL ) )
             {
                 ++scan;
             }
@@ -131835,7 +129750,7 @@
 
             if (*scan == NUL)
             {
-                if (!REG_MULTI || !WITH_NL(OP(p)) || rex.lnum > rex.reg_maxline || rex.reg_line_lbr)
+                if (! (rex.reg_match == NULL)  || ! (( ((int)*(p)) ) >=  ANY + ADD_NL  && ( ((int)*(p)) ) <=  NUPPER + ADD_NL )  || rex.lnum > rex.reg_maxline || rex.reg_line_lbr)
                 {
                     break;
                 }
@@ -131858,7 +129773,7 @@
             {
                 ++scan;
             }
-            else if (rex.reg_line_lbr && *scan == '\n' && WITH_NL(OP(p)))
+            else if (rex.reg_line_lbr && *scan == '\n' &&  (( ((int)*(p)) ) >=  ANY + ADD_NL  && ( ((int)*(p)) ) <=  NUPPER + ADD_NL ) )
             {
                 ++scan;
             }
@@ -131946,8 +129861,8 @@
 
             if (rex.reg_ic)
             {
-                cu = MB_TOUPPER(*opnd);
-                cl = MB_TOLOWER(*opnd);
+                cu =  vim_toupper(*opnd) ;
+                cl =  vim_tolower(*opnd) ;
                 while (count < maxcount && (*scan == cu || *scan == cl))
                 {
                     count++;
@@ -132010,7 +129925,7 @@
 
             if (*scan == NUL)
             {
-                if (!REG_MULTI || !WITH_NL(OP(p)) || rex.lnum > rex.reg_maxline || rex.reg_line_lbr)
+                if (! (rex.reg_match == NULL)  || ! (( ((int)*(p)) ) >=  ANY + ADD_NL  && ( ((int)*(p)) ) <=  NUPPER + ADD_NL )  || rex.lnum > rex.reg_maxline || rex.reg_line_lbr)
                 {
                     break;
                 }
@@ -132021,7 +129936,7 @@
                     break;
                 }
             }
-            else if (rex.reg_line_lbr && *scan == '\n' && WITH_NL(OP(p)))
+            else if (rex.reg_line_lbr && *scan == '\n' &&  (( ((int)*(p)) ) >=  ANY + ADD_NL  && ( ((int)*(p)) ) <=  NUPPER + ADD_NL ) )
             {
                 ++scan;
             }
@@ -132046,12 +129961,12 @@
         break;
 
       case NEWL:
-        while (count < maxcount && ((*scan == NUL && rex.lnum <= rex.reg_maxline && !rex.reg_line_lbr && REG_MULTI) || (*scan == '\n' && rex.reg_line_lbr)))
+        while (count < maxcount && ((*scan == NUL && rex.lnum <= rex.reg_maxline && !rex.reg_line_lbr &&  (rex.reg_match == NULL) ) || (*scan == '\n' && rex.reg_line_lbr)))
         {
             count++;
             if (rex.reg_line_lbr)
             {
-                ADVANCE_REGINPUT();
+                  rex.input += (*mb_ptr2len)(rex.input)  ;
             }
             else
             {
@@ -132122,7 +130037,7 @@
 
     for (i = 0; i < NSUBEXP; ++i)
     {
-        if (REG_MULTI)
+        if ( (rex.reg_match == NULL) )
         {
             bp->save_start[i].se_u.pos = rex.reg_startpos[i];
             bp->save_end[i].se_u.pos = rex.reg_endpos[i];
@@ -132148,7 +130063,7 @@
 
     for (i = 0; i < NSUBEXP; ++i)
     {
-        if (REG_MULTI)
+        if ( (rex.reg_match == NULL) )
         {
             rex.reg_startpos[i] = bp->save_start[i].se_u.pos;
             rex.reg_endpos[i] = bp->save_end[i].se_u.pos;
@@ -132162,7 +130077,7 @@
 }
 
     static int
-regmatch(char_u      *scan, int         *timed_out UNUSED)
+regmatch(char_u      *scan, int         *timed_out  __attribute__((unused)) )
 {
   char_u        *next;
   int           op;
@@ -132189,18 +130104,18 @@
 
         next = regnext(scan);
 
-        op = OP(scan);
-        if (!rex.reg_line_lbr && WITH_NL(op) && REG_MULTI && *rex.input == NUL && rex.lnum <= rex.reg_maxline)
+        op =  ((int)*(scan)) ;
+        if (!rex.reg_line_lbr &&  ((op) >=  ANY + ADD_NL  && (op) <=  NUPPER + ADD_NL )  &&  (rex.reg_match == NULL)  && *rex.input == NUL && rex.lnum <= rex.reg_maxline)
         {
             reg_nextline();
         }
-        else if (rex.reg_line_lbr && WITH_NL(op) && *rex.input == '\n')
+        else if (rex.reg_line_lbr &&  ((op) >=  ANY + ADD_NL  && (op) <=  NUPPER + ADD_NL )  && *rex.input == '\n')
         {
-            ADVANCE_REGINPUT();
+              rex.input += (*mb_ptr2len)(rex.input)  ;
         }
         else
         {
-          if (WITH_NL(op))
+          if ( ((op) >=  ANY + ADD_NL  && (op) <=  NUPPER + ADD_NL ) )
           {
               op -= ADD_NL;
           }
@@ -132229,7 +130144,7 @@
             break;
 
           case RE_BOF:
-            if (rex.lnum != 0 || rex.input != rex.line || (REG_MULTI && rex.reg_firstlnum > 1))
+            if (rex.lnum != 0 || rex.input != rex.line || ( (rex.reg_match == NULL)  && rex.reg_firstlnum > 1))
             {
                 status = RA_NOMATCH;
             }
@@ -132251,14 +130166,14 @@
 
           case RE_MARK:
             {
-                int     mark = OPERAND(scan)[0];
-                int     cmp = OPERAND(scan)[1];
+                int     mark =  ((scan) + 3) [0];
+                int     cmp =  ((scan) + 3) [1];
                 pos_T   *pos;
-                size_t  col = REG_MULTI ? rex.input - rex.line : 0;
+                size_t  col =  (rex.reg_match == NULL)  ? rex.input - rex.line : 0;
 
                 pos = getmark_buf(rex.reg_buf, mark, FALSE);
 
-                if (REG_MULTI)
+                if ( (rex.reg_match == NULL) )
                 {
                     rex.line = reg_getline(rex.lnum);
                     rex.input = rex.line + col;
@@ -132291,7 +130206,7 @@
             break;
 
           case RE_LNUM:
-            if (!REG_MULTI || !re_num_cmp((long_u)(rex.lnum + rex.reg_firstlnum), scan))
+            if (! (rex.reg_match == NULL)  || !re_num_cmp((long_u)(rex.lnum + rex.reg_firstlnum), scan))
             {
                 status = RA_NOMATCH;
             }
@@ -132307,10 +130222,10 @@
           case RE_VCOL:
             {
                 win_T       *wp = rex.reg_win == NULL ? curwin : rex.reg_win;
-                linenr_T    lnum = REG_MULTI ? rex.reg_firstlnum + rex.lnum : 1;
+                linenr_T    lnum =  (rex.reg_match == NULL)  ? rex.reg_firstlnum + rex.lnum : 1;
                 long_u      vcol;
 
-                if (REG_MULTI && (lnum <= 0 || lnum > wp->w_buffer->b_ml.ml_line_count))
+                if ( (rex.reg_match == NULL)  && (lnum <= 0 || lnum > wp->w_buffer->b_ml.ml_line_count))
                 {
                     lnum = 1;
                 }
@@ -132383,7 +130298,7 @@
             }
             else
             {
-                ADVANCE_REGINPUT();
+                  rex.input += (*mb_ptr2len)(rex.input)  ;
             }
             break;
 
@@ -132394,18 +130309,18 @@
             }
             else
             {
-                ADVANCE_REGINPUT();
+                  rex.input += (*mb_ptr2len)(rex.input)  ;
             }
             break;
 
           case SIDENT:
-            if (VIM_ISDIGIT(*rex.input) || !vim_isIDc(c))
+            if ( ((unsigned)(*rex.input) - '0' < 10)  || !vim_isIDc(c))
             {
                 status = RA_NOMATCH;
             }
             else
             {
-                ADVANCE_REGINPUT();
+                  rex.input += (*mb_ptr2len)(rex.input)  ;
             }
             break;
 
@@ -132416,18 +130331,18 @@
             }
             else
             {
-                ADVANCE_REGINPUT();
+                  rex.input += (*mb_ptr2len)(rex.input)  ;
             }
             break;
 
           case SKWORD:
-            if (VIM_ISDIGIT(*rex.input) || !vim_iswordp_buf(rex.input, rex.reg_buf))
+            if ( ((unsigned)(*rex.input) - '0' < 10)  || !vim_iswordp_buf(rex.input, rex.reg_buf))
             {
                 status = RA_NOMATCH;
             }
             else
             {
-                ADVANCE_REGINPUT();
+                  rex.input += (*mb_ptr2len)(rex.input)  ;
             }
             break;
 
@@ -132438,238 +130353,238 @@
             }
             else
             {
-                ADVANCE_REGINPUT();
+                  rex.input += (*mb_ptr2len)(rex.input)  ;
             }
             break;
 
           case SFNAME:
-            if (VIM_ISDIGIT(*rex.input) || !vim_isfilec(c))
+            if ( ((unsigned)(*rex.input) - '0' < 10)  || !vim_isfilec(c))
             {
                 status = RA_NOMATCH;
             }
             else
             {
-                ADVANCE_REGINPUT();
+                  rex.input += (*mb_ptr2len)(rex.input)  ;
             }
             break;
 
           case PRINT:
-            if (!vim_isprintc(PTR2CHAR(rex.input)))
+            if (!vim_isprintc( (has_mbyte ? mb_ptr2char(rex.input) : (int)*(rex.input)) ))
             {
                 status = RA_NOMATCH;
             }
             else
             {
-                ADVANCE_REGINPUT();
+                  rex.input += (*mb_ptr2len)(rex.input)  ;
             }
             break;
 
           case SPRINT:
-            if (VIM_ISDIGIT(*rex.input) || !vim_isprintc(PTR2CHAR(rex.input)))
+            if ( ((unsigned)(*rex.input) - '0' < 10)  || !vim_isprintc( (has_mbyte ? mb_ptr2char(rex.input) : (int)*(rex.input)) ))
             {
                 status = RA_NOMATCH;
             }
             else
             {
-                ADVANCE_REGINPUT();
+                  rex.input += (*mb_ptr2len)(rex.input)  ;
             }
             break;
 
           case RE_WHITE:
-            if (!VIM_ISWHITE(c))
+            if (! ((c) == ' ' || (c) == '\t') )
             {
                 status = RA_NOMATCH;
             }
             else
             {
-                ADVANCE_REGINPUT();
+                  rex.input += (*mb_ptr2len)(rex.input)  ;
             }
             break;
 
           case NWHITE:
-            if (c == NUL || VIM_ISWHITE(c))
+            if (c == NUL ||  ((c) == ' ' || (c) == '\t') )
             {
                 status = RA_NOMATCH;
             }
             else
             {
-                ADVANCE_REGINPUT();
+                  rex.input += (*mb_ptr2len)(rex.input)  ;
             }
             break;
 
           case DIGIT:
-            if (!ri_digit(c))
+            if (! ((c) < 0x100 && (class_tab[c] & RI_DIGIT)) )
             {
                 status = RA_NOMATCH;
             }
             else
             {
-                ADVANCE_REGINPUT();
+                  rex.input += (*mb_ptr2len)(rex.input)  ;
             }
             break;
 
           case NDIGIT:
-            if (c == NUL || ri_digit(c))
+            if (c == NUL ||  ((c) < 0x100 && (class_tab[c] & RI_DIGIT)) )
             {
                 status = RA_NOMATCH;
             }
             else
             {
-                ADVANCE_REGINPUT();
+                  rex.input += (*mb_ptr2len)(rex.input)  ;
             }
             break;
 
           case HEX:
-            if (!ri_hex(c))
+            if (! ((c) < 0x100 && (class_tab[c] & RI_HEX)) )
             {
                 status = RA_NOMATCH;
             }
             else
             {
-                ADVANCE_REGINPUT();
+                  rex.input += (*mb_ptr2len)(rex.input)  ;
             }
             break;
 
           case NHEX:
-            if (c == NUL || ri_hex(c))
+            if (c == NUL ||  ((c) < 0x100 && (class_tab[c] & RI_HEX)) )
             {
                 status = RA_NOMATCH;
             }
             else
             {
-                ADVANCE_REGINPUT();
+                  rex.input += (*mb_ptr2len)(rex.input)  ;
             }
             break;
 
           case OCTAL:
-            if (!ri_octal(c))
+            if (! ((c) < 0x100 && (class_tab[c] & RI_OCTAL)) )
             {
                 status = RA_NOMATCH;
             }
             else
             {
-                ADVANCE_REGINPUT();
+                  rex.input += (*mb_ptr2len)(rex.input)  ;
             }
             break;
 
           case NOCTAL:
-            if (c == NUL || ri_octal(c))
+            if (c == NUL ||  ((c) < 0x100 && (class_tab[c] & RI_OCTAL)) )
             {
                 status = RA_NOMATCH;
             }
             else
             {
-                ADVANCE_REGINPUT();
+                  rex.input += (*mb_ptr2len)(rex.input)  ;
             }
             break;
 
           case WORD:
-            if (!ri_word(c))
+            if (! ((c) < 0x100 && (class_tab[c] & RI_WORD)) )
             {
                 status = RA_NOMATCH;
             }
             else
             {
-                ADVANCE_REGINPUT();
+                  rex.input += (*mb_ptr2len)(rex.input)  ;
             }
             break;
 
           case NWORD:
-            if (c == NUL || ri_word(c))
+            if (c == NUL ||  ((c) < 0x100 && (class_tab[c] & RI_WORD)) )
             {
                 status = RA_NOMATCH;
             }
             else
             {
-                ADVANCE_REGINPUT();
+                  rex.input += (*mb_ptr2len)(rex.input)  ;
             }
             break;
 
           case HEAD:
-            if (!ri_head(c))
+            if (! ((c) < 0x100 && (class_tab[c] & RI_HEAD)) )
             {
                 status = RA_NOMATCH;
             }
             else
             {
-                ADVANCE_REGINPUT();
+                  rex.input += (*mb_ptr2len)(rex.input)  ;
             }
             break;
 
           case NHEAD:
-            if (c == NUL || ri_head(c))
+            if (c == NUL ||  ((c) < 0x100 && (class_tab[c] & RI_HEAD)) )
             {
                 status = RA_NOMATCH;
             }
             else
             {
-                ADVANCE_REGINPUT();
+                  rex.input += (*mb_ptr2len)(rex.input)  ;
             }
             break;
 
           case ALPHA:
-            if (!ri_alpha(c))
+            if (! ((c) < 0x100 && (class_tab[c] & RI_ALPHA)) )
             {
                 status = RA_NOMATCH;
             }
             else
             {
-                ADVANCE_REGINPUT();
+                  rex.input += (*mb_ptr2len)(rex.input)  ;
             }
             break;
 
           case NALPHA:
-            if (c == NUL || ri_alpha(c))
+            if (c == NUL ||  ((c) < 0x100 && (class_tab[c] & RI_ALPHA)) )
             {
                 status = RA_NOMATCH;
             }
             else
             {
-                ADVANCE_REGINPUT();
+                  rex.input += (*mb_ptr2len)(rex.input)  ;
             }
             break;
 
           case LOWER:
-            if (!ri_lower(c))
+            if (! ((c) < 0x100 && (class_tab[c] & RI_LOWER)) )
             {
                 status = RA_NOMATCH;
             }
             else
             {
-                ADVANCE_REGINPUT();
+                  rex.input += (*mb_ptr2len)(rex.input)  ;
             }
             break;
 
           case NLOWER:
-            if (c == NUL || ri_lower(c))
+            if (c == NUL ||  ((c) < 0x100 && (class_tab[c] & RI_LOWER)) )
             {
                 status = RA_NOMATCH;
             }
             else
             {
-                ADVANCE_REGINPUT();
+                  rex.input += (*mb_ptr2len)(rex.input)  ;
             }
             break;
 
           case UPPER:
-            if (!ri_upper(c))
+            if (! ((c) < 0x100 && (class_tab[c] & RI_UPPER)) )
             {
                 status = RA_NOMATCH;
             }
             else
             {
-                ADVANCE_REGINPUT();
+                  rex.input += (*mb_ptr2len)(rex.input)  ;
             }
             break;
 
           case NUPPER:
-            if (c == NUL || ri_upper(c))
+            if (c == NUL ||  ((c) < 0x100 && (class_tab[c] & RI_UPPER)) )
             {
                 status = RA_NOMATCH;
             }
             else
             {
-                ADVANCE_REGINPUT();
+                  rex.input += (*mb_ptr2len)(rex.input)  ;
             }
             break;
 
@@ -132678,8 +130593,8 @@
                 int     len;
                 char_u  *opnd;
 
-                opnd = OPERAND(scan);
-                if (*opnd != *rex.input && (!rex.reg_ic || (!enc_utf8 && MB_TOLOWER(*opnd) != MB_TOLOWER(*rex.input))))
+                opnd =  ((scan) + 3) ;
+                if (*opnd != *rex.input && (!rex.reg_ic || (!enc_utf8 &&  vim_tolower(*opnd)  !=  vim_tolower(*rex.input) )))
                 {
                     status = RA_NOMATCH;
                 }
@@ -132694,13 +130609,13 @@
                     }
                     else
                     {
-                        len = (int)STRLEN(opnd);
+                        len = (int) strlen((char *)(opnd)) ;
                         if (cstrncmp(opnd, rex.input, &len) != 0)
                         {
                             status = RA_NOMATCH;
                         }
                     }
-                    if (status != RA_NOMATCH && enc_utf8 && UTF_COMPOSINGLIKE(rex.input, rex.input + len) && !rex.reg_icombine && OP(next) != RE_COMPOSING)
+                    if (status != RA_NOMATCH && enc_utf8 &&  utf_iscomposing(utf_ptr2char(rex.input + len))  && !rex.reg_icombine &&  ((int)*(next))  != RE_COMPOSING)
                     {
                         status = RA_NOMATCH;
                     }
@@ -132715,7 +130630,7 @@
           case ANYOF:
           case ANYBUT:
             {
-                char_u  *q = OPERAND(scan);
+                char_u  *q =  ((scan) + 3) ;
 
                 if (c == NUL)
                 {
@@ -132735,8 +130650,8 @@
                         len = utfc_ptr2len(q) - utf_ptr2len(q);
                     }
 
-                    MB_CPTR_ADV(rex.input);
-                    MB_CPTR_ADV(q);
+                     rex.input += enc_utf8 ? utf_ptr2len(rex.input) : (*mb_ptr2len)(rex.input) ;
+                     q += enc_utf8 ? utf_ptr2len(q) : (*mb_ptr2len)(q) ;
 
                     if (!enc_utf8 || len == 0)
                     {
@@ -132765,7 +130680,7 @@
                 int opndc = 0;
                 int inpc;
 
-                opnd = OPERAND(scan);
+                opnd =  ((scan) + 3) ;
                 if ((len = (*mb_ptr2len)(opnd)) < 2)
                 {
                     status = RA_NOMATCH;
@@ -132827,7 +130742,7 @@
             {
                 while (utf_iscomposing(utf_ptr2char(rex.input)))
                 {
-                    MB_CPTR_ADV(rex.input);
+                     rex.input += enc_utf8 ? utf_ptr2len(rex.input) : (*mb_ptr2len)(rex.input) ;
                 }
             }
             break;
@@ -132894,7 +130809,7 @@
                 else
                 {
                     rp->rs_no = no;
-                    save_se(&rp->rs_un.sesave, &rex.reg_startpos[no], &rex.reg_startp[no]);
+                      (rex.reg_match == NULL)  ? save_se_multi((&rp->rs_un.sesave), (&rex.reg_startpos[no])) : save_se_one((&rp->rs_un.sesave), (&rex.reg_startp[no])) ;
                 }
             }
             break;
@@ -132928,7 +130843,7 @@
                 else
                 {
                     rp->rs_no = no;
-                    save_se(&rp->rs_un.sesave, &rex.reg_endpos[no], &rex.reg_endp[no]);
+                      (rex.reg_match == NULL)  ? save_se_multi((&rp->rs_un.sesave), (&rex.reg_endpos[no])) : save_se_one((&rp->rs_un.sesave), (&rex.reg_endp[no])) ;
                 }
             }
             break;
@@ -132947,7 +130862,7 @@
 
                 no = op - BACKREF;
                 cleanup_subexpr();
-                if (!REG_MULTI)
+                if (! (rex.reg_match == NULL) )
                 {
                     if (rex.reg_startp[no] == NULL || rex.reg_endp[no] == NULL)
                     {
@@ -132997,9 +130912,9 @@
 
           case BRANCH:
             {
-                if (OP(next) != BRANCH)
+                if ( ((int)*(next))  != BRANCH)
                 {
-                    next = OPERAND(scan);
+                    next =  ((scan) + 3) ;
                 }
                 else
                 {
@@ -133018,16 +130933,16 @@
 
           case BRACE_LIMITS:
             {
-                if (OP(next) == BRACE_SIMPLE)
+                if ( ((int)*(next))  == BRACE_SIMPLE)
                 {
-                    bl_minval = OPERAND_MIN(scan);
-                    bl_maxval = OPERAND_MAX(scan);
+                    bl_minval =  (((long)(scan)[3] << 24) + ((long)(scan)[4] << 16)                    + ((long)(scan)[5] << 8) + (long)(scan)[6]) ;
+                    bl_maxval =   (((long)((scan) + 4)[3] << 24) + ((long)((scan) + 4)[4] << 16)                    + ((long)((scan) + 4)[5] << 8) + (long)((scan) + 4)[6])  ;
                 }
-                else if (OP(next) >= BRACE_COMPLEX && OP(next) < BRACE_COMPLEX + 10)
+                else if ( ((int)*(next))  >= BRACE_COMPLEX &&  ((int)*(next))  < BRACE_COMPLEX + 10)
                 {
-                    no = OP(next) - BRACE_COMPLEX;
-                    brace_min[no] = OPERAND_MIN(scan);
-                    brace_max[no] = OPERAND_MAX(scan);
+                    no =  ((int)*(next))  - BRACE_COMPLEX;
+                    brace_min[no] =  (((long)(scan)[3] << 24) + ((long)(scan)[4] << 16)                    + ((long)(scan)[5] << 8) + (long)(scan)[6]) ;
+                    brace_max[no] =   (((long)((scan) + 4)[3] << 24) + ((long)((scan) + 4)[4] << 16)                    + ((long)((scan) + 4)[5] << 8) + (long)((scan) + 4)[6])  ;
                     brace_count[no] = 0;
                 }
                 else
@@ -133063,7 +130978,7 @@
                     {
                         rp->rs_no = no;
                         reg_save(&rp->rs_un.regsave, &backpos);
-                        next = OPERAND(scan);
+                        next =  ((scan) + 3) ;
                     }
                     break;
                 }
@@ -133081,7 +130996,7 @@
                         {
                             rp->rs_no = no;
                             reg_save(&rp->rs_un.regsave, &backpos);
-                            next = OPERAND(scan);
+                            next =  ((scan) + 3) ;
                         }
                     }
                 }
@@ -133109,18 +131024,18 @@
             {
                 regstar_T       rst;
 
-                if (OP(next) == EXACTLY)
+                if ( ((int)*(next))  == EXACTLY)
                 {
-                    rst.nextb = *OPERAND(next);
+                    rst.nextb = * ((next) + 3) ;
                     if (rex.reg_ic)
                     {
-                        if (MB_ISUPPER(rst.nextb))
+                        if ( vim_isupper(rst.nextb) )
                         {
-                            rst.nextb_ic = MB_TOLOWER(rst.nextb);
+                            rst.nextb_ic =  vim_tolower(rst.nextb) ;
                         }
                         else
                         {
-                            rst.nextb_ic = MB_TOUPPER(rst.nextb);
+                            rst.nextb_ic =  vim_toupper(rst.nextb) ;
                         }
                     }
                     else
@@ -133136,7 +131051,7 @@
                 if (op != BRACE_SIMPLE)
                 {
                     rst.minval = (op == STAR) ? 0 : 1;
-                    rst.maxval = MAX_LIMIT;
+                    rst.maxval =  (32767L << 16L) ;
                 }
                 else
                 {
@@ -133144,7 +131059,7 @@
                     rst.maxval = bl_maxval;
                 }
 
-                rst.count = regrepeat(OPERAND(scan), rst.maxval);
+                rst.count = regrepeat( ((scan) + 3) , rst.maxval);
                 if (got_int)
                 {
                     status = RA_FAIL;
@@ -133196,7 +131111,7 @@
             {
                 rp->rs_no = op;
                 reg_save(&rp->rs_un.regsave, &backpos);
-                next = OPERAND(scan);
+                next =  ((scan) + 3) ;
             }
             break;
 
@@ -133230,7 +131145,7 @@
             break;
 
           case BHPOS:
-            if (REG_MULTI)
+            if ( (rex.reg_match == NULL) )
             {
                 if (behind_pos.rs_u.pos.col != (colnr_T)(rex.input - rex.line) || behind_pos.rs_u.pos.lnum != rex.lnum)
                 {
@@ -133244,13 +131159,13 @@
             break;
 
           case NEWL:
-            if ((c != NUL || !REG_MULTI || rex.lnum > rex.reg_maxline || rex.reg_line_lbr) && (c != '\n' || !rex.reg_line_lbr))
+            if ((c != NUL || ! (rex.reg_match == NULL)  || rex.lnum > rex.reg_maxline || rex.reg_line_lbr) && (c != '\n' || !rex.reg_line_lbr))
             {
                 status = RA_NOMATCH;
             }
             else if (rex.reg_line_lbr)
             {
-                ADVANCE_REGINPUT();
+                  rex.input += (*mb_ptr2len)(rex.input)  ;
             }
             else
             {
@@ -133300,7 +131215,7 @@
                 {
                     if (*s == NUL)
                     {
-                        if (!with_nl || !REG_MULTI || rex.reg_line_lbr || rex.lnum >= rex.reg_maxline)
+                        if (!with_nl || ! (rex.reg_match == NULL)  || rex.reg_line_lbr || rex.lnum >= rex.reg_maxline)
                         {
                             break;
                         }
@@ -133335,7 +131250,7 @@
                 }
                 if (level >= 1)
                 {
-                    if (REG_MULTI && rex.lnum != save_lnum)
+                    if ( (rex.reg_match == NULL)  && rex.lnum != save_lnum)
                     {
                         rex.lnum = save_lnum;
                         rex.line = reg_getline(rex.lnum);
@@ -133378,7 +131293,7 @@
           case RS_MOPEN:
             if (status == RA_NOMATCH)
             {
-                restore_se(&rp->rs_un.sesave, &rex.reg_startpos[rp->rs_no], &rex.reg_startp[rp->rs_no]);
+                 {     if ( (rex.reg_match == NULL) )        *(&rex.reg_startpos[rp->rs_no]) = (&rp->rs_un.sesave)->se_u.pos;     else   *(&rex.reg_startp[rp->rs_no]) = (&rp->rs_un.sesave)->se_u.ptr; } ;
             }
             regstack_pop(&scan);
             break;
@@ -133386,7 +131301,7 @@
           case RS_MCLOSE:
             if (status == RA_NOMATCH)
             {
-                restore_se(&rp->rs_un.sesave, &rex.reg_endpos[rp->rs_no], &rex.reg_endp[rp->rs_no]);
+                 {     if ( (rex.reg_match == NULL) )        *(&rex.reg_endpos[rp->rs_no]) = (&rp->rs_un.sesave)->se_u.pos;     else   *(&rex.reg_endp[rp->rs_no]) = (&rp->rs_un.sesave)->se_u.ptr; } ;
             }
             regstack_pop(&scan);
             break;
@@ -133403,7 +131318,7 @@
                     reg_restore(&rp->rs_un.regsave, &backpos);
                     scan = rp->rs_scan;
                 }
-                if (scan == NULL || OP(scan) != BRANCH)
+                if (scan == NULL ||  ((int)*(scan))  != BRANCH)
                 {
                     status = RA_NOMATCH;
                     regstack_pop(&scan);
@@ -133412,7 +131327,7 @@
                 {
                     rp->rs_scan = regnext(scan);
                     reg_save(&rp->rs_un.regsave, &backpos);
-                    scan = OPERAND(scan);
+                    scan =  ((scan) + 3) ;
                 }
             }
             break;
@@ -133448,7 +131363,7 @@
             regstack_pop(&scan);
             if (status == RA_NOMATCH)
             {
-                scan = OPERAND(scan);
+                scan =  ((scan) + 3) ;
                 status = RA_CONT;
             }
             break;
@@ -133481,7 +131396,6 @@
             }
             else
             {
-
                 reg_save(&(((regbehind_T *)rp) - 1)->save_after, &backpos);
 
                 (((regbehind_T *)rp) - 1)->save_behind = behind_pos;
@@ -133490,7 +131404,7 @@
                 rp->rs_state = RS_BEHIND2;
 
                 reg_restore(&rp->rs_un.regsave, &backpos);
-                scan = OPERAND(rp->rs_scan) + 4;
+                scan =  ((rp->rs_scan) + 3)  + 4;
             }
             break;
 
@@ -133515,10 +131429,10 @@
                 long limit;
 
                 no = OK;
-                limit = OPERAND_MIN(rp->rs_scan);
-                if (REG_MULTI)
+                limit =  (((long)(rp->rs_scan)[3] << 24) + ((long)(rp->rs_scan)[4] << 16)                    + ((long)(rp->rs_scan)[5] << 8) + (long)(rp->rs_scan)[6]) ;
+                if ( (rex.reg_match == NULL) )
                 {
-                    if (limit > 0 && ((rp->rs_un.regsave.rs_u.pos.lnum < behind_pos.rs_u.pos.lnum ? (colnr_T)STRLEN(rex.line) : behind_pos.rs_u.pos.col) - rp->rs_un.regsave.rs_u.pos.col >= limit))
+                    if (limit > 0 && ((rp->rs_un.regsave.rs_u.pos.lnum < behind_pos.rs_u.pos.lnum ? (colnr_T) strlen((char *)(rex.line))  : behind_pos.rs_u.pos.col) - rp->rs_un.regsave.rs_u.pos.col >= limit))
                     {
                         no = FAIL;
                     }
@@ -133532,7 +131446,7 @@
                         {
                             reg_restore(&rp->rs_un.regsave, &backpos);
                             rp->rs_un.regsave.rs_u.pos.col =
-                                                 (colnr_T)STRLEN(rex.line);
+                                                 (colnr_T) strlen((char *)(rex.line)) ;
                         }
                     }
                     else
@@ -133559,7 +131473,7 @@
                     }
                     else
                     {
-                        MB_PTR_BACK(rex.line, rp->rs_un.regsave.rs_u.ptr);
+                         rp->rs_un.regsave.rs_u.ptr -= has_mbyte ? ((*mb_head_off)(rex.line, (rp->rs_un.regsave.rs_u.ptr) - 1) + 1) : 1 ;
                         if (limit > 0 && (long)(behind_pos.rs_u.ptr - rp->rs_un.regsave.rs_u.ptr) > limit)
                         {
                             no = FAIL;
@@ -133569,7 +131483,7 @@
                 if (no == OK)
                 {
                     reg_restore(&rp->rs_un.regsave, &backpos);
-                    scan = OPERAND(rp->rs_scan) + 4;
+                    scan =  ((rp->rs_scan) + 3)  + 4;
                     if (status == RA_MATCH)
                     {
                         status = RA_NOMATCH;
@@ -133643,12 +131557,12 @@
                             }
                             else
                             {
-                                MB_PTR_BACK(rex.line, rex.input);
+                                 rex.input -= has_mbyte ? ((*mb_head_off)(rex.line, (rex.input) - 1) + 1) : 1 ;
                             }
                         }
                         else
                         {
-                            if (rst->count == rst->minval || regrepeat(OPERAND(rp->rs_scan), 1L) == 0)
+                            if (rst->count == rst->minval || regrepeat( ((rp->rs_scan) + 3) , 1L) == 0)
                             {
                                 break;
                             }
@@ -133719,7 +131633,7 @@
     }
 
     cleanup_subexpr();
-    if (REG_MULTI)
+    if ( (rex.reg_match == NULL) )
     {
         if (rex.reg_startpos[0].lnum < 0)
         {
@@ -133772,7 +131686,7 @@
         backpos.ga_growsize = BACKPOS_INITIAL * 8;
     }
 
-    if (REG_MULTI)
+    if ( (rex.reg_match == NULL) )
     {
         prog = (bt_regprog_T *)rex.reg_mmatch->regprog;
         line = reg_getline((linenr_T)0);
@@ -133849,7 +131763,7 @@
                 {
                     break;
                 }
-                MB_PTR_ADV(s);
+                 s += (*mb_ptr2len)(s) ;
             }
         }
         else
@@ -133860,7 +131774,7 @@
                 {
                     break;
                 }
-                MB_PTR_ADV(s);
+                 s += (*mb_ptr2len)(s) ;
             }
         }
         if (s == NULL)
@@ -133885,7 +131799,7 @@
         {
             c = rex.line[col];
         }
-        if (prog->regstart == NUL || prog->regstart == c || (rex.reg_ic && (((enc_utf8 && utf_fold(prog->regstart) == utf_fold(c))) || (c < 255 && prog->regstart < 255 && MB_TOLOWER(prog->regstart) == MB_TOLOWER(c)))))
+        if (prog->regstart == NUL || prog->regstart == c || (rex.reg_ic && (((enc_utf8 && utf_fold(prog->regstart) == utf_fold(c))) || (c < 255 && prog->regstart < 255 &&  vim_tolower(prog->regstart)  ==  vim_tolower(c) ))))
         {
             retval = regtry(prog, col, timed_out);
         }
@@ -133951,7 +131865,8 @@
 theend:
     if (reg_tofreelen > 400)
     {
-        VIM_CLEAR(reg_tofree);
+         vim_free(reg_tofree);
+         (reg_tofree) = NULL;
     }
     if (regstack.ga_maxlen > REGSTACK_INITIAL)
     {
@@ -133964,7 +131879,7 @@
 
     if (retval > 0)
     {
-        if (REG_MULTI)
+        if ( (rex.reg_match == NULL) )
         {
             lpos_T *start = &rex.reg_mmatch->startpos[0];
             lpos_T *end = &rex.reg_mmatch->endpos[0];
@@ -134016,13 +131931,13 @@
     static int
 re_num_cmp(long_u val, char_u *scan)
 {
-    long_u  n = OPERAND_MIN(scan);
+    long_u  n =  (((long)(scan)[3] << 24) + ((long)(scan)[4] << 16)                    + ((long)(scan)[5] << 8) + (long)(scan)[6]) ;
 
-    if (OPERAND_CMP(scan) == '>')
+    if ( (scan)[7]  == '>')
     {
         return val > n;
     }
-    if (OPERAND_CMP(scan) == '<')
+    if ( (scan)[7]  == '<')
     {
         return val < n;
     }
@@ -134039,9 +131954,9 @@
 
 // ==================== regexp_nfa.c ====================
 
-#define NFA_ADD_NL              31
+enum { NFA_ADD_NL = 31 };
 
-#define NFA_DELIM_NL            4
+enum { NFA_DELIM_NL = 4 };
 
 enum
 {
@@ -134250,8 +132165,6 @@
 static int match_follows(nfa_state_T *startstate, int depth);
 static int failure_chance(nfa_state_T *state, int depth);
 
-#define EMIT(c) do {                                                if (post_ptr >= post_end && realloc_post_list() == FAIL)                    return FAIL;                                *post_ptr++ = c;                            } while (0)
-
     static int
 nfa_regcomp_start(char_u      *expr, int         re_flags)
 {
@@ -134260,7 +132173,7 @@
 
     nstate = 0;
     istate = 0;
-    nstate_max = (int)(STRLEN(expr) + 1) * 25;
+    nstate_max = (int)( strlen((char *)(expr))  + 1) * 25;
 
     nstate_max += 1000;
 
@@ -134418,7 +132331,7 @@
     p = p->out;
     while (p->c > 0)
     {
-        len += MB_CHAR2LEN(p->c);
+        len +=  (has_mbyte ? mb_char2len(p->c) : 1) ;
         p = p->out;
     }
     if (p->c != NFA_MCLOSE || p->out->c != NFA_MATCH)
@@ -134459,12 +132372,12 @@
     int   *old_start;
 
     new_max = nstate_max * 3 / 2;
-    new_start = ALLOC_MULT(int, new_max);
+    new_start =  (int *)alloc(sizeof(int) * (new_max)) ;
     if (new_start == NULL)
     {
         return FAIL;
     }
-    mch_memmove(new_start, post_start, nstate_max * sizeof(int));
+     memmove((char *)(new_start), (char *)(post_start), nstate_max * sizeof(int)) ;
     old_start = post_start;
     post_start = new_start;
     post_ptr = new_start + (post_ptr - old_start);
@@ -134476,15 +132389,6 @@
     static int
 nfa_recognize_char_class(char_u *start, char_u *end, int extra_newl)
 {
-#define CLASS_not               0x80
-#define CLASS_af                0x40
-#define CLASS_AF                0x20
-#define CLASS_az                0x10
-#define CLASS_AZ                0x08
-#define CLASS_o7                0x04
-#define CLASS_o9                0x02
-#define CLASS_underscore        0x01
-
     int         newl = FALSE;
     char_u      *p;
     int         config = 0;
@@ -134627,65 +132531,8 @@
     static int
 nfa_emit_equi_class(int c)
 {
-#define EMIT2(c)    EMIT(c); EMIT(NFA_CONCAT);
-
-    if (enc_utf8 || STRCMP(p_enc, "latin1") == 0 || STRCMP(p_enc, "iso-8859-15") == 0)
+    if (enc_utf8 ||  strcmp((char *)(p_enc), (char *)("latin1"))  == 0 ||  strcmp((char *)(p_enc), (char *)("iso-8859-15"))  == 0)
     {
-#define A_grave 0xc0
-#define A_acute 0xc1
-#define A_circumflex 0xc2
-#define A_virguilla 0xc3
-#define A_diaeresis 0xc4
-#define A_ring 0xc5
-#define C_cedilla 0xc7
-#define E_grave 0xc8
-#define E_acute 0xc9
-#define E_circumflex 0xca
-#define E_diaeresis 0xcb
-#define I_grave 0xcc
-#define I_acute 0xcd
-#define I_circumflex 0xce
-#define I_diaeresis 0xcf
-#define N_virguilla 0xd1
-#define O_grave 0xd2
-#define O_acute 0xd3
-#define O_circumflex 0xd4
-#define O_virguilla 0xd5
-#define O_diaeresis 0xd6
-#define O_slash 0xd8
-#define U_grave 0xd9
-#define U_acute 0xda
-#define U_circumflex 0xdb
-#define U_diaeresis 0xdc
-#define Y_acute 0xdd
-#define a_grave 0xe0
-#define a_acute 0xe1
-#define a_circumflex 0xe2
-#define a_virguilla 0xe3
-#define a_diaeresis 0xe4
-#define a_ring 0xe5
-#define c_cedilla 0xe7
-#define e_grave 0xe8
-#define e_acute 0xe9
-#define e_circumflex 0xea
-#define e_diaeresis 0xeb
-#define i_grave 0xec
-#define i_acute 0xed
-#define i_circumflex 0xee
-#define i_diaeresis 0xef
-#define n_virguilla 0xf1
-#define o_grave 0xf2
-#define o_acute 0xf3
-#define o_circumflex 0xf4
-#define o_virguilla 0xf5
-#define o_diaeresis 0xf6
-#define o_slash 0xf8
-#define u_grave 0xf9
-#define u_acute 0xfa
-#define u_circumflex 0xfb
-#define u_diaeresis 0xfc
-#define y_acute 0xfd
-#define y_diaeresis 0xff
         switch (c)
         {
             case 'A':
@@ -134719,17 +132566,316 @@
                 case 0x1eb2:
                     case 0x1eb4:
                         case 0x1eb6:
-                    EMIT2('A') EMIT2(A_grave) EMIT2(A_acute)
-                    EMIT2(A_circumflex) EMIT2(A_virguilla)
-                    EMIT2(A_diaeresis) EMIT2(A_ring)
-                    EMIT2(0x100) EMIT2(0x102) EMIT2(0x104)
-                    EMIT2(0x1cd) EMIT2(0x1de) EMIT2(0x1e0)
-                    EMIT2(0x1fa) EMIT2(0x200) EMIT2(0x202)
-                    EMIT2(0x226) EMIT2(0x23a) EMIT2(0x1e00)
-                    EMIT2(0x1ea0) EMIT2(0x1ea2) EMIT2(0x1ea4)
-                    EMIT2(0x1ea6) EMIT2(0x1ea8) EMIT2(0x1eaa)
-                    EMIT2(0x1eac) EMIT2(0x1eae) EMIT2(0x1eb0)
-                    EMIT2(0x1eb2) EMIT2(0x1eb6) EMIT2(0x1eb4)
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 'A';
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = A_grave;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = A_acute;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = A_circumflex;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = A_virguilla;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = A_diaeresis;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = A_ring;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x100;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x102;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x104;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1cd;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1de;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e0;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1fa;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x200;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x202;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x226;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x23a;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e00;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1ea0;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1ea2;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1ea4;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1ea6;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1ea8;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1eaa;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1eac;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1eae;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1eb0;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1eb2;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1eb6;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1eb4;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
                     return OK;
 
             case 'B':
@@ -134738,9 +132884,66 @@
                         case 0x1e02:
             case 0x1e04:
                 case 0x1e06:
-                    EMIT2('B')
-                    EMIT2(0x181) EMIT2(0x243) EMIT2(0x1e02)
-                    EMIT2(0x1e04) EMIT2(0x1e06)
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 'B';
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x181;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x243;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e02;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e04;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e06;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
                     return OK;
 
             case 'C':
@@ -134753,10 +132956,106 @@
                         case 0x23b:
             case 0x1e08:
                 case 0xa792:
-                    EMIT2('C') EMIT2(C_cedilla)
-                    EMIT2(0x106) EMIT2(0x108) EMIT2(0x10a)
-                    EMIT2(0x10c) EMIT2(0x187) EMIT2(0x23b)
-                    EMIT2(0x1e08) EMIT2(0xa792)
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 'C';
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = C_cedilla;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x106;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x108;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x10a;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x10c;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x187;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x23b;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e08;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0xa792;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
                     return OK;
 
             case 'D':
@@ -134768,9 +133067,96 @@
                     case 0x1e0e:
                         case 0x1e10:
             case 0x1e12:
-                    EMIT2('D') EMIT2(0x10e) EMIT2(0x110) EMIT2(0x18a)
-                    EMIT2(0x1e0a) EMIT2(0x1e0c) EMIT2(0x1e0e)
-                    EMIT2(0x1e10) EMIT2(0x1e12)
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 'D';
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x10e;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x110;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x18a;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e0a;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e0c;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e0e;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e10;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e12;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
                     return OK;
 
             case 'E':
@@ -134800,23 +133186,322 @@
             case 0x1ec2:
                 case 0x1ec4:
                     case 0x1ec6:
-                    EMIT2('E') EMIT2(E_grave) EMIT2(E_acute)
-                    EMIT2(E_circumflex) EMIT2(E_diaeresis)
-                    EMIT2(0x112) EMIT2(0x114) EMIT2(0x116)
-                    EMIT2(0x118) EMIT2(0x11a) EMIT2(0x204)
-                    EMIT2(0x206) EMIT2(0x228) EMIT2(0x246)
-                    EMIT2(0x1e14) EMIT2(0x1e16) EMIT2(0x1e18)
-                    EMIT2(0x1e1a) EMIT2(0x1e1c) EMIT2(0x1eb8)
-                    EMIT2(0x1eba) EMIT2(0x1ebc) EMIT2(0x1ebe)
-                    EMIT2(0x1ec0) EMIT2(0x1ec2) EMIT2(0x1ec4)
-                    EMIT2(0x1ec6)
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 'E';
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = E_grave;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = E_acute;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = E_circumflex;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = E_diaeresis;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x112;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x114;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x116;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x118;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x11a;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x204;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x206;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x228;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x246;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e14;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e16;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e18;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e1a;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e1c;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1eb8;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1eba;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1ebc;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1ebe;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1ec0;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1ec2;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1ec4;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1ec6;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
                     return OK;
 
             case 'F':
                 case 0x191:
                     case 0x1e1e:
                         case 0xa798:
-                    EMIT2('F') EMIT2(0x191) EMIT2(0x1e1e) EMIT2(0xa798)
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 'F';
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x191;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e1e;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0xa798;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
                     return OK;
 
             case 'G':
@@ -134830,10 +133515,116 @@
             case 0x1f4:
                 case 0x1e20:
                     case 0xa7a0:
-                    EMIT2('G') EMIT2(0x11c) EMIT2(0x11e) EMIT2(0x120)
-                    EMIT2(0x122) EMIT2(0x193) EMIT2(0x1e4)
-                    EMIT2(0x1e6) EMIT2(0x1f4) EMIT2(0x1e20)
-                    EMIT2(0xa7a0)
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 'G';
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x11c;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x11e;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x120;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x122;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x193;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e4;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e6;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1f4;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e20;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0xa7a0;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
                     return OK;
 
             case 'H':
@@ -134846,9 +133637,106 @@
                         case 0x1e28:
             case 0x1e2a:
                 case 0x2c67:
-                    EMIT2('H') EMIT2(0x124) EMIT2(0x126) EMIT2(0x21e)
-                    EMIT2(0x1e22) EMIT2(0x1e24) EMIT2(0x1e26)
-                    EMIT2(0x1e28) EMIT2(0x1e2a) EMIT2(0x2c67)
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 'H';
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x124;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x126;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x21e;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e22;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e24;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e26;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e28;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e2a;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x2c67;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
                     return OK;
 
             case 'I':
@@ -134869,19 +133757,221 @@
                         case 0x1e2e:
             case 0x1ec8:
                 case 0x1eca:
-                    EMIT2('I') EMIT2(I_grave) EMIT2(I_acute)
-                    EMIT2(I_circumflex) EMIT2(I_diaeresis)
-                    EMIT2(0x128) EMIT2(0x12a) EMIT2(0x12c)
-                    EMIT2(0x12e) EMIT2(0x130) EMIT2(0x197)
-                    EMIT2(0x1cf) EMIT2(0x208) EMIT2(0x20a)
-                    EMIT2(0x1e2c) EMIT2(0x1e2e) EMIT2(0x1ec8)
-                    EMIT2(0x1eca)
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 'I';
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = I_grave;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = I_acute;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = I_circumflex;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = I_diaeresis;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x128;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x12a;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x12c;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x12e;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x130;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x197;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1cf;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x208;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x20a;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e2c;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e2e;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1ec8;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1eca;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
                     return OK;
 
             case 'J':
                 case 0x134:
                     case 0x248:
-                    EMIT2('J') EMIT2(0x134) EMIT2(0x248)
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 'J';
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x134;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x248;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
                     return OK;
 
             case 'K':
@@ -134893,9 +133983,96 @@
                 case 0x1e34:
                     case 0x2c69:
                         case 0xa740:
-                    EMIT2('K') EMIT2(0x136) EMIT2(0x198) EMIT2(0x1e8)
-                    EMIT2(0x1e30) EMIT2(0x1e32) EMIT2(0x1e34)
-                    EMIT2(0x2c69) EMIT2(0xa740)
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 'K';
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x136;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x198;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e8;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e30;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e32;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e34;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x2c69;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0xa740;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
                     return OK;
 
             case 'L':
@@ -134910,18 +134087,172 @@
                 case 0x1e3a:
                     case 0x1e3c:
                         case 0x2c60:
-                    EMIT2('L') EMIT2(0x139) EMIT2(0x13b)
-                    EMIT2(0x13d) EMIT2(0x13f) EMIT2(0x141)
-                    EMIT2(0x23d) EMIT2(0x1e36) EMIT2(0x1e38)
-                    EMIT2(0x1e3a) EMIT2(0x1e3c) EMIT2(0x2c60)
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 'L';
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x139;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x13b;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x13d;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x13f;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x141;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x23d;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e36;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e38;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e3a;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e3c;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x2c60;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
                     return OK;
 
             case 'M':
                 case 0x1e3e:
                     case 0x1e40:
                         case 0x1e42:
-                    EMIT2('M') EMIT2(0x1e3e) EMIT2(0x1e40)
-                    EMIT2(0x1e42)
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 'M';
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e3e;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e40;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e42;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
                     return OK;
 
             case 'N':
@@ -134935,10 +134266,116 @@
                     case 0x1e48:
                         case 0x1e4a:
             case 0xa7a4:
-                    EMIT2('N') EMIT2(N_virguilla)
-                    EMIT2(0x143) EMIT2(0x145) EMIT2(0x147)
-                    EMIT2(0x1f8) EMIT2(0x1e44) EMIT2(0x1e46)
-                    EMIT2(0x1e48) EMIT2(0x1e4a) EMIT2(0xa7a4)
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 'N';
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = N_virguilla;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x143;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x145;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x147;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1f8;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e44;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e46;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e48;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e4a;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0xa7a4;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
                     return OK;
 
             case 'O':
@@ -134979,20 +134416,386 @@
             case 0x1ede:
                 case 0x1ee0:
                     case 0x1ee2:
-                    EMIT2('O') EMIT2(O_grave) EMIT2(O_acute)
-                    EMIT2(O_circumflex) EMIT2(O_virguilla)
-                    EMIT2(O_diaeresis) EMIT2(O_slash)
-                    EMIT2(0x14c) EMIT2(0x14e) EMIT2(0x150)
-                    EMIT2(0x19f) EMIT2(0x1a0) EMIT2(0x1d1)
-                    EMIT2(0x1ea) EMIT2(0x1ec) EMIT2(0x1fe)
-                    EMIT2(0x20c) EMIT2(0x20e) EMIT2(0x22a)
-                    EMIT2(0x22c) EMIT2(0x22e) EMIT2(0x230)
-                    EMIT2(0x1e4c) EMIT2(0x1e4e) EMIT2(0x1e50)
-                    EMIT2(0x1e52) EMIT2(0x1ecc) EMIT2(0x1ece)
-                    EMIT2(0x1ed0) EMIT2(0x1ed2) EMIT2(0x1ed4)
-                    EMIT2(0x1ed6) EMIT2(0x1ed8) EMIT2(0x1eda)
-                    EMIT2(0x1edc) EMIT2(0x1ede) EMIT2(0x1ee0)
-                    EMIT2(0x1ee2)
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 'O';
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = O_grave;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = O_acute;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = O_circumflex;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = O_virguilla;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = O_diaeresis;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = O_slash;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x14c;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x14e;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x150;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x19f;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1a0;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1d1;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1ea;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1ec;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1fe;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x20c;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x20e;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x22a;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x22c;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x22e;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x230;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e4c;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e4e;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e50;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e52;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1ecc;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1ece;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1ed0;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1ed2;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1ed4;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1ed6;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1ed8;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1eda;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1edc;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1ede;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1ee0;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1ee2;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
                     return OK;
 
             case 'P':
@@ -135000,13 +134803,80 @@
                     case 0x1e54:
                         case 0x1e56:
                             case 0x2c63:
-                    EMIT2('P') EMIT2(0x1a4) EMIT2(0x1e54) EMIT2(0x1e56)
-                    EMIT2(0x2c63)
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 'P';
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1a4;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e54;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e56;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x2c63;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
                     return OK;
 
             case 'Q':
                 case 0x24a:
-                    EMIT2('Q') EMIT2(0x24a)
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 'Q';
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x24a;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
                     return OK;
 
             case 'R':
@@ -135022,10 +134892,136 @@
                 case 0x1e5e:
                     case 0x2c64:
                         case 0xa7a6:
-                    EMIT2('R') EMIT2(0x154) EMIT2(0x156) EMIT2(0x158)
-                    EMIT2(0x210) EMIT2(0x212) EMIT2(0x24c) EMIT2(0x1e58)
-                    EMIT2(0x1e5a) EMIT2(0x1e5c) EMIT2(0x1e5e) EMIT2(0x2c64)
-                    EMIT2(0xa7a6)
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 'R';
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x154;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x156;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x158;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x210;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x212;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x24c;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e58;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e5a;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e5c;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e5e;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x2c64;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0xa7a6;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
                     return OK;
 
             case 'S':
@@ -135041,10 +135037,136 @@
                 case 0x1e68:
                     case 0x2c7e:
                         case 0xa7a8:
-                    EMIT2('S') EMIT2(0x15a) EMIT2(0x15c) EMIT2(0x15e)
-                    EMIT2(0x160) EMIT2(0x218) EMIT2(0x1e60) EMIT2(0x1e62)
-                    EMIT2(0x1e64) EMIT2(0x1e66) EMIT2(0x1e68) EMIT2(0x2c7e)
-                    EMIT2(0xa7a8)
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 'S';
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x15a;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x15c;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x15e;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x160;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x218;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e60;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e62;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e64;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e66;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e68;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x2c7e;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0xa7a8;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
                     return OK;
 
             case 'T':
@@ -135059,9 +135181,126 @@
                             case 0x1e6c:
             case 0x1e6e:
                 case 0x1e70:
-                    EMIT2('T') EMIT2(0x162) EMIT2(0x164) EMIT2(0x166)
-                    EMIT2(0x1ac) EMIT2(0x1ae) EMIT2(0x23e) EMIT2(0x21a)
-                    EMIT2(0x1e6a) EMIT2(0x1e6c) EMIT2(0x1e6e) EMIT2(0x1e70)
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 'T';
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x162;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x164;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x166;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1ac;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1ae;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x23e;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x21a;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e6a;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e6c;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e6e;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e70;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
                     return OK;
 
             case 'U':
@@ -135096,25 +135335,372 @@
                 case 0x1eec:
                     case 0x1eee:
                         case 0x1ef0:
-                    EMIT2('U') EMIT2(U_grave) EMIT2(U_acute)
-                    EMIT2(U_diaeresis) EMIT2(U_circumflex)
-                    EMIT2(0x168) EMIT2(0x16a)
-                    EMIT2(0x16c) EMIT2(0x16e) EMIT2(0x170)
-                    EMIT2(0x172) EMIT2(0x1af) EMIT2(0x1d3)
-                    EMIT2(0x1d5) EMIT2(0x1d7) EMIT2(0x1d9)
-                    EMIT2(0x1db) EMIT2(0x214) EMIT2(0x216)
-                    EMIT2(0x244) EMIT2(0x1e72) EMIT2(0x1e74)
-                    EMIT2(0x1e76) EMIT2(0x1e78) EMIT2(0x1e7a)
-                    EMIT2(0x1ee4) EMIT2(0x1ee6) EMIT2(0x1ee8)
-                    EMIT2(0x1eea) EMIT2(0x1eec) EMIT2(0x1eee)
-                    EMIT2(0x1ef0)
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 'U';
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = U_grave;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = U_acute;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = U_diaeresis;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = U_circumflex;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x168;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x16a;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x16c;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x16e;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x170;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x172;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1af;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1d3;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1d5;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1d7;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1d9;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1db;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x214;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x216;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x244;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e72;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e74;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e76;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e78;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e7a;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1ee4;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1ee6;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1ee8;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1eea;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1eec;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1eee;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1ef0;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
                     return OK;
 
             case 'V':
                 case 0x1b2:
                     case 0x1e7c:
                         case 0x1e7e:
-                    EMIT2('V') EMIT2(0x1b2) EMIT2(0x1e7c) EMIT2(0x1e7e)
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 'V';
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1b2;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e7c;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e7e;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
                     return OK;
 
             case 'W':
@@ -135124,14 +135710,111 @@
                             case 0x1e84:
             case 0x1e86:
                 case 0x1e88:
-                    EMIT2('W') EMIT2(0x174) EMIT2(0x1e80) EMIT2(0x1e82)
-                    EMIT2(0x1e84) EMIT2(0x1e86) EMIT2(0x1e88)
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 'W';
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x174;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e80;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e82;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e84;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e86;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e88;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
                     return OK;
 
             case 'X':
                 case 0x1e8a:
                     case 0x1e8c:
-                    EMIT2('X') EMIT2(0x1e8a) EMIT2(0x1e8c)
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 'X';
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e8a;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e8c;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
                     return OK;
 
             case 'Y':
@@ -135146,11 +135829,126 @@
                 case 0x1ef4:
                     case 0x1ef6:
                         case 0x1ef8:
-                    EMIT2('Y') EMIT2(Y_acute)
-                    EMIT2(0x176) EMIT2(0x178) EMIT2(0x1b3)
-                    EMIT2(0x232) EMIT2(0x24e) EMIT2(0x1e8e)
-                    EMIT2(0x1ef2) EMIT2(0x1ef4) EMIT2(0x1ef6)
-                    EMIT2(0x1ef8)
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 'Y';
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = Y_acute;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x176;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x178;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1b3;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x232;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x24e;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e8e;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1ef2;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1ef4;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1ef6;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1ef8;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
                     return OK;
 
             case 'Z':
@@ -135162,9 +135960,96 @@
                     case 0x1e92:
                         case 0x1e94:
             case 0x2c6b:
-                    EMIT2('Z') EMIT2(0x179) EMIT2(0x17b) EMIT2(0x17d)
-                    EMIT2(0x1b5) EMIT2(0x1e90) EMIT2(0x1e92)
-                    EMIT2(0x1e94) EMIT2(0x2c6b)
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 'Z';
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x179;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x17b;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x17d;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1b5;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e90;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e92;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e94;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x2c6b;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
                     return OK;
 
             case 'a':
@@ -135200,18 +136085,336 @@
                         case 0x1eb5:
             case 0x1eb7:
                 case 0x2c65:
-                    EMIT2('a') EMIT2(a_grave) EMIT2(a_acute)
-                    EMIT2(a_circumflex) EMIT2(a_virguilla)
-                    EMIT2(a_diaeresis) EMIT2(a_ring)
-                    EMIT2(0x101) EMIT2(0x103) EMIT2(0x105)
-                    EMIT2(0x1ce) EMIT2(0x1df) EMIT2(0x1e1)
-                    EMIT2(0x1fb) EMIT2(0x201) EMIT2(0x203)
-                    EMIT2(0x227) EMIT2(0x1d8f) EMIT2(0x1e01)
-                    EMIT2(0x1e9a) EMIT2(0x1ea1) EMIT2(0x1ea3)
-                    EMIT2(0x1ea5) EMIT2(0x1ea7) EMIT2(0x1ea9)
-                    EMIT2(0x1eab) EMIT2(0x1ead) EMIT2(0x1eaf)
-                    EMIT2(0x1eb1) EMIT2(0x1eb3) EMIT2(0x1eb5)
-                    EMIT2(0x1eb7) EMIT2(0x2c65)
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 'a';
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = a_grave;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = a_acute;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = a_circumflex;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = a_virguilla;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = a_diaeresis;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = a_ring;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x101;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x103;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x105;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1ce;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1df;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e1;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1fb;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x201;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x203;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x227;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1d8f;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e01;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e9a;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1ea1;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1ea3;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1ea5;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1ea7;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1ea9;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1eab;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1ead;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1eaf;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1eb1;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1eb3;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1eb5;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1eb7;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x2c65;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
                     return OK;
 
             case 'b':
@@ -135222,8 +136425,86 @@
             case 0x1e03:
                 case 0x1e05:
                     case 0x1e07:
-                    EMIT2('b') EMIT2(0x180) EMIT2(0x253) EMIT2(0x1d6c)
-                    EMIT2(0x1d80) EMIT2(0x1e03) EMIT2(0x1e05) EMIT2(0x1e07)
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 'b';
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x180;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x253;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1d6c;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1d80;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e03;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e05;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e07;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
                     return OK;
 
             case 'c':
@@ -135237,10 +136518,116 @@
                         case 0x1e09:
                             case 0xa793:
             case 0xa794:
-                    EMIT2('c') EMIT2(c_cedilla)
-                    EMIT2(0x107) EMIT2(0x109) EMIT2(0x10b)
-                    EMIT2(0x10d) EMIT2(0x188) EMIT2(0x23c)
-                    EMIT2(0x1e09) EMIT2(0xa793) EMIT2(0xa794)
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 'c';
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = c_cedilla;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x107;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x109;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x10b;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x10d;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x188;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x23c;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e09;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0xa793;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0xa794;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
                     return OK;
 
             case 'd':
@@ -135255,10 +136642,126 @@
                             case 0x1e0f:
             case 0x1e11:
                 case 0x1e13:
-                    EMIT2('d') EMIT2(0x10f) EMIT2(0x111)
-                    EMIT2(0x257) EMIT2(0x1d6d) EMIT2(0x1d81)
-                    EMIT2(0x1d91) EMIT2(0x1e0b) EMIT2(0x1e0d)
-                    EMIT2(0x1e0f) EMIT2(0x1e11) EMIT2(0x1e13)
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 'd';
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x10f;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x111;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x257;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1d6d;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1d81;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1d91;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e0b;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e0d;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e0f;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e11;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e13;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
                     return OK;
 
             case 'e':
@@ -135289,16 +136792,286 @@
                 case 0x1ec3:
                     case 0x1ec5:
                         case 0x1ec7:
-                    EMIT2('e') EMIT2(e_grave) EMIT2(e_acute)
-                    EMIT2(e_circumflex) EMIT2(e_diaeresis)
-                    EMIT2(0x113) EMIT2(0x115)
-                    EMIT2(0x117) EMIT2(0x119) EMIT2(0x11b)
-                    EMIT2(0x205) EMIT2(0x207) EMIT2(0x229)
-                    EMIT2(0x247) EMIT2(0x1d92) EMIT2(0x1e15)
-                    EMIT2(0x1e17) EMIT2(0x1e19) EMIT2(0x1e1b)
-                    EMIT2(0x1e1d) EMIT2(0x1eb9) EMIT2(0x1ebb)
-                    EMIT2(0x1ebd) EMIT2(0x1ebf) EMIT2(0x1ec1)
-                    EMIT2(0x1ec3) EMIT2(0x1ec5) EMIT2(0x1ec7)
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 'e';
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = e_grave;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = e_acute;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = e_circumflex;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = e_diaeresis;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x113;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x115;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x117;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x119;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x11b;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x205;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x207;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x229;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x247;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1d92;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e15;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e17;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e19;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e1b;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e1d;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1eb9;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1ebb;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1ebd;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1ebf;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1ec1;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1ec3;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1ec5;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1ec7;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
                     return OK;
 
             case 'f':
@@ -135307,8 +137080,66 @@
                         case 0x1d82:
             case 0x1e1f:
                 case 0xa799:
-                    EMIT2('f') EMIT2(0x192) EMIT2(0x1d6e) EMIT2(0x1d82)
-                    EMIT2(0x1e1f) EMIT2(0xa799)
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 'f';
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x192;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1d6e;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1d82;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e1f;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0xa799;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
                     return OK;
 
             case 'g':
@@ -135323,10 +137154,126 @@
                             case 0x1d83:
             case 0x1e21:
                 case 0xa7a1:
-                    EMIT2('g') EMIT2(0x11d) EMIT2(0x11f) EMIT2(0x121)
-                    EMIT2(0x123) EMIT2(0x1e5) EMIT2(0x1e7)
-                    EMIT2(0x1f5) EMIT2(0x260) EMIT2(0x1d83)
-                    EMIT2(0x1e21) EMIT2(0xa7a1)
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 'g';
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x11d;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x11f;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x121;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x123;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e5;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e7;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1f5;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x260;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1d83;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e21;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0xa7a1;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
                     return OK;
 
             case 'h':
@@ -135341,10 +137288,126 @@
             case 0x1e96:
                 case 0x2c68:
                     case 0xa795:
-                    EMIT2('h') EMIT2(0x125) EMIT2(0x127) EMIT2(0x21f)
-                    EMIT2(0x1e23) EMIT2(0x1e25) EMIT2(0x1e27)
-                    EMIT2(0x1e29) EMIT2(0x1e2b) EMIT2(0x1e96)
-                    EMIT2(0x2c68) EMIT2(0xa795)
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 'h';
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x125;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x127;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x21f;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e23;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e25;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e27;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e29;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e2b;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e96;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x2c68;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0xa795;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
                     return OK;
 
             case 'i':
@@ -135365,20 +137428,232 @@
                         case 0x1e2f:
             case 0x1ec9:
                 case 0x1ecb:
-                    EMIT2('i') EMIT2(i_grave) EMIT2(i_acute)
-                    EMIT2(i_circumflex) EMIT2(i_diaeresis)
-                    EMIT2(0x129) EMIT2(0x12b) EMIT2(0x12d)
-                    EMIT2(0x12f) EMIT2(0x1d0) EMIT2(0x209)
-                    EMIT2(0x20b) EMIT2(0x268) EMIT2(0x1d96)
-                    EMIT2(0x1e2d) EMIT2(0x1e2f) EMIT2(0x1ec9)
-                    EMIT2(0x1ecb)
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 'i';
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = i_grave;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = i_acute;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = i_circumflex;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = i_diaeresis;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x129;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x12b;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x12d;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x12f;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1d0;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x209;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x20b;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x268;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1d96;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e2d;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e2f;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1ec9;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1ecb;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
                     return OK;
 
             case 'j':
                 case 0x135:
                     case 0x1f0:
                         case 0x249:
-                    EMIT2('j') EMIT2(0x135) EMIT2(0x1f0) EMIT2(0x249)
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 'j';
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x135;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1f0;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x249;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
                     return OK;
 
             case 'k':
@@ -135391,9 +137666,106 @@
                     case 0x1e35:
                         case 0x2c6a:
                             case 0xa741:
-                    EMIT2('k') EMIT2(0x137) EMIT2(0x199) EMIT2(0x1e9)
-                    EMIT2(0x1d84) EMIT2(0x1e31) EMIT2(0x1e33)
-                    EMIT2(0x1e35) EMIT2(0x2c6a) EMIT2(0xa741)
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 'k';
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x137;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x199;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e9;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1d84;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e31;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e33;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e35;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x2c6a;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0xa741;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
                     return OK;
 
             case 'l':
@@ -135408,10 +137780,126 @@
                             case 0x1e3b:
             case 0x1e3d:
                 case 0x2c61:
-                    EMIT2('l') EMIT2(0x13a) EMIT2(0x13c)
-                    EMIT2(0x13e) EMIT2(0x140) EMIT2(0x142)
-                    EMIT2(0x19a) EMIT2(0x1e37) EMIT2(0x1e39)
-                    EMIT2(0x1e3b) EMIT2(0x1e3d) EMIT2(0x2c61)
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 'l';
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x13a;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x13c;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x13e;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x140;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x142;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x19a;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e37;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e39;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e3b;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e3d;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x2c61;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
                     return OK;
 
             case 'm':
@@ -135419,8 +137907,56 @@
                     case 0x1e3f:
                         case 0x1e41:
                             case 0x1e43:
-                    EMIT2('m') EMIT2(0x1d6f) EMIT2(0x1e3f)
-                    EMIT2(0x1e41) EMIT2(0x1e43)
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 'm';
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1d6f;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e3f;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e41;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e43;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
                     return OK;
 
             case 'n':
@@ -135437,11 +137973,146 @@
                 case 0x1e49:
                     case 0x1e4b:
                         case 0xa7a5:
-                    EMIT2('n') EMIT2(n_virguilla)
-                    EMIT2(0x144) EMIT2(0x146) EMIT2(0x148)
-                    EMIT2(0x149) EMIT2(0x1f9) EMIT2(0x1d70)
-                    EMIT2(0x1d87) EMIT2(0x1e45) EMIT2(0x1e47)
-                    EMIT2(0x1e49) EMIT2(0x1e4b) EMIT2(0xa7a5)
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 'n';
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = n_virguilla;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x144;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x146;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x148;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x149;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1f9;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1d70;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1d87;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e45;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e47;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e49;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e4b;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0xa7a5;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
                     return OK;
 
             case 'o':
@@ -135482,20 +138153,386 @@
             case 0x1edf:
                 case 0x1ee1:
                     case 0x1ee3:
-                    EMIT2('o') EMIT2(o_grave) EMIT2(o_acute)
-                    EMIT2(o_circumflex) EMIT2(o_virguilla)
-                    EMIT2(o_diaeresis) EMIT2(o_slash)
-                    EMIT2(0x14d) EMIT2(0x14f) EMIT2(0x151)
-                    EMIT2(0x1a1) EMIT2(0x1d2) EMIT2(0x1eb)
-                    EMIT2(0x1ed) EMIT2(0x1ff) EMIT2(0x20d)
-                    EMIT2(0x20f) EMIT2(0x22b) EMIT2(0x22d)
-                    EMIT2(0x22f) EMIT2(0x231) EMIT2(0x275)
-                    EMIT2(0x1e4d) EMIT2(0x1e4f) EMIT2(0x1e51)
-                    EMIT2(0x1e53) EMIT2(0x1ecd) EMIT2(0x1ecf)
-                    EMIT2(0x1ed1) EMIT2(0x1ed3) EMIT2(0x1ed5)
-                    EMIT2(0x1ed7) EMIT2(0x1ed9) EMIT2(0x1edb)
-                    EMIT2(0x1edd) EMIT2(0x1edf) EMIT2(0x1ee1)
-                    EMIT2(0x1ee3)
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 'o';
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = o_grave;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = o_acute;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = o_circumflex;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = o_virguilla;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = o_diaeresis;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = o_slash;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x14d;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x14f;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x151;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1a1;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1d2;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1eb;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1ed;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1ff;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x20d;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x20f;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x22b;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x22d;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x22f;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x231;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x275;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e4d;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e4f;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e51;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e53;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1ecd;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1ecf;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1ed1;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1ed3;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1ed5;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1ed7;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1ed9;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1edb;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1edd;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1edf;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1ee1;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1ee3;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
                     return OK;
 
             case 'p':
@@ -135505,14 +138542,111 @@
                             case 0x1d88:
             case 0x1e55:
                 case 0x1e57:
-                    EMIT2('p') EMIT2(0x1a5) EMIT2(0x1d71) EMIT2(0x1d7d)
-                    EMIT2(0x1d88) EMIT2(0x1e55) EMIT2(0x1e57)
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 'p';
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1a5;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1d71;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1d7d;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1d88;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e55;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e57;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
                     return OK;
 
             case 'q':
                 case 0x24b:
                     case 0x2a0:
-                    EMIT2('q') EMIT2(0x24b) EMIT2(0x2a0)
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 'q';
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x24b;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x2a0;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
                     return OK;
 
             case 'r':
@@ -135531,10 +138665,166 @@
                         case 0x1e5d:
                             case 0x1e5f:
             case 0xa7a7:
-                    EMIT2('r') EMIT2(0x155) EMIT2(0x157) EMIT2(0x159)
-                    EMIT2(0x211) EMIT2(0x213) EMIT2(0x24d) EMIT2(0x27d)
-                    EMIT2(0x1d72) EMIT2(0x1d73) EMIT2(0x1d89) EMIT2(0x1e59)
-                    EMIT2(0x1e5b) EMIT2(0x1e5d) EMIT2(0x1e5f) EMIT2(0xa7a7)
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 'r';
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x155;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x157;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x159;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x211;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x213;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x24d;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x27d;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1d72;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1d73;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1d89;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e59;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e5b;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e5d;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e5f;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0xa7a7;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
                     return OK;
 
             case 's':
@@ -135552,10 +138842,156 @@
                     case 0x1e67:
                         case 0x1e69:
                             case 0xa7a9:
-                    EMIT2('s') EMIT2(0x15b) EMIT2(0x15d) EMIT2(0x15f)
-                    EMIT2(0x161) EMIT2(0x219) EMIT2(0x23f) EMIT2(0x1d74)
-                    EMIT2(0x1d8a) EMIT2(0x1e61) EMIT2(0x1e63) EMIT2(0x1e65)
-                    EMIT2(0x1e67) EMIT2(0x1e69) EMIT2(0xa7a9)
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 's';
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x15b;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x15d;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x15f;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x161;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x219;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x23f;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1d74;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1d8a;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e61;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e63;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e65;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e67;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e69;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0xa7a9;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
                     return OK;
 
             case 't':
@@ -135573,10 +139009,156 @@
                     case 0x1e71:
                         case 0x1e97:
                             case 0x2c66:
-                    EMIT2('t') EMIT2(0x163) EMIT2(0x165) EMIT2(0x167)
-                    EMIT2(0x1ab) EMIT2(0x1ad) EMIT2(0x21b) EMIT2(0x288)
-                    EMIT2(0x1d75) EMIT2(0x1e6b) EMIT2(0x1e6d) EMIT2(0x1e6f)
-                    EMIT2(0x1e71) EMIT2(0x1e97) EMIT2(0x2c66)
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 't';
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x163;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x165;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x167;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1ab;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1ad;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x21b;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x288;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1d75;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e6b;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e6d;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e6f;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e71;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e97;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x2c66;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
                     return OK;
 
             case 'u':
@@ -135613,18 +139195,346 @@
             case 0x1eed:
                 case 0x1eef:
                     case 0x1ef1:
-                    EMIT2('u') EMIT2(u_grave) EMIT2(u_acute)
-                    EMIT2(u_circumflex) EMIT2(u_diaeresis)
-                    EMIT2(0x169) EMIT2(0x16b)
-                    EMIT2(0x16d) EMIT2(0x16f) EMIT2(0x171)
-                    EMIT2(0x173) EMIT2(0x1d6) EMIT2(0x1d8)
-                    EMIT2(0x215) EMIT2(0x217) EMIT2(0x1b0)
-                    EMIT2(0x1d4) EMIT2(0x1da) EMIT2(0x1dc)
-                    EMIT2(0x289) EMIT2(0x1e73) EMIT2(0x1d7e)
-                    EMIT2(0x1d99) EMIT2(0x1e75) EMIT2(0x1e77)
-                    EMIT2(0x1e79) EMIT2(0x1e7b) EMIT2(0x1ee5)
-                    EMIT2(0x1ee7) EMIT2(0x1ee9) EMIT2(0x1eeb)
-                    EMIT2(0x1eed) EMIT2(0x1eef) EMIT2(0x1ef1)
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 'u';
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = u_grave;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = u_acute;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = u_circumflex;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = u_diaeresis;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x169;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x16b;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x16d;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x16f;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x171;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x173;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1d6;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1d8;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x215;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x217;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1b0;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1d4;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1da;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1dc;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x289;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e73;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1d7e;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1d99;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e75;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e77;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e79;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e7b;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1ee5;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1ee7;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1ee9;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1eeb;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1eed;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1eef;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1ef1;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
                     return OK;
 
             case 'v':
@@ -135632,8 +139542,56 @@
                     case 0x1d8c:
                         case 0x1e7d:
                             case 0x1e7f:
-                    EMIT2('v') EMIT2(0x28b) EMIT2(0x1d8c) EMIT2(0x1e7d)
-                    EMIT2(0x1e7f)
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 'v';
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x28b;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1d8c;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e7d;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e7f;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
                     return OK;
 
             case 'w':
@@ -135644,14 +139602,121 @@
             case 0x1e87:
                 case 0x1e89:
                     case 0x1e98:
-                    EMIT2('w') EMIT2(0x175) EMIT2(0x1e81) EMIT2(0x1e83)
-                    EMIT2(0x1e85) EMIT2(0x1e87) EMIT2(0x1e89) EMIT2(0x1e98)
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 'w';
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x175;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e81;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e83;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e85;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e87;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e89;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e98;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
                     return OK;
 
             case 'x':
                 case 0x1e8b:
                     case 0x1e8d:
-                    EMIT2('x') EMIT2(0x1e8b) EMIT2(0x1e8d)
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 'x';
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e8b;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e8d;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
                     return OK;
 
             case 'y':
@@ -135667,10 +139732,136 @@
                     case 0x1ef5:
                         case 0x1ef7:
             case 0x1ef9:
-                    EMIT2('y') EMIT2(y_acute) EMIT2(y_diaeresis)
-                    EMIT2(0x177) EMIT2(0x1b4) EMIT2(0x233) EMIT2(0x24f)
-                    EMIT2(0x1e8f) EMIT2(0x1e99) EMIT2(0x1ef3)
-                    EMIT2(0x1ef5) EMIT2(0x1ef7) EMIT2(0x1ef9)
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 'y';
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = y_acute;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = y_diaeresis;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x177;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1b4;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x233;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x24f;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e8f;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e99;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1ef3;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1ef5;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1ef7;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1ef9;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
                     return OK;
 
             case 'z':
@@ -135684,15 +139875,131 @@
                         case 0x1e93:
             case 0x1e95:
                 case 0x2c6c:
-                    EMIT2('z') EMIT2(0x17a) EMIT2(0x17c) EMIT2(0x17e)
-                    EMIT2(0x1b6) EMIT2(0x1d76) EMIT2(0x1d8e) EMIT2(0x1e91)
-                    EMIT2(0x1e93) EMIT2(0x1e95) EMIT2(0x2c6c)
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 'z';
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x17a;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x17c;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x17e;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1b6;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1d76;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1d8e;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e91;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e93;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x1e95;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = 0x2c6c;
+                      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                      {
+                          return FAIL;
+                      }
+                      *post_ptr++ = NFA_CONCAT;
                     return OK;
 
         }
     }
 
-    EMIT2(c);
+      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+      {
+          return FAIL;
+      }
+      *post_ptr++ = c;
+      if (post_ptr >= post_end && realloc_post_list() == FAIL)
+      {
+          return FAIL;
+      }
+      *post_ptr++ = NFA_CONCAT;
     return OK;
 }
 
@@ -135719,29 +140026,45 @@
     switch (c)
     {
         case NUL:
-            EMSG_RET_FAIL(_(e_nfa_regexp_end_encountered_prematurely));
+             return (emsg((_(e_nfa_regexp_end_encountered_prematurely))), rc_did_emsg = TRUE, FAIL) ;
 
-        case Magic('^'):
-            EMIT(NFA_BOL);
+        case  ((int)('^') - 256) :
+             if (post_ptr >= post_end && realloc_post_list() == FAIL)
+             {
+                 return FAIL;
+             }
+             *post_ptr++ = NFA_BOL;
             break;
 
-        case Magic('$'):
-            EMIT(NFA_EOL);
+        case  ((int)('$') - 256) :
+             if (post_ptr >= post_end && realloc_post_list() == FAIL)
+             {
+                 return FAIL;
+             }
+             *post_ptr++ = NFA_EOL;
             break;
 
-        case Magic('<'):
-            EMIT(NFA_BOW);
+        case  ((int)('<') - 256) :
+             if (post_ptr >= post_end && realloc_post_list() == FAIL)
+             {
+                 return FAIL;
+             }
+             *post_ptr++ = NFA_BOW;
             break;
 
-        case Magic('>'):
-            EMIT(NFA_EOW);
+        case  ((int)('>') - 256) :
+             if (post_ptr >= post_end && realloc_post_list() == FAIL)
+             {
+                 return FAIL;
+             }
+             *post_ptr++ = NFA_EOW;
             break;
 
-        case Magic('_'):
+        case  ((int)('_') - 256) :
             c = no_Magic(getchr());
             if (c == NUL)
             {
-                EMSG_RET_FAIL(_(e_nfa_regexp_end_encountered_prematurely));
+                 return (emsg((_(e_nfa_regexp_end_encountered_prematurely))), rc_did_emsg = TRUE, FAIL) ;
             }
 
             if (c == '%')
@@ -135752,16 +140075,24 @@
                 {
                     goto delimiter_atom;
                 }
-                EMSG2_RET_FAIL(_(e_invalid_character_after_str), reg_magic == MAGIC_ALL);
+                 return (semsg((const char *)(_(e_invalid_character_after_str)), (reg_magic == MAGIC_ALL) ? "" : "\\"), rc_did_emsg = TRUE, FAIL) ;
             }
             if (c == '^')
             {
-                EMIT(NFA_BOL);
+                 if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                 {
+                     return FAIL;
+                 }
+                 *post_ptr++ = NFA_BOL;
                 break;
             }
             if (c == '$')
             {
-                EMIT(NFA_EOL);
+                 if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                 {
+                     return FAIL;
+                 }
+                 *post_ptr++ = NFA_EOL;
                 break;
             }
 
@@ -135772,33 +140103,34 @@
                 goto collection;
             }
 
-        case Magic('.'):
-        case Magic('i'):
-        case Magic('I'):
-        case Magic('k'):
-        case Magic('K'):
-        case Magic('f'):
-        case Magic('F'):
-        case Magic('p'):
-        case Magic('P'):
-        case Magic('s'):
-        case Magic('S'):
-        case Magic('d'):
-        case Magic('D'):
-        case Magic('x'):
-        case Magic('X'):
-        case Magic('o'):
-        case Magic('O'):
-        case Magic('w'):
-        case Magic('W'):
-        case Magic('h'):
-        case Magic('H'):
-        case Magic('a'):
-        case Magic('A'):
-        case Magic('l'):
-        case Magic('L'):
-        case Magic('u'):
-        case Magic('U'):
+        __attribute__((fallthrough));
+        case  ((int)('.') - 256) :
+        case  ((int)('i') - 256) :
+        case  ((int)('I') - 256) :
+        case  ((int)('k') - 256) :
+        case  ((int)('K') - 256) :
+        case  ((int)('f') - 256) :
+        case  ((int)('F') - 256) :
+        case  ((int)('p') - 256) :
+        case  ((int)('P') - 256) :
+        case  ((int)('s') - 256) :
+        case  ((int)('S') - 256) :
+        case  ((int)('d') - 256) :
+        case  ((int)('D') - 256) :
+        case  ((int)('x') - 256) :
+        case  ((int)('X') - 256) :
+        case  ((int)('o') - 256) :
+        case  ((int)('O') - 256) :
+        case  ((int)('w') - 256) :
+        case  ((int)('W') - 256) :
+        case  ((int)('h') - 256) :
+        case  ((int)('H') - 256) :
+        case  ((int)('a') - 256) :
+        case  ((int)('A') - 256) :
+        case  ((int)('l') - 256) :
+        case  ((int)('L') - 256) :
+        case  ((int)('u') - 256) :
+        case  ((int)('U') - 256) :
             p = vim_strchr(classchars, no_Magic(c));
             if (p == NULL)
             {
@@ -135812,56 +140144,76 @@
                 return FAIL;
             }
 
-            if (enc_utf8 && c == Magic('.') && utf_iscomposing(peekchr()))
+            if (enc_utf8 && c ==  ((int)('.') - 256)  && utf_iscomposing(peekchr()))
             {
                 old_regparse = regparse;
                 c = getchr();
                 goto nfa_do_multibyte;
             }
-            EMIT(nfa_classcodes[p - classchars]);
+             if (post_ptr >= post_end && realloc_post_list() == FAIL)
+             {
+                 return FAIL;
+             }
+             *post_ptr++ = nfa_classcodes[p - classchars];
             if (extra == NFA_ADD_NL)
             {
-                EMIT(NFA_NEWL);
-                EMIT(NFA_OR);
+                 if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                 {
+                     return FAIL;
+                 }
+                 *post_ptr++ = NFA_NEWL;
+                 if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                 {
+                     return FAIL;
+                 }
+                 *post_ptr++ = NFA_OR;
                 regflags |= RF_HASNL;
             }
             break;
 
-        case Magic('n'):
+        case  ((int)('n') - 256) :
             if (reg_string)
             {
-                EMIT(NL);
+                 if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                 {
+                     return FAIL;
+                 }
+                 *post_ptr++ = NL;
             }
             else
             {
-                EMIT(NFA_NEWL);
+                 if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                 {
+                     return FAIL;
+                 }
+                 *post_ptr++ = NFA_NEWL;
                 regflags |= RF_HASNL;
             }
             break;
 
-        case Magic('('):
+        case  ((int)('(') - 256) :
             if (nfa_reg(REG_PAREN) == FAIL)
             {
                 return FAIL;
             }
             break;
 
-        case Magic('|'):
-        case Magic('&'):
-        case Magic(')'):
+        case  ((int)('|') - 256) :
+        case  ((int)('&') - 256) :
+        case  ((int)(')') - 256) :
             semsg(_(e_nfa_regexp_misplaced_chr), no_Magic(c));
             return FAIL;
 
-        case Magic('='):
-        case Magic('?'):
-        case Magic('+'):
-        case Magic('@'):
-        case Magic('*'):
-        case Magic('{'):
+        case  ((int)('=') - 256) :
+        case  ((int)('?') - 256) :
+        case  ((int)('+') - 256) :
+        case  ((int)('@') - 256) :
+        case  ((int)('*') - 256) :
+        case  ((int)('{') - 256) :
             semsg(_(e_nfa_regexp_misplaced_chr), no_Magic(c));
             return FAIL;
 
-        case Magic('~'):
+        case  ((int)('~') - 256) :
             {
                 char_u      *lp;
 
@@ -135870,27 +140222,39 @@
                     emsg(_(e_no_previous_substitute_regular_expression));
                     return FAIL;
                 }
-                for (lp = reg_prev_sub; *lp != NUL; MB_CPTR_ADV(lp))
+                for (lp = reg_prev_sub; *lp != NUL;  lp += enc_utf8 ? utf_ptr2len(lp) : (*mb_ptr2len)(lp) )
                 {
-                    EMIT(PTR2CHAR(lp));
+                     if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                     {
+                         return FAIL;
+                     }
+                     *post_ptr++ =  (has_mbyte ? mb_ptr2char(lp) : (int)*(lp)) ;
                     if (lp != reg_prev_sub)
                     {
-                        EMIT(NFA_CONCAT);
+                         if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                         {
+                             return FAIL;
+                         }
+                         *post_ptr++ = NFA_CONCAT;
                     }
                 }
-                EMIT(NFA_NOPEN);
+                 if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                 {
+                     return FAIL;
+                 }
+                 *post_ptr++ = NFA_NOPEN;
                 break;
             }
 
-        case Magic('1'):
-        case Magic('2'):
-        case Magic('3'):
-        case Magic('4'):
-        case Magic('5'):
-        case Magic('6'):
-        case Magic('7'):
-        case Magic('8'):
-        case Magic('9'):
+        case  ((int)('1') - 256) :
+        case  ((int)('2') - 256) :
+        case  ((int)('3') - 256) :
+        case  ((int)('4') - 256) :
+        case  ((int)('5') - 256) :
+        case  ((int)('6') - 256) :
+        case  ((int)('7') - 256) :
+        case  ((int)('8') - 256) :
+        case  ((int)('9') - 256) :
             {
                 int refnum = no_Magic(c) - '1';
 
@@ -135898,24 +140262,36 @@
                 {
                     return FAIL;
                 }
-                EMIT(NFA_BACKREF1 + refnum);
+                 if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                 {
+                     return FAIL;
+                 }
+                 *post_ptr++ = NFA_BACKREF1 + refnum;
                 rex.nfa_has_backref = TRUE;
             }
             break;
 
-        case Magic('z'):
+        case  ((int)('z') - 256) :
             c = no_Magic(getchr());
             switch (c)
             {
                 case 's':
-                    EMIT(NFA_ZSTART);
+                     if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                     {
+                         return FAIL;
+                     }
+                     *post_ptr++ = NFA_ZSTART;
                     if (re_mult_next("\\zs") == FAIL)
                     {
                         return FAIL;
                     }
                     break;
                 case 'e':
-                    EMIT(NFA_ZEND);
+                     if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                     {
+                         return FAIL;
+                     }
+                     *post_ptr++ = NFA_ZEND;
                     rex.nfa_has_zend = TRUE;
                     if (re_mult_next("\\ze") == FAIL)
                     {
@@ -135928,7 +140304,7 @@
             }
             break;
 
-        case Magic('%'):
+        case  ((int)('%') - 256) :
             c = no_Magic(getchr());
             switch (c)
             {
@@ -135937,7 +140313,11 @@
                     {
                         return FAIL;
                     }
-                    EMIT(NFA_NOPEN);
+                     if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                     {
+                         return FAIL;
+                     }
+                     *post_ptr++ = NFA_NOPEN;
                     break;
 
                 case 'd':
@@ -135972,18 +140352,30 @@
 
                         if (nr < 0 || nr > INT_MAX)
                         {
-                            EMSG2_RET_FAIL(_(e_invalid_character_after_str_2), reg_magic == MAGIC_ALL);
+                             return (semsg((const char *)(_(e_invalid_character_after_str_2)), (reg_magic == MAGIC_ALL) ? "" : "\\"), rc_did_emsg = TRUE, FAIL) ;
                         }
-                        EMIT(nr == 0 ? 0x0a : (long)nr);
+                         if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                         {
+                             return FAIL;
+                         }
+                         *post_ptr++ = nr == 0 ? 0x0a : (long)nr;
                     }
                     break;
 
                 case '^':
-                    EMIT(NFA_BOF);
+                     if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                     {
+                         return FAIL;
+                     }
+                     *post_ptr++ = NFA_BOF;
                     break;
 
                 case '$':
-                    EMIT(NFA_EOF);
+                     if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                     {
+                         return FAIL;
+                     }
+                     *post_ptr++ = NFA_EOF;
                     break;
 
                 case '#':
@@ -135992,15 +140384,27 @@
                         semsg(_(e_atom_engine_must_be_at_start_of_pattern), regparse[1]);
                         return FAIL;
                     }
-                    EMIT(NFA_CURSOR);
+                     if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                     {
+                         return FAIL;
+                     }
+                     *post_ptr++ = NFA_CURSOR;
                     break;
 
                 case 'V':
-                    EMIT(NFA_VISUAL);
+                     if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                     {
+                         return FAIL;
+                     }
+                     *post_ptr++ = NFA_VISUAL;
                     break;
 
                 case 'C':
-                    EMIT(NFA_ANY_COMPOSING);
+                     if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                     {
+                         return FAIL;
+                     }
+                     *post_ptr++ = NFA_ANY_COMPOSING;
                     break;
 
                 case '[':
@@ -136011,7 +140415,7 @@
                         {
                             if (c == NUL)
                             {
-                                EMSG2_RET_FAIL(_(e_missing_sb_after_str), reg_magic == MAGIC_ALL);
+                                 return (semsg((const char *)(_(e_missing_sb_after_str)), (reg_magic == MAGIC_ALL) ? "" : "\\"), rc_did_emsg = TRUE, FAIL) ;
                             }
                             if (nfa_regatom() == FAIL)
                             {
@@ -136021,12 +140425,24 @@
                         getchr();
                         if (n == 0)
                         {
-                            EMSG2_RET_FAIL(_(e_empty_str_brackets), reg_magic == MAGIC_ALL);
+                             return (semsg((const char *)(_(e_empty_str_brackets)), (reg_magic == MAGIC_ALL) ? "" : "\\"), rc_did_emsg = TRUE, FAIL) ;
                         }
-                        EMIT(NFA_OPT_CHARS);
-                        EMIT(n);
+                         if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                         {
+                             return FAIL;
+                         }
+                         *post_ptr++ = NFA_OPT_CHARS;
+                         if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                         {
+                             return FAIL;
+                         }
+                         *post_ptr++ = n;
 
-                        EMIT(NFA_NOPEN);
+                         if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                         {
+                             return FAIL;
+                         }
+                         *post_ptr++ = NFA_NOPEN;
                         break;
                     }
 
@@ -136063,22 +140479,27 @@
                                 idx = 3;
                                 break;
                             default:
-                                EMSG2_RET_FAIL(_(e_invalid_character_after_str), reg_magic == MAGIC_ALL);
+                                 return (semsg((const char *)(_(e_invalid_character_after_str)), (reg_magic == MAGIC_ALL) ? "" : "\\"), rc_did_emsg = TRUE, FAIL) ;
                         }
                         if (delim_nl)
                         {
                             idx += NFA_DELIM_NL;
                             regflags |= RF_HASNL;
                         }
-                        EMIT(base + idx);
+                         if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                         {
+                             return FAIL;
+                         }
+                         *post_ptr++ = base + idx;
                     }
                     break;
 
                 case '>':
-                    if (!VIM_ISDIGIT(*regparse) && *regparse != '\'' && *regparse != '.')
+                    if (! ((unsigned)(*regparse) - '0' < 10)  && *regparse != '\'' && *regparse != '.')
                     {
                         goto delimiter_atom;
                     }
+                __attribute__((fallthrough));
                 default:
                     {
                         long_u  n = 0;
@@ -136095,7 +140516,7 @@
                             cur = TRUE;
                             c = getchr();
                         }
-                        while (VIM_ISDIGIT(c))
+                        while ( ((unsigned)(c) - '0' < 10) )
                         {
                             long_u tmp;
 
@@ -136130,7 +140551,11 @@
                                 {
                                     n = curwin->w_cursor.lnum;
                                 }
-                                EMIT(cmp == '<' ? NFA_LNUM_LT : cmp == '>' ? NFA_LNUM_GT : NFA_LNUM);
+                                 if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                                 {
+                                     return FAIL;
+                                 }
+                                 *post_ptr++ = cmp == '<' ? NFA_LNUM_LT : cmp == '>' ? NFA_LNUM_GT : NFA_LNUM;
                                 if (save_prev_at_start)
                                 {
                                     at_start = TRUE;
@@ -136143,7 +140568,11 @@
                                     n = curwin->w_cursor.col;
                                     n++;
                                 }
-                                EMIT(cmp == '<' ? NFA_COL_LT : cmp == '>' ? NFA_COL_GT : NFA_COL);
+                                 if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                                 {
+                                     return FAIL;
+                                 }
+                                 *post_ptr++ = cmp == '<' ? NFA_COL_LT : cmp == '>' ? NFA_COL_GT : NFA_COL;
                             }
                             else
                             {
@@ -136154,7 +140583,11 @@
                                     getvvcol(curwin, &curwin->w_cursor, NULL, NULL, &vcol, 0);
                                     n = ++vcol;
                                 }
-                                EMIT(cmp == '<' ? NFA_VCOL_LT : cmp == '>' ? NFA_VCOL_GT : NFA_VCOL);
+                                 if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                                 {
+                                     return FAIL;
+                                 }
+                                 *post_ptr++ = cmp == '<' ? NFA_VCOL_LT : cmp == '>' ? NFA_VCOL_GT : NFA_VCOL;
                                 limit = INT_MAX / MB_MAXBYTES;
                             }
                             if (n >= limit)
@@ -136162,13 +140595,25 @@
                                 emsg(_(e_percent_value_too_large));
                                 return FAIL;
                             }
-                            EMIT((int)n);
+                             if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                             {
+                                 return FAIL;
+                             }
+                             *post_ptr++ = (int)n;
                             break;
                         }
                         else if (no_Magic(c) == '\'' && n == 0)
                         {
-                            EMIT(cmp == '<' ? NFA_MARK_LT : cmp == '>' ? NFA_MARK_GT : NFA_MARK);
-                            EMIT(getchr());
+                             if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                             {
+                                 return FAIL;
+                             }
+                             *post_ptr++ = cmp == '<' ? NFA_MARK_LT : cmp == '>' ? NFA_MARK_GT : NFA_MARK;
+                             if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                             {
+                                 return FAIL;
+                             }
+                             *post_ptr++ = getchr();
                             break;
                         }
                     }
@@ -136177,7 +140622,7 @@
             }
             break;
 
-        case Magic('['):
+        case  ((int)('[') - 256) :
 collection:
 
             p = regparse;
@@ -136191,16 +140636,32 @@
                 {
                     if (result >= NFA_FIRST_NL && result <= NFA_LAST_NL)
                     {
-                        EMIT(result - NFA_ADD_NL);
-                        EMIT(NFA_NEWL);
-                        EMIT(NFA_OR);
+                         if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                         {
+                             return FAIL;
+                         }
+                         *post_ptr++ = result - NFA_ADD_NL;
+                         if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                         {
+                             return FAIL;
+                         }
+                         *post_ptr++ = NFA_NEWL;
+                         if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                         {
+                             return FAIL;
+                         }
+                         *post_ptr++ = NFA_OR;
                     }
                     else
                     {
-                        EMIT(result);
+                         if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                         {
+                             return FAIL;
+                         }
+                         *post_ptr++ = result;
                     }
                     regparse = endp;
-                    MB_PTR_ADV(regparse);
+                     regparse += (*mb_ptr2len)(regparse) ;
                     return OK;
                 }
                 startc = -1;
@@ -136208,19 +140669,35 @@
                 if (*regparse == '^')
                 {
                     negated = TRUE;
-                    MB_PTR_ADV(regparse);
-                    EMIT(NFA_START_NEG_COLL);
+                     regparse += (*mb_ptr2len)(regparse) ;
+                     if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                     {
+                         return FAIL;
+                     }
+                     *post_ptr++ = NFA_START_NEG_COLL;
                 }
                 else
                 {
-                    EMIT(NFA_START_COLL);
+                     if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                     {
+                         return FAIL;
+                     }
+                     *post_ptr++ = NFA_START_COLL;
                 }
                 if (*regparse == '-')
                 {
                     startc = '-';
-                    EMIT(startc);
-                    EMIT(NFA_CONCAT);
-                    MB_PTR_ADV(regparse);
+                     if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                     {
+                         return FAIL;
+                     }
+                     *post_ptr++ = startc;
+                     if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                     {
+                         return FAIL;
+                     }
+                     *post_ptr++ = NFA_CONCAT;
+                     regparse += (*mb_ptr2len)(regparse) ;
                 }
                 emit_range = FALSE;
                 while (regparse < endp)
@@ -136248,66 +140725,146 @@
                             switch (charclass)
                             {
                                 case CLASS_ALNUM:
-                                    EMIT(NFA_CLASS_ALNUM);
+                                     if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                                     {
+                                         return FAIL;
+                                     }
+                                     *post_ptr++ = NFA_CLASS_ALNUM;
                                     break;
                                 case CLASS_ALPHA:
-                                    EMIT(NFA_CLASS_ALPHA);
+                                     if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                                     {
+                                         return FAIL;
+                                     }
+                                     *post_ptr++ = NFA_CLASS_ALPHA;
                                     break;
                                 case CLASS_BLANK:
-                                    EMIT(NFA_CLASS_BLANK);
+                                     if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                                     {
+                                         return FAIL;
+                                     }
+                                     *post_ptr++ = NFA_CLASS_BLANK;
                                     break;
                                 case CLASS_CNTRL:
-                                    EMIT(NFA_CLASS_CNTRL);
+                                     if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                                     {
+                                         return FAIL;
+                                     }
+                                     *post_ptr++ = NFA_CLASS_CNTRL;
                                     break;
                                 case CLASS_DIGIT:
-                                    EMIT(NFA_CLASS_DIGIT);
+                                     if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                                     {
+                                         return FAIL;
+                                     }
+                                     *post_ptr++ = NFA_CLASS_DIGIT;
                                     break;
                                 case CLASS_GRAPH:
-                                    EMIT(NFA_CLASS_GRAPH);
+                                     if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                                     {
+                                         return FAIL;
+                                     }
+                                     *post_ptr++ = NFA_CLASS_GRAPH;
                                     break;
                                 case CLASS_LOWER:
                                     wants_nfa = TRUE;
-                                    EMIT(NFA_CLASS_LOWER);
+                                     if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                                     {
+                                         return FAIL;
+                                     }
+                                     *post_ptr++ = NFA_CLASS_LOWER;
                                     break;
                                 case CLASS_PRINT:
-                                    EMIT(NFA_CLASS_PRINT);
+                                     if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                                     {
+                                         return FAIL;
+                                     }
+                                     *post_ptr++ = NFA_CLASS_PRINT;
                                     break;
                                 case CLASS_PUNCT:
-                                    EMIT(NFA_CLASS_PUNCT);
+                                     if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                                     {
+                                         return FAIL;
+                                     }
+                                     *post_ptr++ = NFA_CLASS_PUNCT;
                                     break;
                                 case CLASS_SPACE:
-                                    EMIT(NFA_CLASS_SPACE);
+                                     if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                                     {
+                                         return FAIL;
+                                     }
+                                     *post_ptr++ = NFA_CLASS_SPACE;
                                     break;
                                 case CLASS_UPPER:
                                     wants_nfa = TRUE;
-                                    EMIT(NFA_CLASS_UPPER);
+                                     if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                                     {
+                                         return FAIL;
+                                     }
+                                     *post_ptr++ = NFA_CLASS_UPPER;
                                     break;
                                 case CLASS_XDIGIT:
-                                    EMIT(NFA_CLASS_XDIGIT);
+                                     if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                                     {
+                                         return FAIL;
+                                     }
+                                     *post_ptr++ = NFA_CLASS_XDIGIT;
                                     break;
                                 case CLASS_TAB:
-                                    EMIT(NFA_CLASS_TAB);
+                                     if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                                     {
+                                         return FAIL;
+                                     }
+                                     *post_ptr++ = NFA_CLASS_TAB;
                                     break;
                                 case CLASS_RETURN:
-                                    EMIT(NFA_CLASS_RETURN);
+                                     if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                                     {
+                                         return FAIL;
+                                     }
+                                     *post_ptr++ = NFA_CLASS_RETURN;
                                     break;
                                 case CLASS_BACKSPACE:
-                                    EMIT(NFA_CLASS_BACKSPACE);
+                                     if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                                     {
+                                         return FAIL;
+                                     }
+                                     *post_ptr++ = NFA_CLASS_BACKSPACE;
                                     break;
                                 case CLASS_ESCAPE:
-                                    EMIT(NFA_CLASS_ESCAPE);
+                                     if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                                     {
+                                         return FAIL;
+                                     }
+                                     *post_ptr++ = NFA_CLASS_ESCAPE;
                                     break;
                                 case CLASS_IDENT:
-                                    EMIT(NFA_CLASS_IDENT);
+                                     if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                                     {
+                                         return FAIL;
+                                     }
+                                     *post_ptr++ = NFA_CLASS_IDENT;
                                     break;
                                 case CLASS_KEYWORD:
-                                    EMIT(NFA_CLASS_KEYWORD);
+                                     if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                                     {
+                                         return FAIL;
+                                     }
+                                     *post_ptr++ = NFA_CLASS_KEYWORD;
                                     break;
                                 case CLASS_FNAME:
-                                    EMIT(NFA_CLASS_FNAME);
+                                     if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                                     {
+                                         return FAIL;
+                                     }
+                                     *post_ptr++ = NFA_CLASS_FNAME;
                                     break;
                             }
-                            EMIT(NFA_CONCAT);
+                             if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                             {
+                                 return FAIL;
+                             }
+                             *post_ptr++ = NFA_CONCAT;
                             continue;
                         }
                         if (equiclass != 0)
@@ -136315,7 +140872,7 @@
                             result = nfa_emit_equi_class(equiclass);
                             if (result == FAIL)
                             {
-                                EMSG_RET_FAIL(_(e_error_building_nfa_with_equivalence_class));
+                                 return (emsg((_(e_error_building_nfa_with_equivalence_class))), rc_did_emsg = TRUE, FAIL) ;
                             }
                             continue;
                         }
@@ -136328,13 +140885,13 @@
                     {
                         emit_range = TRUE;
                         startc = oldstartc;
-                        MB_PTR_ADV(regparse);
+                         regparse += (*mb_ptr2len)(regparse) ;
                         continue;
                     }
 
                     if (*regparse == '\\' && !reg_cpo_bsl && regparse + 1 <= endp && (vim_strchr(REGEXP_INRANGE, regparse[1]) != NULL || (!reg_cpo_lit && vim_strchr(REGEXP_ABBR, regparse[1]) != NULL)))
                     {
-                        MB_PTR_ADV(regparse);
+                         regparse += (*mb_ptr2len)(regparse) ;
 
                         if (*regparse == 'n')
                         {
@@ -136345,10 +140902,10 @@
                                 startc = coll_get_char();
                                 if (startc == INT_MAX)
                                 {
-                                    EMSG_RET_FAIL(_(e_unicode_val_too_large));
+                                     return (emsg((_(e_unicode_val_too_large))), rc_did_emsg = TRUE, FAIL) ;
                                 }
                                 got_coll_char = TRUE;
-                                MB_PTR_BACK(old_regparse, regparse);
+                                 regparse -= has_mbyte ? ((*mb_head_off)(old_regparse, (regparse) - 1) + 1) : 1 ;
                             }
                             else
                             {
@@ -136358,7 +140915,7 @@
 
                     if (startc == -1)
                     {
-                        startc = PTR2CHAR(regparse);
+                        startc =  (has_mbyte ? mb_ptr2char(regparse) : (int)*(regparse)) ;
                     }
 
                     if (emit_range)
@@ -136369,37 +140926,69 @@
                         startc = oldstartc;
                         if (startc > endc)
                         {
-                            EMSG_RET_FAIL(_(e_reverse_range_in_character_class));
+                             return (emsg((_(e_reverse_range_in_character_class))), rc_did_emsg = TRUE, FAIL) ;
                         }
 
                         if (endc > startc + 2)
                         {
                             if (startc == 0)
                             {
-                                EMIT(1);
+                                 if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                                 {
+                                     return FAIL;
+                                 }
+                                 *post_ptr++ = 1;
                             }
                             else
                             {
                                 --post_ptr;
                             }
-                            EMIT(endc);
-                            EMIT(NFA_RANGE);
-                            EMIT(NFA_CONCAT);
+                             if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                             {
+                                 return FAIL;
+                             }
+                             *post_ptr++ = endc;
+                             if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                             {
+                                 return FAIL;
+                             }
+                             *post_ptr++ = NFA_RANGE;
+                             if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                             {
+                                 return FAIL;
+                             }
+                             *post_ptr++ = NFA_CONCAT;
                         }
                         else if (has_mbyte && ((*mb_char2len)(startc) > 1 || (*mb_char2len)(endc) > 1))
                         {
                             for (c = startc + 1; c <= endc; c++)
                             {
-                                EMIT(c);
-                                EMIT(NFA_CONCAT);
+                                 if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                                 {
+                                     return FAIL;
+                                 }
+                                 *post_ptr++ = c;
+                                 if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                                 {
+                                     return FAIL;
+                                 }
+                                 *post_ptr++ = NFA_CONCAT;
                             }
                         }
                         else
                         {
                             for (c = startc + 1; c <= endc; c++)
                             {
-                                EMIT(c);
-                                EMIT(NFA_CONCAT);
+                                 if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                                 {
+                                     return FAIL;
+                                 }
+                                 *post_ptr++ = c;
+                                 if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                                 {
+                                     return FAIL;
+                                 }
+                                 *post_ptr++ = NFA_CONCAT;
                             }
                         }
                         emit_range = FALSE;
@@ -136418,15 +141007,31 @@
                         {
                             if (got_coll_char == TRUE && startc == 0)
                             {
-                                EMIT(0x0a);
-                                EMIT(NFA_CONCAT);
+                                 if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                                 {
+                                     return FAIL;
+                                 }
+                                 *post_ptr++ = 0x0a;
+                                 if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                                 {
+                                     return FAIL;
+                                 }
+                                 *post_ptr++ = NFA_CONCAT;
                             }
                             else
                             {
-                                EMIT(startc);
+                                 if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                                 {
+                                     return FAIL;
+                                 }
+                                 *post_ptr++ = startc;
                                 if (!(enc_utf8 && (utf_ptr2len(regparse) != (plen = utfc_ptr2len(regparse)))))
                                 {
-                                    EMIT(NFA_CONCAT);
+                                     if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                                     {
+                                         return FAIL;
+                                     }
+                                     *post_ptr++ = NFA_CONCAT;
                                 }
                             }
                         }
@@ -136442,48 +141047,92 @@
                         {
                             if (c == 0)
                             {
-                                EMIT(1);
+                                 if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                                 {
+                                     return FAIL;
+                                 }
+                                 *post_ptr++ = 1;
                             }
                             else
                             {
-                                EMIT(c);
+                                 if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                                 {
+                                     return FAIL;
+                                 }
+                                 *post_ptr++ = c;
                             }
-                            EMIT(NFA_CONCAT);
+                             if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                             {
+                                 return FAIL;
+                             }
+                             *post_ptr++ = NFA_CONCAT;
                             if ((i += utf_char2len(c)) >= plen)
                             {
                                 break;
                             }
                             c = utf_ptr2char(regparse + i);
                         }
-                        EMIT(NFA_COMPOSING);
-                        EMIT(NFA_CONCAT);
+                         if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                         {
+                             return FAIL;
+                         }
+                         *post_ptr++ = NFA_COMPOSING;
+                         if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                         {
+                             return FAIL;
+                         }
+                         *post_ptr++ = NFA_CONCAT;
                     }
-                    MB_PTR_ADV(regparse);
+                     regparse += (*mb_ptr2len)(regparse) ;
                 }
 
-                MB_PTR_BACK(old_regparse, regparse);
+                 regparse -= has_mbyte ? ((*mb_head_off)(old_regparse, (regparse) - 1) + 1) : 1 ;
                 if (*regparse == '-')
                 {
-                    EMIT('-');
-                    EMIT(NFA_CONCAT);
+                     if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                     {
+                         return FAIL;
+                     }
+                     *post_ptr++ = '-';
+                     if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                     {
+                         return FAIL;
+                     }
+                     *post_ptr++ = NFA_CONCAT;
                 }
 
                 regparse = endp;
-                MB_PTR_ADV(regparse);
+                 regparse += (*mb_ptr2len)(regparse) ;
 
                 if (negated == TRUE)
                 {
-                    EMIT(NFA_END_NEG_COLL);
+                     if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                     {
+                         return FAIL;
+                     }
+                     *post_ptr++ = NFA_END_NEG_COLL;
                 }
                 else
                 {
-                    EMIT(NFA_END_COLL);
+                     if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                     {
+                         return FAIL;
+                     }
+                     *post_ptr++ = NFA_END_COLL;
                 }
 
                 if (extra == NFA_ADD_NL)
                 {
-                    EMIT(reg_string ? NL : NFA_NEWL);
-                    EMIT(NFA_OR);
+                     if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                     {
+                         return FAIL;
+                     }
+                     *post_ptr++ = reg_string ? NL : NFA_NEWL;
+                     if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                     {
+                         return FAIL;
+                     }
+                     *post_ptr++ = NFA_OR;
                 }
 
                 return OK;
@@ -136491,9 +141140,10 @@
 
             if (reg_strict)
             {
-                EMSG_RET_FAIL(_(e_missing_rsb_after_str_lsb));
+                 return (emsg((_(e_missing_rsb_after_str_lsb))), rc_did_emsg = TRUE, FAIL) ;
             }
 
+        __attribute__((fallthrough));
         default:
             {
                 int     plen;
@@ -136505,10 +141155,18 @@
 
                     for (;;)
                     {
-                        EMIT(c);
+                         if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                         {
+                             return FAIL;
+                         }
+                         *post_ptr++ = c;
                         if (i > 0)
                         {
-                            EMIT(NFA_CONCAT);
+                             if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                             {
+                                 return FAIL;
+                             }
+                             *post_ptr++ = NFA_CONCAT;
                         }
                         if ((i += utf_char2len(c)) >= plen)
                         {
@@ -136516,13 +141174,21 @@
                         }
                         c = utf_ptr2char(old_regparse + i);
                     }
-                    EMIT(NFA_COMPOSING);
+                     if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                     {
+                         return FAIL;
+                     }
+                     *post_ptr++ = NFA_COMPOSING;
                     regparse = old_regparse + plen;
                 }
                 else
                 {
                     c = no_Magic(c);
-                    EMIT(c);
+                     if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                     {
+                         return FAIL;
+                     }
+                     *post_ptr++ = c;
                 }
                 return OK;
             }
@@ -136566,23 +141232,35 @@
     skipchr();
     switch (op)
     {
-        case Magic('*'):
-            EMIT(NFA_STAR);
+        case  ((int)('*') - 256) :
+             if (post_ptr >= post_end && realloc_post_list() == FAIL)
+             {
+                 return FAIL;
+             }
+             *post_ptr++ = NFA_STAR;
             break;
 
-        case Magic('+'):
+        case  ((int)('+') - 256) :
             restore_parse_state(&old_state);
             curchr = -1;
             if (nfa_regatom() == FAIL)
             {
                 return FAIL;
             }
-            EMIT(NFA_STAR);
-            EMIT(NFA_CONCAT);
+             if (post_ptr >= post_end && realloc_post_list() == FAIL)
+             {
+                 return FAIL;
+             }
+             *post_ptr++ = NFA_STAR;
+             if (post_ptr >= post_end && realloc_post_list() == FAIL)
+             {
+                 return FAIL;
+             }
+             *post_ptr++ = NFA_CONCAT;
             skipchr();
             break;
 
-        case Magic('@'):
+        case  ((int)('@') - 256) :
             c2 = (long)getdecchrs();
             op = no_Magic(getchr());
             i = 0;
@@ -136614,41 +141292,61 @@
                 semsg(_(e_nfa_regexp_unknown_operator_at_chr), op);
                 return FAIL;
             }
-            EMIT(i);
+             if (post_ptr >= post_end && realloc_post_list() == FAIL)
+             {
+                 return FAIL;
+             }
+             *post_ptr++ = i;
             if (i == NFA_PREV_ATOM_JUST_BEFORE || i == NFA_PREV_ATOM_JUST_BEFORE_NEG)
             {
-                EMIT(c2);
+                 if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                 {
+                     return FAIL;
+                 }
+                 *post_ptr++ = c2;
             }
             break;
 
-        case Magic('?'):
-        case Magic('='):
-            EMIT(NFA_QUEST);
+        case  ((int)('?') - 256) :
+        case  ((int)('=') - 256) :
+             if (post_ptr >= post_end && realloc_post_list() == FAIL)
+             {
+                 return FAIL;
+             }
+             *post_ptr++ = NFA_QUEST;
             break;
 
-        case Magic('{'):
+        case  ((int)('{') - 256) :
 
             greedy = TRUE;
             c2 = peekchr();
-            if (c2 == '-' || c2 == Magic('-'))
+            if (c2 == '-' || c2 ==  ((int)('-') - 256) )
             {
                 skipchr();
                 greedy = FALSE;
             }
             if (!read_limits(&minval, &maxval))
             {
-                EMSG_RET_FAIL(_(e_nfa_regexp_error_reading_repetition_limits));
+                 return (emsg((_(e_nfa_regexp_error_reading_repetition_limits))), rc_did_emsg = TRUE, FAIL) ;
             }
 
-            if (minval == 0 && maxval == MAX_LIMIT)
+            if (minval == 0 && maxval ==  (32767L << 16L) )
             {
                 if (greedy)
                 {
-                    EMIT(NFA_STAR);
+                     if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                     {
+                         return FAIL;
+                     }
+                     *post_ptr++ = NFA_STAR;
                 }
                 else
                 {
-                    EMIT(NFA_STAR_NONGREEDY);
+                     if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                     {
+                         return FAIL;
+                     }
+                     *post_ptr++ = NFA_STAR_NONGREEDY;
                 }
                 break;
             }
@@ -136656,11 +141354,15 @@
             if (maxval == 0)
             {
                 post_ptr = post_start + my_post_start;
-                EMIT(NFA_EMPTY);
+                 if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                 {
+                     return FAIL;
+                 }
+                 *post_ptr++ = NFA_EMPTY;
                 return OK;
             }
 
-            if ((nfa_re_flags & RE_AUTO) && (maxval > 500 || maxval > minval + 200) && (maxval != MAX_LIMIT && minval < 200) && !wants_nfa)
+            if ((nfa_re_flags & RE_AUTO) && (maxval > 500 || maxval > minval + 200) && (maxval !=  (32767L << 16L)  && minval < 200) && !wants_nfa)
             {
                 return FAIL;
             }
@@ -136679,27 +141381,43 @@
                 }
                 if (i + 1 > minval)
                 {
-                    if (maxval == MAX_LIMIT)
+                    if (maxval ==  (32767L << 16L) )
                     {
                         if (greedy)
                         {
-                            EMIT(NFA_STAR);
+                             if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                             {
+                                 return FAIL;
+                             }
+                             *post_ptr++ = NFA_STAR;
                         }
                         else
                         {
-                            EMIT(NFA_STAR_NONGREEDY);
+                             if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                             {
+                                 return FAIL;
+                             }
+                             *post_ptr++ = NFA_STAR_NONGREEDY;
                         }
                     }
                     else
                     {
-                        EMIT(quest);
+                         if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                         {
+                             return FAIL;
+                         }
+                         *post_ptr++ = quest;
                     }
                 }
                 if (old_post_pos != my_post_start)
                 {
-                    EMIT(NFA_CONCAT);
+                     if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                     {
+                         return FAIL;
+                     }
+                     *post_ptr++ = NFA_CONCAT;
                 }
-                if (i + 1 > minval && maxval == MAX_LIMIT)
+                if (i + 1 > minval && maxval ==  (32767L << 16L) )
                 {
                     break;
                 }
@@ -136716,7 +141434,7 @@
 
     if (re_multi_type(peekchr()) != NOT_MULTI)
     {
-        EMSG_RET_FAIL(_(e_nfa_regexp_cant_have_multi_follow_multi));
+         return (emsg((_(e_nfa_regexp_cant_have_multi_follow_multi))), rc_did_emsg = TRUE, FAIL) ;
     }
 
     return OK;
@@ -136733,40 +141451,40 @@
         switch (peekchr())
         {
             case NUL:
-            case Magic('|'):
-            case Magic('&'):
-            case Magic(')'):
+            case  ((int)('|') - 256) :
+            case  ((int)('&') - 256) :
+            case  ((int)(')') - 256) :
                 cont = FALSE;
                 break;
 
-            case Magic('Z'):
+            case  ((int)('Z') - 256) :
                 regflags |= RF_ICOMBINE;
                 skipchr_keepstart();
                 break;
-            case Magic('c'):
+            case  ((int)('c') - 256) :
                 regflags |= RF_ICASE;
                 skipchr_keepstart();
                 break;
-            case Magic('C'):
+            case  ((int)('C') - 256) :
                 regflags |= RF_NOICASE;
                 skipchr_keepstart();
                 break;
-            case Magic('v'):
+            case  ((int)('v') - 256) :
                 reg_magic = MAGIC_ALL;
                 skipchr_keepstart();
                 curchr = -1;
                 break;
-            case Magic('m'):
+            case  ((int)('m') - 256) :
                 reg_magic = MAGIC_ON;
                 skipchr_keepstart();
                 curchr = -1;
                 break;
-            case Magic('M'):
+            case  ((int)('M') - 256) :
                 reg_magic = MAGIC_OFF;
                 skipchr_keepstart();
                 curchr = -1;
                 break;
-            case Magic('V'):
+            case  ((int)('V') - 256) :
                 reg_magic = MAGIC_NONE;
                 skipchr_keepstart();
                 curchr = -1;
@@ -136779,7 +141497,11 @@
                 }
                 if (first == FALSE)
                 {
-                    EMIT(NFA_CONCAT);
+                     if (post_ptr >= post_end && realloc_post_list() == FAIL)
+                     {
+                         return FAIL;
+                     }
+                     *post_ptr++ = NFA_CONCAT;
                 }
                 else
                 {
@@ -136804,15 +141526,27 @@
         return FAIL;
     }
 
-    while (peekchr() == Magic('&'))
+    while (peekchr() ==  ((int)('&') - 256) )
     {
         skipchr();
         if (old_post_pos == (int)(post_ptr - post_start))
         {
-            EMIT(NFA_EMPTY);
+             if (post_ptr >= post_end && realloc_post_list() == FAIL)
+             {
+                 return FAIL;
+             }
+             *post_ptr++ = NFA_EMPTY;
         }
-        EMIT(NFA_NOPEN);
-        EMIT(NFA_PREV_ATOM_NO_WIDTH);
+         if (post_ptr >= post_end && realloc_post_list() == FAIL)
+         {
+             return FAIL;
+         }
+         *post_ptr++ = NFA_NOPEN;
+         if (post_ptr >= post_end && realloc_post_list() == FAIL)
+         {
+             return FAIL;
+         }
+         *post_ptr++ = NFA_PREV_ATOM_NO_WIDTH;
         old_post_pos = (int)(post_ptr - post_start);
         if (nfa_regconcat() == FAIL)
         {
@@ -136820,14 +141554,26 @@
         }
         if (old_post_pos == (int)(post_ptr - post_start))
         {
-            EMIT(NFA_EMPTY);
+             if (post_ptr >= post_end && realloc_post_list() == FAIL)
+             {
+                 return FAIL;
+             }
+             *post_ptr++ = NFA_EMPTY;
         }
-        EMIT(NFA_CONCAT);
+         if (post_ptr >= post_end && realloc_post_list() == FAIL)
+         {
+             return FAIL;
+         }
+         *post_ptr++ = NFA_CONCAT;
     }
 
     if (old_post_pos == (int)(post_ptr - post_start))
     {
-        EMIT(NFA_EMPTY);
+         if (post_ptr >= post_end && realloc_post_list() == FAIL)
+         {
+             return FAIL;
+         }
+         *post_ptr++ = NFA_EMPTY;
     }
 
     return OK;
@@ -136843,14 +141589,14 @@
     {
         if (regnpar >= NSUBEXP)
         {
-            EMSG_RET_FAIL(_(e_nfa_regexp_too_many_parens));
+             return (emsg((_(e_nfa_regexp_too_many_parens))), rc_did_emsg = TRUE, FAIL) ;
         }
         parno = regnpar++;
     }
 
     if (nfa_reg_parse_depth >= REG_MAX_PAREN_DEPTH)
     {
-        EMSG_RET_FAIL(_(e_command_too_complex));
+         return (emsg((_(e_command_too_complex))), rc_did_emsg = TRUE, FAIL) ;
     }
     ++nfa_reg_parse_depth;
 
@@ -136859,17 +141605,21 @@
         goto theend;
     }
 
-    while (peekchr() == Magic('|'))
+    while (peekchr() ==  ((int)('|') - 256) )
     {
         skipchr();
         if (nfa_regbranch() == FAIL)
         {
             goto theend;
         }
-        EMIT(NFA_OR);
+         if (post_ptr >= post_end && realloc_post_list() == FAIL)
+         {
+             return FAIL;
+         }
+         *post_ptr++ = NFA_OR;
     }
 
-    if (paren != REG_NOPAREN && getchr() != Magic(')'))
+    if (paren != REG_NOPAREN && getchr() !=  ((int)(')') - 256) )
     {
         if (paren == REG_NPAREN)
         {
@@ -136884,7 +141634,7 @@
     }
     else if (paren == REG_NOPAREN && peekchr() != NUL)
     {
-        if (peekchr() == Magic(')'))
+        if (peekchr() ==  ((int)(')') - 256) )
         {
             semsg(_(e_unmatched_str_close), reg_magic == MAGIC_ALL ? "" : "\\");
         }
@@ -136898,7 +141648,11 @@
     if (paren == REG_PAREN)
     {
         had_endbrace[parno] = TRUE;
-        EMIT(NFA_MOPEN + parno);
+         if (post_ptr >= post_end && realloc_post_list() == FAIL)
+         {
+             return FAIL;
+         }
+         *post_ptr++ = NFA_MOPEN + parno;
     }
 
     status = OK;
@@ -136915,7 +141669,11 @@
     {
         return NULL;
     }
-    EMIT(NFA_MOPEN);
+     if (post_ptr >= post_end && realloc_post_list() == FAIL)
+     {
+         return FAIL;
+     }
+     *post_ptr++ = NFA_MOPEN;
     return post_start;
 }
 
@@ -137008,7 +141766,7 @@
 static Frag_T empty;
 
     static void
-st_error(int *postfix UNUSED, int *end UNUSED, int *p UNUSED)
+st_error(int *postfix  __attribute__((unused)) , int *end  __attribute__((unused)) , int *p  __attribute__((unused)) )
 {
     emsg(_(e_nfa_regexp_could_not_pop_stack));
 }
@@ -137236,7 +141994,7 @@
                 {
                     return -1;
                 }
-                len += MB_CHAR2LEN(state->c);
+                len +=  (has_mbyte ? mb_char2len(state->c) : 1) ;
                 break;
         }
 
@@ -137268,12 +142026,9 @@
         return NULL;
     }
 
-#define PUSH(s)     st_push((s), &stackp, stack_end)
-#define POP()       st_pop(&stackp, stack);                                 if (stackp < stack)                                     {                                                           st_error(postfix, end, p);                              vim_free(stack);                                        return NULL;                                        }
-
     if (nfa_calc_size == FALSE)
     {
-        stack = ALLOC_MULT(Frag_T, nstate + 1);
+        stack =  (Frag_T *)alloc(sizeof(Frag_T) * (nstate + 1)) ;
         if (stack == NULL)
         {
             return NULL;
@@ -137291,10 +142046,14 @@
             {
                 break;
             }
-            e2 = POP();
-            e1 = POP();
+            e2 =  st_pop(&stackp, stack);
+            if (stackp < stack)
+                {                                                           st_error(postfix, end, p);                              vim_free(stack);                                        return NULL;                                        } ;
+            e1 =  st_pop(&stackp, stack);
+            if (stackp < stack)
+                {                                                           st_error(postfix, end, p);                              vim_free(stack);                                        return NULL;                                        } ;
             patch(e1.out, e2.start);
-            PUSH(frag(e1.start, e2.out));
+             st_push((frag(e1.start, e2.out)), &stackp, stack_end) ;
             break;
 
         case NFA_OR:
@@ -137303,14 +142062,18 @@
                 nstate++;
                 break;
             }
-            e2 = POP();
-            e1 = POP();
+            e2 =  st_pop(&stackp, stack);
+            if (stackp < stack)
+                {                                                           st_error(postfix, end, p);                              vim_free(stack);                                        return NULL;                                        } ;
+            e1 =  st_pop(&stackp, stack);
+            if (stackp < stack)
+                {                                                           st_error(postfix, end, p);                              vim_free(stack);                                        return NULL;                                        } ;
             s = alloc_state(NFA_SPLIT, e1.start, e2.start);
             if (s == NULL)
             {
                 goto theend;
             }
-            PUSH(frag(s, append(e1.out, e2.out)));
+             st_push((frag(s, append(e1.out, e2.out))), &stackp, stack_end) ;
             break;
 
         case NFA_STAR:
@@ -137319,14 +142082,16 @@
                 nstate++;
                 break;
             }
-            e = POP();
+            e =  st_pop(&stackp, stack);
+            if (stackp < stack)
+                {                                                           st_error(postfix, end, p);                              vim_free(stack);                                        return NULL;                                        } ;
             s = alloc_state(NFA_SPLIT, e.start, NULL);
             if (s == NULL)
             {
                 goto theend;
             }
             patch(e.out, s);
-            PUSH(frag(s, list1(&s->out1)));
+             st_push((frag(s, list1(&s->out1))), &stackp, stack_end) ;
             break;
 
         case NFA_STAR_NONGREEDY:
@@ -137335,14 +142100,16 @@
                 nstate++;
                 break;
             }
-            e = POP();
+            e =  st_pop(&stackp, stack);
+            if (stackp < stack)
+                {                                                           st_error(postfix, end, p);                              vim_free(stack);                                        return NULL;                                        } ;
             s = alloc_state(NFA_SPLIT, NULL, e.start);
             if (s == NULL)
             {
                 goto theend;
             }
             patch(e.out, s);
-            PUSH(frag(s, list1(&s->out)));
+             st_push((frag(s, list1(&s->out))), &stackp, stack_end) ;
             break;
 
         case NFA_QUEST:
@@ -137351,13 +142118,15 @@
                 nstate++;
                 break;
             }
-            e = POP();
+            e =  st_pop(&stackp, stack);
+            if (stackp < stack)
+                {                                                           st_error(postfix, end, p);                              vim_free(stack);                                        return NULL;                                        } ;
             s = alloc_state(NFA_SPLIT, e.start, NULL);
             if (s == NULL)
             {
                 goto theend;
             }
-            PUSH(frag(s, append(e.out, list1(&s->out1))));
+             st_push((frag(s, append(e.out, list1(&s->out1)))), &stackp, stack_end) ;
             break;
 
         case NFA_QUEST_NONGREEDY:
@@ -137366,13 +142135,15 @@
                 nstate++;
                 break;
             }
-            e = POP();
+            e =  st_pop(&stackp, stack);
+            if (stackp < stack)
+                {                                                           st_error(postfix, end, p);                              vim_free(stack);                                        return NULL;                                        } ;
             s = alloc_state(NFA_SPLIT, NULL, e.start);
             if (s == NULL)
             {
                 goto theend;
             }
-            PUSH(frag(s, append(e.out, list1(&s->out))));
+             st_push((frag(s, append(e.out, list1(&s->out)))), &stackp, stack_end) ;
             break;
 
         case NFA_END_COLL:
@@ -137382,7 +142153,9 @@
                 nstate++;
                 break;
             }
-            e = POP();
+            e =  st_pop(&stackp, stack);
+            if (stackp < stack)
+                {                                                           st_error(postfix, end, p);                              vim_free(stack);                                        return NULL;                                        } ;
             s = alloc_state(NFA_END_COLL, NULL, NULL);
             if (s == NULL)
             {
@@ -137390,7 +142163,7 @@
             }
             patch(e.out, s);
             e.start->out1 = s;
-            PUSH(frag(e.start, list1(&s->out)));
+             st_push((frag(e.start, list1(&s->out))), &stackp, stack_end) ;
             break;
 
         case NFA_RANGE:
@@ -137398,14 +142171,18 @@
             {
                 break;
             }
-            e2 = POP();
-            e1 = POP();
+            e2 =  st_pop(&stackp, stack);
+            if (stackp < stack)
+                {                                                           st_error(postfix, end, p);                              vim_free(stack);                                        return NULL;                                        } ;
+            e1 =  st_pop(&stackp, stack);
+            if (stackp < stack)
+                {                                                           st_error(postfix, end, p);                              vim_free(stack);                                        return NULL;                                        } ;
             e2.start->val = e2.start->c;
             e2.start->c = NFA_RANGE_MAX;
             e1.start->val = e1.start->c;
             e1.start->c = NFA_RANGE_MIN;
             patch(e1.out, e2.start);
-            PUSH(frag(e1.start, e2.out));
+             st_push((frag(e1.start, e2.out)), &stackp, stack_end) ;
             break;
 
         case NFA_EMPTY:
@@ -137419,7 +142196,7 @@
             {
                 goto theend;
             }
-            PUSH(frag(s, list1(&s->out)));
+             st_push((frag(s, list1(&s->out))), &stackp, stack_end) ;
             break;
 
         case NFA_OPT_CHARS:
@@ -137437,7 +142214,9 @@
             s1 = NULL;
             while (n-- > 0)
             {
-                e = POP();
+                e =  st_pop(&stackp, stack);
+                if (stackp < stack)
+                    {                                                           st_error(postfix, end, p);                              vim_free(stack);                                        return NULL;                                        } ;
                 s = alloc_state(NFA_SPLIT, e.start, NULL);
                 if (s == NULL)
                 {
@@ -137451,7 +142230,7 @@
                 append(e1.out, list1(&s->out1));
                 s1 = s;
             }
-            PUSH(frag(s, e1.out));
+             st_push((frag(s, e1.out)), &stackp, stack_end) ;
             break;
           }
 
@@ -137503,7 +142282,9 @@
                 nstate += pattern ? 4 : 2;
                 break;
             }
-            e = POP();
+            e =  st_pop(&stackp, stack);
+            if (stackp < stack)
+                {                                                           st_error(postfix, end, p);                              vim_free(stack);                                        return NULL;                                        } ;
             s1 = alloc_state(end_state, NULL, NULL);
             if (s1 == NULL)
             {
@@ -137529,12 +142310,12 @@
                 }
                 s1->out= skip;
                 patch(e.out, zend);
-                PUSH(frag(s, list1(&skip->out)));
+                 st_push((frag(s, list1(&skip->out))), &stackp, stack_end) ;
             }
             else
             {
                 patch(e.out, s1);
-                PUSH(frag(s, list1(&s1->out)));
+                 st_push((frag(s, list1(&s1->out))), &stackp, stack_end) ;
                 if (before)
                 {
                     if (n <= 0)
@@ -137593,11 +142374,13 @@
                     goto theend;
                 }
                 patch(list1(&s->out), s1);
-                PUSH(frag(s, list1(&s1->out)));
+                 st_push((frag(s, list1(&s1->out))), &stackp, stack_end) ;
                 break;
             }
 
-            e = POP();
+            e =  st_pop(&stackp, stack);
+            if (stackp < stack)
+                {                                                           st_error(postfix, end, p);                              vim_free(stack);                                        return NULL;                                        } ;
             s = alloc_state(mopen, e.start, NULL);
             if (s == NULL)
             {
@@ -137616,7 +142399,7 @@
                 patch(list1(&s->out1), s1);
             }
 
-            PUSH(frag(s, list1(&s1->out)));
+             st_push((frag(s, list1(&s1->out))), &stackp, stack_end) ;
             break;
 
         case NFA_BACKREF1:
@@ -137644,7 +142427,7 @@
                 goto theend;
             }
             patch(list1(&s->out), s1);
-            PUSH(frag(s, list1(&s1->out)));
+             st_push((frag(s, list1(&s1->out))), &stackp, stack_end) ;
             break;
 
         case NFA_F_PCLOSE:
@@ -137679,7 +142462,7 @@
                 goto theend;
             }
             patch(list1(&s->out), s1);
-            PUSH(frag(s, list1(&s1->out)));
+             st_push((frag(s, list1(&s1->out))), &stackp, stack_end) ;
             break;
 
         case NFA_LNUM:
@@ -137708,7 +142491,7 @@
                 goto theend;
             }
             s->val = n;
-            PUSH(frag(s, list1(&s->out)));
+             st_push((frag(s, list1(&s->out))), &stackp, stack_end) ;
             break;
           }
 
@@ -137725,7 +142508,7 @@
             {
                 goto theend;
             }
-            PUSH(frag(s, list1(&s->out)));
+             st_push((frag(s, list1(&s->out))), &stackp, stack_end) ;
             break;
 
         }
@@ -137738,17 +142521,19 @@
         goto theend;
     }
 
-    e = POP();
+    e =  st_pop(&stackp, stack);
+    if (stackp < stack)
+        {                                                           st_error(postfix, end, p);                              vim_free(stack);                                        return NULL;                                        } ;
     if (stackp != stack)
     {
         vim_free(stack);
-        EMSG_RET_NULL(_(e_nfa_regexp_while_converting_from_postfix_to_nfa_too_many_stats_left_on_stack));
+         return (emsg((_(e_nfa_regexp_while_converting_from_postfix_to_nfa_too_many_stats_left_on_stack))), rc_did_emsg = TRUE, (void *)NULL) ;
     }
 
     if (istate >= nstate)
     {
         vim_free(stack);
-        EMSG_RET_NULL(_(e_nfa_regexp_not_enough_space_to_store_whole_nfa));
+         return (emsg((_(e_nfa_regexp_not_enough_space_to_store_whole_nfa))), rc_did_emsg = TRUE, (void *)NULL) ;
     }
 
     matchstate = &state_ptr[istate++];
@@ -137851,10 +142636,10 @@
     } end;
 };
 
-#define NFA_PIM_UNUSED   0
-#define NFA_PIM_TODO     1
-#define NFA_PIM_MATCH    2
-#define NFA_PIM_NOMATCH  3
+enum { NFA_PIM_UNUSED = 0 };
+enum { NFA_PIM_TODO = 1 };
+enum { NFA_PIM_MATCH = 2 };
+enum { NFA_PIM_NOMATCH = 3 };
 
 typedef struct
 {
@@ -137890,13 +142675,13 @@
     static void
 clear_sub(regsub_T *sub)
 {
-    if (REG_MULTI)
+    if ( (rex.reg_match == NULL) )
     {
-        vim_memset(sub->list.multi, 0xff, sizeof(struct multipos) * rex.nfa_nsubexpr);
+         memset((sub->list.multi), (0xff), (sizeof(struct multipos) * rex.nfa_nsubexpr)) ;
     }
     else
     {
-        vim_memset(sub->list.line, 0, sizeof(struct linepos) * rex.nfa_nsubexpr);
+         memset((sub->list.line), (0), (sizeof(struct linepos) * rex.nfa_nsubexpr)) ;
     }
     sub->in_use = 0;
 }
@@ -137910,14 +142695,14 @@
         return;
     }
 
-    if (REG_MULTI)
+    if ( (rex.reg_match == NULL) )
     {
-        mch_memmove(&to->list.multi[0], &from->list.multi[0], sizeof(struct multipos) * from->in_use);
+         memmove((char *)(&to->list.multi[0]), (char *)(&from->list.multi[0]), sizeof(struct multipos) * from->in_use) ;
         to->orig_start_col = from->orig_start_col;
     }
     else
     {
-        mch_memmove(&to->list.line[0], &from->list.line[0], sizeof(struct linepos) * from->in_use);
+         memmove((char *)(&to->list.line[0]), (char *)(&from->list.line[0]), sizeof(struct linepos) * from->in_use) ;
     }
 }
 
@@ -137933,13 +142718,13 @@
         return;
     }
 
-    if (REG_MULTI)
+    if ( (rex.reg_match == NULL) )
     {
-        mch_memmove(&to->list.multi[1], &from->list.multi[1], sizeof(struct multipos) * (from->in_use - 1));
+         memmove((char *)(&to->list.multi[1]), (char *)(&from->list.multi[1]), sizeof(struct multipos) * (from->in_use - 1)) ;
     }
     else
     {
-        mch_memmove(&to->list.line[1], &from->list.line[1], sizeof(struct linepos) * (from->in_use - 1));
+         memmove((char *)(&to->list.line[1]), (char *)(&from->list.line[1]), sizeof(struct linepos) * (from->in_use - 1)) ;
     }
 }
 
@@ -137951,7 +142736,7 @@
         return;
     }
 
-    if (REG_MULTI)
+    if ( (rex.reg_match == NULL) )
     {
         if (from->list.multi[0].end_lnum >= 0)
         {
@@ -137979,7 +142764,7 @@
     char_u      *sp2;
 
     todo = sub1->in_use > sub2->in_use ? sub1->in_use : sub2->in_use;
-    if (REG_MULTI)
+    if ( (rex.reg_match == NULL) )
     {
         for (i = 0; i < todo; ++i)
         {
@@ -138125,7 +142910,7 @@
     {
         return FALSE;
     }
-    if (REG_MULTI)
+    if ( (rex.reg_match == NULL) )
     {
         return one->end.pos.lnum == two->end.pos.lnum
             && one->end.pos.col == two->end.pos.col;
@@ -138241,7 +143026,7 @@
     return FALSE;
 }
 
-#define ADDSTATE_HERE_OFFSET 10
+enum { ADDSTATE_HERE_OFFSET = 10 };
 
     static regsubs_T *
 addstate(nfa_list_T          *l, nfa_state_T         *state, regsubs_T           *subs_arg, nfa_pim_T           *pim, int                 off_arg, int                 depth)
@@ -138294,11 +143079,12 @@
 
         case NFA_BOL:
         case NFA_BOF:
-            if (rex.input > rex.line && *rex.input != NUL && (nfa_endp == NULL || !REG_MULTI || rex.lnum == nfa_endp->se_u.pos.lnum))
+            if (rex.input > rex.line && *rex.input != NUL && (nfa_endp == NULL || ! (rex.reg_match == NULL)  || rex.lnum == nfa_endp->se_u.pos.lnum))
             {
                 goto skip_add;
             }
 
+        __attribute__((fallthrough));
         case NFA_MOPEN1:
         case NFA_MOPEN2:
         case NFA_MOPEN3:
@@ -138357,7 +143143,7 @@
                     subs = &temp_subs;
                 }
 
-                newt = vim_realloc(l->t, newsize);
+                newt =  realloc((l->t), (newsize)) ;
                 if (newt == NULL)
                 {
                     return NULL;
@@ -138420,9 +143206,9 @@
             }
 
             save_ptr = NULL;
-            CLEAR_FIELD(save_multipos);
+              memset((&(save_multipos)), (0), (sizeof(save_multipos)))  ;
 
-            if (REG_MULTI)
+            if ( (rex.reg_match == NULL) )
             {
                 if (subidx < sub->in_use)
                 {
@@ -138481,7 +143267,7 @@
 
             if (save_in_use == -1)
             {
-                if (REG_MULTI)
+                if ( (rex.reg_match == NULL) )
                 {
                     sub->list.multi[subidx] = save_multipos;
                 }
@@ -138497,11 +143283,12 @@
             break;
 
         case NFA_MCLOSE:
-            if (rex.nfa_has_zend && (REG_MULTI ? subs->norm.list.multi[0].end_lnum >= 0 : subs->norm.list.line[0].end != NULL))
+            if (rex.nfa_has_zend && ( (rex.reg_match == NULL)  ? subs->norm.list.multi[0].end_lnum >= 0 : subs->norm.list.line[0].end != NULL))
             {
                 subs = addstate(l, state->out, subs, pim, off_arg, depth + 1);
                 break;
             }
+        __attribute__((fallthrough));
         case NFA_MCLOSE1:
         case NFA_MCLOSE2:
         case NFA_MCLOSE3:
@@ -138528,7 +143315,7 @@
             {
                 sub->in_use = subidx + 1;
             }
-            if (REG_MULTI)
+            if ( (rex.reg_match == NULL) )
             {
                 save_multipos = sub->list.multi[subidx];
                 if (off == -1)
@@ -138548,7 +143335,7 @@
             {
                 save_ptr = sub->list.line[subidx].end;
                 sub->list.line[subidx].end = rex.input + off;
-                CLEAR_FIELD(save_multipos);
+                  memset((&(save_multipos)), (0), (sizeof(save_multipos)))  ;
             }
 
             subs = addstate(l, state->out, subs, pim, off_arg, depth + 1);
@@ -138558,7 +143345,7 @@
             }
                 sub = &subs->norm;
 
-            if (REG_MULTI)
+            if ( (rex.reg_match == NULL) )
             {
                 sub->list.multi[subidx] = save_multipos;
             }
@@ -138619,16 +143406,16 @@
                 return NULL;
             }
             l->len = newlen;
-            mch_memmove(&(newl[0]), &(l->t[0]), sizeof(nfa_thread_T) * listidx);
-            mch_memmove(&(newl[listidx]), &(l->t[l->n - count]), sizeof(nfa_thread_T) * count);
-            mch_memmove(&(newl[listidx + count]), &(l->t[listidx + 1]), sizeof(nfa_thread_T) * (l->n - count - listidx - 1));
+             memmove((char *)(&(newl[0])), (char *)(&(l->t[0])), sizeof(nfa_thread_T) * listidx) ;
+             memmove((char *)(&(newl[listidx])), (char *)(&(l->t[l->n - count])), sizeof(nfa_thread_T) * count) ;
+             memmove((char *)(&(newl[listidx + count])), (char *)(&(l->t[listidx + 1])), sizeof(nfa_thread_T) * (l->n - count - listidx - 1)) ;
             vim_free(l->t);
             l->t = newl;
         }
         else
         {
-            mch_memmove(&(l->t[listidx + count]), &(l->t[listidx + 1]), sizeof(nfa_thread_T) * (l->n - listidx - 1));
-            mch_memmove(&(l->t[listidx]), &(l->t[l->n - 1]), sizeof(nfa_thread_T) * count);
+             memmove((char *)(&(l->t[listidx + count])), (char *)(&(l->t[listidx + 1])), sizeof(nfa_thread_T) * (l->n - listidx - 1)) ;
+             memmove((char *)(&(l->t[listidx])), (char *)(&(l->t[l->n - 1])), sizeof(nfa_thread_T) * count) ;
         }
     }
     --l->n;
@@ -138667,7 +143454,7 @@
             }
             break;
         case NFA_CLASS_DIGIT:
-            if (VIM_ISDIGIT(c))
+            if ( ((unsigned)(c) - '0' < 10) )
             {
                 return OK;
             }
@@ -138679,7 +143466,7 @@
             }
             break;
         case NFA_CLASS_LOWER:
-            if (MB_ISLOWER(c) && c != 170 && c != 186)
+            if ( vim_islower(c)  && c != 170 && c != 186)
             {
                 return OK;
             }
@@ -138703,7 +143490,7 @@
             }
             break;
         case NFA_CLASS_UPPER:
-            if (MB_ISUPPER(c))
+            if ( vim_isupper(c) )
             {
                 return OK;
             }
@@ -138776,7 +143563,7 @@
         return TRUE;
     }
 
-    if (REG_MULTI)
+    if ( (rex.reg_match == NULL) )
     {
         if (sub->list.multi[subidx].start_lnum < 0 || sub->list.multi[subidx].end_lnum < 0)
         {
@@ -138876,7 +143663,7 @@
 
     if (pim != NULL)
     {
-        if (REG_MULTI)
+        if ( (rex.reg_match == NULL) )
         {
             rex.input = rex.line + pim->end.pos.col;
         }
@@ -138889,7 +143676,7 @@
     if (state->c == NFA_START_INVISIBLE_BEFORE || state->c == NFA_START_INVISIBLE_BEFORE_FIRST || state->c == NFA_START_INVISIBLE_BEFORE_NEG || state->c == NFA_START_INVISIBLE_BEFORE_NEG_FIRST)
     {
         endposp = &endpos;
-        if (REG_MULTI)
+        if ( (rex.reg_match == NULL) )
         {
             if (pim == NULL)
             {
@@ -138915,7 +143702,7 @@
 
         if (state->val <= 0)
         {
-            if (REG_MULTI)
+            if ( (rex.reg_match == NULL) )
             {
                 rex.line = reg_getline(--rex.lnum);
                 if (rex.line == NULL)
@@ -138927,7 +143714,7 @@
         }
         else
         {
-            if (REG_MULTI && (int)(rex.input - rex.line) < state->val)
+            if ( (rex.reg_match == NULL)  && (int)(rex.input - rex.line) < state->val)
             {
                 rex.line = reg_getline(--rex.lnum);
                 if (rex.line == NULL)
@@ -138960,7 +143747,7 @@
         if (*listids == NULL || *listids_len < prog->nstate)
         {
             vim_free(*listids);
-            *listids = ALLOC_MULT(int, prog->nstate);
+            *listids =  (int *)alloc(sizeof(int) * (prog->nstate)) ;
             if (*listids == NULL)
             {
                 emsg(_(e_nfa_regexp_could_not_allocate_memory_for_branch_traversal));
@@ -138994,12 +143781,12 @@
     }
 
     rex.lnum = save_reglnum;
-    if (REG_MULTI)
+    if ( (rex.reg_match == NULL) )
     {
         rex.line = reg_getline(rex.lnum);
     }
     rex.input = rex.line + save_reginput_col;
-    if (result != NFA_TOO_EXPENSIVE)
+    if (result !=  (-1) )
     {
         nfa_match = save_nfa_match;
         rex.nfa_listid = save_nfa_listid;
@@ -139181,27 +143968,27 @@
     for (;;)
     {
         match = TRUE;
-        len2 = MB_CHAR2LEN(regstart);
-        if (enc_utf8 && len2 > 1 && MB_CHAR2LEN(PTR2CHAR(rex.line + col)) != len2)
+        len2 =  (has_mbyte ? mb_char2len(regstart) : 1) ;
+        if (enc_utf8 && len2 > 1 &&  (has_mbyte ? mb_char2len( (has_mbyte ? mb_ptr2char(rex.line + col) : (int)*(rex.line + col)) ) : 1)  != len2)
         {
             len2 = mb_char2len(utf_fold(regstart));
         }
-        for (len1 = 0; match_text[len1] != NUL; len1 += MB_CHAR2LEN(c1))
+        for (len1 = 0; match_text[len1] != NUL; len1 +=  (has_mbyte ? mb_char2len(c1) : 1) )
         {
-            c1 = PTR2CHAR(match_text + len1);
-            c2 = PTR2CHAR(rex.line + col + len2);
-            if (c1 != c2 && (!rex.reg_ic || MB_CASEFOLD(c1) != MB_CASEFOLD(c2)))
+            c1 =  (has_mbyte ? mb_ptr2char(match_text + len1) : (int)*(match_text + len1)) ;
+            c2 =  (has_mbyte ? mb_ptr2char(rex.line + col + len2) : (int)*(rex.line + col + len2)) ;
+            if (c1 != c2 && (!rex.reg_ic ||  (enc_utf8 ? utf_fold(c1) :  vim_tolower(c1) )  !=  (enc_utf8 ? utf_fold(c2) :  vim_tolower(c2) ) ))
             {
                 match = FALSE;
                 break;
             }
             len2 += enc_utf8 ? utf_ptr2len(rex.line + col + len2)
-                                                             : MB_CHAR2LEN(c2);
+                                                             :  (has_mbyte ? mb_char2len(c2) : 1) ;
         }
-        if (match && !(enc_utf8 && utf_iscomposing(PTR2CHAR(rex.line + col + len2))))
+        if (match && !(enc_utf8 && utf_iscomposing( (has_mbyte ? mb_ptr2char(rex.line + col + len2) : (int)*(rex.line + col + len2)) )))
         {
             cleanup_subexpr();
-            if (REG_MULTI)
+            if ( (rex.reg_match == NULL) )
             {
                 rex.reg_startpos[0].lnum = rex.lnum;
                 rex.reg_startpos[0].col = col;
@@ -139217,7 +144004,7 @@
             return 1L;
         }
 
-        col += MB_CHAR2LEN(regstart);
+        col +=  (has_mbyte ? mb_char2len(regstart) : 1) ;
         if (skip_to_start(regstart, &col) == FAIL)
         {
             break;
@@ -139266,7 +144053,7 @@
             }
         }
         bytelen += (int)(s - ls);
-        if (found || !with_nl || !REG_MULTI || rex.reg_line_lbr || lnum >= rex.reg_maxline)
+        if (found || !with_nl || ! (rex.reg_match == NULL)  || rex.reg_line_lbr || lnum >= rex.reg_maxline)
         {
             break;
         }
@@ -139351,7 +144138,7 @@
 
     if (toplevel)
     {
-        if (REG_MULTI)
+        if ( (rex.reg_match == NULL) )
         {
             m->norm.list.multi[0].start_lnum = rex.lnum;
             m->norm.list.multi[0].start_col = (colnr_T)(rex.input - rex.line);
@@ -139370,12 +144157,10 @@
     }
     if (r == NULL)
     {
-        nfa_match = NFA_TOO_EXPENSIVE;
+        nfa_match =  (-1) ;
         goto theend;
     }
 
-#define ADD_STATE_IF_MATCH(state)           if (result)                             {                                           add_state = state->out;                 add_off = clen;                     }
-
     for (;;)
     {
         int     curc;
@@ -139412,7 +144197,7 @@
         ++rex.nfa_listid;
         if (prog->re_engine == AUTOMATIC_ENGINE && (rex.nfa_listid >= NFA_MAX_STATES))
         {
-            nfa_match = NFA_TOO_EXPENSIVE;
+            nfa_match =  (-1) ;
             goto theend;
         }
 
@@ -139427,7 +144212,7 @@
         for (listidx = 0; listidx < thislist->n; ++listidx)
         {
             static int  breakcheck_count = 0;
-            if (unlikely(++breakcheck_count >= 100000))
+            if ( __builtin_expect((++breakcheck_count >= 100000), 0) )
             {
                 ui_breakcheck();
                 breakcheck_count = 0;
@@ -139462,7 +144247,7 @@
             case NFA_END_INVISIBLE:
             case NFA_END_INVISIBLE_NEG:
             case NFA_END_PATTERN:
-                if (nfa_endp != NULL && (REG_MULTI ? (rex.lnum != nfa_endp->se_u.pos.lnum || (int)(rex.input - rex.line) != nfa_endp->se_u.pos.col) : rex.input != nfa_endp->se_u.ptr))
+                if (nfa_endp != NULL && ( (rex.reg_match == NULL)  ? (rex.lnum != nfa_endp->se_u.pos.lnum || (int)(rex.input - rex.line) != nfa_endp->se_u.pos.col) : rex.input != nfa_endp->se_u.ptr))
                 {
                     break;
                 }
@@ -139494,7 +144279,7 @@
                         copy_sub_off(&m->norm, &t->subs.norm);
 
                         result = recursive_regmatch(t->state, NULL, prog, submatch, m, &listids, &listids_len);
-                        if (result == NFA_TOO_EXPENSIVE)
+                        if (result ==  (-1) )
                         {
                             nfa_match = result;
                             goto theend;
@@ -139517,7 +144302,7 @@
                         pim.state = t->state;
                         pim.result = NFA_PIM_TODO;
                         pim.subs.norm.in_use = 0;
-                        if (REG_MULTI)
+                        if ( (rex.reg_match == NULL) )
                         {
                             pim.end.pos.col = (int)(rex.input - rex.line);
                             pim.end.pos.lnum = rex.lnum;
@@ -139529,7 +144314,7 @@
 
                         if (addstate_here(thislist, t->state->out1->out, &t->subs, &pim, &listidx) == NULL)
                         {
-                            nfa_match = NFA_TOO_EXPENSIVE;
+                            nfa_match =  (-1) ;
                             goto theend;
                         }
                     }
@@ -139559,7 +144344,7 @@
                 copy_sub_off(&m->norm, &t->subs.norm);
 
                 result = recursive_regmatch(t->state, NULL, prog, submatch, m, &listids, &listids_len);
-                if (result == NFA_TOO_EXPENSIVE)
+                if (result ==  (-1) )
                 {
                     nfa_match = result;
                     goto theend;
@@ -139569,7 +144354,7 @@
                     int bytelen;
 
                     copy_sub_off(&t->subs.norm, &m->norm);
-                    if (REG_MULTI)
+                    if ( (rex.reg_match == NULL) )
                     {
                         bytelen = m->norm.list.multi[0].end_col
                                                   - (int)(rex.input - rex.line);
@@ -139677,7 +144462,7 @@
                 break;
 
             case NFA_BOF:
-                if (rex.lnum == 0 && rex.input == rex.line && (!REG_MULTI || rex.reg_firstlnum == 1))
+                if (rex.lnum == 0 && rex.input == rex.line && (! (rex.reg_match == NULL)  || rex.reg_firstlnum == 1))
                 {
                     add_here = TRUE;
                     add_state = t->state->out;
@@ -139767,12 +144552,13 @@
                 }
 
                 end = t->state->out1;
-                ADD_STATE_IF_MATCH(end);
+                 if (result)
+                     {                                           add_state = end->out;                 add_off = clen;                     } ;
                 break;
             }
 
             case NFA_NEWL:
-                if (curc == NUL && !rex.reg_line_lbr && REG_MULTI && rex.lnum <= rex.reg_maxline)
+                if (curc == NUL && !rex.reg_line_lbr &&  (rex.reg_match == NULL)  && rex.lnum <= rex.reg_maxline)
                 {
                     go_to_nextline = TRUE;
                     add_state = t->state->out;
@@ -139878,7 +144664,8 @@
                         if (t->state->out->out1 != NULL && t->state->out->out1->c == NFA_END_COMPOSING)
                         {
                             end = t->state->out->out1;
-                            ADD_STATE_IF_MATCH(end);
+                             if (result)
+                                 {                                           add_state = end->out;                 add_off = clen;                     } ;
                         }
                         break;
                     }
@@ -139899,12 +144686,12 @@
                         }
                         if (rex.reg_ic)
                         {
-                            int curc_low = MB_CASEFOLD(curc);
+                            int curc_low =  (enc_utf8 ? utf_fold(curc) :  vim_tolower(curc) ) ;
                             int done = FALSE;
 
                             for ( ; c1 <= c2; ++c1)
                             {
-                                if (MB_CASEFOLD(c1) == curc_low)
+                                if ( (enc_utf8 ? utf_fold(c1) :  vim_tolower(c1) )  == curc_low)
                                 {
                                     result = result_if_matched;
                                     done = TRUE;
@@ -139917,7 +144704,7 @@
                             }
                         }
                     }
-                    else if (state->c < 0 ? check_char_class(state->c, curc) : (curc == state->c || (rex.reg_ic && MB_CASEFOLD(curc) == MB_CASEFOLD(state->c))))
+                    else if (state->c < 0 ? check_char_class(state->c, curc) : (curc == state->c || (rex.reg_ic &&  (enc_utf8 ? utf_fold(curc) :  vim_tolower(curc) )  ==  (enc_utf8 ? utf_fold(state->c) :  vim_tolower(state->c) ) )))
                     {
                         result = result_if_matched;
                         break;
@@ -139955,155 +144742,185 @@
 
             case NFA_IDENT:
                 result = vim_isIDc(curc);
-                ADD_STATE_IF_MATCH(t->state);
+                 if (result)
+                     {                                           add_state = t->state->out;                 add_off = clen;                     } ;
                 break;
 
             case NFA_SIDENT:
-                result = !VIM_ISDIGIT(curc) && vim_isIDc(curc);
-                ADD_STATE_IF_MATCH(t->state);
+                result = ! ((unsigned)(curc) - '0' < 10)  && vim_isIDc(curc);
+                 if (result)
+                     {                                           add_state = t->state->out;                 add_off = clen;                     } ;
                 break;
 
             case NFA_KWORD:
                 result = vim_iswordp_buf(rex.input, rex.reg_buf);
-                ADD_STATE_IF_MATCH(t->state);
+                 if (result)
+                     {                                           add_state = t->state->out;                 add_off = clen;                     } ;
                 break;
 
             case NFA_SKWORD:
-                result = !VIM_ISDIGIT(curc)
+                result = ! ((unsigned)(curc) - '0' < 10) 
                                      && vim_iswordp_buf(rex.input, rex.reg_buf);
-                ADD_STATE_IF_MATCH(t->state);
+                 if (result)
+                     {                                           add_state = t->state->out;                 add_off = clen;                     } ;
                 break;
 
             case NFA_FNAME:
                 result = vim_isfilec(curc);
-                ADD_STATE_IF_MATCH(t->state);
+                 if (result)
+                     {                                           add_state = t->state->out;                 add_off = clen;                     } ;
                 break;
 
             case NFA_SFNAME:
-                result = !VIM_ISDIGIT(curc) && vim_isfilec(curc);
-                ADD_STATE_IF_MATCH(t->state);
+                result = ! ((unsigned)(curc) - '0' < 10)  && vim_isfilec(curc);
+                 if (result)
+                     {                                           add_state = t->state->out;                 add_off = clen;                     } ;
                 break;
 
             case NFA_PRINT:
-                result = vim_isprintc(PTR2CHAR(rex.input));
-                ADD_STATE_IF_MATCH(t->state);
+                result = vim_isprintc( (has_mbyte ? mb_ptr2char(rex.input) : (int)*(rex.input)) );
+                 if (result)
+                     {                                           add_state = t->state->out;                 add_off = clen;                     } ;
                 break;
 
             case NFA_SPRINT:
-                result = !VIM_ISDIGIT(curc) && vim_isprintc(PTR2CHAR(rex.input));
-                ADD_STATE_IF_MATCH(t->state);
+                result = ! ((unsigned)(curc) - '0' < 10)  && vim_isprintc( (has_mbyte ? mb_ptr2char(rex.input) : (int)*(rex.input)) );
+                 if (result)
+                     {                                           add_state = t->state->out;                 add_off = clen;                     } ;
                 break;
 
             case NFA_WHITE:
-                result = VIM_ISWHITE(curc);
-                ADD_STATE_IF_MATCH(t->state);
+                result =  ((curc) == ' ' || (curc) == '\t') ;
+                 if (result)
+                     {                                           add_state = t->state->out;                 add_off = clen;                     } ;
                 break;
 
             case NFA_NWHITE:
-                result = curc != NUL && !VIM_ISWHITE(curc);
-                ADD_STATE_IF_MATCH(t->state);
+                result = curc != NUL && ! ((curc) == ' ' || (curc) == '\t') ;
+                 if (result)
+                     {                                           add_state = t->state->out;                 add_off = clen;                     } ;
                 break;
 
             case NFA_DIGIT:
-                result = ri_digit(curc);
-                ADD_STATE_IF_MATCH(t->state);
+                result =  ((curc) < 0x100 && (class_tab[curc] & RI_DIGIT)) ;
+                 if (result)
+                     {                                           add_state = t->state->out;                 add_off = clen;                     } ;
                 break;
 
             case NFA_NDIGIT:
-                result = curc != NUL && !ri_digit(curc);
-                ADD_STATE_IF_MATCH(t->state);
+                result = curc != NUL && ! ((curc) < 0x100 && (class_tab[curc] & RI_DIGIT)) ;
+                 if (result)
+                     {                                           add_state = t->state->out;                 add_off = clen;                     } ;
                 break;
 
             case NFA_HEX:
-                result = ri_hex(curc);
-                ADD_STATE_IF_MATCH(t->state);
+                result =  ((curc) < 0x100 && (class_tab[curc] & RI_HEX)) ;
+                 if (result)
+                     {                                           add_state = t->state->out;                 add_off = clen;                     } ;
                 break;
 
             case NFA_NHEX:
-                result = curc != NUL && !ri_hex(curc);
-                ADD_STATE_IF_MATCH(t->state);
+                result = curc != NUL && ! ((curc) < 0x100 && (class_tab[curc] & RI_HEX)) ;
+                 if (result)
+                     {                                           add_state = t->state->out;                 add_off = clen;                     } ;
                 break;
 
             case NFA_OCTAL:
-                result = ri_octal(curc);
-                ADD_STATE_IF_MATCH(t->state);
+                result =  ((curc) < 0x100 && (class_tab[curc] & RI_OCTAL)) ;
+                 if (result)
+                     {                                           add_state = t->state->out;                 add_off = clen;                     } ;
                 break;
 
             case NFA_NOCTAL:
-                result = curc != NUL && !ri_octal(curc);
-                ADD_STATE_IF_MATCH(t->state);
+                result = curc != NUL && ! ((curc) < 0x100 && (class_tab[curc] & RI_OCTAL)) ;
+                 if (result)
+                     {                                           add_state = t->state->out;                 add_off = clen;                     } ;
                 break;
 
             case NFA_WORD:
-                result = ri_word(curc);
-                ADD_STATE_IF_MATCH(t->state);
+                result =  ((curc) < 0x100 && (class_tab[curc] & RI_WORD)) ;
+                 if (result)
+                     {                                           add_state = t->state->out;                 add_off = clen;                     } ;
                 break;
 
             case NFA_NWORD:
-                result = curc != NUL && !ri_word(curc);
-                ADD_STATE_IF_MATCH(t->state);
+                result = curc != NUL && ! ((curc) < 0x100 && (class_tab[curc] & RI_WORD)) ;
+                 if (result)
+                     {                                           add_state = t->state->out;                 add_off = clen;                     } ;
                 break;
 
             case NFA_HEAD:
-                result = ri_head(curc);
-                ADD_STATE_IF_MATCH(t->state);
+                result =  ((curc) < 0x100 && (class_tab[curc] & RI_HEAD)) ;
+                 if (result)
+                     {                                           add_state = t->state->out;                 add_off = clen;                     } ;
                 break;
 
             case NFA_NHEAD:
-                result = curc != NUL && !ri_head(curc);
-                ADD_STATE_IF_MATCH(t->state);
+                result = curc != NUL && ! ((curc) < 0x100 && (class_tab[curc] & RI_HEAD)) ;
+                 if (result)
+                     {                                           add_state = t->state->out;                 add_off = clen;                     } ;
                 break;
 
             case NFA_ALPHA:
-                result = ri_alpha(curc);
-                ADD_STATE_IF_MATCH(t->state);
+                result =  ((curc) < 0x100 && (class_tab[curc] & RI_ALPHA)) ;
+                 if (result)
+                     {                                           add_state = t->state->out;                 add_off = clen;                     } ;
                 break;
 
             case NFA_NALPHA:
-                result = curc != NUL && !ri_alpha(curc);
-                ADD_STATE_IF_MATCH(t->state);
+                result = curc != NUL && ! ((curc) < 0x100 && (class_tab[curc] & RI_ALPHA)) ;
+                 if (result)
+                     {                                           add_state = t->state->out;                 add_off = clen;                     } ;
                 break;
 
             case NFA_LOWER:
-                result = ri_lower(curc);
-                ADD_STATE_IF_MATCH(t->state);
+                result =  ((curc) < 0x100 && (class_tab[curc] & RI_LOWER)) ;
+                 if (result)
+                     {                                           add_state = t->state->out;                 add_off = clen;                     } ;
                 break;
 
             case NFA_NLOWER:
-                result = curc != NUL && !ri_lower(curc);
-                ADD_STATE_IF_MATCH(t->state);
+                result = curc != NUL && ! ((curc) < 0x100 && (class_tab[curc] & RI_LOWER)) ;
+                 if (result)
+                     {                                           add_state = t->state->out;                 add_off = clen;                     } ;
                 break;
 
             case NFA_UPPER:
-                result = ri_upper(curc);
-                ADD_STATE_IF_MATCH(t->state);
+                result =  ((curc) < 0x100 && (class_tab[curc] & RI_UPPER)) ;
+                 if (result)
+                     {                                           add_state = t->state->out;                 add_off = clen;                     } ;
                 break;
 
             case NFA_NUPPER:
-                result = curc != NUL && !ri_upper(curc);
-                ADD_STATE_IF_MATCH(t->state);
+                result = curc != NUL && ! ((curc) < 0x100 && (class_tab[curc] & RI_UPPER)) ;
+                 if (result)
+                     {                                           add_state = t->state->out;                 add_off = clen;                     } ;
                 break;
 
             case NFA_LOWER_IC:
-                result = ri_lower(curc) || (rex.reg_ic && ri_upper(curc));
-                ADD_STATE_IF_MATCH(t->state);
+                result =  ((curc) < 0x100 && (class_tab[curc] & RI_LOWER))  || (rex.reg_ic &&  ((curc) < 0x100 && (class_tab[curc] & RI_UPPER)) );
+                 if (result)
+                     {                                           add_state = t->state->out;                 add_off = clen;                     } ;
                 break;
 
             case NFA_NLOWER_IC:
                 result = curc != NUL
-                        && !(ri_lower(curc) || (rex.reg_ic && ri_upper(curc)));
-                ADD_STATE_IF_MATCH(t->state);
+                        && !( ((curc) < 0x100 && (class_tab[curc] & RI_LOWER))  || (rex.reg_ic &&  ((curc) < 0x100 && (class_tab[curc] & RI_UPPER)) ));
+                 if (result)
+                     {                                           add_state = t->state->out;                 add_off = clen;                     } ;
                 break;
 
             case NFA_UPPER_IC:
-                result = ri_upper(curc) || (rex.reg_ic && ri_lower(curc));
-                ADD_STATE_IF_MATCH(t->state);
+                result =  ((curc) < 0x100 && (class_tab[curc] & RI_UPPER))  || (rex.reg_ic &&  ((curc) < 0x100 && (class_tab[curc] & RI_LOWER)) );
+                 if (result)
+                     {                                           add_state = t->state->out;                 add_off = clen;                     } ;
                 break;
 
             case NFA_NUPPER_IC:
                 result = curc != NUL
-                        && !(ri_upper(curc) || (rex.reg_ic && ri_lower(curc)));
-                ADD_STATE_IF_MATCH(t->state);
+                        && !( ((curc) < 0x100 && (class_tab[curc] & RI_UPPER))  || (rex.reg_ic &&  ((curc) < 0x100 && (class_tab[curc] & RI_LOWER)) ));
+                 if (result)
+                     {                                           add_state = t->state->out;                 add_off = clen;                     } ;
                 break;
 
             case NFA_BACKREF1:
@@ -140219,7 +145036,7 @@
             case NFA_SKIP_NL:
               if (curc == NUL)
               {
-                  if (!REG_MULTI || rex.reg_line_lbr || rex.lnum >= rex.reg_maxline)
+                  if (! (rex.reg_match == NULL)  || rex.reg_line_lbr || rex.lnum >= rex.reg_maxline)
                   {
                       break;
                   }
@@ -140265,7 +145082,7 @@
             case NFA_LNUM:
             case NFA_LNUM_GT:
             case NFA_LNUM_LT:
-                result = (REG_MULTI && nfa_re_num_cmp(t->state->val, t->state->c - NFA_LNUM, (long_u)(rex.lnum + rex.reg_firstlnum)));
+                result = ( (rex.reg_match == NULL)  && nfa_re_num_cmp(t->state->val, t->state->c - NFA_LNUM, (long_u)(rex.lnum + rex.reg_firstlnum)));
                 if (result)
                 {
                     add_here = TRUE;
@@ -140309,11 +145126,11 @@
                     }
                     if (!result)
                     {
-                        linenr_T    lnum = REG_MULTI
+                        linenr_T    lnum =  (rex.reg_match == NULL) 
                                             ? rex.reg_firstlnum + rex.lnum : 1;
                         long_u      vcol;
 
-                        if (REG_MULTI && (lnum <= 0 || lnum > wp->w_buffer->b_ml.ml_line_count))
+                        if ( (rex.reg_match == NULL)  && (lnum <= 0 || lnum > wp->w_buffer->b_ml.ml_line_count))
                         {
                             lnum = 1;
                         }
@@ -140333,11 +145150,11 @@
             case NFA_MARK_LT:
               {
                 pos_T   *pos;
-                size_t  col = REG_MULTI ? rex.input - rex.line : 0;
+                size_t  col =  (rex.reg_match == NULL)  ? rex.input - rex.line : 0;
 
                 pos = getmark_buf(rex.reg_buf, t->state->val, FALSE);
 
-                if (REG_MULTI)
+                if ( (rex.reg_match == NULL) )
                 {
                     rex.line = reg_getline(rex.lnum);
                     rex.input = rex.line + col;
@@ -140399,13 +145216,14 @@
 
                 if (!result && rex.reg_ic)
                 {
-                    result = MB_CASEFOLD(c) == MB_CASEFOLD(curc);
+                    result =  (enc_utf8 ? utf_fold(c) :  vim_tolower(c) )  ==  (enc_utf8 ? utf_fold(curc) :  vim_tolower(curc) ) ;
                 }
                 if (result && enc_utf8 && !rex.reg_icombine)
                 {
                     clen = utf_ptr2len(rex.input);
                 }
-                ADD_STATE_IF_MATCH(t->state);
+                 if (result)
+                     {                                           add_state = t->state->out;                 add_off = clen;                     } ;
                 break;
               }
 
@@ -140473,14 +145291,14 @@
                 }
                 if (r == NULL)
                 {
-                    nfa_match = NFA_TOO_EXPENSIVE;
+                    nfa_match =  (-1) ;
                     goto theend;
                 }
             }
 
         }
 
-        if (nfa_match == FALSE && ((toplevel && rex.lnum == 0 && clen != 0 && (rex.reg_maxcol == 0 || (colnr_T)(rex.input - rex.line) < rex.reg_maxcol)) || (nfa_endp != NULL && (REG_MULTI ? (rex.lnum < nfa_endp->se_u.pos.lnum || (rex.lnum == nfa_endp->se_u.pos.lnum && (int)(rex.input - rex.line) < nfa_endp->se_u.pos.col)) : rex.input < nfa_endp->se_u.ptr))))
+        if (nfa_match == FALSE && ((toplevel && rex.lnum == 0 && clen != 0 && (rex.reg_maxcol == 0 || (colnr_T)(rex.input - rex.line) < rex.reg_maxcol)) || (nfa_endp != NULL && ( (rex.reg_match == NULL)  ? (rex.lnum < nfa_endp->se_u.pos.lnum || (rex.lnum == nfa_endp->se_u.pos.lnum && (int)(rex.input - rex.line) < nfa_endp->se_u.pos.col)) : rex.input < nfa_endp->se_u.ptr))))
         {
             if (toplevel)
             {
@@ -140501,8 +145319,8 @@
                     }
                     else
                     {
-                        c = PTR2CHAR(rex.input + clen);
-                        if (c != prog->regstart && (!rex.reg_ic || MB_CASEFOLD(c) != MB_CASEFOLD(prog->regstart)))
+                        c =  (has_mbyte ? mb_ptr2char(rex.input + clen) : (int)*(rex.input + clen)) ;
+                        if (c != prog->regstart && (!rex.reg_ic ||  (enc_utf8 ? utf_fold(c) :  vim_tolower(c) )  !=  (enc_utf8 ? utf_fold(prog->regstart) :  vim_tolower(prog->regstart) ) ))
                         {
                             add = FALSE;
                         }
@@ -140511,7 +145329,7 @@
 
                 if (add)
                 {
-                    if (REG_MULTI)
+                    if ( (rex.reg_match == NULL) )
                     {
                         m->norm.list.multi[0].start_col =
                                          (colnr_T)(rex.input - rex.line) + clen;
@@ -140524,7 +145342,7 @@
                     }
                     if (addstate(nextlist, start->out, m, NULL, clen, 0) == NULL)
                     {
-                        nfa_match = NFA_TOO_EXPENSIVE;
+                        nfa_match =  (-1) ;
                         goto theend;
                     }
                 }
@@ -140535,7 +145353,7 @@
                 char_u      *save_input = rex.input;
                 linenr_T    save_lnum = rex.lnum;
 
-                if (REG_MULTI && clen == 0 && nfa_endp != NULL && rex.lnum < nfa_endp->se_u.pos.lnum)
+                if ( (rex.reg_match == NULL)  && clen == 0 && nfa_endp != NULL && rex.lnum < nfa_endp->se_u.pos.lnum)
                 {
                     char_u  *next_line = reg_getline(rex.lnum + 1);
 
@@ -140555,7 +145373,7 @@
 
                 if (r == NULL)
                 {
-                    nfa_match = NFA_TOO_EXPENSIVE;
+                    nfa_match =  (-1) ;
                     goto theend;
                 }
             }
@@ -140566,7 +145384,7 @@
         {
             rex.input += clen;
         }
-        else if (go_to_nextline || (nfa_endp != NULL && REG_MULTI && rex.lnum < nfa_endp->se_u.pos.lnum))
+        else if (go_to_nextline || (nfa_endp != NULL &&  (rex.reg_match == NULL)  && rex.lnum < nfa_endp->se_u.pos.lnum))
         {
             reg_nextline();
         }
@@ -140599,7 +145417,7 @@
 }
 
     static long
-nfa_regtry(nfa_regprog_T   *prog, colnr_T         col, int             *timed_out UNUSED)
+nfa_regtry(nfa_regprog_T   *prog, colnr_T         col, int             *timed_out  __attribute__((unused)) )
 {
     int         i;
     regsubs_T subs;
@@ -140617,13 +145435,13 @@
     {
         return 0;
     }
-    else if (result == NFA_TOO_EXPENSIVE)
+    else if (result ==  (-1) )
     {
         return result;
     }
 
     cleanup_subexpr();
-    if (REG_MULTI)
+    if ( (rex.reg_match == NULL) )
     {
         for (i = 0; i < subs.norm.in_use; i++)
         {
@@ -140682,7 +145500,7 @@
     int             i;
     colnr_T         col = startcol;
 
-    if (REG_MULTI)
+    if ( (rex.reg_match == NULL) )
     {
         prog = (nfa_regprog_T *)rex.reg_mmatch->regprog;
         line = reg_getline((linenr_T)0);
@@ -140742,7 +145560,7 @@
         if (prog->match_text != NULL && *prog->match_text != NUL && !rex.reg_icombine)
         {
             retval = find_match_text(&col, prog->regstart, prog->match_text);
-            if (REG_MULTI)
+            if ( (rex.reg_match == NULL) )
             {
                 rex.reg_mmatch->rmm_matchcol = col;
             }
@@ -140772,7 +145590,7 @@
 theend:
     if (retval > 0)
     {
-        if (REG_MULTI)
+        if ( (rex.reg_match == NULL) )
         {
             lpos_T *start = &rex.reg_mmatch->startpos[0];
             lpos_T *end = &rex.reg_mmatch->endpos[0];
@@ -140858,13 +145676,15 @@
     prog->pattern = vim_strsave(expr);
 
 out:
-    VIM_CLEAR(post_start);
+     vim_free(post_start);
+     (post_start) = NULL;
     post_ptr = post_end = NULL;
     state_ptr = NULL;
     return (regprog_T *)prog;
 
 fail:
-    VIM_CLEAR(prog);
+     vim_free(prog);
+     (prog) = NULL;
     goto out;
 }
 
@@ -140924,7 +145744,7 @@
 
     regexp_engine = p_re;
 
-    if (STRNCMP(expr, "\\%#=", 4) == 0)
+    if ( strncmp((char *)(expr), (char *)("\\%#="), (4))  == 0)
     {
         int newengine = expr[4] - '0';
 
@@ -141006,7 +145826,7 @@
     result = rmp->regprog->engine->regexec_nl(rmp, line, col, nl);
     rmp->regprog->re_in_use = FALSE;
 
-    if (rmp->regprog->re_engine == AUTOMATIC_ENGINE && result == NFA_TOO_EXPENSIVE)
+    if (rmp->regprog->re_engine == AUTOMATIC_ENGINE && result ==  (-1) )
     {
         int    save_p_re = p_re;
         int    re_flags = rmp->regprog->re_flags;
@@ -141079,7 +145899,7 @@
     result = rmp->regprog->engine->regexec_multi(rmp, win, buf, lnum, col, timed_out);
     rmp->regprog->re_in_use = FALSE;
 
-    if (rmp->regprog->re_engine == AUTOMATIC_ENGINE && result == NFA_TOO_EXPENSIVE)
+    if (rmp->regprog->re_engine == AUTOMATIC_ENGINE && result ==  (-1) )
     {
         int    save_p_re = p_re;
         int    re_flags = rmp->regprog->re_flags;
@@ -141131,10 +145951,6 @@
 static int      yank_copy_line(struct block_def *bd, long y_idx, int exclude_trailing_space);
 static void     dis_msg(char_u *p, int skip_esc);
 
-
-
-
-
     void
 reset_y_append(void)
 {
@@ -141144,7 +145960,7 @@
     int
 valid_yank_reg(int     regname, int     writing)
 {
-    if (       (regname > 0 && ASCII_ISALNUM(regname)) || (!writing && vim_strchr((char_u *) "/#.%:", regname) != NULL) || regname == '"' || regname == '-' || regname == '_')
+    if (       (regname > 0 &&  ( ( ((unsigned)(regname) - 'A' < 26)  ||  ((unsigned)(regname) - 'a' < 26) )  ||  ((unsigned)(regname) - '0' < 10) ) ) || (!writing && vim_strchr((char_u *) "/#.%:", regname) != NULL) || regname == '"' || regname == '-' || regname == '_')
     {
         return TRUE;
     }
@@ -141169,17 +145985,17 @@
         return ret;
     }
     i = regname;
-    if (VIM_ISDIGIT(i))
+    if ( ((unsigned)(i) - '0' < 10) )
     {
         i -= '0';
     }
-    else if (ASCII_ISLOWER(i))
+    else if ( ((unsigned)(i) - 'a' < 26) )
     {
-        i = CharOrdLow(i) + 10;
+        i =  ((i) - 'a')  + 10;
     }
-    else if (ASCII_ISUPPER(i))
+    else if ( ((unsigned)(i) - 'A' < 26) )
     {
-        i = CharOrdUp(i) + 10;
+        i =  ((i) - 'A')  + 10;
         y_append = TRUE;
     }
     else if (regname == '-')
@@ -141205,7 +146021,7 @@
     int         i;
 
     get_yank_register(name, 0);
-    reg = ALLOC_ONE(yankreg_T);
+    reg =  (yankreg_T *)alloc(sizeof(yankreg_T)) ;
     if (reg == NULL)
     {
         return (void *)NULL;
@@ -141220,7 +146036,7 @@
         }
         else
         {
-            reg->y_array = ALLOC_MULT(string_T, reg->y_size);
+            reg->y_array =  (string_T *)alloc(sizeof(string_T) * (reg->y_size)) ;
         }
         if (reg->y_array != NULL)
         {
@@ -141285,7 +146101,7 @@
 
     if (reg_recording == 0)
     {
-        if (c < 0 || (!ASCII_ISALNUM(c) && c != '"'))
+        if (c < 0 || (! ( ( ((unsigned)(c) - 'A' < 26)  ||  ((unsigned)(c) - 'a' < 26) )  ||  ((unsigned)(c) - '0' < 10) )  && c != '"'))
         {
             retval = FAIL;
         }
@@ -141338,7 +146154,7 @@
         return OK;
     }
 
-    plen = STRLEN(p);
+    plen =  strlen((char *)(p)) ;
     get_yank_register(regname, TRUE);
     if (y_append && y_current->y_array != NULL)
     {
@@ -141354,8 +146170,8 @@
             vim_free(p);
             return FAIL;
         }
-        STRCPY(tmp, pp->string);
-        STRCPY(tmp + pp->length, p);
+         strcpy((char *)(tmp), (char *)(pp->string)) ;
+         strcpy((char *)(tmp + pp->length), (char *)(p)) ;
         vim_free(p);
         vim_free(pp->string);
         pp->string = tmp;
@@ -141364,7 +146180,7 @@
     else
     {
         free_yank_all();
-        if ((y_current->y_array = ALLOC_ONE(string_T)) == NULL)
+        if ((y_current->y_array =  (string_T *)alloc(sizeof(string_T)) ) == NULL)
         {
             vim_free(p);
             return FAIL;
@@ -141471,11 +146287,12 @@
             emsg(_(e_no_previous_command_line));
             return FAIL;
         }
-        VIM_CLEAR(new_last_cmdline);
+         vim_free(new_last_cmdline);
+         (new_last_cmdline) = NULL;
         p = vim_strsave_escaped_ext(last_cmdline, (char_u *)"\001\002\003\004\005\006\007" "\010\011\012\013\014\015\016\017" "\020\021\022\023\024\025\026\027" "\030\031\032\033\034\035\036\037", Ctrl_V, FALSE);
         if (p != NULL)
         {
-            if (VIsual_active && STRNCMP(p, "'<,'>", 5) == 0)
+            if (VIsual_active &&  strncmp((char *)(p), (char *)("'<,'>"), (5))  == 0)
             {
                 retval = put_in_typebuf(p + 5, TRUE, TRUE, silent);
             }
@@ -141505,7 +146322,7 @@
             return FAIL;
         }
 
-        remap = colon ? REMAP_NONE : REMAP_YES;
+        remap = colon ?  (-1)  : REMAP_YES;
 
         put_reedit_in_typebuf(silent);
         for (i = y_current->y_size; --i >= 0; )
@@ -141570,7 +146387,7 @@
         return;
     }
 
-    char_u      buf[] = { K_SPECIAL, KS_EXTRA, KE_COMMAND,
+    char_u      buf[] = {  (0x80) , KS_EXTRA, KE_COMMAND,
         's', 't', 'a', 'r', 't', 'i', CAR, NUL };
     if (restart_edit == 'R')
     {
@@ -141584,7 +146401,7 @@
     {
         buf[8] = '!';
     }
-    if (ins_typebuf(buf, REMAP_NONE, 0, TRUE, silent) == OK)
+    if (ins_typebuf(buf,  (-1) , 0, TRUE, silent) == OK)
     {
         restart_edit = NUL;
     }
@@ -141598,7 +146415,7 @@
     put_reedit_in_typebuf(silent);
     if (colon)
     {
-        retval = ins_typebuf((char_u *)"\n", REMAP_NONE, 0, TRUE, silent);
+        retval = ins_typebuf((char_u *)"\n",  (-1) , 0, TRUE, silent);
     }
     if (retval == OK)
     {
@@ -141618,7 +146435,7 @@
         }
         else
         {
-            retval = ins_typebuf(p, esc ? REMAP_NONE : REMAP_YES, 0, TRUE, silent);
+            retval = ins_typebuf(p, esc ?  (-1)  : REMAP_YES, 0, TRUE, silent);
         }
         if (esc)
         {
@@ -141627,7 +146444,7 @@
     }
     if (colon && retval == OK)
     {
-        retval = ins_typebuf((char_u *)":", REMAP_NONE, 0, TRUE, silent);
+        retval = ins_typebuf((char_u *)":",  (-1) , 0, TRUE, silent);
     }
     return retval;
 }
@@ -141684,7 +146501,7 @@
             {
                 if (regname == '-' && y_current->y_type == MCHAR)
                 {
-                    int dir = BACKWARD;
+                    int dir =  (-1) ;
                     if ((State & REPLACE_FLAG) != 0)
                     {
                         pos_T curpos;
@@ -141879,9 +146696,12 @@
 
     for (i = n; --i >= 0; )
     {
-        VIM_CLEAR_STRING(y_current->y_array[i]);
+         vim_free(y_current->y_array[i].string);
+         (y_current->y_array[i].string) = NULL;
+         y_current->y_array[i].length = 0;
     }
-    VIM_CLEAR(y_current->y_array);
+     vim_free(y_current->y_array);
+     (y_current->y_array) = NULL;
 }
 
     void
@@ -141975,7 +146795,9 @@
                 y_current->y_array[y_idx].length = ml_get_len(lnum);
                 if ((y_current->y_array[y_idx].string = vim_strnsave(ml_get(lnum), y_current->y_array[y_idx].length)) == NULL)
                 {
-                    VIM_CLEAR_STRING(y_current->y_array[y_idx]);
+                     vim_free(y_current->y_array[y_idx].string);
+                     (y_current->y_array[y_idx].string) = NULL;
+                     y_current->y_array[y_idx].length = 0;
                     goto fail;
                 }
                 break;
@@ -141986,7 +146808,7 @@
 
                     charwise_block_prep(oap->start, oap->end, &bd, lnum, oap->inclusive);
 
-                    tmp = (int)STRLEN(bd.textstart);
+                    tmp = (int) strlen((char *)(bd.textstart)) ;
                     if (tmp < bd.textlen)
                     {
                         bd.textlen = tmp;
@@ -142006,7 +146828,7 @@
         string_T *new_ptr;
         long j;
 
-        new_ptr = ALLOC_MULT(string_T, curr->y_size + y_current->y_size);
+        new_ptr =  (string_T *)alloc(sizeof(string_T) * (curr->y_size + y_current->y_size)) ;
         if (new_ptr == NULL)
         {
             goto fail;
@@ -142033,13 +146855,15 @@
             }
 
             --j;
-            STRCPY(pnew, curr->y_array[j].string);
-            STRCPY(pnew + curr->y_array[j].length, y_current->y_array[0].string);
+             strcpy((char *)(pnew), (char *)(curr->y_array[j].string)) ;
+             strcpy((char *)(pnew + curr->y_array[j].length), (char *)(y_current->y_array[0].string)) ;
             vim_free(curr->y_array[j].string);
             curr->y_array[j].string = pnew;
             curr->y_array[j].length = curr->y_array[j].length + y_current->y_array[0].length;
             ++j;
-            VIM_CLEAR_STRING(y_current->y_array[0]);
+             vim_free(y_current->y_array[0].string);
+             (y_current->y_array[0].string) = NULL;
+             y_current->y_array[0].length = 0;
             y_idx = 1;
         }
         else
@@ -142123,17 +146947,17 @@
         return FAIL;
     }
     y_current->y_array[y_idx].string = pnew;
-    vim_memset(pnew, ' ', (size_t)bd->startspaces);
+     memset((pnew), (' '), ((size_t)bd->startspaces)) ;
     pnew += bd->startspaces;
-    mch_memmove(pnew, bd->textstart, (size_t)bd->textlen);
+     memmove((char *)(pnew), (char *)(bd->textstart), (size_t)bd->textlen) ;
     pnew += bd->textlen;
-    vim_memset(pnew, ' ', (size_t)bd->endspaces);
+     memset((pnew), (' '), ((size_t)bd->endspaces)) ;
     pnew += bd->endspaces;
     if (exclude_trailing_space)
     {
         int s = bd->textlen + bd->endspaces;
 
-        while (s > 0 && VIM_ISWHITE(*(bd->textstart + s - 1)))
+        while (s > 0 &&  ((*(bd->textstart + s - 1)) == ' ' || (*(bd->textstart + s - 1)) == '\t') )
         {
             s = s - (*mb_head_off)(bd->textstart, bd->textstart + s - 1) - 1;
             pnew--;
@@ -142212,7 +147036,7 @@
 
     if (insert_string.string != NULL)
     {
-        insert_string.length = STRLEN(insert_string.string);
+        insert_string.length =  strlen((char *)(insert_string.string)) ;
 
         y_type = MCHAR;
         {
@@ -142247,7 +147071,7 @@
             plen = ml_get_cursor_len();
             if (dir == FORWARD && *p != NUL)
             {
-                MB_PTR_ADV(p);
+                 p += (*mb_ptr2len)(p) ;
             }
             ptr = vim_strnsave(p, plen - (size_t)(p - p_orig));
             if (ptr == NULL)
@@ -142261,7 +147085,7 @@
             p = oldp + curwin->w_cursor.col;
             if (dir == FORWARD && *p != NUL)
             {
-                MB_PTR_ADV(p);
+                 p += (*mb_ptr2len)(p) ;
             }
             ptr = vim_strnsave(oldp, (size_t)(p - oldp));
             if (ptr == NULL)
@@ -142311,7 +147135,7 @@
         {
             ++lnum;
         }
-        if ((BUFEMPTY() ? u_save(0, 2) : u_save(lnum - 1, lnum)) == FAIL)
+        if (( (curbuf->b_ml.ml_line_count == 1 && *ml_get((linenr_T)1) == NUL)  ? u_save(0, 2) : u_save(lnum - 1, lnum)) == FAIL)
         {
             goto end;
         }
@@ -142397,7 +147221,7 @@
             }
             if (c == TAB)
             {
-                if (dir == BACKWARD && curwin->w_cursor.col)
+                if (dir ==  (-1)  && curwin->w_cursor.col)
                 {
                     curwin->w_cursor.col--;
                 }
@@ -142498,20 +147322,20 @@
             }
 
             ptr = newp;
-            mch_memmove(ptr, oldp, (size_t)bd.textcol);
+             memmove((char *)(ptr), (char *)(oldp), (size_t)bd.textcol) ;
             ptr += bd.textcol;
 
-            vim_memset(ptr, ' ', (size_t)bd.startspaces);
+             memset((ptr), (' '), ((size_t)bd.startspaces)) ;
             ptr += bd.startspaces;
 
             for (j = 0; j < count; ++j)
             {
-                mch_memmove(ptr, y_array[i].string, (size_t)yanklen);
+                 memmove((char *)(ptr), (char *)(y_array[i].string), (size_t)yanklen) ;
                 ptr += yanklen;
 
                 if ((j < count - 1 || !shortline) && spaces > 0)
                 {
-                    vim_memset(ptr, ' ', (size_t)spaces);
+                     memset((ptr), (' '), ((size_t)spaces)) ;
                     ptr += spaces;
                 }
                 else
@@ -142520,10 +147344,10 @@
                 }
             }
 
-            vim_memset(ptr, ' ', (size_t)bd.endspaces);
+             memset((ptr), (' '), ((size_t)bd.endspaces)) ;
             ptr += bd.endspaces;
 
-            mch_memmove(ptr, oldp + bd.textcol + delcount, (size_t)(oldlen - bd.textcol - delcount + 1));
+             memmove((char *)(ptr), (char *)(oldp + bd.textcol + delcount), (size_t)(oldlen - bd.textcol - delcount + 1)) ;
             ml_replace(curwin->w_cursor.lnum, newp, FALSE);
 
             ++curwin->w_cursor.lnum;
@@ -142596,7 +147420,7 @@
             }
             curbuf->b_op_start = curwin->w_cursor;
         }
-        else if (dir == BACKWARD)
+        else if (dir ==  (-1) )
         {
             --lnum;
         }
@@ -142668,14 +147492,14 @@
                     {
                         goto end;
                     }
-                    mch_memmove(newp, oldp, (size_t)col);
+                     memmove((char *)(newp), (char *)(oldp), (size_t)col) ;
                     ptr = newp + col;
                     for (i = 0; i < count; ++i)
                     {
-                        mch_memmove(ptr, y_array[0].string, (size_t)yanklen);
+                         memmove((char *)(ptr), (char *)(y_array[0].string), (size_t)yanklen) ;
                         ptr += yanklen;
                     }
-                    mch_memmove(ptr, oldp + col, (size_t)(oldlen - col) + 1);
+                     memmove((char *)(ptr), (char *)(oldp + col), (size_t)(oldlen - col) + 1) ;
 
                     first_byte_off = mb_head_off(newp, ptr - 1);
 
@@ -142741,8 +147565,8 @@
                     {
                         goto error;
                     }
-                    STRCPY(newp, y_array[y_size - 1].string);
-                    STRCPY(newp + totlen, ptr);
+                     strcpy((char *)(newp), (char *)(y_array[y_size - 1].string)) ;
+                     strcpy((char *)(newp + totlen), (char *)(ptr)) ;
                     ml_append(lnum, newp, (colnr_T)0, FALSE);
                     ++new_lnum;
                     vim_free(newp);
@@ -142753,8 +147577,8 @@
                     {
                         goto error;
                     }
-                    mch_memmove(newp, oldp, (size_t)col);
-                    mch_memmove(newp + col, y_array[0].string, (size_t)(yanklen + 1));
+                     memmove((char *)(newp), (char *)(oldp), (size_t)col) ;
+                     memmove((char *)(newp + col), (char *)(y_array[0].string), (size_t)(yanklen + 1)) ;
                     ml_replace(lnum, newp, FALSE);
 
                     curwin->w_cursor.lnum = lnum;
@@ -142824,7 +147648,7 @@
                     curbuf->b_op_start.lnum++;
                 }
             }
-            mark_adjust(curbuf->b_op_start.lnum + (y_type == MCHAR), (linenr_T)MAXLNUM, nr_lines, 0L);
+            mark_adjust(curbuf->b_op_start.lnum + (y_type == MCHAR), (linenr_T) LONG_MAX , nr_lines, 0L);
 
             if (y_type == MCHAR)
             {
@@ -142976,7 +147800,7 @@
     {
         arg = NULL;
     }
-    attr = HL_ATTR(HLF_8);
+    attr =  highlight_attr[(int)(HLF_8)] ;
 
     msg_puts_title(_("\nType Name Content"));
     for (i = -1; i < NUM_REGISTERS && !got_int; ++i)
@@ -143208,7 +148032,7 @@
     {
         nn = wp->w_width;
     }
-        screen_fill(W_WINROW(wp) + row, W_WINROW(wp) + endrow, wp->w_wincol + off, (int)wp->w_wincol + nn, c1, c2, attr);
+        screen_fill( (wp->w_winrow)  + row,  (wp->w_winrow)  + endrow, wp->w_wincol + off, (int)wp->w_wincol + nn, c1, c2, attr);
     return nn;
 }
 
@@ -143216,7 +148040,7 @@
 win_draw_end(win_T       *wp, int         c1, int         c2, int         draw_margin, int         row, int         endrow, hlf_T       hl)
 {
     int         n = 0;
-    int         attr = HL_ATTR(hl);
+    int         attr =  highlight_attr[(int)(hl)] ;
     int         win_attr = get_win_attr(wp);
     bool        override_success =
         push_highlight_overrides(wp->w_hl, wp->w_hl_len);
@@ -143225,14 +148049,14 @@
 
     if (draw_margin)
     {
-        if ((wp->w_p_nu || wp->w_p_rnu) && vim_strchr(p_cpo, CPO_NUMCOL) == NULL)
+        if ((wp-> w_onebuf_opt.wo_nu  || wp-> w_onebuf_opt.wo_rnu ) && vim_strchr(p_cpo, CPO_NUMCOL) == NULL)
         {
-            n = screen_fill_end(wp, ' ', ' ', n, number_width(wp) + 1, row, endrow, hl_combine_attr(win_attr, HL_ATTR(HLF_N)));
+            n = screen_fill_end(wp, ' ', ' ', n,  7  + 1, row, endrow, hl_combine_attr(win_attr,  highlight_attr[(int)(HLF_N)] ));
         }
     }
 
     {
-        screen_fill(W_WINROW(wp) + row, W_WINROW(wp) + endrow, wp->w_wincol + n, (int)W_ENDCOL(wp), c1, c2, attr);
+        screen_fill( (wp->w_winrow)  + row,  (wp->w_winrow)  + endrow, wp->w_wincol + n, (int) ((wp)->w_wincol + (wp)->w_width) , c1, c2, attr);
     }
 
     set_empty_rows(wp, row);
@@ -143265,7 +148089,7 @@
     static int
 char_needs_redraw(int off_from, int off_to, int cols)
 {
-    if (cols > 0 && ((ScreenLines[off_from] != ScreenLines[off_to] || ScreenAttrs[off_from] != ScreenAttrs[off_to]) || (enc_dbcs != 0 && MB_BYTE2LEN(ScreenLines[off_from]) > 1 && (enc_dbcs == DBCS_JPNU && ScreenLines[off_from] == 0x8e ? ScreenLines2[off_from] != ScreenLines2[off_to] : (cols > 1 && ScreenLines[off_from + 1] != ScreenLines[off_to + 1]))) || (enc_utf8 && (ScreenLinesUC[off_from] != ScreenLinesUC[off_to] || (ScreenLinesUC[off_from] != 0 && comp_char_differs(off_from, off_to)) || ((*mb_off2cells)(off_from, off_from + cols) > 1 && ScreenLines[off_from + 1] != ScreenLines[off_to + 1])))))
+    if (cols > 0 && ((ScreenLines[off_from] != ScreenLines[off_to] || ScreenAttrs[off_from] != ScreenAttrs[off_to]) || (enc_dbcs != 0 &&  mb_bytelen_tab[ScreenLines[off_from]]  > 1 && (enc_dbcs == DBCS_JPNU && ScreenLines[off_from] == 0x8e ? ScreenLines2[off_from] != ScreenLines2[off_to] : (cols > 1 && ScreenLines[off_from + 1] != ScreenLines[off_to + 1]))) || (enc_utf8 && (ScreenLinesUC[off_from] != ScreenLinesUC[off_to] || (ScreenLinesUC[off_from] != 0 && comp_char_differs(off_from, off_to)) || ((*mb_off2cells)(off_from, off_from + cols) > 1 && ScreenLines[off_from + 1] != ScreenLines[off_to + 1])))))
     {
         return TRUE;
     }
@@ -143293,7 +148117,7 @@
 }
 
     void
-screen_line(win_T   *wp, int     row, int     coloff, int     endcol, int     clear_width, colnr_T last_vcol, int     flags UNUSED)
+screen_line(win_T   *wp, int     row, int     coloff, int     endcol, int     clear_width, colnr_T last_vcol, int     flags  __attribute__((unused)) )
 {
     unsigned        off_from;
     unsigned        off_to;
@@ -143354,7 +148178,7 @@
             if (       p_wiv && !force && ScreenAttrs[off_to] != 0 && ScreenAttrs[off_from] != ScreenAttrs[off_to])
             {
                 windgoto(row, col + coloff);
-                out_str(T_CE);
+                out_str( ( term_strings[(int)(KS_CE)] ) );
                 screen_start();
                 force = TRUE;
                 redraw_next = TRUE;
@@ -143481,7 +148305,6 @@
 
     if (clear_width > 0)
     {
-
         while (col < clear_width && ScreenLines[off_to] == ' ' && ScreenAttrs[off_to] == 0 && (!enc_utf8 || ScreenLinesUC[off_to] == 0))
         {
             ScreenCols[off_to] =
@@ -143551,14 +148374,14 @@
         return;
     }
 
-    int content_end = W_WINROW(wp) + wp->w_height;
+    int content_end =  (wp->w_winrow)  + wp->w_height;
 
-    int start_row = (row == 0) ? wp->w_winrow : W_WINROW(wp) + row;
+    int start_row = (row == 0) ? wp->w_winrow :  (wp->w_winrow)  + row;
     for (int r = start_row; r < content_end; ++r)
     {
         int hl;
         int c = fillchar_vsep(&hl, wp, r);
-        screen_fill(r, r + 1, W_ENDCOL(wp), W_ENDCOL(wp) + 1, c, ' ', hl);
+        screen_fill(r, r + 1,  ((wp)->w_wincol + (wp)->w_width) ,  ((wp)->w_wincol + (wp)->w_width)  + 1, c, ' ', hl);
     }
 
     if (wp->w_status_height != 0)
@@ -143575,7 +148398,7 @@
         }
         for (int r = content_end; r < content_end + wp->w_status_height; ++r)
         {
-            screen_fill(r, r + 1, W_ENDCOL(wp), W_ENDCOL(wp) + 1, c, ' ', hl);
+            screen_fill(r, r + 1,  ((wp)->w_wincol + (wp)->w_width) ,  ((wp)->w_wincol + (wp)->w_width)  + 1, c, ' ', hl);
         }
     }
 }
@@ -143676,7 +148499,7 @@
         bytes[1] = ScreenLines2[off];
         bytes[2] = NUL;
     }
-    else if (enc_dbcs && MB_BYTE2LEN(bytes[0]) > 1)
+    else if (enc_dbcs &&  mb_bytelen_tab[bytes[0]]  > 1)
     {
         bytes[1] = ScreenLines[off + 1];
         bytes[2] = NUL;
@@ -143951,7 +148774,7 @@
 
     if (attr > HL_ALL)
     {
-        if (IS_CTERM)
+        if ( (t_colors > 1) )
         {
             aep = syn_cterm_attr2entry(attr);
         }
@@ -143968,49 +148791,49 @@
             attr = aep->ae_attr;
         }
     }
-    if ((attr & HL_BOLD) && *T_MD != NUL)
+    if ((attr & HL_BOLD) && * ( term_strings[(int)(KS_MD)] )  != NUL)
     {
-        out_str(T_MD);
+        out_str( ( term_strings[(int)(KS_MD)] ) );
     }
     else if (aep != NULL && cterm_normal_fg_bold && (t_colors > 1 && aep->ae_u.cterm.fg_color))
     {
-        out_str(T_ME);
+        out_str( ( term_strings[(int)(KS_ME)] ) );
     }
-    if ((attr & HL_STANDOUT) && *T_SO != NUL)
+    if ((attr & HL_STANDOUT) && * ( term_strings[(int)(KS_SO)] )  != NUL)
     {
-        out_str(T_SO);
+        out_str( ( term_strings[(int)(KS_SO)] ) );
     }
-    if ((attr & HL_UNDERCURL) && *T_UCS != NUL)
+    if ((attr & HL_UNDERCURL) && * ( term_strings[(int)(KS_UCS)] )  != NUL)
     {
-        out_str(T_UCS);
+        out_str( ( term_strings[(int)(KS_UCS)] ) );
     }
-    if ((attr & HL_UNDERDOUBLE) && *T_USS != NUL)
+    if ((attr & HL_UNDERDOUBLE) && * ( term_strings[(int)(KS_USS)] )  != NUL)
     {
-        out_str(T_USS);
+        out_str( ( term_strings[(int)(KS_USS)] ) );
     }
-    if ((attr & HL_UNDERDOTTED) && *T_DS != NUL)
+    if ((attr & HL_UNDERDOTTED) && * ( term_strings[(int)(KS_DS)] )  != NUL)
     {
-        out_str(T_DS);
+        out_str( ( term_strings[(int)(KS_DS)] ) );
     }
-    if ((attr & HL_UNDERDASHED) && *T_CDS != NUL)
+    if ((attr & HL_UNDERDASHED) && * ( term_strings[(int)(KS_CDS)] )  != NUL)
     {
-        out_str(T_CDS);
+        out_str( ( term_strings[(int)(KS_CDS)] ) );
     }
-    if (((attr & HL_UNDERLINE) || ((attr & HL_UNDERCURL) && *T_UCS == NUL) || ((attr & HL_UNDERDOUBLE) && *T_USS == NUL) || ((attr & HL_UNDERDOTTED) && *T_DS == NUL) || ((attr & HL_UNDERDASHED) && *T_CDS == NUL)) && *T_US != NUL)
+    if (((attr & HL_UNDERLINE) || ((attr & HL_UNDERCURL) && * ( term_strings[(int)(KS_UCS)] )  == NUL) || ((attr & HL_UNDERDOUBLE) && * ( term_strings[(int)(KS_USS)] )  == NUL) || ((attr & HL_UNDERDOTTED) && * ( term_strings[(int)(KS_DS)] )  == NUL) || ((attr & HL_UNDERDASHED) && * ( term_strings[(int)(KS_CDS)] )  == NUL)) && * ( term_strings[(int)(KS_US)] )  != NUL)
     {
-        out_str(T_US);
+        out_str( ( term_strings[(int)(KS_US)] ) );
     }
-    if ((attr & HL_ITALIC) && *T_CZH != NUL)
+    if ((attr & HL_ITALIC) && * ( term_strings[(int)(KS_CZH)] )  != NUL)
     {
-        out_str(T_CZH);
+        out_str( ( term_strings[(int)(KS_CZH)] ) );
     }
-    if ((attr & HL_INVERSE) && *T_MR != NUL)
+    if ((attr & HL_INVERSE) && * ( term_strings[(int)(KS_MR)] )  != NUL)
     {
-        out_str(T_MR);
+        out_str( ( term_strings[(int)(KS_MR)] ) );
     }
-    if ((attr & HL_STRIKETHROUGH) && *T_STS != NUL)
+    if ((attr & HL_STRIKETHROUGH) && * ( term_strings[(int)(KS_STS)] )  != NUL)
     {
-        out_str(T_STS);
+        out_str( ( term_strings[(int)(KS_STS)] ) );
     }
 
     if (aep != NULL)
@@ -144041,7 +148864,7 @@
                 }
             }
 
-        if (!IS_CTERM)
+        if (! (t_colors > 1) )
         {
             if (aep->ae_u.term.start != NULL)
             {
@@ -144065,7 +148888,7 @@
             {
                 attrentry_T *aep;
 
-                if (IS_CTERM)
+                if ( (t_colors > 1) )
                 {
                     aep = syn_cterm_attr2entry(screen_attr);
                     if (aep != NULL && ((aep->ae_u.cterm.fg_color) || (aep->ae_u.cterm.bg_color)))
@@ -144078,7 +148901,7 @@
                     aep = syn_term_attr2entry(screen_attr);
                     if (aep != NULL && aep->ae_u.term.stop != NULL)
                     {
-                        if (STRCMP(aep->ae_u.term.stop, T_ME) == 0)
+                        if ( strcmp((char *)(aep->ae_u.term.stop), (char *)( ( term_strings[(int)(KS_ME)] ) ))  == 0)
                         {
                             do_ME = TRUE;
                         }
@@ -144100,63 +148923,63 @@
 
             if (screen_attr & HL_STANDOUT)
             {
-                if (STRCMP(T_SE, T_ME) == 0)
+                if ( strcmp((char *)( ( term_strings[(int)(KS_SE)] ) ), (char *)( ( term_strings[(int)(KS_ME)] ) ))  == 0)
                 {
                     do_ME = TRUE;
                 }
                 else
                 {
-                    out_str(T_SE);
+                    out_str( ( term_strings[(int)(KS_SE)] ) );
                 }
             }
             is_under = (screen_attr & (HL_UNDERCURL | HL_UNDERDOUBLE | HL_UNDERDOTTED | HL_UNDERDASHED));
-            if (is_under && *T_UCE != NUL)
+            if (is_under && * ( term_strings[(int)(KS_UCE)] )  != NUL)
             {
-                if (STRCMP(T_UCE, T_ME) == 0)
+                if ( strcmp((char *)( ( term_strings[(int)(KS_UCE)] ) ), (char *)( ( term_strings[(int)(KS_ME)] ) ))  == 0)
                 {
                     do_ME = TRUE;
                 }
                 else
                 {
-                    out_str(T_UCE);
+                    out_str( ( term_strings[(int)(KS_UCE)] ) );
                 }
             }
-            if ((screen_attr & HL_UNDERLINE) || (is_under && *T_UCE == NUL))
+            if ((screen_attr & HL_UNDERLINE) || (is_under && * ( term_strings[(int)(KS_UCE)] )  == NUL))
             {
-                if (STRCMP(T_UE, T_ME) == 0)
+                if ( strcmp((char *)( ( term_strings[(int)(KS_UE)] ) ), (char *)( ( term_strings[(int)(KS_ME)] ) ))  == 0)
                 {
                     do_ME = TRUE;
                 }
                 else
                 {
-                    out_str(T_UE);
+                    out_str( ( term_strings[(int)(KS_UE)] ) );
                 }
             }
             if (screen_attr & HL_ITALIC)
             {
-                if (STRCMP(T_CZR, T_ME) == 0)
+                if ( strcmp((char *)( ( term_strings[(int)(KS_CZR)] ) ), (char *)( ( term_strings[(int)(KS_ME)] ) ))  == 0)
                 {
                     do_ME = TRUE;
                 }
                 else
                 {
-                    out_str(T_CZR);
+                    out_str( ( term_strings[(int)(KS_CZR)] ) );
                 }
             }
             if (screen_attr & HL_STRIKETHROUGH)
             {
-                if (STRCMP(T_STE, T_ME) == 0)
+                if ( strcmp((char *)( ( term_strings[(int)(KS_STE)] ) ), (char *)( ( term_strings[(int)(KS_ME)] ) ))  == 0)
                 {
                     do_ME = TRUE;
                 }
                 else
                 {
-                    out_str(T_STE);
+                    out_str( ( term_strings[(int)(KS_STE)] ) );
                 }
             }
             if (do_ME || (screen_attr & (HL_BOLD | HL_INVERSE)))
             {
-                out_str(T_ME);
+                out_str( ( term_strings[(int)(KS_ME)] ) );
             }
 
             {
@@ -144176,7 +148999,7 @@
                     }
                     if (cterm_normal_fg_bold)
                     {
-                        out_str(T_MD);
+                        out_str( ( term_strings[(int)(KS_MD)] ) );
                     }
                 }
             }
@@ -144188,19 +149011,19 @@
     void
 reset_cterm_colors(void)
 {
-    if (!IS_CTERM)
+    if (! (t_colors > 1) )
     {
         return;
     }
 
         if (cterm_normal_fg_color > 0 || cterm_normal_bg_color > 0)
         {
-            out_str(T_OP);
+            out_str( ( term_strings[(int)(KS_OP)] ) );
             screen_attr = -1;
         }
     if (cterm_normal_fg_bold)
     {
-        out_str(T_ME);
+        out_str( ( term_strings[(int)(KS_ME)] ) );
         screen_attr = -1;
     }
 }
@@ -144215,7 +149038,7 @@
         return;
     }
 
-    if (*T_XN == NUL && row == screen_Rows - 1 && col == screen_Columns - 1)
+    if (* ( term_strings[(int)(KS_XN)] )  == NUL && row == screen_Rows - 1 && col == screen_Columns - 1)
     {
         ScreenAttrs[off] = (sattr_T)-1;
         ScreenCols[off] = -1;
@@ -144405,7 +149228,7 @@
         return;
     }
 
-    norm_term = (!IS_CTERM);
+    norm_term = (! (t_colors > 1) );
     for (row = start_row; row < end_row; ++row)
     {
         if (has_mbyte)
@@ -144422,7 +149245,7 @@
             }
         }
         did_delete = FALSE;
-        if (c2 == ' ' && end_col == Columns && can_clear(T_CE) && (attr == 0 || (norm_term && attr <= HL_ALL && ((attr & ~(HL_BOLD | HL_ITALIC)) == 0))))
+        if (c2 == ' ' && end_col == Columns && can_clear( ( term_strings[(int)(KS_CE)] ) ) && (attr == 0 || (norm_term && attr <= HL_ALL && ((attr & ~(HL_BOLD | HL_ITALIC)) == 0))))
         {
             col = start_col;
             if (c1 != ' ')
@@ -144452,7 +149275,7 @@
                 col = off - LineOffset[row];
                 screen_stop_highlight();
                 term_windgoto(row, col);
-                out_str(T_CE);
+                out_str( ( term_strings[(int)(KS_CE)] ) );
                 screen_start();
                 col = end_col - col;
                 while (col--)
@@ -144652,8 +149475,13 @@
 
     comp_col();
 
-    FOR_ALL_TAB_WINDOWS(tp, wp)
+     for ((tp) = first_tabpage; (tp) != NULL; (tp) = (tp)->tp_next)
+     {
+         for ((wp) = ((tp) == curtab)            ? firstwin : (tp)->tp_firstwin; (wp); (wp) = (wp)->w_next)
+         {
         win_free_lsize(wp);
+         }
+     }
     for (int i = 0; i < AUCMD_WIN_COUNT; ++i)
     {
         if (aucmd_win[i].auc_win != NULL)
@@ -144662,27 +149490,29 @@
         }
     }
 
-    new_ScreenLines = LALLOC_MULT(schar_T, (Rows + 1) * Columns);
-    vim_memset(new_ScreenLinesC, 0, sizeof(u8char_T *) * MAX_MCO);
+    new_ScreenLines =  (schar_T *)lalloc(sizeof(schar_T) * ((Rows + 1) * Columns), FALSE) ;
+     memset((new_ScreenLinesC), (0), (sizeof(u8char_T *) * MAX_MCO)) ;
     if (enc_utf8)
     {
-        new_ScreenLinesUC = LALLOC_MULT(u8char_T, (Rows + 1) * Columns);
+        new_ScreenLinesUC =  (u8char_T *)lalloc(sizeof(u8char_T) * ((Rows + 1) * Columns), FALSE) ;
         for (int i = 0; i < p_mco; ++i)
         {
-            new_ScreenLinesC[i] = LALLOC_CLEAR_MULT(u8char_T, (Rows + 1) * Columns);
+            new_ScreenLinesC[i] =  (u8char_T *)lalloc_clear(sizeof(u8char_T) * ((Rows + 1) * Columns), FALSE) ;
         }
     }
     if (enc_dbcs == DBCS_JPNU)
     {
-        new_ScreenLines2 = LALLOC_MULT(schar_T, (Rows + 1) * Columns);
+        new_ScreenLines2 =  (schar_T *)lalloc(sizeof(schar_T) * ((Rows + 1) * Columns), FALSE) ;
     }
-    new_ScreenAttrs = LALLOC_MULT(sattr_T, (Rows + 1) * Columns);
-    new_ScreenCols = LALLOC_CLEAR_MULT(colnr_T, (Rows + 1) * Columns);
-    new_LineOffset = LALLOC_MULT(unsigned, Rows);
-    new_LineWraps = LALLOC_MULT(char_u, Rows);
-    new_TabPageIdxs = LALLOC_MULT(short, Columns);
-
-    FOR_ALL_TAB_WINDOWS(tp, wp)
+    new_ScreenAttrs =  (sattr_T *)lalloc(sizeof(sattr_T) * ((Rows + 1) * Columns), FALSE) ;
+    new_ScreenCols =  (colnr_T *)lalloc_clear(sizeof(colnr_T) * ((Rows + 1) * Columns), FALSE) ;
+    new_LineOffset =  (unsigned *)lalloc(sizeof(unsigned) * (Rows), FALSE) ;
+    new_LineWraps =  (char_u *)lalloc(sizeof(char_u) * (Rows), FALSE) ;
+    new_TabPageIdxs =  (short *)lalloc(sizeof(short) * (Columns), FALSE) ;
+
+     for ((tp) = first_tabpage; (tp) != NULL; (tp) = (tp)->tp_next)
+     {
+         for ((wp) = ((tp) == curtab)            ? firstwin : (tp)->tp_firstwin; (wp); (wp) = (wp)->w_next)
     {
         if (win_alloc_lines(wp) == FAIL)
         {
@@ -144690,6 +149520,7 @@
             goto give_up;
         }
     }
+     }
     for (int i = 0; i < AUCMD_WIN_COUNT; ++i)
     {
         if (aucmd_win[i].auc_win != NULL && aucmd_win[i].auc_win->w_lines == NULL && win_alloc_lines(aucmd_win[i].auc_win) == FAIL)
@@ -144717,18 +149548,27 @@
 
             done_outofmem_msg = TRUE;
         }
-        VIM_CLEAR(new_ScreenLines);
-        VIM_CLEAR(new_ScreenLinesUC);
+         vim_free(new_ScreenLines);
+         (new_ScreenLines) = NULL;
+         vim_free(new_ScreenLinesUC);
+         (new_ScreenLinesUC) = NULL;
         for (int i = 0; i < p_mco; ++i)
         {
-            VIM_CLEAR(new_ScreenLinesC[i]);
+             vim_free(new_ScreenLinesC[i]);
+             (new_ScreenLinesC[i]) = NULL;
         }
-        VIM_CLEAR(new_ScreenLines2);
-        VIM_CLEAR(new_ScreenAttrs);
-        VIM_CLEAR(new_ScreenCols);
-        VIM_CLEAR(new_LineOffset);
-        VIM_CLEAR(new_LineWraps);
-        VIM_CLEAR(new_TabPageIdxs);
+         vim_free(new_ScreenLines2);
+         (new_ScreenLines2) = NULL;
+         vim_free(new_ScreenAttrs);
+         (new_ScreenAttrs) = NULL;
+         vim_free(new_ScreenCols);
+         (new_ScreenCols) = NULL;
+         vim_free(new_LineOffset);
+         (new_LineOffset) = NULL;
+         vim_free(new_LineWraps);
+         (new_LineWraps) = NULL;
+         vim_free(new_TabPageIdxs);
+         (new_TabPageIdxs) = NULL;
     }
     else
     {
@@ -144739,21 +149579,21 @@
             new_LineOffset[new_row] = new_row * Columns;
             new_LineWraps[new_row] = FALSE;
 
-            (void)vim_memset(new_ScreenLines + new_row * Columns, ' ', (size_t)Columns * sizeof(schar_T));
+            (void) memset((new_ScreenLines + new_row * Columns), (' '), ((size_t)Columns * sizeof(schar_T))) ;
             if (enc_utf8)
             {
-                (void)vim_memset(new_ScreenLinesUC + new_row * Columns, 0, (size_t)Columns * sizeof(u8char_T));
+                (void) memset((new_ScreenLinesUC + new_row * Columns), (0), ((size_t)Columns * sizeof(u8char_T))) ;
                 for (int i = 0; i < p_mco; ++i)
                 {
-                    (void)vim_memset(new_ScreenLinesC[i] + new_row * Columns, 0, (size_t)Columns * sizeof(u8char_T));
+                    (void) memset((new_ScreenLinesC[i] + new_row * Columns), (0), ((size_t)Columns * sizeof(u8char_T))) ;
                 }
             }
             if (enc_dbcs == DBCS_JPNU)
             {
-                (void)vim_memset(new_ScreenLines2 + new_row * Columns, 0, (size_t)Columns * sizeof(schar_T));
+                (void) memset((new_ScreenLines2 + new_row * Columns), (0), ((size_t)Columns * sizeof(schar_T))) ;
             }
-            (void)vim_memset(new_ScreenAttrs + new_row * Columns, 0, (size_t)Columns * sizeof(sattr_T));
-            (void)vim_memset(new_ScreenCols + new_row * Columns, 0, (size_t)Columns * sizeof(colnr_T));
+            (void) memset((new_ScreenAttrs + new_row * Columns), (0), ((size_t)Columns * sizeof(sattr_T))) ;
+            (void) memset((new_ScreenCols + new_row * Columns), (0), ((size_t)Columns * sizeof(colnr_T))) ;
 
             if (!doclear)
             {
@@ -144770,22 +149610,22 @@
                     }
                     if (!(enc_utf8 && ScreenLinesUC == NULL) && p_mco == Screen_mco)
                     {
-                        mch_memmove(new_ScreenLines + new_LineOffset[new_row], ScreenLines + LineOffset[old_row], (size_t)len * sizeof(schar_T));
+                         memmove((char *)(new_ScreenLines + new_LineOffset[new_row]), (char *)(ScreenLines + LineOffset[old_row]), (size_t)len * sizeof(schar_T)) ;
                     }
                     if (enc_utf8 && ScreenLinesUC != NULL && p_mco == Screen_mco)
                     {
-                        mch_memmove(new_ScreenLinesUC + new_LineOffset[new_row], ScreenLinesUC + LineOffset[old_row], (size_t)len * sizeof(u8char_T));
+                         memmove((char *)(new_ScreenLinesUC + new_LineOffset[new_row]), (char *)(ScreenLinesUC + LineOffset[old_row]), (size_t)len * sizeof(u8char_T)) ;
                         for (int i = 0; i < p_mco; ++i)
                         {
-                            mch_memmove(new_ScreenLinesC[i] + new_LineOffset[new_row], ScreenLinesC[i] + LineOffset[old_row], (size_t)len * sizeof(u8char_T));
+                             memmove((char *)(new_ScreenLinesC[i] + new_LineOffset[new_row]), (char *)(ScreenLinesC[i] + LineOffset[old_row]), (size_t)len * sizeof(u8char_T)) ;
                         }
                     }
                     if (enc_dbcs == DBCS_JPNU && ScreenLines2 != NULL)
                     {
-                        mch_memmove(new_ScreenLines2 + new_LineOffset[new_row], ScreenLines2 + LineOffset[old_row], (size_t)len * sizeof(schar_T));
+                         memmove((char *)(new_ScreenLines2 + new_LineOffset[new_row]), (char *)(ScreenLines2 + LineOffset[old_row]), (size_t)len * sizeof(schar_T)) ;
                     }
-                    mch_memmove(new_ScreenAttrs + new_LineOffset[new_row], ScreenAttrs + LineOffset[old_row], (size_t)len * sizeof(sattr_T));
-                    mch_memmove(new_ScreenCols + new_LineOffset[new_row], ScreenCols + LineOffset[old_row], (size_t)len * sizeof(colnr_T));
+                     memmove((char *)(new_ScreenAttrs + new_LineOffset[new_row]), (char *)(ScreenAttrs + LineOffset[old_row]), (size_t)len * sizeof(sattr_T)) ;
+                     memmove((char *)(new_ScreenCols + new_LineOffset[new_row]), (char *)(ScreenCols + LineOffset[old_row]), (size_t)len * sizeof(colnr_T)) ;
                 }
             }
         }
@@ -144838,18 +149678,27 @@
 {
     int         i;
 
-    VIM_CLEAR(ScreenLinesUC);
+     vim_free(ScreenLinesUC);
+     (ScreenLinesUC) = NULL;
     for (i = 0; i < Screen_mco; ++i)
     {
-        VIM_CLEAR(ScreenLinesC[i]);
+         vim_free(ScreenLinesC[i]);
+         (ScreenLinesC[i]) = NULL;
     }
-    VIM_CLEAR(ScreenLines2);
-    VIM_CLEAR(ScreenLines);
-    VIM_CLEAR(ScreenAttrs);
-    VIM_CLEAR(ScreenCols);
-    VIM_CLEAR(LineOffset);
-    VIM_CLEAR(LineWraps);
-    VIM_CLEAR(TabPageIdxs);
+     vim_free(ScreenLines2);
+     (ScreenLines2) = NULL;
+     vim_free(ScreenLines);
+     (ScreenLines) = NULL;
+     vim_free(ScreenAttrs);
+     (ScreenAttrs) = NULL;
+     vim_free(ScreenCols);
+     (ScreenCols) = NULL;
+     vim_free(LineOffset);
+     (LineOffset) = NULL;
+     vim_free(LineWraps);
+     (LineWraps) = NULL;
+     vim_free(TabPageIdxs);
+     (TabPageIdxs) = NULL;
 }
 
     int
@@ -144886,9 +149735,9 @@
         LineWraps[i] = FALSE;
     }
 
-    if (doclear && can_clear(T_CL))
+    if (doclear && can_clear( ( term_strings[(int)(KS_CL)] ) ))
     {
-        out_str(T_CL);
+        out_str( ( term_strings[(int)(KS_CL)] ) );
         did_clear = TRUE;
         clear_cmdline = FALSE;
         mode_displayed = FALSE;
@@ -144925,52 +149774,51 @@
     static void
 lineclear(unsigned off, int width, int attr)
 {
-    (void)vim_memset(ScreenLines + off, ' ', (size_t)width * sizeof(schar_T));
+    (void) memset((ScreenLines + off), (' '), ((size_t)width * sizeof(schar_T))) ;
     if (enc_utf8)
     {
-        (void)vim_memset(ScreenLinesUC + off, 0, (size_t)width * sizeof(u8char_T));
+        (void) memset((ScreenLinesUC + off), (0), ((size_t)width * sizeof(u8char_T))) ;
     }
-    (void)vim_memset(ScreenAttrs + off, attr, (size_t)width * sizeof(sattr_T));
-    (void)vim_memset(ScreenCols + off, -1, (size_t)width * sizeof(colnr_T));
+    (void) memset((ScreenAttrs + off), (attr), ((size_t)width * sizeof(sattr_T))) ;
+    (void) memset((ScreenCols + off), (-1), ((size_t)width * sizeof(colnr_T))) ;
 }
 
     static void
 lineinvalid(unsigned off, int width)
 {
-    (void)vim_memset(ScreenAttrs + off, -1, (size_t)width * sizeof(sattr_T));
-    (void)vim_memset(ScreenCols + off, -1, (size_t)width * sizeof(colnr_T));
+    (void) memset((ScreenAttrs + off), (-1), ((size_t)width * sizeof(sattr_T))) ;
+    (void) memset((ScreenCols + off), (-1), ((size_t)width * sizeof(colnr_T))) ;
 }
 
-
     static void
 linecopy(int to, int from, win_T *wp)
 {
     unsigned    off_to = LineOffset[to] + wp->w_wincol;
     unsigned    off_from = LineOffset[from] + wp->w_wincol;
 
-    mch_memmove(ScreenLines + off_to, ScreenLines + off_from, wp->w_width * sizeof(schar_T));
+     memmove((char *)(ScreenLines + off_to), (char *)(ScreenLines + off_from), wp->w_width * sizeof(schar_T)) ;
     if (enc_utf8)
     {
         int     i;
 
-        mch_memmove(ScreenLinesUC + off_to, ScreenLinesUC + off_from, wp->w_width * sizeof(u8char_T));
+         memmove((char *)(ScreenLinesUC + off_to), (char *)(ScreenLinesUC + off_from), wp->w_width * sizeof(u8char_T)) ;
         for (i = 0; i < p_mco; ++i)
         {
-            mch_memmove(ScreenLinesC[i] + off_to, ScreenLinesC[i] + off_from, wp->w_width * sizeof(u8char_T));
+             memmove((char *)(ScreenLinesC[i] + off_to), (char *)(ScreenLinesC[i] + off_from), wp->w_width * sizeof(u8char_T)) ;
         }
     }
     if (enc_dbcs == DBCS_JPNU)
     {
-        mch_memmove(ScreenLines2 + off_to, ScreenLines2 + off_from, wp->w_width * sizeof(schar_T));
+         memmove((char *)(ScreenLines2 + off_to), (char *)(ScreenLines2 + off_from), wp->w_width * sizeof(schar_T)) ;
     }
-    mch_memmove(ScreenAttrs + off_to, ScreenAttrs + off_from, wp->w_width * sizeof(sattr_T));
-    mch_memmove(ScreenCols + off_to, ScreenCols + off_from, wp->w_width * sizeof(colnr_T));
+     memmove((char *)(ScreenAttrs + off_to), (char *)(ScreenAttrs + off_from), wp->w_width * sizeof(sattr_T)) ;
+     memmove((char *)(ScreenCols + off_to), (char *)(ScreenCols + off_from), wp->w_width * sizeof(colnr_T)) ;
 }
 
     int
 can_clear(char_u *p)
 {
-    return (*p != NUL && (t_colors <= 1 || cterm_normal_bg_color == 0 || *T_UT != NUL));
+    return (*p != NUL && (t_colors <= 1 || cterm_normal_bg_color == 0 || * ( term_strings[(int)(KS_UT)] )  != NUL));
 }
 
     void
@@ -144992,13 +149840,6 @@
     int             goto_cost;
     int             attr;
 
-#define GOTO_COST   7
-#define HIGHL_COST  5
-
-#define PLAN_LE     1
-#define PLAN_CR     2
-#define PLAN_NL     3
-#define PLAN_WRITE  4
     if (ScreenLines == NULL)
     {
         return;
@@ -145021,7 +149862,7 @@
         col = screen_Columns - 1;
     }
 
-    if (screen_attr && *T_MS == NUL)
+    if (screen_attr && * ( term_strings[(int)(KS_MS)] )  == NUL)
     {
         noinvcurs = HIGHL_COST;
     }
@@ -145037,17 +149878,17 @@
         attr = screen_attr;
         if (row == screen_cur_row && col < screen_cur_col)
         {
-            if (*T_LE)
+            if (* ( term_strings[(int)(KS_LE)] ) )
             {
-                bs = T_LE;
+                bs =  ( term_strings[(int)(KS_LE)] ) ;
             }
             else
             {
-                bs = T_BC;
+                bs =  ( term_strings[(int)(KS_BC)] ) ;
             }
             if (*bs)
             {
-                cost = (screen_cur_col - col) * (int)STRLEN(bs);
+                cost = (screen_cur_col - col) * (int) strlen((char *)(bs)) ;
             }
             else
             {
@@ -145170,11 +150011,11 @@
             i = col - screen_cur_col;
             if (i > 0)
             {
-                if (T_ND[0] != NUL && T_ND[1] == NUL)
+                if ( ( term_strings[(int)(KS_ND)] ) [0] != NUL &&  ( term_strings[(int)(KS_ND)] ) [1] == NUL)
                 {
                     while (i-- > 0)
                     {
-                        out_char(*T_ND);
+                        out_char(* ( term_strings[(int)(KS_ND)] ) );
                     }
                 }
                 else
@@ -145211,7 +150052,7 @@
         {
             screen_stop_highlight();
         }
-        if (row == screen_cur_row && (col > screen_cur_col) && *T_CRI != NUL)
+        if (row == screen_cur_row && (col > screen_cur_col) && * ( term_strings[(int)(KS_CRI)] )  != NUL)
         {
             term_cursor_right(col - screen_cur_col);
         }
@@ -145236,7 +150077,7 @@
     if (force || redrawing())
     {
         validate_cursor();
-        windgoto(W_WINROW(curwin) + curwin->w_wrow, curwin->w_wincol + (curwin->w_wcol));
+        windgoto( (curwin->w_winrow)  + curwin->w_wrow, curwin->w_wincol + (curwin->w_wcol));
     }
 }
 
@@ -145277,7 +150118,7 @@
     did_delete = FALSE;
     if (wp->w_next != NULL || wp->w_status_height)
     {
-        if (screen_del_lines(0, W_WINROW(wp) + wp->w_height - line_count, line_count, (int)Rows, FALSE, 0, NULL) == OK)
+        if (screen_del_lines(0,  (wp->w_winrow)  + wp->w_height - line_count, line_count, (int)Rows, FALSE, 0, NULL) == OK)
         {
             did_delete = TRUE;
         }
@@ -145290,21 +150131,21 @@
     {
         wp->w_redr_status = true;
         redraw_cmdline = TRUE;
-        nextrow = W_WINROW(wp) + wp->w_height + wp->w_status_height;
+        nextrow =  (wp->w_winrow)  + wp->w_height + wp->w_status_height;
         lastrow = nextrow + line_count;
         if (lastrow > Rows)
         {
             lastrow = Rows;
         }
-        screen_fill(nextrow - line_count, lastrow - line_count, wp->w_wincol, (int)W_ENDCOL(wp), ' ', ' ', 0);
+        screen_fill(nextrow - line_count, lastrow - line_count, wp->w_wincol, (int) ((wp)->w_wincol + (wp)->w_width) , ' ', ' ', 0);
     }
 
-    if (screen_ins_lines(0, W_WINROW(wp) + row, line_count, (int)Rows, 0, NULL) == FAIL)
+    if (screen_ins_lines(0,  (wp->w_winrow)  + row, line_count, (int)Rows, 0, NULL) == FAIL)
     {
         if (did_delete)
         {
             wp->w_redr_status = true;
-            win_rest_invalid(W_NEXT(wp));
+            win_rest_invalid( ((wp)->w_next) );
         }
         return FAIL;
     }
@@ -145333,14 +150174,14 @@
         return retval;
     }
 
-    if (screen_del_lines(0, W_WINROW(wp) + row, line_count, (int)Rows, FALSE, clear_attr, NULL) == FAIL)
+    if (screen_del_lines(0,  (wp->w_winrow)  + row, line_count, (int)Rows, FALSE, clear_attr, NULL) == FAIL)
     {
         return FAIL;
     }
 
     if (wp->w_next || wp->w_status_height || cmdline_row < Rows - 1)
     {
-        if (screen_ins_lines(0, W_WINROW(wp) + wp->w_height - line_count, line_count, (int)Rows, clear_attr, NULL) == FAIL)
+        if (screen_ins_lines(0,  (wp->w_winrow)  + wp->w_height - line_count, line_count, (int)Rows, clear_attr, NULL) == FAIL)
         {
             wp->w_redr_status = true;
             win_rest_invalid(wp->w_next);
@@ -145379,7 +150220,7 @@
 
     if (row + line_count >= wp->w_height)
     {
-        screen_fill(W_WINROW(wp) + row, W_WINROW(wp) + wp->w_height, wp->w_wincol, (int)W_ENDCOL(wp), ' ', ' ', 0);
+        screen_fill( (wp->w_winrow)  + row,  (wp->w_winrow)  + wp->w_height, wp->w_wincol, (int) ((wp)->w_wincol + (wp)->w_width) , ' ', ' ', 0);
         return OK;
     }
 
@@ -145390,19 +150231,19 @@
 
     if (scroll_region || wp->w_width != topframe->fr_width)
     {
-        if (scroll_region && (wp->w_width == topframe->fr_width || *T_CSV != NUL))
+        if (scroll_region && (wp->w_width == topframe->fr_width || * ( term_strings[(int)(KS_CSV)] )  != NUL))
         {
             scroll_region_set(wp, row);
         }
         if (del)
         {
-            retval = screen_del_lines(W_WINROW(wp) + row, 0, line_count, wp->w_height - row, FALSE, clear_attr, wp);
+            retval = screen_del_lines( (wp->w_winrow)  + row, 0, line_count, wp->w_height - row, FALSE, clear_attr, wp);
         }
         else
         {
-            retval = screen_ins_lines(W_WINROW(wp) + row, 0, line_count, wp->w_height - row, clear_attr, wp);
+            retval = screen_ins_lines( (wp->w_winrow)  + row, 0, line_count, wp->w_height - row, clear_attr, wp);
         }
-        if (scroll_region && (wp->w_width == topframe->fr_width || *T_CSV != NUL))
+        if (scroll_region && (wp->w_width == topframe->fr_width || * ( term_strings[(int)(KS_CSV)] )  != NUL))
         {
             scroll_region_reset();
         }
@@ -145429,15 +150270,15 @@
     redraw_cmdline = TRUE;
 }
 
-#define USE_T_CAL   1
-#define USE_T_CDL   2
-#define USE_T_AL    3
-#define USE_T_CE    4
-#define USE_T_DL    5
-#define USE_T_SR    6
-#define USE_NL      7
-#define USE_T_CD    8
-#define USE_REDRAW  9
+enum { USE_T_CAL = 1 };
+enum { USE_T_CDL = 2 };
+enum { USE_T_AL = 3 };
+enum { USE_T_CE = 4 };
+enum { USE_T_DL = 5 };
+enum { USE_T_SR = 6 };
+enum { USE_NL = 7 };
+enum { USE_T_CD = 8 };
+enum { USE_REDRAW = 9 };
 
     int
 screen_ins_lines(int         off, int         row, int         line_count, int         end, int         clear_attr, win_T       *wp)
@@ -145449,7 +150290,7 @@
     int         cursor_col = 0;
     int         type;
     int         result_empty;
-    int         can_ce = can_clear(T_CE);
+    int         can_ce = can_clear( ( term_strings[(int)(KS_CE)] ) );
 
     if (!screen_valid(TRUE) || line_count <= 0 || line_count > p_ttyscroll || end > Rows)
     {
@@ -145457,7 +150298,7 @@
     }
 
     result_empty = (row + line_count >= end);
-    if (wp != NULL && wp->w_width != topframe->fr_width && *T_CSV == NUL)
+    if (wp != NULL && wp->w_width != topframe->fr_width && * ( term_strings[(int)(KS_CSV)] )  == NUL)
     {
         if (line_count > 3)
         {
@@ -145465,19 +150306,19 @@
         }
         type = USE_REDRAW;
     }
-    else if (can_clear(T_CD) && result_empty)
+    else if (can_clear( ( term_strings[(int)(KS_CD)] ) ) && result_empty)
     {
         type = USE_T_CD;
     }
-    else if (*T_CAL != NUL && (line_count > 1 || *T_AL == NUL))
+    else if (* ( term_strings[(int)(KS_CAL)] )  != NUL && (line_count > 1 || * ( term_strings[(int)(KS_AL)] )  == NUL))
     {
         type = USE_T_CAL;
     }
-    else if (*T_CDL != NUL && result_empty && (line_count > 1 || !can_ce))
+    else if (* ( term_strings[(int)(KS_CDL)] )  != NUL && result_empty && (line_count > 1 || !can_ce))
     {
         type = USE_T_CDL;
     }
-    else if (*T_AL != NUL)
+    else if (* ( term_strings[(int)(KS_AL)] )  != NUL)
     {
         type = USE_T_AL;
     }
@@ -145485,11 +150326,11 @@
     {
         type = USE_T_CE;
     }
-    else if (*T_DL != NUL && result_empty)
+    else if (* ( term_strings[(int)(KS_DL)] )  != NUL && result_empty)
     {
         type = USE_T_DL;
     }
-    else if (*T_SR != NUL && row == 0 && (*T_DA == NUL || can_ce))
+    else if (* ( term_strings[(int)(KS_SR)] )  != NUL && row == 0 && (* ( term_strings[(int)(KS_DA)] )  == NUL || can_ce))
     {
         type = USE_T_SR;
     }
@@ -145503,17 +150344,17 @@
         return screen_del_lines(off, row, line_count, end, FALSE, 0, wp);
     }
 
-    if (*T_DB)
+    if (* ( term_strings[(int)(KS_DB)] ) )
     {
         screen_del_lines(off, end - line_count, line_count, end, FALSE, 0, wp);
     }
 
-    if (wp != NULL && wp->w_wincol != 0 && *T_CSV != NUL && *T_CCS == NUL)
+    if (wp != NULL && wp->w_wincol != 0 && * ( term_strings[(int)(KS_CSV)] )  != NUL && * ( term_strings[(int)(KS_CCS)] )  == NUL)
     {
         cursor_col = wp->w_wincol;
     }
 
-    if (*T_CCS != NUL)
+    if (* ( term_strings[(int)(KS_CCS)] )  != NUL)
     {
         cursor_row = row;
     }
@@ -145592,22 +150433,22 @@
                 {
                     windgoto(cursor_row, cursor_col);
                 }
-                out_str(T_AL);
+                out_str( ( term_strings[(int)(KS_AL)] ) );
             }
             else
             {
-                out_str(T_SR);
+                out_str( ( term_strings[(int)(KS_SR)] ) );
             }
             screen_start();
         }
     }
 
-    if (type == USE_T_SR && *T_DA)
+    if (type == USE_T_SR && * ( term_strings[(int)(KS_DA)] ) )
     {
         for (i = 0; i < line_count; ++i)
         {
             windgoto(off + i, cursor_col);
-            out_str(T_CE);
+            out_str( ( term_strings[(int)(KS_CE)] ) );
             screen_start();
         }
     }
@@ -145635,9 +150476,9 @@
 
     result_empty = row + line_count >= end;
 
-    can_delete = (*T_DB == NUL || can_clear(T_CE));
+    can_delete = (* ( term_strings[(int)(KS_DB)] )  == NUL || can_clear( ( term_strings[(int)(KS_CE)] ) ));
 
-    if (wp != NULL && wp->w_width != topframe->fr_width && *T_CSV == NUL)
+    if (wp != NULL && wp->w_width != topframe->fr_width && * ( term_strings[(int)(KS_CSV)] )  == NUL)
     {
         if (line_count > 3)
         {
@@ -145645,27 +150486,27 @@
         }
         type = USE_REDRAW;
     }
-    else if (can_clear(T_CD) && result_empty)
+    else if (can_clear( ( term_strings[(int)(KS_CD)] ) ) && result_empty)
     {
         type = USE_T_CD;
     }
-    else if (row == 0 && (line_count == 1 || *T_CDL == NUL))
+    else if (row == 0 && (line_count == 1 || * ( term_strings[(int)(KS_CDL)] )  == NUL))
     {
         type = USE_NL;
     }
-    else if (*T_CDL != NUL && line_count > 1 && can_delete)
+    else if (* ( term_strings[(int)(KS_CDL)] )  != NUL && line_count > 1 && can_delete)
     {
         type = USE_T_CDL;
     }
-    else if (can_clear(T_CE) && result_empty && (wp == NULL || wp->w_width == topframe->fr_width))
+    else if (can_clear( ( term_strings[(int)(KS_CE)] ) ) && result_empty && (wp == NULL || wp->w_width == topframe->fr_width))
     {
         type = USE_T_CE;
     }
-    else if (*T_DL != NUL && can_delete)
+    else if (* ( term_strings[(int)(KS_DL)] )  != NUL && can_delete)
     {
         type = USE_T_DL;
     }
-    else if (*T_CDL != NUL && can_delete)
+    else if (* ( term_strings[(int)(KS_CDL)] )  != NUL && can_delete)
     {
         type = USE_T_CDL;
     }
@@ -145674,12 +150515,12 @@
         return FAIL;
     }
 
-    if (wp != NULL && wp->w_wincol != 0 && *T_CSV != NUL && *T_CCS == NUL)
+    if (wp != NULL && wp->w_wincol != 0 && * ( term_strings[(int)(KS_CSV)] )  != NUL && * ( term_strings[(int)(KS_CCS)] )  == NUL)
     {
         cursor_col = wp->w_wincol;
     }
 
-    if (*T_CCS != NUL)
+    if (* ( term_strings[(int)(KS_CCS)] )  != NUL)
     {
         cursor_row = row;
         cursor_end = end;
@@ -145750,7 +150591,7 @@
     else if (type == USE_T_CD)
     {
         windgoto(cursor_row, cursor_col);
-        out_str(T_CD);
+        out_str( ( term_strings[(int)(KS_CD)] ) );
         screen_start();
     }
     else if (type == USE_T_CDL)
@@ -145774,23 +150615,23 @@
             if (type == USE_T_DL)
             {
                 windgoto(cursor_row, cursor_col);
-                out_str(T_DL);
+                out_str( ( term_strings[(int)(KS_DL)] ) );
             }
             else
             {
                 windgoto(cursor_row + i, cursor_col);
-                out_str(T_CE);
+                out_str( ( term_strings[(int)(KS_CE)] ) );
             }
             screen_start();
         }
     }
 
-    if (*T_DB && (type == USE_T_DL || type == USE_T_CDL))
+    if (* ( term_strings[(int)(KS_DB)] )  && (type == USE_T_DL || type == USE_T_CDL))
     {
         for (i = line_count; i > 0; --i)
         {
             windgoto(cursor_end - i, cursor_col);
-            out_str(T_CE);
+            out_str( ( term_strings[(int)(KS_CE)] ) );
             screen_start();
         }
     }
@@ -145841,7 +150682,7 @@
 
         msg_pos_mode();
         cursor_off();
-        attr = HL_ATTR(HLF_CM);
+        attr =  highlight_attr[(int)(HLF_CM)] ;
         if (do_mode)
         {
             msg_puts_attr("--", attr);
@@ -145871,7 +150712,7 @@
                         msg_puts_attr(" ", attr);
                         if ((int)edit_submode_highl < (int)HLF_COUNT)
                         {
-                            sub_attr = HL_ATTR(edit_submode_highl);
+                            sub_attr =  highlight_attr[(int)(edit_submode_highl)] ;
                         }
                         else
                         {
@@ -145919,22 +150760,22 @@
                     switch ((VIsual_select ? 4 : 0) + (VIsual_mode == Ctrl_V) * 2 + (VIsual_mode == 'V'))
                     {
                         case 0:
-                            p = N_(" VISUAL");
+                            p =  " VISUAL" ;
                             break;
                         case 1:
-                            p = N_(" VISUAL LINE");
+                            p =  " VISUAL LINE" ;
                             break;
                         case 2:
-                            p = N_(" VISUAL BLOCK");
+                            p =  " VISUAL BLOCK" ;
                             break;
                         case 4:
-                            p = N_(" SELECT");
+                            p =  " SELECT" ;
                             break;
                         case 5:
-                            p = N_(" SELECT LINE");
+                            p =  " SELECT LINE" ;
                             break;
                         default:
-                            p = N_(" SELECT BLOCK");
+                            p =  " SELECT BLOCK" ;
                             break;
                     }
                     msg_puts_attr(_(p), attr);
@@ -146017,7 +150858,7 @@
     msg_pos_mode();
     if (reg_recording != 0)
     {
-        recording_mode(HL_ATTR(HLF_CM));
+        recording_mode( highlight_attr[(int)(HLF_CM)] );
     }
     msg_clr_eos();
 
@@ -146082,8 +150923,10 @@
     }
 
     {
-        FOR_ALL_TABPAGES(tp)
+         for ((tp) = first_tabpage; (tp) != NULL; (tp) = (tp)->tp_next) 
+         {
             ++tabcount;
+         }
 
         tabwidth = (topframe->fr_width - 1 + tabcount / 2) / tabcount;
         if (tabwidth < 6)
@@ -146098,9 +150941,9 @@
 
             override_success = push_highlight_overrides(tp->tp_curwin->w_hl, tp->tp_curwin->w_hl_len);
 
-            attr_sel = HL_ATTR(HLF_TPS);
-            attr_nosel = HL_ATTR(HLF_TP);
-            attr_fill = HL_ATTR(HLF_TPF);
+            attr_sel =  highlight_attr[(int)(HLF_TPS)] ;
+            attr_nosel =  highlight_attr[(int)(HLF_TP)] ;
+            attr_fill =  highlight_attr[(int)(HLF_TPF)] ;
             attr = attr_nosel;
 
             if (tp->tp_topframe == topframe)
@@ -146142,7 +150985,7 @@
             {
                 if (wincount > 1)
                 {
-                    len = vim_snprintf((char *)NameBuff, MAXPATHL, "%d", wincount);
+                    len = vim_snprintf((char *)NameBuff,  PATH_MAX , "%d", wincount);
                     if (col + len >= Columns - 3)
                     {
                         break;
@@ -146171,7 +151014,7 @@
                     while (len > room)
                     {
                         len -= ptr2cells(p);
-                        MB_PTR_ADV(p);
+                         p += (*mb_ptr2len)(p) ;
                     }
                 }
                 else if (len > room)
@@ -146185,7 +151028,7 @@
                     len = n;
                 }
 
-                screen_puts_len(p, (int)STRLEN(p), 0, col, attr);
+                screen_puts_len(p, (int) strlen((char *)(p)) , 0, col, attr);
                 col += len;
             }
             screen_putchar(' ', 0, col++, attr);
@@ -146239,13 +151082,13 @@
 {
     if (buf_spname(buf) != NULL)
     {
-        vim_strncpy(NameBuff, buf_spname(buf), MAXPATHL - 1);
+        vim_strncpy(NameBuff, buf_spname(buf),  PATH_MAX  - 1);
     }
     else
     {
-        home_replace(buf, buf->b_fname, NameBuff, MAXPATHL, TRUE);
+        home_replace(buf, buf->b_fname, NameBuff,  PATH_MAX , TRUE);
     }
-    trans_characters(NameBuff, MAXPATHL);
+    trans_characters(NameBuff,  PATH_MAX );
 }
 
     int
@@ -146257,12 +151100,12 @@
 
     if (wp == curwin)
     {
-        *attr = HL_ATTR(HLF_S);
+        *attr =  highlight_attr[(int)(HLF_S)] ;
         fill = wp->w_fill_chars.stl;
     }
     else
     {
-        *attr = HL_ATTR(HLF_SNC);
+        *attr =  highlight_attr[(int)(HLF_SNC)] ;
         fill = wp->w_fill_chars.stlnc;
     }
 
@@ -146281,7 +151124,7 @@
         return true;
     }
 
-    if (curwin->w_wincol == W_ENDCOL(wp) + wp->w_vsep_width && row >= curwin->w_winrow && row < W_WINROW(curwin) + curwin->w_height + curwin->w_status_height)
+    if (curwin->w_wincol ==  ((wp)->w_wincol + (wp)->w_width)  + wp->w_vsep_width && row >= curwin->w_winrow && row <  (curwin->w_winrow)  + curwin->w_height + curwin->w_status_height)
     {
         return true;
     }
@@ -146296,11 +151139,11 @@
         push_highlight_overrides(wp->w_hl, wp->w_hl_len);
     if (vsep_row_is_curwin(wp, row))
     {
-        *attr = HL_ATTR(HLF_C);
+        *attr =  highlight_attr[(int)(HLF_C)] ;
     }
     else
     {
-        *attr = HL_ATTR(HLF_CNC);
+        *attr =  highlight_attr[(int)(HLF_CNC)] ;
     }
     if (override_success)
     {
@@ -146335,7 +151178,7 @@
     return (!(p_lz && typed_ahead() && !KeyTyped));
 }
 
-#define COL_RULER 17
+enum { COL_RULER = 17 };
 
     void
 comp_col(void)
@@ -146405,40 +151248,38 @@
     string_T    name;
 };
 
-#define CHARSTAB_ENTRY(cp, name)     {(cp), {(char_u *)(name), STRLEN_LITERAL(name)}}
-
 static fill_chars_T fill_chars;
 static struct charstab filltab[] =
 {
-    CHARSTAB_ENTRY(&fill_chars.stl,         "stl"),
-    CHARSTAB_ENTRY(&fill_chars.stlnc,       "stlnc"),
-    CHARSTAB_ENTRY(&fill_chars.vert,        "vert"),
-    CHARSTAB_ENTRY(&fill_chars.fold,        "fold"),
-    CHARSTAB_ENTRY(&fill_chars.foldopen,    "foldopen"),
-    CHARSTAB_ENTRY(&fill_chars.foldclosed,  "foldclose"),
-    CHARSTAB_ENTRY(&fill_chars.foldsep,     "foldsep"),
-    CHARSTAB_ENTRY(&fill_chars.foldinner,   "foldinner"),
-    CHARSTAB_ENTRY(&fill_chars.diff,        "diff"),
-    CHARSTAB_ENTRY(&fill_chars.eob,         "eob"),
-    CHARSTAB_ENTRY(&fill_chars.lastline,    "lastline"),
-    CHARSTAB_ENTRY(&fill_chars.trunc,       "trunc"),
-    CHARSTAB_ENTRY(&fill_chars.truncrl,     "truncrl"),
+     {(&fill_chars.stl), {(char_u *)("stl"),  (sizeof("stl" "") - 1) }} ,
+     {(&fill_chars.stlnc), {(char_u *)("stlnc"),  (sizeof("stlnc" "") - 1) }} ,
+     {(&fill_chars.vert), {(char_u *)("vert"),  (sizeof("vert" "") - 1) }} ,
+     {(&fill_chars.fold), {(char_u *)("fold"),  (sizeof("fold" "") - 1) }} ,
+     {(&fill_chars.foldopen), {(char_u *)("foldopen"),  (sizeof("foldopen" "") - 1) }} ,
+     {(&fill_chars.foldclosed), {(char_u *)("foldclose"),  (sizeof("foldclose" "") - 1) }} ,
+     {(&fill_chars.foldsep), {(char_u *)("foldsep"),  (sizeof("foldsep" "") - 1) }} ,
+     {(&fill_chars.foldinner), {(char_u *)("foldinner"),  (sizeof("foldinner" "") - 1) }} ,
+     {(&fill_chars.diff), {(char_u *)("diff"),  (sizeof("diff" "") - 1) }} ,
+     {(&fill_chars.eob), {(char_u *)("eob"),  (sizeof("eob" "") - 1) }} ,
+     {(&fill_chars.lastline), {(char_u *)("lastline"),  (sizeof("lastline" "") - 1) }} ,
+     {(&fill_chars.trunc), {(char_u *)("trunc"),  (sizeof("trunc" "") - 1) }} ,
+     {(&fill_chars.truncrl), {(char_u *)("truncrl"),  (sizeof("truncrl" "") - 1) }} ,
 };
 static lcs_chars_T lcs_chars;
 static struct charstab lcstab[] =
 {
-    CHARSTAB_ENTRY(&lcs_chars.eol,          "eol"),
-    CHARSTAB_ENTRY(&lcs_chars.ext,          "extends"),
-    CHARSTAB_ENTRY(&lcs_chars.nbsp,         "nbsp"),
-    CHARSTAB_ENTRY(&lcs_chars.prec,         "precedes"),
-    CHARSTAB_ENTRY(&lcs_chars.space,        "space"),
-    CHARSTAB_ENTRY(&lcs_chars.tab2,         "tab"),
-    CHARSTAB_ENTRY(&lcs_chars.leadtab2,     "leadtab"),
-    CHARSTAB_ENTRY(&lcs_chars.trail,        "trail"),
-    CHARSTAB_ENTRY(&lcs_chars.lead,         "lead"),
-    CHARSTAB_ENTRY(NULL,                    "conceal"),
-    CHARSTAB_ENTRY(NULL,                    "multispace"),
-    CHARSTAB_ENTRY(NULL,                    "leadmultispace")
+     {(&lcs_chars.eol), {(char_u *)("eol"),  (sizeof("eol" "") - 1) }} ,
+     {(&lcs_chars.ext), {(char_u *)("extends"),  (sizeof("extends" "") - 1) }} ,
+     {(&lcs_chars.nbsp), {(char_u *)("nbsp"),  (sizeof("nbsp" "") - 1) }} ,
+     {(&lcs_chars.prec), {(char_u *)("precedes"),  (sizeof("precedes" "") - 1) }} ,
+     {(&lcs_chars.space), {(char_u *)("space"),  (sizeof("space" "") - 1) }} ,
+     {(&lcs_chars.tab2), {(char_u *)("tab"),  (sizeof("tab" "") - 1) }} ,
+     {(&lcs_chars.leadtab2), {(char_u *)("leadtab"),  (sizeof("leadtab" "") - 1) }} ,
+     {(&lcs_chars.trail), {(char_u *)("trail"),  (sizeof("trail" "") - 1) }} ,
+     {(&lcs_chars.lead), {(char_u *)("lead"),  (sizeof("lead" "") - 1) }} ,
+     {(NULL), {(char_u *)("conceal"),  (sizeof("conceal" "") - 1) }} ,
+     {(NULL), {(char_u *)("multispace"),  (sizeof("multispace" "") - 1) }} ,
+     {(NULL), {(char_u *)("leadmultispace"),  (sizeof("leadmultispace" "") - 1) }} 
 };
 
     static char *
@@ -146473,9 +151314,9 @@
     if (is_listchars)
     {
         tab = lcstab;
-        CLEAR_FIELD(lcs_chars);
-        entries = ARRAY_LENGTH(lcstab);
-        if (wp->w_p_lcs[0] == NUL)
+          memset((&(lcs_chars)), (0), (sizeof(lcs_chars)))  ;
+        entries =  (sizeof(lcstab) / sizeof((lcstab)[0])) ;
+        if (wp-> w_onebuf_opt.wo_lcs [0] == NUL)
         {
             value = p_lcs;
         }
@@ -146483,8 +151324,8 @@
     else
     {
         tab = filltab;
-        entries = ARRAY_LENGTH(filltab);
-        if (wp->w_p_fcs[0] == NUL)
+        entries =  (sizeof(filltab) / sizeof((filltab)[0])) ;
+        if (wp-> w_onebuf_opt.wo_fcs [0] == NUL)
         {
             value = p_fcs;
         }
@@ -146513,7 +151354,7 @@
 
                 if (multispace_len > 0)
                 {
-                    lcs_chars.multispace = ALLOC_MULT(int, multispace_len + 1);
+                    lcs_chars.multispace =  (int *)alloc(sizeof(int) * (multispace_len + 1)) ;
                     if (lcs_chars.multispace != NULL)
                     {
                         lcs_chars.multispace[multispace_len] = NUL;
@@ -146527,7 +151368,7 @@
                 if (lead_multispace_len > 0)
                 {
                     lcs_chars.leadmultispace =
-                                      ALLOC_MULT(int, lead_multispace_len + 1);
+                                       (int *)alloc(sizeof(int) * (lead_multispace_len + 1)) ;
                     if (lcs_chars.leadmultispace != NULL)
                     {
                         lcs_chars.leadmultispace[lead_multispace_len] = NUL;
@@ -146560,14 +151401,14 @@
         {
             for (i = 0; i < entries; ++i)
             {
-                if (!(STRNCMP(p, tab[i].name.string, tab[i].name.length) == 0 && p[tab[i].name.length] == ':'))
+                if (!( strncmp((char *)(p), (char *)(tab[i].name.string), (tab[i].name.length))  == 0 && p[tab[i].name.length] == ':'))
                 {
                     continue;
                 }
 
                 s = p + tab[i].name.length + 1;
 
-                if (is_listchars && STRCMP(tab[i].name.string, "multispace") == 0)
+                if (is_listchars &&  strcmp((char *)(tab[i].name.string), (char *)("multispace"))  == 0)
                 {
                     if (round == 0)
                     {
@@ -146604,7 +151445,7 @@
                     break;
                 }
 
-                if (is_listchars && STRCMP(tab[i].name.string, "leadmultispace") == 0)
+                if (is_listchars &&  strcmp((char *)(tab[i].name.string), (char *)("leadmultispace"))  == 0)
                 {
                     if (round == 0)
                     {
@@ -146757,9 +151598,9 @@
 }
 
     char_u *
-get_fillchars_name(expand_T *xp UNUSED, int idx)
+get_fillchars_name(expand_T *xp  __attribute__((unused)) , int idx)
 {
-    if (idx < 0 || idx >= (int)ARRAY_LENGTH(filltab))
+    if (idx < 0 || idx >= (int) (sizeof(filltab) / sizeof((filltab)[0])) )
     {
         return NULL;
     }
@@ -146768,9 +151609,9 @@
 }
 
     char_u *
-get_listchars_name(expand_T *xp UNUSED, int idx)
+get_listchars_name(expand_T *xp  __attribute__((unused)) , int idx)
 {
-    if (idx < 0 || idx >= (int)ARRAY_LENGTH(lcstab))
+    if (idx < 0 || idx >= (int) (sizeof(lcstab) / sizeof((lcstab)[0])) )
     {
         return NULL;
     }
@@ -146792,17 +151633,20 @@
     {
         return e_conflicts_with_value_of_fillchars;
     }
-    FOR_ALL_TAB_WINDOWS(tp, wp)
+     for ((tp) = first_tabpage; (tp) != NULL; (tp) = (tp)->tp_next)
+     {
+         for ((wp) = ((tp) == curtab)            ? firstwin : (tp)->tp_firstwin; (wp); (wp) = (wp)->w_next)
     {
-        if (set_listchars_option(wp, wp->w_p_lcs, FALSE, NULL, 0) != NULL)
+        if (set_listchars_option(wp, wp-> w_onebuf_opt.wo_lcs , FALSE, NULL, 0) != NULL)
         {
             return e_conflicts_with_value_of_listchars;
         }
-        if (set_fillchars_option(wp, wp->w_p_fcs, FALSE, NULL, 0) != NULL)
+        if (set_fillchars_option(wp, wp-> w_onebuf_opt.wo_fcs , FALSE, NULL, 0) != NULL)
         {
             return e_conflicts_with_value_of_fillchars;
         }
     }
+     }
     return NULL;
 }
 
@@ -146856,7 +151700,7 @@
 }
 
     char_u *
-estack_sfile(estack_arg_T which UNUSED)
+estack_sfile(estack_arg_T which  __attribute__((unused)) )
 {
     estack_T    *entry;
 
@@ -146880,22 +151724,22 @@
         return 0;
     }
 
-    if (STRNCMP(arg, "START", where_len) == 0)
+    if ( strncmp((char *)(arg), (char *)("START"), (where_len))  == 0)
     {
         *argp = skipwhite(arg + where_len);
         return DIP_START + DIP_NORTP;
     }
-    if (STRNCMP(arg, "OPT", where_len) == 0)
+    if ( strncmp((char *)(arg), (char *)("OPT"), (where_len))  == 0)
     {
         *argp = skipwhite(arg + where_len);
         return DIP_OPT + DIP_NORTP;
     }
-    if (STRNCMP(arg, "PACK", where_len) == 0)
+    if ( strncmp((char *)(arg), (char *)("PACK"), (where_len))  == 0)
     {
         *argp = skipwhite(arg + where_len);
         return DIP_START + DIP_OPT + DIP_NORTP;
     }
-    if (STRNCMP(arg, "ALL", where_len) == 0)
+    if ( strncmp((char *)(arg), (char *)("ALL"), (where_len))  == 0)
     {
         *argp = skipwhite(arg + where_len);
         return DIP_START + DIP_OPT;
@@ -146952,7 +151796,7 @@
     int         did_one = FALSE;
 
     rtp_copy = vim_strsave(path);
-    buf.string = alloc(MAXPATHL);
+    buf.string = alloc( PATH_MAX );
     if (buf.string != NULL && rtp_copy != NULL)
     {
         size_t  prefixlen = 0;
@@ -146960,8 +151804,8 @@
 
         if (name != NULL)
         {
-            prefixlen = STRLEN(prefix);
-            namelen = STRLEN(name);
+            prefixlen =  strlen((char *)(prefix)) ;
+            namelen =  strlen((char *)(name)) ;
 
             if (p_verbose > 10)
             {
@@ -146981,12 +151825,12 @@
         rtp = rtp_copy;
         while (*rtp != NUL && ((flags & DIP_ALL) || !did_one))
         {
-            buf.length = (size_t)copy_option_part(&rtp, buf.string, MAXPATHL, ",");
+            buf.length = (size_t)copy_option_part(&rtp, buf.string,  PATH_MAX , ",");
 
             if (flags & (DIP_NOAFTER | DIP_AFTER))
             {
                 int is_after = buf.length >= 5
-                    && STRCMP(buf.string + buf.length - 5, "after") == 0;
+                    &&  strcmp((char *)(buf.string + buf.length - 5), (char *)("after"))  == 0;
 
                 if ((is_after && (flags & DIP_NOAFTER)) || (!is_after && (flags & DIP_AFTER)))
                 {
@@ -147002,24 +151846,24 @@
                     did_one = (cookie == NULL);
                 }
             }
-            else if (buf.length + 2 + prefixlen + namelen < MAXPATHL)
+            else if (buf.length + 2 + prefixlen + namelen <  PATH_MAX )
             {
                 char_u  *np;
                 char_u  *tail;
 
                 if (*buf.string != NUL && !after_pathsep(buf.string, buf.string + buf.length))
                 {
-                    STRCPY(buf.string + buf.length, PATHSEPSTR);
-                    buf.length += sizeof(PATHSEP);
+                     strcpy((char *)(buf.string + buf.length), (char *)( "/" )) ;
+                    buf.length += sizeof( ((char_u)'/') );
                 }
-                STRCPY(buf.string + buf.length, prefix);
+                 strcpy((char *)(buf.string + buf.length), (char *)(prefix)) ;
                 buf.length += prefixlen;
                 tail = buf.string + buf.length;
 
                 np = name;
                 while (*np != NUL && ((flags & DIP_ALL) || !did_one))
                 {
-                    copy_option_part(&np, tail, (int)(MAXPATHL - (tail - buf.string)), "\t ");
+                    copy_option_part(&np, tail, (int)( PATH_MAX  - (tail - buf.string)), "\t ");
 
                     if (p_verbose > 10)
                     {
@@ -147117,7 +151961,7 @@
     sort_strings(fnames, gap->ga_len);
     for (i = gap->ga_len - 1; i > 0; --i)
     {
-        if (fnamecmp(fnames[i - 1], fnames[i]) == 0)
+        if ( vim_fnamecmp((char_u *)(fnames[i - 1]), (char_u *)(fnames[i]))  == 0)
         {
             vim_free(fnames[i]);
             for (j = i + 1; j < gap->ga_len; ++j)
@@ -147134,7 +151978,7 @@
 {
     for (int i = 0; dirnames[i] != NULL; ++i)
     {
-        const size_t    buf_len = STRLEN(dirnames[i]) + pat_len + 64;
+        const size_t    buf_len =  strlen((char *)(dirnames[i]))  + pat_len + 64;
         char            *buf = alloc(buf_len);
         if (buf == NULL)
         {
@@ -147188,15 +152032,15 @@
     {
         char_u *match = ((char_u **)gap->ga_data)[i];
         char_u *s = match;
-        char_u *e = s + STRLEN(s);
-        if (e - 4 > s && !keep_ext && STRNICMP(e - 4, ".vim", 4) == 0)
+        char_u *e = s +  strlen((char *)(s)) ;
+        if (e - 4 > s && !keep_ext &&  strncasecmp((char *)(e - 4), (char *)(".vim"), (4))  == 0)
         {
             e -= 4;
             *e = NUL;
         }
 
         int match_pathsep_cnt = (e > s && e[-1] == '/') ? -1 : 0;
-        for (s = e; s > match; MB_PTR_BACK(match, s))
+        for (s = e; s > match;  s -= has_mbyte ? ((*mb_head_off)(match, (s) - 1) + 1) : 1 )
         {
             if (s < match || (vim_ispathsep(*s) && ++match_pathsep_cnt > pat_pathsep_cnt))
             {
@@ -147206,7 +152050,7 @@
         ++s;
         if (s != match)
         {
-            mch_memmove(match, s, e - s + 1);
+             memmove((char *)(match), (char *)(s), e - s + 1) ;
         }
     }
 
@@ -147227,7 +152071,7 @@
     garray_T    ga;
     ga_init2(&ga, sizeof(char *), 10);
 
-    ExpandRTDir_int(pat, STRLEN(pat), flags, FALSE, &ga, dirnames);
+    ExpandRTDir_int(pat,  strlen((char *)(pat)) , flags, FALSE, &ga, dirnames);
 
     if (ga.ga_len == 0)
     {
@@ -147248,16 +152092,16 @@
     garray_T    ga;
     ga_init2(&ga, sizeof(char *), 10);
 
-    size_t pat_len = (int)STRLEN(pat);
+    size_t pat_len = (int) strlen((char *)(pat)) ;
     char *dirnames[] = {"", NULL};
     ExpandRTDir_int(pat, pat_len, runtime_expand_flags, TRUE, &ga, dirnames);
 
     if (runtime_expand_flags == 0)
     {
         char *where_values[] = {"START", "OPT", "PACK", "ALL"};
-        for (size_t i = 0; i < ARRAY_LENGTH(where_values); ++i)
+        for (size_t i = 0; i <  (sizeof(where_values) / sizeof((where_values)[0])) ; ++i)
         {
-            if (STRNCMP(pat, where_values[i], pat_len) == 0)
+            if ( strncmp((char *)(pat), (char *)(where_values[i]), (pat_len))  == 0)
             {
                 char_u *p = vim_strsave((char_u *)where_values[i]);
                 if (p != NULL && ga_add_string(&ga, p) == FAIL)
@@ -147290,7 +152134,7 @@
 
     *num_file = 0;
     *file = NULL;
-    pat_len = (int)STRLEN(pat);
+    pat_len = (int) strlen((char *)(pat)) ;
     ga_init2(&ga, sizeof(char *), 10);
 
     s = alloc(pat_len + 26);
@@ -147307,8 +152151,8 @@
     {
         match = ((char_u **)ga.ga_data)[i];
         s = gettail(match);
-        e = s + STRLEN(s);
-        mch_memmove(match, s, e - s + 1);
+        e = s +  strlen((char *)(s)) ;
+         memmove((char *)(match), (char *)(s), e - s + 1) ;
     }
 
     if (ga.ga_len == 0)
@@ -147328,7 +152172,7 @@
 {
     int clearvars = FALSE;
 
-    if (*fname != NUL && STRNCMP(fname, "++clear", 7) == 0)
+    if (*fname != NUL &&  strncmp((char *)(fname), (char *)("++clear"), (7))  == 0)
     {
         clearvars = TRUE;
         fname = fname + 7;
@@ -147373,11 +152217,10 @@
         cmd_source(eap->arg, eap);
 }
 
-# define USE_FOPEN_NOINH
     static FILE *
 fopen_noinh_readbin(char *filename)
 {
-    int fd_tmp = mch_open(filename, O_RDONLY, 0);
+    int fd_tmp =  open((filename), (O_RDONLY), (0)) ;
 
     if (fd_tmp == -1)
     {
@@ -147392,7 +152235,7 @@
         }
     }
 
-    return fdopen(fd_tmp, READBIN);
+    return fdopen(fd_tmp,  "r" );
 }
 
     static char_u *
@@ -147413,7 +152256,7 @@
     }
     else
     {
-        vim_snprintf((char *)IObuff, IOSIZE, ":source buffer=%d", curbuf->b_fnum);
+        vim_snprintf((char *)IObuff,  (1024+1) , ":source buffer=%d", curbuf->b_fnum);
         fname = vim_strsave(IObuff);
     }
     if (fname == NULL)
@@ -147449,7 +152292,7 @@
 }
 
     static int
-do_source_ext(char_u      *fname, int         check_other, int         is_vimrc, int         *ret_sid UNUSED, exarg_T     *eap, int         clearvars UNUSED)
+do_source_ext(char_u      *fname, int         check_other, int         is_vimrc, int         *ret_sid  __attribute__((unused)) , exarg_T     *eap, int         clearvars  __attribute__((unused)) )
 {
     source_cookie_T         cookie;
     char_u                  *p;
@@ -147461,7 +152304,7 @@
     int                     save_sticky_cmdmod_flags = sticky_cmdmod_flags;
     int                     trigger_source_post = FALSE;
 
-    CLEAR_FIELD(cookie);
+      memset((&(cookie)), (0), (sizeof(cookie)))  ;
     if (fname == NULL)
     {
         fname_exp = do_source_buffer_init(&cookie, eap);
@@ -147508,7 +152351,7 @@
     if (cookie.fp == NULL && check_other)
     {
         p = gettail(fname_exp);
-        if ((*p == '.' || *p == '_') && (STRICMP(p + 1, "vimrc") == 0 || STRICMP(p + 1, "gvimrc") == 0 || STRICMP(p + 1, "exrc") == 0))
+        if ((*p == '.' || *p == '_') && ( strcasecmp((char *)(p + 1), (char *)("vimrc"))  == 0 ||  strcasecmp((char *)(p + 1), (char *)("gvimrc"))  == 0 ||  strcasecmp((char *)(p + 1), (char *)("exrc"))  == 0))
         {
             if (*p == '_')
             {
@@ -147527,13 +152370,13 @@
         if (p_verbose > 0)
         {
             verbose_enter();
-            if (SOURCING_NAME == NULL)
+            if ( (((estack_T *)exestack.ga_data)[exestack.ga_len - 1].es_name)  == NULL)
             {
                 smsg(_("could not source \"%s\""), fname);
             }
             else
             {
-                smsg(_("line %ld: could not source \"%s\""), SOURCING_LNUM, fname);
+                smsg(_("line %ld: could not source \"%s\""),  (((estack_T *)exestack.ga_data)[exestack.ga_len - 1].es_lnum) , fname);
             }
             verbose_leave();
         }
@@ -147543,13 +152386,13 @@
     if (p_verbose > 1)
     {
         verbose_enter();
-        if (SOURCING_NAME == NULL)
+        if ( (((estack_T *)exestack.ga_data)[exestack.ga_len - 1].es_name)  == NULL)
         {
             smsg(_("sourcing \"%s\""), fname);
         }
         else
         {
-            smsg(_("line %ld: sourcing \"%s\""), SOURCING_LNUM, fname);
+            smsg(_("line %ld: sourcing \"%s\""),  (((estack_T *)exestack.ga_data)[exestack.ga_len - 1].es_lnum) , fname);
         }
         verbose_leave();
     }
@@ -147578,8 +152421,8 @@
 
     cookie.conv.vc_type = CONV_NONE;
 
-    firstline = getsourceline(0, (void *)&cookie, 0, TRUE);
-    if (firstline != NULL && STRLEN(firstline) >= 3 && firstline[0] == 0xef && firstline[1] == 0xbb && firstline[2] == 0xbf)
+    firstline = getsourceline(0, (void *)&cookie, 0, GETLINE_CONCAT_CONT);
+    if (firstline != NULL &&  strlen((char *)(firstline))  >= 3 && firstline[0] == 0xef && firstline[1] == 0xbb && firstline[2] == 0xbf)
     {
         convert_setup(&cookie.conv, (char_u *)"utf-8", p_enc);
         p = string_convert(&cookie.conv, firstline + 3, NULL);
@@ -147606,9 +152449,9 @@
     {
         verbose_enter();
         smsg(_("finished sourcing %s"), fname);
-        if (SOURCING_NAME != NULL)
+        if ( (((estack_T *)exestack.ga_data)[exestack.ga_len - 1].es_name)  != NULL)
         {
-            smsg(_("continuing in %s"), SOURCING_NAME);
+            smsg(_("continuing in %s"),  (((estack_T *)exestack.ga_data)[exestack.ga_len - 1].es_name) );
         }
         verbose_leave();
     }
@@ -147692,7 +152535,7 @@
             {
                 break;
             }
-            len = ga.ga_len + (int)STRLEN(buf + ga.ga_len);
+            len = ga.ga_len + (int) strlen((char *)(buf + ga.ga_len)) ;
         }
 
         have_read = TRUE;
@@ -147732,7 +152575,7 @@
 }
 
     char_u *
-getsourceline(int c UNUSED, void *cookie, int indent UNUSED, getline_opt_T options)
+getsourceline(int c  __attribute__((unused)) , void *cookie, int indent  __attribute__((unused)) , getline_opt_T options)
 {
     source_cookie_T     *sp = (source_cookie_T *)cookie;
     char_u              *line;
@@ -147742,7 +152585,7 @@
     int                 do_bar_cont = do_vim9_all
                                          || options == GETLINE_CONCAT_CONTBAR;
 
-    SOURCING_LNUM = sp->sourcing_lnum + 1;
+     (((estack_T *)exestack.ga_data)[exestack.ga_len - 1].es_lnum)  = sp->sourcing_lnum + 1;
 
     if (sp->finished || (!sp->source_from_buf && sp->fp == NULL))
     {
@@ -147778,7 +152621,7 @@
             }
             else if (*p == '|')
             {
-                GA_CONCAT_LITERAL(&ga, " ");
+                 ga_concat_len((&ga), (char_u *)(" "),  (sizeof(" " "") - 1) ) ;
                 ga_concat(&ga, p);
             }
             for (;;)
@@ -147809,7 +152652,7 @@
                     }
                     else
                     {
-                        GA_CONCAT_LITERAL(&ga, " ");
+                         ga_concat_len((&ga), (char_u *)(" "),  (sizeof(" " "") - 1) ) ;
                         ga_concat(&ga, p);
                     }
                 }
@@ -147880,7 +152723,7 @@
 }
 
     void
-ex_scriptversion(exarg_T *eap UNUSED)
+ex_scriptversion(exarg_T *eap  __attribute__((unused)) )
 {
     int         nr;
 
@@ -147928,8 +152771,8 @@
 static void cmdline_search_stat(int dirc, pos_T *pos, pos_T *cursor_pos, int show_top_bot_msg, char_u *msgbuf, size_t msgbuflen, int recompute, int maxcount, long timeout);
 static void update_search_stat(int dirc, pos_T *pos, pos_T *cursor_pos, searchstat_T *stat, int recompute, int maxcount, long timeout);
 
-#define SEARCH_STAT_DEF_TIMEOUT 40L
-#define SEARCH_STAT_BUF_LEN 16
+enum { SEARCH_STAT_DEF_TIMEOUT = 40L };
+enum { SEARCH_STAT_BUF_LEN = 16 };
 
 static spat_T spats[2] =
 {
@@ -147945,7 +152788,7 @@
 static char_u   lastc_bytes[MB_MAXBYTES + 1];
 static int      lastc_bytelen = 1;
 
-static spat_T       saved_spats[ARRAY_LENGTH(spats)];
+static spat_T       saved_spats[ (sizeof(spats) / sizeof((spats)[0])) ];
 static char_u       *saved_mr_pattern = NULL;
 static size_t       saved_mr_patternlen = 0;
 static int          saved_spats_last_idx = 0;
@@ -148081,7 +152924,7 @@
         return;
     }
 
-    for (i = 0; i < (int)ARRAY_LENGTH(spats); ++i)
+    for (i = 0; i < (int) (sizeof(spats) / sizeof((spats)[0])) ; ++i)
     {
         saved_spats[i] = spats[i];
         if (spats[i].pat != NULL)
@@ -148127,7 +152970,7 @@
         return;
     }
 
-    for (i = 0; i < (int)ARRAY_LENGTH(spats); ++i)
+    for (i = 0; i < (int) (sizeof(spats) / sizeof((spats)[0])) ; ++i)
     {
         vim_free(spats[i].pat);
         spats[i] = saved_spats[i];
@@ -148190,10 +153033,6 @@
     set_no_hlsearch(saved_no_hlsearch);
 }
 
-
-
-
-
     int
 ignorecase(char_u *pat)
 {
@@ -148264,7 +153103,7 @@
                 p++;
             }
         }
-        else if (MB_ISUPPER(*p))
+        else if ( vim_isupper(*p) )
         {
             return TRUE;
         }
@@ -148411,7 +153250,7 @@
             at_first_line = FALSE;
         }
 
-        if (dir == BACKWARD && start_pos.col == 0 && (options & SEARCH_START) == 0)
+        if (dir ==  (-1)  && start_pos.col == 0 && (options & SEARCH_START) == 0)
         {
             lnum = pos->lnum - 1;
             at_first_line = FALSE;
@@ -148523,7 +153362,7 @@
                             continue;
                         }
                     }
-                    if (dir == BACKWARD)
+                    if (dir ==  (-1) )
                     {
                         match_ok = FALSE;
                         for (;;)
@@ -148679,7 +153518,7 @@
                 break;
             }
 
-            if (dir == BACKWARD)
+            if (dir ==  (-1) )
             {
                 lnum = buf->b_ml.ml_line_count;
             }
@@ -148689,7 +153528,7 @@
             }
             if (!shortmess(SHM_SEARCH) && shortmess(SHM_SEARCHCOUNT) && (options & SEARCH_MSG))
             {
-                give_warning((char_u *)_(dir == BACKWARD ? top_bot_msg : bot_top_msg), TRUE);
+                give_warning((char_u *)_(dir ==  (-1)  ? top_bot_msg : bot_top_msg), TRUE);
             }
             if (extra_arg != NULL)
             {
@@ -148763,7 +153602,7 @@
     p = skip_regexp_ex(*pat, search_delim, magic_isset(), strcopy, NULL, NULL);
     if (*strcopy != ps)
     {
-        size_t len = STRLEN(*strcopy);
+        size_t len =  strlen((char *)(*strcopy)) ;
         cmdlen += (int)(*patlen - len);
         *pat = *strcopy;
         *patlen = len;
@@ -148780,7 +153619,7 @@
     offset->line = FALSE;
     offset->end = FALSE;
     offset->off = 0;
-    if (*p == '+' || *p == '-' || VIM_ISDIGIT(*p))
+    if (*p == '+' || *p == '-' ||  ((unsigned)(*p) - '0' < 10) )
     {
         offset->line = TRUE;
     }
@@ -148792,9 +153631,9 @@
         }
         ++p;
     }
-    if (VIM_ISDIGIT(*p) || *p == '+' || *p == '-')
+    if ( ((unsigned)(*p) - '0' < 10)  || *p == '+' || *p == '-')
     {
-        if (VIM_ISDIGIT(*p) || VIM_ISDIGIT(*(p + 1)))
+        if ( ((unsigned)(*p) - '0' < 10)  ||  ((unsigned)(*(p + 1)) - '0' < 10) )
         {
             offset->off = atol((char *)p);
         }
@@ -148807,7 +153646,7 @@
             offset->off = 1;
         }
         ++p;
-        while (VIM_ISDIGIT(*p))
+        while ( ((unsigned)(*p) - '0' < 10) )
         {
             ++p;
         }
@@ -148976,7 +153815,7 @@
             }
             else
             {
-                vim_memset(msgbuf, ' ', msgbufsize);
+                 memset((msgbuf), (' '), (msgbufsize)) ;
                 msgbuflen = msgbufsize - 1;
                 msgbuf[msgbuflen] = NUL;
                 if (!cmd_silent)
@@ -148988,15 +153827,15 @@
                     if (enc_utf8 && utf_iscomposing(utf_ptr2char(p)))
                     {
                         msgbuf[1] = ' ';
-                        mch_memmove(msgbuf + 2, p, plen);
+                         memmove((char *)(msgbuf + 2), (char *)(p), plen) ;
                     }
                     else
                     {
-                        mch_memmove(msgbuf + 1, p, plen);
+                         memmove((char *)(msgbuf + 1), (char *)(p), plen) ;
                     }
                     if (off_len > 0)
                     {
-                        mch_memmove(msgbuf + plen + 1, off_buf, off_len);
+                         memmove((char *)(msgbuf + plen + 1), (char *)(off_buf), off_len) ;
                     }
 
                     trunc = msg_strtrunc(msgbuf, TRUE);
@@ -149004,7 +153843,7 @@
                     {
                         vim_free(msgbuf);
                         msgbuf = trunc;
-                        msgbuflen = STRLEN(msgbuf);
+                        msgbuflen =  strlen((char *)(msgbuf)) ;
                     }
 
                     msg_outtrans(msgbuf);
@@ -149057,7 +153896,7 @@
             }
         }
 
-        c = searchit(curwin, curbuf, &pos, NULL, dirc == '/' ? FORWARD : BACKWARD, searchstr, searchstrlen, count, spats[0].off.end + (options & (SEARCH_KEEP + SEARCH_PEEK + SEARCH_HIS + SEARCH_MSG + SEARCH_START + ((pat != NULL && *pat == ';') ? 0 : SEARCH_NOOF))), RE_LAST, sia);
+        c = searchit(curwin, curbuf, &pos, NULL, dirc == '/' ? FORWARD :  (-1) , searchstr, searchstrlen, count, spats[0].off.end + (options & (SEARCH_KEEP + SEARCH_PEEK + SEARCH_HIS + SEARCH_MSG + SEARCH_START + ((pat != NULL && *pat == ';') ? 0 : SEARCH_NOOF))), RE_LAST, sia);
 
         if (dircp != NULL)
         {
@@ -149128,7 +153967,7 @@
                     }
                 }
             }
-            if (!EQUAL_POS(pos, org_pos))
+            if (! (((pos).lnum == (org_pos).lnum) && ((pos).col == (org_pos).col) && ((pos).coladd == (org_pos).coladd)) )
             {
                 has_offset = TRUE;
             }
@@ -149235,14 +154074,14 @@
 
         if (compl_status_adding() && !compl_status_sol())
         {
-            if ((p_ic ? MB_STRICMP(p, pat) : STRCMP(p, pat)) == 0)
+            if ((p_ic ?  mb_strnicmp((char_u *)(p), (char_u *)(pat), (int)MAXCOL)  :  strcmp((char *)(p), (char *)(pat)) ) == 0)
             {
                 return OK;
             }
         }
         else if (*p != NUL)
         {
-            if ((p_ic ? MB_STRNICMP(p, pat, compl_len) : STRNCMP(p, pat, compl_len)) == 0)
+            if ((p_ic ?  mb_strnicmp((char_u *)(p), (char_u *)(pat), (int)(compl_len))  :  strncmp((char *)(p), (char *)(pat), (compl_len)) ) == 0)
             {
                 return OK;
             }
@@ -149303,7 +154142,7 @@
         }
     }
 
-    if (dir == BACKWARD)
+    if (dir ==  (-1) )
     {
         cap->oap->inclusive = FALSE;
     }
@@ -149345,7 +154184,7 @@
                         break;
                     }
                 }
-                else if (STRNCMP(p + col, lastc_bytes, lastc_bytelen) == 0 && stop)
+                else if ( strncmp((char *)(p + col), (char *)(lastc_bytes), (lastc_bytelen))  == 0 && stop)
                 {
                     break;
                 }
@@ -149439,7 +154278,7 @@
             {
                 break;
             }
-            if (*p == ')' && STRNCMP(delim_copy, p + 1, delim_len) == 0 && p[delim_len + 1] == '"')
+            if (*p == ')' &&  strncmp((char *)(delim_copy), (char *)(p + 1), (delim_len))  == 0 && p[delim_len + 1] == '"')
             {
                 found = TRUE;
                 break;
@@ -149581,7 +154420,7 @@
 
     if (flags & FM_BACKWARD)
     {
-        dir = BACKWARD;
+        dir =  (-1) ;
     }
     else if (flags & FM_FORWARD)
     {
@@ -149629,7 +154468,7 @@
                 if (*ptr == '#' && pos.col <= (colnr_T)(ptr - linep))
                 {
                     ptr = skipwhite(ptr + 1);
-                    if (   STRNCMP(ptr, "if", 2) == 0 || STRNCMP(ptr, "endif", 5) == 0 || STRNCMP(ptr, "el", 2) == 0)
+                    if (    strncmp((char *)(ptr), (char *)("if"), (2))  == 0 ||  strncmp((char *)(ptr), (char *)("endif"), (5))  == 0 ||  strncmp((char *)(ptr), (char *)("el"), (2))  == 0)
                     {
                         hash_dir = 1;
                     }
@@ -149645,7 +154484,7 @@
                     }
                     else if (pos.col > 0 && linep[pos.col - 1] == '*')
                     {
-                        comment_dir = BACKWARD;
+                        comment_dir =  (-1) ;
                         backwards = TRUE;
                         pos.col--;
                     }
@@ -149654,7 +154493,7 @@
                 {
                     if (linep[pos.col + 1] == '/')
                     {
-                        comment_dir = BACKWARD;
+                        comment_dir =  (-1) ;
                         backwards = TRUE;
                     }
                     else if (pos.col > 0 && linep[pos.col - 1] == '/')
@@ -149673,7 +154512,7 @@
                 }
                 for (;;)
                 {
-                    initc = PTR2CHAR(linep + pos.col);
+                    initc =  (has_mbyte ? mb_ptr2char(linep + pos.col) : (int)*(linep + pos.col)) ;
                     if (initc == NUL)
                     {
                         break;
@@ -149719,11 +154558,11 @@
             if (initc != '#')
             {
                 ptr = skipwhite(skipwhite(linep) + 1);
-                if (STRNCMP(ptr, "if", 2) == 0 || STRNCMP(ptr, "el", 2) == 0)
+                if ( strncmp((char *)(ptr), (char *)("if"), (2))  == 0 ||  strncmp((char *)(ptr), (char *)("el"), (2))  == 0)
                 {
                     hash_dir = 1;
                 }
-                else if (STRNCMP(ptr, "endif", 5) == 0)
+                else if ( strncmp((char *)(ptr), (char *)("endif"), (5))  == 0)
                 {
                     hash_dir = -1;
                 }
@@ -149758,18 +154597,18 @@
                 ptr = skipwhite(ptr + 1);
                 if (hash_dir > 0)
                 {
-                    if (STRNCMP(ptr, "if", 2) == 0)
+                    if ( strncmp((char *)(ptr), (char *)("if"), (2))  == 0)
                     {
                         count++;
                     }
-                    else if (STRNCMP(ptr, "el", 2) == 0)
+                    else if ( strncmp((char *)(ptr), (char *)("el"), (2))  == 0)
                     {
                         if (count == 0)
                         {
                             return &pos;
                         }
                     }
-                    else if (STRNCMP(ptr, "endif", 5) == 0)
+                    else if ( strncmp((char *)(ptr), (char *)("endif"), (5))  == 0)
                     {
                         if (count == 0)
                         {
@@ -149780,7 +154619,7 @@
                 }
                 else
                 {
-                    if (STRNCMP(ptr, "if", 2) == 0)
+                    if ( strncmp((char *)(ptr), (char *)("if"), (2))  == 0)
                     {
                         if (count == 0)
                         {
@@ -149788,14 +154627,14 @@
                         }
                         count--;
                     }
-                    else if (initc == '#' && STRNCMP(ptr, "el", 2) == 0)
+                    else if (initc == '#' &&  strncmp((char *)(ptr), (char *)("el"), (2))  == 0)
                     {
                         if (count == 0)
                         {
                             return &pos;
                         }
                     }
-                    else if (STRNCMP(ptr, "endif", 5) == 0)
+                    else if ( strncmp((char *)(ptr), (char *)("endif"), (5))  == 0)
                     {
                         count++;
                     }
@@ -149807,7 +154646,9 @@
 
     do_quotes = -1;
     start_in_quotes = MAYBE;
-    CLEAR_POS(&match_pos);
+     (&match_pos)->lnum = 0;
+     (&match_pos)->col = 0;
+     (&match_pos)->coladd = 0;
 
     if ((backwards && comment_dir) || lisp || skip_comments)
     {
@@ -150073,7 +154914,7 @@
             start_in_quotes = FALSE;
         }
 
-        c = PTR2CHAR(linep + pos.col);
+        c =  (has_mbyte ? mb_ptr2char(linep + pos.col) : (int)*(linep + pos.col)) ;
         switch (c)
         {
         case NUL:
@@ -150138,6 +154979,7 @@
                 }
             }
 
+        __attribute__((fallthrough));
         default:
             if (curbuf->b_p_lisp && vim_strchr((char_u *)"{}()[]", c) != NULL && pos.col > 1 && check_prevcol(linep, pos.col, '\\', NULL) && check_prevcol(linep, pos.col - 1, '#', NULL))
             {
@@ -150180,7 +155022,7 @@
         }
     }
 
-    if (comment_dir == BACKWARD && count > 0)
+    if (comment_dir ==  (-1)  && count > 0)
     {
         pos = match_pos;
         return &pos;
@@ -150199,13 +155041,13 @@
     long        save_siso;
     colnr_T     save_dollar_vcol;
     char_u      *p;
-    long        *so = curwin->w_p_so >= 0 ? &curwin->w_p_so : &p_so;
-    long        *siso = curwin->w_p_siso >= 0 ? &curwin->w_p_siso : &p_siso;
+    long        *so = curwin-> w_onebuf_opt.wo_so  >= 0 ? &curwin-> w_onebuf_opt.wo_so  : &p_so;
+    long        *siso = curwin-> w_onebuf_opt.wo_siso  >= 0 ? &curwin-> w_onebuf_opt.wo_siso  : &p_siso;
 
     for (p = curbuf->b_p_mps; *p != NUL; ++p)
     {
         p += mb_ptr2len(p) + 1;
-        if (PTR2CHAR(p) == c)
+        if ( (has_mbyte ? mb_ptr2char(p) : (int)*(p))  == c)
         {
             break;
         }
@@ -150231,12 +155073,12 @@
         return;
     }
 
-    if (!curwin->w_p_wrap)
+    if (!curwin-> w_onebuf_opt.wo_wrap )
     {
         getvcol(curwin, lpos, NULL, &vcol, NULL, 0);
     }
 
-    int col_visible = (curwin->w_p_wrap || (vcol >= curwin->w_leftcol && vcol < curwin->w_leftcol + curwin->w_width));
+    int col_visible = (curwin-> w_onebuf_opt.wo_wrap  || (vcol >= curwin->w_leftcol && vcol < curwin->w_leftcol + curwin->w_width));
     if (!col_visible)
     {
         return;
@@ -150301,7 +155143,9 @@
     regmatch.startpos[0].col = -1;
     if (move)
     {
-        CLEAR_POS(&pos);
+         (&pos)->lnum = 0;
+         (&pos)->col = 0;
+         (&pos)->coladd = 0;
     }
     else
     {
@@ -150347,13 +155191,13 @@
     int         zero_width;
     int         skip_first_backward;
 
-    if (VIsual_active && *p_sel == 'e' && LT_POS(VIsual, curwin->w_cursor))
+    if (VIsual_active && *p_sel == 'e' &&  (((VIsual).lnum != (curwin->w_cursor).lnum)               ? (VIsual).lnum < (curwin->w_cursor).lnum                   : (VIsual).col != (curwin->w_cursor).col                        ? (VIsual).col < (curwin->w_cursor).col                     : (VIsual).coladd < (curwin->w_cursor).coladd) )
     {
         dec_cursor();
     }
 
     skip_first_backward = forward && VIsual_active
-                                           && LT_POS(curwin->w_cursor, VIsual);
+                                           &&  (((curwin->w_cursor).lnum != (VIsual).lnum)               ? (curwin->w_cursor).lnum < (VIsual).lnum                   : (curwin->w_cursor).col != (VIsual).col                        ? (curwin->w_cursor).col < (VIsual).col                     : (curwin->w_cursor).coladd < (VIsual).coladd) ;
 
     orig_pos = pos = curwin->w_cursor;
     if (VIsual_active)
@@ -150401,7 +155245,7 @@
             p_ws = FALSE;
         }
 
-        result = searchit(curwin, curbuf, &pos, &end_pos, (dir ? FORWARD : BACKWARD), spats[last_idx].pat, spats[last_idx].patlen, (long) (i ? count : 1), SEARCH_KEEP | flags, RE_SEARCH, NULL);
+        result = searchit(curwin, curbuf, &pos, &end_pos, (dir ? FORWARD :  (-1) ), spats[last_idx].pat, spats[last_idx].patlen, (long) (i ? count : 1), SEARCH_KEEP | flags, RE_SEARCH, NULL);
 
         p_ws = old_p_ws;
 
@@ -150418,7 +155262,9 @@
         {
             if (forward)
             {
-                CLEAR_POS(&pos);
+                 (&pos)->lnum = 0;
+                 (&pos)->col = 0;
+                 (&pos)->coladd = 0;
             }
             else
             {
@@ -150436,7 +155282,7 @@
     }
 
     curwin->w_cursor = end_pos;
-    if (LT_POS(VIsual, end_pos) && forward)
+    if ( (((VIsual).lnum != (end_pos).lnum)               ? (VIsual).lnum < (end_pos).lnum                   : (VIsual).col != (end_pos).col                        ? (VIsual).col < (end_pos).col                     : (VIsual).coladd < (end_pos).coladd)  && forward)
     {
         if (skip_first_backward)
         {
@@ -150447,7 +155293,7 @@
             dec_cursor();
         }
     }
-    else if (VIsual_active && LT_POS(curwin->w_cursor, VIsual) && forward)
+    else if (VIsual_active &&  (((curwin->w_cursor).lnum != (VIsual).lnum)               ? (curwin->w_cursor).lnum < (VIsual).lnum                   : (curwin->w_cursor).col != (VIsual).col                        ? (curwin->w_cursor).col < (VIsual).col                     : (curwin->w_cursor).coladd < (VIsual).coladd)  && forward)
     {
         curwin->w_cursor = pos;
     }
@@ -150456,11 +155302,11 @@
 
     if (*p_sel == 'e')
     {
-        if (forward && LTOREQ_POS(VIsual, curwin->w_cursor))
+        if (forward &&  ( (((VIsual).lnum != (curwin->w_cursor).lnum)               ? (VIsual).lnum < (curwin->w_cursor).lnum                   : (VIsual).col != (curwin->w_cursor).col                        ? (VIsual).col < (curwin->w_cursor).col                     : (VIsual).coladd < (curwin->w_cursor).coladd)  ||  (((VIsual).lnum == (curwin->w_cursor).lnum) && ((VIsual).col == (curwin->w_cursor).col) && ((VIsual).coladd == (curwin->w_cursor).coladd)) ) )
         {
             inc_cursor();
         }
-        else if (!forward && LTOREQ_POS(curwin->w_cursor, VIsual))
+        else if (!forward &&  ( (((curwin->w_cursor).lnum != (VIsual).lnum)               ? (curwin->w_cursor).lnum < (VIsual).lnum                   : (curwin->w_cursor).col != (VIsual).col                        ? (curwin->w_cursor).col < (VIsual).col                     : (curwin->w_cursor).coladd < (VIsual).coladd)  ||  (((curwin->w_cursor).lnum == (VIsual).lnum) && ((curwin->w_cursor).col == (VIsual).col) && ((curwin->w_cursor).coladd == (VIsual).coladd)) ) )
         {
             inc(&VIsual);
         }
@@ -150518,7 +155364,7 @@
 
     if (show_top_bot_msg && len + 2 < SEARCH_STAT_BUF_LEN)
     {
-        mch_memmove(t + 2, t, len);
+         memmove((char *)(t + 2), (char *)(t), len) ;
         t[0] = 'W';
         t[1] = ' ';
         len += 2;
@@ -150528,7 +155374,7 @@
     {
         len = msgbuflen;
     }
-    mch_memmove(msgbuf + msgbuflen - len, t, len);
+     memmove((char *)(msgbuf + msgbuflen - len), (char *)(t), len) ;
 
     if (dirc == '?' && stat.cur == maxcount + 1)
     {
@@ -150541,7 +155387,7 @@
 }
 
     static void
-update_search_stat(int                 dirc, pos_T               *pos, pos_T               *cursor_pos, searchstat_T        *stat, int                 recompute, int                 maxcount, long                timeout UNUSED)
+update_search_stat(int                 dirc, pos_T               *pos, pos_T               *cursor_pos, searchstat_T        *stat, int                 recompute, int                 maxcount, long                timeout  __attribute__((unused)) )
 {
     int             save_ws = p_ws;
     int             wraparound = FALSE;
@@ -150557,9 +155403,9 @@
     static size_t   lastpatlen = 0;
     static buf_T    *lbuf = NULL;
 
-    CLEAR_POINTER(stat);
+      memset(((stat)), (0), (sizeof(*(stat))))  ;
 
-    if (dirc == 0 && !recompute && !EMPTY_POS(lastpos))
+    if (dirc == 0 && !recompute && ! ((lastpos).lnum == 0 && (lastpos).col == 0 && (lastpos).coladd == 0) )
     {
         stat->cur = cur;
         stat->cnt = cnt;
@@ -150570,19 +155416,21 @@
     }
     last_maxcount = maxcount;
 
-    wraparound = ((dirc == '?' && LT_POS(lastpos, p)) || (dirc == '/' && LT_POS(p, lastpos)));
+    wraparound = ((dirc == '?' &&  (((lastpos).lnum != (p).lnum)               ? (lastpos).lnum < (p).lnum                   : (lastpos).col != (p).col                        ? (lastpos).col < (p).col                     : (lastpos).coladd < (p).coladd) ) || (dirc == '/' &&  (((p).lnum != (lastpos).lnum)               ? (p).lnum < (lastpos).lnum                   : (p).col != (lastpos).col                        ? (p).col < (lastpos).col                     : (p).coladd < (lastpos).coladd) ));
 
-    if (!(chgtick == CHANGEDTICK(curbuf) && (lastpat != NULL && STRNCMP(lastpat, spats[last_idx].pat, lastpatlen) == 0 && lastpatlen == spats[last_idx].patlen) && EQUAL_POS(lastpos, *cursor_pos) && lbuf == curbuf) || wraparound || cur < 0 || (maxcount > 0 && cur > maxcount) || recompute)
+    if (!(chgtick ==  ((curbuf)->b_ct_di.di_tv.vval.v_number)  && (lastpat != NULL &&  strncmp((char *)(lastpat), (char *)(spats[last_idx].pat), (lastpatlen))  == 0 && lastpatlen == spats[last_idx].patlen) &&  (((lastpos).lnum == (*cursor_pos).lnum) && ((lastpos).col == (*cursor_pos).col) && ((lastpos).coladd == (*cursor_pos).coladd))  && lbuf == curbuf) || wraparound || cur < 0 || (maxcount > 0 && cur > maxcount) || recompute)
     {
         cur = 0;
         cnt = 0;
         exact_match = FALSE;
         incomplete = 0;
-        CLEAR_POS(&lastpos);
+         (&lastpos)->lnum = 0;
+         (&lastpos)->col = 0;
+         (&lastpos)->coladd = 0;
         lbuf = curbuf;
     }
 
-    if (EQUAL_POS(lastpos, *cursor_pos) && !wraparound && (dirc == 0 || dirc == '/' ? cur < cnt : cur > 1))
+    if ( (((lastpos).lnum == (*cursor_pos).lnum) && ((lastpos).col == (*cursor_pos).col) && ((lastpos).coladd == (*cursor_pos).coladd))  && !wraparound && (dirc == 0 || dirc == '/' ? cur < cnt : cur > 1))
     {
         cur += dirc == 0 ? 0 : dirc == '/' ? 1 : -1;
     }
@@ -150596,10 +155444,10 @@
         {
             done_search = TRUE;
             cnt++;
-            if (LTOREQ_POS(lastpos, p))
+            if ( ( (((lastpos).lnum != (p).lnum)               ? (lastpos).lnum < (p).lnum                   : (lastpos).col != (p).col                        ? (lastpos).col < (p).col                     : (lastpos).coladd < (p).coladd)  ||  (((lastpos).lnum == (p).lnum) && ((lastpos).col == (p).col) && ((lastpos).coladd == (p).coladd)) ) )
             {
                 cur = cnt;
-                if (LT_POS(p, endpos))
+                if ( (((p).lnum != (endpos).lnum)               ? (p).lnum < (endpos).lnum                   : (p).col != (endpos).col                        ? (p).col < (endpos).col                     : (p).coladd < (endpos).coladd) )
                 {
                     exact_match = TRUE;
                 }
@@ -150627,7 +155475,7 @@
             {
                 lastpatlen = spats[last_idx].patlen;
             }
-            chgtick = CHANGEDTICK(curbuf);
+            chgtick =  ((curbuf)->b_ct_di.di_tv.vval.v_number) ;
             lbuf = curbuf;
             lastpos = p;
         }
@@ -150661,17 +155509,16 @@
         }
     else if (eap->cmdidx == CMD_mkvimrc)
     {
-        fname = (char_u *)VIMRC_FILE;
+        fname = (char_u *) ".vimrc" ;
     }
     else
     {
-        fname = (char_u *)EXRC_FILE;
+        fname = (char_u *) ".exrc" ;
     }
 
-    fd = open_exfile(fname, eap->forceit, WRITEBIN);
+    fd = open_exfile(fname, eap->forceit,  "w" );
     if (fd != NULL)
     {
-
         if (put_line(fd, "vim9script") == FAIL)
         {
             failed = TRUE;
@@ -150748,11 +155595,11 @@
     char_u      *p;
     size_t      len;
 
-    len = STRLEN(string) + 1;
+    len =  strlen((char *)(string))  + 1;
     p = alloc(len);
     if (p != NULL)
     {
-        mch_memmove(p, string, len);
+         memmove((char *)(p), (char *)(string), len) ;
     }
     return p;
 }
@@ -150768,7 +155615,7 @@
         return NULL;
     }
 
-    STRNCPY(p, string, len);
+     strncpy((char *)(p), (char *)(string), (len)) ;
     p[len] = NUL;
     return p;
 }
@@ -150813,7 +155660,7 @@
     {
         if (has_mbyte && (l = (*mb_ptr2len)(p)) > 1)
         {
-            mch_memmove(p2, p, (size_t)l);
+             memmove((char *)(p2), (char *)(p), (size_t)l) ;
             p2 += l;
             p += l - 1;
             continue;
@@ -150860,8 +155707,8 @@
     shname = gettail(p_sh);
     powershell = strstr((char *)shname, "pwsh") != NULL;
 
-    length = (unsigned)STRLEN(string) + 3;
-    for (p = string; *p != NUL; MB_PTR_ADV(p))
+    length = (unsigned) strlen((char *)(string))  + 3;
+    for (p = string; *p != NUL;  p += (*mb_ptr2len)(p) )
     {
         if (*p == '\'')
         {
@@ -150944,7 +155791,14 @@
                 continue;
             }
 
-            MB_COPY_CHAR(p, d);
+             if (has_mbyte)
+             {
+                 mb_copy_char(&(p), &(d));
+             }
+             else
+             {
+                 *(d)++ = *(p)++;
+             }
         }
 
             *d++ = '\'';
@@ -150954,7 +155808,6 @@
     return escaped_string;
 }
 
-
     char_u *
 vim_strnsave_up(char_u *string, size_t len)
 {
@@ -150988,8 +155841,8 @@
 {
     char_u      *q;
 
-    q = ptr + STRLEN(ptr);
-    while (--q > ptr && VIM_ISWHITE(q[0]) && q[-1] != '\\' && q[-1] != Ctrl_V)
+    q = ptr +  strlen((char *)(ptr)) ;
+    while (--q > ptr &&  ((q[0]) == ' ' || (q[0]) == '\t')  && q[-1] != '\\' && q[-1] != Ctrl_V)
     {
         *q = NUL;
     }
@@ -150998,24 +155851,24 @@
     void
 vim_strncpy(char_u *to, char_u *from, size_t len)
 {
-    STRNCPY(to, from, len);
+     strncpy((char *)(to), (char *)(from), (len)) ;
     to[len] = NUL;
 }
 
     void
 vim_strcat(char_u *to, char_u *from, size_t tosize)
 {
-    size_t tolen = STRLEN(to);
-    size_t fromlen = STRLEN(from);
+    size_t tolen =  strlen((char *)(to)) ;
+    size_t fromlen =  strlen((char *)(from)) ;
 
     if (tolen + fromlen + 1 > tosize)
     {
-        mch_memmove(to + tolen, from, tosize - tolen - 1);
+         memmove((char *)(to + tolen), (char *)(from), tosize - tolen - 1) ;
         to[tosize - 1] = NUL;
     }
     else
     {
-        mch_memmove(to + tolen, from, fromlen + 1);
+         memmove((char *)(to + tolen), (char *)(from), fromlen + 1) ;
     }
 }
 
@@ -151040,7 +155893,7 @@
 
     while (len > 0)
     {
-        i = TOLOWER_ASC(*s1) - TOLOWER_ASC(*s2);
+        i =  (((*s1) < 'A' || (*s1) > 'Z') ? (*s1) : (*s1) + ('a' - 'A'))  -  (((*s2) < 'A' || (*s2) > 'Z') ? (*s2) : (*s2) + ('a' - 'A')) ;
         if (i != 0)
         {
             break;
@@ -151147,7 +156000,7 @@
         {
             retval = p;
         }
-        MB_PTR_ADV(p);
+         p += (*mb_ptr2len)(p) ;
     }
     return retval;
 }
@@ -151157,7 +156010,7 @@
     static int
 sort_compare(const void *s1, const void *s2)
 {
-    return STRCMP(*(char **)s1, *(char **)s2);
+    return  strcmp((char *)(*(char **)s1), (char *)(*(char **)s2)) ;
 }
 
     void
@@ -151170,9 +156023,9 @@
 concat_str(char_u *str1, char_u *str2)
 {
     char_u  *dest;
-    size_t  l = str1 == NULL ? 0 : STRLEN(str1);
+    size_t  l = str1 == NULL ? 0 :  strlen((char *)(str1)) ;
 
-    dest = alloc(l + (str2 == NULL ? 0 : STRLEN(str2)) + 1L);
+    dest = alloc(l + (str2 == NULL ? 0 :  strlen((char *)(str2)) ) + 1L);
     if (dest == NULL)
     {
         return NULL;
@@ -151183,11 +156036,11 @@
     }
     else
     {
-        STRCPY(dest, str1);
+         strcpy((char *)(dest), (char *)(str1)) ;
     }
     if (str2 != NULL)
     {
-        STRCPY(dest + l, str2);
+         strcpy((char *)(dest + l), (char *)(str2)) ;
     }
     return dest;
 }
@@ -151202,7 +156055,7 @@
     };
     int idx = positive * (1 + force_sign + force_sign * space_for_positive);
 
-    if (ASCII_ISUPPER(fmt_spec))
+    if ( ((unsigned)(fmt_spec) - 'A' < 26) )
     {
         idx += 4;
     }
@@ -151214,7 +156067,7 @@
 {
     va_list     ap;
     int         str_l;
-    size_t      len = STRLEN(str);
+    size_t      len =  strlen((char *)(str)) ;
     size_t      space;
 
     if (str_m <= len)
@@ -151353,7 +156206,6 @@
         case 'X':
     case 'p':
         {
-
             if (fmt_spec == 'p')
             {
                 return TYPE_POINTER;
@@ -151461,11 +156313,11 @@
 
         if (*ap_types == NULL)
         {
-            new_types = ALLOC_CLEAR_MULT(const char *, arg);
+            new_types =  (const char * *)alloc_clear(sizeof(const char *) * (arg)) ;
         }
         else
         {
-            new_types = vim_realloc((char **)*ap_types, arg * sizeof(const char *));
+            new_types =  realloc(((char **)*ap_types), (arg * sizeof(const char *))) ;
         }
 
         if (new_types == NULL)
@@ -151527,13 +156379,13 @@
     char        *argcopy = NULL;
     const char  *p = pstart;
 
-    while (VIM_ISDIGIT((int)(*p)))
+    while ( ((unsigned)((int)(*p)) - '0' < 10) )
     {
         ++p;
     }
 
     arglen = p - pstart;
-    argcopy = ALLOC_CLEAR_MULT(char, arglen + 1);
+    argcopy =  (char *)alloc_clear(sizeof(char) * (arglen + 1)) ;
     if (argcopy != NULL)
     {
         strncpy(argcopy, pstart, arglen);
@@ -151546,7 +156398,7 @@
     }
 }
 
-# define MAX_ALLOWED_STRING_WIDTH 1048576
+enum { MAX_ALLOWED_STRING_WIDTH = 1048576 };
 
     static int
 get_unsigned_int(const char *pstart, const char **p, unsigned int *uj, int overflow_err)
@@ -151554,7 +156406,7 @@
     *uj = **p - '0';
     ++*p;
 
-    while (VIM_ISDIGIT((int)(**p)) && *uj < MAX_ALLOWED_STRING_WIDTH)
+    while ( ((unsigned)((int)(**p)) - '0' < 10)  && *uj < MAX_ALLOWED_STRING_WIDTH)
     {
         *uj = 10 * *uj + (unsigned int)(**p - '0');
         ++*p;
@@ -151577,7 +156429,7 @@
 }
 
     static int
-parse_fmt_types(const char  ***ap_types, int         *num_posarg, const char  *fmt, typval_T    *tvs UNUSED)
+parse_fmt_types(const char  ***ap_types, int         *num_posarg, const char  *fmt, typval_T    *tvs  __attribute__((unused)) )
 {
     const char  *p = fmt;
     const char  *arg = NULL;
@@ -151586,8 +156438,6 @@
     int         any_arg = 0;
     int         arg_idx;
 
-# define CHECK_POS_ARG do {     if (any_pos && any_arg)     {   semsg(_( e_cannot_mix_positional_and_non_positional_str), fmt);         goto error;     } } while (0);
-
     if (p == NULL)
     {
         return OK;
@@ -151598,7 +156448,7 @@
         if (*p != '%')
         {
             const char    *q = strchr(p + 1, '%');
-            size_t  n = (q == NULL) ? STRLEN(p) : (size_t)(q - p);
+            size_t  n = (q == NULL) ?  strlen((char *)(p))  : (size_t)(q - p);
 
             p += n;
         }
@@ -151614,7 +156464,7 @@
 
             ptype = p;
 
-            while (VIM_ISDIGIT(*ptype))
+            while ( ((unsigned)(*ptype) - '0' < 10) )
             {
                 ++ptype;
             }
@@ -151637,7 +156487,8 @@
                 pos_arg = uj;
 
                 any_pos = 1;
-                CHECK_POS_ARG;
+                 if (any_pos && any_arg)
+                     {   semsg(_( e_cannot_mix_positional_and_non_positional_str), fmt);         goto error;     }   ;
 
                 ++p;
             }
@@ -151666,7 +156517,7 @@
             {
                 p++;
 
-                if (VIM_ISDIGIT((int)(*p)))
+                if ( ((unsigned)((int)(*p)) - '0' < 10) )
                 {
                     unsigned int uj;
 
@@ -151684,7 +156535,8 @@
                     {
                         ++p;
                         any_pos = 1;
-                        CHECK_POS_ARG;
+                         if (any_pos && any_arg)
+                             {   semsg(_( e_cannot_mix_positional_and_non_positional_str), fmt);         goto error;     }   ;
 
                         if (adjust_types(ap_types, uj, num_posarg, arg) == FAIL)
                         {
@@ -151695,10 +156547,11 @@
                 else
                 {
                     any_arg = 1;
-                    CHECK_POS_ARG;
+                     if (any_pos && any_arg)
+                         {   semsg(_( e_cannot_mix_positional_and_non_positional_str), fmt);         goto error;     }   ;
                 }
             }
-            else if (VIM_ISDIGIT((int)(*p)))
+            else if ( ((unsigned)((int)(*p)) - '0' < 10) )
             {
                 const char *digstart = p;
                 unsigned int uj;
@@ -151723,7 +156576,7 @@
                 {
                     p++;
 
-                    if (VIM_ISDIGIT((int)(*p)))
+                    if ( ((unsigned)((int)(*p)) - '0' < 10) )
                     {
                         unsigned int uj;
 
@@ -151735,7 +156588,8 @@
                         if (*p == '$')
                         {
                             any_pos = 1;
-                            CHECK_POS_ARG;
+                             if (any_pos && any_arg)
+                                 {   semsg(_( e_cannot_mix_positional_and_non_positional_str), fmt);         goto error;     }   ;
 
                             ++p;
 
@@ -151753,10 +156607,11 @@
                     else
                     {
                         any_arg = 1;
-                        CHECK_POS_ARG;
+                         if (any_pos && any_arg)
+                             {   semsg(_( e_cannot_mix_positional_and_non_positional_str), fmt);         goto error;     }   ;
                     }
                 }
-                else if (VIM_ISDIGIT((int)(*p)))
+                else if ( ((unsigned)((int)(*p)) - '0' < 10) )
                 {
                     const char *digstart = p;
                     unsigned int uj;
@@ -151777,7 +156632,8 @@
             if (pos_arg != -1)
             {
                 any_pos = 1;
-                CHECK_POS_ARG;
+                 if (any_pos && any_arg)
+                     {   semsg(_( e_cannot_mix_positional_and_non_positional_str), fmt);         goto error;     }   ;
 
                 ptype = p;
             }
@@ -151826,7 +156682,8 @@
                     else
                     {
                         any_arg = 1;
-                        CHECK_POS_ARG;
+                         if (any_pos && any_arg)
+                             {   semsg(_( e_cannot_mix_positional_and_non_positional_str), fmt);         goto error;     }   ;
                     }
                     break;
 
@@ -151981,13 +156838,13 @@
         if (*p != '%')
         {
             const char    *q = strchr(p + 1, '%');
-            size_t  n = (q == NULL) ? STRLEN(p) : (size_t)(q - p);
+            size_t  n = (q == NULL) ?  strlen((char *)(p))  : (size_t)(q - p);
 
             if (str_l < str_m)
             {
                 size_t avail = str_m - str_l;
 
-                mch_memmove(str + str_l, p, n > avail ? avail : n);
+                 memmove((char *)(str + str_l), (char *)(p), n > avail ? avail : n) ;
             }
             p += n;
             str_l += n;
@@ -152006,7 +156863,6 @@
 
             char    length_modifier = '\0';
 
-# define TMP_LEN 350
             char    tmp[TMP_LEN];
 
             const char  *str_arg = NULL;
@@ -152030,7 +156886,7 @@
 
             ptype = p;
 
-            while (VIM_ISDIGIT(*ptype))
+            while ( ((unsigned)(*ptype) - '0' < 10) )
             {
                 ++ptype;
             }
@@ -152083,7 +156939,7 @@
 
                 p++;
 
-                if (VIM_ISDIGIT((int)(*p)))
+                if ( ((unsigned)((int)(*p)) - '0' < 10) )
                 {
                     unsigned int uj;
 
@@ -152123,7 +156979,7 @@
                     justify_left = 1;
                 }
             }
-            else if (VIM_ISDIGIT((int)(*p)))
+            else if ( ((unsigned)((int)(*p)) - '0' < 10) )
             {
                 const char *digstart = p;
                 unsigned int uj;
@@ -152141,7 +156997,7 @@
                 p++;
                 precision_specified = 1;
 
-                if (VIM_ISDIGIT((int)(*p)))
+                if ( ((unsigned)((int)(*p)) - '0' < 10) )
                 {
                     const char *digstart = p;
                     unsigned int uj;
@@ -152160,7 +157016,7 @@
 
                     p++;
 
-                    if (VIM_ISDIGIT((int)(*p)))
+                    if ( ((unsigned)((int)(*p)) - '0' < 10) )
                     {
                         unsigned int uj;
 
@@ -152328,7 +157184,6 @@
                 case 'X':
             case 'p':
                 {
-
                     int arg_sign = 0;
 
                     int int_arg = 0;
@@ -152616,7 +157471,7 @@
                     {
                         if ((abs_f >= 0.001 && abs_f < 10000000.0) || abs_f == 0.0)
                         {
-                            fmt_spec = ASCII_ISUPPER(fmt_spec) ? 'F' : 'f';
+                            fmt_spec =  ((unsigned)(fmt_spec) - 'A' < 26)  ? 'F' : 'f';
                         }
                         else
                         {
@@ -152627,22 +157482,22 @@
 
                     if ((fmt_spec == 'f' || fmt_spec == 'F') && abs_f > 1.0e307)
                     {
-                        STRCPY(tmp, infinity_str(f > 0.0, fmt_spec, force_sign, space_for_positive));
-                        str_arg_l = STRLEN(tmp);
+                         strcpy((char *)(tmp), (char *)(infinity_str(f > 0.0, fmt_spec, force_sign, space_for_positive))) ;
+                        str_arg_l =  strlen((char *)(tmp)) ;
                         zero_padding = 0;
                     }
                     else
                     {
                         if (isnan(f))
                         {
-                            STRCPY(tmp, ASCII_ISUPPER(fmt_spec) ? "NAN" : "nan");
+                             strcpy((char *)(tmp), (char *)( ((unsigned)(fmt_spec) - 'A' < 26)  ? "NAN" : "nan")) ;
                             str_arg_l = 3;
                             zero_padding = 0;
                         }
                         else if (isinf(f))
                         {
-                            STRCPY(tmp, infinity_str(f > 0.0, fmt_spec, force_sign, space_for_positive));
-                            str_arg_l = STRLEN(tmp);
+                             strcpy((char *)(tmp), (char *)(infinity_str(f > 0.0, fmt_spec, force_sign, space_for_positive))) ;
+                            str_arg_l =  strlen((char *)(tmp)) ;
                             zero_padding = 0;
                         }
                         else
@@ -152689,13 +157544,13 @@
                                 {
                                     if (tp[1] == '+')
                                     {
-                                        STRMOVE(tp + 1, tp + 2);
+                                          memmove((char *)((tp + 1)), (char *)((tp + 2)),  strlen((char *)(tp + 2))  + 1)  ;
                                         --str_arg_l;
                                     }
                                     i = (tp[1] == '-') ? 2 : 1;
                                     while (tp[i] == '0')
                                     {
-                                        STRMOVE(tp + i, tp + i + 1);
+                                          memmove((char *)((tp + i)), (char *)((tp + i + 1)),  strlen((char *)(tp + i + 1))  + 1)  ;
                                         --str_arg_l;
                                     }
                                     --tp;
@@ -152706,7 +157561,7 @@
                             {
                                 while (tp > tmp + 2 && *tp == '0' && tp[-1] != '.')
                                 {
-                                    STRMOVE(tp, tp + 1);
+                                      memmove((char *)((tp)), (char *)((tp + 1)),  strlen((char *)(tp + 1))  + 1)  ;
                                     --tp;
                                     --str_arg_l;
                                 }
@@ -152719,7 +157574,7 @@
                             tp = (char *)vim_strchr((char_u *)tmp, fmt_spec == 'e' ? 'e' : 'E');
                             if (tp != NULL && (tp[1] == '+' || tp[1] == '-') && tp[2] == '0' && vim_isdigit(tp[3]) && vim_isdigit(tp[4]))
                             {
-                                STRMOVE(tp + 2, tp + 3);
+                                  memmove((char *)((tp + 2)), (char *)((tp + 3)),  strlen((char *)(tp + 3))  + 1)  ;
                                 --str_arg_l;
                             }
                         }
@@ -152762,7 +157617,7 @@
                     {
                         size_t avail = str_m - str_l;
 
-                        vim_memset(str + str_l, zero_padding ? '0' : ' ', (size_t)pn > avail ? avail : (size_t)pn);
+                         memset((str + str_l), (zero_padding ? '0' : ' '), ((size_t)pn > avail ? avail : (size_t)pn)) ;
                     }
                     str_l += pn;
                 }
@@ -152782,7 +157637,7 @@
                     {
                         size_t avail = str_m - str_l;
 
-                        mch_memmove(str + str_l, str_arg, (size_t)zn > avail ? avail : (size_t)zn);
+                         memmove((char *)(str + str_l), (char *)(str_arg), (size_t)zn > avail ? avail : (size_t)zn) ;
                     }
                     str_l += zn;
                 }
@@ -152794,7 +157649,7 @@
                     {
                         size_t avail = str_m - str_l;
 
-                        vim_memset(str + str_l, '0', (size_t)zn > avail ? avail : (size_t)zn);
+                         memset((str + str_l), ('0'), ((size_t)zn > avail ? avail : (size_t)zn)) ;
                     }
                     str_l += zn;
                 }
@@ -152809,7 +157664,7 @@
                     {
                         size_t avail = str_m - str_l;
 
-                        mch_memmove(str + str_l, str_arg + zero_padding_insertion_ind, (size_t)sn > avail ? avail : (size_t)sn);
+                         memmove((char *)(str + str_l), (char *)(str_arg + zero_padding_insertion_ind), (size_t)sn > avail ? avail : (size_t)sn) ;
                     }
                     str_l += sn;
                 }
@@ -152825,7 +157680,7 @@
                     {
                         size_t avail = str_m - str_l;
 
-                        vim_memset(str + str_l, ' ', (size_t)pn > avail ? avail : (size_t)pn);
+                         memset((str + str_l), (' '), ((size_t)pn > avail ? avail : (size_t)pn)) ;
                     }
                     str_l += pn;
                 }
@@ -152914,19 +157769,19 @@
     int sort_error;
 } findtags_match_args_T;
 
-#define MT_ST_CUR       0
-#define MT_GL_CUR       1
-#define MT_GL_OTH       2
-#define MT_ST_OTH       3
-#define MT_IC_OFF       4
-#define MT_RE_OFF       8
-#define MT_MASK         7
-#define MT_COUNT        16
+enum { MT_ST_CUR = 0 };
+enum { MT_GL_CUR = 1 };
+enum { MT_GL_OTH = 2 };
+enum { MT_ST_OTH = 3 };
+enum { MT_IC_OFF = 4 };
+enum { MT_RE_OFF = 8 };
+enum { MT_MASK = 7 };
+enum { MT_COUNT = 16 };
 
 static char     *mt_names[MT_COUNT/2] =
                 {"FSC", "F C", "F  ", "FS ", " SC", "  C", "   ", " S "};
 
-#define NOTAGFILE       99
+enum { NOTAGFILE = 99 };
 static char_u   *nofile_fname = NULL;
 
 static void taglen_advance(int l);
@@ -152943,7 +157798,7 @@
 
 static char_u   *tagmatchname = NULL;
 
-#define TAG_SEP 0x02
+enum { TAG_SEP = 0x02 };
 
     int
 do_tag(char_u      *tag, int         type, int         count, int         forceit, int         verbose)
@@ -152992,7 +157847,9 @@
     free_string_option(nofile_fname);
     nofile_fname = NULL;
 
-    CLEAR_POS(&saved_fmark.mark);
+     (&saved_fmark.mark)->lnum = 0;
+     (&saved_fmark.mark)->col = 0;
+     (&saved_fmark.mark)->coladd = 0;
     saved_fmark.fnum = 0;
 
     if ((!p_tgst && *tag != NUL))
@@ -153192,7 +158049,7 @@
         {
             name = tag;
         }
-        other_name = (tagmatchname == NULL || STRCMP(tagmatchname, name) != 0);
+        other_name = (tagmatchname == NULL ||  strcmp((char *)(tagmatchname), (char *)(name))  != 0);
         if (new_tag || (cur_match >= num_matches && max_num_matches != MAXCOL) || other_name)
         {
             if (other_name)
@@ -153260,7 +158117,7 @@
                     for (i = idx; i < new_num_matches; ++i)
                     {
                         parse_match(new_matches[i], &tagp2);
-                        if (STRCMP(tagp.tagname, tagp2.tagname) == 0)
+                        if ( strcmp((char *)(tagp.tagname), (char *)(tagp2.tagname))  == 0)
                         {
                             char_u *p = new_matches[i];
                             for (k = i; k > idx; --k)
@@ -153339,7 +158196,8 @@
 
                 if (use_tfu && parse_match(matches[cur_match], &tagp) == OK && tagp.user_data)
                 {
-                    VIM_CLEAR(tagstack[tagstackidx].user_data);
+                     vim_free(tagstack[tagstackidx].user_data);
+                     (tagstack[tagstackidx].user_data) = NULL;
                     tagstack[tagstackidx].user_data = vim_strnsave(tagp.user_data, tagp.user_data_end - tagp.user_data);
                 }
 
@@ -153357,13 +158215,13 @@
                 sprintf((char *)IObuff, _("tag %d of %d%s"), cur_match + 1, num_matches, max_num_matches != MAXCOL ? _(" or more") : "");
                 if (ic)
                 {
-                    STRCAT(IObuff, _("  Using tag with different case!"));
+                     strcat((char *)(IObuff), (char *)(_("  Using tag with different case!"))) ;
                 }
                 if ((num_matches > prev_num_matches || new_tag) && num_matches > 1)
                 {
                     if (ic)
                     {
-                        msg_attr((char *)IObuff, HL_ATTR(HLF_W));
+                        msg_attr((char *)IObuff,  highlight_attr[(int)(HLF_W)] );
                     }
                     else
                     {
@@ -153455,10 +158313,10 @@
         msg_didout = FALSE;
     }
     msg_start();
-    msg_puts_attr(_("  # pri kind tag"), HL_ATTR(HLF_T));
+    msg_puts_attr(_("  # pri kind tag"),  highlight_attr[(int)(HLF_T)] );
     msg_clr_eos();
     taglen_advance(taglen);
-    msg_puts_attr(_("file\n"), HL_ATTR(HLF_T));
+    msg_puts_attr(_("file\n"),  highlight_attr[(int)(HLF_T)] );
 
     for (i = 0; i < num_matches && !got_int; ++i)
     {
@@ -153471,21 +158329,21 @@
         {
             *IObuff = ' ';
         }
-        vim_snprintf((char *)IObuff + 1, IOSIZE - 1, "%2d %s ", i + 1, mt_names[matches[i][0] & MT_MASK]);
+        vim_snprintf((char *)IObuff + 1,  (1024+1)  - 1, "%2d %s ", i + 1, mt_names[matches[i][0] & MT_MASK]);
         msg_puts((char *)IObuff);
         if (tagp.tagkind != NULL)
         {
             msg_outtrans_len(tagp.tagkind, (int)(tagp.tagkind_end - tagp.tagkind));
         }
         msg_advance(13);
-        msg_outtrans_len_attr(tagp.tagname, (int)(tagp.tagname_end - tagp.tagname), HL_ATTR(HLF_T));
+        msg_outtrans_len_attr(tagp.tagname, (int)(tagp.tagname_end - tagp.tagname),  highlight_attr[(int)(HLF_T)] );
         msg_putchar(' ');
         taglen_advance(taglen);
 
         p = tag_full_fname(&tagp);
         if (p != NULL)
         {
-            msg_outtrans_long_attr(p, HL_ATTR(HLF_D));
+            msg_outtrans_long_attr(p,  highlight_attr[(int)(HLF_D)] );
             vim_free(p);
         }
         if (msg_col > 0)
@@ -153509,17 +158367,17 @@
                     ++p;
                 }
 
-                if (STRNCMP(p, "file:", 5) == 0 && vim_isspace(p[5]))
+                if ( strncmp((char *)(p), (char *)("file:"), (5))  == 0 && vim_isspace(p[5]))
                 {
                     p += 5;
                     continue;
                 }
-                if (p == tagp.tagkind || (p + 5 == tagp.tagkind && STRNCMP(p, "kind:", 5) == 0))
+                if (p == tagp.tagkind || (p + 5 == tagp.tagkind &&  strncmp((char *)(p), (char *)("kind:"), (5))  == 0))
                 {
                     p = tagp.tagkind_end;
                     continue;
                 }
-                attr = HL_ATTR(HLF_CM);
+                attr =  highlight_attr[(int)(HLF_CM)] ;
                 while (*p && *p != '\r' && *p != '\n')
                 {
                     if (msg_col + ptr2cells(p) >= cmdline_width)
@@ -153627,7 +158485,8 @@
     void
 tag_freematch(void)
 {
-    VIM_CLEAR(tagmatchname);
+     vim_free(tagmatchname);
+     (tagmatchname) = NULL;
 }
 
     static void
@@ -153645,7 +158504,7 @@
 }
 
     void
-do_tags(exarg_T *eap UNUSED)
+do_tags(exarg_T *eap  __attribute__((unused)) )
 {
     int         i;
     char_u      *name;
@@ -153665,9 +158524,9 @@
             }
 
             msg_putchar('\n');
-            vim_snprintf((char *)IObuff, IOSIZE, "%c%2d %2d %-15s %5ld  ", i == tagstackidx ? '>' : ' ', i + 1, tagstack[i].cur_match + 1, tagstack[i].tagname, tagstack[i].fmark.mark.lnum);
+            vim_snprintf((char *)IObuff,  (1024+1) , "%c%2d %2d %-15s %5ld  ", i == tagstackidx ? '>' : ' ', i + 1, tagstack[i].cur_match + 1, tagstack[i].tagname, tagstack[i].fmark.mark.lnum);
             msg_outtrans(IObuff);
-            msg_outtrans_attr(name, tagstack[i].fmark.fnum == curbuf->b_fnum ? HL_ATTR(HLF_D) : 0);
+            msg_outtrans_attr(name, tagstack[i].fmark.fnum == curbuf->b_fnum ?  highlight_attr[(int)(HLF_D)]  : 0);
             vim_free(name);
         }
         out_flush();
@@ -153685,7 +158544,7 @@
 
     while (len > 0)
     {
-        i = (int)TOUPPER_ASC(*s1) - (int)TOUPPER_ASC(*s2);
+        i = (int) (((*s1) < 'a' || (*s1) > 'z') ? (*s1) : (*s1) - ('a' - 'A'))  - (int) (((*s2) < 'a' || (*s2) > 'z') ? (*s2) : (*s2) - ('a' - 'A')) ;
         if (i != 0)
         {
             return i;
@@ -153782,11 +158641,11 @@
 {
     int         mtt;
 
-    st->tag_fname = alloc(MAXPATHL + 1);
+    st->tag_fname = alloc( PATH_MAX  + 1);
     st->fp = NULL;
-    st->orgpat = ALLOC_ONE(pat_T);
+    st->orgpat =  (pat_T *)alloc(sizeof(pat_T)) ;
     st->orgpat->pat = pat;
-    st->orgpat->len = (int)STRLEN(pat);
+    st->orgpat->len = (int) strlen((char *)(pat)) ;
     st->orgpat->regmatch.regprog = NULL;
     st->flags = flags;
     st->tag_file_sorted = NUL;
@@ -153855,27 +158714,27 @@
     if (st->state == TS_BINARY || st->state == TS_SKIP_BACK)
     {
         sinfo_p->curr_offset_used = sinfo_p->curr_offset;
-        vim_ignored = vim_fseek(st->fp, sinfo_p->curr_offset, SEEK_SET);
+        vim_ignored =  fseeko (st->fp, sinfo_p->curr_offset, SEEK_SET);
         eof = vim_fgets(st->lbuf, st->lbuf_size, st->fp);
         if (!eof && sinfo_p->curr_offset != 0)
         {
-            sinfo_p->curr_offset = vim_ftell(st->fp);
+            sinfo_p->curr_offset =  ftello (st->fp);
             if (sinfo_p->curr_offset == sinfo_p->high_offset)
             {
-                vim_ignored = vim_fseek(st->fp, sinfo_p->low_offset, SEEK_SET);
+                vim_ignored =  fseeko (st->fp, sinfo_p->low_offset, SEEK_SET);
                 sinfo_p->curr_offset = sinfo_p->low_offset;
             }
             eof = vim_fgets(st->lbuf, st->lbuf_size, st->fp);
         }
         while (!eof && vim_isblankline(st->lbuf))
         {
-            sinfo_p->curr_offset = vim_ftell(st->fp);
+            sinfo_p->curr_offset =  ftello (st->fp);
             eof = vim_fgets(st->lbuf, st->lbuf_size, st->fp);
         }
         if (eof)
         {
             st->state = TS_SKIP_BACK;
-            sinfo_p->match_offset = vim_ftell(st->fp);
+            sinfo_p->match_offset =  ftello (st->fp);
             sinfo_p->curr_offset = sinfo_p->curr_offset_used;
             return TAGS_READ_IGNORE;
         }
@@ -153901,16 +158760,16 @@
 {
     char_u      *p;
 
-    if (STRNCMP(st->lbuf, "!_TAG_", 6) != 0)
+    if ( strncmp((char *)(st->lbuf), (char *)("!_TAG_"), (6))  != 0)
     {
         return TRUE;
     }
 
-    if (STRNCMP(st->lbuf, "!_TAG_FILE_SORTED\t", 18) == 0)
+    if ( strncmp((char *)(st->lbuf), (char *)("!_TAG_FILE_SORTED\t"), (18))  == 0)
     {
         st->tag_file_sorted = st->lbuf[18];
     }
-    if (STRNCMP(st->lbuf, "!_TAG_FILE_ENCODING\t", 20) == 0)
+    if ( strncmp((char *)(st->lbuf), (char *)("!_TAG_FILE_ENCODING\t"), (20))  == 0)
     {
         for (p = st->lbuf + 20; *p > ' ' && *p < 127; ++p)
         {
@@ -153929,7 +158788,7 @@
     int         noic = (st->flags & TAG_NOIC);
     off_T       filesize;
 
-    if (STRNCMP(st->lbuf, "!_TAG_", 6) <= 0 || (st->lbuf[0] == '!' && ASCII_ISLOWER(st->lbuf[1])))
+    if ( strncmp((char *)(st->lbuf), (char *)("!_TAG_"), (6))  <= 0 || (st->lbuf[0] == '!' &&  ((unsigned)(st->lbuf[1]) - 'a' < 26) ))
     {
         return findtags_hdr_parse(st);
     }
@@ -153965,14 +158824,14 @@
 
     if (st->state == TS_BINARY)
     {
-        if (vim_fseek(st->fp, 0L, SEEK_END) != 0)
+        if ( fseeko (st->fp, 0L, SEEK_END) != 0)
         {
             st->state = TS_LINEAR;
         }
         else
         {
-            filesize = vim_ftell(st->fp);
-            vim_ignored = vim_fseek(st->fp, 0L, SEEK_SET);
+            filesize =  ftello (st->fp);
+            vim_ignored =  fseeko (st->fp, 0L, SEEK_SET);
 
             sinfo_p->low_offset = 0;
             sinfo_p->low_char = 0;
@@ -153996,7 +158855,7 @@
 
     if (st->orgpat->headlen)
     {
-        CLEAR_FIELD(*tagpp);
+          memset((&(*tagpp)), (0), (sizeof(*tagpp)))  ;
         tagpp->tagname = st->lbuf;
         tagpp->tagname_end = vim_strchr(st->lbuf, TAB);
         if (tagpp->tagname_end == NULL)
@@ -154023,7 +158882,7 @@
             i = (int)tagpp->tagname[0];
             if (margs->sortic)
             {
-                i = (int)TOUPPER_ASC(tagpp->tagname[0]);
+                i = (int) (((tagpp->tagname[0]) < 'a' || (tagpp->tagname[0]) > 'z') ? (tagpp->tagname[0]) : (tagpp->tagname[0]) - ('a' - 'A')) ;
             }
             if (i < sinfo_p->low_char || i > sinfo_p->high_char)
             {
@@ -154036,7 +158895,7 @@
             }
             else
             {
-                tagcmp = STRNCMP(tagpp->tagname, st->orgpat->head, cmplen);
+                tagcmp =  strncmp((char *)(tagpp->tagname), (char *)(st->orgpat->head), (cmplen)) ;
             }
 
             if (tagcmp == 0)
@@ -154059,13 +158918,13 @@
             }
             if (tagcmp < 0)
             {
-                sinfo_p->curr_offset = vim_ftell(st->fp);
+                sinfo_p->curr_offset =  ftello (st->fp);
                 if (sinfo_p->curr_offset < sinfo_p->high_offset)
                 {
                     sinfo_p->low_offset = sinfo_p->curr_offset;
                     if (margs->sortic)
                     {
-                        sinfo_p->low_char = TOUPPER_ASC(tagpp->tagname[0]);
+                        sinfo_p->low_char =  (((tagpp->tagname[0]) < 'a' || (tagpp->tagname[0]) > 'z') ? (tagpp->tagname[0]) : (tagpp->tagname[0]) - ('a' - 'A')) ;
                     }
                     else
                     {
@@ -154079,7 +158938,7 @@
                 sinfo_p->high_offset = sinfo_p->curr_offset;
                 if (margs->sortic)
                 {
-                    sinfo_p->high_char = TOUPPER_ASC(tagpp->tagname[0]);
+                    sinfo_p->high_char =  (((tagpp->tagname[0]) < 'a' || (tagpp->tagname[0]) > 'z') ? (tagpp->tagname[0]) : (tagpp->tagname[0]) - ('a' - 'A')) ;
                 }
                 else
                 {
@@ -154092,7 +158951,7 @@
         }
         else if (st->state == TS_SKIP_BACK)
         {
-            if (MB_STRNICMP(tagpp->tagname, st->orgpat->head, cmplen) != 0)
+            if ( mb_strnicmp((char_u *)(tagpp->tagname), (char_u *)(st->orgpat->head), (int)(cmplen))  != 0)
             {
                 st->state = TS_STEP_FORWARD;
             }
@@ -154104,9 +158963,9 @@
         }
         else if (st->state == TS_STEP_FORWARD)
         {
-            if (MB_STRNICMP(tagpp->tagname, st->orgpat->head, cmplen) != 0)
+            if ( mb_strnicmp((char_u *)(tagpp->tagname), (char_u *)(st->orgpat->head), (int)(cmplen))  != 0)
             {
-                if ((off_T)vim_ftell(st->fp) > sinfo_p->match_offset)
+                if ((off_T) ftello (st->fp) > sinfo_p->match_offset)
                 {
                     return TAG_MATCH_STOP;
                 }
@@ -154118,7 +158977,7 @@
         }
         else
         {
-            if (MB_STRNICMP(tagpp->tagname, st->orgpat->head, cmplen) != 0)
+            if ( mb_strnicmp((char_u *)(tagpp->tagname), (char_u *)(st->orgpat->head), (int)(cmplen))  != 0)
             {
                 return TAG_MATCH_NEXT;
             }
@@ -154180,16 +159039,16 @@
         if (st->orgpat->regmatch.rm_ic)
         {
             match =
-                (MB_STRNICMP(tagpp->tagname, st->orgpat->pat, cmplen) == 0);
+                ( mb_strnicmp((char_u *)(tagpp->tagname), (char_u *)(st->orgpat->pat), (int)(cmplen))  == 0);
             if (match)
             {
                 margs->match_no_ic =
-                    (STRNCMP(tagpp->tagname, st->orgpat->pat, cmplen) == 0);
+                    ( strncmp((char *)(tagpp->tagname), (char *)(st->orgpat->pat), (cmplen))  == 0);
             }
         }
         else
         {
-            match = (STRNCMP(tagpp->tagname, st->orgpat->pat, cmplen) == 0);
+            match = ( strncmp((char *)(tagpp->tagname), (char *)(st->orgpat->pat), (cmplen))  == 0);
         }
     }
 
@@ -154230,7 +159089,7 @@
         return;
     }
 
-    len = (int)STRLEN(conv_line) + 1;
+    len = (int) strlen((char *)(conv_line))  + 1;
     if (len > st->lbuf_size)
     {
         vim_free(st->lbuf);
@@ -154239,7 +159098,7 @@
     }
     else
     {
-        STRCPY(st->lbuf, conv_line);
+         strcpy((char *)(st->lbuf), (char *)(conv_line)) ;
         vim_free(conv_line);
     }
 }
@@ -154294,7 +159153,6 @@
 
     if (st->help_only)
     {
-# define ML_EXTRA 0
         *tagpp->tagname_end = NUL;
         len = (int)(tagpp->tagname_end - tagpp->tagname);
         mfp = alloc(sizeof(char_u) + len + 10 + ML_EXTRA + 1);
@@ -154303,7 +159161,7 @@
             int heuristic;
 
             p = mfp;
-            STRCPY(p, tagpp->tagname);
+             strcpy((char *)(p), (char *)(tagpp->tagname)) ;
 
             heuristic = help_heuristic(tagpp->tagname, margs->match_re ? margs->matchoff : 0, !margs->match_no_ic);
             sprintf((char *)p + len + 1 + ML_EXTRA, "%06d", heuristic);
@@ -154356,18 +159214,18 @@
     }
     else
     {
-        size_t tag_fname_len = STRLEN(st->tag_fname);
+        size_t tag_fname_len =  strlen((char *)(st->tag_fname)) ;
 
-        len = (int)tag_fname_len + (int)STRLEN(st->lbuf) + 3;
+        len = (int)tag_fname_len + (int) strlen((char *)(st->lbuf))  + 3;
         mfp = alloc(sizeof(char_u) + len + 1);
         if (mfp != NULL)
         {
             p = mfp;
             p[0] = mtt + 1;
-            STRCPY(p + 1, st->tag_fname);
+             strcpy((char *)(p + 1), (char *)(st->tag_fname)) ;
             p[tag_fname_len + 1] = TAG_SEP;
             s = p + 1 + tag_fname_len + 1;
-            STRCPY(s, st->lbuf);
+             strcpy((char *)(s), (char *)(st->lbuf)) ;
         }
     }
 
@@ -154377,7 +159235,7 @@
 
             *hash = hash_hash(mfp);
         hi = hash_lookup(&st->ht_match[mtt], mfp, *hash);
-        if (HASHITEM_EMPTY(hi))
+        if ( ((hi)->hi_key == NULL || (hi)->hi_key == &hash_removed) )
         {
             if (hash_add_item(&st->ht_match[mtt], hi, mfp, *hash) == FAIL || ga_grow(&st->ga_match[mtt], 1) == FAIL)
             {
@@ -154406,7 +159264,7 @@
     int                 retval;
     hash_T              hash = 0;
 
-    CLEAR_FIELD(search_info);
+      memset((&(search_info)), (0), (sizeof(search_info)))  ;
 
     for (;;)
     {
@@ -154480,7 +159338,7 @@
 
             if (st->state == TS_STEP_FORWARD || st->state == TS_LINEAR)
             {
-                vim_ignored = vim_fseek(st->fp, search_info.curr_offset, SEEK_SET);
+                vim_ignored =  fseeko (st->fp, search_info.curr_offset, SEEK_SET);
             }
             search_info.curr_offset = 0;
             continue;
@@ -154498,7 +159356,7 @@
         if (retval == TAG_MATCH_FAIL)
         {
             semsg(_(e_format_error_in_tags_file_str), st->tag_fname);
-                semsg(_("Before byte %ld"), (long)vim_ftell(st->fp));
+                semsg(_("Before byte %ld"), (long) ftello (st->fp));
             st->stop_searching = TRUE;
             return;
         }
@@ -154524,8 +159382,7 @@
     findtags_matchargs_init(&margs, st->flags);
 
     {
-
-        st->fp = mch_fopen((char *)st->tag_fname, "r");
+        st->fp =  fopen(((char *)st->tag_fname), ("r")) ;
         if (st->fp == NULL)
         {
             return;
@@ -154577,7 +159434,7 @@
 
     if (st->match_count > 0)
     {
-        matches = ALLOC_MULT(char_u *, st->match_count);
+        matches =  (char_u * *)alloc(sizeof(char_u *) * (st->match_count)) ;
     }
     else
     {
@@ -154733,10 +159590,10 @@
     return retval;
 }
 
-static garray_T tag_fnames = GA_EMPTY;
+static garray_T tag_fnames =  {0, 0, 0, 0, NULL} ;
 
     static void
-found_tagfile_cb(char_u *fname, void *cookie UNUSED)
+found_tagfile_cb(char_u *fname, void *cookie  __attribute__((unused)) )
 {
     if (ga_grow(&tag_fnames, 1) == FAIL)
     {
@@ -154758,7 +159615,7 @@
 
     if (first)
     {
-        CLEAR_POINTER(tnp);
+          memset(((tnp)), (0), (sizeof(*(tnp))))  ;
     }
 
     if (curbuf->b_help)
@@ -154777,13 +159634,13 @@
                 return FAIL;
             }
             ++tnp->tn_hf_idx;
-            vim_strncpy(buf, p_hf, MAXPATHL - STRLEN_LITERAL("tags") - 1);
-            STRCPY(gettail(buf), "tags");
+            vim_strncpy(buf, p_hf,  PATH_MAX  -  (sizeof("tags" "") - 1)  - 1);
+             strcpy((char *)(gettail(buf)), (char *)("tags")) ;
             simplify_filename(buf);
 
             for (i = 0; i < tag_fnames.ga_len; ++i)
             {
-                if (STRCMP(buf, ((char_u **)(tag_fnames.ga_data))[i]) == 0)
+                if ( strcmp((char *)(buf), (char *)(((char_u **)(tag_fnames.ga_data))[i]))  == 0)
                 {
                     return FAIL;
                 }
@@ -154791,7 +159648,7 @@
         }
         else
         {
-            vim_strncpy(buf, ((char_u **)(tag_fnames.ga_data))[tnp->tn_hf_idx++], MAXPATHL - 1);
+            vim_strncpy(buf, ((char_u **)(tag_fnames.ga_data))[tnp->tn_hf_idx++],  PATH_MAX  - 1);
         }
         return OK;
     }
@@ -154830,19 +159687,19 @@
             }
 
             buf[0] = NUL;
-            (void)copy_option_part(&tnp->tn_np, buf, MAXPATHL - 1, " ,");
+            (void)copy_option_part(&tnp->tn_np, buf,  PATH_MAX  - 1, " ,");
 
             r_ptr = vim_findfile_stopdir(buf);
             filename = gettail(buf);
             if (r_ptr != NULL)
             {
-                STRMOVE(r_ptr + 1, r_ptr);
+                  memmove((char *)((r_ptr + 1)), (char *)((r_ptr)),  strlen((char *)(r_ptr))  + 1)  ;
                 ++r_ptr;
             }
-            STRMOVE(filename + 1, filename);
+              memmove((char *)((filename + 1)), (char *)((filename)),  strlen((char *)(filename))  + 1)  ;
             *filename++ = NUL;
 
-            tnp->tn_search_ctx = vim_findfile_init(buf, filename, STRLEN(filename), r_ptr, 100, FALSE, FINDFILE_FILE, tnp->tn_search_ctx, TRUE, curbuf->b_ffname);
+            tnp->tn_search_ctx = vim_findfile_init(buf, filename,  strlen((char *)(filename)) , r_ptr, 100, FALSE, FINDFILE_FILE, tnp->tn_search_ctx, TRUE, curbuf->b_ffname);
             if (tnp->tn_search_ctx != NULL)
             {
                 tnp->tn_did_filefind_init = TRUE;
@@ -154850,7 +159707,7 @@
         }
     }
 
-    STRCPY(buf, fname);
+     strcpy((char *)(buf), (char *)(fname)) ;
     vim_free(fname);
     return OK;
 }
@@ -154911,7 +159768,7 @@
     while ((p = vim_strchr(p, '\t')) != NULL)
     {
         ++p;
-        if (STRNCMP(p, "file:", 5) == 0)
+        if ( strncmp((char *)(p), (char *)("file:"), (5))  == 0)
         {
             return TRUE;
         }
@@ -154925,8 +159782,8 @@
 {
     char_u      *p = lbuf + 1;
 
-    p += STRLEN(p) + 1;
-    return (p - lbuf) + STRLEN(p);
+    p +=  strlen((char *)(p))  + 1;
+    return (p - lbuf) +  strlen((char *)(p)) ;
 }
 
     static int
@@ -154938,7 +159795,7 @@
     char_u *pt;
 
     tagp->tag_fname = lbuf + 1;
-    lbuf += STRLEN(tagp->tag_fname) + 2;
+    lbuf +=  strlen((char *)(tagp->tag_fname))  + 2;
 
     retval = parse_tag_line(lbuf, tagp);
 
@@ -154966,17 +159823,17 @@
         p += 2;
         if (*p++ == TAB)
         {
-            while (ASCII_ISALPHA(*p) || mb_ptr2len(p) > 1)
+            while ( ( ((unsigned)(*p) - 'A' < 26)  ||  ((unsigned)(*p) - 'a' < 26) )  || mb_ptr2len(p) > 1)
             {
-                if (STRNCMP(p, "kind:", 5) == 0)
+                if ( strncmp((char *)(p), (char *)("kind:"), (5))  == 0)
                 {
                     tagp->tagkind = p + 5;
                 }
-                else if (STRNCMP(p, "user_data:", 10) == 0)
+                else if ( strncmp((char *)(p), (char *)("user_data:"), (10))  == 0)
                 {
                     tagp->user_data = p + 10;
                 }
-                else if (STRNCMP(p, "line:", 5) == 0)
+                else if ( strncmp((char *)(p), (char *)("line:"), (5))  == 0)
                 {
                     tagp->tagline = atoi((char *)p + 5);
                 }
@@ -154995,13 +159852,13 @@
                     break;
                 }
                 p = pt;
-                MB_PTR_ADV(p);
+                 p += (*mb_ptr2len)(p) ;
             }
         }
     }
     if (tagp->tagkind != NULL)
     {
-        for (p = tagp->tagkind; *p && *p != '\t' && *p != '\r' && *p != '\n'; MB_PTR_ADV(p))
+        for (p = tagp->tagkind; *p && *p != '\t' && *p != '\r' && *p != '\n';  p += (*mb_ptr2len)(p) )
         {
             ;
         }
@@ -155009,7 +159866,7 @@
     }
     if (tagp->user_data != NULL)
     {
-        for (p = tagp->user_data; *p && *p != '\t' && *p != '\r' && *p != '\n'; MB_PTR_ADV(p))
+        for (p = tagp->user_data; *p && *p != '\t' && *p != '\r' && *p != '\n';  p += (*mb_ptr2len)(p) )
         {
             ;
         }
@@ -155067,7 +159924,7 @@
     lbuf = alloc(len);
     if (lbuf != NULL)
     {
-        mch_memmove(lbuf, lbuf_arg, len);
+         memmove((char *)(lbuf), (char *)(lbuf_arg), len) ;
     }
 
     pbuf = alloc_clear(LSIZE);
@@ -155082,7 +159939,7 @@
     fname = tagp.fname;
 
     str = tagp.command;
-    if (VIM_ISDIGIT(*str))
+    if ( ((unsigned)(*str) - '0' < 10) )
     {
         isdigit = TRUE;
         pbuf[0] = ':';
@@ -155149,7 +160006,7 @@
     }
     keep_help_flag = FALSE;
 
-    if (GETFILE_SUCCESS(getfile_result))
+    if ( ((getfile_result) <= 0) )
     {
         curwin->w_set_curswant = true;
         postponed_split = 0;
@@ -155267,7 +160124,7 @@
             set_no_hlsearch(save_no_hlsearch);
         }
 
-        if (getfile_result == GETFILE_OPEN_OTHER)
+        if (getfile_result ==  (-1) )
         {
             retval = OK;
         }
@@ -155336,11 +160193,11 @@
 
     if ((p_tr || curbuf->b_help) && !vim_isAbsName(fname) && (p = gettail(tag_fname)) != tag_fname)
     {
-        retval = alloc(MAXPATHL);
+        retval = alloc( PATH_MAX );
         if (retval != NULL)
         {
-            STRCPY(retval, tag_fname);
-            vim_strncpy(retval + (p - tag_fname), fname, MAXPATHL - (p - tag_fname) - 1);
+             strcpy((char *)(retval), (char *)(tag_fname)) ;
+            vim_strncpy(retval + (p - tag_fname), fname,  PATH_MAX  - (p - tag_fname) - 1);
             simplify_filename(retval);
         }
     }
@@ -155387,7 +160244,7 @@
 
     for (;;)
     {
-        if (VIM_ISDIGIT(*str))
+        if ( ((unsigned)(*str) - '0' < 10) )
         {
             str = skipdigits(str + 1);
         }
@@ -155413,7 +160270,7 @@
             }
 
         }
-        if (str == NULL || *str != ';' || !(VIM_ISDIGIT(str[1]) || str[1] == '/' || str[1] == '?'))
+        if (str == NULL || *str != ';' || !( ((unsigned)(str[1]) - '0' < 10)  || str[1] == '/' || str[1] == '?'))
         {
             break;
         }
@@ -155421,7 +160278,7 @@
         first_char = *str;
     }
 
-    if (str != NULL && STRNCMP(str, ";\"", 2) == 0)
+    if (str != NULL &&  strncmp((char *)(str), (char *)(";\""), (2))  == 0)
     {
         *pp = str;
         return OK;
@@ -155432,8 +160289,10 @@
     void
 tagstack_clear_entry(taggy_T *item)
 {
-    VIM_CLEAR(item->tagname);
-    VIM_CLEAR(item->user_data);
+     vim_free(item->tagname);
+     (item->tagname) = NULL;
+     vim_free(item->user_data);
+     (item->user_data) = NULL;
 }
 
     int
@@ -155481,7 +160340,7 @@
                  char_u *buf;
 
                  name_buf_size = len + 3;
-                 buf = vim_realloc(name_buf, name_buf_size);
+                 buf =  realloc((name_buf), (name_buf_size)) ;
                  if (buf == NULL)
                  {
                      vim_free(name_buf);
@@ -155490,14 +160349,14 @@
                  name_buf = buf;
              }
 
-             mch_memmove(name_buf, t_p.tagname, len);
+              memmove((char *)(name_buf), (char *)(t_p.tagname), len) ;
              name_buf[len++] = 0;
              name_buf[len++] = (t_p.tagkind != NULL && *t_p.tagkind)
                                                           ? *t_p.tagkind : 'f';
              name_buf[len++] = 0;
-             mch_memmove((*file)[i] + len, t_p.fname, t_p.fname_end - t_p.fname);
+              memmove((char *)((*file)[i] + len), (char *)(t_p.fname), t_p.fname_end - t_p.fname) ;
              (*file)[i][len + (t_p.fname_end - t_p.fname)] = 0;
-             mch_memmove((*file)[i], name_buf, len);
+              memmove((char *)((*file)[i]), (char *)(name_buf), len) ;
         }
     }
 
@@ -155507,9 +160366,7 @@
 
 // ==================== term.c ====================
 
-#define tgetstr tgetstr_defined_wrong
-
-#define BT_EXTRA_KEYS   0x101
+enum { BT_EXTRA_KEYS = 0x101 };
 
 static void parse_builtin_tcap(char_u *s);
 static void gather_termleader(void);
@@ -155519,9 +160376,6 @@
 static int term_7to8bit(char_u *p);
 static void accept_modifiers_for_function_keys(void);
 
-# define LOG_TR1(str) do {   } while (0)
-# define LOG_TRN(fmt,...) do {   } while (0)
-
 typedef enum {
     STATUS_GET,
     STATUS_SENT,
@@ -155543,13 +160397,11 @@
     elapsed_T       start_tv;
 } oscstate_T;
 
-#define TERMREQUEST_INIT {STATUS_GET, -1}
-
-static termrequest_T crv_status = TERMREQUEST_INIT;
+static termrequest_T crv_status =  {STATUS_GET, -1} ;
 
-static termrequest_T u7_status = TERMREQUEST_INIT;
+static termrequest_T u7_status =  {STATUS_GET, -1} ;
 
-static termrequest_T xcc_status = TERMREQUEST_INIT;
+static termrequest_T xcc_status =  {STATUS_GET, -1} ;
 
 static int  detected_8bit = FALSE;
 
@@ -155617,52 +160469,52 @@
     {(int)KS_LE,        "\b"},
     {(int)KS_CM,        "\033[%i%d;%dH"},
     {(int)KS_CRI,       "\033[%dC"},
-    {K_UP,              "\033[A"},
-    {K_DOWN,            "\033[B"},
-    {K_RIGHT,           "\033[C"},
-    {K_LEFT,            "\033[D"},
-    {K_F1,              "\033[11~"},
-    {K_F2,              "\033[12~"},
-    {K_F3,              "\033[13~"},
-    {K_F4,              "\033[14~"},
-    {K_F5,              "\033[15~"},
-    {K_F6,              "\033[17~"},
-    {K_F7,              "\033[18~"},
-    {K_F8,              "\033[19~"},
-    {K_F9,              "\033[20~"},
-    {K_F10,             "\033[21~"},
-    {K_F11,             "\033[23~"},
-    {K_F12,             "\033[24~"},
-    {K_F13,             "\033[25~"},
-    {K_F14,             "\033[26~"},
-    {K_F15,             "\033[28~"},
-    {K_F16,             "\033[29~"},
-    {K_F17,             "\033[31~"},
-    {K_F18,             "\033[32~"},
-    {K_F19,             "\033[33~"},
-    {K_F20,             "\033[34~"},
-    {K_INS,             "\033[2~"},
-    {K_DEL,             "\033[3~"},
-    {K_HOME,            "\033[1~"},
-    {K_END,             "\033[4~"},
-    {K_PAGEUP,          "\033[5~"},
-    {K_PAGEDOWN,        "\033[6~"},
-    {K_KPLUS,           "\033Ok"},
-    {K_KMINUS,          "\033Om"},
-    {K_KDIVIDE,         "\033Oo"},
-    {K_KMULTIPLY,       "\033Oj"},
-    {K_KENTER,          "\033OM"},
-    {K_K0,              "\033Op"},
-    {K_K1,              "\033Oq"},
-    {K_K2,              "\033Or"},
-    {K_K3,              "\033Os"},
-    {K_K4,              "\033Ot"},
-    {K_K5,              "\033Ou"},
-    {K_K6,              "\033Ov"},
-    {K_K7,              "\033Ow"},
-    {K_K8,              "\033Ox"},
-    {K_K9,              "\033Oy"},
-    {K_BS,              "\x7f"},
+    {  (-(('k') + ((int)('u') << 8)))  ,              "\033[A"},
+    {  (-(('k') + ((int)('d') << 8)))  ,            "\033[B"},
+    {  (-(('k') + ((int)('r') << 8)))  ,           "\033[C"},
+    {  (-(('k') + ((int)('l') << 8)))  ,            "\033[D"},
+    {  (-(('k') + ((int)('1') << 8)))  ,              "\033[11~"},
+    {  (-(('k') + ((int)('2') << 8)))  ,              "\033[12~"},
+    {  (-(('k') + ((int)('3') << 8)))  ,              "\033[13~"},
+    {  (-(('k') + ((int)('4') << 8)))  ,              "\033[14~"},
+    {  (-(('k') + ((int)('5') << 8)))  ,              "\033[15~"},
+    {  (-(('k') + ((int)('6') << 8)))  ,              "\033[17~"},
+    {  (-(('k') + ((int)('7') << 8)))  ,              "\033[18~"},
+    {  (-(('k') + ((int)('8') << 8)))  ,              "\033[19~"},
+    {  (-(('k') + ((int)('9') << 8)))  ,              "\033[20~"},
+    {  (-(('k') + ((int)(';') << 8)))  ,             "\033[21~"},
+    {  (-(('F') + ((int)('1') << 8)))  ,             "\033[23~"},
+    {  (-(('F') + ((int)('2') << 8)))  ,             "\033[24~"},
+    {  (-(('F') + ((int)('3') << 8)))  ,             "\033[25~"},
+    {  (-(('F') + ((int)('4') << 8)))  ,             "\033[26~"},
+    {  (-(('F') + ((int)('5') << 8)))  ,             "\033[28~"},
+    {  (-(('F') + ((int)('6') << 8)))  ,             "\033[29~"},
+    {  (-(('F') + ((int)('7') << 8)))  ,             "\033[31~"},
+    {  (-(('F') + ((int)('8') << 8)))  ,             "\033[32~"},
+    {  (-(('F') + ((int)('9') << 8)))  ,             "\033[33~"},
+    {  (-(('F') + ((int)('A') << 8)))  ,             "\033[34~"},
+    {  (-(('k') + ((int)('I') << 8)))  ,             "\033[2~"},
+    {  (-(('k') + ((int)('D') << 8)))  ,             "\033[3~"},
+    {  (-(('k') + ((int)('h') << 8)))  ,            "\033[1~"},
+    {  (-(('@') + ((int)('7') << 8)))  ,             "\033[4~"},
+    {  (-(('k') + ((int)('P') << 8)))  ,          "\033[5~"},
+    {  (-(('k') + ((int)('N') << 8)))  ,        "\033[6~"},
+    {  (-(('K') + ((int)('6') << 8)))  ,           "\033Ok"},
+    {  (-(('K') + ((int)('7') << 8)))  ,          "\033Om"},
+    {  (-(('K') + ((int)('8') << 8)))  ,         "\033Oo"},
+    {  (-(('K') + ((int)('9') << 8)))  ,       "\033Oj"},
+    {  (-(('K') + ((int)('A') << 8)))  ,          "\033OM"},
+    {  (-(('K') + ((int)('C') << 8)))  ,              "\033Op"},
+    {  (-(('K') + ((int)('D') << 8)))  ,              "\033Oq"},
+    {  (-(('K') + ((int)('E') << 8)))  ,              "\033Or"},
+    {  (-(('K') + ((int)('F') << 8)))  ,              "\033Os"},
+    {  (-(('K') + ((int)('G') << 8)))  ,              "\033Ot"},
+    {  (-(('K') + ((int)('H') << 8)))  ,              "\033Ou"},
+    {  (-(('K') + ((int)('I') << 8)))  ,              "\033Ov"},
+    {  (-(('K') + ((int)('J') << 8)))  ,              "\033Ow"},
+    {  (-(('K') + ((int)('K') << 8)))  ,              "\033Ox"},
+    {  (-(('K') + ((int)('L') << 8)))  ,              "\033Oy"},
+    {  (-(('k') + ((int)('b') << 8)))  ,              "\x7f"},
 
     {(int)KS_NAME,      NULL}
 };
@@ -155721,63 +160573,63 @@
     {(int)KS_FD,        "\033[?1004l"},
     {(int)KS_FE,        "\033[?1004h"},
 
-    {K_UP,              "\033O*A"},
-    {K_DOWN,            "\033O*B"},
-    {K_RIGHT,           "\033O*C"},
-    {K_LEFT,            "\033O*D"},
-    {K_XUP,             "\033[@;*A"},
-    {K_XDOWN,           "\033[@;*B"},
-    {K_XRIGHT,          "\033[@;*C"},
-    {K_XLEFT,           "\033[@;*D"},
-    {K_XF1,             "\033O*P"},
-    {K_XF2,             "\033O*Q"},
-    {K_XF3,             "\033O*R"},
-    {K_XF4,             "\033O*S"},
-    {K_F1,              "\033[11;*~"},
-    {K_F2,              "\033[12;*~"},
-    {K_F3,              "\033[13;*~"},
-    {K_F4,              "\033[14;*~"},
-    {K_F5,              "\033[15;*~"},
-    {K_F6,              "\033[17;*~"},
-    {K_F7,              "\033[18;*~"},
-    {K_F8,              "\033[19;*~"},
-    {K_F9,              "\033[20;*~"},
-    {K_F10,             "\033[21;*~"},
-    {K_F11,             "\033[23;*~"},
-    {K_F12,             "\033[24;*~"},
-    {K_S_TAB,           "\033[Z"},
-    {K_HELP,            "\033[28;*~"},
-    {K_UNDO,            "\033[26;*~"},
-    {K_INS,             "\033[2;*~"},
-    {K_HOME,            "\033[@;*H"},
-    {K_KHOME,           "\033[1;*~"},
-    {K_XHOME,           "\033O*H"},
-    {K_ZHOME,           "\033[7;*~"},
-    {K_END,             "\033[@;*F"},
-    {K_KEND,            "\033[4;*~"},
-    {K_XEND,            "\033O*F"},
-    {K_ZEND,            "\033[8;*~"},
-    {K_PAGEUP,          "\033[5;*~"},
-    {K_PAGEDOWN,        "\033[6;*~"},
-    {K_KPLUS,           "\033O*k"},
-    {K_KMINUS,          "\033O*m"},
-    {K_KDIVIDE,         "\033O*o"},
-    {K_KMULTIPLY,       "\033O*j"},
-    {K_KENTER,          "\033O*M"},
-    {K_KPOINT,          "\033O*n"},
-    {K_K0,              "\033O*p"},
-    {K_K1,              "\033O*q"},
-    {K_K2,              "\033O*r"},
-    {K_K3,              "\033O*s"},
-    {K_K4,              "\033O*t"},
-    {K_K5,              "\033O*u"},
-    {K_K6,              "\033O*v"},
-    {K_K7,              "\033O*w"},
-    {K_K8,              "\033O*x"},
-    {K_K9,              "\033O*y"},
-    {K_KDEL,            "\033[3;*~"},
-    {K_PS,              "\033[200~"},
-    {K_PE,              "\033[201~"},
+    {  (-(('k') + ((int)('u') << 8)))  ,              "\033O*A"},
+    {  (-(('k') + ((int)('d') << 8)))  ,            "\033O*B"},
+    {  (-(('k') + ((int)('r') << 8)))  ,           "\033O*C"},
+    {  (-(('k') + ((int)('l') << 8)))  ,            "\033O*D"},
+    {  (-((KS_EXTRA) + ((int)(KE_XUP) << 8)))  ,             "\033[@;*A"},
+    {  (-((KS_EXTRA) + ((int)(KE_XDOWN) << 8)))  ,           "\033[@;*B"},
+    {  (-((KS_EXTRA) + ((int)(KE_XRIGHT) << 8)))  ,          "\033[@;*C"},
+    {  (-((KS_EXTRA) + ((int)(KE_XLEFT) << 8)))  ,           "\033[@;*D"},
+    {  (-((KS_EXTRA) + ((int)(KE_XF1) << 8)))  ,             "\033O*P"},
+    {  (-((KS_EXTRA) + ((int)(KE_XF2) << 8)))  ,             "\033O*Q"},
+    {  (-((KS_EXTRA) + ((int)(KE_XF3) << 8)))  ,             "\033O*R"},
+    {  (-((KS_EXTRA) + ((int)(KE_XF4) << 8)))  ,             "\033O*S"},
+    {  (-(('k') + ((int)('1') << 8)))  ,              "\033[11;*~"},
+    {  (-(('k') + ((int)('2') << 8)))  ,              "\033[12;*~"},
+    {  (-(('k') + ((int)('3') << 8)))  ,              "\033[13;*~"},
+    {  (-(('k') + ((int)('4') << 8)))  ,              "\033[14;*~"},
+    {  (-(('k') + ((int)('5') << 8)))  ,              "\033[15;*~"},
+    {  (-(('k') + ((int)('6') << 8)))  ,              "\033[17;*~"},
+    {  (-(('k') + ((int)('7') << 8)))  ,              "\033[18;*~"},
+    {  (-(('k') + ((int)('8') << 8)))  ,              "\033[19;*~"},
+    {  (-(('k') + ((int)('9') << 8)))  ,              "\033[20;*~"},
+    {  (-(('k') + ((int)(';') << 8)))  ,             "\033[21;*~"},
+    {  (-(('F') + ((int)('1') << 8)))  ,             "\033[23;*~"},
+    {  (-(('F') + ((int)('2') << 8)))  ,             "\033[24;*~"},
+    {  (-(('k') + ((int)('B') << 8)))  ,           "\033[Z"},
+    {  (-(('%') + ((int)('1') << 8)))  ,            "\033[28;*~"},
+    {  (-(('&') + ((int)('8') << 8)))  ,            "\033[26;*~"},
+    {  (-(('k') + ((int)('I') << 8)))  ,             "\033[2;*~"},
+    {  (-(('k') + ((int)('h') << 8)))  ,            "\033[@;*H"},
+    {  (-(('K') + ((int)('1') << 8)))  ,           "\033[1;*~"},
+    {  (-((KS_EXTRA) + ((int)(KE_XHOME) << 8)))  ,           "\033O*H"},
+    {  (-((KS_EXTRA) + ((int)(KE_ZHOME) << 8)))  ,           "\033[7;*~"},
+    {  (-(('@') + ((int)('7') << 8)))  ,             "\033[@;*F"},
+    {  (-(('K') + ((int)('4') << 8)))  ,            "\033[4;*~"},
+    {  (-((KS_EXTRA) + ((int)(KE_XEND) << 8)))  ,            "\033O*F"},
+    {  (-((KS_EXTRA) + ((int)(KE_ZEND) << 8)))  ,            "\033[8;*~"},
+    {  (-(('k') + ((int)('P') << 8)))  ,          "\033[5;*~"},
+    {  (-(('k') + ((int)('N') << 8)))  ,        "\033[6;*~"},
+    {  (-(('K') + ((int)('6') << 8)))  ,           "\033O*k"},
+    {  (-(('K') + ((int)('7') << 8)))  ,          "\033O*m"},
+    {  (-(('K') + ((int)('8') << 8)))  ,         "\033O*o"},
+    {  (-(('K') + ((int)('9') << 8)))  ,       "\033O*j"},
+    {  (-(('K') + ((int)('A') << 8)))  ,          "\033O*M"},
+    {  (-(('K') + ((int)('B') << 8)))  ,          "\033O*n"},
+    {  (-(('K') + ((int)('C') << 8)))  ,              "\033O*p"},
+    {  (-(('K') + ((int)('D') << 8)))  ,              "\033O*q"},
+    {  (-(('K') + ((int)('E') << 8)))  ,              "\033O*r"},
+    {  (-(('K') + ((int)('F') << 8)))  ,              "\033O*s"},
+    {  (-(('K') + ((int)('G') << 8)))  ,              "\033O*t"},
+    {  (-(('K') + ((int)('H') << 8)))  ,              "\033O*u"},
+    {  (-(('K') + ((int)('I') << 8)))  ,              "\033O*v"},
+    {  (-(('K') + ((int)('J') << 8)))  ,              "\033O*w"},
+    {  (-(('K') + ((int)('K') << 8)))  ,              "\033O*x"},
+    {  (-(('K') + ((int)('L') << 8)))  ,              "\033O*y"},
+    {  (-((KS_EXTRA) + ((int)(KE_KDEL) << 8)))  ,            "\033[3;*~"},
+    {  (-(('P') + ((int)('S') << 8)))  ,              "\033[200~"},
+    {  (-(('P') + ((int)('E') << 8)))  ,              "\033[201~"},
 
     {(int)KS_CCO,       "8"},
     {(int)KS_CAB,       "\033[4%dm"},
@@ -155785,32 +160637,32 @@
     {(int)KS_OP,        "\033[0m"},
 
     {BT_EXTRA_KEYS,   ""},
-    {TERMCAP2KEY('k', '0'), "\033[10;*~"},
-    {TERMCAP2KEY('F', '3'), "\033[25;*~"},
-    {TERMCAP2KEY('F', '6'), "\033[29;*~"},
-    {TERMCAP2KEY('F', '7'), "\033[31;*~"},
-    {TERMCAP2KEY('F', '8'), "\033[32;*~"},
-    {TERMCAP2KEY('F', '9'), "\033[33;*~"},
-    {TERMCAP2KEY('F', 'A'), "\033[34;*~"},
-
-    {TERMCAP2KEY('F', 'B'), "\033[42;*~"},
-    {TERMCAP2KEY('F', 'C'), "\033[43;*~"},
-    {TERMCAP2KEY('F', 'D'), "\033[44;*~"},
-    {TERMCAP2KEY('F', 'E'), "\033[45;*~"},
-    {TERMCAP2KEY('F', 'F'), "\033[46;*~"},
-    {TERMCAP2KEY('F', 'G'), "\033[47;*~"},
-    {TERMCAP2KEY('F', 'H'), "\033[48;*~"},
-    {TERMCAP2KEY('F', 'I'), "\033[49;*~"},
-    {TERMCAP2KEY('F', 'J'), "\033[50;*~"},
-    {TERMCAP2KEY('F', 'K'), "\033[51;*~"},
-
-    {TERMCAP2KEY('F', 'L'), "\033[52;*~"},
-    {TERMCAP2KEY('F', 'M'), "\033[53;*~"},
-    {TERMCAP2KEY('F', 'N'), "\033[54;*~"},
-    {TERMCAP2KEY('F', 'O'), "\033[55;*~"},
-    {TERMCAP2KEY('F', 'P'), "\033[56;*~"},
-    {TERMCAP2KEY('F', 'Q'), "\033[57;*~"},
-    {TERMCAP2KEY('F', 'R'), "\033[58;*~"},
+    { (-(('k') + ((int)('0') << 8))) , "\033[10;*~"},
+    { (-(('F') + ((int)('3') << 8))) , "\033[25;*~"},
+    { (-(('F') + ((int)('6') << 8))) , "\033[29;*~"},
+    { (-(('F') + ((int)('7') << 8))) , "\033[31;*~"},
+    { (-(('F') + ((int)('8') << 8))) , "\033[32;*~"},
+    { (-(('F') + ((int)('9') << 8))) , "\033[33;*~"},
+    { (-(('F') + ((int)('A') << 8))) , "\033[34;*~"},
+
+    { (-(('F') + ((int)('B') << 8))) , "\033[42;*~"},
+    { (-(('F') + ((int)('C') << 8))) , "\033[43;*~"},
+    { (-(('F') + ((int)('D') << 8))) , "\033[44;*~"},
+    { (-(('F') + ((int)('E') << 8))) , "\033[45;*~"},
+    { (-(('F') + ((int)('F') << 8))) , "\033[46;*~"},
+    { (-(('F') + ((int)('G') << 8))) , "\033[47;*~"},
+    { (-(('F') + ((int)('H') << 8))) , "\033[48;*~"},
+    { (-(('F') + ((int)('I') << 8))) , "\033[49;*~"},
+    { (-(('F') + ((int)('J') << 8))) , "\033[50;*~"},
+    { (-(('F') + ((int)('K') << 8))) , "\033[51;*~"},
+
+    { (-(('F') + ((int)('L') << 8))) , "\033[52;*~"},
+    { (-(('F') + ((int)('M') << 8))) , "\033[53;*~"},
+    { (-(('F') + ((int)('N') << 8))) , "\033[54;*~"},
+    { (-(('F') + ((int)('O') << 8))) , "\033[55;*~"},
+    { (-(('F') + ((int)('P') << 8))) , "\033[56;*~"},
+    { (-(('F') + ((int)('Q') << 8))) , "\033[57;*~"},
+    { (-(('F') + ((int)('R') << 8))) , "\033[58;*~"},
 
     {(int)KS_NAME,      NULL}
 };
@@ -155906,94 +160758,94 @@
     {(int)KS_RFG,       "[RFG]"},
     {(int)KS_RBG,       "[RBG]"},
     {(int)KS_CF,        "[CF%d]"},
-    {K_UP,              "[KU]"},
-    {K_DOWN,            "[KD]"},
-    {K_LEFT,            "[KL]"},
-    {K_RIGHT,           "[KR]"},
-    {K_XUP,             "[xKU]"},
-    {K_XDOWN,           "[xKD]"},
-    {K_XLEFT,           "[xKL]"},
-    {K_XRIGHT,          "[xKR]"},
-    {K_S_UP,            "[S-KU]"},
-    {K_S_DOWN,          "[S-KD]"},
-    {K_S_LEFT,          "[S-KL]"},
-    {K_C_LEFT,          "[C-KL]"},
-    {K_S_RIGHT,         "[S-KR]"},
-    {K_C_RIGHT,         "[C-KR]"},
-    {K_F1,              "[F1]"},
-    {K_XF1,             "[xF1]"},
-    {K_F2,              "[F2]"},
-    {K_XF2,             "[xF2]"},
-    {K_F3,              "[F3]"},
-    {K_XF3,             "[xF3]"},
-    {K_F4,              "[F4]"},
-    {K_XF4,             "[xF4]"},
-    {K_F5,              "[F5]"},
-    {K_F6,              "[F6]"},
-    {K_F7,              "[F7]"},
-    {K_F8,              "[F8]"},
-    {K_F9,              "[F9]"},
-    {K_F10,             "[F10]"},
-    {K_F11,             "[F11]"},
-    {K_F12,             "[F12]"},
-    {K_S_F1,            "[S-F1]"},
-    {K_S_XF1,           "[S-xF1]"},
-    {K_S_F2,            "[S-F2]"},
-    {K_S_XF2,           "[S-xF2]"},
-    {K_S_F3,            "[S-F3]"},
-    {K_S_XF3,           "[S-xF3]"},
-    {K_S_F4,            "[S-F4]"},
-    {K_S_XF4,           "[S-xF4]"},
-    {K_S_F5,            "[S-F5]"},
-    {K_S_F6,            "[S-F6]"},
-    {K_S_F7,            "[S-F7]"},
-    {K_S_F8,            "[S-F8]"},
-    {K_S_F9,            "[S-F9]"},
-    {K_S_F10,           "[S-F10]"},
-    {K_S_F11,           "[S-F11]"},
-    {K_S_F12,           "[S-F12]"},
-    {K_HELP,            "[HELP]"},
-    {K_UNDO,            "[UNDO]"},
-    {K_BS,              "[BS]"},
-    {K_INS,             "[INS]"},
-    {K_KINS,            "[KINS]"},
-    {K_DEL,             "[DEL]"},
-    {K_KDEL,            "[KDEL]"},
-    {K_HOME,            "[HOME]"},
-    {K_S_HOME,          "[C-HOME]"},
-    {K_C_HOME,          "[C-HOME]"},
-    {K_KHOME,           "[KHOME]"},
-    {K_XHOME,           "[XHOME]"},
-    {K_ZHOME,           "[ZHOME]"},
-    {K_END,             "[END]"},
-    {K_S_END,           "[C-END]"},
-    {K_C_END,           "[C-END]"},
-    {K_KEND,            "[KEND]"},
-    {K_XEND,            "[XEND]"},
-    {K_ZEND,            "[ZEND]"},
-    {K_PAGEUP,          "[PAGEUP]"},
-    {K_PAGEDOWN,        "[PAGEDOWN]"},
-    {K_KPAGEUP,         "[KPAGEUP]"},
-    {K_KPAGEDOWN,       "[KPAGEDOWN]"},
-    {K_MOUSE,           "[MOUSE]"},
-    {K_KPLUS,           "[KPLUS]"},
-    {K_KMINUS,          "[KMINUS]"},
-    {K_KDIVIDE,         "[KDIVIDE]"},
-    {K_KMULTIPLY,       "[KMULTIPLY]"},
-    {K_KENTER,          "[KENTER]"},
-    {K_KPOINT,          "[KPOINT]"},
-    {K_PS,              "[PASTE-START]"},
-    {K_PE,              "[PASTE-END]"},
-    {K_K0,              "[K0]"},
-    {K_K1,              "[K1]"},
-    {K_K2,              "[K2]"},
-    {K_K3,              "[K3]"},
-    {K_K4,              "[K4]"},
-    {K_K5,              "[K5]"},
-    {K_K6,              "[K6]"},
-    {K_K7,              "[K7]"},
-    {K_K8,              "[K8]"},
-    {K_K9,              "[K9]"},
+    {  (-(('k') + ((int)('u') << 8)))  ,              "[KU]"},
+    {  (-(('k') + ((int)('d') << 8)))  ,            "[KD]"},
+    {  (-(('k') + ((int)('l') << 8)))  ,            "[KL]"},
+    {  (-(('k') + ((int)('r') << 8)))  ,           "[KR]"},
+    {  (-((KS_EXTRA) + ((int)(KE_XUP) << 8)))  ,             "[xKU]"},
+    {  (-((KS_EXTRA) + ((int)(KE_XDOWN) << 8)))  ,           "[xKD]"},
+    {  (-((KS_EXTRA) + ((int)(KE_XLEFT) << 8)))  ,           "[xKL]"},
+    {  (-((KS_EXTRA) + ((int)(KE_XRIGHT) << 8)))  ,          "[xKR]"},
+    {  (-((KS_EXTRA) + ((int)(KE_S_UP) << 8)))  ,            "[S-KU]"},
+    {  (-((KS_EXTRA) + ((int)(KE_S_DOWN) << 8)))  ,          "[S-KD]"},
+    {  (-(('#') + ((int)('4') << 8)))  ,          "[S-KL]"},
+    {  (-((KS_EXTRA) + ((int)(KE_C_LEFT) << 8)))  ,          "[C-KL]"},
+    {  (-(('%') + ((int)('i') << 8)))  ,         "[S-KR]"},
+    {  (-((KS_EXTRA) + ((int)(KE_C_RIGHT) << 8)))  ,         "[C-KR]"},
+    {  (-(('k') + ((int)('1') << 8)))  ,              "[F1]"},
+    {  (-((KS_EXTRA) + ((int)(KE_XF1) << 8)))  ,             "[xF1]"},
+    {  (-(('k') + ((int)('2') << 8)))  ,              "[F2]"},
+    {  (-((KS_EXTRA) + ((int)(KE_XF2) << 8)))  ,             "[xF2]"},
+    {  (-(('k') + ((int)('3') << 8)))  ,              "[F3]"},
+    {  (-((KS_EXTRA) + ((int)(KE_XF3) << 8)))  ,             "[xF3]"},
+    {  (-(('k') + ((int)('4') << 8)))  ,              "[F4]"},
+    {  (-((KS_EXTRA) + ((int)(KE_XF4) << 8)))  ,             "[xF4]"},
+    {  (-(('k') + ((int)('5') << 8)))  ,              "[F5]"},
+    {  (-(('k') + ((int)('6') << 8)))  ,              "[F6]"},
+    {  (-(('k') + ((int)('7') << 8)))  ,              "[F7]"},
+    {  (-(('k') + ((int)('8') << 8)))  ,              "[F8]"},
+    {  (-(('k') + ((int)('9') << 8)))  ,              "[F9]"},
+    {  (-(('k') + ((int)(';') << 8)))  ,             "[F10]"},
+    {  (-(('F') + ((int)('1') << 8)))  ,             "[F11]"},
+    {  (-(('F') + ((int)('2') << 8)))  ,             "[F12]"},
+    {  (-((KS_EXTRA) + ((int)(KE_S_F1) << 8)))  ,            "[S-F1]"},
+    {  (-((KS_EXTRA) + ((int)(KE_S_XF1) << 8)))  ,           "[S-xF1]"},
+    {  (-((KS_EXTRA) + ((int)(KE_S_F2) << 8)))  ,            "[S-F2]"},
+    {  (-((KS_EXTRA) + ((int)(KE_S_XF2) << 8)))  ,           "[S-xF2]"},
+    {  (-((KS_EXTRA) + ((int)(KE_S_F3) << 8)))  ,            "[S-F3]"},
+    {  (-((KS_EXTRA) + ((int)(KE_S_XF3) << 8)))  ,           "[S-xF3]"},
+    {  (-((KS_EXTRA) + ((int)(KE_S_F4) << 8)))  ,            "[S-F4]"},
+    {  (-((KS_EXTRA) + ((int)(KE_S_XF4) << 8)))  ,           "[S-xF4]"},
+    {  (-((KS_EXTRA) + ((int)(KE_S_F5) << 8)))  ,            "[S-F5]"},
+    {  (-((KS_EXTRA) + ((int)(KE_S_F6) << 8)))  ,            "[S-F6]"},
+    {  (-((KS_EXTRA) + ((int)(KE_S_F7) << 8)))  ,            "[S-F7]"},
+    {  (-((KS_EXTRA) + ((int)(KE_S_F8) << 8)))  ,            "[S-F8]"},
+    {  (-((KS_EXTRA) + ((int)(KE_S_F9) << 8)))  ,            "[S-F9]"},
+    {  (-((KS_EXTRA) + ((int)(KE_S_F10) << 8)))  ,           "[S-F10]"},
+    {  (-((KS_EXTRA) + ((int)(KE_S_F11) << 8)))  ,           "[S-F11]"},
+    {  (-((KS_EXTRA) + ((int)(KE_S_F12) << 8)))  ,           "[S-F12]"},
+    {  (-(('%') + ((int)('1') << 8)))  ,            "[HELP]"},
+    {  (-(('&') + ((int)('8') << 8)))  ,            "[UNDO]"},
+    {  (-(('k') + ((int)('b') << 8)))  ,              "[BS]"},
+    {  (-(('k') + ((int)('I') << 8)))  ,             "[INS]"},
+    {  (-((KS_EXTRA) + ((int)(KE_KINS) << 8)))  ,            "[KINS]"},
+    {  (-(('k') + ((int)('D') << 8)))  ,             "[DEL]"},
+    {  (-((KS_EXTRA) + ((int)(KE_KDEL) << 8)))  ,            "[KDEL]"},
+    {  (-(('k') + ((int)('h') << 8)))  ,            "[HOME]"},
+    {  (-(('#') + ((int)('2') << 8)))  ,          "[C-HOME]"},
+    {  (-((KS_EXTRA) + ((int)(KE_C_HOME) << 8)))  ,          "[C-HOME]"},
+    {  (-(('K') + ((int)('1') << 8)))  ,           "[KHOME]"},
+    {  (-((KS_EXTRA) + ((int)(KE_XHOME) << 8)))  ,           "[XHOME]"},
+    {  (-((KS_EXTRA) + ((int)(KE_ZHOME) << 8)))  ,           "[ZHOME]"},
+    {  (-(('@') + ((int)('7') << 8)))  ,             "[END]"},
+    {  (-(('*') + ((int)('7') << 8)))  ,           "[C-END]"},
+    {  (-((KS_EXTRA) + ((int)(KE_C_END) << 8)))  ,           "[C-END]"},
+    {  (-(('K') + ((int)('4') << 8)))  ,            "[KEND]"},
+    {  (-((KS_EXTRA) + ((int)(KE_XEND) << 8)))  ,            "[XEND]"},
+    {  (-((KS_EXTRA) + ((int)(KE_ZEND) << 8)))  ,            "[ZEND]"},
+    {  (-(('k') + ((int)('P') << 8)))  ,          "[PAGEUP]"},
+    {  (-(('k') + ((int)('N') << 8)))  ,        "[PAGEDOWN]"},
+    {  (-(('K') + ((int)('3') << 8)))  ,         "[KPAGEUP]"},
+    {  (-(('K') + ((int)('5') << 8)))  ,       "[KPAGEDOWN]"},
+    {  (-((KS_MOUSE) + ((int)( ('X') ) << 8)))  ,           "[MOUSE]"},
+    {  (-(('K') + ((int)('6') << 8)))  ,           "[KPLUS]"},
+    {  (-(('K') + ((int)('7') << 8)))  ,          "[KMINUS]"},
+    {  (-(('K') + ((int)('8') << 8)))  ,         "[KDIVIDE]"},
+    {  (-(('K') + ((int)('9') << 8)))  ,       "[KMULTIPLY]"},
+    {  (-(('K') + ((int)('A') << 8)))  ,          "[KENTER]"},
+    {  (-(('K') + ((int)('B') << 8)))  ,          "[KPOINT]"},
+    {  (-(('P') + ((int)('S') << 8)))  ,              "[PASTE-START]"},
+    {  (-(('P') + ((int)('E') << 8)))  ,              "[PASTE-END]"},
+    {  (-(('K') + ((int)('C') << 8)))  ,              "[K0]"},
+    {  (-(('K') + ((int)('D') << 8)))  ,              "[K1]"},
+    {  (-(('K') + ((int)('E') << 8)))  ,              "[K2]"},
+    {  (-(('K') + ((int)('F') << 8)))  ,              "[K3]"},
+    {  (-(('K') + ((int)('G') << 8)))  ,              "[K4]"},
+    {  (-(('K') + ((int)('H') << 8)))  ,              "[K5]"},
+    {  (-(('K') + ((int)('I') << 8)))  ,              "[K6]"},
+    {  (-(('K') + ((int)('J') << 8)))  ,              "[K7]"},
+    {  (-(('K') + ((int)('K') << 8)))  ,              "[K8]"},
+    {  (-(('K') + ((int)('L') << 8)))  ,              "[K9]"},
 
     {(int)KS_NAME,      NULL}
 };
@@ -156026,9 +160878,7 @@
     {NULL,          NULL},
 };
 
-# define DEFAULT_TERM   (char_u *)"xterm"
-
-static char_u *(term_strings[(int)KS_LAST + 1]);
+static char_u *(term_strings[(int) KS_ESU  + 1]);
 
 static int      need_gather = FALSE;
 static char_u   termleader[256 + 1];
@@ -156039,20 +160889,20 @@
     int     tpr_status;
 } termprop_T;
 
-#define TPR_UNKNOWN         'u'
-#define TPR_YES             'y'
-#define TPR_NO              'n'
-#define TPR_MOUSE_XTERM     'x'
-#define TPR_MOUSE_XTERM2    '2'
-#define TPR_MOUSE_SGR       's'
-
-#define TPR_CURSOR_STYLE            0
-#define TPR_CURSOR_BLINK            1
-#define TPR_UNDERLINE_RGB           2
-#define TPR_MOUSE                   3
-#define TPR_KITTY                   4
-#define TPR_DECRQM                  5
-#define TPR_COUNT                   6
+enum { TPR_UNKNOWN = 'u' };
+enum { TPR_YES = 'y' };
+enum { TPR_NO = 'n' };
+enum { TPR_MOUSE_XTERM = 'x' };
+enum { TPR_MOUSE_XTERM2 = '2' };
+enum { TPR_MOUSE_SGR = 's' };
+
+enum { TPR_CURSOR_STYLE = 0 };
+enum { TPR_CURSOR_BLINK = 1 };
+enum { TPR_UNDERLINE_RGB = 2 };
+enum { TPR_MOUSE = 3 };
+enum { TPR_KITTY = 4 };
+enum { TPR_DECRQM = 5 };
+enum { TPR_COUNT = 6 };
 
 static termprop_T term_props[TPR_COUNT];
 
@@ -156093,11 +160943,11 @@
         {
             break;
         }
-        if (STRCMP(name, "xterm") == 0 && vim_is_xterm(term))
+        if ( strcmp((char *)(name), (char *)("xterm"))  == 0 && vim_is_xterm(term))
         {
             return builtin_terminals[i].bitc_table;
         }
-        if (STRCMP(term, name) == 0)
+        if ( strcmp((char *)(term), (char *)(name))  == 0)
         {
             return builtin_terminals[i].bitc_table;
         }
@@ -156129,7 +160979,7 @@
                             if (term_7to8bit(t))
                             {
                                 *t = term_7to8bit(t);
-                                STRMOVE(t + 1, t + 2);
+                                  memmove((char *)((t + 1)), (char *)((t + 2)),  strlen((char *)(t + 2))  + 1)  ;
                             }
                         }
                         term_strings[p->bt_entry] = s;
@@ -156145,8 +160995,8 @@
         else
         {
             char_u  name[2];
-            name[0] = KEY2TERMCAP0((int)p->bt_entry);
-            name[1] = KEY2TERMCAP1((int)p->bt_entry);
+            name[0] =  ((-((int)p->bt_entry)) & 0xff) ;
+            name[1] =  (((unsigned)(-((int)p->bt_entry)) >> 8) & 0xff) ;
             if (find_termcode(name) == NULL || overwrite)
             {
                 add_termcode(name, (char_u *)p->bt_string, term_8bit);
@@ -156217,22 +161067,22 @@
     static int
 term_strings_not_set(enum SpecialKey idx)
 {
-    return TERM_STR(idx) == NULL || TERM_STR(idx) == empty_option;
+    return  term_strings[(int)(idx)]  == NULL ||  term_strings[(int)(idx)]  == empty_option;
 }
 
     static void
 report_term_error(char *error_msg, char_u *term)
 {
-    mch_errmsg("\r\n");
+     fprintf(stderr, "%s", ("\r\n")) ;
     if (error_msg != NULL)
     {
-        mch_errmsg(error_msg);
-        mch_errmsg("\r\n");
+         fprintf(stderr, "%s", (error_msg)) ;
+         fprintf(stderr, "%s", ("\r\n")) ;
     }
-    mch_errmsg("'");
-    mch_errmsg((char *)term);
-    mch_errmsg(_("' not known, defaulting to 'xterm'"));
-    mch_errmsg("\r\n");
+     fprintf(stderr, "%s", ("'")) ;
+     fprintf(stderr, "%s", ((char *)term)) ;
+     fprintf(stderr, "%s", (_("' not known, defaulting to 'xterm'"))) ;
+     fprintf(stderr, "%s", ("\r\n")) ;
 }
 
     static void
@@ -156248,7 +161098,7 @@
     keyprot_T
 match_keyprotocol(char_u *term)
 {
-    int         len = (int)STRLEN(p_kpc) + 1;
+    int         len = (int) strlen((char *)(p_kpc))  + 1;
     char_u      *buf = alloc(len);
     if (buf == NULL)
     {
@@ -156268,15 +161118,15 @@
         *colon = NUL;
 
         keyprot_T prot;
-        if (STRCMP(colon + 1, "none") == 0)
+        if ( strcmp((char *)(colon + 1), (char *)("none"))  == 0)
         {
             prot = KEYPROTOCOL_NONE;
         }
-        else if (STRCMP(colon + 1, "mok2") == 0)
+        else if ( strcmp((char *)(colon + 1), (char *)("mok2"))  == 0)
         {
             prot = KEYPROTOCOL_MOK2;
         }
-        else if (STRCMP(colon + 1, "kitty") == 0)
+        else if ( strcmp((char *)(colon + 1), (char *)("kitty"))  == 0)
         {
             prot = KEYPROTOCOL_KITTY;
         }
@@ -156286,7 +161136,7 @@
         }
 
         regmatch_T regmatch;
-        CLEAR_FIELD(regmatch);
+          memset((&(regmatch)), (0), (sizeof(regmatch)))  ;
         regmatch.rm_ic = TRUE;
         regmatch.regprog = vim_regcomp(buf, RE_MAGIC);
         if (regmatch.regprog == NULL)
@@ -156345,16 +161195,16 @@
                     wait_return(TRUE);
                     return FAIL;
                 }
-                term = DEFAULT_TERM;
+                term =  (char_u *)"xterm" ;
                 report_default_term(term);
                 set_string_option_direct((char_u *)"term", -1, term, OPT_FREE, 0);
-                display_errors();
+                 fflush(stderr) ;
             }
             out_flush();
                 clear_termoptions();
             parse_builtin_tcap(term);
 
-            if (strstr((char *)requested, "256color") != NULL && (term_strings_not_set(KS_CCO) || atoi((char *)T_CCO) < 256))
+            if (strstr((char *)requested, "256color") != NULL && (term_strings_not_set(KS_CCO) || atoi((char *) ( term_strings[(int)(KS_CCO)] ) ) < 256))
             {
                 apply_builtin_tcap(term, builtin_256colors, TRUE);
             }
@@ -156367,18 +161217,18 @@
 
     }
 
-    if (STRCMP(term, "pcterm") == 0)
+    if ( strcmp((char *)(term), (char *)("pcterm"))  == 0)
     {
-        T_CCS = (char_u *)"yes";
+         ( term_strings[(int)(KS_CCS)] )  = (char_u *)"yes";
     }
     else
     {
-        T_CCS = empty_option;
+         ( term_strings[(int)(KS_CCS)] )  = empty_option;
     }
 
-    if (strstr((char *)term, "kitty") != NULL && (T_CRV == NULL || *T_CRV == NUL))
+    if (strstr((char *)term, "kitty") != NULL && ( ( term_strings[(int)(KS_CRV)] )  == NULL || * ( term_strings[(int)(KS_CRV)] )  == NUL))
     {
-        T_CRV = (char_u *)"\033[>c";
+         ( term_strings[(int)(KS_CRV)] )  = (char_u *)"\033[>c";
     }
 
         get_stty();
@@ -156388,22 +161238,22 @@
         del_p = find_termcode((char_u *)"kD");
         if (bs_p == NULL || *bs_p == NUL)
         {
-            add_termcode((char_u *)"kb", (bs_p = (char_u *)CTRL_H_STR), FALSE);
+            add_termcode((char_u *)"kb", (bs_p = (char_u *) "\010" ), FALSE);
         }
         if ((del_p == NULL || *del_p == NUL) && (bs_p == NULL || *bs_p != DEL))
         {
-            add_termcode((char_u *)"kD", (char_u *)DEL_STR, FALSE);
+            add_termcode((char_u *)"kD", (char_u *) (char_u *)"\177" , FALSE);
         }
     }
 
     term_is_xterm = vim_is_xterm(term);
 
     int did_set_ttym = FALSE;
-    if (T_CXM != NULL && *T_CXM != NUL && !option_was_set((char_u *)"ttym"))
+    if ( ( term_strings[(int)(KS_CXM)] )  != NULL && * ( term_strings[(int)(KS_CXM)] )  != NUL && !option_was_set((char_u *)"ttym"))
     {
-        char_u *p = T_CXM;
+        char_u *p =  ( term_strings[(int)(KS_CXM)] ) ;
 
-        while (*p != NUL && !VIM_ISDIGIT(*p))
+        while (*p != NUL && ! ((unsigned)(*p) - '0' < 10) )
         {
             ++p;
         }
@@ -156484,7 +161334,7 @@
             buf_T       *buf;
             aco_save_T  aco;
 
-            FOR_ALL_BUFFERS(buf)
+             for ((buf) = firstbuf; (buf) != NULL; (buf) = (buf)->b_next) 
             {
                 if (curbuf->b_ml.ml_mfp != NULL)
                 {
@@ -156513,7 +161363,7 @@
         return OK;
     }
 
-    term = T_NAME;
+    term =  ( term_strings[(int)(KS_NAME)] ) ;
     if (term == NULL || *term == NUL)
     {
         return FAIL;
@@ -156528,7 +161378,7 @@
             tcap_entry_T *termp = find_builtin_term(term);
             if (termp != NULL)
             {
-                key = TERMCAP2KEY(name[0], name[1]);
+                key =  (-((name[0]) + ((int)(name[1]) << 8))) ;
                 ++termp;
                 while (termp->bt_entry != (int)KS_NAME)
                 {
@@ -156542,7 +161392,7 @@
             }
         }
 
-    if (SOURCING_NAME == NULL)
+    if ( (((estack_T *)exestack.ga_data)[exestack.ga_len - 1].es_name)  == NULL)
     {
             semsg(_(e_no_str_entry_in_termcap), name);
     }
@@ -156552,7 +161402,7 @@
     static int
 term_is_builtin(char_u *name)
 {
-    return (STRNCMP(name, "builtin_", (size_t)8) == 0);
+    return ( strncmp((char *)(name), (char *)("builtin_"), ((size_t)8))  == 0);
 }
 
     int
@@ -156664,27 +161514,27 @@
 
     if (term == NULL)
     {
-        term = mch_getenv((char_u *)"TERM");
+        term =  (char_u *)getenv((char *)((char_u *)"TERM")) ;
     }
     if (term == NULL || *term == NUL)
     {
-        term = DEFAULT_TERM;
+        term =  (char_u *)"xterm" ;
     }
     set_string_option_direct((char_u *)"term", -1, term, OPT_FREE, 0);
 
     set_string_default("term", term);
     set_string_default("ttytype", term);
 
-    set_termname(T_NAME != NULL ? T_NAME : term);
+    set_termname( ( term_strings[(int)(KS_NAME)] )  != NULL ?  ( term_strings[(int)(KS_NAME)] )  : term);
 }
 
-#define OUT_SIZE        8191
+enum { OUT_SIZE = 8191 };
 
 static char_u           out_buf[OUT_SIZE + 1];
 
 static int              out_pos = 0;
 
-#define MAX_ESC_SEQ_LEN 80
+enum { MAX_ESC_SEQ_LEN = 80 };
 
     void
 out_flush(void)
@@ -156702,11 +161552,11 @@
 }
 
     void
-out_flush_cursor(int     force UNUSED, int     clear_selection UNUSED)
+out_flush_cursor(int     force  __attribute__((unused)) , int     clear_selection  __attribute__((unused)) )
 {
-    mch_disable_flush();
+      ;
     out_flush();
-    mch_enable_flush();
+      ;
 }
 
     void
@@ -156814,48 +161664,48 @@
     void
 term_windgoto(int row, int col)
 {
-    OUT_STR(tgoto((char *)T_CM, col, row));
+     out_str((char_u *)(tgoto((char *) ( term_strings[(int)(KS_CM)] ) , col, row))) ;
 }
 
     void
 term_cursor_right(int i)
 {
-    OUT_STR(tgoto((char *)T_CRI, 0, i));
+     out_str((char_u *)(tgoto((char *) ( term_strings[(int)(KS_CRI)] ) , 0, i))) ;
 }
 
     void
 term_append_lines(int line_count)
 {
-    OUT_STR(tgoto((char *)T_CAL, 0, line_count));
+     out_str((char_u *)(tgoto((char *) ( term_strings[(int)(KS_CAL)] ) , 0, line_count))) ;
 }
 
     void
 term_delete_lines(int line_count)
 {
-    OUT_STR(tgoto((char *)T_CDL, 0, line_count));
+     out_str((char_u *)(tgoto((char *) ( term_strings[(int)(KS_CDL)] ) , 0, line_count))) ;
 }
 
     void
 term_enable_mouse(int enable)
 {
     int on = enable ? 1 : 0;
-    OUT_STR(tgoto((char *)T_CXM, 0, on));
+     out_str((char_u *)(tgoto((char *) ( term_strings[(int)(KS_CXM)] ) , 0, on))) ;
 }
 
     void
 term_set_winsize(int height, int width)
 {
-    OUT_STR(tgoto((char *)T_CWS, width, height));
+     out_str((char_u *)(tgoto((char *) ( term_strings[(int)(KS_CWS)] ) , width, height))) ;
 }
 
     void
 term_font(int n)
 {
-    if (*T_CFO)
+    if (* ( term_strings[(int)(KS_CF)] ) )
     {
         char buf[20];
-        sprintf(buf, (char *)T_CFO, 9 + n);
-        OUT_STR(buf);
+        sprintf(buf, (char *) ( term_strings[(int)(KS_CF)] ) , 9 + n);
+         out_str((char_u *)(buf)) ;
     }
 }
 
@@ -156865,7 +161715,7 @@
     char        buf[20];
     int         i = *s == CSI ? 1 : 2;
 
-    if (n >= 8 && t_colors >= 16 && ((s[0] == ESC && s[1] == '[') || (s[0] == CSI && (i = 1) == 1)) && s[i] != NUL && (STRCMP(s + i + 1, "%p1%dm") == 0 || STRCMP(s + i + 1, "%dm") == 0) && (s[i] == '3' || s[i] == '4'))
+    if (n >= 8 && t_colors >= 16 && ((s[0] == ESC && s[1] == '[') || (s[0] == CSI && (i = 1) == 1)) && s[i] != NUL && ( strcmp((char *)(s + i + 1), (char *)("%p1%dm"))  == 0 ||  strcmp((char *)(s + i + 1), (char *)("%dm"))  == 0) && (s[i] == '3' || s[i] == '4'))
     {
         char *format = "%s%s%%dm";
         char *lead = i == 2 ? ("\033[") : "\233";
@@ -156873,46 +161723,46 @@
                                  : (n >= 16 ? "48;5;" : "10");
 
         sprintf(buf, format, lead, tail);
-        OUT_STR(tgoto(buf, 0, n >= 16 ? n : n - 8));
+         out_str((char_u *)(tgoto(buf, 0, n >= 16 ? n : n - 8))) ;
     }
     else
     {
-        OUT_STR(tgoto((char *)s, 0, n));
+         out_str((char_u *)(tgoto((char *)s, 0, n))) ;
     }
 }
 
     void
 term_fg_color(int n)
 {
-    if (*T_CAF)
+    if (* ( term_strings[(int)(KS_CAF)] ) )
     {
-        term_color(T_CAF, n);
+        term_color( ( term_strings[(int)(KS_CAF)] ) , n);
     }
-    else if (*T_CSF)
+    else if (* ( term_strings[(int)(KS_CSF)] ) )
     {
-        term_color(T_CSF, n);
+        term_color( ( term_strings[(int)(KS_CSF)] ) , n);
     }
 }
 
     void
 term_bg_color(int n)
 {
-    if (*T_CAB)
+    if (* ( term_strings[(int)(KS_CAB)] ) )
     {
-        term_color(T_CAB, n);
+        term_color( ( term_strings[(int)(KS_CAB)] ) , n);
     }
-    else if (*T_CSB)
+    else if (* ( term_strings[(int)(KS_CSB)] ) )
     {
-        term_color(T_CSB, n);
+        term_color( ( term_strings[(int)(KS_CSB)] ) , n);
     }
 }
 
     void
 term_ul_color(int n)
 {
-    if (*T_CAU)
+    if (* ( term_strings[(int)(KS_CAU)] ) )
     {
-        term_color(T_CAU, n);
+        term_color( ( term_strings[(int)(KS_CAU)] ) , n);
     }
 }
 
@@ -156921,7 +161771,7 @@
 {
     char_u      *p;
 
-    if (STRCMP(T_NAME, "linux") == 0 || STRCMP(T_NAME, "screen.linux") == 0 || STRNCMP(T_NAME, "cygwin", 6) == 0 || STRNCMP(T_NAME, "putty", 5) == 0 || ((p = mch_getenv((char_u *)"COLORFGBG")) != NULL && (p = vim_strrchr(p, ';')) != NULL && ((p[1] >= '0' && p[1] <= '6') || p[1] == '8') && p[2] == NUL))
+    if ( strcmp((char *)( ( term_strings[(int)(KS_NAME)] ) ), (char *)("linux"))  == 0 ||  strcmp((char *)( ( term_strings[(int)(KS_NAME)] ) ), (char *)("screen.linux"))  == 0 ||  strncmp((char *)( ( term_strings[(int)(KS_NAME)] ) ), (char *)("cygwin"), (6))  == 0 ||  strncmp((char *)( ( term_strings[(int)(KS_NAME)] ) ), (char *)("putty"), (5))  == 0 || ((p =  (char_u *)getenv((char *)((char_u *)"COLORFGBG")) ) != NULL && (p = vim_strrchr(p, ';')) != NULL && ((p[1] >= '0' && p[1] <= '6') || p[1] == '8') && p[2] == NUL))
     {
         return (char_u *)"dark";
     }
@@ -156931,26 +161781,26 @@
     void
 term_settitle(char_u *title)
 {
-    MAY_WANT_TO_LOG_THIS;
+      ;
 
-    OUT_STR(tgoto((char *)T_TS, 0, 0));
+     out_str((char_u *)(tgoto((char *) ( term_strings[(int)(KS_TS)] ) , 0, 0))) ;
     out_str_nf(title);
-    out_str(T_FS);
+    out_str( ( term_strings[(int)(KS_FS)] ) );
     out_flush();
 }
 
     void
 term_push_title(int which)
 {
-    if ((which & SAVE_RESTORE_TITLE) && T_CST != NULL && *T_CST != NUL)
+    if ((which & SAVE_RESTORE_TITLE) &&  ( term_strings[(int)(KS_CST)] )  != NULL && * ( term_strings[(int)(KS_CST)] )  != NUL)
     {
-        OUT_STR(T_CST);
+         out_str((char_u *)( ( term_strings[(int)(KS_CST)] ) )) ;
         out_flush();
     }
 
-    if ((which & SAVE_RESTORE_ICON) && T_SSI != NULL && *T_SSI != NUL)
+    if ((which & SAVE_RESTORE_ICON) &&  ( term_strings[(int)(KS_SSI)] )  != NULL && * ( term_strings[(int)(KS_SSI)] )  != NUL)
     {
-        OUT_STR(T_SSI);
+         out_str((char_u *)( ( term_strings[(int)(KS_SSI)] ) )) ;
         out_flush();
     }
 }
@@ -156958,15 +161808,15 @@
     void
 term_pop_title(int which)
 {
-    if ((which & SAVE_RESTORE_TITLE) && T_CRT != NULL && *T_CRT != NUL)
+    if ((which & SAVE_RESTORE_TITLE) &&  ( term_strings[(int)(KS_CRT)] )  != NULL && * ( term_strings[(int)(KS_CRT)] )  != NUL)
     {
-        OUT_STR(T_CRT);
+         out_str((char_u *)( ( term_strings[(int)(KS_CRT)] ) )) ;
         out_flush();
     }
 
-    if ((which & SAVE_RESTORE_ICON) && T_SRI != NULL && *T_SRI != NUL)
+    if ((which & SAVE_RESTORE_ICON) &&  ( term_strings[(int)(KS_SRI)] )  != NULL && * ( term_strings[(int)(KS_SRI)] )  != NUL)
     {
-        OUT_STR(T_SRI);
+         out_str((char_u *)( ( term_strings[(int)(KS_SRI)] ) )) ;
         out_flush();
     }
 }
@@ -156978,12 +161828,12 @@
 
     check_options();
 
-    if (*T_CM == NUL)
+    if (* ( term_strings[(int)(KS_CM)] )  == NUL)
     {
         emsg(_(e_terminal_capability_cm_required));
     }
 
-    if (*T_CS != NUL)
+    if (* ( term_strings[(int)(KS_CS)] )  != NUL)
     {
         scroll_region = TRUE;
     }
@@ -156994,86 +161844,86 @@
 
     if (pairs)
     {
-        if (*T_ME == NUL)
+        if (* ( term_strings[(int)(KS_ME)] )  == NUL)
         {
-            T_ME = T_MR = T_MD = T_MB = empty_option;
+             ( term_strings[(int)(KS_ME)] )  =  ( term_strings[(int)(KS_MR)] )  =  ( term_strings[(int)(KS_MD)] )  =  ( term_strings[(int)(KS_MB)] )  = empty_option;
         }
-        if (*T_SO == NUL || *T_SE == NUL)
+        if (* ( term_strings[(int)(KS_SO)] )  == NUL || * ( term_strings[(int)(KS_SE)] )  == NUL)
         {
-            T_SO = T_SE = empty_option;
+             ( term_strings[(int)(KS_SO)] )  =  ( term_strings[(int)(KS_SE)] )  = empty_option;
         }
-        if (*T_US == NUL || *T_UE == NUL)
+        if (* ( term_strings[(int)(KS_US)] )  == NUL || * ( term_strings[(int)(KS_UE)] )  == NUL)
         {
-            T_US = T_UE = empty_option;
+             ( term_strings[(int)(KS_US)] )  =  ( term_strings[(int)(KS_UE)] )  = empty_option;
         }
-        if (*T_CZH == NUL || *T_CZR == NUL)
+        if (* ( term_strings[(int)(KS_CZH)] )  == NUL || * ( term_strings[(int)(KS_CZR)] )  == NUL)
         {
-            T_CZH = T_CZR = empty_option;
+             ( term_strings[(int)(KS_CZH)] )  =  ( term_strings[(int)(KS_CZR)] )  = empty_option;
         }
 
-        if (*T_VE == NUL)
+        if (* ( term_strings[(int)(KS_VE)] )  == NUL)
         {
-            T_VI = empty_option;
+             ( term_strings[(int)(KS_VI)] )  = empty_option;
         }
 
-        if (*T_ME == NUL)
+        if (* ( term_strings[(int)(KS_ME)] )  == NUL)
         {
-            T_ME = T_SE;
-            T_MR = T_SO;
-            T_MD = T_SO;
+             ( term_strings[(int)(KS_ME)] )  =  ( term_strings[(int)(KS_SE)] ) ;
+             ( term_strings[(int)(KS_MR)] )  =  ( term_strings[(int)(KS_SO)] ) ;
+             ( term_strings[(int)(KS_MD)] )  =  ( term_strings[(int)(KS_SO)] ) ;
         }
 
-        if (*T_SO == NUL)
+        if (* ( term_strings[(int)(KS_SO)] )  == NUL)
         {
-            T_SE = T_ME;
-            if (*T_MR == NUL)
+             ( term_strings[(int)(KS_SE)] )  =  ( term_strings[(int)(KS_ME)] ) ;
+            if (* ( term_strings[(int)(KS_MR)] )  == NUL)
             {
-                T_SO = T_MD;
+                 ( term_strings[(int)(KS_SO)] )  =  ( term_strings[(int)(KS_MD)] ) ;
             }
             else
             {
-                T_SO = T_MR;
+                 ( term_strings[(int)(KS_SO)] )  =  ( term_strings[(int)(KS_MR)] ) ;
             }
         }
 
-        if (*T_CZH == NUL)
+        if (* ( term_strings[(int)(KS_CZH)] )  == NUL)
         {
-            T_CZR = T_ME;
-            if (*T_MR == NUL)
+             ( term_strings[(int)(KS_CZR)] )  =  ( term_strings[(int)(KS_ME)] ) ;
+            if (* ( term_strings[(int)(KS_MR)] )  == NUL)
             {
-                T_CZH = T_MD;
+                 ( term_strings[(int)(KS_CZH)] )  =  ( term_strings[(int)(KS_MD)] ) ;
             }
             else
             {
-                T_CZH = T_MR;
+                 ( term_strings[(int)(KS_CZH)] )  =  ( term_strings[(int)(KS_MR)] ) ;
             }
         }
 
-        if (*T_CSB == NUL || *T_CSF == NUL)
+        if (* ( term_strings[(int)(KS_CSB)] )  == NUL || * ( term_strings[(int)(KS_CSF)] )  == NUL)
         {
-            T_CSB = empty_option;
-            T_CSF = empty_option;
+             ( term_strings[(int)(KS_CSB)] )  = empty_option;
+             ( term_strings[(int)(KS_CSF)] )  = empty_option;
         }
 
-        if (*T_CAB == NUL || *T_CAF == NUL)
+        if (* ( term_strings[(int)(KS_CAB)] )  == NUL || * ( term_strings[(int)(KS_CAF)] )  == NUL)
         {
-            T_CAB = empty_option;
-            T_CAF = empty_option;
+             ( term_strings[(int)(KS_CAB)] )  = empty_option;
+             ( term_strings[(int)(KS_CAF)] )  = empty_option;
         }
 
-        if (*T_CSB == NUL && *T_CAB == NUL)
+        if (* ( term_strings[(int)(KS_CSB)] )  == NUL && * ( term_strings[(int)(KS_CAB)] )  == NUL)
         {
-            free_one_termoption(T_CCO);
+            free_one_termoption( ( term_strings[(int)(KS_CCO)] ) );
         }
 
-        p_wiv = (*T_XS != NUL);
+        p_wiv = (* ( term_strings[(int)(KS_XS)] )  != NUL);
     }
     need_gather = TRUE;
 
-    t_colors = atoi((char *)T_CCO);
+    t_colors = atoi((char *) ( term_strings[(int)(KS_CCO)] ) );
     {
-        env_colors = mch_getenv((char_u *)"COLORS");
-        if (env_colors != NULL && SAFE_isdigit(*env_colors))
+        env_colors =  (char_u *)getenv((char *)((char_u *)"COLORS")) ;
+        if (env_colors != NULL &&  (isdigit ((unsigned char)(*env_colors))) )
         {
             int colors = atoi((char *)env_colors);
 
@@ -157098,7 +161948,7 @@
         {
             return -1;
         }
-        if (c == K_SPECIAL)
+        if (c ==  (0x80) )
         {
             if (buf[len] == NUL || buf[len + 1] == NUL)
             {
@@ -157165,14 +162015,14 @@
     static int  old_Columns = 0;
     static int  old_coloff = 0;
 
-    if (old_Rows != Rows || old_Columns != COLUMNS_WITHOUT_TPL() || old_coloff != TPL_LCOL())
+    if (old_Rows != Rows || old_Columns !=  Columns  || old_coloff !=  0 )
     {
         ui_new_shellsize();
     }
-    if (old_Columns != COLUMNS_WITHOUT_TPL() || old_coloff != TPL_LCOL())
+    if (old_Columns !=  Columns  || old_coloff !=  0 )
     {
-        old_Columns = COLUMNS_WITHOUT_TPL();
-        old_coloff = TPL_LCOL();
+        old_Columns =  Columns ;
+        old_coloff =  0 ;
 
         shell_new_columns();
     }
@@ -157260,7 +162110,7 @@
         }
         else
         {
-            if (curwin->w_p_scb)
+            if (curwin-> w_onebuf_opt.wo_scb )
             {
                 do_check_scrollbind(TRUE);
             }
@@ -157300,7 +162150,7 @@
         return;
     }
 
-    if (State == MODE_HITRETURN || State == MODE_SETWSIZE)
+    if (State ==  (0x2000 | MODE_NORMAL)  || State == MODE_SETWSIZE)
     {
         State = MODE_SETWSIZE;
         return;
@@ -157324,7 +162174,7 @@
     void
 out_str_t_TE(void)
 {
-    out_str(T_CTE);
+    out_str( ( term_strings[(int)(KS_CTE)] ) );
 
     if (modify_otherkeys_state == MOKS_ENABLED || modify_otherkeys_state == MOKS_DISABLED)
     {
@@ -157350,7 +162200,7 @@
     void
 out_str_t_TI(void)
 {
-    out_str(T_CTI);
+    out_str( ( term_strings[(int)(KS_CTI)] ) );
 
     send_t_RK = TRUE;
 }
@@ -157360,11 +162210,11 @@
 {
     char_u *p;
 
-    if (T_BE == NULL || *T_BE == NUL || (p = find_termcode((char_u *)"PS")) == NULL || *p == NUL || (p = find_termcode((char_u *)"PE")) == NULL || *p == NUL)
+    if ( ( term_strings[(int)(KS_CBE)] )  == NULL || * ( term_strings[(int)(KS_CBE)] )  == NUL || (p = find_termcode((char_u *)"PS")) == NULL || *p == NUL || (p = find_termcode((char_u *)"PE")) == NULL || *p == NUL)
     {
         return;
     }
-    out_str(T_BE);
+    out_str( ( term_strings[(int)(KS_CBE)] ) );
 }
 
     void
@@ -157373,7 +162223,7 @@
     if (send_t_RK && !work_pending() && !ex_normal_busy && !exiting)
     {
         send_t_RK = FALSE;
-        out_str(T_CRK);
+        out_str( ( term_strings[(int)(KS_CRK)] ) );
         out_flush();
     }
 }
@@ -157381,7 +162231,6 @@
     void
 settmode(tmode_T tmode)
 {
-
     if (!full_screen)
     {
         return;
@@ -157396,11 +162245,11 @@
 
         if (termcap_active && tmode != TMODE_SLEEP && cur_tmode != TMODE_SLEEP)
         {
-            MAY_WANT_TO_LOG_THIS;
+              ;
 
             if (tmode != TMODE_RAW)
             {
-                out_str(T_BD);
+                out_str( ( term_strings[(int)(KS_CBD)] ) );
                 out_str_t_TE();
             }
             else
@@ -157428,16 +162277,16 @@
         return;
     }
 
-    MAY_WANT_TO_LOG_THIS;
+      ;
 
-    out_str(T_TI);
+    out_str( ( term_strings[(int)(KS_TI)] ) );
     out_str_t_TI();
-    out_str(T_KS);
+    out_str( ( term_strings[(int)(KS_KS)] ) );
     out_str_t_BE();
 
-    if (p_ek && *T_FE != NUL)
+    if (p_ek && * ( term_strings[(int)(KS_FE)] )  != NUL)
     {
-        out_str(T_FE);
+        out_str( ( term_strings[(int)(KS_FE)] ) );
     }
 
     out_flush();
@@ -157456,24 +162305,24 @@
         return;
     }
 
-    MAY_WANT_TO_LOG_THIS;
+      ;
 
-    if (p_ek && *T_FD != NUL)
+    if (p_ek && * ( term_strings[(int)(KS_FD)] )  != NUL)
     {
-        out_str(T_FD);
+        out_str( ( term_strings[(int)(KS_FD)] ) );
     }
 
-    out_str(T_BD);
-    out_str(T_KE);
+    out_str( ( term_strings[(int)(KS_CBD)] ) );
+    out_str( ( term_strings[(int)(KS_KE)] ) );
     out_flush();
     termcap_active = FALSE;
 
-    if (*T_TE != NUL && (kitty_protocol_state == KKPS_ENABLED || kitty_protocol_state == KKPS_DISABLED))
+    if (* ( term_strings[(int)(KS_TE)] )  != NUL && (kitty_protocol_state == KKPS_ENABLED || kitty_protocol_state == KKPS_DISABLED))
     {
         out_str_t_TE();
     }
 
-    out_str(T_TE);
+    out_str( ( term_strings[(int)(KS_TE)] ) );
     cursor_on();
     out_str_t_TE();
     screen_start();
@@ -157483,20 +162332,20 @@
     int
 swapping_screen(void)
 {
-    return (full_screen && *T_TI != NUL);
+    return (full_screen && * ( term_strings[(int)(KS_TI)] )  != NUL);
 }
 
     void
 scroll_start(void)
 {
-    if (*T_VS == NUL || *T_CVS == NUL)
+    if (* ( term_strings[(int)(KS_VS)] )  == NUL || * ( term_strings[(int)(KS_CVS)] )  == NUL)
     {
         return;
     }
 
-    MAY_WANT_TO_LOG_THIS;
-    out_str(T_VS);
-    out_str(T_CVS);
+      ;
+    out_str( ( term_strings[(int)(KS_VS)] ) );
+    out_str( ( term_strings[(int)(KS_CVS)] ) );
     screen_start();
 }
 
@@ -157507,7 +162356,7 @@
     void
 cursor_on_force(void)
 {
-    out_str(T_VE);
+    out_str( ( term_strings[(int)(KS_VE)] ) );
     cursor_is_off = FALSE;
     cursor_is_asleep = FALSE;
 }
@@ -157526,7 +162375,7 @@
 {
     if (full_screen && !cursor_is_off)
     {
-        out_str(T_VI);
+        out_str( ( term_strings[(int)(KS_VI)] ) );
         cursor_is_off = TRUE;
     }
 }
@@ -157548,10 +162397,10 @@
     void
 scroll_region_set(win_T *wp, int off)
 {
-    OUT_STR(tgoto((char *)T_CS, W_WINROW(wp) + wp->w_height - 1, W_WINROW(wp) + off));
-    if (*T_CSV != NUL && wp->w_width != Columns)
+     out_str((char_u *)(tgoto((char *) ( term_strings[(int)(KS_CS)] ) ,  (wp->w_winrow)  + wp->w_height - 1,  (wp->w_winrow)  + off))) ;
+    if (* ( term_strings[(int)(KS_CSV)] )  != NUL && wp->w_width != Columns)
     {
-        OUT_STR(tgoto((char *)T_CSV, wp->w_wincol + wp->w_width - 1, wp->w_wincol));
+         out_str((char_u *)(tgoto((char *) ( term_strings[(int)(KS_CSV)] ) , wp->w_wincol + wp->w_width - 1, wp->w_wincol))) ;
     }
     screen_start();
 }
@@ -157559,10 +162408,10 @@
     void
 scroll_region_reset(void)
 {
-    OUT_STR(tgoto((char *)T_CS, (int)Rows - 1, 0));
-    if (*T_CSV != NUL)
+     out_str((char_u *)(tgoto((char *) ( term_strings[(int)(KS_CS)] ) , (int)Rows - 1, 0))) ;
+    if (* ( term_strings[(int)(KS_CSV)] )  != NUL)
     {
-        OUT_STR(tgoto((char *)T_CSV, (int)Columns - 1, 0));
+         out_str((char_u *)(tgoto((char *) ( term_strings[(int)(KS_CSV)] ) , (int)Columns - 1, 0))) ;
     }
     screen_start();
 }
@@ -157587,13 +162436,14 @@
     {
         vim_free(termcodes[--tc_len].code);
     }
-    VIM_CLEAR(termcodes);
+     vim_free(termcodes);
+     (termcodes) = NULL;
     tc_max_len = 0;
 
     need_gather = TRUE;
 }
 
-#define ATC_FROM_TERM 55
+enum { ATC_FROM_TERM = 55 };
 
     static void
 adjust_modlen(int idx)
@@ -157635,18 +162485,18 @@
 
     if (flags != 0 && flags != ATC_FROM_TERM && term_7to8bit(string) != 0)
     {
-        STRMOVE(s, s + 1);
+          memmove((char *)((s)), (char *)((s + 1)),  strlen((char *)(s + 1))  + 1)  ;
         s[0] = term_7to8bit(string);
     }
 
-    len = (int)STRLEN(s);
+    len = (int) strlen((char *)(s)) ;
 
     need_gather = TRUE;
 
     if (tc_len == tc_max_len)
     {
         tc_max_len += 20;
-        new_tc = ALLOC_MULT(struct termcode, tc_max_len);
+        new_tc =  (struct termcode *)alloc(sizeof(struct termcode) * (tc_max_len)) ;
         if (new_tc == NULL)
         {
             tc_max_len -= 20;
@@ -157677,7 +162527,7 @@
             {
                 if (flags == ATC_FROM_TERM && (j = termcode_star(termcodes[i].code, termcodes[i].len)) > 0)
                 {
-                    if (len == termcodes[i].len - j && STRNCMP(s, termcodes[i].code, len - 1) == 0 && s[len - 1] == termcodes[i].code[termcodes[i].len - 1])
+                    if (len == termcodes[i].len - j &&  strncmp((char *)(s), (char *)(termcodes[i].code), (len - 1))  == 0 && s[len - 1] == termcodes[i].code[termcodes[i].len - 1])
                     {
                         vim_free(s);
                         return;
@@ -157711,7 +162561,7 @@
 accept_modifiers_for_function_keys(void)
 {
     regmatch_T regmatch;
-    CLEAR_FIELD(regmatch);
+      memset((&(regmatch)), (0), (sizeof(regmatch)))  ;
     regmatch.rm_ic = TRUE;
     regmatch.regprog = vim_regcomp((char_u *)"^\033[\\d\\+\\~$", RE_MAGIC);
 
@@ -157730,12 +162580,12 @@
         char_u *s = termcodes[i].code;
         if (s != NULL && vim_regexec(&regmatch, s, (colnr_T)0))
         {
-            size_t len = STRLEN(s);
+            size_t len =  strlen((char *)(s)) ;
             char_u *ns = alloc(len + 3);
             if (ns != NULL)
             {
-                mch_memmove(ns, s, len - 1);
-                mch_memmove(ns + len - 1, ";*~", 4);
+                 memmove((char *)(ns), (char *)(s), len - 1) ;
+                 memmove((char *)(ns + len - 1), (char *)(";*~"), 4) ;
                 vim_free(s);
                 termcodes[i].code = ns;
                 termcodes[i].len += 2;
@@ -157836,21 +162686,20 @@
     int         i;
     int         c;
 
-    if (!term_is_8bit(T_NAME))
+    if (!term_is_8bit( ( term_strings[(int)(KS_NAME)] ) ))
     {
         for (i = 0; i < tc_len; ++i)
         {
             c = term_7to8bit(termcodes[i].code);
             if (c != 0)
             {
-                STRMOVE(termcodes[i].code + 1, termcodes[i].code + 2);
+                  memmove((char *)((termcodes[i].code + 1)), (char *)((termcodes[i].code + 2)),  strlen((char *)(termcodes[i].code + 2))  + 1)  ;
                 termcodes[i].code[0] = c;
             }
         }
         need_gather = TRUE;
     }
     detected_8bit = TRUE;
-    LOG_TR1("Switching to 8 bit");
 }
 
 static linenr_T orig_topline = 0;
@@ -157887,13 +162736,13 @@
             }
         }
 
-        mch_memmove(typebuf.tb_buf + typebuf.tb_off + offset, string, (size_t)new_slen);
+         memmove((char *)(typebuf.tb_buf + typebuf.tb_off + offset), (char *)(string), (size_t)new_slen) ;
     }
     else
     {
         if (extra < 0)
         {
-            mch_memmove(buf + offset, buf + offset - extra, (size_t)(*buflen + offset + extra));
+             memmove((char *)(buf + offset), (char *)(buf + offset - extra), (size_t)(*buflen + offset + extra)) ;
         }
         else if (extra > 0)
         {
@@ -157901,9 +162750,9 @@
             {
                 return FAIL;
             }
-            mch_memmove(buf + offset + extra, buf + offset, (size_t)(*buflen - offset));
+             memmove((char *)(buf + offset + extra), (char *)(buf + offset), (size_t)(*buflen - offset)) ;
         }
-        mch_memmove(buf + offset, string, (size_t)new_slen);
+         memmove((char *)(buf + offset), (char *)(string), (size_t)new_slen) ;
         *buflen = *buflen + extra + new_slen;
     }
     return OK;
@@ -157948,7 +162797,7 @@
     *key = simplify_key(*key, &modifiers);
     if (modifiers != 0)
     {
-        string[new_slen++] = K_SPECIAL;
+        string[new_slen++] =  (0x80) ;
         string[new_slen++] = (int)KS_MODIFIER;
         string[new_slen++] = modifiers;
     }
@@ -157956,13 +162805,12 @@
 }
 
     static void
-handle_u7_response(int *arg, char_u *tp UNUSED, int csi_len UNUSED)
+handle_u7_response(int *arg, char_u *tp  __attribute__((unused)) , int csi_len  __attribute__((unused)) )
 {
     if (arg[0] == 2 && arg[1] >= 2)
     {
         char *aw = NULL;
 
-        LOG_TRN("Received U7 status: %s", tp);
         u7_status.tr_progress = STATUS_GOT;
         did_cursorhold = TRUE;
         if (arg[1] == 2)
@@ -157973,7 +162821,7 @@
         {
             aw = "double";
         }
-        if (aw != NULL && STRCMP(aw, p_ambw) != 0)
+        if (aw != NULL &&  strcmp((char *)(aw), (char *)(p_ambw))  != 0)
         {
             set_option_value_give_err((char_u *)"ambw", 0L, (char_u *)aw, 0);
             redraw_asap(UPD_CLEAR);
@@ -157984,7 +162832,6 @@
     {
         int value;
 
-        LOG_TRN("Received compatibility test result: %s", tp);
         xcc_status.tr_progress = STATUS_GOT;
 
         value = arg[1] == 1 ? TPR_YES : TPR_NO;
@@ -157998,7 +162845,6 @@
 {
     int version = arg[1];
 
-    LOG_TRN("Received CRV response: %s", tp);
     crv_status.tr_progress = STATUS_GOT;
     did_cursorhold = TRUE;
 
@@ -158023,7 +162869,7 @@
 
         if ((version == 100 || version == 115) && arg[0] == 0 && arg[2] == 0)
         {
-            if (mch_getenv((char_u *)"COLORS") == NULL)
+            if ( (char_u *)getenv((char *)((char_u *)"COLORS"))  == NULL)
             {
                 may_adjust_color_count(256);
             }
@@ -158120,7 +162966,7 @@
             term_props[TPR_CURSOR_STYLE].tpr_status = TPR_NO;
         }
 
-        if (term_props[TPR_UNDERLINE_RGB].tpr_status != TPR_YES && *T_8U != NUL && !option_was_set((char_u *)"t_8u"))
+        if (term_props[TPR_UNDERLINE_RGB].tpr_status != TPR_YES && * ( term_strings[(int)(KS_8U)] )  != NUL && !option_was_set((char_u *)"t_8u"))
         {
             set_string_option_direct((char_u *)"t_8u", -1, (char_u *)"", OPT_FREE, 0);
         }
@@ -158138,11 +162984,11 @@
 {
     int idx = 0;
 
-    if (IS_SPECIAL(key))
+    if ( ((key) < 0) )
     {
-        buf[idx++] = K_SPECIAL;
-        buf[idx++] = KEY2TERMCAP0(key);
-        buf[idx++] = KEY2TERMCAP1(key);
+        buf[idx++] =  (0x80) ;
+        buf[idx++] =  ((-(key)) & 0xff) ;
+        buf[idx++] =  (((unsigned)(-(key)) >> 8) & 0xff) ;
     }
     else if (has_mbyte)
     {
@@ -158241,7 +163087,7 @@
     }
     if (key_name[0])
     {
-        return TERMCAP2KEY(key_name[0], key_name[1]);
+        return  (-((key_name[0]) + ((int)(key_name[1]) << 8))) ;
     }
     return arg;
 }
@@ -158264,7 +163110,7 @@
 
     if (key == ESC)
     {
-        key = K_ESC;
+        key =   (-((KS_EXTRA) + ((int)(KE_ESC) << 8)))  ;
     }
 
     else if (arg[0] >= 11 && arg[0] <= 24 && trail == '~')
@@ -158283,7 +163129,7 @@
 
     if (arg[0] == ESC)
     {
-        string[0] = K_SPECIAL;
+        string[0] =  (0x80) ;
         string[1] = KS_EXTRA;
         string[2] = KE_ESC;
         new_slen = 3;
@@ -158291,9 +163137,9 @@
     else if (arg[0] >= 11 && arg[0] <= 24 && trail == '~')
     {
         int key = parse_csi_f_keys(arg[0]);
-        string[0] = K_SPECIAL;
-        string[1] = KEY2TERMCAP0(key);
-        string[2] = KEY2TERMCAP1(key);
+        string[0] =  (0x80) ;
+        string[1] =  ((-(key)) & 0xff) ;
+        string[2] =  (((unsigned)(-(key)) >> 8) & 0xff) ;
         new_slen = 3;
     }
     else
@@ -158352,7 +163198,7 @@
             return 0;
     }
 
-    int key = TERMCAP2KEY(key_name[0], key_name[1]);
+    int key =  (-((key_name[0]) + ((int)(key_name[1]) << 8))) ;
     int modifiers = argc == 2 ? decode_modifiers(arg[1]) : 0;
     put_key_modifiers_in_typebuf(key, modifiers, csi_len, offset, buf, bufsize, buflen);
     return csi_len;
@@ -158368,7 +163214,7 @@
     char_u      *ap = argp;
     int         csi_len;
 
-    if (!VIM_ISDIGIT(*ap))
+    if (! ((unsigned)(*ap) - '0' < 10) )
     {
         first = *ap++;
     }
@@ -158391,7 +163237,7 @@
             {
                 arg[argc++] = -1;
             }
-            else if (VIM_ISDIGIT(*ap))
+            else if ( ((unsigned)(*ap) - '0' < 10) )
             {
                 arg[argc] = 0;
                 for (;;)
@@ -158400,7 +163246,7 @@
                     {
                         return -1;
                     }
-                    if (!VIM_ISDIGIT(*ap))
+                    if (! ((unsigned)(*ap) - '0' < 10) )
                     {
                         break;
                     }
@@ -158422,7 +163268,7 @@
             }
         }
 
-        while (ap < tp + len && !(*ap >= '{' && *ap <= '~') && !ASCII_ISALPHA(*ap))
+        while (ap < tp + len && !(*ap >= '{' && *ap <= '~') && ! ( ((unsigned)(*ap) - 'A' < 26)  ||  ((unsigned)(*ap) - 'a' < 26) ) )
         {
             ++ap;
         }
@@ -158447,7 +163293,7 @@
         *slen = csi_len;
     }
 
-    else if (first == -1 && ASCII_ISUPPER(trail) && (argc == 0 || (argc == 2 && arg[0] == 1)))
+    else if (first == -1 &&  ((unsigned)(trail) - 'A' < 26)  && (argc == 0 || (argc == 2 && arg[0] == 1)))
     {
         int res = handle_csi_function_key(argc, arg, trail, csi_len, key_name, offset, buf, bufsize, buflen);
         return res <= 0 ? res : len + res;
@@ -158464,8 +163310,6 @@
 
     else if (first == '?' && trail == 'c')
     {
-        LOG_TRN("Received DA1 response: %s", tp);
-
         *slen = csi_len;
         apply_autocmds(EVENT_TERMRESPONSEALL, (char_u *)"da1", NULL, FALSE, curbuf);
 
@@ -158483,8 +163327,6 @@
 
         if (setting >= 0 && setting <= 4)
         {
-            LOG_TRN("Received DECRPM mode %d: %s", arg[0], tp);
-
             switch (arg[0])
             {
                 case 2026:
@@ -158500,7 +163342,6 @@
         }
         else
         {
-            LOG_TRN("Unknown DECRPM mode %d setting %d", arg[0], setting);
         }
     }
 
@@ -158519,7 +163360,7 @@
         }
     }
 
-    else if (*T_CRV != NUL && ap > argp + 1 && trail == 'c')
+    else if (* ( term_strings[(int)(KS_CRV)] )  != NUL && ap > argp + 1 && trail == 'c')
     {
         handle_version_response(first, arg, argc, tp);
 
@@ -158586,7 +163427,7 @@
                 int is_4digit = i - j >= 21 && resp[j + 11] == '/'
                                                   && resp[j + 16] == '/';
 
-                if (i - j >= 15 && STRNCMP(resp + j + 3, "rgb:", 4) == 0 && (is_4digit || (resp[j + 9] == '/' && resp[j + 12] == '/')))
+                if (i - j >= 15 &&  strncmp((char *)(resp + j + 3), (char *)("rgb:"), (4))  == 0 && (is_4digit || (resp[j + 9] == '/' && resp[j + 12] == '/')))
                 {
                     char_u *tp_r = resp + j + 7;
                     char_u *tp_g = resp + j + (is_4digit ? 12 : 10);
@@ -158595,8 +163436,7 @@
                     {
                         char *new_bg_val = (3 * '6' < *tp_r + *tp_g + *tp_b) ? "light" : "dark";
 
-                        LOG_TRN("Received RBG response: r=%d g=%d b=%d", rval, gval, bval);
-                        if (!option_was_set((char_u *)"bg") && STRCMP(p_bg, new_bg_val) != 0)
+                        if (!option_was_set((char_u *)"bg") &&  strcmp((char *)(p_bg), (char *)(new_bg_val))  != 0)
                         {
                             set_option_value_give_err((char_u *)"bg", 0L, (char_u *)new_bg_val, 0);
                             reset_option_was_set((char_u *)"bg");
@@ -158612,7 +163452,6 @@
     }
     if (i == len)
     {
-        LOG_TR1("not enough characters for RB");
     }
 }
 
@@ -158633,7 +163472,6 @@
     {
         int cur;
 
-        LOG_TRN("Received OSC response: %s", (char*)tp);
         cur = 1 + (tp[0] == ESC);
 
         if (len < cur + 1 + (tp[0] != OSC))
@@ -158642,7 +163480,7 @@
         }
 
         ga_init2(&osc_state.buf, 1, 1024);
-        ELAPSED_INIT(osc_state.start_tv);
+         gettimeofday(&(osc_state.start_tv), NULL) ;
         osc_state.processing = TRUE;
         osc_state.start_char = tp[0];
         last_char = 0;
@@ -158679,7 +163517,7 @@
 
     key_name[1] = (int)KE_IGNORE;
 
-    if (ELAPSED_FUNC(osc_state.start_tv) >= p_ost)
+    if ( elapsed(&(osc_state.start_tv))  >= p_ost)
     {
         semsg(_(e_osc_response_timed_out), osc_state.buf.ga_len, osc_state.buf.ga_data);
 
@@ -158700,7 +163538,6 @@
     int i;
     int j;
 
-    LOG_TRN("Received DCS response: %s", (char*)tp);
     j = 1 + (tp[0] == ESC);
     if (len < j + 3)
     {
@@ -158727,7 +163564,7 @@
     {
         for (i = j + 3; i < len; ++i)
         {
-            if (i - j == 3 && !SAFE_isdigit(tp[i]))
+            if (i - j == 3 && ! (isdigit ((unsigned char)(tp[i]))) )
             {
                 break;
             }
@@ -158745,8 +163582,6 @@
             }
             if ((i - j == 6 && tp[i] == STERM) || (i - j == 7 && tp[i] == '\\'))
             {
-                LOG_TRN("Received cursor shape response: %s", tp);
-
                 key_name[0] = (int)KS_EXTRA;
                 key_name[1] = (int)KE_IGNORE;
                 *slen = i + 1;
@@ -158758,7 +163593,6 @@
 
     if (i == len)
     {
-        LOG_TR1("not enough characters for XT");
         return FAIL;
     }
     return OK;
@@ -158813,7 +163647,7 @@
             len = *buflen - offset;
         }
 
-        if (*tp == K_SPECIAL)
+        if (*tp ==  (0x80) )
         {
             offset += 2;
             continue;
@@ -158862,7 +163696,7 @@
                 {
                     continue;
                 }
-                if (STRNCMP(termcodes[idx].code, tp, (size_t)(slen > len ? len : slen)) == 0)
+                if ( strncmp((char *)(termcodes[idx].code), (char *)(tp), ((size_t)(slen > len ? len : slen)))  == 0)
                 {
                     int     looks_like_mouse_start = FALSE;
 
@@ -158871,7 +163705,7 @@
                         return -1;
                     }
 
-                    if (termcodes[idx].name[0] == 'K' && (VIM_ISDIGIT(termcodes[idx].name[1]) || ASCII_ISUPPER(termcodes[idx].name[1])))
+                    if (termcodes[idx].name[0] == 'K' && ( ((unsigned)(termcodes[idx].name[1]) - '0' < 10)  ||  ((unsigned)(termcodes[idx].name[1]) - 'A' < 26) ))
                     {
                         is_keypad = TRUE;
                         if (keypad_index_found < 0)
@@ -158883,7 +163717,7 @@
 
                     if (slen == 2 && len > 2 && termcodes[idx].code[0] == ESC && termcodes[idx].code[1] == '[')
                     {
-                        if (!SAFE_isdigit(tp[2]))
+                        if (! (isdigit ((unsigned char)(tp[2]))) )
                         {
                             looks_like_mouse_start = TRUE;
                         }
@@ -158938,7 +163772,7 @@
                     {
                         continue;
                     }
-                    if (STRNCMP(termcodes[idx].code, tp, (size_t)(modslen > len ? len : modslen)) == 0)
+                    if ( strncmp((char *)(termcodes[idx].code), (char *)(tp), ((size_t)(modslen > len ? len : modslen)))  == 0)
                     {
                         int         n;
 
@@ -158961,7 +163795,7 @@
                         }
                         else
                         {
-                            for (j = slen - 2; j < len && (SAFE_isdigit(tp[j]) || tp[j] == '-' || tp[j] == ';'); ++j)
+                            for (j = slen - 2; j < len && ( (isdigit ((unsigned char)(tp[j])))  || tp[j] == '-' || tp[j] == ';'); ++j)
                             {
                                 ;
                             }
@@ -158982,7 +163816,7 @@
 
                             slen = j;
                         }
-                        if (termcodes[idx].name[0] == 'K' && (VIM_ISDIGIT(termcodes[idx].name[1]) || ASCII_ISUPPER(termcodes[idx].name[1])))
+                        if (termcodes[idx].name[0] == 'K' && ( ((unsigned)(termcodes[idx].name[1]) - '0' < 10)  ||  ((unsigned)(termcodes[idx].name[1]) - 'A' < 26) ))
                         {
                             is_keypad = TRUE;
                             if (keypad_index_found < 0)
@@ -159081,12 +163915,12 @@
             }
         }
 
-        key = handle_x_keys(TERMCAP2KEY(key_name[0], key_name[1]));
+        key = handle_x_keys( (-((key_name[0]) + ((int)(key_name[1]) << 8))) );
 
         new_slen = modifiers2keycode(modifiers, &key, string);
 
-        key_name[0] = KEY2TERMCAP0(key);
-        key_name[1] = KEY2TERMCAP1(key);
+        key_name[0] =  ((-(key)) & 0xff) ;
+        key_name[1] =  (((unsigned)(-(key)) >> 8) & 0xff) ;
         if (key_name[0] == KS_KEY)
         {
             if (has_mbyte)
@@ -159104,7 +163938,7 @@
         }
         else
         {
-            string[new_slen++] = K_SPECIAL;
+            string[new_slen++] =  (0x80) ;
             string[new_slen++] = key_name[0];
             string[new_slen++] = key_name[1];
         }
@@ -159119,7 +163953,7 @@
 }
 
     char_u *
-replace_termcodes(char_u      *from, char_u      **bufp, scid_T      sid_arg UNUSED, int         flags, int         *did_simplify)
+replace_termcodes(char_u      *from, char_u      **bufp, scid_T      sid_arg  __attribute__((unused)) , int         flags, int         *did_simplify)
 {
     int         i;
     int         slen;
@@ -159139,16 +163973,16 @@
     src = from;
 
     ga_init2(&ga, 1L, 100);
-    if (ga_grow(&ga, (int)(STRLEN(src) * 6 + 1)) == FAIL)
+    if (ga_grow(&ga, (int)( strlen((char *)(src))  * 6 + 1)) == FAIL)
     {
         *bufp = NULL;
         return from;
     }
     result = ga.ga_data;
 
-    if ((flags & REPTERM_FROM_PART) && src[0] == '#' && VIM_ISDIGIT(src[1]))
+    if ((flags & REPTERM_FROM_PART) && src[0] == '#' &&  ((unsigned)(src[1]) - '0' < 10) )
     {
-        result[dlen++] = K_SPECIAL;
+        result[dlen++] =  (0x80) ;
         result[dlen++] = 'k';
         if (src[1] == '0')
         {
@@ -159163,7 +163997,7 @@
 
     while (*src != NUL)
     {
-        if (do_special && ((flags & REPTERM_DO_LT) || STRNCMP(src, "<lt>", 4) != 0))
+        if (do_special && ((flags & REPTERM_DO_LT) ||  strncmp((char *)(src), (char *)("<lt>"), (4))  != 0))
         {
             int fsk_flags = FSK_KEYCODE
                         | ((flags & REPTERM_NO_SIMPLIFY) ? 0 : FSK_SIMPLIFY)
@@ -159182,7 +164016,7 @@
             i = find_term_bykeys(src, &len);
             if (i >= 0)
             {
-                result[dlen++] = K_SPECIAL;
+                result[dlen++] =  (0x80) ;
                 result[dlen++] = termcodes[i].name[0];
                 result[dlen++] = termcodes[i].name[1];
                 src += len;
@@ -159206,11 +164040,11 @@
 
         for (i = (*mb_ptr2len)(src); i > 0; --i)
         {
-            if (*src == K_SPECIAL)
+            if (*src ==  (0x80) )
             {
-                result[dlen++] = K_SPECIAL;
+                result[dlen++] =  (0x80) ;
                 result[dlen++] = KS_SPECIAL;
-                result[dlen++] = KE_FILLER;
+                result[dlen++] =  ('X') ;
             }
             else
             {
@@ -159234,7 +164068,7 @@
 {
     int i;
     int j;
-    int         len = (int)STRLEN(src);
+    int         len = (int) strlen((char *)(src)) ;
     int         found = -1;
     int         foundlen = 1;
     int slen;
@@ -159257,7 +164091,7 @@
 
         if (modslen > 0)
         {
-            if (len > modslen && STRNCMP(termcodes[i].code, src, (size_t)modslen) == 0)
+            if (len > modslen &&  strncmp((char *)(termcodes[i].code), (char *)(src), ((size_t)modslen))  == 0)
             {
                 thislen = 0;
 
@@ -159275,7 +164109,7 @@
                 }
                 else
                 {
-                    for (j = slen - 2; j < len && (SAFE_isdigit(src[j]) || src[j] == '-' || src[j] == ';'); ++j)
+                    for (j = slen - 2; j < len && ( (isdigit ((unsigned char)(src[j])))  || src[j] == '-' || src[j] == ';'); ++j)
                     {
                         ;
                     }
@@ -159301,7 +164135,7 @@
         }
         else
         {
-            if (slen > foundlen && len >= slen && STRNCMP(termcodes[i].code, src, (size_t)slen) == 0)
+            if (slen > foundlen && len >= slen &&  strncmp((char *)(termcodes[i].code), (char *)(src), ((size_t)slen))  == 0)
             {
                 found = i;
                 foundlen = slen;
@@ -159350,15 +164184,11 @@
     int         i;
     int         len;
 
-#define INC3 27
-#define INC2 40
-#define TERMCODE_GAP 2
-
     if (tc_len == 0)
     {
         return;
     }
-    items = ALLOC_MULT(int, tc_len);
+    items =  (int *)alloc(sizeof(int) * (tc_len)) ;
     if (items == NULL)
     {
         return;
@@ -159441,16 +164271,16 @@
     }
     IObuff[4] = ' ';
 
-    p = get_special_key_name(TERMCAP2KEY(name[0], name[1]), 0);
+    p = get_special_key_name( (-((name[0]) + ((int)(name[1]) << 8))) , 0);
     if (p[1] != 't')
     {
-        STRCPY(IObuff + 5, p);
+         strcpy((char *)(IObuff + 5), (char *)(p)) ;
     }
     else
     {
         IObuff[5] = NUL;
     }
-    len = (int)STRLEN(IObuff);
+    len = (int) strlen((char *)(IObuff)) ;
     do
     {
         IObuff[len++] = ' ';
@@ -159496,7 +164326,7 @@
             int modifiers = ta_buf[i + 2];
             int key = ta_buf[i + 3];
 
-            mch_memmove(ta_buf + i + 1, ta_buf + i + 4, (size_t)(len - i - 3));
+             memmove((char *)(ta_buf + i + 1), (char *)(ta_buf + i + 4), (size_t)(len - i - 3)) ;
             len -= 3;
             if (key < 0x80)
             {
@@ -159506,11 +164336,11 @@
         }
         else if (ta_buf[i] == CSI && len - i > 2)
         {
-            c = TERMCAP2KEY(ta_buf[i + 1], ta_buf[i + 2]);
-            if (c == K_DEL || c == K_KDEL || c == K_BS)
+            c =  (-((ta_buf[i + 1]) + ((int)(ta_buf[i + 2]) << 8))) ;
+            if (c ==   (-(('k') + ((int)('D') << 8)))   || c ==   (-((KS_EXTRA) + ((int)(KE_KDEL) << 8)))   || c ==   (-(('k') + ((int)('b') << 8)))  )
             {
-                mch_memmove(ta_buf + i + 1, ta_buf + i + 3, (size_t)(len - i - 2));
-                if (c == K_DEL || c == K_KDEL)
+                 memmove((char *)(ta_buf + i + 1), (char *)(ta_buf + i + 3), (size_t)(len - i - 2)) ;
+                if (c ==   (-(('k') + ((int)('D') << 8)))   || c ==   (-((KS_EXTRA) + ((int)(KE_KDEL) << 8)))  )
                 {
                     ta_buf[i] = DEL;
                 }
@@ -159544,7 +164374,6 @@
     void
 term_set_win_resize(bool state)
 {
-
     if (!state || win_resize_setting == 0 || win_resize_setting == 4)
     {
         if (win_resize_enabled)
@@ -159555,7 +164384,7 @@
         }
         win_resize_enabled = false;
     }
-    else if ((*p_trz == NUL || STRCMP(p_trz, "inband") == 0) && !win_resize_enabled)
+    else if ((*p_trz == NUL ||  strcmp((char *)(p_trz), (char *)("inband"))  == 0) && !win_resize_enabled)
     {
         if (win_resize_setting == 2)
         {
@@ -159570,7 +164399,7 @@
 sync_output_active(void)
 {
     return p_tsy && (sync_output_setting == 1 || sync_output_setting == 2)
-        && *T_BSU != NUL && *T_ESU != NUL;
+        && * ( term_strings[(int)(KS_BSU)] )  != NUL && * ( term_strings[(int)(KS_ESU)] )  != NUL;
 }
 
     void
@@ -159583,10 +164412,10 @@
 
     if (flags & TERM_SYNC_OUTPUT_FLUSH)
     {
-        if (allowed && !in_gui && sync_output_state > 0 && *T_ESU != NUL && *T_BSU != NUL)
+        if (allowed && !in_gui && sync_output_state > 0 && * ( term_strings[(int)(KS_ESU)] )  != NUL && * ( term_strings[(int)(KS_BSU)] )  != NUL)
         {
-            out_str_nf(T_ESU);
-            out_str_nf(T_BSU);
+            out_str_nf( ( term_strings[(int)(KS_ESU)] ) );
+            out_str_nf( ( term_strings[(int)(KS_BSU)] ) );
             out_flush();
         }
         return;
@@ -159594,16 +164423,16 @@
 
     if (flags & TERM_SYNC_OUTPUT_OFF)
     {
-        if (sync_output_state > 0 && *T_ESU != NUL)
+        if (sync_output_state > 0 && * ( term_strings[(int)(KS_ESU)] )  != NUL)
         {
-            out_str_nf(T_ESU);
+            out_str_nf( ( term_strings[(int)(KS_ESU)] ) );
             out_flush();
             sync_output_state = 0;
         }
         return;
     }
 
-    if (!allowed || in_gui || *T_BSU == NUL || *T_ESU == NUL)
+    if (!allowed || in_gui || * ( term_strings[(int)(KS_BSU)] )  == NUL || * ( term_strings[(int)(KS_ESU)] )  == NUL)
     {
         return;
     }
@@ -159614,7 +164443,7 @@
         {
             return;
         }
-        out_str_nf(T_BSU);
+        out_str_nf( ( term_strings[(int)(KS_BSU)] ) );
     }
     else if (flags & TERM_SYNC_OUTPUT_DISABLE)
     {
@@ -159622,7 +164451,7 @@
         {
             return;
         }
-        out_str_nf(T_ESU);
+        out_str_nf( ( term_strings[(int)(KS_ESU)] ) );
         out_flush();
     }
     else
@@ -159636,8 +164465,6 @@
 
 static int      did_add_space = FALSE;
 
-#define WHITECHAR(cc) (VIM_ISWHITE(cc) && (!enc_utf8 || !utf_iscomposing(utf_ptr2char(ml_get_cursor() + 1))))
-
     int
 has_format_option(int x)
 {
@@ -159668,7 +164495,7 @@
     if (!curbuf->b_p_ai && !(State & VREPLACE_FLAG))
     {
         cc = gchar_cursor();
-        if (VIM_ISWHITE(cc))
+        if ( ((cc) == ' ' || (cc) == '\t') )
         {
             save_char = cc;
             pchar_cursor('x');
@@ -159741,7 +164568,6 @@
         }
         if (!(flags & INSCHAR_FORMAT) && leader_len == 0 && !has_format_option(FO_WRAP))
         {
-
             break;
         }
         if ((startcol = curwin->w_cursor.col) == 0)
@@ -159769,12 +164595,12 @@
             {
                 cc = gchar_cursor();
             }
-            if (WHITECHAR(cc))
+            if ( ( ((cc) == ' ' || (cc) == '\t')  && (!enc_utf8 || !utf_iscomposing(utf_ptr2char(ml_get_cursor() + 1)))) )
             {
                 end_col = curwin->w_cursor.col;
 
                 wcc = 0;
-                while (curwin->w_cursor.col > 0 && WHITECHAR(cc))
+                while (curwin->w_cursor.col > 0 &&  ( ((cc) == ' ' || (cc) == '\t')  && (!enc_utf8 || !utf_iscomposing(utf_ptr2char(ml_get_cursor() + 1)))) )
                 {
                     dec_cursor();
                     cc = gchar_cursor();
@@ -159784,7 +164610,7 @@
                         wcc++;
                     }
                 }
-                if (curwin->w_cursor.col == 0 && WHITECHAR(cc))
+                if (curwin->w_cursor.col == 0 &&  ( ((cc) == ' ' || (cc) == '\t')  && (!enc_utf8 || !utf_iscomposing(utf_ptr2char(ml_get_cursor() + 1)))) )
                 {
                     break;
                 }
@@ -159812,7 +164638,7 @@
                     dec_cursor();
                     cc = gchar_cursor();
 
-                    if (WHITECHAR(cc))
+                    if ( ( ((cc) == ' ' || (cc) == '\t')  && (!enc_utf8 || !utf_iscomposing(utf_ptr2char(ml_get_cursor() + 1)))) )
                     {
                         continue;
                     }
@@ -159870,7 +164696,7 @@
                 dec_cursor();
                 cc = gchar_cursor();
 
-                if (WHITECHAR(cc))
+                if ( ( ((cc) == ' ' || (cc) == '\t')  && (!enc_utf8 || !utf_iscomposing(utf_ptr2char(ml_get_cursor() + 1)))) )
                 {
                     continue;
                 }
@@ -159954,7 +164780,7 @@
         }
 
         curwin->w_cursor.col = foundcol;
-        while ((cc = gchar_cursor(), WHITECHAR(cc)) && (!fo_white_par || curwin->w_cursor.col < startcol))
+        while ((cc = gchar_cursor(),  ( ((cc) == ' ' || (cc) == '\t')  && (!enc_utf8 || !utf_iscomposing(utf_ptr2char(ml_get_cursor() + 1)))) ) && (!fo_white_par || curwin->w_cursor.col < startcol))
         {
             inc_cursor();
         }
@@ -160111,7 +164937,7 @@
         return FALSE;
     }
     l = ml_get_len(lnum) - 1;
-    return VIM_ISWHITE(s[l]);
+    return  ((s[l]) == ' ' || (s[l]) == '\t') ;
 }
 
     static int
@@ -160168,14 +164994,14 @@
     {
         char_u  *line2;
 
-        for (idx1 = 0; VIM_ISWHITE(line1[idx1]); ++idx1)
+        for (idx1 = 0;  ((line1[idx1]) == ' ' || (line1[idx1]) == '\t') ; ++idx1)
         {
             ;
         }
         line2 = ml_get(lnum + 1);
         for (idx2 = 0; idx2 < leader2_len; ++idx2)
         {
-            if (!VIM_ISWHITE(line2[idx2]))
+            if (! ((line2[idx2]) == ' ' || (line2[idx2]) == '\t') )
             {
                 if (line1[idx1++] != line2[idx2])
                 {
@@ -160184,7 +165010,7 @@
             }
             else
             {
-                while (VIM_ISWHITE(line1[idx1]))
+                while ( ((line1[idx1]) == ' ' || (line1[idx1]) == '\t') )
                 {
                     ++idx1;
                 }
@@ -160269,12 +165095,12 @@
 
         dec_cursor();
         cc = gchar_cursor();
-        if (!WHITECHAR(cc) && curwin->w_cursor.col > 0 && has_format_option(FO_ONE_LETTER))
+        if (! ( ((cc) == ' ' || (cc) == '\t')  && (!enc_utf8 || !utf_iscomposing(utf_ptr2char(ml_get_cursor() + 1))))  && curwin->w_cursor.col > 0 && has_format_option(FO_ONE_LETTER))
         {
             dec_cursor();
         }
         cc = gchar_cursor();
-        if (WHITECHAR(cc))
+        if ( ( ((cc) == ' ' || (cc) == '\t')  && (!enc_utf8 || !utf_iscomposing(utf_ptr2char(ml_get_cursor() + 1)))) )
         {
             curwin->w_cursor = pos;
             return;
@@ -160285,7 +165111,7 @@
     if (*old != NUL && !trailblank && !wasatend && pos.col > 0 && (State & MODE_INSERT))
     {
         char_u *line = ml_get_curline();
-        if (WHITECHAR(line[pos.col - 1]))
+        if ( ( ((line[pos.col - 1]) == ' ' || (line[pos.col - 1]) == '\t')  && (!enc_utf8 || !utf_iscomposing(utf_ptr2char(ml_get_cursor() + 1)))) )
         {
             curwin->w_cursor = pos;
             return;
@@ -160358,7 +165184,7 @@
     }
 
     cc = gchar_cursor();
-    if (!WHITECHAR(cc))
+    if (! ( ((cc) == ' ' || (cc) == '\t')  && (!enc_utf8 || !utf_iscomposing(utf_ptr2char(ml_get_cursor() + 1)))) )
     {
         did_add_space = FALSE;
     }
@@ -160391,7 +165217,7 @@
         {
             textwidth -= 1;
         }
-        if (curwin->w_p_nu || curwin->w_p_rnu)
+        if (curwin-> w_onebuf_opt.wo_nu  || curwin-> w_onebuf_opt.wo_rnu )
         {
             textwidth -= 8;
         }
@@ -160466,7 +165292,7 @@
     {
         win_T   *wp;
 
-        FOR_ALL_WINDOWS(wp)
+         for ((wp) = firstwin; (wp) != NULL; (wp) = (wp)->w_next) 
         {
             if (wp->w_old_cursor_lnum != 0)
             {
@@ -160578,7 +165404,7 @@
         {
             if (first_par_line && (do_second_indent || do_number_indent) && prev_is_end_par && curwin->w_cursor.lnum < curbuf->b_ml.ml_line_count)
             {
-                if (do_second_indent && !LINEEMPTY(curwin->w_cursor.lnum + 1))
+                if (do_second_indent && ! (*ml_get(curwin->w_cursor.lnum + 1) == NUL) )
                 {
                     if (leader_len == 0 && next_leader_len == 0)
                     {
@@ -160609,7 +165435,7 @@
 
             if (curwin->w_cursor.lnum >= curbuf->b_ml.ml_line_count || !same_leader(curwin->w_cursor.lnum, leader_len, leader_flags, next_leader_len, next_leader_flags))
             {
-                if (next_leader_flags == NULL || STRNCMP(next_leader_flags, "://", 3) != 0 || check_linecomment(ml_get_curline()) == MAXCOL)
+                if (next_leader_flags == NULL ||  strncmp((char *)(next_leader_flags), (char *)("://"), (3))  != 0 || check_linecomment(ml_get_curline()) == MAXCOL)
                 {
                 is_end_par = TRUE;
                 }
@@ -160775,16 +165601,16 @@
             ++pos.lnum;
             goto found;
         }
-        else if (dir == BACKWARD)
+        else if (dir ==  (-1) )
         {
             decl(&pos);
         }
 
         found_dot = FALSE;
-        while (c = gchar_pos(&pos), VIM_ISWHITE(c) || vim_strchr((char_u *)".!?)]\"'", c) != NULL)
+        while (c = gchar_pos(&pos),  ((c) == ' ' || (c) == '\t')  || vim_strchr((char_u *)".!?)]\"'", c) != NULL)
         {
             tpos = pos;
-            if (decl(&tpos) == -1 || (LINEEMPTY(tpos.lnum) && dir == FORWARD))
+            if (decl(&tpos) == -1 || ( (*ml_get(tpos.lnum) == NUL)  && dir == FORWARD))
             {
                 break;
             }
@@ -160814,7 +165640,7 @@
             c = gchar_pos(&pos);
             if (c == NUL || (pos.col == 0 && startPS(pos.lnum, NUL, FALSE)))
             {
-                if (dir == BACKWARD && pos.lnum != startlnum)
+                if (dir ==  (-1)  && pos.lnum != startlnum)
                 {
                     ++pos.lnum;
                 }
@@ -160861,7 +165687,7 @@
             }
         }
 
-        if (EQUAL_POS(prev_pos, pos))
+        if ( (((prev_pos).lnum == (pos).lnum) && ((prev_pos).col == (pos).col) && ((prev_pos).coladd == (pos).coladd)) )
         {
             if ((*func)(&pos) == -1)
             {
@@ -161093,7 +165919,7 @@
         {
             while (cls() == 0)
             {
-                if (curwin->w_cursor.col == 0 && LINEEMPTY(curwin->w_cursor.lnum))
+                if (curwin->w_cursor.col == 0 &&  (*ml_get(curwin->w_cursor.lnum) == NUL) )
                 {
                     goto finished;
                 }
@@ -161103,7 +165929,7 @@
                 }
             }
 
-            if (skip_chars(cls(), BACKWARD))
+            if (skip_chars(cls(),  (-1) ))
             {
                 return OK;
             }
@@ -161149,7 +165975,7 @@
         {
             while (cls() == 0)
             {
-                if (empty && curwin->w_cursor.col == 0 && LINEEMPTY(curwin->w_cursor.lnum))
+                if (empty && curwin->w_cursor.col == 0 &&  (*ml_get(curwin->w_cursor.lnum) == NUL) )
                 {
                     goto finished;
                 }
@@ -161204,7 +166030,7 @@
 
         while (cls() == 0)
         {
-            if (curwin->w_cursor.col == 0 && LINEEMPTY(curwin->w_cursor.lnum))
+            if (curwin->w_cursor.col == 0 &&  (*ml_get(curwin->w_cursor.lnum) == NUL) )
             {
                 break;
             }
@@ -161260,7 +166086,7 @@
     while (decl(posp) != -1)
     {
         c = gchar_pos(posp);
-        if (!VIM_ISWHITE(c))
+        if (! ((c) == ' ' || (c) == '\t') )
         {
             incl(posp);
             break;
@@ -161295,14 +166121,16 @@
     int         include_white = FALSE;
 
     cls_bigword = bigword;
-    CLEAR_POS(&start_pos);
+     (&start_pos)->lnum = 0;
+     (&start_pos)->col = 0;
+     (&start_pos)->coladd = 0;
 
-    if (VIsual_active && *p_sel == 'e' && LT_POS(VIsual, curwin->w_cursor))
+    if (VIsual_active && *p_sel == 'e' &&  (((VIsual).lnum != (curwin->w_cursor).lnum)               ? (VIsual).lnum < (curwin->w_cursor).lnum                   : (VIsual).col != (curwin->w_cursor).col                        ? (VIsual).col < (curwin->w_cursor).col                     : (VIsual).coladd < (curwin->w_cursor).coladd) )
     {
         dec_cursor();
     }
 
-    if (!VIsual_active || EQUAL_POS(curwin->w_cursor, VIsual))
+    if (!VIsual_active ||  (((curwin->w_cursor).lnum == (VIsual).lnum) && ((curwin->w_cursor).col == (VIsual).col) && ((curwin->w_cursor).coladd == (VIsual).coladd)) )
     {
         back_in_line();
         start_pos = curwin->w_cursor;
@@ -161348,7 +166176,7 @@
     while (count > 0)
     {
         inclusive = TRUE;
-        if (VIsual_active && LT_POS(curwin->w_cursor, VIsual))
+        if (VIsual_active &&  (((curwin->w_cursor).lnum != (VIsual).lnum)               ? (curwin->w_cursor).lnum < (VIsual).lnum                   : (curwin->w_cursor).col != (VIsual).col                        ? (curwin->w_cursor).col < (VIsual).col                     : (curwin->w_cursor).coladd < (VIsual).coladd) )
         {
             if (decl(&curwin->w_cursor) == -1)
             {
@@ -161422,7 +166250,7 @@
 
     if (VIsual_active)
     {
-        if (*p_sel == 'e' && inclusive && LTOREQ_POS(VIsual, curwin->w_cursor))
+        if (*p_sel == 'e' && inclusive &&  ( (((VIsual).lnum != (curwin->w_cursor).lnum)               ? (VIsual).lnum < (curwin->w_cursor).lnum                   : (VIsual).col != (curwin->w_cursor).col                        ? (VIsual).col < (curwin->w_cursor).col                     : (VIsual).coladd < (curwin->w_cursor).coladd)  ||  (((VIsual).lnum == (curwin->w_cursor).lnum) && ((VIsual).col == (curwin->w_cursor).col) && ((VIsual).coladd == (curwin->w_cursor).coladd)) ) )
         {
             inc_cursor();
         }
@@ -161454,17 +166282,17 @@
     pos = start_pos;
     findsent(FORWARD, 1L);
 
-    if (VIsual_active && !EQUAL_POS(start_pos, VIsual))
+    if (VIsual_active && ! (((start_pos).lnum == (VIsual).lnum) && ((start_pos).col == (VIsual).col) && ((start_pos).coladd == (VIsual).coladd)) )
     {
 extend:
-        if (LT_POS(start_pos, VIsual))
+        if ( (((start_pos).lnum != (VIsual).lnum)               ? (start_pos).lnum < (VIsual).lnum                   : (start_pos).col != (VIsual).col                        ? (start_pos).col < (VIsual).col                     : (start_pos).coladd < (VIsual).coladd) )
         {
             at_start_sent = TRUE;
             decl(&pos);
-            while (LT_POS(pos, curwin->w_cursor))
+            while ( (((pos).lnum != (curwin->w_cursor).lnum)               ? (pos).lnum < (curwin->w_cursor).lnum                   : (pos).col != (curwin->w_cursor).col                        ? (pos).col < (curwin->w_cursor).col                     : (pos).coladd < (curwin->w_cursor).coladd) )
             {
                 c = gchar_pos(&pos);
-                if (!VIM_ISWHITE(c))
+                if (! ((c) == ' ' || (c) == '\t') )
                 {
                     at_start_sent = FALSE;
                     break;
@@ -161473,8 +166301,8 @@
             }
             if (!at_start_sent)
             {
-                findsent(BACKWARD, 1L);
-                if (EQUAL_POS(curwin->w_cursor, start_pos))
+                findsent( (-1) , 1L);
+                if ( (((curwin->w_cursor).lnum == (start_pos).lnum) && ((curwin->w_cursor).col == (start_pos).col) && ((curwin->w_cursor).coladd == (start_pos).coladd)) )
                 {
                     at_start_sent = TRUE;
                 }
@@ -161494,9 +166322,9 @@
                     find_first_blank(&curwin->w_cursor);
                 }
                 c = gchar_cursor();
-                if (!at_start_sent || (!include && !VIM_ISWHITE(c)))
+                if (!at_start_sent || (!include && ! ((c) == ' ' || (c) == '\t') ))
                 {
-                    findsent(BACKWARD, 1L);
+                    findsent( (-1) , 1L);
                 }
                 at_start_sent = !at_start_sent;
             }
@@ -161505,13 +166333,13 @@
         {
             incl(&pos);
             at_start_sent = TRUE;
-            if (!EQUAL_POS(pos, curwin->w_cursor))
+            if (! (((pos).lnum == (curwin->w_cursor).lnum) && ((pos).col == (curwin->w_cursor).col) && ((pos).coladd == (curwin->w_cursor).coladd)) )
             {
                 at_start_sent = FALSE;
-                while (LT_POS(pos, curwin->w_cursor))
+                while ( (((pos).lnum != (curwin->w_cursor).lnum)               ? (pos).lnum < (curwin->w_cursor).lnum                   : (pos).col != (curwin->w_cursor).col                        ? (pos).col < (curwin->w_cursor).col                     : (pos).coladd < (curwin->w_cursor).coladd) )
                 {
                     c = gchar_pos(&pos);
-                    if (!VIM_ISWHITE(c))
+                    if (! ((c) == ' ' || (c) == '\t') )
                     {
                         at_start_sent = TRUE;
                         break;
@@ -161520,7 +166348,7 @@
                 }
                 if (at_start_sent)
                 {
-                    findsent(BACKWARD, 1L);
+                    findsent( (-1) , 1L);
                 }
                 else
                 {
@@ -161541,11 +166369,11 @@
         return OK;
     }
 
-    while (c = gchar_pos(&pos), VIM_ISWHITE(c))
+    while (c = gchar_pos(&pos),  ((c) == ' ' || (c) == '\t') )
     {
         incl(&pos);
     }
-    if (EQUAL_POS(pos, curwin->w_cursor))
+    if ( (((pos).lnum == (curwin->w_cursor).lnum) && ((pos).col == (curwin->w_cursor).col) && ((pos).coladd == (curwin->w_cursor).coladd)) )
     {
         start_blank = TRUE;
         find_first_blank(&start_pos);
@@ -161553,7 +166381,7 @@
     else
     {
         start_blank = FALSE;
-        findsent(BACKWARD, 1L);
+        findsent( (-1) , 1L);
         start_pos = curwin->w_cursor;
     }
     if (include)
@@ -161583,12 +166411,12 @@
         {
             find_first_blank(&curwin->w_cursor);
             c = gchar_pos(&curwin->w_cursor);
-            if (VIM_ISWHITE(c))
+            if ( ((c) == ' ' || (c) == '\t') )
             {
                 decl(&curwin->w_cursor);
             }
         }
-        else if (c = gchar_cursor(), !VIM_ISWHITE(c))
+        else if (c = gchar_cursor(), ! ((c) == ' ' || (c) == '\t') )
         {
             find_first_blank(&start_pos);
         }
@@ -161596,7 +166424,7 @@
 
     if (VIsual_active)
     {
-        if (EQUAL_POS(start_pos, curwin->w_cursor))
+        if ( (((start_pos).lnum == (curwin->w_cursor).lnum) && ((start_pos).col == (curwin->w_cursor).col) && ((start_pos).coladd == (curwin->w_cursor).coladd)) )
         {
             goto extend;
         }
@@ -161641,7 +166469,7 @@
     old_end = curwin->w_cursor;
     old_start = old_end;
 
-    if (!VIsual_active || EQUAL_POS(VIsual, curwin->w_cursor))
+    if (!VIsual_active ||  (((VIsual).lnum == (curwin->w_cursor).lnum) && ((VIsual).col == (curwin->w_cursor).col) && ((VIsual).coladd == (curwin->w_cursor).coladd)) )
     {
         setpcmark();
         if (what == '{')
@@ -161659,7 +166487,7 @@
             ++curwin->w_cursor.col;
         }
     }
-    else if (LT_POS(VIsual, curwin->w_cursor))
+    else if ( (((VIsual).lnum != (curwin->w_cursor).lnum)               ? (VIsual).lnum < (curwin->w_cursor).lnum                   : (VIsual).col != (curwin->w_cursor).col                        ? (VIsual).col < (curwin->w_cursor).col                     : (VIsual).coladd < (curwin->w_cursor).coladd) )
     {
         old_start = VIsual;
         curwin->w_cursor = VIsual;
@@ -161718,13 +166546,13 @@
             }
         }
 
-        if (EQUAL_POS(start_pos, *end_pos) && VIsual_active)
+        if ( (((start_pos).lnum == (*end_pos).lnum) && ((start_pos).col == (*end_pos).col) && ((start_pos).coladd == (*end_pos).coladd))  && VIsual_active)
         {
             curwin->w_cursor = old_pos;
             return FAIL;
         }
 
-        if (!LT_POS(start_pos, old_start) && !LT_POS(old_end, curwin->w_cursor) && !EQUAL_POS(start_pos, curwin->w_cursor) && VIsual_active)
+        if (! (((start_pos).lnum != (old_start).lnum)               ? (start_pos).lnum < (old_start).lnum                   : (start_pos).col != (old_start).col                        ? (start_pos).col < (old_start).col                     : (start_pos).coladd < (old_start).coladd)  && ! (((old_end).lnum != (curwin->w_cursor).lnum)               ? (old_end).lnum < (curwin->w_cursor).lnum                   : (old_end).col != (curwin->w_cursor).col                        ? (old_end).col < (curwin->w_cursor).col                     : (old_end).coladd < (curwin->w_cursor).coladd)  && ! (((start_pos).lnum == (curwin->w_cursor).lnum) && ((start_pos).col == (curwin->w_cursor).col) && ((start_pos).coladd == (curwin->w_cursor).coladd))  && VIsual_active)
         {
             curwin->w_cursor = old_start;
             decl(&curwin->w_cursor);
@@ -161772,7 +166600,7 @@
         {
             incl(&curwin->w_cursor);
         }
-        else if (LTOREQ_POS(start_pos, curwin->w_cursor))
+        else if ( ( (((start_pos).lnum != (curwin->w_cursor).lnum)               ? (start_pos).lnum < (curwin->w_cursor).lnum                   : (start_pos).col != (curwin->w_cursor).col                        ? (start_pos).col < (curwin->w_cursor).col                     : (start_pos).coladd < (curwin->w_cursor).coladd)  ||  (((start_pos).lnum == (curwin->w_cursor).lnum) && ((start_pos).col == (curwin->w_cursor).col) && ((start_pos).coladd == (curwin->w_cursor).coladd)) ) )
         {
             oap->inclusive = TRUE;
         }
@@ -161811,7 +166639,7 @@
 extend:
         if (start_lnum < VIsual.lnum)
         {
-            dir = BACKWARD;
+            dir =  (-1) ;
         }
         else
         {
@@ -161819,7 +166647,7 @@
         }
         for (i = count; --i >= 0; )
         {
-            if (start_lnum == (dir == BACKWARD ? 1 : curbuf->b_ml.ml_line_count))
+            if (start_lnum == (dir ==  (-1)  ? 1 : curbuf->b_ml.ml_line_count))
             {
                 retval = FAIL;
                 break;
@@ -161837,7 +166665,7 @@
                 }
                 for (;;)
                 {
-                    if (start_lnum == (dir == BACKWARD ? 1 : curbuf->b_ml.ml_line_count))
+                    if (start_lnum == (dir ==  (-1)  ? 1 : curbuf->b_ml.ml_line_count))
                     {
                         break;
                     }
@@ -161851,7 +166679,7 @@
                 {
                     break;
                 }
-                if (start_lnum == (dir == BACKWARD ? 1 : curbuf->b_ml.ml_line_count))
+                if (start_lnum == (dir ==  (-1)  ? 1 : curbuf->b_ml.ml_line_count))
                 {
                     break;
                 }
@@ -162052,8 +166880,8 @@
             return FALSE;
         }
 
-        vis_bef_curs = LT_POS(VIsual, curwin->w_cursor);
-        vis_empty = EQUAL_POS(VIsual, curwin->w_cursor);
+        vis_bef_curs =  (((VIsual).lnum != (curwin->w_cursor).lnum)               ? (VIsual).lnum < (curwin->w_cursor).lnum                   : (VIsual).col != (curwin->w_cursor).col                        ? (VIsual).col < (curwin->w_cursor).col                     : (VIsual).coladd < (curwin->w_cursor).coladd) ;
+        vis_empty =  (((VIsual).lnum == (curwin->w_cursor).lnum) && ((VIsual).col == (curwin->w_cursor).col) && ((VIsual).coladd == (curwin->w_cursor).coladd)) ;
         if (*p_sel == 'e')
         {
             if (vis_bef_curs)
@@ -162066,7 +166894,7 @@
                 dec(&VIsual);
                 did_exclusive_adj = TRUE;
             }
-            vis_empty = EQUAL_POS(VIsual, curwin->w_cursor);
+            vis_empty =  (((VIsual).lnum == (curwin->w_cursor).lnum) && ((VIsual).col == (curwin->w_cursor).col) && ((VIsual).coladd == (curwin->w_cursor).coladd)) ;
             if (!vis_bef_curs && !vis_empty)
             {
                 pos_T t = curwin->w_cursor;
@@ -162147,7 +166975,6 @@
     }
     else
     {
-
     if (line[col_start] == quotechar || !vis_empty)
     {
         int     first_col = col_start;
@@ -162206,16 +167033,16 @@
 
     if (include)
     {
-        if (VIM_ISWHITE(line[col_end + 1]))
+        if ( ((line[col_end + 1]) == ' ' || (line[col_end + 1]) == '\t') )
         {
-            while (VIM_ISWHITE(line[col_end + 1]))
+            while ( ((line[col_end + 1]) == ' ' || (line[col_end + 1]) == '\t') )
             {
                 ++col_end;
             }
         }
         else
         {
-            while (col_start > 0 && VIM_ISWHITE(line[col_start - 1]))
+            while (col_start > 0 &&  ((line[col_start - 1]) == ' ' || (line[col_start - 1]) == '\t') )
             {
                 --col_start;
             }
@@ -162299,19 +167126,17 @@
 
 static char     tz_cache[64];
 
-#define FOR_ALL_TIMERS(t)     for ((t) = first_timer; (t) != NULL; (t) = (t)->tr_next)
-
     static struct tm *
-vim_localtime(const time_t        *timep, struct tm           *result UNUSED)
+vim_localtime(const time_t        *timep, struct tm           *result  __attribute__((unused)) )
 {
     char                *tz;
 
-    tz = (char *)mch_getenv((char_u *)"TZ");
+    tz = (char *) (char_u *)getenv((char *)((char_u *)"TZ")) ;
     if (tz == NULL)
     {
         tz = "";
     }
-    if (STRNCMP(tz_cache, tz, sizeof(tz_cache) - 1) != 0)
+    if ( strncmp((char *)(tz_cache), (char *)(tz), (sizeof(tz_cache) - 1))  != 0)
     {
         tzset();
         vim_strncpy((char_u *)tz_cache, (char_u *)tz, sizeof(tz_cache) - 1);
@@ -162346,7 +167171,7 @@
     }
     if (add_newline)
     {
-        STRCAT(buf, "\n");
+         strcat((char *)(buf), (char *)("\n")) ;
     }
     return buf;
 }
@@ -162385,7 +167210,7 @@
 // ==================== ui.c ====================
 
     void
-ui_write(char_u *s, int len, int console UNUSED)
+ui_write(char_u *s, int len, int console  __attribute__((unused)) )
 {
     if (!(silent_mode && p_verbose == 0))
     {
@@ -162436,13 +167261,13 @@
 
     if (ta_str != NULL)
     {
-        mch_memmove(new, ta_str + ta_off, (size_t)(ta_len - ta_off));
-        mch_memmove(new + ta_len - ta_off, s, (size_t)len);
+         memmove((char *)(new), (char *)(ta_str + ta_off), (size_t)(ta_len - ta_off)) ;
+         memmove((char *)(new + ta_len - ta_off), (char *)(s), (size_t)len) ;
         vim_free(ta_str);
     }
     else
     {
-        mch_memmove(new, s, (size_t)len);
+         memmove((char *)(new), (char *)(s), (size_t)len) ;
     }
     ta_str = new;
     ta_len = newlen;
@@ -162456,7 +167281,7 @@
 
     if (wtime == -1 || wtime > 100L)
     {
-        (void)vim_handle_signal(SIGNAL_UNBLOCK);
+        (void)vim_handle_signal( (-2) );
 
         if ((mapped_ctrl_c | curbuf->b_mapped_ctrl_c) & get_real_state())
         {
@@ -162468,7 +167293,7 @@
 
     if (wtime == -1 || wtime > 100L)
     {
-        (void)vim_handle_signal(SIGNAL_BLOCK);
+        (void)vim_handle_signal( (-1) );
     }
 
     ctrl_c_interrupts = TRUE;
@@ -162487,7 +167312,7 @@
     long        elapsed_time = 0;
     elapsed_T   start_tv;
 
-    ELAPSED_INIT(start_tv);
+     gettimeofday(&(start_tv), NULL) ;
 
     for (;;)
     {
@@ -162508,7 +167333,7 @@
         }
         else
         {
-            elapsed_time = ELAPSED_FUNC(start_tv);
+            elapsed_time =  elapsed(&(start_tv)) ;
             if (wtime >= 0)
             {
                 wait_time = wtime - elapsed_time;
@@ -162552,7 +167377,7 @@
                     }
                     else
                     {
-                        buf[0] = K_SPECIAL;
+                        buf[0] =  (0x80) ;
                         buf[1] = KS_EXTRA;
                         buf[2] = (int)KE_COMPLETE_DELAY;
                     }
@@ -162573,7 +167398,7 @@
                     }
                     else
                     {
-                        buf[0] = K_SPECIAL;
+                        buf[0] =  (0x80) ;
                         buf[1] = KS_EXTRA;
                         buf[2] = (int)KE_CURSORHOLD;
                     }
@@ -162654,7 +167479,7 @@
 }
 
     void
-ui_set_shellsize(int         mustset UNUSED)
+ui_set_shellsize(int         mustset  __attribute__((unused)) )
 {
         mch_set_shellsize();
 }
@@ -162702,7 +167527,7 @@
     recursive = FALSE;
 }
 
-#  define INBUFLEN 250
+enum { INBUFLEN = 250 };
 
 static char_u   inbuf[INBUFLEN + MAX_KEY_CODE_LEN];
 static int      inbufcount = 0;
@@ -162724,13 +167549,13 @@
 {
     garray_T    *gap;
 
-    gap = ALLOC_ONE(garray_T);
+    gap =  (garray_T *)alloc(sizeof(garray_T)) ;
     if (gap != NULL)
     {
         gap->ga_data = alloc(inbufcount + 1);
         if (gap->ga_data != NULL)
         {
-            mch_memmove(gap->ga_data, inbuf, (size_t)inbufcount);
+             memmove((char *)(gap->ga_data), (char *)(inbuf), (size_t)inbufcount) ;
         }
         gap->ga_len = inbufcount;
     }
@@ -162752,13 +167577,13 @@
     {
         if (overwrite || inbufcount + gap->ga_len >= INBUFLEN)
         {
-            mch_memmove(inbuf, gap->ga_data, gap->ga_len);
+             memmove((char *)(inbuf), (char *)(gap->ga_data), gap->ga_len) ;
             inbufcount = gap->ga_len;
         }
         else
         {
-            mch_memmove(inbuf + gap->ga_len, inbuf, inbufcount);
-            mch_memmove(inbuf, gap->ga_data, gap->ga_len);
+             memmove((char *)(inbuf + gap->ga_len), (char *)(inbuf), inbufcount) ;
+             memmove((char *)(inbuf), (char *)(gap->ga_data), gap->ga_len) ;
             inbufcount += gap->ga_len;
         }
         vim_free(gap->ga_data);
@@ -162780,7 +167605,6 @@
     }
 }
 
-
     void
 trash_input_buf(void)
 {
@@ -162798,17 +167622,17 @@
     {
         maxlen = inbufcount;
     }
-    mch_memmove(buf, inbuf, (size_t)maxlen);
+     memmove((char *)(buf), (char *)(inbuf), (size_t)maxlen) ;
     inbufcount -= maxlen;
     if (inbufcount > 0 && maxlen > 0)
     {
-        mch_memmove(inbuf, inbuf + maxlen, (size_t)inbufcount);
+         memmove((char *)(inbuf), (char *)(inbuf + maxlen), (size_t)inbufcount) ;
     }
     return (int)maxlen;
 }
 
     void
-fill_input_buf(int exit_on_error UNUSED)
+fill_input_buf(int exit_on_error  __attribute__((unused)) )
 {
     int         len;
     int         try;
@@ -162831,15 +167655,16 @@
         {
             unconverted = restlen;
         }
-        mch_memmove(inbuf + inbufcount, rest, unconverted);
+         memmove((char *)(inbuf + inbufcount), (char *)(rest), unconverted) ;
         if (unconverted == restlen)
         {
-            VIM_CLEAR(rest);
+             vim_free(rest);
+             (rest) = NULL;
         }
         else
         {
             restlen -= unconverted;
-            mch_memmove(rest, rest + unconverted, restlen);
+             memmove((char *)(rest), (char *)(rest + unconverted), restlen) ;
         }
         inbufcount += unconverted;
     }
@@ -162894,9 +167719,9 @@
         }
         while (len > 0)
         {
-            if (ctrl_c_interrupts && ((inbuf[inbufcount] == Ctrl_C && !key_protocol_enabled()) || (len >= 10 && STRNCMP(inbuf + inbufcount, "\033[27;5;99~", 10) == 0) || (len >= 10 && STRNCMP(inbuf + inbufcount, "\033[27;5;67~", 10) == 0) || (len >= 7 && STRNCMP(inbuf + inbufcount, "\033[99;5u", 7) == 0) || (len >= 7 && STRNCMP(inbuf + inbufcount, "\033[67;5u", 7) == 0)))
+            if (ctrl_c_interrupts && ((inbuf[inbufcount] == Ctrl_C && !key_protocol_enabled()) || (len >= 10 &&  strncmp((char *)(inbuf + inbufcount), (char *)("\033[27;5;99~"), (10))  == 0) || (len >= 10 &&  strncmp((char *)(inbuf + inbufcount), (char *)("\033[27;5;67~"), (10))  == 0) || (len >= 7 &&  strncmp((char *)(inbuf + inbufcount), (char *)("\033[99;5u"), (7))  == 0) || (len >= 7 &&  strncmp((char *)(inbuf + inbufcount), (char *)("\033[67;5u"), (7))  == 0)))
             {
-                mch_memmove(inbuf, inbuf + inbufcount, (size_t)(len));
+                 memmove((char *)(inbuf), (char *)(inbuf + inbufcount), (size_t)(len)) ;
                 inbufcount = 0;
                 got_int = TRUE;
             }
@@ -162913,7 +167738,7 @@
     {
         getout(0);
     }
-    STRCPY(IObuff, _("Vim: Error reading input, exiting...\n"));
+     strcpy((char *)(IObuff), (char *)(_("Vim: Error reading input, exiting...\n"))) ;
     preserve_exit();
 }
 
@@ -162956,7 +167781,7 @@
         for (;;)
         {
             int     w = chartabsize(p, col);
-            MB_PTR_ADV(p);
+             p += (*mb_ptr2len)(p) ;
             if (*p == NUL)
             {
                 break;
@@ -163027,11 +167852,6 @@
 
 // ==================== undo.c ====================
 
-#define UH_MAGIC 0x18dade
-#define UE_MAGIC 0xabc123
-
-#define CRYPT_BUF_SIZE 8192
-
 static void u_unch_branch(u_header_T *uhp);
 static u_entry_T *u_get_headentry(void);
 static void u_getbot(void);
@@ -163045,8 +167865,6 @@
 static void u_saveline(linenr_T lnum);
 static void u_blockfree(buf_T *buf);
 
-#define U_ALLOC_LINE(size) lalloc(size, FALSE)
-
 static long u_newcount;
 static long u_oldcount;
 
@@ -163135,7 +167953,7 @@
     static long
 get_undolevel(void)
 {
-    if (curbuf->b_p_ul == NO_LOCAL_UNDOLEVEL)
+    if (curbuf->b_p_ul ==  (-123456) )
     {
         return p_ul;
     }
@@ -163195,7 +168013,7 @@
 
         if (get_undolevel() >= 0)
         {
-            uhp = U_ALLOC_LINE(sizeof(u_header_T));
+            uhp =  lalloc(sizeof(u_header_T), FALSE) ;
             if (uhp == NULL)
             {
                 goto nomem;
@@ -163292,7 +168110,7 @@
         uhp->uh_flags = (curbuf->b_changed ? UH_CHANGED : 0) +
                        ((curbuf->b_ml.ml_flags & ML_EMPTY) ? UH_EMPTYBUF : 0);
 
-        mch_memmove(uhp->uh_namedm, curbuf->b_namedm, sizeof(pos_T) * NMARKS);
+         memmove((char *)(uhp->uh_namedm), (char *)(curbuf->b_namedm), sizeof(pos_T) *  ('z' - 'a' + 1) ) ;
         uhp->uh_visual = curbuf->b_visual;
 
         curbuf->b_u_newhead = uhp;
@@ -163360,12 +168178,12 @@
         u_getbot();
     }
 
-    uep = U_ALLOC_LINE(sizeof(u_entry_T));
+    uep =  lalloc(sizeof(u_entry_T), FALSE) ;
     if (uep == NULL)
     {
         goto nomem;
     }
-    CLEAR_POINTER(uep);
+      memset(((uep)), (0), (sizeof(*(uep))))  ;
 
     uep->ue_size = size;
     uep->ue_top = top;
@@ -163385,7 +168203,7 @@
 
     if (size > 0)
     {
-        if ((uep->ue_array = U_ALLOC_LINE(sizeof(undoline_T) * size)) == NULL)
+        if ((uep->ue_array =  lalloc(sizeof(undoline_T) * size, FALSE) ) == NULL)
         {
             u_freeentry(uep, 0L);
             goto nomem;
@@ -163896,7 +168714,7 @@
     linenr_T top;
     linenr_T bot;
     linenr_T    lnum;
-    linenr_T    newlnum = MAXLNUM;
+    linenr_T    newlnum =  LONG_MAX ;
     pos_T       new_curpos = curwin->w_cursor;
     long        i;
     u_entry_T *uep;
@@ -163904,7 +168722,7 @@
     u_entry_T   *newlist = NULL;
     int         old_flags;
     int         new_flags;
-    pos_T       namedm[NMARKS];
+    pos_T       namedm[ ('z' - 'a' + 1) ];
     visualinfo_T visualinfo;
     int         empty_buffer;
     u_header_T  *curhead = curbuf->b_u_curhead;
@@ -163916,7 +168734,7 @@
                ((curbuf->b_ml.ml_flags & ML_EMPTY) ? UH_EMPTYBUF : 0);
     setpcmark();
 
-    mch_memmove(namedm, curbuf->b_namedm, sizeof(pos_T) * NMARKS);
+     memmove((char *)(namedm), (char *)(curbuf->b_namedm), sizeof(pos_T) *  ('z' - 'a' + 1) ) ;
     visualinfo = curbuf->b_visual;
     curbuf->b_op_start.lnum = curbuf->b_ml.ml_line_count;
     curbuf->b_op_start.col = 0;
@@ -163961,7 +168779,7 @@
                         break;
                     }
                 }
-                if (i == newsize && newlnum == MAXLNUM && uep->ue_next == NULL)
+                if (i == newsize && newlnum ==  LONG_MAX  && uep->ue_next == NULL)
                 {
                     newlnum = top;
                     new_curpos.lnum = newlnum + 1;
@@ -163978,7 +168796,7 @@
 
         if (oldsize > 0)
         {
-            if ((newarray = U_ALLOC_LINE(sizeof(undoline_T) * oldsize)) == NULL)
+            if ((newarray =  lalloc(sizeof(undoline_T) * oldsize, FALSE) ) == NULL)
             {
                 do_outofmem_msg((long_u)(sizeof(undoline_T) * oldsize));
 
@@ -164031,7 +168849,7 @@
 
         if (oldsize != newsize)
         {
-            mark_adjust(top + 1, top + oldsize, (long)MAXLNUM, (long)newsize - (long)oldsize);
+            mark_adjust(top + 1, top + oldsize, (long) LONG_MAX , (long)newsize - (long)oldsize);
             if (curbuf->b_op_start.lnum > top + oldsize)
             {
                 curbuf->b_op_start.lnum += newsize - oldsize;
@@ -164084,7 +168902,7 @@
 
     curhead->uh_entry = newlist;
     curhead->uh_flags = new_flags;
-    if ((old_flags & UH_EMPTYBUF) && BUFEMPTY())
+    if ((old_flags & UH_EMPTYBUF) &&  (curbuf->b_ml.ml_line_count == 1 && *ml_get((linenr_T)1) == NUL) )
     {
         curbuf->b_ml.ml_flags |= ML_EMPTY;
     }
@@ -164097,7 +168915,7 @@
         unchanged(curbuf, FALSE, TRUE);
     }
 
-    for (i = 0; i < NMARKS; ++i)
+    for (i = 0; i <  ('z' - 'a' + 1) ; ++i)
     {
         if (curhead->uh_namedm[i].lnum != 0)
         {
@@ -164199,30 +169017,30 @@
     u_oldcount -= u_newcount;
     if (u_oldcount == -1)
     {
-        msgstr = N_("more line");
+        msgstr =  "more line" ;
     }
     else if (u_oldcount < 0)
     {
-        msgstr = N_("more lines");
+        msgstr =  "more lines" ;
     }
     else if (u_oldcount == 1)
     {
-        msgstr = N_("line less");
+        msgstr =  "line less" ;
     }
     else if (u_oldcount > 1)
     {
-        msgstr = N_("fewer lines");
+        msgstr =  "fewer lines" ;
     }
     else
     {
         u_oldcount = u_newcount;
         if (u_newcount == 1)
         {
-            msgstr = N_("change");
+            msgstr =  "change" ;
         }
         else
         {
-            msgstr = N_("changes");
+            msgstr =  "changes" ;
         }
     }
 
@@ -164283,7 +169101,7 @@
 }
 
     void
-ex_undolist(exarg_T *eap UNUSED)
+ex_undolist(exarg_T *eap  __attribute__((unused)) )
 {
     garray_T    ga;
     u_header_T  *uhp;
@@ -164305,15 +169123,15 @@
             {
                 break;
             }
-            len = vim_snprintf((char *)IObuff, IOSIZE, "%6ld %7d  ", uhp->uh_seq, changes);
-            add_time(IObuff + len, IOSIZE - len, uhp->uh_time);
+            len = vim_snprintf((char *)IObuff,  (1024+1) , "%6ld %7d  ", uhp->uh_seq, changes);
+            add_time(IObuff + len,  (1024+1)  - len, uhp->uh_time);
 
-            len += (int)STRLEN(IObuff + len);
+            len += (int) strlen((char *)(IObuff + len)) ;
             if (uhp->uh_save_nr > 0)
             {
                 int n = (len >= 33) ? 0 : 33 - len;
 
-                len += vim_snprintf((char *)IObuff + len, IOSIZE - len, "%*.*s  %3ld", n, n, " ", uhp->uh_save_nr);
+                len += vim_snprintf((char *)IObuff + len,  (1024+1)  - len, "%*.*s  %3ld", n, n, " ", uhp->uh_save_nr);
             }
             ((char_u **)(ga.ga_data))[ga.ga_len++] = vim_strnsave(IObuff, len);
         }
@@ -164363,7 +169181,7 @@
         sort_strings((char_u **)ga.ga_data, ga.ga_len);
 
         msg_start();
-        msg_puts_attr(_("number changes  when               saved"), HL_ATTR(HLF_T));
+        msg_puts_attr(_("number changes  when               saved"),  highlight_attr[(int)(HLF_T)] );
         for (i = 0; i < ga.ga_len && !got_int; ++i)
         {
             msg_putchar('\n');
@@ -164380,7 +169198,7 @@
 }
 
     void
-ex_undojoin(exarg_T *eap UNUSED)
+ex_undojoin(exarg_T *eap  __attribute__((unused)) )
 {
     if (curbuf->b_u_newhead == NULL)
     {
@@ -164436,14 +169254,18 @@
 
         if (uep->ue_array[lnum - 1].ul_len != curbuf->b_ml.ml_line_len || memcmp(p, uep->ue_array[lnum - 1].ul_line, uep->ue_array[lnum - 1].ul_len) != 0)
         {
-            CLEAR_POS(&(uhp->uh_cursor));
+             (&(uhp->uh_cursor))->lnum = 0;
+             (&(uhp->uh_cursor))->col = 0;
+             (&(uhp->uh_cursor))->coladd = 0;
             uhp->uh_cursor.lnum = lnum;
             return;
         }
     }
     if (curbuf->b_ml.ml_line_count != uep->ue_size)
     {
-        CLEAR_POS(&(uhp->uh_cursor));
+         (&(uhp->uh_cursor))->lnum = 0;
+         (&(uhp->uh_cursor))->col = 0;
+         (&(uhp->uh_cursor))->coladd = 0;
         uhp->uh_cursor.lnum = lnum;
     }
 }
@@ -164701,7 +169523,8 @@
         return;
     }
 
-    VIM_CLEAR(curbuf->b_u_line_ptr.ul_line);
+     vim_free(curbuf->b_u_line_ptr.ul_line);
+     (curbuf->b_u_line_ptr.ul_line) = NULL;
     curbuf->b_u_line_ptr.ul_len = 0;
     curbuf->b_u_line_ptr.ul_textlen = 0;
     curbuf->b_u_line_lnum = 0;
@@ -164758,11 +169581,13 @@
 {
     buf_T *buf;
 
-    FOR_ALL_BUFFERS(buf)
+     for ((buf) = firstbuf; (buf) != NULL; (buf) = (buf)->b_next) 
+     {
         if (bufIsChanged(buf))
         {
             return TRUE;
         }
+     }
     return FALSE;
 }
 
@@ -164799,48 +169624,45 @@
 
 static int ucmd_locked = 0;
 
-#define USER_CMD(i) (&((ucmd_T *)(ucmds.ga_data))[i])
-#define USER_CMD_GA(gap, i) (&((ucmd_T *)((gap)->ga_data))[i])
-
 static keyvalue_T command_complete_tab[] =
 {
-    KEYVALUE_ENTRY(EXPAND_ARGLIST, "arglist"),
-    KEYVALUE_ENTRY(EXPAND_AUGROUP, "augroup"),
-    KEYVALUE_ENTRY(EXPAND_BEHAVE, "behave"),
-    KEYVALUE_ENTRY(EXPAND_BUFFERS, "buffer"),
-    KEYVALUE_ENTRY(EXPAND_COLORS, "color"),
-    KEYVALUE_ENTRY(EXPAND_COMMANDS, "command"),
-    KEYVALUE_ENTRY(EXPAND_COMPILER, "compiler"),
-    KEYVALUE_ENTRY(EXPAND_DIFF_BUFFERS, "diff_buffer"),
-    KEYVALUE_ENTRY(EXPAND_DIRECTORIES, "dir"),
-    KEYVALUE_ENTRY(EXPAND_DIRS_IN_CDPATH, "dir_in_path"),
-    KEYVALUE_ENTRY(EXPAND_ENV_VARS, "environment"),
-    KEYVALUE_ENTRY(EXPAND_EVENTS, "event"),
-    KEYVALUE_ENTRY(EXPAND_EXPRESSION, "expression"),
-    KEYVALUE_ENTRY(EXPAND_FILES, "file"),
-    KEYVALUE_ENTRY(EXPAND_FILES_IN_PATH, "file_in_path"),
-    KEYVALUE_ENTRY(EXPAND_FILETYPE, "filetype"),
-    KEYVALUE_ENTRY(EXPAND_FILETYPECMD, "filetypecmd"),
-    KEYVALUE_ENTRY(EXPAND_FUNCTIONS, "function"),
-    KEYVALUE_ENTRY(EXPAND_HELP, "help"),
-    KEYVALUE_ENTRY(EXPAND_HIGHLIGHT, "highlight"),
-    KEYVALUE_ENTRY(EXPAND_HISTORY, "history"),
-    KEYVALUE_ENTRY(EXPAND_LOCALES, "locale"),
-    KEYVALUE_ENTRY(EXPAND_MAPCLEAR, "mapclear"),
-    KEYVALUE_ENTRY(EXPAND_MAPPINGS, "mapping"),
-    KEYVALUE_ENTRY(EXPAND_MENUS, "menu"),
-    KEYVALUE_ENTRY(EXPAND_MESSAGES, "messages"),
-    KEYVALUE_ENTRY(EXPAND_SETTINGS, "option"),
-    KEYVALUE_ENTRY(EXPAND_PACKADD, "packadd"),
-    KEYVALUE_ENTRY(EXPAND_RETAB, "retab"),
-    KEYVALUE_ENTRY(EXPAND_RUNTIME, "runtime"),
-    KEYVALUE_ENTRY(EXPAND_SHELLCMD, "shellcmd"),
-    KEYVALUE_ENTRY(EXPAND_SHELLCMDLINE, "shellcmdline"),
-    KEYVALUE_ENTRY(EXPAND_OWNSYNTAX, "syntax"),
-    KEYVALUE_ENTRY(EXPAND_TAGS, "tag"),
-    KEYVALUE_ENTRY(EXPAND_TAGS_LISTFILES, "tag_listfiles"),
-    KEYVALUE_ENTRY(EXPAND_USER, "user"),
-    KEYVALUE_ENTRY(EXPAND_USER_VARS, "var")
+     {(EXPAND_ARGLIST), {((char_u *)"arglist"),  (sizeof("arglist" "") - 1) }} ,
+     {(EXPAND_AUGROUP), {((char_u *)"augroup"),  (sizeof("augroup" "") - 1) }} ,
+     {(EXPAND_BEHAVE), {((char_u *)"behave"),  (sizeof("behave" "") - 1) }} ,
+     {(EXPAND_BUFFERS), {((char_u *)"buffer"),  (sizeof("buffer" "") - 1) }} ,
+     {(EXPAND_COLORS), {((char_u *)"color"),  (sizeof("color" "") - 1) }} ,
+     {(EXPAND_COMMANDS), {((char_u *)"command"),  (sizeof("command" "") - 1) }} ,
+     {(EXPAND_COMPILER), {((char_u *)"compiler"),  (sizeof("compiler" "") - 1) }} ,
+     {(EXPAND_DIFF_BUFFERS), {((char_u *)"diff_buffer"),  (sizeof("diff_buffer" "") - 1) }} ,
+     {(EXPAND_DIRECTORIES), {((char_u *)"dir"),  (sizeof("dir" "") - 1) }} ,
+     {(EXPAND_DIRS_IN_CDPATH), {((char_u *)"dir_in_path"),  (sizeof("dir_in_path" "") - 1) }} ,
+     {(EXPAND_ENV_VARS), {((char_u *)"environment"),  (sizeof("environment" "") - 1) }} ,
+     {(EXPAND_EVENTS), {((char_u *)"event"),  (sizeof("event" "") - 1) }} ,
+     {(EXPAND_EXPRESSION), {((char_u *)"expression"),  (sizeof("expression" "") - 1) }} ,
+     {(EXPAND_FILES), {((char_u *)"file"),  (sizeof("file" "") - 1) }} ,
+     {(EXPAND_FILES_IN_PATH), {((char_u *)"file_in_path"),  (sizeof("file_in_path" "") - 1) }} ,
+     {(EXPAND_FILETYPE), {((char_u *)"filetype"),  (sizeof("filetype" "") - 1) }} ,
+     {(EXPAND_FILETYPECMD), {((char_u *)"filetypecmd"),  (sizeof("filetypecmd" "") - 1) }} ,
+     {(EXPAND_FUNCTIONS), {((char_u *)"function"),  (sizeof("function" "") - 1) }} ,
+     {(EXPAND_HELP), {((char_u *)"help"),  (sizeof("help" "") - 1) }} ,
+     {(EXPAND_HIGHLIGHT), {((char_u *)"highlight"),  (sizeof("highlight" "") - 1) }} ,
+     {(EXPAND_HISTORY), {((char_u *)"history"),  (sizeof("history" "") - 1) }} ,
+     {(EXPAND_LOCALES), {((char_u *)"locale"),  (sizeof("locale" "") - 1) }} ,
+     {(EXPAND_MAPCLEAR), {((char_u *)"mapclear"),  (sizeof("mapclear" "") - 1) }} ,
+     {(EXPAND_MAPPINGS), {((char_u *)"mapping"),  (sizeof("mapping" "") - 1) }} ,
+     {(EXPAND_MENUS), {((char_u *)"menu"),  (sizeof("menu" "") - 1) }} ,
+     {(EXPAND_MESSAGES), {((char_u *)"messages"),  (sizeof("messages" "") - 1) }} ,
+     {(EXPAND_SETTINGS), {((char_u *)"option"),  (sizeof("option" "") - 1) }} ,
+     {(EXPAND_PACKADD), {((char_u *)"packadd"),  (sizeof("packadd" "") - 1) }} ,
+     {(EXPAND_RETAB), {((char_u *)"retab"),  (sizeof("retab" "") - 1) }} ,
+     {(EXPAND_RUNTIME), {((char_u *)"runtime"),  (sizeof("runtime" "") - 1) }} ,
+     {(EXPAND_SHELLCMD), {((char_u *)"shellcmd"),  (sizeof("shellcmd" "") - 1) }} ,
+     {(EXPAND_SHELLCMDLINE), {((char_u *)"shellcmdline"),  (sizeof("shellcmdline" "") - 1) }} ,
+     {(EXPAND_OWNSYNTAX), {((char_u *)"syntax"),  (sizeof("syntax" "") - 1) }} ,
+     {(EXPAND_TAGS), {((char_u *)"tag"),  (sizeof("tag" "") - 1) }} ,
+     {(EXPAND_TAGS_LISTFILES), {((char_u *)"tag_listfiles"),  (sizeof("tag_listfiles" "") - 1) }} ,
+     {(EXPAND_USER), {((char_u *)"user"),  (sizeof("user" "") - 1) }} ,
+     {(EXPAND_USER_VARS), {((char_u *)"var"),  (sizeof("var" "") - 1) }} 
 };
 
 typedef struct
@@ -164852,17 +169674,16 @@
     size_t shortnamelen;
 } addrtype_T;
 
-#define ADDRTYPE_ENTRY(k, fn, sn)       {(k), (fn), STRLEN_LITERAL(fn), (sn), STRLEN_LITERAL(sn)}
 static addrtype_T addr_type_complete_tab[] =
 {
-    ADDRTYPE_ENTRY(ADDR_ARGUMENTS, "arguments", "arg"),
-    ADDRTYPE_ENTRY(ADDR_BUFFERS, "buffers", "buf"),
-    ADDRTYPE_ENTRY(ADDR_LINES, "lines", "line"),
-    ADDRTYPE_ENTRY(ADDR_LOADED_BUFFERS, "loaded_buffers", "load"),
-    ADDRTYPE_ENTRY(ADDR_OTHER, "other", "?"),
-    ADDRTYPE_ENTRY(ADDR_QUICKFIX, "quickfix", "qf"),
-    ADDRTYPE_ENTRY(ADDR_TABS, "tabs", "tab"),
-    ADDRTYPE_ENTRY(ADDR_WINDOWS, "windows", "win")
+     {(ADDR_ARGUMENTS), ("arguments"),  (sizeof("arguments" "") - 1) , ("arg"),  (sizeof("arg" "") - 1) } ,
+     {(ADDR_BUFFERS), ("buffers"),  (sizeof("buffers" "") - 1) , ("buf"),  (sizeof("buf" "") - 1) } ,
+     {(ADDR_LINES), ("lines"),  (sizeof("lines" "") - 1) , ("line"),  (sizeof("line" "") - 1) } ,
+     {(ADDR_LOADED_BUFFERS), ("loaded_buffers"),  (sizeof("loaded_buffers" "") - 1) , ("load"),  (sizeof("load" "") - 1) } ,
+     {(ADDR_OTHER), ("other"),  (sizeof("other" "") - 1) , ("?"),  (sizeof("?" "") - 1) } ,
+     {(ADDR_QUICKFIX), ("quickfix"),  (sizeof("quickfix" "") - 1) , ("qf"),  (sizeof("qf" "") - 1) } ,
+     {(ADDR_TABS), ("tabs"),  (sizeof("tabs" "") - 1) , ("tab"),  (sizeof("tab" "") - 1) } ,
+     {(ADDR_WINDOWS), ("windows"),  (sizeof("windows" "") - 1) , ("win"),  (sizeof("win" "") - 1) } 
 };
 
 static int cmp_addr_type(const void *a, const void *b);
@@ -164887,7 +169708,7 @@
     {
         for (j = 0; j < gap->ga_len; ++j)
         {
-            uc = USER_CMD_GA(gap, j);
+            uc =  (&((ucmd_T *)((gap)->ga_data))[j]) ;
             cp = eap->cmd;
             np = uc->uc_name;
             k = 0;
@@ -164958,7 +169779,7 @@
     {
         if (xp != NULL)
         {
-            xp->xp_context = EXPAND_UNSUCCESSFUL;
+            xp->xp_context =  (-2) ;
         }
         return NULL;
     }
@@ -164990,12 +169811,12 @@
                 return NULL;
             }
 
-            if (STRNICMP(arg, "complete", p - arg) == 0)
+            if ( strncasecmp((char *)(arg), (char *)("complete"), (p - arg))  == 0)
             {
                 xp->xp_context = EXPAND_USER_COMPLETE;
                 xp->xp_pattern = p + 1;
             }
-            else if (STRNICMP(arg, "completeopt", p - arg) == 0)
+            else if ( strncasecmp((char *)(arg), (char *)("completeopt"), (p - arg))  == 0)
             {
                 xp->xp_context = EXPAND_USER_COMPLETEOPT;
                 xp->xp_pattern = p + 1;
@@ -165007,12 +169828,12 @@
                     }
                 }
             }
-            else if (STRNICMP(arg, "nargs", p - arg) == 0)
+            else if ( strncasecmp((char *)(arg), (char *)("nargs"), (p - arg))  == 0)
             {
                 xp->xp_context = EXPAND_USER_NARGS;
                 xp->xp_pattern = p + 1;
             }
-            else if (STRNICMP(arg, "addr", p - arg) == 0)
+            else if ( strncasecmp((char *)(arg), (char *)("addr"), (p - arg))  == 0)
             {
                 xp->xp_context = EXPAND_USER_ADDR_TYPE;
                 xp->xp_pattern = p + 1;
@@ -165034,7 +169855,7 @@
 }
 
     char_u *
-set_context_in_user_cmdarg(char_u          *cmd UNUSED, char_u          *arg, long            argt, int             context, expand_T        *xp, int             forceit)
+set_context_in_user_cmdarg(char_u          *cmd  __attribute__((unused)) , char_u          *arg, long            argt, int             context, expand_T        *xp, int             forceit)
 {
     char_u      *p;
 
@@ -165069,7 +169890,7 @@
             {
                 ++p;
             }
-            MB_PTR_ADV(p);
+             p += (*mb_ptr2len)(p) ;
         }
     }
     xp->xp_pattern = arg;
@@ -165085,24 +169906,24 @@
 }
 
     char_u *
-get_user_commands(expand_T *xp UNUSED, int idx)
+get_user_commands(expand_T *xp  __attribute__((unused)) , int idx)
 {
     buf_T *buf = prevwin_curwin()->w_buffer;
 
     if (idx < buf->b_ucmds.ga_len)
     {
-        return USER_CMD_GA(&buf->b_ucmds, idx)->uc_name;
+        return  (&((ucmd_T *)((&buf->b_ucmds)->ga_data))[idx]) ->uc_name;
     }
 
     idx -= buf->b_ucmds.ga_len;
     if (idx < ucmds.ga_len)
     {
         int     i;
-        char_u  *name = USER_CMD(idx)->uc_name;
+        char_u  *name =  (&((ucmd_T *)(ucmds.ga_data))[idx]) ->uc_name;
 
         for (i = 0; i < buf->b_ucmds.ga_len; ++i)
         {
-            if (STRCMP(name, USER_CMD_GA(&buf->b_ucmds, i)->uc_name) == 0)
+            if ( strcmp((char *)(name), (char *)( (&((ucmd_T *)((&buf->b_ucmds)->ga_data))[i]) ->uc_name))  == 0)
             {
                 return (char_u *)"";
             }
@@ -165113,9 +169934,9 @@
 }
 
     char_u *
-get_user_cmd_addr_type(expand_T *xp UNUSED, int idx)
+get_user_cmd_addr_type(expand_T *xp  __attribute__((unused)) , int idx)
 {
-    if (idx < 0 || idx >= (int)ARRAY_LENGTH(addr_type_complete_tab))
+    if (idx < 0 || idx >= (int) (sizeof(addr_type_complete_tab) / sizeof((addr_type_complete_tab)[0])) )
     {
         return NULL;
     }
@@ -165123,14 +169944,14 @@
 }
 
     char_u *
-get_user_cmd_flags(expand_T *xp UNUSED, int idx)
+get_user_cmd_flags(expand_T *xp  __attribute__((unused)) , int idx)
 {
     static char *user_cmd_flags[] = {
         "addr", "bang", "bar", "buffer", "complete",
         "completeopt", "count", "nargs", "range", "register", "keepscript"
     };
 
-    if (idx < 0 || idx >= (int)ARRAY_LENGTH(user_cmd_flags))
+    if (idx < 0 || idx >= (int) (sizeof(user_cmd_flags) / sizeof((user_cmd_flags)[0])) )
     {
         return NULL;
     }
@@ -165138,11 +169959,11 @@
 }
 
     char_u *
-get_user_cmd_nargs(expand_T *xp UNUSED, int idx)
+get_user_cmd_nargs(expand_T *xp  __attribute__((unused)) , int idx)
 {
     static char *user_cmd_nargs[] = {"0", "1", "_", "*", "?", "+"};
 
-    if (idx < 0 || idx >= (int)ARRAY_LENGTH(user_cmd_nargs))
+    if (idx < 0 || idx >= (int) (sizeof(user_cmd_nargs) / sizeof((user_cmd_nargs)[0])) )
     {
         return NULL;
     }
@@ -165150,9 +169971,9 @@
 }
 
     char_u *
-get_user_cmd_complete(expand_T *xp UNUSED, int idx)
+get_user_cmd_complete(expand_T *xp  __attribute__((unused)) , int idx)
 {
-    if (idx < 0 || idx >= (int)ARRAY_LENGTH(command_complete_tab))
+    if (idx < 0 || idx >= (int) (sizeof(command_complete_tab) / sizeof((command_complete_tab)[0])) )
     {
         return NULL;
     }
@@ -165164,9 +169985,9 @@
 };
 
     char_u *
-get_user_cmd_completeopt(expand_T *xp UNUSED, int idx)
+get_user_cmd_completeopt(expand_T *xp  __attribute__((unused)) , int idx)
 {
-    if (idx < 0 || idx >= (int)ARRAY_LENGTH(user_cmd_completeopt_tab))
+    if (idx < 0 || idx >= (int) (sizeof(user_cmd_completeopt_tab) / sizeof((user_cmd_completeopt_tab)[0])) )
     {
         return NULL;
     }
@@ -165178,7 +169999,7 @@
 {
     int i;
 
-    for (i = 0; i < (int)ARRAY_LENGTH(command_complete_tab); ++i)
+    for (i = 0; i < (int) (sizeof(command_complete_tab) / sizeof((command_complete_tab)[0])) ; ++i)
     {
         if (command_complete_tab[i].key == expand)
         {
@@ -165209,10 +170030,10 @@
     {
         for (i = 0; i < gap->ga_len; ++i)
         {
-            cmd = USER_CMD_GA(gap, i);
+            cmd =  (&((ucmd_T *)((gap)->ga_data))[i]) ;
             a = (long)cmd->uc_argt;
 
-            if (STRNCMP(name, cmd->uc_name, name_len) != 0 || message_filtered(cmd->uc_name))
+            if ( strncmp((char *)(name), (char *)(cmd->uc_name), (name_len))  != 0 || message_filtered(cmd->uc_name))
             {
                 continue;
             }
@@ -165254,7 +170075,7 @@
                 msg_puts(&"    "[4 - len]);
             }
 
-            msg_outtrans_attr(cmd->uc_name, HL_ATTR(HLF_D));
+            msg_outtrans_attr(cmd->uc_name,  highlight_attr[(int)(HLF_D)] );
             len = (int)cmd->uc_namelen + 4;
 
             if (len < 21)
@@ -165300,7 +170121,7 @@
             {
                 if (a & EX_COUNT)
                 {
-                    len += vim_snprintf((char *)IObuff + len, IOSIZE - len, "%ldc", cmd->uc_def);
+                    len += vim_snprintf((char *)IObuff + len,  (1024+1)  - len, "%ldc", cmd->uc_def);
                 }
                 else if (a & EX_DFLALL)
                 {
@@ -165308,7 +170129,7 @@
                 }
                 else if (cmd->uc_def >= 0)
                 {
-                    len += vim_snprintf((char *)IObuff + len, IOSIZE - len, "%ld", cmd->uc_def);
+                    len += vim_snprintf((char *)IObuff + len,  (1024+1)  - len, "%ld", cmd->uc_def);
                 }
                 else
                 {
@@ -165321,11 +170142,11 @@
                 IObuff[len++] = ' ';
             } while (len < 8 - over);
 
-            for (j = 0; j < (int)ARRAY_LENGTH(addr_type_complete_tab); ++j)
+            for (j = 0; j < (int) (sizeof(addr_type_complete_tab) / sizeof((addr_type_complete_tab)[0])) ; ++j)
             {
                 if (addr_type_complete_tab[j].key != ADDR_LINES && addr_type_complete_tab[j].key == cmd->uc_addr_type)
                 {
-                    STRCPY(IObuff + len, addr_type_complete_tab[j].shortname);
+                     strcpy((char *)(IObuff + len), (char *)(addr_type_complete_tab[j].shortname)) ;
                     len += (int)addr_type_complete_tab[j].shortnamelen;
                     break;
                 }
@@ -165339,7 +170160,7 @@
             entry = get_commandtype(cmd->uc_compl);
             if (entry != NULL)
             {
-                STRCPY(IObuff + len, entry->value.string);
+                 strcpy((char *)(IObuff + len), (char *)(entry->value.string)) ;
                 len += (int)entry->value.length;
             }
 
@@ -165408,13 +170229,13 @@
     }
     else
     {
-        entry = (addrtype_T *)bsearch(&target, &addr_type_complete_tab, ARRAY_LENGTH(addr_type_complete_tab), sizeof(addr_type_complete_tab[0]), cmp_addr_type);
+        entry = (addrtype_T *)bsearch(&target, &addr_type_complete_tab,  (sizeof(addr_type_complete_tab) / sizeof((addr_type_complete_tab)[0])) , sizeof(addr_type_complete_tab[0]), cmp_addr_type);
         if (entry == NULL)
         {
             int i;
             char_u      *err = value;
 
-            for (i = 0; err[i] != NUL && !VIM_ISWHITE(err[i]); i++)
+            for (i = 0; err[i] != NUL && ! ((err[i]) == ' ' || (err[i]) == '\t') ; i++)
             {
                 ;
             }
@@ -165437,11 +170258,11 @@
     addrtype_T *at1 = (addrtype_T *)a;
     addrtype_T *at2 = (addrtype_T *)b;
 
-    return STRNCMP(at1->fullname, at2->fullname, MAX(at1->fullnamelen, at2->fullnamelen));
+    return  strncmp((char *)(at1->fullname), (char *)(at2->fullname), (MAX(at1->fullnamelen, at2->fullnamelen))) ;
 }
 
     int
-parse_compl_arg(char_u      *value, int         vallen, int         *complp, long        *argt, char_u      **compl_arg UNUSED)
+parse_compl_arg(char_u      *value, int         vallen, int         *complp, long        *argt, char_u      **compl_arg  __attribute__((unused)) )
 {
     char_u      *arg = NULL;
     int         i;
@@ -165470,7 +170291,7 @@
     }
     else
     {
-        entry = (keyvalue_T *)bsearch(&target, &command_complete_tab, ARRAY_LENGTH(command_complete_tab), sizeof(command_complete_tab[0]), cmp_keyvalue_value_n);
+        entry = (keyvalue_T *)bsearch(&target, &command_complete_tab,  (sizeof(command_complete_tab) / sizeof((command_complete_tab)[0])) , sizeof(command_complete_tab[0]), cmp_keyvalue_value_n);
         if (entry == NULL)
         {
             semsg(_(e_invalid_complete_value_str), value);
@@ -165535,13 +170356,13 @@
             return FAIL;
         }
 
-        for (i = 0; i < (int)ARRAY_LENGTH(user_cmd_completeopt_tab); ++i)
+        for (i = 0; i < (int) (sizeof(user_cmd_completeopt_tab) / sizeof((user_cmd_completeopt_tab)[0])) ; ++i)
         {
             char *name = user_cmd_completeopt_tab[i];
 
-            if (STRLEN(name) == itemlen && STRNCMP(p, name, itemlen) == 0)
+            if ( strlen((char *)(name))  == itemlen &&  strncmp((char *)(p), (char *)(name), (itemlen))  == 0)
             {
-                if (STRCMP(name, "escape") == 0)
+                if ( strcmp((char *)(name), (char *)("escape"))  == 0)
                 {
                     flags |= UCC_ESCAPE;
                 }
@@ -165577,23 +170398,23 @@
         return FAIL;
     }
 
-    if (STRNICMP(attr, "bang", len) == 0)
+    if ( strncasecmp((char *)(attr), (char *)("bang"), (len))  == 0)
     {
         *argt |= EX_BANG;
     }
-    else if (STRNICMP(attr, "buffer", len) == 0)
+    else if ( strncasecmp((char *)(attr), (char *)("buffer"), (len))  == 0)
     {
         *flags |= UC_BUFFER;
     }
-    else if (STRNICMP(attr, "register", len) == 0)
+    else if ( strncasecmp((char *)(attr), (char *)("register"), (len))  == 0)
     {
         *argt |= EX_REGSTR;
     }
-    else if (STRNICMP(attr, "keepscript", len) == 0)
+    else if ( strncasecmp((char *)(attr), (char *)("keepscript"), (len))  == 0)
     {
         *argt |= EX_KEEPSCRIPT;
     }
-    else if (STRNICMP(attr, "bar", len) == 0)
+    else if ( strncasecmp((char *)(attr), (char *)("bar"), (len))  == 0)
     {
         *argt |= EX_TRLBAR;
     }
@@ -165615,7 +170436,7 @@
             }
         }
 
-        if (STRNICMP(attr, "nargs", attrlen) == 0)
+        if ( strncasecmp((char *)(attr), (char *)("nargs"), (attrlen))  == 0)
         {
             if (vallen == 1)
             {
@@ -165655,7 +170476,7 @@
                 return FAIL;
             }
         }
-        else if (STRNICMP(attr, "range", attrlen) == 0)
+        else if ( strncasecmp((char *)(attr), (char *)("range"), (attrlen))  == 0)
         {
             *argt |= EX_RANGE;
             if (vallen == 1 && *val == '%')
@@ -165687,7 +170508,7 @@
                 *addr_type_arg = ADDR_LINES;
             }
         }
-        else if (STRNICMP(attr, "count", attrlen) == 0)
+        else if ( strncasecmp((char *)(attr), (char *)("count"), (attrlen))  == 0)
         {
             *argt |= (EX_COUNT | EX_ZEROR | EX_RANGE);
             if (*addr_type_arg == ADDR_NONE)
@@ -165716,7 +170537,7 @@
                 *def = 0;
             }
         }
-        else if (STRNICMP(attr, "complete", attrlen) == 0)
+        else if ( strncasecmp((char *)(attr), (char *)("complete"), (attrlen))  == 0)
         {
             if (val == NULL)
             {
@@ -165729,7 +170550,7 @@
                 return FAIL;
             }
         }
-        else if (STRNICMP(attr, "completeopt", attrlen) == 0)
+        else if ( strncasecmp((char *)(attr), (char *)("completeopt"), (attrlen))  == 0)
         {
             if (val == NULL)
             {
@@ -165742,7 +170563,7 @@
                 return FAIL;
             }
         }
-        else if (STRNICMP(attr, "addr", attrlen) == 0)
+        else if ( strncasecmp((char *)(attr), (char *)("addr"), (attrlen))  == 0)
         {
             *argt |= EX_RANGE;
             if (val == NULL)
@@ -165773,7 +170594,7 @@
 }
 
     static int
-uc_add_command(char_u      *name, size_t      name_len, char_u      *rep, long        argt, long        def, int         flags, int         compl, char_u      *compl_arg UNUSED, int         compl_opt, cmd_addr_T  addr_type, int         force)
+uc_add_command(char_u      *name, size_t      name_len, char_u      *rep, long        argt, long        def, int         flags, int         compl, char_u      *compl_arg  __attribute__((unused)) , int         compl_opt, cmd_addr_T  addr_type, int         force)
 {
     ucmd_T      *cmd = NULL;
     char_u      *p;
@@ -165808,8 +170629,8 @@
 
     for (i = 0; i < gap->ga_len; ++i)
     {
-        cmd = USER_CMD_GA(gap, i);
-        cmp = STRNCMP(name, cmd->uc_name, name_len);
+        cmd =  (&((ucmd_T *)((gap)->ga_data))[i]) ;
+        cmp =  strncmp((char *)(name), (char *)(cmd->uc_name), (name_len)) ;
         if (cmp == 0)
         {
             if (name_len < cmd->uc_namelen)
@@ -165830,7 +170651,8 @@
                 goto fail;
             }
 
-            VIM_CLEAR(cmd->uc_rep);
+             vim_free(cmd->uc_rep);
+             (cmd->uc_rep) = NULL;
             break;
         }
 
@@ -165851,8 +170673,8 @@
             goto fail;
         }
 
-        cmd = USER_CMD_GA(gap, i);
-        mch_memmove(cmd + 1, cmd, (gap->ga_len - i) * sizeof(ucmd_T));
+        cmd =  (&((ucmd_T *)((gap)->ga_data))[i]) ;
+         memmove((char *)(cmd + 1), (char *)(cmd), (gap->ga_len - i) * sizeof(ucmd_T)) ;
 
         ++gap->ga_len;
 
@@ -165897,7 +170719,7 @@
             return retp;
         }
 
-        if (p[STRLEN(p) - 1] != '}')
+        if (p[ strlen((char *)(p))  - 1] != '}')
         {
             for (;;)
             {
@@ -165962,14 +170784,14 @@
     }
 
     name = p;
-    if (ASCII_ISALPHA(*p))
+    if ( ( ((unsigned)(*p) - 'A' < 26)  ||  ((unsigned)(*p) - 'a' < 26) ) )
     {
-        while (ASCII_ISALNUM(*p))
+        while ( ( ( ((unsigned)(*p) - 'A' < 26)  ||  ((unsigned)(*p) - 'a' < 26) )  ||  ((unsigned)(*p) - '0' < 10) ) )
         {
             ++p;
         }
     }
-    if (!ends_excmd2(eap->arg, p) && !VIM_ISWHITE(*p))
+    if (!ends_excmd2(eap->arg, p) && ! ((*p) == ' ' || (*p) == '\t') )
     {
         emsg(_(e_invalid_command_name));
         goto theend;
@@ -165982,11 +170804,11 @@
     {
         uc_list(name, name_len);
     }
-    else if (!ASCII_ISUPPER(*name))
+    else if (! ((unsigned)(*name) - 'A' < 26) )
     {
         emsg(_(e_user_defined_commands_must_start_with_an_uppercase_letter));
     }
-    else if ((name_len == 1 && *name == 'X') || (name_len <= 4 && STRNCMP(name, "Next", name_len > 4 ? 4 : name_len) == 0))
+    else if ((name_len == 1 && *name == 'X') || (name_len <= 4 &&  strncmp((char *)(name), (char *)("Next"), (name_len > 4 ? 4 : name_len))  == 0))
     {
         emsg(_(e_reserved_name_cannot_be_used_for_user_defined_command));
     }
@@ -166022,7 +170844,7 @@
 }
 
     void
-ex_comclear(exarg_T *eap UNUSED)
+ex_comclear(exarg_T *eap  __attribute__((unused)) )
 {
     uc_clear(&ucmds);
     if (curbuf != NULL)
@@ -166055,7 +170877,7 @@
 
     for (i = 0; i < gap->ga_len; ++i)
     {
-        cmd = USER_CMD_GA(gap, i);
+        cmd =  (&((ucmd_T *)((gap)->ga_data))[i]) ;
         vim_free(cmd->uc_name);
         cmd->uc_namelen = 0;
         vim_free(cmd->uc_rep);
@@ -166073,7 +170895,7 @@
     char_u      *arg = eap->arg;
     int         buffer_only = FALSE;
 
-    if (STRNCMP(arg, "-buffer", 7) == 0 && VIM_ISWHITE(arg[7]))
+    if ( strncmp((char *)(arg), (char *)("-buffer"), (7))  == 0 &&  ((arg[7]) == ' ' || (arg[7]) == '\t') )
     {
         buffer_only = TRUE;
         arg = skipwhite(arg + 7);
@@ -166084,8 +170906,8 @@
     {
         for (i = 0; i < gap->ga_len; ++i)
         {
-            cmd = USER_CMD_GA(gap, i);
-            res = STRCMP(arg, cmd->uc_name);
+            cmd =  (&((ucmd_T *)((gap)->ga_data))[i]) ;
+            res =  strcmp((char *)(arg), (char *)(cmd->uc_name)) ;
             if (res <= 0)
             {
                 break;
@@ -166116,7 +170938,7 @@
 
     if (i < gap->ga_len)
     {
-        mch_memmove(cmd, cmd + 1, (gap->ga_len - i) * sizeof(ucmd_T));
+         memmove((char *)(cmd), (char *)(cmd + 1), (gap->ga_len - i) * sizeof(ucmd_T)) ;
     }
 }
 
@@ -166138,7 +170960,7 @@
             len += 2;
             p += 2;
         }
-        else if (p[0] == '\\' && VIM_ISWHITE(p[1]))
+        else if (p[0] == '\\' &&  ((p[1]) == ' ' || (p[1]) == '\t') )
         {
             len += 1;
             p += 2;
@@ -166148,7 +170970,7 @@
             len += 2;
             p += 1;
         }
-        else if (VIM_ISWHITE(*p))
+        else if ( ((*p) == ' ' || (*p) == '\t') )
         {
             p = skipwhite(p);
             if (*p == NUL)
@@ -166184,7 +171006,7 @@
             *q++ = '\\';
             p += 2;
         }
-        else if (p[0] == '\\' && VIM_ISWHITE(p[1]))
+        else if (p[0] == '\\' &&  ((p[1]) == ' ' || (p[1]) == '\t') )
         {
             *q++ = p[1];
             p += 2;
@@ -166194,7 +171016,7 @@
             *q++ = '\\';
             *q++ = *p++;
         }
-        else if (VIM_ISWHITE(*p))
+        else if ( ((*p) == ' ' || (*p) == '\t') )
         {
             p = skipwhite(p);
             if (*p == NUL)
@@ -166208,7 +171030,14 @@
         }
         else
         {
-            MB_COPY_CHAR(p, q);
+             if (has_mbyte)
+             {
+                 mb_copy_char(&(p), &(q));
+             }
+             else
+             {
+                 *(q)++ = *(p)++;
+             }
         }
     }
     *q++ = '"';
@@ -166225,10 +171054,10 @@
     {
         if (*multi_mods)
         {
-            STRCPY(buf + buflen, " ");
+             strcpy((char *)(buf + buflen), (char *)(" ")) ;
             ++buflen;
         }
-        STRCPY(buf + buflen, mod_str);
+         strcpy((char *)(buf + buflen), (char *)(mod_str)) ;
     }
 
     if (*multi_mods)
@@ -166250,15 +171079,15 @@
 
     if (cmod->cmod_split & WSP_ABOVE)
     {
-        buflen += add_cmd_modifier(buf, buflen, "aboveleft", STRLEN_LITERAL("aboveleft"), multi_mods);
+        buflen += add_cmd_modifier(buf, buflen, "aboveleft",  (sizeof("aboveleft" "") - 1) , multi_mods);
     }
     if (cmod->cmod_split & WSP_BELOW)
     {
-        buflen += add_cmd_modifier(buf, buflen, "belowright", STRLEN_LITERAL("belowright"), multi_mods);
+        buflen += add_cmd_modifier(buf, buflen, "belowright",  (sizeof("belowright" "") - 1) , multi_mods);
     }
     if (cmod->cmod_split & WSP_BOT)
     {
-        buflen += add_cmd_modifier(buf, buflen, "botright", STRLEN_LITERAL("botright"), multi_mods);
+        buflen += add_cmd_modifier(buf, buflen, "botright",  (sizeof("botright" "") - 1) , multi_mods);
     }
 
     if (cmod->cmod_tab > 0)
@@ -166267,7 +171096,7 @@
 
         if (tabnr == tabpage_index(curtab))
         {
-            buflen += add_cmd_modifier(buf, buflen, "tab", STRLEN_LITERAL("tab"), multi_mods);
+            buflen += add_cmd_modifier(buf, buflen, "tab",  (sizeof("tab" "") - 1) , multi_mods);
         }
         else
         {
@@ -166281,15 +171110,15 @@
 
     if (cmod->cmod_split & WSP_TOP)
     {
-        buflen += add_cmd_modifier(buf, buflen, "topleft", STRLEN_LITERAL("topleft"), multi_mods);
+        buflen += add_cmd_modifier(buf, buflen, "topleft",  (sizeof("topleft" "") - 1) , multi_mods);
     }
     if (cmod->cmod_split & WSP_VERT)
     {
-        buflen += add_cmd_modifier(buf, buflen, "vertical", STRLEN_LITERAL("vertical"), multi_mods);
+        buflen += add_cmd_modifier(buf, buflen, "vertical",  (sizeof("vertical" "") - 1) , multi_mods);
     }
     if (cmod->cmod_split & WSP_HOR)
     {
-        buflen += add_cmd_modifier(buf, buflen, "horizontal", STRLEN_LITERAL("horizontal"), multi_mods);
+        buflen += add_cmd_modifier(buf, buflen, "horizontal",  (sizeof("horizontal" "") - 1) , multi_mods);
     }
 
     return buflen;
@@ -166303,16 +171132,16 @@
     int     i;
     static keyvalue_T mod_entry_tab[] =
     {
-        KEYVALUE_ENTRY(CMOD_HIDE, "hide"),
-        KEYVALUE_ENTRY(CMOD_KEEPALT, "keepalt"),
-        KEYVALUE_ENTRY(CMOD_KEEPJUMPS, "keepjumps"),
-        KEYVALUE_ENTRY(CMOD_KEEPMARKS, "keepmarks"),
-        KEYVALUE_ENTRY(CMOD_KEEPPATTERNS, "keeppatterns"),
-        KEYVALUE_ENTRY(CMOD_LOCKMARKS, "lockmarks"),
-        KEYVALUE_ENTRY(CMOD_NOSWAPFILE, "noswapfile"),
-        KEYVALUE_ENTRY(CMOD_UNSILENT, "unsilent"),
-        KEYVALUE_ENTRY(CMOD_NOAUTOCMD, "noautocmd"),
-        KEYVALUE_ENTRY(CMOD_LEGACY, "legacy")
+         {(CMOD_HIDE), {((char_u *)"hide"),  (sizeof("hide" "") - 1) }} ,
+         {(CMOD_KEEPALT), {((char_u *)"keepalt"),  (sizeof("keepalt" "") - 1) }} ,
+         {(CMOD_KEEPJUMPS), {((char_u *)"keepjumps"),  (sizeof("keepjumps" "") - 1) }} ,
+         {(CMOD_KEEPMARKS), {((char_u *)"keepmarks"),  (sizeof("keepmarks" "") - 1) }} ,
+         {(CMOD_KEEPPATTERNS), {((char_u *)"keeppatterns"),  (sizeof("keeppatterns" "") - 1) }} ,
+         {(CMOD_LOCKMARKS), {((char_u *)"lockmarks"),  (sizeof("lockmarks" "") - 1) }} ,
+         {(CMOD_NOSWAPFILE), {((char_u *)"noswapfile"),  (sizeof("noswapfile" "") - 1) }} ,
+         {(CMOD_UNSILENT), {((char_u *)"unsilent"),  (sizeof("unsilent" "") - 1) }} ,
+         {(CMOD_NOAUTOCMD), {((char_u *)"noautocmd"),  (sizeof("noautocmd" "") - 1) }} ,
+         {(CMOD_LEGACY), {((char_u *)"legacy"),  (sizeof("legacy" "") - 1) }} 
     };
 
     if (quote)
@@ -166332,7 +171161,7 @@
     }
     }
 
-    for (i = 0; i < (int)ARRAY_LENGTH(mod_entry_tab); ++i)
+    for (i = 0; i < (int) (sizeof(mod_entry_tab) / sizeof((mod_entry_tab)[0])) ; ++i)
     {
         if (cmod->cmod_flags & mod_entry_tab[i].key)
         {
@@ -166344,11 +171173,11 @@
     {
         if (cmod->cmod_flags & CMOD_ERRSILENT)
         {
-            buflen += add_cmd_modifier(buf, buflen, "silent!", STRLEN_LITERAL("silent!"), &multi_mods);
+            buflen += add_cmd_modifier(buf, buflen, "silent!",  (sizeof("silent!" "") - 1) , &multi_mods);
         }
         else
         {
-            buflen += add_cmd_modifier(buf, buflen, "silent", STRLEN_LITERAL("silent"), &multi_mods);
+            buflen += add_cmd_modifier(buf, buflen, "silent",  (sizeof("silent" "") - 1) , &multi_mods);
         }
     }
 
@@ -166358,7 +171187,7 @@
 
         if (verbose_value == 1)
         {
-            buflen += add_cmd_modifier(buf, buflen, "verbose", STRLEN_LITERAL("verbose"), &multi_mods);
+            buflen += add_cmd_modifier(buf, buflen, "verbose",  (sizeof("verbose" "") - 1) , &multi_mods);
         }
         else
         {
@@ -166421,39 +171250,39 @@
     {
         type = ct_NONE;
     }
-    else if (STRNICMP(p, "args>", l) == 0)
+    else if ( strncasecmp((char *)(p), (char *)("args>"), (l))  == 0)
     {
         type = ct_ARGS;
     }
-    else if (STRNICMP(p, "bang>", l) == 0)
+    else if ( strncasecmp((char *)(p), (char *)("bang>"), (l))  == 0)
     {
         type = ct_BANG;
     }
-    else if (STRNICMP(p, "count>", l) == 0)
+    else if ( strncasecmp((char *)(p), (char *)("count>"), (l))  == 0)
     {
         type = ct_COUNT;
     }
-    else if (STRNICMP(p, "line1>", l) == 0)
+    else if ( strncasecmp((char *)(p), (char *)("line1>"), (l))  == 0)
     {
         type = ct_LINE1;
     }
-    else if (STRNICMP(p, "line2>", l) == 0)
+    else if ( strncasecmp((char *)(p), (char *)("line2>"), (l))  == 0)
     {
         type = ct_LINE2;
     }
-    else if (STRNICMP(p, "range>", l) == 0)
+    else if ( strncasecmp((char *)(p), (char *)("range>"), (l))  == 0)
     {
         type = ct_RANGE;
     }
-    else if (STRNICMP(p, "lt>", l) == 0)
+    else if ( strncasecmp((char *)(p), (char *)("lt>"), (l))  == 0)
     {
         type = ct_LT;
     }
-    else if (STRNICMP(p, "reg>", l) == 0 || STRNICMP(p, "register>", l) == 0)
+    else if ( strncasecmp((char *)(p), (char *)("reg>"), (l))  == 0 ||  strncasecmp((char *)(p), (char *)("register>"), (l))  == 0)
     {
         type = ct_REGISTER;
     }
-    else if (STRNICMP(p, "mods>", l) == 0)
+    else if ( strncasecmp((char *)(p), (char *)("mods>"), (l))  == 0)
     {
         type = ct_MODS;
     }
@@ -166468,7 +171297,7 @@
                 result = 2;
                 if (buf != NULL)
                 {
-                    STRCPY(buf, "''");
+                     strcpy((char *)(buf), (char *)("''")) ;
                 }
             }
             else
@@ -166486,26 +171315,26 @@
         switch (quote)
         {
         case 0:
-            result = STRLEN(eap->arg);
+            result =  strlen((char *)(eap->arg)) ;
             if (buf != NULL)
             {
-                STRCPY(buf, eap->arg);
+                 strcpy((char *)(buf), (char *)(eap->arg)) ;
             }
             break;
         case 1:
         {
             int unesc = (cmd->uc_compl_opt & UCC_ESCAPE) != 0;
 
-            result = STRLEN(eap->arg) + 2;
+            result =  strlen((char *)(eap->arg))  + 2;
             for (p = eap->arg; *p; ++p)
             {
                 if (enc_dbcs != 0 && (*mb_ptr2len)(p) == 2)
                 {
                     ++p;
                 }
-                else if (unesc && *p == '\\' && (VIM_ISWHITE(p[1]) || p[1] == '\\'))
+                else if (unesc && *p == '\\' && ( ((p[1]) == ' ' || (p[1]) == '\t')  || p[1] == '\\'))
                 {
-                    if (VIM_ISWHITE(p[1]))
+                    if ( ((p[1]) == ' ' || (p[1]) == '\t') )
                     {
                         --result;
                     }
@@ -166529,7 +171358,7 @@
                     {
                         *buf++ = *p++;
                     }
-                    else if (unesc && *p == '\\' && (VIM_ISWHITE(p[1]) || p[1] == '\\'))
+                    else if (unesc && *p == '\\' && ( ((p[1]) == ' ' || (p[1]) == '\t')  || p[1] == '\\'))
                     {
                         ++p;
                         if (*p == '\\')
@@ -166560,7 +171389,7 @@
             result = *split_len;
             if (buf != NULL && result != 0)
             {
-                STRCPY(buf, *split_buf);
+                 strcpy((char *)(buf), (char *)(*split_buf)) ;
             }
 
             break;
@@ -166616,7 +171445,7 @@
             {
                 *buf++ = '"';
             }
-            STRCPY(buf, num_buf);
+             strcpy((char *)(buf), (char *)(num_buf)) ;
             buf += num_len;
             if (quote)
             {
@@ -166697,11 +171526,11 @@
 
     if (eap->cmdidx == CMD_USER)
     {
-        cmd = USER_CMD(eap->useridx);
+        cmd =  (&((ucmd_T *)(ucmds.ga_data))[eap->useridx]) ;
     }
     else
     {
-        cmd = USER_CMD_GA(&prevwin_curwin()->w_buffer->b_ucmds, eap->useridx);
+        cmd =  (&((ucmd_T *)((&prevwin_curwin()->w_buffer->b_ucmds)->ga_data))[eap->useridx]) ;
     }
 
     buf = NULL;
@@ -166720,19 +171549,19 @@
             }
             if (buf != NULL)
             {
-                for (ksp = p; *ksp != NUL && *ksp != K_SPECIAL; ++ksp)
+                for (ksp = p; *ksp != NUL && *ksp !=  (0x80) ; ++ksp)
                 {
                     ;
                 }
-                if (*ksp == K_SPECIAL && (start == NULL || ksp < start || end == NULL) && ((ksp[1] == KS_SPECIAL && ksp[2] == KE_FILLER)))
+                if (*ksp ==  (0x80)  && (start == NULL || ksp < start || end == NULL) && ((ksp[1] == KS_SPECIAL && ksp[2] ==  ('X') )))
                 {
                     len = ksp - p;
                     if (len > 0)
                     {
-                        mch_memmove(q, p, len);
+                         memmove((char *)(q), (char *)(p), len) ;
                         q += len;
                     }
-                    *q++ = ksp[1] == KS_SPECIAL ? K_SPECIAL : CSI;
+                    *q++ = ksp[1] == KS_SPECIAL ?  (0x80)  : CSI;
                     p = ksp + 3;
                     continue;
                 }
@@ -166752,7 +171581,7 @@
             }
             else
             {
-                mch_memmove(q, p, len);
+                 memmove((char *)(q), (char *)(p), len) ;
                 q += len;
             }
 
@@ -166777,11 +171606,11 @@
         }
         if (buf != NULL)
         {
-            STRCPY(q, p);
+             strcpy((char *)(q), (char *)(p)) ;
             break;
         }
 
-        totlen += STRLEN(p);
+        totlen +=  strlen((char *)(p)) ;
         buf = alloc(totlen + 1);
         if (buf == NULL)
         {
@@ -166809,8 +171638,8 @@
 
 // ==================== version.c ====================
 
-static char            *Version = VIM_VERSION_SHORT;
-static char     *mediumVersion = VIM_VERSION_MEDIUM;
+static const char      *Version = VIM_VERSION_SHORT;
+static const char *mediumVersion = VIM_VERSION_SHORT;
 
 static char    *longVersion = NULL;
 
@@ -166825,14 +171654,14 @@
     char *date_time = __DATE__ " " __TIME__;
     char *msg = _("%s (%s, compiled %s)");
     size_t len = strlen(msg)
-        + STRLEN_LITERAL(VIM_VERSION_LONG_ONLY)
-        + STRLEN_LITERAL(VIM_VERSION_DATE_ONLY)
+        + sizeof(VIM_VERSION_LONG_ONLY) - 1
+        + sizeof(VIM_VERSION_DATE_ONLY) - 1
         + strlen(date_time);
 
     longVersion = alloc(len);
     if (longVersion == NULL)
     {
-        longVersion = VIM_VERSION_LONG;
+        longVersion = (char *)VIM_VERSION_LONG_ONLY;
     }
     else
     {
@@ -168186,7 +173015,7 @@
     {
         msg_puts(_("\nIncluded patches: "));
         first = -1;
-        i = (int)ARRAY_LENGTH(included_patches) - 1;
+        i = (int) (sizeof(included_patches) / sizeof((included_patches)[0]))  - 1;
         while (--i >= 0)
         {
             if (first < 0)
@@ -168246,22 +173075,22 @@
     }
 
     version_msg(_("   system vimrc file: \""));
-    version_msg(SYS_VIMRC_FILE);
+    version_msg( "$VIM/vimrc" );
     version_msg("\"\n");
     version_msg(_("     user vimrc file: \""));
-    version_msg(USR_VIMRC_FILE);
+    version_msg( "$HOME/.vimrc" );
     version_msg("\"\n");
     version_msg(_(" 2nd user vimrc file: \""));
-    version_msg(USR_VIMRC_FILE2);
+    version_msg( "~/.vim/vimrc" );
     version_msg("\"\n");
     version_msg(_(" 3rd user vimrc file: \""));
-    version_msg((char *)(XDG_VIMRC_FILE));
+    version_msg((char *)( ( (char_u *)getenv((char *)((char_u *)"XDG_CONFIG_HOME"))         ? "$XDG_CONFIG_HOME/vim/vimrc"  : "~/.config/vim/vimrc") ));
     version_msg("\"\n");
     version_msg(_("      user exrc file: \""));
-    version_msg(USR_EXRC_FILE);
+    version_msg( "$HOME/.exrc" );
     version_msg("\"\n");
     version_msg(_("       defaults file: \""));
-    version_msg(VIM_DEFAULTS_FILE);
+    version_msg( "$VIMRUNTIME/defaults.vim" );
     version_msg("\"\n");
     if (*default_vim_dir != NUL)
     {
@@ -168288,7 +173117,7 @@
     void
 maybe_intro_message(void)
 {
-    if (BUFEMPTY() && curbuf->b_fname == NULL && firstwin->w_next == NULL && vim_strchr(p_shm, SHM_INTRO) == NULL)
+    if ( (curbuf->b_ml.ml_line_count == 1 && *ml_get((linenr_T)1) == NUL)  && curbuf->b_fname == NULL && firstwin->w_next == NULL && vim_strchr(p_shm, SHM_INTRO) == NULL)
     {
         intro_message(FALSE);
     }
@@ -168304,26 +173133,26 @@
     char        *p;
     static char *(lines[]) =
     {
-        N_("VIM - Vi IMproved"),
+         "VIM - Vi IMproved" ,
         "",
-        N_("version "),
-        N_("by Bram Moolenaar et al."),
-        N_("Vim is open source and freely distributable"),
+         "version " ,
+         "by Bram Moolenaar et al." ,
+         "Vim is open source and freely distributable" ,
         "",
-        N_("Help poor children in Uganda!"),
-        N_("type  :help Kuwasha<Enter>    for information "),
+         "Help poor children in Uganda!" ,
+         "type  :help Kuwasha<Enter>    for information " ,
         "",
-        N_("type  :q<Enter>               to exit         "),
-        N_("type  :help<Enter>  or  <F1>  for on-line help"),
-        N_("type  :help version9<Enter>   for version info"),
+         "type  :q<Enter>               to exit         " ,
+         "type  :help<Enter>  or  <F1>  for on-line help" ,
+         "type  :help version9<Enter>   for version info" ,
         NULL,
         "",
-        N_("Running in Vi compatible mode"),
-        N_("type  :set nocp<Enter>        for Vim defaults"),
-        N_("type  :help cp-default<Enter> for info on this"),
+         "Running in Vi compatible mode" ,
+         "type  :set nocp<Enter>        for Vim defaults" ,
+         "type  :help cp-default<Enter> for info on this" ,
     };
 
-    blanklines = (int)Rows - (ARRAY_LENGTH(lines) - 1);
+    blanklines = (int)Rows - ( (sizeof(lines) / sizeof((lines)[0]))  - 1);
     if (!p_cp)
     {
         blanklines += 4;
@@ -168344,7 +173173,7 @@
     row = blanklines / 2;
     if ((row >= 2 && topframe->fr_width >= 50) || colon)
     {
-        for (i = 0; i < (int)ARRAY_LENGTH(lines); ++i)
+        for (i = 0; i < (int) (sizeof(lines) / sizeof((lines)[0])) ; ++i)
         {
             p = lines[i];
             if (p == NULL)
@@ -168359,15 +173188,15 @@
             {
                 if (strstr(p, "children") != NULL)
                 {
-                    p = N_("Sponsor Vim development!");
+                    p =  "Sponsor Vim development!" ;
                 }
                 else if (strstr(p, "Kuwasha") != NULL)
                 {
-                    p = N_("type  :help sponsor<Enter>    for information ");
+                    p =  "type  :help sponsor<Enter>    for information " ;
                 }
                 else if (strstr(p, "Orphans") != NULL)
                 {
-                    p = N_("menu  Help->Sponsor           for information    ");
+                    p =  "menu  Help->Sponsor           for information    " ;
                 }
             }
             if (*p != NUL)
@@ -168396,12 +173225,12 @@
     col = vim_strsize(mesg);
     if (add_version)
     {
-        STRCPY(vers, mediumVersion);
+         strcpy((char *)(vers), (char *)(mediumVersion)) ;
         if (highest_patch())
         {
-            if (SAFE_isalpha((int)vers[3]))
+            if ( (isalpha ((unsigned char)((int)vers[3]))) )
             {
-                int len = (SAFE_isalpha((int)vers[4])) ? 5 : 4;
+                int len = ( (isalpha ((unsigned char)((int)vers[4]))) ) ? 5 : 4;
                 sprintf((char *)vers + len, ".%d%s", highest_patch(), mediumVersion + len);
             }
             else
@@ -168409,7 +173238,7 @@
                 sprintf((char *)vers + 3, ".%d", highest_patch());
             }
         }
-        col += (int)STRLEN(vers);
+        col += (int) strlen((char *)(vers)) ;
     }
     col = (topframe->fr_width - col) / 2;
     if (col < 0)
@@ -168432,7 +173261,7 @@
                 clen += byte2cells(p[l]);
             }
         }
-        screen_puts_len(p, l, row, col + firstwin->w_wincol, *p == '<' ? HL_ATTR(HLF_8) : attr);
+        screen_puts_len(p, l, row, col + firstwin->w_wincol, *p == '<' ?  highlight_attr[(int)(HLF_8)]  : attr);
         col += clen;
     }
 
@@ -168443,7 +173272,7 @@
 }
 
     void
-ex_intro(exarg_T *eap UNUSED)
+ex_intro(exarg_T *eap  __attribute__((unused)) )
 {
     screenclear();
     intro_message(TRUE);
@@ -168460,7 +173289,7 @@
 }
 
     void
-ex_vim9script(exarg_T *eap UNUSED)
+ex_vim9script(exarg_T *eap  __attribute__((unused)) )
 {
     current_sctx.sc_version = SCRIPT_VERSION_VIM9;
 }
@@ -168533,18 +173362,14 @@
 
 static win_T *win_alloc(win_T *after, int hidden);
 
-#define NOWIN           ((win_T *)-1)
+enum { WEE_UNDO_SYNC = 0x01 };
+enum { WEE_CURWIN_INVALID = 0x02 };
+enum { WEE_TRIGGER_NEW_AUTOCMDS = 0x04 };
+enum { WEE_TRIGGER_ENTER_AUTOCMDS = 0x08 };
+enum { WEE_TRIGGER_LEAVE_AUTOCMDS = 0x10 };
+enum { WEE_ALLOW_PARSE_MESSAGES = 0x20 };
 
-#define ROWS_AVAIL (Rows - p_ch - tabline_height())
-
-#define WEE_UNDO_SYNC                   0x01
-#define WEE_CURWIN_INVALID              0x02
-#define WEE_TRIGGER_NEW_AUTOCMDS        0x04
-#define WEE_TRIGGER_ENTER_AUTOCMDS      0x08
-#define WEE_TRIGGER_LEAVE_AUTOCMDS      0x10
-#define WEE_ALLOW_PARSE_MESSAGES        0x20
-
-static char *m_onlyone = N_("Already only one window");
+static char *m_onlyone =  "Already only one window" ;
 
 static int split_disallowed = 0;
 
@@ -168566,7 +173391,6 @@
     --close_disallowed;
 }
 
-
     int
 window_layout_locked(enum CMD_index cmd)
 {
@@ -168588,7 +173412,7 @@
     int
 check_can_set_curbuf_disabled(void)
 {
-    if (curwin->w_p_wfb)
+    if (curwin-> w_onebuf_opt.wo_wfb )
     {
         emsg(_(e_winfixbuf_cannot_go_to_buffer));
         return FALSE;
@@ -168599,7 +173423,7 @@
     int
 check_can_set_curbuf_forceit(int forceit)
 {
-    if (!forceit && curwin->w_p_wfb)
+    if (!forceit && curwin-> w_onebuf_opt.wo_wfb )
     {
         emsg(_(e_winfixbuf_cannot_go_to_buffer));
         return FALSE;
@@ -168650,8 +173474,6 @@
         return;
     }
 
-#define CHECK_CMDWIN     do {   if (cmdwin_type != 0)   {           emsg(_(e_invalid_in_cmdline_window));           return;     }     } while (0)
-
     Prenum1 = Prenum == 0 ? 1 : Prenum;
 
     switch (nchar)
@@ -168659,7 +173481,8 @@
     case 'S':
     case Ctrl_S:
     case 's':
-                CHECK_CMDWIN;
+                 if (cmdwin_type != 0)
+                     {           emsg(_(e_invalid_in_cmdline_window));           return;     }
                 reset_VIsual_and_resel();
                 if (bt_quickfix(curbuf))
                 {
@@ -168670,7 +173493,8 @@
 
     case Ctrl_V:
     case 'v':
-                CHECK_CMDWIN;
+                 if (cmdwin_type != 0)
+                     {           emsg(_(e_invalid_in_cmdline_window));           return;     }
                 reset_VIsual_and_resel();
                 if (bt_quickfix(curbuf))
                 {
@@ -168681,7 +173505,8 @@
 
     case Ctrl_HAT:
     case '^':
-                CHECK_CMDWIN;
+                 if (cmdwin_type != 0)
+                     {           emsg(_(e_invalid_in_cmdline_window));           return;     }
                 reset_VIsual_and_resel();
 
                 if (buflist_findnr(Prenum == 0 ? curwin->w_alt_fnum : Prenum) == NULL)
@@ -168705,7 +173530,8 @@
 
     case Ctrl_N:
     case 'n':
-                CHECK_CMDWIN;
+                 if (cmdwin_type != 0)
+                     {           emsg(_(e_invalid_in_cmdline_window));           return;     }
                 reset_VIsual_and_resel();
 newwindow:
                 if (Prenum)
@@ -168716,7 +173542,7 @@
                 {
                     cbuf[0] = NUL;
                 }
-                STRCAT(cbuf, "new");
+                 strcat((char *)(cbuf), (char *)("new")) ;
                 do_cmdline_cmd(cbuf);
                 break;
 
@@ -168736,7 +173562,8 @@
 
     case Ctrl_O:
     case 'o':
-                CHECK_CMDWIN;
+                 if (cmdwin_type != 0)
+                     {           emsg(_(e_invalid_in_cmdline_window));           return;     }
                 reset_VIsual_and_resel();
                 cmd_with_count("only", cbuf, sizeof(cbuf), Prenum);
                 do_cmdline_cmd(cbuf);
@@ -168745,8 +173572,9 @@
     case Ctrl_W:
     case 'w':
     case 'W':
-                CHECK_CMDWIN;
-                if (ONE_WINDOW && Prenum != 1)
+                 if (cmdwin_type != 0)
+                     {           emsg(_(e_invalid_in_cmdline_window));           return;     }
+                if ( (firstwin == lastwin)  && Prenum != 1)
                 {
                     beep_flush();
                 }
@@ -168790,36 +173618,41 @@
                 break;
 
     case 'j':
-    case K_DOWN:
+    case   (-(('k') + ((int)('d') << 8)))  :
     case Ctrl_J:
-                CHECK_CMDWIN;
+                 if (cmdwin_type != 0)
+                     {           emsg(_(e_invalid_in_cmdline_window));           return;     }
                 win_goto_ver(FALSE, Prenum1);
                 break;
 
     case 'k':
-    case K_UP:
+    case   (-(('k') + ((int)('u') << 8)))  :
     case Ctrl_K:
-                CHECK_CMDWIN;
+                 if (cmdwin_type != 0)
+                     {           emsg(_(e_invalid_in_cmdline_window));           return;     }
                 win_goto_ver(TRUE, Prenum1);
                 break;
 
     case 'h':
-    case K_LEFT:
+    case   (-(('k') + ((int)('l') << 8)))  :
     case Ctrl_H:
-    case K_BS:
-                CHECK_CMDWIN;
+    case   (-(('k') + ((int)('b') << 8)))  :
+                 if (cmdwin_type != 0)
+                     {           emsg(_(e_invalid_in_cmdline_window));           return;     }
                 win_goto_hor(TRUE, Prenum1);
                 break;
 
     case 'l':
-    case K_RIGHT:
+    case   (-(('k') + ((int)('r') << 8)))  :
     case Ctrl_L:
-                CHECK_CMDWIN;
+                 if (cmdwin_type != 0)
+                     {           emsg(_(e_invalid_in_cmdline_window));           return;     }
                 win_goto_hor(FALSE, Prenum1);
                 break;
 
     case 'T':
-                CHECK_CMDWIN;
+                 if (cmdwin_type != 0)
+                     {           emsg(_(e_invalid_in_cmdline_window));           return;     }
                 if (one_window())
                 {
                     msg(_(m_onlyone));
@@ -168870,19 +173703,22 @@
 
     case 'x':
     case Ctrl_X:
-                CHECK_CMDWIN;
+                 if (cmdwin_type != 0)
+                     {           emsg(_(e_invalid_in_cmdline_window));           return;     }
                 win_exchange(Prenum);
                 break;
 
     case Ctrl_R:
     case 'r':
-                CHECK_CMDWIN;
+                 if (cmdwin_type != 0)
+                     {           emsg(_(e_invalid_in_cmdline_window));           return;     }
                 reset_VIsual_and_resel();
                 win_rotate(FALSE, (int)Prenum1);
                 break;
 
     case 'R':
-                CHECK_CMDWIN;
+                 if (cmdwin_type != 0)
+                     {           emsg(_(e_invalid_in_cmdline_window));           return;     }
                 reset_VIsual_and_resel();
                 win_rotate(TRUE, (int)Prenum1);
                 break;
@@ -168891,8 +173727,9 @@
     case 'J':
     case 'H':
     case 'L':
-                CHECK_CMDWIN;
-                if (ONE_WINDOW)
+                 if (cmdwin_type != 0)
+                     {           emsg(_(e_invalid_in_cmdline_window));           return;     }
+                if ( (firstwin == lastwin) )
                 {
                     beep_flush();
                 }
@@ -168939,7 +173776,8 @@
 
     case ']':
     case Ctrl_RSB:
-                CHECK_CMDWIN;
+                 if (cmdwin_type != 0)
+                     {           emsg(_(e_invalid_in_cmdline_window));           return;     }
                 if (Prenum)
                 {
                     postponed_split = Prenum;
@@ -168957,7 +173795,8 @@
     case 'F':
     case Ctrl_F:
 wingotofile:
-                CHECK_CMDWIN;
+                 if (cmdwin_type != 0)
+                     {           emsg(_(e_invalid_in_cmdline_window));           return;     }
                 if (check_text_or_curbuf_locked(NULL))
                 {
                     break;
@@ -168978,8 +173817,9 @@
 
                     if (wp == NULL && win_split(0, 0) == OK)
                     {
-                        RESET_BINDING(curwin);
-                        if (do_ecmd(0, ptr, NULL, NULL, ECMD_LASTL, ECMD_HIDE, NULL) == FAIL)
+                         (curwin)-> w_onebuf_opt.wo_scb  = FALSE;
+                         (curwin)-> w_onebuf_opt.wo_crb  = FALSE;
+                        if (do_ecmd(0, ptr, NULL, NULL,  (linenr_T)0 , ECMD_HIDE, NULL) == FAIL)
                         {
                             sig_atomic_t    old_got_int = got_int;
                             got_int = FALSE;
@@ -169007,14 +173847,15 @@
 
     case 'g':
     case Ctrl_G:
-                CHECK_CMDWIN;
+                 if (cmdwin_type != 0)
+                     {           emsg(_(e_invalid_in_cmdline_window));           return;     }
                 ++no_mapping;
                 ++allow_keys;
                 if (xchar == NUL)
                 {
                     xchar = plain_vgetc();
                 }
-                LANGMAP_ADJUST(xchar, TRUE);
+                  ;
                 --no_mapping;
                 --allow_keys;
                 (void)add_to_showcmd(xchar);
@@ -169155,7 +173996,7 @@
     }
     else
     {
-        STRCPY(bufp, cmd);
+         strcpy((char *)(bufp), (char *)(cmd)) ;
     }
 }
 
@@ -169264,9 +174105,9 @@
         oldwin = curwin;
     }
 
-    if (ONE_WINDOW && p_ls == 1 && oldwin->w_status_height == 0)
+    if ( (firstwin == lastwin)  && p_ls == 1 && oldwin->w_status_height == 0)
     {
-        if (!(flags & WSP_FORCE_ROOM) && VISIBLE_HEIGHT(oldwin) <= p_wmh)
+        if (!(flags & WSP_FORCE_ROOM) &&  (oldwin)->w_height  <= p_wmh)
         {
             emsg(_(e_not_enough_room));
             goto theend;
@@ -169289,23 +174130,25 @@
         }
         if (flags & (WSP_BOT | WSP_TOP))
         {
-            minwidth = frame_minwidth(topframe, NOWIN);
+            minwidth = frame_minwidth(topframe,  ((win_T *)-1) );
             available = topframe->fr_width;
             needed += minwidth;
         }
         else if (p_ea)
         {
-            minwidth = frame_minwidth(oldwin->w_frame, NOWIN);
+            minwidth = frame_minwidth(oldwin->w_frame,  ((win_T *)-1) );
             prevfrp = oldwin->w_frame;
             for (frp = oldwin->w_frame->fr_parent; frp != NULL; frp = frp->fr_parent)
             {
                 if (frp->fr_layout == FR_ROW)
                 {
-                    FOR_ALL_FRAMES(frp2, frp->fr_child)
+                     for ((frp2) = frp->fr_child; (frp2) != NULL; (frp2) = (frp2)->fr_next) 
+                     {
                         if (frp2 != prevfrp)
                         {
-                            minwidth += frame_minwidth(frp2, NOWIN);
+                            minwidth += frame_minwidth(frp2,  ((win_T *)-1) );
                         }
+                     }
                 }
                 prevfrp = frp;
             }
@@ -169314,7 +174157,7 @@
         }
         else
         {
-            minwidth = frame_minwidth(oldwin->w_frame, NOWIN);
+            minwidth = frame_minwidth(oldwin->w_frame,  ((win_T *)-1) );
             available = oldwin->w_frame->fr_width;
             needed += minwidth;
         }
@@ -169341,7 +174184,7 @@
             do_equal = TRUE;
         }
 
-        if (oldwin->w_p_wfw)
+        if (oldwin-> w_onebuf_opt.wo_wfw )
         {
             win_setwidth_win(oldwin->w_width + new_size + 1, oldwin);
         }
@@ -169364,7 +174207,7 @@
     {
         layout = FR_COL;
 
-        wmh1 = (p_wmh == 0 ? 1 : p_wmh) + WINBAR_HEIGHT(curwin);
+        wmh1 = (p_wmh == 0 ? 1 : p_wmh) +  0 ;
         needed = wmh1 + statusline_height(oldwin);
         if (flags & WSP_ROOM)
         {
@@ -169372,23 +174215,25 @@
         }
         if (flags & (WSP_BOT | WSP_TOP))
         {
-            minheight = frame_minheight(topframe, NOWIN) + need_status;
+            minheight = frame_minheight(topframe,  ((win_T *)-1) ) + need_status;
             available = topframe->fr_height;
             needed += minheight;
         }
         else if (p_ea)
         {
-            minheight = frame_minheight(oldwin->w_frame, NOWIN) + need_status;
+            minheight = frame_minheight(oldwin->w_frame,  ((win_T *)-1) ) + need_status;
             prevfrp = oldwin->w_frame;
             for (frp = oldwin->w_frame->fr_parent; frp != NULL; frp = frp->fr_parent)
             {
                 if (frp->fr_layout == FR_COL)
                 {
-                    FOR_ALL_FRAMES(frp2, frp->fr_child)
+                     for ((frp2) = frp->fr_child; (frp2) != NULL; (frp2) = (frp2)->fr_next) 
+                     {
                         if (frp2 != prevfrp)
                         {
-                            minheight += frame_minheight(frp2, NOWIN);
+                            minheight += frame_minheight(frp2,  ((win_T *)-1) );
                         }
+                     }
                 }
                 prevfrp = frp;
             }
@@ -169397,7 +174242,7 @@
         }
         else
         {
-            minheight = frame_minheight(oldwin->w_frame, NOWIN) + need_status;
+            minheight = frame_minheight(oldwin->w_frame,  ((win_T *)-1) ) + need_status;
             available = oldwin->w_frame->fr_height;
             needed += minheight;
         }
@@ -169432,7 +174277,7 @@
             do_equal = TRUE;
         }
 
-        if (oldwin->w_p_wfh)
+        if (oldwin-> w_onebuf_opt.wo_wfh )
         {
             set_fraction(oldwin);
             did_set_fraction = TRUE;
@@ -169542,7 +174387,7 @@
     }
     if (curfrp->fr_parent == NULL || curfrp->fr_parent->fr_layout != layout)
     {
-        frp = ALLOC_CLEAR_ONE(frame_T);
+        frp =  (frame_T *)alloc_clear(sizeof(frame_T)) ;
         *frp = *curfrp;
         curfrp->fr_layout = layout;
         frp->fr_parent = curfrp;
@@ -169557,8 +174402,10 @@
         }
         else
         {
-            FOR_ALL_FRAMES(frp, frp->fr_child)
+             for ((frp) = frp->fr_child; (frp) != NULL; (frp) = (frp)->fr_next) 
+             {
                 frp->fr_parent = curfrp;
+             }
         }
     }
 
@@ -169589,7 +174436,7 @@
 
     if (flags & WSP_VERT)
     {
-        wp->w_p_scr = curwin->w_p_scr;
+        wp-> w_onebuf_opt.wo_scr  = curwin-> w_onebuf_opt.wo_scr ;
 
         if (need_status)
         {
@@ -169601,13 +174448,13 @@
             int stl_height = p_ls > 0 ? statusline_height(curfrp->fr_win) : 0;
 
             wp->w_winrow = tabline_height();
-            win_new_height(wp, curfrp->fr_height - stl_height - WINBAR_HEIGHT(wp));
+            win_new_height(wp, curfrp->fr_height - stl_height -  0 );
             wp->w_status_height = stl_height;
         }
         else
         {
             wp->w_winrow = oldwin->w_winrow;
-            win_new_height(wp, VISIBLE_HEIGHT(oldwin));
+            win_new_height(wp,  (oldwin)->w_height );
             wp->w_status_height = oldwin->w_status_height;
         }
         frp->fr_height = curfrp->fr_height;
@@ -169667,7 +174514,7 @@
         if (flags & (WSP_TOP | WSP_BOT))
         {
             int new_fr_height = curfrp->fr_height - new_size
-                                                          + WINBAR_HEIGHT(wp);
+                                                          +  0 ;
 
             if (!((flags & WSP_BOT) && p_ls == 0))
             {
@@ -169691,7 +174538,7 @@
         }
         else
         {
-            wp->w_winrow = oldwin->w_winrow + VISIBLE_HEIGHT(oldwin)
+            wp->w_winrow = oldwin->w_winrow +  (oldwin)->w_height 
                                                     + statusline_height(oldwin);
             wp->w_status_height = old_status_height;
             if (!(flags & WSP_BOT))
@@ -169769,7 +174616,7 @@
 }
 
     static void
-win_init(win_T *newp, win_T *oldp, int flags UNUSED)
+win_init(win_T *newp, win_T *oldp, int flags  __attribute__((unused)) )
 {
     int         i;
 
@@ -169789,7 +174636,8 @@
     newp->w_prev_fraction_row = oldp->w_prev_fraction_row;
 
     remove_highlight_overrides(newp->w_hl);
-    VIM_CLEAR(newp->w_hl);
+     vim_free(newp->w_hl);
+     (newp->w_hl) = NULL;
 
     copy_jumplist(oldp, newp);
     newp->w_localdir = (oldp->w_localdir == NULL)
@@ -169804,8 +174652,8 @@
             newp->w_skipcol = oldp->w_skipcol;
         }
         newp->w_botline = oldp->w_botline;
-        newp->w_prev_height = oldp->w_height - WINBAR_HEIGHT(oldp);
-        newp->w_prev_winrow = oldp->w_winrow + 2 * WINBAR_HEIGHT(oldp);
+        newp->w_prev_height = oldp->w_height -  0 ;
+        newp->w_prev_winrow = oldp->w_winrow + 2 *  0 ;
     }
 
     for (i = 0; i < oldp->w_tagstacklen; i++)
@@ -169841,7 +174689,7 @@
 }
 
     int
-win_valid_popup(win_T *win UNUSED)
+win_valid_popup(win_T *win  __attribute__((unused)) )
 {
     return FALSE;
 }
@@ -169855,11 +174703,13 @@
     {
         return FALSE;
     }
-    FOR_ALL_WINDOWS(wp)
+     for ((wp) = firstwin; (wp) != NULL; (wp) = (wp)->w_next) 
+     {
         if (wp == win)
         {
             return TRUE;
         }
+     }
     return win_valid_popup(win);
 }
 
@@ -169868,11 +174718,13 @@
 {
     win_T   *wp;
 
-    FOR_ALL_WINDOWS(wp)
+     for ((wp) = firstwin; (wp) != NULL; (wp) = (wp)->w_next) 
+     {
         if (wp->w_id == id)
         {
             return wp;
         }
+     }
     return NULL;
 }
 
@@ -169886,9 +174738,9 @@
     {
         return FALSE;
     }
-    FOR_ALL_TABPAGES(tp)
+     for ((tp) = first_tabpage; (tp) != NULL; (tp) = (tp)->tp_next) 
     {
-        FOR_ALL_WINDOWS_IN_TAB(tp, wp)
+         for ((wp) = ((tp) == NULL || (tp) == curtab)             ? firstwin : (tp)->tp_firstwin; (wp); (wp) = (wp)->w_next) 
         {
             if (wp == win)
             {
@@ -169905,8 +174757,10 @@
     win_T       *wp;
     int         count = 0;
 
-    FOR_ALL_WINDOWS(wp)
+     for ((wp) = firstwin; (wp) != NULL; (wp) = (wp)->w_next) 
+     {
         ++count;
+     }
     return count;
 }
 
@@ -169922,7 +174776,7 @@
     }
     else
     {
-        maxcount = (VISIBLE_HEIGHT(curwin) + curwin->w_status_height - (p_wh - p_wmh)) / (p_wmh + statusline_height(curwin));
+        maxcount = ( (curwin)->w_height  + curwin->w_status_height - (p_wh - p_wmh)) / (p_wmh + statusline_height(curwin));
     }
 
     if (maxcount < 2)
@@ -169977,7 +174831,7 @@
     {
         return;
     }
-    if (ONE_WINDOW)
+    if ( (firstwin == lastwin) )
     {
         beep_flush();
         return;
@@ -170074,18 +174928,20 @@
     frame_T     *frp;
     int         n;
 
-    if (ONE_WINDOW)
+    if ( (firstwin == lastwin) )
     {
         beep_flush();
         return;
     }
 
-    FOR_ALL_FRAMES(frp, curwin->w_frame->fr_parent->fr_child)
+     for ((frp) = curwin->w_frame->fr_parent->fr_child; (frp) != NULL; (frp) = (frp)->fr_next) 
+     {
         if (frp->fr_win == NULL)
         {
             emsg(_(e_cannot_rotate_when_another_window_is_split));
             return;
         }
+     }
 
     while (count--)
     {
@@ -170149,7 +175005,7 @@
     int         height = wp->w_height;
     frame_T     *unflat_altfr;
 
-    if (ONE_WINDOW)
+    if ( (firstwin == lastwin) )
     {
         return OK;
     }
@@ -170284,7 +175140,7 @@
 
         if (dir != 'v')
         {
-            n = frame_minwidth(topfr, NOWIN);
+            n = frame_minwidth(topfr,  ((win_T *)-1) );
             if (col + width >= firstwin->w_wincol + topframe->fr_width)
             {
                 extra_sep = 1;
@@ -170306,13 +175162,13 @@
             else
             {
                 next_curwin_size = -1;
-                FOR_ALL_FRAMES(fr, topfr->fr_child)
+                 for ((fr) = topfr->fr_child; (fr) != NULL; (fr) = (fr)->fr_next) 
                 {
                     if (!frame_fixed_width(fr))
                     {
                         continue;
                     }
-                    n = frame_minwidth(fr, NOWIN);
+                    n = frame_minwidth(fr,  ((win_T *)-1) );
                     new_size = fr->fr_width;
                     if (frame_has_win(fr, next_curwin))
                     {
@@ -170359,7 +175215,7 @@
             }
         }
 
-        FOR_ALL_FRAMES(fr, topfr->fr_child)
+         for ((fr) = topfr->fr_child; (fr) != NULL; (fr) = (fr)->fr_next) 
         {
             wincount = 1;
             if (fr->fr_next == NULL)
@@ -170377,7 +175233,7 @@
             }
             else
             {
-                n = frame_minwidth(fr, NOWIN);
+                n = frame_minwidth(fr,  ((win_T *)-1) );
                 wincount = (n + (fr->fr_next == NULL ? extra_sep : 0))
                                                                 / (p_wmw + 1);
                 m = frame_minwidth(fr, next_curwin);
@@ -170448,13 +175304,13 @@
             else
             {
                 next_curwin_size = -1;
-                FOR_ALL_FRAMES(fr, topfr->fr_child)
+                 for ((fr) = topfr->fr_child; (fr) != NULL; (fr) = (fr)->fr_next) 
                 {
                     if (!frame_fixed_height(fr))
                     {
                         continue;
                     }
-                    n = frame_minheight(fr, NOWIN);
+                    n = frame_minheight(fr,  ((win_T *)-1) );
                     new_size = fr->fr_height;
                     if (frame_has_win(fr, next_curwin))
                     {
@@ -170501,7 +175357,7 @@
             }
         }
 
-        FOR_ALL_FRAMES(fr, topfr->fr_child)
+         for ((fr) = topfr->fr_child; (fr) != NULL; (fr) = (fr)->fr_next) 
         {
             wincount = 1;
             if (fr->fr_next == NULL)
@@ -170519,7 +175375,7 @@
             }
             else
             {
-                n = frame_minheight(fr, NOWIN);
+                n = frame_minheight(fr,  ((win_T *)-1) );
                 wincount = frame_wincount_in_height(fr);
                 m = frame_minheight(fr, next_curwin);
                 if (has_next_curwin)
@@ -170599,7 +175455,7 @@
 
     ++RedrawingDisabled;
 
-    for (wp = firstwin; wp != NULL && !ONE_WINDOW; )
+    for (wp = firstwin; wp != NULL && ! (firstwin == lastwin) ; )
     {
         if (wp->w_buffer == buf && (!keep_curwin || wp != curwin) && !(win_locked(wp) || wp->w_buffer->b_locked > 0))
         {
@@ -170625,7 +175481,8 @@
         nexttp = tp->tp_next;
         if (tp != curtab)
         {
-            FOR_ALL_WINDOWS_IN_TAB(tp, wp)
+             for ((wp) = ((tp) == NULL || (tp) == curtab)             ? firstwin : (tp)->tp_firstwin; (wp); (wp) = (wp)->w_next) 
+             {
                 if (wp->w_buffer == buf && !(win_locked(wp) || wp->w_buffer->b_locked > 0))
                 {
                     if (window_layout_locked(CMD_SIZE))
@@ -170637,6 +175494,7 @@
                     nexttp = first_tabpage;
                     break;
                 }
+             }
         }
     }
 
@@ -170664,7 +175522,7 @@
     win_T       *wp;
     int         seen_one = FALSE;
 
-    FOR_ALL_WINDOWS(wp)
+     for ((wp) = firstwin; (wp) != NULL; (wp) = (wp)->w_next) 
     {
         if (!is_aucmd_win(wp))
         {
@@ -170681,7 +175539,7 @@
     static int
 close_last_window_tabpage(win_T       *win, int         free_buf, tabpage_T   *prev_curtab)
 {
-    if (!ONE_WINDOW)
+    if (! (firstwin == lastwin) )
     {
         return FALSE;
     }
@@ -170713,7 +175571,6 @@
     static void
 win_close_buffer(win_T *win, int action, int abort_if_last)
 {
-
     if (win->w_buffer != NULL)
     {
         bufref_T    bufref;
@@ -170913,7 +175770,7 @@
         }
     }
 
-    if (ONE_WINDOW && curwin->w_locked && curbuf->b_locked_split && first_tabpage->tp_next != NULL)
+    if ( (firstwin == lastwin)  && curwin->w_locked && curbuf->b_locked_split && first_tabpage->tp_next != NULL)
     {
         apply_autocmds(EVENT_TABLEAVE, NULL, NULL, FALSE, curbuf);
     }
@@ -170987,7 +175844,7 @@
 snapshot_windows_scroll_size(void)
 {
     win_T *wp;
-    FOR_ALL_WINDOWS(wp)
+     for ((wp) = firstwin; (wp) != NULL; (wp) = (wp)->w_next) 
     {
         wp->w_last_topline = wp->w_topline;
         wp->w_last_leftcol = wp->w_leftcol;
@@ -171009,18 +175866,14 @@
     }
 }
 
-#define CWSR_SCROLLED   1
-#define CWSR_RESIZED    2
-
     static void
-check_window_scroll_resize(int     *size_count, win_T   **first_scroll_win, win_T   **first_size_win, list_T  *winlist UNUSED, dict_T  *v_event UNUSED)
+check_window_scroll_resize(int     *size_count, win_T   **first_scroll_win, win_T   **first_size_win, list_T  *winlist  __attribute__((unused)) , dict_T  *v_event  __attribute__((unused)) )
 {
-
     win_T *wp;
-    FOR_ALL_WINDOWS(wp)
+     for ((wp) = firstwin; (wp) != NULL; (wp) = (wp)->w_next) 
     {
-        int ignore_scroll = event_ignored(EVENT_WINSCROLLED, wp->w_p_eiw);
-        int size_changed = !event_ignored(EVENT_WINRESIZED, wp->w_p_eiw)
+        int ignore_scroll = event_ignored(EVENT_WINSCROLLED, wp-> w_onebuf_opt.wo_eiw );
+        int size_changed = !event_ignored(EVENT_WINRESIZED, wp-> w_onebuf_opt.wo_eiw )
                             && (wp->w_last_width != wp->w_width || wp->w_last_height != wp->w_height);
         if (size_changed)
         {
@@ -171237,7 +176090,7 @@
     int row;
     int col;
 
-    if (tp == NULL ? ONE_WINDOW : tp->tp_firstwin == tp->tp_lastwin)
+    if (tp == NULL ?  (firstwin == lastwin)  : tp->tp_firstwin == tp->tp_lastwin)
     {
         return NULL;
     }
@@ -171260,7 +176113,7 @@
 
     if (frp_close->fr_parent->fr_layout == FR_COL)
     {
-        if (frp2->fr_win != NULL && frp2->fr_win->w_p_wfh)
+        if (frp2->fr_win != NULL && frp2->fr_win-> w_onebuf_opt.wo_wfh )
         {
             frp = frp_close->fr_prev;
             frp3 = frp_close->fr_next;
@@ -171278,7 +176131,7 @@
                 }
                 if (frp3 != NULL)
                 {
-                    if (frp3->fr_win != NULL && !frp3->fr_win->w_p_wfh)
+                    if (frp3->fr_win != NULL && !frp3->fr_win-> w_onebuf_opt.wo_wfh )
                     {
                         frp2 = frp3;
                         wp = frp3->fr_win;
@@ -171293,7 +176146,7 @@
     }
     else
     {
-        if (frp2->fr_win != NULL && frp2->fr_win->w_p_wfw)
+        if (frp2->fr_win != NULL && frp2->fr_win-> w_onebuf_opt.wo_wfw )
         {
             frp = frp_close->fr_prev;
             frp3 = frp_close->fr_next;
@@ -171311,7 +176164,7 @@
                 }
                 if (frp3 != NULL)
                 {
-                    if (frp3->fr_win != NULL && !frp3->fr_win->w_p_wfw)
+                    if (frp3->fr_win != NULL && !frp3->fr_win-> w_onebuf_opt.wo_wfw )
                     {
                         frp2 = frp3;
                         wp = frp3->fr_win;
@@ -171357,8 +176210,10 @@
 
     frp->fr_parent->fr_layout = frp->fr_layout;
     frp->fr_parent->fr_child = frp->fr_child;
-    FOR_ALL_FRAMES(frp2, frp->fr_child)
+     for ((frp2) = frp->fr_child; (frp2) != NULL; (frp2) = (frp2)->fr_next) 
+     {
         frp2->fr_parent = frp->fr_parent;
+     }
     frp->fr_parent->fr_win = frp->fr_win;
     if (frp->fr_win != NULL)
     {
@@ -171454,7 +176309,7 @@
     frame_T *other_fr;
     frame_T *target_fr;
 
-    if (tp == NULL ? ONE_WINDOW : tp->tp_firstwin == tp->tp_lastwin)
+    if (tp == NULL ?  (firstwin == lastwin)  : tp->tp_firstwin == tp->tp_lastwin)
     {
         return alt_tabpage()->tp_curwin->w_frame;
     }
@@ -171552,11 +176407,13 @@
         return frp->fr_win == wp;
     }
 
-    FOR_ALL_FRAMES(p, frp->fr_child)
+     for ((p) = frp->fr_child; (p) != NULL; (p) = (p)->fr_next) 
+     {
         if (frame_has_win(p, wp))
         {
             return TRUE;
         }
+     }
     return FALSE;
 }
 
@@ -171578,17 +176435,17 @@
             set_option_value((char_u *)"cmdheight", new_ch, NULL, 0);
         }
         min_set_ch = save_ch;
-        height = MIN(height, ROWS_AVAIL);
+        height = MIN(height,  (Rows - p_ch - tabline_height()) );
     }
     if (topfrp->fr_win != NULL)
     {
-        win_new_height(topfrp->fr_win, height - topfrp->fr_win->w_status_height - WINBAR_HEIGHT(topfrp->fr_win));
+        win_new_height(topfrp->fr_win, height - topfrp->fr_win->w_status_height -  0 );
     }
     else if (topfrp->fr_layout == FR_ROW)
     {
         do
         {
-            FOR_ALL_FRAMES(frp, topfrp->fr_child)
+             for ((frp) = topfrp->fr_child; (frp) != NULL; (frp) = (frp)->fr_next) 
             {
                 frame_new_height(frp, height, topfirst, wfh, set_ch);
                 if (frp->fr_height > height)
@@ -171603,7 +176460,6 @@
     }
     else
     {
-
         frp = topfrp->fr_child;
         if (wfh)
         {
@@ -171684,24 +176540,28 @@
 {
     if (frp->fr_win != NULL)
     {
-        return frp->fr_win->w_p_wfh;
+        return frp->fr_win-> w_onebuf_opt.wo_wfh ;
     }
 
     if (frp->fr_layout == FR_ROW)
     {
-        FOR_ALL_FRAMES(frp, frp->fr_child)
+         for ((frp) = frp->fr_child; (frp) != NULL; (frp) = (frp)->fr_next) 
+         {
             if (frame_fixed_height(frp))
             {
                 return TRUE;
             }
+         }
         return FALSE;
     }
 
-    FOR_ALL_FRAMES(frp, frp->fr_child)
+     for ((frp) = frp->fr_child; (frp) != NULL; (frp) = (frp)->fr_next) 
+     {
         if (!frame_fixed_height(frp))
         {
             return FALSE;
         }
+     }
     return TRUE;
 }
 
@@ -171710,24 +176570,28 @@
 {
     if (frp->fr_win != NULL)
     {
-        return frp->fr_win->w_p_wfw;
+        return frp->fr_win-> w_onebuf_opt.wo_wfw ;
     }
 
     if (frp->fr_layout == FR_COL)
     {
-        FOR_ALL_FRAMES(frp, frp->fr_child)
+         for ((frp) = frp->fr_child; (frp) != NULL; (frp) = (frp)->fr_next) 
+         {
             if (frame_fixed_width(frp))
             {
                 return TRUE;
             }
+         }
         return FALSE;
     }
 
-    FOR_ALL_FRAMES(frp, frp->fr_child)
+     for ((frp) = frp->fr_child; (frp) != NULL; (frp) = (frp)->fr_next) 
+     {
         if (!frame_fixed_width(frp))
         {
             return FALSE;
         }
+     }
     return TRUE;
 }
 
@@ -171743,8 +176607,10 @@
     }
     else if (frp->fr_layout == FR_ROW)
     {
-        FOR_ALL_FRAMES(frp, frp->fr_child)
+         for ((frp) = frp->fr_child; (frp) != NULL; (frp) = (frp)->fr_next) 
+         {
             frame_add_statusline(frp);
+         }
     }
     else
     {
@@ -171784,7 +176650,7 @@
     {
         do
         {
-            FOR_ALL_FRAMES(frp, topfrp->fr_child)
+             for ((frp) = topfrp->fr_child; (frp) != NULL; (frp) = (frp)->fr_next) 
             {
                 frame_new_width(frp, width, leftfirst, wfw);
                 if (frp->fr_width > width)
@@ -171797,7 +176663,6 @@
     }
     else
     {
-
         frp = topfrp->fr_child;
         if (wfw)
         {
@@ -171897,8 +176762,10 @@
     }
     else if (frp->fr_layout == FR_COL)
     {
-        FOR_ALL_FRAMES(frp, frp->fr_child)
+         for ((frp) = frp->fr_child; (frp) != NULL; (frp) = (frp)->fr_next) 
+         {
             frame_set_vsep(frp, add);
+         }
     }
     else
     {
@@ -171920,7 +176787,7 @@
     static void
 frame_fix_height(win_T *wp)
 {
-    wp->w_frame->fr_height = VISIBLE_HEIGHT(wp) + wp->w_status_height;
+    wp->w_frame->fr_height =  (wp)->w_height  + wp->w_status_height;
 }
 
     static int
@@ -171945,14 +176812,14 @@
                 {
                     ++m;
                 }
-                m += WINBAR_HEIGHT(curwin);
+                m +=  0 ;
             }
         }
     }
     else if (topfrp->fr_layout == FR_ROW)
     {
         m = 0;
-        FOR_ALL_FRAMES(frp, topfrp->fr_child)
+         for ((frp) = topfrp->fr_child; (frp) != NULL; (frp) = (frp)->fr_next) 
         {
             n = frame_minheight(frp, next_curwin);
             if (n > m)
@@ -171964,8 +176831,10 @@
     else
     {
         m = 0;
-        FOR_ALL_FRAMES(frp, topfrp->fr_child)
+         for ((frp) = topfrp->fr_child; (frp) != NULL; (frp) = (frp)->fr_next) 
+         {
             m += frame_minheight(frp, next_curwin);
+         }
     }
 
     return m;
@@ -171985,7 +176854,7 @@
     else if (topfrp->fr_layout == FR_ROW)
     {
         c = 0;
-        FOR_ALL_FRAMES(frp, topfrp->fr_child)
+         for ((frp) = topfrp->fr_child; (frp) != NULL; (frp) = (frp)->fr_next) 
         {
             n = frame_wincount_in_height(frp);
             if (n > c)
@@ -171997,8 +176866,10 @@
     else
     {
         c = 0;
-        FOR_ALL_FRAMES(frp, topfrp->fr_child)
+         for ((frp) = topfrp->fr_child; (frp) != NULL; (frp) = (frp)->fr_next) 
+         {
             c += frame_wincount_in_height(frp);
+         }
     }
 
     return c;
@@ -172029,7 +176900,7 @@
     else if (topfrp->fr_layout == FR_COL)
     {
         m = 0;
-        FOR_ALL_FRAMES(frp, topfrp->fr_child)
+         for ((frp) = topfrp->fr_child; (frp) != NULL; (frp) = (frp)->fr_next) 
         {
             n = frame_minwidth(frp, next_curwin);
             if (n > m)
@@ -172041,8 +176912,10 @@
     else
     {
         m = 0;
-        FOR_ALL_FRAMES(frp, topfrp->fr_child)
+         for ((frp) = topfrp->fr_child; (frp) != NULL; (frp) = (frp)->fr_next) 
+         {
             m += frame_minwidth(frp, next_curwin);
+         }
     }
 
     return m;
@@ -172099,7 +176972,7 @@
         win_close(wp, !buf_hide(wp->w_buffer) && !bufIsChanged(wp->w_buffer));
     }
 
-    if (message && !ONE_WINDOW)
+    if (message && ! (firstwin == lastwin) )
     {
         emsg(_(e_other_window_contains_changes));
     }
@@ -172157,7 +177030,8 @@
     }
     win_init_some(wp, curwin);
 
-    RESET_BINDING(wp);
+     (wp)-> w_onebuf_opt.wo_scb  = FALSE;
+     (wp)-> w_onebuf_opt.wo_crb  = FALSE;
     new_frame(wp);
     return wp;
 }
@@ -172169,7 +177043,8 @@
     ++buf->b_nwindows;
     win_init_empty(wp);
 
-    VIM_CLEAR(wp->w_localdir);
+     vim_free(wp->w_localdir);
+     (wp->w_localdir) = NULL;
 }
 
     static int
@@ -172196,7 +177071,8 @@
     {
         win_init(curwin, oldwin, 0);
 
-        RESET_BINDING(curwin);
+         (curwin)-> w_onebuf_opt.wo_scb  = FALSE;
+         (curwin)-> w_onebuf_opt.wo_crb  = FALSE;
     }
 
     new_frame(curwin);
@@ -172205,7 +177081,7 @@
         return FAIL;
     }
     topframe = curwin->w_frame;
-    topframe->fr_width = COLUMNS_WITHOUT_TPL();
+    topframe->fr_width =  Columns ;
     topframe->fr_height = Rows - p_ch;
 
     return OK;
@@ -172214,7 +177090,7 @@
     static void
 new_frame(win_T *wp)
 {
-    frame_T *frp = ALLOC_CLEAR_ONE(frame_T);
+    frame_T *frp =  (frame_T *)alloc_clear(sizeof(frame_T)) ;
 
     wp->w_frame = frp;
     if (frp == NULL)
@@ -172228,9 +177104,9 @@
     void
 win_init_size(void)
 {
-    firstwin->w_height = ROWS_AVAIL;
-    firstwin->w_prev_height = ROWS_AVAIL;
-    topframe->fr_height = ROWS_AVAIL;
+    firstwin->w_height =  (Rows - p_ch - tabline_height()) ;
+    firstwin->w_prev_height =  (Rows - p_ch - tabline_height()) ;
+    topframe->fr_height =  (Rows - p_ch - tabline_height()) ;
     firstwin->w_width = topframe->fr_width;
 }
 
@@ -172239,7 +177115,7 @@
 {
     tabpage_T   *tp;
 
-    tp = ALLOC_CLEAR_ONE(tabpage_T);
+    tp =  (tabpage_T *)alloc_clear(sizeof(tabpage_T)) ;
     if (tp == NULL)
     {
         return NULL;
@@ -172396,11 +177272,13 @@
 {
     tabpage_T   *tp;
 
-    FOR_ALL_TABPAGES(tp)
+     for ((tp) = first_tabpage; (tp) != NULL; (tp) = (tp)->tp_next) 
+     {
         if (tp == tpc)
         {
             return TRUE;
         }
+     }
     return FALSE;
 }
 
@@ -172410,11 +177288,11 @@
     tabpage_T   *tp;
     win_T       *wp;
 
-    FOR_ALL_TABPAGES(tp)
+     for ((tp) = first_tabpage; (tp) != NULL; (tp) = (tp)->tp_next) 
     {
         if (tp == tpc)
         {
-            FOR_ALL_WINDOWS_IN_TAB(tp, wp)
+             for ((wp) = ((tp) == NULL || (tp) == curtab)             ? firstwin : (tp)->tp_firstwin; (wp); (wp) = (wp)->w_next) 
             {
                 if (win_valid_any_tab(wp))
                 {
@@ -172514,7 +177392,7 @@
     tp->tp_prevwin = prevwin;
     tp->tp_firstwin = firstwin;
     tp->tp_lastwin = lastwin;
-    tp->tp_old_Rows = ROWS_AVAIL;
+    tp->tp_old_Rows =  (Rows - p_ch - tabline_height()) ;
     if (tp->tp_old_Columns != -1)
     {
         tp->tp_old_Columns = topframe->fr_width;
@@ -172551,11 +177429,11 @@
 
     reset_dragwin();
 
-    if (curtab->tp_old_Rows != ROWS_AVAIL || (old_off != firstwin->w_winrow))
+    if (curtab->tp_old_Rows !=  (Rows - p_ch - tabline_height())  || (old_off != firstwin->w_winrow))
     {
         shell_new_rows();
     }
-    if (curtab->tp_old_Columns != COLUMNS_WITHOUT_TPL() || curtab->tp_old_coloff != TPL_LCOL())
+    if (curtab->tp_old_Columns !=  Columns  || curtab->tp_old_coloff !=  0 )
     {
         if (starting == 0)
         {
@@ -172654,7 +177532,8 @@
 {
     if (trigger_enter_autocmds || trigger_leave_autocmds)
     {
-        CHECK_CMDWIN;
+         if (cmdwin_type != 0)
+             {           emsg(_(e_invalid_in_cmdline_window));           return;     }
     }
 
     set_keep_msg(NULL, 0);
@@ -172731,11 +177610,13 @@
     }
     else
     {
-        FOR_ALL_TABPAGES(tp)
+         for ((tp) = first_tabpage; (tp) != NULL; (tp) = (tp)->tp_next) 
+         {
             if (tp->tp_next == curtab)
             {
                 break;
             }
+         }
         if (tp == NULL)
         {
             return;
@@ -172760,7 +177641,6 @@
     void
 win_goto(win_T *wp)
 {
-
     if (text_or_buf_locked())
     {
         beep_flush();
@@ -172946,9 +177826,9 @@
 
         if (globaldir == NULL)
         {
-            char_u      cwd[MAXPATHL];
+            char_u      cwd[ PATH_MAX ];
 
-            if (mch_dirname(cwd, MAXPATHL) == OK)
+            if (mch_dirname(cwd,  PATH_MAX ) == OK)
             {
                 globaldir = vim_strsave(cwd);
             }
@@ -172971,7 +177851,8 @@
     else if (globaldir != NULL)
     {
         vim_ignored = mch_chdir((char *)globaldir);
-        VIM_CLEAR(globaldir);
+         vim_free(globaldir);
+         (globaldir) = NULL;
         last_chdir_reason = NULL;
         shorten_fnames(TRUE);
     }
@@ -173067,7 +177948,7 @@
         redraw_later(UPD_VALID);
     }
 
-    if (curwin->w_height < p_wh && !curwin->w_p_wfh)
+    if (curwin->w_height < p_wh && !curwin-> w_onebuf_opt.wo_wfh )
     {
         win_setheight((int)p_wh);
     }
@@ -173076,15 +177957,13 @@
         win_setheight(1);
     }
 
-    if (curwin->w_width < p_wiw && !curwin->w_p_wfw)
+    if (curwin->w_width < p_wiw && !curwin-> w_onebuf_opt.wo_wfw )
     {
         win_setwidth((int)p_wiw);
     }
 
     setmouse();
 
-    DO_AUTOCHDIR;
-
     return did_decrement;
 }
 
@@ -173099,11 +177978,13 @@
     }
     else
     {
-        FOR_ALL_WINDOWS(wp)
+         for ((wp) = firstwin; (wp) != NULL; (wp) = (wp)->w_next) 
+         {
             if (wp->w_buffer == buf)
             {
                 break;
             }
+         }
     }
     if (wp != NULL)
     {
@@ -173123,14 +178004,17 @@
         return wp;
     }
 
-    FOR_ALL_TABPAGES(tp)
+     for ((tp) = first_tabpage; (tp) != NULL; (tp) = (tp)->tp_next) 
+     {
         if (tp != curtab)
         {
-            FOR_ALL_WINDOWS_IN_TAB(tp, wp)
+             for ((wp) = ((tp) == NULL || (tp) == curtab)             ? firstwin : (tp)->tp_firstwin; (wp); (wp) = (wp)->w_next) 
+             {
                 if (wp->w_buffer == buf)
                 {
                     break;
                 }
+             }
             if (wp != NULL)
             {
                 goto_tabpage_win(tp, wp);
@@ -173141,6 +178025,7 @@
                 break;
             }
         }
+     }
     return wp;
 }
 
@@ -173151,7 +178036,7 @@
 {
     win_T       *new_wp;
 
-    new_wp = ALLOC_CLEAR_ONE(win_T);
+    new_wp =  (win_T *)alloc_clear(sizeof(win_T)) ;
     if (new_wp == NULL)
     {
         return NULL;
@@ -173171,17 +178056,17 @@
     {
         win_append(after, new_wp);
     }
-    new_wp->w_wincol = TPL_LCOL();
-    new_wp->w_width = COLUMNS_WITHOUT_TPL();
+    new_wp->w_wincol =  0 ;
+    new_wp->w_width =  Columns ;
 
     new_wp->w_topline = 1;
     new_wp->w_botline = 2;
     new_wp->w_cursor.lnum = 1;
     new_wp->w_scbind_pos = 1;
 
-    new_wp->w_allbuf_opt.wo_so = new_wp->w_p_so = -1;
-    new_wp->w_allbuf_opt.wo_sop = new_wp->w_p_sop = -1;
-    new_wp->w_allbuf_opt.wo_siso = new_wp->w_p_siso = -1;
+    new_wp->w_allbuf_opt.wo_so = new_wp-> w_onebuf_opt.wo_so  = -1;
+    new_wp->w_allbuf_opt.wo_sop = new_wp-> w_onebuf_opt.wo_sop  = -1;
+    new_wp->w_allbuf_opt.wo_siso = new_wp-> w_onebuf_opt.wo_siso  = -1;
 
     new_wp->w_fraction = 0;
     new_wp->w_prev_fraction_row = -1;
@@ -173227,11 +178112,13 @@
         {
             prevwin = NULL;
         }
-        FOR_ALL_TABPAGES(ttp)
+         for ((ttp) = first_tabpage; (ttp) != NULL; (ttp) = (ttp)->tp_next) 
+         {
             if (ttp->tp_prevwin == wp)
             {
                 ttp->tp_prevwin = NULL;
             }
+         }
     }
     win_free_lsize(wp);
 
@@ -173242,13 +178129,16 @@
     vim_free(wp->w_localdir);
     vim_free(wp->w_prevdir);
 
-    FOR_ALL_BUFFERS(buf)
-        FOR_ALL_BUF_WININFO(buf, wip)
+     for ((buf) = firstbuf; (buf) != NULL; (buf) = (buf)->b_next) 
+     {
+         for ((wip) = (buf)->b_wininfo; (wip) != NULL; (wip) = (wip)->wi_next) 
+         {
             if (wip->wi_win == wp)
             {
                 wininfo_T       *wip2;
 
-                FOR_ALL_BUF_WININFO(buf, wip2)
+                 for ((wip2) = (buf)->b_wininfo; (wip2) != NULL; (wip2) = (wip2)->wi_next) 
+                 {
                     if (wip2 != wip && wip2->wi_win == NULL)
                     {
                         if (wip2->wi_next != NULL)
@@ -173266,9 +178156,12 @@
                         free_wininfo(wip2);
                         break;
                     }
+                 }
 
                 wip->wi_win = NULL;
             }
+         }
+     }
 
     clear_matches(wp);
 
@@ -173294,7 +178187,7 @@
     int
 win_unlisted(win_T *wp)
 {
-    return is_aucmd_win(wp) || WIN_IS_POPUP(wp);
+    return is_aucmd_win(wp) ||  0 ;
 }
 
     static void
@@ -173410,7 +178303,7 @@
 win_alloc_lines(win_T *wp)
 {
     wp->w_lines_valid = 0;
-    wp->w_lines = ALLOC_CLEAR_MULT(wline_T, Rows);
+    wp->w_lines =  (wline_T *)alloc_clear(sizeof(wline_T) * (Rows)) ;
     if (wp->w_lines == NULL)
     {
         return FAIL;
@@ -173423,14 +178316,15 @@
 {
     if (wp != NULL)
     {
-        VIM_CLEAR(wp->w_lines);
+         vim_free(wp->w_lines);
+         (wp->w_lines) = NULL;
     }
 }
 
     void
 shell_new_rows(void)
 {
-    int         h = (int)ROWS_AVAIL;
+    int         h = (int) (Rows - p_ch - tabline_height()) ;
 
     if (firstwin == NULL)
     {
@@ -173467,7 +178361,7 @@
         return;
     }
 
-    int w = COLUMNS_WITHOUT_TPL();
+    int w =  Columns ;
 
     frame_new_width(topframe, w, FALSE, TRUE);
     if (!frame_check_width(topframe, w))
@@ -173499,9 +178393,9 @@
         return;
     }
 
-    ((int *)gap->ga_data)[gap->ga_len++] = ROWS_AVAIL - last_stl_height(FALSE);
+    ((int *)gap->ga_data)[gap->ga_len++] =  (Rows - p_ch - tabline_height())  - last_stl_height(FALSE);
 
-    FOR_ALL_WINDOWS(wp)
+     for ((wp) = firstwin; (wp) != NULL; (wp) = (wp)->w_next) 
     {
         ((int *)gap->ga_data)[gap->ga_len++] =
             wp->w_width + wp->w_vsep_width;
@@ -173516,12 +178410,12 @@
     int i;
     int j;
 
-    if (win_count() * 2 + 1 == gap->ga_len && ((int *)gap->ga_data)[0] == ROWS_AVAIL - last_stl_height(FALSE))
+    if (win_count() * 2 + 1 == gap->ga_len && ((int *)gap->ga_data)[0] ==  (Rows - p_ch - tabline_height())  - last_stl_height(FALSE))
     {
         for (j = 0; j < 2; ++j)
         {
             i = 1;
-            FOR_ALL_WINDOWS(wp)
+             for ((wp) = firstwin; (wp) != NULL; (wp) = (wp)->w_next) 
             {
                 frame_setwidth(wp->w_frame, ((int *)gap->ga_data)[i++]);
                 win_setheight_win(((int *)gap->ga_data)[i++], wp);
@@ -173535,7 +178429,7 @@
 win_comp_pos(void)
 {
     int         row = tabline_height();
-    int         col = TPL_LCOL();
+    int         col =  0 ;
 
     frame_comp_pos(topframe, &row, &col);
 }
@@ -173559,7 +178453,7 @@
             redraw_win_later(wp, UPD_NOT_VALID);
             wp->w_redr_status = true;
         }
-        h = VISIBLE_HEIGHT(wp) + wp->w_status_height;
+        h =  (wp)->w_height  + wp->w_status_height;
         *row += h > topfrp->fr_height ? topfrp->fr_height : h;
         *col += wp->w_width + wp->w_vsep_width;
     }
@@ -173567,7 +178461,7 @@
     {
         startrow = *row;
         startcol = *col;
-        FOR_ALL_FRAMES(frp, topfrp->fr_child)
+         for ((frp) = topfrp->fr_child; (frp) != NULL; (frp) = (frp)->fr_next) 
         {
             if (topfrp->fr_layout == FR_ROW)
             {
@@ -173614,7 +178508,7 @@
         {
             height = 1;
         }
-        height += WINBAR_HEIGHT(curwin);
+        height +=  0 ;
     }
 
     frame_setheight(win->w_frame, height + win->w_status_height);
@@ -173663,9 +178557,9 @@
         {
             room = 0;
             room_reserved = 0;
-            FOR_ALL_FRAMES(frp, curfrp->fr_parent->fr_child)
+             for ((frp) = curfrp->fr_parent->fr_child; (frp) != NULL; (frp) = (frp)->fr_next) 
             {
-                if (frp != curfrp && frp->fr_win != NULL && frp->fr_win->w_p_wfh)
+                if (frp != curfrp && frp->fr_win != NULL && frp->fr_win-> w_onebuf_opt.wo_wfh )
                 {
                     room_reserved += frp->fr_height;
                 }
@@ -173681,7 +178575,7 @@
             }
             else
             {
-                room_cmdline = Rows - p_ch - (lastwin->w_winrow + VISIBLE_HEIGHT(lastwin) + lastwin->w_status_height);
+                room_cmdline = Rows - p_ch - (lastwin->w_winrow +  (lastwin)->w_height  + lastwin->w_status_height);
                 if (room_cmdline < 0)
                 {
                     room_cmdline = 0;
@@ -173697,7 +178591,7 @@
                 height = room + room_cmdline;
                 break;
             }
-            frame_setheight(curfrp->fr_parent, height + frame_minheight(curfrp->fr_parent, NOWIN) - (int)p_wmh - 1);
+            frame_setheight(curfrp->fr_parent, height + frame_minheight(curfrp->fr_parent,  ((win_T *)-1) ) - (int)p_wmh - 1);
         }
 
         take = height - curfrp->fr_height;
@@ -173736,7 +178630,7 @@
             while (frp != NULL && take != 0)
             {
                 h = frame_minheight(frp, NULL);
-                if (room_reserved > 0 && frp->fr_win != NULL && frp->fr_win->w_p_wfh)
+                if (room_reserved > 0 && frp->fr_win != NULL && frp->fr_win-> w_onebuf_opt.wo_wfh )
                 {
                     if (room_reserved >= frp->fr_height)
                     {
@@ -173846,9 +178740,9 @@
         {
             room = 0;
             room_reserved = 0;
-            FOR_ALL_FRAMES(frp, curfrp->fr_parent->fr_child)
+             for ((frp) = curfrp->fr_parent->fr_child; (frp) != NULL; (frp) = (frp)->fr_next) 
             {
-                if (frp != curfrp && frp->fr_win != NULL && frp->fr_win->w_p_wfw)
+                if (frp != curfrp && frp->fr_win != NULL && frp->fr_win-> w_onebuf_opt.wo_wfw )
                 {
                     room_reserved += frp->fr_width;
                 }
@@ -173863,12 +178757,12 @@
             {
                 break;
             }
-            if (run == 2 || curfrp->fr_height >= ROWS_AVAIL)
+            if (run == 2 || curfrp->fr_height >=  (Rows - p_ch - tabline_height()) )
             {
                 width = room;
                 break;
             }
-            frame_setwidth(curfrp->fr_parent, width + frame_minwidth(curfrp->fr_parent, NOWIN) - (int)p_wmw - 1);
+            frame_setwidth(curfrp->fr_parent, width + frame_minwidth(curfrp->fr_parent,  ((win_T *)-1) ) - (int)p_wmw - 1);
         }
 
         take = width - curfrp->fr_width;
@@ -173897,7 +178791,7 @@
             while (frp != NULL && take != 0)
             {
                 w = frame_minwidth(frp, NULL);
-                if (room_reserved > 0 && frp->fr_win != NULL && frp->fr_win->w_p_wfw)
+                if (room_reserved > 0 && frp->fr_win != NULL && frp->fr_win-> w_onebuf_opt.wo_wfw )
                 {
                     if (room_reserved >= frp->fr_width)
                     {
@@ -174063,8 +178957,10 @@
         {
             room = 0;
         }
-        FOR_ALL_FRAMES(fr, curfr->fr_next)
+         for ((fr) = curfr->fr_next; (fr) != NULL; (fr) = (fr)->fr_next) 
+         {
             room += fr->fr_height - frame_minheight(fr, NULL);
+         }
         fr = curfr;
     }
 
@@ -174180,8 +179076,10 @@
     {
         left = FALSE;
         room = 0;
-        FOR_ALL_FRAMES(fr, curfr->fr_next)
+         for ((fr) = curfr->fr_next; (fr) != NULL; (fr) = (fr)->fr_next) 
+         {
             room += fr->fr_width - frame_minwidth(fr, NULL);
+         }
         fr = curfr;
     }
 
@@ -174235,7 +179133,7 @@
     redraw_all_later(UPD_NOT_VALID);
 }
 
-#define FRACTION_MULT   16384L
+enum { FRACTION_MULT = 16384L };
 
     void
 set_fraction(win_T *wp)
@@ -174256,7 +179154,7 @@
 
     skip_update_topline = TRUE;
     win_T       *wp;
-    FOR_ALL_WINDOWS(wp)
+     for ((wp) = firstwin; (wp) != NULL; (wp) = (wp)->w_next) 
     {
         if (wp->w_height != wp->w_prev_height)
         {
@@ -174399,7 +179297,7 @@
 
     if (!exiting && *p_spk == 'c')
     {
-        if (!wp->w_p_sms)
+        if (!wp-> w_onebuf_opt.wo_sms )
         {
             wp->w_skipcol = 0;
         }
@@ -174415,7 +179313,7 @@
     int line_size;
     int         height = wp->w_height;
 
-    if (height > 0 && (!wp->w_p_scb || wp == curwin) && (height < wp->w_buffer->b_ml.ml_line_count || wp->w_topline > 1))
+    if (height > 0 && (!wp-> w_onebuf_opt.wo_scb  || wp == curwin) && (height < wp->w_buffer->b_ml.ml_line_count || wp->w_topline > 1))
     {
         lnum = wp->w_cursor.lnum;
         if (lnum < 1)
@@ -174513,11 +179411,10 @@
     void
 win_comp_scroll(win_T *wp)
 {
-
-    wp->w_p_scr = ((unsigned)wp->w_height >> 1);
-    if (wp->w_p_scr == 0)
+    wp-> w_onebuf_opt.wo_scr  = ((unsigned)wp->w_height >> 1);
+    if (wp-> w_onebuf_opt.wo_scr  == 0)
     {
-        wp->w_p_scr = 1;
+        wp-> w_onebuf_opt.wo_scr  = 1;
     }
 }
 
@@ -174532,7 +179429,7 @@
         frp = frp->fr_parent;
     }
 
-    while (frp->fr_prev != NULL && frp->fr_layout == FR_LEAF && frp->fr_win->w_p_wfh)
+    while (frp->fr_prev != NULL && frp->fr_layout == FR_LEAF && frp->fr_win-> w_onebuf_opt.wo_wfh )
     {
         frp = frp->fr_prev;
     }
@@ -174648,8 +179545,10 @@
     {
         if (!statusline)
         {
-            FOR_ALL_FRAMES(fp, fr->fr_child)
+             for ((fp) = fr->fr_child; (fp) != NULL; (fp) = (fp)->fr_next) 
+             {
                 last_status_rec(fp, statusline);
+             }
         }
         else
         {
@@ -174657,7 +179556,8 @@
             int         max_stlh = 0;
             int         new_row_height;
 
-            FOR_ALL_FRAMES(fp, fr->fr_child)
+             for ((fp) = fr->fr_child; (fp) != NULL; (fp) = (fp)->fr_next) 
+             {
                 if (fp->fr_win != NULL && fp->fr_win->w_status_height == 0)
                 {
                     int h = statusline_height(fp->fr_win);
@@ -174666,6 +179566,7 @@
                         max_stlh = h;
                     }
                 }
+             }
             if (max_stlh == 0)
             {
                 return;
@@ -174699,7 +179600,7 @@
                 new_row_height = fr->fr_height;
             }
 
-            FOR_ALL_FRAMES(fp, fr->fr_child)
+             for ((fp) = fr->fr_child; (fp) != NULL; (fp) = (fp)->fr_next) 
             {
                 if (fp->fr_win != NULL)
                 {
@@ -174738,7 +179639,7 @@
 }
 
     int
-statusline_height(win_T *wp UNUSED)
+statusline_height(win_T *wp  __attribute__((unused)) )
 {
     int stl_height = 1;
 
@@ -174761,7 +179662,7 @@
     int
 last_stl_height(int         morewin)
 {
-    return (p_ls == 2 || (p_ls == 1 && (morewin || !ONE_WINDOW)))
+    return (p_ls == 2 || (p_ls == 1 && (morewin || ! (firstwin == lastwin) )))
                 ? STATUS_HEIGHT : 0;
 }
 
@@ -174790,7 +179691,7 @@
     }
 
     total = 0;
-    FOR_ALL_TABPAGES(tp)
+     for ((tp) = first_tabpage; (tp) != NULL; (tp) = (tp)->tp_next) 
     {
         n = frame_minheight(tp->tp_topframe, NULL);
         if (total < n)
@@ -174814,11 +179715,13 @@
         return FALSE;
     }
 
-    FOR_ALL_WINDOWS(wp)
+     for ((wp) = firstwin; (wp) != NULL; (wp) = (wp)->w_next) 
+     {
         if (wp->w_buffer != NULL && (!((bt_help(wp->w_buffer) && !bt_help(curbuf))) || wp == curwin) && !is_aucmd_win(wp))
         {
             ++count;
         }
+     }
     return (count <= 1);
 }
 
@@ -174828,7 +179731,10 @@
     win_T       *wp;
     tabpage_T   *tp;
 
-    FOR_ALL_TAB_WINDOWS(tp, wp)
+     for ((tp) = first_tabpage; (tp) != NULL; (tp) = (tp)->tp_next)
+     {
+         for ((wp) = ((tp) == curtab)            ? firstwin : (tp)->tp_firstwin; (wp); (wp) = (wp)->w_next)
+         {
         if ((do_curwin || wp != curwin) && wp->w_buffer == curbuf)
         {
             int need_adjust;
@@ -174859,6 +179765,8 @@
                 wp->w_save_cursor.w_topline_corr = wp->w_topline;
             }
         }
+         }
+     }
 }
 
     void
@@ -174879,10 +179787,13 @@
     win_T       *wp;
     tabpage_T   *tp;
 
-    FOR_ALL_TAB_WINDOWS(tp, wp)
+     for ((tp) = first_tabpage; (tp) != NULL; (tp) = (tp)->tp_next)
+     {
+         for ((wp) = ((tp) == curtab)            ? firstwin : (tp)->tp_firstwin; (wp); (wp) = (wp)->w_next)
+         {
         if (wp->w_buffer == curbuf)
         {
-            if (EQUAL_POS(wp->w_save_cursor.w_cursor_corr, wp->w_cursor) && wp->w_save_cursor.w_cursor_save.lnum != 0)
+            if ( (((wp->w_save_cursor.w_cursor_corr).lnum == (wp->w_cursor).lnum) && ((wp->w_save_cursor.w_cursor_corr).col == (wp->w_cursor).col) && ((wp->w_save_cursor.w_cursor_corr).coladd == (wp->w_cursor).coladd))  && wp->w_save_cursor.w_cursor_save.lnum != 0)
             {
                 wp->w_cursor = wp->w_save_cursor.w_cursor_save;
             }
@@ -174895,6 +179806,8 @@
                 wp->w_valid &= ~VALID_TOPLINE;
             }
         }
+         }
+     }
 }
 
     int
@@ -174912,7 +179825,7 @@
     static int
 make_snapshot_rec(frame_T *fr, frame_T **frp)
 {
-    *frp = ALLOC_CLEAR_ONE(frame_T);
+    *frp =  (frame_T *)alloc_clear(sizeof(frame_T)) ;
     if (*frp == NULL)
     {
         return FAIL;
@@ -175067,11 +179980,13 @@
 
     if (topfrp->fr_layout == FR_ROW)
     {
-        FOR_ALL_FRAMES(frp, topfrp->fr_child)
+         for ((frp) = topfrp->fr_child; (frp) != NULL; (frp) = (frp)->fr_next) 
+         {
             if (frp->fr_height != height)
             {
                 return FALSE;
             }
+         }
     }
 
     return TRUE;
@@ -175089,17 +180004,18 @@
 
     if (topfrp->fr_layout == FR_COL)
     {
-        FOR_ALL_FRAMES(frp, topfrp->fr_child)
+         for ((frp) = topfrp->fr_child; (frp) != NULL; (frp) = (frp)->fr_next) 
+         {
             if (frp->fr_width != width)
             {
                 return FALSE;
             }
+         }
     }
 
     return TRUE;
 }
 
-
    int
 win_locked(win_T *wp)
 {
@@ -175108,11 +180024,10 @@
 
 // ==================== main.c ====================
 
-#define EDIT_NONE   0
-#define EDIT_FILE   1
-#define EDIT_STDIN  2
-#define EDIT_TAG    3
-#define EDIT_QF     4
+enum { EDIT_NONE = 0 };
+enum { EDIT_FILE = 1 };
+enum { EDIT_STDIN = 2 };
+enum { EDIT_TAG = 3 };
 
 static int file_owned(char *fname);
 static void mainerr(int, char_u *);
@@ -175132,18 +180047,12 @@
 
 static char *(main_errors[]) =
 {
-    N_("Unknown option argument"),
-#define ME_UNKNOWN_OPTION       0
-    N_("Too many edit arguments"),
-#define ME_TOO_MANY_ARGS        1
-    N_("Argument missing after"),
-#define ME_ARG_MISSING          2
-    N_("Garbage after option argument"),
-#define ME_GARBAGE              3
-    N_("Too many \"+command\", \"-c command\" or \"--cmd command\" arguments"),
-#define ME_EXTRA_CMD            4
-    N_("Invalid argument for"),
-#define ME_INVALID_ARG          5
+     "Unknown option argument" ,
+     "Too many edit arguments" ,
+     "Argument missing after" ,
+     "Garbage after option argument" ,
+     "Too many \"+command\", \"-c command\" or \"--cmd command\" arguments" ,
+     "Invalid argument for" ,
 };
 
 static mparm_T  params;
@@ -175157,7 +180066,6 @@
     int
 vim_main2(void)
 {
-
     if (recoverymode && params.fname == NULL)
     {
         recover_names(NULL, TRUE, NULL, 0, NULL);
@@ -175165,7 +180073,6 @@
     }
 
     set_init_3();
-    TIME_MSG("inits 3");
 
     if (params.no_swap_file)
     {
@@ -175184,22 +180091,19 @@
         read_stdin();
     }
 
-    if ((did_emsg || msg_didout) && *T_TI != NUL && params.edit_type != EDIT_STDIN)
+    if ((did_emsg || msg_didout) && * ( term_strings[(int)(KS_TI)] )  != NUL && params.edit_type != EDIT_STDIN)
     {
         newline_on_exit = TRUE;
     }
 
     settmode(TMODE_RAW);
-    TIME_MSG("setting raw mode");
 
     if (need_wait_return || msg_didany)
     {
         wait_return(TRUE);
-        TIME_MSG("waiting for return");
     }
 
     starttermcap();
-    TIME_MSG("start termcap");
 
     setmouse();
     if (scroll_region)
@@ -175208,7 +180112,7 @@
     }
     scroll_start();
 
-    term_push_title(SAVE_RESTORE_BOTH);
+    term_push_title( (SAVE_RESTORE_TITLE | SAVE_RESTORE_ICON) );
 
     if (exmode_active)
     {
@@ -175217,13 +180121,11 @@
     else
     {
         screenclear();
-        TIME_MSG("clearing screen");
     }
 
     no_wait_return = TRUE;
 
     create_windows(&params);
-    TIME_MSG("opening buffers");
 
     if (exmode_active)
     {
@@ -175231,7 +180133,6 @@
     }
 
     apply_autocmds(EVENT_BUFENTER, NULL, NULL, FALSE, curbuf);
-    TIME_MSG("BufEnter autocommands");
     setpcmark();
 
     edit_buffers(&params, start_dir);
@@ -175243,9 +180144,8 @@
     {
         swap_exists_did_quit = FALSE;
 
-        vim_snprintf((char *)IObuff, IOSIZE, "ta %s", params.tagname);
+        vim_snprintf((char *)IObuff,  (1024+1) , "ta %s", params.tagname);
         do_cmdline_cmd(IObuff);
-        TIME_MSG("jumping to tag");
 
         if (swap_exists_did_quit)
         {
@@ -175264,25 +180164,20 @@
     redraw_all_later(UPD_NOT_VALID);
     no_wait_return = FALSE;
 
-    DO_AUTOCHDIR;
-
     if (p_im)
     {
         need_start_insertmode = TRUE;
     }
 
     apply_autocmds(EVENT_VIMENTER, NULL, NULL, FALSE, curbuf);
-    TIME_MSG("VimEnter autocommands");
 
     if (restart_edit != 0)
     {
-        stuffcharReadbuff(K_NOP);
+        stuffcharReadbuff(  (-((KS_EXTRA) + ((int)(KE_NOP) << 8)))  );
     }
 
     do_redraw = TRUE;
 
-    TIME_MSG("before starting main loop");
-
     main_loop(FALSE, FALSE);
 
     return 0;
@@ -175296,24 +180191,20 @@
 
     (void)mb_init();
 
-    if ((IObuff = alloc(IOSIZE)) == NULL || (NameBuff = alloc(MAXPATHL)) == NULL)
+    if ((IObuff = alloc( (1024+1) )) == NULL || (NameBuff = alloc( PATH_MAX )) == NULL)
     {
         mch_exit(0);
     }
-    TIME_MSG("Allocated generic buffers");
 }
 
     void
 common_init_2(mparm_T *paramp)
 {
-
     init_locale();
-    TIME_MSG("locale set");
 
     early_arg_scan(paramp);
 
     stdout_isatty = (mch_check_win(paramp->argc, paramp->argv) != FAIL);
-    TIME_MSG("window checked");
 
     if (win_alloc_first() == FAIL)
     {
@@ -175327,7 +180218,6 @@
 
     init_homedir();
     set_init_1(paramp->clean);
-    TIME_MSG("inits 1");
 
 }
 
@@ -175375,7 +180265,7 @@
 }
 
     void
-state_no_longer_safe(char *reason UNUSED)
+state_no_longer_safe(char *reason  __attribute__((unused)) )
 {
     was_safe = FALSE;
 }
@@ -175397,7 +180287,7 @@
     }
     recursive = true;
 
-    if (!finish_op && (has_cursormoved()) && !EQUAL_POS(last_cursormoved, curwin->w_cursor))
+    if (!finish_op && (has_cursormoved()) && ! (((last_cursormoved).lnum == (curwin->w_cursor).lnum) && ((last_cursormoved).col == (curwin->w_cursor).col) && ((last_cursormoved).coladd == (curwin->w_cursor).coladd)) )
     {
         if (has_cursormoved())
         {
@@ -175406,10 +180296,10 @@
         last_cursormoved = curwin->w_cursor;
     }
 
-    if (!finish_op && has_textchanged() && curbuf->b_last_changedtick != CHANGEDTICK(curbuf))
+    if (!finish_op && has_textchanged() && curbuf->b_last_changedtick !=  ((curbuf)->b_ct_di.di_tv.vval.v_number) )
     {
         apply_autocmds(EVENT_TEXTCHANGED, NULL, NULL, FALSE, curbuf);
-        curbuf->b_last_changedtick = CHANGEDTICK(curbuf);
+        curbuf->b_last_changedtick =  ((curbuf)->b_ct_di.di_tv.vval.v_number) ;
     }
 
     update_topline();
@@ -175506,9 +180396,9 @@
             }
             else if (must_redraw)
             {
-                mch_disable_flush();
+                  ;
                 update_screen(0);
-                mch_enable_flush();
+                  ;
             }
             else if (redraw_cmdline || clear_cmdline || redraw_mode)
             {
@@ -175601,14 +180491,14 @@
         for (tp = first_tabpage; tp != NULL; tp = next_tp)
         {
             next_tp = tp->tp_next;
-            FOR_ALL_WINDOWS_IN_TAB(tp, wp)
+             for ((wp) = ((tp) == NULL || (tp) == curtab)             ? firstwin : (tp)->tp_firstwin; (wp); (wp) = (wp)->w_next) 
             {
                 if (wp->w_buffer == NULL || !buf_valid(wp->w_buffer))
                 {
                     continue;
                 }
                 buf = wp->w_buffer;
-                if (CHANGEDTICK(buf) != -1)
+                if ( ((buf)->b_ct_di.di_tv.vval.v_number)  != -1)
                 {
                     bufref_T bufref;
 
@@ -175616,7 +180506,7 @@
                     apply_autocmds(EVENT_BUFWINLEAVE, buf->b_fname, buf->b_fname, FALSE, buf);
                     if (bufref_valid(&bufref))
                     {
-                        CHANGEDTICK(buf) = -1;
+                         ((buf)->b_ct_di.di_tv.vval.v_number)  = -1;
                     }
 
                     next_tp = first_tabpage;
@@ -175625,7 +180515,8 @@
             }
         }
 
-        FOR_ALL_BUFFERS(buf)
+         for ((buf) = firstbuf; (buf) != NULL; (buf) = (buf)->b_next) 
+         {
             if (buf->b_ml.ml_mfp != NULL)
             {
                 bufref_T bufref;
@@ -175637,6 +180528,7 @@
                     break;
                 }
             }
+         }
 
         if (is_autocmd_blocked())
         {
@@ -175683,7 +180575,7 @@
 }
 
     static void
-early_arg_scan(mparm_T *parmp UNUSED)
+early_arg_scan(mparm_T *parmp  __attribute__((unused)) )
 {
     int         argc = parmp->argc;
     char        **argv = parmp->argv;
@@ -175691,14 +180583,14 @@
 
     for (i = 1; i < argc; i++)
     {
-        if (STRCMP(argv[i], "--") == 0)
+        if ( strcmp((char *)(argv[i]), (char *)("--"))  == 0)
         {
             break;
         }
 
         else if (strncmp(argv[i], "-nb", (size_t)3) == 0)
         {
-            mch_errmsg(_("'-nb' cannot be used: not enabled at compile time\n"));
+             fprintf(stderr, "%s", (_("'-nb' cannot be used: not enabled at compile time\n"))) ;
             mch_exit(2);
         }
 
@@ -175726,45 +180618,45 @@
 
     initstr = gettail((char_u *)parmp->argv[0]);
 
-    if (TOLOWER_ASC(initstr[0]) == 'r')
+    if ( (((initstr[0]) < 'A' || (initstr[0]) > 'Z') ? (initstr[0]) : (initstr[0]) + ('a' - 'A'))  == 'r')
     {
         restricted = TRUE;
         ++initstr;
     }
 
-    if (TOLOWER_ASC(initstr[0]) == 'e' && (TOLOWER_ASC(initstr[1]) == 'v' || TOLOWER_ASC(initstr[1]) == 'g'))
+    if ( (((initstr[0]) < 'A' || (initstr[0]) > 'Z') ? (initstr[0]) : (initstr[0]) + ('a' - 'A'))  == 'e' && ( (((initstr[1]) < 'A' || (initstr[1]) > 'Z') ? (initstr[1]) : (initstr[1]) + ('a' - 'A'))  == 'v' ||  (((initstr[1]) < 'A' || (initstr[1]) > 'Z') ? (initstr[1]) : (initstr[1]) + ('a' - 'A'))  == 'g'))
     {
         parmp->evim_mode = TRUE;
         ++initstr;
     }
 
-    if (TOLOWER_ASC(initstr[0]) == 'g')
+    if ( (((initstr[0]) < 'A' || (initstr[0]) > 'Z') ? (initstr[0]) : (initstr[0]) + ('a' - 'A'))  == 'g')
     {
         main_start_gui();
     }
 
-    if (STRNICMP(initstr, "view", 4) == 0)
+    if ( strncasecmp((char *)(initstr), (char *)("view"), (4))  == 0)
     {
         readonlymode = TRUE;
         curbuf->b_p_ro = TRUE;
         p_uc = 10000;
         initstr += 4;
     }
-    else if (STRNICMP(initstr, "vim", 3) == 0)
+    else if ( strncasecmp((char *)(initstr), (char *)("vim"), (3))  == 0)
     {
         initstr += 3;
     }
 
-    if (STRICMP(initstr, "diff") == 0)
+    if ( strcasecmp((char *)(initstr), (char *)("diff"))  == 0)
     {
-        mch_errmsg(_("This Vim was not compiled with the diff feature."));
-        mch_errmsg("\n");
+         fprintf(stderr, "%s", (_("This Vim was not compiled with the diff feature."))) ;
+         fprintf(stderr, "%s", ("\n")) ;
         mch_exit(2);
     }
 
-    if (STRNICMP(initstr, "ex", 2) == 0)
+    if ( strncasecmp((char *)(initstr), (char *)("ex"), (2))  == 0)
     {
-        if (STRNICMP(initstr + 2, "im", 2) == 0)
+        if ( strncasecmp((char *)(initstr + 2), (char *)("im"), (2))  == 0)
         {
             exmode_active = EXMODE_VIM;
         }
@@ -175834,11 +180726,11 @@
                 break;
 
             case '-':
-                if (STRICMP(argv[0] + argv_idx, "help") == 0)
+                if ( strcasecmp((char *)(argv[0] + argv_idx), (char *)("help"))  == 0)
                 {
                     usage();
                 }
-                else if (STRICMP(argv[0] + argv_idx, "version") == 0)
+                else if ( strcasecmp((char *)(argv[0] + argv_idx), (char *)("version"))  == 0)
                 {
                     cmdline_width = Columns = 80;
                     info_message = TRUE;
@@ -175847,46 +180739,46 @@
                     msg_didout = FALSE;
                     mch_exit(0);
                 }
-                else if (STRNICMP(argv[0] + argv_idx, "clean", 5) == 0)
+                else if ( strncasecmp((char *)(argv[0] + argv_idx), (char *)("clean"), (5))  == 0)
                 {
                     parmp->use_vimrc = (char_u *)"DEFAULTS";
                     parmp->clean = TRUE;
                     set_option_value_give_err((char_u *)"vif", 0L, (char_u *)"NONE", 0);
                 }
-                else if (STRNICMP(argv[0] + argv_idx, "literal", 7) == 0)
+                else if ( strncasecmp((char *)(argv[0] + argv_idx), (char *)("literal"), (7))  == 0)
                 {
                 }
-                else if (STRNICMP(argv[0] + argv_idx, "nofork", 6) == 0)
+                else if ( strncasecmp((char *)(argv[0] + argv_idx), (char *)("nofork"), (6))  == 0)
                 {
                 }
-                else if (STRNICMP(argv[0] + argv_idx, "noplugin", 8) == 0)
+                else if ( strncasecmp((char *)(argv[0] + argv_idx), (char *)("noplugin"), (8))  == 0)
                 {
                     p_lpl = FALSE;
                 }
-                else if (STRNICMP(argv[0] + argv_idx, "not-a-term", 10) == 0)
+                else if ( strncasecmp((char *)(argv[0] + argv_idx), (char *)("not-a-term"), (10))  == 0)
                 {
                     parmp->not_a_term = TRUE;
                 }
-                else if (STRNICMP(argv[0] + argv_idx, "gui-dialog-file", 15) == 0)
+                else if ( strncasecmp((char *)(argv[0] + argv_idx), (char *)("gui-dialog-file"), (15))  == 0)
                 {
                     want_argument = TRUE;
                     argv_idx += 15;
                 }
-                else if (STRNICMP(argv[0] + argv_idx, "ttyfail", 7) == 0)
+                else if ( strncasecmp((char *)(argv[0] + argv_idx), (char *)("ttyfail"), (7))  == 0)
                 {
                     parmp->tty_fail = TRUE;
                 }
-                else if (STRNICMP(argv[0] + argv_idx, "cmd", 3) == 0)
+                else if ( strncasecmp((char *)(argv[0] + argv_idx), (char *)("cmd"), (3))  == 0)
                 {
                     want_argument = TRUE;
                     argv_idx += 3;
                 }
-                else if (STRNICMP(argv[0] + argv_idx, "startuptime", 11) == 0)
+                else if ( strncasecmp((char *)(argv[0] + argv_idx), (char *)("startuptime"), (11))  == 0)
                 {
                     want_argument = TRUE;
                     argv_idx += 11;
                 }
-                else if (STRNICMP(argv[0] + argv_idx, "log", 3) == 0)
+                else if ( strncasecmp((char *)(argv[0] + argv_idx), (char *)("log"), (3))  == 0)
                 {
                     want_argument = TRUE;
                     argv_idx += 3;
@@ -175906,7 +180798,7 @@
                 break;
 
             case 'A':
-                mch_errmsg(_(e_arabic_cannot_be_used_not_enabled_at_compile_time));
+                 fprintf(stderr, "%s", (_(e_arabic_cannot_be_used_not_enabled_at_compile_time))) ;
                 mch_exit(2);
                 break;
 
@@ -175936,7 +180828,7 @@
                 break;
 
             case 'F':
-                mch_errmsg(_(e_farsi_support_has_been_removed));
+                 fprintf(stderr, "%s", (_(e_farsi_support_has_been_removed))) ;
                 mch_exit(2);
                 break;
 
@@ -175946,7 +180838,7 @@
                 break;
 
             case 'H':
-                mch_errmsg(_(e_hebrew_cannot_be_used_not_enabled_at_compile_time));
+                 fprintf(stderr, "%s", (_(e_hebrew_cannot_be_used_not_enabled_at_compile_time))) ;
                 mch_exit(2);
                 break;
 
@@ -175958,6 +180850,7 @@
             case 'M':
                 reset_modifiable();
 
+            __attribute__((fallthrough));
             case 'm':
                 p_write = FALSE;
                 break;
@@ -176033,7 +180926,7 @@
                 if (argv[0][argv_idx] != NUL)
                 {
                     set_option_value_give_err((char_u *)"verbosefile", 0L, (char_u *)argv[0] + argv_idx, 0);
-                    argv_idx = (int)STRLEN(argv[0]);
+                    argv_idx = (int) strlen((char *)(argv[0])) ;
                 }
                 break;
 
@@ -176072,6 +180965,7 @@
                     argv_idx = -1;
                     break;
                 }
+            __attribute__((fallthrough));
             case 'S':
             case 'i':
             case 'd':
@@ -176115,11 +181009,11 @@
 
                         if (argc < 1)
                         {
-                            a = SESSION_FILE;
+                            a =  "Session.vim" ;
                         }
                         else if (argv[0][0] == '-')
                         {
-                            a = SESSION_FILE;
+                            a =  "Session.vim" ;
                             ++argc;
                             --argv;
                         }
@@ -176127,7 +181021,7 @@
                         {
                             a = argv[0];
                         }
-                        p = alloc(STRLEN(a) + 4);
+                        p = alloc( strlen((char *)(a))  + 4);
                         if (p == NULL)
                         {
                             mch_exit(2);
@@ -176167,18 +181061,18 @@
                     if (scriptin[0] != NULL)
                     {
 scripterror:
-                        mch_errmsg(_("Attempt to open script file again: \""));
-                        mch_errmsg(argv[-1]);
-                        mch_errmsg(" ");
-                        mch_errmsg(argv[0]);
-                        mch_errmsg("\"\n");
+                         fprintf(stderr, "%s", (_("Attempt to open script file again: \""))) ;
+                         fprintf(stderr, "%s", (argv[-1])) ;
+                         fprintf(stderr, "%s", (" ")) ;
+                         fprintf(stderr, "%s", (argv[0])) ;
+                         fprintf(stderr, "%s", ("\"\n")) ;
                         mch_exit(2);
                     }
-                    if ((scriptin[0] = mch_fopen(argv[0], READBIN)) == NULL)
+                    if ((scriptin[0] =  fopen((argv[0]), ( "r" )) ) == NULL)
                     {
-                        mch_errmsg(_("Cannot open for reading: \""));
-                        mch_errmsg(argv[0]);
-                        mch_errmsg("\"\n");
+                         fprintf(stderr, "%s", (_("Cannot open for reading: \""))) ;
+                         fprintf(stderr, "%s", (argv[0])) ;
+                         fprintf(stderr, "%s", ("\"\n")) ;
                         mch_exit(2);
                     }
                     if (save_typebuf() == FAIL)
@@ -176211,16 +181105,17 @@
                         argv_idx = -1;
                         break;
                     }
+                __attribute__((fallthrough));
                 case 'W':
                     if (scriptout != NULL)
                     {
                         goto scripterror;
                     }
-                    if ((scriptout = mch_fopen(argv[0], c == 'w' ? APPENDBIN : WRITEBIN)) == NULL)
+                    if ((scriptout =  fopen((argv[0]), (c == 'w' ?  "a"  :  "w" )) ) == NULL)
                     {
-                        mch_errmsg(_("Cannot open for script output: \""));
-                        mch_errmsg(argv[0]);
-                        mch_errmsg("\"\n");
+                         fprintf(stderr, "%s", (_("Cannot open for script output: \""))) ;
+                         fprintf(stderr, "%s", (argv[0])) ;
+                         fprintf(stderr, "%s", ("\"\n")) ;
                         mch_exit(2);
                     }
                     break;
@@ -176275,11 +181170,11 @@
     {
         if (!stdout_isatty)
         {
-            mch_errmsg(_("Vim: Warning: Output is not to a terminal\n"));
+             fprintf(stderr, "%s", (_("Vim: Warning: Output is not to a terminal\n"))) ;
         }
         if (!input_isatty)
         {
-            mch_errmsg(_("Vim: Warning: Input is not from a terminal\n"));
+             fprintf(stderr, "%s", (_("Vim: Warning: Input is not from a terminal\n"))) ;
         }
         out_flush();
         if (parmp->tty_fail && (!stdout_isatty || !input_isatty))
@@ -176290,7 +181185,6 @@
         {
             ui_delay(2005L, TRUE);
         }
-        TIME_MSG("Warning delay");
     }
 }
 
@@ -176309,7 +181203,6 @@
 
     no_wait_return = FALSE;
     msg_didany = i;
-    TIME_MSG("reading stdin");
 
     check_swap_exists_action();
 
@@ -176318,7 +181211,7 @@
 }
 
     static void
-create_windows(mparm_T *parmp UNUSED)
+create_windows(mparm_T *parmp  __attribute__((unused)) )
 {
     int         dorewind;
     int         done = 0;
@@ -176329,7 +181222,7 @@
     }
     if (parmp->window_count == 0)
     {
-        parmp->window_count = GARGCOUNT;
+        parmp->window_count =  (global_alist.al_ga.ga_len) ;
     }
     if (parmp->window_count > 1)
     {
@@ -176340,12 +181233,10 @@
         if (parmp->window_layout == WIN_TABS)
         {
             parmp->window_count = make_tabpages(parmp->window_count);
-            TIME_MSG("making tab pages");
         }
         else if (firstwin->w_next == NULL)
         {
             parmp->window_count = make_windows(parmp->window_count, parmp->window_layout == WIN_VER);
-            TIME_MSG("making windows");
         }
         else
         {
@@ -176515,7 +181406,7 @@
         {
             curwin->w_arg_idx = arg_idx;
             swap_exists_did_quit = FALSE;
-            (void)do_ecmd(0, arg_idx < GARGCOUNT ? alist_name(&GARGLIST[arg_idx]) : NULL, NULL, NULL, ECMD_LASTL, ECMD_HIDE, curwin);
+            (void)do_ecmd(0, arg_idx <  (global_alist.al_ga.ga_len)  ? alist_name(& ((aentry_T *)global_alist.al_ga.ga_data) [arg_idx]) : NULL, NULL, NULL,  (linenr_T)0 , ECMD_HIDE, curwin);
             if (swap_exists_did_quit)
             {
                 if (got_int || only_one_window())
@@ -176526,7 +181417,7 @@
                 win_close(curwin, TRUE);
                 advance = FALSE;
             }
-            if (arg_idx == GARGCOUNT - 1)
+            if (arg_idx ==  (global_alist.al_ga.ga_len)  - 1)
             {
                 arg_had_last = TRUE;
             }
@@ -176556,7 +181447,6 @@
     win_enter(win, FALSE);
 
     --autocmd_no_leave;
-    TIME_MSG("editing files in windows");
     if (parmp->window_count > 1 && parmp->window_layout != WIN_TABS)
     {
         win_equal(curwin, FALSE, 'b');
@@ -176569,7 +181459,6 @@
     char_u      **cmds = parmp->pre_commands;
     int         cnt = parmp->n_pre_commands;
     int         i;
-    ESTACK_CHECK_DECLARATION;
 
     if (cnt <= 0)
     {
@@ -176578,21 +181467,17 @@
 
     curwin->w_cursor.lnum = 0;
     estack_push(ETYPE_ARGS, (char_u *)_("pre-vimrc command line"), 0);
-    ESTACK_CHECK_SETUP;
     for (i = 0; i < cnt; ++i)
     {
         do_cmdline_cmd(cmds[i]);
     }
-    ESTACK_CHECK_NOW;
     estack_pop();
-    TIME_MSG("--cmd commands");
 }
 
     static void
 exe_commands(mparm_T *parmp)
 {
     int         i;
-    ESTACK_CHECK_DECLARATION;
 
     msg_scroll = TRUE;
     if (parmp->tagname == NULL && curwin->w_cursor.lnum <= 1)
@@ -176600,7 +181485,6 @@
         curwin->w_cursor.lnum = 0;
     }
     estack_push(ETYPE_ARGS, (char_u *)"command line", 0);
-    ESTACK_CHECK_SETUP;
     for (i = 0; i < parmp->n_commands; ++i)
     {
         do_cmdline_cmd(parmp->commands[i]);
@@ -176609,7 +181493,6 @@
             vim_free(parmp->commands[i]);
         }
     }
-    ESTACK_CHECK_NOW;
     estack_pop();
     if (curwin->w_cursor.lnum == 0)
     {
@@ -176621,7 +181504,6 @@
         msg_scroll = FALSE;
     }
 
-    TIME_MSG("executing command arguments");
 }
 
     static void
@@ -176631,20 +181513,19 @@
 
     if (parmp->evim_mode)
     {
-        (void)do_source((char_u *)EVIM_FILE, FALSE, DOSO_NONE, NULL);
-        TIME_MSG("source evim file");
+        (void)do_source((char_u *) "$VIMRUNTIME/evim.vim" , FALSE, DOSO_NONE, NULL);
     }
 
     if (parmp->use_vimrc != NULL)
     {
-        if (STRCMP(parmp->use_vimrc, "DEFAULTS") == 0)
+        if ( strcmp((char *)(parmp->use_vimrc), (char *)("DEFAULTS"))  == 0)
         {
-            if (do_source((char_u *)VIM_DEFAULTS_FILE, FALSE, DOSO_NONE, NULL) != OK)
+            if (do_source((char_u *) "$VIMRUNTIME/defaults.vim" , FALSE, DOSO_NONE, NULL) != OK)
             {
                 emsg(_(e_failed_to_source_defaults));
             }
         }
-        else if (STRCMP(parmp->use_vimrc, "NONE") == 0 || STRCMP(parmp->use_vimrc, "NORC") == 0)
+        else if ( strcmp((char *)(parmp->use_vimrc), (char *)("NONE"))  == 0 ||  strcmp((char *)(parmp->use_vimrc), (char *)("NORC"))  == 0)
         {
         }
         else
@@ -176657,14 +181538,13 @@
     }
     else if (!silent_mode)
     {
-
-        (void)do_source((char_u *)SYS_VIMRC_FILE, FALSE, DOSO_NONE, NULL);
+        (void)do_source((char_u *) "$VIM/vimrc" , FALSE, DOSO_NONE, NULL);
 
         if (process_env((char_u *)"VIMINIT", TRUE) != OK)
         {
-            if (do_source((char_u *)USR_VIMRC_FILE, TRUE, DOSO_VIMRC, NULL) == FAIL && do_source((char_u *)USR_VIMRC_FILE2, TRUE, DOSO_VIMRC, NULL) == FAIL && do_source((char_u *)XDG_VIMRC_FILE, TRUE, DOSO_VIMRC, NULL) == FAIL && process_env((char_u *)"EXINIT", FALSE) == FAIL && do_source((char_u *)USR_EXRC_FILE, FALSE, DOSO_NONE, NULL) == FAIL && !has_dash_c_arg)
+            if (do_source((char_u *) "$HOME/.vimrc" , TRUE, DOSO_VIMRC, NULL) == FAIL && do_source((char_u *) "~/.vim/vimrc" , TRUE, DOSO_VIMRC, NULL) == FAIL && do_source((char_u *) ( (char_u *)getenv((char *)((char_u *)"XDG_CONFIG_HOME"))         ? "$XDG_CONFIG_HOME/vim/vimrc"  : "~/.config/vim/vimrc") , TRUE, DOSO_VIMRC, NULL) == FAIL && process_env((char_u *)"EXINIT", FALSE) == FAIL && do_source((char_u *) "$HOME/.exrc" , FALSE, DOSO_NONE, NULL) == FAIL && !has_dash_c_arg)
             {
-                if (do_source((char_u *)VIM_DEFAULTS_FILE, FALSE, DOSO_NONE, NULL) == FAIL)
+                if (do_source((char_u *) "$VIMRUNTIME/defaults.vim" , FALSE, DOSO_NONE, NULL) == FAIL)
                 {
                     emsg(_(e_failed_to_source_defaults));
                 }
@@ -176673,20 +181553,20 @@
 
         if (p_exrc)
         {
-            if (!file_owned(VIMRC_FILE))
+            if (!file_owned( ".vimrc" ))
             {
                 secure = p_secure;
             }
 
             i = FAIL;
-            if (fullpathcmp((char_u *)USR_VIMRC_FILE, (char_u *)VIMRC_FILE, FALSE, TRUE) != FPC_SAME && fullpathcmp((char_u *)USR_VIMRC_FILE2, (char_u *)VIMRC_FILE, FALSE, TRUE) != FPC_SAME && fullpathcmp((char_u *)SYS_VIMRC_FILE, (char_u *)VIMRC_FILE, FALSE, TRUE) != FPC_SAME)
+            if (fullpathcmp((char_u *) "$HOME/.vimrc" , (char_u *) ".vimrc" , FALSE, TRUE) != FPC_SAME && fullpathcmp((char_u *) "~/.vim/vimrc" , (char_u *) ".vimrc" , FALSE, TRUE) != FPC_SAME && fullpathcmp((char_u *) "$VIM/vimrc" , (char_u *) ".vimrc" , FALSE, TRUE) != FPC_SAME)
             {
-                i = do_source((char_u *)VIMRC_FILE, TRUE, DOSO_VIMRC, NULL);
+                i = do_source((char_u *) ".vimrc" , TRUE, DOSO_VIMRC, NULL);
             }
 
             if (i == FAIL)
             {
-                if (!file_owned(EXRC_FILE))
+                if (!file_owned( ".exrc" ))
                 {
                     secure = p_secure;
                 }
@@ -176694,9 +181574,9 @@
                 {
                     secure = 0;
                 }
-                if (       fullpathcmp((char_u *)USR_EXRC_FILE, (char_u *)EXRC_FILE, FALSE, TRUE) != FPC_SAME)
+                if (       fullpathcmp((char_u *) "$HOME/.exrc" , (char_u *) ".exrc" , FALSE, TRUE) != FPC_SAME)
                 {
-                    (void)do_source((char_u *)EXRC_FILE, FALSE, DOSO_NONE, NULL);
+                    (void)do_source((char_u *) ".exrc" , FALSE, DOSO_NONE, NULL);
                 }
             }
         }
@@ -176706,14 +181586,13 @@
         }
         secure = 0;
     }
-    TIME_MSG("sourcing vimrc file(s)");
 }
 
     static void
 main_start_gui(void)
 {
-    mch_errmsg(_(e_gui_cannot_be_used_not_enabled_at_compile_time));
-    mch_errmsg("\n");
+     fprintf(stderr, "%s", (_(e_gui_cannot_be_used_not_enabled_at_compile_time))) ;
+     fprintf(stderr, "%s", ("\n")) ;
     mch_exit(2);
 }
 
@@ -176722,9 +181601,8 @@
 {
     char_u      *initstr;
     sctx_T      save_current_sctx;
-    ESTACK_CHECK_DECLARATION;
 
-    if ((initstr = mch_getenv(env)) == NULL || *initstr == NUL)
+    if ((initstr =  (char_u *)getenv((char *)(env)) ) == NULL || *initstr == NUL)
     {
         return FAIL;
     }
@@ -176734,13 +181612,11 @@
         vimrc_found(NULL, NULL);
     }
     estack_push(ETYPE_ENV, env, 0);
-    ESTACK_CHECK_SETUP;
     save_current_sctx = current_sctx;
     current_sctx.sc_version = 1;
 
     do_cmdline_cmd(initstr);
 
-    ESTACK_CHECK_NOW;
     estack_pop();
     current_sctx = save_current_sctx;
     return OK;
@@ -176752,7 +181628,7 @@
     stat_T      s;
     uid_t       uid = getuid();
 
-    return !(mch_stat(fname, &s) != 0 || s.st_uid != uid || mch_lstat(fname, &s) != 0 || s.st_uid != uid);
+    return !( stat((fname), (&s))  != 0 || s.st_uid != uid ||  lstat((fname), (&s))  != 0 || s.st_uid != uid);
 }
 
     static void
@@ -176761,16 +181637,16 @@
     reset_signals();
 
     init_longVersion();
-    mch_errmsg(longVersion);
-    mch_errmsg("\n");
-    mch_errmsg(_(main_errors[n]));
+     fprintf(stderr, "%s", (longVersion)) ;
+     fprintf(stderr, "%s", ("\n")) ;
+     fprintf(stderr, "%s", (_(main_errors[n]))) ;
     if (str != NULL)
     {
-        mch_errmsg(": \"");
-        mch_errmsg((char *)str);
-        mch_errmsg("\"");
+         fprintf(stderr, "%s", (": \"")) ;
+         fprintf(stderr, "%s", ((char *)str)) ;
+         fprintf(stderr, "%s", ("\"")) ;
     }
-    mch_errmsg(_("\nMore info with: \"vim -h\"\n"));
+     fprintf(stderr, "%s", (_("\nMore info with: \"vim -h\"\n"))) ;
 
     mch_exit(1);
 }
@@ -176784,9 +181660,9 @@
     static void
 main_msg(char *s)
 {
-    mch_msg("   ");
-    mch_msg(s);
-    mch_msg("\n");
+     printf("%s", ("   ")) ;
+     printf("%s", (s)) ;
+     printf("%s", ("\n")) ;
 }
 
     static void
@@ -176795,28 +181671,28 @@
     int         i;
     static char *(use[]) =
     {
-        N_("[file ..]       edit specified file(s)"),
-        N_("-               read text from stdin"),
-        N_("-t tag          edit file where tag is defined"),
+         "[file ..]       edit specified file(s)" ,
+         "-               read text from stdin" ,
+         "-t tag          edit file where tag is defined" ,
     };
 
     reset_signals();
 
     init_longVersion();
-    mch_msg(longVersion);
-    mch_msg(_("\n\nUsage:"));
+     printf("%s", (longVersion)) ;
+     printf("%s", (_("\n\nUsage:"))) ;
     for (i = 0; ; ++i)
     {
-        mch_msg(_(" vim [arguments] "));
-        mch_msg(_(use[i]));
-        if (i == ARRAY_LENGTH(use) - 1)
+         printf("%s", (_(" vim [arguments] "))) ;
+         printf("%s", (_(use[i]))) ;
+        if (i ==  (sizeof(use) / sizeof((use)[0]))  - 1)
         {
             break;
         }
-        mch_msg(_("\n   or:"));
+         printf("%s", (_("\n   or:"))) ;
     }
 
-    mch_msg(_("\n\nArguments:\n"));
+     printf("%s", (_("\n\nArguments:\n"))) ;
     main_msg(_("--\t\t\tOnly file names after this"));
     main_msg(_("-v\t\t\tVi mode (like \"vi\")"));
     main_msg(_("-e\t\t\tEx mode (like \"ex\")"));
@@ -176877,7 +181753,7 @@
 
     mch_early_init();
 
-    CLEAR_FIELD(params);
+      memset((&(params)), (0), (sizeof(params)))  ;
     params.argc = argc;
     params.argv = argv;
     params.want_full_screen = TRUE;
@@ -176889,7 +181765,7 @@
 
     for (i = 1; i < argc; ++i)
     {
-        if (STRICMP(argv[i], "--clean") == 0)
+        if ( strcasecmp((char *)(argv[i]), (char *)("--clean"))  == 0)
         {
             params.clean = TRUE;
             break;
@@ -176901,15 +181777,12 @@
     parse_command_name(&params);
 
     command_line_scan(&params);
-    TIME_MSG("parsing arguments");
 
-    if (GARGCOUNT > 0)
+    if ( (global_alist.al_ga.ga_len)  > 0)
     {
-        params.fname = alist_name(&GARGLIST[0]);
+        params.fname = alist_name(& ((aentry_T *)global_alist.al_ga.ga_data) [0]);
     }
 
-    TIME_MSG("expanding arguments");
-
     ++RedrawingDisabled;
 
     if (recoverymode && params.fname == NULL)
@@ -176918,7 +181791,6 @@
     }
 
     mch_init();
-    TIME_MSG("shell init");
 
     check_tty(&params);
 
@@ -176931,16 +181803,15 @@
         }
     }
 
-    if (GARGCOUNT > 1 && !silent_mode && !is_not_a_term())
+    if ( (global_alist.al_ga.ga_len)  > 1 && !silent_mode && !is_not_a_term())
     {
-        printf(_("%d files to edit\n"), GARGCOUNT);
+        printf(_("%d files to edit\n"),  (global_alist.al_ga.ga_len) );
     }
 
     if (params.want_full_screen && !silent_mode)
     {
         termcapinit(params.term);
         screen_start();
-        TIME_MSG("Termcap init");
     }
 
     ui_get_shellsize();
@@ -176953,7 +181824,6 @@
     msg_col = 0;
     screenalloc(FALSE);
     set_init_2();
-    TIME_MSG("inits 2");
 
     msg_scroll = TRUE;
     no_wait_return = TRUE;
@@ -176961,9 +181831,8 @@
     init_mappings();
 
     init_highlight(TRUE, FALSE);
-    TIME_MSG("init highlight");
 
-    if (params.use_vimrc != NULL && (STRCMP(params.use_vimrc, "NONE") == 0 || STRCMP(params.use_vimrc, "DEFAULTS") == 0))
+    if (params.use_vimrc != NULL && ( strcmp((char *)(params.use_vimrc), (char *)("NONE"))  == 0 ||  strcmp((char *)(params.use_vimrc), (char *)("DEFAULTS"))  == 0))
     {
         p_lpl = FALSE;
     }

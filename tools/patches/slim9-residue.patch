--- a/vim.c
+++ b/vim.c
@@ -215,10 +215,6 @@
 
 // ---------------- begin vim.h ----------------
 
-// ---------------- begin protodef.h ----------------
-
-// ---------------- end protodef.h ----------------
-
 // ---------------- begin config.h ----------------
 
 enum { VIM_SIZEOF_INT = 4 };
@@ -227,10 +223,6 @@
 
 enum { ROOT_UID = 0 };
 
-// ---------------- begin feature.h ----------------
-
-// ---------------- end feature.h ----------------
-
 // ---------------- begin os_unix.h ----------------
 
 typedef void (*sighandler_T)  (int) ;
@@ -1958,10 +1950,6 @@
 
 // ---------------- end option.h ----------------
 
-// ---------------- begin beval.h ----------------
-
-// ---------------- end beval.h ----------------
-
 // ---------------- begin structs.h ----------------
 
 typedef struct
@@ -4131,10 +4119,6 @@
 
 // ---------------- end xdiff.h ----------------
 
-// ---------------- begin alloc.h ----------------
-
-// ---------------- end alloc.h ----------------
-
 enum { MOUSE_LEFT = 0x00 };
 enum { MOUSE_MIDDLE = 0x01 };
 enum { MOUSE_RIGHT = 0x02 };
@@ -5030,7 +5014,6 @@
 static int vim_handle_signal(int sig);
 static int mch_check_win(int argc, char **argv);
 static int mch_input_isatty(void);
-void  ex_ni (exarg_T *eap);
 static int mch_can_restore_title(void);
 static int mch_can_restore_icon(void);
 static void mch_settitle(char_u *title, char_u *icon);
@@ -5395,9 +5378,6 @@
 static void ex_history(exarg_T *eap);
 
 // ---------------- end cmdhist.pro ----------------
-// ---------------- begin linematch.pro ----------------
-
-// ---------------- end linematch.pro ----------------
 // ---------------- begin drawline.pro ----------------
 static int win_line(win_T *wp, linenr_T lnum, int startrow, int endrow, int number_only, spellvars_T *spv);
 
@@ -5467,9 +5447,6 @@
 
 // ---------------- end edit.pro ----------------
 // ---------------- begin eval.pro ----------------
-void  ex_ni (exarg_T *eap);
-void  ex_ni (exarg_T *eap);
-void  ex_ni (exarg_T *eap);
 static char_u *do_string_sub(char_u *str, size_t len, char_u *pat, char_u *sub, typval_T *expr, char_u *flags, size_t *ret_len);
 
 // ---------------- end eval.pro ----------------
@@ -5511,7 +5488,6 @@
 static void ex_drop(exarg_T *eap);
 static char_u *skip_vimgrep_pat(char_u *p, char_u **s, int *flags);
 static char_u *skip_vimgrep_pat_ext(char_u *p, char_u **s, int *flags, char_u **nulp, int *cp);
-void  ex_ni (exarg_T *eap);
 
 // ---------------- end ex_cmds.pro ----------------
 // ---------------- begin ex_cmds2.pro ----------------
@@ -5523,10 +5499,6 @@
 static int check_fname(void);
 static int buf_write_all(buf_T *buf, int forceit);
 static void ex_listdo(exarg_T *eap);
-void  ex_ni (exarg_T *eap);
-void  ex_ni (exarg_T *eap);
-void  ex_script_ni (exarg_T *eap);
-void  ex_ni (exarg_T *eap);
 static void ex_checktime(exarg_T *eap);
 
 // ---------------- end ex_cmds2.pro ----------------
@@ -6149,8 +6121,6 @@
 static void show_sb_text(void);
 static void msg_sb_eol(void);
 static int msg_use_printf(void);
-void  fprintf(stderr, "%s", (char *str)) ;
-void  printf("%s", (char *str)) ;
 static void repeat_message(void);
 static void msg_clr_eos(void);
 static void msg_clr_eos_force(void);
@@ -6791,21 +6761,16 @@
 static int do_in_runtimepath(char_u *name, int flags, void (*callback)(char_u *fname, void *ck), void *cookie);
 static int source_runtime(char_u *name, int flags);
 static int source_in_path(char_u *path, char_u *name, int flags, int *ret_sid);
-void  ex_ni (exarg_T *eap);
-void  ex_ni (exarg_T *eap);
 static void remove_duplicates(garray_T *gap);
 static int ExpandRTDir(char_u *pat, int flags, int *num_file, char_u ***file, char *dirnames[]);
 static int expand_runtime_cmd(char_u *pat, int *numMatches, char_u ***matches);
 static int ExpandPackAddDir(char_u *pat, int *num_file, char_u ***file);
 static void ex_source(exarg_T *eap);
-void  ex_ni (exarg_T *eap);
 static int do_source(char_u *fname, int check_other, int is_vimrc, int *ret_sid);
-void  ex_ni (exarg_T *eap);
 static char_u *getsourceline(int c, void *cookie, int indent, getline_opt_T options);
 static int sourcing_a_script(exarg_T *eap);
 static void ex_scriptencoding(exarg_T *eap);
 static void ex_scriptversion(exarg_T *eap);
-void  ex_ni (exarg_T *eap);
 
 // ---------------- end scriptfile.pro ----------------
 // ---------------- begin screen.pro ----------------
@@ -6854,7 +6819,6 @@
 static int redrawing(void);
 static int messaging(void);
 static void comp_col(void);
-int  7 ;
 static char *set_fillchars_option(win_T *wp, char_u *val, int apply, char *errbuf, size_t errbuflen);
 static char *set_listchars_option(win_T *wp, char_u *val, int apply, char *errbuf, size_t errbuflen);
 static char_u *get_fillchars_name(expand_T *xp, int idx);
@@ -6863,7 +6827,6 @@
 
 // ---------------- end screen.pro ----------------
 // ---------------- begin session.pro ----------------
-void  ex_ni (exarg_T *eap);
 static void ex_mkrc(exarg_T *eap);
 static int put_eol(FILE *fd);
 static int put_line(FILE *fd, char *s);
@@ -7139,9 +7102,6 @@
 static int in_vim9script(void);
 static void ex_vim9script(exarg_T *eap);
 static int vim9_comment_start(char_u *p);
-void  ex_ni (exarg_T *eap);
-void  ex_ni (exarg_T *eap);
-void  ex_ni (exarg_T *eap);
 
 // ---------------- end vim9script.pro ----------------
 // ---------------- begin window.pro ----------------
@@ -7712,7 +7672,7 @@
 enum { WM_SHOWN = 1 };
 enum { WM_SCROLLED = 2 };
 
-static char *Version;
+static const char *Version;
 static char *longVersion;
 
 static char_u *default_vim_dir;
@@ -12931,7 +12891,6 @@
     (void)do_buffer_ext(*eap->cmd == 's' ? DOBUF_SPLIT : DOBUF_GOTO, start, dir, count, (eap->forceit ? DOBUF_FORCEIT : 0) | (skip_help_buf ? DOBUF_SKIPHELP : 0));
     if (swap_exists_action == SEA_QUIT && *eap->cmd == 's')
     {
-
         win_close(curwin, TRUE);
         swap_exists_action = save_sea;
         swap_exists_did_quit = TRUE;
@@ -12950,7 +12909,6 @@
 
     if (swap_exists_action == SEA_QUIT)
     {
-
         swap_exists_action = SEA_NONE;
         swap_exists_did_quit = TRUE;
         close_buffer(curwin, curbuf, DOBUF_UNLOAD, FALSE, FALSE, TRUE);
@@ -12980,7 +12938,6 @@
     }
     else if (swap_exists_action == SEA_RECOVER)
     {
-
         msg_scroll = TRUE;
         ml_recover(FALSE);
         msg_puts("\n");
@@ -14918,7 +14875,7 @@
 
     if (curbuf->b_ml.ml_flags & ML_EMPTY)
     {
-        bufferlen += vim_snprintf_safelen(buffer + bufferlen,  (1024+1)  - bufferlen, "%s", _(no_lines_msg));
+        bufferlen += vim_snprintf_safelen(buffer + bufferlen,  (1024+1)  - bufferlen, "%s", _((char *)no_lines_msg));
     }
     else if (p_ru)
     {
@@ -15062,7 +15019,6 @@
                 }
                 else
                 {
-
                     if (buflen <  (sizeof(buf) - 20) )
                     {
                         *p = NUL;
@@ -15365,7 +15321,6 @@
             }
             if (swap_exists_action == SEA_QUIT)
             {
-
                 win_close(curwin, TRUE);
                 --open_wins;
                 swap_exists_action = SEA_NONE;
@@ -15457,7 +15412,20 @@
 
 // ---------------- begin version.h ----------------
 
+// Nothing but the preprocessor can build a string literal from a constant, so
+// the version strings are character arrays built from the two numbers instead.
+// The numbers stay the single source of truth and sizeof still gives the
+// length; what an array cannot do is literal concatenation, so the places that
+// pasted "VIM - Vi IMproved " onto it take it through %s.
+enum { VIM_VERSION_MAJOR = 9 };
+enum { VIM_VERSION_MINOR = 2 };
 enum { VIM_VERSION_BUILD = 286 };
+static const char VIM_VERSION_DATE_ONLY[] = "2026 Feb 14";
+
+static const char VIM_VERSION_SHORT[] = {'0' + VIM_VERSION_MAJOR, '.', '0' + VIM_VERSION_MINOR, NUL};
+static const char VIM_VERSION_NODOT[] = {'v', 'i', 'm', '0' + VIM_VERSION_MAJOR, '0' + VIM_VERSION_MINOR, NUL};
+static const char VIM_VERSION_LONG_ONLY[] = {'V', 'I', 'M', ' ', '-', ' ', 'V', 'i', ' ', 'I', 'M', 'p', 'r', 'o', 'v', 'e', 'd', ' ', '0' + VIM_VERSION_MAJOR, '.', '0' + VIM_VERSION_MINOR, NUL};
+enum { VIM_VERSION_100 = VIM_VERSION_MAJOR * 100 + VIM_VERSION_MINOR };
 
 enum { VIM_VERSION_PATCHLEVEL = 0 };
 
@@ -15496,7 +15464,7 @@
                     e = s + 3;
                 }
                 vers = getdigits(&e);
-                if (*e == ':' && (s[0] != 'V' ||  strncmp((char *)(skipwhite(e + 1)), (char *)("set"), (3))  == 0) && (s[3] == ':' || ( ( 9  * 100 +  2 )  >= vers &&  (isdigit ((unsigned char)(s[3]))) ) || ( ( 9  * 100 +  2 )  < vers && s[3] == '<') || ( ( 9  * 100 +  2 )  > vers && s[3] == '>') || ( ( 9  * 100 +  2 )  == vers && s[3] == '=')))
+                if (*e == ':' && (s[0] != 'V' ||  strncmp((char *)(skipwhite(e + 1)), (char *)("set"), (3))  == 0) && (s[3] == ':' || (VIM_VERSION_100 >= vers &&  (isdigit ((unsigned char)(s[3]))) ) || (VIM_VERSION_100 < vers && s[3] == '<') || (VIM_VERSION_100 > vers && s[3] == '>') || (VIM_VERSION_100 == vers && s[3] == '=')))
                 {
                     break;
                 }
@@ -15666,7 +15634,6 @@
     char_u *
 buf_spname(buf_T *buf)
 {
-
     if (bt_nofilename(buf))
     {
         if (buf->b_fname != NULL)
@@ -16881,7 +16848,6 @@
                 }
                 if (backup != NULL)
                 {
-
                     if (vim_rename(fname, backup) == 0)
                     {
                         break;
@@ -17050,7 +17016,6 @@
                     {
                         if (backup_copy)
                         {
-
                             if ( stat(((char *)fname), (&st))  < 0)
                             {
                                 vim_rename(backup, fname);
@@ -17628,7 +17593,6 @@
     void
 changed(void)
 {
-
     if (!curbuf->b_changed)
     {
         int     save_msg_scroll = msg_scroll;
@@ -17869,7 +17833,6 @@
     void
 inserted_bytes(linenr_T lnum, colnr_T col, int added  __attribute__((unused)) )
 {
-
     changed_bytes(lnum, col);
 }
 
@@ -23616,7 +23579,6 @@
 
             if (curbuf->b_ind_if_for_while)
             {
-
                 pos_T       cursor_save = curwin->w_cursor;
                 pos_T       outermost;
                 char_u      *line;
@@ -23872,7 +23834,6 @@
             }
             else if (start_brace == BRACE_AT_START && lookfor_cpp_namespace)
             {
-
                 lookfor_cpp_namespace = TRUE;
             }
             else
@@ -26400,7 +26361,6 @@
 
     if (context != EXPAND_FILES && context != EXPAND_FILES_IN_PATH && context != EXPAND_SHELLCMD && context != EXPAND_DIRECTORIES && context != EXPAND_DIRS_IN_CDPATH)
     {
-
         if (context == EXPAND_FINDFUNC || context == EXPAND_HELP || context == EXPAND_COLORS || context == EXPAND_COMPILER || context == EXPAND_OWNSYNTAX || context == EXPAND_FILETYPE || context == EXPAND_KEYMAP || context == EXPAND_PACKADD || context == EXPAND_RUNTIME || ((context == EXPAND_TAGS_LISTFILES || context == EXPAND_TAGS) && fname[0] == '/'))
         {
             retval = vim_strnsave(fname, len);
@@ -27154,6 +27114,7 @@
             {
                 arg = xp->xp_pattern + 1;
             }
+        __attribute__((fallthrough));
         case CMD_buffer:
         case CMD_sbuffer:
         case CMD_pbuffer:
@@ -29568,7 +29529,6 @@
     static void
 draw_screen_line(win_T *wp, winlinevars_T *wlv)
 {
-
     wlv->screen_line_flags |= SLF_INC_VCOL;
     wlv_screen_line(wp, wlv, TRUE);
     wlv->screen_line_flags &= ~SLF_INC_VCOL;
@@ -29691,7 +29651,6 @@
 
     if (number_only == 0)
     {
-
         if (VIsual_active && wp->w_buffer == curwin->w_buffer)
         {
             pos_T *top;
@@ -30039,7 +29998,6 @@
 
         if (wlv.draw_state ==  (   (   (WL_START + 1)    + 1)    + 1)  && (area_highlighting || extra_check))
         {
-
             int *area_attr_p =
                                                             &area_attr;
 
@@ -31707,7 +31665,6 @@
         }
         else
         {
-
             j = -1;
             row = 0;
             for (i = 0; i < wp->w_lines_valid && i < Rows; i++)
@@ -33237,6 +33194,7 @@
                 break;
             }
 
+        __attribute__((fallthrough));
         case Ctrl_C:
             if (c == Ctrl_C && cmdwin_type != 0)
             {
@@ -33601,6 +33559,7 @@
         case   (-(('k') + ((int)('B') << 8)))  :
             c = TAB;
 
+        __attribute__((fallthrough));
         case TAB:
             inserted_space = FALSE;
             if (ins_tab())
@@ -33612,6 +33571,7 @@
 
         case   (-(('K') + ((int)('A') << 8)))  :
             c = CAR;
+        __attribute__((fallthrough));
         case CAR:
         case NL:
             if (cmdwin_type != 0)
@@ -33678,6 +33638,7 @@
                 goto normalchar;
             }
 
+        __attribute__((fallthrough));
         case Ctrl_P:
         case Ctrl_N:
             if (*curbuf->b_p_cpt == NUL && (ctrl_x_mode_normal() || ctrl_x_mode_whole_line()) && !compl_status_local())
@@ -33784,7 +33745,6 @@
     void
 ins_redraw(int ready)
 {
-
     if (char_avail())
     {
         return;
@@ -35462,6 +35422,7 @@
             {
                 break;
             }
+        __attribute__((fallthrough));
         case   (-(('#') + ((int)('4') << 8)))  :
         case   (-(('%') + ((int)('i') << 8)))  :
         case   (-((KS_EXTRA) + ((int)(KE_S_UP) << 8)))  :
@@ -39678,7 +39639,7 @@
             int save_State = State;
 
             State = MODE_CMDLINE;
-            theline = eap->ea_getline(NUL, eap->cookie, indent, TRUE);
+            theline = eap->ea_getline(NUL, eap->cookie, indent, GETLINE_CONCAT_CONT);
             State = save_State;
         }
         lines_left = Rows - 1;
@@ -40558,7 +40519,7 @@
                                 msg_putchar('^');
                             }
 
-                            resp = getexmodeline('?', NULL, 0, TRUE);
+                            resp = getexmodeline('?', NULL, 0, GETLINE_CONCAT_CONT);
                             if (resp != NULL)
                             {
                                 typed = *resp;
@@ -42933,7 +42894,6 @@
     next_cmdline = cmdline;
     do
     {
-
         if (next_cmdline == NULL)
         {
             did_emsg = FALSE;
@@ -51107,6 +51067,7 @@
                 {
                     goto cmdline_not_changed;
                 }
+        __attribute__((fallthrough));
         case   (-((KS_EXTRA) + ((int)(KE_LEFTMOUSE) << 8)))  :
         case   (-((KS_EXTRA) + ((int)(KE_RIGHTMOUSE) << 8)))  :
                 cmdline_left_right_mouse(c, &ignore_drag_release);
@@ -51185,6 +51146,7 @@
                     }
                     goto cmdline_changed;
                 }
+        __attribute__((fallthrough));
         case   (-(('k') + ((int)('u') << 8)))  :
         case   (-(('k') + ((int)('d') << 8)))  :
         case   (-((KS_EXTRA) + ((int)(KE_S_UP) << 8)))  :
@@ -53716,7 +53678,6 @@
                     }
                     else if (conv_restlen > 0)
                     {
-
                         if (fio_flags != 0 || iconv_fd != (iconv_t)-1)
                         {
                             if (can_retry)
@@ -54822,7 +54783,6 @@
     int
 check_file_readonly(char_u      *fname, int         perm  __attribute__((unused)) )
 {
-
     return ((perm & 0222) == 0 ||  access(((char *)fname), (W_OK)) );
 }
 
@@ -58701,7 +58661,6 @@
                         {
                             if ((path_with_url(file_path.string) || (mch_getperm(file_path.string) >= 0 && (search_ctx->ffsc_find_what == FINDFILE_BOTH || ((search_ctx->ffsc_find_what == FINDFILE_DIR) == mch_isdir(file_path.string))))) && (ff_check_visited(&search_ctx->ffsc_visited_list ->ffvl_visited_list, file_path.string, file_path.length, (char_u *)"", 0) == OK))
                             {
-
                                 stackp->ffs_filearray_cur = i + 1;
                                 ff_push(search_ctx, stackp);
 
@@ -60487,7 +60446,6 @@
 
     do
     {
-
         if (looped_around && (whole_line ? current_pos.lnum == circly_end.lnum :  (((current_pos).lnum == (circly_end).lnum) && ((current_pos).col == (circly_end).col) && ((current_pos).coladd == (circly_end).coladd)) ))
         {
             break;
@@ -62809,7 +62767,6 @@
                 }
                 else
                 {
-
                     if (max_mlen < mlen)
                     {
                         max_mlen = mlen;
@@ -63483,7 +63440,6 @@
     script_char = -1;
     while (scriptin[curscript] != NULL && script_char < 0)
     {
-
         if (got_int || (script_char = getc(scriptin[curscript])) < 0)
         {
             closescript();
@@ -66190,7 +66146,6 @@
         taep = &(((attrentry_T *)table->ga_data)[i]);
         if (       aep->ae_attr == taep->ae_attr && ((table == &term_attr_table && (aep->ae_u.term.start == NULL) == (taep->ae_u.term.start == NULL) && (aep->ae_u.term.start == NULL ||  strcmp((char *)(aep->ae_u.term.start), (char *)(taep->ae_u.term.start))  == 0) && (aep->ae_u.term.stop == NULL) == (taep->ae_u.term.stop == NULL) && (aep->ae_u.term.stop == NULL ||  strcmp((char *)(aep->ae_u.term.stop), (char *)(taep->ae_u.term.stop))  == 0)) || (table == &cterm_attr_table && aep->ae_u.cterm.fg_color == taep->ae_u.cterm.fg_color && aep->ae_u.cterm.bg_color == taep->ae_u.cterm.bg_color && aep->ae_u.cterm.ul_color == taep->ae_u.cterm.ul_color && aep->ae_u.cterm.font == taep->ae_u.cterm.font)))
         {
-
         return i +  (HL_ALL + 1) ;
         }
     }
@@ -68469,7 +68424,6 @@
     {
         for (i = oap->line_count; --i >= 0 && !got_int; )
         {
-
             if (i > 1 && (i % 50 == 0 || i == oap->line_count - 1) && oap->line_count > p_report)
             {
                 smsg(_("%ld lines to indent... "), i);
@@ -69026,7 +68980,6 @@
             {
                 if (got_tab || (eap->forceit && num_spaces > 1))
                 {
-
                     len = num_spaces = vcol - start_vcol;
                     num_tabs = 0;
                     if (!curbuf->b_p_et)
@@ -70033,7 +69986,6 @@
 
     if (p_ic && curbuf->b_p_inf && len > 0)
     {
-
         if (has_mbyte)
         {
             p = str;
@@ -71887,6 +71839,7 @@
             {
                 compl_cont_status &= ~CONT_LOCAL;
             }
+        __attribute__((fallthrough));
         default:
             if (c == Ctrl_X)
             {
@@ -74826,16 +74779,19 @@
                 {
                     break;
                 }
+            __attribute__((fallthrough));
             case '~':
                 if (!magic_isset())
                 {
                     break;
                 }
+            __attribute__((fallthrough));
             case '\\':
                 if (ctrl_x_mode_dictionary() || ctrl_x_mode_thesaurus())
                 {
                     break;
                 }
+            __attribute__((fallthrough));
             case '^':
             case '$':
                 m++;
@@ -75831,7 +75787,6 @@
                 }
                 for (mp = *mpp; mp != NULL && !got_int; mp = *mpp)
                 {
-
                     if ((mp->m_mode & mode) == 0)
                     {
                         mpp = &(mp->m_next);
@@ -79918,7 +79873,6 @@
 {
     switch (enc_dbcs)
     {
-
         case DBCS_JPNU:
         case DBCS_JPN:
             {
@@ -85048,7 +85002,6 @@
 
     for (i = 0; i <= mht->mht_mask; i++)
     {
-
           memset((&(tails)), (0), (sizeof(tails)))  ;
 
         for (mhi = mht->mht_buckets[i]; mhi != NULL; mhi = mhi->mhi_next)
@@ -87546,7 +87499,7 @@
     {
         if ((flags & ML_DEL_MESSAGE))
         {
-            set_keep_msg((char_u *)_(no_lines_msg), 0);
+            set_keep_msg((char_u *)_((char *)no_lines_msg), 0);
         }
 
         i = ml_replace((linenr_T)1, (char_u *)"", TRUE);
@@ -88102,7 +88055,6 @@
 
     if (top == buf->b_ml.ml_stack_size)
     {
-
         newstack =  (infoptr_T *)alloc(sizeof(infoptr_T) * (buf->b_ml.ml_stack_size + STACK_INCR)) ;
         if (newstack == NULL)
         {
@@ -89183,7 +89135,6 @@
 
     if (!emsg_off || vim_strchr(p_debug, 't') != NULL)
     {
-
         if (emsg_silent != 0)
         {
             if (emsg_noredir == 0)
@@ -91160,6 +91111,7 @@
                 skip_redraw = TRUE;
                 need_wait_return = FALSE;
             }
+        __attribute__((fallthrough));
         case 'q':
         case Ctrl_C:
         case ESC:
@@ -92488,7 +92440,6 @@
     void
 vim_beep(unsigned val)
 {
-
     if (emsg_silent != 0 || in_assert_fails)
     {
         return;
@@ -92761,7 +92712,7 @@
         return NULL;
     }
     vimdir_len =  strlen((char *)(vimdir)) ;
-    concat_fnames(vimdir, vimdir_len, (char_u *) "vim"    # 9        # 2     ,  (sizeof( "vim"    # 9        # 2      "") - 1) , TRUE, &p);
+    concat_fnames(vimdir, vimdir_len, (char_u *)VIM_VERSION_NODOT, sizeof(VIM_VERSION_NODOT) - 1, TRUE, &p);
     if (p.string != NULL && mch_isdir(p.string))
     {
         return p.string;
@@ -92851,7 +92802,7 @@
             if (!vimruntime)
             {
                 pend = remove_tail(p, pend, (char_u *) "runtime" );
-                pend = remove_tail(p, pend, (char_u *) "vim"    # 9        # 2     );
+                pend = remove_tail(p, pend, (char_u *)VIM_VERSION_NODOT);
             }
 
             if (pend > p && after_pathsep(p, pend))
@@ -93472,7 +93423,6 @@
 
         if (virtual_active() && addspaces && wcol >= 0 && ((col != wcol && col != wcol + 1) || csize > 1))
         {
-
             if (line[idx] == NUL)
             {
                 int     correct = wcol - col;
@@ -97731,7 +97681,6 @@
     void
 set_topline(win_T *wp, linenr_T lnum)
 {
-
     wp->w_botline += lnum - wp->w_topline;
     if (wp->w_botline > wp->w_buffer->b_ml.ml_line_count + 1)
     {
@@ -100355,7 +100304,6 @@
 
         if (!lit)
         {
-
               ;
         }
 
@@ -100775,7 +100723,6 @@
     void
 end_visual_mode_keep_button(void)
 {
-
     VIsual_active = FALSE;
     setmouse();
     mouse_dragging = 0;
@@ -101773,7 +101720,6 @@
     int
 nv_screengo(oparg_T *oap, int dir, long dist)
 {
-
     int         linelen = linetabsize_no_outer(curwin, curwin->w_cursor.lnum);
 
     int         retval = OK;
@@ -102038,11 +101984,13 @@
                         curwin->w_cursor.lnum = curwin->w_botline;
                     }
                 }
+    __attribute__((fallthrough));
     case NL:
     case CAR:
     case   (-(('K') + ((int)('A') << 8)))  :
                 beginline(BL_WHITE | BL_FIX);
 
+    __attribute__((fallthrough));
     case 't':
         scroll_cursor_top(0, TRUE);
                 redraw_later(UPD_VALID);
@@ -102052,6 +102000,7 @@
     case '.':
         beginline(BL_WHITE | BL_FIX);
 
+    __attribute__((fallthrough));
     case 'z':
         scroll_cursor_halfway(TRUE, FALSE);
                 redraw_later(UPD_VALID);
@@ -102072,9 +102021,11 @@
                 {
                     curwin->w_cursor.lnum = curwin->w_topline - 1;
                 }
+    __attribute__((fallthrough));
     case '-':
                 beginline(BL_WHITE | BL_FIX);
 
+    __attribute__((fallthrough));
     case 'b':
         scroll_cursor_bot(0, TRUE);
                 redraw_later(UPD_VALID);
@@ -102084,6 +102035,7 @@
     case 'H':
                 cap->count1 *= curwin->w_width / 2;
 
+    __attribute__((fallthrough));
     case 'h':
     case   (-(('k') + ((int)('l') << 8)))  :
                 if (!curwin-> w_onebuf_opt.wo_wrap )
@@ -102095,6 +102047,7 @@
     case 'L':
         cap->count1 *= curwin->w_width / 2;
 
+    __attribute__((fallthrough));
     case 'l':
     case   (-(('k') + ((int)('r') << 8)))  :
                 if (!curwin-> w_onebuf_opt.wo_wrap )
@@ -103301,7 +103254,6 @@
     }
     else
     {
-
     if (  (cap->cmdchar == '[' && vim_strchr((char_u *)"{(*/#mM", cap->nchar) != NULL) || (cap->cmdchar == ']' && vim_strchr((char_u *)"})*/#mM", cap->nchar) != NULL))
     {
         nv_bracket_block(cap, &old_pos);
@@ -104279,7 +104231,6 @@
     static void
 n_start_visual_mode(int c)
 {
-
     VIsual_mode = c;
     VIsual_active = TRUE;
     VIsual_reselect = TRUE;
@@ -104631,6 +104582,7 @@
 
     case   (-(('k') + ((int)('b') << 8)))  :
         cap->nchar = Ctrl_H;
+    __attribute__((fallthrough));
     case 'h':
     case 'H':
     case Ctrl_H:
@@ -104762,6 +104714,7 @@
 
     case '\'':
         cap->arg = TRUE;
+    __attribute__((fallthrough));
     case '`':
         nv_gomark(cap);
         break;
@@ -104797,6 +104750,7 @@
     case 'q':
     case 'w':
         oap->cursor_start = curwin->w_cursor;
+    __attribute__((fallthrough));
     case '~':
     case 'u':
     case 'U':
@@ -104891,7 +104845,6 @@
     static void
 n_opencmd(cmdarg_T *cap)
 {
-
     if (checkclearopq(cap->oap))
     {
         return;
@@ -105492,6 +105445,7 @@
                     break;
                 }
 
+            __attribute__((fallthrough));
             case 'a':
                 if (virtual_active() && (curwin->w_cursor.coladd > 0 || *ml_get_cursor() == NUL || *ml_get_cursor() == TAB))
                 {
@@ -107683,7 +107637,6 @@
         {
             if (t > 0 && prev_was_comment)
             {
-
                 char_u *new_curr = skip_comment(curr, TRUE, insert_space, &prev_was_comment);
                 comments[t] = (int)(new_curr - curr);
                 curr = new_curr;
@@ -108223,7 +108176,6 @@
 
         if (       do_bin && do_hex && ! ((col > 0 && (ptr[col] == 'X' || ptr[col] == 'x') && ptr[col - 1] == '0' && (!has_mbyte || !(*mb_head_off)(ptr, ptr + col - 1)) && vim_isxdigit(ptr[col + 1]))))
         {
-
             col = pos->col;
 
             while (col > 0 && vim_isdigit(ptr[col]))
@@ -108684,7 +108636,7 @@
     {
         if (dict == NULL)
         {
-            msg(_(no_lines_msg));
+            msg(_((char *)no_lines_msg));
             return;
         }
     }
@@ -109474,6 +109426,7 @@
                 bangredo = TRUE;
             }
 
+        __attribute__((fallthrough));
         case OP_INDENT:
         case OP_COLON:
 
@@ -111799,7 +111752,6 @@
     void
 set_init_1(int clean_arg)
 {
-
     p_cp = FALSE;
 
     if ( (char_u *)getenv((char *)((char_u *)"VIM_POSIX"))  != NULL)
@@ -113956,7 +113908,6 @@
     char *
 did_set_modifiable(optset_T *args  __attribute__((unused)) )
 {
-
     redraw_titles();
 
     return NULL;
@@ -114302,7 +114253,6 @@
     static void
 update_global_undolevels(long value, long old_value)
 {
-
     p_ul = old_value;
     u_sync(TRUE);
     p_ul = value;
@@ -116941,7 +116891,6 @@
     }
     else if (option_flags & P_FLAGLIST)
     {
-
         if (*xp->xp_pattern != NUL)
         {
             return FAIL;
@@ -118356,7 +118305,6 @@
 
     if (errmsg == NULL)
     {
-
         if (((varp == &p_enc && *p_tenc != NUL) || varp == &p_tenc))
         {
             if (convert_setup(&input_conv, p_tenc, p_enc) == FAIL || convert_setup(&output_conv, p_enc, p_tenc) == FAIL)
@@ -119742,7 +119690,6 @@
 
     if ((xp->xp_pattern > args->oe_set_arg && *(xp->xp_pattern-1) == ':') || xp->xp_pattern == args->oe_set_arg || *(xp->xp_pattern-1) == ',')
     {
-
         if (*xp->xp_pattern == '!')
         {
             return expand_hl_occasions(args, numMatches, matches, '!');
@@ -120007,10 +119954,6 @@
 
 // ==================== os_unix.c ====================
 
-// ---------------- begin os_unixx.h ----------------
-
-// ---------------- end os_unixx.h ----------------
-
 static int ignore_sigtstp = FALSE;
 
 static int get_x11_title(int);
@@ -121209,7 +121152,6 @@
     void
 mch_early_init(void)
 {
-
     signal_stack = alloc(get_signal_stack_size());
     init_signal_stack();
 
@@ -121771,7 +121713,6 @@
             }
             else if ((options & (SHELL_READ|SHELL_WRITE)))
             {
-
                 if (p_stmp)
                 {
                     (void)setsid();
@@ -122222,7 +122163,6 @@
     int         result;
 
     {
-
         struct timeval  tv;
         struct timeval  *tvp;
         static fd_set rfds;
@@ -122895,9 +122835,22 @@
 
 enum { PUM_DEF_HEIGHT = 10 };
 
- void     pum_set_##border(int enable)     {    pum_##border = enable ? 1 : 0;     } 
- void     pum_set_##shadow(int enable)     {    pum_##shadow = enable ? 1 : 0;     } 
- void     pum_set_##margin(int enable)     {    pum_##margin = enable ? 1 : 0;     } 
+// Token pasting is the one thing C has no answer to, so the three functions
+// this macro defined are written out.
+void pum_set_border(int enable)
+{
+    pum_border = enable ? 1 : 0;
+}
+
+void pum_set_shadow(int enable)
+{
+    pum_shadow = enable ? 1 : 0;
+}
+
+void pum_set_margin(int enable)
+{
+    pum_margin = enable ? 1 : 0;
+}
 
     static void
 pum_compute_size(void)
@@ -128101,6 +128054,7 @@
             goto collection;
         }
 
+      __attribute__((fallthrough));
       case  ((int)('.') - 256) :
       case  ((int)('i') - 256) :
       case  ((int)('I') - 256) :
@@ -128243,7 +128197,6 @@
             c = no_Magic(getchr());
             switch (c)
             {
-
                 case 's':
                     ret = regnode(MOPEN + 0);
                           if (re_mult_next("\\zs") == FAIL)
@@ -128489,6 +128442,7 @@
                     {
                         goto delimiter_atom;
                     }
+                __attribute__((fallthrough));
                 default:
                           if ( ((unsigned)(c) - '0' < 10)  || c == '<' || c == '>' || c == '\'' || c == '.')
                           {
@@ -128918,6 +128872,7 @@
             }
         }
 
+      __attribute__((fallthrough));
       default:
         {
             int         len;
@@ -131441,7 +131396,6 @@
             }
             else
             {
-
                 reg_save(&(((regbehind_T *)rp) - 1)->save_after, &backpos);
 
                 (((regbehind_T *)rp) - 1)->save_behind = behind_pos;
@@ -132435,7 +132389,6 @@
     static int
 nfa_recognize_char_class(char_u *start, char_u *end, int extra_newl)
 {
-
     int         newl = FALSE;
     char_u      *p;
     int         config = 0;
@@ -132578,7 +132531,6 @@
     static int
 nfa_emit_equi_class(int c)
 {
-
     if (enc_utf8 ||  strcmp((char *)(p_enc), (char *)("latin1"))  == 0 ||  strcmp((char *)(p_enc), (char *)("iso-8859-15"))  == 0)
     {
         switch (c)
@@ -140151,6 +140103,7 @@
                 goto collection;
             }
 
+        __attribute__((fallthrough));
         case  ((int)('.') - 256) :
         case  ((int)('i') - 256) :
         case  ((int)('I') - 256) :
@@ -140546,6 +140499,7 @@
                     {
                         goto delimiter_atom;
                     }
+                __attribute__((fallthrough));
                 default:
                     {
                         long_u  n = 0;
@@ -141189,6 +141143,7 @@
                  return (emsg((_(e_missing_rsb_after_str_lsb))), rc_did_emsg = TRUE, FAIL) ;
             }
 
+        __attribute__((fallthrough));
         default:
             {
                 int     plen;
@@ -143129,6 +143084,7 @@
                 goto skip_add;
             }
 
+        __attribute__((fallthrough));
         case NFA_MOPEN1:
         case NFA_MOPEN2:
         case NFA_MOPEN3:
@@ -143332,6 +143288,7 @@
                 subs = addstate(l, state->out, subs, pim, off_arg, depth + 1);
                 break;
             }
+        __attribute__((fallthrough));
         case NFA_MCLOSE1:
         case NFA_MCLOSE2:
         case NFA_MCLOSE3:
@@ -148348,7 +148305,6 @@
 
     if (clear_width > 0)
     {
-
         while (col < clear_width && ScreenLines[off_to] == ' ' && ScreenAttrs[off_to] == 0 && (!enc_utf8 || ScreenLinesUC[off_to] == 0))
         {
             ScreenCols[off_to] =
@@ -152465,7 +152421,7 @@
 
     cookie.conv.vc_type = CONV_NONE;
 
-    firstline = getsourceline(0, (void *)&cookie, 0, TRUE);
+    firstline = getsourceline(0, (void *)&cookie, 0, GETLINE_CONCAT_CONT);
     if (firstline != NULL &&  strlen((char *)(firstline))  >= 3 && firstline[0] == 0xef && firstline[1] == 0xbb && firstline[2] == 0xbf)
     {
         convert_setup(&cookie.conv, (char_u *)"utf-8", p_enc);
@@ -155023,6 +154979,7 @@
                 }
             }
 
+        __attribute__((fallthrough));
         default:
             if (curbuf->b_p_lisp && vim_strchr((char_u *)"{}()[]", c) != NULL && pos.col > 1 && check_prevcol(linep, pos.col, '\\', NULL) && check_prevcol(linep, pos.col - 1, '#', NULL))
             {
@@ -155562,7 +155519,6 @@
     fd = open_exfile(fname, eap->forceit,  "w" );
     if (fd != NULL)
     {
-
         if (put_line(fd, "vim9script") == FAIL)
         {
             failed = TRUE;
@@ -156250,7 +156206,6 @@
         case 'X':
     case 'p':
         {
-
             if (fmt_spec == 'p')
             {
                 return TYPE_POINTER;
@@ -157229,7 +157184,6 @@
                 case 'X':
             case 'p':
                 {
-
                     int arg_sign = 0;
 
                     int int_arg = 0;
@@ -159428,7 +159382,6 @@
     findtags_matchargs_init(&margs, st->flags);
 
     {
-
         st->fp =  fopen(((char *)st->tag_fname), ("r")) ;
         if (st->fp == NULL)
         {
@@ -162278,7 +162231,6 @@
     void
 settmode(tmode_T tmode)
 {
-
     if (!full_screen)
     {
         return;
@@ -163358,7 +163310,6 @@
 
     else if (first == '?' && trail == 'c')
     {
-
         *slen = csi_len;
         apply_autocmds(EVENT_TERMRESPONSEALL, (char_u *)"da1", NULL, FALSE, curbuf);
 
@@ -163376,8 +163327,6 @@
 
         if (setting >= 0 && setting <= 4)
         {
-            LOG_TRN("Received DECRPM mode %d: %s", arg[0], tp);
-
             switch (arg[0])
             {
                 case 2026:
@@ -163393,7 +163342,6 @@
         }
         else
         {
-            LOG_TRN("Unknown DECRPM mode %d setting %d", arg[0], setting);
         }
     }
 
@@ -163488,7 +163436,6 @@
                     {
                         char *new_bg_val = (3 * '6' < *tp_r + *tp_g + *tp_b) ? "light" : "dark";
 
-                        LOG_TRN("Received RBG response: r=%d g=%d b=%d", rval, gval, bval);
                         if (!option_was_set((char_u *)"bg") &&  strcmp((char *)(p_bg), (char *)(new_bg_val))  != 0)
                         {
                             set_option_value_give_err((char_u *)"bg", 0L, (char_u *)new_bg_val, 0);
@@ -163635,7 +163582,6 @@
             }
             if ((i - j == 6 && tp[i] == STERM) || (i - j == 7 && tp[i] == '\\'))
             {
-
                 key_name[0] = (int)KS_EXTRA;
                 key_name[1] = (int)KE_IGNORE;
                 *slen = i + 1;
@@ -164428,7 +164374,6 @@
     void
 term_set_win_resize(bool state)
 {
-
     if (!state || win_resize_setting == 0 || win_resize_setting == 4)
     {
         if (win_resize_enabled)
@@ -164623,7 +164568,6 @@
         }
         if (!(flags & INSCHAR_FORMAT) && leader_len == 0 && !has_format_option(FO_WRAP))
         {
-
             break;
         }
         if ((startcol = curwin->w_cursor.col) == 0)
@@ -167031,7 +166975,6 @@
     }
     else
     {
-
     if (line[col_start] == quotechar || !vis_empty)
     {
         int     first_col = col_start;
@@ -171695,8 +171638,8 @@
 
 // ==================== version.c ====================
 
-static char            *Version =     # 9     "."    # 2     ;
-static char     *mediumVersion =      # 9     "."    # 2      ;
+static const char      *Version = VIM_VERSION_SHORT;
+static const char *mediumVersion = VIM_VERSION_SHORT;
 
 static char    *longVersion = NULL;
 
@@ -171711,18 +171654,18 @@
     char *date_time = __DATE__ " " __TIME__;
     char *msg = _("%s (%s, compiled %s)");
     size_t len = strlen(msg)
-        +  (sizeof( "VIM - Vi IMproved "      # 9     "."    # 2        "") - 1) 
-        +  (sizeof( "2026 Feb 14"  "") - 1) 
+        + sizeof(VIM_VERSION_LONG_ONLY) - 1
+        + sizeof(VIM_VERSION_DATE_ONLY) - 1
         + strlen(date_time);
 
     longVersion = alloc(len);
     if (longVersion == NULL)
     {
-        longVersion =    "VIM - Vi IMproved "      # 9     "."    # 2        " ("  "2026 Feb 14"   ")" ;
+        longVersion = (char *)VIM_VERSION_LONG_ONLY;
     }
     else
     {
-        vim_snprintf(longVersion, len, msg,  "VIM - Vi IMproved "      # 9     "."    # 2       ,  "2026 Feb 14" , date_time);
+        vim_snprintf(longVersion, len, msg, VIM_VERSION_LONG_ONLY, VIM_VERSION_DATE_ONLY, date_time);
     }
 }
 
@@ -175628,7 +175571,6 @@
     static void
 win_close_buffer(win_T *win, int action, int abort_if_last)
 {
-
     if (win->w_buffer != NULL)
     {
         bufref_T    bufref;
@@ -175927,7 +175869,6 @@
     static void
 check_window_scroll_resize(int     *size_count, win_T   **first_scroll_win, win_T   **first_size_win, list_T  *winlist  __attribute__((unused)) , dict_T  *v_event  __attribute__((unused)) )
 {
-
     win_T *wp;
      for ((wp) = firstwin; (wp) != NULL; (wp) = (wp)->w_next) 
     {
@@ -176519,7 +176460,6 @@
     }
     else
     {
-
         frp = topfrp->fr_child;
         if (wfh)
         {
@@ -176723,7 +176663,6 @@
     }
     else
     {
-
         frp = topfrp->fr_child;
         if (wfw)
         {
@@ -177702,7 +177641,6 @@
     void
 win_goto(win_T *wp)
 {
-
     if (text_or_buf_locked())
     {
         beep_flush();
@@ -179473,7 +179411,6 @@
     void
 win_comp_scroll(win_T *wp)
 {
-
     wp-> w_onebuf_opt.wo_scr  = ((unsigned)wp->w_height >> 1);
     if (wp-> w_onebuf_opt.wo_scr  == 0)
     {
@@ -180129,7 +180066,6 @@
     int
 vim_main2(void)
 {
-
     if (recoverymode && params.fname == NULL)
     {
         recover_names(NULL, TRUE, NULL, 0, NULL);
@@ -180264,7 +180200,6 @@
     void
 common_init_2(mparm_T *paramp)
 {
-
     init_locale();
 
     early_arg_scan(paramp);
@@ -180915,6 +180850,7 @@
             case 'M':
                 reset_modifiable();
 
+            __attribute__((fallthrough));
             case 'm':
                 p_write = FALSE;
                 break;
@@ -181029,6 +180965,7 @@
                     argv_idx = -1;
                     break;
                 }
+            __attribute__((fallthrough));
             case 'S':
             case 'i':
             case 'd':
@@ -181168,6 +181105,7 @@
                         argv_idx = -1;
                         break;
                     }
+                __attribute__((fallthrough));
                 case 'W':
                     if (scriptout != NULL)
                     {
@@ -181600,7 +181538,6 @@
     }
     else if (!silent_mode)
     {
-
         (void)do_source((char_u *) "$VIM/vimrc" , FALSE, DOSO_NONE, NULL);
 
         if (process_env((char_u *)"VIMINIT", TRUE) != OK)
